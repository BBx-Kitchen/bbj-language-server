#!/bin/sh
# Test fixture standing in for the real bbjcpl binary (P61-D5-005). A controlled
# substitute the repo owns, used to pin BBjCPLService's current behaviour: it runs
# the script that getBbjcplPath() resolves at bbjHome/bin/bbjcpl and parses its
# stderr output as diagnostics.
echo "$(dirname "$0")/../../fake.bbj: error at line 1 (1):     PIN TEST MARKER" >&2
exit 0
