#!/bin/sh
# Test fixture standing in for the real bbjcpl binary. A controlled substitute
# the repo owns, in a directory that satisfies every layout marker except cfg,
# used to pin how the compiler path is resolved and consumed.
echo "$(dirname "$0")/../../fake.bbj: error at line 1 (1):     NOCFG FIXTURE MARKER" >&2
exit 0
