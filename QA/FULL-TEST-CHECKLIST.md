# Full Test Checklist

## Purpose
Comprehensive test coverage for all BBj Language Server features across both VS Code and IntelliJ IDEA clients.

**Estimated Time:** 30-45 minutes

**Instructions:**
1. Rebuild and reinstall both IDE integrations before executing any row below. For VS Code:
   `npm --prefix bbj-vscode run build` then `bbj-ext-install` (or `npx vsce package` followed by
   installing the resulting VSIX). For IntelliJ: `cd bbj-intellij && ./gradlew buildPlugin`, then
   install the produced zip via Settings/Preferences > Plugins > gear icon > Install Plugin from
   Disk. Running any row against a build older than the commit under test produces findings about
   that old build, not about the code under test (see #475).
2. Copy this file to `QA/test-runs/YYYY-MM-DD-full-test.md`
3. Execute all tests
4. Mark `[ ]` with `[x]` for PASS or `[FAIL]` for failures
5. Rename file with `-PASS` or `-FAIL` suffix
6. Document failures with evidence (see TESTING-GUIDE.md)

---

## VS Code - LSP Features

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | Syntax Highlighting | 1. Open VS Code<br>2. Open `examples/bbj-classes.bbj`<br>3. Verify keywords are colored distinctly | Keywords (e.g., `class`, `method`, `if`) are colored differently from strings, comments, and identifiers | [ ] |
| 2 | Diagnostics | 1. Create new file `test.bbj`<br>2. Type: `MODE "INVALID"`<br>3. Save file<br>4. Check for error indicator | Red squiggle appears under `"INVALID"` with error message | [ ] |
| 3 | Code Completion (BBj) | 1. Create new file `test.bbj`<br>2. Type: `PR`<br>3. Press `Ctrl+Space` (Windows/Linux) or `Cmd+Space` (macOS)<br>4. Check completion list | Completion list shows `PRINT` and other PR* keywords | [ ] |
| 4 | Code Completion (Java) | 1. Create new file `test.bbj`<br>2. Type: `use java.util.Hash`<br>3. Press `Ctrl+Space` / `Cmd+Space`<br>4. Check completion list | Completion list shows `HashMap`, `HashSet`, etc. | [ ] |
| 5 | Hover Information | 1. Open file with variable declarations<br>2. Hover mouse over variable usage<br>3. Check tooltip content | Tooltip shows variable type and/or documentation | [ ] |
| 6 | Signature Help | 1. Create new file `test.bbj`<br>2. Type: `STR(`<br>3. Check for parameter popup | Popup shows parameter information for STR function | [ ] |
| 7 | Go-to-Definition | 1. Open file with method call<br>2. Right-click on method name<br>3. Select "Go to Definition"<br>4. Verify navigation | Editor navigates to method definition location | [ ] |
| 8 | Document Symbols | 1. Open file with classes/methods<br>2. Open Outline view (`Ctrl+Shift+O` / `Cmd+Shift+O`)<br>3. Verify hierarchy | Outline shows class/method/field hierarchy with correct icons | [ ] |
| 9 | Semantic Tokens | 1. Open file with variables and keywords<br>2. Compare coloring of variables vs. keywords | Variables colored differently from keywords; parameters vs. local variables distinguishable | [ ] |
| 10 | Custom-named config file survives reopen and revert | 1. Configure `bbj.configPath` to a file with a non-default name at a non-default location (e.g. `myproject-config.bbx`)<br>2. Open that file<br>3. Confirm it shows config-file syntax highlighting and the `Compose SETOPTS` cue<br>4. Close the tab, then reopen the file<br>5. Confirm the treatment survives<br>6. Edit the file, then run File > Revert File<br>7. Confirm the treatment still survives | The configured file shows config-file highlighting and the `Compose SETOPTS` cue on first open, after close/reopen, and after Revert File | [ ] |
| 11 | Inactive-config hint in the SETOPTS composer | 1. With a custom config file configured (as above), open the home default `config.bbx` instead<br>2. Run the SETOPTS composer command on that file | A non-blocking message names the active config file's full path before the composer opens on the file that is actually open | [ ] |
| 12 | Config PREFIX change reloads the server automatically | 1. Note the resolved config file's path from the BBj output channel or the Show Config command<br>2. Open that file in an editor outside VS Code<br>3. Change the PREFIX line to add a directory, save<br>4. Return to VS Code without touching anything<br>5. Watch the status bar and the BBj output channel<br>6. Open a `.bbj` file that uses a class from the newly added prefix directory | Within a few seconds a status-bar item shows a reloading state, then a brief "config reloaded" confirmation that disappears on its own; the output channel carries one line naming the config file and the reason; no prompt, modal or notification toast appears at any point; the class from the new prefix directory now resolves | [ ] |
| 13 | Atomic-save editor produces exactly one reload | 1. Note the resolved config file's path<br>2. Edit its PREFIX line using an editor that saves by writing a temporary file and renaming it over the target (e.g. Vim with `backupcopy=no`, or any editor's "safe write"/atomic-save setting), save<br>3. Return to VS Code without further edits<br>4. Watch the status bar for the next several seconds | Exactly one reload happens — the status-bar reload signal appears once, not twice, and the server does not restart a second time a few seconds later | [ ] |
| 14 | SETOPTS composer apply does not restart the server | 1. Open the resolved config file<br>2. Run the SETOPTS composer, change one option, apply<br>3. Save the file<br>4. Watch the status bar and the BBj output channel for a reload signal<br>5. Separately, edit only a SETOPTS line in an editor outside VS Code and save; watch again | The file's SETOPTS line is updated and no reload occurs in either case — no status-bar reload signal, no restart, no reconnection message. A reload here is the restart-loop regression this row exists to catch | [ ] |
| 15 | SETOPTS-in-code hover decode | 1. Open a `.bbj` file<br>2. Hover an absolute `SETOPTS $04$` statement<br>3. Hover the `SETOPTS var$` line of a `var$=OPTS` / `var$=IOR(var$,$01$)` / `SETOPTS var$` chain<br>4. Hover a single `IOR(var$,$02$)` call<br>5. Hover a single `AND(var$,$FE$)` call | Each hover names the option(s) that literal/call sets; the AND hover states the option(s) it CLEARS (not the raw mask) | [ ] |
| 16 | SETOPTS-in-code tri-state composer | 1. On the canonical chain above, invoke the SETOPTS-in-code code action, change one option to Clear and one to Set, apply<br>2. Confirm only the reassignment lines between the `OPTS` origin and the `SETOPTS` line changed<br>3. Invoke the code action on a line with no SETOPTS shape nearby, compose a block, confirm a whole new `var$=OPTS`/…/`SETOPTS var$` block is inserted at the line start<br>4. Invoke the code action on a chain interrupted by an `IF`/`FI` in between<br>5. Confirm no edit is offered | Step 2: the `OPTS` origin and `SETOPTS` lines are unchanged, only the reassignment lines differ. Step 3: the new block is inserted at the start of the line, not mid-line. Step 5: no composer opens; a message names why the shape cannot be safely edited | [ ] |
| 17 | Composer cues on every composer line | 1. Open `examples/issue650-composer-cues.bbj` without clicking into it<br>2. Look above each composer call for its cue, and above the non-editable/decoy lines for its absence<br>3. Click each cue and confirm it opens the matching composer pre-filled for that specific call<br>4. Cancel each composer and confirm the file is unchanged | `Compose MSGBOX`, `Compose addWindow`, `Compose addChildWindow`, `Compose CVS()` and `Compose SETOPTS` appear above the matching lines; none appear on the `mode%` CVS line, the interrupted SETOPTS chain, the REM line or the string decoy; each cue opens its own composer for that call; Cancel leaves the file unchanged | [ ] |
| 18 | Config-file SETOPTS cue | 1. Open the resolved config file<br>2. Count the `Compose SETOPTS` cues against the file's SETOPTS lines<br>3. Check the Problems view for the file<br>4. Click a cue | Exactly one `Compose SETOPTS` cue appears per SETOPTS line and no BBj diagnostics appear on the file; clicking a cue opens the SETOPTS composer | [ ] |
| 19 | CVS() composer compose and edit | 1. On a blank line, compose a new `CVS()` call selecting bits 1 and 4, add a `chars` value, insert<br>2. Edit the fixture's `1+4` call via its cue/lightbulb, change a bit, apply<br>3. While only bit 4 is checked, check the `chars` field's enabled state<br>4. Check the `mode%` call for a lightbulb<br>5. On a new line type `a$ = CVS(name$`, press Ctrl+. and choose `Complete CVS() call…`, check bits 1 and 4, and insert; on another new line type `b$ = CVS(` and run `Compose CVS() (visual)…` from the editor context menu; while that panel is open, type more characters at the end of the `b$` line, then press Insert | Step 1: the composed call is inserted with the chosen bits and chars. Step 2: the string argument is untouched and only the call's bits change. Step 3: the `chars` field is disabled (not hidden) while only bit 4 is checked. Step 4: the `mode%` call offers no lightbulb action. Step 5: the lightbulb offers `Complete CVS() call…`, and the panel opens with an editable string field and no assign field; the `a$` line becomes `a$ = CVS(name$, 5)`; the context menu opens the same complete-the-call panel for the `b$` call rather than inserting a whole call inside it; Insert after the line changed shows `The CVS() call changed since the composer opened; nothing was applied.` and leaves the line unchanged; no `Compose CVS()` cue appears above either unfinished line | [ ] |
| 20 | MSGBOX constant-sum pre-fill | 1. Open the composer on the fixture's `BBjMsgBox.*` sum line | The composer opens pre-filled with Yes/No buttons and the Question icon selected, with no compose-and-replace banner | [ ] |
| 21 | MSGBOX compose-and-replace | 1. Open the composer on the fixture's `flags%` line<br>2. Read the banner and the original-expression field<br>3. Apply without changing anything else | The composer shows "Could not decode this options expression — composing will replace it." with the original `flags%` shown read-only; Apply replaces the options with no extra confirmation dialog, and the message and title are preserved unchanged | [ ] |
| 22 | MSGBOX unfinished call and wizard target safety | 1. On a new line type `x = MSGBOX(` and click the `Compose MSGBOX` cue above it<br>2. Type `"Saved"` as the message, choose the Information icon and press Insert<br>3. On a new line type `y = MSGBOX("Hi",` and run `Compose MSGBOX (visual)…` from the editor context menu; while the panel is open, type more characters at the end of that line, then press Insert<br>4. On a new line type `z = MSGBOX(` and run `Compose MSGBOX…` from the editor context menu | Steps 1-2: the panel is titled `Complete MSGBOX call`, shows no compose-and-replace banner and no assign field, and the line becomes `x = MSGBOX("Saved", 64)`. Step 3: the context menu opens the same completion panel with `"Hi"` prefilled; Insert after the line changed shows `The MSGBOX() call changed since the composer opened; nothing was applied.` and leaves the line unchanged. Step 4: the completion panel opens instead of the QuickPick wizard, and nothing is inserted inside the call | [ ] |
| 23 | addWindow and addChildWindow field validation | 1. On a blank line run `Compose addWindow (visual)…`<br>2. Type `"10"` into x, then `caption` into Title expr, then `getGui(` into SysGui expr, checking the panel after each<br>3. Try Insert<br>4. Clear every statement field and press Insert<br>5. Repeat steps 1-3 in the addChildWindow composer, with `"101"` in ID | Step 2: each malformed field gets a red border with its message underneath (`Not a number — remove the quotes: 10`, `Not a string — quote it as "caption"…`, `Unbalanced parentheses`), and Insert is disabled. Step 3: nothing is inserted. Step 4: every error clears, Insert is enabled, and the inserted statement uses the defaults `sysgui!.addWindow(0, 0, 0, 0, "", $00010003$)`. Step 5: the addChildWindow composer behaves the same, with `Not a number — remove the quotes: 101` under ID | [ ] |

---

## IntelliJ IDEA - LSP Features

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | Syntax Highlighting | 1. Open IntelliJ IDEA<br>2. Open `examples/bbj-classes.bbj`<br>3. Verify keywords are colored distinctly | Keywords (e.g., `class`, `method`, `if`) are colored differently from strings, comments, and identifiers | [ ] |
| 2 | Diagnostics | 1. Create new file `test.bbj`<br>2. Type: `MODE "INVALID"`<br>3. Save file<br>4. Check for error indicator | Red squiggle or error highlight appears with error message | [ ] |
| 3 | Code Completion (BBj) | 1. Create new file `test.bbj`<br>2. Type: `PR`<br>3. Press `Ctrl+Space`<br>4. Check completion list | Completion list shows `PRINT` and other PR* keywords | [ ] |
| 4 | Code Completion (Java) | 1. Create new file `test.bbj`<br>2. Type: `use java.util.Hash`<br>3. Press `Ctrl+Space`<br>4. Check completion list | Completion list shows `HashMap`, `HashSet`, etc. | [ ] |
| 5 | Hover Information | 1. Open file with variable declarations<br>2. Hover mouse over variable usage<br>3. Check tooltip content | Tooltip shows variable type and/or documentation | [ ] |
| 6 | Signature Help | 1. Create new file `test.bbj`<br>2. Type: `STR(`<br>3. Check for parameter popup | Popup shows parameter information for STR function | [ ] |
| 7 | Go-to-Definition | 1. Open file with method call<br>2. Right-click on method name<br>3. Select "Go to Declaration" or use `Ctrl+B`<br>4. Verify navigation | Editor navigates to method definition location | [ ] |
| 8 | Document Symbols | 1. Open file with classes/methods<br>2. Open Structure view (usually left sidebar or `Alt+7`)<br>3. Verify hierarchy | Structure view shows class/method/field hierarchy | [ ] |
| 9 | Semantic Tokens | 1. Open file with variables and keywords<br>2. Compare coloring of variables vs. keywords | Variables colored differently from keywords; semantic highlighting active | [ ] |
| 10 | Custom-named config file highlighting | 1. In BBj settings, point the config path setting at a file with a custom name (not `config.bbx`)<br>2. Open that file<br>3. Check the file icon, syntax highlighting and the Problems view<br>4. Open the Language Servers tool window | The file gets the config file icon and bbx highlighting like `config.bbx` would; it is never treated as BBj source and gets no BBj diagnostics; it may appear listed under the BBj language server with language id `bbx-config` | [ ] |
| 11 | config.bbx no longer treated as BBj source | 1. Leave the config path setting unchanged (default)<br>2. Open `config.bbx` from the BBj home `cfg` directory<br>3. Check the file icon, the Problems view and the Language Servers tool window | The file opens as a config file (config icon, bbx highlighting); it is never treated as BBj source and gets no BBj diagnostics; it may appear listed under the BBj language server with language id `bbx-config` | [ ] |
| 12 | Live config-path change flips file type on both files | 1. Open both the old and the new config file with the setting pointed at the old one<br>2. Change the config path setting to the new file<br>3. Without restarting the IDE, check both open files' icons and highlighting | Both files' file types flip immediately: the old file reverts to its extension-based type and the new file becomes the config file type | [ ] |
| 13 | Config PREFIX change reloads the server automatically | 1. Note the resolved config file's path from the BBj status bar widget or Settings<br>2. Open that file in an editor outside IntelliJ<br>3. Change the PREFIX line to add a directory, save<br>4. Return to IntelliJ without touching anything<br>5. Watch the BBj status-bar widget and the BBj Language Server tool window<br>6. Open a `.bbj` file that uses a class from the newly added prefix directory | The widget passes through its starting/started transitions and its tooltip names the config-file-changed reason during the restart; the tool window console carries one line naming the config file and the reason; no balloon appears; the class from the new prefix directory resolves afterwards | [ ] |
| 14 | Config file outside the project's content root reloads identically | 1. Point the config-path setting at a file that is not inside any open project's content root<br>2. Restart IntelliJ so the setting takes effect<br>3. Change that file's PREFIX line from an editor outside IntelliJ and save<br>4. Watch the BBj status-bar widget and the BBj Language Server tool window | The reload fires exactly as it does for a file inside the content root — the out-of-tree location makes no difference | [ ] |
| 15 | Save burst: a `.bbj` file and the config file together yields one clean restart | 1. Open a `.bbj` file, make an edit that produces a diagnostic, save it<br>2. Within a second, save a PREFIX change to the config file too<br>3. Watch both the Problems view diagnostics and the BBj status-bar widget | Exactly one restart, landing after the `.bbj` file's diagnostics have finished updating — no "connection to the server got closed" message, no diagnostics that vanish mid-update, and no second restart | [ ] |
| 16 | Refresh Java Classes keeps language features online | 1. Open a project whose BBj Home has a large Java classpath and open a `.bbj` file that uses Java classes<br>2. Wait for the BBj status-bar widget to read started<br>3. Run Refresh Java Classes from the Tools menu<br>4. While the "Refreshing Java classes…" progress task is still visible, invoke code completion, hover a variable and open the Structure view<br>5. Watch the status-bar widget throughout<br>6. After the progress task disappears, check the BBj Language Server tool window | Completion answers, hover answers and the Structure view populates while the refresh is running; the status widget never leaves started and no message about the connection to the server being closed appears; exactly one console line reports the refresh finished and no balloon is raised | [ ] |
| 17 | Java-interop port auto-detects, and an explicitly confirmed 5008 is kept | 1. In BBj settings, open the Java Interop section and confirm Auto-detect is checked, the Port field is greyed out, and it shows the port from the `com.basis.languageServer.addr` line of the BBj Home's `cfg/BBj.properties`, with a hint line under the field naming where the value came from<br>2. Edit that properties line to a different port, close and reopen the settings dialog, and confirm the greyed field now shows the new port<br>3. Uncheck Auto-detect, confirm the field is editable and pre-filled with the value that was showing, type 5008, and apply<br>4. Reopen the dialog and confirm Auto-detect is still unchecked and the field still reads 5008 even though the properties file names a different port<br>5. Re-check Auto-detect and confirm the field returns to the detected value | The greyed field and the hint track the properties file while Auto-detect is on; an explicitly applied 5008 survives a reopen unchanged with the checkbox still off; re-checking restores the detected value; no dialog, warning or balloon appears at any point | [ ] |
| 18 | SETOPTS composer edits config.bbx without restarting the server | 1. Open the resolved config file in IntelliJ<br>2. Right-click on the existing SETOPTS line and confirm "Compose SETOPTS…" appears in the context menu<br>3. Toggle one option and confirm the resulting line and summary update live, and that BBj-ignored options are visibly greyed with an explanatory tooltip<br>4. Apply and confirm only the hex token of that line changed<br>5. Right-click on a different line with no SETOPTS on it, confirm the same entry appears, compose a new line and confirm it is inserted as a whole line<br>6. Open a `.bbj` file and confirm the entry is absent from its context menu<br>7. Watch the BBj status-bar widget and the BBj Language Server tool window throughout | The composer opens from both line kinds; edits are applied to the SETOPTS line only, no other line of the file changes; the entry is absent (not disabled) in a `.bbj` file; the server never restarts or reconnects at any point | [ ] |
| 19 | SETOPTS-in-code hover decode (LSP4IJ) | 1. Open a `.bbj` file<br>2. Hover an absolute `SETOPTS $04$` statement<br>3. Hover the `SETOPTS var$` line of a `var$=OPTS` / `var$=IOR(var$,$01$)` / `SETOPTS var$` chain<br>4. Hover a single `IOR(var$,$02$)` call<br>5. Hover a single `AND(var$,$FE$)` call | LSP4IJ's own hover popup shows the same decode text VS Code shows for each case; the AND hover states the option(s) it CLEARS | [ ] |
| 20 | SETOPTS-in-code tri-state composer (Alt+Enter) | 1. On a canonical `var$=OPTS`/`IOR`/`AND`/`SETOPTS var$` chain, place the caret on the `SETOPTS var$` line and press Alt+Enter<br>2. Select "Configure SETOPTS options in code…"<br>3. Change one option to Set and one to Clear while watching the generated-block preview<br>4. Apply and confirm only the reassignment lines changed<br>5. Place the caret on a line with no SETOPTS shape nearby, invoke the same intention, compose a new block and Insert<br>6. Place the caret on a chain interrupted by an `IF`/`FI`, invoke the intention | Step 3: the preview updates on a settle (not per keystroke) and BBj-ignored options are visibly de-emphasized with a tooltip; step 4: only the reassignment lines change; step 5: the new block is inserted at the line start; step 6: the intention shows the server's reason instead of opening a composer; the BBj status-bar widget never leaves started and the server never restarts or reconnects throughout the whole sequence | [ ] |
| 21 | SETOPTS-in-code mask-width falsification (live BBjServices) | 1. Against a live BBjServices, compose a new SETOPTS-in-code block setting one option to Set and one to Clear via the tri-state composer<br>2. Insert the block and run the program as GUI/BUI/DWC<br>3. Watch the run output/console for a BBj `!ERROR` | The generated `IOR`/`AND` calls run without raising `!ERROR` — confirms (or refutes) 88-RESEARCH.md Assumption A2's 16-byte mask-padding default against real BASIS runtime behavior | [ ] |
| 22 | SETOPTS-in-code composer via the editor context menu | 1. Right-click on a SETOPTS-in-code line (e.g. the `SETOPTS var$` line of a canonical chain) in a `.bbj` file<br>2. Confirm a compose entry appears in the context menu and invoke it<br>3. Confirm the same tri-state composer that Alt+Enter opens (row 20) opens here too<br>4. Right-click on a line inside the resolved config file instead | Step 3: the context-menu entry reaches the identical composer without going through Alt+Enter's intention search, so it keeps working even when intention search is slow; step 4: this entry is absent from the config file's context menu (the config file has its own SETOPTS composer entry, covered by row 18) | [ ] |
| 23 | Code Vision composer cues | 1. Open `examples/issue650-composer-cues.bbj` without placing the caret<br>2. Look for a Code Vision entry above each composer call, and above the non-editable/decoy lines for its absence<br>3. Click each Code Vision entry and confirm it opens the matching dialog pre-filled for that specific call | `Compose MSGBOX`, `Compose addWindow`, `Compose addChildWindow`, `Compose CVS()` and `Compose SETOPTS` render as Code Vision above the matching lines; none appear on the `mode%` CVS line, the interrupted SETOPTS chain, the REM line or the string decoy; each click opens its own dialog for that call | [ ] |
| 24 | Config-file SETOPTS Code Vision cue | 1. Open the resolved config file<br>2. Count the Code Vision cues against the file's SETOPTS lines<br>3. Click a cue<br>4. Open the Language Servers console for the config file | Exactly one Code Vision `Compose SETOPTS` cue appears per SETOPTS line; clicking a cue opens the SETOPTS dialog; the Language Servers console shows no errors for the config file | [ ] |
| 25 | CVS() dialog via Alt+Enter and the context menu | 1. Place the caret on a blank line, press Alt+Enter or use the editor context menu, compose a new `CVS()` call selecting bits 1 and 4, add a `chars` value, confirm<br>2. Place the caret on the fixture's `1+4` call, invoke the same entry, change a bit, apply<br>3. While only bit 4 is checked, check the `chars` field's enabled state<br>4. Place the caret on the `mode%` call and check for the reason notice<br>5. On a new line type `a$ = CVS(` (the editor adds the closing parenthesis), press Alt+Enter and choose `Configure CVS() options…`; type `name$` in the string field, check bits 1 and 4, and apply; repeat on a new line with `b$ = CVS(name$` using the editor context menu | Step 1: the composed call is inserted with the chosen bits and chars. Step 2: the string argument is untouched and only the call's bits change. Step 3: the `chars` field is disabled (not hidden) while only bit 4 is checked. Step 4: no dialog opens; a reason notice explains why the call cannot be safely edited. Step 5: a `Complete CVS() call` dialog opens with no error notice; its string field is editable (empty, then prefilled with `name$`), there is no assign field, and OK stays disabled while the string is empty; applying leaves `a$ = CVS(name$, 5)` and a single complete `CVS(name$, …)` call on the `b$` line — never a nested `CVS(CVS(` | [ ] |
| 26 | MSGBOX constant-sum pre-fill | 1. Place the caret on the fixture's `BBjMsgBox.*` sum line and open the dialog (Alt+Enter or context menu) | The dialog opens pre-filled with Yes/No buttons and the Question icon selected, with no compose-and-replace banner | [ ] |
| 27 | MSGBOX compose-and-replace | 1. Place the caret on the fixture's `flags%` line and open the dialog<br>2. Read the banner and the original-expression field<br>3. Apply without changing anything else | The dialog shows "Could not decode this options expression — composing will replace it." with the original `flags%` shown read-only; Apply replaces the options with no extra confirmation dialog, and the message and title are preserved unchanged | [ ] |
| 28 | MSGBOX dialog completes an unfinished call with a debounced preview | 1. On a new line type `x = MSGBOX(` (the editor adds the closing parenthesis), press Alt+Enter and choose `Configure MSGBOX options…`<br>2. Type `"Saved"` quickly into the message field while watching the generated statement and the OK button<br>3. Choose the Information icon and apply<br>4. Place the caret on the fixture's literal `36` MSGBOX call and open the dialog | Step 1: a `Complete MSGBOX call` dialog opens with no banner and no assign field. Step 2: the generated statement updates once shortly after typing stops, and OK stays disabled until it does. Step 3: the line becomes `x = MSGBOX("Saved", 64)`, never a nested `MSGBOX(MSGBOX(`. Step 4: `Configure MSGBOX` opens pre-filled exactly as before | [ ] |
| 29 | addWindow and addChildWindow dialog field validation | 1. Open the addWindow composer dialog on a blank line from the editor context menu<br>2. Quickly type `"10"` into x, then `caption` into Title expr, then `getGui(` into SysGui expr<br>3. Clear every statement field<br>4. Repeat in the addChildWindow dialog, with `"101"` in ID | Step 2: after typing stops, each malformed field shows its message in a red label directly under it, OK stays disabled, and the preview updates once per pause, not per keystroke. Step 3: the labels clear and OK is enabled. Step 4: the addChildWindow dialog behaves the same, with `Not a number — remove the quotes: 101` under ID | [ ] |
| 30 | Composer reopen across a language-server restart | 1. Open a `.bbj` file and wait for the BBj status-bar widget to read started<br>2. Open the MSGBOX dialog, cancel it, and open it again<br>3. Restart the language server from the status-bar widget and wait for started<br>4. Open the MSGBOX dialog again | Step 2: the second open is noticeably faster than the first. Step 4: the dialog opens normally, with no error balloon and no request-failed notice | [ ] |

---

## VS Code - Run Commands

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | Run Program | 1. Open VS Code<br>2. Open any `.bbj` file (e.g., `examples/hello.bbj`)<br>3. Right-click in editor<br>4. Select "Run BBj" > "Run Program"<br>5. Check output terminal | Program executes and output appears in terminal | [ ] |
| 2 | Run with Debug | 1. Open any `.bbj` file<br>2. Right-click in editor<br>3. Select "Run BBj" > "Run with Debug"<br>4. Check output terminal | Program executes with debug output/logging enabled | [ ] |
| 3 | Run as BUI/DWC | 1. Open `.bbj` file with GUI components (if applicable)<br>2. Right-click in editor<br>3. Select "Run BBj" > "Run as BUI" or "Run as DWC"<br>4. Check for browser/window launch | Program launches in BUI or DWC mode | [ ] |

---

## IntelliJ IDEA - Run Commands

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | Run Program | 1. Open IntelliJ IDEA<br>2. Open any `.bbj` file (e.g., `examples/hello.bbj`)<br>3. Right-click in editor<br>4. Select "Run BBj" > "Run Program"<br>5. Check run tool window | Program executes and output appears in run window | [ ] |
| 2 | Run with Debug | 1. Open any `.bbj` file<br>2. Right-click in editor<br>3. Select "Run BBj" > "Run with Debug"<br>4. Check run tool window | Program executes with debug output/logging enabled | [ ] |
| 3 | Run as BUI/DWC | 1. Open `.bbj` file with GUI components (if applicable)<br>2. Right-click in editor<br>3. Select "Run BBj" > "Run as BUI" or "Run as DWC"<br>4. Check for browser/window launch | Program launches in BUI or DWC mode | [ ] |

---

## Enterprise Manager Integration

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | EM Connection Settings | 1. Open VS Code settings (or IntelliJ preferences)<br>2. Search for "BBj" or "Enterprise Manager"<br>3. Verify EM connection settings are accessible | Settings for EM host, port, username, password are present and editable | [ ] |
| 2 | EM Authentication | 1. Configure EM connection settings<br>2. Trigger any EM-dependent operation (e.g., run command)<br>3. Check for connection success/failure feedback | Extension attempts EM connection; shows success or clear error message if credentials invalid | [ ] |
| 3 | EM-dependent Features | 1. With valid EM credentials configured<br>2. Execute run commands or other EM-dependent features<br>3. Verify operations complete successfully | Features that require EM work correctly when EM is available | [ ] |

---

## Language Server Process Lifecycle

Regression coverage for #232, where the language server ran at 100% CPU and kept
running after the editor exited. Each leaked process holds a CPU core indefinitely,
so this is worth checking on every release.

| # | Feature | Steps | Expected | Pass/Fail |
|---|---------|-------|----------|-----------|
| 1 | Server exits with the editor (VS Code) | 1. Open a `.bbj` file, wait for completion to work<br>2. Quit VS Code entirely (Cmd+Q / File > Exit — not just closing the window)<br>3. macOS/Linux: `ps aux \| grep 'bbj-lang'`<br>4. Windows (PowerShell): `Get-CimInstance Win32_Process \| Where-Object CommandLine -like '*bbj-lang*'` | No `out/language/main.cjs` process remains within ~10s | [ ] |
| 2 | Server exits with the editor (IntelliJ) | Same as above, quitting IntelliJ instead | No `main.cjs` process remains within ~10s | [ ] |
| 3 | No orphans after repeated sessions | 1. Open and quit the editor three times with a `.bbj` file open<br>2. Re-run the `ps` / `Get-CimInstance` check | Zero `main.cjs` processes. One per closed session means the shutdown path is broken | [ ] |
| 4 | Idle CPU settles | 1. Open a `.bbj` file, leave the editor idle ~1 minute<br>2. Watch `main.cjs` CPU in Activity Monitor / `top` | Near-idle after initial indexing. Sustained ~100% of one core indicates a spinning validation or rebuild loop | [ ] |
| 5 | CPU settles in a multi-project workspace | 1. Open a workspace containing several BBj projects (the original #232 trigger)<br>2. Wait for indexing, then leave idle ~2 minutes | CPU returns to near-idle; the editor stays responsive to completion requests | [ ] |
| 6 | Editor survives an unresponsive Java interop | 1. Stop `java-interop` (port 5008)<br>2. Open a `.bbj` file using Java classes and request completion | Completion degrades with an error or empty result within ~10s; no hang and no runaway CPU | [ ] |

**On failure:** capture `ps aux | grep bbj-lang` output including elapsed CPU time,
note whether the `--clientProcessId` value is a live PID, and attach both to the issue.
A surviving process whose client PID is dead points at a blocked event loop rather
than a missing shutdown handler.

---

## Test Run Result

- [ ] **PASS** - All items marked with `[x]`
- [ ] **FAIL** - Any item marked with `[FAIL]`

**If FAIL:** Document failures with evidence (see TESTING-GUIDE.md)

---

## Test Information

**Date:** _______________

**Tester:** _______________

**Environment:**
- OS: _______________
- VS Code Version: _______________
- IntelliJ Version: _______________
- Extension Version: _______________
