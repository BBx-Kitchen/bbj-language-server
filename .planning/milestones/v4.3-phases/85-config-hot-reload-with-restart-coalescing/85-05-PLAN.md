---
phase: 85-config-hot-reload-with-restart-coalescing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md
  - QA/FULL-TEST-CHECKLIST.md
autonomous: true
requirements: [CFG-03]

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The phase's manual test surface is written down before the code lands: every behavior a hermetic test cannot reach has a QA row naming exact steps and an exact expected result."
    - "The five hot-reload behaviors named in 85-CONTEXT's Integration Points each have a checklist row: an external-editor PREFIX change in both IDEs, an atomic-save editor, an out-of-workspace config file, a SETOPTS composer apply that must NOT restart, and a .bbj save burst plus a config save yielding one clean restart (D-06, D-08, D-09, D-13)."
    - "Phase 84's three unfilled checklist gaps stay out of scope — this plan adds rows for phase 85 behaviors only."
    - "The seal-time API-coverage gate has a reasoned declaration to read rather than a missing file."
  artifacts:
    - .planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md
  key_links:
    - "COVERAGE.md -> the api-coverage.verify-pre gate: a reasoned 'no external API integration' declaration is accepted in place of a capability matrix."
    - "QA/FULL-TEST-CHECKLIST.md new rows -> the UAT run that closes CFG-03: each row is a hand-executable check for a behavior no vitest or JUnit test can reach."
  prohibitions:
    - "COVERAGE.md must be a single line under 200 characters of the form `No external API integration: <reason>` — no capability matrix, since this phase integrates no external service."
    - "No fabricated matrix rows for capabilities that do not exist."
    - "Do not renumber, reword or remove any existing QA checklist row; append within the correct section only."
    - "No plan identifiers, D-xx, C-xx, COMP or CR-xx tokens in the checklist rows — they are read by a human tester, not by the planner."
    - "No `git add -A` / `git add .` — exact-path staging only, on the current branch, no new branch."
---

<objective>
Write down the phase's human-verifiable surface and its API-coverage declaration: the QA checklist rows for the five hot-reload behaviors no hermetic test can reach, and the one-line `COVERAGE.md` the seal-time gate reads.

Purpose: three of Phase 84's UAT-covered behaviors were never written into the checklist and became a recorded follow-up. Writing the rows in the same phase that creates the behavior is what stops that repeating; the `COVERAGE.md` declaration keeps the seal gate from blocking on prose that merely mentions an interface.
Output: `.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md` and new rows in `QA/FULL-TEST-CHECKLIST.md`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<executor_rules>
**Shell and file-access rules (project CLAUDE.md — follow verbatim):**
- Search and read with the built-in tools first: `Grep` for content search, `Glob` for file discovery, `Read` for file contents. Fall back to shell `grep`/`find`/`cat` only when a built-in tool genuinely cannot do the job.
- Every shell path is absolute and complete, named from the repo root (`/home/coder/repos/bbj-language-server/...`). Never use `.`, `..` or `~` as a search target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`.
- No blind recursive scans. Never point a read at `.env*`, `*.pem`, `*.key` or a credential store.
- Stage commits with `git add <exact path>` only — never `git add -A` or `git add .`. Commit on the current branch; do not create or switch branches.
</executor_rules>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-config-hot-reload-with-restart-coalescing/85-CONTEXT.md
@QA/FULL-TEST-CHECKLIST.md
@.planning/phases/84-config-path-resolution-discoverability-foundation/COVERAGE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The thinnest end-to-end documentation slice — the user-visible reload path, written down</name>
  <files>QA/FULL-TEST-CHECKLIST.md, .planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md</files>
  <read_first>
    - QA/FULL-TEST-CHECKLIST.md (whole file — the table column layout `| # | Test | Steps | Expected Result | Pass |`, the `<br>`-separated numbered steps convention, the section headings, and the existing config rows 10-11 in the VS Code LSP section and 10-12 in the IntelliJ LSP section)
    - .planning/phases/84-config-path-resolution-discoverability-foundation/COVERAGE.md (the exact one-line declaration form to mirror)
    - .planning/phases/85-config-hot-reload-with-restart-coalescing/85-CONTEXT.md section "Integration Points" (the five checklist rows named there) and section "Signal UX" (what the tester must see)
  </read_first>
  <action>
Write `.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md` containing exactly one line, under 200 characters, in the form `No external API integration: <reason>`. The reason must state what this phase touches instead: an internal LSP custom notification plus Node filesystem watching and each IDE's own restart machinery, no third-party service. Mirror Phase 84's file byte-for-byte in shape (single line, no heading, no table) — the seal-time gate accepts a reasoned declaration in place of a capability matrix, and a fabricated matrix row for a capability that does not exist is worse than none.

In `QA/FULL-TEST-CHECKLIST.md`, append one row to the end of the **VS Code - LSP Features** table (numbered 12, following the existing row 11) and one row to the end of the **IntelliJ IDEA - LSP Features** table (numbered 13, following the existing row 12), covering the phase's headline behavior end-to-end:

- VS Code row 12, "Config PREFIX change reloads the server automatically": steps are (1) note the resolved config file's path from the BBj output channel or the Show Config command, (2) open that file in an editor *outside* VS Code, (3) change the PREFIX line to add a directory, save, (4) return to VS Code without touching anything, (5) watch the status bar and the BBj output channel, (6) open a `.bbj` file that uses a class from the newly added prefix directory. Expected result: within a few seconds a status-bar item shows a reloading state, then a brief "config reloaded" confirmation that disappears on its own; the output channel carries one line naming the config file and the reason; no prompt, modal or notification toast appears at any point; the class from the new prefix directory now resolves.
- IntelliJ row 13, same behavior for IntelliJ: same external edit, then watch the BBj status-bar widget and the BBj Language Server tool window. Expected result: the widget passes through its starting/started transitions and its tooltip names the config-file-changed reason during the restart; the tool window console carries one line naming the config file and the reason; no balloon appears; the class from the new prefix directory resolves afterwards.

Match the existing rows' formatting exactly: pipe-delimited, `<br>`-separated numbered steps, an unchecked `[ ]` in the Pass column, no trailing whitespace. Do not renumber, reword or remove any existing row.
  </action>
  <verify>
    <automated>awk 'NR==1 && /^No external API integration: ./ && length($0) < 200 {ok=1} END{exit !(ok && NR<=1)}' /home/coder/repos/bbj-language-server/.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md</automated>
    <fails_when>non-zero exit — the file is missing, is more than one line, does not start with the declaration prefix followed by a reason, or is 200 characters or longer</fails_when>
    <automated>grep -E '^\| 1[23] \|' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md | grep -ic reload</automated>
    <fails_when>non-zero exit, or the printed count is less than 2 (fewer than the two new reload rows were appended)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md` exists, is one line, is under 200 characters, and begins with `No external API integration: `.
    - `COVERAGE.md` contains no markdown table and no `| capability |` header.
    - `QA/FULL-TEST-CHECKLIST.md` contains a row numbered 12 in the VS Code LSP Features table whose Expected Result names a status-bar signal and explicitly rules out a prompt, modal or toast.
    - `QA/FULL-TEST-CHECKLIST.md` contains a row numbered 13 in the IntelliJ LSP Features table whose Expected Result names the status widget tooltip and the tool-window console line and explicitly rules out a balloon.
    - Every existing checklist row number, wording and Pass state is unchanged — verify with `git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md` showing only added lines.
  </acceptance_criteria>
  <done>The phase's headline user-visible behavior is written down as a hand-executable check for both IDEs, and the seal-time coverage gate has a reasoned declaration to read.</done>
</task>

<task type="auto">
  <name>Task 2: The four remaining hand-only behaviors</name>
  <files>QA/FULL-TEST-CHECKLIST.md</files>
  <read_first>
    - QA/FULL-TEST-CHECKLIST.md (the rows added in Task 1 and the surrounding table formatting)
    - .planning/phases/85-config-hot-reload-with-restart-coalescing/85-CONTEXT.md sections "Reload semantics", "Self-write suppression", "Debounce, quiescence and coalescing"
  </read_first>
  <action>
Append four further rows to `QA/FULL-TEST-CHECKLIST.md`, continuing the numbering established in Task 1. Place each in the section that matches the IDE it exercises; where a check applies to both IDEs, write it once per section rather than one row saying "both".

1. **Atomic-save editor.** Steps: edit the resolved config file's PREFIX line in an editor known to save by writing a temporary file and renaming it over the target (name at least one such editor or the relevant "safe write" setting in the step text), save, return to the IDE. Expected result: exactly one reload happens — the status signal appears once, not twice, and the server does not restart a second time a few seconds later.
2. **Config file outside the workspace/project root.** Steps: point the config-path setting at a file that is not inside any open workspace folder or content root, restart the IDE so the setting takes effect, then change that file's PREFIX line from outside the IDE and save. Expected result: the reload fires exactly as it does for an in-workspace file — the out-of-tree location makes no difference.
3. **SETOPTS composer apply does not restart the server.** Steps: open the resolved config file, run the SETOPTS composer, change one option, apply, then save the file. Expected result: the file's SETOPTS line is updated and **no** reload occurs — no status-bar reload signal, no restart, no reconnection message. Add a second step pair covering an external editor that changes only a SETOPTS line: same expected result. Note in the Expected Result column that a reload here is the restart-loop regression this row exists to catch.
4. **Save burst: a `.bbj` file and the config file together.** Steps: open a `.bbj` file, make an edit that produces a diagnostic, save it, and within a second save a PREFIX change to the config file too; watch both the diagnostics and the status bar. Expected result: exactly one restart, and it lands *after* the `.bbj` file's diagnostics have finished updating — no "connection to the server got closed" message, no diagnostics that vanish mid-update, and no second restart.

Keep the existing column layout and `<br>` step separators. Do not renumber, reword or remove any existing row.
  </action>
  <verify>
    <automated>for k in atomic outside SETOPTS burst; do grep -qi -- "$k" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md || { echo "MISSING $k"; exit 1; }; done; echo OK</automated>
    <fails_when>non-zero exit, or the output prints a line beginning "MISSING " instead of OK</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --numstat -- QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>the deletions column is non-zero (an existing row was changed or removed rather than rows being appended), or the command prints nothing (no rows were added)</fails_when>
  </verify>
  <acceptance_criteria>
    - A checklist row covers an atomic write-temp-then-rename save and expects exactly one reload.
    - A checklist row covers a config file outside every workspace folder / content root and expects the reload to fire identically.
    - A checklist row covers a SETOPTS composer apply plus save and expects **no** reload, naming the restart loop as the regression it guards.
    - A checklist row covers a `.bbj` save plus a config save in one burst and expects exactly one restart landing after diagnostics settle.
    - `git diff --numstat -- QA/FULL-TEST-CHECKLIST.md` reports zero deletions.
  </acceptance_criteria>
  <done>All five behaviors named in the phase's Integration Points have a hand-executable checklist row with an unambiguous expected result, including the two negative checks that a passing automated suite cannot demonstrate to a user.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Files this plan creates (new, not drift):

- `.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md` — the reasoned no-external-API declaration read by the seal-time coverage gate
- `QA/FULL-TEST-CHECKLIST.md` — six appended rows (two headline reload rows, atomic save, out-of-workspace config, SETOPTS-no-restart, save burst)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → release gate | `COVERAGE.md` is read by the seal-time API-coverage gate and decides whether the phase can seal. |
| checklist → human tester | QA rows are the instructions a release tester follows by hand. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-21 | Repudiation | a fabricated coverage matrix hiding an undecided capability gap | medium | mitigate | The declaration form is used only because this phase integrates no external API; the verify step asserts the exact single-line form and rejects a table, so a matrix cannot be smuggled in unreviewed. |
| T-85-22 | Tampering | an edit that silently renumbers or removes an existing QA row | medium | mitigate | The verify step asserts `git diff --numstat` reports zero deletions for the checklist, so only appended lines can pass. |
| T-85-23 | Information disclosure | checklist steps naming a real customer path or credential | low | accept | The rows name only placeholder paths and settings names; no environment-specific value is written. |
| T-85-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager install task and no new dependency; the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
- `.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md` is a single line under 200 characters beginning `No external API integration: `.
- `git -C /home/coder/repos/bbj-language-server diff --numstat -- QA/FULL-TEST-CHECKLIST.md` reports zero deletions and a non-zero addition count.
- The checklist contains rows for the external PREFIX change (both IDEs), the atomic save, the out-of-workspace config file, the SETOPTS-no-restart case and the save burst.
</verification>

<success_criteria>
- The seal-time API-coverage gate has a reasoned declaration and does not block on prose.
- Every hot-reload behavior a hermetic test cannot reach has a checklist row with exact steps and an exact expected result, including the two negative checks (no restart on a SETOPTS write, no restart mid-validation).
- No existing checklist row was altered.
</success_criteria>

<output>
Create `.planning/phases/85-config-hot-reload-with-restart-coalescing/85-05-SUMMARY.md` when done.
</output>
