---
phase: 92-host-side-hygiene-focus-guards
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/document-formatter.ts
  - bbj-vscode/test/document-formatter.test.ts
autonomous: true
requirements: [RESP-06]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Two overlapping format requests for one document URI with different content spawn the formatter twice, and each resolves with the output computed from its own content (D-08)."
    - "Two overlapping format requests for one URI with identical content still share one spawn, so the Save All dedupe tests in the `P62-D3-001` block pass unchanged (D-08)."
    - "When a request with newer content replaces the in-flight entry, the older run settling afterwards does not evict the newer entry: a third request identical to the newer content still shares the newer spawn (concurrency edge)."
    - "Once a run settles on either the resolve or the reject path, its in-flight entry is removed only if it is still the current entry for that URI."
  prohibitions:
    - statement: "MUST NOT replace the user's interim edits with formatter output computed from older document text."
      status: resolved
      verification: test
  artifacts:
    - path: bbj-vscode/src/document-formatter.ts
      provides: "inFlightFormats keyed by URI holding { content, promise }; reuse only on identical content"
      contains: "content === documentContent"
    - path: bbj-vscode/test/document-formatter.test.ts
      provides: "overlapping different-content race test and newer-entry survival test"
      contains: "spawn"
  key_links:
    - from: "DocumentFormatter.provideDocumentFormattingEdits"
      to: "inFlightFormats"
      via: "content-equality check before sharing the in-flight promise"
      pattern: "\\.content === documentContent"
---

<objective>
RESP-06 (#499). A format request shares an in-flight formatter run only when the text it read is identical to the text that run started with. Otherwise it spawns a fresh run against the current text (D-08).

Purpose: today a second request during interim edits receives the first run's output and replaces the newer buffer with formatted old text.
Output: a content-aware in-flight map in `document-formatter.ts` and two new race tests. Every existing formatter test still passes.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-PATTERNS.md

<interfaces>
Verified during planning (base commit `3ec25f02`):

- `document-formatter.ts`:
  - line 17: `const inFlightFormats = new Map<string, Promise<string>>();`
  - lines 54-67: `documentContent = unsavedContentMap.get(uri) || document.getText()`, then share-or-spawn, with a `clearInFlight` identity guard (`inFlightFormats.get(uriKey) === formatPromise`) on both settle paths.
  - `runFormatter(args, documentContent)` pipes `documentContent` into `p.stdin.end(...)`.
- `document-formatter.test.ts`:
  - mocks `vscode` with `__testState.onDidChangeTextDocument` captured, so a test can inject an interim edit into the module's unsaved-content map;
  - mocks `child_process.spawn`, with `makeFakeProcess()` exposing `stdin.end` as a `vi.fn`;
  - `makeDocument(uriPath, text)`;
  - the `P62-D3-001` describe holds `spawnCollectingImpl()`, which collects every fake process in spawn order.
- `unsavedContentMap` is module-level and keyed by URI, so new tests use URIs no other test uses.
- `test/no-shell-command-construction.test.ts` requires `verifyFormatterArtifacts(` to appear before `cp.spawn(` in this file; do not reorder `runFormatter`.

Shell rules: absolute paths only; `git -C /home/coder/repos/bbj-language-server …`; never chain `cd` with grep/find/cat/sed/head/tail/git; stage exact paths only.
Register rule: no plan, decision, threat or pitfall ids in source or test text (the pre-existing `P62-…` finding ids and issue numbers such as `#499` are fine).
Deviations: record them in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- No new files or exported symbols.
- `inFlightFormats` changes value type to `{ content: string; promise: Promise<string> }`, still module-private.
- New tests are added inside the existing `P62-D3-001` describe of `document-formatter.test.ts`.

## Flagged assumptions

- The newer run replaces the in-flight entry for its URI. The older run still resolves its own caller with its own output. Whether VS Code then applies that older result is VS Code's own document-version handling, outside this module.

## Edge coverage (spec-less probe)

- **RESP-06 concurrency:** resolved (explicit). See the different-content, identical-content and newer-entry-survival truths; Tasks 1 and 2 test them.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A format request made after an interim edit gets output computed from its own text, not the earlier in-flight run's</name>
  <files>bbj-vscode/src/document-formatter.ts, bbj-vscode/test/document-formatter.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/document-formatter.ts — whole file
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/document-formatter.test.ts — mocks (1-75) and the `P62-D3-001` block (97-160)
  </read_first>
  <behavior>
    - Test (race, issue #499), URI `/tmp/format-race-499.bbj`:
      - request 1 is made with text `rem before`;
      - an interim edit to `rem after` is injected through `__testState.onDidChangeTextDocument`;
      - request 2 is made for the same URI with text `rem after`.
      - Expect: spawn is called twice; process 0's `stdin.end` receives `rem before` and process 1's receives `rem after`.
      - Process 1 then emits `formatted after` and closes with code 0, and only after that process 0 emits `formatted before` and closes with code 0.
      - Expect: request 2 resolves with `newText` `formatted after` and request 1 with `formatted before`.
  </behavior>
  <action>
  1. RED. Add the race test above inside the `P62-D3-001` describe and use `spawnCollectingImpl()`. Confirm it fails today: one spawn, and request 2 resolves with request 1's output.
  2. GREEN. In `document-formatter.ts`:
     - Change `inFlightFormats` to `Map<string, { content: string; promise: Promise<string> }>`.
     - In `provideDocumentFormattingEdits`, read `const inFlight = inFlightFormats.get(uriKey);`. Reuse `inFlight.promise` only when `inFlight.content === documentContent`.
     - Otherwise start `this.runFormatter(args, documentContent)`, store `{ content: documentContent, promise }` under `uriKey` (replacing any older entry), and attach the cleanup to both settle paths.
     - The cleanup deletes the key only when `inFlightFormats.get(uriKey)?.promise` is identical to this run's promise, so an older run's settle cannot evict a newer entry.
     - Rewrite the block comment above the map (lines 12-16): share only for identical content, because a request made after an interim edit must never receive output formatted from older text (issue #499); keep the Save All rationale.
  3. Run the file; every test in it must pass, including the untouched `P62-D3-001` identical-content cases.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/document-formatter.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'content === documentContent' /home/coder/repos/bbj-language-server/bbj-vscode/src/document-formatter.ts` prints 1.
    - The race test and all four pre-existing `P62-D3-001` tests pass in the vitest run.
  </acceptance_criteria>
  <done>A format request made after interim edits spawns its own run and applies only its own output. Identical-content bursts still share one spawn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A newer run's in-flight entry survives the older run settling, so identical follow-up requests keep sharing it</name>
  <files>bbj-vscode/test/document-formatter.test.ts, bbj-vscode/src/document-formatter.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/document-formatter.ts — the updated share-or-spawn block from Task 1
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/no-shell-command-construction.test.ts — lines 108-121, the verifier-before-spawn guard over this file
  </read_first>
  <behavior>
    - Test, URI `/tmp/format-race-499-newer.bbj`:
      - request A (`rem a`) is made; an interim edit to `rem b` is injected; request B (`rem b`) is made (2 spawns);
      - process 0 (A) emits output and closes with code 0; request A is awaited;
      - request C for the same URI with text `rem b` is made.
      - Expect: spawn is still called exactly 2 times.
      - Process 1 emits `formatted b` and closes with code 0.
      - Expect: B and C both resolve with `formatted b`.
    - Test, URI `/tmp/format-race-499-cleared.bbj`: after both runs of a different-content pair settle, one more request with the latest content spawns again (3 spawns total), proving no entry leaks.
  </behavior>
  <action>
  1. Add the two tests above inside the `P62-D3-001` describe.
  2. If either fails, fix only the cleanup identity comparison in `document-formatter.ts` so it compares the stored entry's `promise` with the settling run's own promise; do not add a timer, a counter or a version field.
  3. Run the formatter test file, then the source-guard suite that fences this file, then lint and the register check.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/document-formatter.test.ts test/no-shell-command-construction.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 3ec25f02 -- bbj-vscode/src/document-formatter.ts bbj-vscode/test/document-formatter.test.ts | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text)</fails_when>
  </verify>
  <acceptance_criteria>
    - The vitest run reports both new tests passing, and zero failed tests across `document-formatter.test.ts` and `no-shell-command-construction.test.ts`.
    - Lint exits 0 and the register check prints nothing.
  </acceptance_criteria>
  <done>Overlapping format requests are fenced both ways: different text never shares, identical text always shares with the newest run, and no in-flight entry leaks after settling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live editor buffer → formatter stdin → edits applied to the buffer | formatted output replaces the whole document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-05 | Tampering | `provideDocumentFormattingEdits` in-flight sharing (user's own document via a logic race) | medium | mitigate | share only when `inFlight.content === documentContent`; race test with two different-content requests asserts each gets its own output |
| T-92-06 | Denial of Service | in-flight map growth | low | mitigate | entry deleted on both settle paths when still current; test asserts a later request spawns again after settle |
| T-92-SC | Tampering | npm installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. `test/document-formatter.test.ts` passes, including the new race, newer-entry and cleanup tests and all pre-existing cases.
2. `test/no-shell-command-construction.test.ts` still passes (verifier call precedes spawn).
3. Lint passes; the register check prints nothing.
</verification>

<success_criteria>
- ROADMAP criterion 2: a format request never applies content computed from an earlier in-flight request over the user's interim edits.
- D-08 is implemented exactly: share only for identical text; the one-spawn dedupe for identical content is kept.
</success_criteria>

<output>
Create `.planning/phases/92-host-side-hygiene-focus-guards/92-02-SUMMARY.md` when done. Record the new test names and any deviation.
</output>
