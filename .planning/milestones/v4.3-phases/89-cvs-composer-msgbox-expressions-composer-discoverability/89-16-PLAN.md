---
phase: 89-cvs-composer-msgbox-expressions-composer-discoverability
plan: 16
type: execute
wave: 2
depends_on: ["89-14", "89-15"]
gap_closure: true
gap_ids: [G-89-3]
autonomous: true
requirements: [DISC-03]
files_modified:
  - bbj-vscode/test/functional/installed-extension-e2e.test.ts
  - QA/FULL-TEST-CHECKLIST.md
  - .planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The language server inside the reinstalled VS Code bundle decodes `a$ = CVS(` at character 9 as `found: true`, `editable: false`, `incomplete: true`, with span 5..9, an empty string prefill and no reason. It decodes `a$ = CVS(name$)` as incomplete with `str` `name$`, and still decodes the fixture's `1+4` line as editable (DISC-03)."
    - "The IntelliJ plugin zip rebuilt after both code plans lists a `language-server/main.cjs` entry from this build."
    - "QA/FULL-TEST-CHECKLIST.md gains two steps with explicit no-nesting expected results: IntelliJ row 25 (typing `a$ = CVS(` then pressing Alt+Enter) and VS Code row 19 (the lightbulb and context-menu equivalent, including the stale-line refusal). No other row changes."
    - "The MSGBOX unfinished-call nesting is recorded as a pending todo for the composer-robustness phase, not fixed in Phase 89."
    - statement: "In IntelliJ, re-running UAT test 3 against a plugin zip rebuilt from the final tree opens the CVS() composer at every entry point, including typing `CVS(` then Alt+Enter, with no error notice. Applying leaves one complete CVS() call. The VS Code lightbulb equivalent behaves the same (D-06, D-14)."
      verification: backstop
  prohibitions:
    - "No source change under `bbj-vscode/src` or `bbj-intellij/src`. This plan proves, documents and stages; the code plans 89-14 and 89-15 own every behaviour."
    - "The existing QA rows keep their wording; only a step 5 and its expected-result sentence are appended to rows 19 and 25."
  artifacts:
    - path: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      provides: "installed-bundle decode assertions for an unfinished CVS call"
    - path: "QA/FULL-TEST-CHECKLIST.md"
      provides: "the unfinished-call steps in VS Code row 19 and IntelliJ row 25"
    - path: ".planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md"
      provides: "the recorded MSGBOX follow-up"
  key_links:
    - from: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      to: "bbj-vscode/src/cvs-composer.ts"
      via: "`bbj/composer/cvs/decodeCall` sent to the installed bundle's server returns the `incomplete` outcome"
    - from: "QA/FULL-TEST-CHECKLIST.md"
      to: ".planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-UAT.md"
      via: "the row 25 step 5 is the reproduction for UAT test 3's reported issue"
---

<objective>
Finish closing UAT gap G-89-3 (DISC-03, #649) at the level users experience it. This plan:
- proves the rebuilt and reinstalled VS Code bundle serves the new `incomplete` decode outcome end to end, and rebuilds the IntelliJ zip;
- adds the missing unfinished-call steps to the QA checklist in both IDEs;
- records the MSGBOX nesting follow-up decided in 89-14-PLAN.md (gap decision 7);
- stages the re-run of UAT test 3, plus the VS Code lightbulb equivalent, as end-of-phase human checks against artifacts rebuilt from the final tree.

Purpose: the gap closes only when the artifacts the tester installs carry the fix, and a human confirms the reported flow.
Output: one installed-bundle e2e test, QA checklist steps, a pending todo, and two human checks.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-UAT.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-14-PLAN.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-14-SUMMARY.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-15-SUMMARY.md
@.planning/debug/g-89-3-cvs-composer-unfinished-call.md

<interfaces>
From plans 89-14 and 89-15:
- `bbj/composer/cvs/decodeCall` with `{ line, character }` returns the incomplete outcome for a CVS call with no mask yet. For `a$ = CVS(` at 9 that is `{"found":true,"editable":false,"incomplete":true,"edit":{"callStart":5,"callEnd":9},"initial":{"str":"","bits":[],"chars":""},"trailingArgs":[]}`.
- VS Code labels:
  - the lightbulb action is `Complete CVS() call…`;
  - the panel title is `Complete CVS() call`;
  - `Compose CVS() (visual)…` in the palette and editor context menu is position-aware;
  - the stale warning is `The CVS() call changed since the composer opened; nothing was applied.`
- IntelliJ labels:
  - the Alt+Enter intention stays `Configure CVS() options…`;
  - the dialog title is `Complete CVS() call`, with OK `Apply`;
  - the not-editable reason notice is unchanged for `mode%`.
- No cue appears on half-typed CVS lines.

Verified in the tree during planning:
- `bbj-vscode/test/functional/installed-extension-e2e.test.ts`:
  - lines 993-1066 hold the `new decode payloads on the installed bundle` describe, gated by `describe.skipIf(!installPresent)`;
  - that describe spawns `install!.serverPath` over IPC, opens the cue fixture, and has a CVS test at 1052-1065 that sends `{ line: lineText }`;
  - line 1068 is the skip-notice test.
- The `bbj/composer/cvs/decodeCall` handler is `(p) => decodeCvsCall(p.line, p.character)`.
- `QA/FULL-TEST-CHECKLIST.md`:
  - row format is `| N | Name | numbered steps joined by <br> | expected | [ ] |`;
  - VS Code row 19 (line 45) has four steps;
  - IntelliJ row 25 (line 79) has four steps.
- The todo format, from `.planning/todos/pending/2026-09-06-configured-node-path-suppresses-cached-download-fallback.md`: front matter `created`, `title`, `area`, `severity`, `files`, then `## Problem` and `## What's needed`.
- Build and install:
  - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (at `/usr/local/bin/bbj-ext-install`), which writes `/tmp/bbj-lang.vsix` and installs it into `~/.ext-test`;
  - `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q` produces `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`;
  - a Swing stack trace during `buildSearchableOptions` is headless noise when the exit code is 0.
- The workflow's human checks live in `<verify><human-check>` blocks and are harvested into UAT, as in plan 89-13.

Shell rules:
- Absolute paths only.
- Never chain `cd` with grep/find/cat/sed/head/tail; use `git -C /home/coder/repos/bbj-language-server`.
- Run vitest with cwd = bbj-vscode: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run <file>`.
- Gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`.
- Before reinstalling, stop any competing Gradle or e2e run.
- Stage exact paths only.

Comment discipline: test comments, test names and assertion messages must not contain planning register ids (plan numbers such as 89-16, decision ids, gap ids, threat ids, C-/CR-/WR-/IN- ids). GitHub issue numbers are fine. Planning files such as the todo may name the gap.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Gap decisions this plan carries (full record in 89-14-PLAN.md)

- Decision 4: no cue on half-typed lines. The QA steps state that no cue appears above an unfinished call.
- Decision 7: the MSGBOX( nesting is out of this plan set and goes to the composer-robustness phase as a pending todo.
- The build commands in the human checks reflect the standing rule that UAT runs against distributables rebuilt from the final tree, after any code-review fixes.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the rebuilt, installed bundles carry the unfinished-call outcome end to end</name>
  <files>bbj-vscode/test/functional/installed-extension-e2e.test.ts</files>
  <precondition>`bbj-ext-install` is on PATH, `/home/coder/.ext-test/extensions/extensions.json` exists, and the commits of plans 89-14 and 89-15 are on the current branch.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/installed-extension-e2e.test.ts lines 1-100 and 993-1075 — install discovery, `findPosition`, the decode-payload describe and the skip notice
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/cvs-composer.ts — `CvsDecodeCallResult.incomplete` and `decodeCvsCall` as plan 89-14 left them
    - /home/coder/repos/bbj-language-server/.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-14-SUMMARY.md — the recorded decode verdicts
  </read_first>
  <action>
    In the `new decode payloads on the installed bundle` describe, add one test after the existing CVS test: `bbj/composer/cvs/decodeCall on an unfinished call returns the incomplete outcome with the call span`. Keep the 30-second timeout and the stderr message on the first expectation. It sends two requests built from literal line texts, with no fixture change:
    - `{ line: 'a$ = CVS(', character: 9 }` must return `found` true, `editable` false, `incomplete` true, `edit` `{ callStart: 5, callEnd: 9 }`, `initial.str` empty, `initial.bits` empty, and `reason` undefined.
    - `{ line: 'a$ = CVS(name$)' }` must return `incomplete` true and `initial.str` `name$`.

    Then run, in order:
    - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`
    - `bbj-ext-install`
    - the e2e file
    - `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q`
    - `unzip -l` on the zip

    Record in the SUMMARY: the installed extension version, `installedTimestamp`, the zip path, and the `language-server/main.cjs` entry timestamp from the listing.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit or any `error TS` line</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/functional/installed-extension-e2e.test.ts</automated>
    <fails_when>non-zero exit, a failed test, or every test in the file reported as skipped (the install was not refreshed)</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>unzip -l /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip</automated>
    <fails_when>non-zero exit, or no listed entry ends in `language-server/main.cjs`</fails_when>
  </verify>
  <acceptance_criteria>
    - The new test ran (non-zero pass count for the describe) against a bundle reinstalled after this plan's build, and passed, together with the existing `1+4` editable test.
    - The SUMMARY records the extension version, `installedTimestamp`, zip path and `main.cjs` entry timestamp.
  </acceptance_criteria>
  <done>The artifacts users install decode an unfinished CVS call as a composable call, and the IntelliJ zip carries the same freshly built server.</done>
</task>

<task type="auto">
  <name>Task 2: QA steps for completing an unfinished CVS() call in both IDEs, the MSGBOX follow-up todo, and the UAT test 3 re-run as end-of-phase human checks</name>
  <files>QA/FULL-TEST-CHECKLIST.md, .planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md lines 38-50 and 72-84 — rows 19 and 25 and the row format
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-06-configured-node-path-suppresses-cached-download-fallback.md — the todo format
    - /home/coder/repos/bbj-language-server/.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-UAT.md — test 3 and gap G-89-3
    - /home/coder/repos/bbj-language-server/.planning/debug/g-89-3-cvs-composer-unfinished-call.md lines 89-116 — the MSGBOX probe and the compose-new write-shape evidence
  </read_first>
  <action>
    IntelliJ row 25: append a step 5 to the steps cell, after step 4, in the same `<br>` format:
    - On a new line type `a$ = CVS(` (the editor adds the closing parenthesis), press Alt+Enter and choose `Configure CVS() options…`.
    - Type `name$` in the string field, check bits 1 and 4, and apply.
    - Repeat on a new line with `b$ = CVS(name$` using the editor context menu.

    Append to the expected cell: `Step 5:`
    - a `Complete CVS() call` dialog opens with no error notice;
    - its string field is editable (empty, then prefilled with `name$`), there is no assign field, and OK stays disabled while the string is empty;
    - applying leaves `a$ = CVS(name$, 5)` and a single complete `CVS(name$, …)` call on the `b$` line — never a nested `CVS(CVS(`.

    VS Code row 19: append a step 5 in the same format:
    - On a new line type `a$ = CVS(name$`, press Ctrl+. and choose `Complete CVS() call…`, check bits 1 and 4, and insert.
    - On another new line type `b$ = CVS(` and run `Compose CVS() (visual)…` from the editor context menu.
    - While that panel is open, type more characters at the end of the `b$` line, then press Insert.

    Append to the expected cell: `Step 5:`
    - the lightbulb offers `Complete CVS() call…`, and the panel opens with an editable string field and no assign field;
    - the `a$` line becomes `a$ = CVS(name$, 5)`;
    - the context menu opens the same complete-the-call panel for the `b$` call rather than inserting a whole call inside it;
    - Insert after the line changed shows `The CVS() call changed since the composer opened; nothing was applied.` and leaves the line unchanged;
    - no `Compose CVS()` cue appears above either unfinished line.

    Change no other row or cell.

    Create the todo `.planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md`.
    - Front matter:
      - `created` (current ISO timestamp)
      - `title: MSGBOX compose-new nests a second call inside an unfinished MSGBOX( call`
      - `area: composer`
      - `severity: minor`
      - `files`: `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java`, `bbj-vscode/src/msgbox-composer.ts`, `bbj-vscode/src/msgbox-composer-ui.ts`, `bbj-vscode/src/msgbox-composer-webview.ts`
    - `## Problem`, from the debug evidence:
      - `x = MSGBOX(` and `x = MSGBOX()` decode as `found: false`, so both IDEs take the compose-new path and insert a whole composed statement at the caret inside the unfinished call (IntelliJ: `openMsgbox` → `insertAtCaret`);
      - a dialog does open, so no error is shown, but the result nests one call inside the other;
      - it was found while diagnosing Phase 89 UAT gap G-89-3, whose CVS() counterpart was fixed by giving the unfinished call a composable decode outcome and a guarded replace of its span.
    - `## What's needed`:
      - an unfinished-call decode outcome for MSGBOX and a guarded replace of the partial call span, designed together with MSGBOX's own `replace` payload, `sameMsgbox` and banner semantics rather than copied from CVS;
      - it belongs with Phase 90 (Composer Robustness), next to its success criterion on re-resolving the MSGBOX target before an edit;
      - deliberately not fixed in Phase 89.

    Also apply the deviation rule from `<interfaces>`.
  </action>
  <verify>
    <automated>grep -n "Complete CVS() call" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit or fewer than 2 matching lines (row 19 and row 25)</fails_when>
    <automated>test -f /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md</automated>
    <fails_when>non-zero exit</fails_when>
    <human-check>
      <test>First rebuild both distributables from the final tree, after any code-review fixes: `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (→ `/tmp/bbj-lang.vsix`); then `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --console=plain -q` (→ `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`). Install the zip in IntelliJ and re-run UAT test 3 in full: Alt+Enter on a complete CVS( call, the editor context menu, and the Compose CVS() cue. Then run QA row 25 step 5: type `a$ = CVS(`, press Alt+Enter, and repeat with `b$ = CVS(name$` from the context menu.</test>
      <expected>
        - Every entry point opens the CVS() composer; the unfinished calls open as `Complete CVS() call` with no error notice.
        - The eight operations render as one flat checkbox list, with no byte-group headers and no scroll pane.
        - The chars field stays visible but greyed out, with its BBj 19.0/19.10 tooltip, while no chars-customizable bit is checked.
        - OK stays disabled until the first preview resolves, and while the string is empty.
        - Applying leaves exactly one complete CVS() call on each line.
      </expected>
      <why_human>The Alt+Enter intention popup, the modal dialog's layout and the resulting document write happen in a running IntelliJ that this devcontainer cannot drive headlessly.</why_human>
    </human-check>
    <human-check>
      <test>Install the rebuilt `/tmp/bbj-lang.vsix` in VS Code and run QA row 19 step 5: the lightbulb on `a$ = CVS(name$`, the editor context menu on `b$ = CVS(`, and typing on the `b$` line while its panel is open before pressing Insert.</test>
      <expected>
        - The lightbulb offers `Complete CVS() call…`, and applying yields `a$ = CVS(name$, 5)`.
        - The context menu opens the complete-the-call panel for the `b$` call rather than inserting a nested call.
        - Insert after the line changed shows the "changed since the composer opened" warning and writes nothing.
        - No `Compose CVS()` cue appears above either unfinished line.
      </expected>
      <why_human>The lightbulb menu, the webview panel, and a live edit made beside the non-modal panel all need a running VS Code.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Rows 19 and 25 each gained exactly one step 5 and a matching `Step 5:` expected sentence. `git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md` shows edits to those two rows only.
    - The todo exists with the five front-matter keys and both sections, and names Phase 90 as its destination.
  </acceptance_criteria>
  <done>The manual checklist and the phase's UAT both carry the exact flow the tester reported, in both IDEs, and the MSGBOX sibling defect is recorded rather than lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → gap closure claim | Unit tests alone cannot show that the installed artifacts carry the fix |
| rebuilt artifact → human UAT | A human check is only meaningful against the final build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-52 | Repudiation | gap declared closed from unit tests while the installed bundle is stale | medium | mitigate | The e2e test decodes an unfinished call against the reinstalled bundle's own server; `unzip -l` proves the plugin zip carries a freshly built `main.cjs` |
| T-89-53 | Repudiation | UAT re-run against an artifact built before code-review fixes | medium | mitigate | Both human checks begin with explicit rebuild-from-final-tree commands for the VSIX and the plugin zip |
| T-89-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install`, then `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/functional/installed-extension-e2e.test.ts`
2. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q`, then `unzip -l /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`
3. Register scan over the test file's added lines: `git -C /home/coder/repos/bbj-language-server diff HEAD~2..HEAD -- bbj-vscode/test/functional/installed-extension-e2e.test.ts | grep '^+' | grep -nE '\b8[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-8[0-9]-[0-9]+\b|\bG-8[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'` prints nothing. Widen the range if this plan made more than two commits.
4. `git -C /home/coder/repos/bbj-language-server diff HEAD~2..HEAD --stat -- bbj-vscode/src bbj-intellij/src` prints nothing.
5. The two human checks above are harvested into UAT for the end-of-phase re-run of test 3.
</verification>

<success_criteria>
- The installed VS Code bundle and the rebuilt IntelliJ zip carry the unfinished-call outcome, proven by an e2e decode and a zip listing.
- The QA checklist reproduces UAT test 3's reported flow in IntelliJ and its VS Code equivalent, with explicit no-nesting expectations.
- The MSGBOX sibling defect is a pending todo aimed at Phase 90, and the human re-run of test 3 is staged against artifacts rebuilt from the final tree.
</success_criteria>

<output>
Create `.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-16-SUMMARY.md` when done. Record:
- the extension version, `installedTimestamp`, zip path and `main.cjs` entry timestamp;
- the exact step 5 text added to rows 19 and 25;
- the todo path;
- the two human checks, verbatim.
</output>
