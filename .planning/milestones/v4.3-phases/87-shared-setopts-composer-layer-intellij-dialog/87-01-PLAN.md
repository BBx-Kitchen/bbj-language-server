---
phase: 87-shared-setopts-composer-layer-intellij-dialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/language/composer-commands.ts
  - bbj-vscode/test/composer-commands.test.ts
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerRequestContractTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFlowTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java
autonomous: true
requirements: [DISC-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC3: every new SETOPTS DTO crossing the LSP4IJ boundary is parsed by LSP4IJ's own MessageJsonHandler in ComposerModelsJsonBoundaryTest, including the reserved-word `byte` JSON key and an oversized-int negative control; the SETOPTS vector crosses as a hex String, never as a numeric bitmask."
    - "SC1 (server half): `bbj/composer/setopts/decodeCall` returns the edit target and the prefill selection for an existing SETOPTS line, and `found: false` for any line the composer cannot round-trip (D-05, D-06)."
    - "SC1 (server half): `bbj/composer/setopts/preview` returns the resulting hex digits, the composed line, the human summary, the mask-inputs-enabled flag and the unknown-bit list for one selection, computed by `setoptsPreview` and nothing else (D-09)."
    - "The aggregate `bbj/composer/catalogs` response carries a `setopts` field with all 50 catalog bits (label, detail, bbj, bbjDetail, since) and the 7 byte groups in catalog order (D-07, D-08)."
    - "Both new request names appear in ComposerRequestContractTest's reflective interface scan AND its literal DECLARED_REQUESTS set AND as quoted literals in composer-commands.ts."
    - "`DecodeEquality.sameSetopts` reports a mismatch when any compared field of the decode result changed, comparing `hexRange` element-wise rather than by array identity."
  artifacts:
    - bbj-vscode/src/language/composer-commands.ts
    - bbj-vscode/test/composer-commands.test.ts
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java
  key_links:
    - "composer-commands.ts `composerHandlers` -> `setopts-catalog.ts` pure functions (parseSetOptsLine, setoptsPreview, parseVector) — the only place SETOPTS arithmetic runs."
    - "BbjComposerServer `@JsonRequest` names -> the quoted literals in composer-commands.ts, pinned by ComposerRequestContractTest."
    - "ComposerModels SETOPTS DTO field names -> the TypeScript handler's JSON keys, pinned by ComposerModelsJsonBoundaryTest."
    - "ComposerFlowTest.FakeComposerServer -> BbjComposerServer: every added interface method needs an @Override stub or the test file fails to compile."
  prohibitions:
    - "No SETOPTS byte/bit arithmetic may be reimplemented in Java — the catalog, vector parse/encode and preview computation exist only in setopts-catalog.ts."
    - "No SETOPTS DTO field may represent the option vector as a numeric bitmask; the vector crosses the boundary as a hex String end-to-end."
    - "No try/catch or input-sanitising branch may be added to a composer-commands.ts handler — every handler stays a pure pass-through, consistent with the rest of the file."
---

<objective>
Add the shared `bbj/composer/setopts/*` command layer to the language server and the matching
LSP4IJ boundary types on the IntelliJ side, so the IntelliJ dialog built in plans 02 and 03 can
drive the exact same SETOPTS catalog and flag arithmetic VS Code's composer already uses (DISC-04,
#633).

Purpose: this is the "one shared layer both IDEs consume" half of the phase goal. It adds zero new
domain logic — every handler is a thin pass-through to `bbj-vscode/src/setopts-catalog.ts`, exactly
the shape `composer-commands.ts` already uses for MSGBOX/addWindow/addChildWindow.

Output: two new LSP requests, a `setopts` field on the existing aggregate catalog request, the
Gson-serializable DTOs for both directions, the `sameSetopts` stale-edit comparator, and the
cross-language contract plus JSON-boundary coverage that proves the wire shape (success criterion 3).
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-shared-setopts-composer-layer-intellij-dialog/87-CONTEXT.md
@.planning/phases/87-shared-setopts-composer-layer-intellij-dialog/87-RESEARCH.md
@.planning/phases/87-shared-setopts-composer-layer-intellij-dialog/87-PATTERNS.md
@bbj-vscode/src/setopts-catalog.ts
@bbj-vscode/src/language/composer-commands.ts
@bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
</context>

## Artifacts this phase produces

Phase-wide roll-up (this plan owns the entries marked **[01]**; plan 02 owns **[02]**, plan 03 owns **[03]**):

| Symbol | Kind | Plan |
|---|---|---|
| `bbj/composer/setopts/decodeCall` | new LSP request name | **[01]** |
| `bbj/composer/setopts/preview` | new LSP request name | **[01]** |
| `setopts` field on `bbj/composer/catalogs` | extended LSP response | **[01]** |
| `composerHandlers['bbj/composer/setopts/decodeCall']` | new TS handler | **[01]** |
| `composerHandlers['bbj/composer/setopts/preview']` | new TS handler | **[01]** |
| `BbjComposerServer.setoptsDecodeCall` | new `@JsonRequest` method | **[01]** |
| `BbjComposerServer.setoptsPreview` | new `@JsonRequest` method | **[01]** |
| `ComposerModels.SetoptsBit` | new DTO | **[01]** |
| `ComposerModels.SetoptsByteGroup` | new DTO | **[01]** |
| `ComposerModels.SetoptsCatalogs` | new DTO (+ `ComposerCatalogs.setopts` field) | **[01]** |
| `ComposerModels.SetoptsSelectionBit` | new DTO | **[01]** |
| `ComposerModels.SetoptsSelection` | new DTO (also the decode result's `initial` payload) | **[01]** |
| `ComposerModels.SetoptsPreviewParams` | new DTO | **[01]** |
| `ComposerModels.SetoptsPreview` | new DTO | **[01]** |
| `ComposerModels.SetoptsUnknownBits` | new DTO | **[01]** |
| `ComposerModels.SetoptsDecodeCallParams` | new DTO | **[01]** |
| `ComposerModels.SetoptsEdit` | new DTO | **[01]** |
| `ComposerModels.SetoptsDecodeResult` | new DTO | **[01]** |
| `DecodeEquality.sameSetopts` | new comparator | **[01]** |
| `concurrency/PreviewDebouncer` | new class | [02] |
| `concurrency/PreviewDebouncerTest` | new test file | [02] |
| `composer/SetoptsComposerDialog` | new Swing dialog | [02] |
| `ComposerLauncher.Kind.SETOPTS` | new enum constant | [03] |
| `actions/BbjComposeSetoptsAction` | new `AnAction` | [03] |
| `bbj.composeSetopts` | new plugin.xml action id | [03] |
| `actions/BbjComposeSetoptsActionSourceGuardTest` | new test file | [03] |
| QA `FULL-TEST-CHECKLIST.md` IntelliJ row 18 | new hand-check row | [03] |

## Flagged assumptions (read before starting)

1. **DISC-04 edge probe came back `unclassified`.** The deterministic edge-coverage probe could not
   classify DISC-04 (it is a single UI-feature requirement, not a numbered edge-case list), so it
   stays `unresolved` rather than auto-resolved. The phase's four ROADMAP success criteria are used
   as the concrete edge-shaped criteria instead (lifted into `must_haves.truths` across the three
   plans). This is recorded, not silently dropped: if a reviewer expects a per-edge DISC-04 table,
   there is none and none is claimed.
2. **D-06 open question resolved as Option B (caret-line, not file-first-line).** RESEARCH.md
   Pitfall 5 / assumption A1 left this open. It is now locked: the IntelliJ action targets the line
   under the caret and composes a new line there when that line is not a SETOPTS line. Rationale —
   it is the literal reading of D-01/D-03/D-06 ("the current line"), it matches VS Code's own
   CodeLens/Code Action entry points (`argForLine`, which are per-line), it needs no file-wide scan
   on every `update()`, and it means `decodeCall` never needs a caret-column parameter. It diverges
   only from VS Code's *Command Palette* fallback (`argForActiveEditor`, which prefers the file's
   first SETOPTS line); that divergence is deliberate and must not be filed as a bug later.
   Reversibility: reversible.
3. **`SETOPTS_BITS` has 50 entries, not 58.** 87-CONTEXT.md says 58; the source
   (`bbj-vscode/src/setopts-catalog.ts`) has 50 (bytes 1/2/4: 8 each, byte 3: 8, byte 7: 7,
   byte 8: 7, byte 9: 4 — the 51st `byte:` occurrence in the file is `MASK_REPLACEMENT_BIT`, not a
   catalog entry). Code wins; assert 50.
4. **Assumption-delta checkpoint: `no-change`.** The detector fired on the compound term
   "custom-request capability" in the ROADMAP section text. No identity-model transition happens in
   this phase: `setopts` is a fourth kind added alongside the existing `msgbox`/`addwindow`/
   `addchildwindow` kinds, which is the established multi-kind pattern, not a generalisation of a
   core assumption. Recorded as `no-change`; no plan change follows from it.
5. **`bbj-vscode/test/composer-commands.test.ts` already exists** (87-RESEARCH.md and 87-VALIDATION.md
   both list it as a Wave 0 gap — that is stale). It has one `describe('composer LS command layer
   (#433)')` block with 14 tests. Extend it; do not create a second file.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end `bbj/composer/setopts/decodeCall` — one request through every layer</name>
  <files>bbj-vscode/src/language/composer-commands.ts, bbj-vscode/test/composer-commands.test.ts, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerRequestContractTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFlowTest.java</files>
  <read_first>
    - bbj-vscode/src/setopts-catalog.ts (whole file — `parseSetOptsLine` at line 260, `SetOptsLineInfo` at 245, `SetOptsSelection` at 285, `getBit`, `maskChar`, `rawTail`, `MASK_COMMA_BYTE`, `MASK_DOT_BYTE`)
    - bbj-vscode/src/language/composer-commands.ts (whole file — the msgbox `decodeCall` handler at lines 82-104 is the direct template)
    - bbj-vscode/test/composer-commands.test.ts (the `call()` helper and existing test style)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java (whole file — the `MsgboxDecodeResult`/`MsgboxEdit`/`AddWindowEdit` DTO conventions and the `long`-for-raw-bits note)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java (whole file)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerRequestContractTest.java (whole file — `DECLARED_REQUESTS` at lines 42-53 and the two tests that read it)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java (whole file — the `parse()` helper at 37-46 and `aMsgboxDecodeCallResponseParsesThroughTheLsp4jGson` at 124-144)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFlowTest.java (lines 80-140 — `FakeComposerServer`)
  </read_first>
  <behavior>
    TypeScript handler `bbj/composer/setopts/decodeCall`, params `{ line: string }`:
    - `'SETOPTS 08004020000000'` returns `found: true`, `edit.hexRange` `[8, 22]`, `edit.hexDigits` `'08004020000000'`, `edit.insertOffset` absent, and an `initial` payload whose `bits` are exactly the catalog bits set in that vector, with `maskComma`/`maskDot`/`rawTail` read from the same vector.
    - `'SETOPTS'` (bare keyword, no digits) returns `found: true`, `edit.insertOffset` `7`, no `hexRange`, no `hexDigits`, and an `initial` payload of `{ bits: [], maskComma: '', maskDot: '', rawTail: '' }`.
    - `'  setopts 0800'` (leading whitespace, lower case) is recognised — `found: true` — because `parseSetOptsLine` is case-insensitive and skips leading whitespace.
    - `'PREFIX /usr/lib/'`, `'SETOPTS 0800 extra'` and `'SETOPTS ZZZZ'` each return `found: false` and nothing else — the composer must not touch what it cannot round-trip.
    Java boundary: a real JSON-RPC response envelope for that request parses through LSP4IJ's own `MessageJsonHandler` into `SetoptsDecodeResult`, with the JSON key `"byte"` landing in the `byteNo` field of every `SetoptsSelectionBit`.
  </behavior>
  <action>
Wire ONE request from the TypeScript handler through the DTOs and the LSP4IJ proxy interface to a
passing cross-language contract test and a real JSON-boundary round trip. No dialog, no launcher, no
second request — those are Task 2 and plans 02/03.

**TypeScript half** (`bbj-vscode/src/language/composer-commands.ts`):
- Add a SETOPTS import block after the existing `addchildwindow-composer.js` import, importing
  `SETOPTS_BITS`, `BYTE_GROUPS`, `parseVector`, `parseSetOptsLine`, `setoptsPreview`, `getBit`,
  `maskChar`, `rawTail`, `MASK_COMMA_BYTE`, `MASK_DOT_BYTE`, and the types `SetOptsSelection` and
  `SetOptsVector` from `'../setopts-catalog.js'`. Import only what Task 1 and Task 2 use together —
  a single import statement, added once.
- Add a module-private helper `setoptsInitialSelection(v: SetOptsVector | undefined): SetOptsSelection`
  that returns `{ bits: [], maskComma: '', maskDot: '', rawTail: '' }` when `v` is undefined, and
  otherwise `{ bits: SETOPTS_BITS.filter(b => getBit(v, b.byte, b.mask)).map(b => ({ byte: b.byte, mask: b.mask })), maskComma: maskChar(v, MASK_COMMA_BYTE), maskDot: maskChar(v, MASK_DOT_BYTE), rawTail: rawTail(v) }`.
  This composes already-exported catalog primitives; it introduces no new arithmetic. Deliberately do
  NOT export it and do NOT touch `bbj-vscode/src/setopts-composer-webview.ts`, whose own private
  `initialSelection` does the same composition — the phase boundary says VS Code needs zero code
  changes, and collapsing the two onto one export is left as a follow-up note for the SUMMARY.
- Add the handler `'bbj/composer/setopts/decodeCall': (p: { line: string })` inside `composerHandlers`,
  placed in a new `// ---- SETOPTS (#633) ----` section after the addChildWindow section. It calls
  `parseSetOptsLine(p.line)`; on `undefined` it returns `{ found: false }`; otherwise it returns
  `{ found: true, edit: { hexRange: info.hexRange, insertOffset: info.insertOffset, hexDigits: info.hexDigits }, initial: setoptsInitialSelection(info.vector) }`.
- Add no try/catch and no validation branch — every handler in this file is a pure pass-through.

**Java DTO half** (`ComposerModels.java`), appended as a new `// ---- SETOPTS (#633) ----` section at
the end of the class, all fields public and Gson-serializable:
- `SetoptsSelectionBit` — `@SerializedName("byte") public int byteNo;` and `public long mask;`, plus a
  `(int byteNo, long mask)` constructor and a no-arg constructor Gson can use. `byte` is a Java
  reserved word, so the `@SerializedName("byte")` annotation (import `com.google.gson.annotations.SerializedName`)
  is what keeps the wire key identical to the TypeScript shape.
- `SetoptsSelection` — `public List<SetoptsSelectionBit> bits;`, `public String maskComma = "";`,
  `public String maskDot = "";`, `public String rawTail = "";`. This one type is used both as the
  preview request's selection and as the decode result's `initial` payload (the same JSON shape),
  mirroring how `AddChildWindowDecodeResult` reuses `AddWindowEdit`/`AddWindowInitial`.
- `SetoptsEdit` — `public int[] hexRange;`, `public Integer insertOffset;`, `public String hexDigits;`.
  Offsets stay plain `int`: they are bounded positions inside one config line, never LSP
  `Position.character` values, so no `END_OF_LINE_CHARACTER` sentinel applies here.
- `SetoptsDecodeResult` — `public boolean found;`, `public SetoptsEdit edit;`, `public SetoptsSelection initial;`.
- `SetoptsDecodeCallParams` — `public String line;` and a `(String line)` constructor. No `character`
  field: the trigger is line-scoped (D-03, D-06 Option B), so a caret column would be unused surface
  the boundary test would then have to carry for no behavioural reason.

**Proxy half** (`BbjComposerServer.java`): add
`@JsonRequest("bbj/composer/setopts/decodeCall") CompletableFuture<SetoptsDecodeResult> setoptsDecodeCall(SetoptsDecodeCallParams params);`
with a javadoc line stating it decodes one config.bbx line into the edit target plus the prefill
selection, and returns `found=false` for a line the composer cannot round-trip. Add the two matching
imports.

**The three lockstep files** (RESEARCH.md Pitfall 4 — miss one and the build breaks):
- `ComposerRequestContractTest.DECLARED_REQUESTS`: add `"bbj/composer/setopts/decodeCall"` and update
  the javadoc above it, which currently says "The ten names this test expects" — after this task it
  is eleven, after Task 2 twelve; write the number the set actually has when the task ends.
- `ComposerFlowTest.FakeComposerServer`: add an `@Override public CompletableFuture<SetoptsDecodeResult> setoptsDecodeCall(SetoptsDecodeCallParams params) { throw new UnsupportedOperationException(); }`
  stub alongside the other unused-request stubs, plus its import. Without it the whole test file fails
  to compile.
- `ComposerModelsJsonBoundaryTest`: add `aSetoptsDecodeCallResponseParsesThroughTheLsp4jGson`, built
  with the existing private `parse(...)` helper against request name `"bbj/composer/setopts/decodeCall"`,
  result type `SetoptsDecodeResult.class` and param type `SetoptsDecodeCallParams.class`. The envelope
  must carry `"edit":{"hexRange":[8,22],"insertOffset":null,"hexDigits":"08004020000000"}` and
  `"initial":{"bits":[{"byte":3,"mask":2}],"maskComma":",","maskDot":".","rawTail":""}`. Assert
  `found` is true, `hexRange` length 2 with `[0]` == 8, `hexDigits` equals the literal above, and —
  the point of the test — that `initial.bits.get(0).byteNo` is 3, proving the `@SerializedName("byte")`
  mapping survives LSP4IJ's own deserializer.

**TypeScript tests** (`composer-commands.test.ts`, inside the existing describe block): add
`setopts/decodeCall decodes an existing line, a bare keyword, and refuses what it cannot round-trip`
covering every case listed in the behavior block above.
  </action>
  <reversibility rating="costly">The request name `bbj/composer/setopts/decodeCall` becomes a cross-process contract that Phase 88 extends; renaming it later means changing the TS handler, the proxy interface and the contract test's literal set in one commit. Not one-way — the plugin bundles the very server it talks to, so the two sides always ship together.</reversibility>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/composer-commands.test.ts && cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerRequestContractTest' --tests 'com.basis.bbj.intellij.composer.ComposerModelsJsonBoundaryTest' --tests 'com.basis.bbj.intellij.composer.ComposerFlowTest'</automated>
    <fails_when>non-zero exit; "BUILD FAILED" in the Gradle output; a Vitest summary line containing "failed"; or a Gradle compile error naming ComposerFlowTest (the FakeComposerServer stub was not added)</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/composer-commands.test.ts` passes, and its output includes a test name containing `setopts/decodeCall`.
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerRequestContractTest'` passes, meaning `DECLARED_REQUESTS` and the reflective scan of `BbjComposerServer` agree and `bbj/composer/setopts/decodeCall` was found as a quoted literal in `composer-commands.ts`.
    - `ComposerModelsJsonBoundaryTest` contains a test whose body parses request name `bbj/composer/setopts/decodeCall` and asserts `initial.bits.get(0).byteNo` equals 3 from an envelope whose JSON key is `"byte"`.
    - `grep -c "setoptsDecodeCall" bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFlowTest.java` returns at least 1.
    - `bbj-vscode/src/setopts-composer-webview.ts` and `bbj-vscode/src/setopts-composer-ui.ts` are byte-identical to their pre-task state.
  </acceptance_criteria>
  <done>One SETOPTS request is live end-to-end: the TypeScript handler answers it, the proxy interface declares it, the DTOs carry it across LSP4IJ's real deserializer, and the cross-language contract test proves both sides name it identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand to `bbj/composer/setopts/preview` and the `setopts` catalog field</name>
  <files>bbj-vscode/src/language/composer-commands.ts, bbj-vscode/test/composer-commands.test.ts, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerRequestContractTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFlowTest.java</files>
  <read_first>
    - bbj-vscode/src/setopts-catalog.ts (`setoptsPreview` at 310-335, `SetOptsPreview` at 295-303, `SETOPTS_BITS` at 58-116, `BYTE_GROUPS` at 35-43, `encodeVector`, `describeVector`)
    - bbj-vscode/src/language/composer-commands.ts (as edited by Task 1 — the `bbj/composer/catalogs` handler at lines 53-57 and the msgbox/addwindow `preview` handlers)
    - bbj-vscode/test/composer-commands.test.ts (the `catalogs returns both composers option sets` test, which asserts exact catalog lengths)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java (as edited by Task 1 — `ComposerCatalogs` at 42-48, `CatalogItem` at 22-28, `MsgboxPreviewParams` at 66-70 for the param-wrapper convention)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java (`aComposerCatalogsResponseParsesThroughTheLsp4jGson` at 48-78 and the negative control at 217-244)
  </read_first>
  <behavior>
    TypeScript handler `bbj/composer/setopts/preview`, params `{ original?: string; selection: SetOptsSelection }`:
    - With `original` undefined and an empty selection, returns `hexDigits: '00000000'`, `line: 'SETOPTS 00000000'`, `summary: '(default settings)'`, `maskInputsEnabled: false`, `unknownByBytes: []` — the `emptyVector()` baseline.
    - With `original: '08004020000000'` and a selection that adds byte 1 mask 0x40, the returned `hexDigits` keeps all 14 digits and differs from the original only in byte 1 — the lossless round-trip guarantee, because `setoptsPreview` starts from the original rather than from zero.
    - With `original` carrying a bit no catalog entry models, the returned `unknownByBytes` names that byte and mask and the digit survives in `hexDigits`.
    - With a `rawTail` containing a non-hex character, the returned `hexDigits` is unchanged from the original — `setoptsPreview` silently ignores an invalid tail (the dialog, not this handler, is what refuses to apply it).
    `bbj/composer/catalogs` gains a `setopts` field with exactly 50 `bits` and exactly 7 `byteGroups`, the byte groups ordered 1, 2, 3, 4, 7, 8, 9.
  </behavior>
  <action>
Expand the proven slice to the second request and the catalog payload the dialog renders from.

**TypeScript** (`composer-commands.ts`):
- Add `setopts: { bits: SETOPTS_BITS, byteGroups: Object.keys(BYTE_GROUPS).map(Number).map(byte => ({ byte, label: BYTE_GROUPS[byte] })) }`
  to the `'bbj/composer/catalogs'` handler's returned object, after `addchildwindow`. `BYTE_GROUPS` is
  reshaped from a numeric-keyed record into an ordered array deliberately: a JSON object's key order
  is not a contract across Gson, and D-07 requires the dialog to render section headers in catalog
  order. `bits` passes through verbatim so the `label`, `detail`, `bbj`, `bbjDetail` and `since`
  fields all reach the client — D-08's de-emphasis needs `bbj` and `bbjDetail`.
- Add the handler `'bbj/composer/setopts/preview': (p: { original?: string; selection: SetOptsSelection }) => setoptsPreview(p.original ? parseVector(p.original) : undefined, p.selection)`
  in the SETOPTS section. It is a one-expression pass-through; add no reshaping and no validation.

**Java DTOs** (`ComposerModels.java`, in the SETOPTS section):
- `SetoptsBit` — `@SerializedName("byte") public int byteNo;`, `public long mask;`, `public String label;`,
  `public String detail;`, `public String bbj;`, `public String bbjDetail;`, `public String since;`.
  `bbj` carries the string `ignored` or `bbj-specific` or null; the dialog reads it for D-08.
- `SetoptsByteGroup` — `@SerializedName("byte") public int byteNo;`, `public String label;`.
- `SetoptsCatalogs` — `public List<SetoptsBit> bits;`, `public List<SetoptsByteGroup> byteGroups;`.
- Add `public SetoptsCatalogs setopts;` to the existing `ComposerCatalogs` class.
- `SetoptsUnknownBits` — `@SerializedName("byte") public int byteNo;`, `public long mask;`.
- `SetoptsPreview` — `public String hexDigits;`, `public String line;`, `public String summary;`,
  `public boolean maskInputsEnabled;`, `public List<SetoptsUnknownBits> unknownByBytes;`.
- `SetoptsPreviewParams` — `public String original;`, `public SetoptsSelection selection;`, and a
  `(String original, SetoptsSelection selection)` constructor. `original` is null in compose-new mode.
  Note in a javadoc line that the vector crosses as a hex `String`, never as a number, which is what
  keeps the whole 32-bit-sign-bit overflow class (G-81-4) out of this DTO family.

**Proxy** (`BbjComposerServer.java`): add
`@JsonRequest("bbj/composer/setopts/preview") CompletableFuture<SetoptsPreview> setoptsPreview(SetoptsPreviewParams params);`
with a javadoc line naming `setoptsPreview` in `setopts-catalog.ts` as the single computation behind it.

**Lockstep files:** add `"bbj/composer/setopts/preview"` to `DECLARED_REQUESTS` (and correct the count
word in its javadoc to twelve), and add the matching `@Override` stub to `ComposerFlowTest.FakeComposerServer`.

**JSON boundary tests** (`ComposerModelsJsonBoundaryTest`), three additions:
- `aSetoptsCatalogsResponseParsesThroughTheLsp4jGson` — extend the existing catalogs envelope with a
  `"setopts"` object carrying one bit (`{"byte":8,"mask":64,"label":"MKEYED verb creates XKEYED files","detail":null,"bbj":"bbj-specific","bbjDetail":null,"since":null}`)
  and one byte group (`{"byte":1,"label":"Errors, console & listing"}`), then assert `setopts.bits.get(0).byteNo`
  is 8, `setopts.bits.get(0).bbj` is `bbj-specific`, and `setopts.byteGroups.get(0).label` is the
  literal above. Either extend the existing catalogs test or add a sibling — do not delete the
  existing assertions.
- `aSetoptsPreviewResponseParsesThroughTheLsp4jGson` — parse a full `SetoptsPreview` envelope
  (`hexDigits`, `line`, `summary`, `maskInputsEnabled`, and an `unknownByBytes` entry with key
  `"byte"`), asserting `hexDigits` is the exact string, `maskInputsEnabled` is true, and
  `unknownByBytes.get(0).byteNo` is the expected byte.
- `theSetoptsPreviewParamsSerializeWithTheWireKeyByte` — the request direction, which the existing
  family does not cover: build a `SetoptsPreviewParams` holding one `SetoptsSelectionBit(3, 2)`,
  serialize it with `new com.google.gson.Gson().toJson(params)`, and assert the resulting JSON
  contains the substring `"byte":3` and does not contain the substring `"byteNo"`. Without this, a
  dropped `@SerializedName` would still pass every response-direction test while silently sending a
  key the server ignores.

**TypeScript tests** (`composer-commands.test.ts`): extend the existing catalogs test with
`expect(c.setopts.bits).toHaveLength(50)` and `expect(c.setopts.byteGroups.map((g: any) => g.byte)).toEqual([1, 2, 3, 4, 7, 8, 9])`,
and add `setopts/preview starts from the original vector and never from zero` covering every case in
the behavior block — the lossless round-trip case is the one that matters most, since it is the
structural mitigation for the data-loss threat in this plan's threat model.
  </action>
  <reversibility rating="reversible">Reshaping `BYTE_GROUPS` into an ordered array and the exact DTO field set are internal wire details; both sides ship together and the contract test catches any drift.</reversibility>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/composer-commands.test.ts && cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerRequestContractTest' --tests 'com.basis.bbj.intellij.composer.ComposerModelsJsonBoundaryTest' --tests 'com.basis.bbj.intellij.composer.ComposerFlowTest'</automated>
    <fails_when>non-zero exit; "BUILD FAILED" in the Gradle output; a Vitest summary line containing "failed"; or an assertion message naming `setopts.bits` (the catalog length drifted from 50)</fails_when>
  </verify>
  <acceptance_criteria>
    - The catalogs test asserts `c.setopts.bits` has length 50 and `c.setopts.byteGroups` maps to `[1, 2, 3, 4, 7, 8, 9]`.
    - A Vitest test proves that previewing `original: '08004020000000'` with one added bit returns a 14-digit `hexDigits` that differs from the original in exactly one byte.
    - `ComposerModelsJsonBoundaryTest` gains three tests: a catalogs assertion reaching `setopts.bits.get(0).bbj`, a `SetoptsPreview` response parse, and a params-serialization assertion on the substring `"byte":3`.
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerRequestContractTest'` passes with twelve declared request names.
  </acceptance_criteria>
  <done>Both SETOPTS requests and the `setopts` catalog field are live, round-trip through LSP4IJ's real deserializer in both directions, and the lossless "start from the original" guarantee is pinned by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `DecodeEquality.sameSetopts` and its field-wise coverage</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java</files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java (whole file — the class javadoc states the equality contract, `sameWindowEdit` at 98-110 shows the `Arrays.equals` rule for `int[]` ranges)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java (whole file — the fixture-plus-mutator test style)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java (the SETOPTS DTOs added in Tasks 1 and 2)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java (`rangeArraysAreNeverComparedByIdentity` — the `Arrays.equals(` count and the two forbidden identity comparisons)
  </read_first>
  <behavior>
    - Two structurally identical `SetoptsDecodeResult` values match, even when their `hexRange` arrays are distinct instances.
    - Changing `found`, `edit.hexRange` (any element), `edit.insertOffset`, `edit.hexDigits`, `initial.bits`, `initial.maskComma`, `initial.maskDot` or `initial.rawTail` breaks the match.
    - A null on either side returns false; two nulls return true; neither case throws.
  </behavior>
  <action>
Add `public static boolean sameSetopts(SetoptsDecodeResult a, SetoptsDecodeResult b)` to
`DecodeEquality`, following the existing comparators exactly: return `a == b` when either is null,
then compare `a.found == b.found` and delegate to two new private helpers, `sameSetoptsEdit` and
`sameSetoptsSelection`.

`sameSetoptsEdit` compares `Arrays.equals(a.hexRange, b.hexRange)`, `Objects.equals(a.insertOffset, b.insertOffset)`
and `Objects.equals(a.hexDigits, b.hexDigits)`. Never compare `hexRange` by array reference — a fresh
re-decode always returns a new array instance, so identity comparison would report every re-decode as
a mismatch and turn the stale-edit guard into a permanent refusal.

`sameSetoptsSelection` compares `maskComma`, `maskDot` and `rawTail` with `Objects.equals`, and
compares `bits` element-wise: same size, and for each index the same `byteNo` and `mask`. The DTO
classes have no `equals` override, so `Objects.equals` on the lists alone would compare by reference
and always report a mismatch — write the explicit element loop.

Extend the class javadoc's closing sentence (which states that a field added to a decode result must
be added to its comparator) so it names the SETOPTS result too.

In `DecodeEqualityTest`, add a SETOPTS section mirroring the existing MSGBOX/addWindow sections: a
`baseSetopts()` fixture builder, an identical-match test, one mutator test per compared field listed
in the behavior block, a null-handling test, and a test that two fixtures built with separately
allocated but equal `hexRange` arrays still match (`assertNotSame` on the arrays, `assertTrue` on the
comparison) — the same shape the existing array test uses.
  </action>
  <reversibility rating="reversible">A pure comparator with no persisted state and no published surface.</reversibility>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.DecodeEqualityTest' --tests 'com.basis.bbj.intellij.composer.ComposerApplyGuardSourceGuardTest'</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output, or a failure message naming `rangeArraysAreNeverComparedByIdentity`</fails_when>
  </verify>
  <acceptance_criteria>
    - `DecodeEqualityTest` contains at least eight SETOPTS assertions: identical-match, one per mutated field (`found`, `hexRange`, `insertOffset`, `hexDigits`, `bits`, `maskComma`, `maskDot`, `rawTail`), null handling, and the distinct-array-instance case.
    - `ComposerApplyGuardSourceGuardTest` still passes unchanged — `Arrays.equals(` occurs at least twice in `DecodeEquality.java` and neither forbidden identity comparison appears.
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*'` is green for the whole composer test package.
  </acceptance_criteria>
  <done>The stale-edit guard has a SETOPTS comparator with field-wise coverage, ready for plan 03 to hand to `StaleEditGuard.applyIfUnchanged`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ plugin process → language server (LSP4IJ JSON-RPC) | Request params built from dialog state cross a process boundary and are deserialized by the server; responses cross back and are deserialized by lsp4j's Gson into the DTOs added here. |
| Language server → `setopts-catalog.ts` | Handler params reach pure domain functions that compute the hex string a host will later write into the user's config file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Tampering | `bbj/composer/setopts/preview` handler | medium | mitigate | The handler passes `original` through to `setoptsPreview`, which starts from the original vector rather than from zero — unmodeled bytes 10-16 and unknown bits survive. Pinned by the Task 2 lossless-round-trip Vitest test. |
| T-87-02 | Tampering | SETOPTS DTOs at the LSP4IJ boundary | medium | mitigate | The vector crosses as a hex `String`, never a numeric bitmask, removing the 32-bit-sign-bit overflow class (G-81-4) entirely; offsets stay bounded `int`. Pinned by `ComposerModelsJsonBoundaryTest`'s SETOPTS parses plus the existing oversized-int negative control. |
| T-87-03 | Tampering | `@SerializedName("byte")` on three DTOs | medium | mitigate | A dropped annotation would silently send/receive a key the other side ignores, producing a wrong hex string. Pinned in both directions: response parsing asserts `byteNo` is populated from `"byte"`, and the params-serialization test asserts the emitted JSON contains `"byte":3`. |
| T-87-04 | Denial of Service | new requests on the shared proxy interface | low | accept | Both handlers are pure, allocation-bounded computations over one line of text; the client bounds every call with `ComposerFlow`'s existing timeouts. No new mitigation. |
| T-87-05 | Information Disclosure | handler params/results | low | accept | Params carry one line of the user's own config file within the user's own session; nothing is logged or transmitted beyond the existing LSP channel. |
| T-87-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. 87-RESEARCH.md's Package Legitimacy Audit records that the phase adds no dependency to `package.json` or `build.gradle.kts`, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/composer-commands.test.ts` — green.
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*'` — green (contract, boundary, flow, decode-equality, and every existing composer source guard).
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew compileJava compileTestJava --offline` — green.
- `git -C /home/coder/repos/bbj-language-server diff --name-only` lists no file under `bbj-vscode/src/setopts-composer-*.ts`.
</verification>

<success_criteria>
- Twelve request names are declared on `BbjComposerServer`, all found as quoted literals in the language-server sources.
- The `setopts` catalog carries 50 bits and 7 ordered byte groups.
- Previewing with an `original` never loses an unmodeled byte or an unknown bit.
- `sameSetopts` exists with field-wise coverage and element-wise `hexRange` comparison.
</success_criteria>

<output>
Create `.planning/phases/87-shared-setopts-composer-layer-intellij-dialog/87-01-SUMMARY.md` when done
</output>
