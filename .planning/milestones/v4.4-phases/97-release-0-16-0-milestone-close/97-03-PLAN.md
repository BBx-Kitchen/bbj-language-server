---
phase: 97-release-0-16-0-milestone-close
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/test/functional/issue447-real-interop.test.ts
  - .planning/todos/pending/
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The issue447 capability test passes against both java-interop backend shapes — the older one without `getAllClassNames` and the current one that has it — instead of hard-coding the older server's answer (folded todo 6)."
    - "Either the eleven failing `linking.test.ts` interop assertions pass after a warm-up fix, or the residual is re-filed as a new pending todo and named in the SUMMARY — the phase is not blocked on an open-ended investigation."
    - "`gradle-wrapper-hygiene.test.ts` is confirmed green against the committed wrapper, so folded todo 5 is bookkeeping only and no code change is made for it."
    - statement: "Neither test file reaches for `DocumentBuilder.build`, which triggers CPL/interop traffic and fails on CI."
      verification: backstop
  artifacts:
    - bbj-vscode/test/functional/issue447-real-interop.test.ts
  key_links:
    - "`shouldRunBBjTests()` -> `test.runIf(run)` / `describe.runIf(isInteropRunning)` — the gate both files already use; it stays."
    - "`JavaInteropService.ensureCompleteClassIndex()` / `hasCompleteClassIndex()` -> the capability whose answer differs per backend."
  prohibitions:
    - "No use of DocumentBuilder.build in any test touched here — parseHelper only."
    - "No change to bbj-vscode/src/ in this plan; this is test-tier housekeeping."
    - "No attempt to fix the shouldRunBBjTests() bare-TCP-connect false positive — that is a separately tracked debt item, explicitly out of scope."
    - "No planning identifiers (plan numbers, D-xx, REL-xx, T-97-xx) in any test comment; GitHub issue numbers are fine."
    - "No git branch creation, switching, renaming or `git stash`; no `git add -A` or `git add .`."
---

<objective>
Close the two test-housekeeping folded todos: the issue447 capability test still asserts the old
backend's answer and fails against this environment's live `:5008`, and the eleven `linking.test.ts`
"Interop related tests" failures need a real, time-boxed investigation rather than an assumed fix.
Folded todo 5 (gradle-wrapper-hygiene) was already fixed in September and needs verification only.

Purpose: 0.16.0's code wave must leave the local suite honest, so the whole-suite gate in plan 97-05
means something.
Output: one rewritten test, one investigation outcome (fix or re-filed todo), one confirmation.

## Artifacts this phase produces (plan 97-03)

- `bbj-vscode/test/functional/issue447-real-interop.test.ts` — the capability test rewritten to accept both backend shapes.
- Possibly `.planning/todos/pending/<date>-linking-interop-<slug>.md` — a new todo, only on the branch where the warm-up hypothesis does not hold.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/DEBT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RESEARCH.md
@.planning/todos/pending/2026-09-03-update-live-interop-tests-for-getallclassnames-backend.md
@.planning/todos/pending/2026-09-05-gradle-wrapper-hygiene-fixture-declares-stale-gradle-version.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`; `cd` is
  allowed only directly in front of a build tool.
- Vitest MUST run with cwd = `bbj-vscode` (project memory: running it from the repo root gives
  ENOENT in the language-configuration and textmate suites): `cd
  /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run <file>`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.
- No planning identifiers in test comments; GitHub issue numbers are fine.

**Known-state notes the executor must not re-derive.** RESEARCH.md ran all three targeted files this
session: `gradle-wrapper-hygiene.test.ts` 19/19 pass (the fixture now derives its expected lines from
the real `gradle-wrapper.properties` at test time, so the Gradle 9.7.1 wrapper does not break it);
`issue447-real-interop.test.ts` 1 fail / 1 pass, the failure being the capability test asserting
`false` where this environment's backend answers `true`; `linking.test.ts -t Interop` 11 fail / 7
pass / 24 skipped, matching the documented baseline. RESEARCH also **refuted** the todo's own leading
hypothesis: `completeClassIndex`/`hasCompleteClassIndex(` are referenced only inside
`bbj-vscode/src/language/java-interop.ts` (by the missing-`use` quick-fix helpers) and nothing in
`bbj-scope.ts`/`bbj-scope-local.ts`/`bbj-linker.ts` consults them, so a complete class index cannot by
itself change how `Map`/`Map.Entry`/`getValue` resolve. The project's own recorded memory points at a
cold-resolution warm-up gap instead.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the issue447 capability test passes against the live backend as it is today</name>
  <files>bbj-vscode/test/functional/issue447-real-interop.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/issue447-real-interop.test.ts (whole file — 59 lines; the `describe`/`beforeAll`/`test.runIf(run)` skeleton stays unchanged)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts (the `ensureCompleteClassIndex` / `hasCompleteClassIndex` / `resolveClassCandidatesBySimpleName` implementations — read enough to state what each returns on both backend shapes)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-helper.ts (`shouldRunBBjTests`, `initializeWorkspace`)
  </read_first>
  <action>
This is the thin end-to-end slice: one real test, against the real `:5008` backend, through the real
`JavaInteropService`, green on both backend shapes.

Rewrite only the body and title of the first test in the file (currently
`'capability detection: current server lacks getAllClassNames and degrades gracefully'`). Keep the
file's imports, `describe(...)`, `beforeAll(...)`, `linkingErrors(...)` helper, the second test and
the `test.runIf(run)` gating exactly as they are.

The rewritten test must:
  - call `ensureCompleteClassIndex()` once and hold its result in a local;
  - assert that result is a boolean (`typeof … === 'boolean'`), not a specific value — the backend
    shape is an environment fact, not a product invariant;
  - assert `hasCompleteClassIndex()` agrees with that result (same value), which is the real
    invariant: the probe's answer and the cached capability flag must not disagree;
  - assert the user-visible behaviour that must hold on **either** branch —
    `resolveClassCandidatesBySimpleName('HashMap')` contains `'java.util.HashMap'`;
  - carry a comment (no planning identifiers) recording that the deployed backend may or may not
    expose the augmented endpoint, and that the suggestion path must work either way.
Retitle it to state the invariant rather than one backend's answer, e.g.
`'capability detection: the index probe and the cached flag agree, and suggestions work either way'`.
Keep the 60000 ms timeout argument.

Run it against the live backend. If `shouldRunBBjTests()` returns false in this environment the test
skips and proves nothing — in that case set `RUN_BBJ_TESTS=1` for the run and say in the SUMMARY
which mode produced the result.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/issue447-real-interop.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a non-zero failed count, or it reports 0 tests run (the suite skipped and proved nothing)</fails_when>
    <automated>grep -c 'toBe(false)' /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/issue447-real-interop.test.ts</automated>
    <fails_when>the printed count is anything other than 0 — a backend-specific answer is still hard-coded</fails_when>
  </verify>
  <acceptance_criteria>
    - The file's first test no longer contains `toBe(false)`; it asserts `typeof` on the probe result and equality between the probe result and the cached flag.
    - It still asserts `expect(candidates).toContain('java.util.HashMap')`.
    - `describe`, `beforeAll`, `linkingErrors` and the second test are byte-identical to their pre-plan text.
    - The targeted vitest run reports 2 passed, 0 failed with `RUN_BBJ_TESTS=1`.
  </acceptance_criteria>
  <done>The capability test states a product invariant and passes against the backend actually running.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the gradle-wrapper-hygiene fixture is green (folded todo 5, no code change)</name>
  <files>bbj-vscode/test/gradle-wrapper-hygiene.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/gradle-wrapper-hygiene.test.ts (lines 1-60 — confirm the fixture derives its expected properties lines from the real file at test time rather than hard-coding a version)
    - /home/coder/repos/bbj-language-server/gradle/wrapper/gradle-wrapper.properties (if present) and /home/coder/repos/bbj-language-server/bbj-intellij/gradle/wrapper/gradle-wrapper.properties — the committed wrapper the fixture derives from
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-05-gradle-wrapper-hygiene-fixture-declares-stale-gradle-version.md
  </read_first>
  <action>
Verification only — make no edit to `gradle-wrapper-hygiene.test.ts` or to any wrapper file.

Run the test file and record: the pass count, and the Gradle distribution version the committed
wrapper declares. Then state, in one sentence for the SUMMARY, the fact that closes the todo: the
fixture derives its expected lines from the real wrapper properties at test time, so a wrapper
version bump cannot make it stale again. If — contrary to the researched state — the file is not
green, stop and report rather than editing anything: that would be a live regression in a phase
whose whole purpose is to ship, and it needs the maintainer.

Do **not** move the todo file here. All six todo files move together in plan 97-11 under the
milestone-close bookkeeping step.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/gradle-wrapper-hygiene.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a non-zero failed count</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --exit-code -- bbj-vscode/test/gradle-wrapper-hygiene.test.ts</automated>
    <fails_when>non-zero exit — the file was edited, but this todo is a bookkeeping close-out only</fails_when>
  </verify>
  <acceptance_criteria>
    - The targeted vitest run reports all tests passed, 0 failed.
    - `gradle-wrapper-hygiene.test.ts` is unmodified in the working tree.
    - The SUMMARY records the observed pass count and the wrapper version the fixture read.
  </acceptance_criteria>
  <done>Folded todo 5 is verified as already fixed, with an observed run rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Time-boxed investigation of the eleven linking interop failures</name>
  <files>bbj-vscode/test/linking.test.ts, .planning/todos/pending/</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/linking.test.ts (lines 1-40 and the `describe.runIf(isInteropRunning)("Interop related tests", …)` block — the failing assertions and the gate)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-helper.ts (`initializeWorkspace`, `shouldRunBBjTests`)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/bbj-test-module.ts (`createBBjTestServices` — note this file uses the test module, not the live `NodeFileSystem` wiring the issue447 file uses)
    - /home/coder/repos/bbj-language-server/.planning/DEBT.md (item 5 — the `shouldRunBBjTests()` bare-TCP-connect false positive; context only, explicitly out of scope)
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-03-update-live-interop-tests-for-getallclassnames-backend.md
  </read_first>
  <action>
Investigate, do not assume. Time-box to roughly 45 minutes of work; the phase must not block on an
open-ended hunt.

Step 1 — reproduce and characterise. Run the interop subset and record the failure messages
verbatim (the researched baseline is 11 failed / 7 passed / 24 skipped, with
`Could not resolve reference to JavaPackageLike named 'Map'`/`'Entry'` and an unresolved `getValue`).

Step 2 — test the warm-up hypothesis, which is the project's own recorded cold-resolution gotcha:
before the failing assertions run, explicitly warm the specific classes the failures name
(`java.util.Map`, `java.util.Map.Entry`) through the same `JavaInteropService` the suite already
uses, in the existing `beforeAll` of the `Interop related tests` describe block (add one if the
block has none), and re-run only this file. Use `parseHelper` for any parsing — never
`DocumentBuilder.build`, which triggers CPL/interop traffic and fails on CI.

Step 3 — branch on the result, and take exactly one branch:
  - **Warm-up fixes it:** keep the warm-up, with a comment (no planning identifiers) stating that
    large Java classes must be resolved once before the assertions that depend on them. Leave the
    suite gate (`describe.runIf(isInteropRunning)`) as it is.
  - **Warm-up does not fix it:** revert the experiment so `linking.test.ts` is byte-identical to its
    pre-plan state, and file a new todo at
    `.planning/todos/pending/2026-09-20-linking-interop-failures-survive-class-warmup.md` recording:
    the verbatim failure messages, that the complete-class-index hypothesis was refuted by source
    inspection, that the warm-up hypothesis was tested and refuted too, the exact command that
    reproduces it, and that the debt item about the connection probe's false positive is a plausible
    independent contributor which this phase deliberately did not touch. Say plainly in the SUMMARY
    that folded todo 6 shipped as "issue447 fixed, linking residual re-filed".

Either way, do not change anything under `bbj-vscode/src/`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/linking.test.ts -t "Interop" || ls /home/coder/repos/bbj-language-server/.planning/todos/pending/ | grep -q 'linking-interop'</automated>
    <fails_when>non-zero exit — the interop subset still fails AND no follow-up todo whose filename contains "linking-interop" exists under .planning/todos/pending/</fails_when>
    <automated>grep -c 'DocumentBuilder' /home/coder/repos/bbj-language-server/bbj-vscode/test/linking.test.ts</automated>
    <fails_when>the printed count is anything other than 0 — the investigation reached for the document builder instead of parseHelper</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --exit-code -- bbj-vscode/src</automated>
    <fails_when>non-zero exit — a source file under bbj-vscode/src was modified by a test-tier investigation</fails_when>
  </verify>
  <acceptance_criteria>
    - The verbatim failure messages of the baseline run are recorded for the SUMMARY.
    - Exactly one branch was taken: either `linking.test.ts` carries a class warm-up and the interop subset reports 0 failed, or `linking.test.ts` is byte-identical to its pre-plan state and `.planning/todos/pending/2026-09-20-linking-interop-failures-survive-class-warmup.md` exists and names the reproduction command.
    - `linking.test.ts` contains zero occurrences of `DocumentBuilder`.
    - Nothing under `bbj-vscode/src` was modified.
  </acceptance_criteria>
  <done>Folded todo 6's second half is settled by evidence — fixed, or re-filed with the refuted hypotheses recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> live java-interop service on :5008 | An external, out-of-repo service whose shape changes independently of this repository. |
| Local environment -> release gate | A locally green suite must not hide a CI-red one, and vice versa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-08 | Repudiation | Local suite baseline | medium | mitigate | The capability test asserts a product invariant (probe answer agrees with cached flag; suggestions resolve either way) instead of one backend's answer, so a green local run means the same thing as a green CI run. |
| T-97-09 | Tampering | Test-tier investigation scope | low | mitigate | An exit-code diff gate asserts nothing under `bbj-vscode/src` changed — a test-drift investigation cannot silently become a language-server edit. |
| T-97-10 | Information Disclosure | Live interop probe | low | accept | The probe talks to `127.0.0.1:5008` only and exchanges class metadata; no credential or user data crosses the boundary. |

No package-manager install occurs in this plan, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/issue447-real-interop.test.ts` reports 2 passed, 0 failed.
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/gradle-wrapper-hygiene.test.ts` reports 0 failed and the file is unmodified.
- The linking investigation ended in exactly one of its two branches, with evidence for the branch taken.
</verification>

<success_criteria>
- The issue447 capability test is backend-shape agnostic and green against the live service.
- Folded todo 5 is verified green with no code change.
- Folded todo 6's linking half is either fixed or re-filed, never silently dropped.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md` when done. Record which
branch the linking investigation took, the verbatim baseline failure messages, and whether the
issue447 run needed `RUN_BBJ_TESTS=1` to execute rather than skip.
</output>