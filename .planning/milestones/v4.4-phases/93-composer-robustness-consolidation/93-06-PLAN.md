---
phase: 93-composer-robustness-consolidation
plan: 06
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBase.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBaseSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFieldValidationSourceGuardTest.java
autonomous: true
requirements: [COMP-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The addWindow and addChildWindow dialogs share one base, so a fix to the shared addWindow-family flow is written once instead of hand-applied to two files (COMP-06)."
    - "The shared preview seam — `ComposerFlow`, `ComposerFlow.once`, `PreviewDebouncer` over `AlarmScheduler`, the 300ms delay and the sequence counter — is constructed exactly once, on the base."
    - "The OK-gating discipline is preserved exactly: OK is disabled up front before the first preview round trip, again the instant a new preview is scheduled, and again on a failed preview."
    - "Each subclass keeps its own field set, its own schematic panel type, its own preview request and its own `apply` — those genuinely differ and are not forced into the base."
    - "Both dialogs still gate OK on the server's own `valid` verdict and still render every one of the server's per-field error strings exactly once."
    - "`addGroupedChecks` is reconciled deliberately, not by whichever copy an extraction kept first: the two copies differ, and the difference is a per-checkbox tooltip."
    - statement: "Both dialogs open, preview, and write exactly as they do today, with no change to any label, default value or generated statement."
      verification: backstop
  artifacts:
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBase.java
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBaseSourceGuardTest.java
  key_links:
    - "`ComposerDialogRefreshSourceGuardTest` counts the debounce and OK-gating literals per dialog file; every one of those moves to the base for these two paths and must be re-pointed (D-11)."
    - "`ComposerFieldValidationSourceGuardTest` pins the `valid` gate and the per-field error reads in each subclass; those stay in the subclass `apply` and must keep passing."
  prohibitions:
    - "The two dialogs are not merged into one class and neither class is renamed — `ComposerLauncher` constructs each by name and each has its own title, button text and default selection."
    - "`ComposerFlow`, `StaleEditGuard`, `ComposerNotices`, `PreviewDebouncer` and `AlarmScheduler` are not re-implemented or bypassed; the base composes the existing seams."
    - "No helper consolidated in plan 93-01 is re-introduced as a private copy on the base or either subclass."
    - "The reconciliation of `addGroupedChecks` does not silently add or remove a user-visible tooltip; either the behaviour is proven identical or the difference is preserved behind a hook."
    - "The new guard imports no shared test utility; it carries its own private helper copies (D-12)."
    - "No planning identifier (COMP-06, D-01, D-11, D-12, plan numbers, C-xx, CR-xx) appears in any source or test file."
  flagged_assumptions:
    - "COMP-06's `concurrency` probe row is largely inapplicable: both dialogs are constructed and updated on the EDT, and the only concurrency in play is the existing sequence-counter and debouncer discipline, which this plan moves intact rather than changes. The relevant guarantee is captured in the OK-gating truth above. Flagged rather than silently dropped."
---

<objective>
Give the addWindow and addChildWindow dialogs one shared base, so the preview seam, the debounce
wiring and the OK-gating discipline they hold in duplicate are written once.

Purpose: COMP-06 (#630). The two dialogs are 397 and 411 lines and are structurally the same dialog:
identical seam construction, identical debounce wiring, identical sequence-counter discipline,
identical grouped-checkbox rendering, identical selection helpers and identical getters. They differ
only in their field set, their schematic panel type and their preview DTO. This plan lands before
plan 93-08's SETOPTS validation work so that any dialog-side validation the phase adds is written
once into a base rather than twice into two files — the ordering the roadmap fixes.
Output: `AddWindowFamilyComposerDialogBase`, two thin subclasses, one new guard, two re-pointed guards.
</objective>

<shell_rules>
The permission hooks block opaque shell commands. Every task below must obey these:
- Use absolute paths from `/home/coder/repos/bbj-language-server/...`. Never `.`, `..` or `~`.
- Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`. `cd` is allowed ONLY directly in
  front of a build tool that needs its project directory
  (`cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test`).
- Use `git -C /home/coder/repos/bbj-language-server <subcommand>` for git.
- Prefer the Read tool over shell `cat`; scope any `grep` with `--include` to the exact subtree.
- Never read `.env*`, `*.pem`, `*.key`. Stage with `git add <exact path>`, never `git add -A`.
</shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-composer-robustness-consolidation/93-CONTEXT.md
@.planning/phases/93-composer-robustness-consolidation/93-PATTERNS.md
@.planning/phases/93-composer-robustness-consolidation/93-RESEARCH.md
@.planning/phases/93-composer-robustness-consolidation/93-01-SUMMARY.md
</context>

<interface_context>
**Identical in both dialogs** (line numbers: `AddWindowComposerDialog` / `AddChildWindowComposerDialog`):
`PREVIEW_DEBOUNCE_MS = 300L` (`53`/`54`); the `project`, `server`, `catalogs`, `seq`, `flow`,
`balloonOnce`, `previewDebouncer` fields (`55-61`/`56-62`); `flagChecks`, `eventChecks`,
`eventEnabled`, `eventPanel` (`63-66`/`64-67`); `statementField`, `flagsSummary`, `eventSummary`,
`geometryPanel`; `editMode`, `initial`, `preservedFlagBits`, `preservedEventBits`; the `statement`,
`flagsHex`, `eventHex` volatiles; the whole seam-construction block in the constructor
(`115-124`/`120-129`); `preselect` (`141`/`146`); `updateEventEnabled` (`211`/`218`); `selected(Map)`
(`244`/`254`); `scheduleRefresh` (`259`/`269`); `errorText` (`327`/`341`); `prefill` (`332`/`346`);
`setSelected` (`342`/`356`); `getStatement`, `getFlagsHex`, `getEventHex`, `isEventEnabled`
(`373-389`/`387-403`); the `SimpleDocumentListener` record (`392`/`406`). Both take
`AddWindowCatalogs` — the same DTO.

**Genuinely different, and must stay in the subclass:** the text-field set (7 fields vs 9); the
schematic type (`WindowSchematicPanel` vs `ChildWindowSchematicPanel`, taking `WindowRender` vs
`ChildWindowRender`); the preview DTOs (`AddWindowPreview`/`AddWindowPreviewInput`/
`AddWindowPreviewParams` vs the `AddChildWindow*` trio); the `refresh()` body (`267`/`277`) and the
`apply(...)` body (`308`/`320`); `createCenterPanel()`, which lays out different fields and different
titled-border strings; the dialog title and OK-button text (`125-126`/`130-131`); the constructor's
default selection (`preselect(0x00000001L, 0x00000002L, 0x00010000L)` vs
`preselect(0x00010000L)`); and the balloon label string passed to `ComposerNotices.requestFailed`
(`"addWindow"` vs `"addChildWindow"`).

**The near-duplicate that needs reconciliation, not cut-paste:** `addGroupedChecks`
(`AddWindowComposerDialog:225-242` vs `AddChildWindowComposerDialog:232-252`) is identical EXCEPT
that the addChildWindow copy has an extra branch at `:245-247`:
`if (it.detail != null) { cb.setToolTipText(it.detail); }`. The addWindow copy has no tooltip branch.
Merging naively either adds tooltips to addWindow checkboxes or removes them from addChildWindow
checkboxes — both are user-visible changes this phase has not accepted.

**Guards over these two files.** `ComposerDialogRefreshSourceGuardTest` asserts PER DIALOG FILE:
`flow.observe(` == 1 (`:135`); `Preview unavailable — ` == 1 — already re-pointed by plan 93-01;
`setOKActionEnabled(false)` == 3 (`:175`) with the first one positioned before the first literal
`refresh();` (`:183-188`); `private void scheduleRefresh()` present and `previewDebouncer.trigger()`
== 1 (`:205-211`); `PREVIEW_DEBOUNCE_MS = 300L` == 1, `new AlarmScheduler(getDisposable())` == 1,
`new PreviewDebouncer(` == 1, `new Alarm(` == 0 (`:228-234`); `this::refresh` == 1 and `-> refresh()`
== 0 (`:249-252`); `ComposerFlow.once(` == 1 (`:262`); `seq.incrementAndGet()` count and
`mySeq == seq.get()` == 2 (`:274-280`); `scheduleRefresh()`'s body contains `seq.incrementAndGet()`
(`:299-306`); the sequence increment precedes `flow.observe(` (`:314-319`);
`ModalityState.any()` >= 1 (`:326-329`); `@Nullable Project project` == 0 and
`@NotNull Project project` >= 1 (`:354-361`). Both files are in `DIALOG_SOURCES` (`:61`) and
`DEBOUNCED_DIALOG_SOURCES` (`:73`).

`ComposerFieldValidationSourceGuardTest` asserts per dialog: `setOKActionEnabled(p.valid)` == 1,
`setOKActionEnabled(true)` == 0, each `p.*Error` field read exactly once (7 for addWindow, 9 for
addChildWindow), and zero occurrences of four forbidden validation-message fragments. All of these
concern `apply(...)`, which stays in the subclass.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Shared base carrying the addWindow dialog end-to-end</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBase.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java
  </files>
  <read_first>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java (all 397 lines — the file being split),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java lines 230-255 (the `addGroupedChecks` copy WITH the tooltip branch, the one near-duplicate needing reconciliation),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjRunActionBase.java (the repo's base + thin-subclass precedent: constructor, shared concrete methods, abstract hook)
  </read_first>
  <action>
Before writing any code, settle the `addGroupedChecks` tooltip difference. Determine whether any
addWindow flag or event-bit catalog item carries a non-null `detail` by reading the addwindow catalog
definition the language server ships in `bbj-vscode/src/` (the `addwindow` entry assembled in
`bbj-vscode/src/language/composer-commands.ts`'s catalogs handler). If no addWindow catalog item
carries a `detail`, the tooltip branch is unreachable for addWindow and the shared method can carry
it unconditionally — record that finding in the method's javadoc and in the SUMMARY. If any item does
carry a `detail`, keep the behaviours distinct by giving the base a
`protected boolean showsCatalogTooltips()` hook that addChildWindow overrides, so no user-visible
tooltip is added or removed by this refactor.

Create
`bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBase.java`
in package `com.basis.bbj.intellij.composer` as
`public abstract class AddWindowFamilyComposerDialogBase extends DialogWrapper`, following
`BbjRunActionBase`'s shape. Move onto it, unchanged in behaviour:

- `PREVIEW_DEBOUNCE_MS = 300L` and the `project`, `server`, `catalogs`, `seq`, `flow`, `balloonOnce`,
  `previewDebouncer` fields, all `protected` or `private` as their use requires.
- A `protected` constructor taking `(@NotNull Project project, @NotNull BbjComposerServer server,
  @NotNull AddWindowCatalogs catalogs, @Nullable ComposerModels.AddWindowInitial initial,
  boolean editMode, long preservedFlagBits, long preservedEventBits)` that calls `super(project)`,
  assigns the fields and constructs the seam exactly as `AddWindowComposerDialog:115-124` does today:
  `ComposerFlow.once(...)` for `balloonOnce`, a `ComposerFlow` over `ModalityState.any()` with
  `ComposerFlow.REFRESH_TIMEOUT_MILLIS`, and a `PreviewDebouncer` over
  `new AlarmScheduler(getDisposable())` with the 300ms delay and `this::refresh`. Keep
  `@NotNull Project project` on the signature — a guard pins that the renderer's non-null contract is
  visible here.
- The shared checkbox state: `flagChecks`, `eventChecks`, `eventEnabled`, `eventPanel`.
- The shared display fields `statementField`, `flagsSummary`, `eventSummary`, and `geometryPanel`.
- `preselect(long...)`, `updateEventEnabled()`, `addGroupedChecks(JPanel, List<CatalogItem>,
  Map<Long, JBCheckBox>)` (reconciled per the decision above), `selected(Map)`,
  `setSelected(Map, List<Long>)`, `errorText(String)`, `prefill(AddWindowInitial)`,
  `scheduleRefresh()`, the `previewUnavailable(String)` wrapper that delegates to
  `ComposerSwingHelpers.previewUnavailable(flagsSummary, reason)` and then calls
  `setOKActionEnabled(false)`, the four getters `getStatement`/`getFlagsHex`/`getEventHex`/
  `isEventEnabled` over `protected volatile` state, and the `SimpleDocumentListener` record.
- One abstract hook, declared on a single line ending in `;` with no body:
  `protected abstract void refresh();`.

Leave `createCenterPanel()` and `apply(...)` entirely to the subclass — they lay out different field
sets and consume different preview DTOs.

Convert `AddWindowComposerDialog` to `public final class AddWindowComposerDialog extends
AddWindowFamilyComposerDialogBase`. It keeps: both constructors (delegating to the base constructor
and then setting its own title, OK-button text, `init()`, the up-front `setOKActionEnabled(false)`,
its `preselect(0x00000001L, 0x00000002L, 0x00010000L)` default and its `refresh()` call, in exactly
today's order); its seven text fields and seven error labels; its `WindowSchematicPanel`; its
`createCenterPanel()`; its `refresh()` implementation, including `flow.observe(...)`, the
`mySeq == seq.get()` checks on both the success and failure paths, and the
`ComposerNotices.requestFailed("addWindow", ...)` balloon; and its `apply(AddWindowPreview p)`,
including `setOKActionEnabled(p.valid)` and the seven `p.*Error` reads.

Do not reintroduce any helper plan 93-01 moved to `ComposerSwingHelpers` — read `93-01-SUMMARY.md`
first to confirm what now lives there.

Cite `#630` in rationale comments. Never write a requirement id, a decision id or a plan number into
source or test files.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.ComposerFieldValidationSourceGuardTest" --tests "*.ComposerSwingHelpersSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output, or "cannot find symbol" in the output. `ComposerDialogRefreshSourceGuardTest` is EXPECTED to fail on its per-file counts for `AddWindowComposerDialog.java` at this point and is re-pointed in Task 3; `ComposerFieldValidationSourceGuardTest` must pass, because everything it pins stays in the subclass</fails_when>
  </verify>
  <acceptance_criteria>
    - `AddWindowFamilyComposerDialogBase.java` declares `public abstract class AddWindowFamilyComposerDialogBase extends DialogWrapper` and contains exactly one `new PreviewDebouncer(`, one `new AlarmScheduler(getDisposable())`, one `ComposerFlow.once(` and one `PREVIEW_DEBOUNCE_MS = 300L`.
    - `AddWindowFamilyComposerDialogBase.java` declares `protected abstract void refresh();` on a line ending in `;`.
    - `AddWindowComposerDialog.java` contains `extends AddWindowFamilyComposerDialogBase` exactly once and zero occurrences of `new PreviewDebouncer(`, `new AlarmScheduler(`, `ComposerFlow.once(` and `PREVIEW_DEBOUNCE_MS`.
    - `AddWindowComposerDialog.java` still contains `setOKActionEnabled(p.valid)` exactly once, `setOKActionEnabled(true)` zero times, and each of `p.receiverError`, `p.sysguiError`, `p.titleError`, `p.xError`, `p.yError`, `p.widthError`, `p.heightError` exactly once.
    - `AddWindowComposerDialog.java` still contains `ComposerNotices.requestFailed("addWindow"` exactly once and `mySeq == seq.get()` exactly twice.
    - The `addGroupedChecks` tooltip reconciliation is recorded in the shared method's javadoc.
    - `ComposerFieldValidationSourceGuardTest` passes.
  </acceptance_criteria>
  <done>The base exists and the addWindow dialog runs through it end-to-end, with its validation contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the addChildWindow dialog to the same base</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java
  </files>
  <read_first>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java (all 411 lines — the file being slimmed),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBase.java (the base as written in Task 1),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java (the converted reference subclass)
  </read_first>
  <action>
Convert `AddChildWindowComposerDialog` to `public final class AddChildWindowComposerDialog extends
AddWindowFamilyComposerDialogBase`, mirroring the addWindow conversion exactly.

Delete from it everything the base now owns: the debounce constant, the seam fields and their
construction block, the shared checkbox state, `preselect`, `updateEventEnabled`,
`addGroupedChecks`, `selected`, `setSelected`, `errorText`, `prefill`, `scheduleRefresh`, the
`previewUnavailable` wrapper, the four getters, and the `SimpleDocumentListener` record.

Keep, unchanged: both constructors, delegating to the base constructor and then setting the title
`"Configure child window flags"` / `"Compose addChildWindow"`, the OK-button text, `init()`, the
up-front `setOKActionEnabled(false)`, its own `preselect(0x00010000L)` default and its `refresh()`
call, in exactly today's order; its nine text fields and nine error labels; its
`ChildWindowSchematicPanel`; its `createCenterPanel()` with its own titled borders and its
nine-field document-listener loop; its `refresh()` building an `AddChildWindowPreviewInput` and
observing `server.addChildWindowPreview(...)`, including the
`ComposerNotices.requestFailed("addChildWindow", ...)` balloon and both `mySeq == seq.get()` checks;
and its `apply(AddChildWindowPreview p)` with `setOKActionEnabled(p.valid)` and all nine `p.*Error`
reads.

The base's `previewUnavailable` wrapper targets `flagsSummary`, which is the same field this dialog
targeted before — confirm that by reading its pre-conversion body rather than assuming.

If Task 1 introduced a `showsCatalogTooltips()` hook, override it here to preserve this dialog's
existing per-checkbox tooltips.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.ComposerFieldValidationSourceGuardTest" --tests "*.ComposerApplyGuardSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output, or "cannot find symbol" in the output. `ComposerDialogRefreshSourceGuardTest` is still EXPECTED to fail here and is re-pointed in Task 3</fails_when>
  </verify>
  <acceptance_criteria>
    - `AddChildWindowComposerDialog.java` contains `extends AddWindowFamilyComposerDialogBase` exactly once.
    - Across both subclass files combined, `new PreviewDebouncer(`, `new AlarmScheduler(`, `ComposerFlow.once(`, `private void scheduleRefresh()`, `private static void setSelected(` and `private void prefill(` each appear zero times.
    - `AddChildWindowComposerDialog.java` still contains `setOKActionEnabled(p.valid)` exactly once and each of its nine `p.*Error` reads exactly once.
    - `AddChildWindowComposerDialog.java` still contains `ComposerNotices.requestFailed("addChildWindow"` exactly once and `preselect(0x00010000L)` exactly once.
    - `ComposerFieldValidationSourceGuardTest` and `ComposerApplyGuardSourceGuardTest` both pass.
  </acceptance_criteria>
  <done>Both addWindow-family dialogs are thin subclasses over one base, with their own fields, schematics and preview contracts intact.</done>
</task>

<task type="auto">
  <name>Task 3: New base guard and re-pointed refresh guard</name>
  <files>
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBaseSourceGuardTest.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerFieldValidationSourceGuardTest.java
  </files>
  <read_first>
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java (all 375 lines — every per-file count listed in this plan's interface context),
    bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmTokenTrustWindowSourceGuardTest.java lines 36-116 (the private helpers, the one-pin-per-extracted-body shape and the per-subclass delegation pin),
    bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/OffEdtDispatchSourceGuardTest.java lines 110-123 (the abstract-declaration edge case, for the base's `refresh()` hook)
  </read_first>
  <action>
Create
`bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/AddWindowFamilyComposerDialogBaseSourceGuardTest.java`,
a plain JUnit 5 guard with its OWN private copies of `countOccurrences`, `extractMethodBody`,
`withoutCommentLines`, `readSource` and `UncheckedIOExceptionForTest` — never a shared test helper.
Apply `withoutCommentLines` before every count-based assertion. Assert against the base:
`new PreviewDebouncer(`, `new AlarmScheduler(getDisposable())`, `ComposerFlow.once(` and
`PREVIEW_DEBOUNCE_MS = 300L` each exactly once; `new Alarm(` exactly zero times; `this::refresh`
exactly once; `@NotNull Project project` at least once and `@Nullable Project project` exactly zero
times; inside the extracted body of `scheduleRefresh(`, exactly one `setOKActionEnabled(false)`,
exactly one `seq.incrementAndGet()` and exactly one `previewDebouncer.trigger()`, with the sequence
increment positioned before the trigger. Using the abstract-declaration shape, assert the line
declaring `protected abstract void refresh(` ends in `;` and carries no body. Add a delegation pin
per subclass: each of the two subclass files contains `extends AddWindowFamilyComposerDialogBase`
exactly once, `flow.observe(` exactly once, and `mySeq == seq.get()` exactly twice.

Re-point `ComposerDialogRefreshSourceGuardTest` for the two addWindow-family paths only, keeping
every other dialog's assertions at full breadth:
- Introduce a constant for the base source path and assert the debounce-seam literals
  (`PREVIEW_DEBOUNCE_MS = 300L`, `new AlarmScheduler(getDisposable())`, `new PreviewDebouncer(`,
  `this::refresh`, `ComposerFlow.once(`) exactly once on the base for these two dialogs, rather than
  once per subclass file.
- `eachDialogDisablesOkBeforeItsFirstPreviewRoundTripAndOnAnyLaterFailure()` — for the two
  subclasses, the three disables now split across base and subclass. Assert the total across the
  subclass file plus the base equals three, and keep the ordering assertion that the subclass's own
  up-front disable precedes its first literal `refresh();` call.
- `debouncedDialogsRouteEveryListenerThroughTheOkDisablingScheduleHelperRatherThanTriggeringTheDebouncerDirectly()`
  — for the two subclasses, assert `previewDebouncer.trigger()` appears exactly once on the base and
  zero times in each subclass, and that `scheduleRefresh()` is declared on the base.
- `eachDialogChecksItsSequenceOnBothTheSuccessAndTheFailurePath()` — keep `mySeq == seq.get()` == 2
  per subclass; move the `seq.incrementAndGet()` expectation for these two so the base's one and the
  subclass's one are counted separately.
- Keep `noDialogObservesOnlyTheSuccessSideOfItsPreviewRequestAnyMore()`, `new Alarm(` == 0,
  `-> refresh()` == 0 and `noPlatformTestFrameworkCreptIn()` sweeping every file at full breadth,
  extended to include the base file. Negative and zero assertions keep their full breadth.

In `ComposerFieldValidationSourceGuardTest`, add the base to the files swept by the forbidden
validation-message assertions, so a validation string cannot hide on the base. Leave the
`setOKActionEnabled(p.valid)` and per-field error assertions pointed at the two subclasses, because
`apply(...)` stays there.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `AddWindowFamilyComposerDialogBaseSourceGuardTest.java` declares its own `private static int countOccurrences(` and `private static String extractMethodBody(`, imports no shared test utility, and contains zero `BasePlatformTestCase`.
    - It asserts the `protected abstract void refresh(` declaration line ends in `;`.
    - It contains a delegation pin for each of the two subclass files.
    - `ComposerDialogRefreshSourceGuardTest` still sweeps all six dialogs for `new Alarm(` == 0 and `-> refresh()` == 0, and now includes the base file in those sweeps.
    - `ComposerFieldValidationSourceGuardTest` sweeps the base for the four forbidden validation-message fragments.
    - `./gradlew test` exits 0 across the whole IntelliJ suite.
  </acceptance_criteria>
  <done>The shared base is pinned at least as strongly as the two files it replaced, and the whole IntelliJ suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server preview response → dialog `apply` | a server verdict decides whether OK is clickable |
| dialog OK → `ComposerLauncher` write path | an accepted selection becomes a write into live source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-16 | Tampering | OK-gating discipline across the base/subclass split | high | mitigate | The three OK disables and the sequence-counter checks are pinned across base and subclass with explicit totals, so consolidation cannot drop a disable and let an un-previewed selection be written |
| T-93-17 | Tampering | subclass `apply` validation gate | high | mitigate | `setOKActionEnabled(p.valid)` and every per-field error read stay in the subclass and stay pinned by `ComposerFieldValidationSourceGuardTest`, which is also widened to sweep the base for client-authored validation strings |
| T-93-18 | Denial of Service (local) | debounce seam | medium | mitigate | `new Alarm(` stays forbidden outside `AlarmScheduler` and the single 300ms `PreviewDebouncer` is pinned on the base, so the base cannot introduce a second, unbounded refresh path |
| T-93-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan adds zero packages to `bbj-intellij/build.gradle.kts`; no install task exists, so the package-legitimacy gate has nothing to clear |
</threat_model>

<artifacts_this_phase_produces>
Phase 93 creates these symbols across all eight plans. This plan (93-06) produces the addWindow-family
base; the rest are listed so no plan re-creates or renames another's artifact.

**New shared classes (4):** `composer/ComposerSwingHelpers.java` (93-01),
`composer/AddWindowFamilyComposerDialogBase.java` (this plan),
`composer/ComposerIntentionBase.java` (93-04), `actions/BbjComposeActionBase.java` (93-07).

**New plain-Java seams (2):** `composer/ComposerEditRanges.java` (93-03),
`composer/ComposerCatalogsCheck.java` (93-05).

**Thin subclasses (11, existing files, names unchanged):**
`composer/Configure{Msgbox,AddWindow,AddChildWindow,SetoptsInCode,Cvs}Intention.java` (93-04);
`actions/BbjCompose{Msgbox,AddWindow,AddChildWindow,Setopts,SetoptsInCode,Cvs}Action.java` (93-07).
`AddWindowComposerDialog` and `AddChildWindowComposerDialog` become thin subclasses in this plan.

**New enum constant:** `ComposerNotices.Reason.MALFORMED_EDIT` + `malformedEdit(String)` (93-03).

**New DTO fields:** `valid` and `rawTailError` on `SetOptsPreview` (TypeScript) and
`ComposerModels.SetoptsPreview` (Java); `valid` on the composeTriState result on both hosts (93-02).

**New source-guard test classes (6):** `ComposerSwingHelpersSourceGuardTest` (93-01),
`ComposerEditRangesTest` + `ComposerLauncherRangeGuardSourceGuardTest` (93-03),
`ComposerIntentionBaseSourceGuardTest` (93-04), `ComposerCatalogsCheckTest` +
`ComposerCatalogsShapeSourceGuardTest` (93-05),
`AddWindowFamilyComposerDialogBaseSourceGuardTest` (this plan),
`BbjComposeActionBaseSourceGuardTest` (93-07).
</artifacts_this_phase_produces>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test` exits 0.
- `ComposerFieldValidationSourceGuardTest`, `ComposerApplyGuardSourceGuardTest` and
  `ComposerSwingHelpersSourceGuardTest` all pass — proof the split preserved the validation gate, the
  write-path invariants and plan 93-01's consolidation.
- At UAT, open both the addWindow and addChildWindow composers in create and edit mode and confirm
  identical dialogs, identical default selections and identical generated statements.
</verification>

<success_criteria>
The addWindow and addChildWindow dialogs share one base carrying the preview seam, the debounce
wiring, the grouped-checkbox rendering and the OK-gating discipline exactly once; each subclass keeps
only its own fields, schematic, preview request and `apply`; both dialogs behave identically to
today; the whole IntelliJ JUnit suite is green.
</success_criteria>

<output>
Create `.planning/phases/93-composer-robustness-consolidation/93-06-SUMMARY.md` when done. Record how
the `addGroupedChecks` tooltip difference was reconciled — whether the addWindow catalog proved to
carry no `detail` values, or a hook preserved the difference — so a later reader can see the choice
was deliberate rather than accidental.
</output>
</content>
