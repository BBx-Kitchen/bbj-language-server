---
phase: 95-java-interop-status-accuracy-widget-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["95-02"]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/EffectiveInteropPortSourceGuardTest.java
autonomous: true
requirements: [IOP-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The UI placeholder, the persisted default and the changed-from-default check all read one named constant, BbjInteropPortDetector.DEFAULT_PORT, so they cannot drift apart (D-11)."
    - "The numeric value stays 5008 — InteropPortSettings.migratedAutoDetect keys its one-time upgrade inference on savedPort == DEFAULT_PORT, and IntelliJ's serializer omits a field equal to its Java default, so changing the value would silently break both (D-11)."
    - "A source guard pins the single surviving occurrence after comment-stripping, so a later hand-edit cannot quietly reintroduce the drift (D-12)."
    - "Comment-stripping is load-bearing, not cosmetic: prose mentions in BbjInteropPortDetector, InteropPortSettings and BbjSettingsComponent would otherwise trip the count (D-12)."
  assumptions:
    - "IOP-04 (#594) — the spec-less probe fallback classified this requirement as unclassified/unresolved. FLAGGED: no edge category was derived; coverage comes from CONTEXT.md D-11/D-12 and RESEARCH.md Pitfall 3 instead."
    - "#594's own evidence is stale. Its cited lines (BbjSettings.java:30,107,111,116,150) no longer correspond to the file, and its claim of three files carrying the literal is wrong — BbjSettingsConfigurable carries none, routing entirely through InteropPortSettings."
  prohibitions:
    - "MUST NOT introduce a second named port constant such as one on BbjSettings, despite #594's literal wording — a second constant would recreate the exact drift the issue complains about."
    - "MUST NOT change the numeric value away from 5008 while re-pointing; the upgrade-inference and serializer behaviours both key on it."
  artifacts:
    - path: "bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/EffectiveInteropPortSourceGuardTest.java"
      provides: "Existing guard, extended with the single-occurrence assertion"
      contains: "DEFAULT_PORT"
  key_links:
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjInteropPortDetector.java"
      via: "placeholder and both empty/invalid fallbacks read the shared constant"
      pattern: "BbjInteropPortDetector\\.DEFAULT_PORT"
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjInteropPortDetector.java"
      via: "the persisted State field default reads the shared constant"
      pattern: "javaInteropPort = BbjInteropPortDetector\\.DEFAULT_PORT"
---

<!-- planner-discipline-allow: 5008 -->

<objective>
Give the java-interop port default exactly one definition. `BbjInteropPortDetector.DEFAULT_PORT` is
already the de facto shared constant with three consumers; re-point the last raw literals at it and
pin the result with a source guard so the drift cannot come back (D-11, D-12).

Purpose: ROADMAP criterion 4. Note two corrections this plan carries deliberately. First, **no
`BbjSettings.DEFAULT_JAVA_INTEROP_PORT` is introduced**, despite #594's literal wording:
`BbjInteropPortDetector.DEFAULT_PORT` is deliberately `com.intellij`-free so the platform-free
classes `InteropPortSettings` and `InteropPortPresentation` can reference it, and moving the
canonical constant onto `BbjSettings` would point those plain-JUnit-tested classes at a
`PersistentStateComponent`. Close #594 as done with this reasoning, following the Phase 93 D-05 and
Phase 94 D-05 precedent, not as partially implemented. Second, #594's evidence is stale: its cited
line numbers no longer match the file and only **two** files still hold a raw literal, not three.

Output: three re-pointed literal sites and one new guard method.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/95-java-interop-status-accuracy-widget-consolidation/95-CONTEXT.md
@.planning/phases/95-java-interop-status-accuracy-widget-consolidation/95-RESEARCH.md
@.planning/phases/95-java-interop-status-accuracy-widget-consolidation/95-PATTERNS.md

<interfaces>
Contracts the executor needs. Do not go exploring for these.

`BbjInteropPortDetector` (BbjInteropPortDetector.java:36), the canonical constant:
  `public static final int DEFAULT_PORT = 5008;`
It lives in package `com.basis.bbj.intellij` — the SAME package as both files being edited, so
neither needs a new import. `BbjSettingsComponent` already references the class at its line 52
(`private int javaInteropAutoDetectPort = BbjInteropPortDetector.DEFAULT_PORT;`) and
`BbjSettings` already references it at its line 93.

`EffectiveInteropPortSourceGuardTest` (test/.../lsp/) already carries the helper pair this plan
reuses rather than re-copying:
  `private static String stripComments(String text)` — strips block and line comments (lines 69-72)
  `private static int countOccurrences(String text, String literal)` (lines 74-82)
  `private static String readSource(Path path)` — fails with a named message when absent
Its `MAIN_SOURCE_ROOT` equivalent does not exist here; `Lsp4ijImportAllowlistTest` shows the
`Paths.get("src", "main", "java").toAbsolutePath()` + `Files.walk` idiom for a tree-wide scan.

The three code sites to re-point, verified this planning pass:
  BbjSettings.java:30          `public int javaInteropPort = 5008;`
  BbjSettingsComponent.java:189 `javaInteropPortField.setText("5008");`
  BbjSettingsComponent.java:461 `return 5008; // Default when empty`
  BbjSettingsComponent.java:466 `return 5008; // Default when invalid`

The prose-only mentions that comment-stripping must remove, all four verified this pass:
  BbjInteropPortDetector.java:28 and :60, InteropPortSettings.java:32, :49 (twice on one line), :53,
  and BbjSettingsComponent.java:195 — a trailing comment on a `return null;` line whose code carries
  no literal. **CONTEXT.md D-12 did not enumerate the `:195` site**; RESEARCH.md found it. A guard
  written against D-12's three-site list would miscount.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the last three raw port literals at the canonical constant</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java</files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjInteropPortDetector.java (lines 25-50 — the canonical constant and its contract)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java (lines 20-40 — the State field block)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java (lines 182-200 and 455-470 — the placeholder and both fallbacks)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/InteropPortSettings.java (lines 25-60 — the upgrade-inference rule that keys on the constant's value)
  </read_first>
  <action>
    Replace the three raw literal sites with references to `BbjInteropPortDetector.DEFAULT_PORT`.
    Both files already live in package `com.basis.bbj.intellij` alongside the detector and already
    reference the class, so no import statement is added anywhere.

    In `BbjSettings.java`, the `State` field becomes
    `public int javaInteropPort = BbjInteropPortDetector.DEFAULT_PORT;`. Reword its trailing comment
    so it names the shared constant rather than repeating the numeral — the comment is currently
    both redundant and a future drift source.

    **The compile-time value must stay 5008.** `DEFAULT_PORT` is a `static final int`, so this
    remains a compile-time constant and the field's Java default is unchanged. That matters twice
    over: `InteropPortSettings.migratedAutoDetect` keys its one-time upgrade inference on
    `savedPort == DEFAULT_PORT`, and IntelliJ's serializer omits a field equal to its Java default.
    A different value would silently break both. Verify by reading `InteropPortSettings` before
    editing, and confirm `InteropPortSettingsTest` and `BbjSettingsLoadStateTest` still pass.

    In `BbjSettingsComponent.java`, the placeholder at line 189 becomes
    `javaInteropPortField.setText(String.valueOf(BbjInteropPortDetector.DEFAULT_PORT));`, and both
    `return` statements in `getJavaInteropPort()` — the empty-text fallback and the
    `NumberFormatException` fallback — return `BbjInteropPortDetector.DEFAULT_PORT`. Keep each
    trailing comment's meaning ("default when empty", "default when invalid") but drop the numeral
    from it, and do the same for the `return null;` comment a few lines below the placeholder, so
    the file carries no stale numeral in prose either.

    Do not introduce a second constant anywhere — no `BbjSettings.DEFAULT_JAVA_INTEROP_PORT`.
    Adding one would recreate the exact drift #594 complains about, and would point the
    platform-free `InteropPortSettings` and `InteropPortPresentation` at a
    `PersistentStateComponent`.

    Shell rules from CLAUDE.md: Read/Glob/Grep over shell `cat`/`find`; absolute complete paths from
    `/home/coder/repos/bbj-language-server/...`, never `.`, `..` or `~`; never chain `cd` with
    `grep`/`find`/`cat`/`sed`/`head`/`tail` — `cd` only in front of `./gradlew`; `git add` with exact
    paths only.

    No `D-`, plan-number, `C-` or `CR-` identifier in any `.java` file. `#594` is fine.
  </action>
  <acceptance_criteria>
    - `BbjSettings.java` declares `javaInteropPort` initialized from `BbjInteropPortDetector.DEFAULT_PORT`, and the comment-stripped source of that file contains no raw port numeral.
    - `BbjSettingsComponent.java`'s comment-stripped source contains no raw port numeral; its placeholder and both `getJavaInteropPort()` fallbacks read the shared constant.
    - No new import statement was added to either file.
    - No second named port constant exists anywhere in `src/main/java`.
    - `InteropPortSettingsTest`, `BbjInteropPortDetectorTest`, `InteropPortPresentationTest` and `BbjSettingsLoadStateTest` all still pass, proving the upgrade-inference and persisted-default behaviours are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.InteropPortSettingsTest" --tests "*.BbjInteropPortDetectorTest" --tests "*.InteropPortPresentationTest" --tests "*.BbjSettingsLoadStateTest" --tests "*.EffectiveInteropPortSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "no tests found for given includes" in the output</fails_when>
  </verify>
  <done>The UI placeholder, the persisted default and both parse fallbacks all read one named constant, with the numeric value and every dependent behaviour unchanged; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the single surviving occurrence so the drift cannot return</name>
  <files>bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/EffectiveInteropPortSourceGuardTest.java</files>
  <read_first>
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/EffectiveInteropPortSourceGuardTest.java (whole file — the guard being extended and the helpers being reused)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijImportAllowlistTest.java (lines 145-170 — the `Files.walk` tree-scan idiom to copy)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjInteropPortDetector.java (line 36 — the one occurrence that must survive)
  </read_first>
  <action>
    Extend `EffectiveInteropPortSourceGuardTest` with a new test method rather than creating a new
    guard file: its private `stripComments` helper is already the one D-12 names as the copy target,
    and reusing it in place avoids a fourth copy of the same regex.

    Add a method that walks `Paths.get("src", "main", "java").toAbsolutePath()` with `Files.walk`,
    filters to `.java` files, reads each, applies the existing `stripComments`, and sums
    `countOccurrences(stripped, "5008")` across the tree. Assert the total is exactly **1**, and
    assert separately that the single occurrence lies in `BbjInteropPortDetector.java` — build a
    map of file path to per-file count and assert it has exactly one non-zero entry, whose key ends
    with `BbjInteropPortDetector.java`. Include the full per-file map in the assertion message so a
    future failure names the offending file instead of just a number.

    Add a second, smaller assertion in the same method as an anti-vacuity control: the stripped
    source of `BbjInteropPortDetector.java` alone must contain exactly 1 occurrence. Without this,
    a comment-stripping regex that over-matches and swallows the real
    `public static final int DEFAULT_PORT = 5008;` line would make the tree-wide assertion pass
    vacuously at zero-plus-nothing — RESEARCH.md flags exactly that failure mode. The control
    assertion makes the guard fail loudly instead.

    Comment-stripping is required, not optional, and is the reason this guard can assert a count of
    one at all: `BbjInteropPortDetector` mentions the port in prose at two places,
    `InteropPortSettings` at three, and `BbjSettingsComponent` at one — the trailing comment on a
    `return null;` line that CONTEXT.md's D-12 enumeration missed and RESEARCH.md found. Before
    asserting, confirm by reading that the stripped count for `BbjSettingsComponent.java` is zero;
    if it is not, a prose site was missed and Task 1 is incomplete.

    Then run the phase regression gate with `--rerun-tasks` — a plain `./gradlew test` can report
    `Task :test UP-TO-DATE` and mask a stale green against this plan's edits.

    Before committing, grep this plan's source diff for planning identifiers in any `.java` file and
    strip any that survived. Same shell rules as Task 1.
  </action>
  <acceptance_criteria>
    - `EffectiveInteropPortSourceGuardTest` gains a method asserting the tree-wide comment-stripped count of the port numeral across `src/main/java` is exactly 1.
    - That method also asserts the sole occurrence's file path ends with `BbjInteropPortDetector.java`.
    - The anti-vacuity control asserts `BbjInteropPortDetector.java`'s own stripped count is exactly 1, so an over-matching strip regex fails the guard rather than passing it.
    - The assertion message carries the per-file count map.
    - The guard fails if a raw port numeral is reintroduced into any `src/main/java` file outside the detector — verify this by making a throwaway edit, observing red, and restoring the file byte-identically before the final run.
    - The whole IntelliJ JUnit suite is green under `--rerun-tasks`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.EffectiveInteropPortSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "no tests found for given includes" in the output</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks</automated>
    <fails_when>non-zero exit, or a line matching "Task :test" followed by "FAILED" in the output, or the run reports the test task as UP-TO-DATE instead of executing</fails_when>
  </verify>
  <done>Exactly one definition of the default port survives in `src/main/java`, proven by a guard that was observed failing on a deliberate reintroduction and that cannot pass vacuously; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings state → effective port used for every connection | A serialized field default silently governs which port the plugin and language server talk to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-08 | Tampering | the java-interop default port definition | low | mitigate | A hand-edit reintroducing a raw literal would let the UI placeholder, the persisted default and the changed-from-default check drift apart, pointing the plugin at one port while the language server uses another. The extended `EffectiveInteropPortSourceGuardTest` asserts exactly one comment-stripped occurrence tree-wide, with an anti-vacuity control so the guard cannot pass by over-stripping. |
| T-95-11 | Tampering | `InteropPortSettings.migratedAutoDetect` upgrade inference | medium | mitigate | Changing the constant's numeric value while re-pointing would silently break the one-time upgrade inference (which keys on `savedPort == DEFAULT_PORT`) and the serializer's omit-if-equal-to-default behaviour, corrupting existing users' saved settings on upgrade. The value is held at 5008 and the behaviour is pinned by the pre-existing `InteropPortSettingsTest` and `BbjSettingsLoadStateTest`, both required green by Task 1. |
| T-95-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager dependency and no new library. 95-RESEARCH.md's Package Legitimacy Audit records the phase as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` packages. No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

## Artifacts this phase produces

New symbols created by this plan (excluded from drift verification — they do not exist yet):

| Kind | Symbol / path |
|---|---|
| test method | the tree-wide single-occurrence assertion added to `EffectiveInteropPortSourceGuardTest` — `bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/EffectiveInteropPortSourceGuardTest.java` |

This plan creates no new source file and no new class. Every other change is a re-point of an
existing literal to an existing constant.

## Flagged assumptions (spec-less probe fallback)

No SPEC.md exists, so the edge-coverage report was derived from requirement text alone.
**IOP-04 (#594) came back unclassified / unresolved** — no edge category was derived. It is surfaced
here rather than dropped or auto-resolved with a backstop: coverage comes instead from CONTEXT.md
D-11 and D-12 and from RESEARCH.md Pitfall 3, which is materially stronger than the generic probe
shapes would have produced.

A second flagged assumption rides into this plan: **#594's own evidence is stale.** Its cited lines
in `BbjSettings.java` no longer correspond to the file, and its claim that three files carry the
literal is wrong — `BbjSettingsConfigurable` carries none, routing entirely through
`InteropPortSettings`. The plan works from the line numbers re-verified during this planning pass,
not from the issue text.

<verification>
- `./gradlew test --rerun-tasks` green across the whole IntelliJ module.
- The extended guard fails on a deliberate reintroduction and passes on the clean tree.
- `InteropPortSettingsTest` and `BbjSettingsLoadStateTest` prove the upgrade inference and the
  persisted default are unchanged.
</verification>

<success_criteria>
- The UI placeholder, the persisted default and the changed-from-default check read one constant
  (ROADMAP criterion 4).
- #594 is closable as done on the recorded reasoning — one canonical constant, deliberately not the
  one the issue named — rather than as partially implemented.
</success_criteria>

<output>
Create `.planning/phases/95-java-interop-status-accuracy-widget-consolidation/95-03-SUMMARY.md` when done.
Record the falsification evidence for the new guard (what was edited to make it red, and that the
file was restored byte-identically), and the #594 closure reasoning.
</output>
