---
phase: 44-site-chrome
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - documentation/docusaurus.config.ts
  - documentation/sidebars.ts
  - documentation/src/pages/index.tsx
autonomous: true

must_haves:
  truths:
    - "Site title is 'BBj Language Support' (not 'BBj Language Server')"
    - "Tagline is IDE-neutral, focusing on capability not IDE names"
    - "Navbar shows 'VS Code Guide' and 'IntelliJ Guide' as separate items (no 'User Guide' or 'Developer Guide')"
    - "Navbar right side has GitHub, VS Code Marketplace, and JetBrains Marketplace links"
    - "Footer has three columns: VS Code Guide, IntelliJ Guide, Resources"
    - "Footer Resources column includes JetBrains Marketplace alongside VS Code Marketplace"
    - "Landing page hero has three buttons: Get Started, VS Code Marketplace, JetBrains Marketplace"
    - "Landing page feature cards reference both VS Code and IntelliJ explicitly"
    - "Developer Commands card reflects actual v3.x commands without decompile reference"
  artifacts:
    - path: "documentation/docusaurus.config.ts"
      provides: "Site configuration with dual-IDE navbar, footer, title, tagline"
    - path: "documentation/src/pages/index.tsx"
      provides: "Landing page with three hero buttons and updated feature cards"
    - path: "documentation/sidebars.ts"
      provides: "Sidebar configuration for new directory structure"
  key_links:
    - from: "documentation/docusaurus.config.ts navbar items"
      to: "documentation/docs/vscode/"
      via: "to: '/docs/vscode' link"
      pattern: "/docs/vscode"
    - from: "documentation/docusaurus.config.ts navbar items"
      to: "documentation/docs/intellij/"
      via: "to: '/docs/intellij' link"
      pattern: "/docs/intellij"
    - from: "documentation/src/pages/index.tsx hero"
      to: "VS Code getting started page"
      via: "Get Started button linking to /docs/vscode/getting-started"
      pattern: "/docs/vscode/getting-started"
    - from: "documentation/docusaurus.config.ts footer"
      to: "JetBrains Marketplace"
      via: "href link to plugins.jetbrains.com"
      pattern: "plugins.jetbrains.com"
---

<objective>
Update site configuration (title, tagline, navbar, footer), sidebar, and landing page to present both VS Code and IntelliJ as equal first-class citizens with correct links to the new URL structure.

Purpose: Complete the site chrome transformation so the documentation site reflects dual-IDE support in all navigation and branding elements.
Output: Updated docusaurus.config.ts, sidebars.ts, and index.tsx with dual-IDE presentation.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-site-chrome/44-CONTEXT.md
@.planning/phases/44-site-chrome/44-01-SUMMARY.md

@documentation/docusaurus.config.ts
@documentation/sidebars.ts
@documentation/src/pages/index.tsx
@documentation/src/pages/index.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site configuration — title, tagline, navbar, and footer</name>
  <files>documentation/docusaurus.config.ts</files>
  <action>
    Update `documentation/docusaurus.config.ts` with the following changes:

    **Title & Tagline:**
    - `title`: Change from `'BBj Language Server'` to `'BBj Language Support'`
    - `tagline`: Change to an IDE-neutral tagline like `'Language intelligence for BBj development'` (per user decision — focuses on capability, not IDE names)

    **Navbar:**
    - `navbar.title`: Change to `'BBj Language Support'`
    - Replace the single `User Guide` item with two separate items:
      ```ts
      {
        to: '/docs/vscode',
        position: 'left',
        label: 'VS Code Guide',
      },
      {
        to: '/docs/intellij',
        position: 'left',
        label: 'IntelliJ Guide',
      },
      ```
    - Remove the `Developer Guide` navbar item entirely
    - Keep GitHub link on the right
    - Keep VS Code Marketplace link on the right
    - Add JetBrains Marketplace link on the right:
      ```ts
      {
        href: 'https://plugins.jetbrains.com/plugin/com.basis.bbj',
        label: 'JetBrains Marketplace',
        position: 'right',
      },
      ```

    **Footer:**
    Replace existing footer `links` array with three columns:

    Column 1 — "VS Code Guide":
    - Getting Started → `/docs/vscode/getting-started`
    - Features → `/docs/vscode/features`
    - Configuration → `/docs/vscode/configuration`
    - Commands → `/docs/vscode/commands`

    Column 2 — "IntelliJ Guide":
    - Getting Started → `/docs/intellij/getting-started`
    - Features → `/docs/intellij/features`
    - Configuration → `/docs/intellij/configuration`
    - Commands → `/docs/intellij/commands`

    Column 3 — "Resources":
    - BBj Documentation → `https://documentation.basis.cloud/BASISHelp/WebHelp/bbjoverview/bbjoverview.htm` (external)
    - GitHub → `https://github.com/BBx-Kitchen/bbj-language-server` (external)
    - VS Code Marketplace → `https://marketplace.visualstudio.com/items?itemName=basis-intl.bbj-lang` (external)
    - JetBrains Marketplace → `https://plugins.jetbrains.com/plugin/com.basis.bbj` (external)

    **onBrokenLinks:** Temporarily change from `'throw'` to `'warn'` since the sidebar autogeneration and restructuring may produce transient broken links that will resolve once all plans complete. (The executor should change it back to `'throw'` if the build passes cleanly.)
  </action>
  <verify>
    - `grep "BBj Language Support" documentation/docusaurus.config.ts` finds both title and navbar title
    - `grep "Developer Guide" documentation/docusaurus.config.ts` returns NO matches
    - `grep "JetBrains Marketplace" documentation/docusaurus.config.ts` finds navbar and footer entries
    - `grep "/docs/vscode" documentation/docusaurus.config.ts` finds navbar and footer entries
    - `grep "/docs/intellij" documentation/docusaurus.config.ts` finds navbar and footer entries
  </verify>
  <done>Site title is "BBj Language Support", tagline is IDE-neutral, navbar has VS Code Guide + IntelliJ Guide + GitHub + VS Code Marketplace + JetBrains Marketplace, footer has three columns with both IDE guides and resources including JetBrains Marketplace.</done>
</task>

<task type="auto">
  <name>Task 2: Update landing page hero and feature cards for dual-IDE support</name>
  <files>
    documentation/src/pages/index.tsx
    documentation/sidebars.ts
  </files>
  <action>
    **Landing page (index.tsx):**

    1. Update the `Layout` component's `description` prop to match the new IDE-neutral tagline (e.g., `"Language intelligence for BBj development"` — must match the tagline set in docusaurus.config.ts).

    2. Update the hero section buttons to show three buttons with equal prominence:
       - "Get Started" → links to `/docs/vscode/getting-started` (use `to` prop for internal link, `button--secondary button--lg`)
       - "VS Code Marketplace" → links to `https://marketplace.visualstudio.com/items?itemName=basis-intl.bbj-lang` (use `href` prop for external link, `button--secondary button--lg`)
       - "JetBrains Marketplace" → links to `https://plugins.jetbrains.com/plugin/com.basis.bbj` (use `href` prop for external link, `button--secondary button--lg`)

       All three marketplace buttons must have the same size and styling class for equal visual weight. Remove `margin-left--md` from individual buttons — the `.buttons` CSS class already uses `gap: 1rem` for spacing.

    3. Update the feature cards (FeatureList array):
       - **Intelligent Code Completion**: Change description to mention "in VS Code or IntelliJ" instead of just implying VS Code
       - **Real-time Validation**: Change "The extension highlights issues" to "Highlights issues in VS Code or IntelliJ" or similar dual-IDE phrasing
       - **Java Integration**: Already IDE-neutral, keep as-is
       - **Developer Commands**: Update description to say "Run BBj programs directly from VS Code or IntelliJ as GUI, BUI, or DWC applications. Refresh Java classes and manage your BBj projects with built-in commands." Remove the "decompile" reference. Match actual v3.x commands: run as GUI/BUI/DWC, refresh Java classes.
       - **Code Navigation**: Already IDE-neutral, keep as-is
       - **Syntax Highlighting**: Change "The extension provides" to reference both IDEs explicitly

    **Sidebars (sidebars.ts):**
    The current autogenerated sidebar (`{type: 'autogenerated', dirName: '.'}`) should continue to work with the new directory structure since Docusaurus autogeneration uses `_category_.json` files. Keep as-is — no changes needed unless the build fails.
  </action>
  <verify>
    - `grep "JetBrains Marketplace" documentation/src/pages/index.tsx` finds the hero button
    - `grep "VS Code Marketplace" documentation/src/pages/index.tsx` finds the hero button
    - `grep "/docs/vscode/getting-started" documentation/src/pages/index.tsx` finds the Get Started link
    - `grep "decompile" documentation/src/pages/index.tsx` returns NO matches
    - `grep "VS Code or IntelliJ" documentation/src/pages/index.tsx` finds multiple matches in feature cards
    - Run `cd documentation && npm run build 2>&1 | tail -20` to verify the Docusaurus build succeeds (or at least check for major errors)
  </verify>
  <done>Landing page shows three hero buttons (Get Started, VS Code Marketplace, JetBrains Marketplace) with equal visual weight, feature cards reference both IDEs explicitly, Developer Commands card reflects actual v3.x commands without decompile, and the Docusaurus build passes.</done>
</task>

</tasks>

<verification>
- Site title and navbar title show "BBj Language Support"
- Tagline is IDE-neutral
- Navbar left: VS Code Guide, IntelliJ Guide
- Navbar right: GitHub, VS Code Marketplace, JetBrains Marketplace
- No "Developer Guide" appears anywhere in navbar or footer
- Footer has three columns: VS Code Guide (4 links), IntelliJ Guide (4 links), Resources (4 links)
- Landing page hero: 3 buttons with equal styling
- Feature cards: "VS Code or IntelliJ" referenced where relevant
- Developer Commands: no "decompile", has run GUI/BUI/DWC and refresh Java classes
- `cd documentation && npm run build` succeeds
</verification>

<success_criteria>
Documentation site chrome fully updated for dual-IDE support. Both VS Code and IntelliJ are presented as equal first-class citizens in the site title, navbar, footer, hero section, and feature cards. The site builds successfully with the new configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/44-site-chrome/44-02-SUMMARY.md`
</output>
