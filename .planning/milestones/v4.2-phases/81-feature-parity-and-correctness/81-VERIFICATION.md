---
phase: 81-feature-parity-and-correctness
verified: 2026-09-05T18:15:00Z
status: passed
score: 3/3 roadmap success criteria verified (38/38 plan-level must-have truths checked against source, including 81-07's 7 gap-closure truths for G-81-5)
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 3/3 roadmap success criteria (31/31 plan-level must-have truths)
  gaps_closed:
    - "G-81-5: the compile-error balloon crashed with `java.lang.NoSuchMethodError: 'java.lang.String org.eclipse.lsp4j.Diagnostic.getMessage()'` on any live IDE whose installed LSP4IJ (0.20.0+) bundles lsp4j 1.0.0, whose `Diagnostic.getMessage()` returns `Either<String, MarkupContent>` instead of the `String` the plugin was compiled against (LSP4IJ 0.19.0 / lsp4j 0.21.1). Fixed at the code level in 81-07: `CompileResultPresenter` no longer calls the typed accessor at all — a new package-private `messageTextOf(Object)` resolves `getMessage` by reflection via `Class.getMethod`, and a bounded-depth (`MAX_MESSAGE_UNWRAP_DEPTH = 3`) normaliser (`unwrapMessage`) recognises a plain `String`, the left or right branch of a two-branch value (duck-typed `getLeft`/`getRight`), or a markup-shaped value's own `getValue()` text, falling back to `String.valueOf(value)` for anything else. Every reflective failure (`ReflectiveOperationException`/`RuntimeException`) degrades to `null` -> empty text rather than propagating. Pinned by: an end-to-end wire-to-balloon test (`CompileResultJsonBoundaryTest#aParsedCompileErrorsResponseRendersAsLineColumnMessage`, parses a real envelope through LSP4IJ's own `MessageJsonHandler` and renders it through `CompileResultPresenter.present` to the exact string `3:1 Syntax error: bad code`); six shape/failure-mode tests in `CompileResultPresenterTest` using duck-typed stand-in classes sharing no interface with the real client library; and a `DiagnosticMessageAccessSourceGuardTest` that fails the build if a typed `.getMessage()` call or an import naming `MarkupContent`/`jsonrpc.messages.Either` reappears in the rendering seam. The Gradle-time LSP4IJ pin was also raised from 0.19.0 to 0.21.0 (the current Marketplace release), resolving cleanly with the whole module green, so the real-diagnostic test now additionally exercises the genuine two-branch accessor rather than only a stand-in."
  gaps_remaining:
    - "Live re-confirmation that a syntax-error compile balloon now renders `line:col message` (rather than the `NoSuchMethodError`, and rather than the earlier `MessageIssueException`) in a real IDE with a plugin rebuilt and installed from this branch, and that a repeat compile also shows the balloon (the 'subsequent compiles stayed silent' half of G-81-5's symptom). The code-level fix is proven by tests spanning the JSON-RPC boundary and the render step, but the on-screen balloon after a plugin rebuild has not yet been re-exercised against a live IDE's actual installed LSP4IJ. This is `81-UAT.md` test 6, currently `[pending]`. See Human Verification."
  regressions: []
---

# Phase 81: Feature Parity and Correctness Verification Report

**Phase Goal:** IntelliJ's Compile action, bracket matching, and REM toggle match VS Code's behavior and BBj's actual syntax rules.
**Verified:** 2026-09-05
**Status:** human_needed
**Re-verification:** Yes — after gap closure 81-07 (closes UAT gap G-81-5)

## Context

This is a re-verification following the previous `81-VERIFICATION.md` (status `human_needed`, verified 2026-09-05T16:30:00Z), which itself followed gap-closure plan 81-06 (closing G-81-4, the JSON-RPC position overflow). That verification left one item open: a live re-check of whether the syntax-error compile balloon renders correctly. The user ran that live re-check (`81-UAT.md` test 5) and it surfaced a **new** blocker, G-81-5: once the payload could deserialize (81-06's fix), the plugin's rendering code ran for the first time in a live IDE and crashed with `NoSuchMethodError` on `Diagnostic.getMessage()`, because the live IDE's installed LSP4IJ (0.21.0, per the user's own report) bundles a newer lsp4j (1.0.0) whose `getMessage()` returns a two-branch value instead of the `String` the plugin was compiled against (LSP4IJ 0.19.0 / lsp4j 0.21.1). Gap-closure plan 81-07 (gap_closure: true, wave 4, depends on 81-05 and 81-06) has been executed and its SUMMARY reviewed, but per this verifier's adversarial mandate, none of that SUMMARY prose is treated as evidence. Every claim below was checked directly against source at HEAD `f17c92ea`.

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Invoking "Compile BBj File" in IntelliJ sends a `bbj/compile` request to the shared language server, which runs bbjcpl through the existing `BBjCPLService`, and the action displays success or the returned diagnostics — with no bbjcpl invocation logic duplicated on the IntelliJ side (#571) | ✓ VERIFIED (code + tests); one live re-check pending → human verification | Full chain unchanged and re-confirmed from prior passes. **New this pass:** the defect UAT found on this path after 81-06's fix — the rendering seam's typed `Diagnostic.getMessage()` call resolving to an incompatible descriptor on a newer LSP4IJ/lsp4j — is now fixed at the code level. Read `CompileResultPresenter.java` in full: `renderOne` calls `messageTextOf(diagnostic)` (line 162), not `diagnostic.getMessage()`. `messageTextOf` (line 180) calls `unwrapMessage(invokeNoArg(diagnostic, "getMessage"), 0)`. `invokeNoArg` (line 221) resolves `target.getClass().getMethod(methodName)` reflectively, calls `setAccessible(true)` when needed, and catches `ReflectiveOperationException \| RuntimeException`, returning `null` on any failure. `unwrapMessage` (line 193) normalises a `String`, a two-branch value's `getLeft`/`getRight`, or a markup value's `getValue`, bounded by `MAX_MESSAGE_UNWRAP_DEPTH = 3`. Verified directly: `grep -v` comment-filtered `.getMessage()` count in the file = 0; comment-filtered `MarkupContent` count = 0; comment-filtered `jsonrpc.messages.Either` count = 0. `CompileResultJsonBoundaryTest.java` read in full: 5 `@Test` methods, including the new `aParsedCompileErrorsResponseRendersAsLineColumnMessage`, which parses a real `compile-errors` envelope through LSP4IJ's own `MessageJsonHandler` and feeds the result straight into `CompileResultPresenter.present(...)` — asserting the exact body `3:1 Syntax error: bad code`. `CompileResultPresenterTest.java` read in full: 18 `@Test` methods (12 pre-existing + 6 new: `aStringShapedMessageValueIsUsedAsIs`, `aTwoWayMessageValueIsReadFromItsLeftBranch`, `aTwoWayMessageValueIsReadFromItsMarkupRightBranch`, `anUnreadableMessageValueYieldsEmptyTextInsteadOfThrowing`, `aRealDiagnosticFromThisClasspathStillYieldsItsMessage`, `aDiagnosticWithNoMessageStillRendersItsLocation`), using duck-typed stand-in classes (`StringMessageStandIn`, `ObjectMessageStandIn`, `EitherStandIn`, `MarkupStandIn`) sharing no interface with the real lsp4j types. `DiagnosticMessageAccessSourceGuardTest.java` read in full: 3 `@Test` methods asserting zero typed `.getMessage()` calls, presence of `getMethod`/`"getMessage"`/`ReflectiveOperationException`, and zero occurrences of `MarkupContent`/`jsonrpc.messages.Either` in comment-filtered source. `build.gradle.kts` line 34 reads `plugin("com.redhat.devtools.lsp4ij:0.21.0")` with an explanatory comment on lines 30-33 recording that the plugin descriptor cannot pin the dependency's runtime version, so the build/test pin tracks the Marketplace's current release. Orchestrator-supplied gate facts (not re-run by this verifier per shell rules): `./gradlew test` just ran green at 326 tests, 0 failures, 0 errors; `./gradlew buildPlugin` produced `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip` (confirmed present on disk, 1,010,885 bytes, timestamped 17:46). `git diff --stat 1e52ced5 HEAD -- bbj-vscode` is empty — no TypeScript changed since the pre-81-07 baseline. `git diff --stat 1e52ced5 HEAD -- bbj-intellij/src/main` names only `CompileResultPresenter.java` (83 lines changed) — no other Java main source touched. The one still-open item is the live re-check that the balloon now actually renders correctly against the IDE's real installed LSP4IJ — see Human Verification (`81-UAT.md` test 6). |
| 2 | Typing `PRINT "value (not a bracket)"` or a doubled-quote string in a BBj file does not trigger bracket matching, navigation, or auto-close on the parenthesis inside the string literal, verified by regression tests for both cases (#568) | ✓ VERIFIED (code + tests + live UAT) | Unchanged from all prior verification passes; not touched by 81-07. `BbjStringCommentScannerTest` and the bracket-matcher exclusion logic are untouched (confirmed via `git diff --stat 1e52ced5 HEAD` — no files under `bracket`/`lexer`/`scanner` paths listed). `81-UAT.md` test 1: `result: pass`. No further human verification needed for this criterion. |
| 3 | Toggling the line-comment shortcut on a line already prefixed with `rem`, `Rem`, or `REM` (word-bounded) removes the prefix instead of adding a second one, verified by regression tests for lowercase and mixed case (#540) | ✓ VERIFIED (code + tests + live UAT) | Unchanged from all prior verification passes; not touched by 81-07. `RemToggleSeamTest`/`BbjCommenterSelfManagingSourceGuardTest` untouched (not in the 81-07 diff). `81-UAT.md` test 2: `result: pass`. No further human verification needed for this criterion. |

**Score:** 3/3 roadmap success criteria verified at the code/test level. Criteria 2 and 3 carry live human confirmation (`81-UAT.md` tests 1 and 2, both `pass`) and need no further verification. Criterion 1's second underlying protocol/runtime defect (G-81-5, the cross-lsp4j-generation `NoSuchMethodError`) is now closed at the code level, with fresh tests proving the tolerant read on both a duck-typed stand-in classpath and, via the raised 0.21.0 Gradle pin, the genuine two-branch client library. The specific live re-check — does the balloon actually render correctly on the IDE's real installed LSP4IJ, with the plugin rebuilt from this branch — has not yet been re-exercised and remains a human-verification item (`81-UAT.md` test 6, `[pending]`).

### Plan-Level Must-Haves

All must-have truths across all seven plans (81-01 through 81-07) were checked directly against source. 81-01 through 81-06's 31 must-haves were re-confirmed present via an existence/regression sanity check (unchanged since the prior verification pass — no regressions found; one pre-existing path nit noted below, not a code issue). 81-07's 7 new gap-closure truths were fully verified in this pass:

| # | Must-have (81-07) | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Invoking "Compile BBj File" on a file with a syntax error shows the error balloon listing `line:col message` on an IDE whose LSP4IJ bundles *either* client-library generation | ✓ VERIFIED (code + tests); live → human | See roadmap truth #1 above. `messageTextOf`/`unwrapMessage`/`invokeNoArg` handle both a plain `String` and a two-branch value uniformly; `aRealDiagnosticFromThisClasspathStillYieldsItsMessage` plus the raised 0.21.0 pin means both generations are now exercised by tests, one via a stand-in and one via the real client library. |
| 2 | The rendering seam reads a diagnostic's message text without binding to one client-library method descriptor: resolves the accessor on the runtime class and normalises a plain string, a two-branch value, and a markup-shaped value to the same displayed text | ✓ VERIFIED | `unwrapMessage` (lines 193-213) has explicit branches for `String`, `getLeft`/`getRight` (two-branch), and `getValue` (markup), in that order, each recursing at `depth + 1`, bounded by `MAX_MESSAGE_UNWRAP_DEPTH = 3`. |
| 3 | A diagnostic whose message cannot be read at all renders its location instead of throwing | ✓ VERIFIED | `renderOne` returns `text` alone when range is null, and `location` alone when `text.isEmpty()`; `invokeNoArg` catches `ReflectiveOperationException \| RuntimeException` and returns `null`, which `unwrapMessage` turns into `""`. Confirmed by `anUnreadableMessageValueYieldsEmptyTextInsteadOfThrowing` (3 sub-cases: no accessor, throwing accessor, null-returning accessor) and `aDiagnosticWithNoMessageStillRendersItsLocation` (asserts exact body `"3:5"`, no trailing separator). |
| 4 | A `compile-errors` response envelope parsed through LSP4IJ's own `MessageJsonHandler` renders end to end into balloon body text | ✓ VERIFIED | `CompileResultJsonBoundaryTest#aParsedCompileErrorsResponseRendersAsLineColumnMessage`: parses via `MessageJsonHandler`, feeds `result.success/reason/message/diagnostics` straight into `CompileResultPresenter.present(...)` exactly as `BbjCompileAction` does, asserts body `"3:1 Syntax error: bad code"`. |
| 5 | A source guard fails the build if a typed message accessor, or an import of a version-specific message type, reappears in the rendering seam | ✓ VERIFIED | `DiagnosticMessageAccessSourceGuardTest`'s 3 tests: `theRenderingSeamCallsNoTypedMessageAccessor` (0 occurrences of `.getMessage()` in comment-filtered source), `theMessageIsReadThroughAReflectiveLookupWithAFallback` (asserts presence of `getMethod`, `"getMessage"`, `ReflectiveOperationException`), `theRenderingSeamBindsToNoClientLibraryMessageType` (0 occurrences of `MarkupContent` and `jsonrpc.messages.Either`). |
| 6 | The Gradle-time LSP4IJ pin is a deliberately decided, recorded outcome | ✓ VERIFIED | `build.gradle.kts` line 34: `plugin("com.redhat.devtools.lsp4ij:0.21.0")`, raised from 0.19.0, with a 4-line explanatory comment (lines 30-33) naming why the version is stated deliberately and citing #571. SUMMARY documents the decision and its rationale (coordinate resolved cleanly, whole module green at 326 tests). |
| 7 | `./gradlew test` and `./gradlew buildPlugin` are both green, and no plugin behaviour outside the compile-diagnostic rendering path changes | ✓ VERIFIED (orchestrator-supplied gate facts, cited not re-run) | Orchestrator gate facts: `./gradlew test` just ran green at 326 tests, 0 failures, 0 errors (81-06's baseline of 316 + 10 new: 1 boundary + 6 presenter + 3 guard); `./gradlew buildPlugin` produced the archive, confirmed present on disk. `git diff --stat 1e52ced5 HEAD -- bbj-intellij/src/main` names only `CompileResultPresenter.java` — no other plugin behavior's source changed. |

**81-07 prohibitions (all directly re-checked against source/git):**

| Prohibition | Verification tier | Status | Evidence |
|---|---|---|---|
| MUST NOT change the `bbj/compile` method name, params/result field names, or the nine `reason` values, or `CompileModels`' field types | judgment | ✓ kept | `CompileModels.java` and `BbjComposerServer.java` not in the 81-07 diff (`git diff --stat 1e52ced5 HEAD` lists only 5 files, none of which are these); `present(...)`'s `switch (reason)` block (lines 60-111) is byte-identical to the prior verification's transcription — all nine cases present, no new/removed case. |
| MUST NOT change any TypeScript file | automated | ✓ kept | `git diff --stat 1e52ced5 HEAD -- bbj-vscode` produces no output. |
| MUST NOT import a version-specific message type into the rendering seam; MUST NOT add a new runtime dependency | automated | ✓ kept | Comment-filtered `MarkupContent`/`jsonrpc.messages.Either` counts are both 0 in `CompileResultPresenter.java`. `import` block (lines 1-7) is unchanged: `org.eclipse.lsp4j.Diagnostic`, `org.eclipse.lsp4j.Range`, `java.lang.reflect.Method`, `java.util.List` — no new dependency. |
| MUST NOT let version tolerance leak into `BbjCompileAction`, `CompileModels` or `BbjComposerServer` | judgment | ✓ kept | None of those three files appear in `git diff --stat 1e52ced5 HEAD` — the presenter is the only touched main-source file. |
| MUST NOT keep a Gradle pin bump that requires editing any main source other than `CompileResultPresenter.java` | judgment | ✓ kept | `git diff --stat 1e52ced5 HEAD -- bbj-intellij/src/main` names exactly one file: `CompileResultPresenter.java`. The pin was kept at 0.21.0 (not reverted) because it resolved cleanly with the whole module green — the hard revert rule never triggered, consistent with the SUMMARY's own account. |
| MUST NOT write a planning identifier into any source or test comment | automated | ✓ kept | `git diff --unified=0 1e52ced5 HEAD -- bbj-intellij/src bbj-intellij/build.gradle.kts`, grepped for `D-0`/`G-81`/`SEC-`/`81-07` in added (`+`) lines — zero matches. Comments reference only `#571`, matching the rest of the phase. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bbj-intellij/src/main/java/com/basis/bbj/intellij/compile/CompileResultPresenter.java` | Shape-tolerant, reflective message read | ✓ VERIFIED | Read in full (235 lines); `messageTextOf`, `unwrapMessage`, `invokeNoArg` all present as documented; `contains: "getMethod"` requirement satisfied (line 226). |
| `bbj-intellij/src/test/java/com/basis/bbj/intellij/compile/CompileResultPresenterTest.java` | Both message shapes + unreadable fallback pinned | ✓ VERIFIED | 18 `@Test` methods (confirmed by `grep -c '@Test'`); `contains: "getLeft"` satisfied (line 240); no `import org.eclipse.lsp4j.MarkupContent`. |
| `bbj-intellij/src/test/java/com/basis/bbj/intellij/compile/DiagnosticMessageAccessSourceGuardTest.java` | Guard against typed accessor/version-specific import regressing | ✓ VERIFIED | Exists; 3 `@Test` methods; `contains: "CompileResultPresenter.java"` satisfied (line 25, the guarded path). |
| `bbj-intellij/src/test/java/com/basis/bbj/intellij/compile/CompileResultJsonBoundaryTest.java` | Wire-to-balloon end-to-end proof | ✓ VERIFIED | 5 `@Test` methods (1 pre-existing-count-of-4 plus the new end-to-end test); `contains: "CompileResultPresenter.present"` satisfied (line 137). |
| `bbj-intellij/build.gradle.kts` | Recorded LSP4IJ generation decision | ✓ VERIFIED | Line 34 `plugin("com.redhat.devtools.lsp4ij:0.21.0")` with rationale comment; `contains: "com.redhat.devtools.lsp4ij"` satisfied. |
| All 81-01–81-06 artifacts (compile-command.ts, bbj-cpl-service.ts, lsp-position.ts, bbj-cpl-parser.ts, bbj-document-validator.ts, BbjStringCommentScanner.java, BbjPairedBraceMatcher.java, RemToggleSeam.java, BbjCommenter.java, CompilerInitOptions.java, BbjSettings\*.java, CompileModels.java, BbjComposerServer.java, BbjCompileAction.java, etc.) | Present, unchanged since prior full-content verification | ✓ VERIFIED (existence + no regression) | Spot-checked 8 representative files across both stacks, all present on disk. One path nit carried forward from the prior report: `BbjComposerServer.java` lives at `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/BbjComposerServer.java` (a `composer/` subpackage), not directly under `intellij/` — a documentation nit, not a code issue, and not touched by 81-07 regardless. |

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| `BbjCompileAction.java` | `CompileResultPresenter.java` | the unchanged `present(...)` call site — action not touched by 81-07 | ✓ WIRED (carried forward; `BbjCompileAction.java` absent from the 81-07 diff) |
| `CompileResultPresenter.java` | the client library loaded in LSP4IJ's own plugin classloader at run time | a reflective `getMethod` lookup instead of a compile-time descriptor | ✓ WIRED |
| `CompileResultJsonBoundaryTest.java` | `CompileResultPresenter.java` | parses a real envelope via `parse(...)`, then renders via `CompileResultPresenter.present(...)` | ✓ WIRED (read in full, confirmed exact call and assertion) |
| `build.gradle.kts` | `CompileResultPresenterTest.java` | the pinned LSP4IJ version decides which client-library generation `aRealDiagnosticFromThisClasspathStillYieldsItsMessage` exercises | ✓ WIRED (pin raised to 0.21.0, whole module green per orchestrator gate facts) |
| All 81-01–81-06 key links | — | Unchanged; not touched by 81-07 | ✓ WIRED (carried forward, no regression found) |

### Behavioral Spot-Checks

Per shell rules for this task, Gradle/vitest were not independently re-run by this verifier; the orchestrator's fresh gate run is cited as evidence instead of a redundant re-run.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 81-07's touched compile-package tests (34 total: 5 boundary + 18 presenter + 3 guard + 8 action guard) | Orchestrator-supplied: `./gradlew test` (superset, whole module) | 326 tests, 0 failures, 0 errors | ✓ PASS (accepted; not independently re-run) |
| Whole IntelliJ module, plugin archive | Orchestrator-supplied: `./gradlew buildPlugin` | Produced `bbj-intellij-0.1.0.zip` | ✓ PASS — confirmed via direct `ls` on `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip` (1,010,885 bytes, timestamp 17:46, this session) |
| No typed `.getMessage()` call in presenter (non-comment) | `grep -v '^[[:space:]]*\(//\|\*\|/\*\)' CompileResultPresenter.java \| grep -Fc '.getMessage()'` | `0` | ✓ PASS |
| No `MarkupContent`/`Either` in presenter (non-comment) | Same pipeline, `grep -Fc 'MarkupContent'` and `grep -Fc 'jsonrpc.messages.Either'` | `0`, `0` | ✓ PASS |
| No TypeScript changed since baseline 1e52ced5 | `git diff --stat 1e52ced5 HEAD -- bbj-vscode` | (empty) | ✓ PASS |
| Only `CompileResultPresenter.java` changed under `bbj-intellij/src/main` since baseline | `git diff --stat 1e52ced5 HEAD -- bbj-intellij/src/main` | 1 file, 80 insertions / 3 deletions | ✓ PASS |
| No planning identifiers added by 81-07 | `git diff --unified=0 1e52ced5 HEAD -- bbj-intellij/src bbj-intellij/build.gradle.kts` grepped for `D-0\|G-81\|SEC-\|81-07` in added lines | No output | ✓ PASS |
| No new debt markers in 81-07's 5 touched files | `grep -n -E "TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER\|TBD"` across all 5 files | No output | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` files exist in this repository and no plan/summary references probe-based verification.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| PARITY-01 | 81-01, 81-04, 81-05, 81-06, 81-07 | `bbj/compile` request, IntelliJ setting, IntelliJ action, position-overflow fix (#571), cross-lsp4j-generation message-read fix (#571) | ✓ SATISFIED (code + tests); one live re-check pending | See roadmap truth #1 above. |
| PARITY-02 | 81-02 | Bracket inertness inside string literals (#568) | ✓ SATISFIED (code + tests + live UAT pass) | See roadmap truth #2 above. |
| PARITY-03 | 81-03 | Case-insensitive REM toggle (#540) | ✓ SATISFIED (code + tests + live UAT pass) | See roadmap truth #3 above. |

No orphaned requirements — `REQUIREMENTS.md`'s traceability table maps exactly PARITY-01/02/03 to Phase 81 (all marked "Complete"), and all seven plans' `requirements:` frontmatter fields claim one of these three IDs (PARITY-01: 81-01, 81-04, 81-05, 81-06, 81-07; PARITY-02: 81-02; PARITY-03: 81-03), matching 1:1. `git log` HEAD confirmed at `f17c92ea`.

### Anti-Patterns Found

None introduced by 81-07. All 5 files it created/modified are free of `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/`TBD`. No planning identifiers leaked into added comment lines (verified directly, not from SUMMARY). No regressions found in the artifacts 81-01–81-06 touched (existence-checked this pass; full-content re-verification was performed in the prior two passes and is not repeated here since git confirms nothing else changed).

### Human Verification Required

One item remains — narrower in scope than the prior verification's item, since it targets the specific new fix (G-81-5) rather than the prior fix (G-81-4), which is now understood to have been necessary but not sufficient on its own:

### 1. Compile-error balloon after the G-81-5 fix

**Test:** Rebuild and install the plugin from this branch (bbj-vscode `npm run build`, then bbj-intellij `./gradlew buildPlugin`; install the zip from disk and confirm the installed BBj build is the local one, not a Marketplace auto-update — the same environmental trap already recorded twice in this phase, G-81-3). Note which LSP4IJ version the IDE actually has installed under Settings > Plugins. With a BBj home and a compile output directory configured, invoke Tools > Compile BBj File on a `.bbj` file containing a syntax error, then a second time on the same file, then on a valid file.
**Expected:** Both syntax-error compiles show an error balloon whose body lists the compiler's errors as `line:col message`, with the same text in the language-server console. No `NoSuchMethodError` and no `MessageIssueException` appears in `idea.log` or the IDE's fatal-error notifier. The valid file still shows `Compiled "<file>"`.
**Why human:** The only place the real runtime LSP4IJ (and whichever lsp4j generation it bundles) is exercised is a live IDE, which this repo's test module deliberately excludes (C-01, Phase 79/80 practice). The fix is proven at the value level by tests covering both message shapes and the full wire-to-balloon path (`CompileResultPresenterTest`'s 18 tests, `CompileResultJsonBoundaryTest`'s 5 tests, `DiagnosticMessageAccessSourceGuardTest`'s 3 tests), and the build/test pin now also exercises the genuine two-branch client library at 0.21.0 — but the on-screen balloon after a plugin rebuild, against whatever LSP4IJ version the live IDE actually resolves, has not yet been re-exercised. This is `81-UAT.md` test 6, currently `[pending]`.

### Gaps Summary

No code-level gaps found. G-81-5 (the UAT-discovered `NoSuchMethodError` crash on a syntax-error compile, once G-81-4's payload fix let the response reach the presenter for the first time in a live IDE) is closed at the code level: `CompileResultPresenter` no longer calls a typed `Diagnostic.getMessage()`; it resolves the accessor reflectively and normalises the result across the three shapes a diagnostic's message can take (plain string, either branch of a two-branch value, markup value), with every reflective failure degrading to the diagnostic's location rather than an unhandled error. This is proven by a wire-to-balloon end-to-end test through LSP4IJ's own `MessageJsonHandler`, six shape/failure-mode tests using duck-typed stand-ins, a real-diagnostic test that (thanks to the Gradle pin being raised to 0.21.0) now exercises the genuine two-branch client library rather than only a stand-in, and a source guard preventing the typed accessor or a version-specific import from reappearing. No Java main source other than `CompileResultPresenter.java` was touched, no TypeScript changed, the `bbj/compile` contract and reason vocabulary are untouched, and no planning identifiers or new debt markers were introduced. All three roadmap success criteria are satisfied at the code/test level; two of the three (bracket matching, REM toggle) additionally carry live human confirmation. The phase cannot be marked `passed` because the specific live re-check of the G-81-5 fix — does the balloon actually render correctly now, in a real IDE with a rebuilt plugin, against whatever LSP4IJ version that IDE resolves — has not yet happened. This is `81-UAT.md` test 6 (`[pending]`), consistent with every prior verification pass in this milestone that defers on-screen/keystroke confirmation to a human running the live IDE.

---

*Verified: 2026-09-05*
*Verifier: Claude (gsd-verifier)*
