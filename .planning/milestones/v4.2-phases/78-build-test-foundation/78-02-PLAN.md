---
phase: 78-build-test-foundation
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - bbj-intellij/gradle/wrapper/gradle-wrapper.properties
  - bbj-intellij/gradle/wrapper/gradle-wrapper.jar
  - bbj-intellij/gradlew
  - bbj-intellij/gradlew.bat
  - .github/dependabot.yml
  - .planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt
autonomous: true
requirements: [BUILD-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`bbj-intellij/gradle/wrapper/gradle-wrapper.properties` declares Gradle 8.14.5 — the latest 8.x — and pins `distributionSha256Sum` to the published `gradle-8.14.5-bin.zip.sha256` value (D-04, D-05)."
    - "The committed `gradle-wrapper.jar` SHA-256 equals the value published at `https://services.gradle.org/distributions/gradle-8.14.5-wrapper.jar.sha256`, fetched at execution time rather than copied from a planning document (D-05)."
    - "All four wrapper artifacts — `gradle-wrapper.jar`, `gradle-wrapper.properties`, `gradlew`, `gradlew.bat` — were regenerated together by the `wrapper` task and committed in one change; none was hand-edited (D-04, #503)."
    - "`node bbj-vscode/tools/check-gradle-wrapper.mjs` reports 0 findings against the bumped wrapper, and the existing `gradle/actions/wrapper-validation@v6` steps in the three Gradle workflows are unchanged — the CI tamper guard is inherited, not duplicated (D-05)."
    - "`cd bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew dependencies` exits 0 on the JDK 25 host and enumerates the full transitive tree, captured in `78-DEPENDENCIES.txt` (D-07, #576)."
    - "`./gradlew test` and `./gradlew buildPlugin` still succeed on 8.14.5 with `intellij-platform-gradle-plugin` 2.11.0, whose declared minimum is 8.13 (D-06)."
    - "`.github/dependabot.yml` watches the `gradle` ecosystem in `/bbj-intellij` on a weekly schedule, so wrapper and dependency drift is surfaced continuously (D-08, #576)."
  artifacts:
    - path: "bbj-intellij/gradle/wrapper/gradle-wrapper.properties"
      provides: "Pinned Gradle 8.14.5 distribution URL and checksum"
      contains: "gradle-8.14.5-bin.zip"
    - path: "bbj-intellij/gradle/wrapper/gradle-wrapper.jar"
      provides: "Wrapper bootstrap JAR matching the declared 8.14.5 release"
    - path: ".github/dependabot.yml"
      provides: "Weekly gradle-ecosystem update surveillance for /bbj-intellij"
      contains: "package-ecosystem: \"gradle\""
    - path: ".planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt"
      provides: "Enumerated transitive dependency tree — the closing evidence for #576"
      contains: "compileClasspath"
  key_links:
    - from: "bbj-intellij/gradle/wrapper/gradle-wrapper.properties"
      to: "services.gradle.org distribution"
      via: "distributionUrl plus distributionSha256Sum pin, with validateDistributionUrl left on"
      pattern: "distributionSha256Sum=6f74b601422d6d6fc4e1f9a1ab6522f642c2fdcbc15ae33ebd30ba3d7198e854"
    - from: "bbj-vscode/tools/check-gradle-wrapper.mjs"
      to: "bbj-intellij/gradle/wrapper/gradle-wrapper.jar"
      via: "GRADLE_CHECKSUMS lookup asserting the JAR matches the declared release"
      pattern: "8\\.14\\.5"
    - from: ".github/dependabot.yml"
      to: "bbj-intellij"
      via: "gradle package-ecosystem stanza pointed at the /bbj-intellij directory"
      pattern: "directory: \"/bbj-intellij\""
  prohibitions:
    - "Wrapper files must never be hand-edited, patched, or downloaded from anywhere other than the `wrapper` task run with an explicit distribution checksum — a checksum written by hand proves nothing about the bytes beside it."
    - "The existing CI wrapper-validation guard must never be weakened, removed, or reordered to sit after a Gradle invocation in order to make a version bump pass; a second, redundant validation step must not be added either."
    - "The wrapper must not be moved off the Gradle 8.x line, and no version catalog or dependency locking may be introduced (REQUIREMENTS.md Out of Scope)."
    - "`distributionSha256Sum` must never be dropped and `validateDistributionUrl` must never be turned off to get a regeneration to complete."
    - "A red result from the 8.14.5 compatibility proof must never be resolved by bumping `intellij-platform-gradle-plugin` past 2.11.0 — D-06 says stop and surface it."
---

<objective>
Put `bbj-intellij` on a current, checksum-pinned, CI-validated Gradle 8.14.5 wrapper whose full transitive dependency tree can be enumerated, and add continuous update surveillance for the module.

Purpose: BUILD-02 / issues #503 and #576. The wrapper is the single most trust-sensitive artifact in the repository — it is executable code fetched over the network and run before any review gate. Regenerating it correctly (task-generated, checksum-pinned, independently verified against the publisher) and keeping the existing CI guard intact is what makes every other `./gradlew` invocation in v4.2 trustworthy.

Output: regenerated wrapper quartet at 8.14.5, a `gradle` Dependabot stanza for `/bbj-intellij`, and `78-DEPENDENCIES.txt` holding the enumerated tree.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-build-test-foundation/78-CONTEXT.md
@.planning/phases/78-build-test-foundation/78-PATTERNS.md
@.planning/phases/78-build-test-foundation/78-01-SUMMARY.md
@bbj-intellij/gradle/wrapper/gradle-wrapper.properties
@.github/dependabot.yml
@bbj-vscode/tools/check-gradle-wrapper.mjs
</context>

<environment_facts>
Verified 2026-09-04 (78-CONTEXT.md "Verified state of `main`"):

- The wrapper currently declares Gradle **8.13** with `distributionSha256Sum=20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78`, and the committed `gradle-wrapper.jar` already matches the published 8.13 wrapper JAR (commit `f7908c0`, 2026-08-21). The research's "JAR from the 8.10-8.12.1 line" claim is stale — this is a version bump, not a mismatch repair.
- `bbj-vscode/tools/check-gradle-wrapper.mjs` already tabulates 8.14.5 in `GRADLE_CHECKSUMS`: wrapper `7d3a4ac4de1c32b59bc6a4eb8ecb8e612ccd0cf1ae1e99f66902da64df296172`, bin `6f74b601422d6d6fc4e1f9a1ab6522f642c2fdcbc15ae33ebd30ba3d7198e854`. No table edit is needed. On the current tree it prints `1 wrapper(s), 7 workflow file(s), 3 Gradle job(s), 0 findings.` and exits 0.
- `gradle/actions/wrapper-validation@v6` already runs before every `./gradlew` step in `pr-validation.yml`, `preview.yml`, and `manual-release.yml`; `workflow-hygiene.yml` already runs the checker script. Both guards are inherited — do not add a third.
- `.github/dependabot.yml` currently holds one `npm` stanza for `/bbj-vscode` with an `ignore:` list for `chevrotain` and `typescript`.
- Gradle's compatibility matrix lists Java 24 as the newest JVM that can *run* Gradle 8.14 and Java 25 as N/A — which is exactly why plan `78-01` must land first: the daemon JVM criteria are what let the JDK 25 client drive an 8.x daemon.
</environment_facts>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — regenerate the wrapper to 8.14.5 and drive a build with it</name>
  <precondition>Network egress to `services.gradle.org` is available — the `wrapper` task and the first invocation of the regenerated wrapper both download `gradle-8.14.5-bin.zip`, and `--offline` does not cover the wrapper's own distribution fetch.</precondition>
  <reversibility rating="reversible">The wrapper quartet is regenerable at any version by re-running the same task; `git revert` restores 8.13 exactly (D-04).</reversibility>
  <read_first>
    - `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` (all 8 current lines, including `networkTimeout` and `validateDistributionUrl` which must survive the regeneration)
    - `.planning/phases/78-build-test-foundation/78-PATTERNS.md` § "`bbj-intellij/gradle/wrapper/*` — wrapper regeneration"
    - `.planning/phases/78-build-test-foundation/78-CONTEXT.md` D-04 and D-05
    - `.planning/phases/78-build-test-foundation/78-01-SUMMARY.md` (confirm the daemon JVM criteria landed — this task needs a daemon that starts on JDK 25)
  </read_first>
  <files>bbj-intellij/gradle/wrapper/gradle-wrapper.properties, bbj-intellij/gradle/wrapper/gradle-wrapper.jar, bbj-intellij/gradlew, bbj-intellij/gradlew.bat</files>
  <action>
    Prove the whole wrapper path end to end in one slice: task-driven regeneration → pinned distribution download → 8.14.5 daemon on JDK 17 criteria → evaluated build script → task list.

    From `bbj-intellij/` with `JAVA_HOME=/opt/java/default`, run the `wrapper` task with `--gradle-version 8.14.5` and `--gradle-distribution-sha256-sum 6f74b601422d6d6fc4e1f9a1ab6522f642c2fdcbc15ae33ebd30ba3d7198e854`. Run it **twice**, passing both options on each run: the first run rewrites `gradle-wrapper.properties`, the second executes under 8.14.5 itself and regenerates `gradle-wrapper.jar`, `gradlew`, and `gradlew.bat` from that release. Passing the checksum option on the second run too is what keeps the pin from being dropped.

    Never hand-edit any of the four files, and never fetch a wrapper JAR from a browser, a mirror, or another repository (D-04). If the `wrapper` task refuses to run, fix the cause rather than writing the properties by hand.

    Confirm the regeneration preserved `networkTimeout=10000` and `validateDistributionUrl=true`; if the task dropped either, re-add it — but note in the SUMMARY that the task dropped it. Confirm `distributionUrl` names `gradle-8.14.5-bin.zip` on the `services.gradle.org` host, and that the release is on the 8.x line (Gradle 9.x is out of scope per REQUIREMENTS.md).

    Do not touch `.github/workflows/` and do not add a new CI validation step — `gradle/actions/wrapper-validation@v6` already runs before every Gradle invocation and `workflow-hygiene.yml` already runs the checker script (D-05).

    Leave the D-05 independent checksum comparison and the Dependabot entry to Task 2; this task's job is the regeneration and the end-to-end proof that a build actually runs on the new wrapper.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew --offline -q tasks</automated>
    <fails_when>non-zero exit, or the output contains "What went wrong", or the output contains "25.0.4.1"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q 'gradle-8.14.5-bin.zip' bbj-intellij/gradle/wrapper/gradle-wrapper.properties && grep -q '^distributionSha256Sum=6f74b601422d6d6fc4e1f9a1ab6522f642c2fdcbc15ae33ebd30ba3d7198e854$' bbj-intellij/gradle/wrapper/gradle-wrapper.properties && grep -q '^validateDistributionUrl=true$' bbj-intellij/gradle/wrapper/gradle-wrapper.properties</automated>
    <fails_when>non-zero exit — the declared release is not 8.14.5, the distribution pin is absent or differs from the published bin checksum, or URL validation was turned off</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && git status --porcelain bbj-intellij/gradle/wrapper/gradle-wrapper.jar bbj-intellij/gradlew bbj-intellij/gradlew.bat | wc -l | grep -qx '3'</automated>
    <fails_when>the count is not 3 — one or more of the JAR, gradlew, and gradlew.bat was not regenerated alongside the properties file (#503 requires all four to move together)</fails_when>
  </verify>
  <acceptance_criteria>
    - `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` contains a `distributionUrl` line naming `gradle-8.14.5-bin.zip` on `services.gradle.org`, the exact line `distributionSha256Sum=6f74b601422d6d6fc4e1f9a1ab6522f642c2fdcbc15ae33ebd30ba3d7198e854`, `networkTimeout=10000`, and `validateDistributionUrl=true`.
    - `git status --porcelain` shows all four of `gradle-wrapper.properties`, `gradle-wrapper.jar`, `gradlew`, `gradlew.bat` as modified — the quartet moved together.
    - `cd bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew --offline -q tasks` exits 0 on the 8.14.5 wrapper with no `What went wrong` line, proving the `78-01` daemon criteria still steer the daemon on the new release.
    - `git diff --name-only .github/workflows` prints nothing.
    - The SUMMARY records both `wrapper` invocations verbatim and their exit codes.
  </acceptance_criteria>
  <done>The wrapper quartet is regenerated at 8.14.5 by the wrapper task with the distribution pinned, and a real build runs on it from the host default JDK.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the wrapper against the publisher and add Dependabot surveillance</name>
  <precondition>Network egress to `services.gradle.org` is available — the published `.sha256` files are fetched at execution time rather than trusted from a planning document (D-05).</precondition>
  <read_first>
    - `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` (as regenerated by Task 1)
    - `.github/dependabot.yml` (the full file — the existing `npm` stanza whose indentation and quoting style the new stanza matches)
    - `bbj-vscode/tools/check-gradle-wrapper.mjs` — the `GRADLE_CHECKSUMS` table, the JAR-vs-declared-version check, and the validation-step ordering rule
    - `.planning/phases/78-build-test-foundation/78-PATTERNS.md` § "`.github/dependabot.yml` — new `gradle` ecosystem entry"
    - `.planning/phases/78-build-test-foundation/78-CONTEXT.md` D-05 and D-08 (including the Claude's Discretion note on wrapper-version updates)
  </read_first>
  <files>.github/dependabot.yml</files>
  <action>
    Independently confirm the bytes, then arrange for the bump to be noticed next time.

    First, fetch both published checksums with `curl -L` (the URLs 301-redirect, so follow redirects): `https://services.gradle.org/distributions/gradle-8.14.5-wrapper.jar.sha256` and `https://services.gradle.org/distributions/gradle-8.14.5-bin.zip.sha256`. Compare the first against `sha256sum` of the committed `bbj-intellij/gradle/wrapper/gradle-wrapper.jar`, and the second against the `distributionSha256Sum` line Task 1 wrote. Both must match exactly. Record both fetched values and both local values in the SUMMARY. If either differs, stop — do not adjust the local value to match a mismatch; re-run the regeneration instead.

    Second, append a sibling stanza to the `updates:` list in `.github/dependabot.yml` for `package-ecosystem: "gradle"`, `directory: "/bbj-intellij"`, weekly schedule — matching the existing `npm` stanza's 2-space nesting and double-quoted scalars exactly (D-08). **Discretion resolved (D-08):** do NOT add an `ignore:` block for wrapper-version updates. Letting wrapper bumps flow through means `workflow-hygiene.yml` fails any Dependabot wrapper PR whose checksums are not yet in `GRADLE_CHECKSUMS` — that failure is the intended control, forcing a human to record the new checksums before the bump can merge. Add a short YAML comment above the stanza stating that interaction so the next reader does not mistake the red check for a bug, and repeat it in the PR description.

    Third, run the repository's own pre-flight, `node bbj-vscode/tools/check-gradle-wrapper.mjs`, from the repository root. It must report 0 findings. Do not edit `GRADLE_CHECKSUMS` — 8.14.5 is already tabulated. Do not add a second wrapper-validation step to any workflow: the three Gradle workflows already run `gradle/actions/wrapper-validation@v6` before their Gradle invocations and that ordering is itself asserted by the checker (D-05).
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && test "$(curl -sSL https://services.gradle.org/distributions/gradle-8.14.5-wrapper.jar.sha256)" = "$(sha256sum bbj-intellij/gradle/wrapper/gradle-wrapper.jar | cut -d' ' -f1)"</automated>
    <fails_when>non-zero exit — the committed wrapper JAR's SHA-256 differs from the value Gradle publishes for the 8.14.5 wrapper JAR, or the fetch returned empty</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && test "$(curl -sSL https://services.gradle.org/distributions/gradle-8.14.5-bin.zip.sha256)" = "$(grep '^distributionSha256Sum=' bbj-intellij/gradle/wrapper/gradle-wrapper.properties | cut -d= -f2)"</automated>
    <fails_when>non-zero exit — the pinned distribution checksum differs from the published bin.zip checksum, or the fetch returned empty</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && node bbj-vscode/tools/check-gradle-wrapper.mjs</automated>
    <fails_when>non-zero exit, or the final line does not end with "0 findings.", or the output contains "Refusing to report success"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q 'package-ecosystem: "gradle"' .github/dependabot.yml && grep -q 'directory: "/bbj-intellij"' .github/dependabot.yml && grep -q 'package-ecosystem: "npm"' .github/dependabot.yml</automated>
    <fails_when>non-zero exit — the gradle stanza is missing, points at the wrong directory, or the pre-existing npm stanza was clobbered</fails_when>
  </verify>
  <acceptance_criteria>
    - `curl -sSL https://services.gradle.org/distributions/gradle-8.14.5-wrapper.jar.sha256` returns a value equal to `sha256sum bbj-intellij/gradle/wrapper/gradle-wrapper.jar`; both values are quoted in the SUMMARY.
    - `curl -sSL https://services.gradle.org/distributions/gradle-8.14.5-bin.zip.sha256` returns a value equal to the `distributionSha256Sum` line's value; both values are quoted in the SUMMARY.
    - `.github/dependabot.yml` contains a second entry under `updates:` with `package-ecosystem: "gradle"`, `directory: "/bbj-intellij"`, and `interval: "weekly"`, using the same indentation and double-quoted scalar style as the npm stanza, preceded by a comment naming the hygiene-gate interaction.
    - The npm stanza and both of its `ignore:` entries (`chevrotain`, `typescript`) are unchanged.
    - `node bbj-vscode/tools/check-gradle-wrapper.mjs` exits 0 and its last line ends with `0 findings.`
    - `git diff --name-only .github/workflows` prints nothing and `bbj-vscode/tools/check-gradle-wrapper.mjs` is unmodified.
  </acceptance_criteria>
  <done>The committed wrapper bytes are confirmed against the publisher at execution time, the repository's own hygiene checker is green, and Dependabot now watches the module weekly.</done>
</task>

<task type="auto">
  <name>Task 3: Enumerate the transitive tree and prove plugin 2.11.0 on 8.14.5</name>
  <read_first>
    - `bbj-intellij/build.gradle.kts` (the `dependencies { }` block — `intellijIdeaCommunity("2024.2")`, the bundled TextMate plugin, LSP4IJ 0.19.0, and the JUnit BOM entries whose fan-out this task enumerates)
    - `.planning/phases/78-build-test-foundation/78-CONTEXT.md` D-06, D-07, D-15
  </read_first>
  <files>.planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt</files>
  <action>
    Produce the closing evidence for #576 and confirm the platform plugin survived the bump.

    From `bbj-intellij/` with `JAVA_HOME=/opt/java/default`, run the `dependencies` report task across all configurations with `--console=plain`, redirecting stdout and stderr into `.planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt`. Prefix the file with the exact command line and the Gradle version that produced it. D-07 notes what to expect: the `compileClasspath` tree is short (IDEA Community 2024.2, the bundled TextMate plugin, LSP4IJ 0.19.0) while the runtime and test trees carry the JUnit BOM fan-out — a report showing only a handful of lines total means the run did not resolve what it should have.

    Then run the D-06 compatibility proof on the new wrapper: the `test` task, then `buildPlugin` (which itself depends on `test`). `intellij-platform-gradle-plugin` 2.11.0 declares 8.13 as its minimum and 8.14.5 has never been exercised in this repository, so this is a real check, not a formality. If either goes red because of the plugin rather than the code, **stop and surface it in the SUMMARY** — bumping the platform plugin past 2.11.0 is not in this phase's scope (D-06), and neither is downgrading the wrapper to make it pass.

    Record the exit code of each command in the SUMMARY alongside the JUnit test and class counts, per D-15.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew dependencies --console=plain > /home/coder/repos/bbj-language-server/.planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt 2>&1</automated>
    <fails_when>non-zero exit, or the redirected file contains "What went wrong", or the file contains no line matching "compileClasspath"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q 'runtimeClasspath' .planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt && grep -q 'testRuntimeClasspath' .planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt</automated>
    <fails_when>non-zero exit — the report covered only one configuration instead of the full set, so the transitive tree was not enumerated</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew buildPlugin</automated>
    <fails_when>non-zero exit, or the output contains "FAILED", or the output does not contain "BUILD SUCCESSFUL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt` exists, is non-empty, opens with the exact command line and Gradle version that produced it, and contains sections for `compileClasspath`, `runtimeClasspath`, and `testRuntimeClasspath`.
    - The report names `ideaIC` / IDEA Community `2024.2`, LSP4IJ `0.19.0`, and at least one `org.junit` coordinate — the fan-out D-07 predicts.
    - `JAVA_HOME=/opt/java/default ./gradlew buildPlugin` exits 0 with `BUILD SUCCESSFUL` on the 8.14.5 wrapper, and the run includes the `test` task (it is wired via `buildPlugin dependsOn test`).
    - The SUMMARY records the exit code of `dependencies`, `test`, and `buildPlugin`, plus the JUnit class and test counts, and states explicitly whether `intellij-platform-gradle-plugin` 2.11.0 needed anything to work on 8.14.5.
    - No change was made to `bbj-intellij/build.gradle.kts`, `bbj-intellij/settings.gradle.kts`, or any file under `bbj-intellij/src/` — `git status --porcelain bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/src` prints nothing.
  </acceptance_criteria>
  <done>The transitive dependency tree is enumerated and captured, and the IntelliJ platform plugin is proven to work on the new wrapper.</done>
</task>

</tasks>

## Artifacts this phase produces

Created or changed by **this plan**:

| Artifact | Kind | Notes |
|---|---|---|
| `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` | modified | `distributionUrl` → `gradle-8.14.5-bin.zip`; `distributionSha256Sum` → `6f74b601…8854` |
| `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` | modified (regenerated) | SHA-256 must equal the published `gradle-8.14.5-wrapper.jar.sha256` |
| `bbj-intellij/gradlew` | modified (regenerated) | Written by the `wrapper` task running under 8.14.5 |
| `bbj-intellij/gradlew.bat` | modified (regenerated) | Written by the `wrapper` task running under 8.14.5 |
| `.github/dependabot.yml` | modified | New `gradle` ecosystem stanza, `directory: "/bbj-intellij"`, weekly |
| `.planning/phases/78-build-test-foundation/78-DEPENDENCIES.txt` | new tracked file | Enumerated transitive tree — closing evidence for #576 |
| `wrapper`, `dependencies`, `buildPlugin` | Gradle tasks invoked (not authored) | No new Gradle task is defined by this plan |

Produced by sibling plans: `78-01` creates `bbj-intellij/gradle/gradle-daemon-jvm.properties` and `78-PROVISIONING-PROOF.txt`; `78-03` adds the `verifyLanguageServerBundle` Gradle task, the `BbjLanguageServerBundleSourceGuardTest` JUnit class, and a one-line `CLAUDE.md` note.

## Flagged assumptions (spec-less probe)

| Probe row | Category | Status | Flagged assumption |
|---|---|---|---|
| BUILD-02 | unclassified | unresolved | The classifier returned no edge category for BUILD-02 ("unclassified — review manually"). **Assumption carried:** "current 8.x" is read as *the latest 8.x release at planning time*, 8.14.5, and "a CI wrapper-validation step guards against tampering" is read as *the already-present* `gradle/actions/wrapper-validation@v6` plus `check-gradle-wrapper.mjs`, not as a new step to author (D-05). If a newer 8.x ships between planning and execution, this plan still targets 8.14.5 — the version whose checksums `check-gradle-wrapper.mjs` already tabulates — rather than chasing the newest release and forcing a table edit. |

## Prohibition-probe breadcrumbs

Canon items surfaced by the recall pass and **not** minted as prohibitions: dependency-confusion and repository-substitution attacks against the Gradle Plugin Portal and Maven Central are canon supply-chain security — covered by `/gsd-secure-phase`; not minted here.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services.gradle.org → the build machine | The wrapper downloads and then executes a Gradle distribution before any code review gate runs |
| Committed `gradle-wrapper.jar` → every developer and CI runner | The JAR is executable code in the repository; a swapped JAR compromises every build |
| Dependabot → `.github/` | An automated actor opens PRs that change build inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-05 | Tampering | `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` and the downloaded `gradle-8.14.5-bin.zip` | high | mitigate | Regenerate only via the `wrapper` task with `--gradle-distribution-sha256-sum` (Task 1); independently compare the resulting JAR's SHA-256 against the publisher's `gradle-8.14.5-wrapper.jar.sha256` fetched at execution time (Task 2); `gradle/actions/wrapper-validation@v6` runs before every `./gradlew` step in the three Gradle workflows and `check-gradle-wrapper.mjs` asserts both the pin and that ordering. |
| T-78-06 | Spoofing | `distributionUrl` in `gradle-wrapper.properties` | medium | mitigate | `validateDistributionUrl=true` is preserved through the regeneration and asserted by Task 1's second verify command; the acceptance criteria require the URL to name `services.gradle.org` and the 8.14.5 release. |
| T-78-07 | Tampering | Dependabot-proposed Gradle updates merging without checksum review | medium | mitigate | The stanza only opens PRs on a weekly cadence; `workflow-hygiene.yml` fails any wrapper bump whose checksums are absent from `GRADLE_CHECKSUMS`, forcing a human to record them first. D-08 names this as the intended control; a YAML comment and the PR description document it. |
| T-78-SC | Tampering | Package-manager installs (supply chain) | high | mitigate | This phase performs no npm, pip, or cargo install, so the package-legitimacy checkpoint protocol does not apply. The external artifacts it does introduce — the Gradle distribution, the wrapper JAR, and the foojay settings plugin from `78-01` — are each version-pinned and verified by checksum or by official-namespace publisher, with `wrapper-validation@v6` as the standing CI control. |
| T-78-08 | Repudiation | A wrapper bump landing without recorded provenance | low | accept | The regeneration commands and both fetched checksums are recorded verbatim in the plan SUMMARY, and the four wrapper files move in a single reviewable commit; no further attestation is warranted at ASVS L1. |
</threat_model>

<verification>
1. `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` declares 8.14.5 with the published bin checksum, `networkTimeout`, and `validateDistributionUrl=true`.
2. The committed `gradle-wrapper.jar` SHA-256 equals the publisher's `gradle-8.14.5-wrapper.jar.sha256`, fetched live.
3. All four wrapper files were regenerated together by the `wrapper` task.
4. `node bbj-vscode/tools/check-gradle-wrapper.mjs` exits 0 with `0 findings.`
5. `cd bbj-intellij && JAVA_HOME=/opt/java/default ./gradlew dependencies` exits 0 and `78-DEPENDENCIES.txt` holds `compileClasspath`, `runtimeClasspath`, and `testRuntimeClasspath` sections.
6. `./gradlew buildPlugin` (and therefore `test`) exits 0 on 8.14.5 with plugin 2.11.0.
7. `.github/dependabot.yml` has both the untouched npm stanza and the new gradle stanza; `.github/workflows/` is unchanged.
</verification>

<success_criteria>
- BUILD-02 is satisfied: current 8.x wrapper, checksum-pinned and publisher-verified, CI-guarded by the existing validation step, with the transitive tree enumerable on a JDK 25 host.
- Nothing about the wrapper was taken on trust from a planning document — every checksum was fetched from the publisher at execution time.
- The module is now under weekly Dependabot surveillance, with the hygiene gate as the deliberate friction on future wrapper bumps.
</success_criteria>

<output>
Create `.planning/phases/78-build-test-foundation/78-02-SUMMARY.md` when done. Include: both `wrapper` command lines and exit codes, the four checksum values (2 fetched, 2 local), the checker script's final line, the `dependencies`/`test`/`buildPlugin` exit codes with JUnit counts, and an explicit statement about `intellij-platform-gradle-plugin` 2.11.0 on 8.14.5.
</output>
