# Phase 78: Build & Test Foundation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-04
**Phase:** 78-build-test-foundation
**Mode:** `--auto` (recommended option selected for every question; no AskUserQuestion prompts)
**Areas discussed:** Daemon JVM mechanism, Wrapper target and regeneration, Fail-fast bundle check, CI and docs touch, Verification evidence

`[--auto] Selected all gray areas: Daemon JVM mechanism, Wrapper target and regeneration, Fail-fast bundle check, CI and docs touch, Verification evidence.`

---

## Daemon JVM mechanism (#570)

`[auto] Q: "How does the build become JDK-independent?"` → Selected: "Toolchain block + committed daemon-JVM criteria (recommended)"

| Option | Description | Selected |
|--------|-------------|----------|
| Toolchain block + daemon-JVM criteria | `java.toolchain` 17 for compile/test plus `gradle/gradle-daemon-jvm.properties` so the daemon runs on 17 whatever JVM launches `gradlew` | ✓ |
| Toolchain block only (research recipe) | Fails: the #570 error is Gradle's Kotlin DSL compiler in the daemon JVM, before `build.gradle.kts` is read; Gradle 8.14.5 does not support running on Java 25 | |
| Gradle 9.x daemon | Supports Java 25 but forces a platform-plugin bump and sandbox migration; out of scope | |
| `org.gradle.java.home` / `~/.gradle` hints | Machine-local, not committable; is the "manual JDK switching" the criterion forbids | |

`[auto] Q: "Provisioning when no JDK 17 is installed?"` → Selected: "foojay-resolver-convention 1.0.0 + `updateDaemonJvm --jvm-version=17`, no vendor pin (recommended)"

| Option | Description | Selected |
|--------|-------------|----------|
| foojay resolver, version-only criteria | Generated `toolchainUrl.*` entries download 17 when absent; any local 17 accepted first | ✓ |
| foojay resolver, vendor pinned (adoptium) | Stricter match; a locally installed non-Temurin 17 would trigger a download | |
| No resolver | Criteria file only; relocates #570 to machines without a JDK 17 | |

**Notes:** Scratch verification on this host (wrapper 8.13, JDK 25 client, criteria file only): `tasks`, `dependencies`, `test` all succeeded. Bootstrap needs one run on `/opt/java/17` because `updateDaemonJvm` itself needs a working daemon.

---

## Wrapper target and regeneration (#503, #576)

`[auto] Q: "Which Gradle version?"` → Selected: "8.14.5 via `./gradlew wrapper` (recommended)"

| Option | Description | Selected |
|--------|-------------|----------|
| 8.14.5, regenerated by the wrapper task | Latest 8.x; already tabulated in `check-gradle-wrapper.mjs`; verify JAR and bin checksums against services.gradle.org at execution time | ✓ |
| Stay on 8.13 | JAR already matches (commit f7908c0) but requirement says "regenerated to a current 8.x release" and #576 calls 8.13 stale | |
| 9.7.1 (current) | Out of scope | |

`[auto] Q: "CI validation step?"` → Selected: "Keep the existing wrapper-validation@v6 + workflow-hygiene guards, add nothing (recommended)"

`[auto] Q: "Dependabot gradle entry?"` → Selected: "Add weekly `gradle` ecosystem entry for `/bbj-intellij` (recommended; REQUIREMENTS.md names it as part of the satisfying set)"

`[auto] Q: "Where does the dependency tree evidence live?"` → Selected: "Run `./gradlew dependencies` on JDK 25 and record it in the SUMMARY or an adjacent file (recommended)"

---

## Fail-fast bundle check (#517)

`[auto] Q: "Where does the check live?"` → Selected: "One dedicated task both copy sites depend on (recommended)"

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated verification task | Single check, single message, testable; `copyLanguageServer` and `prepareSandbox` depend on it | ✓ |
| `doFirst` in each copy site | Duplicated message and logic | |
| Configuration-time check | Would break `tasks`, `wrapper`, `updateDaemonJvm`, `dependencies` on a clean clone | |
| Gradle `Exec` running `npm run build` | Hides the dependency, couples Gradle to Node, duplicates CI artifact flow | |

`[auto] Q: "Message content?"` → Selected: "Expected absolute path + `cd bbj-vscode && npm ci && npm run build` + CI artifact note, via GradleException (recommended)"

`[auto] Q: "Guard the other copied files too?"` → Selected: "No, only `main.cjs` (tracked files are always present)"

---

## CI and docs touch

`[auto] Q: "Change CI JDK setup?"` → Selected: "No; keep setup-java 17, no JDK matrix (recommended)"

`[auto] Q: "Docs?"` → Selected: "One-line CLAUDE.md note at most; respect uncommitted local edits (recommended)"

---

## Verification evidence

`[auto] Q: "What proves the criteria?"` → Selected: "JDK 25 runs of `tasks`, `dependencies`, `test`, `buildPlugin`, hygiene script; move-aside proof for the fail-fast; optional foojay download proof (recommended)"

`[auto] Q: "Regression test for the fail-fast?"` → Selected: "Actual run recorded + source-guard JUnit at planner's discretion; no TestKit (recommended)"

---

## Claude's Discretion

- Task name / DSL shape of the fail-fast check
- Dependabot entry ignoring wrapper updates or not
- Native-access warnings on JDK 24+ clients (leave alone by default)
- Plan splitting (one to three plans)

## Deferred Ideas

- Gradle 9.x + platform-plugin bump; version catalogs / dependency locking; Phase 83 regression coverage; wider CI JDK matrix
- Two keyword-matched todos reviewed and not folded (EM config sentinel run-action fix; live-interop vitest drift) — outside the Gradle-build boundary
