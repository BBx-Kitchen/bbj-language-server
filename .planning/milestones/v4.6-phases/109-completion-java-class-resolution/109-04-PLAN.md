---
phase: 109-completion-java-class-resolution
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/language/java-interop.ts
  - bbj-vscode/test/counting-java-interop.ts
  - bbj-vscode/test/java-interop-local-types.test.ts
autonomous: true
requirements: [JINT-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Resolving a class whose members use `int`, `void`, `byte[]` or `java.lang.Object[]` sends the backend exactly one request, for that class; no primitive, `void`, array or blank type name reaches `getRawClass` (ROADMAP criterion 4)"
    - "The filter sits at the single choke point: `resolveClassByName` recognises the eight Java primitives, `void`, any name ending in `[]` and a blank name before any cache lookup, so every caller is covered: resolveClass Phase 2 field, return, parameter and constructor types, the scope's FQN preload and USE resolution, and the auto-import probe (D-08)"
    - "D-08 finding, from both backends' getProperTypeName: it erases exactly one array dimension, so a multi-dimensional member type arrives array-suffixed (`byte[][]` becomes `byte[]`, `Object[][]` becomes `java.lang.Object[]`) and enters resolveClass Phase 2 like any other member type; a test models that shape"
    - "Behaviour-neutral (D-07): the local result is what today's round trip produced. A primitive or `void` becomes a zero-member JavaClass with packageName `java.lang` and no error (the backends answer them through Primitives.allPrimitiveTypes and Class.getPackageName); an array or blank name becomes a zero-member JavaClass carrying the backend's not-found error `Class not found: <name>`. Both go through the same resolveClass pipeline (classpath-tree registration, cache, Phase 2), never createStubClass, and an array is not mapped to its component class"
    - "With bbj.debug on, the `Resolving class …` debug line is not written for a primitive, `void`, array or blank name, and is still written for a real class (ROADMAP criterion 4)"
    - "A real class whose name merely contains or starts with a primitive name (`java.lang.Integer`, `java.lang.Byte`, a package segment `bytes`, a class `Voider`) is still fetched from the backend exactly as before; the filter matches whole type names only"
    - statement: "An empty or whitespace type name produces no backend request and today's result for it is preserved"
      verification: backstop
    - "Two concurrent lookups of the same primitive/array name return the identical cached object and issue zero backend requests"
  artifacts:
    - path: "bbj-vscode/src/language/java-interop.ts"
      provides: "local primitive/void/array/blank resolution at the top of resolveClassByName; debug-line skip in resolveClass"
      exports: ["JAVA_PRIMITIVE_TYPE_NAMES", "isLocalJavaTypeName"]
    - path: "bbj-vscode/test/counting-java-interop.ts"
      provides: "CountingJavaInteropService (real resolveClassByName/resolveClass, counting getRawClass that answers like the real backends), createCountingInteropServices, backendLikeDto, rawMethod, rawField"
      exports: ["CountingJavaInteropService", "createCountingInteropServices", "backendLikeDto", "rawMethod", "rawField"]
    - path: "bbj-vscode/test/java-interop-local-types.test.ts"
      provides: "request counts, behaviour neutrality, logging, adjacency, blank and concurrency tests"
  key_links:
    - from: "JavaInteropService.resolveClassByName"
      to: "resolveClass(localJavaTypeDto(name), token, _depth)"
      via: "isLocalJavaTypeName check as the first statement"
      pattern: "isLocalJavaTypeName\\("
    - from: "CountingJavaInteropService.getRawClass"
      to: "backendLikeDto"
      via: "records the requested spelling, answers like InteropService.loadClassInfo"
      pattern: "rawClassCalls\\.push"
  prohibitions:
    - requirement_id: JINT-01
      category: trust
      statement: "MUST NOT change what completion, hover, the overload selector or the unknown-member check see for a primitive, `void`, array or blank type name (D-07)"
      flagged: true
    - requirement_id: JINT-01
      category: scope
      statement: "MUST NOT map an array type to its component class, and MUST NOT stop fetching any real class name (D-07)"
      flagged: true
  assumptions:
    - statement: "The backends' answers modelled by backendLikeDto (primitive or void: packageName java.lang, no error; anything Class.forName rejects: `Class not found: <name>` with empty member lists; name echoed from the request) were read from bbj-ls and java-interop/ InteropService.loadClassInfo on 2026-09-25; an older bbj-ls that answered differently would make today's result differ from the local one only in that message text"
      source: "/home/coder/repos/bbj-ls/src/main/java/bbj/interop/InteropService.java:402-471; java-interop/src/main/java/bbj/interop/InteropService.java:166-238"
      flagged: true
---

<objective>
Stop sending primitive types, `void`, array types and blank names to the java-interop backend as class
lookups, while every consumer keeps seeing exactly the result it sees today (JINT-01, issue #660, D-07,
D-08).

Purpose: resolveClass Phase 2 passes every member type string to resolveClassByName, so a cold start
issues a round trip for `int`, `void`, `byte[]` and the like, each answered with an empty class.
Output: a local branch at the top of `resolveClassByName`, a debug-line skip in `resolveClass`, a reusable
counting test double, and `test/java-interop-local-types.test.ts`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-completion-java-class-resolution/109-CONTEXT.md
@.planning/phases/109-completion-java-class-resolution/109-RESEARCH.md
@bbj-vscode/test/java-interop-service.test.ts

<interfaces>
From java-interop.ts:
- `getResolvedClass(className: string): JavaClass | undefined` (443-449) reads `resolvedClasses` (an LRU keyed by the class name, limit 5000).
- `protected async getRawClass(className: string, token?): Promise<JavaClass>` (465-476) is the only function that sends `getClassInfo`; it is reached only through `doResolveClassByName` (938-976), which only `resolveClassByName` calls.
- `async resolveClassByName(className, token?, _depth = 0): Promise<JavaClass>` (894-936): guards in order: `resolvedClasses` fast path, `_inFlightPhase2`, `_pendingResolutions`, depth limit (uncached `createStubClass`), then `doResolveClassByName`.
- `private createStubClass(className, cache = true)` (985-1007) sets `error: 'Resolution failed or depth limit exceeded'`; wrong shape for this change.
- `protected async resolveClass(javaClass: Mutable<JavaClass>, token?, _depth = 0)` (1016-1165): cache and in-flight checks on `javaClass.name`; `$type`; `packageName` defaults to `extractPackageName(name)` when the DTO has none; `classes`/`constructors` defaults; class `deprecated` from `isDeprecated`; `storeJavaClass(javaClass, packageName)` (cuts `name` to the simple name under its package in the classpath tree); the debug line "Resolving class NAME: N methods, N fields" at 1032; Phase 1 member flags; `resolvedClasses.set`; Phase 2 awaits `javadocProvider.getDocumentation` then calls `resolveClassByName(field.type | method.returnType | parameter.type | constructor.returnType, token, _depth + 1)`.
- `logger` from `./logger.js`: `logger.setLevel(LogLevel.DEBUG)`, `logger.debug(msg)` writes `console.log('[debug] ' + msg)` only at DEBUG; the default level is WARN.
Backends (read only): bbj-ls `InteropService.loadClassInfo` (402-471) and java-interop/ (166-238) echo `classInfo.name = className`; a name matching one of Guava's nine `Primitives.allPrimitiveTypes()` (eight primitives and void) resolves to that primitive class with `packageName = clazz.getPackageName()` (`java.lang`), no fields/methods/constructors; any name `Class.forName` rejects returns empty lists and `error = "Class not found: " + className`. `getProperTypeName` returns `getComponentType().getCanonicalName()` for an array, one dimension only.
Test pattern (java-interop-service.test.ts 55-181): `MockableJavaInteropService`/`CyclicFakeInteropService` extend JavaInteropService directly, initialize `JavadocProvider` in the constructor, override `getRawClass` with per-name counting, and `createServices(factory)` injects the double through a `java: { JavaInteropService }` module. The standard `JavaInteropTestService` (bbj-test-module.ts 172-175) overrides `resolveClassByName` itself and must not be used for these tests.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`. Allowed
  `cd` form: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Use `git -C` for git. Stage
  with `git add <exact path>` only. Never use `git stash`.
- vitest runs with cwd `bbj-vscode`; never the `basic` reporter. These tests never open a socket: the
  counting double rejects `connect()` and answers `getRawClass` itself.
- Source and test text must not contain planning identifiers (D-NN, COMP-/JINT- ids, 109-0N, CR-/WR-/IN-NN,
  T-109-NN). Issue numbers such as #660 are fine.
- Commit messages must not contain a GitHub closing keyword followed by an issue number; write "issue #660".
- Do not modify `bbj-test-module.ts` or the backends. Plan 109-05 builds on this plan's double and edits
  the same `resolveClassByName` next.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark JINT-01 complete in
  REQUIREMENTS.md; plan 109-06 closes it after the live check.
- A failing test elsewhere is compared with the phase base (the parent of the first commit tagged
  `(109-0N)`) before it is called pre-existing; never label it environment noise without that comparison.

## Artifacts this phase produces

- (109-01) `test/completion-method-body.test.ts`; `109-COMP03-MEASUREMENT.md`; the un-skipped DEF FN test.
- (109-02) the `MemberCall` class-reference branch in `bbj-scope.ts`; `test/completion-class-reference.test.ts`.
- (109-03) `argumentTypeOf`, `OverloadCandidate`, `overloadCandidates`, `bestOverloadCandidates`;
  overload re-selection in `bbj-type-inferer.ts`; `test/overload-return-type.test.ts`.
- (this plan) `JAVA_PRIMITIVE_TYPE_NAMES`, `isLocalJavaTypeName` (exported) and the module-private
  `localJavaTypeDto` in `java-interop.ts`, the local branch in `resolveClassByName`, the debug-line skip in
  `resolveClass`; `test/counting-java-interop.ts` (`CountingJavaInteropService`,
  `createCountingInteropServices`, `backendLikeDto`, `rawMethod`, `rawField`);
  `test/java-interop-local-types.test.ts`.
- (109-05) `canonicalJavaClassName` and canonical keys in `java-interop.ts`;
  `test/java-interop-nested-class-names.test.ts`.
- (109-06) `test/functional/java-class-lookups-real-interop.test.ts`; Final state of the COMP-03 record;
  the #561 action; the phase regression gate.

<tasks>

<task type="tracer">
  <name>Task 1: A class with an `int` member resolves with one backend request, end to end through the real resolver</name>
  <files>bbj-vscode/test/counting-java-interop.ts, bbj-vscode/test/java-interop-local-types.test.ts, bbj-vscode/src/language/java-interop.ts</files>
  <reversibility rating="reversible">A guard at the top of one method plus a logging condition; removing them restores today's round trips.</reversibility>
  <read_first>
    - bbj-vscode/src/language/java-interop.ts (lines 1-60, 438-476, 886-1165, 1190-1253, 1398-1410)
    - bbj-vscode/test/java-interop-service.test.ts (lines 1-181, the double and createServices pattern)
    - bbj-vscode/test/bbj-test-module.ts (lines 61-85 JavadocProvider init, 172-189 the resolveClassByName override to avoid)
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/InteropService.java (lines 402-512, read only)
    - bbj-vscode/src/language/logger.ts
  </read_first>
  <behavior>
    - Scripted class `com.test.Types` (packageName `com.test`, method `count()` returning `int`): `resolveClassByName('com.test.Types')` leaves `rawClassCalls` equal to `['com.test.Types']`
    - That method's `resolvedReturnType.ref` is a JavaClass named `int` with packageName `java.lang`, no `error`, and no fields, methods or constructors; `getResolvedClass('int')` returns the same object
  </behavior>
  <action>
Build the counting double, prove the round trip today, then add the local branch.

1. Create `bbj-vscode/test/counting-java-interop.ts` (not a test file itself):
   - `export const BACKEND_PRIMITIVE_NAMES` = the nine names Guava's `Primitives.allPrimitiveTypes()` covers: boolean, byte, char, double, float, int, long, short, void.
   - Types `RawMethod`, `RawField`, `RawClassInfo` for the backend DTO shape (`name`, `simpleName?`, `packageName?`, `isDeprecated?`, `fields`, `methods`, `constructors`, `error?`); `export function rawMethod(name, returnType, parameterTypes: string[] = [], isStatic = false)` and `export function rawField(name, type, isStatic = false)` returning fresh objects.
   - `export function backendLikeDto(className: string, script?: () => Omit<RawClassInfo, 'name'>): JavaClass`, modelling `loadClassInfo`: always `name = className` (the backend echoes the requested spelling); with a script, the script's fresh data; else for a primitive name `{ simpleName: className, packageName: 'java.lang', isDeprecated: false, fields: [], methods: [], constructors: [] }`; else `{ fields: [], methods: [], constructors: [], error: 'Class not found: ' + className }`. Doc comment cites bbj-ls and java-interop/ `InteropService.loadClassInfo` by file name.
   - `export class CountingJavaInteropService extends JavaInteropService` with `readonly rawClassCalls: string[] = []`, `readonly scripts = new Map<string, () => Omit<RawClassInfo, 'name'>>()` keyed by requested spelling, a constructor that initializes `JavadocProvider` like `JavaInteropTestService`, `protected override connect()` rejecting (hermetic), `protected override async getRawClass(className)` that pushes `className` and returns `backendLikeDto(className, this.scripts.get(className))`, and `resolveRaw(dto: JavaClass): Promise<JavaClass>` calling the protected `resolveClass`.
   - `export function createCountingInteropServices(): { shared, BBj, interop: CountingJavaInteropService }` following java-interop-service.test.ts's `createServices`.
2. Red first: create `bbj-vscode/test/java-interop-local-types.test.ts` with the two behavior cases in `describe('primitive, void and array type names never reach the backend (issue #660)', …)`, a fresh service per test. Confirm the first fails today with `['com.test.Types', 'int']`.
3. In `java-interop.ts`:
   - Export `JAVA_PRIMITIVE_TYPE_NAMES: ReadonlySet<string>` (the eight Java primitives and `void`) and `export function isLocalJavaTypeName(name: string): boolean`, true for a whole-name member of that set, a name ending in `[]`, or a name that is empty after trimming. Doc comment in plain words: these are never classes on the backend's classpath; primitives and void keep the backend's own answer (package java.lang, no members) and arrays and blank names its not-found answer, built locally (issue #660).
   - Add a module-private `localJavaTypeDto(name: string): Mutable<JavaClass>` returning the backend-shaped raw object: for a primitive or `void`, `name`, `simpleName: name`, `packageName: 'java.lang'`, `isDeprecated: false`, empty `fields`/`methods`/`constructors`; otherwise `name`, empty lists and `error: 'Class not found: ' + name` (no packageName, so resolveClass derives it exactly as for today's answer). Do not use `createStubClass`.
   - Make the first statement of `resolveClassByName`: when `isLocalJavaTypeName(className)`, return `this.resolveClass(localJavaTypeDto(className), token, _depth)`. resolveClass's own cache and in-flight checks return the already-resolved object on every later or concurrent lookup. Extend the method's doc comment with one sentence on this branch.
   - In `resolveClass`, write the `Resolving class …` debug line only when `!isLocalJavaTypeName(className)`.
4. Run the new file, the existing interop suites and tsc.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/java-interop-local-types.test.ts test/java-interop-service.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx tsc -p tsconfig.json</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isLocalJavaTypeName" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints 1 and `grep -c "isLocalJavaTypeName(" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints 3 or more (definition, resolveClassByName, resolveClass)
    - `grep -c "rawClassCalls.push" /home/coder/repos/bbj-language-server/bbj-vscode/test/counting-java-interop.ts` prints 1
    - The first behavior case failed before step 3 and passes after
    - `git -C /home/coder/repos/bbj-language-server diff --stat HEAD -- bbj-vscode/test/bbj-test-module.ts` prints nothing
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>A class with an `int` member resolves with one backend request through the real resolveClassByName, and the counting double is committed for 109-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every primitive, void, array and blank name is local and behaviour-neutral; real names still go out</name>
  <files>bbj-vscode/test/java-interop-local-types.test.ts, bbj-vscode/src/language/java-interop.ts</files>
  <read_first>
    - bbj-vscode/test/java-interop-local-types.test.ts and bbj-vscode/test/counting-java-interop.ts (after Task 1)
    - bbj-vscode/src/language/java-interop.ts (lines 886-1165 after Task 1)
    - bbj-vscode/src/language/validations/check-unknown-java-member.ts (lines 27-47, isFullyResolvedJavaClass reads `error`; read only)
  </read_first>
  <behavior>
    - For each of the nine primitive/void names and `byte[]`, `java.lang.Object[]`, `int[][]`, `''` and `'  '`: `resolveClassByName(name)` on a fresh service adds nothing to `rawClassCalls`
    - Neutrality: for each of those names, the projection `{ name, packageName, error, deprecated, fields.length, methods.length, constructors.length, classes.length, $container.$type }` of the local result equals the projection of `resolveRaw(backendLikeDto(name))` on a second fresh service (today's round trip, which went through the same pipeline); `byte[]` keeps `error` `Class not found: byte[]` and is not replaced by the `byte` class
    - Multi-dimensional member types (D-08): scripted `com.test.Masks` with a method `apply(byte[])` returning `java.lang.Object[]` and a field of type `long` (the shapes a `byte[][]` parameter and an `Object[][]` return arrive in after the backend's one-level erasure) resolves with `rawClassCalls` equal to `['com.test.Masks']`
    - Adjacency: `java.lang.Integer`, `java.lang.Byte`, `com.bytes.Foo`, `Voider` and `Integer` each add exactly one entry, spelled as requested
    - Concurrency: `Promise.all` of two `resolveClassByName('int')` and two `resolveClassByName('byte[]')` gives identical objects per name and no backend request
    - Logging: with `logger.setLevel(LogLevel.DEBUG)` and `console.log` spied, resolving `com.test.Masks` writes a line containing `Resolving class com.test.Masks` and no line containing `Resolving class ` followed by `byte[]`, `java.lang.Object[]` or `long`; the level is set back to WARN afterwards
  </behavior>
  <action>
Pin every name class the filter covers and every neighbour it must not touch.

1. Add the behavior cases to `java-interop-local-types.test.ts` in `describe('the local result is what the backend answered before', …)`, `describe('real class names still reach the backend', …)` and `describe('logging on a cold start', …)`, a fresh service per test (createCountingInteropServices). Restore the logger level and the console spy in `afterEach`.
2. If a neutrality projection differs, change `localJavaTypeDto` so it matches the backend-shaped answer; never change `backendLikeDto` to match the implementation, since it models the real backends.
3. Run the new file with every existing interop suite, then the unknown-member, completion and hover suites (they read resolved classes), then tsc.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/java-interop-local-types.test.ts test/java-interop-service.test.ts test/java-interop-timeouts.test.ts test/java-interop-breaker.test.ts test/java-interop-parse-lane.test.ts test/java-class-reload.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts test/completion-test.test.ts test/hover.test.ts test/method-return-java-type.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "java.lang.Object\[\]" /home/coder/repos/bbj-language-server/bbj-vscode/test/java-interop-local-types.test.ts` prints 1 or more, and the same for `Voider`, `com.bytes.Foo`, `Promise.all`, `LogLevel.DEBUG` and `backendLikeDto`
    - Every test file named in the two verify commands passes
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>Primitive, void, array and blank names never reach the backend, their results match today's answers field for field, real names still go out once each, and the debug log shows no lookup for them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server → java-interop backend (:5008) | every resolveClassByName miss becomes a getClassInfo request |
| backend class data → language server | member type strings come from reflection on the classpath |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-08 | Denial of service | getClassInfo traffic on cold start | medium | mitigate | Primitive, void, array and blank names are answered locally at the single choke point; counting-double tests assert zero requests for them and exactly one per real class. |
| T-109-09 | Tampering (developer trust) | results seen by completion, hover, overload selection, VAL-03 | medium | mitigate | The local answer copies the backend's own answer shape (packageName java.lang for primitives, the not-found error for arrays) through the same resolveClass pipeline; neutrality tests compare field by field against the modelled round trip. |
| T-109-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- java-interop-local-types.test.ts and every existing interop suite pass; unknown-member, completion, hover and method-return suites pass; tsc clean
- bbj-test-module.ts unchanged; register check clean
</verification>

<success_criteria>
- ROADMAP criterion 4 (unit level): no class lookup and no `Resolving class` debug line for a primitive, `void` or array type; 109-06 repeats it against the real backend
</success_criteria>

<output>
Create `.planning/phases/109-completion-java-class-resolution/109-04-SUMMARY.md` when done
</output>
