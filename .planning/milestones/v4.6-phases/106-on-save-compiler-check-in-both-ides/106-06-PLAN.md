---
phase: 106-on-save-compiler-check-in-both-ides
plan: 06
type: execute
wave: 4
depends_on: ["106-05"]
files_modified:
  - bbj-vscode/src/language/bbj-document-builder.ts
  - bbj-vscode/src/language/bbj-document-validator.ts
  - bbj-vscode/src/language/bbj-kept-check.ts
  - bbj-vscode/test/on-save-kept-errors.test.ts
  - bbj-vscode/test/bbj-cpl-fallback-dedup.test.ts
autonomous: true
requirements: [TRIG-04, DIAG-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Under on-save, a save's check result that arrives after the user already typed is kept, not dropped, and its errors are placed on the lines they moved to; 105 D-07's stale-verdict drop no longer applies to on-save (D-01)"
    - "Under debounced, a verdict for text that has since moved on is still dropped, exactly as before (105 D-07 unchanged; TRIG-05)"
    - "A newer save supersedes a check still in flight: the older result never overwrites the newer one, whichever arrives first (edge: concurrency of TRIG-02)"
    - "Under on-save, a bbjcpl fallback result also stays until the next save and follows its lines (D-01, either source); overlapping Langium syntax complaints the check saw give way to bbjcpl's own diagnostic, other complaints stay Errors and are never downgraded (D-09, D-10)"
    - "A bbjcpl fallback result whose checked text is not on disk is kept without suppressing any Langium diagnostic (D-11)"
    - "A check that finds no errors replaces the previous save's kept errors with none"
    - "Switching to on-save keeps each open file's current compiler errors, from a verdict or from bbjcpl, until that file's next save (D-08)"
    - "Switching from on-save to debounced keeps each file's current compiler errors until that file's first debounced check result, and the next edit starts that check as usual; steady-state debounced never reads kept checks (D-08, TRIG-05)"
    - "Switching to off clears every kept check and change log, and closing a document forgets its own (D-08)"
    - "A Langium validation and a check result landing in either order end in the same published list (edge: concurrency of TRIG-04)"
  artifacts:
    - path: "bbj-vscode/src/language/bbj-document-builder.ts"
      provides: "per-document check sequence, on-save keep-moved-on-result branch, fallback kept checks, storedUnderOnSave on every stored check"
      contains: "checkSequence"
    - path: "bbj-vscode/src/language/bbj-kept-check.ts"
      provides: "KeptCheck.storedUnderOnSave"
      contains: "storedUnderOnSave"
    - path: "bbj-vscode/src/language/bbj-document-validator.ts"
      provides: "validateDocument reads a kept check under on-save always, under debounced only when it was stored under on-save"
      contains: "storedUnderOnSave"
    - path: "bbj-vscode/test/on-save-kept-errors.test.ts"
      provides: "in-flight, supersession, fallback-kept, mode-switch, close and order-independence tests"
  key_links:
    - from: "debouncedCompile callback"
      to: "checkSequence"
      via: "a sequence number per cycle; a superseded on-save cycle stores and publishes nothing"
      pattern: "checkSequence\\.(get|set)\\("
    - from: "debouncedCompile fallback branch"
      to: "setKeptCheck({ kind: 'fallback', ... })"
      via: "seen from reconcileWithFallbackCheck when the checked text is on disk, empty otherwise"
      pattern: "kind: 'fallback'"
  prohibitions:
    - requirement_id: DIAG-01
      category: integrity
      statement: "MUST NOT let a kept bbjcpl result whose checked text differs from the editor text hide or downgrade a live Langium diagnostic"
      flagged: true
---

<objective>
Finish TRIG-04: keep a save's result even when the user typed while the check ran, let a newer
save supersede an older one, keep bbjcpl fallback results the same way as verdicts, and make runtime
mode switches keep each file's current errors.

Purpose: plan 05 keeps a verdict that arrives for the current text. D-01 also covers a result that
arrives after the text moved on and a result from either source. D-08 asks that a mode switch keep
current results. This plan also completes DIAG-01 for kept fallback results.
Output: builder changes for the in-flight, superseded and fallback cases; a `storedUnderOnSave`
flag read by the validator; end-to-end tests; the whole-suite gate.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-CONTEXT.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-04-SUMMARY.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-05-SUMMARY.md
@bbj-vscode/src/language/bbj-document-builder.ts
@bbj-vscode/src/language/bbj-kept-check.ts
@bbj-vscode/src/language/bbj-document-validator.ts
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` forms are `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`
  and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`. Use
  `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>`
  only, never `-A` or `.`. Never use `git stash`. Prefer the Read tool over shell reads.
- Source, test and doc text must not contain planning identifiers: no decision numbers of the
  form D-NN, no requirement ids (the TRIG, DIAG and JINT families), no plan numbers (106-0N), no
  review ids (CR-NN, WR-NN). GitHub issue numbers such as #522 and #696 are fine.
- Tests use `createBBjTestServices(EmptyFileSystem)` and revalidate with `builder.update([uri], [])`;
  never `DocumentBuilder.build`. Every builder-level test mocks `BBj.compiler.BBjCPLService.compile`
  with `vi.spyOn`, scripts verdicts through `JavaInteropTestService`, and drives text through
  `test/fake-text-document-connection.ts`. Hold a request in flight with `mockImplementationOnce`
  returning a promise the test resolves later.
- Whole-suite runs use `--maxWorkers=2` and `RUN_BBJ_TESTS=0`, judged on `numFailedTests`. For any
  failing test name, run that file on the phase base (`1427d059`) in a scratch worktree
  (`git -C /home/coder/repos/bbj-language-server worktree add --detach /tmp/phase-106-base 1427d059`,
  symlink `bbj-vscode/node_modules` from this clone) and compare names; a name that passes on the
  base is a regression to fix, not environment noise. Remove the worktree afterwards.
- Run vitest with cwd `bbj-vscode`. Never use vitest's `basic` reporter (vitest 4.1.10 has none).
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md or another ledger.

## Artifacts this plan produces

- `bbj-document-builder.ts`: private `checkSequence` map; on-save branch that keeps a verdict for
  text that has moved on (seen computed against the checked text); on-save fallback that completes
  after typing; `KeptCheck` of kind `'fallback'` stored in every mode except off; `storedUnderOnSave`
  set on every stored check.
- `bbj-kept-check.ts`: `KeptCheck.storedUnderOnSave: boolean`.
- `bbj-document-validator.ts`: `validateDocument` reads a kept check under on-save always, and under
  debounced only when `storedUnderOnSave` is true.
- Tests in `bbj-vscode/test/on-save-kept-errors.test.ts` and `bbj-vscode/test/bbj-cpl-fallback-dedup.test.ts`.

<tasks>

<task type="tracer">
  <name>Task 1: A save's verdict that arrives after the user typed is kept on the moved lines, and a newer save supersedes it</name>
  <files>bbj-vscode/src/language/bbj-document-builder.ts, bbj-vscode/test/on-save-kept-errors.test.ts</files>
  <read_first>
    - bbj-vscode/src/language/bbj-document-builder.ts (debouncedCompile: textDocumentBeforeRequest, versionBeforeRequest, checkedText, stillCurrent, the verdict branch, the moved-on/cancelled early return, and its doc comment)
    - bbj-vscode/src/language/bbj-kept-check.ts (setKeptCheck, pruneContentChangesThrough, contentChangesSince)
    - bbj-vscode/src/language/bbj-document-validator.ts (composeOnSaveDiagnostics)
    - bbj-vscode/test/live-parse-interleaving.test.ts (the two "overlapping cycles for the same document" tests: how a held request is set up)
    - bbj-vscode/test/on-save-kept-errors.test.ts (harness)
  </read_first>
  <action>
Keep an on-save check's result when the text moved on while it ran, and let a newer check supersede an older one (D-01; CONTEXT discretion: "a newer save supersedes it").

1. Add `private readonly checkSequence = new Map<string, number>()` keyed like `cplDebounceTimers`. At the start of the timer callback, take the next sequence number for the key and store it; a cycle is superseded once the stored number differs from its own.
2. Under on-save (trigger read when the outcome is handled): a superseded cycle stores nothing and publishes nothing (the connection-wide clear for an `'unavailable'` outcome still runs, as today). A verdict whose document still holds the same text-document object is handled by the verdict branch even when its version moved on. Compute `seen` with `composeWithVerdict` against the checked text and version (`liveText: checkedText`, `liveVersion: versionBeforeRequest`), which gives exactly today's arguments when the text is still current. Store the verdict state and the kept check for `versionBeforeRequest`, prune the change log through that version, and publish `composeOnSaveDiagnostics` built from the latest Langium baseline, the live text and version, and `contentChangesSince(uri, versionBeforeRequest, liveVersion)`.
3. Under debounced, keep the version-based stale guard exactly as it is: a moved-on verdict or a cancelled request still returns without any state change or publish. Update `debouncedCompile`'s doc comment to describe both rules.
4. Tests in `on-save-kept-errors.test.ts` (write them first):
   - tracer: on-save; after the open check, hold the next parse; `save(uri)`; `change` to version 2 inserting a line at the top; resolve the held parse with an error on one-based line 2; flush; the published `BBj Parser` diagnostic is on zero-based line 2, and the next validation after another keystroke still shows it there.
   - supersession, newer first: save (held A), change, save again (held B); resolve B with error X, then A with error Y; only X is ever published or kept.
   - supersession, older first: same setup, resolve A first; A publishes nothing and stores nothing; B then publishes X.
   - debounced regression: a verdict held while the text moves on is still dropped (no publish, no stored verdict or kept check change).
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/on-save-kept-errors.test.ts test/live-parse-interleaving.test.ts test/on-save-trigger.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "checkSequence" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-document-builder.ts` prints 3 or more
    - The four new tests pass, and every test in `live-parse-interleaving.test.ts` passes unchanged
  </acceptance_criteria>
  <done>Under on-save, a save's verdict is kept and correctly placed even when the user typed while it ran, a newer save always wins, and debounced keeps its stale-verdict drop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bbjcpl fallback results are kept until the next save and follow their lines</name>
  <files>bbj-vscode/src/language/bbj-document-builder.ts, bbj-vscode/src/language/bbj-document-validator.ts, bbj-vscode/test/on-save-kept-errors.test.ts, bbj-vscode/test/bbj-cpl-fallback-dedup.test.ts</files>
  <read_first>
    - bbj-vscode/src/language/bbj-document-builder.ts (fallback branch from plan 04: forgetVerdict, compile, baseline, base, checkedTextIsOnDisk, reconcileWithFallbackCheck)
    - bbj-vscode/src/language/bbj-document-validator.ts (composeOnSaveDiagnostics, fallback kind: hierarchy first, no hierarchy after)
    - bbj-vscode/src/language/bbj-kept-check.ts (composeWithKeptCheck: fallback kind never downgrades)
    - bbj-vscode/test/bbj-cpl-fallback-dedup.test.ts (plan 04's on-save tracer test, which must stay green)
  </read_first>
  <behavior>
    - On-save save with bbjcpl reporting the first flagged line: after inserting a line at the top, validation shows the BBjCPL diagnostic on the shifted line, the Langium complaint on that line stays gone, and the complaint on the second flagged line is still an Error with no downgraded code
    - Editing the first flagged line to different invalid text: the BBjCPL diagnostic stays and a Langium Error for the new text shows on the same line
    - A fallback compile held while the user types after the save: when it resolves, its diagnostic is kept and published on the shifted line
    - A later save whose compile returns [] removes the previously kept BBjCPL diagnostic
    - An on-save open whose checked text is not on disk (readFile resolves different text): the kept BBjCPL diagnostic shows and every Langium diagnostic stays, none dropped or downgraded
    - Under debounced, the published list for a fallback cycle is exactly what plan 04 publishes (base, reconcileWithFallbackCheck, or mergeDiagnostics), and plan 04's tests pass unchanged
  </behavior>
  <action>
Keep fallback results like verdicts under on-save (D-01 either source), with the fallback's dedup rules (D-09, D-10, D-11).

1. Fallback branch staleness: under on-save, replace the pre-compile "text moved on" return with the supersession check plus the same-text-document-object check (a save's check completes even when the user typed after the save); under debounced keep the existing `!stillCurrent` return.
2. After the compile, compute `seen`: `reconcileWithFallbackCheck(base, cplDiags, textLineLookup(baseline.validatedText ?? checkedText), textLineLookup(checkedText)).seen` when bbjcpl reported something and `checkedTextIsOnDisk` is true, otherwise an empty set. Store `setKeptCheck(uri, { kind: 'fallback', version: versionBeforeRequest, diagnostics: cplDiags, seen })` in every mode except off (an empty `cplDiags` stores an empty kept check, which clears the previous save's errors), and prune the change log through that version. Under on-save, publish `composeOnSaveDiagnostics` from the latest baseline, the live text and version, and `contentChangesSince(uri, versionBeforeRequest, liveVersion)`; under debounced, publish exactly what plan 04's branch publishes. A superseded on-save fallback cycle stores and publishes nothing.
3. Confirm `composeOnSaveDiagnostics`' fallback path (hierarchy on the Langium list first, kept BBjCPL diagnostics appended after, never through Rule 0) and fix it if a test shows otherwise.
4. Add the behavior cases (tests first) to `on-save-kept-errors.test.ts`, and extend `bbj-cpl-fallback-dedup.test.ts` with an assertion that after plan 04's on-save tracer the stored kept check has kind `'fallback'` and a non-empty `seen`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/on-save-kept-errors.test.ts test/bbj-cpl-fallback-dedup.test.ts test/live-parse-interleaving.test.ts test/document-builder.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "kind: 'fallback'" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-document-builder.ts` prints 1 or more
    - Every behavior case is its own passing `test(...)`; plan 04's tests pass unchanged
  </acceptance_criteria>
  <done>Under on-save, bbjcpl fallback errors stay until the next save on the lines they belong to, show once per finding when the checked text is on disk, and never suppress anything when it is not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mode switches keep current errors, off and close forget them, and the whole suite is green</name>
  <files>bbj-vscode/src/language/bbj-kept-check.ts, bbj-vscode/src/language/bbj-document-validator.ts, bbj-vscode/src/language/bbj-document-builder.ts, bbj-vscode/test/on-save-kept-errors.test.ts</files>
  <read_first>
    - bbj-vscode/src/language/bbj-kept-check.ts (KeptCheck)
    - bbj-vscode/src/language/bbj-document-validator.ts (validateDocument's on-save branch and the debounced verdict path; constructor close subscription)
    - bbj-vscode/src/language/bbj-document-builder.ts (every setKeptCheck call; runBbjcplForDocuments off branch and the mode-switch rule from plan 01)
    - .planning/phases/106-on-save-compiler-check-in-both-ides/106-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - Debounced verdict, then switch to on-save, then type: the BBj error stays on its (shifted) line until the next save
    - Debounced bbjcpl fallback result, then switch to on-save, then type: the BBjCPL error stays until the next save
    - On-save kept errors, then switch to debounced: a rebuild (the settings-change reload) keeps them; the next edit keeps them in validation until its debounced check result arrives, which replaces them; after that, typing behaves exactly as steady-state debounced
    - Steady-state debounced (no switch): typing after a verdict never shows the verdict's diagnostics on the next validation, as before
    - Switch to off: runBbjcplForDocuments clears every kept check and change log, and validation shows Langium diagnostics only
    - Close: the document's kept check and change log are gone; reopening under on-save runs one open check
    - Order independence: verdict first then Langium validation of newer text, and Langium first then the held verdict, end with deep-equal document.diagnostics
  </behavior>
  <action>
Make runtime switches keep current results without changing steady-state debounced (D-08, TRIG-05).

1. Add `storedUnderOnSave: boolean` to `KeptCheck` (doc comment: whether the trigger was on-save when this check was stored). Set it at every `setKeptCheck` call in the builder from the trigger at store time.
2. In `validateDocument`, use the kept-check composition when the trigger is on-save and a kept check exists (as now), and also when the trigger is debounced and the kept check has `storedUnderOnSave === true`: this keeps an on-save file's current errors after a switch to debounced until that file's first debounced check stores a fresh kept check with the flag false. Otherwise keep the existing verdict path byte-for-byte. Off stays untouched.
3. Add the behavior cases (tests first) to `on-save-kept-errors.test.ts`. Use a fresh harness per test.
4. Run the whole suite and judge it on `numFailedTests`, as the verify command below does. For any failing test name, run it on the phase base as described in the executor rules and list each name with its base result in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/on-save-kept-errors.test.ts test/on-save-trigger.test.ts test/bbj-kept-check.test.ts test/bbj-cpl-fallback-dedup.test.ts test/bbj-document-validator.test.ts test/live-parse-interleaving.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-106-06-vitest.json; node -e "const r=require('/tmp/phase-106-06-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-106-06-vitest.json</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 1427d059 -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bTRIG-0[1-7]\b|\bDIAG-01\b|\bJINT-03\b|\b106-0[1-7]\b'</automated>
    <fails_when>any added line is printed (a planning identifier reached source or tests), or git exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "storedUnderOnSave" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-document-validator.ts` prints 1 or more
    - Every behavior case is its own passing `test(...)`, including the steady-state debounced regression
    - The whole-suite run prints `numFailedTests=0` with a non-zero total
    - The register check prints nothing
  </acceptance_criteria>
  <done>Switching modes keeps each file's current compiler errors, off and close forget them, steady-state debounced is unchanged, and the whole vitest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interop socket / bbjcpl process → language server | a check result may arrive after the editor text changed, or after a newer check started |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-15 | Tampering (integrity of shown errors) | on-save keep-moved-on-result branch | medium | mitigate | A result is kept only for the same text-document object and only when no newer check started (`checkSequence`); its diagnostics are placed through the recorded change chain from its own version, and `seen` is computed against the text the check actually saw. Pinned by the in-flight and both supersession-order tests. |
| T-106-16 | Tampering (integrity of shown errors) | kept bbjcpl fallback | medium | mitigate | `seen` is empty unless `checkedTextIsOnDisk` proves bbjcpl compiled the checked text, so a kept fallback result for other text never suppresses a Langium diagnostic. Pinned by the not-on-disk test. |
| T-106-17 | Denial of service | superseded checks | low | accept | A superseded cycle does no further work after its request returns; at most one cycle per save is started, as in plan 01. |
| T-106-SC | Tampering | package installs | low | accept | No package install in this plan. |
</threat_model>

<verification>
- The six-file targeted vitest command exits 0
- Whole suite: `numFailedTests=0` with `RUN_BBJ_TESTS=0 --maxWorkers=2`
- Register check prints nothing
</verification>

<success_criteria>
- On-save keeps a save's results (either source) across typing, correctly placed, until the next save; newer saves win
- Debounced keeps its stale-verdict drop and steady-state behaviour
- Mode switches keep current results; off and close forget them
- Whole vitest suite green
</success_criteria>

<output>
Create `.planning/phases/106-on-save-compiler-check-in-both-ides/106-06-SUMMARY.md` when done
</output>
