---
phase: 106-on-save-compiler-check-in-both-ides
plan: 07
type: execute
wave: 5
depends_on: ["106-02", "106-03", "106-06"]
files_modified:
  - .planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md
autonomous: false
requirements: [TRIG-02, TRIG-06, JINT-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A VSIX and an IntelliJ zip built from the final tree carry the same language-server bundle (the zip's main.cjs is byte-identical to bbj-vscode/out/language/main.cjs)"
    - "The whole vitest suite reports numFailedTests=0 (RUN_BBJ_TESTS=0, --maxWorkers=2) and the IntelliJ JUnit suite passes with --rerun-tasks"
    - "In a running VS Code and a running IntelliJ with on-save: opening a file checks it once, typing checks nothing, each save checks once with no delay, and the last errors stay on their lines while typing until the next save (TRIG-01..04 in both IDEs)"
    - "In IntelliJ the Compiler check setting is shown under BBj Compiler with its hint, and the chosen mode applies after Apply and after an IDE restart (TRIG-06)"
    - "With the live parse unavailable, a finding reported by bbjcpl shows once per line in both IDEs (DIAG-01)"
    - "A few Phase 105 'after' samples re-taken on the real large workspace show the first live diagnostic in about 5-6 s in each IDE (criterion 5, JINT-03)"
  artifacts:
    - path: ".planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md"
      provides: "metric, environment, runbook reference, re-check results and verdict"
      contains: "## Results"
  key_links:
    - from: "106-MEASUREMENT.md Results"
      to: "105-MEASUREMENT.md Results (5.3 s VS Code, 6 s IntelliJ after)"
      via: "same metric and runbook, re-taken on the final 106 build"
      pattern: "105"
  prohibitions:
    - requirement_id: JINT-03
      category: privacy
      statement: "MUST NOT record corpus file names, program names or source text in 106-MEASUREMENT.md or any other planning, test or commit text; numbers and environment notes only"
      flagged: true
  assumptions:
    - statement: "LSP4IJ 0.21.0 sends textDocument/didSave once the server advertises save (research Assumption A1, cited, not read from LSP4IJ source); UAT step 11 confirms it in a running IntelliJ"
      source: "106-RESEARCH.md Assumptions Log A1"
      flagged: true
---

<objective>
Prove the phase in running IDEs: build both distributables from the final tree, run the whole
suites, hand-test on-save, the IntelliJ setting and the fallback dedup in VS Code and IntelliJ, and
re-check the Phase 105 timing now that the live parse no longer waits on the shared connection.

Purpose: ROADMAP success criteria 2, 4 and 5 need real clients (didSave from both IDEs, the
IntelliJ restart path, the large-workspace timing). No CI covers IntelliJ UI or LSP4IJ.
Output: `/tmp/phase-106-uat/` with the VSIX and zip, `106-MEASUREMENT.md`, and the tester's approval.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-CONTEXT.md
@.planning/milestones/v4.5-phases/105-live-diagnostics-responsiveness-on-large-workspaces/105-MEASUREMENT.md
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` forms are `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`
  and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`. Use
  `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>`
  only, never `-A` or `.`. Never use `git stash`.
- Planning, test and commit text must not contain planning identifiers where the source rules
  forbid them; `106-MEASUREMENT.md` itself is a planning file and may name requirements.
- No corpus file names, program names, paths inside the corpus, or source text in
  `106-MEASUREMENT.md`, commits or the SUMMARY (105 D-11): numbers and environment notes only.
- A runtime sequence used as evidence (for example "one check per save") must come from a real
  log (the VS Code server trace or IntelliJ's `idea.log`), never a hand-derived trace.
- If code-review fixes land after this plan, rebuild both distributables with Task 1's commands
  and repeat the UAT steps the fixes touch before the phase is verified.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md or another ledger.

## Artifacts this plan produces

- `/tmp/phase-106-uat/bbj-lang.vsix` and `/tmp/phase-106-uat/bbj-intellij-<version>.zip` with
  their SHA-256 values (outside the repository).
- `.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md` (Metric,
  Environment, Runbook, Results, Hand-verification results).

<tasks>

<task type="tracer">
  <name>Task 1: Build both distributables from the final tree, run both whole suites, and prepare the measurement record</name>
  <precondition>Plans 106-01 to 106-06 have SUMMARY.md files, and BBjServices answers on port 5008 (`bash -c 'exec 3<>/dev/tcp/127.0.0.1/5008'` exits 0) for the live-parse UAT steps.</precondition>
  <files>.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md</files>
  <read_first>
    - .planning/milestones/v4.5-phases/105-live-diagnostics-responsiveness-on-large-workspaces/105-MEASUREMENT.md (Metric, Environment, Runbook and Results sections to mirror)
    - .planning/phases/106-on-save-compiler-check-in-both-ides/106-06-SUMMARY.md (whole-suite result and any base comparisons)
    - /home/coder/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/devcontainer-bbj-setup.md (BBjServices on :5008, ext-test VS Code on :13338)
  </read_first>
  <action>
Produce the exact builds the tester will use, and gate them on both whole suites.

1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (packages this clone into `/tmp/bbj-lang.vsix` and installs it into the ext-test code-server profile on port 13338), then `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --console=plain -q` (use `clean buildPlugin` if Gradle reports the zip up to date with an old timestamp). Prove the zip carries the fresh server with `unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs` (adjust the zip name if the version differs). Copy the VSIX and the zip into `/tmp/phase-106-uat/` and record `sha256sum` of both.
2. Whole suites: the vitest command in the verify block (judged on `numFailedTests`), and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain`.
3. Write `106-MEASUREMENT.md` with: **Metric** (the same as 105: time from the `textDocument/didChange` that introduces an invalid line, in a file opened while the initial build is still running, to the first `textDocument/publishDiagnostics` for that file carrying a `BBj Parser` diagnostic; taken with the trigger at `debounced`, the default, since typing starts no check under on-save); **Environment** (BBj build identifier and the shipped `bbj-ls.jar` size and date on this container, the final commit hash, both SHA-256 values, Node version, CPU and memory; a slot for the tester's machine and IDE versions); **Runbook** (a reference to 105-MEASUREMENT.md's Runbook section, used unchanged, with the "after" build only); **Results** (an empty table: IDE, Sample 1-3 in seconds, Median, 105 median for comparison: 5.3 s VS Code, 6 s IntelliJ); **Hand-verification results** (empty, filled after the checkpoint). No corpus names.
  </action>
  <verify>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs && ls /tmp/phase-106-uat/*.vsix /tmp/phase-106-uat/*.zip</automated>
    <fails_when>cmp prints a difference or exits non-zero, or ls reports either file missing</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-106-07-vitest.json; node -e "const r=require('/tmp/phase-106-07-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-106-07-vitest.json</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain</automated>
    <fails_when>non-zero exit or "BUILD FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `/tmp/phase-106-uat/` holds one `.vsix` and one `.zip`, and the cmp check prints nothing
    - Whole vitest suite prints `numFailedTests=0`; the Gradle test run passes
    - `grep -c -E '^## (Metric|Environment|Runbook|Results|Hand-verification results)' /home/coder/repos/bbj-language-server/.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md` prints 5
    - `grep -c -E '[A-Za-z0-9_-]+\.bbj\b' /home/coder/repos/bbj-language-server/.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md` prints 0
  </acceptance_criteria>
  <done>Both distributables are built from the final tree with the same server bundle, both whole suites are green, and the measurement record is ready for the tester's numbers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hand UAT in VS Code and IntelliJ, and the Phase 105 timing re-check</name>
  <read_first>
    - .planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md (Runbook reference and empty Results table)
    - .planning/milestones/v4.5-phases/105-live-diagnostics-responsiveness-on-large-workspaces/105-MEASUREMENT.md (Runbook)
  </read_first>
  <what-built>On-save compiler checks in the language server (save capability, save/open-only checks, kept errors that follow their lines, one error per finding on the bbjcpl fallback, the live parse on its own connection), the IntelliJ "Compiler check:" setting, and the rewritten setting description and docs. Builds: `/tmp/phase-106-uat/bbj-lang.vsix` (also installed in the ext-test VS Code on port 13338) and `/tmp/phase-106-uat/bbj-intellij-<version>.zip`.</what-built>
  <how-to-verify>
    VS Code (install the VSIX; set `"bbj.compiler.trigger": "on-save"`, `"files.autoSave": "off"`, and `"bbj.trace.server": "verbose"` so the trace shows each `parseProgram`/bbjcpl run):
    1. Open a BBj file that has a syntax error: its compiler error (source `BBj Parser`) appears without saving.
    2. On another line, type a new invalid statement: no new compiler diagnostic appears and the trace shows no check; the language server's own error for the new line appears.
    3. Insert three lines above the compiler error: the error moves down with its line.
    4. Fix the erroneous line without saving: the compiler error stays. Press Ctrl+S: one check runs at once and the error is gone.
    5. Reintroduce an error, save, then delete that whole line: the compiler error disappears.
    6. Press Ctrl+S with no change: exactly one check runs (trace).
    7. Switch the setting to `debounced`: no check runs for the open files at the switch; typing then checks about 500 ms after you stop, as before. Switch to `off`: the compiler errors disappear.
    8. Set `files.autoSave` to `afterDelay` with on-save: each auto-save runs one check.
    9. Stop BBjServices (live parse unavailable), set on-save, save a file with a syntax error: that line shows one error, bbjcpl's own message with source `BBjCPL`, and errors on other lines are still shown. Restart BBjServices afterwards.
    10. Hover `bbj.compiler.trigger` in the Settings UI: the three descriptions match what you saw, and on-save is recommended for large workspaces.
    IntelliJ (install the zip from disk):
    11. Settings, Languages & Frameworks > BBj: under "BBj Compiler", a "Compiler check:" dropdown (Debounced / On save / Off, default Debounced) with the large-workspace hint. Choose On save and Apply: the language server restarts. Repeat steps 1-5 in IntelliJ; in `idea.log` (with the plugin's Debug log level) confirm the server received the save and ran one check per save.
    12. Reopen Settings: On save is still selected. Restart the IDE: on-save behaviour holds from startup.
    13. Read the Live Compiler Diagnostics sections of both feature docs (`documentation/docs/vscode/features.md`, `documentation/docs/intellij/features.md`): they describe the three modes as observed.
    Timing re-check (criterion 5), setting at `debounced` in both IDEs: follow 105-MEASUREMENT.md's Runbook with this build on the real large workspace and take three samples per IDE. Expect about 5-6 s (105: 5.3 s VS Code, 6 s IntelliJ).
  </how-to-verify>
  <acceptance_criteria>
    - The tester replies "approved" with six timing samples (three per IDE, in seconds) and the tester's machine and IDE versions, or lists failing step numbers with what was seen
    - Every runtime claim in the reply that is used as evidence comes from the VS Code trace or `idea.log`, not from memory
  </acceptance_criteria>
  <resume-signal>Type "approved" followed by the six timing samples (VS Code s1, s2, s3; IntelliJ s1, s2, s3) and your machine/IDE versions, or list the failing step numbers and what you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the re-check results and hand-verification outcome</name>
  <files>.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md
    - the tester's resume message from Task 2
  </read_first>
  <action>
Fill `106-MEASUREMENT.md` from the tester's reply: the Results table (three samples per IDE, the median of each, the 105 median beside it) and one verdict sentence per IDE (whether the first live diagnostic still arrives in about 5-6 s); the tester's machine and IDE versions in Environment; and in Hand-verification results, one line per UAT step (1-13) with pass or the tester's own observation. Numbers and environment notes only: no corpus file names, program names or source text. If the tester reported a failing step, record it as failing and stop: the orchestrator routes it to gap closure.
  </action>
  <verify>
    <automated>test "$(grep -c -E '^\| (VS Code|IntelliJ) ' /home/coder/repos/bbj-language-server/.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md)" -ge 2 && test "$(grep -c -E '[A-Za-z0-9_-]+\.bbj\b' /home/coder/repos/bbj-language-server/.planning/phases/106-on-save-compiler-check-in-both-ides/106-MEASUREMENT.md)" = 0 && echo measurement-ok</automated>
    <fails_when>"measurement-ok" is not printed (a Results row per IDE is missing, or a .bbj file name appears)</fails_when>
  </verify>
  <acceptance_criteria>
    - The Results table has a row for VS Code and a row for IntelliJ with three samples and a median each
    - Hand-verification results has one line per UAT step 1-13
    - No `.bbj` file name appears in the file
  </acceptance_criteria>
  <done>The re-check numbers, the verdict against Phase 105's timing, and the per-step UAT outcome are recorded with numbers and environment notes only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private large workspace → planning record | measurements are taken on proprietary code and written into a repository file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-18 | Information disclosure | `106-MEASUREMENT.md`, SUMMARY, commit messages | medium | mitigate | Numbers and environment notes only; acceptance greps for `.bbj` file names (0 allowed); the executor rules forbid program names, corpus paths and source text. |
| T-106-19 | Repudiation | UAT evidence | low | mitigate | Runtime claims come from the VS Code trace or `idea.log`, not hand-derived sequences (v4.4 standing decision). |
| T-106-SC | Tampering | package installs | low | accept | No package install; the builds use the existing lockfiles. |
</threat_model>

<verification>
- `/tmp/phase-106-uat/` holds the VSIX and zip; the zip's server bundle equals `bbj-vscode/out/language/main.cjs`
- Whole vitest suite `numFailedTests=0`; Gradle test green with `--rerun-tasks`
- Tester approval with timing samples recorded in `106-MEASUREMENT.md`
</verification>

<success_criteria>
- On-save behaves as specified in both running IDEs, including the IntelliJ setting and the fallback dedup
- The first live diagnostic on the large workspace still arrives in about 5-6 s in both IDEs
- Measurement record contains numbers and environment notes only
</success_criteria>

<output>
Create `.planning/phases/106-on-save-compiler-check-in-both-ides/106-07-SUMMARY.md` when done
</output>
