---
phase: 106-on-save-compiler-check-in-both-ides
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsConfigurable.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerInitOptionsTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java
  - bbj-vscode/package.json
  - documentation/docs/vscode/features.md
  - documentation/docs/intellij/features.md
autonomous: true
requirements: [TRIG-06, TRIG-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The IntelliJ trigger reaches the language server as the flat initializationOptions key compilerTrigger, the key bbj-ws-manager.ts already reads for VS Code, from startup and after every settings Apply through the existing debounced restart; there is no didChangeConfiguration path for IntelliJ (D-12)"
    - "An IntelliJ user can choose Debounced, On save or Off in a 'Compiler check:' dropdown under the existing BBj Compiler separator, next to Compile output directory, with a one-line hint below it recommending On save for large workspaces; the default is Debounced (D-13)"
    - "A missing, blank or unknown persisted IntelliJ value is sent to the server as debounced"
    - "The VS Code setting description and its three enumDescriptions describe the modes as implemented: on-save checks only on open and save and keeps the last errors until the next save, off means no compiler checks at all, and on-save is recommended for large workspaces instead of the old slow-completion advice (D-14)"
    - "Both feature docs describe the three modes as implemented, recommend on-save for large workspaces, describe off plainly, and carry one line saying automatic saves count as saves (D-07, D-14)"
    - "The IntelliJ feature doc names the new Compiler check setting and no longer says the plugin lacks the setting (D-14)"
    - "Normalizing a trigger value twice gives the same value, and after Apply isModified() reports false for the stored value (edge: idempotency of TRIG-06)"
    - statement: "Several Apply clicks in a row coalesce into one language-server restart that carries the last chosen trigger (edge: concurrency of TRIG-06; existing scheduleRestart debounce, checked in hand UAT)"
      verification: backstop
    - statement: "package.json, the VS Code feature doc and the IntelliJ feature doc describe each mode the same way; no two descriptions of one mode contradict (edge: idempotency of TRIG-07)"
      verification: backstop
    - "Each feature doc says in one line that automatic saves (VS Code files.autoSave afterDelay; IntelliJ's own automatic saves) count as saves, so on-save then checks about as often as debounced (edge: concurrency of TRIG-07, D-07)"
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java"
      provides: "COMPILER_TRIGGER_KEY, wire values, display names, normalizeTrigger and display-name mapping"
      contains: "COMPILER_TRIGGER_KEY = \"compilerTrigger\""
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java"
      provides: "persisted compilerTrigger field, default debounced"
      contains: "public String compilerTrigger = \"debounced\""
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java"
      provides: "adds compilerTrigger to initializationOptions"
      contains: "CompilerInitOptions.COMPILER_TRIGGER_KEY"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java"
      provides: "Compiler check dropdown and hint label"
      contains: "Compiler check:"
    - path: "bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java"
      provides: "source guard over the five wiring sites"
    - path: "bbj-vscode/package.json"
      provides: "bbj.compiler.trigger description and enumDescriptions"
    - path: "documentation/docs/vscode/features.md"
      provides: "Live Compiler Diagnostics section, three modes"
    - path: "documentation/docs/intellij/features.md"
      provides: "Live Compiler Diagnostics section, Compiler check setting"
  key_links:
    - from: "BbjSettingsConfigurable.apply()"
      to: "BbjServerService.scheduleRestart()"
      via: "state.compilerTrigger stored before the restart that re-sends initialization options"
      pattern: "state\\.compilerTrigger = myComponent\\.getCompilerTrigger\\(\\)"
    - from: "BbjLanguageServerFactory initializeParams"
      to: "bbj-ws-manager.ts onInitialize (initializationOptions.compilerTrigger allow-list)"
      via: "flat key compilerTrigger"
      pattern: "COMPILER_TRIGGER_KEY"
  prohibitions:
    - requirement_id: TRIG-07
      category: values
      statement: "MUST NOT change the default compiler trigger: bbj.compiler.trigger stays default debounced in package.json, and the IntelliJ setting defaults to Debounced (REQUIREMENTS Out of Scope)"
      flagged: true
---

<objective>
Give IntelliJ users the compiler-trigger setting and make the VS Code setting text and both feature
docs describe the three modes as they now behave.

Purpose: TRIG-06 and TRIG-07. The server already reads `initializationOptions.compilerTrigger`
(VS Code sends it); IntelliJ sends nothing today, and the docs still describe on-save as a
placeholder and off as a slow-completion fix.
Output: an IntelliJ "Compiler check:" dropdown wired through `CompilerInitOptions` and the server
factory, JUnit coverage, a rewritten `bbj.compiler.trigger` description, and two updated docs.
</objective>

<assumption_delta_decision>
Detector signal: `chosen` ("an IntelliJ user can choose debounced, on-save or off").
Primary noun: the server's compiler trigger mode (`getCompilerTrigger()`, the three values
`debounced` / `on-save` / `off`), already delivered by VS Code as `initializationOptions.compilerTrigger`.
Decision: no-change. The generalization (a per-user choice of mode) already exists on the server;
IntelliJ stores and sends the same wire value through the same key, so no identity or model moves.
Invariant already covered: `CompilerInitOptionsTest` pins the key name and that only the server's
three allowed values are ever sent.
</assumption_delta_decision>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-CONTEXT.md
@.planning/phases/106-on-save-compiler-check-in-both-ides/106-PATTERNS.md
@bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java
@bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerOutputDirectorySourceGuardTest.java
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` forms are `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`
  and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`. Use
  `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>`
  only, never `-A` or `.`. Never use `git stash`. Prefer the Read tool over shell reads.
- Source, test and doc text must not contain planning identifiers: no decision numbers of the
  form D-NN, no requirement ids (the TRIG, DIAG and JINT families), no plan numbers (106-0N), no
  review ids (CR-NN, WR-NN). GitHub issue numbers such as #571 and #696 are fine.
- IntelliJ tests are plain JUnit 5 with no IntelliJ platform dependency, in the style of
  `CompilerInitOptionsTest` and `CompilerOutputDirectorySourceGuardTest`. Run Gradle with
  `--rerun-tasks` so a stale UP-TO-DATE result cannot hide a failure.
- The plugin UI must not reference `BbjSettings.` inside `BbjSettingsComponent` (an existing source
  guard forbids it); read display names from `CompilerInitOptions`.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md or another ledger.

## Artifacts this plan produces

- `CompilerInitOptions` (plain Java, no IntelliJ import): `COMPILER_TRIGGER_KEY = "compilerTrigger"`,
  `TRIGGER_DEBOUNCED = "debounced"`, `TRIGGER_ON_SAVE = "on-save"`, `TRIGGER_OFF = "off"`,
  `TRIGGER_DISPLAY_NAMES` (`List.of("Debounced", "On save", "Off")`), `normalizeTrigger(String)`,
  `triggerDisplayName(String wireValue)`, `triggerFromDisplayName(String displayName)`.
- `BbjSettings.State.compilerTrigger` (persisted, default `"debounced"`).
- `BbjLanguageServerFactory` initializeParams: `options.addProperty(CompilerInitOptions.COMPILER_TRIGGER_KEY, …)`.
- `BbjSettingsComponent`: fields `compilerTriggerCombo` and `compilerTriggerHintLabel`, accessors
  `getCompilerTrigger()` / `setCompilerTrigger(String wireValue)`.
- `BbjSettingsConfigurable`: `isModified` / `apply` / `reset` wiring for `compilerTrigger`.
- New test `bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java`;
  extended `CompilerInitOptionsTest`.
- `bbj-vscode/package.json` `bbj.compiler.trigger` description and enumDescriptions (enum and
  default unchanged).
- `documentation/docs/vscode/features.md` and `documentation/docs/intellij/features.md` Live
  Compiler Diagnostics sections.

<tasks>

<task type="tracer">
  <name>Task 1: The IntelliJ trigger setting reaches the language server through initialization options</name>
  <reversibility rating="costly">The persisted setting name, its three wire values and the init-option key form a contract between saved IntelliJ settings, the plugin and the server; renaming any of them later needs a coordinated change on both sides and drops users' saved choice.</reversibility>
  <precondition>`/home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs` exists; if it does not, run `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build` first (the Gradle build needs the bundled server).</precondition>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerInitOptionsTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java</files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java (whole file; its class comment wrongly says compilerTrigger already travels this way)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java (State fields, line 36)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java (createClientFeatures / initializeParams, lines 40-66)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerInitOptionsTest.java
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerOutputDirectorySourceGuardTest.java (readSource/countOccurrences helpers and the ordering assertion)
    - bbj-vscode/src/language/bbj-ws-manager.ts (lines 106-114: the server's allow-list for compilerTrigger)
  </read_first>
  <action>
Send the IntelliJ trigger as the flat init option the server already reads (D-12), following the #571 `compilerOutputDirectory` path exactly.

1. `CompilerInitOptions.java`: add `COMPILER_TRIGGER_KEY = "compilerTrigger"`, the three wire constants (`TRIGGER_DEBOUNCED`, `TRIGGER_ON_SAVE`, `TRIGGER_OFF`), `TRIGGER_DISPLAY_NAMES = List.of("Debounced", "On save", "Off")` (dropdown order), `normalizeTrigger(String raw)` (trims; returns the raw value when it is exactly one of the three wire values, otherwise `"debounced"`, including for null and blank), `triggerDisplayName(String wireValue)` (display name for the normalized wire value) and `triggerFromDisplayName(String displayName)` (wire value for a display name, `"debounced"` for anything else). Keep the class free of IntelliJ imports. Fix the class comment: it currently claims the trigger already travels this way; say instead that both the output directory and the trigger travel as flat init options, and that a settings Apply restarts the server, which re-sends them.
2. `BbjSettings.State`: add `public String compilerTrigger = "debounced";` with a trailing comment in the style of its neighbours (default debounced; one of debounced, on-save, off).
3. `BbjLanguageServerFactory.initializeParams`: add `options.addProperty(CompilerInitOptions.COMPILER_TRIGGER_KEY, CompilerInitOptions.normalizeTrigger(state.compilerTrigger));` next to the output-directory property and before `params.setInitializationOptions(options)`. Extend the existing flat-key comment to cover both keys.
4. `CompilerInitOptionsTest`: add tests — the key equals `"compilerTrigger"`; each wire value normalizes to itself; null, empty, blank and an unknown value (for example `"sometimes"`) normalize to `"debounced"`; a padded wire value is trimmed; normalizing twice is stable; display names round-trip through `triggerFromDisplayName(triggerDisplayName(v))` for all three wire values; `TRIGGER_DISPLAY_NAMES` is exactly Debounced, On save, Off in that order.
5. New `CompilerTriggerSourceGuardTest` (copy the helpers from `CompilerOutputDirectorySourceGuardTest`): `BbjSettings` declares `public String compilerTrigger = "debounced"` exactly once; `BbjLanguageServerFactory` uses `CompilerInitOptions.COMPILER_TRIGGER_KEY` exactly once and before `params.setInitializationOptions(options)`; `BbjLanguageClient` never mentions `compilerTrigger` (LSP4IJ resolves this plugin's flat client settings to null, #571); `CompilerInitOptions` has no `import com.intellij`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --tests 'com.basis.bbj.intellij.lsp.CompilerInitOptionsTest' --tests 'com.basis.bbj.intellij.lsp.CompilerTriggerSourceGuardTest' --tests 'com.basis.bbj.intellij.lsp.CompilerOutputDirectorySourceGuardTest' --console=plain</automated>
    <fails_when>non-zero exit, "BUILD FAILED" in the output, or "No tests found for given includes"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'COMPILER_TRIGGER_KEY = "compilerTrigger"' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java` prints 1
    - `grep -c 'public String compilerTrigger = "debounced"' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettings.java` prints 1
    - `grep -c 'CompilerInitOptions.COMPILER_TRIGGER_KEY' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java` prints 1
    - `grep -c 'import com.intellij' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/CompilerInitOptions.java` prints 0
    - The three-class Gradle run passes
  </acceptance_criteria>
  <done>The persisted IntelliJ trigger, normalized to one of the three values the server accepts, is sent as `initializationOptions.compilerTrigger` on every server start, and plain JUnit tests pin the value mapping and the wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntelliJ users choose the trigger in a "Compiler check:" dropdown</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsConfigurable.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java</files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java (fields lines 40-60, logLevelCombo construction lines 152-156, javaInteropPortHintLabel lines 211-215, form layout lines 289-320, output-directory accessors lines 520-527)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsConfigurable.java (isModified, apply, reset)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjSettingsComponentSourceGuardTest.java (tokens the component must not contain)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/CompilerTriggerSourceGuardTest.java (from Task 1)
  </read_first>
  <behavior>
    - The component source declares compilerTriggerCombo, builds it from CompilerInitOptions.TRIGGER_DISPLAY_NAMES, and lays out exactly one "Compiler check:" row plus the hint label directly after the "Compile output directory:" row and before the Node.js Runtime separator
    - The configurable reads getCompilerTrigger() in isModified and apply (2 occurrences), calls setCompilerTrigger( once in reset, and assigns state.compilerTrigger before scheduleRestart()
  </behavior>
  <action>
Add the dropdown the user sees (D-13) and carry its value through isModified, apply and reset (D-12).

1. `BbjSettingsComponent`: add `private final ComboBox<String> compilerTriggerCombo;` built with `new CollectionComboBoxModel<>(CompilerInitOptions.TRIGGER_DISPLAY_NAMES)` and "Debounced" selected, and `private final JBLabel compilerTriggerHintLabel` with the text "On save is recommended for large workspaces." styled like the existing `javaInteropPortHintLabel` (a plain `JBLabel` row). In the form builder, directly after the "Compile output directory:" row under the "BBj Compiler" separator, add `.addLabeledComponent(new JBLabel("Compiler check:"), compilerTriggerCombo, 1, false)` and then `.addComponent(compilerTriggerHintLabel)`. Add `public @NotNull String getCompilerTrigger()` (returns `CompilerInitOptions.triggerFromDisplayName` of the selected item) and `public void setCompilerTrigger(@NotNull String wireValue)` (selects `CompilerInitOptions.triggerDisplayName(wireValue)`). Do not reference `BbjSettings.` in this class.
2. `BbjSettingsConfigurable`: in `isModified`, add `|| !Objects.equals(myComponent.getCompilerTrigger(), CompilerInitOptions.normalizeTrigger(state.compilerTrigger))`; in `apply`, add `state.compilerTrigger = myComponent.getCompilerTrigger();` next to the output-directory assignment (before the restart loop); in `reset`, add `myComponent.setCompilerTrigger(CompilerInitOptions.normalizeTrigger(state.compilerTrigger));` with a short comment. The existing debounced `scheduleRestart()` carries the change to the server; add no other restart or configuration push.
3. Extend `CompilerTriggerSourceGuardTest` with the two behavior assertions (write them first, see them fail, then implement steps 1-2). Use `indexOf` ordering checks for the row position and for assignment-before-restart, as the output-directory guard does.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain</automated>
    <fails_when>non-zero exit or "BUILD FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'new JBLabel("Compiler check:")' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java` prints 1
    - `grep -c 'On save is recommended for large workspaces' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java` prints 1
    - `grep -c 'getCompilerTrigger()' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsConfigurable.java` prints 2
    - `grep -c 'BbjSettings\.' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjSettingsComponent.java` prints 0
    - The whole IntelliJ JUnit suite passes with `--rerun-tasks`
  </acceptance_criteria>
  <done>The IntelliJ settings page shows a "Compiler check:" dropdown (Debounced / On save / Off, default Debounced) with the large-workspace hint, and Apply stores the choice before the restart that re-sends it.</done>
</task>

<task type="auto">
  <name>Task 3: The VS Code setting text and both feature docs describe the three modes as implemented</name>
  <files>bbj-vscode/package.json, documentation/docs/vscode/features.md, documentation/docs/intellij/features.md</files>
  <read_first>
    - bbj-vscode/package.json (the "bbj.compiler.trigger" block, lines 600-615)
    - documentation/docs/vscode/features.md (Live Compiler Diagnostics section, lines 50-74)
    - documentation/docs/intellij/features.md (Live Compiler Diagnostics section, lines 53-68)
    - documentation/docs/intellij/configuration.md (lines 10-18: the settings path Languages & Frameworks > BBj)
    - .planning/phases/106-on-save-compiler-check-in-both-ides/106-CONTEXT.md (D-01, D-02, D-07, D-12, D-14)
  </read_first>
  <action>
Rewrite the three user-facing texts so they match the behaviour (D-14) and mention auto-save once each (D-07). Plain, short sentences, no marketing tone, no planning identifiers. Keep `enum` and `"default": "debounced"` in `package.json` unchanged.

1. `bbj-vscode/package.json`, `bbj.compiler.trigger`:
   - `description`: controls when the BBj compiler checks the open file; with BBj 26.03 or later and BBjServices running the BBj parser checks the current text, otherwise BBjCPL compiles the saved file; the language server's own error checking runs in every mode; on-save is recommended for large workspaces. Drop the sentence that says checks run 500 ms after typing in general (that is only the debounced mode).
   - `enumDescriptions[0]` (debounced): default; checks about 500 ms after you stop typing, and when you open or save the file.
   - `enumDescriptions[1]` (on-save): checks only when you open or save the file; typing runs no compiler check; the last check's errors stay visible until the next save; recommended for large workspaces; with `files.autoSave` set to `afterDelay`, every auto-save counts as a save.
   - `enumDescriptions[2]` (off): no compiler checks at all, neither the live compiler check nor BBjCPL; the language server's own error checking keeps running. Remove the old advice about slow code completion.
2. `documentation/docs/vscode/features.md`, section "Live Compiler Diagnostics": keep the first paragraph. Replace the modes paragraph and list with: `bbj.compiler.trigger` controls when compiler checks run; `debounced` (default) checks about 500 ms after you stop typing and when you open or save a file (the delay is fixed); `on-save` checks only when you open or save a file, typing runs no compiler work, and the last check's errors stay visible on their lines while you edit until the next save replaces them, while lines you type or change are checked by the language server's own diagnostics; `off` runs no compiler checks at all, only the language server's own checks remain, and the Compile BBj Program command still works. Then one line: if `files.autoSave` is `afterDelay`, each auto-save counts as a save, so `on-save` then checks about as often as `debounced`. Keep the bbjcpl fallback paragraph and "Changing the setting takes effect immediately, without restarting." Replace the closing paragraph about slow completion with a recommendation to use `on-save` in large workspaces.
3. `documentation/docs/intellij/features.md`, section "Live Compiler Diagnostics": replace the paragraph saying the plugin has no timing setting with the **Compiler check** setting (Settings, `Languages & Frameworks > BBj`, under **BBj Compiler**) and its three choices, described exactly as in the VS Code doc (Debounced default, On save, Off), plus: applying a change restarts the language server, which then uses the new choice; On save is recommended for large workspaces; one line that IntelliJ's own automatic saves (for example when the IDE is idle or loses focus) count as saves. Delete the closing paragraph that says VS Code's setting has no counterpart in the plugin. Keep the bbjcpl fallback paragraph.
  </action>
  <verify>
    <automated>node -e "const p=require('/home/coder/repos/bbj-language-server/bbj-vscode/package.json').contributes.configuration; const all=[].concat(p).flatMap(c=>Object.entries(c.properties||{})); const t=Object.fromEntries(all)['bbj.compiler.trigger']; const ok=JSON.stringify(t.enum)==='[\"debounced\",\"on-save\",\"off\"]' && t.default==='debounced' && /open or save/.test(t.enumDescriptions[1]) && /large workspace/i.test(t.enumDescriptions[1]) && !/behaves the same/i.test(JSON.stringify(t)) && !/workaround/i.test(JSON.stringify(t)); console.log(ok?'trigger-setting-ok':'trigger-setting-BAD'); process.exit(ok?0:1)"</automated>
    <fails_when>non-zero exit, or "trigger-setting-BAD" printed, or a TypeError because the setting block is missing</fails_when>
    <automated>test "$(grep -c -i 'workaround\|behaves the same' /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md)" = 0 && test "$(grep -c 'no IntelliJ equivalent\|has no setting for when' /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md)" = 0 && test "$(grep -c 'Compiler check' /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md)" -ge 1 && test "$(grep -c 'files.autoSave' /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md)" -ge 1 && echo docs-ok</automated>
    <fails_when>non-zero exit, or "docs-ok" is not printed</fails_when>
  </verify>
  <acceptance_criteria>
    - The package.json check prints `trigger-setting-ok` (enum and default unchanged, on-save text mentions open or save and large workspaces, no old placeholder or slow-completion wording)
    - The docs check prints `docs-ok`
    - `grep -c -i 'large workspace' /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md` prints 1 or more, and the same for `documentation/docs/intellij/features.md`
    - `grep -c -i 'automatic saves\|auto-save' /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md` prints 1 or more
    - `git -C /home/coder/repos/bbj-language-server diff 1427d059 -- documentation/docs bbj-vscode/package.json | grep -E '^\+' | grep -c -E '\bD-[0-9]{2}\b|\bTRIG-0[1-7]\b|\bDIAG-01\b|\bJINT-03\b'` prints 0
  </acceptance_criteria>
  <done>The VS Code setting description and both feature docs describe debounced, on-save and off as implemented, recommend on-save for large workspaces, mention auto-save once each, and the IntelliJ doc names the new setting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ persisted settings (BbjSettings.xml) → plugin → language server initializationOptions | a user- or file-supplied string becomes the server's trigger mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-07 | Tampering | `BbjSettings.State.compilerTrigger` read from a hand-edited settings file | low | mitigate | `CompilerInitOptions.normalizeTrigger` sends only one of the three allowed values (anything else becomes debounced), and the server's existing literal allow-list in `bbj-ws-manager.ts` ignores anything else a second time. Pinned by `CompilerInitOptionsTest`. |
| T-106-08 | Repudiation | settings Apply without a restart | low | accept | Apply already schedules the debounced restart for every setting; the new field rides the same path, and the source guard pins assignment before `scheduleRestart()`. |
| T-106-SC | Tampering | package installs | low | accept | No package install in this plan. |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain` exits 0
- The package.json and docs checks print `trigger-setting-ok` and `docs-ok`
</verification>

<success_criteria>
- IntelliJ: Compiler check dropdown with the three choices, default Debounced, sent as `compilerTrigger` at every start
- VS Code description and both docs match the implemented behaviour and recommend on-save for large workspaces
- Default trigger unchanged in both IDEs
</success_criteria>

<output>
Create `.planning/phases/106-on-save-compiler-check-in-both-ides/106-03-SUMMARY.md` when done
</output>
