---
phase: 108-intellij-crash-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageClient.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijImportAllowlistTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijCouplingCanaryTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjLanguageServerSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjStatusFeedSourceGuardTest.java
  - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md
autonomous: false
requirements: [LIFE-01, LIFE-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The probe build changes no behaviour (D-13): it classifies no new crash, schedules no new restart and raises no new UI. The status classifier now gets the real from-state. LSP4IJ always emits `stopping` before `stopped` (LanguageServerWrapper.stop, tag 0.21.0), so it keeps returning NOT_A_STOP, as Phase 97's real idea.log showed for this same feed/from-state pair"
    - "BbjLanguageServer overrides addUnexpectedServerStopHandler(Runnable). LSP4IJ's handler goes to super first, unchanged, and exactly one own handler per provider instance is registered beside it. In this plan the own handler only logs pid, exit code and thread name at INFO (D-01, D-13)"
    - "BbjLanguageServer overrides stop(): it logs pid and process liveness at INFO, then calls super.stop() exactly once, so the excerpt shows whether a stop() came before the process ended (deliberate) or after it (crash) (D-13)"
    - "BbjServerService.updateStatus is fed from the LSPClientFeatures#handleServerStatusChanged override in createClientFeatures(). That override sits beside Phase 106's initializeParams override, which is unchanged. BbjLanguageClient's override keeps only its console line (D-04)"
    - "Every `BBj language server status: X -> Y` line names the real previous status: currentStatus before it advances. The stale two-behind field is removed and a comment-aware source guard pins the line (D-05, LIFE-02)"
    - "Every vendor member the probe depends on is pinned by a reflective canary: addUnexpectedServerStopHandler, stop, getPid, isAlive, protected getProcessHandler, package-private isStopped, ProcessHandler.getExitCode, LSPProcessListener.processTerminated, LSPClientFeatures.handleServerStatusChanged and getProject. The import allowlist records ServerStatus for BbjLanguageServerFactory (D-01, D-04)"
    - "Every new log line contains the exact text `BBj language server`, so `grep \"BBj language server\" idea.log` captures the whole probe"
    - "A VSIX and an IntelliJ zip built from this tree are in /tmp/phase-108-probe/. The zip's main.cjs is byte-identical to bbj-vscode/out/language/main.cjs, and its plugin jar contains the hook override and the client-features status override"
    - "The maintainer pastes the real macOS idea.log excerpt for kill -9, closing the last BBj file (three times) and Settings Apply (three times) into 108-UAT-ARTIFACTS.md. No hand-derived trace stands in for it (D-13, D-15)"
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java"
      provides: "unexpected-stop hook registration beside LSP4IJ's own, and the stop() INFO line"
      contains: "super.addUnexpectedServerStopHandler(handler)"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java"
      provides: "the single status-feed site"
      contains: "public void handleServerStatusChanged(@NotNull ServerStatus status)"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java"
      provides: "real from-state in the transition log line"
      contains: "\"BBj language server status: \" + currentStatus + \" -> \" + status"
    - path: "bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjStatusFeedSourceGuardTest.java"
      provides: "one status-feed site, console-only client override, real from-state log line"
      contains: "class BbjStatusFeedSourceGuardTest"
    - path: ".planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md"
      provides: "probe build identity, runbook, derived expectations, the pasted observed excerpt"
      contains: "## Probe build"
  key_links:
    - from: "LSP4IJ LanguageServerWrapper.start (provider.addUnexpectedServerStopHandler, before provider.start)"
      to: "BbjLanguageServer.addUnexpectedServerStopHandler override"
      via: "forwards LSP4IJ's handler to super, then registers this::onUnexpectedStop once"
      pattern: "super\\.addUnexpectedServerStopHandler\\(this::onUnexpectedStop\\)"
    - from: "LSPClientFeatures#handleServerStatusChanged (called for every status, including stopped)"
      to: "BbjServerService.updateStatus"
      via: "invokeLater with two isDisposed guards"
      pattern: "BbjServerService\\.getInstance\\(project\\)\\.updateStatus\\(status\\)"
  assumptions:
    - statement: "LSP4IJ's deliberate stops send the LSP `exit` notification (shutdownAll in LanguageServerWrapper, tag 0.21.0) before provider.stop() sets the stopped flag, and the Node server exits by itself on `exit`. If the process ended before the flag was set, the hook would fire on a deliberate stop. From the source, stop() runs right after the notification is written, while the process-end event has to wait for the child to exit and for the platform to drain its output. The probe repeats each deliberate stop three times to sample this race; a hook line in those windows contradicts D-01"
      source: "planning read of LanguageServerWrapper.shutdownAll at tag 0.21.0 (not in 108-RESEARCH.md)"
      flagged: true
    - statement: "The unexpected-stop handler runs on the platform's process-wait thread, not the EDT (research Assumption A1). The probe line logs the thread name so the excerpt shows it"
      source: "108-RESEARCH.md Assumptions Log A1"
      flagged: true
    - statement: "Edge probe for LIFE-02 returned one unclassified item. No edge category fits a log-line fix, so it stays unresolved. The comment-aware source guard and the from-states in the observed excerpt are the checks"
      source: "spec-less edge probe (LIFE-02, unclassified)"
      flagged: true
---

<objective>
Ship the probe build (D-13). It wires the LSP4IJ unexpected-stop hook and a stop() log line into
the connection provider, moves the status feed to the client-features hook (D-04) and logs real
from-states (D-05). It changes no behaviour. Then build both distributables and have the maintainer
run kill -9, close the last BBj file and apply settings in IntelliJ on macOS, pasting the real
`idea.log` excerpt.

Purpose: Phase 97 was approved on a hand-derived lifecycle trace and reverted. Every later plan in
this phase builds on the sequence this probe observes, not on source reading.
Output: hook and stop() INFO lines, the moved status feed, the LIFE-02 log fix, canaries and source
guards, `/tmp/phase-108-probe/` builds, `108-UAT-ARTIFACTS.md` with the maintainer's excerpt.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-intellij-crash-detection/108-CONTEXT.md
@.planning/phases/108-intellij-crash-detection/108-RESEARCH.md
@.planning/phases/108-intellij-crash-detection/108-PATTERNS.md
@.planning/milestones/v4.4-phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md
@bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java
@bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java
@bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java

<interfaces>
- LSP4IJ 0.21.0 `com.redhat.devtools.lsp4ij.server.OSProcessStreamConnectionProvider` (javap of the pinned jar): `public void addUnexpectedServerStopHandler(java.lang.Runnable)`, `public void stop()`, `public java.lang.Long getPid()` (null-safe), `public boolean isAlive()`, `protected com.intellij.execution.process.OSProcessHandler getProcessHandler()`, package-private `boolean isStopped()` (not callable from our package; never call it). None of these is final.
- `com.redhat.devtools.lsp4ij.server.LSPProcessListener` (package-private class): `processTerminated(com.intellij.execution.process.ProcessEvent)` runs every unexpected-stop handler only when `provider.isStopped()` is false.
- IntelliJ Platform 2024.2 `com.intellij.execution.process.ProcessHandler`: `public java.lang.Integer getExitCode()`, which is written before `processTerminated` fires.
- LSP4IJ `LanguageServerWrapper.start()` (tag 0.21.0, line 424) creates a fresh provider per start and calls `provider.addUnexpectedServerStopHandler(...)` once, before `provider.start()`.
- `LSPClientFeatures`: `public void handleServerStatusChanged(ServerStatus)` (overridable), `public final Project getProject()`.
- `BbjServerService.updateStatus(@NotNull ServerStatus status)`: lines 139-217. Fields `currentStatus` (line 49) and the stale from-state field (line 50). Log line at 149-150, classifier call at 146-147, stale assignment at 206.
- `BbjLanguageServerFactory.createClientFeatures()`: anonymous `LSPClientFeatures` at lines 40-73. Its `initializeParams` override ends at line 64, after the `COMPILER_TRIGGER_KEY` option.
- `BbjLanguageClient.handleServerStatusChanged(ServerStatus serverStatus)`: lines 41-56. `service.updateStatus(serverStatus);` is at line 54.
- `BbjLanguageServerSourceGuardTest` already has private `stripComments(String)` and a string- and comment-aware `bodyOf(String source, String declarationMarker)`.
</interfaces>
</context>

<!-- planner-discipline-allow: previousStatus -->
<!-- planner-discipline-allow: updateStatus( -->

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` forms are `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`
  and `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Prefer
  `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`. Use
  `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>` only,
  never `-A` or `.`. Never use `git stash` or a path-wide `git restore --source`. Prefer the Read
  tool over shell reads.
- Source and test text (code, comments, javadoc, assertion messages, test names) must not contain
  planning identifiers: no D-NN, no LIFE-NN, no 108-NN plan numbers, no T-108 threat ids, no
  CR/WR/IN review ids, no "Pitfall N". GitHub issue numbers such as #1672 and #1673 are fine.
- New source guards run on comment-stripped text. A guard that reads raw text can be broken by a
  comment.
- `BbjServerServiceRestartSourceGuardTest` counts some literals in raw text, including comments:
  `Thread.sleep`, `new Alarm(`, `CRASH_RESTART_DELAY_MS`, `BoundedWait.until(`,
  `setWillDisable(false)`, `manager.stop(SERVER_ID)`. Do not add comment text containing any of them to
  `BbjServerService.java`.
- IntelliJ gates use `--rerun-tasks` for the whole suite. A plain `test` can report UP-TO-DATE.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md or another ledger. Do not
  mark LIFE-01 or LIFE-02 complete in REQUIREMENTS.md. The phase verifier closes them.
- Commits: conventional subject. Put the `Co-Authored-By:` trailer in the final paragraph of the
  message.

## Artifacts this phase produces

- (this plan) `BbjLanguageServer`: override `addUnexpectedServerStopHandler(Runnable)`, override
  `stop()`, private `onUnexpectedStop()`, private boolean field `ownStopHandlerRegistered`, INFO
  lines `BBj language server unexpected-stop handler registered beside LSP4IJ's own`,
  `BBj language server process ended without a stop request (pid …, exit code …, thread …)`,
  `BBj language server connection stop requested (pid …, process alive: …)`.
  `BbjLanguageServerFactory`: `handleServerStatusChanged(ServerStatus)` override in the anonymous
  `LSPClientFeatures`. `BbjLanguageClient.handleServerStatusChanged`: console line only.
  `BbjServerService`: the stale from-state field is removed and the log line reads `currentStatus`.
  Tests: `Lsp4ijCouplingCanaryTest.theUnexpectedStopMembersThisPluginHooksStillExist`,
  `Lsp4ijCouplingCanaryTest.theClientFeaturesStatusMembersThisPluginOverridesStillExist`, new
  `BbjLanguageServerSourceGuardTest` cases, new `lsp/BbjStatusFeedSourceGuardTest.java`, and the
  `ServerStatus` allowlist entry for the factory. `108-UAT-ARTIFACTS.md` (Probe build section) and
  `/tmp/phase-108-probe/` (VSIX, zip, extracted plugin jar).
- (108-02) `BbjServerService.reportUnexpectedExit(Long, Integer)`, private `applyCrashPolicy(Long, Integer)`,
  `isAutoRestartAbandoned()`, private `requestGatedRestart(long)`, `volatile` crash-state fields,
  the disarm after the plugin's own stop; `ExpectedStopGuard.classifyExit(long)`, which replaces
  `classify(String, String, long)` and `StopKind.NOT_A_STOP`; the `project` field in `BbjLanguageServer`.
  The crash and give-up WARN lines.
- (108-03) The `BBj: Crashed` widget state in `BbjStatusBarWidget`, the give-up gate in
  `BbjServerCrashNotificationProvider`, `ui/BbjServerCrashNotificationProviderSourceGuardTest.java`,
  and two lines in `documentation/docs/intellij/features.md`.
- (108-04) `/tmp/phase-108-uat/` final builds and the Final UAT section of `108-UAT-ARTIFACTS.md`.

<tasks>

<task type="tracer">
  <name>Task 1: Probe path end to end: hook and stop() logging, the status feed moved to client features, real from-states</name>
  <reversibility rating="costly">Overrides two LSP4IJ provider members (addUnexpectedServerStopHandler, stop). An LSP4IJ upgrade that changes either needs the override and its canaries reworked together.</reversibility>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageClient.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijImportAllowlistTest.java</files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java (whole file; LOG style at lines 45 and 59-60)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java (createClientFeatures, lines 39-73)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageClient.java (handleServerStatusChanged, lines 41-56)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java (fields at lines 48-62, updateStatus at lines 139-217)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijImportAllowlistTest.java (ALLOWLIST, lines 44-63)
    - .planning/phases/108-intellij-crash-detection/108-RESEARCH.md (Findings 1, 2, 4, 5, 6; Code Examples; Pitfall 1)
    - .planning/phases/108-intellij-crash-detection/108-PATTERNS.md (BbjLanguageServer, BbjLanguageServerFactory, BbjLanguageClient sections)
    - `git -C /home/coder/repos/bbj-language-server show bb0a49f0` (reference only; it does not apply cleanly, so hand-merge)
  </read_first>
  <action>
Wire the probe path through every layer it touches. Add logging only; change no behaviour (D-13).

1. `BbjLanguageServer` (per D-01, D-13):
   - Add a private boolean field `ownStopHandlerRegistered` with no initializer.
   - Override `public void addUnexpectedServerStopHandler(Runnable handler)`. First call
     `super.addUnexpectedServerStopHandler(handler)`, which forwards LSP4IJ's own handler unchanged
     and first, so LSP4IJ's own recovery (its `serverError`, its `stop(ctx)`, its notification) runs
     before ours. Then, only while `ownStopHandlerRegistered` is false: set it to true, call
     `super.addUnexpectedServerStopHandler(this::onUnexpectedStop)`, and log INFO
     `BBj language server unexpected-stop handler registered beside LSP4IJ's own`. Never wrap,
     replace or drop LSP4IJ's handler.
   - Add `private void onUnexpectedStop()`. It reads the pid through the inherited public `getPid()`,
     and the exit code through the inherited protected `getProcessHandler()` followed by
     `getExitCode()`; if the handler is null, the exit code is unknown. It logs INFO
     `BBj language server process ended without a stop request (pid <pid>, exit code <code>, thread <name>)`,
     where the thread name comes from `Thread.currentThread().getName()` and a null pid or exit code
     prints as `unknown`. It does nothing else in this plan: no call into `BbjServerService`, no
     restart, no UI. It runs on the platform's process-wait thread (research Finding 5).
   - Override `public void stop()`. Log INFO
     `BBj language server connection stop requested (pid <pid>, process alive: <isAlive()>)`, then
     call `super.stop()` exactly once. LSP4IJ calls stop() for every deliberate stop, and again after
     its own unexpected-stop handling. The excerpt therefore shows whether a stop() came before the
     process ended or after it.
   - No reflection, and no attempt to reach the package-private `isStopped()`. Every new log line
     contains the exact text `BBj language server`.
2. `BbjLanguageServerFactory` (per D-04): inside the anonymous `LSPClientFeatures` body returned by
   `createClientFeatures()`, and after the existing `initializeParams` override, add an override of
   `public void handleServerStatusChanged(@NotNull ServerStatus status)`. Leave `initializeParams`
   untouched, including Phase 106's `COMPILER_TRIGGER_KEY` option. The new override calls
   `super.handleServerStatusChanged(status)`, takes `Project project = getProject()`, returns if
   `project.isDisposed()`, then calls `ApplicationManager.getApplication().invokeLater(...)`. The
   lambda returns if `project.isDisposed()` and otherwise calls
   `BbjServerService.getInstance(project).updateStatus(status)`. Add the imports
   `com.redhat.devtools.lsp4ij.ServerStatus`, `com.intellij.openapi.application.ApplicationManager`
   and `com.basis.bbj.intellij.ui.BbjServerService`. Merge by hand: `bb0a49f0` does not apply
   (research Finding 6).
   Why this site: LSP4IJ calls the client features for every status change, but only calls the
   language client while it is non-null, and it nulls the client before publishing `stopped`
   (LanguageServerWrapper.updateStatus and stop, tag 0.21.0).
3. `BbjLanguageClient` (per D-04): in `handleServerStatusChanged`, delete only the
   `service.updateStatus(serverStatus);` call. Keep the super call, both disposed guards, the
   `invokeLater` and the `Server status:` console line. Reword the class javadoc to say that the
   client logs status changes to the console, and that the status feed runs from the client-features
   hook in `BbjLanguageServerFactory`.
4. `BbjServerService.updateStatus` (per D-05):
   - The log line and the classifier input both read `currentStatus`, which still holds the previous
     status until `this.currentStatus = status;` runs. The log line becomes
     `"BBj language server status: " + currentStatus + " -> " + status + " (classified as " + stopKind + ")"`.
     The classifier call becomes
     `expectedStop.classify(status.name(), currentStatus.name(), System.currentTimeMillis())`.
   - Delete the stale `previousStatus` field and its assignment.
   - Change nothing else: not the crash branch, the `started` branch, `requestRestart`, `doRestart`
     or `clearCrashState` (D-13).
   Why the classifier input moves too: once the feed sees `stopped`, the stale field would hand the
   classifier `started` on every deliberate stop, and each stop would become a crash with an
   auto-restart. With the real from-state it gets `stopping`, because LSP4IJ always emits `stopping`
   before `stopped` (LanguageServerWrapper.stop, tag 0.21.0). So the verdict stays NOT_A_STOP as it
   is today, and Phase 97's real idea.log showed exactly that for this pair (97-UAT-ARTIFACTS.md,
   Round 1).
5. `Lsp4ijImportAllowlistTest`: add `"ServerStatus"` to the `BbjLanguageServerFactory.java` entry.
   The map stays at 12 entries.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "com.basis.bbj.intellij.lsp.Lsp4ijImportAllowlistTest" --tests "com.basis.bbj.intellij.lsp.Lsp4ijOverrideSiteSourceGuardTest" --tests "com.basis.bbj.intellij.lsp.BbjServerServiceRestartSourceGuardTest" --tests "com.basis.bbj.intellij.lsp.BbjLanguageServerSourceGuardTest" --tests "com.basis.bbj.intellij.lsp.Lsp4ijCouplingCanaryTest" --tests "com.basis.bbj.intellij.concurrency.ExpectedStopGuardTest" --console=plain</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" or "No tests found for given includes" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'super.addUnexpectedServerStopHandler(' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java` prints 2, and `grep -c 'super.stop()' …/BbjLanguageServer.java` prints 1
    - `grep -c 'LOG.info("BBj language server' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java` prints at least 3
    - `grep -c 'public void handleServerStatusChanged(@NotNull ServerStatus status)' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java` prints 1, and `grep -c 'COMPILER_TRIGGER_KEY' …/BbjLanguageServerFactory.java` prints 1 (Phase 106 option intact)
    - `grep -c 'updateStatus(' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageClient.java` prints 0, and `grep -c 'Server status: ' …/BbjLanguageClient.java` prints 1
    - `grep -c '"BBj language server status: " + currentStatus + " -> " + status' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java` prints 1, and `grep -c 'previousStatus' …/BbjServerService.java` prints 0
    - `grep -nE '\b108-[0-9]{2}\b|\bD-[0-9]{2}\b|\bLIFE-0[0-9]\b|\bT-108-[0-9]+\b|\b(CR|WR|IN)-[0-9]{2}\b|Pitfall [0-9]'` over the five files in `<files>` prints nothing
    - The targeted Gradle run in `<verify>` ends with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>The provider logs hook registration, a process end without a stop request, and every stop() call. The status feed runs from the client-features hook. The status log line names the real previous status. Existing guards and the allowlist are green, and no crash or restart behaviour has changed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the probe wiring, build both distributables, prepare the probe record</name>
  <files>bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijCouplingCanaryTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjLanguageServerSourceGuardTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjStatusFeedSourceGuardTest.java, .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md</files>
  <read_first>
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijCouplingCanaryTest.java (style: theCommandDispatchMembersTheCueActionReliesOnStillExist, theConnectionProviderMembersThisPluginUsesStillExist)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjLanguageServerSourceGuardTest.java (stripComments and bodyOf helpers, lines 128-370)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijOverrideSiteSourceGuardTest.java (existing guard shape for the two override sites)
    - `git -C /home/coder/repos/bbj-language-server show cb3ce7f8` (canary content to re-apply with reworded comments)
    - the four main-source files changed by Task 1
    - /home/coder/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/uat-build-both-extensions-first.md (build commands, UP-TO-DATE trap, cmp proof)
  </read_first>
  <action>
Pin what Task 1 wired, then produce the exact build the maintainer will run.

1. `Lsp4ijCouplingCanaryTest` (per D-01, D-04). Follow the class style: one assertion per member,
   each with a message saying what breaks.
   - Add `@Test void theUnexpectedStopMembersThisPluginHooksStillExist()`, which pins on
     `OSProcessStreamConnectionProvider`:
     - `addUnexpectedServerStopHandler(Runnable)` and `stop()`: both public, not final, returning void.
     - `getPid()`: public, returning `Long`.
     - `isAlive()`: public, returning boolean.
     - `getProcessHandler()` via `getDeclaredMethod`: protected, returning
       `com.intellij.execution.process.OSProcessHandler`.
     - `isStopped()` via `getDeclaredMethod`: returns boolean and is neither public, protected nor
       private. It is the package-private flag LSP4IJ's own process listener reads; the test
       documents it and never calls it.
     It also pins `com.intellij.execution.process.ProcessHandler.getExitCode()` returning `Integer`,
     and `Class.forName("com.redhat.devtools.lsp4ij.server.LSPProcessListener")` exposing
     `processTerminated(com.intellij.execution.process.ProcessEvent)`.
   - Add `@Test void theClientFeaturesStatusMembersThisPluginOverridesStillExist()`, which pins
     `LSPClientFeatures.handleServerStatusChanged(ServerStatus)` (void, not final) and `getProject()`
     (returns Project). This is the content of `cb3ce7f8`, with its comments reworded: this hook is
     the status feed for display and logging, not crash detection.
2. `BbjLanguageServerSourceGuardTest`: add three tests, all on the output of the file's existing
   `stripComments`, using its existing `bodyOf`:
   - Body of `public void addUnexpectedServerStopHandler(`: `super.addUnexpectedServerStopHandler(`
     appears twice. `super.addUnexpectedServerStopHandler(handler)` appears once and comes before the
     single `super.addUnexpectedServerStopHandler(this::onUnexpectedStop)`. `ownStopHandlerRegistered`
     appears before the own registration.
   - Body of `public void stop()`: `super.stop()` appears exactly once, after a `LOG.info(`.
   - The whole stripped file contains no `setAccessible(`, no `getDeclaredField(`, no
     `getDeclaredMethod(` and no `isStopped(`, so the provider reaches no vendor internals.
   Name the tests in plain English.
3. New `BbjStatusFeedSourceGuardTest` in package `com.basis.bbj.intellij.lsp`. Copy the
   `stripComments` helper and the string- and comment-aware `bodyOf` from
   `BbjLanguageServerSourceGuardTest` (the per-guard private-helper convention) and run every
   assertion on stripped text:
   - Factory: in the body of `public void handleServerStatusChanged(@NotNull ServerStatus status)`,
     `super.handleServerStatusChanged(` appears once, `invokeLater(` once, `isDisposed()` twice and
     `.updateStatus(status)` once, and the updateStatus call comes after the invokeLater. The
     `createClientFeatures()` body still contains `public void initializeParams(` exactly once.
     Together these pin one status-feed site.
   - Client: the body of `public void handleServerStatusChanged(ServerStatus serverStatus)` contains
     no `updateStatus(` and exactly one `logToConsole(`.
   - Service: the body of `public void updateStatus(@NotNull ServerStatus status)` contains
     `"BBj language server status: " + currentStatus + " -> " + status` exactly once, before
     `this.currentStatus = status;`. The stripped `BbjServerService.java` contains no `previousStatus`.
   The class javadoc describes the invariants in plain English: one status-feed site, and a log line
   that names the real previous status.
4. Build both distributables (D-13):
   - Run `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then
     `bbj-ext-install`, then
     `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --console=plain -q`.
     A Swing stack trace during buildSearchableOptions is headless noise when the exit code is 0.
   - Create `/tmp/phase-108-probe/`. Copy `/tmp/bbj-lang.vsix` and
     `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip` into it, and record `sha256sum` and
     the byte size of both.
   - Extract `bbj-intellij/lib/bbj-intellij-0.1.0.jar` from the zip to
     `/tmp/phase-108-probe/plugin.jar`, and confirm with `javap -p -classpath` that
     `com.basis.bbj.intellij.lsp.BbjLanguageServer` declares `addUnexpectedServerStopHandler`, `stop`
     and `onUnexpectedStop`, and that `com.basis.bbj.intellij.lsp.BbjLanguageServerFactory$1`
     declares `handleServerStatusChanged`.
5. Run the whole IntelliJ suite with `--rerun-tasks`.
6. Write `.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md` with the heading
   `# Phase 108 — UAT Artifacts`, a redaction rule, and a `## Probe build` section. The redaction
   rule: replace the macOS home directory with `<home>` and the project directory with `<project>`
   in any pasted line, and never paste BBj source text. The `## Probe build` section has five
   subsections:
   - `### Build identity`: commit from `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`,
     plus the sha256 and size of both files.
   - `### Runbook (macOS)`: the steps from Task 3.
   - `### Expected signals (derived)`: a table `| # | After | Expected line (substring) | Derived from | Observed? |`
     with the rows below. Each "Derived from" cell names the code path, and each "Observed?" cell is
     left empty.
   - `### Probe excerpt (observed)`: a note asking for a `--- step N (HH:MM:SS) ---` marker before
     each step's lines, followed by an empty fenced `text` block.
   - `### Probe verdict`: the text `Pending: recorded by the next plan before any behaviour change`.

   Rows for the expected-signals table:
   - P1, every server start: `unexpected-stop handler registered`. Derived from
     LanguageServerWrapper.start line 424 → our override.
   - P2, after kill -9: `process ended without a stop request (pid <pid>, exit code 137, thread <name>)`.
     Derived from LSPProcessListener.processTerminated with isStopped false. On macOS a SIGKILL exit
     reads 128+9.
   - P3, after P2: `connection stop requested (pid <pid>, process alive: false)`. Derived from
     LSP4IJ's own handler → stop(ctx) → shutdownAll → provider.stop().
   - P4, after the kill: `status: started -> stopping (classified as NOT_A_STOP)` then
     `status: stopping -> stopped (classified as NOT_A_STOP)`.
   - P5, after the kill: no `Scheduled a BBj language server restart` line and no
     `Restarting the BBj language server` line. The restart comes from LSP4IJ on editor activity, as
     `stopped -> starting` with no Scheduled line.
   - P6, after each of three closes of the last BBj file: `status: started -> stopping`, then
     `connection stop requested (pid <pid>, process alive: true)`, then `status: stopping -> stopped`,
     and no `process ended without a stop request`.
   - P7, after each of three Settings Apply runs: `Scheduled a BBj language server restart in 500 ms`,
     `Restarting the BBj language server; status before the stop: started`,
     `connection stop requested (…, process alive: true)`, and the status lines. No
     `process ended without a stop request`.
   - P8, on every status line: the from-state equals the to-state of the previous status line.
   - A closing line: D-01 is contradicted if P2 is missing after the kill, or if a
     `process ended without a stop request` line appears in a P6 or P7 window.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "com.basis.bbj.intellij.lsp.Lsp4ijCouplingCanaryTest" --tests "com.basis.bbj.intellij.lsp.BbjLanguageServerSourceGuardTest" --tests "com.basis.bbj.intellij.lsp.BbjStatusFeedSourceGuardTest" --tests "com.basis.bbj.intellij.lsp.Lsp4ijImportAllowlistTest" --tests "com.basis.bbj.intellij.lsp.Lsp4ijOverrideSiteSourceGuardTest" --console=plain</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" or "No tests found for given includes" in the output</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain</automated>
    <fails_when>non-zero exit or "BUILD FAILED" in the output</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs && unzip -p /tmp/phase-108-probe/bbj-intellij-0.1.0.zip bbj-intellij/lib/bbj-intellij-0.1.0.jar > /tmp/phase-108-probe/plugin.jar && javap -p -classpath /tmp/phase-108-probe/plugin.jar com.basis.bbj.intellij.lsp.BbjLanguageServer | grep -c 'addUnexpectedServerStopHandler' && ls /tmp/phase-108-probe/bbj-lang.vsix /tmp/phase-108-probe/bbj-intellij-0.1.0.zip</automated>
    <fails_when>cmp prints a difference or exits non-zero, grep prints 0 (the zip carries a stale plugin jar), or ls reports a missing file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'void theUnexpectedStopMembersThisPluginHooksStillExist' /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijCouplingCanaryTest.java` prints 1, and `grep -c 'void theClientFeaturesStatusMembersThisPluginOverridesStillExist' …/Lsp4ijCouplingCanaryTest.java` prints 1
    - `/home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/BbjStatusFeedSourceGuardTest.java` exists and `grep -c 'static String stripComments' …/BbjStatusFeedSourceGuardTest.java` prints 1
    - `grep -nE '\b108-[0-9]{2}\b|\bD-[0-9]{2}\b|\bLIFE-0[0-9]\b|\bT-108-[0-9]+\b|\b(CR|WR|IN)-[0-9]{2}\b|Pitfall [0-9]'` over the three test files in `<files>` prints nothing
    - `grep -c -E '^### (Build identity|Runbook \(macOS\)|Expected signals \(derived\)|Probe excerpt \(observed\)|Probe verdict)' /home/coder/repos/bbj-language-server/.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md` prints 5, and `grep -c -E '^\| P[1-8] ' …/108-UAT-ARTIFACTS.md` prints 8
    - The whole-suite Gradle run ends with BUILD SUCCESSFUL; `/tmp/phase-108-probe/` holds the VSIX, the zip and plugin.jar, and both sha256 values are in the Build identity subsection
  </acceptance_criteria>
  <done>The hook, stop() override and status feed are pinned by canaries and comment-aware guards. Both distributables come from this tree, and the zip provably carries the probe. The record lists every expected probe line as derived, with its code path, ready for the observed excerpt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Probe run in IntelliJ on macOS: kill -9, close the last BBj file, Settings Apply</name>
  <read_first>
    - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md (Probe build section, expected signals P1-P8)
  </read_first>
  <what-built>A probe build with no behaviour change. The connection provider logs when its unexpected-stop hook is registered, when the process ends without a stop request (pid, exit code, thread) and every stop() call. The status feed runs from LSP4IJ's client-features hook, and each status line names the real previous status. Builds: `/tmp/phase-108-probe/bbj-intellij-0.1.0.zip` and `/tmp/phase-108-probe/bbj-lang.vsix` (the VSIX is not under test here).</what-built>
  <how-to-verify>
    1. In IntelliJ on macOS: Settings, Plugins, gear icon, Install Plugin from Disk, choose `bbj-intellij-0.1.0.zip`, restart the IDE. Help, Show Log in Finder gives the `idea.log` path (for example `~/Library/Logs/JetBrains/IntelliJIdea2026.2/idea.log`).
    2. Open a BBj project and one `.bbj` file. Wait for the status bar to show `BBj: Ready`. Note the time and write `--- step 2 (HH:MM:SS) ---` in the excerpt block.
    3. Kill the server: `kill -9 $(pgrep -f 'bbj-intellij/lib/language-server/main.cjs')`. If more than one pid matches, use the newest. Wait 10 s without touching IntelliJ and note which notifications appear (LSP4IJ shows its own "stopped unexpectedly" error). Then click into the editor and move the cursor until the widget shows `BBj: Ready` again.
    4. Close every BBj editor tab and wait 15 s. Reopen a `.bbj` file and wait for `BBj: Ready`. Do this three times, noting the time for each.
    5. Settings, Languages & Frameworks, BBj: change one setting (for example Log level) and press Apply. Wait for `BBj: Ready`. Change it back and Apply, then Apply one more change. That is three Applies in total, with a time noted for each.
    6. Run `grep "BBj language server" <path to idea.log>`. Paste the lines from this IDE session into `108-UAT-ARTIFACTS.md` under `### Probe excerpt (observed)`, with the step markers. Replace your home directory with `<home>` and the project directory with `<project>`.
  </how-to-verify>
  <acceptance_criteria>
    - The excerpt block in 108-UAT-ARTIFACTS.md holds the real `grep "BBj language server"` output for steps 2-5, with step markers
    - No `/Users/<name>` path and no BBj source text appears in the pasted lines
  </acceptance_criteria>
  <resume-signal>Type "probe pasted" once the excerpt is in 108-UAT-ARTIFACTS.md, adding anything you saw on screen (which notifications and banners appeared, and when). Or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node.js language-server process → plugin | the process's pid and exit code reach plugin log lines through LSP4IJ's process listener |
| maintainer's idea.log → public repository | pasted log lines enter a committed planning file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-108-01 | Information disclosure | `108-UAT-ARTIFACTS.md` probe excerpt | medium | mitigate | The redaction rule in the file and in the checkpoint says to replace the home directory with `<home>` and the project directory with `<project>`, and to paste no BBj source text. The next plan checks `grep -cE '/Users/[^<]'` prints 0 before it commits anything. |
| T-108-02 | Information disclosure | new INFO lines in `BbjLanguageServer` | low | accept | They log pid, exit code, thread name and liveness only. No command line, environment or process output is added (the existing launch line is unchanged). |
| T-108-03 | Denial of service | the probe build | low | mitigate | The probe changes no restart or crash behaviour (D-13). The classifier still returns NOT_A_STOP because `stopping` always precedes `stopped`, and Task 1 changes no restart path. |
| T-108-04 | Repudiation | probe evidence | low | mitigate | The runtime sequence comes only from the maintainer's real idea.log excerpt (D-15 and the v4.4 standing decision). The expected table is labelled derived until marked. |
| T-108-SC | Tampering | package installs | low | accept | No package is installed. The builds use the existing lockfiles and the pinned Gradle wrapper. |
</threat_model>

<verification>
- Targeted canaries and guards green; whole IntelliJ suite green with `--rerun-tasks`
- `/tmp/phase-108-probe/` zip carries the fresh server bundle and a plugin jar with the hook override
- 108-UAT-ARTIFACTS.md holds the derived P1-P8 table and the maintainer's pasted excerpt
</verification>

<success_criteria>
- The probe build logs hook registration, the unexpected process end, every stop() call and real from-states, and it changes no behaviour
- The maintainer's real macOS excerpt for kill -9, three closes of the last file and three Settings Applies is recorded
</success_criteria>

<output>
Create `.planning/phases/108-intellij-crash-detection/108-01-SUMMARY.md` when done
</output>
