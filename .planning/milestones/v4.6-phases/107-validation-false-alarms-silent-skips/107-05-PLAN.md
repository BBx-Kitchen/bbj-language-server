---
phase: 107-validation-false-alarms-silent-skips
plan: 05
type: execute
wave: 2
depends_on: ["107-03"]
files_modified:
  - bbj-vscode/src/language/validations/check-unknown-java-member.ts
  - bbj-vscode/test/unknown-java-member.test.ts
autonomous: true
requirements: [VAL-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every unknown-member Error the check produces over the private corpus's accepted files, validated with the live java-interop backend on :5008 (real class data, not the test double's five fake classes), is reviewed by line shape (D-12)"
    - "Each finding is classified as (a) a genuinely unknown member on a certain receiver, (b) a member that exists on the real class, (c) a wrong receiver type, or (d) other; the counts per class and the shapes of (b), (c) and (d) are recorded in own words (D-12, D-13)"
    - "Every (b), (c) or (d) shape gets a synthetic failing unit test and a guard in check-unknown-java-member.ts that keeps today's Warning for it; after the last guard a re-run of the affected files shows only class (a) findings (ROADMAP criterion 5: no new Error on a member that exists)"
    - "Guards added here only narrow the check: every test from 107-03 still passes, including the positive cases and the live BBjAPI() file (D-10, D-11)"
    - "No corpus file name, id, path or source line enters the repository, a SUMMARY or a commit message; the probe and its output stay in the corpus repository's snapshots folder (D-13)"
    - "Edge (concurrency / cold resolution): the probe warms the implicit imports and polls until BBjAPI's members are linked before validating any file, so no finding comes from a class caught mid-resolution; a class that times out stays an error-carrying stub, which the check skips"
  artifacts:
    - path: "/home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe.jsonl"
      provides: "every unknown-member Error over the accepted corpus with the live backend (outside the repository)"
    - path: "bbj-vscode/test/unknown-java-member.test.ts"
      provides: "a synthetic test per false-positive shape found, if any"
  key_links:
    - from: "phase-107-live-member-probe.mts"
      to: "checkUnknownJavaMember via createBBjServices(NodeFileSystem) and JavaInteropService on :5008"
      via: "per-file validation, filtering diagnostics with UNKNOWN_JAVA_MEMBER_CODE"
      pattern: "bbj-unknown-java-member"
  prohibitions:
    - requirement_id: VAL-03
      category: trust
      statement: "MUST NOT accept a finding as 'genuinely unknown' without checking the member against the live class description and the receiver's real type; when unsure, classify it as a false positive and guard it"
      flagged: true
    - requirement_id: VAL-03
      category: confidentiality
      statement: "MUST NOT write a corpus file name, id, path or source line into the repository, a commit message, a planning file or a SUMMARY (D-13)"
      flagged: true
  assumptions:
    - statement: "The standard harness validates with the test double's five fake Java classes, so it cannot show VAL-03's behaviour on real classes; this live-backend probe is the D-12 review of 'every new Error-severity diagnostic', and 107-06 uses its output to classify the harness's test-double artifacts"
      source: "worker.mts uses createBBjTestServices; planning analysis"
      flagged: true
    - statement: "Only the accepted corpus is reviewed: a new Error on a rejected file can only reduce B, while a false Error matters on code the compiler accepts"
      source: "planning decision"
      flagged: true
---

<objective>
Review every unknown-member Error the new check produces on real BBj code, using the live
java-interop backend, and guard every shape that is not a genuinely unknown member (VAL-03, D-12).

Purpose: the unit tests prove the guards on synthetic shapes; only real code with real class data can
show whether the check is conservative enough (flag only what is certain).
Output: a probe and its findings outside the repository, a guard plus a synthetic test for each
false-positive shape, and a classification by counts and own-words shapes in the SUMMARY.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-CONTEXT.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-03-SUMMARY.md
@bbj-vscode/src/language/validations/check-unknown-java-member.ts
@bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts

<interfaces>
- `/home/coder/repos/bbj-corpus/manifest.jsonl`: one JSON record per accepted file with `id` and `bytes`; the file is `/home/coder/repos/bbj-corpus/corpus/<id>` (16,884 records on 2026-09-23).
- worker.mts endpoint branch (lines 36-60) shows the live-services idiom: `createBBjServices(NodeFileSystem)` imported by absolute path from `${lsRepo}/bbj-vscode/src/language/bbj-module.ts` and `JavaInteropService.setConnectionConfig(host, port)`, run under `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx`.
- The warm-up (initialize JavadocProvider, `setConnectionConfig('127.0.0.1', 5008)`, `initializeWorkspace`, `loadImplicitImports()` when needed, poll until `getResolvedClass('BBjAPI')` has a member with `$container`) is in `test/functional/unknown-java-member-real-interop.test.ts` from 107-03.
- `setCompilerTrigger('off')` from `bbj-vscode/src/language/bbj-document-validator.ts` stops live-parse and bbjcpl scheduling during the probe.
- Diagnostics from the check carry `data.code === 'bbj-unknown-java-member'` and messages `Method|Field|Static method|Static field '<member>' is not defined on <class>`.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  Allowed `cd` form: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Run probes by
  absolute path with `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx`. Use `git -C`
  for git. Stage with `git add <exact path>` only. Never use `git stash`.
- No corpus text may leave the private repository (D-13): classify locally, describe shapes in your own
  words, write synthetic tests from scratch with invented names. Run `leak-guard.mjs` on every changed
  file in this repository before committing it.
- Probe scripts, logs and outputs live in `/home/coder/repos/bbj-corpus/conformance/snapshots/` with a
  `phase-107-` prefix. One harness run or probe at a time (`pgrep -f "conformance/run.mjs"` and
  `pgrep -f "phase-107-.*probe"` print nothing before starting).
- Long runs go to the background with progress written to a log every 500 files; poll the log instead
  of waiting silently.
- Do not modify `bbj-scope.ts`, `bbj-linker.ts`, `java-interop.ts` or `bbj-test-module.ts`. Guards only
  narrow the check; never delete a 107-03 test or weaken its assertion.
- Source and test text must not contain planning identifiers (D-NN, VAL-family ids, 107-0N, WR-A,
  CR-/WR-/IN-NN, threat ids). Run vitest with cwd `bbj-vscode`; never the `basic` reporter.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark VAL-03 complete in
  REQUIREMENTS.md.

## Artifacts this phase produces

- (107-01) `elseClaims` counter in `line-break-validation.ts`; line-break tests; fixture lines.
- (107-02) `symbol?.` reads in `check-variable-scoping.ts`; guard in `bbj-scope-local.ts`; tests.
- (107-03) `validations/check-unknown-java-member.ts` (`UNKNOWN_JAVA_MEMBER_CODE`,
  `isFullyResolvedJavaClass`, `hasCertainReceiverType`, `checkUnknownJavaMember`,
  `registerUnknownJavaMemberChecks`), `dropShadowedMemberLinkingDiagnostics`, registration, tests.
- (this plan) outside the repository: `snapshots/phase-107-live-member-probe.mts`,
  `phase-107-live-member-probe.jsonl`, `phase-107-live-member-probe.log`; in the repository, any new
  guard in `check-unknown-java-member.ts` and one synthetic test per false-positive shape in
  `test/unknown-java-member.test.ts`.
- (107-04, 107-06) `107-CONFORMANCE.md`; todo close-out.

<tasks>

<task type="tracer">
  <name>Task 1: The live probe finds and classifies unknown-member Errors on a first slice, end to end</name>
  <files>bbj-vscode/src/language/validations/check-unknown-java-member.ts, bbj-vscode/test/unknown-java-member.test.ts</files>
  <precondition>BBjServices accepts connections on 127.0.0.1:5008 (`timeout 3 bash -c '</dev/tcp/127.0.0.1/5008'` exits 0) and 107-03's live functional test passes on this machine</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/worker.mts (lines 1-60 imports and the endpoint-services setup, 180-252 the per-file loop)
    - bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts (the warm-up)
    - bbj-vscode/src/language/validations/check-unknown-java-member.ts
    - .planning/phases/107-validation-false-alarms-silent-skips/107-03-SUMMARY.md
  </read_first>
  <action>
Build the D-12 review instrument and prove it on a small slice.

1. Write `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe.mts`. It imports the language server by absolute path the way worker.mts does, creates `createBBjServices(NodeFileSystem)`, calls `setCompilerTrigger('off')`, performs the warm-up from the functional test, then reads `manifest.jsonl`, optionally limited by a `--from N --count M` slice, and for each file: `factory.fromString(text, URI.file('/probe/<index>.bbj'))`, `documents.addDocument`, `builder.build([document], { validation: true })`, collect diagnostics whose `data?.code === 'bbj-unknown-java-member'`, append one JSON line per finding to the output file (`id`, 1-based `line`, `message`), then `builder.update([], [uri])`. A per-file error is recorded as its own line (`id`, `crash`), never stops the run. A progress line `files done: <n>, findings: <m>` goes to stderr every 500 files and once more after the last file. Call `process.exit(0)` at the end (open sockets keep the process alive otherwise).
2. Run it on the first 200 files: `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe.mts --from 0 --count 200 > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe-slice.jsonl 2> /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe-slice.log`.
3. Classify each finding locally: read the line and enough context to know the receiver; check the member against the live class description (for example with a small script calling `getResolvedClass(<class>)` on the same services, or the `getClassInfo` request) and decide (a) genuinely unknown on a certain receiver, (b) exists on the real class, (c) wrong receiver type (for example a receiver typed through a variable whose declared or first-assigned type differs from what the program really holds), (d) other.
4. For each (b), (c) or (d) shape: add a synthetic failing test to `unknown-java-member.test.ts` (invented names; with the test double's classes where the shape allows, otherwise in the live functional file's style), add the narrowest guard to `checkUnknownJavaMember` or `hasCertainReceiverType` that keeps today's Warning for it, and re-run the slice until only (a) findings remain.
5. Record in the SUMMARY: slice size, finding count per class, and each false-positive shape in own words with the guard that now covers it. Run the leak guard on changed repository files and the register check.
  </action>
  <verify>
    <automated>test -f /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe-slice.jsonl && grep -c "files done" /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe-slice.log</automated>
    <fails_when>non-zero exit (the slice output is missing), or the progress count is 0 (the probe never reported progress, so nothing was validated)</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts && RUN_BBJ_TESTS=1 npx vitest run test/functional/unknown-java-member-real-interop.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in either summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The probe script and its outputs exist only under `/home/coder/repos/bbj-corpus/conformance/snapshots/`; `git -C /home/coder/repos/bbj-language-server status --porcelain` lists none of them
    - The SUMMARY gives the slice's finding count per class (a)-(d) and one own-words line per false-positive shape
    - `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/bbj-vscode/test/unknown-java-member.test.ts` exits 0
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>The live probe works end to end on a slice, every slice finding is classified, and any false-positive shape found there is guarded and pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The whole accepted corpus shows only genuinely unknown members</name>
  <files>bbj-vscode/src/language/validations/check-unknown-java-member.ts, bbj-vscode/test/unknown-java-member.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe-slice.log (Task 1 run)
    - bbj-vscode/src/language/validations/check-unknown-java-member.ts (after Task 1)
    - bbj-vscode/test/unknown-java-member.test.ts (after Task 1)
  </read_first>
  <behavior>
    - Each new false-positive shape found in the full run has a synthetic test that failed before its guard and passes after
    - Every 107-03 test still passes, including the live BBjAPI() file
  </behavior>
  <action>
Run the review over every accepted file and close it (D-12).

1. Run the probe over the whole manifest in the background: output `phase-107-live-member-probe.jsonl`, progress `phase-107-live-member-probe.log`. Poll the log. If the run dies, restart from the last completed index with `--from`.
2. Classify every finding as in Task 1. Group identical shapes; for (b), (c) and (d) shapes add a synthetic failing test and the narrowest guard, as in Task 1 step 4.
3. Re-run the probe on the files that had (b), (c) or (d) findings (write their ids to a local list and add a `--ids <file>` option) until only (a) findings remain. Keep the final full-run output and the re-run output side by side in the snapshots folder.
4. Run `unknown-java-member.test.ts`, the live functional file, and the whole suite with `RUN_BBJ_TESTS=0 --maxWorkers=2`.
5. SUMMARY: files probed, crashes (count), findings per class before and after the guards, each false-positive shape in own words with its guard and test name, and a table of class (a) findings by shape in own words (for example "unknown method name on a constructed object of a JDK collection class": count). No ids, names or source lines. The SUMMARY also names the guard list 107-06 uses to classify the harness's test-double artifacts.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts && RUN_BBJ_TESTS=1 npx vitest run test/functional/unknown-java-member-real-interop.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in either summary line</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-107-05-vitest.json; node -e "const r=require('/tmp/phase-107-05-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-107-05-vitest.json</fails_when>
    <human-check>At end of phase, read the SUMMARY's classification table: every remaining unknown-member Error on the corpus is a genuinely unknown member (class a), and each guarded false-positive shape is one the user agrees BBj accepts at runtime.</human-check>
  </verify>
  <acceptance_criteria>
    - `test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-live-member-probe.jsonl` succeeds, and the log's last progress line shows the manifest's full record count
    - The SUMMARY's after-guard counts show zero class (b), (c) and (d) findings
    - Whole suite prints `numFailedTests=0`
    - `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/bbj-vscode/test/unknown-java-member.test.ts /home/coder/repos/bbj-language-server/bbj-vscode/src/language/validations/check-unknown-java-member.ts` exits 0
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>Every unknown-member Error over the accepted corpus is a genuinely unknown member, every false-positive shape is guarded and pinned by a synthetic test, and the record carries counts and own-words shapes only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus → public repository | proprietary source is read locally; only counts, own-words shapes and synthetic tests may enter the repository |
| java-interop backend → language server | real class descriptions decide what counts as a member |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-12 | Information disclosure | SUMMARY, tests, commit messages | high | mitigate | Probe and outputs stay in the untracked snapshots folder of the corpus repository; tests are synthetic; `leak-guard.mjs` runs on every changed tracked file. |
| T-107-13 | Tampering (developer trust) | `checkUnknownJavaMember` | medium | mitigate | Every corpus finding is classified against the live class description; every non-genuine shape is guarded and pinned by a synthetic test before the plan closes. |
| T-107-14 | Denial of service | BBjServices on :5008 | low | accept | One probe process at a time against the local backend; warm-up once, classes cached for the run. |
| T-107-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- The full-corpus probe output and its guarded re-run exist outside the repository; only class (a) findings remain
- `unknown-java-member.test.ts` and the live functional file pass; whole suite `numFailedTests=0`
- Leak guard and register check clean
</verification>

<success_criteria>
- A local conformance run shows no new Error on a member that exists (ROADMAP criterion 5, last part)
</success_criteria>

<output>
Create `.planning/phases/107-validation-false-alarms-silent-skips/107-05-SUMMARY.md` when done
</output>
