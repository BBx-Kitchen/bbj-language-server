---
phase: 107-validation-false-alarms-silent-skips
plan: 06
type: execute
wave: 3
depends_on: ["107-02", "107-04", "107-05"]
files_modified:
  - .planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md
  - .planning/todos/pending/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md
  - .planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md
  - .planning/todos/pending/2026-09-24-flag-unknown-method-on-java-object-as-error.md
  - .planning/todos/completed/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md
  - .planning/todos/completed/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md
  - .planning/todos/completed/2026-09-24-flag-unknown-method-on-java-object-as-error.md
  - .planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A final harness run on the phase's last tree, in the same mode as the 107-04 base run, is snapshotted and compared with the base by file set, never by totals alone (D-13)"
    - "The re-flagged Phase 98 files (the 107-04 target set) carry no line-break error in the final tree, per the per-file probe (ROADMAP criterion 3; D-03)"
    - "No file newly enters B (raw `missed`, and `missedReconciled` when the endpoint mode was used), compared by file set; any newcomer is listed with its per-file finding from 107-04 for the human check (ROADMAP criterion 3; D-13)"
    - "A2 is reported three ways: the raw harness count; the count without files whose only Errors are unknown-member Errors on the test double's fake classes for members the live backend has (harness artifacts, from 107-05's live output); and the count without genuinely unknown members accepted by 107-05's review. The comparable A2 (both groups removed) is at or below 22; every other newly entered A2 file is a regression that stops the plan (ROADMAP criterion 3; D-13)"
    - "The harness's validation-crash count (details.json `checkExceptions`) falls from the base's value (2 on 2026-09-23) to 0, the corpus-side evidence for VAL-02 (ROADMAP criterion 4)"
    - "The whole vitest suite reports numFailedTests=0 with RUN_BBJ_TESTS=0 --maxWorkers=2, and the live-gated files (linking.test.ts, functional/issue440-real-interop.test.ts, functional/unknown-java-member-real-interop.test.ts) fail on no name that passes on the phase base (D-12)"
    - "The phase's source and test diff contains no planning identifier (register check), and every tracked file the phase changed passes leak-guard.mjs (D-13)"
    - "The three folded todos move to .planning/todos/completed/ and a new pending todo records the deferred VAL-03 extras (the Rule 2 exemption for member linking warnings and the 'NamedElement' wording) plus the D-11 note to revisit a severity setting only if false positives appear"
    - "Edge (concurrency): the final harness run starts only when no other harness run or probe is active, and details.json is snapshotted before it"
  artifacts:
    - path: ".planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md"
      provides: "final numbers, file-set comparisons, gate verdicts"
      contains: "## 4. Final measurement"
    - path: ".planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md"
      provides: "deferred VAL-03 extras"
  key_links:
    - from: "phase-107-final-details.json"
      to: "phase-107-base-details.json"
      via: "file-set diffs of falseAlarms, missed, missedReconciled"
      pattern: "phase-107-base-details"
    - from: "newly entered A2 files"
      to: "phase-107-live-member-probe.jsonl"
      via: "artifact classification of unknown-member Errors seen only with the test double"
      pattern: "phase-107-live-member-probe"
  prohibitions:
    - requirement_id: VAL-01
      category: confidentiality
      statement: "MUST NOT write a corpus file name, id, path or source line into 107-CONFORMANCE.md, a todo, a SUMMARY or a commit message (D-13)"
      flagged: true
    - requirement_id: VAL-03
      category: integrity
      statement: "MUST NOT exclude a newly entered A2 file from the comparable A2 count unless every one of its Error diagnostics is an unknown-member Error classified as a harness artifact or an accepted genuinely unknown member; everything else counts and is reported"
      flagged: true
  assumptions:
    - statement: "The harness validates with the test double (worker.mts uses createBBjTestServices), whose five fake Java classes carry only a few real members each, so the unknown-member check can raise the raw A2 count for members that exist on the real classes. The comparable A2 count removes only files proven to be such artifacts (member present in the live backend's description) or accepted genuinely unknown members; the human check at the end of the phase accepts or rejects this reading of the A2 <= 22 gate"
      source: "worker.mts, bbj-test-module.ts; planning analysis"
      flagged: true
    - statement: "Requirement checkboxes in REQUIREMENTS.md are left to the phase verifier; no plan in this phase edits REQUIREMENTS.md"
      source: "project memory: continuation executors flipped shared requirements early"
      flagged: true
---

<objective>
Measure the finished phase against its base on the private corpus, run the whole suites, check the
diff for planning identifiers and corpus text, and close the folded todos (VAL-01, VAL-02, VAL-03;
D-12, D-13).

Purpose: success criterion 3 and the last part of criterion 5 are only observable on the private
corpus, and the phase closes on a file-set comparison with the base, not on totals.
Output: `107-CONFORMANCE.md` section 4 with gate verdicts, snapshots outside the repository, the
three todos in `completed/`, and one new pending todo for the deferred extras.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-CONTEXT.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-02-SUMMARY.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-04-SUMMARY.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-05-SUMMARY.md

<interfaces>
- Snapshots from earlier plans (outside the repository): `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-base-details.json` (+ `-summary.json`, `-run.log`; the log says whether `--endpoint` was used), `phase-107-val01-details.json`, `phase-107-file-probe.mts` (per-file Error diagnostics with the harness's own services), `phase-107-live-member-probe.jsonl` (every unknown-member Error with the live backend, after 107-05's guards).
- `details.json`: `falseAlarms` (A2, first validation diagnostic per file), `missed` (raw B), `missedReconciled` (B with the endpoint verdict), `checkExceptions`; `summary.json` totals.
- Harness: `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server [--endpoint 127.0.0.1:5008]`; leak check: `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs <files>`.
- Todo convention: a resolved todo moves unchanged from `.planning/todos/pending/` to `.planning/todos/completed/` (see commit 41acf680, "close 1 resolved todo(s)").
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  Allowed `cd` form: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Use `git -C` for
  git. Stage with `git add <exact path>` (or `git -C … mv` for the todo moves) only. Never use `git stash`.
- No corpus text may leave the private repository (D-13): counts, message groups and own-words shapes
  only. Run `leak-guard.mjs` on every tracked file you write. Write markdown table separator rows as
  `| --- | --- |` (short dashes with spaces): a long unspaced dash row can equal a generic corpus line.
- One harness run or probe at a time; snapshot `details.json` and `summary.json` before the run;
  compare file sets.
- Whole-suite runs use `RUN_BBJ_TESTS=0 --maxWorkers=2`, judged on `numFailedTests`. A failing name is
  compared with the phase base in a scratch worktree (`/tmp/phase-107-base-wt`, symlinked node_modules,
  copied generated folder), never labelled environment noise without that comparison.
- Do not edit REQUIREMENTS.md, source or test files in this plan. A regression found here stops the plan
  and is reported; the orchestrator routes it to gap closure.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md.

## Artifacts this phase produces

- (107-01) `elseClaims` counter in `line-break-validation.ts`; line-break tests; fixture lines.
- (107-02) `symbol?.` reads in `check-variable-scoping.ts`; guard in `bbj-scope-local.ts`; tests.
- (107-03, 107-05) `validations/check-unknown-java-member.ts` (`UNKNOWN_JAVA_MEMBER_CODE`,
  `isFullyResolvedJavaClass`, `hasCertainReceiverType`, `checkUnknownJavaMember`,
  `registerUnknownJavaMemberChecks`), `dropShadowedMemberLinkingDiagnostics`, registration,
  `test/unknown-java-member.test.ts`, `test/functional/unknown-java-member-real-interop.test.ts`,
  the updated issue #440 expectation in `test/linking.test.ts`.
- (107-04) `107-CONFORMANCE.md` sections 1-3; base and re-measure snapshots.
- (this plan) `107-CONFORMANCE.md` section 4; outside the repository `snapshots/phase-107-final-details.json`,
  `phase-107-final-summary.json`, `phase-107-final-run.log`; the three todos in
  `.planning/todos/completed/`; new `.planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md`.

<tasks>

<task type="tracer">
  <name>Task 1: Final measurement against the base, suites and diff checks, end to end</name>
  <files>.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md</files>
  <precondition>The snapshots phase-107-base-details.json, phase-107-file-probe.mts and phase-107-live-member-probe.jsonl exist under /home/coder/repos/bbj-corpus/conformance/snapshots/, and BBjServices accepts connections on 127.0.0.1:5008 if the base run used --endpoint</precondition>
  <read_first>
    - .planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md (sections 1-3)
    - .planning/phases/107-validation-false-alarms-silent-skips/107-04-SUMMARY.md and 107-05-SUMMARY.md
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-base-run.log (first lines: the mode used)
    - .planning/milestones/v4.5-phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md (the A2 = 22 exit gate)
  </read_first>
  <action>
Close the measurement (D-03, D-12, D-13).

1. Confirm no harness run or probe is active, snapshot `details.json`/`summary.json` if they differ from the latest `phase-107-*` snapshot, and run the harness on the main checkout in the base run's mode, output to `snapshots/phase-107-final-run.log`. Copy the outputs to `phase-107-final-details.json` and `phase-107-final-summary.json`.
2. File-set comparison with `phase-107-base-details.json`:
   - Target set (from 107-04): run `phase-107-file-probe.mts` on it; every file has zero `bbj-line-break` Errors.
   - B: ids newly in `missed`, and newly in `missedReconciled` if the endpoint was used; for each, the finding from 107-04 (or a fresh local reading), in own words.
   - A2: ids newly in `falseAlarms` and ids that left it. For every newly entered id, run the file probe and list all its Error diagnostics with codes. Classify the file as: harness artifact when every Error carries `bbj-unknown-java-member` and none of those lines appears in `phase-107-live-member-probe.jsonl` for that id; accepted genuinely unknown member when every Error carries that code and each line does appear there (class a in 107-05); otherwise regression.
   - `checkExceptions` count base versus final.
   If any regression appears, stop and report it; do not adjust code in this plan.
3. Suites: the whole suite `RUN_BBJ_TESTS=0 --maxWorkers=2` (verify command); then `RUN_BBJ_TESTS=1 npx vitest run test/linking.test.ts`, `… test/functional/issue440-real-interop.test.ts` and `… test/functional/unknown-java-member-real-interop.test.ts`, each on its own; compare failing names with the same files on the phase base.
4. Register check over the phase diff and leak guard over every line the phase added to a tracked file (verify commands). The guard scans added lines only because `.planning/ROADMAP.md` already carries, before this phase, a long unspaced table separator row (Progress table) that equals a generic corpus line; a hit on any added line is a real finding to fix.
5. Append `## 4. Final measurement` to 107-CONFORMANCE.md: date, commit, mode; a table of A, A2 (raw), A2 without harness artifacts, A2 comparable (without artifacts and accepted genuinely unknown members), raw B, reconciled B, checkExceptions, each with base value and delta; the target set result; newly entered B files by count with own-words findings; the artifact and accepted counts by message group; the suite results; and one verdict line per criterion-3 clause (re-flagged files clean, comparable A2 at or below 22, no new B). Run the leak guard on it.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-107-06-vitest.json; node -e "const r=require('/tmp/phase-107-06-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-107-06-vitest.json</fails_when>
    <automated>B=$(git -C /home/coder/repos/bbj-language-server rev-parse "$(git -C /home/coder/repos/bbj-language-server log --reverse --format=%H --grep='(107-0[1-6])' | head -1)^") && ! git -C /home/coder/repos/bbj-language-server diff "$B" HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b' && echo register-clean</automated>
    <fails_when>"register-clean" is not printed: the base could not be resolved, or an added source or test line carries a planning identifier</fails_when>
    <automated>B=$(git -C /home/coder/repos/bbj-language-server rev-parse "$(git -C /home/coder/repos/bbj-language-server log --reverse --format=%H --grep='(107-0[1-6])' | head -1)^") && rm -rf /tmp/phase-107-added && mkdir -p /tmp/phase-107-added && for f in $(git -C /home/coder/repos/bbj-language-server diff --name-only --diff-filter=AM "$B" HEAD); do git -C /home/coder/repos/bbj-language-server diff "$B" HEAD -- "$f" | grep '^+' | grep -v '^+++' | sed 's/^+//' > "/tmp/phase-107-added/f_$(echo "$f" | tr '/' '_')"; done && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /tmp/phase-107-added/*</automated>
    <fails_when>non-zero exit, any line starting with "LEAK", or "checked 0 file(s)" (nothing was scanned)</fails_when>
    <human-check>At end of phase, read 107-CONFORMANCE.md section 4: the re-flagged files are clean, the comparable A2 is at or below 22 and the artifact/accepted split is plausible, no file newly entered B (or each newcomer's finding is acceptable), and checkExceptions fell to 0. Approve or name the clause that fails.</human-check>
  </verify>
  <acceptance_criteria>
    - `test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-final-details.json` succeeds
    - `grep -c "^## 4. Final measurement" /home/coder/repos/bbj-language-server/.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md` prints 1, and the section has one verdict line for each of the three criterion-3 clauses
    - The SUMMARY lists the failing names of the three live-gated files on HEAD and on the base, and they are equal apart from tests this phase added or deliberately changed (named)
    - Whole suite prints `numFailedTests=0`; register check prints `register-clean`; leak guard exits 0
  </acceptance_criteria>
  <done>The final tree is measured against the base by file set, every gate clause has a recorded verdict, the suites are green, and the phase diff carries no planning identifiers and no corpus text.</done>
</task>

<task type="auto">
  <name>Task 2: Close the folded todos and record the deferred extras</name>
  <files>.planning/todos/pending/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md, .planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md, .planning/todos/pending/2026-09-24-flag-unknown-method-on-java-object-as-error.md, .planning/todos/completed/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md, .planning/todos/completed/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md, .planning/todos/completed/2026-09-24-flag-unknown-method-on-java-object-as-error.md, .planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md</files>
  <read_first>
    - .planning/phases/107-validation-false-alarms-silent-skips/107-CONTEXT.md (Folded Todos and Deferred Ideas)
    - .planning/todos/pending/2026-09-24-flag-unknown-method-on-java-object-as-error.md ("Optional extras")
    - one completed todo for the frontmatter shape, e.g. .planning/todos/completed/2026-09-23-live-parse-waits-on-shared-connection-breaker.md
  </read_first>
  <action>
Close the bookkeeping the discussion folded into this phase.

1. Only if Task 1 recorded no regression: move the three folded todos unchanged with `git -C /home/coder/repos/bbj-language-server mv .planning/todos/pending/<name> .planning/todos/completed/<name>` for the balance-rule todo, the use-before-assignment todo and the unknown-Java-member todo.
2. Create `.planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md` with frontmatter `created: 2026-09-24`, a `title` naming the two deferred extras, `area: validation`, `severity: minor`, and a `files:` list (`bbj-vscode/src/language/bbj-document-validator.ts` for the hierarchy, `bbj-vscode/src/language/bbj-linker.ts` for the message wording). Body: "Problem" (a member linking Warning on an uncertain receiver is still hidden by Rule 2 when any Error exists, and its message says "NamedElement"), "Deferred from" (Phase 107 discussion, deferred ideas), "Options" (exempt unresolved Java member linking warnings from Rule 2 while keeping Rule 1; reword the member linking message), and a note that a setting to downgrade the unknown-member Error stays unbuilt unless false positives appear. No corpus text.
3. Run the leak guard on the new todo, stage exactly the moved and new paths, and commit with the subject `docs(phase-107): close 3 resolved todo(s)`.
  </action>
  <verify>
    <automated>test -f /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md && test -f /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md && test -f /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-09-24-flag-unknown-method-on-java-object-as-error.md && test ! -e /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-24-flag-unknown-method-on-java-object-as-error.md && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md && echo closeout-ok</automated>
    <fails_when>"closeout-ok" is not printed: a todo was not moved, is still pending, the new todo is missing, or the leak guard reports a LEAK line</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server show --stat --format=%s HEAD` names the three renames and the new todo under the subject `docs(phase-107): close 3 resolved todo(s)`
    - `grep -c "NamedElement" /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-24-unknown-java-member-linking-warning-extras.md` prints 1 or more
  </acceptance_criteria>
  <done>The three folded todos are in completed/, the deferred extras are recorded as one pending todo, and the commit carries no corpus text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus → public repository | final numbers and findings are written into a tracked planning file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-15 | Information disclosure | 107-CONFORMANCE.md, todos, SUMMARY | high | mitigate | Counts, message groups and own-words shapes only; `leak-guard.mjs` over every tracked file the phase changed. |
| T-107-16 | Repudiation (gate evidence) | A2/B verdicts | medium | mitigate | File-set diffs against a base measured on the phase base commit; the artifact exclusion is rule-based (every Error an unknown-member Error, line absent from the live probe) and reported next to the raw count for the human check. |
| T-107-17 | Tampering (planning identifiers in public code) | phase diff | medium | mitigate | Register check over the whole source and test diff from the phase base. |
| T-107-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- Final snapshot exists; 107-CONFORMANCE.md section 4 has a verdict per criterion-3 clause
- Whole suite `numFailedTests=0`; live-gated files match the base by failing name
- Register check clean over the phase diff; leak guard clean over every changed tracked file
- Three todos in completed/, one new pending todo
</verification>

<success_criteria>
- ROADMAP criterion 3 verdicts recorded from a before/after file-set comparison
- ROADMAP criterion 4 corroborated by checkExceptions falling to 0
- ROADMAP criterion 5's corpus clause corroborated by 107-05's review and the artifact classification
</success_criteria>

<output>
Create `.planning/phases/107-validation-false-alarms-silent-skips/107-06-SUMMARY.md` when done
</output>
