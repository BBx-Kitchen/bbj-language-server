---
phase: 107-validation-false-alarms-silent-skips
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/language/validations/check-unknown-java-member.ts
  - bbj-vscode/src/language/bbj-validator.ts
  - bbj-vscode/src/language/bbj-document-validator.ts
  - bbj-vscode/test/unknown-java-member.test.ts
  - bbj-vscode/test/linking.test.ts
  - bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts
autonomous: true
requirements: [VAL-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`declare java.lang.String s!` then `s!.anyInvalidMethod()` shows exactly one diagnostic on `anyInvalidMethod`: an Error `Method 'anyInvalidMethod' is not defined on String` carrying code `bbj-unknown-java-member`, and no linking Warning for that reference (ROADMAP criterion 5; D-10)"
    - "Non-call access uses field wording: `x! = s!.anyInvalidField` gives `Field 'anyInvalidField' is not defined on String` (D-10)"
    - "The same unknown member on another fully resolved class fires too: `h! = new java.util.HashMap()` then `h!.anyInvalidMethod()` gives one Error on HashMap (ROADMAP criterion 5)"
    - "With a live java-interop backend on :5008, `BBjAPI().anyInvalidMethod()`, `x! = BBjAPI().anyInvalidMethod()` and `api! = BBjAPI()` then `api!.anyInvalidMethod()` each show one Error `Method 'anyInvalidMethod' is not defined on BBjAPI`, while a real member such as `getSysGui` stays clean (ROADMAP criterion 5; the live test is skipped where :5008 is closed, as in CI)"
    - "Class-reference receivers honour the static-only rule: `use java.lang.String` then `String.someInstanceField` gives one Error `Static field 'someInstanceField' is not defined on String`; `String.CASE_INSENSITIVE_ORDER` stays clean; instance receivers reach static and instance members alike (D-10)"
    - "Guard cases keep today's diagnostics and get no Error: an unresolved or cold class (error-carrying stub or no type), the synthetic BBjAPI fallback in the test double, a BBj class receiver, a template-string array field, `.class` in any case, a case-different spelling of a real member (`CHARAT`), and an inherited member such as `getClass` (D-10, D-12)"
    - "The receiver's type must also be certain: a DECLAREd or otherwise declared variable, field or parameter, a constructor call, a CAST, a string literal, a class reference, a `BBjAPI()` call, or an undeclared variable whose first assignment is one of these. A receiver typed through a method's return type or a Java field's type (for example `h!.getClass().anyInvalidMethod()`) keeps today's Warning, because BBj resolves object calls on the runtime object and a declared return type is only a lower bound (D-10's carried rule: flag only what is certain)"
    - "The Error survives the diagnostic hierarchy: with other Errors in the file (`a = 1 b = 2`) it stays visible while the unrelated linking Warning is hidden as before (Rule 2), and applyDiagnosticHierarchy keeps it when a parse error is present (Rule 1 filters only linking-coded diagnostics) (ROADMAP criterion 5; D-10)"
    - "Exactly one diagnostic per unknown member: dropShadowedMemberLinkingDiagnostics removes the linking diagnostic whose range equals the Error's range, before the pre-hierarchy list is remembered, so no later composition brings it back (D-10)"
    - "No setting downgrades the check: bbj-vscode/package.json and the IntelliJ settings are unchanged by this phase (D-11)"
    - "Every guard case named in D-12 has a unit test, and the whole vitest suite reports numFailedTests=0 with RUN_BBJ_TESTS=0 (D-12)"
    - "VAL-03 is tracked in REQUIREMENTS.md and ROADMAP.md as the discussion commit recorded it (D-09); this plan's frontmatter declares it"
    - "Edge (concurrency / cold resolution): a receiver whose class is still resolving, failed, timed out or hit the depth limit (an error-carrying stub, or no type yet) never gets the Error; the check reads only member names and isStatic, which the interop service fills before it publishes a class"
    - "The check never throws on broken member syntax: `s!.` with no member produces no 'An error occurred during validation' diagnostic"
  artifacts:
    - path: "bbj-vscode/src/language/validations/check-unknown-java-member.ts"
      provides: "the unknown-Java-member check, its guards and its registration"
      exports: ["UNKNOWN_JAVA_MEMBER_CODE", "isFullyResolvedJavaClass", "hasCertainReceiverType", "checkUnknownJavaMember", "registerUnknownJavaMemberChecks"]
    - path: "bbj-vscode/src/language/bbj-document-validator.ts"
      provides: "duplicate linking diagnostic removal"
      exports: ["dropShadowedMemberLinkingDiagnostics"]
    - path: "bbj-vscode/src/language/bbj-validator.ts"
      provides: "registration call"
      contains: "registerUnknownJavaMemberChecks(registry, services)"
    - path: "bbj-vscode/test/unknown-java-member.test.ts"
      provides: "positive, guard-case, certainty, hierarchy and dedup tests with the test double"
    - path: "bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts"
      provides: "live BBjAPI() cases against :5008"
  key_links:
    - from: "registerValidationChecks in bbj-validator.ts"
      to: "registerUnknownJavaMemberChecks"
      via: "called after registerFunctionCallChecks(registry)"
      pattern: "registerUnknownJavaMemberChecks\\(registry, services\\)"
    - from: "BBjDocumentValidator.validateDocument"
      to: "dropShadowedMemberLinkingDiagnostics"
      via: "applied to super.validateDocument's result before rememberLangiumDiagnostics"
      pattern: "dropShadowedMemberLinkingDiagnostics\\(await super\\.validateDocument"
    - from: "checkUnknownJavaMember"
      to: "services.types.Inferer.getType(receiver)"
      via: "receiver type read, then isFullyResolvedJavaClass"
      pattern: "isFullyResolvedJavaClass\\("
  prohibitions:
    - requirement_id: VAL-03
      category: trust
      statement: "MUST NOT report the Error when the receiver's class or type is uncertain (unresolved, cold, stub, synthetic, BBj class, package, or typed only through a method return or field); when in doubt the Warning stays, because a false Error misleads developers into changing correct code"
      flagged: true
    - requirement_id: VAL-03
      category: scope
      statement: "MUST NOT change bbj-scope.ts, bbj-linker.ts or java-interop.ts (Phase 109 owns resolveClass and the member scope); they are read-only references here"
      flagged: true
  assumptions:
    - statement: "The certainty guard (receiver typed by a declaration, constructor, CAST, literal, class reference or BBjAPI() call) is a planning-time reading of D-10's carried 'flag only what is certain' rule and of the discretion on how 'fully resolved' is detected. It keeps every example D-10 and criterion 5 name. 107-05's live corpus review measures whether it is enough"
      source: "planning analysis: BBj dispatches object method calls on the runtime object"
      flagged: true
    - statement: "Class-reference access to a member that exists only as an instance member is reported as an Error with 'Static …' wording (the literal reading of D-10's static-only rule). The live-gated issue #440 test in linking.test.ts that expected one linking Warning for `String.someInstanceField` is updated to expect this Error instead"
      source: "D-10 wording; test/linking.test.ts 'Instance field does NOT resolve on a Java class reference'"
      flagged: true
    - statement: "A genuine parser error in this grammar swallows the rest of the file in every shape research tried, so Rule 1 survival is proven with a direct applyDiagnosticHierarchy test; an integration test is added only if a local recovery shape is found"
      source: "107-RESEARCH.md Pitfall 1"
      flagged: true
---

<objective>
Report a method call or field read that does not exist on a fully resolved Java class as one Error
that the diagnostic hierarchy cannot hide, and keep today's Warning for every uncertain receiver
(VAL-03, D-09..D-12).

Purpose: `BBjAPI().anyInvalidMethod()` is only a linking Warning today, which Rule 1 and Rule 2 hide
in any file with another error, and nothing else reports it.
Output: `validations/check-unknown-java-member.ts`, its registration, the duplicate-diagnostic
filter in `BBjDocumentValidator`, unit tests with the test double and a live-gated functional test.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-CONTEXT.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-RESEARCH.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-PATTERNS.md
@.planning/todos/pending/2026-09-24-flag-unknown-method-on-java-object-as-error.md
@bbj-vscode/src/language/bbj-validator.ts
@bbj-vscode/src/language/bbj-document-validator.ts
@bbj-vscode/src/language/bbj-type-inferer.ts
@bbj-vscode/test/bbj-test-module.ts

<interfaces>
Read-only sources (do not modify):
- `TypeInferer.getType(expression: Expression): JavaPackage | Class | undefined` (bbj-type-inferer.ts). SymbolRef: `symbol.ref` is an Assignment → type of `value`; a Class → itself; a FieldDecl/ArrayDecl/VariableDecl → `getClass(type)`; MethodDecl → return type. ConstructorCall → `getClass(klass)`. MemberCall → JavaField `resolvedType`, JavaMethod `resolvedReturnType ?? getResolvedClass(returnType)`, literal member text `class` → `java.lang.Class`. CastExpression → cast type. StringLiteral → `java.lang.String`. MethodCall → type of `method`.
- `JavaClass` (generated/ast.ts): `name`, `error?: string`, `methods: JavaMethod[]`, `fields: JavaField[]`; `JavaMethod`/`JavaField` have `name` and `isStatic: boolean`. `createStubClass` in java-interop.ts sets `error` for every failed, aborted or depth-limited resolution; the test double's `stubClass` sets `error: 'not resolved (test double)'`.
- `isTemplateStringArray(array: ArrayDecl): boolean` exported from `bbj-scope-local.ts`; bbj-linker.ts `doLink` skips `isSymbolRef(receiver) && isArrayDecl(receiver.symbol.ref) && isTemplateStringArray(receiver.symbol.ref)`.
- bbj-scope.ts lines 198-233: class reference = receiver is a SymbolRef whose `symbol.ref` is a JavaClass (read inside try/catch for cyclic references); such a scope offers only `isStatic` members plus `class`.
- `MemberCall { receiver: Expression; member?: Reference<NamedElement> }`; `MethodCall { method: Expression; args }`; a call `s!.foo()` is `MethodCall{ method: MemberCall }`.
- `registerValidationChecks(services)` in bbj-validator.ts ends with `registerClassChecks(registry, services); registerVariableScopingChecks(registry); registerFunctionCallChecks(registry);`.
- `BBjDocumentValidator.validateDocument` line 275: `const diagnostics = await super.validateDocument(document, options, cancelToken);` then line 283 `rememberLangiumDiagnostics(document, diagnostics, validatedText)`. Langium pushes linking diagnostics (`processLinkingErrors`, code `DocumentValidator.LinkingError` = 'linking-error', `range: reference.$refNode?.range`) before `validateAst`, into the same list.
- `applyDiagnosticHierarchy(diagnostics, suppressEnabled, maxErrors)` exported from bbj-document-validator.ts.
Test-double facts measured at planning time (createBBjTestServices): the String fake's `name` is `String`, HashMap's is `HashMap`; `declare java.util.ArrayList a!` gives no receiver type plus the Warning "Type 'java.util.ArrayList' cannot be resolved."; `BBjAPI()` gives no receiver type (it resolves to a LibFunction); `s!.CHARAT(0)` fails linking (the member scope is case-sensitive); `dim rec$:"NAME:C(10)"` then `rec.nosuchfield = 1` gives no diagnostic; `a = 1 b = 2` gives two Error `bbj-line-break` diagnostics with no parser error.
</interfaces>
</context>

<assumption_delta_decision>
noun: other errors in the same file (criterion 5, "also when the file has other errors")
decision: no-change
rationale: the pluralization signal describes the new Error coexisting with other diagnostics in one file, not a second entity or model; it is covered by the Rule 2 integration test and the Rule 1 applyDiagnosticHierarchy test in Task 3.
</assumption_delta_decision>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` form is `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`.
  Use `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>`
  only, never `-A` or `.`. Never use `git stash`. Never run a path-wide `git restore --source`.
  Prefer the Read tool over shell reads.
- Source and test text must not contain planning identifiers: no D-NN, no VAL-family ids, no 107-0N
  plan numbers, no review ids (WR-A, CR-NN, WR-NN, IN-NN), no threat ids. Issue numbers (#440) are fine.
- Do not modify `bbj-scope.ts`, `bbj-linker.ts`, `java-interop.ts` or `bbj-test-module.ts`.
- Unit tests use `createBBjTestServices(EmptyFileSystem)` with `parseHelper`/`validationHelper`, never
  `DocumentBuilder.build`. The only live test is the functional file in Task 3, gated on
  `shouldRunBBjTests()` exactly like `test/functional/issue440-real-interop.test.ts`.
- Run vitest with cwd `bbj-vscode`. Whole-suite runs use `RUN_BBJ_TESTS=0 --maxWorkers=2` and are judged
  on `numFailedTests`. For any failing test name, run that file on the phase base in a scratch worktree
  (`git -C /home/coder/repos/bbj-language-server worktree add --detach /tmp/phase-107-base-wt <base>`,
  symlink `bbj-vscode/node_modules` from the main checkout, copy `bbj-vscode/src/language/generated/`
  from the main checkout since the grammar is unchanged, run the file, remove the worktree) and list the
  name with its base result in the SUMMARY; never call a failure "environment noise" without that
  comparison. Live-gated baseline on this machine: `linking.test.ts` "Interop related tests" has 11
  known failures and the issue447 capability test 1; a different set of names is a regression.
  Never use vitest's `basic` reporter.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark VAL-03 complete
  in REQUIREMENTS.md; the phase verifier closes it.

## Artifacts this phase produces

- (107-01) `elseClaims` counter in `line-break-validation.ts`; tests and fixture lines.
- (107-02) `symbol?.` reads in `check-variable-scoping.ts`; guard in `bbj-scope-local.ts`; tests.
- (this plan) new module `bbj-vscode/src/language/validations/check-unknown-java-member.ts` exporting
  `UNKNOWN_JAVA_MEMBER_CODE` (`'bbj-unknown-java-member'`), `isFullyResolvedJavaClass(type)`,
  `hasCertainReceiverType(receiver)`, `checkUnknownJavaMember(memberCall, accept, typeInferer)`,
  `registerUnknownJavaMemberChecks(registry, services)`; `dropShadowedMemberLinkingDiagnostics(diagnostics)`
  exported from `bbj-document-validator.ts`; the registration call in `bbj-validator.ts`; new
  `test/unknown-java-member.test.ts`; new `test/functional/unknown-java-member-real-interop.test.ts`;
  the updated issue #440 instance-field expectation in `test/linking.test.ts`.
- (107-05) further guards in the same module and tests, if the live corpus review finds false positives.
- (107-04, 107-06) `107-CONFORMANCE.md`; snapshots and probes outside the repository; todo close-out.

<tasks>

<task type="tracer">
  <name>Task 1: An unknown method on a declared Java object is one Error on the member name, end to end</name>
  <files>bbj-vscode/src/language/validations/check-unknown-java-member.ts, bbj-vscode/src/language/bbj-validator.ts, bbj-vscode/src/language/bbj-document-validator.ts, bbj-vscode/test/unknown-java-member.test.ts</files>
  <reversibility rating="reversible">New validation module plus one filter call; removing the registration and the filter restores today's behaviour, and no contract outside the language server reads the new code.</reversibility>
  <read_first>
    - bbj-vscode/src/language/bbj-validator.ts (lines 7-75 imports and registerValidationChecks, 195-273 checkMemberCallUsingAccessLevels for the accept shape)
    - bbj-vscode/src/language/bbj-document-validator.ts (lines 117-183 applyDiagnosticHierarchy, 270-389 validateDocument and processLinkingErrors)
    - bbj-vscode/src/language/bbj-type-inferer.ts (whole file)
    - bbj-vscode/src/language/bbj-scope.ts (lines 190-235, read only)
    - bbj-vscode/src/language/bbj-linker.ts (lines 71-87 and 112-131, read only)
    - bbj-vscode/src/language/validations/check-classes.ts (lines 1-30, the register function shape)
    - bbj-vscode/test/bbj-test-module.ts (fake classes: String with charAt, static CASE_INSENSITIVE_ORDER, instance someInstanceField; HashMap with put and getClass; stubClass)
  </read_first>
  <action>
Build the check with every D-10 guard, register it, and remove the duplicate linking diagnostic.

1. Red first: create `bbj-vscode/test/unknown-java-member.test.ts` with `const services = createBBjTestServices(EmptyFileSystem)`, `const parse = parseHelper<Model>(services.BBj)`, `beforeAll(() => initializeWorkspace(services.shared))`, and a helper that returns the diagnostics whose message mentions a given member name. Tests in `describe('Unknown member on a fully resolved Java class', …)`:
   - 'an unknown method on a declared Java object is one Error on the member name': `declare java.lang.String s!\ns!.anyInvalidMethod()\n` → exactly one diagnostic mentions anyInvalidMethod; severity Error; message `Method 'anyInvalidMethod' is not defined on String`; `data.code` is `UNKNOWN_JAVA_MEMBER_CODE`; its range is line 1, characters 3 to 19; and no diagnostic in the document has `data.code === DocumentValidator.LinkingError`.
   - 'field access uses field wording': `declare java.lang.String s!\nx! = s!.anyInvalidField\n` → one Error `Field 'anyInvalidField' is not defined on String`.
   - 'an unknown method on a constructed Java object is one Error': `h! = new java.util.HashMap()\nh!.anyInvalidMethod()\n` → one Error `Method 'anyInvalidMethod' is not defined on HashMap`.
   Confirm they fail (today each gives one Warning with code linking-error).
2. Create `bbj-vscode/src/language/validations/check-unknown-java-member.ts`:
   - `export const UNKNOWN_JAVA_MEMBER_CODE = 'bbj-unknown-java-member'`.
   - `export function isFullyResolvedJavaClass(type: unknown): type is JavaClass`: `isJavaClass(type) && !type.error`. Doc comment: `error` is set on every stub the interop service returns for a failed, cancelled, cold or depth-limited resolution, so only a class the backend actually described passes; the synthetic BBjAPI fallback is a BBj class and never passes.
   - `export function hasCertainReceiverType(receiver: Expression, depth = 0): boolean`: false when `depth > 8`; true for a ConstructorCall, a CastExpression, a StringLiteral; for a MethodCall, true only when its `method` is a SymbolRef whose `symbol?.$refText` lowercases to `bbjapi`; for a SymbolRef read `receiver.symbol?.ref` inside try/catch (cyclic references throw; return false) and return true for a JavaClass (class reference) or a VariableDecl that is not an ArrayDecl (DECLARE, typed field, typed parameter), recurse into `value` with `depth + 1` for an Assignment (an undeclared variable takes its first assignment's certainty), false otherwise; false for a MemberCall and everything else. Doc comment in plain words: BBj resolves object method calls on the runtime object, so a type reached through a method's return type or a field's type is only a lower bound and must not produce an Error.
   - `export function checkUnknownJavaMember(memberCall: MemberCall, accept: ValidationAcceptor, typeInferer: TypeInferer): void`, guards in this order, each returning silently: no `memberCall.member`; member text lowercased is `class`; receiver is a SymbolRef whose `symbol?.ref` (inside try/catch) is an ArrayDecl that `isTemplateStringArray` accepts; `memberCall.member.ref` (inside try/catch; a throw also returns) is defined, meaning linking resolved it; `typeInferer.getType(receiver)` is not `isFullyResolvedJavaClass`; `!hasCertainReceiverType(receiver)`. Then `isClassRef` = receiver is a SymbolRef whose ref (try/catch) is a JavaClass. Compare the member text lowercased with every `receiverType.methods` and `receiverType.fields` name lowercased; for a class reference only members with `isStatic` count. Any match returns. Otherwise the kind is method when `isMethodCall(memberCall.$container) && memberCall.$container.method === memberCall`, else field; the message is `Method '<text>' is not defined on <receiverType.name>` or `Field '…'`, prefixed `Static method` / `Static field` for a class reference; `accept('error', message, { node: memberCall, property: 'member', data: { code: UNKNOWN_JAVA_MEMBER_CODE } })`.
   - `export function registerUnknownJavaMemberChecks(registry: ValidationRegistry, services: BBjServices): void` registering `MemberCall: (memberCall, accept) => checkUnknownJavaMember(memberCall, accept, services.types.Inferer)`. Import `BBjServices` as a type only.
   No setting gates the check (D-11); `typeResolutionWarningsEnabled` does not apply to it.
3. In `bbj-validator.ts` import `registerUnknownJavaMemberChecks` and call `registerUnknownJavaMemberChecks(registry, services);` after `registerFunctionCallChecks(registry);`.
4. In `bbj-document-validator.ts` add `export function dropShadowedMemberLinkingDiagnostics(diagnostics: Diagnostic[]): Diagnostic[]`: collect the ranges of diagnostics whose `data?.code === UNKNOWN_JAVA_MEMBER_CODE`; if there are none return the input array unchanged; otherwise return a copy without every diagnostic whose `data?.code === DocumentValidator.LinkingError` and whose range has the same start and end line and character as one of them. Doc comment: the check targets the same member CST node the linker's diagnostic does, so equal ranges identify the same reference; removing it here, before the list is remembered, keeps one diagnostic per unknown member on every later path (hierarchy, verdict, kept check). In `validateDocument` change line 275 to `const diagnostics = dropShadowedMemberLinkingDiagnostics(await super.validateDocument(document, options, cancelToken));`.
5. Run the new file and tsc.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx tsc -p tsconfig.json</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const UNKNOWN_JAVA_MEMBER_CODE = 'bbj-unknown-java-member'" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/validations/check-unknown-java-member.ts` prints 1
    - `grep -c "registerUnknownJavaMemberChecks(registry, services)" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-validator.ts` prints 1
    - `grep -c "dropShadowedMemberLinkingDiagnostics(await super.validateDocument" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-document-validator.ts` prints 1
    - `git -C /home/coder/repos/bbj-language-server diff --stat HEAD -- bbj-vscode/src/language/bbj-scope.ts bbj-vscode/src/language/bbj-linker.ts bbj-vscode/src/language/java-interop.ts bbj-vscode/package.json` prints nothing (read-only files and no new setting, D-11)
    - The three tests failed before steps 2-4 and pass after
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>An unknown member on a declared or constructed Java object is one Error on the member name, with no duplicate linking Warning, and the check, registration and filter are committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every guard case keeps today's diagnostics, and the suite agrees</name>
  <files>bbj-vscode/test/unknown-java-member.test.ts, bbj-vscode/test/linking.test.ts, bbj-vscode/src/language/validations/check-unknown-java-member.ts</files>
  <read_first>
    - bbj-vscode/test/unknown-java-member.test.ts (after Task 1)
    - bbj-vscode/src/language/validations/check-unknown-java-member.ts (after Task 1)
    - bbj-vscode/test/linking.test.ts (lines 1-60 helpers, 295-425 the live-gated "Interop related tests" block, especially 'Instance field does NOT resolve on a Java class reference - issue #440')
  </read_first>
  <behavior>
    - Unresolved/cold class: `declare java.util.ArrayList a!\na!.anyInvalidMethod()\n` keeps its linking Warning for anyInvalidMethod and gets no diagnostic with the new code
    - `isFullyResolvedJavaClass` is false for `{ $type: 'JavaClass', name: 'X', error: 'Resolution failed or depth limit exceeded', methods: [], fields: [] }` and true for `services.BBj.java.JavaInteropService.getResolvedClass('java.lang.String')`
    - Synthetic BBjAPI: `BBjAPI().anyInvalidMethod()\n` keeps exactly its one linking Warning, no Error
    - BBj class receiver: `class public Foo\nclassend\ndeclare Foo f!\nf!.nothing()\n` keeps its Warning, no Error
    - Static rule: `use java.lang.String\nx! = String.CASE_INSENSITIVE_ORDER\n` has no diagnostics; `use java.lang.String\nx! = String.someInstanceField\n` has one Error `Static field 'someInstanceField' is not defined on String` and no linking diagnostic; `declare java.lang.String s!` then `x! = s!.CASE_INSENSITIVE_ORDER` and then `x! = s!.someInstanceField` have no diagnostics
    - Inherited: `declare java.util.HashMap h!\nx! = h!.getClass()\n` has no diagnostics
    - `.class`: `declare java.lang.String s!` then `x! = s!.class`, `x! = s!.CLASS`, and `use java.lang.String` then `x! = String.class`: no diagnostic with the new code
    - Template string: `dim rec$:"NAME:C(10)"\nrec.nosuchfield = 1\n` has no diagnostics
    - Case-insensitive: `declare java.lang.String s!\nx! = s!.CHARAT(0)\n` keeps its Warning, no Error
    - Uncertain receiver: `declare java.util.HashMap h!\nx! = h!.getClass().anyInvalidMethod()\n` and `declare java.util.HashMap h!\nc! = h!.getClass()\nc!.anyInvalidMethod()\n` keep their Warning, no Error; `hasCertainReceiverType` of the `h!.getClass()` call is false and of `new java.util.HashMap()` true
    - Broken syntax: `declare java.lang.String s!\ns!.\n` has no message starting 'An error occurred during validation'
  </behavior>
  <action>
Pin every D-12 guard case and reconcile the existing suite.

1. Add the behavior cases to `unknown-java-member.test.ts` in a `describe('Receivers that keep today\'s diagnostics', …)` block and a `describe('Static-only access through a class reference', …)` block, with plain labels (for example 'an unresolved class keeps its linking warning', 'the method-less BBjAPI fallback keeps its linking warning', 'a member reached through a method return keeps its linking warning'). For the Warning-kept cases assert both that the linking-coded Warning for that member is still present and that no diagnostic carries `UNKNOWN_JAVA_MEMBER_CODE`. Call the exported predicates directly for the two pure cases (build the receiver nodes by parsing and picking the MemberCall receiver with `AstUtils.streamAllContents(...).filter(isMemberCall)`).
2. If a case fails, fix the check in `check-unknown-java-member.ts` so the guard holds; never weaken an assertion to make it pass.
3. In `linking.test.ts`, update 'Instance field does NOT resolve on a Java class reference - issue #440' (live-gated): it now expects no linking-coded diagnostic for someInstanceField and exactly one Error diagnostic `Static field 'someInstanceField' is not defined on String`. Keep the test name.
4. Run the whole suite: `RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-107-03-vitest.json`. If any test fails because an unknown member on a preloaded fake class is now an Error, decide per test whether that is the intended new behaviour (update the expectation, name the test in the SUMMARY) or a false positive (fix the guard). Compare any other failure with the phase base as the executor rules describe.
5. Run `RUN_BBJ_TESTS=1 npx vitest run test/linking.test.ts` and compare its failing names with the base run of the same file (known baseline: 11 names in "Interop related tests"); the issue #440 instance-field test must pass.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-107-03-vitest.json; node -e "const r=require('/tmp/phase-107-03-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-107-03-vitest.json</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "Static field 'someInstanceField' is not defined on String" /home/coder/repos/bbj-language-server/bbj-vscode/test/linking.test.ts` prints 1
    - `grep -c "CHARAT" /home/coder/repos/bbj-language-server/bbj-vscode/test/unknown-java-member.test.ts` prints 1 or more, and the same for `getClass().anyInvalidMethod()` and `NAME:C(10)`
    - Whole suite prints `numFailedTests=0`; the SUMMARY lists every pre-existing test whose expectation changed and why
    - The SUMMARY lists the failing names of `RUN_BBJ_TESTS=1 npx vitest run test/linking.test.ts` on HEAD and on the phase base, and the sets are equal
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>Every D-12 guard case and the certainty guard are pinned, the issue #440 expectation reflects the new Error, and the whole suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The Error survives other errors, and BBjAPI() fires against the live backend</name>
  <files>bbj-vscode/test/unknown-java-member.test.ts, bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts</files>
  <precondition>BBjServices accepts connections on 127.0.0.1:5008 (`timeout 3 bash -c '</dev/tcp/127.0.0.1/5008'` exits 0); without it the live test skips and the literal BBjAPI() case of criterion 5 stays unproven</precondition>
  <read_first>
    - bbj-vscode/test/functional/issue440-real-interop.test.ts (whole file: gating, setConnectionConfig, JavadocProvider init, warm-up polling)
    - bbj-vscode/src/language/bbj-ws-manager.ts (lines 180-222: classpath and implicit-import loading during initializeWorkspace)
    - bbj-vscode/src/language/java-interop.ts (lines 655-700 loadImplicitImports, read only)
    - bbj-vscode/src/language/bbj-document-validator.ts (applyDiagnosticHierarchy)
  </read_first>
  <behavior>
    - Rule 2: `declare java.lang.String s!\ns!.anyInvalidMethod()\na = 1 b = 2\nq = nosuchvar\n` keeps the Error for anyInvalidMethod next to the line-break Errors, and has no diagnostic mentioning nosuchvar (unrelated Warning hidden as before)
    - Rule 1: `applyDiagnosticHierarchy([parse error with code DocumentValidator.ParsingError, linking Warning with code DocumentValidator.LinkingError, Error with UNKNOWN_JAVA_MEMBER_CODE], true, 20)` returns the parse error and the unknown-member Error and drops the linking Warning
    - `dropShadowedMemberLinkingDiagnostics`: a linking diagnostic with the same range as an unknown-member Error is removed; one with a different range is kept; a list without unknown-member Errors is returned as the same array
    - Live (runs only when :5008 is reachable): `BBjAPI().anyInvalidMethod()`, `x! = BBjAPI().anyInvalidMethod()` and `api! = BBjAPI()` + newline + `api!.anyInvalidMethod()` each give exactly one Error `Method 'anyInvalidMethod' is not defined on BBjAPI` and no linking diagnostic for it; `api! = BBjAPI()` + newline + `sg! = api!.getSysGui()` gives no diagnostic mentioning getSysGui
  </behavior>
  <action>
Prove criterion 5's survival and its literal BBjAPI() example.

1. Add the Rule 2, Rule 1 and filter cases to `unknown-java-member.test.ts` in `describe('The unknown-member Error in files with other errors', …)`. Build the synthetic diagnostics for the pure tests as plain `Diagnostic` objects with explicit ranges.
2. Try at most three local parser-error shapes (for example a lone `)` line, `x$ = "abc` without a closing quote on its own line) above the unknown-member line; if one leaves the later line linked and validated, add it as an integration test; otherwise record in the SUMMARY that each tried shape swallowed the rest of the file, matching research, and rely on the Rule 1 unit test.
3. Create `bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts` following `issue440-real-interop.test.ts`: `const run = await shouldRunBBjTests()`, `createBBjServices(NodeFileSystem)`, `beforeAll` (skip when not run) initializing JavadocProvider, `setConnectionConfig('127.0.0.1', 5008)`, `initializeWorkspace(services.shared)`, then, if `getResolvedClass('BBjAPI')` is still undefined, `await interop.loadImplicitImports()`, then poll up to 90 s until `getResolvedClass('BBjAPI')` has a method whose `$container` is set (the cold-resolution warm-up). Tests use `test.runIf(run)` with a 60 s timeout and `parseHelper<Model>(services.BBj)(text, { validation: true })`. If BBjAPI() cannot be made to resolve to its JavaClass in bare services after following the workspace manager's own loading sequence, stop and report it in the SUMMARY; do not weaken the assertions.
4. Run the new unit file, then the functional file with `RUN_BBJ_TESTS=1`, then the register check.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/unknown-java-member.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/unknown-java-member-real-interop.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0, or the summary shows every test skipped (the live case did not run)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "is not defined on BBjAPI" /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts` prints 1 or more
    - `grep -c "shouldRunBBjTests" /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts` prints 1 or more (skipped in CI)
    - The live file reports its BBjAPI tests as passed, not skipped, on this machine
    - `grep -c "nosuchvar" /home/coder/repos/bbj-language-server/bbj-vscode/test/unknown-java-member.test.ts` prints 1 or more
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>The Error is proven to survive Rule 1 and Rule 2, exactly one diagnostic remains per unknown member, and BBjAPI().anyInvalidMethod() fires against the live backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BBj source text → validator | untrusted program text chooses receivers and member names |
| java-interop backend → language server | class descriptions (member names, isStatic, error) come from the :5008 service |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-06 | Tampering (developer trust) | `checkUnknownJavaMember` | medium | mitigate | A false Error pushes users to "fix" correct code. Fires only on a fully resolved class (`!error`), a certain receiver type, and a name absent case-insensitively; every D-12 guard case and the certainty guard are unit-tested; 107-05 reviews every Error across the corpus against the live backend. |
| T-107-07 | Denial of service (of diagnostics) | the new check | low | mitigate | Broken member syntax and cyclic references are guarded (optional reads, try/catch around `.ref` exactly as bbj-scope.ts and the type inferer do); a test asserts `s!.` produces no validation-crash diagnostic. |
| T-107-08 | Spoofing (stale interop data) | class descriptions | low | accept | A class description older or newer than the runtime BBj could list different members; bbjcpl on save stays authoritative and 107-05's corpus review surfaces such cases. |
| T-107-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- `unknown-java-member.test.ts` passes; the live functional file passes with `RUN_BBJ_TESTS=1` on this machine
- Whole suite `numFailedTests=0` with `RUN_BBJ_TESTS=0 --maxWorkers=2`; `linking.test.ts` live failures equal the base set by name
- `bbj-scope.ts`, `bbj-linker.ts`, `java-interop.ts` and `package.json` unchanged; register check clean
</verification>

<success_criteria>
- An unknown member on a fully resolved Java class with a certain receiver type is one Error that survives the hierarchy (ROADMAP criterion 5)
- Unresolved, cold, synthetic, BBj-class, template-string and uncertain receivers keep today's Warning
</success_criteria>

<output>
Create `.planning/phases/107-validation-false-alarms-silent-skips/107-03-SUMMARY.md` when done
</output>
