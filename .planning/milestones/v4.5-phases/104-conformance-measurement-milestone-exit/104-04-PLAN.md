---
phase: 104-conformance-measurement-milestone-exit
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs
  - /home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs
  - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md
  - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md
  - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md
autonomous: true
requirements: [CONF-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`leak-guard.mjs` compares backtick-normalized text on both sides and checks every corpus source line three ways: the whole line, its first 110 characters (the width `run.mjs`'s `code()` cuts report excerpts to) and its first 30 characters (a partial quote); candidates under 15 characters are skipped. A report excerpt that was truncated, backtick-substituted or only partly quoted is reported as `LEAK <file> line <n>: corpus source line`, and the guard still never prints the matched text."
    - "A self-test next to the guard sends a synthetic, corpus-shaped line of more than 110 characters with a backtick through `run.mjs`'s own `code()` transform into the guard, along with a partial quote of it. It fails unless both are reported as leaks, a clean synthetic file passes, and the guard's output holds none of the synthetic text. Run against the unfixed guard, it fails."
    - "The fixed guard, run over the pre-fix revision of `100-CONFORMANCE.md` (commit eb6faab6), reports the four table rows the phase verification found at lines 76, 176, 279 and 419."
    - "`98-`, `99-`, `100-`, `103-`, `104-CONFORMANCE.md` and `PROJECT.md` pass the fixed guard on both corpus builds: the current data and the September 1 baseline at commit cdaf3761 (D-05). A separate scanner also passes them with 0 hits. It checks the 30-character prefix of every corpus and reject line of both builds and of every historical `details.json` source. It also checks whole-line containment of details and reject sources of 15 or more characters (D-10)."
    - "No markdown file under `.planning/` that the milestone branch adds or changes has a 30-character window that equals the start of a line found only in private corpus sources. A line that also exists in this repository's own examples or test data, or in a publishable source, is recorded but not edited."
    - "Each rephrased line describes its diagnostic or statement shape in own words. It keeps its counts and table structure and stays one line, and it names no real variable, expression, string, class or file. The SUMMARY and the commit messages refer to lines by file and line number only (D-10)."
    - "bbj-corpus gets exactly two commits, the self-test and then the guard fix, each staged by exact path. `eval/` and the corpus data are untouched, and nothing is pushed (D-12). The baseline worktree is removed at the end. CONF-02 and CONF-03 are not marked complete, and this plan pushes nothing to PR #691."
  artifacts:
    - path: "/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs"
      provides: "Leak guard that matches normalized whole lines, report-truncated prefixes and quoted leading parts of corpus source lines"
      contains: "QUOTED_PREFIX"
    - path: "/home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs"
      provides: "Self-test: synthetic fixture through run.mjs's code() into the guard, three leak shapes, one clean file, no-echo check"
      contains: "SELF-TEST FAIL"
    - path: "/home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md"
      provides: "The phase 100 record with every corpus-derived excerpt rephrased in own words"
      contains: "## Closing run"
  key_links:
    - from: "/home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs"
      to: "/home/coder/repos/bbj-corpus/conformance/run.mjs"
      via: "loads the single `const code = ` line of run.mjs and imports it, so the fixture goes through the real report transform, not a copy"
      pattern: "const code = "
    - from: "/home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs"
      to: "/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs"
      via: "spawns the guard with --data pointing at a temporary synthetic fixture root and checks exit status and LEAK lines"
      pattern: "leak-guard\\.mjs"
    - from: "/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs"
      to: "/home/coder/repos/bbj-corpus/conformance/details.json"
      via: "every string property named `source` becomes up to three normalized candidates"
      pattern: "details\\.json"
---

<objective>
Close the one open gap from the phase 104 verification, ROADMAP success criterion 4: "no corpus content and no proprietary
BBj source text in this repository". First, fix the private leak guard so it catches what a person can
actually copy out of `REPORT.md`. Then find every corpus-derived excerpt in the milestone's public
planning text, using both the fixed guard and a separate, stricter scanner, and rephrase each one in
own words, as commit 5633abf7 did (D-10).

Purpose: The phase 104 verification found real corpus text in `100-CONFORMANCE.md` that
`leak-guard.mjs` had reported clean. The cause is in the guard's matching: it compares full-length
stored lines against report text that `run.mjs`'s `code()` helper has already truncated to 110
characters and backtick-substituted (104-REVIEW.md, the Critical finding).

Planner findings. These come from probes run while planning, against commit eb6faab6; they are recorded
as line numbers only. The executor re-derives every hit with the tools and does not trust this list:
- The review's suggested fix alone (normalize backticks, add the 110-character prefix) does **not**
  flag the four rows the verifier found. Those rows quote a leading part of a source line that is at
  least 30 characters long but shorter than both 110 characters and the whole line. So this plan
  adds a third candidate, the 30-character leading prefix, which is the verifier's own method. With it,
  the guard flags exactly those four rows (lines 76, 176, 279, 419 of `100-CONFORMANCE.md`) and
  nothing else in the six records.
- The guard only knows lines that went through a `details.json` or a reject record. A scan against
  every corpus and reject line found three more lines in `100-CONFORMANCE.md`. Line 479 was found by
  its 30-character prefix. Lines 252 and 671 contain a whole 15-20-character line from an earlier
  run's details.
- In the rest of the milestone's planning markdown, three lines have a 30-character window that
  matches a line found only in private sources: `ROADMAP.md` lines 373 and 415, and
  `100-05-SUMMARY.md` line 175. Other windows matched lines that also exist in this repository's own
  `examples/` or test data, or in a publishable source. Those are not leaks.

Output: two bbj-corpus commits (the self-test, then the guard fix); up to three commits in this
repository that rephrase lines in place; the scanner kept as ignored scratch under
`bbj-corpus/conformance/snapshots/` for re-verification; the baseline worktree created and removed
again.

Not in this plan:
- The review's three warnings and two info items.
- Any change under `bbj-vscode/` or `bbj-intellij/`.
- Marking CONF-02 or CONF-03 complete.
- Any push.

The text this plan removes is already in the pushed history of PR #691. A squash merge keeps it out of
`main`, but GitHub keeps the PR's commits. Pushing this fix, and deciding whether to rewrite that
history, are the user's decisions.
</objective>

<executor_shell_rules>
Read this before running anything. The permission hooks block opaque shell reads and cannot tell a
scoped search from a secret read, so a blocked command leaves a background executor stuck on an
unanswered prompt.

- Every shell path is absolute and complete. Never use `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. Use `git -C <repo> …` for every
  git command, never `cd`.
- `git add <exact path>` only, in both repositories. Never `-A`, never `.`, never a directory.
- No blind recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or any
  credential store.
- Never `git stash`. Never create or switch a git branch in either repository. In bbj-corpus, never use
  `git reset`, `git rebase` or `git checkout` in the main working tree.
- bbj-corpus: `eval/` belongs to another project, so never read, stage, move or delete anything under
  it. Never modify corpus data (`corpus/`, `rejects/`, `manifest.jsonl`, `rejects.jsonl`,
  `build-info.json`, `sources.json`) in either the main tree or the baseline worktree. The repository
  has no remote: never push.
- bbj-language-server: never push. The PR update belongs to the user.
- Prefer the Read tool for reading files. For content search, use `grep` against an exact absolute file
  path (the Grep tool may be unavailable here).
- Commits: plain `git -C <repo> commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"`.
  The GSD commit helper writes no trailer, so do not use it. If your session provides a
  `Claude-Session:` trailer, put it in the same `-m` paragraph as `Co-Authored-By`.
- **Never reproduce corpus text.** Do not echo, quote or paste any line you replace, or any part of it,
  and do not paste any corpus line, identifier or expression, into a commit message, the SUMMARY, your
  final message or a terminal command's arguments. Refer to lines by file and line number only. Both
  tools print file, line, kind or family and source ids, never the matched text. Keep it that way.
- Record resolved deviations in the SUMMARY only. Do not add `WINDOWS.md` entries. Do not mark CONF-02 or
  CONF-03 complete in `REQUIREMENTS.md`, because the phase re-verification does that.
</executor_shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-conformance-measurement-milestone-exit/104-VERIFICATION.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-REVIEW.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-CONTEXT.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This list covers this gap plan only. Plans 01-03 list the phase's earlier artifacts.

bbj-corpus (private, local-only, no remote):
- `conformance/leak-guard.mjs` [04, modified]. Constants `REPORT_TRUNCATE` = 110, `QUOTED_PREFIX` = 30
  and `MIN_PATTERN` = 15, plus a `normalize` step (trim, then every backtick becomes `'`) applied to
  patterns and to scanned lines. Each corpus source line gives up to three candidates. Each
  (line, kind) is reported once. The CLI, output format and exit codes 0/1/2 are unchanged.
- `conformance/leak-guard.test.mjs` [04, new]. Run as `node conformance/leak-guard.test.mjs`. It prints
  `ok <case>` per passing case and `SELF-TEST FAIL <case>: <reason>` per failing one, and exits 1 on any
  failure.
- `conformance/snapshots/phase-104-04-prefix-scan.mjs` [04, new, ignored scratch, never committed].
  Run as `node --max-old-space-size=6144 <scanner> [--broad] <file>…`. It prints
  `HIT <file> line <n>: prefix|exact [sources: …]` per hit, prints
  `PUBLIC <file> line <n>: …` for hits it does not count in `--broad` mode, and exits 1 on any counted hit.
- Scratch worktree `/home/coder/repos/bbj-corpus-baseline`, detached at `cdaf3761`, created in task 2
  and removed after the SUMMARY check.

bbj-language-server (public):
- `.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md` [04]: flagged
  lines rephrased in place.
- `.planning/ROADMAP.md` [04]: flagged lines inside the phase 99/100 success criteria rephrased in place.
- `.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md` [04]: one
  flagged line rephrased in place.
- Any other public file the scans flag, handled the same way and named in the SUMMARY.
- `.planning/phases/104-conformance-measurement-milestone-exit/104-04-SUMMARY.md` [04].
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: PSRV-0|CONF-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|(CR|WR|IN)-[0-9][0-9]|T-10[0-9]|LEAK|HIT|SELF-TEST FAIL -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end: the fixed guard flags the verifier's four rows, and those rows are rephrased until it passes</name>
  <files>/home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs (new), /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs, /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</files>
  <precondition>`git -C /home/coder/repos/bbj-corpus status --porcelain` prints exactly `?? eval/` and `git -C /home/coder/repos/bbj-corpus log -1 --format=%h -- conformance/leak-guard.mjs` prints `ebcb461f`.</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs: the whole file (127 lines), which is the file being fixed. Read the pattern collection at lines 40-95 and the scan at lines 101-127.
    - /home/coder/repos/bbj-corpus/conformance/run.mjs: line 192, the `code()` helper the self-test must load and use (trim, backtick to `'`, first 110 characters).
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-REVIEW.md: lines 47-127, the Critical finding (mechanism, suggested fix, suggested regression fixture).
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-VERIFICATION.md: lines 8-32, the gap (truth, reason, missing items).
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md: lines 66-80, 166-180, 269-283 and 409-423, the message tables of the runs for plans 01-04 that hold the four rows.
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md: line 118, and /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md line 185. These show how earlier hits were rephrased: invented names and own-words shapes. Read these current lines rather than the old diff.
  </read_first>
  <behavior>
    - Fixture sanity: `code()` output for the long synthetic line is shorter than the line itself, so truncation is exercised. Neither `code()` output contains a backtick between its enclosing pair, so substitution is exercised.
    - Case truncated: a markdown table row that holds `code()` output of the long synthetic line gives guard exit 1 and the line `LEAK <target> line <n>: corpus source line` for that row.
    - Case backtick: a prose line that holds `code()` output of the short synthetic line gives guard exit 1 and a LEAK line for it.
    - Case partial: a row shaped like a diagnostic message that holds the first 40 characters of the long synthetic line (backtick-normalized), followed by an ellipsis, gives guard exit 1 and a LEAK line for it.
    - Case clean: a sentence made of invented words gives guard exit 0 and `clean: <target>`.
    - Case no-echo: for the three leak cases, the guard's stdout contains no 15-character slice of either synthetic line.
  </behavior>
  <action>
    Step 1 (RED). Create `/home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs`, a plain Node ESM
    script with no arguments and no dependencies beyond `node:` built-ins (`fs`, `os`, `path`,
    `child_process`, `url`). It works as follows:
    - It reads `run.mjs` from its own directory and selects the lines that start with `const code = `.
      Exactly one must exist, or it fails with the reason "run.mjs code() not found". It imports that
      line through a `data:text/javascript,` URL built from the line plus an `export default code;`
      line (encodeURIComponent). The test never copies the transform.
    - It creates a fixture root with `mkdtempSync` under `os.tmpdir()`, prefix `leak-guard-fixture-`, and
      writes two files there:
      - `manifest.jsonl`: one record with an invented id such as `fixture/zz-synthetic-accepted.bbj`,
        source `fixture` and no origin.
      - `rejects.jsonl`: one record whose `errors` array holds two entries, with `source` set to
        SYNTH_LONG and SYNTH_SHORT.
    - SYNTH_LONG and SYNTH_SHORT are invented at authoring time and are not copied from any corpus file.
      They are shaped like a BBj line of several `;`-separated statements and use only identifiers that
      share one distinctive invented prefix.
      - SYNTH_LONG is at least 150 characters long, with a backtick inside a string literal before
        position 100.
      - SYNTH_SHORT is 40-100 characters long and also contains a backtick.
      - Neither line contains a digit-dash-digit sequence or a name that looks like a corpus id.
    - Each case writes its target `.md` file into the fixture root and runs
      `spawnSync(process.execPath, [<test dir>/leak-guard.mjs, '--data', <fixture root>, <target>], { encoding: 'utf8' })`.
      It then checks the exit status and the stdout lines exactly as `<behavior>` states.
    - It prints `ok <case>` or `SELF-TEST FAIL <case>: <reason>`. The reason states the expectation and
      does not print fixture text. It removes the fixture root in a `finally` block and exits 1 when any
      case failed.
    - Comments use plain words and carry no planning identifiers.

    Run it against the unchanged guard. Cases truncated, backtick and partial must fail; record which
    ones failed in the SUMMARY. Commit with subject "conformance: a self-test for the leak guard",
    staging exactly `conformance/leak-guard.test.mjs`.

    Step 2 (GREEN). Fix `/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs` as the review's
    finding describes, plus one addition:
    - Add constants `REPORT_TRUNCATE = 110` (the width `run.mjs`'s `code()` cuts report excerpts to),
      `QUOTED_PREFIX = 30` (the shortest leading part of a source line treated as a quote of it) and
      `MIN_PATTERN = 15` (the existing floor).
    - Add `normalize(text)`: trim, then replace every backtick with `'`, which is the substitution
      `code()` applies. Apply it to every pattern when collecting and to every scanned line before
      matching.
    - For kind `corpus source line`, at both collection points (reject `errors[].source` and details.json
      `source`), store up to three candidates:
      - the whole normalized line;
      - its first `REPORT_TRUNCATE` characters;
      - its first `QUOTED_PREFIX` characters, but only when the line has at least that many.

      Skip any candidate shorter than `MIN_PATTERN` and dedupe on kind plus candidate, as today.
    - Id, file name and origin patterns keep today's rules, now normalized. Do not give them a new
      length floor.
    - A line hits a candidate when the normalized line contains it. For speed, keep the
      `QUOTED_PREFIX`-length candidates in a Set and look up each `QUOTED_PREFIX`-wide window of the
      normalized line; use `includes` for the rest.
    - Report each (line, kind) at most once, in the unchanged `LEAK <file> line <n>: <kind>` format. Never
      print the matched text or the pattern. Keep `clean: <file>`, the final `checked … pattern(s)` line
      (now counting candidates), `--data`, and exit codes 0/1/2. Leave the id-shape regex exactly as it
      is.
    - Rewrite the header comment in plain words, with no planning identifiers. Say that report excerpts
      are truncated and backtick-substituted before anyone can copy them, so the guard compares
      normalized text and checks whole lines, report-width prefixes and quoted leading parts.

    Run the self-test until every case prints `ok`.

    Step 3 (end-to-end on the real record, read-only). Write the pinned pre-fix revision
    (`git -C /home/coder/repos/bbj-language-server show eb6faab6:.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md`)
    to `/tmp/bbj-104-04-100-before.md`, then run the fixed guard over it. The guard must report
    `corpus source line` at lines 76, 176, 279 and 419. Then commit with subject
    "conformance: leak guard catches truncated, backtick-substituted and partly quoted excerpts",
    staging exactly `conformance/leak-guard.mjs`. Only commit once the self-test, this check and the
    register check (third verify command) all pass.

    Step 4 (rephrase the four rows in own words). Each of the four rows is a line of a message table,
    counting how often a diagnostic message occurred in the runs for plans 01-04. Read the rows with the
    Read tool, then edit each row in place with the Edit tool so that one line stays one line.
    - Keep the count column and the language server's fixed message wording.
    - Replace the program-specific text the message carries with an own-words shape description, for
      example a parenthesized phrase naming the kind of statement or expression. Use the same wording in
      all four rows.
    - Use no real variable, expression, string, class or file name. Do not copy the replaced text
      anywhere else.

    Run the fixed guard over the file (default data root). It must exit 0. Then commit with subject
    "docs(104-04): replace corpus-derived diagnostic text in the phase 100 conformance record",
    staging exactly that one file.
  </action>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs</automated>
    <fails_when>non-zero exit or any `SELF-TEST FAIL` line: a leak shape is missed, the clean file is flagged, the guard's output echoes fixture text, or run.mjs's `code()` line cannot be loaded</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server show eb6faab6:.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md > /tmp/bbj-104-04-100-before.md && { node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /tmp/bbj-104-04-100-before.md > /tmp/bbj-104-04-guard-before.txt; test $? -eq 1; } && grep -qx 'LEAK /tmp/bbj-104-04-100-before.md line 76: corpus source line' /tmp/bbj-104-04-guard-before.txt && grep -qx 'LEAK /tmp/bbj-104-04-100-before.md line 176: corpus source line' /tmp/bbj-104-04-guard-before.txt && grep -qx 'LEAK /tmp/bbj-104-04-100-before.md line 279: corpus source line' /tmp/bbj-104-04-guard-before.txt && grep -qx 'LEAK /tmp/bbj-104-04-100-before.md line 419: corpus source line' /tmp/bbj-104-04-guard-before.txt</automated>
    <fails_when>non-zero exit: the fixed guard still reports the pinned pre-fix record clean, or misses one of the four rows the verification found</fails_when>
    <automated>! grep -nE 'PSRV-0|CONF-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|(CR|WR|IN)-[0-9][0-9]|T-10[0-9]' /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs</automated>
    <fails_when>non-zero exit with a printed `file:line:` match: a planning identifier reached the guard or its self-test</fails_when>
    <automated>node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</automated>
    <fails_when>non-zero exit or any `LEAK` line: a row the fixed guard sees is still in the record</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-corpus show --name-only --format= HEAD~1` prints exactly `conformance/leak-guard.test.mjs` and `git -C /home/coder/repos/bbj-corpus show --name-only --format= HEAD` prints exactly `conformance/leak-guard.mjs`
    - `grep -c 'QUOTED_PREFIX' /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs` ≥ 2, `grep -c 'REPORT_TRUNCATE' /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs` ≥ 2 and `grep -c 'const code = ' /home/coder/repos/bbj-corpus/conformance/leak-guard.test.mjs` ≥ 1
    - The SUMMARY records that the self-test failed the truncated, backtick and partial cases against the unfixed guard (RED) and passes against the fixed one (GREEN)
    - `git -C /home/coder/repos/bbj-language-server show --name-only --format= HEAD` prints exactly `.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md`, and `git -C /home/coder/repos/bbj-language-server diff --numstat HEAD~1 HEAD` shows equal added and deleted line counts for it (rows edited in place)
    - `git -C /home/coder/repos/bbj-corpus status --porcelain` prints exactly `?? eval/`
  </acceptance_criteria>
  <done>The guard catches report-truncated, backtick-substituted and partly quoted excerpts, proven by a synthetic self-test and on the verifier's real finding. The four rows are rephrased, and the guard passes the record. All of it is committed by exact path (D-12).</done>
</task>

<task type="auto">
  <name>Task 2: Six conformance records clean on both corpus builds, by the fixed guard and a separate prefix scanner</name>
  <files>/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs (new, ignored scratch, never committed), /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md; conditionally 98-, 99-, 103-, 104-CONFORMANCE.md and /home/coder/repos/bbj-language-server/.planning/PROJECT.md if a scan flags them</files>
  <precondition>Task 1 is committed. `/home/coder/repos/bbj-corpus-baseline` either does not exist, or is a registered worktree at `cdaf3761` with an empty `git -C /home/coder/repos/bbj-corpus-baseline status --porcelain`.</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs: as committed in task 1. The scanner reads the same inputs, so reuse its `readJsonl`/`walk` shape.
    - /home/coder/repos/bbj-corpus/sources.json: each source's `id` and `publishable` flag. `ls-examples` and `ls-test-data` are this repository's own `examples/` and `bbj-vscode/test/test-data/`.
    - /home/coder/repos/bbj-corpus/conformance/.gitignore: confirms `snapshots/` is ignored.
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md: lines 248-256, 472-483 and 666-675, around the expected scanner-only hits. Also re-read any other line a scan reports.
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md: lines 118, 731 and 745, the earlier own-words rephrasings to match in style.
  </read_first>
  <action>
    Step 1. Set up the baseline worktree, per D-05 (the milestone's own corpus build).
    - If it is absent, run
      `git -C /home/coder/repos/bbj-corpus worktree add --detach /home/coder/repos/bbj-corpus-baseline cdaf3761`.
      This creates no branch.
    - Assert that `/home/coder/repos/bbj-corpus-baseline/build-info.json` has `corpus_entries` 11898,
      `reject_entries` 1210 and `compiler.build` "build on September 1 2026", and that its status is
      empty.
    - Never edit anything in the worktree. Leave it in place until the SUMMARY check in `<output>`.

    Step 2. Write the separate scanner at
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs`. It is ignored
    scratch (D-14) and is never committed. It deliberately uses a wider pattern source than the guard,
    so the fix is not judged only by the tool being fixed.
    - Usage: `node --max-old-space-size=6144 <scanner> [--broad] <file>…`.
      - Relative target paths resolve against `/home/coder/repos/bbj-language-server`.
      - The two roots are fixed: `/home/coder/repos/bbj-corpus` and `/home/coder/repos/bbj-corpus-baseline`.
        If either root has no `manifest.jsonl`, exit 2 with a usage line.
    - Normalize text the same way as the guard: trim, then backtick becomes `'`.
    - Prefix family. For each root, read every record of `manifest.jsonl` (file `<root>/corpus/<id>`)
      and `rejects.jsonl` (file `<root>/rejects/<id>`). Read each file as latin1, split it on
      `/\r?\n/` and normalize each line. For every line of 30 or more characters, use its first 30
      characters as a key, mapped to the set of the record's `source` ids.
    - The prefix family also takes, with source `(details)`, the first 30 characters of every
      normalized string of 30 or more characters that is either:
      - a property named `source` at any depth in every committed revision of
        `conformance/details.json`. List the revisions with
        `git -C /home/coder/repos/bbj-corpus log --format=%H -- conformance/details.json` and read each
        with `git -C … show <hash>:conformance/details.json`, using a maxBuffer of at least 1 GiB;
      - such a property in every `/home/coder/repos/bbj-corpus/conformance/snapshots/*details*.json`;
      - an `errors[].source` of either root's `rejects.jsonl`.
    - Exact family (default mode only): the same details and reject `source` strings, normalized, of 15
      or more characters. A target line hits when its normalized form contains one.
    - Scanning. Normalize each target line (backtick becomes `'`) and slide a 30-character window over
      it; a window equal to a key is a hit. Report each (line, family) once as
      `HIT <file> line <n>: prefix [sources: <ids>]` or `HIT <file> line <n>: exact`. Print source ids
      only, never text.
    - `--broad` mode: prefix family only. A hit counts as private unless it is public. It is public when
      its source set contains `ls-examples` or `ls-test-data` (this repository's own content, which the
      corpus imports), or a source marked `"publishable": true` in either root's `sources.json`. Print a
      public hit as `PUBLIC <file> line <n>: prefix [sources: <ids>]`; it does not count. A hit whose
      only source is `(details)` counts as private.
    - Output. Always print `scanned <n> file(s) against <k> prefix key(s) and <m> exact string(s)`. Exit
      1 on any counted hit. Otherwise print `prefix-scan clean: <n> file(s)` and exit 0.

    Step 3. Measure. Run the fixed guard over the six records (98-, 99-, 100-, 103-, 104-CONFORMANCE.md
    and PROJECT.md) twice, once with the default data root and once with
    `--data /home/coder/repos/bbj-corpus-baseline`. Then run the scanner in default mode (both families,
    no triage) over the same six files. Every hit in these six records counts, including one that also
    has a public source. Note each hit by file, line and family or kind only.

    Step 4. Rephrase, per D-10. The planner's probe expects scanner-only hits in `100-CONFORMANCE.md`:
    - line 479 (prefix): a table cell quoting a statement;
    - lines 252 and 671 (exact): short inline code quotes in prose.

    Rephrase each hit as an own-words shape description, or as a synthetic stand-in with invented names
    that keeps the statement form the sentence is about. Edit in place with the Edit tool, so one line
    stays one line. Treat any other hit, in any of the six files, the same way. Re-run all three commands
    until each exits 0. A rephrasing can itself collide with a corpus line; if so, rephrase again.

    Step 5. Commit, staging exactly the files you changed, with subject
    "docs(104-04): replace the remaining corpus-derived text in the conformance records". If nothing
    was flagged, skip the commit and say so in the SUMMARY.
  </action>
  <verify>
    <automated>F="/home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/PROJECT.md"; node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs $F && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs --data /home/coder/repos/bbj-corpus-baseline $F</automated>
    <fails_when>non-zero exit or any `LEAK` line: a record still carries an id, file name, origin name or source-line excerpt on either corpus build</fails_when>
    <automated>node --max-old-space-size=6144 /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/PROJECT.md</automated>
    <fails_when>non-zero exit or any `HIT` line: a 30-character window equals the start of a corpus line, or a details or reject source line is contained in a record</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server show eb6faab6:.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md > /tmp/bbj-104-04-100-before.md && { node --max-old-space-size=6144 /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs /tmp/bbj-104-04-100-before.md > /tmp/bbj-104-04-scan-before.txt; test $? -eq 1; } && grep -q '^HIT /tmp/bbj-104-04-100-before.md line 76: prefix' /tmp/bbj-104-04-scan-before.txt && grep -q '^HIT /tmp/bbj-104-04-100-before.md line 479: prefix' /tmp/bbj-104-04-scan-before.txt && grep -q '^HIT /tmp/bbj-104-04-100-before.md line 252: exact' /tmp/bbj-104-04-scan-before.txt && grep -q '^HIT /tmp/bbj-104-04-100-before.md line 671: exact' /tmp/bbj-104-04-scan-before.txt</automated>
    <fails_when>non-zero exit: the scanner is silent on the pinned pre-fix record, so a clean result from it would prove nothing</fails_when>
    <automated>test "$(git -C /home/coder/repos/bbj-corpus-baseline rev-parse --short=8 HEAD)" = cdaf3761 && test -z "$(git -C /home/coder/repos/bbj-corpus-baseline status --porcelain)" && git -C /home/coder/repos/bbj-corpus check-ignore -q conformance/snapshots/phase-104-04-prefix-scan.mjs</automated>
    <fails_when>non-zero exit: the baseline worktree is missing, not at cdaf3761 or not pristine, or the scanner is not ignored scratch</fails_when>
  </verify>
  <acceptance_criteria>
    - Every file in `git -C /home/coder/repos/bbj-language-server show --name-only --format= HEAD` is one of the six records (when the task committed)
    - `git -C /home/coder/repos/bbj-language-server diff --numstat HEAD~1 HEAD` shows equal added and deleted counts per file (lines edited in place)
    - `git -C /home/coder/repos/bbj-corpus status --porcelain` prints exactly `?? eval/`, so the scanner stays ignored and nothing in bbj-corpus changed in this task
    - The SUMMARY lists every hit found in steps 3-4 by file, line and family or kind only, with the count per file before and after
  </acceptance_criteria>
  <done>All six conformance records pass the fixed guard on both corpus builds and the separate scanner with 0 hits. The scanner is shown to fire on the pre-fix record, and every rephrased line is committed by exact path (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: The milestone's planning markdown swept; lines found only in private sources rephrased</name>
  <files>/home/coder/repos/bbj-language-server/.planning/ROADMAP.md, /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md; conditionally any other planning markdown file the broad scan flags</files>
  <precondition>Task 2's scanner exists and exits 0 on the six records; `git -C /home/coder/repos/bbj-language-server rev-parse --verify origin/main` exits 0.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md: lines 364-380 (phase 99 success criteria) and 407-420 (phase 100 success criteria), where the expected hits are. Edit this file with scoped Edit calls only, never Write.
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md: lines 170-180.
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs: its `--broad` triage, as written in task 2.
  </read_first>
  <action>
    Step 1. Sweep. Run the scanner in `--broad` mode over every markdown file under `.planning/` that the
    milestone branch adds or changes. The list comes from
    `git -C /home/coder/repos/bbj-language-server diff --name-only --diff-filter=AM origin/main...HEAD -- ':(glob).planning/**/*.md'`
    (about 185 files; it includes this plan). This sweep uses the prefix family only, which is the
    verifier's method. The whole-line family is too noisy here: it matches short generic statements and
    markdown table rules, many of them also in public sources.

    Step 2. Rephrase each counted (private) hit, per D-10. The planner's probe expects:
    - `ROADMAP.md` line 373 and line 415: code quoted inside the phase 99 and phase 100 success criteria.
      Replace the real identifiers with invented ones, or with a shape description, keeping each
      criterion's meaning (which statement form must parse).
    - `100-05-SUMMARY.md` line 175: a quoted call in a bullet.

    Edit in place so one line stays one line. For `ROADMAP.md`, use only scoped Edit replacements of the
    exact line text. Treat any other counted hit the same way. For each `PUBLIC` line, record the file,
    the line and the public source id in the SUMMARY; do not edit it.

    Note: the fixed guard is not the gate for `ROADMAP.md` in this plan. It reports the progress table's
    rule line (dashes and pipes only, no letters), because one corpus line consists of the same
    characters. That is not corpus content. Do not edit the table to silence it.

    Step 3. Re-run the broad scan until it exits 0. Then re-run task 2's first two verify commands over
    the six records, to confirm nothing regressed.

    Step 4. Commit, staging exactly the files you changed, with subject
    "docs(104-04): replace corpus-derived text in the roadmap and a phase 100 summary". If nothing was
    counted, skip the commit and say so in the SUMMARY.
  </action>
  <verify>
    <automated>node --max-old-space-size=6144 /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs --broad $(git -C /home/coder/repos/bbj-language-server diff --name-only --diff-filter=AM origin/main...HEAD -- ':(glob).planning/**/*.md')</automated>
    <fails_when>non-zero exit or any `HIT` line: a milestone planning file still holds a 30-character window equal to the start of a line found only in private corpus sources</fails_when>
    <automated>node --max-old-space-size=6144 /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-04-prefix-scan.mjs --broad /home/coder/repos/bbj-language-server/.planning/ROADMAP.md /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md</automated>
    <fails_when>non-zero exit or any `HIT` line: one of the two expected files still carries a private-only excerpt</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --quiet eb6faab6 HEAD -- bbj-vscode bbj-intellij</automated>
    <fails_when>non-zero exit: this plan changed product source, which it must not</fails_when>
  </verify>
  <acceptance_criteria>
    - Every file in `git -C /home/coder/repos/bbj-language-server show --name-only --format= HEAD` is under `.planning/` and ends in `.md` (when the task committed)
    - `git -C /home/coder/repos/bbj-language-server diff --numstat HEAD~1 HEAD` shows equal added and deleted counts per file (lines edited in place)
    - `git -C /home/coder/repos/bbj-language-server diff --name-only eb6faab6 HEAD` lists only paths under `.planning/`
    - The SUMMARY lists every counted hit (fixed) and every `PUBLIC` line (left as is, with its source id) by file and line only
  </acceptance_criteria>
  <done>No planning file the milestone adds or changes carries a line found only in private corpus sources. Lines shared with this repository's own examples or with public sources are recorded, and nothing outside `.planning/` changed (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus → public planning text | The executor reads flagged public lines and private evidence, and writes own-words replacements into files that are pushed publicly later. |
| tool output → transcripts, SUMMARY, commit messages | Anything the guard or the scanner prints can land in a log or a committed file. |
| bbj-corpus working tree ↔ another project | `eval/` belongs to another project, and the corpus data is shared. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-15 | Information Disclosure | 100-CONFORMANCE.md, ROADMAP.md, 100-05-SUMMARY.md and any other flagged record | high | mitigate | The fixed guard is run on both corpus builds, and the separate scanner on both builds plus every historical details source. Every counted hit is rephrased and the checks re-run until exit 0 (tasks 1-3). |
| T-104-16 | Information Disclosure | guard and scanner output | medium | mitigate | Both print file, line, kind or family and source ids only. The self-test's no-echo case fails if the guard's stdout holds any 15-character slice of the fixture lines (task 1). |
| T-104-17 | Information Disclosure | 104-04-SUMMARY.md and commit messages | high | mitigate | File and line references only, with fixed commit subjects. Before its commit, the SUMMARY must pass the guard on both builds and the `--broad` scanner (`<output>`). |
| T-104-18 | Repudiation | the fix judged only by the tool being fixed | medium | mitigate | The separate scanner draws on every corpus line of both builds, not only the guard's sources. The guard must flag the pinned pre-fix rows and the scanner must fire on the pinned pre-fix record, so a clean result is not mere silence (tasks 1-2). |
| T-104-19 | Tampering | bbj-corpus commits | high | mitigate | Two commits, each staging one exact path. `git show --name-only` is checked, `eval/` status is asserted, and there is no stash, reset, checkout or push (task 1, D-12). |
| T-104-20 | Denial of Service | false positives from the new prefix candidate blocking later SUMMARY commits | low | mitigate | The self-test's clean case runs, and the six records and PROJECT.md must end at 0 hits with the new candidate. |
| T-104-21 | Tampering | baseline worktree | low | mitigate | It is created detached, with no branch, and asserted pristine at cdaf3761 before use. It is never edited, and is removed and pruned after the SUMMARY check. |
| T-104-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; both scripts use Node built-ins only. |
</threat_model>

<verification>
- The self-test passes against the fixed guard and failed against the unfixed one. The fixed guard flags the verifier's four rows in the pinned pre-fix record.
- The six conformance records pass the fixed guard on both corpus builds, and the separate scanner with 0 hits. The scanner fires on the pinned pre-fix record.
- The `--broad` scan over the milestone's planning markdown exits 0.
- bbj-corpus has exactly two new commits (self-test, then fix) and `eval/` is untouched. This repository has only `.planning/` changes since eb6faab6, and nothing is pushed.
- The baseline worktree is gone after the SUMMARY check.
</verification>

<success_criteria>
- ROADMAP success criterion 4 holds: the public records carry no corpus-derived excerpt that either the fixed guard or the separate scanner can find, on either corpus build.
- The leak guard no longer has the false negatives the review described or the one the verifier observed, and a self-test keeps them from coming back.
- The fix is ready for re-verification: the scanner stays at its scratch path, and the SUMMARY names every changed line by position only.
</success_criteria>

<output>
Create `.planning/phases/104-conformance-measurement-milestone-exit/104-04-SUMMARY.md` when done. It holds
counts, commit hashes, and hits by file, line and family or kind only; no replaced text. The SUMMARY
records:
- the RED and GREEN self-test results;
- the two bbj-corpus commit hashes and the public commit hashes;
- the per-file hit counts before and after, and the `PUBLIC` lines left as they are;
- the scanner's scratch path, for re-verification;
- a "Next steps" note: pushing these commits to PR #691, and deciding whether to rewrite that PR's
  pushed history, are the user's decisions; the phase needs re-verification.

Before committing the SUMMARY, run
`node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs <SUMMARY>`, the same command with
`--data /home/coder/repos/bbj-corpus-baseline`, and the scanner with `--broad <SUMMARY>`. All three
must exit 0.

Then remove the scratch worktree with `git -C /home/coder/repos/bbj-corpus worktree remove /home/coder/repos/bbj-corpus-baseline`
(no force; it is pristine) and `git -C /home/coder/repos/bbj-corpus worktree prune`. Confirm that
`git -C /home/coder/repos/bbj-corpus worktree list` no longer lists it and that
`git -C /home/coder/repos/bbj-corpus status --porcelain` prints exactly `?? eval/`.
</output>
