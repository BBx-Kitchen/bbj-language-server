---
phase: 104-conformance-measurement-milestone-exit
plan: 03
type: execute
wave: 3
depends_on: [104-01, 104-02]
files_modified:
  - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md
  - /home/coder/repos/bbj-language-server/.planning/PROJECT.md
  - /home/coder/repos/bbj-corpus/README.md
  - /home/coder/repos/bbj-corpus/conformance/REPORT.md
  - /home/coder/repos/bbj-corpus/conformance/summary.json
  - /home/coder/repos/bbj-corpus/conformance/details.json
  - /home/coder/repos/bbj-corpus/conformance/history.jsonl
autonomous: true
requirements: [CONF-03, CONF-02]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The closing measurement runs on the branch HEAD after every commit this phase makes under `bbj-vscode` or `bbj-intellij`, so the harness records `sourceModified: false`, and it reads the milestone's own corpus build (11,898 accepted, 1,210 rejects, compiler build of September 1 2026) from the detached baseline worktree, asserted before each run (D-05)."
    - "Two closing runs are recorded, each preceded by a `details.json` snapshot and compared by file set rather than totals — endpoint off, continuing the fake-interop series, then endpoint on, the gate run — both appended to the one `history.jsonl`, the endpoint-on line carrying its `endpoint` field (D-15)."
    - "The gate run shows A ≤ 25 and A2 ≤ 25 on Langium's raw view and B ≤ 60 of 1,210 (5 %) on the reconciled view, with zero endpoint failures and no crash; a run with endpoint failures does not count (D-03, D-04)."
    - "The endpoint-on run's raw falseRejects, falseAlarms and missed sets equal the endpoint-off run's sets exactly, so the raw columns keep the baseline's meaning, and every accepted file went to the endpoint (D-01, D-02, D-03)."
    - "`104-CONFORMANCE.md` records the gate table for both runs with raw and reconciled views, the milestone arc from 168 / 267 / 54.4 %, each residual list-A and A2 group in own words with the reason it remains, the residual B misses and why they remain, the accepted files that drew a BBj Parser error, the endpoint failure count and a pointer to the scoping-check todo — and no corpus id, path or source line (D-02, D-10, D-11)."
    - "PROJECT.md gains one `**Conformance starting point:**` line pointing at `104-CONFORMANCE.md`; no per-residual todo or seed is created (D-10)."
    - "bbj-corpus commits the closing run's REPORT.md, summary.json, details.json and history.jsonl plus a README exit line, by exact path, with `eval/` untouched and nothing pushed; the baseline worktree is removed afterwards (D-12, D-14)."
    - "Before the push every commit body in `origin/main..HEAD` is scanned for closing keywords and the result reported; HEAD reaches PR #691's head ref by fast-forward only; #691's title and body carry the exit numbers and a phase 104 summary; merging and `/gsd-complete-milestone` stay with the user (D-08)."
    - "An optional run on the rebuilt corpus is recorded only as the next milestone's starting point, in its own section, never in the gate table; the committed results stay the gate run's."
  artifacts:
    - path: "/home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md"
      provides: "The v4.5 exit record the next milestone starts from"
      contains: "Exit gate: A ="
    - path: "/home/coder/repos/bbj-language-server/.planning/PROJECT.md"
      provides: "Pointer from the project file to the exit record"
      contains: "Conformance starting point"
    - path: "/home/coder/repos/bbj-corpus/conformance/summary.json"
      provides: "The gate run's machine-readable result, committed"
      contains: "\"gate\""
  key_links:
    - from: "/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-summary.json"
      to: "/home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md"
      via: "the fixed-format exit-gate line must carry exactly the gate run's A, A2 and B"
      pattern: "Exit gate: A = \\d+"
    - from: "/home/coder/repos/bbj-language-server/.planning/PROJECT.md"
      to: "/home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md"
      via: "one-line pointer under Next Milestone Goals"
      pattern: "104-CONFORMANCE\\.md"
    - from: "local HEAD"
      to: "origin/gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility"
      via: "fast-forward push to PR #691's head ref"
      pattern: "HEAD:gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility"
---

<objective>
Take the milestone's closing measurement, write it down where the next milestone starts from it, commit
the corpus-side results, and grow PR #691 with the exit numbers.

Purpose: CONF-03 — on the corpus build of the baseline the milestone ends with A ≤ 25, A2 ≤ 25 and
B ≤ 5 % with the endpoint active (the suite half was proven by plan 02); and roadmap success criterion 4
— the result and the residual entries are recorded with no corpus content in this repository.
Output: two closing runs (plus an optional informational run on the rebuilt corpus), `104-CONFORMANCE.md`,
the PROJECT.md pointer, one bbj-corpus commit, the baseline worktree removed, the branch pushed to PR
#691's head by fast-forward, and #691's title and body updated.
</objective>

<executor_shell_rules>
Read this before running anything. The permission hooks block opaque shell reads and cannot tell a
scoped search from a secret read; a blocked command stalls a background executor on an unanswered
prompt.

- Prefer the Read tool for reading files; for content search use `grep` against an exact absolute
  file path (the Grep tool may be unavailable here).
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. Use `git -C <repo> …` for
  every git command. Call the harness as `node /home/coder/repos/bbj-corpus/conformance/run.mjs …`.
- No bare recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or any
  credential store.
- `git add <exact path>` only, in both repositories — never `-A`, never `.`, never a directory. In
  bbj-corpus `eval/` belongs to another project: never read, stage, move or delete anything under it;
  never stash, reset, rebase or check out a branch in its main working tree; never edit corpus data;
  never push (no remote).
- In bbj-language-server: never `git push --force` or `--force-with-lease`; never push to `main`; do not
  merge PR #691, mark it ready, or change its reviewers or labels.
- Commits: plain `git -C <repo> commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"`
  (the GSD commit helper writes no trailer).
- Long runs (every endpoint run): start with the Bash tool's `run_in_background` or `nohup … &` with
  output to a log under `/home/coder/repos/bbj-corpus/conformance/snapshots/`, then poll the log and
  the process list; never block one tool call past ten minutes.
- Nothing corpus-derived goes into this repository or the pull request: counts, commit hashes,
  keywords of the BBj language and own-words shape descriptions only — no corpus id, file name, path
  under `corpus/` or `rejects/`, origin name or source line. Private evidence you read (REPORT.md
  examples, details.json ids) stays in your terminal.
- Record deviations in the SUMMARY only; no `WINDOWS.md` entries. Do not mark CONF-02 or CONF-03
  complete in `REQUIREMENTS.md`; the phase verification does that.
</executor_shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-conformance-measurement-milestone-exit/104-CONTEXT.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-RESEARCH.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-02-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md
</context>

<artifacts_this_phase_produces>
Phase-wide list; the plan that produces each item is in brackets.

bbj-corpus (private, local-only):
- `conformance/run.mjs` [01] — `--endpoint <host>:<port>`, `--data <dir>`; endpoint-mode summary keys
  `endpoint`, `corpus.reconciled`, `rejects.reconciled`, `checkExceptions`, `gate { A, A2, B, endpointFailures }`;
  details.json keys `missedReconciled`, `acceptedWithError`, `acceptedDrewBbjParserError`,
  `endpointFailures`, `checkExceptions`; REPORT rows labelled "with the BBj verdict".
- `conformance/worker.mts` [01] — 5th argv `endpoint`, `reconcileFile`, `classifyFailureKind`,
  `RETRY_KINDS`, `PARSE_WATCHDOG_MS`, `CHECK_EXCEPTION`; per-job `reconciled` and `checkExceptions`.
- `conformance/leak-guard.mjs` [01, new] — `node leak-guard.mjs [--data <dir>] <file>…`.
- `conformance/.gitignore` [01] — `snapshots/`, `REPORT-sample.md`.
- `README.md` §"Conformance of the language server" [01 endpoint mode, 03 exit line].
- `conformance/REPORT.md`, `summary.json`, `details.json`, `history.jsonl` [01 commits the 98-103
  results; 03 commits the closing run].
- Scratch: `conformance/snapshots/phase-104-*` [01-03]; worktree `/home/coder/repos/bbj-corpus-baseline` [01 → removed 03].

bbj-language-server (public):
- `bbj-vscode/test/test-data/conformance/README.md` [02, new].
- `.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md` [02, new].
- `.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md` [03, new] — with the
  fixed-format line `**Exit gate: A = <A> (≤ 25), A2 = <A2> (≤ 25), B = <B> of 1,210 (<pct> %, ≤ 5 %), endpoint failures = 0 — passed.**`
  and the headings listed in task 2.
- `.planning/PROJECT.md` — `**Conformance starting point:**` line under "Next Milestone Goals" [03].
- PR #691 — title naming phases 98-105 and a "Phase 104 — exit measurement" body section [03].
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: CONF-0|PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|CR-[0-9]|WR-[0-9]|(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9] -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end closing measurement — endpoint off, then endpoint on, on the September 1 build, gate checked</name>
  <files>/home/coder/repos/bbj-corpus/conformance/REPORT.md, /home/coder/repos/bbj-corpus/conformance/summary.json, /home/coder/repos/bbj-corpus/conformance/details.json, /home/coder/repos/bbj-corpus/conformance/history.jsonl (written by the runs; snapshots under conformance/snapshots/, ignored)</files>
  <precondition>Plans 01 and 02 are complete; `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode bbj-intellij` prints nothing; 127.0.0.1:5008 accepts a connection; 104-01-SUMMARY.md names the Phase 103 endpoint-off baseline commit and the chosen shard count.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md — baseline commit hash, shard count, measured rate, both runtime projections, probe comparison
    - /home/coder/repos/bbj-corpus/README.md — §"Conformance of the language server" (the procedure plan 01 wrote)
    - /home/coder/repos/bbj-corpus/conformance/run.mjs — summary/details keys as committed
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/conformance-harness-overwrites-details.md — snapshot before every full run, diff file sets
  </read_first>
  <action>
    Step 0 — preconditions of a valid gate (per D-05): the language-server tree is clean under
    `bbj-vscode` and `bbj-intellij` (no later commit in this plan touches them); record
    `git -C /home/coder/repos/bbj-language-server rev-parse --short HEAD`. If the worktree
    `/home/coder/repos/bbj-corpus-baseline` is missing, recreate it with
    `git -C /home/coder/repos/bbj-corpus worktree add --detach /home/coder/repos/bbj-corpus-baseline cdaf3761`.
    Before EACH run on it assert `build-info.json` there reads `corpus_entries` 11898,
    `reject_entries` 1210, compiler build "build on September 1 2026", and that
    `git -C /home/coder/repos/bbj-corpus-baseline status --porcelain` prints nothing.

    Step 1 — endpoint-off closing run (per D-15). Snapshot first:
    `cp /home/coder/repos/bbj-corpus/conformance/details.json /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-before-off-details.json`,
    and extract the previous endpoint-off run on the same build as the comparison base:
    `git -C /home/coder/repos/bbj-corpus show <Phase 103 endpoint-off baseline commit from 104-01-SUMMARY>:conformance/details.json > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-103-off-details.json`.
    Run `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --data /home/coder/repos/bbj-corpus-baseline`
    (about 80 s). Check its summary: 11898 / 1210 files, `sourceModified` false, no `endpoint` key,
    `crashes` 0. Compare file sets with the set-diff command (second verify command's shape, pointed at
    `phase-104-103-off-details.json` and the new `details.json`, keys falseRejects, falseAlarms, missed).
    If any file moved, attribute it: list `git -C /home/coder/repos/bbj-language-server log --oneline 311c0170..HEAD -- bbj-vscode/src`,
    and where the cause is not evident from the moved files' groups, replay the harness flag-off against
    a scratch language-server worktree at `311c0170` (Node 22 for `langium:generate`, symlinked
    `node_modules`, removed afterwards) on the same `--data`; record counts and an own-words cause.

    Step 2 — endpoint-on gate run (per D-02, D-03, D-04, D-15). Snapshot:
    `cp /home/coder/repos/bbj-corpus/conformance/details.json /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-before-on-details.json`.
    Assert the build again. Start in the background:
    `nohup node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --data /home/coder/repos/bbj-corpus-baseline --shards <N from 104-01-SUMMARY> > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-run.log 2>&1 &`
    and poll until it has finished. Run the gate check (first verify command). If it fails only on
    `endpoint.failures` or `crashes`, re-run once at a lower shard count without taking a new snapshot
    (`phase-104-before-on-details.json` must stay the endpoint-off run's); if it still fails, or fails on
    A, A2 or B, STOP — write nothing into this repository, do not push, and report the failing fields.
    Then confirm (per D-01, D-03) that the endpoint-on run's raw sets equal the endpoint-off run's —
    set-diff `phase-104-before-on-details.json` against the new `details.json` for falseRejects,
    falseAlarms and missed must show left 0 and entered 0 for each; any movement is a harness defect:
    STOP and report. Also set-diff `missedReconciled` against
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-endpoint-details.json` and record
    the counts.

    Step 3 — keep the gate outputs: copy `REPORT.md`, `summary.json`, `details.json` to
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-REPORT.md`,
    `phase-104-gate-summary.json`, `phase-104-gate-details.json`.

    Step 4 — optional informational run on the rebuilt corpus (the orchestrator allowed it; it is the
    next milestone's starting point, never the gate). Only if 104-01-SUMMARY's projection for the
    rebuilt `main` corpus is 120 minutes or less: start
    `nohup node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --shards <N> > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-nextbaseline-run.log 2>&1 &`
    (no `--data`), poll to the end, copy its `summary.json` and `details.json` to
    `snapshots/phase-104-nextbaseline-summary.json` / `-details.json`, then copy the three
    `snapshots/phase-104-gate-*` files back over `REPORT.md`, `summary.json` and `details.json` so the
    files plan 03 commits are the gate run's (`history.jsonl` keeps every appended line). Otherwise skip
    it and record the projection that ruled it out.

    Step 5 — record in the SUMMARY: both closing commands, runtimes, shard count, calls, retries, every
    gate number, the three set-diff results (counts only), and the informational run's numbers or the
    reason it was skipped.
  </action>
  <verify>
    <automated>node -e 'const s=require("/home/coder/repos/bbj-corpus/conformance/summary.json");const f=[];if(s.corpus.files!==11898)f.push("corpus.files");if(s.rejects.files!==1210)f.push("rejects.files");if(s.compiler.build!=="build on September 1 2026")f.push("compiler");if(s.sampled!==false)f.push("sampled");if(s.languageServer.sourceModified!==false)f.push("sourceModified");if(!s.gate||!(s.gate.A<=25))f.push("A");if(!s.gate||!(s.gate.A2<=25))f.push("A2");if(!s.gate||!(s.gate.B<=60))f.push("B");if(!s.endpoint||s.endpoint.failures!==0||s.gate.endpointFailures!==0)f.push("endpoint.failures");if(s.crashes!==0)f.push("crashes");if(s.endpoint&&s.endpoint.calls!==13108)f.push("calls");if(f.length){console.error("GATE FAIL: "+f.join(", "));process.exit(1)}console.log("GATE PASS A="+s.gate.A+" A2="+s.gate.A2+" B="+s.gate.B+" of 1210, "+s.endpoint.calls+" calls, 0 failures")'</automated>
    <fails_when>non-zero exit or `GATE FAIL` — wrong corpus build, a sampled run, a modified source tree, a gate number over its limit, any endpoint failure or crash, or not every one of the 13,108 files sent to the endpoint</fails_when>
    <automated>node -e 'const [a,b]=["/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-before-on-details.json","/home/coder/repos/bbj-corpus/conformance/details.json"].map(f=>require(f));let moved=0;for(const k of ["falseRejects","falseAlarms","missed"]){const x=new Set(a[k].map(e=>e.id)),y=new Set(b[k].map(e=>e.id));const left=[...x].filter(i=>!y.has(i)).length,entered=[...y].filter(i=>!x.has(i)).length;moved+=left+entered;console.log(k+": off "+x.size+", on (raw) "+y.size+", left "+left+", entered "+entered)}process.exit(moved?1:0)'</automated>
    <fails_when>non-zero exit — the endpoint-on run's raw sets differ from the endpoint-off run's, so the raw columns no longer mean what the baseline meant</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --quiet "$(node -p 'require("/home/coder/repos/bbj-corpus/conformance/summary.json").languageServer.commit')" HEAD -- bbj-vscode bbj-intellij</automated>
    <fails_when>non-zero exit — the language server changed between the measured commit and HEAD, so the measurement is not of the final tree</fails_when>
    <automated>node -e 'const L=require("fs").readFileSync("/home/coder/repos/bbj-corpus/conformance/history.jsonl","utf8").trim().split("\n").map(l=>JSON.parse(l)).filter(h=>h.corpus.files===11898&&h.date>="2026-09-23");const off=L.filter(h=>!h.endpoint).pop(),on=L.filter(h=>h.endpoint).pop();if(!off||!on||on.endpoint.failures!==0||off.languageServer.commit!==on.languageServer.commit){console.error("HISTORY INCOMPLETE");process.exit(1)}console.log("history: endpoint-off A="+off.corpus.falseRejects+" A2="+off.corpus.falseAlarms+" B="+off.rejects.missed+"; endpoint-on B="+on.rejects.reconciled.missed)'</automated>
    <fails_when>non-zero exit or `HISTORY INCOMPLETE` — the history lacks this phase's endpoint-off or failure-free endpoint-on line on the September 1 build, or the two latest such lines were measured on different language-server commits</fails_when>
  </verify>
  <acceptance_criteria>
    - `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-before-off-details.json`, `phase-104-before-on-details.json`, `phase-104-gate-summary.json`, `phase-104-gate-details.json` and `phase-104-gate-REPORT.md` exist
    - `cmp /home/coder/repos/bbj-corpus/conformance/summary.json /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-summary.json` exits 0 (the committed files will be the gate run's even if the informational run happened)
    - `git -C /home/coder/repos/bbj-corpus-baseline status --porcelain` prints nothing
    - The SUMMARY holds every gate number, the three set-diff results as counts, and the informational run's outcome
  </acceptance_criteria>
  <done>The milestone's exit gate has been measured on the final tree against its own corpus build, with both views, zero endpoint failures and file sets compared, and the gate outputs are the ones on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Write the exit record and the PROJECT.md pointer, leak-guarded</name>
  <files>/home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md, /home/coder/repos/bbj-language-server/.planning/PROJECT.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md — the record format to follow (header framing, gate tables, own-words residual notes)
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md — §"The completed residue list" (line 636) and §"Gate table" (547): the list-A residue already described in own words
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md — §7 "Remaining A2 groups — final disposition"
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md — phase-boundary numbers for the arc table
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md — the IF-shaped A2 residue
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md — the todo to point at
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-REPORT.md and phase-104-gate-details.json — private; read the groups, write only own-words shapes
    - /home/coder/repos/bbj-corpus/conformance/history.jsonl — phase-boundary numbers (private file, numbers only leave it)
    - /home/coder/repos/bbj-language-server/.planning/PROJECT.md — lines 100-125: "Measurement" paragraph and "Next Milestone Goals"
  </read_first>
  <action>
    Part A — `104-CONFORMANCE.md` (per D-10), using exactly these headings in this order:
    `# Phase 104 — Conformance Measurement and Milestone Exit`, then an opening paragraph (this is the
    milestone's formal exit gate, measured by the harness's own endpoint mode; only counts and own-words
    shape descriptions are recorded — no corpus file name, path, id or source line). Then:

    `## Exit gate` — first the single line, exactly in this format with the gate run's numbers:
    `**Exit gate: A = <A> (≤ 25), A2 = <A2> (≤ 25), B = <B> of 1,210 (<pct> %, ≤ 5 %), endpoint failures = 0 — passed.**`
    with `<pct>` to one decimal. Then a table with columns Measure | Milestone start | Endpoint off
    (this phase) | Endpoint on — Langium alone (raw) | Endpoint on — with the BBj verdict (reconciled) |
    Target, rows A (of 11,898), A2 (of 11,898), B (of 1,210, with shares), endpoint failures; and one
    sentence that the gate reads A and A2 raw and B reconciled (D-03).

    `## What was measured` — both commands, date, language-server commit, `sourceModified: false`,
    shard count, runtimes, calls, retries; the corpus build (11,898 / 1,210, compiler build of
    September 1 2026, the milestone baseline's) and why it was read from a detached worktree: the corpus
    repository's main branch was rebuilt during this phase with a newer compiler and more sources, and
    roadmap criterion 2 names the baseline's build.

    `## The milestone in one table` — start (168 / 267 / 658, 54.4 %), each phase boundary that was
    measured (98, 99, 100, 103 endpoint off, 103 probe), this phase's endpoint-off run and endpoint-on run.

    `## File sets, not totals` — the three set-diff results from task 1 as counts (endpoint off against
    the Phase 103 endpoint-off run; endpoint-on raw against endpoint off; reconciled B against plan 01's
    reproduction run), with any attribution task 1 made (D-15).

    `## Compiler-accepted files once the verdict is applied` — accepted files with an Error once the
    verdict is applied, with a surviving language-server syntax Error, and that drew a BBj Parser error
    (endpoint and compiler disagree, D-02) — each disagreement described by shape in own words.

    `## Residual list A` — each remaining raw list-A group in own words and why it remains (from the
    Phase 100 residue list, updated to the gate run).

    `## Residual list A2` — each remaining raw A2 group in own words and why it remains, including the
    single-line IF shapes of the pending balance-rule todo and the non-line-break semantic checks that
    stay visible by design.

    `## Residual list B` — the reconciled misses split by the harness's reject kind (semantic / syntax),
    stating plainly that semantic-only rejects (undefined labels, functions and similar) are not visible
    to the syntax-only endpoint by design, so the next milestone must not read them as a parser gap, and
    that the save-time `bbjcpl` run is the authority for them; any syntax-kind miss described by shape
    with its reason; and how many rejects count as flagged only through a check-exception diagnostic.

    `## Endpoint failures and check exceptions` — the failure count and kinds (0), retries, the
    check-exception counts, and a pointer to
    `.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md` (D-11).

    `## Next milestone baseline (informational, not the gate)` — the rebuilt corpus's size and compiler
    build and the informational run's raw A, A2, B and reconciled B, or the reason it was not run; state
    that the next milestone should re-measure on whatever build is current when it starts.

    `## How to re-run` — the corpus README's procedure and the pointer README in
    `bbj-vscode/test/test-data/conformance/`.

    Part B — PROJECT.md (per D-10): under `## Next Milestone Goals`, directly after the heading, add
    one line: `**Conformance starting point:**` followed by: the v4.5 exit measurement, its residual
    list-A, A2 and B entries and the next-milestone baseline are in
    `.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md` (it moves with the
    v4.5 phase archive at milestone close). No figures in this line. Create no todo or seed for any
    residual (D-10).

    Part C — guards and commit: run the verify commands; then stage exactly the two files and commit
    with subject "docs(104-03): record the v4.5 exit measurement".
  </action>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/PROJECT.md && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs --data /home/coder/repos/bbj-corpus-baseline /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md /home/coder/repos/bbj-language-server/.planning/PROJECT.md</automated>
    <fails_when>non-zero exit or any `LEAK` line — a corpus id, file name, origin name or source line reached the record or PROJECT.md</fails_when>
    <automated>node -e 'const t=require("fs").readFileSync("/home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md","utf8");const s=require("/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-gate-summary.json");const m=/\*\*Exit gate: A = (\d+) \(≤ 25\), A2 = (\d+) \(≤ 25\), B = (\d+) of 1,210 \(([\d.]+) %, ≤ 5 %\), endpoint failures = 0 — passed\.\*\*/.exec(t);if(!m||+m[1]!==s.gate.A||+m[2]!==s.gate.A2||+m[3]!==s.gate.B){console.error("EXIT GATE LINE MISSING OR WRONG");process.exit(1)}const heads=["## Exit gate","## What was measured","## The milestone in one table","## File sets, not totals","## Compiler-accepted files once the verdict is applied","## Residual list A","## Residual list A2","## Residual list B","## Endpoint failures and check exceptions","## Next milestone baseline (informational, not the gate)","## How to re-run"];const miss=heads.filter(h=>!t.includes("\n"+h+"\n"));if(miss.length){console.error("MISSING SECTIONS: "+miss.join(" | "));process.exit(1)}console.log("record ok")'</automated>
    <fails_when>non-zero exit — the exit-gate line is absent or does not carry the gate run's numbers, or a required section heading is missing</fails_when>
    <automated>grep -c 'Conformance starting point:\*\*.*104-CONFORMANCE.md' /home/coder/repos/bbj-language-server/.planning/PROJECT.md</automated>
    <fails_when>prints anything other than 1 — the pointer is missing or duplicated</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server show --name-only --format= HEAD` lists exactly `.planning/PROJECT.md` and `.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md`
    - `grep -c '2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md' /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md` ≥ 1
    - `git -C /home/coder/repos/bbj-language-server diff --name-only HEAD~1 HEAD -- .planning/todos .planning/seeds` prints nothing (no residual todo or seed created by this task)
    - The "Residual list B" section states the reject-kind split and names save-time `bbjcpl` as the authority for semantic rejects
  </acceptance_criteria>
  <done>The exit record carries the gate, both views, the arc, the residuals in own words and the next milestone's starting point, PROJECT.md points at it, and both pass the leak guard.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the corpus side, remove the worktree, push to PR #691 and update its title and body</name>
  <files>/home/coder/repos/bbj-corpus/README.md, /home/coder/repos/bbj-corpus/conformance/REPORT.md, /home/coder/repos/bbj-corpus/conformance/summary.json, /home/coder/repos/bbj-corpus/conformance/details.json, /home/coder/repos/bbj-corpus/conformance/history.jsonl; conditionally the earlier CONFORMANCE.md records named in 104-01-SUMMARY's leak-guard audit</files>
  <reversibility rating="costly">A fast-forward push of #691's head is public; undoing it needs a force push of the old head, which rewrites a branch reviewers may have fetched. The push and the PR update are the user's decision.</reversibility>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md — the leak-guard audit of the earlier records
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/v45-pr-691-held-until-milestone-done.md
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/git-push-https-not-ssh.md
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/squash-merge-commit-bodies-close-issues.md
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-05-PLAN.md — task 3, the push and PR-update pattern this repeats
  </read_first>
  <action>
    Step 1 — corpus README exit line: under §"Conformance of the language server" in
    `/home/coder/repos/bbj-corpus/README.md`, after the "First run" line, add one line in the same
    style: the v4.5 exit, language server `<commit>` on the September 1 build, A, A2 (raw) and B with
    the BBj verdict, endpoint mode, 0 failures (private repository, numbers are fine here).

    Step 2 — corpus commit (per D-12): `git -C /home/coder/repos/bbj-corpus status --porcelain` must
    show exactly ` M` for `README.md`, `conformance/REPORT.md`, `conformance/details.json`,
    `conformance/history.jsonl`, `conformance/summary.json` and `?? eval/` (snapshots are ignored);
    anything else, stop and report. Confirm `summary.json` still equals
    `snapshots/phase-104-gate-summary.json`. Stage exactly those five paths; subject
    "conformance: v4.5 closing measurement, endpoint off and on, September 1 build".

    Step 3 — remove the baseline worktree (per D-14's scratch rule):
    `git -C /home/coder/repos/bbj-corpus worktree remove /home/coder/repos/bbj-corpus-baseline`
    (no force — it is pristine) and `git -C /home/coder/repos/bbj-corpus worktree prune`.

    Step 4 — earlier-record leaks: if 104-01-SUMMARY's audit reported hits in an earlier
    CONFORMANCE.md, run the guard on it again while a copy of the data is still readable (the rebuilt
    `main` data suffices for ids and reject lines), replace each hit in place with an own-words
    description, re-run the guard until clean, stage exactly those files and commit with subject
    "docs(104-03): replace corpus-derived text in an earlier conformance record". No hits: skip.

    Step 5 — pre-push scans (per D-08). Closing keywords over every commit body since origin/main:
    `git -C /home/coder/repos/bbj-language-server log origin/main..HEAD --format=%B | grep -niE '(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9]+'`
    — report every hit in the SUMMARY and in your final message (the squash message is edited at merge
    time by the user; do not rewrite history). Phase scope: `git -C /home/coder/repos/bbj-language-server diff --name-only 61ed5a88..HEAD -- bbj-vscode bbj-intellij`
    must print only `bbj-vscode/test/test-data/conformance/README.md`.

    Step 6 — push by fast-forward only (per D-08):
    `git -C /home/coder/repos/bbj-language-server push origin HEAD:gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`,
    then keep the local branch's own upstream in step:
    `git -C /home/coder/repos/bbj-language-server push origin HEAD:gsd/phase-103-one-set-of-errors-diagnostic-reconciliation`.
    A non-fast-forward rejection means someone else pushed: stop and report; never force.

    Step 7 — PR #691 (per D-08). Read `gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json title,body`.
    Title: name phases 98-105 and the exit measurement in user terms (for example "v4.5 phases 98-105:
    parser conformance, live compiler diagnostics, one set of errors, live diagnostics on large
    workspaces, and the exit measurement"). Body: keep the existing sections; add phase 104 to "What's
    in it"; add a section "## Phase 104 — exit measurement" in prose: the milestone baseline and the exit
    numbers (A and A2 as the language server alone, B with BBj's own parser verdict, of the baseline's
    11,898 accepted and 1,210 rejected programs, zero endpoint failures); that the private conformance
    harness gained an endpoint mode and stays local, never in CI; the test gate (the `bbj-vscode` suite
    with BBjServices up and the way CI runs it, the IntelliJ suite) and that both distributables were
    built from the final tree; that the regression-file folder now carries a maintainer README; that
    the residual semantic-only rejects are left to the save-time compiler run by design. Update "Holding
    this pull request open": the milestone's phases are complete, and merging the pull request and
    completing the milestone stay with the maintainer. No requirement, decision, plan or review
    identifier; no closing keyword; no corpus name or text. The body's last line is exactly
    `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. Write it to
    `/tmp/bbj-104-03-pr-body.md`, run the leak guard over that file and the identifier/keyword grep of
    the third verify command against it, then
    `gh pr edit 691 --repo BBx-Kitchen/bbj-language-server --title "<title>" --body-file /tmp/bbj-104-03-pr-body.md`.
    Do not merge, do not mark ready, do not change reviewers or labels.

    Step 8 — SUMMARY: the corpus commit hash, the closing-keyword scan result, the pushed commit, the
    PR title, and a note that `/gsd-complete-milestone` and the merge are the user's next steps.
  </action>
  <verify>
    <automated>git -C /home/coder/repos/bbj-language-server fetch origin && git -C /home/coder/repos/bbj-language-server merge-base --is-ancestor "$(git -C /home/coder/repos/bbj-language-server log -1 --format=%H -- .planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md)" origin/gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility</automated>
    <fails_when>non-zero exit — PR #691's head does not contain the exit-record commit (push missing or rejected)</fails_when>
    <automated>gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json state,headRefName,title --jq '.state+" | "+.headRefName+" | "+.title'</automated>
    <fails_when>non-zero exit, a state other than `OPEN`, a head ref other than `gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`, or a title that does not contain `98-105`</fails_when>
    <automated>! gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json body --jq .body | grep -qiE 'CONF-0|PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|CR-[0-9]|WR-[0-9]|(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9]'</automated>
    <fails_when>non-zero exit — the pull request body carries a planning identifier or a closing keyword</fails_when>
    <automated>test "$(git -C /home/coder/repos/bbj-corpus show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')" = "README.md conformance/REPORT.md conformance/details.json conformance/history.jsonl conformance/summary.json " && test -z "$(git -C /home/coder/repos/bbj-corpus worktree list --porcelain | grep bbj-corpus-baseline)"</automated>
    <fails_when>non-zero exit — the corpus commit holds other paths than the five, or the baseline worktree is still registered</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json body --jq .body | tail -n 1` prints exactly `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and the body contains `## Phase 104 — exit measurement`
    - `/tmp/bbj-104-03-pr-body.md` passes `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs` (exit 0)
    - `git -C /home/coder/repos/bbj-language-server diff --name-only 61ed5a88..HEAD -- bbj-vscode bbj-intellij` prints only `bbj-vscode/test/test-data/conformance/README.md`
    - `git -C /home/coder/repos/bbj-corpus status --porcelain -- eval` prints `?? eval/`
    - No push in this task used `--force` or `--force-with-lease`; the SUMMARY reports the closing-keyword scan result
  </acceptance_criteria>
  <done>The corpus side is committed by exact path, the scratch worktree is gone, PR #691 carries the whole milestone by fast-forward with the exit numbers in its title and body, and merging stays with the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private evidence → public record | The executor reads corpus ids and groups privately and writes `104-CONFORMANCE.md`, PROJECT.md and the PR body, all public. |
| branch head → PR #691 | Everything pushed becomes public, including every commit body. |
| bbj-corpus working tree ↔ another project | `eval/` belongs to another project; `main` was rebuilt by it during this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-09 | Information Disclosure | 104-CONFORMANCE.md, PROJECT.md, PR body, earlier records | high | mitigate | `leak-guard.mjs` on both data roots before each commit and on the PR body file; earlier-record hits from plan 01's audit are replaced before the push; planning-identifier and closing-keyword grep on the live PR body. Tasks 2 and 3. |
| T-104-10 | Tampering | bbj-corpus commit | high | mitigate | Status must show exactly the five expected paths plus untracked `eval/`; exact-path staging; the committed summary is checked equal to the gate snapshot. Task 3. |
| T-104-11 | Tampering | the gate's input | high | mitigate | Build-info counts and compiler asserted before each run; the gate check itself rejects any corpus other than 11,898 / 1,210 and any call count other than 13,108. Task 1. |
| T-104-12 | Repudiation | a gate run with failures, or a measurement of a non-final tree | medium | mitigate | Zero endpoint failures and no crash required; `sourceModified` false and no `bbj-vscode`/`bbj-intellij` diff between the measured commit and HEAD. Task 1. |
| T-104-13 | Tampering | PR #691's head ref | medium | mitigate | Fast-forward push only; a rejection stops the task; no merge, no ready-for-review. Task 3. |
| T-104-14 | Repudiation | squash-merge closing keywords | medium | mitigate | Every commit body since origin/main is scanned and the result reported before the push. Task 3. |
| T-104-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed. |
</threat_model>

<verification>
- The gate check prints `GATE PASS` on the committed `summary.json`, the endpoint-on raw sets equal the endpoint-off sets, and HEAD carries no language-server change since the measured commit.
- `104-CONFORMANCE.md` carries the exact exit-gate line and all required sections, and passes the leak guard on both data roots.
- The bbj-corpus commit holds exactly five paths; the baseline worktree is gone; `eval/` untouched.
- PR #691 is open, its head contains the exit record, its title names 98-105, and its body passes the identifier and keyword gate.
</verification>

<success_criteria>
- CONF-03: A ≤ 25, A2 ≤ 25 (raw) and B ≤ 5 % of 1,210 (reconciled) with zero endpoint failures on the baseline build, suites green (plan 02).
- Roadmap criterion 4: the result and every residual entry are recorded for the next milestone with no corpus content in this repository.
- The milestone branch is on PR #691, awaiting the user's merge and `/gsd-complete-milestone`.
</success_criteria>

<output>
Create `.planning/phases/104-conformance-measurement-milestone-exit/104-03-SUMMARY.md` when done
(counts and own-words only; run `leak-guard.mjs` over it before committing).
</output>
