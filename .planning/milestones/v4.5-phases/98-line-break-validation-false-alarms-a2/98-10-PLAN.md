---
phase: 98-line-break-validation-false-alarms-a2
plan: 10
type: execute
wave: 7
depends_on: ["98-09"]
gap_closure: true
autonomous: false
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-05, CONF-01]
files_modified:
  - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
  - .planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The conformance harness is re-run once against the final tree, after every gap fix has landed, and the three measures are recorded with their gate verdicts — A2 at or below 25, A not above 168, B against the 658 baseline (CONF-01, D-17)."
    - "The two fixes made in this wave are shown not to have cost anything on the measurement: A2 does not rise above the recorded 22 by more than the headroom to the gate, and no new conflicting-DECLARE error group appears."
    - "A human, not an executor, decides whether the residual regression is accepted; the decision is recorded in 98-VERIFICATION.md as an overrides entry naming the must-have, the reason, who accepted it and when — or as a rejection that keeps the phase open."
    - "REQUIREMENTS.md's Phase 98 entries are set only after that decision and only in the direction the decision supports."
  artifacts:
    - ".planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md — the closing re-run section with the final three measures and per-gate verdicts"
    - ".planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md — an overrides entry, or a recorded rejection, for the residual regression"
    - ".planning/REQUIREMENTS.md — Phase 98 requirement entries set to match the recorded outcome"
  key_links:
    - "the gap fixes in this wave -> the closing measurement: the re-run is the only thing that shows a validator or lexer change did not move A, A2 or B in the wrong direction."
    - "the human decision -> 98-VERIFICATION.md's overrides block: an executor narrative is not an acceptance; the gate is only closable by a recorded entry carrying an accepted_by and an accepted_at."
  prohibitions:
    - "The executor must NOT accept the residual regression on its own authority, must NOT write an overrides entry before the human has answered, and must NOT treat silence or an auto-advance setting as approval."
    - "The executor must NOT widen or delete a check to move a number. If a measure misses its gate, that is reported, not engineered away."
    - "No corpus file name, corpus path or corpus source line may be written into any tracked file."
    - "REQUIREMENTS.md must NOT be touched before the checkpoint resolves."
---

<!-- planner-discipline-allow: VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9] -->

<objective>
Close verification gaps 1c and 1d: measure the phase once more now that every gap fix has landed, and
put the one decision this phase cannot make for itself in front of a human — whether the residual
regression of invalid files that stopped being flagged is acceptable for Phase 98 to close.

Purpose: the phase's own closing plan flagged this as needing "a human or orchestrator decision" and
then closed without one. An executor's narrative is not that decision. This plan produces the
evidence, asks the question, and records the answer where the gate can see it.

Output: the closing measurement, a recorded human decision, and the phase's requirement entries set
to match it.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md

<working_rules>
These have stalled executors in this repository before. Follow them literally.

- Shell: absolute paths only. Never chain `cd` with grep, find, cat, sed, head or tail. Use
  `git -C /home/coder/repos/bbj-language-server ...` instead of changing directory. `cd` is allowed
  only directly in front of a build or test tool that needs its project directory.
- Staging: `git add <exact path>` only. Never `git add -A`, never `git add .`, never `git stash`.
- vitest needs cwd = bbj-vscode: run
  `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run <file>`. Whole-suite runs use
  `--maxWorkers=2` and are judged on `numFailedTests`, not on "failed suites". `RUN_BBJ_TESTS=0`
  avoids the known local interop baseline failures (linking.test.ts 11, issue447 1).
- Planning identifiers stay in planning files. Never write a decision, plan, requirement or review
  identifier into a source, test or fixture file. GitHub issue numbers are fine.
- Deviations go into this plan's SUMMARY.md only, never into WINDOWS.md.
- Scratch directory for every temporary file and log:
  `/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad`.
- Only numbers and message-group wording go into tracked files. No corpus file name, path or source
  line, ever (D-17).
</working_rules>

<measurement_facts>
- The harness is run as
  `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`.
  It takes about 70 seconds, overwrites `REPORT.md`, `summary.json` and `details.json` in
  `/home/coder/repos/bbj-corpus/conformance/`, appends one line to `history.jsonl`, and prints the
  summary as JSON on stdout.
- Baseline (history line 1, commit `d8071b24`): A 168, A2 267, B 658 of 1,210.
- Phase-boundary final (history line 4, commit `5cf8dec7`): A 167, A2 22, B 665 of 1,210.
- Gates from ROADMAP Phase 98 success criterion 5: A2 at or below 25; A not regressed above 168;
  B not regressed above 658.
- The harness records `sourceModified` true when `bbj-vscode/src` has uncommitted changes. Commit the
  wave's work before the run so the recorded commit identifies the measured tree.
- The previous plan left snapshots in the scratch directory: `details-phase-boundary.json`,
  `summary-phase-boundary.json` and `history-before.jsonl`, plus its regression working note. Use
  them for the before/after comparison rather than re-deriving anything.
- Watch item handed over by the wave's validator plan: a conflicting DECLARE between two different
  BBj built-in scalar types is now reported without a classpath, at error severity inside a method
  body. If the re-run shows a new conflicting-DECLARE error group in A2, the pre-authorised narrowing
  is to emit that branch as a warning in both scopes — narrow it, re-run, and record the outcome.
  Do not delete the branch.
</measurement_facts>

<edge_assumptions>
No SPEC.md exists for this phase, so the edge-coverage probe returned every applicable edge as
unresolved. Surfaced here as explicit assumptions, not silently dropped:

- CONF-01 / adjacency: a measure landing exactly on its gate number counts as meeting it; the gate
  wording is "at or below" for A2 and "not regressed" for A and B.
- CONF-01 / empty: the corpus and rejects sets are assumed non-empty and unchanged in size (11,898
  and 1,210). If either count differs from the recorded runs, the comparison is not like-for-like and
  that must be said in the record.
- CONF-01 / ordering: the harness shards by size and merges by file id, so the measure is
  order-independent; no ordering assertion is made.
- CONF-01 / concurrency: the harness runs four shards by default. Run it with the default shard count,
  as every recorded run did, so the numbers stay comparable.
- VALID-01 through VALID-05 / unclassified: re-attested here only through the whole-suite gate and the
  absence of their message groups from the A2 breakdown, exactly as the phase's earlier closing plan
  did.
- Assumed and NOT verified: that the compiler verdicts behind the corpus and rejects sets are
  unchanged since the baseline run.
</edge_assumptions>
</context>

<artifacts_this_phase_produces>
Created by this plan:

- No new files. The human decision is recorded inside the existing verification report, not in a new
  document.

Modified by this plan:

- `.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md` — a new numbered
  section after the one the previous gap plan added, holding the closing re-run: the three measures
  with deltas against both the baseline and the phase-boundary run, the per-gate verdict, the A2
  message-group breakdown if it changed, and an attestation that the wave's fixes added no new
  error-severity group.
- `.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md` — either an
  `overrides:` block in the frontmatter with `must_have`, `reason`, `accepted_by` and `accepted_at`,
  or a recorded rejection with the required follow-up; plus a short closing note in the body.
- `.planning/REQUIREMENTS.md` — the Phase 98 requirement checkboxes and status table rows, set once,
  after the decision.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the conformance harness on the final tree and record the closing measurement</name>
  <files>.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md</files>
  <read_first>
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md (whole file — match
      its section numbering, its table style, and the no-corpus-text discipline it already follows)
    - /home/coder/repos/bbj-corpus/conformance/run.mjs (what it writes and what its stdout summary
      contains)
    - /home/coder/repos/bbj-corpus/conformance/history.jsonl (the four recorded runs)
    - The measurement_facts block in this plan's context section
    - The previous gap plan's SUMMARY and the scratch snapshots it left behind
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md, decisions D-16 and D-17
  </read_first>
  <action>
    Before measuring, make the tree honest: confirm every gap plan in this wave is committed, that
    `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src` prints
    nothing, and that the generated parser under `bbj-vscode/src/language/generated/` is no older than
    `bbj-vscode/src/language/bbj.langium`. None of the wave's plans changed the grammar, so no
    regeneration should be needed; if the timestamps say otherwise, regenerate under Node 22 with
    `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"` prefixed onto the generate call,
    and say so in the SUMMARY.

    Run the whole suite first and record `numFailedTests`; a red suite makes the measurement
    meaningless. Then run the register check over the whole phase source diff from the phase base
    commit `5fb113cb` to HEAD.

    Then run the harness exactly once with the default shard count and capture its stdout JSON into
    the scratch directory. Record in a new numbered section of 98-CONFORMANCE.md, continuing the
    file's numbering — the previous gap plan added section 9, so this heading begins exactly
    `## 10.` — holding: the command, the date, the measured commit and whether the harness reported
    the source as modified, and a table of the three measures with the baseline value, the
    phase-boundary value, the new value, both deltas, the gate, and a PASS or FAIL verdict per gate.

    Then compare the new A2 message-group breakdown against the one already recorded for the
    phase-boundary run and state what changed. Attest explicitly whether any conflicting-DECLARE,
    METHODRET or RESTORE message group appears at error severity — the wave changed exactly those
    areas. If a new conflicting-DECLARE error group appears, apply the pre-authorised narrowing named
    in the measurement_facts block, re-run the harness once more, and record both runs.

    If B has moved again relative to the 665 recorded at the phase boundary, say by how much and in
    which direction, and cross-check against the newly-uncaught set the previous plan identified:
    state whether those same files are still not caught. The scratch snapshots from that plan are
    there for exactly this comparison.

    Write no corpus file name, path or source line. Counts, message-group wording and your own
    descriptions only.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-10.json > /dev/null 2>&1; node -e "const r=require('/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-10.json');console.log('numFailedTests='+r.numFailedTests)"</automated>
    <fails_when>the printed line is anything other than `numFailedTests=0`, or the node call errors because the JSON report was not written</fails_when>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('/home/coder/repos/bbj-corpus/conformance/history.jsonl','utf8').trim().split('\n').map(JSON.parse);const last=h[h.length-1];console.log('runs='+h.length);console.log('sourceModified='+last.languageServer.sourceModified);console.log('A='+last.corpus.falseRejects,'A2='+last.corpus.falseAlarms,'B='+last.rejects.missed+'/'+last.rejects.files);console.log('gate_A2='+(last.corpus.falseAlarms<=25),'gate_A='+(last.corpus.falseRejects<=168),'gate_B='+(last.rejects.missed<=658))"</automated>
    <fails_when>`runs=` is not greater than 4 (the harness was never re-run), or `sourceModified=true` (the measured tree had uncommitted source changes and the recorded commit does not identify it), or `gate_A2=false`, or `gate_A=false`. A `gate_B=false` is an expected possible outcome and is NOT a failure of this task — it is the input to the next task's decision.</fails_when>
    <automated>node -e "const t=require('fs').readFileSync('/home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md','utf8');const secs=(t.match(/^## \d+\./gm)||[]);console.log('sections='+secs.length);console.log('has_closing_run='+/PASS|FAIL/.test(t.split(secs[secs.length-1])[1]||'')) "</automated>
    <fails_when>`sections=` is less than 10 (the closing section was not appended after the regression-investigation section the previous plan added), or `has_closing_run=false` (the new section carries no per-gate verdict)</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff -U0 5fb113cb..HEAD -- bbj-vscode | grep '^+' | grep -cE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'</automated>
    <fails_when>the printed count is anything other than 0 — a planning identifier leaked into the phase's source diff</fails_when>
  </verify>
  <acceptance_criteria>
    - The whole suite reports numFailedTests equal to 0 before the harness is run.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src` prints
      nothing at the moment the harness runs, and the newest history line records
      `sourceModified: false`.
    - 98-CONFORMANCE.md gained a new numbered section holding the command, the date, the measured
      commit, and a three-row table with baseline, phase-boundary and new values, both deltas, the
      gate and a PASS or FAIL verdict per measure.
    - The section states explicitly whether any conflicting-DECLARE, METHODRET or RESTORE message
      group appears at error severity in the new A2 breakdown.
    - If B moved from 665, the section states the direction and size of the move and whether the
      previously identified newly-uncaught files are still not caught.
    - The register check over the whole phase source diff prints 0.
    - No corpus file name, path or source line appears anywhere in the file.
  </acceptance_criteria>
  <done>The final tree is measured once, the three gates have explicit verdicts, and the record says plainly which gate is met and which is not.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Does Phase 98 close with the residual regression of previously-flagged invalid files accepted, or does it stay open until that regression is fixed?</decision>
  <context>
    Roadmap Phase 98 success criterion 5 requires A2 at or below 25 with A and B both not regressed.
    A2 and A pass comfortably. B — invalid code the language server does not flag — regressed from
    the 658 baseline, and the phase's own closing plan recorded that as "accepted, unfixed residue"
    without anyone other than the executor accepting it. That is the only thing still keeping this
    phase from a clean pass.

    Before answering, read the closing measurement section just added to 98-CONFORMANCE.md for the
    current numbers, and the regression-investigation section added by the previous gap plan for the
    per-file evidence: how many files moved, what the pre-phase language server used to report for
    each, what the compiler actually complains about, and whether the claimed mechanism was confirmed
    or refuted for each one.

    The mechanism, where confirmed, is this: a branch target whose label name is also a language word
    used to be caught by a line-break diagnostic that was itself wrong. Fixing that false alarm means
    the reference now reaches the linker, which reports it correctly as an unresolved reference — and
    the conformance harness excludes linking diagnostics by design, because it runs with a fake Java
    classpath. The file is still wrong and the language server still says so; the measurement just
    cannot see it. Catching it as an error-severity diagnostic instead would mean adding a new
    undefined-branch-target validation, which the milestone already assigns to the compiler-parser
    endpoint work in Phases 101 to 103, and which this phase's own scope and threat model exclude.

    Anything the previous plan marked REFUTED is a different story and is listed there with its own
    disposition; weigh those separately.

    No executor may make this call. Nothing in the phase's artifacts records it yet.
  </context>
  <options>
    <option id="accept">
      <name>Accept the residual regression and close Phase 98</name>
      <pros>The phase's user-visible goal is met: A2 fell from 267 to well under its gate and A improved. The regressed files are still reported by the language server, just under a diagnostic category this measurement excludes. Fixing it properly is already scheduled work. Phase 99 is unblocked immediately.</pros>
      <cons>Roadmap criterion 5 as literally worded is not met, and the milestone carries the delta forward to its exit measurement. If any file was REFUTED rather than confirmed, accepting also accepts a mechanism nobody has explained.</cons>
    </option>
    <option id="reject">
      <name>Keep Phase 98 open and require a fix for the regression</name>
      <pros>The literal gate is honoured and no unexplained loss of detection ships. Anything refuted gets fixed rather than absorbed.</pros>
      <cons>The only real fix for the confirmed mechanism is a new undefined-branch-target error-severity check — architecturally significant, outside this phase's stated scope, and duplicating work the endpoint phases already own. It would also risk re-introducing false alarms of exactly the kind this phase removed.</cons>
    </option>
    <option id="accept-partial">
      <name>Accept only the confirmed files and require a fix for anything refuted</name>
      <pros>Draws the line where the evidence draws it. Nothing unexplained is absorbed.</pros>
      <cons>Only meaningful if the previous plan actually found a refuted file; if every file was confirmed this is the same as accepting.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with `accept`, `reject`, or `accept-partial`, plus the name to record as the accepting party.
    Anything else — including no answer — is not an acceptance. Do not proceed on an auto-advance
    setting; this checkpoint is not auto-approvable.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the decision and set the phase's requirement entries to match it</name>
  <files>.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md (whole file — the
      frontmatter's `gaps`, `deferred` and `overrides_applied` fields, and the Gaps Summary body)
    - .planning/REQUIREMENTS.md lines 20-60 (the Phase 98 requirement lines) and lines 85-120 (the
      status table and the mapping note)
    - The closing measurement section written in task 1 and the human's answer from the checkpoint
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md, decision D-16
  </read_first>
  <action>
    Record the answer given at the checkpoint. Edit 98-VERIFICATION.md with scoped edits only; never
    rewrite the file.

    If the answer was to accept, add an `overrides:` block to the frontmatter, as a sibling of `gaps`
    and `deferred`, with one entry carrying four keys: `must_have` quoting the failed truth verbatim
    from the existing `gaps` block, `reason` stating in one or two sentences why the delta is
    accepted and pointing at the evidence sections by name, `accepted_by` naming the person the
    human gave, and `accepted_at` as an ISO 8601 UTC timestamp. Set `overrides_applied` to the number
    of entries. Add a short paragraph at the end of the Gaps Summary recording that the decision was
    taken, by whom and on what evidence. Do not edit the `gaps` block itself — the gap was real and
    its record stays.

    If the answer was to accept only the confirmed files, do the same but scope `must_have` and
    `reason` to the confirmed subset, and leave the refuted portion recorded as still open with the
    follow-up the previous plan named.

    If the answer was to reject, add no overrides entry. Instead record the rejection and the required
    fix in the Gaps Summary, leave the phase's status as it is, stop before touching REQUIREMENTS.md,
    and report that the phase stays open.

    Then, and only if the decision was accept or accept-partial and task 1's gates for A2 and A both
    passed and the whole suite was green, update .planning/REQUIREMENTS.md with scoped edits: tick the
    Phase 98 requirement lines the measurement supports and set their status-table rows to Complete.
    Where the decision was accept-partial and a requirement is still not supported, leave that one
    unticked and say which in the SUMMARY. Never rewrite REQUIREMENTS.md wholesale, and never tick a
    requirement whose evidence is not in a recorded artifact.

    Finish by stating in the SUMMARY exactly which requirement entries were set, which were left, and
    the one-line reason for each.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('/home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md','utf8');const fm=t.split('---')[1]||'';const hasOv=/^overrides:/m.test(fm);console.log('overrides_block='+hasOv);if(hasOv){for(const k of ['must_have','reason','accepted_by','accepted_at']){console.log(k+'='+new RegExp('^\\\\s+'+k+':','m').test(fm))}}console.log('gaps_block_intact='+/^gaps:/m.test(fm))"</automated>
    <fails_when>`gaps_block_intact=false`; or, when the decision was to accept, `overrides_block=false` or any of the four key lines prints false; or, when the decision was to reject, `overrides_block=true`</fails_when>
    <automated>node -e "const t=require('fs').readFileSync('/home/coder/repos/bbj-language-server/.planning/REQUIREMENTS.md','utf8');const ids=['VALID-01','VALID-02','VALID-03','VALID-04','VALID-05','CONF-01'];for(const id of ids){const line=t.split('\n').find(l=>l.includes('**'+id+'**'))||'';console.log(id+'='+(line.startsWith('- [x]')?'ticked':line.startsWith('- [ ]')?'unticked':'not_found'))}"</automated>
    <fails_when>any identifier prints `not_found`; or, when the decision was to reject, any identifier prints `ticked`; or, when the decision was to accept and both the A2 and A gates passed, any identifier prints `unticked` without a stated reason in the SUMMARY</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md</automated>
    <fails_when>the output is empty (neither file was updated after the decision), or either file shows more than 30 changed lines, which would mean it was rewritten rather than edited in place</fails_when>
  </verify>
  <acceptance_criteria>
    - The human's answer is recorded verbatim in the SUMMARY, together with the name given.
    - On accept or accept-partial: 98-VERIFICATION.md carries an `overrides:` block whose entry has
      `must_have`, `reason`, `accepted_by` and `accepted_at`, `overrides_applied` matches the entry
      count, and the original `gaps` block is unchanged.
    - On reject: no overrides entry exists, the rejection and the required fix are recorded in the
      Gaps Summary, and REQUIREMENTS.md is untouched.
    - REQUIREMENTS.md's six Phase 98 entries each print `ticked` or `unticked` with a stated reason,
      never `not_found`.
    - Neither edited planning file shows more than 30 changed lines.
  </acceptance_criteria>
  <reversibility rating="one-way">Ticking a shared milestone requirement and recording an acceptance is a governance act that downstream phases and the milestone exit measurement read as settled; undoing it means retracting a recorded human sign-off. The preceding checkpoint is the gate for it.</reversibility>
  <done>The decision exists as a recorded artifact with an accepting party and a timestamp, and the phase's requirement entries say exactly what the evidence and that decision support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus -> tracked planning files | Proprietary BBj source text and file names cross into a public repository if any is copied verbatim. |
| executor judgement -> recorded human sign-off | An acceptance written without a human answer would let the phase close on a gate its own plan declares failed. |
| corpus and rejects sets -> language server | Arbitrary third-party BBj source is parsed and validated in-process by the harness workers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-10-01 | Spoofing | the overrides entry in 98-VERIFICATION.md | high | mitigate | An executor-authored acceptance would impersonate a human decision. The checkpoint is marked blocking-human and explicitly not auto-approvable; task 3 requires a named accepting party captured at the checkpoint, and its verify fails when the reject path produced an overrides entry. |
| T-98-10-02 | Information Disclosure | 98-CONFORMANCE.md and the SUMMARY | high | mitigate | D-17 forbids corpus file names, paths and source lines in tracked files. Task 1 records counts, message-group wording and paraphrase only; its acceptance criteria assert the absence of any corpus-derived identifier. |
| T-98-10-03 | Repudiation | the closing measurement | medium | mitigate | The harness records the measured commit and whether the source tree was dirty. Task 1 requires a clean `bbj-vscode/src` and asserts the newest history line reports the source as unmodified, so the number is attributable to an exact commit. |
| T-98-10-04 | Tampering | conformance measures | high | mitigate | Moving a number by widening or deleting a check is forbidden by this plan's prohibitions. The only pre-authorised code change in this plan is the named severity narrowing for the scalar DECLARE branch, and it must be recorded with both runs' numbers. |
| T-98-10-05 | Denial of Service | full harness run over the corpus and rejects sets | low | accept | The run is the project's established measurement path, takes about 70 seconds, runs outside CI by standing milestone constraint, and reports crashes as part of its own summary. |
| T-98-10-06 | Tampering | npm/pip/cargo installs | high | mitigate | No package install task exists. The only network-touching command is the Node 22 runtime resolution the phase's earlier plans already used, and only if the generated parser turns out to be stale; no project dependency is added or upgraded. |
</threat_model>

<verification>
1. Whole suite: `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2` — judged on `numFailedTests` being 0. Failed suites with zero failed tests are hook timeouts under contention; re-run each alone before judging.
2. Harness: one full run recorded in `history.jsonl` with `sourceModified: false`, and its A, A2 and B values transcribed into 98-CONFORMANCE.md with per-gate verdicts.
3. Register check over the whole phase source diff: `git -C /home/coder/repos/bbj-language-server diff -U0 5fb113cb..HEAD -- bbj-vscode | grep '^+' | grep -cE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'` prints 0.
4. `git -C /home/coder/repos/bbj-language-server status --porcelain` shows no file under `bbj-vscode/src/language/generated/`, no scratch file inside the repository, and exactly one worktree in `git worktree list`.
5. 98-VERIFICATION.md's `gaps` block is unchanged and the decision is recorded in the shape the answer requires.
6. No corpus file name, path or source line in any tracked file.
</verification>

<success_criteria>
- The harness was re-run once on a committed, clean final tree and the three measures are recorded
  with explicit PASS or FAIL verdicts against their gates.
- A2 is at or below 25 and A is not above 168; any movement in either is explained against the
  wave's changes.
- No new conflicting-DECLARE, METHODRET or RESTORE message group appears at error severity, or the
  pre-authorised narrowing was applied and both runs recorded.
- A human answered the decision checkpoint and the answer is recorded with an accepting party and a
  timestamp, in the shape that answer requires.
- The phase's requirement entries match the recorded outcome, set only after the decision.
- Whole suite green, register check clean, no corpus text in any tracked file.
</success_criteria>

<output>
Create `.planning/phases/98-line-break-validation-false-alarms-a2/98-10-SUMMARY.md` when done.
Record in it: the three closing measures with their deltas and verdicts, whether the A2 breakdown
gained or lost any group attributable to this wave, the human's verbatim answer and the accepting
party, which requirement entries were set and which were left with the reason for each, and anything
still open for the milestone to carry forward.
</output>
