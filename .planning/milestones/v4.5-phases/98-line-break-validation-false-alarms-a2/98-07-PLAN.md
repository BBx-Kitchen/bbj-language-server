---
phase: 98-line-break-validation-false-alarms-a2
plan: 07
type: execute
wave: 5
depends_on: ["98-06"]
gap_closure: true
autonomous: true
requirements: [VALID-02]
files_modified:
  - bbj-vscode/src/language/bbj-token-builder.ts
  - bbj-vscode/test/line-break-validation.test.ts
  - bbj-vscode/test/test-data/conformance/restore-numeric.bbj
  - .planning/ROADMAP.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A RESTORE statement whose target is a symbolic label (an asterisk followed by a name) parses as ONE RestoreStatement and produces no line-break diagnostic — the form the grammar's lineref alternative already declares legal (D-01)."
    - "An identifier that merely contains or equals the word restore, used in an expression, still parses as an ordinary identifier: an assignment multiplying such a variable is NOT swallowed by the RESTORE statement token."
    - "Every RESTORE form already covered before this plan (numeric target, user-label target, bare verb at end of line, bare verb before a trailing comment, verb after a leading label, verb inside a single-line IF) keeps producing zero line-break diagnostics (D-14)."
    - "ROADMAP.md's Phase 98 success criterion 2 no longer promises the continued LEN= item, matching D-18's reassignment of that construct to Phase 100 and the already-amended REQUIREMENTS.md text."
  artifacts:
    - "bbj-vscode/src/language/bbj-token-builder.ts — RESTORE_NO_NL pattern accepting a symbolic-label operand, with its doc comment updated"
    - "bbj-vscode/test/line-break-validation.test.ts — new positive cases for symbolic-label RESTORE and new negative keyword-as-identifier cases"
    - "bbj-vscode/test/test-data/conformance/restore-numeric.bbj — symbolic-label RESTORE lines added (D-12, D-13)"
    - ".planning/ROADMAP.md — Phase 98 success criterion 2 amended"
  key_links:
    - "RESTORE_NO_NL lexer pattern -> RestoreStatement's lineref alternative: the lexer must emit RESTORE_NO_NL for exactly the operand shapes the grammar can consume, and for nothing else."
    - "restore-numeric.bbj -> conformance-regressions.test.ts: every fixture line must parse with zero lexer/parser errors and validate with zero error-severity diagnostics, linking excluded (D-10)."
  prohibitions:
    - "The RESTORE statement token must NOT match when the verb word is part of a longer identifier."
    - "The RESTORE statement token must NOT swallow a spaced multiplication operator that follows a variable named after the verb."
    - "No plan, decision, requirement or review identifier may appear in any source, test or fixture text produced by this plan."
    - "No corpus file name, corpus path or corpus source line may be copied into any tracked file."
    - "The fix must NOT be made by loosening line-break-validation.ts to tolerate the wrong parse — the defect is in the lexer (D-01, D-02)."
---

<!-- planner-discipline-allow: VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9] -->

<objective>
Close verification gap 2: a RESTORE whose target is a symbolic label — a form this phase's own
grammar change declares legal — still produces the exact class of false line-break alarm the phase
exists to remove, because RESTORE_NO_NL's operand lookahead has no asterisk in its character class.

Purpose: the phase goal is "the line-break validator agrees with bbjcpl on code it accepts". A form
the grammar itself accepts but the lexer refuses to tokenize breaks that goal at the exact point the
phase claimed to fix it (D-01, D-02).

Output: a narrowed RESTORE_NO_NL pattern, positive and adversarial negative regression tests, an
extended conformance fixture, and ROADMAP.md's Phase 98 criterion 2 brought in line with D-18.
</objective>

<assumption_delta_decision>
Noun: the RESTORE statement's line reference.
Decision: no-change.
Rationale: the scan's only signal is the word "also" in the roadmap's prose about a label name also
being a language word — an English adverb, not a hidden second entity; the statement still carries at
most one line reference and no cardinality assumption changes.
</assumption_delta_decision>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-REVIEW.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md

<working_rules>
These have stalled executors in this repository before. Follow them literally.

- Shell: absolute paths only. Never chain `cd` with grep, find, cat, sed, head or tail. Use
  `git -C /home/coder/repos/bbj-language-server ...` instead of changing directory. `cd` is allowed
  only directly in front of a build or test tool that needs its project directory.
- Staging: `git add <exact path>` only. Never `git add -A`, never `git add .`, never `git stash`.
- vitest needs cwd = bbj-vscode: run
  `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run <file>`. Running vitest from
  the repository root through `npm --prefix ... exec` fails with ENOENT on fixture paths. Whole-suite
  runs use `--maxWorkers=2` and are judged on `numFailedTests`, not on "failed suites" — a failed
  suite with zero failed tests is a beforeAll hook timeout under contention. Known local baseline
  failures unrelated to this phase: linking.test.ts interop (11) and issue447 (1) when BBjServices is
  reachable on :5008; `RUN_BBJ_TESTS=0` avoids them.
- Tests parse with parseHelper / validationHelper, never DocumentBuilder.build against live interop.
- No grammar change in this plan. bbj-token-builder.ts is a lexer file; changing it does NOT require
  `npm run langium:generate`. Do not run langium:generate here and do not run `npm run build`.
- Planning identifiers stay in planning files. Decision, plan, requirement and review identifiers
  appear in this plan for traceability only. Never write one into a source file, a test file, a
  fixture comment or a REM line — describe the behaviour instead. GitHub issue numbers are fine.
- Deviations go into this plan's SUMMARY.md only, never into WINDOWS.md.
- Do not touch .planning/REQUIREMENTS.md. Its Phase 98 checkboxes were deliberately reverted to
  unchecked; only the phase's final gap plan may set them again.
</working_rules>

<observed_behaviour>
Recorded from the current tree before this plan was written, so the executor can tell a real change
from a coincidence. Each line is: input -> parsed statement types / line-break diagnostics.

- `RESTORE *foo` + newline -> RestoreStatement, ExpressionStatement / "needs to end with a line
  break: RESTORE", "needs to start in a new line: *foo"   (the defect)
- `restore *break` + newline -> same two statements, same two diagnostics (case-insensitive)
- `x = restore * 2` + newline -> one LetStatement, no line-break diagnostic   (MUST NOT REGRESS)
- `y = restore + 1` + newline -> one LetStatement, no line-break diagnostic
- `x = restorex * 2` + newline -> one LetStatement, no line-break diagnostic
- `restorex = 1` + newline -> one LetStatement, no line-break diagnostic
- `RESTORE` + newline -> one RestoreStatement, no diagnostic
- `RESTORE 0` + newline -> one RestoreStatement, no diagnostic
- `RESTORE;rem trailing` + newline -> one CompoundStatement, no diagnostic
- `IF flag THEN RESTORE 0 FI` + newline -> IfStatement, RestoreStatement, IfEndStatement, no
  line-break diagnostic
- `GOTO *RETRY`, `GOTO *NEXT`, `gosub *same`, `SETERR *RETRY` -> one statement each, no diagnostic.
  Symbolic labels already work everywhere else a label reference is accepted, including ones whose
  name is also a language word.

The declared library symbolic labels live in `bbj-vscode/src/language/lib/labels.bbl`:
*PROCEED *NEXT *SAME *RETRY *BREAK *CONTINUE *ESCAPE *RETURN *STOP *END *EXIT *ENDIF
</observed_behaviour>

<edge_assumptions>
No SPEC.md exists for this phase, so the edge-coverage probe returned every applicable edge as
unresolved. These are surfaced as explicit assumptions, not silently dropped. They are assumptions
this plan makes, not work items:

- VALID-02 / empty input: a bare verb with no operand and an empty document are both already
  covered by existing cases and stay covered; the lexer decides "has an operand" and an absent
  operand keeps falling through to the bare keyword alternative.
- VALID-02 / encoding: operand matching is ASCII-only by construction — the lookahead classes name
  ASCII ranges. A non-ASCII name character after the asterisk is assumed not to occur in BBj source
  and is not handled.
- VALID-02 / idempotency: lexing is pure; re-validating the same document twice yields the same
  diagnostics. Not separately asserted.
- VALID-02 / concurrency: vitest may run files in parallel workers; each test builds its own
  document through validationHelper, so no shared mutable lexer state is assumed.
- VALID-01, VALID-03, VALID-04, VALID-05 / unclassified: out of this plan's scope; unchanged here
  and re-attested by the phase's final gap plan.
</edge_assumptions>
</context>

<artifacts_this_phase_produces>
Created by this plan:

- No new files.

Modified by this plan:

- `bbj-vscode/src/language/bbj-token-builder.ts` — the `RESTORE_NO_NL` terminal's `PATTERN` and its
  doc comment.
- `bbj-vscode/test/line-break-validation.test.ts` — new positive cases inside the existing describe
  block "Line break validation: RESTORE with a numeric or label reference"; one new statement-shape
  assertion in that block; one new describe block for the verb-word-as-identifier negative cases
  (name it in behaviour terms, for example "Line break validation: the RESTORE verb word used as a
  name").
- `bbj-vscode/test/test-data/conformance/restore-numeric.bbj` — three or more symbolic-label
  RESTORE lines (upper, lower, mixed case) and an updated leading REM prose line.
- `.planning/ROADMAP.md` — Phase 98 success criterion 2 text.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A failing test that pins the symbolic-label RESTORE defect and the identifier shapes that must survive the fix</name>
  <files>bbj-vscode/test/line-break-validation.test.ts</files>
  <read_first>
    - bbj-vscode/test/line-break-validation.test.ts (whole file; especially the describe block
      "Line break validation: RESTORE with a numeric or label reference" at about line 114 and the
      lineBreakDiagnostics helper it uses)
    - bbj-vscode/src/language/bbj-token-builder.ts lines 119-150 (the EXIT_NO_NL and RESTORE_NO_NL
      terminal cases — the pattern shape the next task changes)
    - bbj-vscode/src/language/lib/labels.bbl (the declared symbolic label names)
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-REVIEW.md, section CR-02
    - The observed_behaviour block in this plan's context section
  </read_first>
  <behavior>
    New positive cases (currently FAIL — this is the defect, per D-01):
    - `RESTORE *RETRY` + newline produces zero line-break diagnostics and exactly one parsed
      statement of type RestoreStatement.
    - `restore *retry` + newline — same, lower case.
    - `ReStOrE *ReTrY` + newline — same, mixed case (BBj is case-insensitive).
    - `RESTORE *NEXT` + newline — same, for a symbolic label whose name is also a language word.
    - `RESTORE *mytarget` + newline — same statement shape; an undeclared symbolic label may produce
      a linking diagnostic, which is not a line-break diagnostic and is not asserted against.

    New negative cases (currently PASS and must keep passing — the guards the earlier TABLE token in
    this phase did not have, D-14):
    - `x = restore * 2` + newline parses as exactly one LetStatement and produces zero line-break
      diagnostics. A variable named after the verb, multiplied, must not become a RESTORE statement.
    - `y = restore + 1` + newline — same.
    - `x = restorex * 2` + newline and `restorex = 1` + newline — one LetStatement each, zero
      line-break diagnostics: the verb word embedded in a longer identifier.
    - `RESTORE;rem trailing` + newline — zero line-break diagnostics (bare verb, trailing comment).
    - `IF flag THEN RESTORE 0 FI` + newline — zero line-break diagnostics.
    - Every case already in the "RESTORE with a numeric or label reference" describe block stays
      unchanged and passing, including `a = 1 restore 0` which must still be flagged.
  </behavior>
  <action>
    Extend the existing describe block "Line break validation: RESTORE with a numeric or label
    reference" in bbj-vscode/test/line-break-validation.test.ts. Do not create a new test file and do
    not modify the other three line-break test files.

    Add the positive inputs listed in the behavior block to that describe block's positiveCases
    array, with labels written in behaviour terms — for example "symbolic label target, uppercase
    keyword" and "symbolic label whose name is also a language word".

    Add a second assertion dimension the existing block does not have: a test that asserts the parsed
    statement SHAPE, not only the absence of diagnostics. Read it from
    result.document.parseResult.value.statements and compare the mapped $type values against an
    expected array. A diagnostics-only assertion is not sufficient here — the defect this task pins
    is precisely that the input splits into two statements, and a future pattern change could keep
    the diagnostics quiet while still producing the wrong tree. For every symbolic-label input the
    expected shape is exactly one RestoreStatement.

    Add a new describe block for the adversarial verb-word-as-identifier cases, named in behaviour
    terms. Each case asserts both zero line-break diagnostics and the expected statement shape of one
    LetStatement.

    This task intentionally leaves the suite red: the positive symbolic-label cases must fail now,
    and the next task turns them green. Commit the red test as its own commit so the defect is on
    record in history; say so plainly in the commit body.

    Keep every user name invented (mytarget, restorex). Write no corpus text and no planning
    identifier into any comment or label.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/line-break-validation.test.ts --reporter=basic > /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/98-07-red.log 2>&1; echo "vitest_exit=$?"; tail -40 /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/98-07-red.log</automated>
    <fails_when>the printed line reads `vitest_exit=0` (the new symbolic-label cases pass already and therefore prove nothing), or the log contains "No test files found", or a failing test name in the log refers to something other than a symbolic-label RESTORE case (for example one of the new identifier cases failing, which would mean the shape assertion itself is wrong)</fails_when>
  </verify>
  <acceptance_criteria>
    - bbj-vscode/test/line-break-validation.test.ts contains the literal inputs `RESTORE *RETRY`,
      `restore *retry`, `RESTORE *NEXT`, `x = restore * 2`, `y = restore + 1` and `x = restorex * 2`.
    - At least one new assertion reads parseResult.value.statements and compares mapped $type values
      against an expected array.
    - The recorded log shows at least one failing test, and every failing test name refers to a
      symbolic-label RESTORE case.
    - `git -C /home/coder/repos/bbj-language-server diff -- bbj-vscode/test/line-break-validation.test.ts | grep '^+' | grep -qE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'` exits 1 (no match).
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/test/line-break-single-line-if.test.ts bbj-vscode/test/line-break-walk-termination.test.ts bbj-vscode/test/line-break-walk-timeout.test.ts` prints nothing.
  </acceptance_criteria>
  <done>The new symbolic-label RESTORE cases fail for the documented reason, the new identifier-shape cases pass, and both are committed in one red commit.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow RESTORE_NO_NL so it accepts a symbolic-label operand and nothing else new</name>
  <files>bbj-vscode/src/language/bbj-token-builder.ts, bbj-vscode/test/test-data/conformance/restore-numeric.bbj</files>
  <read_first>
    - bbj-vscode/src/language/bbj-token-builder.ts lines 100-150 (the ASTERISK_EXPRESSION, EXIT_NO_NL
      and RESTORE_NO_NL cases, and the doc comment above RESTORE_NO_NL that currently describes the
      operand as a digit, letter or underscore)
    - bbj-vscode/src/language/bbj-token-builder.ts lines 216-262 (TABLE_DATA and
      BRANCH_TARGET_EXCLUSION) — the house style for a bounded, commented lexer pattern and the
      statement-anchor lesson from earlier in this phase
    - bbj-vscode/src/language/bbj.langium lines 216-218 (RestoreStatement), 471-481 (LabelRef,
      UserLabelRef, SymbolicLabelRef) and line 934 (SymbolicLabelName)
    - bbj-vscode/test/test-data/conformance/restore-numeric.bbj (the fixture's shape and its leading
      REM convention, D-13)
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-REVIEW.md, section CR-02
  </read_first>
  <action>
    Change RESTORE_NO_NL's pattern in bbj-token-builder.ts. Today its lookahead, after one or more
    horizontal whitespace characters, is the single character class of digits, letters and
    underscore. Replace that with a non-capturing alternation of two branches: the existing single
    character class unchanged, OR an escaped asterisk immediately followed by a name-start class of
    letters and underscore. Keep the case-insensitive flag, keep LINE_BREAKS false, and keep every
    quantifier bounded exactly as it is today.

    Do NOT implement the review report's suggested form, which simply adds an asterisk to the
    existing single character class. That form also matches an asterisk followed by whitespace, which
    turns the currently-working expression `x = restore * 2` into a RESTORE statement — verified
    against the current tree, see the observed_behaviour block. Requiring a name-start character
    directly after the asterisk keeps that expression an ordinary multiplication while still matching
    a symbolic-label target. Record this narrowing in the SUMMARY's decisions, together with the one
    residual ambiguity it does not resolve: an unspaced multiplication such as `x = restore *foo` is
    ambiguous in the language itself and stays resolved in favour of the statement.

    Update the doc comment above the token so it describes the operand as a digit, letter, underscore,
    or an asterisk followed by a symbolic-label name, and states why the asterisk is honoured only
    when a name character follows it. Keep the existing explanation of why the "has an operand"
    decision is made in the lexer rather than the parser.

    Extend bbj-vscode/test/test-data/conformance/restore-numeric.bbj with symbolic-label RESTORE
    lines in upper, lower and mixed case, using a declared library symbolic label so the fixture stays
    free of any diagnostic (D-12, D-13). Update the fixture's leading REM prose so it describes the
    added shape in behaviour terms. Keep the file hand-written and minimal, invent all user names, and
    write no identifier of any planning artifact into it.

    Do not change bbj.langium. Do not run langium:generate. Do not touch line-break-validation.ts.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/line-break-validation.test.ts test/conformance-regressions.test.ts test/example-files.test.ts test/parser.test.ts --reporter=basic 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, a non-zero failed-test count in the summary line, or "No test files found" in the output</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-07.json > /dev/null 2>&1; node -e "const r=require('/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-07.json');console.log('numFailedTests='+r.numFailedTests)"</automated>
    <fails_when>the printed line is anything other than `numFailedTests=0`, or the node call errors because the JSON report was not written</fails_when>
    <automated>grep -v '^[[:space:]]*[/*]' /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-token-builder.ts | grep -cF '[0-9A-Za-z_*]'</automated>
    <fails_when>the printed count is anything other than 0 — a non-zero count means the over-wide single character class was used instead of the alternation</fails_when>
    <automated>grep -cF '\*[A-Za-z_]' /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-token-builder.ts</automated>
    <fails_when>the printed count is 0 — the name-start requirement after the asterisk is not present in the file</fails_when>
  </verify>
  <acceptance_criteria>
    - bbj-vscode/src/language/bbj-token-builder.ts contains a RESTORE_NO_NL pattern whose lookahead
      requires a letter or underscore directly after the asterisk, and contains no single character
      class combining digits, letters, underscore and asterisk.
    - Every test in test/line-break-validation.test.ts passes, including the symbolic-label cases
      added in task 1 and the identifier cases `x = restore * 2`, `y = restore + 1`,
      `x = restorex * 2`.
    - test/conformance-regressions.test.ts passes with the extended restore-numeric.bbj.
    - The whole-suite JSON report shows numFailedTests equal to 0.
    - restore-numeric.bbj contains at least three symbolic-label RESTORE lines differing only in
      case, and its leading REM prose mentions the symbolic-label target.
    - `git -C /home/coder/repos/bbj-language-server diff -- bbj-vscode | grep '^+' | grep -qE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'` exits 1 (no match).
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src/language/generated` prints nothing.
  </acceptance_criteria>
  <reversibility rating="reversible">A single lexer lookahead pattern plus fixture lines; reverting is a one-line revert with no generated-code or AST-type consequences.</reversibility>
  <done>A symbolic-label RESTORE (and its case variants) parses as one RestoreStatement with no line-break diagnostic, every prior RESTORE form still passes, an expression multiplying a variable named after the verb is untouched, and the whole suite reports zero failed tests.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md Phase 98 success criterion 2 to match the LEN= reassignment</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 314-350 (the whole Phase 98 detail block; criterion 2 is at line 323
      and criterion 4 at line 325 shows how an earlier amendment to a criterion was worded)
    - .planning/REQUIREMENTS.md lines 25-29 (VALID-02's text, already written without the LEN= item)
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md, decision D-18
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md, the deferred block
      in the frontmatter and item 3 of the Gaps Summary
  </read_first>
  <action>
    Edit ROADMAP.md's Phase 98 success criterion 2 with a scoped Edit — never rewrite the file, and
    never pass the whole file to a write tool. Remove the continued LEN= item from the list of
    constructs the criterion promises, and append a short parenthetical in the same style criterion 4
    already uses for its own amendment, recording that the construct was reclassified as a variable
    literally named LEN colliding with a fused keyword literal — a wrong-AST parser defect — and moved
    to Phase 100 (cite it as D-18). Keep the approximate file count in the criterion honest: either
    state that the count was taken before the reassignment, or drop the count, rather than leaving a
    number that no longer matches the listed constructs.

    Do not change any other criterion, any other phase's block, the plan list, or the wave headings.
    Do not touch .planning/REQUIREMENTS.md — its VALID-02 text was already corrected.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('/home/coder/repos/bbj-language-server/.planning/ROADMAP.md','utf8');const b=t.split('### Phase 98:')[1].split('### Phase 99:')[0];const c2=b.split(/^  2\. /m)[1].split(/^  3\. /m)[0];console.log('criterion2_promises_len='+/LEN=/.test(c2.split('(')[0]));console.log('d18_cited='+/D-18/.test(b));console.log('phase99_present='+/### Phase 99:/.test(t));console.log('criteria_count='+(b.match(/^  \d\. /gm)||[]).length);console.log('plan_lines='+(b.match(/98-\d\d-PLAN\.md/g)||[]).length)"</automated>
    <fails_when>the output prints `criterion2_promises_len=true`, or `d18_cited=false`, or `phase99_present=false`, or `criteria_count=` anything other than 5, or `plan_lines=` anything other than 10</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --numstat -- .planning/ROADMAP.md</automated>
    <fails_when>the changed-line counts exceed 3 added and 3 removed, which would mean more than criterion 2 was rewritten</fails_when>
  </verify>
  <acceptance_criteria>
    - Phase 98 success criterion 2 in .planning/ROADMAP.md no longer lists the continued LEN= item
      among the constructs that produce no line-break error.
    - The Phase 98 block cites D-18 as the reason for the change.
    - The Phase 98 block still has exactly 5 numbered success criteria and exactly 10 plan list lines.
    - The Phase 99 heading and the rest of the file are intact.
    - The diff against .planning/ROADMAP.md touches at most 3 added and 3 removed lines.
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 98 criterion 2 and REQUIREMENTS.md's VALID-02 describe the same scope, with the reassignment reason recorded inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor buffer -> language server lexer | Arbitrary, possibly adversarial BBj source text crosses into a regex-driven tokenizer running in the extension host process. |
| workspace files -> conformance fixture loader | Fixture text read from disk is parsed and validated by the test harness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-07-01 | Denial of Service | RESTORE_NO_NL lookahead in bbj-token-builder.ts | medium | mitigate | The new branch adds only bounded, fixed-length lookahead atoms (one escaped asterisk plus one character class); no unbounded or nested quantifier is introduced, so the pattern cannot backtrack catastrophically on a long line. Task 2's acceptance criteria forbid the unbounded form and the whole-suite gate covers the existing long-line fixtures. |
| T-98-07-02 | Tampering | conformance fixture folder | low | accept | Fixtures are hand-written, committed, and reviewed; they carry no executable content beyond BBj source parsed in-process by the test harness. |
| T-98-07-03 | Information Disclosure | tracked planning and fixture files | medium | mitigate | D-17 forbids corpus file names, paths and source lines in tracked files. This plan writes no corpus-derived text; the fixture uses invented names only, asserted by the acceptance criteria. |
| T-98-07-04 | Tampering | npm/pip/cargo installs | high | mitigate | No package install task exists in this plan; no dependency is added or upgraded. If one becomes necessary, stop and route it through the phase's package-legitimacy gate with a blocking human checkpoint. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-07-final.json` then read `numFailedTests` from the JSON: it must be 0. Failed suites with zero failed tests are hook timeouts under contention — re-run each such file alone before judging.
2. Register check over this plan's whole source diff:
   `git -C /home/coder/repos/bbj-language-server diff -- bbj-vscode | grep '^+' | grep -qE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'` must exit 1.
3. `git -C /home/coder/repos/bbj-language-server status --porcelain` must show no file under `bbj-vscode/src/language/generated/` and no stray scratch file inside the repository.
4. `.planning/REQUIREMENTS.md` must be unchanged by this plan.
</verification>

<success_criteria>
- A symbolic-label RESTORE, in upper, lower and mixed case, parses as exactly one RestoreStatement
  and produces zero line-break diagnostics.
- An expression using the verb word as a variable name, with a spaced multiplication operator after
  it, still parses as exactly one LetStatement.
- The verb word embedded in a longer identifier is never tokenized as the statement verb.
- Every RESTORE case present before this plan still passes, including the one that must stay flagged.
- restore-numeric.bbj gained the symbolic-label shape and still validates clean.
- ROADMAP.md Phase 98 criterion 2 matches REQUIREMENTS.md's VALID-02 scope and cites D-18.
- Whole suite reports numFailedTests 0; register check clean; no generated parser committed.
</success_criteria>

<output>
Create `.planning/phases/98-line-break-validation-false-alarms-a2/98-07-SUMMARY.md` when done.
Record in it: the exact final pattern, the rejected wider alternative and why, the residual unspaced
ambiguity, and the before/after statement shapes observed for each adversarial identifier case.
</output>
