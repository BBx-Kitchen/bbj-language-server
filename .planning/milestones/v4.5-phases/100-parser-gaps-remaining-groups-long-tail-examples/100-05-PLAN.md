---
phase: 100-parser-gaps-remaining-groups-long-tail-examples
plan: 05
type: execute
wave: 4
depends_on: [100-03]
files_modified:
  - bbj-vscode/test/examples-compile.test.ts
  - bbj-vscode/test/functional/installed-extension-e2e.test.ts
  - examples/invalid/README.md
  - examples/dim-examples.bbj
  - examples/files.bbj
  - examples/imports/issue50.bbj
  - examples/imports/onlyStaticField.bbj
  - examples/issue181-release-syntax.bbj
  - examples/issue182-clipfromstr-syntax.bbj
  - examples/issue198-member-access-levels-instance.bbj
  - examples/issue198-member-access-levels-static.bbj
  - examples/issue207-member-access-levels-auto-getter-setter.bbj
  - examples/issue224.bbj
  - examples/issue246.bbj
  - examples/issue44-return.bbj
  - examples/issue650-composer-cues.bbj
  - examples/issue_378_class_bbjstring_bbjnumber.bbj
  - examples/mnemonics.bbj
  - examples/msgbox.bbj
  - examples/using-java.bbj
  - .planning/todos/pending/2026-09-21-file-dialog-functions-accepted-on-a-false-premise.md
autonomous: true
requirements: [EXMP-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every BBj program file under `examples/`, at any depth, either compiles with the real compiler or lives in `examples/invalid/` (criterion 5)."
    - "The compile verdict is computed from the compiler's stdout AND stderr, because it exits zero and writes its errors to stderr; a check reading stdout alone reports every file as compiling."
    - "A failing example whose error is not its point is repaired with the smallest edit that makes the compiler accept it while the construct the file was about stays in it (D-15)."
    - "A file whose purpose is the error moves to `examples/invalid/`, a flat folder with a short README saying these files fail to compile on purpose and what each demonstrates, with its issue-numbered name kept (D-16)."
    - "A mixed file is split: the valid body stays under its name and compiles, and the erroneous lines move into a sibling file in `examples/invalid/` (D-17)."
    - "An example added as supported syntax that the compiler rejects and for which no valid spelling exists moves to `examples/invalid/` with an explicit no-diagnostic-today entry and a filed todo; the grammar is NOT tightened in this phase (D-18)."
    - "The assertion has two layers: an always-on layer in which every valid example parses and validates clean and every file in `examples/invalid/` has a sidecar expectation checked through the parse and validation helpers, and a layer gated by the BBj environment switch in which every valid example compiles and every invalid one fails to compile (D-19)."
    - "A file in `examples/invalid/` that the language server cannot flag at all carries an explicit no-diagnostic-today entry, so the gap is visible instead of silent (D-19)."
    - "The configuration file and the library file are not programs: they stay where they are and are excluded by extension, with the reason stated in the test (D-20)."
    - "Moving or splitting a file does not break the three tests that reference single example files; references are checked before each move, and the two hardcoded fixture lines in the installed-extension test are updated in the same task as the example they quote (D-21)."
    - "An empty `examples/invalid/` folder fails the test rather than passing vacuously, and a sidecar with no expected diagnostic is written as an explicit no-diagnostic-today entry rather than an empty list."
    - "The test's file lists are sorted, so a failure names the same file on every run regardless of directory-read order."
    - "The compiler-gated layer spawns one compile per file with a bounded timeout; a spawn that times out is reported as a failure for that file, never silently skipped."
    - "A file is excluded by its extension, not by a name match, so a configuration file sitting next to a program of a similar name is excluded for the right reason."
    - "No requirement, plan or decision identifier appears in any example file, sidecar, README or test name; GitHub issue numbers are fine."
    - statement: "Every example already parses cleanly with the language server on the tree this plan starts from, so the always-on layer is green from the moment it is written; if a repaired example stops parsing cleanly, that is a grammar regression to report rather than a reason to weaken the assertion."
      verification: backstop
  artifacts:
    - bbj-vscode/test/examples-compile.test.ts
    - examples/invalid/README.md
    - .planning/todos/pending/2026-09-21-file-dialog-functions-accepted-on-a-false-premise.md
  key_links:
    - "`examples/issue650-composer-cues.bbj` <-> the two hardcoded line-text assertions in `bbj-vscode/test/functional/installed-extension-e2e.test.ts`. That test matches the fixture's failing line as a literal string, twice; repairing the file without updating both strings breaks the test, and updating them in a later task leaves a red tree in between."
    - "`examples/imports/*.bbj` <-> the import and linking tests that resolve across those files. A file in that folder is not only an example; moving one changes what another one can resolve."
    - "The always-on layer <-> the compiler-gated layer. The first runs everywhere and asserts what the language server sees; the second runs only where BBj is installed and asserts what the compiler sees. A file that satisfies one and not the other is exactly the gap the two layers exist to expose."
    - "A sidecar expectation <-> the file it describes. A sidecar with no file, or a file in the invalid folder with no sidecar, turns the folder into an unchecked dumping ground; the test asserts the pairing in both directions."
  prohibitions:
    - statement: "MUST NOT tighten a grammar rule that turns out to have been added on a false premise — the file moves and a todo is filed, and the rule stays as it is in this phase."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT delete an example, or strip the construct an example exists to demonstrate, in order to make it compile — the repair is the smallest edit that keeps the point."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT move the configuration file or the library file, or change any path in the tests that reference them."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT weaken, skip or delete an assertion in the new test to make a file pass — a file that cannot satisfy a layer is reported, not accommodated."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT add a new editor capability, a new diagnostic or a new strict check while repairing examples."
      status: unresolved
      verification: judgment
---

<objective>
Stop the repository's own `examples/` from disagreeing with the compiler: every BBj program either
compiles or lives in `examples/invalid/` with its expected diagnostics asserted by a test.

Purpose: 17 of the 92 BBj programs under `examples/` fail to compile today. Some fail on something
that is not their point — an object-typed name without its suffix, a string variable assigned a
number, a function definition missing its keyword, a relative file-qualified path, a verb given one
argument too many, two assignments joined by a colon. Some are mixed, valid except for a few lines at
the end. At least one was added as supported syntax the compiler does not actually accept, which is
the case the phase decided to move and file a todo for rather than tighten the grammar. Nothing
asserts any of this today: no test reads `examples/` at all (D-15 to D-21).

The parse side is already clean — every one of the 92 files parses with zero lexer and parser errors
on the tree this plan starts from — so the always-on layer is green from the moment it is written and
its job is to keep it that way.

Output: a new examples test with both layers, the `examples/invalid/` folder with its README and one
sidecar per file, the repairs and splits, the todo for the false-premise case, and the two hardcoded
fixture lines in the installed-extension test following the example they quote.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONTEXT.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-RESEARCH.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-PATTERNS.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `100-01-PLAN.md` — it is the phase-wide list and it names this plan as the owner of
`bbj-vscode/test/examples-compile.test.ts`, the `examples/invalid/` folder and its README, the
per-file sidecar expectations, the todo file, and the two updated lines in
`bbj-vscode/test/functional/installed-extension-e2e.test.ts`.

## Working rules for the executor (violating these stalls the run on a permission prompt)

- Absolute paths only. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`/`git`. Use
  `git -C /home/coder/repos/bbj-language-server ...`. The only allowed `cd` is in front of
  `npm`/`npx` in `/home/coder/repos/bbj-language-server/bbj-vscode`.
- Never use `git stash`. Stage with `git add <exact path>` only — never `git add -A` or `git add .`.
  Move a file with `git -C /home/coder/repos/bbj-language-server mv <exact old> <exact new>` so the
  history follows it.
- Commit with plain `git -C /home/coder/repos/bbj-language-server commit`, not the gsd commit helper:
  the helper writes a subject only, and every commit here must carry the trailer
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` in the same paragraph as the body.
- vitest runs with cwd = `bbj-vscode`. The always-on layer runs with `RUN_BBJ_TESTS=0`; the
  compiler-gated layer runs with `RUN_BBJ_TESTS=1` and only for this plan's own test file, so the
  rest of the BBj-dependent suite is not dragged in. Never pass `basic` as a vitest reporter name.
- Compile probes and the gated layer use `/opt/bbx/bin/bbjcpl -N <file>` through `spawnSync` with a
  bounded timeout, reading stdout AND stderr: the compiler exits zero and writes its errors to
  stderr, so a check reading stdout alone reports a false acceptance.
- Tests parse with `parseHelper` / `validationHelper`. Never `DocumentBuilder.build`.
- This plan changes no grammar and no lexer. `bbj-vscode/src/` must be untouched at the end.
- Register check: no plan id, decision id, requirement id or phase number in example content, sidecar
  content, README text, test names or test comments. GitHub issue numbers are fine and are kept in
  file names.
- Before moving or splitting any file, grep the test tree for its name and for any literal line of its
  text, and handle every hit in the same task as the move.
- Record deviations in the SUMMARY only. Do not open a WINDOWS.md ledger entry.

## Edge-probe accounting for this plan

Four probe rows were surfaced for EXMP-01 and all four are authored as explicit `must_haves.truths`:
*adjacency* as "a file is excluded by its extension, not by a name match, so a configuration file
next to a similarly-named program is excluded for the right reason"; *empty* as "an invalid-folder
file with no language-server diagnostic carries an explicit no-diagnostic-today entry rather than an
empty list, and an empty invalid folder fails the test rather than passing vacuously"; *ordering* as
"the file lists are sorted so a failure names the same file on every run"; *concurrency* as "the
compiler-gated layer spawns one compile per file with a bounded timeout and reports a timeout as a
failure for that file rather than skipping it".

4 surfaced = 4 authored + 0 flagged; nothing dropped.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "an example is either compiling or deliberately invalid and asserted" — one of each</name>
  <precondition>`/opt/bbx/bin/bbjcpl` exists and is executable; without it the compiler-gated layer cannot be exercised locally and the plan's triage has no oracle.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-RESEARCH.md — the EXMP-01 row of the requirements table, Open Questions 3, 4 and 5, Pitfall 1, and the compile-probe snippet in "Code Examples"
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-PATTERNS.md, sections "examples-compile.test.ts" and "examples/*.bbj repairs and splits"
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/conformance-regressions.test.ts (the folder-scan-and-assert shape to follow)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/example-files.test.ts (the other scan-a-directory test, and the environment-switch convention)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-helper.ts (`initializeWorkspace`)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-cpl-service.ts (how this project already invokes the compiler)
  </read_first>
  <files>
    bbj-vscode/test/examples-compile.test.ts,
    examples/invalid/README.md
  </files>
  <action>
    Build the sweep, build the test with both layers, and take exactly one repair and one move through
    it. No other file until the path is green.

    1. Write a sweep under the session scratchpad that walks `examples/` recursively, collects every
       file whose name ends in the BBj program extension, compiles each with the compiler through
       `spawnSync` with a bounded timeout, and writes to the scratchpad, per failing file: the path
       relative to the examples root, the number of reported error lines, and each error line. Reading
       stdout alone is the trap here; concatenate both streams. Expect 92 files and 17 failures on the
       tree this plan starts from; if the counts differ, record the difference and continue with what
       the sweep actually found rather than with the expected numbers.
    2. Triage each of the 17 into one of the four dispositions, by reading the file and deciding what
       its point is: **repair in place** when the error is not the file's point; **move to
       `examples/invalid/`** when the error IS its point; **split** when the file is valid except for
       a few lines whose point is the error; **move plus todo** when the file was added as supported
       syntax the compiler does not accept and no valid spelling exists. Write the disposition table
       to the scratchpad before editing anything. The research file's Open Questions already argue
       three of them — the composer-cue file's colon join, the mixed suffix-convention class, and the
       two file-dialog functions — read those arguments and either follow them or say why not.
    3. Create `examples/invalid/` and its `README.md`: a short prose file saying these programs fail
       to compile on purpose, that they exist so the diagnostics they should draw stay asserted, and
       one line per file naming what it demonstrates. Keep the issue-numbered file names. No
       requirement, plan or decision identifier anywhere in it.
    4. Decide the sidecar format and write it down in the README: one sidecar file per invalid
       example, named after it, carrying a short reason and a list of expected diagnostics, each with
       a line number, a message fragment to match on rather than a whole message, and a severity — and
       an explicit no-diagnostic-today marker for a file the language server cannot flag at all,
       distinct from an empty list.
    5. Create `bbj-vscode/test/examples-compile.test.ts` with the two layers:
       - **always-on**: one services instance, one top-level `beforeAll` calling `initializeWorkspace`
         and creating a `validationHelper`; walk `examples/` recursively, sorted, excluding the
         `examples/invalid/` subtree; assert every remaining program parses with zero lexer and zero
         parser errors and validates with zero error-severity diagnostics once linking diagnostics are
         excluded, matching the filter the conformance test already uses. Then walk
         `examples/invalid/`, sorted; assert the folder is not empty; assert every program there has a
         sidecar and every sidecar has a program; and for each, assert each expected diagnostic is
         present by line, severity and message fragment — or, for a no-diagnostic-today entry, record
         that the language server produces nothing and assert only that the sidecar says so, so the
         gap stays visible rather than becoming a silent pass.
       - **compiler-gated**: skipped unless the BBj environment switch is on; compile every valid
         program and assert an empty combined output, compile every invalid one and assert a non-empty
         one, one spawn per file with a bounded timeout, a timeout reported as that file's failure.
       State in a comment why the non-program files — the configuration file and the library file —
       are excluded: they are not programs, and the extension filter is what excludes them, not a
       name match.
    6. Take ONE repair and ONE move through the whole path: pick a single-error repair from the
       disposition table, make the smallest edit that compiles while keeping the construct, and move
       one file whose point is the error into the invalid folder with its sidecar. Re-run the sweep for
       those two files, then run the test in both modes. Both must be green before Task 2 starts.
    7. Delete nothing from the sweep output; Task 2 works from the disposition table.
  </action>
  <acceptance_criteria>
    - The sweep walks `examples/` recursively, uses both output streams as the verdict, and its file and failure counts are recorded; any difference from 92 and 17 is recorded rather than assumed away.
    - A disposition table exists in the scratchpad with one row per failing file and one of the four dispositions, and the three files the research file argues about are each either followed or explicitly overruled with a reason.
    - `examples/invalid/README.md` exists, states that the files fail to compile on purpose, documents the sidecar format including the no-diagnostic-today marker, and names what each file it lists demonstrates.
    - `bbj-vscode/test/examples-compile.test.ts` exists with both layers, one `createBBjServices(` call and one top-level `beforeAll`; it walks recursively and sorted, excludes the invalid subtree from the valid scan, asserts the invalid folder is non-empty, asserts the sidecar pairing in both directions, and carries a comment stating why the non-program files are excluded by extension.
    - The compiler-gated layer is skipped when the BBj environment switch is off and uses a bounded timeout per spawn with a timeout reported as a failure.
    - Exactly one file has been repaired and exactly one moved at the end of this task, and both the always-on and the compiler-gated run are green.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/examples-compile.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the run reports 0 tests collected</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/examples-compile.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the compiler-gated describe block reports as skipped when the switch is on</fails_when>
  </verify>
  <done>Every example has a compiler verdict and a disposition, the invalid folder and its README exist with a documented sidecar format, the two-layer test exists and is green, and one repair and one move have travelled the whole path.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining repairs, splits, moves and the filed todo</name>
  <read_first>
    - the disposition table this plan's Task 1 wrote to the scratchpad, and the per-file error lines from the sweep
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-RESEARCH.md, Open Questions 3, 4 and 5
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/installed-extension-e2e.test.ts — the two places that hold the composer-cue example's failing line as a literal string (locate by the variable names in the line, not by line number)
    - each example file named in this task's action, before editing it — the point of the file decides the repair
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/ (the todo file format to follow)
  </read_first>
  <files>
    examples/dim-examples.bbj,
    examples/files.bbj,
    examples/imports/issue50.bbj,
    examples/imports/onlyStaticField.bbj,
    examples/issue181-release-syntax.bbj,
    examples/issue182-clipfromstr-syntax.bbj,
    examples/issue198-member-access-levels-instance.bbj,
    examples/issue198-member-access-levels-static.bbj,
    examples/issue207-member-access-levels-auto-getter-setter.bbj,
    examples/issue224.bbj,
    examples/issue246.bbj,
    examples/issue44-return.bbj,
    examples/issue650-composer-cues.bbj,
    examples/issue_378_class_bbjstring_bbjnumber.bbj,
    examples/mnemonics.bbj,
    examples/msgbox.bbj,
    examples/using-java.bbj,
    examples/invalid/README.md,
    bbj-vscode/test/functional/installed-extension-e2e.test.ts,
    .planning/todos/pending/2026-09-21-file-dialog-functions-accepted-on-a-false-premise.md
  </files>
  <action>
    Work the disposition table family by family, recompiling each file immediately after editing it so
    a failure is attributable to one edit. The sweep this plan ran found these failing files and first
    error lines; the two files Task 1 already handled drop out of the list.

    **Family: an object-typed or wrongly-suffixed name.** `issue198-member-access-levels-instance.bbj`
    and `issue198-member-access-levels-static.bbj` (field and method declarations whose object-typed
    names carry no suffix), `issue207-member-access-levels-auto-getter-setter.bbj` (a protected field
    of a class type), `using-java.bbj` (a method declaration and a string assigned to an
    object-suffixed name), `issue182-clipfromstr-syntax.bbj` and `mnemonics.bbj` and `issue224.bbj`
    (a number assigned to a string-suffixed name, or a string to a numeric one). The point of each of
    these files is the access level, the Java interop or the mnemonic — not the suffix — so the
    default is a repair that keeps the construct. Decide per file whether the right smallest edit is
    the suffix or the value.

    **Family: a relative file-qualified path.** `imports/issue50.bbj` and
    `imports/onlyStaticField.bbj`. Before touching either, grep the test tree for both file names —
    the import and linking tests resolve across this folder, and a repair that changes what one file
    exports changes what another resolves. Repair to the spelling the compiler accepts, keeping the
    cross-file import the files exist to demonstrate.

    **Family: a verb or definition given the wrong shape.** `files.bbj` (a verb with one argument too
    many) and `issue44-return.bbj` (a function definition missing its keyword). Repair to the form the
    compiler accepts, keeping the verb and the function the files are about.

    **Family: mixed — split under D-17.** `dim-examples.bbj` ends with bare substring expressions
    whose point is that they are expressions, and `issue181-release-syntax.bbj` carries a type error
    next to valid release forms. Keep the valid body under the original name so it compiles, and move
    the erroneous lines into a sibling file in `examples/invalid/` with its own sidecar and README
    entry. `issue_378_class_bbjstring_bbjnumber.bbj` is the judgement call the research file flags:
    read the file's own intent once more, repair the lines whose point is not the error, and split out
    only the line whose point IS the wrong suffix — if none is, repair all three.

    **The cross-file one.** `issue650-composer-cues.bbj` fails on two assignments joined by a colon.
    Repair the join to the separator the compiler accepts AND update BOTH hardcoded copies of that
    line in `bbj-vscode/test/functional/installed-extension-e2e.test.ts` **in this same task** — that
    test matches the fixture's text as a literal string twice, and splitting the two edits across
    tasks leaves a red tree in between. Confirm the file still demonstrates two window-creation calls
    on one physical line, which is the property the test is checking.

    **The false-premise one.** `issue246.bbj` calls two file-dialog functions the compiler rejects even
    with trivial arguments and no named argument at all — the named-argument spelling is not the
    problem. Follow the phase's own disposition: move the file to `examples/invalid/`, give it a
    sidecar whose entry is the explicit no-diagnostic-today marker, add its README line, and file a
    todo at `.planning/todos/pending/2026-09-21-file-dialog-functions-accepted-on-a-false-premise.md`
    recording that the grammar accepts two call forms the compiler does not, that the rule was not
    tightened here because strict checks are deferred, and what a later phase would have to decide.
    Do not tighten the grammar. `msgbox.bbj` uses the same named-argument spelling but on a function
    the compiler does accept — establish with the compiler whether its failure is the named argument
    or an unrelated argument type, and repair it if a valid spelling exists rather than assuming it
    shares the other file's fate.

    After each family, re-run the sweep for the files it touched and re-run the always-on layer. When
    all families are done, extend `examples/invalid/README.md` so every file now in the folder has its
    line, and write a sidecar for every one of them. Then run the register check over the whole diff
    of this plan, extended to cover `examples` as well as `bbj-vscode`.
  </action>
  <acceptance_criteria>
    - Every file in the disposition table has been repaired, split or moved, and the sweep reports zero failing programs outside `examples/invalid/`.
    - Every program under `examples/invalid/` fails to compile, has a sidecar, and has a line in the README.
    - The composer-cue example and both hardcoded copies of its line in `bbj-vscode/test/functional/installed-extension-e2e.test.ts` agree, and the example still holds two window-creation calls on one physical line.
    - `.planning/todos/pending/2026-09-21-file-dialog-functions-accepted-on-a-false-premise.md` exists and records the two call forms, the reason the grammar was not tightened, and what a later phase must decide.
    - `git -C /home/coder/repos/bbj-language-server diff --stat -- bbj-vscode/src/language/bbj.langium` reports no change — no grammar rule was tightened or widened by this plan.
    - No example file was deleted: `git -C /home/coder/repos/bbj-language-server diff --name-status HEAD -- examples` shows no delete that is not the other half of a rename into `examples/invalid/`.
    - `examples/config.bbx`, `examples/config.min` and `examples/functions.bbl` are unchanged and unmoved.
    - The register check over this plan's whole diff, covering `examples` and `bbj-vscode`, produces no match.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/examples-compile.test.ts test/imports.test.ts test/linking.test.ts test/textmate-bbx-highlighting.test.ts test/utils.test.ts test/example-files.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary beyond the interop-dependent linking failures recorded in the project's standing notes, or the run reports 0 tests collected</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 -- bbj-vscode examples | grep '^+' | grep -qE 'PARSE-0|CONF-0|EXMP-0|D-[0-9][0-9]|100-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source, a fixture or an example</fails_when>
  </verify>
  <done>Every failing example is repaired, split or moved with its sidecar and README line; the cross-file line agrees with the test that quotes it; the false-premise case is moved with a filed todo and no grammar change; and no planning identifier reached the diff.</done>
</task>

<task type="auto">
  <name>Task 3: Close the loop — full sweep, both layers, and the record</name>
  <read_first>
    - the sweep output and the disposition table from Tasks 1 and 2
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md (the rows this plan is responsible for)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/examples-compile.test.ts (as written by Task 1 and extended by Task 2)
  </read_first>
  <files>bbj-vscode/test/examples-compile.test.ts</files>
  <action>
    Prove the whole set, not only the files that were touched.

    1. Re-run the recursive sweep over all of `examples/` from scratch. Every program outside the
       invalid folder must compile with an empty combined output; every program inside it must fail.
       Record both counts.
    2. Run the always-on layer and the compiler-gated layer, each once, and record the assertion counts
       so a later reader can see the test actually exercised every file rather than an empty list.
    3. Re-check the three tests that reference single example files — the two that reference the
       non-program files and the installed-extension one — and confirm each still resolves the path it
       expects. If any reference is now stale, fix it here.
    4. Run the whole vitest suite and judge it on the failed-test count, not the failed-suite count.
       The installed-extension test is a known environment failure when the installed bundle is stale;
       a `beforeAll` hook timeout under contention is not a failure. Any genuinely newly failing test
       is a stop-and-report.
    5. If any program cannot satisfy its layer — a valid example that does not validate clean, or an
       invalid example the language server cannot flag and whose sidecar does not say so — do not
       weaken the assertion, skip the file or delete the example. Report it and stop.
  </action>
  <acceptance_criteria>
    - The fresh sweep reports every program outside `examples/invalid/` compiling and every program inside it failing, with both counts recorded.
    - The always-on layer and the compiler-gated layer each ran once and each reports a non-zero number of files checked.
    - The three tests that reference single example files all resolve their paths.
    - The whole-suite run reports a failed-test count of 0, ignoring the known stale-bundle installed-extension environment failure and hook-timeout failed suites with no failed tests.
    - No assertion in the examples test was weakened, skipped or deleted, and no example was deleted.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/examples-compile.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the compiler-gated describe block reports as skipped</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary, other than the known stale-bundle installed-extension environment failure (a non-zero exit caused only by hook-timeout failed suites with a failed-test count of 0 is not a failure)</fails_when>
  </verify>
  <done>Every BBj program under `examples/` either compiles or is deliberately invalid with an asserted sidecar, both layers ran over a non-empty file list, the three single-file references still resolve, and the whole suite is green on failed tests.</done>
</task>

</tasks>

<!-- planner-discipline-allow: PARSE-0|CONF-0|EXMP-0|D-[0-9][0-9]|100-[0-9][0-9] -->
<!-- planner-discipline-allow: ^+ -->

<threat_model>
ASVS level 1; block on high. This plan edits example source files in this repository and adds a test
that spawns the local BBj compiler on those repository-local files. It adds no network surface, no
authentication, no session or access-control path, and it changes no grammar, lexer or validator.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository -> the local BBj compiler | the new test's gated layer spawns the compiler once per example file, on paths resolved from inside this repository only |
| developer workspace -> language-server parser and validator | the always-on layer parses and validates each example in-process |
| this repository -> the public | every file this plan writes is world-readable, including example content, sidecars and the README |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-100-05-01 | Information Disclosure | example repairs, sidecars, `examples/invalid/README.md` | high | mitigate | No corpus text enters this plan at all: every edit is to a file already in this public repository, and the sweep reads only `examples/`. The register check in Task 2 covers `examples` as well as `bbj-vscode`, so no planning identifier reaches an example, a sidecar or the README either. |
| T-100-05-02 | Tampering | the compile verdict used to decide every disposition | high | mitigate | The compiler exits zero and writes its errors to stderr; the sweep and the gated layer both concatenate stdout and stderr, and Task 3 re-runs the sweep from scratch over the whole set rather than trusting the per-file checks made during editing. |
| T-100-05-03 | Command Injection | the gated layer's compiler spawn | medium | mitigate | The compiler is invoked through an argument array with `spawnSync`, never through a shell string, and every path is resolved from the repository-local examples directory rather than taken from input. This matches how the project's own compiler service already invokes it. |
| T-100-05-04 | Denial of Service | the gated layer running one compile per example | medium | mitigate | The layer is skipped unless the BBj environment switch is on, so it never runs in CI; each spawn carries a bounded timeout, and a timeout is reported as that file's failure rather than hanging the run. |
| T-100-05-05 | Repudiation | an example that cannot satisfy a layer being quietly accommodated | high | mitigate | Weakening, skipping or deleting an assertion is a prohibition, and a file the language server cannot flag must carry the explicit no-diagnostic-today marker rather than an empty expectation — so a gap stays visible in the tracked sidecar instead of disappearing into a green run. |
| T-100-05-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no project package; the research file records the package legitimacy audit as not applicable. No dependency is added or upgraded, and no network-touching command is needed because no grammar regeneration happens here. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/examples-compile.test.ts test/imports.test.ts test/linking.test.ts test/textmate-bbx-highlighting.test.ts test/utils.test.ts test/example-files.test.ts`
   — green (the blast-radius set for moving and repairing example files).
2. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/examples-compile.test.ts`
   — the compiler-gated layer actually runs and is green.
3. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`
   — judged on `numFailedTests: 0`, ignoring the known stale-bundle installed-extension environment
   failure.
4. `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src` prints nothing.
</verification>

<success_criteria>
- Every BBj program under `examples/`, at any depth, compiles or lives in `examples/invalid/`.
- Every deliberately-invalid program has a sidecar with its expected diagnostics or an explicit
  no-diagnostic-today entry, and a README line.
- The always-on layer asserts the valid set parses and validates clean; the gated layer asserts the
  compiler agrees in both directions.
- The non-program files stay where they are and are excluded by extension with the reason stated.
- The three tests that reference single example files still pass, and the two hardcoded lines follow
  the example they quote.
- The false-premise case is moved with a filed todo and no grammar change.
- No planning identifier in any example, sidecar, README or test.
</success_criteria>

<output>
Create `.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md` when
done. Record in it: the sweep's file and failure counts before and after, and any difference from the
expected 92 and 17; the disposition table, one row per failing file with the disposition and the
smallest edit made; where the judgement calls landed and why, especially the mixed suffix-convention
class and the two file-dialog functions; the sidecar format as shipped; which files carry a
no-diagnostic-today entry; the assertion counts each layer reported; and anything a layer could not
assert. Record deviations in the SUMMARY only — do not open a WINDOWS.md ledger entry.
</output>
