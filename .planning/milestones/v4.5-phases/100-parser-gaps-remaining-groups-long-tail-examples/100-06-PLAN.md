---
phase: 100-parser-gaps-remaining-groups-long-tail-examples
plan: 06
type: execute
wave: 5
depends_on: [100-04, 100-05]
files_modified:
  - .planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md
  - .planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md
autonomous: true
requirements: [PARSE-04, PARSE-05, PARSE-06, PARSE-08, PARSE-09, EXMP-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The conformance harness has been run once more after the LAST source change of the phase, including any code-review fix, and before any acceptance is asked for (D-26)."
    - "The closing gate table records: list A at or below 25; A2 at or below the Phase 99 close of 23; and B with its own per-file evidence status, recorded and not gated (D-05, D-11)."
    - "A measured value of exactly the gate threshold passes and one step past it fails: list A of 25 passes and 26 fails, A2 of 23 passes and 24 fails, and the recorded verdict says which side of the threshold the measured number sits on."
    - "Every number in the gate table is an integer file count taken from the harness details file — never a percentage, a rate or a rounded value."
    - "The A2 message groups that appeared or disappeared since the Phase 99 close are named from a file-set difference against the snapshots, not from a change in totals (D-26)."
    - "Every file that newly left the caught set is counted by file-set difference and classified as having lost an accidental catch or as a rule now accepting an invalid form, with the classification grounded in a per-file look rather than a plausible mechanism (D-05)."
    - "The shape-level residue list is complete: every remaining list-A entry is covered by a row with an own-words shape description, a file count and one of the four reason categories, and no row is still marked pending (D-11, D-12, D-13)."
    - "Each of the phase's five success criteria, in the wording the context commit amended, has a recorded verdict and named evidence; each requirement identifier is mapped to the fixture and the test that cover it (D-01)."
    - "The whole vitest suite on the final tree reports zero failed tests, judged on the failed-test count rather than the failed-suite count, and the phase's whole source diff carries no planning identifier."
    - "The two deliberate non-goals are stated with their evidence: no blanket reserved-word rule anywhere, and no new editor capability for any construct this phase touched."
    - "Only counts, first-word groups, message groups, word lists and own-words shape descriptions reach tracked files — no corpus file name, path or source line (D-27)."
    - statement: "The closing run and the last per-group run report the same numbers when no source changed between them, so any recorded movement is attributable to a specific commit rather than to run-to-run variation (D-26)."
      verification: backstop
    - statement: "The harness run measures a tree that does not change underneath it: the source is clean and the generated parser current at the moment the run starts, and a run whose tree was modified in flight describes no committed tree and is repeated."
      verification: backstop
  artifacts:
    - .planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md
    - .planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md
  key_links:
    - "`bbj.langium` and `bbj-token-builder.ts` <-> the gitignored generated parser <-> the harness, which loads the language server from source. If the generated parser is older than the grammar at the moment of the run, every number in the closing table describes a tree that was never committed."
    - "The snapshot of the harness's details file <-> the fresh one it overwrites. The snapshot is the only way a file-set difference can be computed at all; without it only totals remain, and a pair of offsetting movements is invisible."
    - "The closing table <-> the roadmap's five success criteria. A gate row with no criterion behind it, or a criterion with no row, is how a phase seals green while a promise is unmet."
    - "The residue table's pending rows <-> the orchestrator's per-file look between plans 04 and 06. A pending row still pending at this point means the fourth criterion is unmet, whatever the counts say."
  prohibitions:
    - statement: "MUST NOT widen a grammar rule, weaken a check, adjust a fixture or edit an example in order to reach a gate number — a missed gate is recorded and escalated, never engineered away."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT write a cause, an attribution or an explanation for a moved file that was not established by looking at that file."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT mark a requirement complete in the requirements file while any plan declaring it is still open, and MUST NOT leave a residue row marked pending while claiming the fourth criterion holds."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT put corpus source text, corpus file names or corpus paths into any tracked file in this repository."
      status: unresolved
      verification: judgment
---

<objective>
Close the phase's measurement: re-run the private conformance harness once more on the final tree,
write the gate table, confirm the shape-level residue list is complete rather than pending, and record
a verdict with named evidence for each of the phase's five success criteria.

Purpose: the per-group runs in plans 01 to 04 each measured one construct against the tree as it stood
then, and plan 05 changed no grammar at all. The gate is about the phase as a whole on the tree that
will actually ship, after the last source change including any code-review fix. The expected drop from
52 to at or below 25 is a projection, not a promise: a file can hide a second cause behind the first,
so if a gate is missed this plan escalates rather than reaching for the number.

This plan changes no source. It runs the harness, computes file-set differences against the snapshots,
verifies the residue list is closed, and writes the record.

Output: the closing sections of `100-CONFORMANCE.md` — the closing run, the gate table, the A2 and B
movement sections, and the closing attestation — and a reconciled `100-VALIDATION.md`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONTEXT.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-01-SUMMARY.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-02-SUMMARY.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-03-SUMMARY.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-04-SUMMARY.md
@.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-05-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `100-01-PLAN.md` — it is the phase-wide list. This plan creates no source symbol; it
writes the closing sections of `100-CONFORMANCE.md` and reconciles `100-VALIDATION.md`.

## Working rules for the executor (violating these stalls the run on a permission prompt)

- Absolute paths only. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`/`git`. Use
  `git -C /home/coder/repos/bbj-language-server ...`. The only allowed `cd` is in front of
  `npm`/`npx` in `/home/coder/repos/bbj-language-server/bbj-vscode`.
- Never use `git stash`. Stage with `git add <exact path>` only — never `git add -A` or `git add .`.
- Commit with plain `git -C /home/coder/repos/bbj-language-server commit`, not the gsd commit helper:
  the helper writes a subject only, and every commit here must carry the trailer
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` in the same paragraph as the body.
- vitest runs with cwd = `bbj-vscode` and `RUN_BBJ_TESTS=0`. Never pass `basic` as a vitest reporter name.
- `npm run build` is NOT needed for the harness run: it loads the language server from source, so only
  the generated directory has to be current. Refresh it with `npm run langium:generate` under Node 22
  — `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"`, then prefix the generate call
  with `PATH="$NODE22_BIN:$PATH"` — and only if the grammar is newer than the generated files.
- Do not read anything under `/home/coder/repos/bbj-corpus/corpus/`. The harness entry point, its
  report and its details file may be read; their contents leave this session as counts, group names,
  word lists and own-words shape descriptions only.
- Planning identifiers are expected and fine inside `.planning/`. The register check applies to the
  source diff only, and here it covers `bbj-vscode` and `examples`.
- This plan does not mark a requirement complete in `REQUIREMENTS.md`. It records verdicts and
  evidence; the requirement ticks happen at phase transition, and never while a declaring plan is
  still open.
- Record deviations in the SUMMARY only. Do not open a WINDOWS.md ledger entry.

## Edge-probe accounting for this plan

Three of PARSE-09's six probe rows are carried here — plan 04 carries the other three — and all three
are authored as explicit `must_haves.truths`: *boundary* as "a measured value of exactly the gate
threshold passes and one step past it fails, and the verdict says which side the number sits on";
*precision* as "every gate number is an integer file count from the details file, never a percentage
or a rounded value"; *concurrency* as the backstop "the run measures a tree that does not change
underneath it, and a run whose tree moved in flight is repeated".

3 surfaced here = 3 authored + 0 flagged; nothing dropped. Across the phase, 23 surfaced = 22
authored + 1 flagged (the unclassified PARSE-06 row, flagged in plan 02's accounting).

<tasks>

<task type="auto">
  <name>Task 1: Prove the final tree is the tree being measured</name>
  <read_first>
    - the five plan SUMMARY files in /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/ (what each plan changed, which still-flagged case each pinned, what each left open — and the phase base commit recorded in the first one)
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md (the sampling contract and the whole-suite judging rule)
  </read_first>
  <files>.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-VALIDATION.md</files>
  <action>
    Confirm coherence before any number is written down.

    1. Compare the modification time of `bbj-vscode/src/language/bbj.langium` and of
       `bbj-vscode/src/language/bbj-token-builder.ts` with that of
       `bbj-vscode/src/language/generated/ast.ts`. If either source is newer, regenerate under Node 22
       as described in the working rules. Do not run `npm run build`.
    2. Confirm the working tree is clean under `bbj-vscode/src` and `examples`, and that nothing under
       the generated directory is staged or modified in git's view.
    3. Run the whole vitest suite with two workers and judge it on the reported failed-test count, not
       the failed-suite count: a suite that fails because a `beforeAll` hook timed out under
       contention is environment noise, and the installed-extension test is a known environment
       failure when the installed bundle is stale. If any test genuinely fails, stop and report rather
       than measuring.
    4. Confirm every fixture this phase promised is present in the conformance folder and collected by
       the conformance test, and that the phase's parser test file holds one describe block per
       construct group, each with its still-flagged case. Confirm the examples test exists and that
       `examples/invalid/` holds at least one program, each with a sidecar.
    5. Run the register check over the phase's whole source diff, covering `bbj-vscode` and `examples`.
       Resolve the base as the phase base commit recorded in plan 01's SUMMARY; if that is missing,
       fall back to the merge base of HEAD with `origin/main`, which is a safe superset because every
       earlier phase enforced the same check over its own diff.
    6. Reconcile `100-VALIDATION.md`'s Per-Task Verification Map: for each row, confirm the named
       command exists and was run, set the status column, and note any row whose file column still
       reads as missing. Set `wave_0_complete` and `nyquist_compliant` honestly.
  </action>
  <acceptance_criteria>
    - The vitest run reports a failed-test count of 0, other than the known stale-bundle installed-extension environment failure.
    - `bbj-vscode/src/language/generated/ast.ts` is no older than either `bbj.langium` or `bbj-token-builder.ts`.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src examples` prints nothing.
    - `ls -1 /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/` lists every fixture this phase promised alongside the fourteen inherited ones.
    - `bbj-vscode/test/examples-compile.test.ts` exists, and every program under `examples/invalid/` has a sidecar.
    - The register check over the phase's whole source diff, covering `bbj-vscode` and `examples`, produces no match.
    - Every row of the Per-Task Verification Map in `100-VALIDATION.md` carries a status and a real task reference.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary, other than the known stale-bundle installed-extension environment failure (a non-zero exit caused only by hook-timeout failed suites with a failed-test count of 0 is not a failure)</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 "$(git -C /home/coder/repos/bbj-language-server merge-base HEAD origin/main)"..HEAD -- bbj-vscode examples | grep '^+' | grep -qE 'PARSE-0|CONF-0|EXMP-0|D-[0-9][0-9]|100-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source</fails_when>
  </verify>
  <done>The final tree has a current generated parser, a clean source and examples status, a suite green on failed tests, every promised fixture and the examples test live, a clean register check, and a reconciled verification map.</done>
</task>

<task type="auto">
  <name>Task 2: Closing harness run, the gate table, and the completed residue list</name>
  <precondition>`/home/coder/repos/bbj-corpus/conformance/run.mjs` and `/home/coder/repos/bbj-corpus/conformance/snapshots/` both exist and are readable, and the per-plan before-snapshots from plans 01 to 04 are still present in the snapshots directory.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md (the four per-plan run sections, the baselines, and the residue table with its pending rows)
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md — the closing-run, gate-table and movement-section format to repeat
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md, the "Phase 100" block (the five success criteria in their amended wording) and the "Phase 104" block (where the closing measurement reads the residue from)
    - /home/coder/repos/bbj-corpus/conformance/REPORT.md — the A-by-first-word and A2-by-message group tables; read for counts and group names only
  </read_first>
  <files>.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</files>
  <action>
    Measure the final tree once and write the gate (D-05, D-11, D-12, D-13, D-26, D-27).

    1. Copy `/home/coder/repos/bbj-corpus/conformance/details.json` to
       `/home/coder/repos/bbj-corpus/conformance/snapshots/details-100-06-before.json`, then run the
       harness with the command in the verify block. State in the record which commit was measured and
       that the source was unmodified at the time. If anything under `bbj-vscode/src` or `examples`
       changes while the run is in flight, discard the run and repeat it — a run over a moving tree
       describes no committed tree.
    2. Append a **Closing run** section with the command, the date, the commit, and one table row per
       measure carrying: the Phase 99 close baseline (A 52, A2 23, B 666 of 1,210), the value at the
       last per-group run, this run's value, both deltas, the gate, and the verdict. Restate the
       direction of worse for each measure so a later reader can check the arithmetic instead of
       trusting it. Every value is an integer file count from the details file — never a percentage or
       a rate.
    3. Append a **Gate table** with exactly three rows: list A at or below 25; A2 at or below 23; and
       B recorded with its per-file evidence status. For each of the first two, state the measured
       value, the threshold, and which side of the threshold it sits on — a value equal to the
       threshold passes, one past it fails. B is not a gate, but a rise needs the file-set count and
       the hand-over.
    4. Append an **A2 movement** section naming which message groups present at the Phase 99 close are
       now absent and which are new, established by a file-set difference between this run and the
       recorded Phase 99 closing set — not by the totals. A parser fix can unmask a validator false
       alarm in the same files, which happened twice in the previous phase; say whether it happened
       here.
    5. Append a **B movement** section with the count of newly-uncaught files from the file-set
       difference against the snapshots, and each one's classification — lost an accidental catch, or
       a rule now accepting an invalid form — from the orchestrator's per-file look. If a file has not
       had that look, write it as unclassified with its count and say so; do not invent a mechanism.
       For any file classified as a rule accepting an invalid form, say whether it was fixed here or
       accepted with a reason.
    6. **Complete the residue list.** Read the residue table plan 04 opened. Every row still marked
       pending must now be filled from the orchestrator's per-file look: an own-words shape
       description, a file count, one of exactly four reason categories — not a program, compiler
       quirk, deliberately out of scope, or valid but disproportionate to fix now — and
       fixed-or-stays. Confirm the row counts sum to the measured list-A total. If any row is still
       pending at this point, the fourth criterion does not hold: record that verdict honestly and
       carry it into Task 3's conditional stop. Do not guess a category to close a row.
    7. If a gate is missed, do not adjust a rule, a check, a fixture or an example to convert it. List
       what the still-rejected files stop at, by message group with counts, disposition each as
       accepted residue or handed to a later phase, and carry the miss into Task 3's stop.
    8. Every number in this section must be reproducible from the command, the snapshots and the
       harness report alone.
  </action>
  <acceptance_criteria>
    - `/home/coder/repos/bbj-corpus/conformance/snapshots/details-100-06-before.json` exists and was written before the run.
    - `100-CONFORMANCE.md` carries a Closing run section, a three-row Gate table, an A2 movement section and a B movement section.
    - Each gate row states the measured value, the threshold and which side of the threshold the value sits on, and every recorded number is an integer file count.
    - The A2 movement claim cites a file-set difference, not a change in totals.
    - The B movement section carries a count and a classification per newly-uncaught file, or an explicit unclassified marker with its count — and no invented mechanism.
    - The residue table has no row still marked pending, or the fourth criterion is explicitly recorded as not holding; every filled row's category is one of the four permitted ones, and the row counts sum to the measured list-A total.
    - No corpus file name, corpus path or corpus source line appears anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server</automated>
    <fails_when>non-zero exit, or the run's output carries no falseRejects/falseAlarms/missed counts</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked measurement record</fails_when>
  </verify>
  <done>The final tree has been measured once, the gate table states each gate's verdict with reproducible integer arithmetic and its position relative to the threshold, the A2 and B movements are established by file-set difference, and the residue list is complete or its incompleteness is recorded as a failed criterion.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion-by-criterion closing attestation, and the conditional stop</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md (the closing sections written by Task 2)
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md, the "Phase 100" block — the five success criteria in the wording the context commit amended
    - /home/coder/repos/bbj-language-server/.planning/REQUIREMENTS.md — the six requirement identifiers this phase owns and the out-of-scope table
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/COVERAGE.md if it exists (leave it as it is; do not expand it)
  </read_first>
  <files>.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</files>
  <action>
    Write the attestation, then decide whether the phase may seal autonomously.

    1. Append a **Closing attestation** section with one entry per success criterion, in the amended
       wording. Each entry carries a verdict — holds, partially holds, or does not hold — and the
       named evidence: for the construct criteria, the fixture file name, the test command that
       asserts it, and the still-flagged case that keeps the rule honest; for the measurement
       criterion, the closing numbers, the gate verdicts, the residue row count and the fixture list;
       for the examples criterion, the two layers' commands, the count of valid and of deliberately
       invalid programs, and which of them carry a no-diagnostic-today entry. Also state, per
       requirement identifier, which fixture and which test cover it, so the requirement-to-evidence
       chain is readable without re-deriving it.
    2. State explicitly, in one line each, the two things this phase deliberately did not do: no
       blanket reserved-word rule and no generic keyword fallback anywhere, and no new editor
       capability — no hover text, no outline entry, no completion, no new validation — for any
       construct touched. Name the evidence that each holds: the grammar and token-builder diffs being
       confined to the rules and the two wiring sites named in the artifacts table, and the provider
       files being untouched.
    3. State which grammar rule was NOT tightened and why: the call forms accepted on a false premise
       are recorded with a filed todo, because the strict checks are deferred, and the reviewed
       single-line balance-rule todo was deliberately not folded into this phase.
    4. Leave `COVERAGE.md` exactly as it is if it exists. It carries the short no-external-API
       declaration the seal-time detector needs; expanding it only gives the prose detector more to
       trip on.
    5. Append a **Developer verification block** to the record, holding exactly what a human needs to
       judge the phase at end-of-phase review, in a short readable form: each gate row with its
       measured value, threshold and verdict; whether the residue list is complete and how many rows
       it holds; whether B rose and how each newly-uncaught file was classified; the per-criterion
       verdicts; and the count of valid and of deliberately-invalid examples. This block is what the
       phase verifier harvests; write it for a reader who has not seen the run.
    6. **The conditional stop.** If every gate row reads PASS, no residue row is still pending, and B
       did not rise above the Phase 99 recorded 666, finish autonomously — no checkpoint. If ANY gate
       row is a FAIL, or any residue row is still pending, or B rose, do not seal: stop and return a
       blocking human checkpoint using the executor's checkpoint return format, reporting
       `**Gate:** blocking-human`, which gate or row missed, by how much, what Task 2 already tried,
       and the two options the decision is between — accept the shortfall as recorded residue, or hand
       a named shape to a later phase. Do not adjust a rule, a check, a fixture or an example to
       convert a FAIL into a PASS.
  </action>
  <acceptance_criteria>
    - `100-CONFORMANCE.md` carries a Closing attestation section with one entry per success criterion, each with a verdict and named evidence.
    - Each of the six requirement identifiers is mapped to its fixture and its test.
    - The two deliberate non-goals are stated with their evidence, and the untightened rule is stated with its filed todo.
    - `COVERAGE.md`, if it exists, is unchanged: `git -C /home/coder/repos/bbj-language-server status --porcelain -- .planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/COVERAGE.md` prints nothing.
    - The record carries a Developer verification block holding every gate row with its measured value, threshold and verdict, the residue completeness and row count, the B classification, the per-criterion verdicts, and the two example counts.
    - If any gate row is a FAIL, any residue row is still pending, or B rose, the plan returned a blocking human checkpoint instead of sealing, and no source file was touched to move a number.
    - No source file was touched by this plan: `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode examples` prints nothing.
    - No corpus file name, corpus path or corpus source line appears anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/parser-keyword-statements.test.ts test/conformance-regressions.test.ts test/examples-compile.test.ts test/example-files.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the run reports 0 tests collected</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked measurement record</fails_when>
    <human-check>Read the Developer verification block in `100-CONFORMANCE.md` and confirm the phase may close: each gate row's measured value against its threshold and its verdict; whether the shape-level residue list is complete and how many rows it holds; whether B rose and how every newly-uncaught file was classified; the verdict and named evidence for each of the five success criteria; and the counts of valid and deliberately-invalid examples. If a gate is missed or a residue row is still pending, decide between accepting the shortfall as recorded residue and handing a named shape to a later phase.</human-check>
  </verify>
  <done>Every success criterion has a recorded verdict with named evidence, the requirement-to-evidence chain is readable, the deliberate non-goals and the untightened rule are stated, and the phase either closes cleanly or stops for a human decision on a missed gate.</done>
</task>

</tasks>

<!-- planner-discipline-allow: PARSE-0|CONF-0|EXMP-0|D-[0-9][0-9]|100-[0-9][0-9] -->
<!-- planner-discipline-allow: ^+ -->
<!-- planner-discipline-allow: bbj-corpus/corpus|/corpus/ -->

<threat_model>
ASVS level 1; block on high. This plan changes no source. It reads a private corpus's harness output
and writes counts, group names and own-words shapes into tracked planning files.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus -> this public repository | the harness report and its details file describe proprietary customer source; every tracked file in this repository is world-readable |
| this repository -> the harness | the harness loads the language server from source and executes it over the corpus in a local process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-100-06-01 | Information Disclosure | the closing sections and the completed residue table in `100-CONFORMANCE.md` | high | mitigate | Only counts, first-word groups, message groups, word lists and own-words shape descriptions are written; the residue rows describe shapes in the executor's own words and the per-file mapping stays in the private corpus repository. The corpus directory is never read. Enforced by the corpus-path grep in Tasks 2 and 3 and by the acceptance criteria forbidding a file name anywhere in the record. |
| T-100-06-02 | Repudiation | the gate table, the B movement section and the residue completion | high | mitigate | A cause may not be written for a moved file without a per-file look, and a residue row may not be closed with a guessed category; a missed gate or a still-pending row is recorded as a failed criterion and brought to the developer rather than converted into a pass. No rule, check, fixture or example may be adjusted to move a number. |
| T-100-06-03 | Tampering | the measured tree | medium | mitigate | Task 1 asserts the generated parser is no older than the grammar and the token builder and that the source and examples status is clean before the run; Task 2 discards and repeats a run whose tree changed in flight, so the closing numbers describe the committed tree and not a local edit. |
| T-100-06-04 | Denial of Service | the harness run | low | accept | One local run of about a minute over a fixed corpus, never in CI, with no java-interop contact. |
| T-100-06-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no project package; the research file records the package legitimacy audit as not applicable. The only network-touching command is the Node 22 runtime resolution, and only if the generated parser turns out to be stale. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`
   — judged on `numFailedTests: 0`, ignoring hook-timeout failed suites and the known stale-bundle
   installed-extension environment failure.
2. `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`
   — run once on the final tree, with a snapshot of the harness's details file taken first.
3. The register check over the phase's whole source diff, covering `bbj-vscode` and `examples`,
   produces no match.
4. `git -C /home/coder/repos/bbj-language-server status --porcelain` shows nothing under
   `bbj-vscode/src/language/generated/` and no scratch file.
</verification>

<success_criteria>
- The harness has been run once more after the last source change and before any acceptance.
- The gate table states, with reproducible integer arithmetic: list A at or below 25, A2 at or below
  23, and B with its per-file evidence status — each with its position relative to its threshold.
- The A2 groups that cleared or appeared are named from a file-set difference, not from totals.
- Every newly-uncaught file is classified from a per-file look, or recorded as unclassified.
- The shape-level residue list has no pending row, or the fourth criterion is recorded as not holding.
- Each of the five success criteria has a verdict and named evidence, and each requirement identifier
  is mapped to its fixture and test.
- Either every gate passes, the residue list is closed and B held, and the phase closes
  autonomously — or a blocking checkpoint reports the miss. Never a number engineered into a pass.
</success_criteria>

<output>
Create `.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-06-SUMMARY.md` when
done. Record in it: the closing numbers and the commit measured; which gates passed and which did not,
with each measured value against its threshold; the A2 groups that cleared and any that appeared; how
each newly-uncaught file was classified; whether the residue list closed completely and how many rows
it holds; whether the conditional stop fired and what it reported; and anything handed to a later phase, by shape.
Record deviations in the SUMMARY only — do not open a WINDOWS.md ledger entry.
</output>
