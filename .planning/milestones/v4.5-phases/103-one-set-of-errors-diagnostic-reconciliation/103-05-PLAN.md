---
phase: 103-one-set-of-errors-diagnostic-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [103-01, 103-02, 103-03, 103-04]
files_modified: []
autonomous: true
requirements: [PSRV-06, PSRV-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Against the endpoint-present server, in VS Code and IntelliJ alike, a line both parsers reject shows one diagnostic — BBj's, source `BBj Parser` — and a line BBj accepts shows Langium's complaint as a Warning with source `bbj`, never red once the verdict has arrived (PSRV-06, PSRV-07, D-04, D-09)."
    - "Saving while the endpoint answers adds no `BBjCPL` diagnostic and leaves no `bbjcpl` run in the debug log (PSRV-06, D-01)."
    - "While the developer types on other lines, a downgraded warning stays yellow until the next verdict; it never flashes red (D-08)."
    - "With the pre-endpoint jar swapped in, both IDEs behave as 0.16.x did: Langium's complaints stay red, no live-parser diagnostic appears, saving runs `bbjcpl` and merges its result onto Langium's line, Java completion works, and no dialog appears (roadmap criterion 3, D-01, D-03)."
    - "Both distributables are built from the final tree, after every source change in this phase."
    - "The whole suite is green on the final tree, judged by failing test names against the documented local baseline."
    - "No requirement, decision, plan, threat or review identifier appears in any line the phase adds to source or test files, and no commit body carries a closing keyword."
    - "The phase reaches PR #691 by a fast-forward push of its branch head only — never a force push — and #691's title and body describe the one-set-of-errors behaviour in user terms."
    - statement: "The jar swap leaves `/opt/bbx/.lib/bbjls/` holding exactly the two jars it held before, with both backups kept outside that directory, because the BBjServices wrapper loads every jar it finds there."
      verification: backstop
  artifacts: []
  key_links:
    - "The `bbj-ext-install` shell command packages and force-installs the VS Code extension into the isolated `~/.ext-test` profile served on port 13338; the IntelliJ zip must carry the same freshly built `out/language/main.cjs`."
    - "`/opt/bbx/bin/stopbbjservices`, a jar copy, `/opt/bbx/bin/bbjservices`, then a poll of 127.0.0.1:5008 is the verified deploy loop; the pre-endpoint jar is `/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` at 23389 bytes."
    - "The live-diagnostics mode line is logged at info level and a `bbjcpl` run leaves a debug line, so the BBj debug setting must be on in each IDE before either can be observed."
    - "PR #691's head branch is `gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`; the user holds it open until the whole v4.5 milestone is done, and it grows as later phases land."
  prohibitions:
    - statement: "A runtime log line used as hand-check evidence is copied out of a real output channel or a real `idea.log`, never derived from reading the code."
      status: unverified
      verification: flagged
    - statement: "No backup jar is ever left inside `/opt/bbx/.lib/bbjls/`."
      status: unverified
      verification: flagged
    - statement: "Nothing is pushed to `main`, nothing is force-pushed, and PR #691 is not merged or marked ready by this plan."
      status: unverified
      verification: flagged
    - statement: "No corpus file content, corpus file name or proprietary BBj source text reaches the pushed branch or the pull request text."
      status: unverified
      verification: flagged
---

<objective>
Gate the final tree, build what ships, stage the two hand checks, and grow the milestone pull request.

Purpose: success criteria 1-3 are statements about what a developer sees in two editors, against two
different servers. Every behaviour is pinned hermetically by plans 01-04, but "one diagnostic per
line", "no red on an accepted file" and "an older BBj is untouched" close only when a human looks at
both IDEs against the endpoint build and again against the pre-endpoint jar — built from the final
tree. The hand checks are staged here and harvested by the end-of-phase verification, as in
Phase 102.
Output: a green suite and clean register, two installed distributables, two self-contained hand-check
runbooks in the SUMMARY, the phase branch pushed, and PR #691 fast-forwarded with an updated title and
body.
</objective>

<executor_shell_rules>
Read this before running anything. This repository's permission hooks block opaque shell reads and
cannot tell a scoped search from a secret read; a blocked command stalls a background executor on an
unanswered prompt.

- Prefer the Read/Glob tools for reading and finding files. For content search use `grep` with an
  absolute path and an exact file or an `--include` filter.
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. `cd` is allowed only
  directly in front of a build tool that needs its project directory (`npx vitest`, `npm`,
  `./gradlew`). Use `git -C /home/coder/repos/bbj-language-server …` for every git command.
- No bare recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or
  any credential store.
- `git add <exact path>` only — never `-A`, never `.`. Never `git stash`. Never `git push --force`
  or `--force-with-lease`.
- vitest runs with `bbj-vscode` as the working directory. Never pass vitest a reporter named `basic`.
- No planning identifiers in source, tests, commit subjects or pull-request text. GitHub issue
  numbers are fine.
- Record deviations in this plan's SUMMARY only. Do not add entries to `WINDOWS.md`. Do not mark
  PSRV-06 or PSRV-07 complete in `REQUIREMENTS.md`; the phase verification does that after the hand
  checks.
</executor_shell_rules>

<!-- planner-discipline-allow: PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|CR-[0-9]|WR-[0-9]|T-103- -->

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONTEXT.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-01-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-02-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-03-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-04-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md
</context>

<artifacts_this_phase_produces>
This plan creates no source artifact. It gates, builds and ships what plans 01-04 produced:

- `bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts` — `LINE_BREAK_DIAGNOSTIC_CODE`,
  `DOWNGRADED_SYNTAX_CODE`, `VerdictState`, `LineTextLookup`, `documentLineText`,
  `syntaxComplaintKey`, `isSyntaxComplaint`, `isDowngradedSyntaxWarning`, `downgradeSyntaxComplaint`,
  `lineSpansOverlap`, `reconcileWithVerdict`, `applyVerdictCarryOver`, the verdict-state accessors,
  `rememberLangiumDiagnostics`, `recallLangiumDiagnostics` (01)
- `bbj-vscode/src/language/bbj-parser-service.ts` — `LiveParseOutcome` (01), generation-reset clear (02)
- `bbj-vscode/src/language/bbj-document-validator.ts` — exported `applyDiagnosticHierarchy`,
  `applyConfiguredDiagnosticHierarchy` (01), carry-over and the close hook (03)
- `bbj-vscode/src/language/bbj-document-builder.ts` — live parse first and the outcome handling (01, 02)
- `bbj-vscode/src/language/validations/line-break-validation.ts` — `data.code` tags (03)
- `bbj-vscode/test/bbj-diagnostic-reconciliation.test.ts` (01), `bbj-vscode/test/bbj-document-validator.test.ts` (03),
  extended `test/bbj-parser-service.test.ts`, `test/document-builder.test.ts` (01, 02),
  `test/functional/parse-program-live.test.ts` (04)
- `.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md` (04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Green suite on the final tree, clean register</name>
  <files>none — this task changes no file unless the suite exposes a regression this phase caused, in which case the fix goes into the file that caused it</files>
  <read_first>
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/regression-gate-compare-against-base-commit.md — a failing test is environment drift only if the same test name also fails on the phase base; relabelling without comparing has hidden real failures before
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/whole-suite-hook-timeouts-contention.md — a suite reported as failing with zero failed tests is a worker-startup timeout
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/register-check-source-diff-before-push.md — the register check runs on the added lines of the phase diff
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/squash-merge-commit-bodies-close-issues.md — why commit bodies are scanned for closing keywords
  </read_first>
  <action>
    Step 1 — whole suite: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm test -- --maxWorkers=2`.
    Judge it on failing test NAMES. The documented local baseline with BBjServices up is the
    interop-drift failures in `test/linking.test.ts` (11) and at most one in
    `test/functional/issue447-real-interop.test.ts`. Any other failing name: build a scratch worktree
    at the phase base (`gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`), symlink
    `bbj-vscode/node_modules`, regenerate with Node 22, run the same file there, and compare names.
    A name that fails here and passes on the base is a regression this phase caused: fix it in the
    responsible file, commit, and rerun. Remove the scratch worktree afterwards.

    Step 2 — register check over every line the phase added to source and tests (the verify command
    below). If it fires, reword the offending line to describe behaviour, commit, rerun.

    Step 3 — commit-body scan over the phase's commits (the verify command below). A closing keyword
    in any body would close an unrelated issue when the milestone squash-merges.

    Step 4 — confirm the phase changed no IntelliJ file and no auto-parsed fixture.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npm test -- --maxWorkers=2</automated>
    <fails_when>non-zero exit accompanied by any failing test name outside the documented baseline (the interop-drift cases in `test/linking.test.ts`, at most one in `test/functional/issue447-real-interop.test.ts`); a run whose only failures are those names, or which reports a failing suite with zero failed tests, is the known baseline and passes</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility..HEAD -- bbj-vscode/src bbj-vscode/test | grep '^+' | grep -qE 'PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|CR-[0-9]|WR-[0-9]|T-103-'</automated>
    <fails_when>exit status non-zero, i.e. a planning identifier appears in a line the phase added to source or tests</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server log --format=%B gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility..HEAD | grep -niE '(closes|fixes|resolves) #'</automated>
    <fails_when>exit status non-zero, i.e. a commit body carries a closing keyword</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --stat gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility..HEAD -- bbj-intellij bbj-vscode/test/test-data</automated>
    <fails_when>the command prints any line — this phase changes no IntelliJ file and adds no auto-parsed fixture</fails_when>
  </verify>
  <acceptance_criteria>
    - The whole-suite run reports no failing test name outside the documented baseline, and the SUMMARY lists the failing names it saw
    - The register check and the commit-body scan both exit 0
    - `git -C /home/coder/repos/bbj-language-server status --porcelain --untracked-files=no` prints nothing
  </acceptance_criteria>
  <done>The final tree is green against the baseline by name, and no planning identifier or closing keyword is on its way to the public repository.</done>
</task>

<task type="auto">
  <name>Task 2: Build both distributables from the final tree and stage both hand checks</name>
  <files>none — this task changes no repository file; it builds and installs the two distributables, takes one jar backup outside the BBj load directory, and stages two human checks in the SUMMARY</files>
  <precondition>BBjServices is running, 127.0.0.1:5008 accepts a connection, `/opt/bbx/.lib/bbjls/` holds exactly two files, the deployed `bbj-ls.jar` answers parseProgram (plan 04's gated test passed), and `/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` exists at 23389 bytes.</precondition>
  <read_first>
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/devcontainer-bbj-setup.md — `bbj-ext-install`, the isolated code-server on port 13338, the jar deploy loop, backups outside the load directory
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/uat-build-both-extensions-first.md — build both from the final tree; prove the IntelliJ zip carries the fresh `main.cjs`
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-parser-service.ts — the exact mode log lines and `BBJ_PARSER_SOURCE`
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-cpl-service.ts — the exact debug line a `bbjcpl` run leaves
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-04-SUMMARY.md — the invented accepted-document fixture text
    - /home/coder/repos/bbj-language-server/.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-04-PLAN.md — the Phase 102 hand-check shape this one follows
  </read_first>
  <action>
    Build: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build`, then the
    `bbj-ext-install` shell command. Then `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin`,
    and prove the zip carries the fresh server:
    `unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/<zip> bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs`
    (if Gradle reports UP-TO-DATE with an old zip, run `./gradlew clean buildPlugin`).

    Jar backup: `cp /opt/bbx/.lib/bbjls/bbj-ls.jar /opt/bbx/.lib/bbjls-backup/bbj-ls.jar.phase-103-endpoint`
    and record both backups' byte sizes (the endpoint build was 38861 bytes at plan time; record what
    you find). Never place a backup inside `/opt/bbx/.lib/bbjls/`. Do NOT swap the jar yourself: the
    tester needs each server running while looking at two IDEs.

    Write both human checks below into the SUMMARY, substituting the exact strings from the shipped
    code — the on and off mode lines, `BBJ_PARSER_SOURCE`, Langium's source `bbj`, the `bbjcpl`
    debug-line prefix — and the fixture text from plan 04's SUMMARY. Do not paraphrase them. Every log
    line the tester reports must come from the real output channel or the real `idea.log`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build</automated>
    <fails_when>non-zero exit, or any line matching `error TS`, or `/home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs` is absent afterwards</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin</automated>
    <fails_when>non-zero exit, or the output contains `FAILED`, or no zip exists under `/home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/`</fails_when>
    <automated>ls -l /opt/bbx/.lib/bbjls/ /opt/bbx/.lib/bbjls-backup/</automated>
    <fails_when>the load directory holds anything other than exactly two files, or the backup directory lacks `bbj-ls.jar.26.02` at 23389 bytes or `bbj-ls.jar.phase-103-endpoint` at the same size as the deployed `bbj-ls.jar`</fails_when>
    <human-check>
      **A. Endpoint present — verify in both IDEs.**

      Setup (done by the executor, confirm it): both distributables built from the final tree and
      installed; the BBj debug setting on in both IDEs; `ls -l /opt/bbx/.lib/bbjls/` shows two files.

      **VS Code (browser tab on port 13338):**
      1. Open a `.bbj` file from `examples/`. In the BBj output channel find exactly one line saying
         live compiler diagnostics are on. Paste it.
      2. On new lines, type the accepted-document fixture from plan 04's SUMMARY and stop typing. The
         language server's complaint may appear red at first. Within about a second it turns into a
         Warning with the same message and source `bbj`. No red remains on those lines.
      3. Keep typing for a few seconds on a DIFFERENT line. The warning from step 2 stays yellow the
         whole time — it never flashes red.
      4. Type a clearly invalid line and stop. After the pause that line carries exactly ONE
         diagnostic: BBj's message, source `BBj Parser`. No second diagnostic from `bbj` on that line.
      5. Save. No diagnostic with source `BBjCPL` appears, and the log shows no `bbjcpl` debug line
         for this save.
      6. Set the compiler trigger setting to `off`, type one character: the step 2 complaint is red
         again. Set the trigger back.
      7. Close the file and reopen it: the step 2 complaint is red until the first verdict, then yellow.

      **IntelliJ:** repeat steps 1-5 and 7. Hover shows the source in the tooltip. Paste the mode line
      from `idea.log`.

      **Answer:** Did step 2 turn yellow and stay yellow through step 3 in BOTH IDEs? Was there exactly
      one diagnostic on the invalid line? Did saving add nothing from `BBjCPL` and leave no `bbjcpl`
      line in the log? Paste the mode lines and one warning's message and source.

      **B. Pre-endpoint jar — verify both IDEs behave as 0.16.x.**

      Swap (run verbatim, in order):
      1. `/opt/bbx/bin/stopbbjservices` — wait for it to exit.
      2. `cp /opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02 /opt/bbx/.lib/bbjls/bbj-ls.jar`
      3. `ls -l /opt/bbx/.lib/bbjls/` — exactly two files, `bbj-ls.jar` 23389 bytes.
      4. `/opt/bbx/bin/bbjservices`, then wait until 127.0.0.1:5008 accepts a connection (30-60 s).
      5. Reload the VS Code (ext test) tab and restart the language server in IntelliJ.

      In VS Code and in IntelliJ:
      6. The log holds exactly ONE line saying live compiler diagnostics are off because the endpoint
         is not available. Paste it.
      7. Type the accepted-document fixture: the complaint is red and STAYS red — it never turns yellow.
      8. Type an invalid line: only the language server's own error (`bbj`), no `BBj Parser` diagnostic.
      9. Save a file with an error: a `bbjcpl` debug line appears in the log and a save-time
         diagnostic appears; on a line the language server also flags, that diagnostic reads source
         `BBjCPL` with the language server's message (the 0.16.x merge).
      10. Java completion works; no dialog, popup or warning about the endpoint appears.

      Restore:
      11. `/opt/bbx/bin/stopbbjservices`
      12. `cp /opt/bbx/.lib/bbjls-backup/bbj-ls.jar.phase-103-endpoint /opt/bbx/.lib/bbjls/bbj-ls.jar`
      13. `ls -l /opt/bbx/.lib/bbjls/` — exactly two files, `bbj-ls.jar` at the recorded endpoint size.
      14. `/opt/bbx/bin/bbjservices`, wait for 5008, reload both IDEs; the fixture from check A,
          step 2, turns yellow again once typing stops.

      **Answer:** Did both IDEs keep the complaint red, show no live-parser diagnostic, and merge
      `bbjcpl`'s result on save? Paste the off-mode lines. Any dialog or endpoint warning? After the
      restore, does the load directory show two files at the endpoint size?
    </human-check>
  </verify>
  <acceptance_criteria>
    - `/home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs` and the plugin zip under `bbj-intellij/build/distributions/` have modification times later than the phase branch's last source commit, and the `cmp` of the zip's `main.cjs` against `out/language/main.cjs` exits 0
    - `bbj-ext-install` reported a successful force-install
    - `ls -1 /opt/bbx/.lib/bbjls/ | grep -c '\.jar$'` prints `2`, and both backups exist outside that directory
    - The SUMMARY carries both hand checks with every log line, source string and fixture text substituted from the shipped code and plan 04's SUMMARY
  </acceptance_criteria>
  <done>Both distributables built from the final tree are installed, the endpoint build is backed up outside the load directory, and two self-contained hand checks — endpoint present and pre-endpoint — are staged for the phase verification.</done>
</task>

<task type="auto">
  <name>Task 3: Push the phase branch and grow PR #691</name>
  <reversibility rating="costly">A fast-forward push of #691's head is public; undoing it needs a force push of the old head, which rewrites a branch reviewers may have fetched.</reversibility>
  <files>/home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-05-SUMMARY.md</files>
  <read_first>
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/v45-pr-691-held-until-milestone-done.md — #691 is held open until the milestone is done and grows as later phases land
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/git-push-https-not-ssh.md — the remote is HTTPS and a plain push works
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/squash-merge-commit-bodies-close-issues.md
  </read_first>
  <action>
    Step 1 — push the phase branch:
    `git -C /home/coder/repos/bbj-language-server push -u origin gsd/phase-103-one-set-of-errors-diagnostic-reconciliation`.

    Step 2 — fast-forward #691's head to the same commit with a plain push:
    `git -C /home/coder/repos/bbj-language-server push origin gsd/phase-103-one-set-of-errors-diagnostic-reconciliation:gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`.
    If git rejects it as not a fast-forward, stop and report in the SUMMARY — someone else pushed to
    #691; do not force.

    Step 3 — update #691. Read its current title and body with
    `gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json title,body`. Set the title to name the
    widened scope in user terms (for example "v4.5 phases 98-103: parser conformance, live compiler
    diagnostics and one set of errors"). Keep the existing description of the earlier phases, but
    refresh what is stale and reword what the body gate below forbids: the earlier phase's hand checks
    have since passed in both IDEs, so its awaiting-verification paragraph becomes one sentence saying
    so; a line naming a requirement identifier or a plan file is reworded to describe the behaviour;
    the note about squash merges describes "a closing keyword" in words instead of quoting one. Then
    add a section describing, in prose:
    with live compiler diagnostics on, BBj's parser is the authority on syntax — a line both flag shows
    BBj's diagnostic alone; the language server's own lexer, parser and line-break complaints on lines
    BBj accepts stay visible as warnings; the save-time `bbjcpl` run is skipped while the endpoint
    answers and runs as before when a parse fails or the endpoint is absent; an older BBj behaves
    exactly as 0.16.x; and which hand checks are staged. Keep the note that the pull request stays open
    until the milestone is finished. No requirement, decision, plan or review identifier; no closing
    keyword; no corpus name or text; the body ends with the line
    `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. Apply it with
    `gh pr edit 691 --repo BBx-Kitchen/bbj-language-server --title … --body-file <a temp file>`.
    Do not merge, do not mark ready for review, do not change reviewers or labels.

    Step 4 — write the SUMMARY: the failing-test names from task 1, the build evidence and both staged
    hand checks from task 2, the pushed commit, and the updated PR title.
  </action>
  <verify>
    <automated>git -C /home/coder/repos/bbj-language-server ls-remote origin gsd/phase-103-one-set-of-errors-diagnostic-reconciliation gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility</automated>
    <fails_when>either ref is missing, or the two refs point at different commits, or that commit is not the phase branch's last source or test commit (`git -C /home/coder/repos/bbj-language-server log -1 --format=%H -- bbj-vscode` must be an ancestor of, or equal to, it)</fails_when>
    <automated>gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json state,headRefName,isDraft,title</automated>
    <fails_when>non-zero exit, or `state` is not `OPEN`, or `headRefName` is not `gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`, or the title does not mention 103</fails_when>
    <automated>! gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json body --jq .body | grep -qiE 'PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]|CR-[0-9]|WR-[0-9]|(closes|fixes|resolves) #'</automated>
    <fails_when>exit status non-zero, i.e. the pull request body carries a planning identifier or a closing keyword</fails_when>
  </verify>
  <acceptance_criteria>
    - `origin/gsd/phase-103-one-set-of-errors-diagnostic-reconciliation` and `origin/gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility` point at the same commit, which contains every source and test commit of the phase
    - No push in this task used `--force` or `--force-with-lease`
    - PR #691 is OPEN, its title names phases 98-103, and the last line of its body is exactly `🤖 Generated with [Claude Code](https://claude.com/claude-code)`
    - `103-05-SUMMARY.md` reproduces both hand checks verbatim with the substituted strings, so the verifier harvests a self-contained procedure
  </acceptance_criteria>
  <done>The phase branch is pushed, #691 carries phases 98-103 by fast-forward, its title and body describe the new behaviour in user terms, and the SUMMARY holds both staged hand checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase branch → the public repository and PR #691 | Everything pushed becomes public: source, tests, planning files and every commit body. |
| the local BBj jar load directory | Every jar in `/opt/bbx/.lib/bbjls/` is loaded into BBjServices, so its contents decide which server the IDEs talk to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-103-15 | Information Disclosure | the pushed diff and the pull request text | high | mitigate | Register check over every added source and test line, a planning-identifier and closing-keyword check over the PR body, and the phase-wide rule that no corpus name or text enters this repository. Tasks 1 and 3. |
| T-103-16 | Tampering | `/opt/bbx/.lib/bbjls/` during the swap | medium | mitigate | Both backups live outside the load directory; the file count and the active jar's size are checked before and after; the tester runs the swap and restore verbatim. Task 2. |
| T-103-17 | Repudiation | commit bodies at squash-merge time | medium | mitigate | Every phase commit body is scanned for a closing keyword before the push. Task 1. |
| T-103-18 | Tampering | hand-check evidence | medium | mitigate | Every runtime log line accepted as evidence is copied from a real output channel or a real `idea.log`; the staged checks quote the shipped strings. Task 2. |
| T-103-19 | Tampering | PR #691's head branch | medium | mitigate | Plain fast-forward push only; a rejection stops the task instead of forcing; the PR is not merged or marked ready. Task 3. |
| T-103-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; the builds use the dependencies already locked in the repository. |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm test -- --maxWorkers=2` — no failing test name outside the documented baseline.
- The register check and the commit-body scan exit 0.
- `ls -l /opt/bbx/.lib/bbjls/` lists exactly two files with the endpoint build active.
- `gh pr view 691 --repo BBx-Kitchen/bbj-language-server --json state` reports `OPEN` and the head ref matches local HEAD.
</verification>

<success_criteria>
- Green suite and clean register on the final tree.
- Both distributables built from the final tree and installed; both hand checks staged verbatim.
- PR #691 fast-forwarded to the phase head, title and body updated, still open and held.
</success_criteria>

<output>
Create `.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-05-SUMMARY.md` when done.
</output>
