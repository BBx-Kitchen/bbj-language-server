---
phase: 101-bbj-parser-endpoint-in-bbj-ls
plan: 04
type: execute
wave: 4
depends_on: ["101-03"]
files_modified:
  - /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java
  - /home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md
autonomous: false
requirements: [PSRV-01, PSRV-02]

user_setup:
  - service: BASIS GitLab (git.basis.cloud)
    why: "The phase branch has to be pushed to ssh://git@git.basis.cloud:10022/BASIS/bbj-ls.git and a merge request opened against `develop`. SSH access from this container has never been exercised and `glab` is not installed."
    dashboard_config:
      - task: "Confirm an SSH key on this machine is authorised for git.basis.cloud:10022, or push the branch yourself from a machine that is"
        location: "BASIS GitLab -> User Settings -> SSH Keys"
      - task: "Open the merge request against `develop`, pasting the prepared description"
        location: "BASIS GitLab -> BASIS/bbj-ls -> Merge requests -> New merge request"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A plain JSON-RPC client against the locally running BBjServices exercises the whole endpoint: syntax errors with positions, a clean program, referenced programs resolved through a workspace root and through a prefix entry, a missing reference, supersession, and the size-cap error code (D-15)."
    - "A superseded request observed over the real socket receives lsp4j's RequestCancelled code and no errors list, in both the still-queued and the already-running interleaving (D-11)."
    - "The original 26.02 `bbj-ls.jar`, restored from the backup outside the load directory and running under BBjServices, answers the same `parseProgram` probe with MethodNotFound and writes no stack trace to the service log — observed on a real older server, not only on an in-process stand-in (D-15, D-17)."
    - "After the new jar is swapped back in, `getClassInfo`, `loadClasspath` and `getAllClassNames` each still answer with a result rather than an error (D-17)."
    - "The endpoint's contract — method name, request and result fields, coordinate convention, category list, error-code numbers, guards, system properties, and the inherited cross-connection cache caveat — is written down in one text that Phase 102 and the BASIS reviewers both read (D-18)."
  artifacts:
    - /home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md
  key_links:
    - "`/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` is the only copy of the pre-endpoint build; the replay swaps it in and back out, and the load directory must hold exactly two jars at every resting point."
    - "The MR description, not the Java, is what Phase 102's researcher reads — every field name and number in it has to match the committed source exactly."
    - "The branch is pushed to BASIS GitLab over SSH; merging is the BASIS maintainers' call and does not block Phase 102."
  prohibitions:
    - statement: "The supplied document text is never written to disk, never copied into a temp file, and never emitted into any log line."
      status: unverified
      verification: flagged
    - statement: "An endpoint failure — exception, timeout, over-size document, missing BBj class, protected program — is never returned as an entry in the result's error list; it is always a JSON-RPC ResponseError."
      status: unverified
      verification: flagged
    - statement: "No text copied from BBj's own source tree or the internal parser handoff notes appears in bbj-ls source, Javadoc, tests, commit messages, the GitHub issue, the MR description, or this repository's summaries."
      status: unverified
      verification: flagged
---

<objective>
Close the phase: finish the plain-client suite so every behaviour the four success criteria name is
exercised over the real socket, replay the older-server probe against the actual 26.02 jar so "no
hang, no stack trace" is observed rather than asserted against a stand-in (D-15, D-17), confirm the
pre-existing interop requests survived the swap, write the contract document Phase 102 codes
against, and push the branch and open the merge request (D-18).

Merging is the BASIS maintainers' call and never blocks. SSH access from this container to
`git.basis.cloud` has not been exercised, so the push is attempted first and escalated to a human
only if it actually fails — that is an authentication gate, not pre-planned manual work.

Purpose: turn working code into a delivered, documented contract.
Output: the complete integration suite, a verified older-server replay, `101-MR-DESCRIPTION.md`,
a pushed branch and an open merge request.
</objective>

<executor_shell_rules>
Read this before running anything. This repository's permission hooks block opaque shell reads,
and they cannot tell a scoped search from a secret read. A chained `cd … && grep`, a relative
path, or a bare recursive `grep -r` trips that block and stalls a background executor.

- Prefer the Read/Grep/Glob tools over shell for reading and searching.
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target. Never chain `cd`
  with `grep`, `find`, `cat`, `sed`, `head` or `tail`. `cd` is allowed only directly in front of a
  build tool that needs its project directory — for Maven prefer
  `mvn -f /home/coder/repos/bbj-ls/pom.xml …` with no `cd` at all; for git use
  `git -C /home/coder/repos/bbj-ls …`.
- No blind recursive `grep -r` / `find` over a directory. Scope with exact files or
  `--include` / `-name` filters.
- Never read `.env*`, `*.pem`, `*.key`, credential stores, or `~/.m2/settings.xml`.
- `git add <exact path>` only, never `-A` or `.`. No `git stash`.
- Disclosure rule (D-19): no text copied from `/home/coder/repos/trunk/**` or
  `/home/coder/repos/tmp/grammar-info.md` into bbj-ls source, Javadoc, tests, commit messages,
  the GitHub issue, the MR description, or this repository's SUMMARY.md. Name interfaces, methods
  and JSON keys; never paste BBj source.
- No planning identifiers in source, tests, Javadoc or commit messages — requirement ids,
  decision ids, plan ids and review ids all stay in `.planning/`. GitHub issue numbers are fine.
  Every code task below carries a register-check verify command that enforces this on the branch
  diff.
</executor_shell_rules>

<!-- planner-discipline-allow: PSRV-0|D-[0-9][0-9]|101-[0-9][0-9]|CR-[0-9] -->

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-CONTEXT.md
@.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-RESEARCH.md
@.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supersession, the size cap and the timeout, proved over the real socket</name>
  <precondition>`git -C /home/coder/repos/bbj-ls branch --show-current` reports the phase branch created by plan 01 (a name starting `feat/`), not `develop`, and a TCP connect to 127.0.0.1:5008 succeeds.</precondition>
  <files>/home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java (the file being modified — the existing client wiring, the reachability gate, the eight scenarios already present)
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/ParserWorker.java (the five application code constants, the latest-wins map, the overrunning marker and the two system-property names these tests assert against)
    - /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseGuardsTest.java (what the guards already cover in process, so the live scenarios add the socket-level half rather than repeating it)
    - /opt/bbx/bin/bbjservices around the line that assigns `BASIS_JAVAARGS` (to learn whether extra JVM system properties can be supplied to BBjServices non-destructively — this decides whether the live timeout scenario is possible at all)
  </read_first>
  <action>
    Finish the plain-client suite (D-15) against the jar plan 03 deployed. Redeploy first only if
    `/opt/bbx/.lib/bbjls/bbj-ls.jar` is older than `/home/coder/repos/bbj-ls/target/bbj-ls.jar`;
    otherwise the running service is already the build under test.

    Add these scenarios to `ParseProgramIntegrationTest`:

    1. Supersession, queued interleaving — fire two `parseProgram` requests for the same
       `canonicalName` back to back without awaiting the first, with different `text` and
       different `version` tokens. Assert the first future completes exceptionally with a
       `ResponseErrorException` whose code equals `ResponseErrorCode.RequestCancelled.getValue()`,
       and that the second returns a normal result whose `version` is the second token and whose
       errors correspond to the second text (for instance, the second text has a syntax error on a
       line number the first text does not).
    2. Supersession, in-flight interleaving — make the first request's text large enough to take
       measurable time (a few thousand generated lines, comfortably under the size cap), send it,
       wait briefly so the worker has certainly started it, then send the second. Assert the same
       two outcomes. If the timing proves flaky on this machine, keep the assertion on the
       *outcome* (first cancelled, second returned) and do not assert which interleaving occurred —
       both are correct and the guarantee is the same.
    3. Identical tokens are still two requests — fire two requests for the same `canonicalName`
       with the *same* `version` token and the *same* `text`. Assert the earlier one is still
       cancelled with the same code and the later one still returns a result. This pins that the
       server never merges or short-circuits on an equal token.
    4. Size cap — send a `text` comfortably larger than the default cap (build it in memory, do
       not commit a fixture). Assert the call completes exceptionally with the size-cap code, and
       assert explicitly that the failure did NOT arrive as a normal result with an entry in the
       errors list. This is the socket-level half of the structural guarantee that a failure is
       never a syntax error.
    5. Timeout, conditional. Read `/opt/bbx/bin/bbjservices` to find how `BASIS_JAVAARGS` is
       supplied. If a supported, reversible mechanism exists for adding a JVM system property to
       the BBjServices process, record the current value, restart with
       `bbj.interop.parse.timeoutMs` set to a very small value, send one ordinary parse request,
       assert the call fails with the timeout code rather than hanging, then restore the original
       value and restart. If no such mechanism exists without editing installation configuration
       destructively, do NOT invent one: skip the live timeout scenario, state plainly in the plan
       SUMMARY that the timeout code is covered in process by `ParseGuardsTest` and not over the
       socket, and carry that as a named, accepted gap rather than a silent one.
    6. Legacy-server probe method — add one test method named for the older-server case, annotated
       `@EnabledIfSystemProperty(named = "bbj.interop.it.legacyServer", matches = "true")`, which
       sends `parseProgram` once and asserts the failure code is -32601. It is disabled in every
       normal run and is the exact command task 2 uses against the restored 26.02 jar. Expect the
       normal suite to report a Skipped count of exactly 1 from this point on.

    No fixture files are committed; all fixture text is generated in the test. No planning
    identifiers anywhere.

    Commit on the phase branch.
  </action>
  <verify>
    <automated>mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dbbj.interop.it.requireServer=true</automated>
    <fails_when>non-zero exit, or "BUILD FAILURE" / "No tests were executed!" / "Tests run: 0" appears in the output, or the aggregate summary reports a non-zero Failures or Errors count, or a Skipped count other than 1 (the legacy-server probe, disabled unless its property is set)</fails_when>
    <automated>grep -c '@Test' /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java</automated>
    <fails_when>the printed count is less than 12</fails_when>
    <automated>grep -c 'RequestCancelled' /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java</automated>
    <fails_when>the printed count is less than 3 — all three supersession scenarios must assert the cancellation code</fails_when>
    <automated>! git -C /home/coder/repos/bbj-ls diff -U0 develop..HEAD -- src pom.xml README.md | grep '^+' | grep -qE 'PSRV-0|D-[0-9][0-9]|101-[0-9][0-9]|CR-[0-9]'</automated>
    <fails_when>exit status non-zero, i.e. grep found a planning identifier in an added line of the branch diff</fails_when>
  </verify>
  <acceptance_criteria>
    - `ParseProgramIntegrationTest.java` declares at least 12 `@Test` methods
    - `ParseProgramIntegrationTest.java` contains `@EnabledIfSystemProperty` exactly once, naming `bbj.interop.it.legacyServer`
    - `ParseProgramIntegrationTest.java` contains an assertion on the size-cap code value `-33003`
    - `ParseProgramIntegrationTest.java` contains no absolute path under `/home/coder` and none under `/opt`
    - `mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dbbj.interop.it.requireServer=true` exits 0 with `Failures: 0, Errors: 0` and `Skipped: 1`
    - The plan SUMMARY states, in one sentence, whether the live timeout scenario ran or was skipped, and why
  </acceptance_criteria>
  <done>A newer request cancels an older one over the real socket in both interleavings and even with identical tokens; an over-size document comes back as a JSON-RPC error and never as a syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Replay the probe against the real 26.02 jar, then restore and smoke the old requests</name>
  <files>No source file changes. This task swaps deployed jars in and out of /opt/bbx/.lib/bbjls/ and drives the existing test class /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java.</files>
  <read_first>
    - /home/coder/repos/bbj-ls/src/test/java/bbj/interop/ParseProgramIntegrationTest.java (the legacy-probe method added by task 1 and the client wiring it reuses)
    - .planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-RESEARCH.md section 6 (start and stop mechanics, the wrapper's load-every-jar rule, the exact current contents and sizes of the load directory)
    - .planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-01-SUMMARY.md (the recorded size and checksum of the backed-up 26.02 jar)
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/InteropService.java (the three pre-existing request names and their parameter shapes, for the post-swap smoke)
  </read_first>
  <action>
    Observe the older-server behaviour on a real older server rather than on the in-process
    stand-in (D-15, D-17). This whole task is automatable — do not ask a human to run any of it.

    Down-swap: run `/opt/bbx/bin/stopbbjservices` and wait for it to exit; copy
    `/opt/bbx/.lib/bbjls/bbj-ls.jar` aside to a scratch path OUTSIDE `/opt/bbx/.lib/bbjls/` so the
    new build is not lost; copy `/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` into
    `/opt/bbx/.lib/bbjls/bbj-ls.jar`; confirm the load directory still holds exactly two `.jar`
    files; run `/opt/bbx/bin/bbjservices`; poll `127.0.0.1:5008` with a bounded TCP-connect retry
    loop until it accepts.

    Capture the service log's size before the probe (the log directory the launcher names through
    its own log-directory property — read `/opt/bbx/bin/bbjservices` for the variable rather than
    guessing a path), then run the legacy probe:
    `mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dtest=ParseProgramIntegrationTest#<legacy method name> -Dbbj.interop.it.requireServer=true -Dbbj.interop.it.legacyServer=true`.
    Record three observations: the probe returned the MethodNotFound code; it returned within a
    couple of seconds rather than hanging; and the service's standard-error log grew by no stack
    trace — compare the tail of the log after the probe against the captured size and quote the
    added lines (if any) in the plan SUMMARY.

    Up-swap and smoke: stop BBjServices again, restore the new build from the scratch path into
    `/opt/bbx/.lib/bbjls/bbj-ls.jar`, delete the scratch copy, start BBjServices, wait for
    `127.0.0.1:5008`. Then send, through the same lsp4j client the test uses, one `getClassInfo`
    request for a class that certainly exists, one `loadClasspath` request and one
    `getAllClassNames` request, and record that each returned a result rather than an error — a
    throwaway client run is fine here, nothing needs to be committed for the smoke. Finally re-run
    the whole suite to confirm the box is back in its working state.

    `/opt/bbx/.lib/bbjls/` must hold exactly `bbj-ls.jar` and `org.eclipse.lsp4j.jsonrpc-0.20.1.jar`
    at every resting point in this task, and `/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` must
    still exist unchanged when the task ends.
  </action>
  <verify>
    <automated>mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dbbj.interop.it.requireServer=true</automated>
    <fails_when>non-zero exit, or "BUILD FAILURE" / "No tests were executed!" / "Tests run: 0" appears in the output, or the aggregate summary reports a non-zero Failures or Errors count, or a Skipped count other than 1</fails_when>
    <automated>ls -1 /opt/bbx/.lib/bbjls/</automated>
    <fails_when>the listing is anything other than exactly the two lines `bbj-ls.jar` and `org.eclipse.lsp4j.jsonrpc-0.20.1.jar`</fails_when>
    <automated>cmp -s /opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02 /opt/bbx/.lib/bbjls/bbj-ls.jar; echo $?</automated>
    <fails_when>the printed value is 0, i.e. the deployed jar is still the 26.02 backup — the up-swap did not happen</fails_when>
    <human-check>
      Confirm from the plan SUMMARY that the older-server replay is grounded in real output, not a
      derived trace: it must quote the probe's actual error code and message, the measured
      round-trip time, and the literal lines (or "none") that the BBjServices standard-error log
      gained while the probe ran. A hand-written sequence with no captured output is not evidence.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The plan SUMMARY quotes the legacy probe's observed error code as -32601 and its observed message text
    - The plan SUMMARY states the measured probe round-trip time and it is under 5 seconds
    - The plan SUMMARY quotes the lines the BBjServices standard-error log gained during the probe, or states "none"
    - The plan SUMMARY records one line per pre-existing request (`getClassInfo`, `loadClasspath`, `getAllClassNames`) saying it returned a result after the up-swap
    - `/opt/bbx/.lib/bbjls-backup/bbj-ls.jar.26.02` is present and 23389 bytes
    - `mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dbbj.interop.it.requireServer=true` exits 0 with `Failures: 0, Errors: 0` and `Skipped: 1`
  </acceptance_criteria>
  <done>A real pre-endpoint BBjServices answers the probe with -32601 inside seconds and logs no stack trace; the new jar is back in place and the three older interop requests still answer.</done>
</task>

<task type="auto">
  <name>Task 3: Write the contract document and push the branch</name>
  <files>/home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md</files>
  <read_first>
    - This plan's "## MR description" section below (the source text to copy — it is the contract, and every field name and number in it must be checked against the committed source before it is written out)
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/data/ParseProgramParams.java, data/ParseProgramResult.java and data/ParseError.java (the field names the document claims)
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/ParserWorker.java (the five code constants, the two system-property names and their defaults the document claims)
    - .planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-01-SUMMARY.md (the GitHub issue number and the branch name the document references)
  </read_first>
  <action>
    Write the one text Phase 102 and the BASIS reviewers both read (D-18).

    Copy the "## MR description" section of this plan verbatim into
    `/home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md`,
    dropping only that heading line itself, and substitute every bracketed placeholder with the
    real value read from the committed source and from plan 01's SUMMARY. Before writing, check
    each field name, each numeric code, each property name and each default against the actual
    Java — if any of them drifted during plans 02 and 03, the source wins and this document is
    corrected to match, with the drift noted in the plan SUMMARY. The document is the only place
    in this repository where the contract is stated, and it contains no BBj source text.

    Then attempt the push:
    `git -C /home/coder/repos/bbj-ls push -u origin <branch>`. This is the one step whose
    prerequisite has never been exercised on this container.
    - If it succeeds, record the pushed ref and move to task 4.
    - If it fails with an authentication, host-key or connection error against
      `git.basis.cloud:10022`, do NOT retry in a loop and do NOT attempt to create or install an
      SSH key. Record the exact error text in the plan SUMMARY and move to task 4, which is the
      gate for a human to finish it. `glab` is not installed on this machine, so a merge request
      cannot be opened from the command line regardless of whether the push works.

    Commit the new document in THIS repository (`git -C /home/coder/repos/bbj-language-server add`
    with the exact path). It is the only file this repository gains from Phase 101 besides the
    plan and summary artifacts.
  </action>
  <verify>
    <automated>grep -c 'parseProgram' /home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -E '\-3300[1-5]' /home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md</automated>
    <fails_when>the printed count is less than 5 — all five application codes must be documented</fails_when>
    <automated>! grep -nE '\[[a-z-]+\]' /home/coder/repos/bbj-language-server/.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md</automated>
    <fails_when>exit status non-zero, i.e. grep matched — a bracketed placeholder was left unsubstituted</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server status --porcelain -- /home/coder/repos/bbj-language-server/bbj-vscode /home/coder/repos/bbj-language-server/bbj-intellij /home/coder/repos/bbj-language-server/java-interop /home/coder/repos/bbj-language-server/examples</automated>
    <fails_when>the output is non-empty — this phase changes no source file in this repository</fails_when>
  </verify>
  <acceptance_criteria>
    - `101-MR-DESCRIPTION.md` names the method `parseProgram` and lists all five `ParseProgramParams` field names and both `ParseProgramResult` field names
    - `101-MR-DESCRIPTION.md` lists all six `ParseError` field names and states that `categories` is a list
    - `101-MR-DESCRIPTION.md` contains `bbj.interop.parse.timeoutMs` and `bbj.interop.parse.maxBytes` with their defaults
    - `101-MR-DESCRIPTION.md` contains the words `RequestCancelled` and `MethodNotFound`
    - `101-MR-DESCRIPTION.md` contains no bracketed placeholder and no BBj source text
    - The plan SUMMARY records either the pushed ref name or the exact push error text
  </acceptance_criteria>
  <done>The contract is written down, checked against the committed Java, and committed to this repository; the branch push has been attempted once and its outcome recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Open the merge request on BASIS GitLab</name>
  <action>Finish delivery on BASIS GitLab: make sure the branch is on `git.basis.cloud` and open the merge request against `develop`</action>
  <instructions>
    Everything that could be automated has been: the branch exists in `/home/coder/repos/bbj-ls`
    with the full endpoint on it, the suite is green against a live BBjServices, the older-server
    probe was replayed against the real 26.02 jar, and the merge-request description is written and
    committed at
    `.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md`.

    Two things need you, because `glab` is not installed here and SSH access from this container to
    `git.basis.cloud:10022` has never been exercised:

    1. If the push failed (the plan SUMMARY records the exact error), push the branch from a
       machine whose SSH key is authorised, or authorise a key for this one. The remote is
       `ssh://git@git.basis.cloud:10022/BASIS/bbj-ls.git` and the branch name is in the SUMMARY.
    2. Open a merge request in BASIS/bbj-ls targeting `develop` and paste the contents of
       `101-MR-DESCRIPTION.md` as the description. Do not edit it down — Phase 102's research reads
       it as the contract.

    Merging is the BASIS maintainers' call and does not block Phase 102; an open merge request is
    enough to call this phase done.
  </instructions>
  <verification>Once you say the merge request is open, the merge-request URL (or "MR not yet opened — awaiting SSH access") is recorded in this plan's SUMMARY, together with the branch name and the commit SHAs.</verification>
  <resume-signal>Reply with the merge-request URL, or "pushed, MR pending", or "blocked — no SSH access"</resume-signal>
</task>

</tasks>

## MR description

*(Task 3 copies everything below this heading, drops this heading line, and substitutes each
bracketed placeholder with the value read from the committed source.)*

### Add a `parseProgram` request to the language-server interop service

Closes nothing on its own; it is the server half of live compiler diagnostics in the BBj VS Code
and IntelliJ extensions. Tracking issue: BBx-Kitchen/bbj-language-server#[issue-number].

#### What this adds

One new JSON-RPC request on the existing per-connection `InteropService`, beside `getClassInfo`
and `getAllClassNames`, on the same socket and the same launcher. It runs BBj's own parser over
document text supplied by the caller and returns the parser's errors. No other request changes.

The method name is the bare Java method name, `parseProgram`, exactly as every existing request in
this service is named. A `bbj-ls` build that predates this change answers the same call with
lsp4j's standard `MethodNotFound` (-32601), which is how the language server detects an older
BBjServices — once per connection, by probing, never by comparing version strings. No capability
request and no version query is added.

#### Request — `ParseProgramParams`

| Field | Type | Meaning |
|---|---|---|
| `text` | `String` | The full current text of the active document, including unsaved edits. |
| `canonicalName` | `String` | The document's path as the language server knows it. It is also the name the prefix algorithm answers for, so the active document resolves to the supplied text rather than to whatever is on disk. Matched by exact string equality — not case-folded, not Unicode-normalized. |
| `version` | `String` | Opaque to the server and echoed back unchanged. The server never interprets, compares or de-duplicates on it. |
| `prefixes` | `List<String>` | The PREFIX directories the caller wants referenced programs resolved through. May be empty. |
| `workspaceRoots` | `List<String>` | The workspace roots the caller wants referenced programs resolved through. May be empty. |

There is no type-checking flag and no password field. Type checking is off on this path, and this
endpoint does not open protected programs.

#### Result — `ParseProgramResult`

| Field | Type | Meaning |
|---|---|---|
| `version` | `String` | The request's own token, unchanged. |
| `errors` | `List<ParseError>` | The parser's errors, in the parser's own order. Always present; empty on a clean parse, never null. |

#### `ParseError`

| Field | Type | Meaning |
|---|---|---|
| `categories` | `List<String>` | BBj's own error-type strings for this error. This is a **list**, not a single value: one error line can legitimately carry several categories at once. On this path the observed values are `SyntaxError`, `LineNumberError`, `LineOrderError`, `DuplicateDeclarationError`, `ClassError` and `BadUseDeclarationError`. A type-check category cannot appear because type checking is off. The service passes through whatever the parser emits and validates nothing against this list. |
| `message` | `String` | The parser's own message, unchanged. |
| `editorStartLine` | `int` | BBj's editor starting line, verbatim. |
| `editorEndLine` | `int` | BBj's editor ending line, verbatim. |
| `startCharacter` | `int` | BBj's starting character position, verbatim. |
| `endCharacter` | `int` | BBj's ending character position, verbatim. |

**Coordinate convention.** These are the parser's editor-coordinate fields passed through
untouched: one-based lines and one-based character positions, and for the whole-line fallback case
the range spans the reconstructed line, starting at character position 1. The service does not
clamp, convert, zero-base or re-count anything, and it deliberately does not carry the interpreter
line fields. Converting to an editor's own zero-based range is the caller's job, and is where the
continuation-line, user-line-number, CRLF and no-final-newline cases are handled.

#### Referenced programs

The active document is parsed from the supplied text through an in-memory stream — it is never
read from or written to disk, and no temporary file is created. Programs it `USE`s or `CALL`s are
resolved by a prefix algorithm implemented in this service, in this order: an absolute path is
taken as-is; otherwise the active document's own directory, then each workspace root in the order
supplied, then each PREFIX entry in the order supplied; first existing regular file wins.
Referenced programs are read from disk as last saved — there is no overlay of other unsaved
editor buffers.

`config.bbx` is never read and `setConfig` is never called, so global-scope USE declarations from a
configuration file are invisible to this path. That is consistent with type checking being off,
since those declarations are only consumed by type resolution.

A reference that resolves nowhere is reported by BBj as its own error on the referencing line and
returned as an ordinary entry in `errors`. The service neither suppresses nor re-categorises it;
whether an IDE shows it as an error or a warning is the client's policy.

#### Versioning and supersession

Every request builds a fresh program-source UUID, which is what makes the parser re-parse rather
than serve its cached AST for the same canonical name.

Requests are latest-wins per connection and canonical name. A newer request for a document
supersedes an older one: a queued older request is never parsed at all, and an older request that
had already started finishes but its result is discarded. Either way the superseded caller
receives a JSON-RPC error with lsp4j's standard `RequestCancelled` code — never an errors list.
This holds even when the two requests carry identical version tokens and identical text; the
service never merges or short-circuits on equality.

#### Threading

One daemon worker thread per connection, torn down when the connection closes. All of one
connection's parses are serialised on it; different connections parse in parallel. This is
required, not merely tidy: the program factory and its type resolver are backed by plain
unsynchronised maps, and lsp4j's reader loop can dispatch a second request on one connection while
the first is still pending.

The program factory is built **once per connection** and reused for every request on it. Rebuilding
it per request would be a correctness bug, not just a cost: the parser's AST cache is keyed on
canonical name and relies on a long-lived per-instance record of the last source identity seen for
that name in order to invalidate itself, so a freshly built factory that has never seen a name can
serve a stale program another connection left in the shared cache.

**Known inherited limitation.** That shared program cache is process-wide and keyed on canonical
name alone. Two different connections parsing the *same* canonical name concurrently each keep
their own identity record and can therefore still race on it. This is a property of the parser API
rather than of this service, and it is not worked around here — synthesising a per-connection
canonical name would break the very name that reference resolution depends on. Flagging it so it
is a known quantity for reviewers and for the language-server side.

#### Failures are never syntax errors

Every failure travels as a JSON-RPC `ResponseError`, never as an entry in `errors`. The codes sit
outside the JSON-RPC reserved band (-32768 to -32000) and outside lsp4j's LSP reserved band
(-32899 to -32800):

| Code | Condition |
|---|---|
| -33001 | The parser threw while loading or serialising the program. |
| -33002 | The parse exceeded the configured timeout, or arrived while a previous parse on this connection was still overrunning. |
| -33003 | The request exceeded the configured size cap — either the document's byte length or the combined number of prefix and workspace-root entries. |
| -33004 | The parser service could not be obtained, or a required class was missing at runtime. |
| -33005 | The program is protected; this endpoint carries no password. |

Each failure is logged exactly once at `WARNING` with the peer address, the code and the message.
No stack trace, and the document text never appears in a log line.

#### Guards

| System property | Default | Effect |
|---|---|---|
| `bbj.interop.parse.timeoutMs` | [timeout-default] | How long a caller waits for one parse before receiving -33002. |
| `bbj.interop.parse.maxBytes` | [maxbytes-default] | Largest accepted document, in UTF-8 bytes; over it the request is rejected with -33003 before it reaches the worker. |

Both follow the existing `-Dbbj.interop.verbose` convention.

A note on the timeout: the parser exposes no cancellation hook and its scan-and-parse loop does not
check the interrupt flag, so a timeout can only stop the *caller* waiting — it cannot stop the
work. Rather than build a second worker thread (which would need a second factory, reintroducing
the staleness problem above), a connection whose parse is overrunning fails further requests fast
with -33002 until that parse finishes on its own. The size cap is therefore the load-bearing guard,
and the timeout is a liveness guarantee for the caller.

#### Trust boundary

`parseProgram` joins an existing, unauthenticated localhost socket whose current requests already
expose the full BBj classpath. The paths it opens — the canonical name, the prefix entries and the
workspace roots — are supplied by the language server, which is a process on the same machine that
already reads those same files directly today. No new privilege boundary is crossed, and no
sandboxing is added for that reason; flagging it explicitly so the decision is visible rather than
implied.

#### Build

`ParserServiceAPI` is added as a `provided` dependency. It and `BBjStartup` are not on a public
registry; the README now documents installing both into the local Maven repository straight from a
BBj installation with `mvn install:install-file`, so the project builds with no BASIS Nexus
credentials. No Surefire configuration was added — Maven's default binding already runs JUnit 5.

#### Tests

This adds the first tests to the repository, under `src/test/java/bbj/interop`:
`ParseProgramIntegrationTest` drives a real lsp4j client against a running BBjServices on
127.0.0.1:5008 and covers syntax errors with positions, a clean program, an empty document, two
errors on different lines, a reference resolved through a workspace root, a reference resolved
through a PREFIX entry, a missing reference, supersession in both interleavings and with identical
tokens, and the size-cap code. It skips itself when the port is unreachable, unless
`bbj.interop.it.requireServer=true` is set. `MethodNotFoundProbeTest` needs no BBjServices at all:
it wires two in-process launchers together over pipes and pins that a service without the method
answers -32601 inside a bounded wait and writes nothing to standard error. `ParseGuardsTest`
covers the size cap and the code constants in process.

The older-server behaviour was additionally replayed by hand against the original pre-endpoint
`bbj-ls.jar` running under BBjServices: [replay-observation].

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor process → 127.0.0.1:5008 | The full endpoint is now reachable; this plan exercises it as an untrusted-shaped caller would. |
| build machine → `/opt/bbx/.lib/bbjls/` | The replay swaps two different jars in and out of the directory BBjServices loads wholesale. |
| this repository → BASIS GitLab | The contract document and the branch leave this machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-07 | Tampering | `/opt/bbx/.lib/bbjls/` during the down-swap and up-swap | high | mitigate | Task 2 moves the new build to a scratch path outside the load directory before restoring the 26.02 jar, and a verify command asserts the directory holds exactly two files and that the deployed jar is not byte-identical to the backup when the task ends. |
| T-101-01 | Denial of Service | the live size-cap scenario | high | mitigate | Task 1 sends an over-cap document on purpose and asserts it is rejected with -33003 rather than parsed — the guard is exercised, not assumed. |
| T-101-08 | Information Disclosure | `101-MR-DESCRIPTION.md`, the merge request, the SUMMARY | medium | mitigate | D-19: the document names interfaces, methods, JSON keys and numbers only; task 3's verify greps it for unsubstituted placeholders and the register check covers the branch diff. The document is reviewed against the committed Java before it is written. |
| T-101-14 | Elevation of Privilege | the SSH push to `git.basis.cloud` | medium | mitigate | Task 3 attempts the push once and, on any authentication or host-key error, stops and escalates. It never creates or installs an SSH key, never disables host-key checking, and never retries in a loop. |
</threat_model>

## Artifacts this phase produces

Java classes in `/home/coder/repos/bbj-ls/src/main/java/bbj/interop/`:
- `data/ParseProgramParams` (plan 01) — `text`, `canonicalName`, `version`, `prefixes`, `workspaceRoots`
- `data/ParseProgramResult` (plan 01) — `version`, `errors`
- `data/ParseError` (plan 01) — `categories`, `message`, `editorStartLine`, `editorEndLine`, `startCharacter`, `endCharacter`
- `BbjPrefixAlgorithm` (plan 02) — the connection-scoped `PrefixAlgorithmIF` implementation
- `ParserWorker` (plan 02, extended in plan 03) — per-connection parse execution, latest-wins queue, guards

Modified Java classes:
- `InteropService` (plans 01-03) — gains the `parseProgram` `@JsonRequest` method, the per-connection worker field, the size-cap check and a remote-address constructor
- `LanguageService` (plan 03) — gains connection-close teardown for the per-connection worker and the remote-address plumb

Test classes in `/home/coder/repos/bbj-ls/src/test/java/bbj/interop/`:
- `BBjServicesAvailability` (plan 01) — reachability gate + `bbj.interop.it.requireServer` forcing property
- `ParseProgramIntegrationTest` (plans 01, 02, 04) — the live-socket integration suite, including the legacy-server probe method gated by `bbj.interop.it.legacyServer`
- `MethodNotFoundProbeTest` (plan 01) — in-process older-server probe
- `ParseGuardsTest` (plan 03) — size-cap and error-code unit checks

JSON-RPC surface:
- method `parseProgram` (bare name, derived from the Java method name)

System properties:
- `bbj.interop.parse.timeoutMs` (default 10000) — plan 03
- `bbj.interop.parse.maxBytes` (default 4194304) — plan 03
- `bbj.interop.it.requireServer` (test-only, default unset) — plan 01
- `bbj.interop.it.legacyServer` (test-only, default unset) — plan 04

Application error codes (plan 03), all outside JSON-RPC's reserved `-32768..-32000` band and
outside lsp4j's LSP band `-32899..-32800`:
- `-33001` parse failed (the parser threw)
- `-33002` parse timed out, or a previous parse on this connection is still overrunning
- `-33003` document too large, or too many prefix/workspace-root entries
- `-33004` parser service unavailable or a BBj class is missing
- `-33005` program is protected
- supersession uses lsp4j's own `ResponseErrorCode.RequestCancelled`

Build and delivery:
- `pom.xml` gains `com.basis:ParserServiceAPI:${bbj.version}` at `provided` scope (plan 01) and,
  only if the build needs it, `com.google.code.gson:gson` at `provided` scope (plan 02)
- `README.md` gains the offline `install:install-file` instructions (plan 01)
- a GitHub issue in `BBx-Kitchen/bbj-language-server` (plan 01)
- the `bbj-ls` branch `feat/<issue>-parse-program-endpoint` off `develop` (plan 01)
- `.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-MR-DESCRIPTION.md` and the BASIS GitLab
  merge request targeting `develop` (plan 04)

<verification>
- `mvn -f /home/coder/repos/bbj-ls/pom.xml test -Dbbj.interop.it.requireServer=true` is green with
  0 failures, 0 errors and exactly 1 skip (the legacy-server probe).
- The older-server replay's evidence in the SUMMARY is captured output — an error code, a measured
  time, and the log lines gained or "none" — not a derived sequence.
- `/opt/bbx/.lib/bbjls/` holds exactly two jars and the deployed one is not the 26.02 backup.
- `git -C /home/coder/repos/bbj-language-server status --porcelain` shows changes under
  `.planning/` only; `bbj-vscode/`, `bbj-intellij/`, `java-interop/` and `examples/` are untouched.
- The merge request URL, or an explicit "MR not yet opened — awaiting SSH access", is recorded in
  the SUMMARY together with the branch name and the commit SHAs.
</verification>

<success_criteria>
- A plain client against the locally running BBjServices exercises every behaviour the phase's four
  criteria name, and the suite is green.
- A real pre-endpoint `bbj-ls.jar` answers the same probe with MethodNotFound, quickly, with no
  stack trace in the service log — observed, with the output recorded.
- The three pre-existing interop requests still answer after the jar is swapped back.
- The contract is written down once, matches the committed Java, and is on a merge request (or
  explicitly recorded as awaiting SSH access).
</success_criteria>

<output>
Create `.planning/phases/101-bbj-parser-endpoint-in-bbj-ls/101-04-SUMMARY.md` when done. Because
the code lives in another repository, the SUMMARY must record: the `bbj-ls` branch name, every
commit SHA on it across plans 01-04, the GitHub issue number and URL, the merge request URL (or
"MR not yet opened — awaiting SSH access" plus the exact push error), the final test counts, the
captured legacy-probe output, and whether the live timeout scenario ran or was skipped and why.
</output>
