---
phase: 102-live-compiler-diagnostics-with-backward-compatibility
plan: 03
type: execute
wave: 3
depends_on: [102-01, 102-02]
files_modified:
  - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts
  - /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md
  - /home/coder/repos/bbj-language-server/documentation/docs/vscode/index.md
  - /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md
  - /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md
  - /home/coder/repos/bbj-language-server/documentation/docs/intellij/index.md
  - /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md
autonomous: true
requirements: [PSRV-05, PSRV-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The coordinate convention the hand-written fixtures encode is confirmed once against the real endpoint: the same four invented documents sent to the running BBjServices produce converted editor ranges on the expected lines (PSRV-05, D-15)."
    - "The live confirmation test is skipped, not failed, when BBjServices is unreachable or the gate variable is unset, so the default suite and CI stay hermetic (D-15)."
    - "The published documentation of both extensions states that live compiler diagnostics need BBj 26.03 or later, while keeping BBj 25.00 as the base prerequisite (PSRV-09, D-13)."
    - "Both extensions' feature pages describe live compiler diagnostics: what they do, that they follow the existing compiler trigger setting, and that an older BBj simply keeps the save-time compiler check (D-13)."
    - "No new configuration-page section, no new setting and no new IDE surface is documented, because none was added (D-01, D-13)."
  artifacts:
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts
  key_links:
    - "The live test reaches the endpoint through `services.BBj.java.JavaInteropService.parseProgram(...)` after `setConnectionConfig('127.0.0.1', 5008)`, and converts with the same exported converter the hermetic fixtures use — one conversion implementation, confirmed against the real parser."
    - "The gate is the existing `shouldRunBBjTests()` helper plus `test.runIf(run)`, the same pair the other real-interop suites in `test/functional/` already use."
    - "Both `getting-started.md` prerequisite lists, both `index.md` requirement lists and both `features.md` validation sections are edited in the same shape, so the two guides stay parallel."
  prohibitions:
    - statement: "The live confirmation test never drives a full document build against the running service — it calls the request method directly, because a build reaches the compiler and interop paths and is flaky locally and red on CI."
      status: unverified
      verification: flagged
    - statement: "No BBj source text, corpus file or proprietary sample enters the test fixtures or the documentation — every fixture program is invented and minimal."
      status: unverified
      verification: flagged
    - statement: "The documentation never promises a status surface, a notification or a setting that this phase did not add."
      status: unverified
      verification: flagged
---

<objective>
Confirm the coordinate convention once against the real parser, and tell users what the feature
needs.

Purpose: the hermetic fixture suite in plan 02 encodes coordinates measured during research. One
gated test sends the same four invented documents to the running endpoint so the convention is
re-confirmed on every `RUN_BBJ_TESTS` run rather than trusted from a research note — and the two
published guides say plainly that the feature needs BBj 26.03 or later, which is the documentation
half of PSRV-09.
Output: `test/functional/parse-program-live.test.ts`, and six edited documentation pages.
</objective>

<executor_shell_rules>
Read this before running anything. This repository's permission hooks block opaque shell reads and
cannot tell a scoped search from a secret read; a blocked command stalls a background executor on an
unanswered prompt.

- Prefer the Read/Glob tools for reading and finding files. For content search use `grep` with an
  absolute path and an exact file or an `--include` filter.
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. `cd` is allowed only
  directly in front of `npx vitest` or `npm`; use `git -C /home/coder/repos/bbj-language-server …`
  for every git command.
- No bare recursive `grep -r` or `find` over a directory.
- Never read `.env*`, `*.pem`, `*.key` or any credential store.
- `git add <exact path>` only — never `-A`, never `.`. Never `git stash`.
- vitest must run with `bbj-vscode` as the working directory. Never pass vitest a reporter named `basic` — no such reporter exists in vitest 4 and the run dies on the flag.
- No planning identifiers in source, tests, doc pages or commit messages. GitHub issue numbers are
  fine.
</executor_shell_rules>

<!-- planner-discipline-allow: PSRV-0|D-[0-9][0-9]|102-[0-9][0-9]|CR-[0-9] -->

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-CONTEXT.md
@.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-RESEARCH.md
@.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-PATTERNS.md
@.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-01-SUMMARY.md
@.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol and file this phase creates. Plan 03 owns those marked (03).

**New files**
- `bbj-vscode/src/language/bbj-parser-service.ts` (01)
- `bbj-vscode/test/bbj-parser-service.test.ts` (01)
- `bbj-vscode/test/parser-coordinate-converter.test.ts` (02)
- `bbj-vscode/test/functional/parse-program-live.test.ts` (03)

**New exported symbols in `bbj-parser-service.ts`** — `BBJ_PARSER_SOURCE`, `BBjParserService`
(`isEnabled`, `requestLiveParse`), `BBjParserServiceContext`, `parseErrorToRange`,
`parseErrorsToDiagnostics` (01, refined by 02)

**New exported symbols in `java-interop.ts`** — `ParseProgramParams`, `ParseProgramResult`,
`ParseError`, `JavaInteropService.parseProgram()`, `JavaInteropService.connectionGeneration` (01)

**New exported symbol in `bbj-document-validator.ts`** — `getMaxErrors()` (02)

**New test seams in `bbj-test-module.ts`** — exported `JavaInteropTestService` with
`parseProgram()`, `scriptParseProgram()`, `simulateReconnect()` (01)

**Modified, no new symbol** — `bbj-module.ts` registration (01), `bbj-document-builder.ts` debounce
tail and trigger-off clear (01), six documentation pages under
`documentation/docs/{vscode,intellij}/` (03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The real parser confirms the convention once</name>
  <precondition>BBjServices is running and a TCP connect to 127.0.0.1:5008 succeeds, and the jar under /opt/bbx/.lib/bbjls/ is the build that declares the parse request (the deployed phase-101 jar, 36620 bytes, not the 23389-byte pre-endpoint backup in /opt/bbx/.lib/bbjls-backup/).</precondition>
  <files>/home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/issue447-real-interop.test.ts — the exact shape to mirror: `const run = await shouldRunBBjTests()` at the top of an async `describe`, `setConnectionConfig('127.0.0.1', 5008)` and `initializeWorkspace(services.shared)` in a long-timeout `beforeAll` that returns early when the gate is closed, and `test.runIf(run)(…, …, 60000)`
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-helper.ts — `shouldRunBBjTests()` and `isPortOpen()`: how the gate resolves when the environment variable is unset, set to 1 and set to 0
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts — `parseProgram()` and the three DTO interfaces plan 01 added, and `setConnectionConfig`
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-parser-service.ts — the exported converter functions this test reuses rather than re-implementing
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/parser-coordinate-converter.test.ts — the four fixture documents plan 02 wrote; this test sends the same four texts, so keep them recognisably the same programs
    - .planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-RESEARCH.md — the Live Probe Evidence section: the four measured records, and the note that the endpoint is a shared development resource not to be probed exhaustively
  </read_first>
  <action>
    Create `bbj-vscode/test/functional/parse-program-live.test.ts` as a gated confirmation, not a
    second test suite for the converter.

    Structure it exactly like the existing real-interop suite in the same folder: an async `describe`
    whose first statement resolves the gate with `shouldRunBBjTests()`, a `services` built from
    `createBBjServices(NodeFileSystem)`, a `beforeAll` with a 120000 ms timeout that returns
    immediately when the gate is closed and otherwise calls `setConnectionConfig('127.0.0.1', 5008)`
    and `initializeWorkspace(services.shared)`, and tests declared with `test.runIf(run)(…)` and a
    60000 ms timeout each. When the gate is closed the file must contribute skipped tests, never a
    failure and never a connection attempt.

    One test per PSRV-05 document shape, four in total, each sending the same invented text plan 02's
    fixtures use — the colon-continued statement, the line-numbered program, the CRLF variant, and
    the two-line program whose last line is unterminated. Build the request with `text` set to the
    fixture string, `canonicalName` set to a path under the OS temp directory ending in `.bbj`,
    `version` set to a distinctive string, and `prefixes` and `workspaceRoots` set to empty arrays.
    Always send a non-null `canonicalName` (D-16): the deployed jar is the reviewed build and must not be
    relied on to tolerate a null there.

    Assert the shape, not the arithmetic: the result's `version` equals the sent token; `errors` is a
    non-empty array for each of the four (each fixture carries one deliberate error); and, after
    running each returned record through the exported converter with the fixture's own line count,
    the converted range's start line is the expected zero-based line and its end character is the
    end-of-line sentinel. Never assert the reported end character's raw value — it was measured
    exceeding the anchor line's true length in three of four cases, so such an assertion would be
    green against one BBj build and red against the next.

    Add one clean-program test: an invented two-line valid program returns an empty `errors` array —
    the negative control that proves the four error assertions are not passing on a service that
    reports errors for everything.

    Keep every fixture inline and invented. Do not add a `.bbj` file anywhere under
    `bbj-vscode/test/test-data/`. Do not drive a document build against the live service: call
    `services.BBj.java.JavaInteropService.parseProgram(...)` directly. A build on this path reaches
    the compiler and interop services and is flaky locally and red on CI.

    Run the suite once with the gate open to confirm it passes against the deployed jar, and once with
    the gate closed to confirm it skips rather than fails.

    Commit on the phase branch. Subject names the behaviour; the body ends with the
    `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` trailer and contains no `Closes #` or
    `Fixes #` keyword.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts</automated>
    <fails_when>non-zero exit, or any line contains `failed`, or the summary reports `5 skipped` (the gate did not open against the running service), or the summary reports fewer than `5 passed`</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/functional/parse-program-live.test.ts</automated>
    <fails_when>non-zero exit, or any line contains `failed` — with the gate closed the file must skip, never fail and never connect</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server status --short -- bbj-vscode/test/test-data</automated>
    <fails_when>the command prints any line at all</fails_when>
  </verify>
  <acceptance_criteria>
    - `bbj-vscode/test/functional/parse-program-live.test.ts` exists and contains `shouldRunBBjTests`, `test.runIf(run)` and `setConnectionConfig('127.0.0.1', 5008)`
    - The file contains `parseProgram(` and contains no occurrence of `DocumentBuilder` and no occurrence of `parseHelper`
    - The file imports the converter from `../../src/language/bbj-parser-service.js` rather than re-implementing any coordinate arithmetic
    - The file contains five `test.runIf(` declarations (four error shapes plus the clean-program control)
    - `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts` exits 0 and reports `5 passed`
    - `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/functional/parse-program-live.test.ts` exits 0 and reports `5 skipped`
    - No test name or comment in the file contains a requirement, decision, plan or review identifier
  </acceptance_criteria>
  <done>The four invented PSRV-05 documents and one clean program run against the real endpoint behind the existing gate; the converted ranges land on the expected lines, and with the gate closed the file skips without touching the socket.</done>
</task>

<task type="auto">
  <name>Task 2: The VS Code guide states the version the feature needs</name>
  <files>
    /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md,
    /home/coder/repos/bbj-language-server/documentation/docs/vscode/index.md,
    /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md
  </files>
  <read_first>
    - /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md — the file being modified: the `## Prerequisites` bullet list, whose BBj bullet currently reads `- **BBj** version 25.00 or higher installed`
    - /home/coder/repos/bbj-language-server/documentation/docs/vscode/index.md — the file being modified: the `## Requirements` bullet list, whose BBj bullet currently reads `- **BBj** 25.00 or higher`
    - /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md — the file being modified: the `## Validation and Diagnostics` section, its four-bullet list and the sentence that follows it
    - /home/coder/repos/bbj-language-server/bbj-vscode/package.json — the description text of the compiler trigger setting, so the feature paragraph names the setting exactly as the settings UI does
  </read_first>
  <action>
    Say what the feature needs, in the voice these pages already use, and nothing more (the minimal
    documentation option was the one chosen: a prerequisites line plus a features paragraph, no new
    configuration-page section).

    In `getting-started.md`'s `## Prerequisites` list: keep the existing BBj bullet exactly as it is
    and add one bullet directly beneath it stating that live compiler diagnostics — the compiler's own
    syntax errors appearing while you type — need BBj 26.03 or later, and that an earlier BBj keeps
    the save-time compiler check. The version must appear as the literal `26.03`.

    In `index.md`'s `## Requirements` list: add the same one-liner as a bullet beneath the existing
    BBj bullet, in that list's terser voice. The version must appear as the literal `26.03`.

    In `features.md`: leave the existing `## Validation and Diagnostics` bullet list and its closing
    sentence in place and add a short entry for live compiler diagnostics near that section — as a
    `### Live Compiler Diagnostics` subsection, not a rewrite of the bullet list. It says three
    things, in prose: that with BBj 26.03 or later the BBj compiler's own parser checks the open file
    as you type and its syntax errors appear without saving; that the feature follows the existing
    `bbj.compiler.trigger` setting, so setting that to `off` turns it off along with the save-time
    check; and that with an earlier BBj, or with BBjServices not running, the editor simply keeps the
    save-time compiler check and nothing else changes.

    Do not document a status bar, a notification, a log line, a troubleshooting procedure or a new
    setting — none was added. Do not change the existing BBj 25.00 base prerequisite. Write plain
    user-facing prose: no requirement, decision, plan or review identifier appears anywhere on these
    pages.

    Commit on the phase branch, subject naming the change, trailer as in task 1.
  </action>
  <verify>
    <automated>grep -c '26\.03' /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md /home/coder/repos/bbj-language-server/documentation/docs/vscode/index.md /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md</automated>
    <fails_when>any of the three printed lines ends in `:0`</fails_when>
    <automated>grep -c 'BBj\*\* version 25.00 or higher installed' /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md</automated>
    <fails_when>the printed count is not 1 — the existing base prerequisite was altered rather than added to</fails_when>
    <automated>grep -c 'bbj.compiler.trigger' /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>! grep -nE 'PSRV-0|D-[0-9][0-9]|102-[0-9][0-9]|CR-[0-9]' /home/coder/repos/bbj-language-server/documentation/docs/vscode/getting-started.md /home/coder/repos/bbj-language-server/documentation/docs/vscode/index.md /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md</automated>
    <fails_when>exit status non-zero, i.e. grep found a planning identifier on a published page</fails_when>
    <automated>! grep -niE 'status bar|statusbar|notification|popup' /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md</automated>
    <fails_when>exit status non-zero, i.e. the page promises a surface this phase did not add — read the match and confirm it is not pre-existing text unrelated to this feature before treating it as a failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `documentation/docs/vscode/getting-started.md` contains `26.03` and still contains the unchanged line `- **BBj** version 25.00 or higher installed`
    - `documentation/docs/vscode/index.md` contains `26.03` and still contains the unchanged line `- **BBj** 25.00 or higher`
    - `documentation/docs/vscode/features.md` contains a heading `### Live Compiler Diagnostics`, contains `26.03` and contains `bbj.compiler.trigger`
    - `documentation/docs/vscode/features.md` still contains the heading `## Validation and Diagnostics` and its four existing bullets
    - `git -C /home/coder/repos/bbj-language-server diff --stat -- documentation/docs/vscode` shows three changed files and no file with more deletions than insertions
  </acceptance_criteria>
  <done>A VS Code user reading the published guide learns that live compiler diagnostics need BBj 26.03 or later, that they follow the existing compiler trigger setting, and that an older BBj keeps the save-time check.</done>
</task>

<task type="auto">
  <name>Task 3: The IntelliJ guide says the same thing</name>
  <files>
    /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md,
    /home/coder/repos/bbj-language-server/documentation/docs/intellij/index.md,
    /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md
  </files>
  <read_first>
    - /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md — the file being modified: the `## Prerequisites` bullet list, whose BBj bullet currently reads `- **BBj** version 25.00 or higher installed`
    - /home/coder/repos/bbj-language-server/documentation/docs/intellij/index.md — the file being modified: the `## Requirements` bullet list, whose BBj bullet currently reads `- **BBj** 25.00 or higher`
    - /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md — the file being modified: the `## Validation and Diagnostics` section and the separate `## Java Interop` section further down, which already documents the BBjServices dependency
    - /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md — the VS Code page as task 2 left it, so the two guides say the same thing in each one's own voice
  </read_first>
  <action>
    Mirror task 2 into the IntelliJ guide. The plugin consumes the same language server, so the user-
    facing behaviour is identical and the wording should be recognisably parallel — adapted to this
    guide's voice, not copied verbatim where the surrounding text differs.

    `getting-started.md`'s `## Prerequisites` and `index.md`'s `## Requirements` each get the same
    one-line addition beneath the existing BBj bullet, naming the literal `26.03` and saying that an
    earlier BBj keeps the save-time compiler check. Keep the existing BBj 25.00 bullets untouched.

    `features.md` gets the same short `### Live Compiler Diagnostics` subsection near
    `## Validation and Diagnostics` — what it does, that it follows the `bbj.compiler.trigger`
    setting, that an older BBj or a stopped BBjServices keeps the save-time check. Put it near the
    validation section, not inside the `## Java Interop` section: it is a diagnostics feature that
    happens to use the same service, and the Java Interop section documents a different capability.

    Do not document a status bar, a notification, a log line, a troubleshooting procedure or a new
    setting. No planning identifier appears on these pages.

    Commit on the phase branch, subject naming the change, trailer as in task 1.
  </action>
  <verify>
    <automated>grep -c '26\.03' /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md /home/coder/repos/bbj-language-server/documentation/docs/intellij/index.md /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md</automated>
    <fails_when>any of the three printed lines ends in `:0`</fails_when>
    <automated>grep -c 'BBj\*\* version 25.00 or higher installed' /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md</automated>
    <fails_when>the printed count is not 1 — the existing base prerequisite was altered rather than added to</fails_when>
    <automated>grep -c 'Live Compiler Diagnostics' /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md /home/coder/repos/bbj-language-server/documentation/docs/vscode/features.md</automated>
    <fails_when>either printed line ends in `:0` — the two guides are out of step</fails_when>
    <automated>! grep -nE 'PSRV-0|D-[0-9][0-9]|102-[0-9][0-9]|CR-[0-9]' /home/coder/repos/bbj-language-server/documentation/docs/intellij/getting-started.md /home/coder/repos/bbj-language-server/documentation/docs/intellij/index.md /home/coder/repos/bbj-language-server/documentation/docs/intellij/features.md</automated>
    <fails_when>exit status non-zero, i.e. grep found a planning identifier on a published page</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server status --short -- bbj-intellij</automated>
    <fails_when>the command prints any line at all — this phase changes no IntelliJ plugin source</fails_when>
  </verify>
  <acceptance_criteria>
    - `documentation/docs/intellij/getting-started.md` contains `26.03` and still contains the unchanged line `- **BBj** version 25.00 or higher installed`
    - `documentation/docs/intellij/index.md` contains `26.03` and still contains the unchanged line `- **BBj** 25.00 or higher`
    - `documentation/docs/intellij/features.md` contains a heading `### Live Compiler Diagnostics` and contains `bbj.compiler.trigger`
    - The `### Live Compiler Diagnostics` heading in `documentation/docs/intellij/features.md` appears at a line number lower than the `## Java Interop` heading in the same file
    - `git -C /home/coder/repos/bbj-language-server status --short -- bbj-intellij` prints nothing
    - `git -C /home/coder/repos/bbj-language-server diff --stat -- documentation/docs/intellij` shows three changed files and no file with more deletions than insertions
  </acceptance_criteria>
  <done>An IntelliJ user reading the published guide learns the same three things a VS Code user does, and no plugin source changed to make it true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture text → the running BBjServices | The gated test sends invented BBj text over the localhost socket to the shared development service. The text must be invented, never corpus or proprietary sample code. |
| the published documentation → users | The two guides are public; anything written there is a promise about what the extensions do and what they need. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-08 | Information Disclosure | `test/functional/parse-program-live.test.ts` fixtures and the six documentation pages | medium | mitigate | Every fixture is an invented, minimal BBj program written for this test; no corpus file, no BBj source text and no customer sample is used, and the documentation describes behaviour only. The standing constraint that no proprietary BBj source text enters this public repository applies to both. Tasks 1-3. |
| T-102-09 | Tampering | the published version claim | low | mitigate | The version the pages promise is asserted as the literal `26.03` by a grep over all six pages, so a typo or a half-applied edit fails the task rather than shipping a wrong prerequisite. Tasks 2-3. |
| T-102-10 | Denial of Service | the shared BBjServices on 127.0.0.1:5008 | low | accept | The gated test sends five small requests per run and is skipped by default; the service is a shared development resource, which is why the suite confirms the convention rather than probing it exhaustively. No rate limiting is added. |
| T-102-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase; every dependency used is already declared and already imported elsewhere in this package. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts` — five passing.
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/functional/parse-program-live.test.ts` — five skipped when the gate is closed and the port is unreachable, zero failures.
- `grep -c '26\.03'` over all six documentation pages — every line non-zero.
- `git -C /home/coder/repos/bbj-language-server status --short -- bbj-intellij` — empty.
</verification>

<success_criteria>
- The coordinate convention is confirmed against the real endpoint for all four PSRV-05 document
  shapes plus a clean-program control, behind the existing gate.
- With the gate closed the new suite skips and never opens a socket.
- Both published guides state that live compiler diagnostics need BBj 26.03 or later, keep BBj 25.00
  as the base prerequisite, and describe the feature, its trigger setting and its older-BBj
  behaviour.
- No IntelliJ plugin source changed.
</success_criteria>

<output>
Create `.planning/phases/102-live-compiler-diagnostics-with-backward-compatibility/102-03-SUMMARY.md` when done.
</output>
