---
phase: 91-language-server-responsiveness
plan: 06
type: execute
wave: 3
depends_on: ["91-01", "91-02", "91-03", "91-04", "91-05"]
files_modified: []
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, RESP-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The VS Code VSIX and the IntelliJ plugin zip, both rebuilt from the final tree, each carry this phase's language server: the breaker's short-circuit message is present in the bundled `main.cjs` of both (D-14)."
    - "The whole plain `npm test` suite, run with `RUN_BBJ_TESTS=0`, reports zero failed tests. No test added in this phase is gated on reaching the live interop service, lint passes, and no planning register id appears in any source or test line added since the phase base (D-13)."
    - "No file under `bbj-intellij/` changed in this phase (D-05)."
    - statement: "In VS Code, with the rebuilt VSIX installed and the IntelliJ zip rebuilt from the same tree, open a BBj file that uses Java classes. Stopping BBjServices and adding lines with unresolved Java classes shows exactly one error popup and no multi-second stall. After BBjServices restarts, the unresolved-class diagnostics clear on their own with no edit and no Refresh Java Classes; at most a caret move onto an affected class name is needed, because recovery is request-driven (D-14, D-01, D-05)"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT change any file under bbj-vscode/src, bbj-vscode/test or bbj-intellij in this plan; it proves and stages, and the code plans own every behaviour."
      status: resolved
      verification: test
  artifacts: []
  key_links: []
---

<objective>
Close Phase 91 at the level users experience it, for RESP-01 through RESP-04:
- run the whole-suite gate;
- rebuild both distributables from the final tree and prove each carries the new language server;
- stage D-14's live outage-and-recovery check as an end-of-phase human check.

Purpose: the phase is done only when the artifacts a tester installs carry every fix, and a human confirms the outage and recovery behaviour against a real BBjServices.
Output: build evidence in the SUMMARY and one harvested human check.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-language-server-responsiveness/91-CONTEXT.md
@.planning/phases/91-language-server-responsiveness/91-VALIDATION.md
@.planning/phases/91-language-server-responsiveness/91-03-SUMMARY.md
@.planning/phases/91-language-server-responsiveness/91-05-SUMMARY.md

<!-- planner-discipline-allow: RUN_BBJ_TESTS -->
<!-- planner-discipline-allow: shouldRunBBjTests -->

<interfaces>
Verified during planning:

- Build and install, from plans 89-13, 90-08 and the devcontainer:
  - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`.
  - `bbj-ext-install` (`/usr/local/bin/bbj-ext-install`) runs `npx vsce package --no-dependencies --out /tmp/bbj-lang.vsix`, which also runs the minified prepublish bundle and lint. It then installs the VSIX into the `VS Code (ext test)` instance and prints `Extension installed`.
  - The VSIX is a zip whose language server is at `extension/out/language/main.cjs`.
  - `/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --offline --console=plain -q` produces `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`. That zip lists an entry ending in `language-server/main.cjs`, copied from `bbj-vscode/out/language/main.cjs`.
  - Use `clean`: Gradle once judged the zip up to date after a source change.
  - A Swing stack trace during `buildSearchableOptions` is headless noise when the exit code is 0.
- Plan 91-03 introduced the literal `Java interop service unavailable (circuit open)`. Minification keeps string literals, so it is the bundle marker.
- Phase base commit: `174985f7`.
- Whole-suite gate:
  - Pass criterion: the `Tests` summary line reports no failed test.
  - "Test Files … failed" with zero failed tests is `beforeAll` hook-timeout contention, not a failure; run with `--maxWorkers=2`.
  - Known local drift against a live :5008 (linking interop, issue447) does not apply with `RUN_BBJ_TESTS=0`.
- The live check happens in one IDE (D-14), VS Code. Recovery is request-driven (D-01, plan 91-03): the quick-fix lookup VS Code issues when the caret moves onto an unresolved class name carries the half-open probe, and no background timer exists.
- The workflow's human checks live in `<verify><human-check>` blocks and are harvested into UAT, as in plan 90-08.

Shell rules:
- Absolute paths only.
- Use `git -C /home/coder/repos/bbj-language-server …` for git.
- Never chain `cd` with grep/find/cat/sed/head/tail/git.
- Run gradle through the wrapper's absolute path with `-p`.
- Stop any competing Gradle or vitest run before rebuilding.
- Stage exact paths only.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

None. No symbol, source file or tracked file is created. The plan produces build outputs (`/tmp/bbj-lang.vsix`, `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`) and SUMMARY evidence.

## Flagged assumptions

- D-14 says the stale diagnostics clear "on their own, with no edit". Under D-01's request-driven recovery, some request must reach the server after the peer returns. The human check therefore allows a caret move (not an edit) onto an affected class name. If the tester expects recovery with no interaction at all, that needs a background timer, which D-01 rules out. Record the observed behaviour in UAT either way.

<tasks>

<task type="tracer">
  <name>Task 1: The rebuilt VS Code and IntelliJ distributables carry this phase's language server end to end</name>
  <files>none (build outputs only; no tracked file changes)</files>
  <precondition>`bbj-ext-install` is on PATH, and the commits of plans 91-01 through 91-05 are on the current branch</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/91-language-server-responsiveness/91-03-SUMMARY.md — the breaker message and constants as shipped
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts — confirm the literal `Java interop service unavailable (circuit open)` is present
  </read_first>
  <action>
  Record `BASE`: the output of `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`. Then run, in order:

  1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`
  2. `bbj-ext-install`
  3. The VSIX bundle check.
  4. `/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --offline --console=plain -q`
  5. `unzip -l` on the zip.
  6. The zip bundle check.
  7. `sha256sum /tmp/bbj-lang.vsix /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`

  Record in the SUMMARY:
  - `BASE`;
  - both sha256 digests;
  - the zip path;
  - the `language-server/main.cjs` entry timestamp from the listing;
  - the marker counts from both bundle checks.

  Change no tracked file.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
    <automated>bbj-ext-install</automated>
    <fails_when>non-zero exit, or no "Extension installed" line in the output</fails_when>
    <automated>unzip -p /tmp/bbj-lang.vsix extension/out/language/main.cjs | grep -c "Java interop service unavailable (circuit open)"</automated>
    <fails_when>non-zero exit, or a printed count of 0 (the VSIX does not carry the rebuilt server)</fails_when>
    <automated>/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --offline --console=plain -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip '*language-server/main.cjs' | grep -c "Java interop service unavailable (circuit open)"</automated>
    <fails_when>non-zero exit, or a printed count of 0 (the plugin zip does not carry the rebuilt server)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both bundle checks print a count of at least 1.
    - The SUMMARY records `BASE`, both sha256 digests, the zip path, the `main.cjs` entry timestamp and both marker counts.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src bbj-vscode/test bbj-intellij/src` prints nothing.
  </acceptance_criteria>
  <done>A tester installing either artifact gets the language server with the breaker, the in-flight registry, the path-keyed index and per-request completion tokens.</done>
</task>

<task type="auto">
  <name>Task 2: The whole suite, lint and register check are green across the phase, and the live outage-and-recovery check is staged against the rebuilt artifacts</name>
  <files>none (verification only; no tracked file changes)</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/91-language-server-responsiveness/91-VALIDATION.md — the manual-only verification row
    - /home/coder/repos/bbj-language-server/.planning/phases/91-language-server-responsiveness/91-CONTEXT.md — D-13 and D-14
  </read_first>
  <action>
  1. Run the whole plain suite with the interop-gated suites off, and judge it on failed tests only.
     - If a test fails, stop and report it in the SUMMARY; do not fix code in this plan.
     - A hook-timeout "failed suite" with zero failed tests is contention. Rerun once with `--maxWorkers=2` and note it.
  2. Run lint.
  3. Run the phase-wide register check.
  4. Check that the phase added no test gated on reaching the live interop service (D-13), and changed no IntelliJ file (D-05).
  5. The human check below is harvested into UAT. It requires artifacts rebuilt from the final tree: after any code-review fixes land, both distributables are rebuilt before the check runs.
  </action>
  <verify>
    <automated>RUN_BBJ_TESTS=0 npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode test -- --maxWorkers=2</automated>
    <fails_when>the "Tests" summary line reports one or more failed tests ("Test Files … failed" with zero failed tests is beforeAll hook-timeout contention and does not count)</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 174985f7 -- bbj-vscode/src bbj-vscode/test | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|\bG-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text), or non-zero exit</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 174985f7 -- bbj-vscode/test | grep '^+' | grep -nE 'shouldRunBBjTests|RUN_BBJ_TESTS'</automated>
    <fails_when>any line printed (a test added in this phase is gated on the live interop service)</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff 174985f7 --stat -- bbj-intellij</automated>
    <fails_when>any output line (an IntelliJ file changed in this phase)</fails_when>
    <human-check>
      <test>
First rebuild both distributables from the final tree, after any code-review fixes:
- VS Code: `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (writes `/tmp/bbj-lang.vsix` and installs it into VS Code (ext test)).
- IntelliJ: `/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --console=plain -q` (writes `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`).

Then, in VS Code (ext test), reload the window and do the following:
1. With BBjServices running, open a `.bbj` file containing `use java.util.HashMap`, `declare HashMap m!` and `m! = new HashMap()`. Wait until diagnostics settle.
2. Stop BBjServices.
3. Add these lines: `use java.util.concurrent.ConcurrentSkipListMap`, `declare ConcurrentSkipListMap a!`, `use java.util.concurrent.LinkedTransferQueue`, `declare LinkedTransferQueue b!`, `use java.util.concurrent.Phaser`, `declare Phaser c!`. Keep typing and hovering normally for about ten seconds.
4. Start BBjServices and wait until it is fully up. Wait at least 30 more seconds.
5. Without typing anything and without running Refresh Java Classes, move the caret onto `ConcurrentSkipListMap` in the `declare` line. If nothing changes within a few seconds, move it off and back once more.
      </test>
      <expected>
        - Steps 2-3: exactly one "Failed to connect to the Java interop service…" error popup appears during the whole outage. Typing, completion and hover stay responsive, with no multi-second freeze. The three new `declare` lines show unresolved-class diagnostics.
        - Steps 4-5: within about 20 seconds the unresolved-class diagnostics on those lines clear by themselves. No popup or information message appears on recovery, and no edit or Refresh Java Classes was needed.
      </expected>
      <why_human>Stopping and restarting a real BBjServices, watching IDE popups, and judging editor responsiveness need a live IDE and a real interop peer. The automated suite uses a fake peer by design (D-13).</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The whole-suite `Tests` line reports 0 failed tests, and the SUMMARY records the passed and skipped counts.
    - Lint exits 0.
    - The register check, the gated-test check and the IntelliJ diff check all print nothing.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src bbj-vscode/test bbj-intellij/src` prints nothing.
    - The human check is harvested into `91-UAT.md` by the workflow.
  </acceptance_criteria>
  <done>Every automated gate for RESP-01..04 is green on the final tree, and the one live check D-14 requires is staged against artifacts rebuilt from that tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → phase closure claim | unit tests alone cannot show that the installed artifacts carry the fixes |
| rebuilt artifact → tester install | a stale VSIX or zip would make UAT test the old server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-91-19 | Repudiation | phase closed on source-tree tests while the installed bundles still ship the old server | medium | mitigate | bundle marker check inside both the VSIX and the plugin zip; sha256 digests and the `main.cjs` timestamp in the SUMMARY |
| T-91-20 | Tampering | UAT run against artifacts built before code-review fixes | low | mitigate | the human check requires rebuilding both distributables from the final tree first |
| T-91-SC | Tampering | npm/pip/cargo installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. Both bundle marker checks print a count of at least 1.
2. The whole suite reports 0 failed tests with `RUN_BBJ_TESTS=0`, and lint exits 0.
3. The phase-wide register check, the gated-test check and the IntelliJ diff check print nothing.
4. The D-14 human check is harvested into UAT and run against artifacts rebuilt from the final tree.
</verification>

<success_criteria>
- All four ROADMAP success criteria for Phase 91 are backed by green automated tests on the final tree.
- Both distributables carry the new server.
- D-14's live outage-and-recovery check is staged.
</success_criteria>

<output>
Create `.planning/phases/91-language-server-responsiveness/91-06-SUMMARY.md` when done. Record:
- `BASE`, both sha256 digests, the zip path and the `main.cjs` timestamp;
- both marker counts;
- the whole-suite counts;
- any rerun for contention;
- any deviation.
</output>
