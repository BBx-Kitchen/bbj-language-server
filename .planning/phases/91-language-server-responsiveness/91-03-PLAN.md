---
phase: 91-language-server-responsiveness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/language/java-interop.ts
  - bbj-vscode/test/fake-interop-peer.ts
  - bbj-vscode/test/java-interop-breaker.test.ts
autonomous: true
requirements: [RESP-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With the java-interop peer unreachable, 20 distinct class lookups started together settle within 10050 ms of fake time, with exactly one socket attempt. The first connect-level failure opens the breaker, and every later lookup short-circuits without touching the socket (D-01, D-02)."
    - "Each of those lookups returns a stub carrying `error`, and none is stored: `getResolvedClass(name)` is undefined afterwards, so the class resolves normally once the peer is back. A class the backend answered but could not find is still cached as before (D-03)."
    - "Exactly one error popup (`notifyJavaConnectionError`, with its existing text) is sent per outage, on the closed-to-open transition. Failed half-open probes send none, recovery sends none, and a second outage after a recovery sends a second popup (D-05)."
    - "After the cooldown, the next lookup is the single half-open probe, and any concurrent caller short-circuits. Probe success closes the breaker and the class resolves for real with no `clearCache()` call. Probe failure reopens it with the cooldown multiplied by INTEROP_BREAKER_BACKOFF_FACTOR, capped at INTEROP_BREAKER_MAX_COOLDOWN_MS. No background timer exists (D-01)."
    - "`clearCache()` resets the breaker to closed, so the next lookup attempts a socket at once. A connect attempt started before the reset can no longer change breaker state or report recovery (D-01)."
    - "A connected peer that is slow to answer `getClassInfo` does not trip the breaker: the 10 s request timeout returns an uncached stub, and the next lookup still sends its request (D-02, D-03)."
    - "These are classified as transport failures and never cached: the 10 s request timeout, the 30 s chain timeout, a dropped connection (`ConnectionError`, or `ResponseError` code PendingResponseRejected) and a breaker short-circuit. Any other failure keeps today's cached stub (D-03)."
    - "`onConnectionRecovered` listeners fire exactly once per half-open-to-closed transition. They run asynchronously (connect never awaits them) and never fire on `clearCache()` (D-04)."
    - "A class-candidate lookup that the complete class index answers locally still starts the half-open probe once the cooldown has elapsed, without waiting for it. A quick-fix request issued by a caret move after a peer restart can therefore bring recovery without an edit (D-01, D-14)."
    - "`loadImplicitImports()` can run again without `clearCache()` and adds no duplicate entry to the synthetic classpath document's classes (D-04)."
    - "The global resolution lock (`acquireLock`, `drainLockQueue`, `lockQueue`, `lockHeld`, `currentLockToken`) is unchanged. The breaker lives in `connect()` and adds no queue (D-06)."
    - "No file under `bbj-intellij/` changes, and IntelliJ's status-bar interop health probe is neither modified nor consulted (D-05)."
  prohibitions:
    - statement: "MUST NOT show more than one error popup per outage, or any message on recovery."
      status: resolved
      verification: test
    - statement: "MUST NOT start timers or background connections to probe the peer; every probe rides on a lookup that a request made."
      status: resolved
      verification: judgment
    - statement: "MUST NOT permanently mark a class as unresolvable because of a transport failure."
      status: resolved
      verification: test
  artifacts:
    - path: "bbj-vscode/src/language/java-interop.ts"
      provides: "three-state breaker in connect(), InteropTransportError and its classifier, uncached transport stubs, onConnectionRecovered, probeIfDue, re-runnable loadImplicitImports"
      contains: "Java interop service unavailable (circuit open)"
    - path: "bbj-vscode/test/fake-interop-peer.ts"
      provides: "FakePeerInteropService and createFakePeerServices, a scriptable peer behind the real connect path"
      contains: "FakePeerInteropService"
    - path: "bbj-vscode/test/java-interop-breaker.test.ts"
      provides: "fake-timer regression tests for #504"
  key_links:
    - from: "bbj-vscode/src/language/java-interop.ts"
      to: "bbj-vscode/src/language/bbj-notifications.ts"
      via: "connect() calls notifyJavaConnectionError once, on the closed-to-open transition only"
      pattern: "notifyJavaConnectionError\\("
    - from: "bbj-vscode/src/language/java-interop.ts"
      to: "bbj-vscode/src/language/java-interop.ts"
      via: "doResolveClassByName passes the negation of isInteropTransportFailure(e) as createStubClass's cache flag"
      pattern: "createStubClass\\(className, !isInteropTransportFailure"
    - from: "bbj-vscode/test/fake-interop-peer.ts"
      to: "bbj-vscode/src/language/java-interop.ts"
      via: "overrides createSocket() and wrapSocket() only, so the real connect() breaker runs under test"
      pattern: "override (createSocket|wrapSocket)"
---

<objective>
Stop an unreachable java-interop peer from costing one connect timeout per unresolved class, and make resolution resume once the peer is back, without `clearCache()` (RESP-02, #504).

Purpose: today every lookup attempts its own socket and shows its own popup, serialized behind the resolution lock. Every failure stub is also cached for the life of the process, so a class that failed during an outage stays broken after the peer returns.
Output:
- a request-driven three-state breaker in `java-interop.ts`;
- uncached transport stubs;
- a recovery event for plan 91-05 to wire;
- a scriptable fake peer;
- the breaker regression tests.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-language-server-responsiveness/91-CONTEXT.md
@.planning/phases/91-language-server-responsiveness/91-RESEARCH.md
@.planning/phases/91-language-server-responsiveness/91-PATTERNS.md

<interfaces>
Verified in the tree during planning. Line numbers are as of phase base 174985f7, in `bbj-vscode/src/language/java-interop.ts`:

- `connect()`, 156-169:
  - returns `this.connection` when set;
  - otherwise returns the shared in-flight `connectingPromise`;
  - otherwise sets `connectingPromise = this.establishConnection()`, awaits it, and clears it in `finally`.
- `establishConnection()`, 176-195 (private):
  - awaits `createSocket()`;
  - on a failure it calls `notifyJavaConnectionError(detail)` and `console.error`, then rethrows;
  - otherwise builds the connection with `createMessageConnection(new SocketMessageReader(socket), new SocketMessageWriter(socket))`, registers identity-guarded `onClose`/`onError` handlers that drop `this.connection`, calls `listen()`, and assigns `this.connection`.
- `createSocket()`, 212-229 (protected): rejects with the socket error (`ECONNREFUSED` etc.) or with `Socket connection to Java service timed out after 10s`.
- `getRawClass(className, token)`, 262-273: awaits `connect()`, then races `sendRequest(getClassInfoRequest)` against a 10 s timer that rejects `new Error('Java class resolution timeout for …')`.
- Other connect callers:
  - `sendRequestSafe`, 287-295, used by `loadClasspath` (303-314);
  - `loadImplicitImports`, 321-385, which does a direct `connect()`, then `getClassInfos` per package and a direct `resolveClass`. At 330-337 it pushes a simple-name copy into `this.classpath.classes` and sets it in `resolvedClasses`.
  - `ensureCompleteClassIndex`, 402-422:
    - It returns early at 403-405 once `completeIndexResolved` is true.
    - Otherwise it awaits `connect()` and `getAllClassNames`.
    - It latches only a `METHOD_NOT_FOUND` answer.
  - `resolveClassCandidatesBySimpleName` (465-482) and `findClassCandidatesByPrefix` (490-513) call `ensureCompleteClassIndex`.
- Resolution path:
  - `resolveClassByName`, 545-580.
  - `doResolveClassByName`, 582-611: creates a 30 s chain timer that rejects `new Error('Java class resolution chain timed out after …')`, acquires the lock, and its catch at 605-608 returns `this.createStubClass(className)` (cached).
  - `createStubClass(className, cache = true)`, 620-642: returns an existing `resolvedClasses` entry when present.
- `clearCache()`, 873-906.
- `acquireLock`/`drainLockQueue`, 914-946. These must not change (D-06).
- `bbj-vscode/src/language/bbj-notifications.ts`:
  - `notifyJavaConnectionError(errorDetail)` sends `_connection?.window.showErrorMessage('Failed to connect to the Java interop service. … (detail)')`.
  - `initNotifications(connection)` installs the connection. A test can install a stub `{ window: { showErrorMessage: vi.fn() } }` cast to `Connection`, and install `null` (cast) afterwards.
- `vscode-jsonrpc/node.js` re-exports `ConnectionError`, `ConnectionErrors`, `ResponseError` and `ErrorCodes`. `ErrorCodes.PendingResponseRejected` is -32097. A disposed connection rejects every pending request with that code.
- Test analogs:
  - `test/java-interop-service.test.ts`: `MockableJavaInteropService` initializes `JavadocProvider` in its constructor and overrides `createSocket()`. `createServices()` injects a test module.
  - `test/java-interop-timeouts.test.ts`: fake timers, and a fake connection object with only `sendRequest`. Its assertions match `/timeout/i` for `getRawClass`, so timeout messages must keep the word timeout.
- vitest collects only `*.test.ts`, so `test/fake-interop-peer.ts` is a plain helper module.
- Vitest's `vi.useFakeTimers()` also fakes `Date.now()`. The breaker clock is `Date.now()`. If the installed vitest does not fake Date by default, pass `toFake` including `Date`.

Breaker constants are a discretionary choice (RESEARCH A1): initial cooldown 5000 ms, backoff factor 2, cap 30000 ms, and one connect-level failure opens the breaker. Tests read the exported constants and never hard-code these numbers.

Shell rules:
- Absolute paths only, and `git -C /home/coder/repos/bbj-language-server …` for git.
- Never chain `cd` with grep/find/cat/sed/head/tail/git.
- Run vitest as `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run <file> --root /home/coder/repos/bbj-language-server/bbj-vscode`.
- Stage exact paths only.

Comment discipline: source comments, test names and assertion messages must not contain planning register ids (plan numbers, decision ids, threat ids, gap ids, `Pitfall N`, or C-/CR-/WR-/IN- ids). GitHub issue numbers such as #504 are fine.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md; RESP-02 is also declared by plan 91-05.
</interfaces>
</context>

## Artifacts this plan produces

- In `bbj-vscode/src/language/java-interop.ts`:
  - `export class InteropTransportError extends Error`;
  - `export function isInteropTransportFailure(error: unknown): boolean`;
  - `export const INTEROP_BREAKER_INITIAL_COOLDOWN_MS`, `INTEROP_BREAKER_BACKOFF_FACTOR`, `INTEROP_BREAKER_MAX_COOLDOWN_MS`.
- `JavaInteropService` members:
  - `protected wrapSocket(socket: Socket): MessageConnection`;
  - `public onConnectionRecovered(listener)`;
  - private breaker state fields (state, probe due time, cooldown, generation);
  - private `recoveryListeners`;
  - `private probeIfDue(): void`;
  - a private map of implicit-import simple-name copies.
- The short-circuit message text `Java interop service unavailable (circuit open)`. Plan 91-06 greps it in the rebuilt bundles.
- `bbj-vscode/test/fake-interop-peer.ts`:
  - `export class FakePeerInteropService`, with public `peerUp`, `connectDelayMs`, `answerRequests`, `socketAttempts`, `sentRequests`, `packageClasses`, `classInfo(className)`, `dropConnection()`, `seedCompleteClassIndex(fqns)` and `classpathClassCount()`;
  - `export function createFakePeerServices()`.
- `bbj-vscode/test/java-interop-breaker.test.ts`, with describe `java-interop circuit breaker (#504)`.

## Flagged assumptions

- **Edge probe:** the RESP-02 row came back `unclassified`, so no edge category was auto-resolved. The truths above (D-01, D-02, D-03, D-05) pin the breaker's boundary behaviour instead. Review manually.
- **D-01 vs D-14:** recovery is request-driven with no timer. The only language-server paths that reach a Java class lookup without an edit are the quick-fix and auto-import candidate lookups, and with the complete class index built those answer locally and never touch the connection. The phase would then have no no-edit recovery path, so `probeIfDue()` lets those lookups start the single half-open probe. This follows the "Retry on next request" choice recorded in the discussion. Recovery with no request at all would need a background timer, which D-01 rules out.
- **Discretionary numbers:** the cooldown values (5 s, ×2, capped at 30 s) are not a researched value.
- **Backend parity (RESEARCH A3):** every backend version is assumed to create a fresh InteropService per connection. The D-14 live check confirms this.

<tasks>

<task type="tracer">
  <name>Task 1: Twenty unresolved classes against an unreachable peer settle within one connect timeout, with one socket attempt, one popup and nothing cached</name>
  <files>bbj-vscode/test/fake-interop-peer.ts, bbj-vscode/test/java-interop-breaker.test.ts, bbj-vscode/src/language/java-interop.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts lines 1-300 and 538-650 and 868-947
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-notifications.ts — whole file
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/java-interop-service.test.ts lines 1-136 — `MockableJavaInteropService`, `createServices`, `minimalJavaClass`
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/java-interop-timeouts.test.ts — fake timers and the fake connection object
  </read_first>
  <action>
  1. Create the test helper `bbj-vscode/test/fake-interop-peer.ts`.
     - `export class FakePeerInteropService extends JavaInteropService`. Its constructor initializes `JavadocProvider` exactly as `MockableJavaInteropService` does.
     - Public knobs:
       - `peerUp`, default false;
       - `connectDelayMs`, default 10000, the socket connect timeout (0 means refused at once);
       - `answerRequests`, default true;
       - `packageClasses`, a Map from package name to a function returning fresh DTOs;
       - `classInfo(className)`, returning a fresh minimal DTO: `$type 'JavaClass'`, `name`, `packageName` taken from the name, and empty `classes`, `fields`, `methods` and `constructors`.
     - Public counters: `socketAttempts`, and `sentRequests` (method and params of each request).
     - `override createSocket()`:
       - Increments `socketAttempts`.
       - When `connectDelayMs` is 0, settles at once.
       - Otherwise settles after `connectDelayMs` via `setTimeout`.
       - It resolves a placeholder socket object if `peerUp` is true at settle time.
       - Otherwise it rejects with `new Error('connect ECONNREFUSED 127.0.0.1:5008')` (delay 0) or `new Error('Socket connection to Java service timed out after 10s')`.
     - `override wrapSocket()` returns a fake message connection with:
       - `listen()` and `dispose()`;
       - `onClose(listener)` and `onError(listener)`, keeping the last listener of each;
       - `sendRequest(type, params, token)`, which records the request and then:
         - when `answerRequests` is false, returns a promise kept in a pending list that only settles by rejecting when the token is cancelled;
         - `getClassInfo` resolves `classInfo(params.className)`;
         - `getClassInfos` resolves the `packageClasses` entry or an empty array;
         - `loadClasspath` resolves true;
         - `getTopLevelPackages` resolves an empty array;
         - `getAllClassNames` rejects with an object whose `code` is -32601.
     - `dropConnection()` rejects every pending request with `new ResponseError(ErrorCodes.PendingResponseRejected, 'Pending response rejected since connection got disposed')`, then invokes the stored close listener.
     - `seedCompleteClassIndex(fqns)` calls `buildCompleteClassIndex`.
     - `classpathClassCount()` returns `this.classpath.classes.length`.
     - `export function createFakePeerServices()` returns `{ shared, BBj, interop }`. It is built like `createServices()` in java-interop-service.test.ts, with a module injecting `FakePeerInteropService`.
  2. Write the test first (D-01, D-02, D-03, D-05) in `bbj-vscode/test/java-interop-breaker.test.ts`, describe `java-interop circuit breaker (#504)`.
     - `beforeEach`: install a stub connection through `initNotifications`, with `window.showErrorMessage` a `vi.fn()`.
     - `afterEach`: `vi.useRealTimers()` and install `null`.
     - Test `twenty unresolved classes against an unreachable peer settle within one connect timeout`:
       - Take services from `createFakePeerServices()`, then call `vi.useFakeTimers()`.
       - Start `resolveClassByName('test.Missing0')` through `'test.Missing19'` together, and track settlement.
       - `await vi.advanceTimersByTimeAsync(10000)`, then `50`.
       - All 20 have settled.
       - `socketAttempts` is 1 and `showErrorMessage` was called once.
       - Every result has `error`, and `getResolvedClass(name)` is undefined for all 20.
     - Run it and confirm it fails before step 3. Record the observed counts for the SUMMARY.
  3. Production changes in `java-interop.ts`.
     a. Seam: move the `createMessageConnection(new SocketMessageReader(socket), new SocketMessageWriter(socket))` call in `establishConnection()` into `protected wrapSocket(socket)`, used in the same place.
     b. Transport failures:
        - Export `class InteropTransportError extends Error`. It sets `name` and keeps the original error on a readonly property.
        - Export `function isInteropTransportFailure(error)`. It is true for an `InteropTransportError`, for a vscode-jsonrpc `ConnectionError`, and for a `ResponseError` whose `code` is `ErrorCodes.PendingResponseRejected`, and false otherwise.
        - Import those names from `vscode-jsonrpc/node.js` next to the existing imports.
     c. Constants: export `INTEROP_BREAKER_INITIAL_COOLDOWN_MS = 5_000`, `INTEROP_BREAKER_BACKOFF_FACTOR = 2` and `INTEROP_BREAKER_MAX_COOLDOWN_MS = 30_000`. Their doc comment says no issue names these numbers and one connect-level failure opens the breaker (#504).
     d. Private breaker fields:
        - the state (`closed`, `open` or `half-open`), starting closed;
        - the `Date.now()` time at which a probe becomes due;
        - the current cooldown, starting at the initial value;
        - a generation counter.
     e. `connect()`:
        - After the live-connection return, when the state is `open` and `Date.now()` is before the due time, throw `new InteropTransportError('Java interop service unavailable (circuit open)')` with no socket attempt.
        - Keep the same-tick `connectingPromise` sharing.
        - The creating call converts a rejection of `establishConnection()` into an `InteropTransportError` carrying the original message.
        - When that happens with the breaker closed and the generation unchanged, set the state open, set the due time to `Date.now()` plus the cooldown, and call `notifyJavaConnectionError(originalMessage)` there, once (D-05).
     f. Remove the `notifyJavaConnectionError` call from `establishConnection()`'s catch, and keep its `console.error`.
     g. In `doResolveClassByName`'s catch, return `this.createStubClass(className, !isInteropTransportFailure(e))`, and keep its warning (D-03).
     h. Leave `acquireLock`, `drainLockQueue`, `lockQueue`, `lockHeld` and `currentLockToken` untouched (D-06). The breaker sits in `connect()`, which `getRawClass`, `sendRequestSafe`, `loadImplicitImports` and `ensureCompleteClassIndex` already share.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/java-interop-breaker.test.ts test/java-interop-timeouts.test.ts test/java-interop-service.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or the summary reports a failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the pre-change result of the new test (settled count, socket attempts, popup count), and that the test failed.
    - The new test passes, and the existing timeout and service suites pass unchanged.
    - `grep -n "Java interop service unavailable (circuit open)" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints exactly one line.
    - `grep -n "override wrapSocket" /home/coder/repos/bbj-language-server/bbj-vscode/test/fake-interop-peer.ts` prints one line, and so does `grep -n "override createSocket" /home/coder/repos/bbj-language-server/bbj-vscode/test/fake-interop-peer.ts`.
    - In `git -C /home/coder/repos/bbj-language-server diff 174985f7 -- bbj-vscode/src/language/java-interop.ts`, no removed line lies inside `acquireLock` or `drainLockQueue`.
  </acceptance_criteria>
  <done>An outage costs one connect timeout and one popup however many classes are unresolved, and nothing it produces is cached.</done>
</task>

<task type="auto">
  <name>Task 2: After the cooldown a single half-open probe re-tests the peer; success resumes resolution without clearCache and signals recovery once; failure backs off silently; clearCache resets</name>
  <files>bbj-vscode/test/java-interop-breaker.test.ts, bbj-vscode/src/language/java-interop.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts — `connect()`, the breaker fields and `clearCache()` as Task 1 left them
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/java-interop-breaker.test.ts
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/fake-interop-peer.ts
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/logger.ts — `logger.error`
  </read_first>
  <action>
  1. Tests first (D-01, D-04, D-05). Each test uses `connectDelayMs = 0` unless stated, and reads cooldown values from the exported constants, never literal numbers.
     a. `the peer coming back is picked up by the next lookup after the cooldown, without clearCache`:
        - Open the breaker with one lookup, then set `peerUp = true`.
        - Spy `clearCache`, and register a `vi.fn()` with `onConnectionRecovered`.
        - A lookup at the initial cooldown minus 1 ms returns a stub and adds no socket attempt.
        - Advance 1 ms. `resolveClassByName('test.Later')` then has no `error`, and `getResolvedClass('test.Later')` is defined.
        - After `advanceTimersByTimeAsync(0)` the listener was called once, and a further lookup leaves it at once.
        - The `clearCache` spy was never called, and `showErrorMessage` is still at one call.
     b. `failed probes back off up to the cap and stay silent`:
        - Open the breaker.
        - Each round: advance the expected cooldown and do one lookup (exactly one new socket attempt); advance to 1 ms before the next due time and do one lookup (no new attempt).
        - The expected cooldown multiplies by the backoff factor each round and stops at the cap after enough rounds.
        - `showErrorMessage` stays at one call.
     c. `only one probe runs while half-open`:
        - Use `connectDelayMs = 10000`. Open the breaker by advancing 10000.
        - Advance the initial cooldown, then start `loadClasspath(['/x.jar'])` twice in the same tick.
        - After `advanceTimersByTimeAsync(10)`, the second call has settled (false) while the first is still pending, and `socketAttempts` grew by exactly one.
     d. `clearCache resets the breaker and a stale probe cannot report recovery`:
        - Open the breaker, then call `clearCache()`. The next lookup attempts a socket at once and, still failing, sends a second popup.
        - Open it again with `connectDelayMs = 10000` and advance the cooldown.
        - Start a lookup (this is the probe), set `peerUp = true`, call `clearCache()`, then advance 10000.
        - The recovery listener was never called.
     e. `a second outage after recovery shows a second popup`:
        - Recover as in (a), call `dropConnection()`, and set `peerUp = false`.
        - One lookup brings `showErrorMessage` to two calls in total.

     Confirm (a) through (d) fail before step 2.
  2. Implement in `java-interop.ts` (D-01, D-04).
     - `connect()`, when `open` and the due time has passed: switch to `half-open` and make this call the probe, which does not join `connectingPromise`.
     - While `half-open`, every other caller throws the same `InteropTransportError` at once.
     - Probe success: set the state closed, reset the cooldown to the initial value, then notify the recovery listeners.
     - Probe failure: set the state open, multiply the cooldown by the backoff factor up to the cap, reset the due time, and send no popup. The existing `console.error` in `establishConnection` is the log.
     - Capture the generation when a connect attempt starts. If it changed by the time the attempt settles, the outcome updates no breaker state and fires no listener.
     - A successful connect while closed keeps the state closed.
     - Add `public onConnectionRecovered(listener)`; the listener returns nothing or a promise. Store listeners in a private array.
     - Fire each listener only on the half-open-to-closed transition, scheduled with `Promise.resolve().then(...)`, and log a rejection through `logger.error`. `connect()` never awaits a listener: a listener may rebuild documents that re-enter resolution.
     - `clearCache()` increments the generation, sets the state closed, and resets the cooldown and due time.
     - Add a short comment where the probe starts: a probe issued from inside a resolution holds the unchanged resolution lock for at most one connect attempt per cooldown window.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/java-interop-breaker.test.ts test/java-interop-service.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or the summary reports a failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records that tests (a) through (d) failed before step 2.
    - All breaker tests pass, and the service suite still passes.
    - `grep -n "onConnectionRecovered(" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints at least one line.
    - `grep -n "setInterval" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints nothing.
  </acceptance_criteria>
  <done>The breaker recovers on the first request after the cooldown, backs off silently while the peer stays down, and resets on clearCache.</done>
</task>

<task type="auto">
  <name>Task 3: A slow-but-connected peer never trips the breaker; request, chain and dropped-connection failures are never cached; candidate lookups carry the probe; implicit imports reload safely</name>
  <files>bbj-vscode/test/java-interop-breaker.test.ts, bbj-vscode/src/language/java-interop.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts lines 255-430 and 575-615 and 868-910 — as Tasks 1-2 left it
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/java-interop-breaker.test.ts
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/fake-interop-peer.ts
  </read_first>
  <action>
  1. Tests first (D-01, D-02, D-03, D-04, D-14).
     a. `a connected but slow peer does not trip the breaker`:
        - Set `peerUp = true` and `answerRequests = false`.
        - Look up `test.SlowA` and advance 10000. It settled with `error`, and `getResolvedClass('test.SlowA')` is undefined.
        - A lookup of `test.SlowB` records a `getClassInfo` request for `test.SlowB` in `sentRequests`.
        - `socketAttempts` is 1, and `showErrorMessage` was not called.
     b. `transport failures are classified`:
        - `isInteropTransportFailure` is true for an `InteropTransportError`, for `new ConnectionError(ConnectionErrors.Closed, 'Connection is closed.')`, and for `new ResponseError(ErrorCodes.PendingResponseRejected, 'x')`.
        - It is false for `new ResponseError(-32603, 'internal')` and for `new Error('boom')`.
     c. `a connection dropped mid-request returns an uncached stub`:
        - Set `peerUp = true` and `answerRequests = false`. Start `resolveClassByName('test.Dropped')`, then call `dropConnection()`.
        - After `advanceTimersByTimeAsync(0)` it settled with `error` and is not cached.
        - With `peerUp = false` and `connectDelayMs = 0`, the next lookup opens the breaker with one popup.
     d. `a candidate lookup answered from the complete class index still starts the probe`:
        - Call `seedCompleteClassIndex(['java.util.HashMap'])`, open the breaker, set `peerUp = true`, and advance the initial cooldown.
        - `await resolveClassCandidatesBySimpleName('HashMap')` resolves `['java.util.HashMap']`.
        - After `advanceTimersByTimeAsync(0)`, `socketAttempts` grew by one and a registered recovery listener was called once.
     e. `implicit imports load again without clearCache and without duplicate classpath entries`:
        - Set `peerUp = true`. Set `packageClasses` for `java.lang` to a function returning fresh DTOs for `java.lang.Alpha` and `java.lang.Beta`.
        - Await `loadImplicitImports()` and record `classpathClassCount()`.
        - Await it again. The count is unchanged, and `getResolvedClass('Alpha')` is defined.

     Confirm (a), (d) and (e) fail before step 2.
  2. Implement in `java-interop.ts`.
     - `getRawClass`'s 10 s race and `doResolveClassByName`'s 30 s race reject with `InteropTransportError`, keeping their current message text (D-02, D-03).
     - `ensureCompleteClassIndex()`: in the branch that returns early because `completeIndexResolved` is already true, first call a private `probeIfDue()`.
       - When the breaker is `open` and the due time has passed, `probeIfDue()` starts `connect()` without awaiting it and swallows the rejection.
       - The breaker's own bookkeeping handles the outcome, and the lookup still answers from the index at once (D-01, D-14).
     - `loadImplicitImports()`:
       - Keep a private map of the simple-name copies already added, keyed by package plus simple name.
       - When a key is already present, set `resolvedClasses` to the stored copy and do not push a second entry into `this.classpath.classes`.
       - `clearCache()` clears the map (D-04).
  3. Run lint, build, the interop suites and the register check.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/java-interop-breaker.test.ts test/java-interop-timeouts.test.ts test/java-interop-service.test.ts test/completion-test.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or the summary reports a failed test</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 174985f7 -- bbj-vscode/src/language/java-interop.ts bbj-vscode/test/fake-interop-peer.ts bbj-vscode/test/java-interop-breaker.test.ts | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|\bG-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text), or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records that tests (a), (d) and (e) failed before step 2.
    - Every test in `java-interop-breaker.test.ts` passes, and so do the timeout, service and completion suites.
    - `grep -n "createStubClass(className, !isInteropTransportFailure" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints exactly one line.
    - `grep -n "probeIfDue()" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts` prints at least two lines (the definition and its call).
    - `git -C /home/coder/repos/bbj-language-server diff 174985f7 --stat -- bbj-intellij` prints nothing.
    - Lint and build exit 0, and the register check prints nothing.
  </acceptance_criteria>
  <done>Only a connect-level failure opens the breaker. No transport failure is cached. A caret-driven quick-fix request can bring recovery. The recovery reload can run again safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server → java-interop peer (:5008) | an external local service that may be stopped, restarted, slow or unreachable |
| breaker → client notifications | the server decides how often the IDE shows an error popup |
| recovery listener → document rebuild | a listener runs while resolution may be in progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-91-07 | Denial of Service | `connect()` / `doResolveClassByName` against an unreachable peer | high | mitigate | breaker short-circuit after one connect-level failure; the twenty-class test proves about one connect timeout in total |
| T-91-08 | Denial of Service | breaker tripping on a slow-but-connected peer | medium | mitigate | only `establishConnection()` rejections count; slow-peer test |
| T-91-09 | Tampering | transport-failure stubs cached for the process lifetime | medium | mitigate | `createStubClass(className, !isInteropTransportFailure(e))`; uncached and classifier tests |
| T-91-10 | Repudiation | popup storm hiding the real state | low | mitigate | notification only on closed to open; count tests across two outages |
| T-91-11 | Denial of Service | recovery listener awaited inside `connect()`, re-entering resolution under the lock | medium | mitigate | listeners fire via `Promise.resolve().then`, never awaited; recovery test |
| T-91-12 | Denial of Service | repeated recovery reloads growing the synthetic classpath | low | mitigate | implicit-import copy map; re-run test |
| T-91-SC | Tampering | npm/pip/cargo installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. The breaker, timeout, service and completion suites are green, and each new test was red before its production step.
2. Lint and build exit 0, and the register check prints nothing.
3. `git -C /home/coder/repos/bbj-language-server diff 174985f7 --stat -- bbj-intellij` prints nothing.
</verification>

<success_criteria>
- ROADMAP success criterion 2 holds at the service level:
  - an unreachable peer costs about one connect timeout in total;
  - resolution resumes without `clearCache()` once the peer is back;
  - a slow-but-healthy peer does not trip the breaker.
- D-01, D-02, D-03, D-05 and D-06 hold. D-04's recovery event exists for plan 91-05 to wire.
</success_criteria>

<output>
Create `.planning/phases/91-language-server-responsiveness/91-03-SUMMARY.md` when done. Record:
- the pre-change failing counts;
- the chosen constant values;
- any deviation.
</output>
