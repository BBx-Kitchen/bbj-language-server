---
phase: 92-host-side-hygiene-focus-guards
plan: "05"
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - bbj-vscode/src/extension.ts
  - bbj-vscode/test/extension-activation.test.ts
autonomous: true
requirements: [RESP-08]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every `vscode.commands.registerCommand(...)` call in `activate()` is pushed onto `context.subscriptions` (D-09)."
    - "The `registerDocumentFormattingEditProvider` registration and all three `client.onNotification(...)` handlers in `activate()` (`bbj/bbjcplAvailability`, `CONFIG_RELOAD_METHOD`, `RESOLVED_CONFIG_PATH_METHOD`) are pushed onto `context.subscriptions` (D-09)."
    - "Every Disposable those calls return during an activation is present in that activation's `context.subscriptions`, as a runtime assertion on the mock return values (D-09)."
    - "In the mocked harness, `registerCommand` throws `command 'X' already exists` for an id that is still registered. After disposing the first activation's subscriptions, a second `activate()` does not throw, and no command id is left registered (D-10)."
    - "Registering a still-registered id collides: two activations without disposal in between throw `already exists`, which proves the harness would catch a double registration (adjacency edge)."
    - "A first activation behaves as before: the two existing `client.start()` tests still pass (empty edge)."
    - statement: "No registration pushed in activate() depends on the order in which context.subscriptions is disposed; each Disposable releases only its own registration (ordering edge)."
      verification: backstop
    - "The restart gate still reuses the single client instance, so notification handlers are disposed only on deactivation, never on a config-reload restart (D-09)."
  prohibitions:
    - statement: "MUST NOT dispose the language client's notification handlers during a config-reload restart; they may be released only when the extension deactivates."
      status: resolved
      verification: test
  artifacts:
    - path: bbj-vscode/src/extension.ts
      provides: "activate() pushes every command, formatting-provider and notification Disposable"
      contains: "context.subscriptions.push(vscode.commands.registerCommand("
    - path: bbj-vscode/test/extension-activation.test.ts
      provides: "duplicate-throwing registerCommand mock, negative control, double-activate test, Disposable membership test"
      contains: "already exists"
  key_links:
    - from: "extension.ts activate()"
      to: "context.subscriptions"
      via: "context.subscriptions.push(...) around each registration"
      pattern: "context\\.subscriptions\\.push\\(\\s*client\\.onNotification\\("
---

<objective>
RESP-08 (#531). A second `activate()` in the same extension host no longer throws on duplicate commands or leaks providers and notification handlers. Every Disposable returned inside `activate()` goes onto `context.subscriptions` (D-09), proven by a double-activate test against a mock that throws on duplicate command ids as real VS Code does (D-10).

Purpose: today 16 commands, the formatter provider and three notification handlers are registered bare, so a re-activation crashes on `command 'bbj.config' already exists` and handlers accumulate.
Output: mechanical wrapping in `extension.ts` and an extended `extension-activation.test.ts`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-RESEARCH.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-01-SUMMARY.md

<interfaces>
Verified during planning. Base commit `3ec25f02`; plan 92-01 first edits the `bbj.runBUI`/`bbj.runDWC` handler bodies, which is why this plan is wave 2. Re-read the file for current line numbers.

- `extension.ts` `activate()` starts around line 678. Bare registrations:
  - `vscode.commands.registerCommand` for `bbj.config`, `bbj.properties`, `bbj.em`, `bbj.loginEM`, `bbj.run`, `bbj.runBUI`, `bbj.runDWC`, `bbj.compile`, `bbj.denumber`, `bbj.decompile`, `bbj.decompileReadonly`, `bbj.configureCompileOptions`, `bbj.refreshJavaClasses`, `bbj.showClasspathEntries` (14 ids in `activate()` itself);
  - `vscode.languages.registerDocumentFormattingEditProvider("bbj", DocumentFormatter)`;
  - `client.onNotification('bbj/bbjcplAvailability', …)`, `client.onNotification(CONFIG_RELOAD_METHOD, …)`, `client.onNotification(RESOLVED_CONFIG_PATH_METHOD, …)`.
- Existing correct shape in the same file: `context.subscriptions.push(\n vscode.window.onDidChangeActiveTextEditor(...)\n);`.
- `vscode-languageclient` ^10.1.0: `onNotification(...)` returns `Disposable` (`node_modules/vscode-languageclient/lib/common/client.d.ts` lines 384-388).
- Registrations performed by other modules during `activate()`:
  - composer modules (`msgbox`, `addwindow`, `addchildwindow`, `composer-lens`, `cvs`, `setopts`) are mocked in `extension-activation.test.ts`;
  - `setopts-in-code-ui.ts` is real there, and already pushes its `registerCommand('bbj.composeSetoptsInCode', …)` and `registerCodeActionsProvider` onto `context.subscriptions`.
- `extension-activation.test.ts`:
  - `vi.mock('vscode')` with `disposable()` returning `{ dispose: vi.fn() }`;
  - `commands.registerCommand: vi.fn()`, `languages.registerDocumentFormattingEditProvider: vi.fn()`, `registerCodeActionsProvider: vi.fn()`, all returning undefined;
  - `LanguageClient` mock class with `onNotification = vi.fn()`;
  - `startMock` drives `client.start()`;
  - `activate` is synchronous.
  - Composer tests use `vi.hoisted` for shared mock state (e.g. `test/composer-lens-command.test.ts`).
- `config-reload-host.test.ts` locates `client.onNotification(CONFIG_RELOAD_METHOD` by substring and parses the call's balanced parentheses. Wrapping the call in `context.subscriptions.push(` keeps that substring and call intact. Its own mocks return undefined and it never disposes subscriptions, so pushing undefined there is harmless.
- `restart-gate.ts` stops and starts the same client (Phase 85); it never disposes `context.subscriptions`.

Shell rules: absolute paths only; `git -C /home/coder/repos/bbj-language-server …`; never chain `cd` with grep/find/cat/sed/head/tail/git; stage exact paths only.
Register rule: no plan, decision, threat or pitfall ids in source or test text (the pre-existing `P62-D2-004` id and issue numbers such as `#531` are fine).
Deviations: record them in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- No new files or exported symbols.
- In `extension-activation.test.ts`:
  - hoisted mock state `registeredCommandIds` (a `Set<string>`) and `onNotificationMock`;
  - helpers `makeContext()` and `disposeSubscriptions(context)`;
  - a new describe block for re-activation.

## Flagged assumptions

- The document-formatter module's import-time `onDidChangeTextDocument`/`onDidCloseTextDocument` listeners, and the module-level `client`/`restartGate` variables, stay as they are. CONTEXT puts them in scope only if the double-activate test fails because of them. If it does, the executor fixes exactly the failing registration and records it in the SUMMARY.
- A VS Code window reload starts a fresh extension host and cannot reproduce #531 by hand, so this requirement has no live UAT step. The automated test is the proof.

## Edge coverage (spec-less probe)

- **RESP-08 adjacency:** resolved (explicit). See the negative-control truth; Task 1 tests it.
- **RESP-08 empty:** resolved (explicit). See the first-activation truth; the existing tests run in Task 1's verify.
- **RESP-08 ordering:** resolved (backstop). See the order-independence truth; no test can observe disposal order, so it abstains to human review at verify time.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A second activation after disposing the first registers every command again without throwing</name>
  <files>bbj-vscode/test/extension-activation.test.ts, bbj-vscode/src/extension.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/extension-activation.test.ts — whole file
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts — `activate()` in full (from `export function activate(` to `export function deactivate(`)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/composer-lens-command.test.ts — lines 1-40, the `vi.hoisted` pattern
  </read_first>
  <behavior>
    - The mock `registerCommand(id, handler)` throws `new Error(\`command '${id}' already exists\`)` when `registeredCommandIds` still holds `id`. Otherwise it adds `id` and returns `{ dispose: () => { registeredCommandIds.delete(id); } }`.
    - `beforeEach` clears `registeredCommandIds` (each test simulates a fresh extension host) and sets `startMock` to resolve.
    - Negative control: `activate(makeContext())`, then `expect(() => activate(makeContext())).toThrow(/already exists/)`.
    - Re-activation (issue #531):
      - `const first = makeContext(); activate(first); disposeSubscriptions(first);` then expect `registeredCommandIds.size` to be 0;
      - `const second = makeContext();` expect `() => activate(second)` not to throw;
      - expect `registeredCommandIds.size` to be greater than 0; `disposeSubscriptions(second)`; expect the size to be 0 again.
    - The two existing `P62-D2-004` tests keep passing unchanged in intent.
  </behavior>
  <action>
  1. RED. In `extension-activation.test.ts`:
     - Add `const { registeredCommandIds, onNotificationMock } = vi.hoisted(() => ({ registeredCommandIds: new Set<string>(), onNotificationMock: vi.fn(() => ({ dispose: vi.fn() })) }));`.
     - Change the `vscode` mock: `commands.registerCommand` gets the duplicate-throwing implementation above; `languages.registerDocumentFormattingEditProvider`, `registerCodeActionsProvider` and `registerCodeLensProvider` return `disposable()`.
     - Change the `LanguageClient` mock's `onNotification` to `onNotificationMock`.
     - Add `makeContext()`, returning the existing context shape with a fresh `subscriptions: []`, and `disposeSubscriptions(context)`, which calls `dispose()` on every entry.
     - Add the `beforeEach`, and a new `describe('extension re-activation (#531)')` with the negative control and the re-activation test.

     Confirm the re-activation test fails with `already exists` and the negative control passes.
  2. GREEN. In `extension.ts` `activate()`, wrap every `vscode.commands.registerCommand(...)` call in `context.subscriptions.push(...)`, one push per registration. Write the opening on one line as `context.subscriptions.push(vscode.commands.registerCommand(`, with the matching `));` after the handler. Handler bodies stay byte-identical inside the wrapper. That includes the `bbj.loginEM`, `bbj.runBUI`, `bbj.runDWC`, `bbj.refreshJavaClasses` and `bbj.showClasspathEntries` handlers as plan 92-01 left them.
  3. Run the activation harnesses. If the re-activation test still throws for a command registered outside `activate()`'s own calls, fix exactly that module's registration to push its Disposable and record it in the SUMMARY.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/extension-activation.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, an "already exists" failure in the re-activation test, or a summary line reporting any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'context.subscriptions.push(vscode.commands.registerCommand(' /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts` prints 14.
    - The negative control, the re-activation test and both existing `P62-D2-004` tests pass.
  </acceptance_criteria>
  <done>Re-activating the extension in one host re-registers every command cleanly, and the harness demonstrably fails on any command left undisposed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The formatting provider and all three notification handlers are released with the activation, and every returned Disposable is accounted for</name>
  <files>bbj-vscode/src/extension.ts, bbj-vscode/test/extension-activation.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts — the formatting-provider registration and the three `client.onNotification(` calls in `activate()`
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/config-reload-host.test.ts — lines 510-580, the `client.onNotification(CONFIG_RELOAD_METHOD` parse that must keep passing
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/restart-gate.ts — confirm restart never touches `context.subscriptions`
  </read_first>
  <behavior>
    - Membership test:
      - `mockClear()` on `registerCommand`, `registerDocumentFormattingEditProvider` and `onNotificationMock`;
      - `const ctx = makeContext(); activate(ctx);`
      - for each of the three mocks, every `mock.results[i].value` is contained in `ctx.subscriptions`;
      - `onNotificationMock` was called at least 3 times, and its first argument set includes `bbj/bbjcplAvailability`;
      - `registerDocumentFormattingEditProvider` was called once.
    - `config-reload-host.test.ts` and `config-file-association.test.ts` still pass.
  </behavior>
  <action>
  1. RED. Add the membership test to the re-activation describe. Confirm it fails for the formatting provider and the notification handlers.
  2. GREEN. In `activate()`:
     - wrap `vscode.languages.registerDocumentFormattingEditProvider("bbj", DocumentFormatter)` in `context.subscriptions.push(...)`;
     - wrap each of the three `client.onNotification(...)` calls the same way, handler bodies byte-identical.

     Do not touch `startLanguageClient`, `restartGate` or `deactivate()`.
  3. Run the three activation harnesses plus the extension.ts source-guard suites, then build, lint and the register check.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/extension-activation.test.ts test/config-reload-host.test.ts test/config-file-association.test.ts test/target-resolution.test.ts test/no-shell-command-construction.test.ts test/em-secret-env-channel.test.ts test/setopts-in-code-ui.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 3ec25f02 -- bbj-vscode/src/extension.ts bbj-vscode/test/extension-activation.test.ts | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text)</fails_when>
  </verify>
  <acceptance_criteria>
    - The membership test passes for all three registration kinds.
    - `grep -c 'client.onNotification(CONFIG_RELOAD_METHOD' /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts` prints 1.
    - Zero failed tests across the seven targeted files; build and lint exit 0; the register check prints nothing.
  </acceptance_criteria>
  <done>Every command, the formatting provider and every notification handler registered by an activation is released with that activation, and nothing else about restarts changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension host lifecycle → extension registrations | activation and deactivation own every command, provider and handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-12 | Denial of Service | `activate()` duplicate `registerCommand` crash on re-activation | medium | mitigate | push every command Disposable; double-activate test against a duplicate-throwing mock plus a negative control |
| T-92-13 | Denial of Service | accumulating notification handlers and formatting providers | low | mitigate | push provider and three notification Disposables; runtime membership test over mock return values |
| T-92-14 | Repudiation | config reload silently stops after a restart disposes handlers | low | mitigate | restart gate untouched and never disposes subscriptions; `config-reload-host.test.ts` still passes |
| T-92-SC | Tampering | npm installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. `extension-activation.test.ts` passes: negative control, re-activation, membership and both existing start tests.
2. The other activation harnesses and the extension.ts source-guard suites still pass.
3. Build and lint pass; the register check prints nothing.
</verification>

<success_criteria>
- ROADMAP criterion 4: reactivating the VS Code extension in the same host does not double-register commands, providers or notifications, because every registration from the prior activation is disposed.
- D-09 and D-10 are implemented exactly.
</success_criteria>

<output>
Create `.planning/phases/92-host-side-hygiene-focus-guards/92-05-SUMMARY.md` when done. Record the registration counts wrapped, whether any module outside `activate()` needed a fix, and any deviation.
</output>
