---
phase: 92-host-side-hygiene-focus-guards
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjFileVisibility.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjFileVisibilityTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjStatusBarWidgetSourceGuardTest.java
autonomous: true
requirements: [RESP-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both IntelliJ status-bar widgets subscribe `FileEditorManagerListener.FILE_EDITOR_MANAGER` on their existing `messageBusConnection` and call `updateVisibility()` from `selectionChanged`. The server-status subscription stays, and `dispose()` still disconnects the single connection (D-11)."
    - "Widget visibility is decided by the selected files' resolved file type name, never by extension. Both widgets delegate to one shared `BbjFileVisibility.showsForSelection` decision (D-12)."
    - "The shared decision shows the widgets for the `BBj` file type, which is registered for `bbj;bbjt;src;bbx` so an ordinary `.bbx` program shows them. It hides them for `BBx Config` (`config.bbx`, `config.min` and a custom-named config file via the overrider), for types that are not BBj such as `PLAIN_TEXT`/`UNKNOWN` (`.bbl`), and for `JAVA` or `JSON` (D-12)."
    - "Plain JUnit covers the decision without a live IntelliJ Application, and a source guard proves both widgets are wired to it (D-13)."
    - "With no selected file the widgets hide. With several selected files (split editors) they show when at least one selected file has the `BBj` type. A null or blank type name counts as not BBj (RESP-09 unclassified edge)."
  prohibitions:
    - statement: "MUST NOT treat the BBj config file (config.bbx, config.min, or a custom configured name) as a BBj program because its extension is bbx."
      status: resolved
      verification: test
  artifacts:
    - path: bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjFileVisibility.java
      provides: "shared static visibility decision over file type names plus a thin VirtualFile[] wrapper"
      contains: "static boolean showsForSelection"
    - path: bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjFileVisibilityTest.java
      provides: "plain-JUnit cases for the decision and name-drift guards against BbjFileType/plugin.xml"
      contains: "BBx Config"
    - path: bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjStatusBarWidgetSourceGuardTest.java
      provides: "source guard over both widget sources"
      contains: "FILE_EDITOR_MANAGER"
  key_links:
    - from: bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java
      to: bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjFileVisibility.java
      via: "updateVisibility() → BbjFileVisibility.showsForSelection(...)"
      pattern: "BbjFileVisibility\\.showsForSelection\\("
    - from: bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java
      to: bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjFileVisibility.java
      via: "updateVisibility() → BbjFileVisibility.showsForSelection(...)"
      pattern: "BbjFileVisibility\\.showsForSelection\\("
---

<objective>
RESP-09 (#610). Both IntelliJ status-bar widgets show or hide on a bare editor-tab switch (D-11). The file type decides visibility, so the config file hides them whatever its name (D-12). Plain JUnit plus a source guard prove it (D-13).

Purpose: today the widgets re-evaluate only when the server status changes, and they use a hard-coded extension list that counts `.bbl`, misses `.bbx` programs, and would wrongly count `config.bbx` if `bbx` were added.
Output: one new package-private helper class, two thin widget changes, two new JUnit test classes.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-PATTERNS.md

<!-- planner-discipline-allow: getExtension( -->

<interfaces>
Verified during planning (base commit `3ec25f02`):

- `ui/BbjStatusBarWidget.java`:
  - constructor lines 57-62: `messageBusConnection = project.getMessageBus().connect();` then one `subscribe(BbjServerService.BbjServerStatusListener.TOPIC, this::updateStatus)`;
  - `updateStatus` wraps its work in `invokeLater` and calls `updateVisibility()`;
  - `updateVisibility()` (107-118) loops `FileEditorManager.getInstance(project).getSelectedFiles()` over a hard-coded extension list;
  - `dispose()` (166-170) disconnects the connection.
- `ui/BbjJavaInteropStatusBarWidget.java` is the same shape: subscribe at 55-59, `updateVisibility()` at 97-108, `dispose()` at 146-150.
- `plugin.xml` lines 170-188:
  - `<fileType name="BBj" … extensions="bbj;bbjt;src;bbx"/>`;
  - `<fileType name="BBx Config" … fileNamesCaseInsensitive="config.bbx;config.min"/>`;
  - `<fileTypeOverrider implementation="com.basis.bbj.intellij.config.BbjConfigFileTypeOverrider"/>`, which returns `BbjConfigFileType.INSTANCE` for the active or default-named config file.
- `BbjFileType.getName()` returns `"BBj"`; `BbjConfigFileType.getName()` returns `"BBx Config"`. No test in this repo constructs a `FileType` singleton. Keep it that way: the decision takes plain strings.
- Precedent for static helpers called from plain JUnit on a class that imports `VirtualFile`: `ConfigPathsTest` calls `BbjConfigPathService`'s static helpers.
- `FileEditorManagerListener.selectionChanged` runs on the EDT, so the handler calls `updateVisibility()` directly with no `invokeLater`.
- Source-guard idiom to copy, `readSource`/`countOccurrences` with paths relative to the Gradle project dir: `bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeOverriderSourceGuardTest.java`.
- `lsp/Lsp4ijImportAllowlistTest` allows `ServerStatus` in `BbjStatusBarWidget.java`; the new imports are platform `fileEditor` types, not lsp4ij.
- Grounded test command: `/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij test --tests '<FQCN>' --offline --console=plain -q` exited 0 during planning.

Shell rules: absolute paths only; run Gradle through the wrapper's absolute path with `-p`, never `cd`; `git -C /home/coder/repos/bbj-language-server …`; stop any competing Gradle run first; stage exact paths only.
Register rule: no plan, decision, threat or pitfall ids in source or test text (issue numbers such as `#610` are fine).
Deviations: record them in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- New class `com.basis.bbj.intellij.ui.BbjFileVisibility`, package-private and final, with a private constructor. Members:
  - `static final String BBJ_FILE_TYPE_NAME`
  - `static boolean isBbjProgramFileTypeName(@Nullable String fileTypeName)`
  - `static boolean showsForFileTypeNames(@NotNull List<String> fileTypeNames)`
  - `static boolean showsForSelection(@NotNull VirtualFile[] selectedFiles)`
- New test class `com.basis.bbj.intellij.ui.BbjFileVisibilityTest`.
- New test class `com.basis.bbj.intellij.ui.BbjStatusBarWidgetSourceGuardTest`.
- In each widget, a new anonymous `FileEditorManagerListener` subscription.

## Flagged assumptions

- A `.bbl` file resolves to a platform type that is not BBj (`PLAIN_TEXT` or `UNKNOWN`), because `plugin.xml` never registers `bbl`. The test pins the `plugin.xml` extension list, not the platform's fallback name.
- The one live confirmation (bare tab switch, `config.bbx` hides) is the human check in plan 92-06.

## Edge coverage (spec-less probe)

- **RESP-09 unclassified:** resolved (explicit). See the empty/multiple-selection and null/blank-name truth; Task 1 tests it.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The language-server widget shows for a BBj program tab and hides for config and non-BBj tabs on a bare selection change, through the shared file-type decision</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjFileVisibility.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjFileVisibilityTest.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java — whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjFileType.java and BbjConfigFileType.java — the `getName()` literals
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml — lines 168-190
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeOverriderSourceGuardTest.java — the readSource/countOccurrences idiom
  </read_first>
  <behavior>
    - isBbjProgramFileTypeName("BBj") is true (a .bbj/.bbx/.src/.bbjt program)
    - isBbjProgramFileTypeName is false for "BBx Config" (config.bbx, config.min, custom-named config), "PLAIN_TEXT" and "UNKNOWN" (.bbl), "JAVA", "JSON", "bbj" (exact name), "" and null
    - showsForFileTypeNames(List.of()) is false; List.of("PLAIN_TEXT", "BBj") is true; List.of("BBx Config") is false; List.of("BBx Config", "JAVA") is false
    - drift guard: BBJ_FILE_TYPE_NAME equals "BBj"; BbjFileType.java source contains `return "BBj";`; BbjConfigFileType.java source contains `return "BBx Config";`; plugin.xml's BBj fileType entry carries `extensions="bbj;bbjt;src;bbx"` (includes bbx, excludes bbl)
  </behavior>
  <action>
  1. RED. Create `bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjFileVisibilityTest.java`: package-private JUnit 5 class covering the behaviours above. Read sources and `plugin.xml` as text with `Paths.get("src", "main", …).toAbsolutePath()` and `Files.readString`, as the config source-guard test does. Each case's assertion message names the file it stands for. Confirm the test fails to compile or fails.
  2. GREEN. Create `BbjFileVisibility.java` in package `com.basis.bbj.intellij.ui`:
     - `final class` with a private constructor;
     - class Javadoc: visibility follows the resolved file type, not the extension, because the overrider gives the config file its own type whatever its name, and `.bbl` has no BBj type (issue #610).
     - Members:
       - `static final String BBJ_FILE_TYPE_NAME = "BBj";`
       - `static boolean isBbjProgramFileTypeName(@Nullable String fileTypeName)` returns `BBJ_FILE_TYPE_NAME.equals(fileTypeName)`
       - `static boolean showsForFileTypeNames(@NotNull List<String> fileTypeNames)` returns true when any element passes
       - `static boolean showsForSelection(@NotNull VirtualFile[] selectedFiles)` maps each file to `file.getFileType().getName()` and delegates to `showsForFileTypeNames`
  3. Wire `BbjStatusBarWidget.java`:
     - Replace the body of `updateVisibility()` with the single statement `panel.setVisible(BbjFileVisibility.showsForSelection(FileEditorManager.getInstance(project).getSelectedFiles()));`, which removes the hard-coded extension comparison loop.
     - After the existing server-status `subscribe`, add a second `messageBusConnection.subscribe(FileEditorManagerListener.FILE_EDITOR_MANAGER, new FileEditorManagerListener() { … })` whose `selectionChanged(@NotNull FileEditorManagerEvent event)` calls `updateVisibility()` directly (EDT already).
     - Import `com.intellij.openapi.fileEditor.FileEditorManagerListener` and `com.intellij.openapi.fileEditor.FileEditorManagerEvent`; drop the `VirtualFile` import if unused.
     - Leave `dispose()` unchanged.
  4. Run the test class and confirm it passes.
  </action>
  <verify>
    <automated>/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij test --tests 'com.basis.bbj.intellij.ui.BbjFileVisibilityTest' --offline --console=plain -q</automated>
    <fails_when>non-zero exit, "No tests found for given includes", or any "FAILED" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getExtension(' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java` prints 0.
    - `grep -c 'FileEditorManagerListener.FILE_EDITOR_MANAGER' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java` prints 1.
    - The Gradle run exits 0 with every `BbjFileVisibilityTest` case passing.
  </acceptance_criteria>
  <done>The language-server widget re-evaluates on every editor selection change, and the file-type decision that drives it is unit-tested, including `config.bbx` and `.bbl`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The java-interop widget follows tab switches the same way, and a source guard pins both widgets to the shared decision</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/ui/BbjStatusBarWidgetSourceGuardTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java — whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java — the Task 1 wiring to mirror
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/lsp/Lsp4ijImportAllowlistTest.java — lines 50-65, confirm the widget allowlist entry
  </read_first>
  <behavior>
    - For each of BbjStatusBarWidget.java and BbjJavaInteropStatusBarWidget.java, count occurrences of:
      - `FileEditorManagerListener.FILE_EDITOR_MANAGER`: 1
      - `messageBusConnection.subscribe(`: 2
      - `selectionChanged(`: 1
      - `BbjFileVisibility.showsForSelection(`: 1
      - `getExtension(`: 0
      - `"bbl"`: 0
      - `messageBusConnection.disconnect()`: 1
    - For each widget, the text between `selectionChanged(` and the next `}` contains `updateVisibility()`.
    - BbjFileVisibility.java contains `getFileType()` once and no `getExtension(`.
  </behavior>
  <action>
  1. RED. Create `BbjStatusBarWidgetSourceGuardTest.java` in package `com.basis.bbj.intellij.ui` (test sources) with the counts above, one `@Test` per property, and assertion messages that say which wiring broke. Confirm the interop widget assertions fail.
  2. GREEN. Apply to `BbjJavaInteropStatusBarWidget.java` the same two changes Task 1 made to `BbjStatusBarWidget.java`:
     - the single-statement `updateVisibility()` delegating to `BbjFileVisibility.showsForSelection(...)`, removing the hard-coded extension comparison loop;
     - a second `subscribe` on the existing `messageBusConnection` for `FileEditorManagerListener.FILE_EDITOR_MANAGER` whose `selectionChanged` calls `updateVisibility()`.

     Keep the java-interop status subscription and `dispose()` unchanged.
  3. Run both new test classes plus the lsp4ij import allowlist test. Then run the register check.
  </action>
  <verify>
    <automated>/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij test --tests 'com.basis.bbj.intellij.ui.BbjFileVisibilityTest' --tests 'com.basis.bbj.intellij.ui.BbjStatusBarWidgetSourceGuardTest' --tests 'com.basis.bbj.intellij.lsp.Lsp4ijImportAllowlistTest' --offline --console=plain -q</automated>
    <fails_when>non-zero exit, "No tests found for given includes", or any "FAILED" line</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 3ec25f02 -- bbj-intellij/src | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getExtension(' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java` prints 0.
    - `grep -c 'BbjFileVisibility.showsForSelection(' /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjJavaInteropStatusBarWidget.java` prints 1.
    - The Gradle run exits 0 with all three test classes passing; the register check prints nothing.
  </acceptance_criteria>
  <done>Both widgets follow bare tab switches through one tested file-type decision, and the wiring is fenced against a regression to extension lists or a lost subscription.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ file-type resolution → status-bar UI | a UI-only visibility decision; no data or process crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-07 | Denial of Service | `FILE_EDITOR_MANAGER` subscription leak after widget disposal | low | mitigate | subscribe on the existing `messageBusConnection`; source guard asserts exactly one `disconnect()` remains in `dispose()` |
| T-92-08 | Spoofing | config file shown as a BBj program (misleading server status) | low | mitigate | decision by resolved file type name `BBj`; JUnit case for `BBx Config` plus drift guards on `BbjFileType`/`plugin.xml` |
| T-92-SC | Tampering | Gradle/npm dependencies | low | accept | no dependency is added |
</threat_model>

<verification>
1. `BbjFileVisibilityTest`, `BbjStatusBarWidgetSourceGuardTest` and `Lsp4ijImportAllowlistTest` pass through the grounded Gradle command.
2. Neither widget source contains an extension comparison; both subscribe `FILE_EDITOR_MANAGER` exactly once.
3. The register check over `bbj-intellij/src` prints nothing.
</verification>

<success_criteria>
- ROADMAP criterion 5 is backed by automated tests. Plan 92-06 stages the live tab-switch confirmation.
- D-11, D-12 and D-13's automated half are implemented exactly.
</success_criteria>

<output>
Create `.planning/phases/92-host-side-hygiene-focus-guards/92-03-SUMMARY.md` when done. Record the JUnit test counts and any deviation.
</output>
