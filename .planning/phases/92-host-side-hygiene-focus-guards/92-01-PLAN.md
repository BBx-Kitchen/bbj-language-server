---
phase: 92-host-side-hygiene-focus-guards
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/src/Commands/target-resolution.ts
  - bbj-vscode/test/target-resolution.test.ts
  - bbj-vscode/src/Commands/Commands.cjs
  - bbj-vscode/src/extension.ts
autonomous: true
requirements: [RESP-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Run, Run BUI, Run DWC, Compile, Denumber, Decompile (Replace) and Decompile (Read-only), invoked with no argument and no active editor, show one shared warning `No active BBj file. Open or select a BBj file and try again.` and do not throw (D-07)."
    - "A passed file argument wins over the active editor for all seven commands, so right-clicking file B in the Explorer while A is focused targets B (D-05)."
    - "For Run, Run BUI, Run DWC, Compile and Denumber, the active-editor fallback accepts only a document with language id exactly `bbj` whose extension is not `.bbjt`; a focused `.txt`, `settings.json` or `bbx-config` document gets the warning. No `bbx` language id is accepted, because `package.json` declares none (D-06)."
    - "A passed argument is used as-is, with no language check (D-06)."
    - "The two Decompile commands accept an active editor with language id exactly `bbj`, including `.bbjt`, and otherwise show the same warning instead of returning silently (D-07)."
    - "An absent `params`, a `params` without `fsPath`, and an empty-string `fsPath` are all treated as no argument (empty-input edge)."
    - "The language and extension checks are exact, case-sensitive string comparisons that mirror the menus' `resourceLangId` and `resourceExtname` context keys (encoding edge)."
    - "Run BUI and Run DWC show the warning before any EM credential prompt. They resolve the target once, before `ensureValidToken`, and pass that resolved path on, so a focus change during the prompt cannot retarget the run (concurrency edge)."
    - "With AutoSaveUponRun on, Run saves the active editor only when it is the run target."
  prohibitions:
    - statement: "MUST NOT hand a file the user did not target to bbj, bbjcpl or bbjlst: never a focused non-BBj document, and never the active editor when an explicit file argument was passed."
      status: resolved
      verification: test
    - statement: "MUST NOT prompt for Enterprise Manager credentials when there is no BBj file to run."
      status: resolved
      verification: test
  artifacts:
    - path: bbj-vscode/src/Commands/target-resolution.ts
      provides: "vscode-free target resolution: NO_ACTIVE_BBJ_FILE_MESSAGE, toActiveEditorSnapshot, resolveRunTarget, resolveDecompileTarget"
      contains: "export function resolveRunTarget"
    - path: bbj-vscode/test/target-resolution.test.ts
      provides: "unit tests for the pure module plus source guards over Commands.cjs and extension.ts wiring"
      contains: "resolveDecompileTarget"
    - path: bbj-vscode/src/Commands/Commands.cjs
      provides: "runTargetOrWarn / decompileTargetOrWarn used by run, runWeb, compile, decompile, decompileReplace, decompileReadonly"
      contains: "runTargetOrWarn("
    - path: bbj-vscode/src/extension.ts
      provides: "bbj.runBUI / bbj.runDWC resolve the target before ensureValidToken"
      contains: "resolveRunTarget("
  key_links:
    - from: bbj-vscode/src/Commands/Commands.cjs
      to: bbj-vscode/src/Commands/target-resolution.ts
      via: "require(\"./target-resolution\")"
      pattern: "require\\(\"\\./target-resolution\"\\)"
    - from: bbj-vscode/src/extension.ts
      to: bbj-vscode/src/Commands/target-resolution.ts
      via: "import from './Commands/target-resolution.js'"
      pattern: "from './Commands/target-resolution.js'"
---

<objective>
RESP-07 (#512). The seven BBj run, compile and decompile commands stop throwing when nothing is focused and stop acting on the wrong file. Every command resolves its target argument-first (D-05). The active-editor fallback accepts only what the command's own menu would accept (D-06). Any other case gets one shared warning (D-07).

Purpose: Alt+G / Alt+C / Alt+N and the palette currently dereference `params.fsPath` on `undefined` and throw.
Output: a new vscode-free `target-resolution.ts` with unit tests; `Commands.cjs` and the two web-run handlers in `extension.ts` route through it; source guards prove the wiring, because `Commands.cjs` cannot load under vitest.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-RESEARCH.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-PATTERNS.md

<!-- planner-discipline-allow: active.document.fileName : params.fsPath -->

<interfaces>
Verified during planning (base commit `3ec25f02`):

- `Commands.cjs` is CommonJS. vitest cannot load it (see `bbj-vscode/test/no-shell-command-construction.test.ts` header), so its wiring is proven by source scans over extracted function bodies. `bbj-vscode/test/config-path-consumers.test.ts` lines 120-138 hold the `extractBraceBlock(source, marker)` helper to copy.
- `Commands.cjs` requires sibling TypeScript modules without an extension, e.g. `require("./process-args")` (esbuild resolves `.ts`). `process-args.ts` lines 31-33 carry the "No `vscode` import here, intentionally" convention to copy.
- Current target lookups in `Commands.cjs`:
  - `runWeb` (line 91-92), `run` (281-282), `compile` (336-337) and `decompile` (161-162) each use an editor-first ternary over `vscode.window.activeTextEditor` and `params.fsPath`.
  - `decompileReplace` and `decompileReadonly` use `resolveTargetFileName(params)` (lines 150-156), which is argument-first and returns silently when nothing is found.
  - `denumber` calls `decompile(params, { denumber: true })`.
- `run` saves the active document when `webConfig.AutoSaveUponRun && active` (line 317).
- `package.json` `contributes.languages` declares only `bbj` (extensions `.bbj .bbjt .src .bbx .bbl`) and `bbx-config`. The run/runBUI/runDWC/compile/denumber menus use `(resourceLangId == bbj && resourceExtname != .bbjt) || resourceLangId == bbx`; the `bbx` term is dead. The decompile commands have no menu entries: they are invoked from the palette or from `maybePromptTokenized`, which passes a uri.
- In `extension.ts` (lines 786-797), `bbj.runBUI` and `bbj.runDWC` call `await ensureValidToken(context)` (which can open the EM login prompt) and then `Commands.runBUI(params, creds)` / `Commands.runDWC(params, creds)`. `runWeb` only reads `params.fsPath`.
- `test/config-path-consumers.test.ts` asserts that the bodies of `run: function` and `const runWeb = ` still contain `stripSentinel(getActiveConfigPath())`; keep that call.
- `extension-activation.test.ts`, `config-reload-host.test.ts` and `config-file-association.test.ts` import `extension.ts` with `Commands.cjs` mocked; the new module is real and vscode-free, so it needs no mock.

Shell rules: absolute paths only; `git -C /home/coder/repos/bbj-language-server …`; never chain `cd` with grep/find/cat/sed/head/tail/git; stage exact paths only.
Register rule: no plan, decision, threat or pitfall ids in source or test text (issue numbers such as `#512` are fine).
Deviations: record them in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- New file `bbj-vscode/src/Commands/target-resolution.ts` with exports:
  - `NO_ACTIVE_BBJ_FILE_MESSAGE`
  - `interface ActiveEditorSnapshot`
  - `toActiveEditorSnapshot`
  - `isRunnableBbjDocument`
  - `resolveRunTarget`
  - `resolveDecompileTarget`
- New file `bbj-vscode/test/target-resolution.test.ts`.
- New module-private helpers in `Commands.cjs`: `runTargetOrWarn`, `decompileTargetOrWarn`. The old argument-then-editor helper there is removed.

## Flagged assumptions

- VS Code's `resourceExtname` context key is taken to be the extension exactly as written, not lower-cased. `prog.BBJT` therefore passes the run check, just as the menu shows Run for it. If VS Code lower-cases the key, the mirror is still exact for every lower-case file name.
- `.bbl` files carry language id `bbj` in VS Code, so the active-editor fallback accepts them for Run and Compile, exactly as the menus already do. No behaviour change is intended there.

## Edge coverage (spec-less probe)

- **RESP-07 empty:** resolved (explicit). See the empty-input truth; Task 1 tests it.
- **RESP-07 encoding:** resolved (explicit). See the case-sensitive truth; Task 1 tests it.
- **RESP-07 concurrency:** resolved (explicit). See the resolve-once-before-the-prompt truth; Task 2's source guard tests it.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile with nothing focused shows the shared warning, end to end through the new pure resolver</name>
  <files>bbj-vscode/src/Commands/target-resolution.ts, bbj-vscode/test/target-resolution.test.ts, bbj-vscode/src/Commands/Commands.cjs</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs — the whole file; the command bodies and the old resolver at lines 150-156
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/process-args.ts — lines 1-41, the module-header convention
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/config-path-consumers.test.ts — lines 105-140, the `extractBraceBlock` source-guard helper
    - /home/coder/repos/bbj-language-server/bbj-vscode/package.json — lines 23-61 (languages) and 233-350 (menus)
  </read_first>
  <behavior>
    - resolveRunTarget(undefined, undefined) and resolveDecompileTarget(undefined, undefined) return undefined
    - an empty-string fsPath with no editor returns undefined
    - an argument wins over a different, valid BBj active editor, and is returned unchanged even for a `.txt` or `.bbjt` path
    - run fallback: `bbj` documents named `.bbj`, `.bbx`, `.src`, `.bbl` are accepted; a `bbj` document named `.bbjt` is rejected; `plaintext` `.txt`, `jsonc` `settings.json`, `bbx-config` `config.bbx` and language id `BBJ` are rejected
    - decompile fallback: a `bbj` document named `.bbjt` is accepted; `plaintext`, `jsonc` and `bbx-config` documents are rejected
    - toActiveEditorSnapshot(undefined) returns undefined; for `{ document: { fileName, languageId } }` it returns a plain `{ fileName, languageId }`
    - NO_ACTIVE_BBJ_FILE_MESSAGE equals `No active BBj file. Open or select a BBj file and try again.`
    - source guard: the `compile: function` body in Commands.cjs calls `runTargetOrWarn(params)` before `getBBjHome()`; the `runTargetOrWarn` helper body calls `resolveRunTarget(` and `showWarningMessage(NO_ACTIVE_BBJ_FILE_MESSAGE)`
  </behavior>
  <action>
  1. RED. Create `bbj-vscode/test/target-resolution.test.ts` with the behaviours above:
     - a `describe` for the pure module;
     - a `describe` of source guards over `Commands.cjs`. Read the file with `fs.readFileSync(path.join(__dirname, '..', 'src/Commands/Commands.cjs'), 'utf-8')` and copy the `extractBraceBlock` helper from `config-path-consumers.test.ts`.

     Run it and confirm it fails because the module does not exist.
  2. GREEN, pure module. Create `bbj-vscode/src/Commands/target-resolution.ts` with a TypeFox MIT header like `process-args.ts` and a module doc comment. The comment says no `vscode` import on purpose, and that the checks mirror the run/compile/denumber menus' `when` clause (issue #512). Import only `path` from Node. Export:
     - `const NO_ACTIVE_BBJ_FILE_MESSAGE = 'No active BBj file. Open or select a BBj file and try again.'`
     - `interface ActiveEditorSnapshot { fileName: string; languageId: string }`
     - `toActiveEditorSnapshot(editor: { document: { fileName: string; languageId: string } } | undefined): ActiveEditorSnapshot | undefined`
     - `isRunnableBbjDocument(doc: ActiveEditorSnapshot): boolean`: `doc.languageId === 'bbj'` and `path.extname(doc.fileName) !== '.bbjt'`
     - `resolveRunTarget(argFsPath: string | undefined, active: ActiveEditorSnapshot | undefined): string | undefined`. Return a non-empty `argFsPath` as-is. Otherwise return `active.fileName` when `active` passes `isRunnableBbjDocument`. Otherwise return undefined.
     - `resolveDecompileTarget(argFsPath, active)`: the same order, but the fallback only requires `active.languageId === 'bbj'`.
  3. GREEN, wiring. In `Commands.cjs`:
     - Add `const { NO_ACTIVE_BBJ_FILE_MESSAGE, toActiveEditorSnapshot, resolveRunTarget, resolveDecompileTarget } = require("./target-resolution");` beside the `process-args` require.
     - Add module-private `runTargetOrWarn(params)`. It computes `resolveRunTarget(params && params.fsPath, toActiveEditorSnapshot(vscode.window.activeTextEditor))`. When the result is undefined it calls `vscode.window.showWarningMessage(NO_ACTIVE_BBJ_FILE_MESSAGE)`. It returns the result.
     - Add `decompileTargetOrWarn(params)`, identical but using `resolveDecompileTarget`.
     - In `compile: function (params)`, replace the editor-first ternary with `const fileName = runTargetOrWarn(params); if (!fileName) return;` as the first statements, before `getBBjHome()`.

     Do not touch other commands in this task.
  4. Run the test and confirm it passes.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/target-resolution.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, a "No test files found" line, or a summary line reporting any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "from 'vscode'" /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/target-resolution.ts` prints nothing.
    - `grep -c 'runTargetOrWarn(params)' /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs` prints at least 1.
    - The vitest run reports every test in `test/target-resolution.test.ts` passed, including the case where an argument wins over an editor and the `.bbjt` run/decompile split.
  </acceptance_criteria>
  <done>Compile with no argument and no BBj editor shows the shared warning instead of throwing. The resolver is unit-tested and proven wired into `compile`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run, Run BUI, Run DWC, Denumber and both Decompile commands use the same resolution, and web runs warn before any credential prompt</name>
  <files>bbj-vscode/src/Commands/Commands.cjs, bbj-vscode/src/extension.ts, bbj-vscode/test/target-resolution.test.ts</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs — `runWeb` (62-142), `decompile` (158-164), `run` (274-322), `decompileReplace` / `decompileReadonly` (389-439)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts — imports (1-40) and the `bbj.runBUI` / `bbj.runDWC` handlers (783-797)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/config-path-consumers.test.ts — lines 181-210, the run/runWeb body assertions that must keep passing
  </read_first>
  <behavior>
    - source guard: the bodies of `run: function`, `const runWeb = ` and `const decompile = ` call `runTargetOrWarn(params)`; the bodies of `decompileReplace: function` and `decompileReadonly: function` call `decompileTargetOrWarn(params)`; in each body that calls `getBBjHome()`, the resolver call comes first
    - source guard: Commands.cjs contains no editor-first ternary of the form `active ? active.document.fileName : params.fsPath`, and no `resolveTargetFileName`
    - source guard: `decompileTargetOrWarn`'s body calls `resolveDecompileTarget(` and `showWarningMessage(NO_ACTIVE_BBJ_FILE_MESSAGE)`
    - source guard: in extension.ts, inside the `"bbj.runBUI"` handler and inside the `"bbj.runDWC"` handler, `resolveRunTarget(` appears before `ensureValidToken(`, `showWarningMessage(NO_ACTIVE_BBJ_FILE_MESSAGE)` is present, and the Commands call passes `{ fsPath: target }`
    - source guard: the `run: function` body's auto-save condition compares `active.document.fileName === fileName`
  </behavior>
  <action>
  1. RED. Add the source guards above to `test/target-resolution.test.ts`:
     - one `describe` for `Commands.cjs`;
     - one `describe` for `extension.ts`, read via `path.join(__dirname, '..', 'src/extension.ts')`. Slice each handler from its `"bbj.runBUI"` / `"bbj.runDWC"` marker with the same brace extractor.

     Confirm the new guards fail.
  2. `Commands.cjs`:
     - `run`: replace the editor-first ternary with `const fileName = runTargetOrWarn(params); if (!fileName) return;` before `getBBjHome()`. Keep `const active = vscode.window.activeTextEditor;`, used only for auto-save, and change the auto-save condition to `webConfig.AutoSaveUponRun && active && active.document.fileName === fileName`.
     - `runWeb(params, client, credentials)`: move target resolution to the top, before `getBBjHome()`, via `runTargetOrWarn(params)` with the early return; remove the editor-first ternary. Keep `stripSentinel(getActiveConfigPath())`.
     - `decompile(params, options)`: `const fileName = runTargetOrWarn(params); if (!fileName) return;` before `getBBjHome()`. Denumber shares the run menu's `when` clause.
     - `decompileReplace` and `decompileReadonly`: replace the old silent lookup with `decompileTargetOrWarn(params)` and an early return. In `decompileReadonly` the resolution comes before `getBBjHome()`.
     - Delete the now-unused old resolver function and its doc comment.
  3. `extension.ts`: add `import { NO_ACTIVE_BBJ_FILE_MESSAGE, resolveRunTarget, toActiveEditorSnapshot } from './Commands/target-resolution.js';`. In both the `bbj.runBUI` and `bbj.runDWC` handlers, as the first statements:
     - `const target = resolveRunTarget(params?.fsPath, toActiveEditorSnapshot(vscode.window.activeTextEditor));`
     - when `!target`, call `vscode.window.showWarningMessage(NO_ACTIVE_BBJ_FILE_MESSAGE)` and return;
     - then the existing `ensureValidToken` block;
     - then `Commands.runBUI({ fsPath: target }, creds)` / `Commands.runDWC({ fsPath: target }, creds)`.

     Do not wrap any registration in `context.subscriptions.push` here; a later plan owns that.
  4. Run the targeted suites below; all must pass. Then build and lint.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/target-resolution.test.ts test/config-path-consumers.test.ts test/no-shell-command-construction.test.ts test/em-secret-env-channel.test.ts test/extension-activation.test.ts test/config-reload-host.test.ts test/config-file-association.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 3ec25f02 -- bbj-vscode/src/Commands bbj-vscode/src/extension.ts bbj-vscode/test/target-resolution.test.ts | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'active.document.fileName : params.fsPath' /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs` prints 0.
    - `grep -c 'decompileTargetOrWarn(params)' /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs` prints 2.
    - `grep -c 'runTargetOrWarn(params)' /home/coder/repos/bbj-language-server/bbj-vscode/src/Commands/Commands.cjs` prints 4 (run, runWeb, compile, decompile).
    - `grep -c 'fsPath: target' /home/coder/repos/bbj-language-server/bbj-vscode/src/extension.ts` prints 2.
    - The targeted vitest run, build, lint and register check all meet their pass conditions.
  </acceptance_criteria>
  <done>No command of the seven throws or silently returns on a missing target. All of them prefer the passed file, reject non-BBj editors per their menu rule, and web runs never prompt for credentials without a file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor/menu state → process launch | the command decides which file path is handed to bbj, bbjcpl or bbjlst |
| command argument → process launch | `params.fsPath` can come from another extension's `executeCommand` call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-01 | Tampering | `run`/`compile`/`decompile` target choice | medium | mitigate | argument-first resolution plus an exact `bbj`/non-`.bbjt` fallback check in `resolveRunTarget`, unit-tested; a focused non-BBj document is never launched |
| T-92-02 | Denial of Service | seven command handlers on an undefined `params` | low | mitigate | every handler returns after the shared warning; source guards pin the resolver call in each body |
| T-92-03 | Information Disclosure | EM credential prompt in `bbj.runBUI` / `bbj.runDWC` | low | mitigate | target resolved before `ensureValidToken`, source-guarded, so no credential prompt appears without a file |
| T-92-04 | Tampering | passed `fsPath` used without a language check | low | accept | unchanged trust: argv is still built by `process-args.ts` and launched via `execFile` with no shell; the menus already filter the argument |
| T-92-SC | Tampering | npm installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. `test/target-resolution.test.ts` passes: pure-module cases plus the source guards for all seven commands and both web-run handlers.
2. The pre-existing Commands.cjs and extension.ts guard suites (`config-path-consumers`, `no-shell-command-construction`, `em-secret-env-channel`) and the three activation harnesses still pass.
3. Build and lint pass; the register check prints nothing.
</verification>

<success_criteria>
- ROADMAP criterion 3: invoking Run, Compile, Decompile or Denumber with no active BBj editor shows a graceful "no active BBj file" message instead of an error.
- D-05, D-06 and D-07 are implemented exactly and each is backed by a test.
</success_criteria>

<output>
Create `.planning/phases/92-host-side-hygiene-focus-guards/92-01-SUMMARY.md` when done. Record the final warning text, the test count, and any deviation.
</output>
