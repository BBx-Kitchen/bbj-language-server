---
phase: 92-host-side-hygiene-focus-guards
plan: "06"
type: execute
wave: 3
depends_on: ["92-01", "92-02", "92-03", "92-04", "92-05"]
files_modified: []
autonomous: true
requirements: [RESP-05, RESP-06, RESP-07, RESP-08, RESP-09]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The VS Code VSIX, rebuilt from the final tree, carries this phase's host code. Its `extension/out/extension.cjs` contains the shared warning `No active BBj file. Open or select a BBj file and try again.` (D-07)."
    - "The IntelliJ plugin zip, rebuilt with `clean buildPlugin`, carries this phase's widget code: its plugin jar lists `com/basis/bbj/intellij/ui/BbjFileVisibility.class`. The build ran the whole IntelliJ JUnit suite green, because `buildPlugin` depends on `test` (D-13)."
    - "The whole plain vitest suite with `RUN_BBJ_TESTS=0` reports zero failed tests, and lint passes."
    - "No planning register id appears in any source or test line added since `3ec25f02`, and no file under `bbj-vscode/src/language` changed in this phase."
    - statement: "In IntelliJ, with the rebuilt plugin installed and the language server started, a bare tab switch with no server-status change makes both status-bar widgets follow at once. BBj tab: shown. Non-BBj tab: hidden. `config.bbx`: hidden. A `.bbx` program tab: shown. BBj tab again: shown (D-11, D-12, D-13)."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT change any file under bbj-vscode/src, bbj-vscode/test or bbj-intellij/src in this plan; it proves and stages, and plans 92-01 through 92-05 own every behaviour."
      status: resolved
      verification: test
  artifacts: []
  key_links: []
---

<objective>
Close Phase 92 for RESP-05 through RESP-09:
- rebuild both distributables from the final tree and prove each carries this phase's code;
- run the whole-suite, lint and register gates;
- stage D-13's one live IntelliJ tab-switch check.

Purpose: the phase is done only when the artifacts a tester installs carry every fix, and a human confirms the one behaviour that needs a running IDE.
Output: build evidence in the SUMMARY and one harvested human check.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-VALIDATION.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-01-SUMMARY.md
@.planning/phases/92-host-side-hygiene-focus-guards/92-03-SUMMARY.md

<!-- planner-discipline-allow: RUN_BBJ_TESTS -->

<interfaces>
Verified during planning:

- Build and install:
  - `bbj-ext-install` (`/usr/local/bin/bbj-ext-install`) packages `/tmp/bbj-lang.vsix` and installs it into VS Code (ext test), printing `Extension installed`.
  - The VSIX host bundle entry is `extension/out/extension.cjs` (package.json `"main": "./out/extension.cjs"`).
  - `/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --offline --console=plain -q` produces `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`, whose plugin jar entry is `bbj-intellij/lib/bbj-intellij-0.1.0.jar`.
  - `buildPlugin` depends on `test`.
  - A Swing stack trace during `buildSearchableOptions` is headless noise when the exit code is 0.
- A jar's central directory stores entry names uncompressed. During planning, `unzip -p <zip> 'bbj-intellij/lib/bbj-intellij-0.1.0.jar' | grep -a -c 'com/basis/bbj/intellij/ui/BbjStatusBarWidget.class'` printed 2 against the existing zip.
- Phase base commit: `3ec25f02`.
- Whole-suite gate: judge on the `Tests` summary line only. "Test Files … failed" with zero failed tests is `beforeAll` hook-timeout contention; rerun once with `--maxWorkers=2`.
- The human check lives in a `<verify><human-check>` block and is harvested into UAT. VS Code live checks are not locked for this phase: #531 cannot be reproduced by a window reload, and the decompile and format races are proven by automated tests.

Shell rules:
- Absolute paths only.
- Use `git -C /home/coder/repos/bbj-language-server …`.
- Never chain `cd` with grep/find/cat/sed/head/tail/git.
- Run Gradle through the wrapper's absolute path with `-p`.
- Stop any competing Gradle or vitest run before rebuilding.
- Stage exact paths only.

Deviations: record them in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

None. No symbol, source file or tracked file is created. The plan produces build outputs (`/tmp/bbj-lang.vsix`, `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`) and SUMMARY evidence.

## Flagged assumptions

- The optional VS Code live checks CONTEXT lists as suggestions are left out to keep the phase lean:
  - Alt+G / Compile with nothing focused;
  - Explorer right-click Run on a file other than the focused one.

  Plan 92-01's automated tests cover both behaviours. The user can still try them in UAT.

<tasks>

<task type="tracer">
  <name>Task 1: The rebuilt VSIX and IntelliJ zip each carry this phase's code end to end</name>
  <files>none (build outputs only; no tracked file changes)</files>
  <precondition>`bbj-ext-install` is on PATH, and the commits of plans 92-01 through 92-05 are on the current branch</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/92-host-side-hygiene-focus-guards/92-01-SUMMARY.md — the shipped warning text
    - /home/coder/repos/bbj-language-server/.planning/phases/92-host-side-hygiene-focus-guards/92-03-SUMMARY.md — the shipped helper class name
  </read_first>
  <action>
  Record `BASE`, the output of `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`. If either SUMMARY shows a different warning text or class name, use the shipped one in the marker checks and note it.

  Then run, in order:
  1. The VS Code build.
  2. `bbj-ext-install`.
  3. The VSIX marker check.
  4. The IntelliJ `clean buildPlugin`.
  5. The zip marker check.
  6. `sha256sum /tmp/bbj-lang.vsix /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`.

  Record in the SUMMARY: `BASE`, both digests, both marker counts, and the IntelliJ test totals if Gradle printed them. Change no tracked file.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
    <automated>bbj-ext-install</automated>
    <fails_when>non-zero exit, or no "Extension installed" line in the output</fails_when>
    <automated>unzip -p /tmp/bbj-lang.vsix extension/out/extension.cjs | grep -F -c "No active BBj file. Open or select a BBj file and try again."</automated>
    <fails_when>non-zero exit, or a printed count of 0 (the VSIX does not carry the rebuilt host code)</fails_when>
    <automated>/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --offline --console=plain -q</automated>
    <fails_when>non-zero exit (includes any failing IntelliJ JUnit test, since buildPlugin depends on test)</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip 'bbj-intellij/lib/bbj-intellij-0.1.0.jar' | grep -a -c 'com/basis/bbj/intellij/ui/BbjFileVisibility.class'</automated>
    <fails_when>non-zero exit, or a printed count of 0 (the plugin zip does not carry the widget fix)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both marker checks print a count of at least 1.
    - The SUMMARY records `BASE`, both sha256 digests and both marker counts.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src bbj-vscode/test bbj-intellij/src` prints nothing.
  </acceptance_criteria>
  <done>A tester installing either artifact gets every Phase 92 fix, and the IntelliJ JUnit suite passed as part of building the zip.</done>
</task>

<task type="auto">
  <name>Task 2: The whole suite, lint and register gates are green across the phase, and the IntelliJ tab-switch check is staged against the rebuilt zip</name>
  <files>none (verification only; no tracked file changes)</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/92-host-side-hygiene-focus-guards/92-VALIDATION.md — the manual-only verification row
    - /home/coder/repos/bbj-language-server/.planning/phases/92-host-side-hygiene-focus-guards/92-CONTEXT.md — D-13 and the Specific Ideas section
  </read_first>
  <action>
  1. Run the whole plain vitest suite with the interop-gated suites off. Judge it on failed tests only.
     - If a test fails, stop and report it in the SUMMARY; do not fix code in this plan.
     - A hook-timeout "failed suite" with zero failed tests is contention: rerun once and note it.
  2. Run lint.
  3. Run the phase-wide register check.
  4. Run the language-server boundary check.
  5. The human check below is harvested into UAT. It requires artifacts rebuilt from the final tree: after any code-review fixes land, both distributables are rebuilt before the check runs.
  </action>
  <verify>
    <automated>RUN_BBJ_TESTS=0 npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode test -- --maxWorkers=2</automated>
    <fails_when>the "Tests" summary line reports one or more failed tests ("Test Files … failed" with zero failed tests is beforeAll hook-timeout contention and does not count)</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run lint</automated>
    <fails_when>non-zero exit, or any line reporting an error</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff 3ec25f02 -- bbj-vscode/src bbj-vscode/test bbj-intellij/src | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|\bG-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>any line printed (a planning register id was added to source or test text), or non-zero exit</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff 3ec25f02 --stat -- bbj-vscode/src/language</automated>
    <fails_when>any output line (a language-server file changed in this host-side phase)</fails_when>
    <human-check>
      <test>
First rebuild the IntelliJ plugin from the final tree, after any code-review fixes:
`/home/coder/repos/bbj-language-server/bbj-intellij/gradlew -p /home/coder/repos/bbj-language-server/bbj-intellij clean buildPlugin --console=plain -q` writes `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`. Install it in IntelliJ (Settings → Plugins → gear → Install Plugin from Disk) and restart the IDE.

Then, in a project containing a `.bbj` program, a `.bbx` program, a non-BBj file (e.g. `README.md` or a `.java` file) and `config.bbx`:
1. Open all four files as tabs. Wait until the BBj status widget reads `BBj: Ready`, and do not stop or restart the server during the steps below.
2. Click the `.bbj` tab.
3. Click the non-BBj tab.
4. Click the `config.bbx` tab.
5. Click the `.bbx` program tab.
6. Click the `.bbj` tab again.
      </test>
      <expected>
        - Step 2: both status-bar widgets (`BBj: …` and `Java: …`) are visible.
        - Step 3: both disappear immediately, with no server-status change.
        - Step 4: both stay hidden for `config.bbx` (the config file).
        - Step 5: both appear for the `.bbx` program.
        - Step 6: both are visible.
        - Each change happens on the tab click itself, not later.
      </expected>
      <why_human>Platform `FILE_EDITOR_MANAGER` delivery, `FileTypeOverrider` resolution for the real `config.bbx` and status-bar repaint need a running IntelliJ; the automated seam test and source guard prove the decision and the wiring only (D-13).</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The whole-suite `Tests` line reports 0 failed tests, and the SUMMARY records the passed and skipped counts.
    - Lint exits 0.
    - The register check and the language-server boundary check print nothing.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src bbj-vscode/test bbj-intellij/src` prints nothing.
    - The workflow harvests the human check into `92-UAT.md`.
  </acceptance_criteria>
  <done>Every automated gate for RESP-05..09 is green on the final tree, and the one live check D-13 requires is staged against a plugin rebuilt from that tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → phase closure claim | unit tests alone cannot show that the installed artifacts carry the fixes |
| rebuilt artifact → tester install | a stale VSIX or zip would make UAT test old code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-15 | Repudiation | phase closed while the installed bundles still ship old code | medium | mitigate | marker checks inside the VSIX host bundle and the plugin jar; sha256 digests and `BASE` in the SUMMARY |
| T-92-16 | Tampering | UAT run against artifacts built before code-review fixes | low | mitigate | the human check requires rebuilding the plugin from the final tree first |
| T-92-SC | Tampering | npm/Gradle installs | low | accept | no dependency is added |
</threat_model>

<verification>
1. Both marker checks print a count of at least 1, and `clean buildPlugin` (with its JUnit suite) exits 0.
2. The whole vitest suite reports 0 failed tests with `RUN_BBJ_TESTS=0`, and lint exits 0.
3. The register check and the language-server boundary check print nothing.
4. The D-13 human check is harvested into UAT and run against a plugin rebuilt from the final tree.
</verification>

<success_criteria>
- All five ROADMAP success criteria for Phase 92 are backed by green automated tests on the final tree.
- Both distributables carry the phase's code.
- The IntelliJ tab-switch check is staged.
</success_criteria>

<output>
Create `.planning/phases/92-host-side-hygiene-focus-guards/92-06-SUMMARY.md` when done. Record:
- `BASE`, both sha256 digests and both marker counts;
- the vitest and IntelliJ test totals;
- any rerun for contention;
- any deviation.
</output>
