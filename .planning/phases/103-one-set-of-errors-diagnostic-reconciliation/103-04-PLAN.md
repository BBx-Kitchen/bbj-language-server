---
phase: 103-one-set-of-errors-diagnostic-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [103-02, 103-03]
files_modified:
  - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts
  - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md
autonomous: true
requirements: [PSRV-06, PSRV-07]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Against the real endpoint, an invented line that both BBj's parser and Langium reject ends up with BBj's diagnostic alone on that line after reconciliation (PSRV-06, D-09, D-10)."
    - "Against the real endpoint, an invented document BBj's parser accepts but Langium flags carries no Error-severity lexer, parser or line-break diagnostic after reconciliation; Langium's complaint is still there as a Warning with its own message and source (PSRV-07, D-04, D-07)."
    - "A full harness run on the phase tree — the endpoint-absent path, since the harness uses the fake interop — reproduces the pre-phase A, A2 and B file sets exactly, so the suppression is shown to be conditional at corpus scale (roadmap criterion 3)."
    - "The working measurement with the endpoint active records list B out of 1,210 against the 658 milestone start and the 669 current value, the number of compiler-accepted files still carrying a language-server syntax Error after the verdict, and endpoint failures separately (roadmap criterion 4)."
    - "`103-CONFORMANCE.md` carries aggregate numbers and shape descriptions in our own words only — no corpus file name, id, path or source text."
  artifacts:
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts
  key_links:
    - "The live check takes BBj's verdict from the real `JavaInteropService.parseProgram()` and Langium's list from hermetic test services, then runs the shipped `reconcileWithVerdict` and `applyDiagnosticHierarchy` — the product's own decision code, not a re-implementation."
    - "The endpoint probe is a scratch script in the private corpus repository's untracked `conformance/snapshots/` directory; it reuses the harness's worker setup and the shipped reconciliation code, and is not the harness's endpoint mode, which belongs to Phase 104."
    - "The harness overwrites `details.json` on every full run, so the snapshot is taken before the run and file sets are compared, not totals."
  prohibitions:
    - statement: "No corpus file content, corpus file name or id, or proprietary BBj source text enters this repository — test fixtures are invented and the conformance record carries aggregate numbers only."
      status: unverified
      verification: flagged
    - statement: "The harness itself (`run.mjs`, `worker.mts`) is not modified in this phase, and nothing in the private corpus repository is committed."
      status: unverified
      verification: flagged
---

<objective>
Watch the reconciliation decide against the real BBj parser, then take the phase-boundary working
measurement.

Purpose: every earlier test scripts BBj's answer. This plan asks the real endpoint twice — once in a
gated test that pins the two headline behaviours (one diagnostic on a line both reject, no syntax
error on a document BBj accepts), and once across the whole reject set and the compiler-accepted
files Langium still flags — so success criteria 3 and 4 rest on measured numbers. The formal exit
gate stays in Phase 104; this is the working measurement the roadmap asks for.
Output: two gated tests in `test/functional/parse-program-live.test.ts` and `103-CONFORMANCE.md`.

<flagged_assumptions>
1. **The accepted-document fixture.** The pending A2 todo records an invented nested single-line
   `IF … ELSE … FI` shape that Langium flags ("needs to start in a new line: else") and the
   compiler accepts. It is the first candidate; the test's own preconditions decide whether it still
   holds against the deployed endpoint.
2. **B may not reach 5 %.** BBj's parser endpoint reports syntax; some rejects are semantic
   (undefined labels or functions) and bbjcpl-only. The measurement records what it finds; it does
   not tune anything to move the number, and it is not this phase's gate.
3. **Failures are counted separately.** In the product a failed cycle falls back to `bbjcpl` on the
   saved file; the probe does not model that, so each failure is reported by kind rather than folded
   into B.
</flagged_assumptions>
</objective>

<executor_shell_rules>
Read this before running anything. This repository's permission hooks block opaque shell reads and
cannot tell a scoped search from a secret read; a blocked command stalls a background executor on an
unanswered prompt.

- Prefer the Read/Glob tools for reading and finding files. For content search use `grep` with an
  absolute path and an exact file or an `--include` filter.
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. `cd` is allowed only
  directly in front of `npx vitest`, `npx tsc`, `npm` or `node`/`tsx` running the conformance
  harness. Use `git -C <repo> …` for every git command.
- No bare recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or
  any credential store.
- `git add <exact path>` only — never `-A`, never `.`. Never `git stash`. Commit nothing in
  `/home/coder/repos/bbj-corpus`.
- vitest runs with `bbj-vscode` as the working directory. Never pass vitest a reporter named `basic`.
- No planning identifiers in source, tests or commit subjects. GitHub issue numbers are fine.
- No corpus text, corpus file names or corpus ids in any file of this repository — including this
  plan's SUMMARY. Describe shapes in your own words.
- Record deviations in this plan's SUMMARY only. Do not add entries to `WINDOWS.md`. Do not mark
  PSRV-06 or PSRV-07 complete in `REQUIREMENTS.md`.
</executor_shell_rules>

<!-- planner-discipline-allow: PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9] -->

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONTEXT.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-01-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-02-SUMMARY.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every file and symbol this phase creates. This plan owns everything marked (04).

**New files**
- `bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts` (01)
- `bbj-vscode/test/bbj-diagnostic-reconciliation.test.ts` (01)
- `bbj-vscode/test/bbj-document-validator.test.ts` (03)
- `.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md` (04)

**New exported symbols in `bbj-diagnostic-reconciliation.ts`** (01)
- `LINE_BREAK_DIAGNOSTIC_CODE`, `DOWNGRADED_SYNTAX_CODE`, `VerdictState`, `LineTextLookup`
- `documentLineText`, `syntaxComplaintKey`, `isSyntaxComplaint`, `isDowngradedSyntaxWarning`, `downgradeSyntaxComplaint`, `lineSpansOverlap`
- `reconcileWithVerdict`, `applyVerdictCarryOver`
- `getVerdictState`, `setVerdictState`, `clearVerdictState`, `clearAllVerdictStates`
- `rememberLangiumDiagnostics`, `recallLangiumDiagnostics`

**New or newly exported symbols elsewhere**
- `bbj-parser-service.ts`: `LiveParseOutcome` (01)
- `bbj-document-validator.ts`: exported `applyDiagnosticHierarchy`, new `applyConfiguredDiagnosticHierarchy` (01); explicit `BBjDocumentValidator` constructor (03)

**Modified, no new exported symbol**
- `bbj-document-builder.ts` (01, 02); `bbj-parser-service.ts` (01, 02); `validations/line-break-validation.ts` (03)
- `test/bbj-parser-service.test.ts` (01, 02), `test/document-builder.test.ts` (01, 02)
- `test/functional/parse-program-live.test.ts` — two gated reconciliation checks (04)

**Outside this repository, never committed**
- `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-before-details.json`, `phase-103-before-summary.json`, `phase-103-endpoint-probe.mts`, `phase-103-endpoint-probe.json` (04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The real BBj parser and the shipped reconciliation agree on the two headline cases</name>
  <files>/home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts</files>
  <precondition>BBjServices is running, a TCP connect to 127.0.0.1:5008 succeeds, and `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts` passes before this task changes the file (the deployed jar answers parseProgram with a result, not MethodNotFound).</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts — the file being modified: the `shouldRunBBjTests()` gate, the NodeFileSystem services, `setConnectionConfig`, the direct `parseProgram()` calls and why a full document build is avoided here
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts — `reconcileWithVerdict`, `documentLineText`, `recallLangiumDiagnostics`, `isSyntaxComplaint`, `lineSpansOverlap`
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-parser-service.ts — `parseErrorsToDiagnostics`
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/bbj-document-validator.test.ts — the hermetic `validationHelper` setup plan 03 used
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md — the invented nested single-line `IF … ELSE … FI` repro Langium flags and the compiler accepts
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/java-interop-cold-resolution-gotcha.md — why this file calls the endpoint directly rather than building documents over live interop
  </read_first>
  <action>
    Add one `describe('reconciliation against the live endpoint (real interop)', …)` to the file,
    gated by the file's existing `run` flag so a closed gate contributes skipped tests only and no
    connection is attempted.

    Langium's side stays hermetic: a separate `createBBjTestServices(EmptyFileSystem)` (its interop
    double never connects) with `initializeWorkspace` inside the gated `beforeAll`, and a
    `validationHelper<Program>`; the raw list is `recallLangiumDiagnostics(result.document)`. BBj's
    side is the file's existing real `JavaInteropService.parseProgram()` with a temp-directory
    `canonicalName`, converted by `parseErrorsToDiagnostics(result.errors, <line count>, 20)`. The
    decision is the shipped code: `reconcileWithVerdict(raw, bbjDiagnostics, documentLineText(result.document.textDocument))`
    followed by `applyDiagnosticHierarchy(…, true, 20)`.

    Test "a line both parsers reject keeps BBj's diagnostic alone": an invented one-to-three-line text
    with one clear syntax error. Assert the preconditions first — the real result has at least one
    error, and the raw list has a syntax complaint whose span overlaps a BBj diagnostic's span. Then:
    every BBj diagnostic is present exactly once, and no syntax complaint in the reconciled list
    overlaps any BBj span.

    Test "a document the BBj parser accepts carries no language-server syntax error": start with the
    todo's nested single-line IF repro. Preconditions — the real result has zero errors, and the raw
    list holds at least one line-break-coded or parsing-error diagnostic. Then: no diagnostic with a
    syntax code is at Error severity, and every former complaint is present as a Warning with its
    original message and source `'bbj'`. If the repro no longer satisfies both preconditions, try at
    most ten other invented one-to-three-line shapes from the milestone's recorded residue (STATE.md
    decisions for phases 98-100 describe them in words — for example a variable list after CLEAR, a
    line number in front of an interface member). Never copy corpus text. If none satisfies both
    preconditions, keep the test with the best candidate, mark it `test.skip` with a one-line reason,
    and record the attempt in the SUMMARY — the conformance measurement in task 2 then carries the
    accepted-file claim alone.

    Record the fixture text that satisfied test two verbatim in the SUMMARY: plan 05's hand check
    types it into both IDEs.

    Commit: a subject naming the behaviour, no planning identifier, a `Co-Authored-By:` trailer.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts</automated>
    <fails_when>non-zero exit, or any failed test, or the new describe block reports fewer than 1 passed</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/functional/parse-program-live.test.ts</automated>
    <fails_when>non-zero exit, or any test reported as passed or failed rather than skipped — a closed gate must not reach the endpoint</fails_when>
    <automated>! grep -nE 'PSRV-0|D-[0-9][0-9]|10[0-9]-[0-9][0-9]' /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts</automated>
    <fails_when>exit status non-zero, i.e. a planning identifier reached a shipped test</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/functional/parse-program-live.test.ts` contains `describe('reconciliation against the live endpoint (real interop)'` and calls `reconcileWithVerdict(` and `parseErrorsToDiagnostics(`
    - Both new tests use `test.runIf(run)` (or `test.skip` with a reason for the second, as described)
    - `RUN_BBJ_TESTS=1` run: exit 0, and the "keeps BBj's diagnostic alone" test passed
    - `RUN_BBJ_TESTS=0` run: exit 0 with every test in the file skipped
    - The SUMMARY quotes the accepted-document fixture text (invented) or records that none was found
  </acceptance_criteria>
  <done>Against the deployed BBj parser, the shipped reconciliation leaves BBj's diagnostic alone on a line both reject, and leaves no syntax Error on a document BBj accepts — pinned in a gated test.</done>
</task>

<task type="auto">
  <name>Task 2: The phase-boundary working measurement — unchanged without the endpoint, measured with it</name>
  <files>/home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md</files>
  <precondition>The same endpoint precondition as task 1 holds; `/home/coder/repos/bbj-corpus/conformance/run.mjs`, `worker.mts` and `node_modules/.bin/tsx` exist; `/home/coder/repos/bbj-corpus/conformance/summary.json` reports corpus falseRejects 9, falseAlarms 22 and rejects missed 669 of 1,210 (the value at plan time).</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/run.mjs — the classification (`caughtBy`, `isSyntaxReject`), what it writes and that it wipes `conformance/work/` at start
    - /home/coder/repos/bbj-corpus/conformance/worker.mts — the per-file validate setup to mirror (test services with the fake interop, `fromString`, `addDocument`, `build` with validation, the linking and environment exclusions, `update([], [uri])` afterwards)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/parse-program-live.test.ts — the real-interop services setup to mirror for the endpoint calls
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/conformance-harness-overwrites-details.md — snapshot before every full run, into `conformance/snapshots/`, compare file sets not totals
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/regression-gate-compare-against-base-commit.md — how to run the same thing on the phase base in a scratch worktree (Node 22 for `langium:generate`)
    - /home/coder/repos/bbj-language-server/.planning/phases/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md — the shape of a conformance record in this project (aggregates, own-words shape descriptions, no corpus paths)
  </read_first>
  <action>
    Step 1 — snapshot. Copy `/home/coder/repos/bbj-corpus/conformance/details.json` and `summary.json`
    to `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-before-details.json` and
    `phase-103-before-summary.json`.

    Step 2 — the endpoint-absent run at corpus scale. Run the harness unchanged:
    `cd /home/coder/repos/bbj-corpus/conformance && node run.mjs --ls /home/coder/repos/bbj-language-server`.
    The harness uses the fake interop, so this is exactly the older-BBj path. Compare the new
    `details.json` against the snapshot by id SETS for `falseRejects`, `falseAlarms` and `missed`,
    not by totals. Expected: identical sets (A 9, A2 22, B 669). If any set differs, do not explain it
    from messages: build a scratch worktree of the phase base
    (`git -C /home/coder/repos/bbj-language-server worktree add --detach /tmp/phase-103-base gsd/phase-102-live-compiler-diagnostics-with-backward-compatibility`,
    symlink `bbj-vscode/node_modules`, regenerate with Node 22), run the harness with
    `--ls /tmp/phase-103-base`, and diff the sets again. A difference present on this branch and absent
    on the base is a regression of the older-BBj path: stop, report it in the SUMMARY with the id count
    (no ids in this repository), and do not continue to step 3. Remove the scratch worktree afterwards.

    Step 3 — the endpoint-active probe. Write
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts` (untracked,
    private, never committed anywhere) and run it with the harness's `tsx`, passing the language
    server repository path. It mirrors `worker.mts`'s imports and per-file setup, with two
    differences: it ALWAYS builds with validation (the product validates every open document, even one
    with syntax errors) and reads Langium's list with `recallLangiumDiagnostics(document)` — imported
    from `${ls}/bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts`, the same source file the
    validator loads, so both see one module instance and one remembered list (check that the list is
    non-empty for a file with a known syntax error before the full run); and it asks
    the real endpoint — one `createBBjServices(NodeFileSystem)` instance configured like the gated test
    file, `parseProgram` called sequentially, one file at a time, with a temp-directory
    `canonicalName`. Per file: a result → `reconcileWithVerdict` then `applyDiagnosticHierarchy(…, true, 20)`
    (the verdict path); a failure → `applyDiagnosticHierarchy(raw, true, 20)` and the failure kind
    recorded. Classify with `run.mjs`'s own rules (linking-error codes and the "File … could not be
    resolved" environment message excluded; caught = any Error remains). Inputs: all 1,210 rejects
    from `rejects.jsonl` (a verdict can also turn a Langium-caught reject into a miss, so every reject
    is re-measured, not only the 669), plus the 31 A and A2 corpus files from the fresh
    `details.json`. Write the per-file results to `snapshots/phase-103-endpoint-probe.json`.

    Step 4 — `103-CONFORMANCE.md` in this phase's directory, aggregates only:
    - the endpoint-absent run: A, A2, B and whether each file set is identical to the snapshot;
    - the endpoint-active measurement: B out of 1,210 with its percentage, set beside 658 (milestone
      start), 669 (endpoint absent now) and the 5 % target (60 files); rejects caught only because of
      BBj's diagnostic; rejects the BBj parser accepted, split into the harness's syntax and semantic
      kinds; endpoint failures by kind;
    - the compiler-accepted files: of the 31, how many still carry a language-server syntax Error after
      the verdict (expected 0 where the verdict is clean), how many drew a BBj error (an endpoint and
      `bbjcpl` disagreement), how many keep a non-syntax validation Error (by design: semantic
      diagnostics stay);
    - one paragraph saying what this is: a single scratch-probe run at the phase boundary, the product's
      own reconciliation code, not the harness's endpoint mode (Phase 104), not a gate.
    Describe any notable group in your own words. No corpus file name, id, path or source line.

    Commit only `103-CONFORMANCE.md` in this repository (a `docs(103): …` subject is fine here —
    planning files may carry the phase number), with a `Co-Authored-By:` trailer.
  </action>
  <verify>
    <automated>test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-before-details.json</automated>
    <fails_when>non-zero exit — the pre-run snapshot is missing or empty, so the file-set comparison has nothing to compare against</fails_when>
    <automated>test -s /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md</automated>
    <fails_when>non-zero exit — the conformance record was not written</fails_when>
    <automated>! grep -nE '(samples|corpus|rejects)/[A-Za-z0-9_.-]+\.(bbj|txt)|[0-9]{3,6}-[0-9a-f]{8}' /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md</automated>
    <fails_when>exit status non-zero, i.e. a corpus file path or corpus id leaked into the public repository</fails_when>
    <automated>git -C /home/coder/repos/bbj-corpus status --short -- conformance/run.mjs conformance/worker.mts</automated>
    <fails_when>the command prints any line — the harness itself was modified</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server status --short --untracked-files=no -- bbj-vscode</automated>
    <fails_when>the command prints any line — this task changes no tracked source or test file</fails_when>
  </verify>
  <acceptance_criteria>
    - `103-CONFORMANCE.md` states A, A2 and B for the endpoint-absent run and states, per list, whether the file set is identical to the pre-phase snapshot
    - `103-CONFORMANCE.md` states B out of 1,210 with the endpoint active, as a number and a percentage, next to 658, 669 and 60
    - `103-CONFORMANCE.md` states how many of the 31 compiler-accepted files still carry a language-server syntax Error after the verdict, and endpoint failures by kind
    - The corpus-path grep above exits 0 (no corpus path or id in the file)
    - `git -C /home/coder/repos/bbj-corpus log -1 --format=%H` is unchanged by this task (nothing committed in the corpus repository)
    - If step 2 found a set difference caused by this branch, the SUMMARY reports it and step 3 was not run
  </acceptance_criteria>
  <done>The older-BBj path is shown unchanged at corpus scale by identical file sets, and the endpoint-active numbers for list B and for compiler-accepted files are on record in aggregate form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus → public repository | The corpus holds internal and third-party code; only aggregate numbers may cross into this repository. |
| test process → BBjServices on 127.0.0.1:5008 | Invented fixture text and, in the probe, corpus text are sent to the local endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-103-12 | Information Disclosure | `103-CONFORMANCE.md`, test fixtures, the SUMMARY | high | mitigate | The record carries aggregates and own-words shape descriptions only; a grep for corpus paths and corpus id patterns gates the file; test fixtures are invented; the probe script and its per-file output stay in the corpus repository's untracked snapshots directory. Tasks 1-2. |
| T-103-13 | Tampering | the measurement record | medium | mitigate | `details.json` is snapshotted before the harness overwrites it, and file sets are compared, not totals; a set difference is bisected against the phase base before any cause is written down. Task 2. |
| T-103-14 | Denial of Service | the local endpoint under 1,241 sequential requests | low | accept | Requests are sequential and the endpoint enforces its own size cap and timeout; this is a one-off local run on a dev container. |
| T-103-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; the harness's existing `tsx` is reused. |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/parse-program-live.test.ts` — zero failed tests.
- `103-CONFORMANCE.md` exists, passes the corpus-path grep, and records both runs.
- Nothing committed in `/home/coder/repos/bbj-corpus`.
</verification>

<success_criteria>
- The real BBj parser and the shipped reconciliation agree on both headline cases.
- The endpoint-absent harness run reproduces the pre-phase file sets.
- The endpoint-active list B and the accepted-file syntax-error count are recorded in aggregate.
</success_criteria>

<output>
Create `.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-04-SUMMARY.md` when done. Quote the accepted-document fixture text (invented) from task 1 for plan 05.
</output>
