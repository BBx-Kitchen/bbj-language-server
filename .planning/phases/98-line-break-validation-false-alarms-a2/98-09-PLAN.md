---
phase: 98-line-break-validation-false-alarms-a2
plan: 09
type: execute
wave: 6
depends_on: ["98-07", "98-08"]
gap_closure: true
autonomous: true
requirements: [CONF-01]
files_modified:
  - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The exact set of corpus files that moved from caught to not-caught between the baseline measurement and the phase-boundary measurement is identified by direct comparison, not inferred from counts."
    - "For each of those files the claimed mechanism — a branch target whose label name is also a language word and which is not declared anywhere in the file, so the diagnostic that used to catch it was the wrong one and the correct one is now a linking diagnostic the harness excludes by design — is either CONFIRMED against the file's own text and the pre-phase diagnostic, or REFUTED with the actual mechanism written down."
    - "Every refuted file has a disposition: fixed in this plan when its cause is inside this phase's line-break, DECLARE or METHODRET scope, or routed with a named destination and a stated reason when it is not."
    - "The record written into 98-CONFORMANCE.md carries counts, message wording and behaviour descriptions in the executor's own words only — no corpus file name, no corpus path, no corpus source line (D-17)."
  artifacts:
    - ".planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md — a new section holding the per-file confirm/refute evidence for the regression and the disposition of anything refuted"
  key_links:
    - "the recorded phase-boundary details file -> the baseline language-server tree: the newly-uncaught set is the intersection of today's not-caught list with the baseline tree's caught verdicts, computed by running the harness worker directly against a checkout of the baseline commit."
    - "a pre-phase line-break diagnostic on a branch-target line -> the current tree's linking diagnostic for the same reference: the mechanism is only confirmed when both halves are observed, not when only the first is."
  prohibitions:
    - "This plan must NOT run the full harness entry point: it must never overwrite the recorded phase-boundary measurement files or append a line to the run history. Only the worker is invoked directly."
    - "No corpus file name, corpus path or corpus source line may be written into any tracked file."
    - "No scratch file, probe script, worktree or baseline checkout may be left inside the repository working tree or registered in its worktree list when this plan finishes."
    - "The executor must NOT decide that the residual regression is acceptable. This plan gathers evidence; the acceptance decision belongs to the human checkpoint in the phase's final gap plan."
    - "A file must NOT be recorded as confirmed on the strength of the diagnostic message text alone; the file's own flagged lines must be read and described."
---

<!-- planner-discipline-allow: VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9] -->

<objective>
Close verification gap 1a and 1b: the phase's closing measurement recorded that seven files of
invalid code stopped being flagged (B rose from 658 to 665 of 1,210), attributed the whole move to
one mechanism, and conceded the attribution was "plausible, not corpus-verified". A phase cannot ask
a human to accept a regression whose cause has never been checked against the files that regressed.

Purpose: turn a plausible story into per-file evidence, so the human decision in the phase's final
gap plan is made on facts. If any of the seven turns out to be a different mechanism, that is a real
unfixed defect this phase introduced and it gets a disposition here, not a shrug.

Output: a per-file confirm/refute record in 98-CONFORMANCE.md, a disposition for anything refuted,
and a clean working tree.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-VERIFICATION.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-06-SUMMARY.md

<working_rules>
These have stalled executors in this repository before. Follow them literally.

- Shell: absolute paths only. Never chain `cd` with grep, find, cat, sed, head or tail. Use
  `git -C /home/coder/repos/bbj-language-server ...` instead of changing directory. `cd` is allowed
  only directly in front of a build or test tool that needs its project directory.
- Staging: `git add <exact path>` only. Never `git add -A`, never `git add .`, never `git stash`.
- vitest needs cwd = bbj-vscode. Whole-suite runs use `--maxWorkers=2` and are judged on
  `numFailedTests`. `RUN_BBJ_TESTS=0` avoids the known local interop baseline failures.
- Tests parse with parseHelper / validationHelper, never DocumentBuilder.build against live interop.
- Planning identifiers stay in planning files. Never write a decision, plan, requirement or review
  identifier into a source, test or fixture file. GitHub issue numbers are fine.
- Deviations go into this plan's SUMMARY.md only, never into WINDOWS.md.
- Do not touch .planning/REQUIREMENTS.md. Its Phase 98 checkboxes were deliberately reverted to
  unchecked; only the phase's final gap plan may set them again.
- Scratch directory for every temporary file, probe script, log and checkout:
  `/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad`.
  Nothing temporary goes inside the repository.
</working_rules>

<measurement_facts>
Established by reading the harness and its recorded output before this plan was written. Treat these
as given; re-derive only if something does not match.

- Harness entry point: `/home/coder/repos/bbj-corpus/conformance/run.mjs`. Worker:
  `/home/coder/repos/bbj-corpus/conformance/worker.mts`. The tsx binary the harness uses is
  `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx`.
- `run.mjs` OVERWRITES `REPORT.md`, `summary.json` and `details.json` and APPENDS to `history.jsonl`
  on every full run. `details.json` therefore holds only the most recent full run. Right now that is
  the phase-boundary run. Snapshot it before anything else.
- `details.json` has three keys: `falseRejects`, `falseAlarms`, `missed`. Each entry in `missed` is
  an object with `id` (a path relative to `/home/coder/repos/bbj-corpus/rejects/`), `compiler`
  (an object with `line` and `source` — what the compiler complained about), and, when the language
  server produced warnings only, `warning`.
- The B measure counts exactly those reject files for which the language server produced neither a
  lexer/parser error nor an error-severity validation diagnostic. In worker output terms:
  `syntaxErrors === 0 && validationErrors === 0`. Linking diagnostics are excluded by the worker
  itself, by design, because the run uses a fake Java classpath.
- `history.jsonl` line 1 is the baseline: language server commit `d8071b24`, dated 2026-09-20, with
  `falseAlarms` 267 and `missed` 658 of 1,210. Line 4 is the phase-boundary final run: commit
  `5cf8dec7`, `falseAlarms` 22, `missed` 665 of 1,210.
- The per-bucket move from baseline to phase boundary was: parser-caught 321 to 320, validation-error
  caught 223 to 219 and 4 to 2, with the not-caught buckets rising by the matching total of 7.
- Commit `d8071b24` is not reachable from any branch but the object still exists in this repository.
  Its `bbj-vscode` tree differs from the phase's base commit `5fb113cb` only in `bbj-ws-manager.ts`,
  `package.json` and one config-path test — none of them in the grammar, lexer or validators. If
  `d8071b24` is ever unavailable, `5fb113cb` is the documented fallback baseline; record which one
  was used.
- `bbj-vscode/src/language/generated/` is gitignored, so any fresh checkout must regenerate it before
  it can be used. `npm run langium:generate` aborts on this machine's default Node (v24) — resolve a
  Node 22 binary with `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"` and prefix
  the generate call with `PATH="$NODE22_BIN:$PATH"`.
- The worker takes four positional arguments: the language-server repository path, a jobs JSON file,
  an output JSON file, and the mode (`validate`). The jobs file is an array of objects with `id` and
  `file`. The output is an object keyed by the job `id`.
</measurement_facts>

<edge_assumptions>
No SPEC.md exists for this phase, so the edge-coverage probe returned every applicable edge as
unresolved. Surfaced here as explicit assumptions, not silently dropped:

- CONF-01 / adjacency: when the baseline and the current tree produce the SAME verdict for a file,
  it is assumed not to have moved. A file that changed which diagnostic it produced while staying
  caught is invisible to this comparison and is assumed irrelevant to B.
- CONF-01 / empty: an empty or unreadable corpus file is assumed not to occur; if the worker reports
  a crash for any file, stop and report rather than classifying it.
- CONF-01 / ordering: the worker's verdict is per-file and order-independent; the probe uses a single
  shard, so no cross-shard ordering effect is possible.
- CONF-01 / concurrency: the probe runs one worker process. Do not shard it — a partial shard write
  would silently shrink the comparison set.
- VALID-01 through VALID-05 / unclassified: out of this plan's scope.
- Assumed and NOT verified: that the compiler verdicts recorded in the corpus manifest are unchanged
  between the two runs. The compiler build string is identical in both history lines, which is
  consistent with that, but it is not independently checked here.
</edge_assumptions>
</context>

<artifacts_this_phase_produces>
Created by this plan:

- A new section in `.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md`,
  numbered after the existing section 8, titled for the regression investigation. It contains: the
  method used, the size of the newly-uncaught set, one row per file with a paraphrased shape
  description and a CONFIRMED or REFUTED verdict, and the disposition of every refuted file.
- Scratch-only, never committed and deleted at the end of task 3: a jobs JSON file, a baseline worker
  output JSON file, a baseline language-server checkout under the scratch directory, and any probe
  script used to inspect a synthetic reproduction.

Modified by this plan:

- `.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md` only — unless task 3
  fixes a refuted file, in which case the source and test files it touches are named in the SUMMARY
  and a synthetic regression fixture is added under `bbj-vscode/test/test-data/conformance/` per the
  existing convention.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Snapshot the phase-boundary measurement and stand up a usable baseline language-server tree</name>
  <files>/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad</files>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/run.mjs (the whole file — in particular how the three
      measures are classified, what is written to disk, and what the details file contains)
    - /home/coder/repos/bbj-corpus/conformance/worker.mts (the whole file — its four arguments, its
      per-file result fields, and which diagnostic categories it filters out)
    - /home/coder/repos/bbj-corpus/conformance/history.jsonl (all four lines)
    - The measurement_facts block in this plan's context section
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md sections 1, 2 and 6
  </read_first>
  <action>
    First, before touching anything else, copy the recorded phase-boundary measurement out of harm's
    way: copy `/home/coder/repos/bbj-corpus/conformance/details.json`,
    `/home/coder/repos/bbj-corpus/conformance/summary.json` and
    `/home/coder/repos/bbj-corpus/conformance/history.jsonl` into the scratch directory under the
    names `details-phase-boundary.json`, `summary-phase-boundary.json` and `history-before.jsonl`.
    Every later step reads the snapshot, never the live file.

    Then create a detached checkout of the baseline commit under the scratch directory with
    `git -C /home/coder/repos/bbj-language-server worktree add --detach <scratch>/baseline-tree d8071b24`.
    If that commit object is gone, use `5fb113cb` instead and record the substitution.

    Make the checkout runnable without installing anything: symlink the main checkout's dependency
    directory into it with
    `ln -s /home/coder/repos/bbj-language-server/bbj-vscode/node_modules <scratch>/baseline-tree/bbj-vscode/node_modules`.
    Then regenerate the parser inside the checkout, which is mandatory because the generated folder is
    gitignored and the checkout has none: resolve Node 22 with
    `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"` and run
    `cd <scratch>/baseline-tree/bbj-vscode && PATH="$NODE22_BIN:$PATH" npm run langium:generate`.
    Chevrotain ambiguity warnings during generation are pre-existing and benign.

    Do NOT run `run.mjs` at any point in this plan. It would overwrite the recorded measurement and
    append a misleading line to the run history.

    Confirm the checkout is the tree you think it is and that it produces a parser: check that
    `git -C <scratch>/baseline-tree rev-parse --short HEAD` prints the baseline commit, and that
    `<scratch>/baseline-tree/bbj-vscode/src/language/generated/ast.ts` exists and is newer than
    `<scratch>/baseline-tree/bbj-vscode/src/language/bbj.langium`.
  </action>
  <verify>
    <automated>ls -l /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/details-phase-boundary.json /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/history-before.jsonl; node -e "const d=require('/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/details-phase-boundary.json');console.log('missed='+d.missed.length,'falseAlarms='+d.falseAlarms.length,'falseRejects='+d.falseRejects.length)"</automated>
    <fails_when>either file is absent, or the printed line is not exactly `missed=665 falseAlarms=22 falseRejects=167` — any other numbers mean the snapshot was taken of a different run than the recorded phase-boundary one</fails_when>
    <automated>git -C /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/baseline-tree rev-parse --short HEAD; ls -l /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/baseline-tree/bbj-vscode/src/language/generated/ast.ts /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/baseline-tree/bbj-vscode/src/language/bbj.langium</automated>
    <fails_when>the printed commit is neither `d8071b24` nor the recorded fallback `5fb113cb`, or either listed path is missing, or the generated parser file's timestamp is older than the grammar file's</fails_when>
  </verify>
  <acceptance_criteria>
    - The scratch directory contains `details-phase-boundary.json`, `summary-phase-boundary.json` and
      `history-before.jsonl`.
    - The snapshotted details file reports 665 missed, 22 false alarms and 167 false rejects.
    - `git -C /home/coder/repos/bbj-language-server worktree list` lists exactly one extra worktree,
      under the scratch directory, at the baseline commit.
    - The baseline checkout has a generated parser file newer than its grammar file.
    - `/home/coder/repos/bbj-corpus/conformance/history.jsonl` is byte-identical to the snapshot
      taken at the start of this task (no harness entry point was run).
    - `git -C /home/coder/repos/bbj-language-server status --porcelain` shows no new file inside the
      repository working tree.
  </acceptance_criteria>
  <done>The recorded phase-boundary measurement is safe in the scratch directory and a baseline language-server checkout exists that can be handed to the harness worker.</done>
</task>

<task type="auto">
  <name>Task 2: Identify the newly-uncaught files and confirm or refute the claimed mechanism for each</name>
  <files>/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad</files>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/worker.mts (its result fields: syntaxErrors,
      validationErrors, validationWarnings, first, firstValidation, crash)
    - /home/coder/repos/bbj-corpus/conformance/run.mjs lines 59-77 (the caughtBy function and how
      the missed set is derived) and lines 129-136 (what goes into the details file)
    - The measurement_facts and edge_assumptions blocks in this plan's context section
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md section 6 and the
      "On the B-regression cause" section of 98-VERIFICATION.md (the claim being tested)
    - bbj-vscode/src/language/bbj-token-builder.ts lines 249-262 (the branch-target exclusion
      lookbehind and the list of end-of-line keyword tokens it suppresses) — the mechanism's own
      implementation
  </read_first>
  <action>
    Build a jobs file naming every file in the snapshotted not-caught set. Each element is an object
    whose `id` is the string `rejects/` followed by the entry's `id`, and whose `file` is
    `/home/coder/repos/bbj-corpus/rejects/` followed by the same `id`. One command that does it:
    `node -e "const d=require('<scratch>/details-phase-boundary.json');require('fs').writeFileSync('<scratch>/probe-jobs.json',JSON.stringify(d.missed.map(e=>({id:'rejects/'+e.id,file:'/home/coder/repos/bbj-corpus/rejects/'+e.id}))))"`

    Run the harness worker once, single process, against the baseline checkout:
    `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx /home/coder/repos/bbj-corpus/conformance/worker.mts <scratch>/baseline-tree <scratch>/probe-jobs.json <scratch>/probe-baseline.json validate`
    This writes only into the scratch directory. It does not touch the recorded measurement files and
    does not append to the run history.

    Derive the newly-uncaught set: every job id whose baseline result has `syntaxErrors` greater than
    zero OR `validationErrors` greater than zero. Those are the files the pre-phase language server
    caught and the phase-boundary language server does not. Expect exactly seven. If the count is
    seven, then by arithmetic no file moved the other way and the set is complete. If it is not
    seven, say so plainly and record the discrepancy — a larger set means some files also became
    caught, and the record must state both numbers rather than forcing the expected answer.

    If any baseline result carries a `crash` field, stop and report: the baseline checkout is not
    sound and no classification made from it is trustworthy.

    For each file in the newly-uncaught set, gather three pieces of evidence and write them into a
    scratch working note:
    (a) the diagnostic the BASELINE language server produced — from that entry's `first.message` when
        it was a lexer or parser error, otherwise `firstValidation.severity` and
        `firstValidation.message`, plus the line number and the source line the worker recorded;
    (b) what the compiler complained about — the `compiler.line` and `compiler.source` recorded for
        the same id in the snapshotted details file;
    (c) the file's own text around those lines, read directly from
        `/home/coder/repos/bbj-corpus/rejects/` plus the id. Read the flagged line and enough of its
        neighbourhood to see whether the branch target named on it is declared as a label anywhere in
        the file. Describe the shape in your own words. Do not classify from the diagnostic message
        text alone — that is exactly the shortcut this task exists to replace.

    Classify each file as CONFIRMED only when all three hold: the baseline diagnostic was a
    line-break message ("needs to start in a new line" or "needs to end with a line break"); the line
    it sat on is a branch statement whose target name is also a language word; and that target is not
    declared as a label anywhere in the file, which is what the compiler is complaining about.
    Anything else is REFUTED, and the actual mechanism must be written down in behaviour terms.

    For at least one CONFIRMED file, close the second half of the mechanism as well: build a minimal
    synthetic reproduction with invented names — a branch to a keyword-named label that is never
    declared — and observe what the CURRENT tree reports for it. Do this with a scratch script under
    the scratch directory that imports the services by absolute path, the same way the harness worker
    does; never create a test file inside the repository, because the suite would pick it up. Record
    whether the reference now produces a linking-category diagnostic (which the worker filters out by
    design) and at what severity, and record the same observation for the baseline checkout if it
    differs.

    Write nothing corpus-derived into a tracked file in this task. The working note stays in the
    scratch directory; task 3 paraphrases it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const S='/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/';const b=JSON.parse(fs.readFileSync(S+'probe-baseline.json','utf8'));const ids=Object.keys(b);const crashed=ids.filter(i=>b[i].crash);const caught=ids.filter(i=>(b[i].syntaxErrors>0)||(b[i].validationErrors>0));console.log('probed='+ids.length);console.log('crashed='+crashed.length);console.log('newly_uncaught='+caught.length)"</automated>
    <fails_when>`probed=` is anything other than 665, or `crashed=` is anything other than 0, or `newly_uncaught=` is 0 (the comparison found nothing, meaning the baseline checkout is not actually the pre-phase tree)</fails_when>
    <automated>grep -c -i 'CONFIRMED\|REFUTED' /tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/regression-working-note.md</automated>
    <fails_when>the printed count is smaller than the `newly_uncaught` count printed by the previous command — every file in the set must carry an explicit verdict</fails_when>
  </verify>
  <acceptance_criteria>
    - `probe-jobs.json` names 665 files and `probe-baseline.json` holds 665 results with zero crashes.
    - The newly-uncaught set is computed by the stated rule and its size is recorded; if it is not 7,
      the discrepancy is written down rather than adjusted away.
    - The scratch working note carries, for every file in that set, the baseline diagnostic, the
      compiler complaint, a shape description written in the executor's own words from the file's own
      lines, and a CONFIRMED or REFUTED verdict.
    - At least one confirmed file has the second half of the mechanism observed on a synthetic
      reproduction with invented names, with the diagnostic category and severity recorded.
    - `/home/coder/repos/bbj-corpus/conformance/history.jsonl` is still byte-identical to
      `history-before.jsonl`, and `details.json` is still byte-identical to
      `details-phase-boundary.json`.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain` shows no new file inside the
      repository working tree.
  </acceptance_criteria>
  <done>Every file that stopped being flagged is identified by name-free evidence and carries a verdict grounded in its own text, not in the message wording.</done>
</task>

<task type="auto">
  <name>Task 3: Record the evidence, dispose of anything refuted, and tear the baseline tree down</name>
  <files>.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md</files>
  <read_first>
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md (whole file — match
      the existing section numbering, table style and the no-corpus-text discipline it already
      follows)
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md, decisions D-16 and
      D-17
    - The scratch working note written in task 2
    - bbj-vscode/test/test-data/conformance/keyword-branch-targets.bbj and
      bbj-vscode/test/conformance-regressions.test.ts (the fixture convention to follow if a fix is
      needed, D-12 and D-13)
  </read_first>
  <action>
    Append a new numbered section to 98-CONFORMANCE.md, continuing the file's existing numbering after
    section 8. The file's headings are of the form "## 8. Title", so the new one begins exactly
    `## 9.` followed by a heading naming the regression investigation. It must contain, in this order:

    The method, in two or three sentences: that the recorded phase-boundary not-caught list was
    replayed against a checkout of the baseline commit using the harness worker directly, that the
    newly-uncaught set is the intersection of today's not-caught list with the baseline tree's caught
    verdicts, and which baseline commit was used.

    The numbers: how many files were probed, how many came back newly uncaught, and whether that
    matches the plus-seven the recorded run reported. State any discrepancy plainly.

    One table row per newly-uncaught file. Columns: an ordinal (1, 2, 3 — never a corpus file name),
    the message group wording of the diagnostic the pre-phase language server produced, a
    one-sentence description of the file's shape in your own words, and the verdict CONFIRMED or
    REFUTED. For a refuted row, add the actual mechanism in the same sentence style.

    The second half of the mechanism: what the current tree reports for the synthetic reproduction,
    which diagnostic category it falls into, and why the harness excludes that category.

    A closing paragraph stating what the evidence does and does not establish, and stating explicitly
    that the decision whether to accept the residual regression is not made in this section.

    Write no corpus file name, no corpus path and no corpus source line anywhere in it (D-17). Counts,
    message-group wording and your own behaviour descriptions only.

    Then dispose of every REFUTED file. If a refuted file's cause is inside this phase's scope — a
    line-break mask, the conflicting-DECLARE check or either METHODRET check — fix it now, with a
    synthetic regression fixture under `bbj-vscode/test/test-data/conformance/` named for its
    construct and a matching test, following the existing convention (D-12, D-13); the fixture holds
    only hand-written minimal shapes with invented names. If the cause is outside this phase's scope,
    record a named destination — a specific later phase — and the reason, in the same table row and
    in the SUMMARY. Do not leave a refuted file with no disposition, and do not fix a file by widening
    a check so that it stops reporting something it should report.

    Finally, remove the baseline checkout with
    `git -C /home/coder/repos/bbj-language-server worktree remove --force <scratch>/baseline-tree`
    followed by `git -C /home/coder/repos/bbj-language-server worktree prune`, and confirm the
    worktree list is back to one entry. Leave the scratch JSON files in place for the phase's final
    gap plan to compare against; they are outside the repository.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('/home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md','utf8');const s=t.split(/^## 9\./m)[1]||'';console.log('section9_present='+(s.length>0));console.log('verdicts='+((s.match(/CONFIRMED|REFUTED/g)||[]).length));console.log('mentions_decision_deferred='+/not made in this section|belongs to|human/i.test(s))"</automated>
    <fails_when>`section9_present=false`, or `verdicts=0`, or `mentions_decision_deferred=false` — the last one means the section fails to say the acceptance decision is made elsewhere</fails_when>
    <automated>grep -n -E '/home/coder/repos/bbj-corpus|rejects/|corpus/' /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md | grep -v 'conformance/run.mjs' | grep -v 'bbj-corpus/conformance'</automated>
    <fails_when>any line is printed — a corpus file path or a rejects-relative identifier leaked into a tracked file, which the phase's disclosure rule forbids</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server worktree list; git -C /home/coder/repos/bbj-language-server status --porcelain</automated>
    <fails_when>the worktree list prints more than one line, or the status output names any file outside the set this plan declares it modifies</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-09.json > /dev/null 2>&1; node -e "const r=require('/tmp/claude-1000/-home-coder-repos-bbj-language-server/4c7a6bad-4edc-40f9-978d-7447c6e90bf6/scratchpad/suite-98-09.json');console.log('numFailedTests='+r.numFailedTests)"</automated>
    <fails_when>the printed line is anything other than `numFailedTests=0`, or the node call errors because the JSON report was not written</fails_when>
  </verify>
  <acceptance_criteria>
    - 98-CONFORMANCE.md has a new numbered section after section 8 containing the method, the counts,
      one verdict row per newly-uncaught file, the second-half mechanism observation, and an explicit
      statement that the acceptance decision is not made there.
    - Every row carries CONFIRMED or REFUTED; every REFUTED row carries a disposition that is either
      a fix made in this plan or a named later phase with a reason.
    - No corpus file name, corpus path or corpus-relative identifier appears anywhere in the file.
    - If a fix was made, a matching fixture exists under `bbj-vscode/test/test-data/conformance/` and
      `test/conformance-regressions.test.ts` passes.
    - The repository worktree list is back to a single entry and the working tree holds no scratch
      file.
    - Whole-suite JSON report shows numFailedTests equal to 0.
    - `git -C /home/coder/repos/bbj-language-server diff -- bbj-vscode | grep '^+' | grep -qE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'` exits 1 (no match).
  </acceptance_criteria>
  <done>The regression's cause is on record per file with a verdict grounded in the files themselves, anything refuted has a fix or a named destination, and no baseline checkout or scratch artefact is left in the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus -> tracked planning files | Proprietary BBj source text and file names cross into a public repository if any is copied verbatim. |
| corpus file -> language server worker | Arbitrary third-party BBj source is parsed and validated in-process by the harness worker. |
| scratch checkout -> repository git metadata | A worktree registers state inside the repository's git directory that outlives the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-09-01 | Information Disclosure | 98-CONFORMANCE.md and the SUMMARY | high | mitigate | D-17 forbids corpus file names, paths and source lines in tracked files. Task 3's verify greps the file for any corpus path or rejects-relative identifier and fails on a single hit; the record carries ordinals, message-group wording and paraphrase only. |
| T-98-09-02 | Tampering | recorded measurement files in the private harness directory | high | mitigate | The harness entry point overwrites the details, summary and report files and appends to the run history. This plan never invokes it: the worker is called directly with scratch-only output paths, and both the history file and the details file are asserted byte-identical to the snapshot afterwards. |
| T-98-09-03 | Repudiation | the regression's recorded cause | high | mitigate | The prior record attributed the move to one mechanism without checking the files. This plan requires a per-file verdict derived from the file's own lines plus both halves of the mechanism, and forbids classifying from the diagnostic message text alone. |
| T-98-09-04 | Denial of Service | harness worker over 665 third-party files | low | accept | The worker is the project's own established measurement path, runs in a throwaway process against files already parsed many times by earlier runs, and a crash is surfaced as a stop condition rather than being classified. |
| T-98-09-05 | Tampering | repository git metadata | medium | mitigate | The baseline checkout is created outside the repository working tree and removed with an explicit worktree remove plus prune; task 3's verify asserts the worktree list is back to one entry and the status output is clean. |
| T-98-09-06 | Tampering | npm/pip/cargo installs | high | mitigate | No package install task exists. The one network-touching command resolves a Node 22 runtime through the same invocation earlier plans in this phase already used; no project dependency is added or upgraded, and the dependency directory is reused by symlink rather than installed afresh. |
</threat_model>

<verification>
1. `/home/coder/repos/bbj-corpus/conformance/history.jsonl` and `details.json` are byte-identical to the snapshots taken at the start of task 1 — the recorded phase-boundary measurement is intact.
2. `git -C /home/coder/repos/bbj-language-server worktree list` prints exactly one line.
3. `git -C /home/coder/repos/bbj-language-server status --porcelain` names only the files this plan declares it modifies.
4. `grep -n -E '/home/coder/repos/bbj-corpus|rejects/|corpus/' .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md` prints nothing beyond the harness path already present in the file's existing sections.
5. Whole suite: `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2` — judged on `numFailedTests` being 0.
6. `.planning/REQUIREMENTS.md` unchanged by this plan.
</verification>

<success_criteria>
- The newly-uncaught file set is identified by direct comparison against a baseline checkout, not
  inferred from counts, and its size is stated against the plus-seven the recorded run reported.
- Every file in that set carries CONFIRMED or REFUTED, grounded in its own lines.
- Both halves of the claimed mechanism are observed at least once: the pre-phase diagnostic that used
  to catch the file, and the diagnostic category the current tree produces instead.
- Every refuted file has a fix in this plan or a named later phase and a reason.
- The record contains no corpus name, path or source line.
- The recorded measurement files and the repository worktree list are left exactly as found.
</success_criteria>

<output>
Create `.planning/phases/98-line-break-validation-false-alarms-a2/98-09-SUMMARY.md` when done.
Record in it: which baseline commit was used and why, the probed and newly-uncaught counts, the
confirmed/refuted split, the disposition of every refuted file, and the exact scratch file paths the
phase's final gap plan can reuse.
</output>