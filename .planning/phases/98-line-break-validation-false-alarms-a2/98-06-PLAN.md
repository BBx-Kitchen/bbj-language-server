---
phase: 98-line-break-validation-false-alarms-a2
plan: 06
type: execute
wave: 4
depends_on: ["98-01", "98-02", "98-03", "98-04", "98-05"]
files_modified:
  # Documentation by default. A source file under bbj-vscode/src/language/ may additionally be
  # touched ONLY under the D-16 trivially-related exception in Task 3, which requires the file to
  # be named, the whole suite re-run and the harness re-run. This is the last wave, so no other
  # plan can conflict.
  - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
  - .planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md
  - bbj-vscode/src/language/bbj.langium
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-05, CONF-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The whole vitest suite reports `numFailedTests: 0`; \"failed suites\" caused by `beforeAll` hook timeouts under contention are not failures."
    - "The private conformance harness, run by Claude at the phase boundary, reports A2 at or below 25, down from a baseline of 267 (D-17)."
    - "Neither A nor B is worse than its baseline (A = 168, A2 = 267, B = 658 of 1,210)."
    - "Every A2 file still remaining is triaged: each remaining message group is recorded with its file count and a one-line cause, and is marked either fixed, accepted residue, or handed to Phase 100's long-tail triage (D-16)."
    - "Only numbers and message-group names with counts are written into tracked files — no corpus file name, no corpus path, no corpus source line (D-17)."
    - "Success criterion 4 is evaluated against its amended wording: no error-severity diagnostic on compiler-accepted code, with unrelated resolved DECLAREs of one name inside a single method body as the one named exception (D-05, D-09)."
    - "Every construct fixed in this phase has a synthetic regression fixture under `bbj-vscode/test/test-data/conformance/`, and the conformance test asserts all of them."
    - "No planning identifier and no corpus text appears anywhere in the phase's source diff."
  artifacts:
    - .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
    - .planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md
  key_links:
    - "The harness loads the language server from source via tsx <-> `bbj-vscode/src/language/generated/`. The generated parser must be current for the run to measure this phase's grammar changes; `npm run build` does NOT refresh it and is not needed."
    - "The A2 gate <-> the per-plan regression fixtures. The harness number is measured outside this repository and never runs in CI; the fixtures are the only thing that keeps these constructs fixed after the measurement is over."
  prohibitions:
    - statement: "MUST NOT silence, widen, or disable a validation check wholesale in order to reach the A2 number — the criterion is no error on code the compiler accepts, never no error."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT copy any corpus file name, corpus path, or corpus source line into a tracked file; only counts and message-group names may be recorded."
      status: unresolved
      verification: judgment
---

<objective>
Measure the phase at its boundary and close it honestly: the whole test suite green, the private
conformance harness reporting A2 at or below 25 with A and B not regressed, and every remaining A2
file triaged into a recorded message group with a cause.

Purpose: the corpus and the harness live outside this public repository and never run in CI, so
this one measured run is the only evidence the phase's goal was reached — and the residue triage is
what stops an unexplained remainder from quietly becoming Phase 100's surprise.

Output: `98-CONFORMANCE.md` in the phase directory holding the before/after numbers, the residue
table, and the criterion-4 attestation; plus a short `COVERAGE.md` declaration recording that this
phase integrates no external API.

This plan is the last wave. It changes no source file by default; a fix is allowed only where a
remaining group turns out to be trivially related to a change this phase already made, and every
such fix is recorded (D-16).
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-VALIDATION.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-01-SUMMARY.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-02-SUMMARY.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-03-SUMMARY.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-04-SUMMARY.md
@.planning/phases/98-line-break-validation-false-alarms-a2/98-05-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Kind | Name / path |
|------|-------------|
| Phase notes | `.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md` — before/after numbers, residue table, criterion-4 attestation |
| Gate declaration | `.planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md` — a single-line "no external API integration" declaration |

Phase-wide probe accounting, for the record: 12 probe items surfaced across the phase's six
requirements; 8 were authored into plan `must_haves.truths` (6 with an explicit acceptance
criterion, 2 as backstop markers) and 4 unclassified rows were surfaced as flagged assumptions in
plans 01, 03, 04 and 05. 12 = 8 + 4; nothing was dropped.

## Working rules for the executor (violating these stalls the run on a permission prompt)

- Absolute paths only. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`/`git`.
  Use `git -C /home/coder/repos/bbj-language-server ...`. The only allowed `cd` is in front of
  `npm`/`npx` in `/home/coder/repos/bbj-language-server/bbj-vscode`.
- Never use `git stash`. Stage with `git add <exact path>` only.
- vitest runs with cwd = `bbj-vscode` and `RUN_BBJ_TESTS=0`.
- Do not read or copy anything from `/home/coder/repos/bbj-corpus/corpus/`. The harness entry point
  and its report are outside that directory and may be read; their contents may be summarised as
  counts and message-group names only.
- `npm run build` is NOT needed for the harness run. The harness loads the language server from
  source via tsx, so only `bbj-vscode/src/language/generated/` must be current — refresh it with
  `npm run langium:generate` under Node 22 if `bbj.langium` is newer than the generated files:
  `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"` then prefix the generate call
  with `PATH="$NODE22_BIN:$PATH"`.
- Register check: no plan ids, decision ids, requirement ids or phase numbers in source comments,
  test names, test comments, or regression-file names/content. GitHub issue numbers are fine.
  Planning identifiers are expected and fine inside `.planning/` files.

<tasks>

<task type="auto">
  <name>Task 1: Whole-suite gate on the final tree</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-VALIDATION.md (the sampling contract and the whole-suite judging rule)
    - the five plan SUMMARY files in /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/ (what each plan changed and what each left open)
  </read_first>
  <files>bbj-vscode/src/language/bbj.langium</files>
  <action>
    Confirm the tree is coherent before measuring anything.

    1. Compare the modification time of `bbj-vscode/src/language/bbj.langium` with that of
       `bbj-vscode/src/language/generated/ast.ts`. If the grammar is newer, regenerate under
       Node 22 as described in the working rules. Do not run `npm run build`; it does not
       regenerate the grammar and the harness does not need a bundle.
    2. Run the whole vitest suite with two workers and judge it on the reported failed-test count,
       not on the failed-suite count: a suite that fails because a `beforeAll` hook timed out under
       contention is environment noise, not a regression. If any test genuinely fails, stop and
       report it rather than proceeding to the measurement.
    3. Confirm every conformance fixture the five plans promised is present and collected, and that
       the conformance test asserts all of them.
    4. Run the register check over the whole phase's source diff against the phase's base commit.
  </action>
  <acceptance_criteria>
    - The vitest run reports a failed-test count of 0.
    - `ls -1 /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/` lists at
      least seven `.bbj` fixtures covering the TABLE statement, the numeric restore, keyword branch
      targets, the exit/load/save group, the single-line IF forms, the trailing-comma print, the
      unclosed function body, and the DECLARE/METHODRET group.
    - `bbj-vscode/src/language/generated/ast.ts` is no older than `bbj-vscode/src/language/bbj.langium`.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain` lists nothing under
      `bbj-vscode/src/language/generated/` and no scratch probe file.
    - The register check over the phase's whole source diff produces no match.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary (a non-zero exit caused only by hook-timeout failed suites with a failed-test count of 0 is not a failure)</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 -- bbj-vscode | grep '^+' | grep -qE 'VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source</fails_when>
  </verify>
  <done>The final tree has a current generated parser, a green suite judged on failed tests, all promised fixtures present, and a clean register check.</done>
</task>

<task type="auto">
  <name>Task 2: Run the private conformance harness and record the numbers</name>
  <precondition>`/home/coder/repos/bbj-corpus/conformance/run.mjs` exists and is readable by node; the corpus and harness live outside this repository and are never run in CI.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONTEXT.md (the measurement decisions, especially what may and may not be written into tracked files)
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md (the "Phase 98" block — success criterion 5 and its numbers)
    - /home/coder/repos/bbj-corpus/conformance/REPORT.md (the A / A2 / B legend and the A2 message-group table — read for counts and group names only)
  </read_first>
  <files>.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md</files>
  <action>
    Run the harness once against this repository. It takes about a minute. Before asserting the
    gate, read the harness's own report legend and confirm which direction counts as "worse" for
    each of A, A2 and B — record the definition you used, so a later reader can check the
    arithmetic rather than trusting it.

    Create `98-CONFORMANCE.md` in the phase directory with four sections:
    1. **Numbers.** A table with one row per category (A, A2, B) holding the baseline (A = 168,
       A2 = 267, B = 658 of 1,210), the measured value, the delta, and pass/fail against the gate:
       A2 at or below 25, A not worse than 168, B not worse than 658.
    2. **What the run measured.** The command, the date, and a one-line note that the harness loads
       the language server from source, so the measurement reflects the generated parser as it
       stood at the run.
    3. **Remaining A2 groups.** One row per remaining message group: the message-group name, the
       file count, and a one-line cause. Nothing else — no corpus file name, no corpus path, no
       corpus source line (D-17).
    4. **Criterion-4 attestation.** State, against the amended wording (D-05, D-09), whether any
       error-severity diagnostic remains on compiler-accepted code other than the one named
       exception — unrelated resolved DECLAREs of one name inside a single method body — and name
       the evidence.

    If the A2 gate is not met, do not adjust a check to reach it. Record the shortfall, the groups
    responsible, and stop for a decision.
  </action>
  <acceptance_criteria>
    - `98-CONFORMANCE.md` exists with all four sections.
    - The measured A2 value is at or below 25, and A and B are recorded as not worse than their
      baselines, with the direction-of-worse definition stated.
    - `! grep -rniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md` succeeds — no corpus path leaked.
    - The remaining-groups table has one row per group with a count and a cause, and no file name.
  </acceptance_criteria>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server</automated>
    <fails_when>non-zero exit, or the run's reported A2 count above 25, or an A count above 168, or a B count worse than 658 of 1,210</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked notes file</fails_when>
  </verify>
  <done>The harness has been run once at the phase boundary, the gate is recorded as met with its arithmetic reproducible, and no corpus identifier reached a tracked file.</done>
</task>

<task type="auto">
  <name>Task 3: Triage the residue and close the phase's gate declarations</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md (the remaining-groups table written by Task 2)
    - the five plan SUMMARY files in the phase directory (each records the shapes it left open — the `LEN` group from plan 04 in particular)
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-VALIDATION.md (the Per-Task Verification Map to reconcile against)
  </read_first>
  <files>
    .planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md,
    .planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md
  </files>
  <action>
    1. Disposition every remaining A2 group in the table (D-16). Each row gets exactly one of:
       **fixed** — it turned out trivially related to a change this phase already made, so it was
       fixed here (name the file and the one-line change, and re-run the harness once to confirm the
       new number); **accepted residue** — it stays, with a one-line reason; or **handed to
       Phase 100** — it belongs to the long-tail triage or to a named Phase 100 requirement. The
       `LEN` group — a language word used as a variable name — is handed to Phase 100 under
       PARSE-08, as plan 04 recorded. Do not drive toward zero; the gate is A2 at or below 25.
       If a fix is made, list the touched source file in the SUMMARY and re-run the whole suite.
    2. Add a closing section to `98-CONFORMANCE.md` recording, for each of the phase's five named
       success criteria, whether it holds and what the evidence is — the fixture name and the test
       command for criteria 1 to 3, the criterion-4 attestation from Task 2 for criterion 4, and the
       measured numbers plus the fixture list for criterion 5.
    3. Write `COVERAGE.md` in the phase directory containing a single declaration line beginning
       `No external API integration:` followed by a one-line reason naming what this phase touches
       instead. Keep the whole file under 200 characters. This phase integrates no external service
       and adds no SDK; the declaration exists because the seal-time detector scans phase prose and
       would otherwise block on an unexamined verdict.
    4. Reconcile `98-VALIDATION.md`'s Per-Task Verification Map: confirm each requirement row's
       named command exists and was run, and note any row whose file column still reads as missing.
  </action>
  <acceptance_criteria>
    - Every row in the remaining-groups table carries exactly one disposition of fixed, accepted
      residue, or handed to Phase 100.
    - `98-CONFORMANCE.md` has a closing section with one entry per named success criterion and its
      evidence.
    - `COVERAGE.md` exists, its first non-blank line starts with `No external API integration:`, and
      `wc -c < /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md` reports fewer than 200.
    - If any source file was touched under the trivially-related exception, the whole suite was
      re-run green and the harness re-run, and both are recorded.
    - No corpus file name, corpus path or corpus source line appears in either file.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary (a non-zero exit caused only by hook-timeout failed suites with a failed-test count of 0 is not a failure)</fails_when>
    <automated>head -1 /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/COVERAGE.md | grep -q "No external API integration:"</automated>
    <fails_when>non-zero exit, meaning the declaration file is missing or its first line does not open with the expected declaration phrase</fails_when>
  </verify>
  <done>Every remaining A2 group has a disposition, each named success criterion has recorded evidence, the API-coverage declaration is in place, and the validation map is reconciled.</done>
</task>

</tasks>

<!-- planner-discipline-allow: VALID-0|CONF-0|D-[0-9][0-9]|98-[0-9][0-9] -->
<!-- planner-discipline-allow: ^+ -->

<threat_model>
ASVS level 1; block on high. A measurement and documentation plan. It runs an existing local
harness against a local corpus and writes counts into this repository. No source change is expected;
no authentication, session, access-control, cryptography or network surface is involved.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus and harness -> this public repository | the harness reads proprietary BBj source that must never cross into a tracked file |
| this repository -> public GitHub | every tracked file in this repository is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-02 | Information Disclosure | `98-CONFORMANCE.md` residue table | high | mitigate | Only counts and message-group names may be written (D-17). Task 2 and Task 3 each carry an automated negative check that no corpus path string reached the notes file, and the executor is forbidden from reading anything under the corpus directory itself. |
| T-98-14 | Repudiation | the A2 gate | medium | mitigate | The gate is recorded with its baseline, measured value, delta and the direction-of-worse definition the executor used, so the arithmetic is reproducible rather than asserted. A shortfall must be recorded and stopped on, never closed by adjusting a check. |
| T-98-15 | Tampering | a trivially-related fix made during triage | low | mitigate | Any such fix requires a named file, a one-line description, a re-run of the whole suite and a re-run of the harness, all recorded — so a fix cannot be slipped in unmeasured. |
| T-98-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. 98-RESEARCH.md records the Package Legitimacy Audit as not applicable, and no task runs a package-manager install. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2` — judged on the failed-test count being 0.
2. `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server` — A2 at or below 25, A and B not worse than their baselines.
3. `98-CONFORMANCE.md` and `COVERAGE.md` exist, and neither contains a corpus file name, corpus path or corpus source line.
4. `git -C /home/coder/repos/bbj-language-server status --porcelain` lists nothing under `bbj-vscode/src/language/generated/`.
</verification>

<success_criteria>
- Whole suite green on the failed-test count.
- A2 at or below 25 from a baseline of 267; A and B not regressed.
- Every remaining A2 group recorded with a count, a one-line cause and a disposition.
- Each of the phase's five named success criteria has recorded evidence, criterion 4 evaluated
  against its amended wording.
- No corpus identifier and no planning identifier crossed into a tracked source file.
</success_criteria>

<output>
Create `.planning/phases/98-line-break-validation-false-alarms-a2/98-06-SUMMARY.md` when done.
Record: the measured A, A2 and B values with their deltas; the residue table's dispositions in
one line each; any trivially-related fix made during triage with its file and change; and anything
handed to Phase 100. Record deviations in the SUMMARY only — do not open a WINDOWS.md ledger entry.
</output>