---
phase: 67-apply-easy-fixes
plan: 11
type: execute
wave: 11
depends_on: [67-10]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjNodeDownloader.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjCompileAction.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjEMTokenStore.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjIcons.java
  - bbj-intellij/src/main/resources/icons/bbj-config.svg
  - bbj-intellij/src/main/resources/icons/bbj-config_dark.svg
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerLogToolWindowFactory.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjColorSettingsPage.java
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`BbjNodeDownloader.java`'s download path is expressed as a `Platform` helper plus discrete buildUrl / download / extract / install / cleanup steps, invoked in the same order and with the same conditions as the original single method."
    - "Every Javadoc claim corrected in this plan describes what the code beside it actually does, verified by reading that code."
    - "`BbjIcons.java` declares no icon constant pointing at a resource that is not present under `bbj-intellij/src/main/resources/icons/`."
    - "The two field additions `P63-D7-004` names are absent from `ComposerModels.java` — that record is deferred, not applied, and its ledger row says so with the reason and the test that would prove it."
    - "Every `bbj-intellij/` ledger row's `verification:` field states `review-only — no compile, no test ran (D-14)`."
    - statement: "The nine applied IntelliJ edits do not change plugin behaviour."
      verification: backstop
  artifacts:
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  key_links:
    - "P63-D8-005 and P63-D7-004 target the same file, ComposerModels.java. D8-005 is applied (a class-comment correction); D7-004 is deferred by D-15. The applied commit must touch comment lines only."
    - "No JDK 17 exists on this machine and `./gradlew build` aborts on a build-script version check, so nothing in this plan is compiled or tested. That gap is recorded in every row, not glossed."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT describe an unverifiable IntelliJ edit as verified — every row in this plan states that no compile and no test ran, and none claims a passing build"
    - requirement_id: FIX-02
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT apply a deferred record to avoid leaving a row open — P63-D7-004 stays unapplied until a supported JDK exists, and the ledger records the deferral rather than a silent application"
---

<objective>
Apply the 9 `bbj-intellij/` easy-fix findings and record the deferral of the tenth.

Purpose: this is the plan the environment cannot verify. `bbj-intellij/build.gradle.kts` targets
Java 17 and the only installed JDK is Temurin 25.0.3 at `/opt/java/default`, so `./gradlew build`
aborts on a build-script version check — not a bootstrap failure, a version check. Per D-14 the
verification is reading each finding's named edit against the applied diff, and the ledger says
plainly that no compile and no test ran for any of the nine.

Output: 9 commits, 10 ledger rows closed (9 applied, 1 deferred).
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each record
with the awk one-liner against `.planning/reviews/63-COVERAGE.md`. All ten records are `low`
severity; nine are D4/D8, so none takes a regression test (D-11).

**Java style to preserve.** `bbj-intellij` groups IntelliJ platform imports and project imports with
no blank-line separation, and uses Javadoc block comments on classes and public methods. Match the
surrounding file in every edit.

**The comment-only proof.** For each D8 row, after committing, confirm every changed line lies
inside a comment:

    git show <sha> -U0 -- <file> | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)'

Every printed line's content after the leading `+`/`-` must begin with `*`, `/**`, `*/` or `//`.
Paste that output into the row's `verification:` field. This is the strongest check available
without a compiler, and it is a real one.
</phase_conventions>

<tasks>

<task type="auto">
  <name>Task 1: BbjNodeDownloader, BbjCompileAction, BbjEMTokenStore, BbjIcons</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjNodeDownloader.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjCompileAction.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjEMTokenStore.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjIcons.java,
    bbj-intellij/src/main/resources/icons/bbj-config.svg,
    bbj-intellij/src/main/resources/icons/bbj-config_dark.svg
  </files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjNodeDownloader.java — the whole file. The
      `P63-D4-001` refactor spans `97-165` plus the sites at `50,103,125,136-139,148,152`, and
      `P63-D8-001` targets the Javadoc at `42-45`. Read both together before either edit.
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjCompileAction.java — lines `14-17`
      and `20-22`, plus the action's actual body so the appended caveat is accurate
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjEMTokenStore.java — lines `15-18`
      plus the code that actually stores the token, so the softened claim matches it
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjIcons.java — the whole file (short); line
      `14` is the `CONFIG` constant
    - `git ls-files bbj-intellij/src/main/resources/icons/` — the tracked icon set, and its count,
      before any deletion
    - Finding records `P63-D4-001`, `P63-D8-001`, `P63-D8-002`, `P63-D8-003`, `P63-D4-014` via the
      awk extractor
  </read_first>
  <action>
    Five findings, one commit each, no regression tests (D-11: D4/D8).

    1. **P63-D4-001** (`BbjNodeDownloader.java`, D4, low, effort 4). Exact edit per the record's
       test-5 clause: extract a small `Platform` helper/enum and split `downloadAndExtractNode` into
       buildUrl / download / extract / install / cleanup steps.

       **This row carries more behavioural risk than the other eight and must be treated
       accordingly.** D-14 groups it with the edits that "cannot change bytecode behaviour", but a
       five-way method split is a control-flow restructuring, not a rename — and no compiler will
       catch a mistake here. Before committing, write out in the ledger row's `notes:` a
       statement-by-statement trace showing that each extracted step contains the same statements, in
       the same order, under the same conditions, with the same exception and cleanup semantics as
       the original — in particular that any `finally`/cleanup block still runs on every path it ran
       on before. Also record the divergence from D-14's characterisation, per CONTEXT §Specific
       Ideas: a discrepancy is a finding, not something to adjust silently.
       Commit: `refactor(P63-D4-001): extract a Platform helper and split downloadAndExtractNode`.
    2. **P63-D8-001** (`BbjNodeDownloader.java:42-45`, D8). Exact edit per the test-5 clause: add one
       sentence to the Javadoc noting the directory-creation side effect. Confirm from the code which
       directory is created and under what condition before writing the sentence.
       Commit: `docs(P63-D8-001): note the directory-creation side effect in the Javadoc`.
    3. **P63-D8-002** (`actions/BbjCompileAction.java:14-17,20-22`, D8). Exact edit per the test-5
       clause: append a "not yet implemented" caveat naming the referral `P63-D7-001` to the doc
       comment. Read the action body first and confirm the capability is in fact not implemented
       before asserting it. Commit: `docs(P63-D8-002): mark the compile action as not yet implemented`.
    4. **P63-D8-003** (`actions/BbjEMTokenStore.java:15-18`, D8). Exact edit per the test-5 clause:
       soften the storage claim to say the token is stored via IntelliJ's PasswordSafe, backed by
       whichever credential store the user has configured — a native keychain, KeePass, or none.
       The point of the fix is that the original claim overstates the guarantee; the replacement must
       not overstate it either. Commit: `docs(P63-D8-003): describe PasswordSafe backing accurately`.
    5. **P63-D4-014** (`BbjIcons.java:14`, D4). Exact edit per the test-5 clause: delete the `CONFIG`
       icon constant at line 14 and the two now-unused `bbj-config*.svg` resource files under
       `bbj-intellij/src/main/resources/icons/`. Before deleting, confirm they are genuinely unused:
       `grep -rn 'CONFIG\|bbj-config' bbj-intellij/src/` and check
       `bbj-intellij/src/main/resources/META-INF/plugin.xml` for any icon reference. If any reference
       survives, do not delete — record the reference in the ledger row and leave the row as a
       reasoned partial. Never touch anything under `bbj-intellij/build/`; that is build output.
       Commit: `refactor(P63-D4-014): remove the unused config icon and its resources`.
  </action>
  <verify>
    <automated>test ! -f bbj-intellij/src/main/resources/icons/bbj-config.svg &amp;&amp; test ! -f bbj-intellij/src/main/resources/icons/bbj-config_dark.svg &amp;&amp; echo ICONS_REMOVED</automated>
    <automated>for f in $(git ls-files 'bbj-intellij/src/main/java/**/*.java'); do python3 -c "import sys;s=open('$f').read();sys.exit(0 if s.count('{')==s.count('}') else 1)" || echo "UNBALANCED $f"; done; echo BRACE_CHECK_DONE</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -5` shows commits scoped `P63-D4-001`, `P63-D8-001`, `P63-D8-002`,
      `P63-D8-003`, `P63-D4-014`
    - For each of the three D8 commits, the comment-only proof described in `<phase_conventions>`
      prints only lines beginning `*`, `/**`, `*/` or `//`, and that output is pasted into the row's
      `verification:` field
    <!-- planner-discipline-allow: bbj-config, CONFIG -->
    - `grep -rn 'bbj-config' bbj-intellij/src/` returns no hits at HEAD
    - `git ls-files bbj-intellij/src/main/resources/icons/ | wc -l` is exactly two fewer than the
      count recorded before the deletion
    - `git status --porcelain bbj-intellij/build/` shows nothing staged or committed from the build
      output directory
    - The brace-balance check above prints no `UNBALANCED` line
    - The `P63-D4-001` row's `notes:` contains the statement-by-statement ordering trace AND the
      recorded divergence from D-14's "cannot change bytecode behaviour" characterisation
    - Every one of the five rows' `verification:` field contains the exact text
      `review-only — no compile, no test ran (D-14)`
  </acceptance_criteria>
  <done>
    Five IntelliJ findings applied, each verified by the strongest check available without a
    compiler, with the one genuinely behavioural row carrying an explicit ordering trace.
  </done>
</task>

<task type="auto">
  <name>Task 2: ComposerModels (applied part only), the two UI Javadocs, and the color settings sample</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerLogToolWindowFactory.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjColorSettingsPage.java
  </files>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java — the whole
      file. `P63-D8-005` targets the class comment at `1-14`; `P63-D7-004` targets `18-23,75-84` and
      is **deferred** — read its record so you know precisely what must NOT be added.
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerLogToolWindowFactory.java — the
      Javadoc at `14-17` and the window construction at `20-41`, so the corrected Javadoc describes
      what the window actually shows
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java — the class doc at
      `24-28` and whatever `scheduleRestart()` currently does (or does not do)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjColorSettingsPage.java — lines `114-122`,
      the block-comment sample inside the demo text
    - Finding records `P63-D8-005`, `P63-D8-006`, `P63-D8-007`, `P63-D8-008`, and `P63-D7-004` via
      the awk extractor
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-15 (the deferral) and §D-14
  </read_first>
  <action>
    Four findings applied, one deferred.

    1. **P63-D8-005** (`ComposerModels.java:1-14`, D8). Exact edit per the record's test-5 clause:
       soften the class comment's "mirroring" claim to say the models carry the language server's
       `bbj/composer/*` params and results relevant to the IntelliJ dialogs, or add a one-line note
       naming the two intentionally-unused optional fields. Say which branch you took in the ledger
       row's `notes:`. The commit must touch **comment lines only** — `P63-D7-004`, whose record names
       two field additions in this same file, is deferred per D-15 and must not be applied here, now
       or incidentally. Read its record to know exactly what to leave alone.
       Commit: `docs(P63-D8-005): describe what the composer models actually carry`.
    2. **P63-D8-006** (`ui/BbjServerLogToolWindowFactory.java:14-17,20-41`, D8). The record's test-5
       clause offers two branches: correct the Javadoc to describe what the window actually shows,
       **or** — explicitly named in the record as behaviour-changing and outside this record's
       easy-fix scope — wire the process's stdout/stderr into the console. Take the Javadoc branch and
       record that the record itself puts the other branch out of scope.
       Commit: `docs(P63-D8-006): describe what the server log tool window actually shows`.
    3. **P63-D8-007** (`ui/BbjServerService.java:24-28`, D8). The record's test-5 clause offers:
       remove the "debounced restart scheduling" claim from the class doc, **or** wire
       `scheduleRestart()` as the behaviour-changing alternative `P63-D2-013` itself names.
       `P63-D2-013` is a separate record and not in this phase's apply set — take the doc branch and
       record that reasoning. Commit: `docs(P63-D8-007): drop the unimplemented debounced-restart claim`.
    4. **P63-D8-008** (`BbjColorSettingsPage.java:117-120`, D8). Exact edit per the test-5 clause:
       correct the block-comment opener in the demo sample at line 117 to the form the record names.
       This is sample text inside the settings-page preview, so the change is visible to a user
       opening the BBj color settings page — set `user_facing: yes` on this row.
       Commit: `docs(P63-D8-008): correct the block-comment opener in the color settings sample`.
    5. **P63-D7-004** — **not applied.** Per D-15 it is the one easy-fix record this phase
       deliberately holds: D-11 requires a regression test for a D7 parity fix, no Gradle test can run
       without a JDK 17, and applying it untested or reclassifying it are both worse than holding it.
       Land no commit. Set the ledger row to `verdict: deferred`, `commit: none — deferred per D-15`,
       and write into `notes:` (a) the reason in those terms, (b) the two field additions the record
       names, quoted from the record, and (c) the test that would prove it once a supported JDK
       exists — a Gradle test asserting the IntelliJ DTOs round-trip the language server's
       `bbj/composer/*` params field-for-field.
  </action>
  <verify>
    <automated>git diff --stat HEAD~4..HEAD -- bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java</automated>
    <automated>for f in $(git ls-files 'bbj-intellij/src/main/java/**/*.java'); do python3 -c "import sys;s=open('$f').read();sys.exit(0 if s.count('{')==s.count('}') else 1)" || echo "UNBALANCED $f"; done; echo BRACE_CHECK_DONE</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` shows four `docs(P63-D8-00N):` commits for 005, 006, 007, 008
    - For each of the four, the comment-only proof from `<phase_conventions>` prints only lines whose
      content begins `*`, `/**`, `*/` or `//`, and that output is pasted into the row's `verification:`
    - The `P63-D8-005` commit's diff adds no field declaration and no method to `ComposerModels.java`
      — the comment-only proof above is the check
    - `git diff <plan-start>..HEAD -- bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java`
      shows only comment-line changes; the class's field set is identical before and after
    - The `P63-D7-004` row has `verdict: deferred`, `commit: none — deferred per D-15`, and a `notes:`
      field containing all three required elements (reason, the quoted field additions, the proving test)
    - The brace-balance check prints no `UNBALANCED` line
    - The `P63-D8-008` row has `user_facing: yes`
    - Every one of the five rows' `verification:` field contains `review-only — no compile, no test ran (D-14)`
      (the deferred row states that no edit was made at all)
  </acceptance_criteria>
  <done>
    Four more IntelliJ doc claims match their code, and the one deferred record is recorded as
    deferred with its reason and its future proof, not quietly applied or quietly dropped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the ten ledger rows, re-record the gradle gap, and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the ten `P63-*` rows)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md §`## gradlew build`
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-07, §D-08, §D-09, §D-14, §D-15
  </read_first>
  <action>
    Complete every field of the ten `P63-*` rows. Every applied row's `verification:` says
    `review-only — no compile, no test ran (D-14)` followed by the comment-only proof output or, for
    `P63-D4-001`, the ordering trace. Decide `user_facing:` per row: a Javadoc correction is not
    user-facing, but `P63-D8-008` changes sample text a user sees in the settings page and
    `P63-D4-014` removes an icon resource — decide each against its own record and say why in `notes:`
    where the answer is not obvious.

    Then re-run `./gradlew build` from `bbj-intellij/` and capture its output verbatim. Compare it to
    `67-BASELINE.md`'s `## gradlew build` section: the expected result is the identical build-script
    Java version check failure (`build.gradle.kts` sets `sourceCompatibility`/`targetCompatibility`
    to `JavaVersion.VERSION_17`; the only installed JDK is Temurin 25.0.3 at `/opt/java/default`).
    If the failure text differs in any way, that is a change caused by this plan's edits and must be
    investigated before the plan closes. Append the captured output and the comparison verdict to
    `67-BASELINE.md` under `### Plan 67-11 gradle re-check`.

    Then run the plan-level baseline delta for `bbj-vscode/` as usual (`npm test`, `npm run lint`) —
    no `bbj-vscode/` file changed in this plan, so the expected verdict is `identical`. Append
    `### Plan 67-11 delta`.
    Commit: `docs(67-11): close the IntelliJ rows, record the gradle gap and the baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-intellij &amp;&amp; ./gradlew build 2>&amp;1 | tail -30; true</automated>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - None of the ten `P63-*` rows contains `TBD` or `pending` in any field
    - Nine rows have `verdict: applied` with a resolvable `commit:` sha; one (`P63-D7-004`) has
      `verdict: deferred` with `commit: none — deferred per D-15`
    - All ten rows' `verification:` fields contain `review-only — no compile, no test ran (D-14)`
    - All ten rows have `fail_before:` beginning `inapplicable —` with a reason naming either D-11's
      no-behaviour-change dimension default or the absence of a JDK 17
    - `67-BASELINE.md` contains `### Plan 67-11 gradle re-check` with the captured output and a
      verdict of `identical to baseline` or a written explanation of the difference
    - `67-BASELINE.md` contains `### Plan 67-11 delta` with a verdict of `identical`
    - `cd bbj-vscode && npm run lint` exits 0 with zero warnings
    - Every row has `user_facing:` set to exactly `yes` or `no`
  </acceptance_criteria>
  <done>
    Ten rows closed, the JDK gap re-measured against the baseline rather than assumed, and the
    unverifiability of these nine edits recorded in every one of their rows.
  </done>
</task>

</tasks>

<flagged_assumptions>
Surfaced by this plan and carried into the close-out (plan 67-12), not silently accepted:

1. **D-14 characterises `P63-D4-001` as an edit that "cannot change bytecode behaviour."** It is a
   five-way split of `downloadAndExtractNode` plus a new `Platform` helper — a control-flow
   restructuring, not a rename. With no compiler and no test available, this row's risk is materially
   higher than the eight comment/naming edits it is grouped with. The plan's mitigation is the
   mandatory statement-by-statement ordering trace in its ledger row; the divergence from D-14's
   wording is recorded rather than adjusted away.
2. **The `must_haves` truth "the nine applied IntelliJ edits do not change plugin behaviour" is
   marked `verification: backstop`.** No compile and no test can confirm it in this environment. At
   verify time it abstains to `human_needed` rather than passing silently — which is the correct
   outcome, and the same shape D-14 already asks the close-out to state.
</flagged_assumptions>

<artifacts_this_phase_produces>
No new file paths. Two files are **deleted**:
`bbj-intellij/src/main/resources/icons/bbj-config.svg` and
`bbj-intellij/src/main/resources/icons/bbj-config_dark.svg` (P63-D4-014), together with the `CONFIG`
constant at `BbjIcons.java:14`.

New symbols in Java (P63-D4-001 only): a `Platform` helper/enum and the private step methods split
out of `downloadAndExtractNode` — `buildUrl`, `download`, `extract`, `install`, `cleanup` or the
names the finding record uses. All are internal to `BbjNodeDownloader`; no public plugin API, no
extension-point signature, and no `plugin.xml` entry changes.

`bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` gains **no** field:
`P63-D7-004`'s two additions are deferred per D-15.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote Node distribution → `BbjNodeDownloader` | the downloader builds a URL, fetches an archive, extracts it and installs it onto the local filesystem |
| user credentials → `BbjEMTokenStore` / IntelliJ PasswordSafe | an EM token is persisted through PasswordSafe, whose actual backing store depends on user configuration |
| plugin resources → IntelliJ runtime | `plugin.xml` and the icon resource set are loaded by the platform at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-11-01 | Tampering | archive extraction path handling inside the split `extract` step (P63-D4-001) | high | mitigate | The refactor must move statements, not change them: no path-construction, no extraction-target and no validation logic may be altered. The statement-by-statement ordering trace in the ledger row is the check, since no compiler or test will catch a change here. Any pre-existing zip-slip exposure is a D1 concern, therefore `major-refactor`, and routed to Phase 68 — this plan must not make it worse. |
| T-67-11-02 | Tampering | download URL construction moving into a new `Platform` helper (P63-D4-001) | high | mitigate | The built URL must be byte-identical for every platform branch the original handled. Record the original and refactored URL forms per branch in the ledger row's trace. |
| T-67-11-03 | Denial of Service | a cleanup/`finally` block no longer running after the split (P63-D4-001) | medium | mitigate | The ordering trace explicitly asserts that any cleanup runs on every path it ran on before, including exception paths. |
| T-67-11-04 | Information Disclosure | the softened `BbjEMTokenStore` Javadoc (P63-D8-003) | medium | mitigate | The correction reduces an overstated security guarantee to an accurate one — a user relying on the original claim would have believed the token was always in a native keychain. The replacement must not overstate either; it names the "or none" case explicitly. |
| T-67-11-05 | Denial of Service | deleting an icon resource still referenced at runtime (P63-D4-014) | medium | mitigate | Before deleting, grep `bbj-intellij/src/` and check `META-INF/plugin.xml` for any reference; a surviving reference blocks the deletion and the row becomes a reasoned partial. A missing icon resource would throw at plugin startup with no compile-time signal. |
| T-67-11-06 | Repudiation | presenting unverified Java edits as verified | high | mitigate | Every row's `verification:` field states `review-only — no compile, no test ran (D-14)`, and the corresponding `must_haves` truth is marked `verification: backstop` so it abstains at verify time rather than passing silently. |
| T-67-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan; `bbj-intellij/build.gradle.kts` is not edited and no Gradle dependency changes. |
</threat_model>

<verification>
- `test ! -f bbj-intellij/src/main/resources/icons/bbj-config.svg` and the `_dark` variant both succeed
- `grep -rn 'bbj-config' bbj-intellij/src/` returns no hits
- Every tracked `bbj-intellij` Java file has balanced braces
- Each D8 commit's changed lines are all comment lines, proven by the `git show -U0` check
- `./gradlew build` from `bbj-intellij/` fails with the same build-script Java version check as the baseline
- `cd bbj-vscode && npm test && npm run lint` — verdict `identical`, lint clean
- Ten ledger rows closed: 9 applied, 1 deferred
</verification>

<success_criteria>
- 9 IntelliJ findings applied as single commits; `P63-D7-004` recorded as deferred with its reason and proving test
- Every row states that no compile and no test ran
- `P63-D4-001`'s row carries a statement-by-statement ordering trace and the recorded divergence from D-14
- Nothing under `bbj-intellij/build/` is committed
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-11-SUMMARY.md` when done
</output>
</content>
</invoke>
