---
phase: 67-apply-easy-fixes
plan: 05
type: execute
wave: 5
depends_on: [67-04]
files_modified:
  - bbj-vscode/src/language/bbj-value-converter.ts
  - bbj-vscode/src/language/bbj-lexer.ts
  - bbj-vscode/src/language/bbj-cpl-parser.ts
  - bbj-vscode/src/language/validations/check-variable-scoping.ts
  - bbj-vscode/src/language/lib/events.ts
  - bbj-vscode/test/value-converter.test.ts
  - bbj-vscode/test/lexer.test.ts
  - bbj-vscode/test/cpl-parser.test.ts
  - bbj-vscode/test/variable-scoping.test.ts
  - bbj-vscode/test/builtin-functions-library.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A BBj string literal containing a doubled quote yields a single quote in the converted AST value; a literal with no doubled quote is unchanged."
    - "A source file with mixed CRLF and LF line endings tokenizes with every token offset matching the original text, and each line's own original EOL is preserved rather than replaced by one globally detected EOL."
    - "A BBjCPL diagnostic reported on line 0 or with a non-numeric line field maps to a non-negative zero-based line, never to -1."
    - "Variable-scoping validation skips excluded subtrees by pruning the iterator, so no node inside an excluded subtree is visited, and the diagnostics produced are identical to the pre-fix diagnostics for non-excluded input."
    - "The builtin events library declares each event exactly once; no name is declared twice."
  artifacts:
    - bbj-vscode/test/value-converter.test.ts
  key_links:
    - "P61-D8-002 (bbj.langium:948) names P61-D2-005 as its alternative resolution; D2-005 lands here, so plan 67-06 judges that comment against the fixed converter."
    - "bbj-lexer.ts's prepareLineSplitter runs before every parse — a length-non-preserving change there shifts every downstream offset, so offset equality is a required assertion, not an optional one."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a regression test as fail-before unless the red commit exists in git history and the test was observed failing on the pre-fix tree — no vacuous test, no attested-but-unobserved red"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make the suite look better by deleting, skipping, weakening or timeout-tuning any existing test or test config — every change to a test's status must trace to a finding ID"
---

<objective>
Apply the five remaining single-fix D2 correctness findings in `bbj-vscode/src/language/`:
value conversion, the line-splitting lexer, the BBjCPL output parser, variable-scoping validation,
and the builtin events library.

Purpose: each is one file, one named edit, one red-then-green pair. Grouping them keeps the
per-plan commit count in the D-09 band while every file stays owned by exactly one plan.

Output: 10 commits, 5 ledger rows closed.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each record
with the awk one-liner. The `classification:` test-5 clause names the exact edit. Remember BBj is
case-insensitive — any pattern or comparison touched here must stay case-insensitive.
</phase_conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Value conversion, line-ending handling, and CPL line clamping</name>
  <files>
    bbj-vscode/src/language/bbj-value-converter.ts,
    bbj-vscode/src/language/bbj-lexer.ts,
    bbj-vscode/src/language/bbj-cpl-parser.ts,
    bbj-vscode/test/value-converter.test.ts,
    bbj-vscode/test/lexer.test.ts,
    bbj-vscode/test/cpl-parser.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-value-converter.ts (17 lines — read fully)
    - bbj-vscode/src/language/bbj-lexer.ts (37 lines — read fully, especially `prepareLineSplitter`)
    - bbj-vscode/src/language/bbj-cpl-parser.ts — the range `40-46` plus the enclosing function
    - Finding records `P61-D2-005`, `P61-D2-006`, `P61-D2-009` via the awk extractor against
      `.planning/reviews/61-COVERAGE.md`
    - bbj-vscode/src/language/bbj.langium — the `STRING_LITERAL` terminal (its `([^"]|"{2})*` body is
      what makes the doubled-quote un-escape correct) and the comment at line 948
    - bbj-vscode/test/lexer.test.ts and bbj-vscode/test/cpl-parser.test.ts — the existing homes
    - bbj-vscode/test/validation.test.ts:1-23 — Shared Pattern A, the import/harness shape to match
  </read_first>
  <behavior>
    - P61-D2-005 red: a BBj string literal written with a doubled quote (the grammar's own
      `"{2}` escape) converts to an AST value that still contains both quote characters.
    - P61-D2-005 green: it converts to a single quote character; a literal containing no doubled
      quote converts exactly as before; an empty literal converts to the empty string.
    - P61-D2-006 red: a source text mixing CRLF and LF line endings loses the per-line ending — every
      line is re-emitted with one globally detected EOL, so token offsets shift relative to the
      original text.
    - P61-D2-006 green: each line's own original EOL is preserved (or the input is normalized before
      the function in a length-preserving way), and every token offset equals the offset computed
      against the original text. A file with a single line ending style, and a file with no trailing
      newline, both tokenize exactly as before.
    - P61-D2-009 red: a BBjCPL diagnostic reporting line 0 maps to zero-based line -1.
    - P61-D2-009 green: it maps to 0; a diagnostic on line 5 still maps to 4.
  </behavior>
  <action>
    Three findings, each red-then-green, in this order.

    1. **P61-D2-005** (`bbj-value-converter.ts:14`, D2, medium, effort 2). Exact edit per the
       record's test-5 clause: `input.slice(1, -1).replace(/""/g, '"')`. Create
       `bbj-vscode/test/value-converter.test.ts` for the red test rather than extending
       `test/parser.test.ts`, which carries three disabled assertions tracked separately as DEBT-02
       and must not be disturbed by this phase. Commits:
       `test(P61-D2-005): add failing test for doubled-quote string literal conversion`, then
       `fix(P61-D2-005): un-escape doubled quotes when converting string literals`.
    2. **P61-D2-006** (`bbj-lexer.ts:11-34`, D2, medium, effort 4). Exact edit per the test-5 clause:
       track and re-emit each line's own original EOL instead of a single detected `eol`, **or**
       reject/normalize before this function runs in a length-preserving way. Whichever branch you
       take, say which in the ledger row's `notes:` and prove length preservation — the red test must
       assert token offsets against the original text, not just that parsing succeeds. Add the tests
       to `bbj-vscode/test/lexer.test.ts`. Commits:
       `test(P61-D2-006): add failing test for mixed CRLF and LF line endings`, then
       `fix(P61-D2-006): preserve each line's original EOL in prepareLineSplitter`.
    3. **P61-D2-009** (`bbj-cpl-parser.ts:40-46`, D2, low, effort 2). Exact edit per the test-5
       clause: `Math.max(0, parseInt(match[1], 10) - 1)`. Add the test to
       `bbj-vscode/test/cpl-parser.test.ts`. Commits:
       `test(P61-D2-009): add failing test for a CPL diagnostic reported on line 0`, then
       `fix(P61-D2-009): clamp the converted CPL line number at zero`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/value-converter.test.ts test/lexer.test.ts test/cpl-parser.test.ts test/parser.test.ts test/example-files.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -6` shows `test(P61-D2-005):`→`fix(P61-D2-005):`→`test(P61-D2-006):`→`fix(P61-D2-006):`→`test(P61-D2-009):`→`fix(P61-D2-009):`
    - Checking out each of the three red commits and running its test file exits non-zero; each
      observed failure is recorded in the matching `fail_before:` field
    - `bbj-vscode/test/value-converter.test.ts` exists and asserts the doubled-quote, no-doubled-quote
      and empty-literal cases
    - The `P61-D2-006` test asserts token offsets against the original mixed-ending text, and also
      covers a single-ending file and a file with no trailing newline
    - `git show --stat` for `fix(P61-D2-006):` touches only `bbj-vscode/src/language/bbj-lexer.ts`
    - `cd bbj-vscode && npx vitest run test/value-converter.test.ts test/lexer.test.ts test/cpl-parser.test.ts test/parser.test.ts test/example-files.test.ts` exits 0
    - `cd bbj-vscode && npm run build` exits 0
    - The `P61-D2-006` ledger row's `notes:` names which of the record's two branches was taken
  </acceptance_criteria>
  <done>
    Doubled quotes un-escape, mixed line endings survive tokenization with offsets intact, and CPL
    line numbers never go negative — each proven by a test observed red first.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Variable-scoping traversal pruning and the duplicate event declarations</name>
  <files>
    bbj-vscode/src/language/validations/check-variable-scoping.ts,
    bbj-vscode/src/language/lib/events.ts,
    bbj-vscode/test/variable-scoping.test.ts,
    bbj-vscode/test/builtin-functions-library.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/validations/check-variable-scoping.ts — the range `205-220` plus the
      enclosing function and `walkStatements`'s exclusion logic, which the fix must mirror
    - bbj-vscode/src/language/lib/events.ts — lines `50-70` and `515-540` (the two declaration sites
      the record names: `57`/`528` and `62`/`533`)
    - Finding records `P61-D2-010` and `P61-D2-019` via the awk extractor. `P61-D2-010`'s
      `evidence:` field carries a reproduction — start the red test from it.
    - bbj-vscode/test/variable-scoping.test.ts (404 lines — the existing home; read its describe
      structure and one representative test, not the whole file)
    - bbj-vscode/test/builtin-functions-library.test.ts
  </read_first>
  <behavior>
    - P61-D2-010 red: a program whose excluded subtree contains a variable reference produces a
      diagnostic that should not be there — the bare `continue` in the `for...of` loop skips the
      node but still descends into its children.
    - P61-D2-010 green: the excluded subtree is pruned, no node inside it is visited, and every
      diagnostic produced for non-excluded input is identical to the pre-fix set.
    - P61-D2-019 red: an assertion that each event name in the builtin events library is declared
      exactly once fails on the duplicates at lines 520-533.
    - P61-D2-019 green: the assertion passes; every event previously resolvable still resolves.
  </behavior>
  <action>
    Two findings, each red-then-green.

    1. **P61-D2-010** (`check-variable-scoping.ts:205-220`, D2, medium, effort 4). Exact edit per the
       record's test-5 clause: use the TreeStream iterator's `prune()` method, **or** switch to a
       manual recursive walk mirroring `walkStatements`'s exclusion logic, instead of the bare
       `continue` in the `for...of` loop. Prefer `prune()` — it is the smaller edit and does not
       duplicate traversal logic; record the choice in the ledger row's `notes:`. Add the tests to
       `bbj-vscode/test/variable-scoping.test.ts` following Shared Pattern A. The green run must show
       every pre-existing case in that 404-line file still passing — a scoping fix that silences an
       existing diagnostic is a regression. Commits:
       `test(P61-D2-010): add failing test for descent into an excluded scoping subtree`, then
       `fix(P61-D2-010): prune excluded subtrees instead of continuing past them`.
    2. **P61-D2-019** (`lib/events.ts:57,528,62,533`, D2, low, effort 2). Exact edit per the test-5
       clause: remove or merge the duplicate declarations at lines 520-533. Read all four named
       lines before deciding remove-vs-merge — if the two declarations differ in any field the merge
       must preserve the union, and if they are identical the later one is simply removed. State
       which in the ledger row's `notes:`. Put the red test in
       `bbj-vscode/test/builtin-functions-library.test.ts` as a data-integrity assertion over the
       events library; if that file's harness does not reach `lib/events.ts`, create
       `bbj-vscode/test/events-library.test.ts` instead and record the choice. Commits:
       `test(P61-D2-019): add failing test for duplicate event declarations`, then
       `fix(P61-D2-019): remove the duplicate event declarations`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/variable-scoping.test.ts test/builtin-functions-library.test.ts test/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` shows `test(P61-D2-010):`→`fix(P61-D2-010):`→`test(P61-D2-019):`→`fix(P61-D2-019):`
    - Checking out each red commit and running its test file exits non-zero; each observed failure is
      recorded in the matching `fail_before:`
    - `cd bbj-vscode && npx vitest run test/variable-scoping.test.ts` exits 0 and reports no fewer
      passing tests than it did on the phase baseline — record the pre and post counts in the
      `P61-D2-010` ledger row's `notes:`
    - `cd bbj-vscode && npx vitest run test/builtin-functions-library.test.ts test/validation.test.ts` exits 0
    - `cd bbj-vscode && npm run build` exits 0
    - `git show --stat` for each `fix(` commit touches exactly one source file
    - Both ledger rows' `notes:` record the branch chosen from the record's two-option test-5 clause
  </acceptance_criteria>
  <done>
    Scoping validation no longer descends into excluded subtrees, and the events library declares
    each event once — both proven red first, with the existing diagnostic set unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the five ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the five rows for this plan)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09
  </read_first>
  <action>
    Complete every field of the rows `P61-D2-005`, `P61-D2-006`, `P61-D2-009`, `P61-D2-010`,
    `P61-D2-019`. Write `failure_scenario:` and `fix_applied:` as standalone DOC-01-ready prose.
    All five are D2 correctness defects with editor-visible symptoms — decide `user_facing:` per row
    against the record's own `failure_scenario`, and say `yes` where a BBj developer would see a
    wrong string value, a mis-highlighted mixed-ending file, a diagnostic on the wrong line, a
    spurious scoping warning, or a duplicated completion entry.

    Then run the plan-level baseline delta (D-09): from `bbj-vscode/`, `npm test` and `npm run lint`.
    `npm run lint` must still exit 0 with zero warnings — plan 67-04 reached that state and no later
    plan may regress it. Compare failing-test NAMES against the baseline gate set;
    identical-or-smaller. Exclude `beforeAll` hook-timeout suites as flaky per D-08 with the
    exclusion argued per occurrence. Append `### Plan 67-05 delta`. `./gradlew build` is not re-run.
    Commit: `docs(67-05): close the remaining language-core D2 rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - None of the five rows contains `TBD` or `pending` in any field
    - Every row's `commit:` is a `red-sha + green-sha` pair and both shas resolve via `git cat-file -e`
    - Every row's `fail_before:` begins `observed at ` with a resolvable sha
    - Every row has `user_facing:` set to exactly `yes` or `no`
    - `67-BASELINE.md` contains `### Plan 67-05 delta` with a verdict of `identical` or `smaller`
      and a recorded `npm run lint` exit code of 0 with zero warnings
  </acceptance_criteria>
  <done>Five rows closed with real shas; lint stays clean and the suite is no worse than baseline.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New file paths created by this plan:
- `bbj-vscode/test/value-converter.test.ts` — vitest module for `BBjValueConverter`
- `bbj-vscode/test/events-library.test.ts` — created only if `builtin-functions-library.test.ts`'s
  harness cannot reach `src/language/lib/events.ts`; the choice is recorded in the P61-D2-019 row

New symbols in source: none exported. The edits are a `.replace(/""/g, '"')` call in the value
converter, per-line EOL tracking inside `prepareLineSplitter`, a `Math.max(0, …)` clamp in the CPL
parser, a `prune()` call (or an equivalent recursive walk) in variable-scoping validation, and a
deletion of duplicate literal declarations in `lib/events.ts`. No public API, grammar rule, or LSP
contract changes — INVENTORY §3c test 2 requires this of every easy-fix record, and
`bbj.langium` is not edited by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.bbj` source text → lexer / value converter | arbitrary workspace file content, including hostile line endings and quote sequences |
| BBjCPL compiler stdout → CPL parser | the external compiler's diagnostic text is parsed with a regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-05-01 | Tampering | `prepareLineSplitter` offset shift (P61-D2-006) | high | mitigate | A length-non-preserving line-ending change shifts every downstream token offset, which would mis-place every diagnostic and every quick-fix range. The regression test asserts offset equality against the original text; this is the plan's blocking check, not an optional extra. |
| T-67-05-02 | Tampering | `bbj-value-converter.ts` un-escaping (P61-D2-005) | medium | mitigate | The edit is a literal `.replace(/""/g, '"')` on an already-sliced literal — it performs no evaluation, no interpolation and no unescaping of any other sequence. Phase 61's D1 sweep confirmed this converter evaluates nothing; the fix must not change that. |
| T-67-05-03 | Denial of Service | regex over untrusted CPL output (P61-D2-009) | low | accept | The clamp adds no new regex; Phase 61's D1 sweep found no catastrophic-backtracking shape in this parser's patterns. Recorded, not separately mitigated. |
| T-67-05-04 | Denial of Service | traversal pruning (P61-D2-010) | medium | mitigate | Pruning strictly reduces the nodes visited; the fix cannot increase traversal cost. Result equality against the pre-fix diagnostic set guards against pruning too much. |
| T-67-05-05 | Spoofing | a duplicate event declaration shadowing the intended one (P61-D2-019) | low | mitigate | Removing or merging the duplicate makes resolution single-valued; the regression test asserts each name is declared exactly once. |
| T-67-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install and no dependency change; INVENTORY §3c test 3 guarantees it for every easy-fix record. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0
- `cd bbj-vscode && npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && npx vitest run test/value-converter.test.ts test/lexer.test.ts test/cpl-parser.test.ts test/variable-scoping.test.ts test/builtin-functions-library.test.ts test/validation.test.ts test/parser.test.ts test/example-files.test.ts` exits 0
- Five ledger rows closed with no `pending`/`TBD`
- `### Plan 67-05 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- 5 D2 rows applied, each with a red commit observed failing and a green commit that follows it directly
- Token offsets are proven unchanged for single-ending input and proven correct for mixed-ending input
- `bbj.langium` is not edited (its comment fix is plan 67-06's `P61-D8-002`)
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-05-SUMMARY.md` when done
</output>
</content>
</invoke>
