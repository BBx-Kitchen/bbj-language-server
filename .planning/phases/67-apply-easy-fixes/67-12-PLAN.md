---
phase: 67-apply-easy-fixes
plan: 12
type: execute
wave: 12
depends_on: [67-11]
files_modified:
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
autonomous: false
requirements: [FIX-01, FIX-02, FIX-03, FIX-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The phase-close failing-test NAME set is identical to or a subset of the phase-start deterministic gate set recorded in 67-BASELINE.md; a set exactly equal to the baseline passes the D-07 gate, and any name absent from the baseline fails it."
    - "A suite marked failed purely by a `beforeAll` hook timeout in `WorkspaceManager.initializeWorkspace()` is excluded from the comparison as flaky per D-08, and each exclusion names the suite, quotes the timeout, and states whether it reproduced on a re-run."
    - "Failing-test names are compared as an unordered set; file-level and skip-level counts appear in 67-BASELINE.md only under an observations heading and are never cited as gate criteria (D-08)."
    - "`cd bbj-vscode && npm run lint` exits 0 with zero warnings at phase close."
    - "`67-APPLY-SET.md` contains exactly 77 rows: 73 applied or no-op, 2 excluded (P64-D8-003, P64-D8-004), 1 deferred (P63-D7-004), and the arithmetic 77 → 74 applied records → 73 distinct edits is written out."
    - "Every commit made during Phase 67 carries at least one finding ID in its subject, or is one of the plan-level documentation commits whose subject is scoped `67` or `67-NN`."
    - "Every ledger row with `verdict: applied` names a commit sha that resolves via `git cat-file -e`, and every row with `test_required: yes` names a red sha that is an ancestor of its green sha."
    - "The close-out states in plain words that FIX-03 was not literally met, names the interop failures and the JDK-17 gap as the reason, and does not claim a green suite."
    - "The close-out states in plain words that FIX-04 is not literally true at the end of Phase 67, because `.planning/reviews/EASY-FIXES.md` is Phase 68's DOC-01 deliverable, and points at the ledger fields Phase 68 lifts."
  artifacts:
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  key_links:
    - "67-APPLY-SET.md's failure_scenario / fix_applied / user_facing / commit fields are exactly what Phase 68's DOC-01 lifts into EASY-FIXES.md — a row missing any of them forces Phase 68 to re-derive."
    - "67-BASELINE.md's start capture and close capture are the two halves of the D-07 gate; neither is meaningful without the other."
  prohibitions:
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT state or imply that a requirement is met when it is not — the close-out names the residual failures and the un-written EASY-FIXES.md in plain words rather than restating the requirement's wording as though it had been satisfied"
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reach an identical-or-smaller verdict by widening the flaky exclusion — a suite may be excluded only for a `beforeAll` hook timeout, argued per occurrence, never because its failure is inconvenient"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit REQUIREMENTS.md, INVENTORY.md, any closed 6N-COVERAGE.md, or the GitHub tracker — FIX-01's and FIX-03's text stand unchanged (D-07, D-12), and Phase 69 is the only phase that files issues"
    - requirement_id: FIX-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT create `.planning/reviews/EASY-FIXES.md` or `MAJOR-REFACTORS.md` — those are Phase 68's DOC-01/DOC-02 deliverables, and writing them here would claim a phase boundary this phase does not own"
---

<objective>
Close Phase 67: measure the end state against the start baseline, audit the ledger for completeness,
and write down what was and was not achieved.

Purpose: this phase's two requirements that cannot be literally met — FIX-03's green suite and
FIX-04's `EASY-FIXES.md` — are the ones most likely to be quietly restated as though they had been.
D-07 and the FIX-04 discretion item both say the honest thing instead. This plan writes that down
and puts it in front of a human before the phase seals.

Output: the completed `67-BASELINE.md` phase-close section, the completed `67-APPLY-SET.md` with its
close-out audit, and a human-approved statement of the shortfalls.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The phase-close measurement and the FIX-03 verdict</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md — the whole file: `## start_commit`,
      `## npm test` with its `### Deterministic failures (gate set)`, `## npm run lint`,
      `## gradlew build`, `## Flaky exclusions (D-08)`, and every `### Plan 67-NN delta` appended
      by plans 67-01 through 67-11
    - .planning/reviews/INVENTORY.md §"Test & Build Baseline (D-05, D-06)" — the 11 named failing
      tests and the two-run variance table. Read this section only; do not read the whole file, and
      do not restate suite numbers from anywhere else.
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-07, §D-08, §D-09
    - .planning/REQUIREMENTS.md — FIX-03's exact wording, which is quoted but not edited
  </read_first>
  <action>
    Run the phase-close capture and write the verdict into `67-BASELINE.md`'s
    `## Phase-close delta` section, which plan 67-01 left empty.

    Record `git rev-parse HEAD` as `close_commit`. Then, from `bbj-vscode/`, run `npm test` twice —
    D-08's flaky-hook analysis is only meaningful across two runs — and `npm run lint` once. From
    `bbj-intellij/`, run `./gradlew build` once. Capture each verbatim.

    Then write these subsections:

    - `### Close capture` — the raw failing-test names from both `npm test` runs, the lint result,
      and the gradle failure text.
    - `### Flaky exclusions at close (D-08)` — for each suite marked failed purely by a `beforeAll`
      hook timeout in `WorkspaceManager.initializeWorkspace()`: the suite name, the quoted timeout,
      and whether it reproduced on the second run. A suite that failed in both runs for the same
      hook timeout is still flaky by D-08's definition, but say so explicitly rather than by
      omission. Nothing else may be excluded.
    - `### Gate comparison` — the phase-start deterministic gate set and the phase-close set, as two
      lists of full `file > describe > test` names, plus the set difference in both directions. The
      gate passes when the close set is identical to or a subset of the start set. State the verdict
      as `identical`, `smaller`, or `LARGER — gate failed` followed by every name that is new.
    - `### Observations (not gate criteria)` — file-level counts, skip counts, and total passing
      counts at start and close. D-08 forbids these as gate criteria; they are recorded because they
      are informative, and labelled so nobody later mistakes them for the gate.
    - `### FIX-03 verdict` — the plain-words statement. It must say that FIX-03 as written was not
      achieved; name the deterministic interop failures in
      `test/linking.test.ts > Linking Tests > Interop related tests` and their cause (the
      java-interop peer is unreachable and opening a listener on port 5008 does not fix them, per
      Phase 64 D-06); name the `./gradlew build` failure and its cause (`build.gradle.kts` targets
      `JavaVersion.VERSION_17` and the only installed JDK is Temurin 25.0.3 at `/opt/java/default`;
      Gradle 8.13 itself starts fine, so this is a version check, not a bootstrap rejection); state
      what *was* achieved — `npm run lint` clean, and a failure set identical to or smaller than the
      start baseline; and state that FIX-03's text in `REQUIREMENTS.md` is deliberately not edited.
      Do not restate FIX-03's wording as though it had been met.

    `REQUIREMENTS.md` is not edited by this task or any other in this phase.
    Commit: `docs(67): record the phase-close baseline delta and the FIX-03 verdict`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -60</automated>
    <automated>cd bbj-intellij &amp;&amp; ./gradlew build 2>&amp;1 | tail -20; true</automated>
    <automated>git diff --quiet HEAD -- .planning/REQUIREMENTS.md &amp;&amp; git log --oneline -20 -- .planning/REQUIREMENTS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `67-BASELINE.md` contains all five new subsections: `### Close capture`,
      `### Flaky exclusions at close (D-08)`, `### Gate comparison`,
      `### Observations (not gate criteria)`, `### FIX-03 verdict`
    - `### Gate comparison` lists both sets as full `file > describe > test` names and states a
      verdict of `identical`, `smaller`, or `LARGER — gate failed` with every new name enumerated
    - Every entry under `### Flaky exclusions at close (D-08)` names a `beforeAll` hook timeout; no
      exclusion has any other stated cause
    - `cd bbj-vscode && npm run lint` exits 0 with zero warnings
    - `### FIX-03 verdict` contains the words `not achieved` (or an equally plain equivalent), names
      the interop failures, names the JDK-17 version check, and states that `REQUIREMENTS.md` is not edited
    - `git log --oneline -- .planning/REQUIREMENTS.md` shows no commit authored during Phase 67
    - `git rev-parse HEAD` is recorded in the file as `close_commit`
  </acceptance_criteria>
  <done>
    The end state is measured against the start state by the rule D-07 and D-08 define, and the
    shortfall is written in plain words rather than papered over.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ledger completeness audit and the FIX-01 / FIX-02 / FIX-04 close-out</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md — the whole file, all 77 rows
    - .planning/phases/67-apply-easy-fixes/derive-apply-set.mjs
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-01, §D-03, §D-04, §D-12, §D-13, §D-15,
      §Claude's Discretion (the FIX-04 boundary)
    - .planning/ROADMAP.md §"Phase 68: Deliverable Documents" — DOC-01's field requirements, so the
      close-out can name exactly which ledger fields Phase 68 lifts
  </read_first>
  <action>
    Audit the ledger mechanically, then write the close-out.

    **Re-derivation check.** Re-run `node derive-apply-set.mjs` and confirm it still reports
    `total=77` with the split `61=44 62=14 63=10 64=8 65=0 66=1`. Confirm every finding ID the script
    emits has a corresponding row in `67-APPLY-SET.md` and that no row exists for an ID the script
    does not emit. A mismatch in either direction is a finding, not something to adjust silently.

    **Row completeness.** For all 77 rows assert: no field holds `TBD` or `pending`; `verdict:` is
    one of `applied`, `no-op`, `excluded`, `deferred`; every `applied` row's `commit:` sha resolves
    via `git cat-file -e`; every `test_required: yes` row's `fail_before:` names a red sha that
    `git merge-base --is-ancestor <red> <green>` confirms precedes its green sha; every row has
    `failure_scenario:`, `fix_applied:` and `user_facing:` populated; every `excluded`, `deferred`
    and `no-op` row carries a written reason.

    **Commit-trace check.** For the whole phase, `git log <start_commit>..HEAD --format=%s` and
    confirm every subject either names a finding ID in its scope or is a plan-level documentation
    commit scoped `67` or `67-NN`. List any subject that matches neither.

    **Write `## Close-out`** at the end of `67-APPLY-SET.md`, with:
    - `### Denominator` — 77 rows in; 2 excluded (`P64-D8-003`, `P64-D8-004`, on their own recorded
      reason per D-03); 1 deferred (`P63-D7-004`, per D-15); 74 applied records; 73 distinct edits,
      because `P61-D2-011` and `P66-D2-001` are one edit (D-04). Include the observed count of `no-op`
      rows and name each one with the commit that resolved it.
    - `### Commit reconciliation` — the projected 102 code commits (73 edits + 29 red-test commits,
      where 29 counts *edits* while D-11's table's 30 counts *records*) against the actual count from
      the commit-trace check. Explain any difference; every `no-op` row reduces the count by one or
      two, and that is expected, not an error.
    - `### FIX-01 verdict` — whether every applied edit landed as its own atomic commit referencing
      its finding ID, with the D-12 reading stated explicitly: a red/green commit pair is the atomic
      unit for a behaviour-changing fix, and the D-04 merged commit closes two rows. Note that
      FIX-01's text in `REQUIREMENTS.md` is not edited.
    - `### FIX-02 verdict` — how many fixes required a regression test, how many carried an observed
      red state, and how many recorded `fail_before: inapplicable` with which reason (D-13's D5 rows,
      D-16's workflow rows, D-11's no-behaviour-change dimensions).
    - `### FIX-04 verdict` — the plain-words statement. FIX-04 is **not literally true** at the end
      of Phase 67, because `.planning/reviews/EASY-FIXES.md` does not exist and is Phase 68's DOC-01
      deliverable. State what Phase 67 did instead: every row carries the exact fields DOC-01
      requires — finding ID, `file:line`, dimension, verified failure scenario, the fix applied, and
      the commit hash — plus a `user_facing:` flag, so Phase 68 assembles without re-deriving
      anything. Name the count of `user_facing: yes` rows. Do not create `EASY-FIXES.md`.
    - `### Recorded departures` — the D-06 departure (`P64-D4-004` applied without its paired
      `P64-D3-002`), the D-11→D-16 override on the three workflow rows, the off-scale `effort: 1`
      annotations carried verbatim, and any discovery a plan surfaced for Phase 68 (for example a
      real parse defect uncovered by `P61-D5-004`, or the `P63-D4-001` risk divergence from D-14).

    Commit: `docs(67): audit the apply set and record the FIX-01, FIX-02 and FIX-04 verdicts`.
  </action>
  <verify>
    <automated>cd .planning/phases/67-apply-easy-fixes &amp;&amp; node derive-apply-set.mjs > /dev/null &amp;&amp; echo DERIVE_OK</automated>
    <automated>grep -c '^row: ' .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md | grep -x 77</automated>
    <automated>grep -cE '^(verdict|commit): *(TBD|pending)' .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/67-apply-easy-fixes/derive-apply-set.mjs` exits 0 and reports
      `61=44 62=14 63=10 64=8 65=0 66=1 total=77`
    - `grep -c '^row: ' 67-APPLY-SET.md` prints `77` and `grep -c '^finding_id: ' 67-APPLY-SET.md` prints `77`
    - No line in `67-APPLY-SET.md` matches `^[a-z_]+: *(TBD|pending)`
    - `grep -c '^verdict: *excluded'` prints `2`, `grep -c '^verdict: *deferred'` prints `1`
    - Every `commit:` sha in the file resolves: a loop over the extracted shas running
      `git cat-file -e <sha>` exits 0 for each
    - For every row with `test_required: yes` and a `red + green` commit pair,
      `git merge-base --is-ancestor <red> <green>` exits 0
    - `git log <start_commit>..HEAD --format=%s` yields no subject that lacks both a finding ID and a
      `67`/`67-NN` doc scope — any such subject is listed in `### Commit reconciliation`
    - `67-APPLY-SET.md` contains the headings `## Close-out`, `### Denominator`,
      `### Commit reconciliation`, `### FIX-01 verdict`, `### FIX-02 verdict`, `### FIX-04 verdict`,
      `### Recorded departures`
    - `### FIX-04 verdict` states that FIX-04 is not literally true at the end of Phase 67 and names
      the count of `user_facing: yes` rows
    - `test ! -f .planning/reviews/EASY-FIXES.md` and `test ! -f .planning/reviews/MAJOR-REFACTORS.md` both succeed
    - `git status --porcelain .planning/reviews/` is empty — no COVERAGE file and not INVENTORY.md was touched
  </acceptance_criteria>
  <done>
    The denominator is provably closed: 77 rows, every one dispositioned with a written reason, every
    applied row traceable to a real commit, and the two requirements that were not literally met
    named as such.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of the two shortfall statements</name>
  <what-built>
    Phase 67 applied 73 distinct edits across roughly 100 commits, and closed all 77 ledger rows.
    Two of the phase's four requirements are **not** literally satisfied, by design, and the
    close-out says so:

    - **FIX-03** ("`npm test` and `npm run lint` are clean in `bbj-vscode/`, and `./gradlew build`
      succeeds in `bbj-intellij/`") — lint is clean. The suite is not: the deterministic interop
      failures in `test/linking.test.ts` remain, because the java-interop peer is unreachable here
      and opening a listener on port 5008 does not fix them. `./gradlew build` still aborts on a
      build-script Java version check, because the only installed JDK is Temurin 25.0.3 and
      `build.gradle.kts` targets Java 17. The gate applied instead was D-07's baseline delta:
      the close failure set is identical to or smaller than the start failure set.
    - **FIX-04** ("No applied fix changes user-facing behavior without that change being recorded in
      EASY-FIXES.md") — `.planning/reviews/EASY-FIXES.md` does not exist and is Phase 68's DOC-01
      deliverable. Phase 67 recorded every behaviour change in `67-APPLY-SET.md` in the exact field
      shape DOC-01 lifts, and deliberately did not write the document.

    Additionally, nine `bbj-intellij/` edits were applied with **no compile and no test** (D-14), and
    one easy-fix record (`P63-D7-004`) was deliberately **not applied** (D-15), both for the same
    missing-JDK reason.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/67-apply-easy-fixes/67-BASELINE.md` §`### FIX-03 verdict` and
       §`### Gate comparison`. Confirm the verdict matches the evidence above it and that no suite is
       excluded for a reason other than a `beforeAll` hook timeout.
    2. Read `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` §`## Close-out`. Confirm
       §`### FIX-04 verdict` says FIX-04 is not literally true at the end of this phase, and that
       §`### Recorded departures` lists every departure you expect.
    3. Spot-check three ledger rows of your choosing against `git show <commit>` — confirm the
       `fix_applied:` prose matches the diff and the `user_facing:` flag matches the failure scenario.
    4. Run `cd bbj-vscode && npm run lint` and confirm it exits 0 with zero warnings.

    The question to answer: are these two statements honest and complete, or is something being
    understated?
  </how-to-verify>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md §`### FIX-03 verdict`, §`### Gate comparison`
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md §`## Close-out`
    - .planning/REQUIREMENTS.md — FIX-01 through FIX-04 as written
  </read_first>
  <acceptance_criteria>
    - The human's response is appended verbatim to `67-APPLY-SET.md` under
      `### Close-out review` with the date
    - If the human names anything understated, that correction is applied to the verdict text and
      the corrected text is re-shown before the phase closes
    - `.planning/REQUIREMENTS.md` remains unmodified after the review
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or name what is understated</resume-signal>
</task>

</tasks>

<flagged_assumptions>
Carried forward, not resolved by this phase:

1. **FIX-04's edge probe returned `unclassified`** — the deterministic edge probe could not classify
   FIX-04 ("no applied fix changes user-facing behavior without that change being recorded in
   EASY-FIXES.md") into any edge category. It is therefore recorded here as an explicit unresolved
   assumption rather than auto-resolved with a backstop predicate. The substance of the ambiguity is
   real and matches CONTEXT's own FIX-04 discretion item: the requirement names a document this phase
   does not own, so "recorded in EASY-FIXES.md" cannot be evaluated within Phase 67 at all. Phase 67
   discharges its half by populating the ledger fields DOC-01 lifts; the requirement becomes
   evaluable only after Phase 68 writes the document. Verification of FIX-04 at this phase's seal
   should route to human judgement, not to a pass or a fail.
2. **The `must_haves` truth about the nine unverified IntelliJ edits (plan 67-11) is
   `verification: backstop`** and abstains to `human_needed` rather than passing. That is the correct
   outcome while no JDK 17 exists; provisioning one is recorded in CONTEXT §Deferred Ideas.
</flagged_assumptions>

<artifacts_this_phase_produces>
No new file paths. This plan completes two files created in plan 67-01:
- `.planning/phases/67-apply-easy-fixes/67-BASELINE.md` gains `### Close capture`,
  `### Flaky exclusions at close (D-08)`, `### Gate comparison`,
  `### Observations (not gate criteria)` and `### FIX-03 verdict`
- `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` gains `## Close-out` with
  `### Denominator`, `### Commit reconciliation`, `### FIX-01 verdict`, `### FIX-02 verdict`,
  `### FIX-04 verdict`, `### Recorded departures` and `### Close-out review`

Deliberately NOT created: `.planning/reviews/EASY-FIXES.md` and
`.planning/reviews/MAJOR-REFACTORS.md` (Phase 68's DOC-01/DOC-02). No source symbols are created or
changed by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 67 ledger → Phase 68 DOC-01 | `67-APPLY-SET.md` is lifted into a published deliverable; a wrong or missing row propagates into the milestone's public record |
| close-out verdict → milestone completion audit | the FIX-03 and FIX-04 verdicts are what a later audit reads to decide whether the milestone met its own requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-12-01 | Repudiation | a close-out that restates a requirement's wording as though it were met | high | mitigate | Both shortfall verdicts are written in plain words, name their causes, and are put in front of a human in a blocking checkpoint before the phase seals. The `must_haves` truths assert the presence of the "not achieved" language, not merely the presence of a verdict heading. |
| T-67-12-02 | Tampering | widening the D-08 flaky exclusion to reach an identical-or-smaller verdict | high | mitigate | Only a `beforeAll` hook timeout in `WorkspaceManager.initializeWorkspace()` may be excluded, argued per occurrence; the acceptance criteria assert that every exclusion names that cause and nothing else. |
| T-67-12-03 | Tampering | a ledger row naming a commit that does not exist or a red sha that does not precede its green | medium | mitigate | The audit runs `git cat-file -e` over every sha and `git merge-base --is-ancestor` over every red/green pair; both are machine checks, not readings. |
| T-67-12-04 | Repudiation | a row silently absent from the ledger | high | mitigate | `derive-apply-set.mjs` is re-run and its 77-ID output is diffed against the file's rows in both directions; the script exits non-zero on any count other than 77. |
| T-67-12-05 | Tampering | editing REQUIREMENTS.md, INVENTORY.md or a closed COVERAGE file to close a gap | high | mitigate | `git log -- .planning/REQUIREMENTS.md` must show no Phase 67 commit and `git status --porcelain .planning/reviews/` must be empty; both are asserted in the acceptance criteria. |
| T-67-12-06 | Information Disclosure | pasting raw command output containing absolute paths or tokens into a committed artefact | low | mitigate | Capture test names, lint warnings and the gradle failure text; redact any absolute path containing a credential before committing. |
| T-67-12-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no package-manager install; `npm test` and `npm run lint` use the tree already installed. |
</threat_model>

<verification>
- `node .planning/phases/67-apply-easy-fixes/derive-apply-set.mjs` exits 0 reporting total=77
- `grep -c '^row: ' 67-APPLY-SET.md` prints 77 and no field holds `TBD` or `pending`
- Every `commit:` sha resolves and every red sha precedes its green sha
- `cd bbj-vscode && npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && npm test` failing-test name set is identical to or a subset of the start gate set
- `cd bbj-intellij && ./gradlew build` fails with the same version check as the start baseline
- `git status --porcelain .planning/reviews/` is empty; `.planning/REQUIREMENTS.md` has no Phase 67 commit
- `.planning/reviews/EASY-FIXES.md` and `MAJOR-REFACTORS.md` do not exist
- The human checkpoint is approved and the response is recorded
</verification>

<success_criteria>
- 77 ledger rows, all dispositioned, all traceable, arithmetic written out
- The FIX-03 and FIX-04 shortfalls stated plainly and approved by a human
- No requirement text, finding record, COVERAGE file or tracker entry modified by this phase
- Phase 68's DOC-01 can be assembled from `67-APPLY-SET.md` without re-deriving anything
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-12-SUMMARY.md` when done
</output>
</content>
</invoke>
