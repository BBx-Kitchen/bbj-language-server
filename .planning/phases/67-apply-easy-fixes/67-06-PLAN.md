---
phase: 67-apply-easy-fixes
plan: 06
type: execute
wave: 6
depends_on: [67-05]
files_modified:
  - bbj-vscode/src/language/bbj-validator.ts
  - bbj-vscode/src/language/bbj-linker.ts
  - bbj-vscode/src/language/assertions.ts
  - bbj-vscode/src/language/main.ts
  - bbj-vscode/src/language/bbj-cpl-service.ts
  - bbj-vscode/src/language/bbj.langium
  - CLAUDE.md
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-03, FIX-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The dead validator methods at `bbj-validator.ts:266-311` are either removed or reachable from a registered check; the file contains no unreferenced private validation method left in place."
    - "`bbj-linker.ts` formats a source location through one shared helper called by both former call sites, and the formatted location strings for the same input are byte-identical to the pre-fix output."
    - "`bbj-vscode/src/language/assertions.ts` is either deleted or referenced from at least one real assertion site; `grep -rn 'assertions.js\\|assertTrue' bbj-vscode/src bbj-vscode/test` returns either zero hits or a genuine call site."
    - "The two handlers in `main.ts` call one shared `reloadJavaClassesAndRevalidate()` helper, and reload-plus-revalidate behaviour is unchanged."
    - "Running `npm run langium:generate` after the `bbj.langium` comment edit leaves `bbj-vscode/src/language/generated/` byte-identical — the edit changed a comment, not a grammar rule."
    - "CLAUDE.md's Key-services bullet list names `validations/check-function-calls.ts`, its Completion bullet accounts for the other LSP feature providers registered under `bbj-module.ts`'s `lsp` service group, and its TextMate section lists all four shared files."
  artifacts:
    - CLAUDE.md
  key_links:
    - "P61-D8-002's own record names P61-D2-005 (landed in plan 67-05) as its alternative resolution; judge the comment at bbj.langium:948 against the fixed value converter before editing it."
    - "bbj.langium is grammar source. INVENTORY §3c test 2 forbids a grammar change in an easy fix, so the generated-output-unchanged check is the proof that this stayed a comment edit."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let a documentation edit assert a capability the code does not have — a doc fix that replaces one inaccurate claim with another is worse than the original, because a reader who has read it stops checking"
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make the suite look better by deleting, skipping, weakening or timeout-tuning any existing test or test config — every change to a test's status must trace to a finding ID"
---

<objective>
Apply the 9 no-behaviour-change findings: 4 maintainability fixes and 2 comment fixes in
`bbj-vscode/src/language/`, plus the 3 stale-text fixes in the repo-root `CLAUDE.md`.

This is the first plan in the phase to leave `bbj-vscode/src/`. D-02: apply scope is every tree
except `.planning/`, and a finding is not deferred for living outside `src/` — so `P61-D8-003`,
`P61-D8-005` and `P62-D8-001` are applied to the repo-root `CLAUDE.md` in this phase, on the same
finding-keyed commit terms as any source fix, rather than handed to a later phase.

Purpose: D4 and D8 findings need no regression test per D-11, so they land as single commits —
but "no behaviour change" is a claim that must be *demonstrated*, which is why every extraction and
deletion in this plan is gated on the existing suite staying exactly as green as it was.

Output: 8–9 commits, 9 ledger rows closed.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. D4/D8 findings take a
single commit with no regression test (D-11); the verification is that the existing suite and build
stay green, plus the specific static check named per finding below.
</phase_conventions>

<tasks>

<task type="auto">
  <name>Task 1: The four maintainability findings — dead code, duplicated location formatting, duplicated reload handlers</name>
  <files>
    bbj-vscode/src/language/bbj-validator.ts,
    bbj-vscode/src/language/bbj-linker.ts,
    bbj-vscode/src/language/assertions.ts,
    bbj-vscode/src/language/main.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-validator.ts — the range `266-311` plus the check-registration
      block at the top of the class, and `src/language/validations/check-classes.ts`, whose copy of
      the same logic is why the range is dead
    - bbj-vscode/src/language/bbj-linker.ts — the range `155-212` (`getSourceLocation` and
      `getSourceLocationForNode`, the near-duplicate pair)
    - bbj-vscode/src/language/assertions.ts (4 lines — read fully)
    - bbj-vscode/src/language/main.ts — the ranges `32-73` and `147-188` (the two handlers)
    - Finding records `P61-D4-006`, `P61-D4-008`, `P61-D4-009`, `P61-D4-012` via the awk extractor
    - bbj-vscode/src/language/bbj-module.ts — how the validator and linker are registered, so a
      deletion cannot orphan a DI binding
  </read_first>
  <action>
    Four findings, one commit each, no regression tests (D-11: D4, no behaviour change). Before the
    first edit, capture the current suite result so "unchanged" is measurable:
    `cd bbj-vscode && npm test 2>&1 | tail -40` and note the passing/failing counts and names.

    1. **P61-D4-006** (`bbj-validator.ts:266-311`, D4, low, effort 2). The record's test-5 clause
       offers: delete the range, **or** wire it up if it was meant to replace `check-classes.ts`'s
       copy. Read both copies before choosing. If they are equivalent, delete the dead range — the
       record's test-4 clause states the existing suite staying green after removal is the evidence.
       If they differ, do not silently pick one: record the difference in the ledger row's `notes:`
       and take the branch you can defend. Confirm no DI binding or external reference points at the
       removed methods before deleting. Commit: `fix(P61-D4-006): remove the unreachable validator methods`
       (or `fix(P61-D4-006): wire the validator methods into the registered checks`).
    2. **P61-D4-008** (`bbj-linker.ts:155-212`, D4, low, effort 2). Exact edit per the test-5 clause:
       extract a shared `resolveWorkspaceRoot()` / `formatSourceLocation(uri, line)` helper called by
       both `getSourceLocation` and `getSourceLocationForNode`. Behaviour-preserving: the formatted
       location strings must be byte-identical for the same input. Verify with
       `npx vitest run test/linking.test.ts test/imports.test.ts test/unresolvable-type.test.ts` —
       linking-error messages embed these strings. Commit:
       `fix(P61-D4-008): extract shared source-location formatting helpers`.
    3. **P61-D4-009** (`assertions.ts:1-4`, D4, low, effort 2). The record's test-5 clause offers:
       delete `assertions.ts`, **or** wire it into a real assertion site. Phase 61's sweep confirmed
       via `grep -rn "assertions.js|assertTrue" bbj-vscode/src bbj-vscode/test` that the file has zero
       consumers anywhere in the tree — re-run that grep to confirm it is still true at HEAD, then
       delete the file. Commit: `fix(P61-D4-009): delete the unused assertions module`.
    4. **P61-D4-012** (`main.ts:32-73,147-188`, D4, medium, effort 4). Exact edit per the test-5
       clause: extract a private `reloadJavaClassesAndRevalidate()` helper called by both handlers.
       `main.ts` is the language server entry point and compiles into `out/language/main.cjs`, the
       single binary both IDEs consume — `npm run build` is a required gate on this commit, not just
       vitest. Commit: `fix(P61-D4-012): extract reloadJavaClassesAndRevalidate for both handlers`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; npx vitest run test/linking.test.ts test/imports.test.ts test/unresolvable-type.test.ts test/validation.test.ts test/classes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` contains exactly four commits, scoped `P61-D4-006`, `P61-D4-008`,
      `P61-D4-009`, `P61-D4-012`, and none of them is a `test(` commit
    - `git show --stat` for each touches exactly one source file (the `P61-D4-009` commit is a
      deletion of `bbj-vscode/src/language/assertions.ts`)
    <!-- planner-discipline-allow: assertions.js, assertTrue -->
    - `grep -rn 'assertions.js' bbj-vscode/src bbj-vscode/test` returns no hits at HEAD
    - `grep -c 'formatSourceLocation' bbj-vscode/src/language/bbj-linker.ts` is at least 3 (the
      declaration plus both call sites)
    - `grep -c 'reloadJavaClassesAndRevalidate' bbj-vscode/src/language/main.ts` is at least 3
    - `cd bbj-vscode && npm run build` exits 0 and produces `out/language/main.cjs`
    - `cd bbj-vscode && npm run lint` exits 0 with zero warnings
    - The suite result at HEAD matches the pre-task capture: the same test names pass and the same
      names fail — record both captures in the `P61-D4-006` ledger row's `notes:`
  </acceptance_criteria>
  <done>
    Four maintainability findings applied with the "no behaviour change" claim demonstrated rather
    than asserted: same build, same lint, same test names passing and failing.
  </done>
</task>

<task type="auto">
  <name>Task 2: The two source comment fixes and the three CLAUDE.md corrections</name>
  <files>
    bbj-vscode/src/language/bbj-cpl-service.ts,
    bbj-vscode/src/language/bbj.langium,
    CLAUDE.md
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-cpl-service.ts — the ranges `48-49` (the class comment) and
      `203-207` (the `setTimeout()` site the comment describes)
    - bbj-vscode/src/language/bbj.langium — line `948` and enough surrounding context to confirm it
      is a comment and not a grammar rule
    - bbj-vscode/src/language/bbj-value-converter.ts as it stands after plan 67-05's `P61-D2-005` fix
      — `P61-D8-002`'s record names that fix as its alternative resolution
    - bbj-vscode/src/language/bbj-module.ts — the `lsp` service group, so the CLAUDE.md Completion
      bullet can name the actual set of registered feature providers rather than a guess
    - bbj-vscode/src/language/validations/ — the directory listing, to confirm
      `check-function-calls.ts` exists before naming it in CLAUDE.md
    - CLAUDE.md — lines around `34` and `90-92`
    - Finding records `P61-D8-004`, `P61-D8-002`, `P61-D8-003`, `P61-D8-005`, `P62-D8-001` via the
      awk extractor (`P62-D8-001` is in `62-COVERAGE.md`, the rest in `61-COVERAGE.md`)
  </read_first>
  <action>
    Five findings, one commit each, no regression tests (D-11: D8, no behaviour change). Every one of
    these replaces an inaccurate statement — verify the replacement against the code before writing
    it, because a doc fix that installs a second wrong claim is worse than the original.

    1. **P61-D8-004** (`bbj-cpl-service.ts:48-49,203-207`, D8, low, effort 2). Exact edit per the
       test-5 clause: update the class comment to state the integration is complete, and either wire
       `setTimeout()` to a settings path or update its comment to reflect that it is currently
       unused. Wiring a settings path would be a behaviour change and is outside a D8 easy fix — take
       the comment branch and record that reasoning in the ledger row's `notes:`. Commit:
       `docs(P61-D8-004): correct the CPL service class and setTimeout comments`.
    2. **P61-D8-002** (`bbj.langium:948`, D8, low, effort 2). Its record offers: fix the comment, or
       fix the code per `P61-D2-005` so the comment becomes true. `P61-D2-005` landed in plan 67-05.
       Read line 948 against the fixed converter: if the comment is now accurate, record
       `verdict: no-op` with `commit: none — resolved by P61-D2-005 <sha>, per this record's own
       alternative resolution` and land no commit. Otherwise correct exactly that comment. Either
       way, immediately after (or, for the no-op, once) run `npm run langium:generate` and confirm
       `git status --porcelain bbj-vscode/src/language/generated/` is empty — this is the proof the
       edit stayed a comment and did not touch a grammar rule, which INVENTORY §3c test 2 forbids in
       an easy fix. Commit if edited: `docs(P61-D8-002): correct the grammar comment about literal conversion`.
    3. **P61-D8-003** (`CLAUDE.md:34`, D8, low, effort 2). Exact edit per the test-5 clause: add
       `validations/check-function-calls.ts` to the Key-services bullet's list. Commit:
       `docs(P61-D8-003): list check-function-calls.ts among the validation services`.
    4. **P61-D8-005** (`CLAUDE.md` §Architecture → Langium Pipeline → Key services, the Completion
       bullet; D8, low, effort **1** — off-scale, annotated in-record as rounded down for ISSUE-03;
       carry the annotation into the ledger row's `notes:` verbatim, do not re-round). Exact edit per
       the test-5 clause: add one bullet, or extend the existing Completion bullet, noting the other
       ten LSP feature providers registered under `bbj-module.ts`'s `lsp` service group. Read
       `bbj-module.ts` and name the providers that are actually registered — do not write "ten" as a
       number if the count you find differs; write what you find and note the discrepancy in the
       ledger row. Commit: `docs(P61-D8-005): note the remaining LSP feature providers`.
    5. **P62-D8-001** (`CLAUDE.md:90-92`, D8, low, effort 2). Exact edit per the test-5 clause: list
       all four files under the "TextMate grammar" bullet, not just `bbj.tmLanguage.json`. Identify
       the four by listing `bbj-vscode/syntaxes/` and cross-checking against `bbj-vscode/package.json`'s
       `contributes.grammars` — name the files that are actually shared, not four plausible names.
       Commit: `docs(P62-D8-001): list all four shared TextMate grammar files`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run langium:generate &amp;&amp; git status --porcelain src/language/generated/ | wc -l | grep -x 0</automated>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'validations/check-function-calls.ts' CLAUDE.md` is at least 1, and
      `ls bbj-vscode/src/language/validations/check-function-calls.ts` succeeds
    - CLAUDE.md's TextMate section names four distinct file paths, and each one exists on disk
    - CLAUDE.md's Completion bullet (or the bullet added beside it) names LSP feature providers that
      each appear in `bbj-vscode/src/language/bbj-module.ts`'s `lsp` service group — verify each
      named provider with `grep -c '<provider>' bbj-vscode/src/language/bbj-module.ts`
    - `cd bbj-vscode && npm run langium:generate` leaves `git status --porcelain src/language/generated/` empty
    - The `P61-D8-002` ledger row's `verdict:` is `applied` or `no-op`, never `pending`
    - `git log --format=%s -5` contains only `docs(P6N-D8-NNN):` subjects for this task, no `fix(` or `test(`
    - `git show --stat` for each CLAUDE.md commit touches only `CLAUDE.md`
    - `cd bbj-vscode && npm run build` exits 0 and `npm run lint` exits 0 with zero warnings
  </acceptance_criteria>
  <done>
    Five stale statements now match the code they describe, and the grammar file's generated output
    is proven unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the nine ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the nine rows for this plan)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09, §D-11
  </read_first>
  <action>
    Complete every field of the rows `P61-D4-006`, `P61-D4-008`, `P61-D4-009`, `P61-D4-012`,
    `P61-D8-004`, `P61-D8-002`, `P61-D8-003`, `P61-D8-005`, `P62-D8-001`. Set
    `test_required: no (D-11 D4)` / `no (D-11 D8)` and `fail_before: inapplicable — D-11 classifies
    this dimension as no-behaviour-change, so there is no failing state to observe`. Set
    `user_facing: no` for all nine unless a specific record's failure scenario says otherwise — a
    documentation correction is not a user-facing behaviour change, and recording it as one would
    inflate Phase 68's DOC-01 with rows that describe no behaviour.

    Then run the plan-level baseline delta (D-09): from `bbj-vscode/`, `npm test` and `npm run lint`.
    Because this plan deletes a file and extracts three helpers with no new tests behind them, the
    delta is the only evidence that "no behaviour change" held — treat a larger failure set as a
    blocker, not an observation. Append `### Plan 67-06 delta`. `./gradlew build` is not re-run.
    Commit: `docs(67-06): close maintainability and doc rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - None of the nine rows contains `TBD` or `pending` in any field
    - Every applied row's `commit:` sha resolves via `git cat-file -e`
    - Every one of the nine rows has `fail_before:` beginning `inapplicable —`
    - The two off-scale-effort rows (`P61-D8-005`) carry the in-record annotation verbatim in `notes:`
    - `67-BASELINE.md` contains `### Plan 67-06 delta` with a verdict of `identical` or `smaller` and
      a recorded `npm run lint` exit code of 0 with zero warnings
    - Every row has `user_facing:` set to exactly `yes` or `no`
  </acceptance_criteria>
  <done>Nine rows closed; the no-behaviour-change claim is backed by an unchanged suite result.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new file paths in this plan. One file is **deleted**:
`bbj-vscode/src/language/assertions.ts` (P61-D4-009), confirmed to have zero consumers.

New symbols in source, all private/internal — no export, no public API, no LSP contract change:
- `resolveWorkspaceRoot()` and `formatSourceLocation(uri, line)` in `bbj-linker.ts` (P61-D4-008)
- a private `reloadJavaClassesAndRevalidate()` in `main.ts` (P61-D4-012)

Documentation surface changed: `CLAUDE.md`'s Key-services bullet list, its Completion bullet, and its
TextMate grammar file list. `bbj.langium` may gain a corrected comment; its generated output under
`bbj-vscode/src/language/generated/` is unchanged and that is asserted, not assumed.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grammar source → generated AST/parser | an edit to `bbj.langium` regenerates `src/language/generated/`; a rule change here alters how all untrusted `.bbj` input is parsed |
| repository documentation → future contributors and agents | `CLAUDE.md` is read as instruction by both humans and coding agents; a wrong statement propagates into future changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-06-01 | Tampering | an unintended grammar-rule change in `bbj.langium` (P61-D8-002) | high | mitigate | Run `npm run langium:generate` after the edit and require `git status --porcelain src/language/generated/` to be empty. A non-empty diff means the edit was not a comment and the commit must be reverted — INVENTORY §3c test 2 forbids a grammar change in an easy fix. |
| T-67-06-02 | Tampering | deleting a validator range that is actually reachable (P61-D4-006) | medium | mitigate | Confirm no DI binding in `bbj-module.ts` and no external reference points at the removed methods before deleting; the suite-result comparison against the pre-task capture is the second gate. |
| T-67-06-03 | Repudiation | a documentation fix that installs a new inaccurate claim | medium | mitigate | Each doc edit is verified against the code it describes by a grep on the named file or symbol, listed in the acceptance criteria; no claim is written from memory. |
| T-67-06-04 | Denial of Service | `main.ts` extraction breaking the language-server entry point (P61-D4-012) | high | mitigate | `npm run build` must succeed and produce `out/language/main.cjs` — the single binary both IDEs consume — as a gate on that commit, not only vitest. |
| T-67-06-05 | Information Disclosure | `bbj-linker.ts` location formatting leaking absolute workspace paths into diagnostics | low | accept | The extraction preserves the existing formatted strings byte-for-byte; whatever the messages disclose today, they disclose identically after. Any change to that disclosure would be a behaviour change and outside a D4 easy fix. |
| T-67-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install and no dependency change; INVENTORY §3c test 3 guarantees it for every easy-fix record. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0 and emits `out/language/main.cjs`
- `cd bbj-vscode && npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && npm run langium:generate` leaves `src/language/generated/` unchanged
- `cd bbj-vscode && npm test` produces the same passing/failing test names as the pre-task capture
- Nine ledger rows closed with no `pending`/`TBD`
- `### Plan 67-06 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- 9 rows dispositioned: 8 applied as single commits, 1 (`P61-D8-002`) applied or argued no-op
- `assertions.ts` deleted with zero remaining references
- Every doc claim written in this plan is grep-verified against the code before commit
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-06-SUMMARY.md` when done
</output>
</content>
</invoke>
