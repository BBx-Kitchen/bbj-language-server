---
phase: 67-apply-easy-fixes
plan: 03
type: execute
wave: 3
depends_on: [67-02]
files_modified:
  - bbj-vscode/src/language/bbj-ws-manager.ts
  - bbj-vscode/src/language/bbj-document-builder.ts
  - bbj-vscode/test/ws-manager.test.ts
  - bbj-vscode/test/document-builder.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A workspace opened with two or more folders contributes prefixes and classpath entries from every folder, not from the first folder only."
    - "A workspace-setup failure is visible — it reaches `logger.error` and/or a user-facing error message — instead of being swallowed silently."
    - "A callback that throws inside the document-builder hook at `bbj-document-builder.ts:155-190` is caught and logged via `logger.error`, and does not abort the build."
    - "An index update resolves fsPath-to-BbjClass through a Map built once per update rather than re-scanning `allElements()` per lookup, and the resolved results are identical to the pre-fix results for the same input."
    - "`trackBbjcplAvailability`'s dedup behaviour and `debouncedCompile`'s timer behaviour are asserted directly by a committed test against a BBjDocumentBuilder built with mocked FileSystemProvider/CPLService/textDocuments."
    - "The comment at `bbj-ws-manager.ts:180` states only what the post-P61-D2-016 code actually does."
  artifacts:
    - bbj-vscode/test/ws-manager.test.ts
    - bbj-vscode/test/document-builder.test.ts
  key_links:
    - "P61-D8-006 is resolvable by P61-D2-016 per its own record; D2-016 must land first so the comment can be judged against the fixed code."
    - "bbj-document-builder.ts drives every document build; a regression here fails the whole suite, so the plan baseline delta is the real gate."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a regression test as fail-before unless the red commit exists in git history and the test was observed failing on the pre-fix tree — no vacuous test, no attested-but-unobserved red"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make the suite look better by deleting, skipping, weakening or timeout-tuning any existing test or test config — every change to a test's status must trace to a finding ID"
---

<objective>
Apply the 6 easy-fix findings on the two workspace/build lifecycle services:
`bbj-ws-manager.ts` (3) and `bbj-document-builder.ts` (3).

Purpose: these two services run on every workspace open and every document build, so a defect here
is felt on every keystroke. Both files carry multiple fixes and must be serialized in one plan.

Output: 10 commits, 6 ledger rows closed, two new unit-test modules.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each finding
record with the awk one-liner; never `cat` a COVERAGE file. The `classification:` field's test-5
clause names the exact edit. Per-commit check: `npm run build` + `npx vitest run <touched test files>`
from `bbj-vscode/`.
</phase_conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bbj-ws-manager.ts — multi-folder workspace, silent setup failure, stale comment</name>
  <files>
    bbj-vscode/src/language/bbj-ws-manager.ts,
    bbj-vscode/test/ws-manager.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-ws-manager.ts — the whole file (293 lines), once, before editing
    - Finding records `P61-D2-015`, `P61-D2-016`, `P61-D8-006` via the awk extractor against
      `.planning/reviews/61-COVERAGE.md`
    - bbj-vscode/test/use-project-root.test.ts and bbj-vscode/test/test-helper.ts — the existing
      workspace-initialization test idiom (`initializeWorkspace(services.shared)`) to match
    - bbj-vscode/test/bbj-test-module.ts
  </read_first>
  <behavior>
    - P61-D2-015 red: initialize a workspace with two folders where each contributes a distinct
      prefix and classpath entry; assert the merged result contains both. Pre-fix only the first
      folder's contribution is present, so the assertion fails.
    - P61-D2-015 green: prefixes and classpath entries from both folders are present, and the
      single-folder case produces exactly what it produced before.
    - P61-D2-016 red: force the setup path at `:179-182` to throw; assert the failure is observable
      (a `logger.error` spy is called, and/or an error message is surfaced). Pre-fix nothing is
      observable, so the assertion fails.
    - P61-D2-016 green: the throw reaches `logger.error` with the underlying error, and the caller
      does not proceed as though setup succeeded.
  </behavior>
  <action>
    Three findings in this order — `P61-D8-006` must come last because its record names
    `P61-D2-016` as an alternative resolution.

    1. **P61-D2-015** (`bbj-ws-manager.ts:106-141`, D2, medium, effort 4). Exact edit per the
       record's test-5 clause: loop over all `folders`, merging prefixes and classpath, rather than
       reading `folders[0]` only. Create `bbj-vscode/test/ws-manager.test.ts` in the red commit,
       following the `initializeWorkspace` idiom from `test/use-project-root.test.ts`. Commits:
       `test(P61-D2-015): add failing test for multi-folder workspace prefix and classpath merge`,
       then `fix(P61-D2-015): merge prefixes and classpath across all workspace folders`.
    2. **P61-D2-016** (`:179-182`, D2, medium, effort 2). Exact edit per the test-5 clause: remove
       the misleading comment, route the catch through `logger.error`, and surface the failure so it
       is visible instead of silent. The record offers `showErrorMessage` as an option — the
       language server has no direct VS Code API, so prefer the project's own
       `bbj-notifications.ts` client-notification path if the file already imports it, otherwise
       `logger.error` alone; state which you chose in the ledger row's `notes:`. Commits:
       `test(P61-D2-016): add failing test for silently swallowed workspace setup failure`, then
       `fix(P61-D2-016): surface workspace setup failures through logger.error`.
    3. **P61-D8-006** (`:180`, D8, low, effort 2). Its record offers: remove/replace the misleading
       comment, **or** fix the underlying handling per `P61-D2-016` so the comment becomes true.
       `P61-D2-016`'s own edit already removes that comment. Read line 180 as it now stands: if the
       comment is gone or now accurate, record `verdict: no-op` with `commit: none — resolved by
       P61-D2-016 <sha>, per this record's own alternative resolution` and land no commit. If a
       misleading clause survives, correct exactly that clause:
       `docs(P61-D8-006): correct the workspace-setup comment`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/ws-manager.test.ts test/use-project-root.test.ts test/imports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` shows `test(P61-D2-015):` immediately followed by `fix(P61-D2-015):`,
      then `test(P61-D2-016):` immediately followed by `fix(P61-D2-016):`
    - Checking out each red commit and running `npx vitest run test/ws-manager.test.ts` exits
      non-zero; the observed failure text is recorded in that row's `fail_before:`
    - `bbj-vscode/test/ws-manager.test.ts` exists and calls `initializeWorkspace`
    - `cd bbj-vscode && npx vitest run test/ws-manager.test.ts test/use-project-root.test.ts test/imports.test.ts` exits 0
    - `cd bbj-vscode && npm run build` exits 0
    - The `P61-D8-006` ledger row's `verdict:` is `applied` or `no-op`, never `pending`, and if
      `no-op` its `commit:` names P61-D2-016's green sha as the resolution
    - `git show --stat` for each `fix(` commit touches only `bbj-vscode/src/language/bbj-ws-manager.ts`
  </acceptance_criteria>
  <done>
    Multi-folder workspaces are handled, setup failures are visible, and the comment at line 180
    tells the truth — each closed by its own commit or an argued no-op.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bbj-document-builder.ts — swallowed callback error, per-lookup rescan, missing unit coverage</name>
  <files>
    bbj-vscode/src/language/bbj-document-builder.ts,
    bbj-vscode/test/document-builder.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-document-builder.ts — the whole file (412 lines), once, before editing
    - Finding records `P61-D2-017`, `P61-D3-005`, `P61-D5-016` via the awk extractor
    - bbj-vscode/test/bbj-test-module.ts (the DI shapes to mock: FileSystemProvider, CPLService, textDocuments)
    - bbj-vscode/src/language/bbj-module.ts (`createBBjServices` — how the builder is wired, so the
      mocked construction matches the real one)
    - bbj-vscode/test/lazy-prefix-loading.test.ts (an existing test that exercises the build pipeline)
  </read_first>
  <behavior>
    - P61-D2-017 red: a callback registered on the hook at `:155-190` throws; assert `logger.error`
      is called with the error and the build completes. Pre-fix the throw escapes, so the assertion fails.
    - P61-D2-017 green: the throw is caught, logged via `logger.error`, and the build completes.
    - P61-D3-005 red: with N documents in the index, assert `allElements()` is invoked a bounded
      number of times per index update (once), not once per lookup. Pre-fix it is invoked per lookup.
    - P61-D3-005 green: one `allElements()` scan per index update; the fsPath-to-BbjClass results are
      byte-identical to the pre-fix results for the same input.
    - P61-D5-016: `trackBbjcplAvailability` deduplicates repeated availability signals, and
      `debouncedCompile` coalesces rapid successive calls into one compile after the debounce window.
      These pass immediately (D-13) — the fix is the test.
  </behavior>
  <action>
    Three findings in this order.

    1. **P61-D2-017** (`bbj-document-builder.ts:155-190`, D2, medium, effort 2). Exact edit per the
       test-5 clause: wrap the callback body in try/catch, logging via `logger.error` on failure.
       Create `bbj-vscode/test/document-builder.test.ts` in the red commit, constructing a
       `BBjDocumentBuilder` with mocked FileSystemProvider / CPLService / textDocuments as
       `P61-D5-016`'s own test-5 clause prescribes — build that harness once here and reuse it for
       the next two findings. Commits: `test(P61-D2-017): add failing test for swallowed
       document-build callback error`, then `fix(P61-D2-017): catch and log document-build callback errors`.
    2. **P61-D3-005** (`:359-411`, D3, medium, effort 4) — D3 requires a regression test per D-11.
       Exact edit per the test-5 clause: build an fsPath-to-BbjClass `Map` once per index update
       instead of re-scanning `allElements()` per lookup. Assert both the call-count reduction and
       result equivalence — a performance fix that changes results is a regression, not a fix.
       Commits: `test(P61-D3-005): add failing test for per-lookup allElements rescan`, then
       `fix(P61-D3-005): build an fsPath-to-BbjClass map once per index update`.
    3. **P61-D5-016** (`:90-222`, D5, medium, effort 4) — test-is-the-fix per D-13. Add the direct
       assertions the record names: `trackBbjcplAvailability`'s dedup and `debouncedCompile`'s timer
       behaviour, against the mocked-construction harness from step 1. Use vitest fake timers for the
       debounce window rather than real sleeps. Single commit:
       `test(P61-D5-016): assert trackBbjcplAvailability dedup and debouncedCompile timing`.
       Set `fail_before: inapplicable — a D5 row adds a missing test against code that already
       works, so no red state is producible (D-13)`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/document-builder.test.ts test/lazy-prefix-loading.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -5` shows `test(P61-D2-017):`→`fix(P61-D2-017):`→`test(P61-D3-005):`→`fix(P61-D3-005):`→`test(P61-D5-016):` in that order
    - Checking out the `P61-D2-017` and `P61-D3-005` red commits and running
      `npx vitest run test/document-builder.test.ts` each exit non-zero, with the observed failure
      recorded in the matching `fail_before:` field
    - `bbj-vscode/test/document-builder.test.ts` constructs a `BBjDocumentBuilder` with mocked
      collaborators and contains assertions naming `trackBbjcplAvailability` and `debouncedCompile`
    - The `P61-D3-005` test asserts both a bounded `allElements()` call count AND result equivalence
      between the map-based and scan-based resolution for the same input
    - `cd bbj-vscode && npx vitest run test/document-builder.test.ts test/lazy-prefix-loading.test.ts` exits 0
    - `cd bbj-vscode && npm run build` exits 0
    - The `P61-D5-016` ledger row's `fail_before:` begins `inapplicable —`
  </acceptance_criteria>
  <done>
    The build hook no longer swallows callback errors, index updates scan once instead of per lookup
    with results proven unchanged, and the builder finally has direct unit coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the six ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the six rows for this plan)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md (`### Deterministic failures (gate set)`)
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09
  </read_first>
  <action>
    Complete every field of the rows `P61-D2-015`, `P61-D2-016`, `P61-D8-006`, `P61-D2-017`,
    `P61-D3-005`, `P61-D5-016`. Write `failure_scenario:` and `fix_applied:` as standalone prose
    that reads correctly without the COVERAGE record beside it — Phase 68's DOC-01 lifts these
    verbatim. Decide `user_facing:` per row: a multi-folder workspace losing half its classpath and
    a silently swallowed setup failure are both observable by a BBj developer, so those are `yes`.

    Then run the plan-level baseline delta (D-09): from `bbj-vscode/`, `npm test` and `npm run lint`.
    Compare failing-test NAMES against the baseline gate set; the gate is identical-or-smaller.
    Exclude `beforeAll` hook-timeout suites as flaky per D-08, naming the suite, quoting the timeout,
    and noting whether it reproduced on a re-run; append each to `## Flaky exclusions (D-08)`.
    This plan changes a service on the build hot path, so if the delta is anything other than
    identical-or-smaller, stop and investigate before committing — do not record a worse set as
    acceptable. Append `### Plan 67-03 delta`. `./gradlew build` is not re-run.
    Commit: `docs(67-03): close workspace and document-builder rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - None of the six rows contains `TBD` or `pending` in any field
    - Every applied row's `commit:` sha resolves via `git cat-file -e`
    - Every `test_required: yes` row has `fail_before:` beginning `observed at ` with a resolvable sha
    - Every row has `user_facing:` set to exactly `yes` or `no`
    - `67-BASELINE.md` contains `### Plan 67-03 delta` with a verdict line reading `identical` or `smaller`
    - The lint exit code and warning list are recorded in that subsection
  </acceptance_criteria>
  <done>Six rows closed with real shas; the suite is no worse than the baseline.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New file paths created by this plan:
- `bbj-vscode/test/ws-manager.test.ts` — vitest module for `BBjWorkspaceManager`
- `bbj-vscode/test/document-builder.test.ts` — vitest module for `BBjDocumentBuilder`, built with
  mocked FileSystemProvider / CPLService / textDocuments

New symbols in source, all private/internal — no export, no public API, no LSP contract change:
- a folder-iterating merge in the workspace-initialization path (P61-D2-015)
- a `logger.error` call in the previously-silent catch at `bbj-ws-manager.ts:179-182` (P61-D2-016)
- a try/catch wrapper around the document-build callback body (P61-D2-017)
- an fsPath-to-BbjClass `Map` built once per index update (P61-D3-005)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace folder paths → workspace manager | folder URIs and classpath entries originate from the client's workspace configuration |
| document content → document builder | every opened `.bbj` file's text flows through the build hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-03-01 | Denial of Service | per-lookup `allElements()` rescan (P61-D3-005) | medium | mitigate | Replace the per-lookup scan with one Map build per index update; the regression test asserts a bounded call count, and result equivalence guards against a fix that trades correctness for speed. |
| T-67-03-02 | Repudiation | silently swallowed workspace-setup failure (P61-D2-016) | medium | mitigate | Route the catch through `logger.error` so a failed setup leaves an audit trail instead of presenting as a successful start. |
| T-67-03-03 | Denial of Service | an unhandled callback throw aborting the document build (P61-D2-017) | medium | mitigate | Wrap the callback body in try/catch with `logger.error`; the build survives one bad callback. |
| T-67-03-04 | Information Disclosure | `logger.error` newly printing workspace folder paths | low | accept | Folder paths are already present in existing log output on this code path; the fix logs the error object, not new path content. Recorded, not separately mitigated. |
| T-67-03-05 | Elevation of Privilege | classpath entries merged from a second, less-trusted workspace folder (P61-D2-015) | medium | accept | Merging all folders is the documented intent of a multi-root workspace and is what the finding requires. No `easy-fix` record covers per-folder classpath trust; the trust question belongs to Phase 65's D1 corpus, all of which is `major-refactor` and routed to Phase 68. Recorded in the ledger row's `notes:`. |
| T-67-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install and no dependency change in this plan; INVENTORY §3c test 3 guarantees it for every easy-fix record. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0
- `cd bbj-vscode && npx vitest run test/ws-manager.test.ts test/document-builder.test.ts test/use-project-root.test.ts test/lazy-prefix-loading.test.ts test/imports.test.ts` exits 0
- Every commit in this plan carries a `P61-*` finding ID in its subject
- Six ledger rows closed with no `pending`/`TBD`
- `### Plan 67-03 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- 6 rows dispositioned: 4 applied with red-proven tests, 1 applied as test-is-the-fix, 1 applied-or-argued-no-op
- No file outside the two services, the two new test files, and `.planning/` is touched
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-03-SUMMARY.md` when done
</output>
</content>
</invoke>
