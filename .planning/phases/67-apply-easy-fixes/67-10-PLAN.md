---
phase: 67-apply-easy-fixes
plan: 10
type: execute
wave: 10
depends_on: [67-09]
files_modified:
  - .github/workflows/build.yml
  - .github/workflows/pr-validation.yml
  - bbj-vscode/vitest.config.ts
  - bbj-vscode/package.json
  - bbj-vscode/package-lock.json
  - bbj-vscode/test/language-configuration.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: false
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.github/workflows/build.yml`'s `on:` block declares exactly one trigger, `pull_request`, and the file parses as YAML."
    - "Every `actions/checkout` and `actions/setup-node` reference in `build.yml` is on the `@v4` major, matching every other workflow in the repository."
    - "`.github/workflows/pr-validation.yml`'s `paths:` filter includes a glob that matches tracked files under `bbj-vscode/src/language/`, so a change to the language server triggers the IntelliJ build gate."
    - "`bbj-vscode/package.json`'s `bbj` language contribution lists `.bbl` among its extensions."
    - "`bbj-vscode/package-lock.json`'s root `version` equals `bbj-vscode/package.json`'s `version`."
    - "`npm ci` succeeds in `bbj-vscode/` after the lockfile edits, and the failing-test name set is identical to or smaller than the phase baseline."
    - "`bbj-vscode/vitest.config.ts`'s threshold comments state that the thresholds apply only to `npm run test:coverage`, which no workflow invokes."
    - statement: "The six transitive packages updated by P64-D6-013 resolve to versions whose advisories are closed, and `npm audit` reports no new advisory that was absent before the edit."
      verification: backstop
  artifacts:
    - .github/workflows/build.yml
    - .github/workflows/pr-validation.yml
    - bbj-vscode/package-lock.json
  key_links:
    - "P64-D4-004's own disposition asks for it to be applied alongside P64-D3-002, which is major-refactor and out of scope; D-06 records the departure rather than dropping it."
    - "P64-D2-004 is a D2 record, but D-11's regression-test default cannot be met by a workflow file; D-16 governs and the override is argued in the ledger row."
    - "P64-D6-013 changes resolved transitive package versions. No Package Legitimacy Audit exists for this project (research is disabled), so all six packages are treated as [ASSUMED] and gated behind a blocking human checkpoint."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-01
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT add, upgrade or remove any declared dependency in bbj-vscode/package.json — every easy-fix record passed INVENTORY §3c test 3 precisely because it changes no declared dependency, and a lockfile fix that alters the manifest has left the easy-fix envelope"
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a workflow or lockfile fix as verified beyond the check that actually ran — no CI run occurs in this environment, so the ledger says static validity only, never 'CI green'"
---

<objective>
Apply the 6 remaining Phase-64 build/CI/dependency findings plus the one cross-IDE parity finding on
`bbj-vscode/package.json`.

D-02: `.github/workflows/` and `bbj-vscode/package-lock.json` are both inside the apply scope, so
`P64-D2-004`, `P64-D6-004` and `P64-D4-004` (the workflows) and `P64-D6-009` and `P64-D6-013` (the
lockfile) are applied here rather than deferred for living outside `src/`. The tree they live in
changes only how they are verified — which is D-16's job below — never whether they are applied.

Purpose: these are the fixes this environment cannot verify the normal way. No GitHub Actions run
happens here and no CI result is available, so D-16 caps their verification at the strongest check
their artefact type admits — YAML parse for the workflows, `npm ci` plus `npm audit` plus the
baseline-delta run for the lockfile. The ledger says exactly that and claims nothing more.

Output: 9 commits, 7 ledger rows closed, one blocking human checkpoint cleared.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each record
with the awk one-liner against `.planning/reviews/64-COVERAGE.md` (and `62-COVERAGE.md` for
`P62-D7-002`). D6 findings take one `chore(...)` commit each.

**YAML validity check** (D-16). `actionlint` was not on this machine at planning time; re-check with
`command -v actionlint` and use it if present. Otherwise the check is a YAML parse via the
`js-yaml` already present in `bbj-vscode/node_modules` (v4.3.0, which keeps `on:` as a string key):

    cd bbj-vscode && node -e "const y=require('js-yaml'),fs=require('fs');const d=y.load(fs.readFileSync('../.github/workflows/build.yml','utf8'));console.log(Object.keys(d.on).join(','))"

Record in each workflow row's `verification:` field which check actually ran, and record plainly that
**no CI run occurred**.
</phase_conventions>

<tasks>

<task type="auto">
  <name>Task 1: The three workflow fixes and the vitest threshold comment</name>
  <files>
    .github/workflows/build.yml,
    .github/workflows/pr-validation.yml,
    bbj-vscode/vitest.config.ts
  </files>
  <read_first>
    - .github/workflows/build.yml — the whole file. Targets: the `on:` block at `4-6` and the two
      action references at `18` and `20`. The third reference, `actions/upload-artifact@v4` at `:41`,
      is already correct and must not be touched.
    - .github/workflows/pr-validation.yml — the whole file, especially the `paths:` list at `8-13`
      and the two jobs `build-vscode` (`16-38`) and `validate-intellij` (`40-61`)
    - bbj-vscode/vitest.config.ts — lines `8`, `18-27` (the `thresholds` block and its comments)
    - bbj-vscode/package.json — the `test:coverage` script, so the corrected comment names it exactly
    - Finding records `P64-D4-004`, `P64-D6-004`, `P64-D2-004`, `P64-D8-005` via the awk extractor
  </read_first>
  <action>
    Four findings, one commit each, no regression tests — D-11's default is overridden for the three
    workflow rows by D-16, and `P64-D8-005` is a D8 comment fix. Argue the `P64-D2-004` override in
    its ledger row: it is a D2 record, D-11 says D2 requires a regression test, and a GitHub Actions
    workflow cannot carry a vitest regression test in this repository, so D-16's tool-native check
    stands in its place.

    1. **P64-D4-004** (`build.yml:4-6`, D4, low, effort 2). Exact edit per the record's test-5 clause:
       delete lines 4-6 — the `push:` trigger and its `branches:` list naming a branch that no longer
       exists in this repository — leaving `on:` with the `pull_request` trigger only. Per D-06 the
       record asks for this to be applied alongside `P64-D3-002`'s decision about the `paths:` filter;
       `P64-D3-002` is `major-refactor` and out of scope, so the sequencing cannot be honoured — state
       that in the ledger row's `notes:` in those terms. Commit:
       `chore(P64-D4-004): drop the dead push trigger from build.yml`.
    2. **P64-D6-004** (`build.yml:18-20`, D6, low, effort 2). Exact edit per the test-5 clause: change
       `@v3` to `@v4` on the `actions/checkout` and `actions/setup-node` references. Task 1's deletion
       shifts those lines, so match on the action name rather than the line number. Leave
       `actions/upload-artifact@v4` alone. Commit:
       `chore(P64-D6-004): move build.yml onto the v4 action majors`.
    3. **P64-D2-004** (`pr-validation.yml:8-13`, D2, medium, effort 2). The second glob,
       `bbj-vscode/out/language/**`, can never match: `bbj-vscode/.gitignore` line 1 ignores `/out/`
       and `git ls-files bbj-vscode/out` returns nothing, so no pull request can contain a change
       under it. Replace it with `bbj-vscode/src/language/**`, the 53 tracked files that
       `out/language/main.cjs` is actually built from. Confirm the counts yourself before editing:
       `git ls-files bbj-vscode/out | wc -l` and `git ls-files bbj-vscode/src/language | wc -l`, and
       record both in the ledger row's `verification:`. Commit:
       `fix(P64-D2-004): trigger pr-validation on language-server source changes`.
    4. **P64-D8-005** (`bbj-vscode/vitest.config.ts:25-26`, D8, low, effort 2). Exact edit per the
       test-5 clause: correct the comment to say the thresholds apply only to `npm run test:coverage`,
       which nothing currently automates. Confirm that claim before writing it —
       `grep -rn 'test:coverage\|--coverage' .github/workflows/` must return nothing, and coverage
       must still be `enabled: false` by default at `vitest.config.ts:8`. Do not change any threshold
       value; this is a comment fix. Commit:
       `docs(P64-D8-005): state that coverage thresholds apply only to the coverage script`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; node -e "const y=require('js-yaml'),fs=require('fs');for(const f of ['build.yml','pr-validation.yml']){const d=y.load(fs.readFileSync('../.github/workflows/'+f,'utf8'));console.log(f,Object.keys(d.on).join(','))}"</automated>
    <automated>cd bbj-vscode &amp;&amp; npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parse above prints `build.yml pull_request` — exactly one trigger key — and
      `pr-validation.yml` with its trigger keys intact
    - `node -e` over `pr-validation.yml` prints a `paths:` list containing `bbj-vscode/src/language/**`,
      and `git ls-files bbj-vscode/src/language | wc -l` is greater than 0
    - Every `uses:` value in `build.yml` that names `actions/checkout` or `actions/setup-node` ends
      in `@v4` — verify with
      `node -e "const y=require('js-yaml'),fs=require('fs');const d=y.load(fs.readFileSync('.github/workflows/build.yml','utf8'));console.log(JSON.stringify(Object.values(d.jobs).flatMap(j=>j.steps).map(s=>s.uses).filter(Boolean)))"`
    <!-- planner-discipline-allow: test:coverage, --coverage -->
    - `grep -rn 'test:coverage' .github/workflows/` returns no hits, and `bbj-vscode/vitest.config.ts`
      still contains `enabled: false`
    - No threshold number in `vitest.config.ts` changed: `git show --stat` for the `P64-D8-005` commit
      shows only comment lines modified
    - `git log --format=%s -4` shows `chore(P64-D4-004):`, `chore(P64-D6-004):`, `fix(P64-D2-004):`,
      `docs(P64-D8-005):`
    - The `P64-D2-004` ledger row's `notes:` argues the D-11→D-16 override, and the `P64-D4-004` row's
      `notes:` records the D-06 departure
    - Each of the four rows' `verification:` field names the check that ran and states that no CI run occurred
  </acceptance_criteria>
  <done>
    The dead trigger is gone, `build.yml` is on the repository's own action majors, the IntelliJ build
    gate fires on language-server changes, and the coverage comment no longer promises an enforced floor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate for the six transitive packages P64-D6-013 updates</name>
  <what-built>
    Nothing yet. This gate runs BEFORE any lockfile-mutating command in Task 3.

    `P64-D6-013` remediates six moderate advisories by a transitive, lockfile-only update. All six
    enter through `@vscode/vsce@3.7.1` at `package-lock.json:2172`; `ajv` also enters through
    dev-only `eslint`. The packages and their currently-resolved versions are:

    | package | resolved now | advisory |
    |---|---|---|
    | `ajv` | 8.17.1 | GHSA-2g4f-4pwh-qvx6 — ReDoS via the `$data` option |
    | `markdown-it` | 14.1.0 | GHSA-6v5v-wf23-fmfq, GHSA-38c4-r59v-3vqw — quadratic/ReDoS DoS |
    | `qs` | 6.14.1 | GHSA-q8mj-m7cp-5q26, GHSA-w7fw-mjwx-w883 |
    | `uuid` | 8.3.2 | GHSA-w5hq-g745-h8pq — missing buffer bounds check |
    | `@azure/msal-node` | 3.8.6 | transitively via `uuid` |
    | `@azure/identity` | 4.13.0 | transitively via `@azure/msal-node` |

    This project has research disabled, so no `## Package Legitimacy Audit` table exists. Under the
    gate's fallback policy every one of the six is therefore treated as **[ASSUMED]**, and an
    [ASSUMED] package cannot be installed without a human confirming its legitimacy. This checkpoint
    is not auto-approvable regardless of `mode: yolo`.
  </what-built>
  <how-to-verify>
    For each of the six packages, open `https://www.npmjs.com/package/<name>` and confirm:
    1. The package page exists and the name matches character-for-character (no typosquat homoglyph).
    2. The repository link points at the expected upstream project.
    3. Weekly downloads and publish history are consistent with a long-established package.
    4. The version `npm audit fix --package-lock-only --dry-run` proposes is a published release of
       that same package, not a fork or a re-publish under a similar name.

    To see what will actually change before approving, run from `bbj-vscode/`:
    `npm audit fix --package-lock-only --dry-run` and read the proposed version moves. This command
    is read-only — it writes nothing.

    Approve only if every one of the six checks out. If any package looks wrong, say so instead of
    approving; the row is then recorded as blocked, not applied.
  </how-to-verify>
  <read_first>
    - Finding record `P64-D6-013` via the awk extractor against `.planning/reviews/64-COVERAGE.md`
      — its `evidence:` field carries every advisory URL and the `npm ls` dependency paths
    - `.planning/reviews/64-COVERAGE.md` §`### SEC-08 Dependency Triage` — the row-by-row triage
    - bbj-vscode/package.json — to confirm none of the six is a declared dependency
  </read_first>
  <acceptance_criteria>
    - The human's response is recorded verbatim in the `P64-D6-013` ledger row's `notes:`
    - If approved: the row's `notes:` also records that no Package Legitimacy Audit table exists for
      this project and that the six were confirmed as [ASSUMED]→verified by human review against npmjs.com
    - If not approved: the row's `verdict:` is `deferred` with the human's stated reason, and Task 3
      skips the `P64-D6-013` step while still applying `P64-D6-009`
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the lockfile update, or name the package that looks wrong</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The .bbl language extension, the two lockfile fixes, and the plan close-out</name>
  <files>
    bbj-vscode/package.json,
    bbj-vscode/package-lock.json,
    bbj-vscode/test/language-configuration.test.ts,
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - bbj-vscode/package.json — the `contributes.languages` entry for language id `bbj` at `30-35`,
      and the `version` field at `:3`
    - bbj-vscode/package-lock.json — lines `1-6` only (`version` and `lockfileVersion`); never read
      the whole file, it is thousands of lines
    - Finding records `P62-D7-002` (in `62-COVERAGE.md`), `P64-D6-009`, `P64-D6-013` via the awk extractor
    - bbj-vscode/test/language-configuration.test.ts as created in plan 67-09 — the home for the
      `P62-D7-002` package.json-shape assertion
    - The Task 2 checkpoint outcome
  </read_first>
  <behavior>
    - P62-D7-002 red: an assertion that `package.json`'s `bbj` language contribution lists `.bbl`
      among its extensions fails.
    - P62-D7-002 green: it passes; every extension already listed is still listed.
  </behavior>
  <action>
    Three findings then the plan close-out. Order matters: `P62-D7-002` edits the manifest,
    `P64-D6-009` regenerates the lockfile from it, and `P64-D6-013` layers the advisory updates on top
    — running them in any other order merges two commits' diffs.

    1. **P62-D7-002** (`bbj-vscode/package.json:30-35`, D7, medium, effort 2) — D7 requires a
       regression test per D-11. Exact edit per the record's test-5 clause: add `.bbl` to the `bbj`
       language contribution's `extensions` array. The record's test-4 clause names the test shape: a
       package.json-shape assertion. Add it to `bbj-vscode/test/language-configuration.test.ts`.
       Commits: `test(P62-D7-002): add failing test for the missing .bbl language extension`, then
       `fix(P62-D7-002): register .bbl for the bbj language`.
    2. **P64-D6-009** (`package-lock.json:3`, D6, low, effort 2). Exact edit per the record's test-5
       clause: run `npm install --package-lock-only` in `bbj-vscode/` and commit the resulting change.
       The lockfile root `version` is `0.11.0` while `package.json:3` is `0.12.0`; the 593-entry
       dependency graph is already in sync and must stay so. Before committing, confirm the diff is
       confined to the root version: `git diff --stat bbj-vscode/package-lock.json` should show a
       small change, and `node -e "const l=require('./package-lock.json');console.log(l.version, l.packages[''].version, Object.keys(l.packages).filter(k=>k.startsWith('node_modules/')).length)"`
       from `bbj-vscode/` should print the manifest version twice and `593`. If the entry count moved,
       the command did more than the record names — reset and investigate. Commit:
       `chore(P64-D6-009): resync the lockfile root version with the manifest`.
    3. **P64-D6-013** (`package-lock.json:2172`, D6, medium, effort 2) — only if Task 2 was approved.
       Apply the transitive, lockfile-only advisory remediation with
       `npm audit fix --package-lock-only` from `bbj-vscode/`. Then verify per D-16, in this order:
       `npm ci`, then `npm audit`, then the baseline-delta suite run below. Confirm
       `git diff bbj-vscode/package.json` is empty — the manifest must not change, or the fix has left
       the easy-fix envelope and must be reset. Record in the ledger row which advisories closed and
       whether `npm audit` reports any advisory that was absent before. Commit:
       `chore(P64-D6-013): remediate six moderate transitive advisories via the lockfile`.

    **Close-out.** Complete every field of the seven rows for this plan (`P64-D4-004`, `P64-D6-004`,
    `P64-D2-004`, `P64-D8-005`, `P62-D7-002`, `P64-D6-009`, `P64-D6-013`). Each workflow and lockfile
    row's `verification:` says what ran and states plainly that no CI run occurred. Then run the
    plan-level baseline delta (D-09): `npm test` and `npm run lint` from `bbj-vscode/`. Because this
    plan reinstalls the dependency tree, a changed failure set here means a dependency change broke
    something — treat it as a blocker, not an observation. Append `### Plan 67-10 delta`.
    Commit: `docs(67-10): close build, CI and dependency rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm ci &amp;&amp; npm audit --json > /dev/null; npm run build &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
    <automated>cd bbj-vscode &amp;&amp; node -e "const l=require('./package-lock.json'),p=require('./package.json');if(l.version!==p.version||l.packages[''].version!==p.version)process.exit(1);console.log('LOCK_VERSION_OK',l.version)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd bbj-vscode && node -e "const l=require('./package-lock.json'),p=require('./package.json');console.log(l.version===p.version && l.packages[''].version===p.version)"` prints `true`
    - `cd bbj-vscode && node -e "const l=require('./package-lock.json');console.log(Object.keys(l.packages).filter(k=>k.startsWith('node_modules/')).length)"` prints `593` after `P64-D6-009` and is
      recorded again after `P64-D6-013` with any change explained in that row's `notes:`
    - `git diff <plan-start>..HEAD -- bbj-vscode/package.json` shows only the `.bbl` extension addition
      — no dependency, devDependency or version field changed by the lockfile commits
    - `cd bbj-vscode && node -e "const p=require('./package.json');const l=p.contributes.languages.find(x=>x.id==='bbj');console.log(l.extensions.includes('.bbl'))"` prints `true`
    - Checking out the `P62-D7-002` red commit and running
      `npx vitest run test/language-configuration.test.ts` exits non-zero
    - `cd bbj-vscode && npm ci` exits 0
    - `cd bbj-vscode && npm run build && npm run lint && npm test` — lint exits 0 with zero warnings
      and the failing-test name set is identical to or smaller than the baseline gate set
    - All seven rows are complete with no `TBD`/`pending`, and each workflow and lockfile row's
      `verification:` contains the words `no CI run occurred`
    - The `P64-D6-013` row records the human checkpoint outcome and, if approved, the advisories closed
  </acceptance_criteria>
  <done>
    `.bbl` files open as BBj in VS Code, the lockfile agrees with the manifest, six moderate
    advisories are closed behind a human legitimacy check, and every row says exactly what was
    verified and what was not.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new file paths in this plan. The `P62-D7-002` test is added to
`bbj-vscode/test/language-configuration.test.ts`, created in plan 67-09.

New symbols: none in source. The changes are configuration values — one `on:` trigger removed and
two action majors bumped in `build.yml`, one `paths:` glob replaced in `pr-validation.yml`, one
`.bbl` string added to `package.json`'s `bbj` language `extensions` array, two comment lines
corrected in `vitest.config.ts`, and the `package-lock.json` root `version` plus six transitive
resolved versions.

No declared dependency is added, upgraded or removed in `bbj-vscode/package.json`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dependency tree | `npm ci` and `npm audit fix --package-lock-only` resolve and fetch package versions from the registry |
| GitHub Actions trigger surface → CI runners | `on:` and `paths:` determine which events can reach a job that holds repository secrets |
| `package.json` `contributes` → VS Code client | the language-extension mapping decides which files the extension claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-10-SC | Tampering | `npm audit fix --package-lock-only` resolving six transitive packages (P64-D6-013) | high | mitigate | No Package Legitimacy Audit table exists for this project (research disabled), so all six packages fall back to [ASSUMED]. Task 2 is a blocking human checkpoint verifying each against `npmjs.com/package/<name>` before any lockfile-mutating command runs. Not auto-approvable under `mode: yolo`. |
| T-67-10-01 | Denial of Service | the six moderate advisories (ReDoS in `ajv`/`markdown-it`/`qs`, bounds check in `uuid`) | medium | mitigate | Remediated by the lockfile update. Their reachable path is `@vscode/vsce` during packaging and publishing, so the practical consequence is a failed or hung release job, not a compromised one. |
| T-67-10-02 | Elevation of Privilege | widening `pr-validation.yml`'s `paths:` filter (P64-D2-004) | medium | accept | The replacement glob broadens which pull requests trigger the job. `pr-validation.yml`'s jobs build and validate only; the secret-holding publish jobs live in `preview.yml` and `manual-release.yml` and are untouched. Confirm that before committing — if `pr-validation.yml` does reference a secret, stop and record it as a discovered finding for Phase 68. |
| T-67-10-03 | Repudiation | claiming CI verification that never ran | high | mitigate | No GitHub Actions run is possible in this environment. Every workflow row's `verification:` states the check that actually ran (YAML parse, or `actionlint` if present) and the words `no CI run occurred`. |
| T-67-10-04 | Tampering | a lockfile command silently changing more than the record names | high | mitigate | After each lockfile command, assert the `node_modules/` entry count and confirm `git diff bbj-vscode/package.json` is empty. A moved entry count or a manifest diff means reset and investigate. |
| T-67-10-05 | Spoofing | a typosquatted replacement for one of the six packages | high | mitigate | The Task 2 checkpoint requires character-for-character name confirmation and a repository-link check on npmjs.com for each package. |
| T-67-10-06 | Information Disclosure | `npm audit --json` output pasted into a committed artefact | low | mitigate | Record advisory IDs and version moves in the ledger, not raw audit JSON, which can embed local paths. |
</threat_model>

<verification>
- `cd bbj-vscode && npm ci` exits 0
- `cd bbj-vscode && npm run build` exits 0 and `npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && npm test` — failing-test name set identical to or smaller than the baseline gate set
- Both workflow files parse via js-yaml (or `actionlint` if available)
- `bbj-vscode/package-lock.json`'s root version equals `bbj-vscode/package.json`'s version
- `git diff` on `bbj-vscode/package.json` shows only the `.bbl` addition
- Seven ledger rows closed with no `pending`/`TBD`, each naming what was and was not verified
</verification>

<success_criteria>
- 7 rows dispositioned: 6 applied, plus `P64-D6-013` applied or deferred on the human checkpoint's outcome
- No declared dependency changed in `bbj-vscode/package.json`
- No row claims a CI result; every workflow and lockfile row states the check that actually ran
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-10-SUMMARY.md` when done
</output>
</content>
</invoke>
