---
phase: 67-apply-easy-fixes
plan: 09
type: execute
wave: 9
depends_on: [67-08]
files_modified:
  - bbj-vscode/syntaxes/bbj.tmLanguage.json
  - bbj-vscode/test/textmate-highlighting.test.ts
  - bbj-vscode/src/extension.ts
  - bbj-vscode/test/extension-activation.test.ts
  - bbj-vscode/bbj-language-configuration.json
  - bbj-vscode/test/language-configuration.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A character inside a double-quoted or single-quoted BBj string carries the string scope and no escape scope — the string patterns arrays no longer include the character-escape rule."
    - "A bare `REM` at end of line with no trailing space or tab is recognised as a comment, and `REM ` followed by text is still recognised exactly as before."
    - "`IOL=` and `LEN=` are recognised as keywords when immediately followed by a value, which the pre-fix trailing word-boundary assertion prevented."
    - "`bbj-language-configuration.json` parses with strict `JSON.parse`."
    - "A `LanguageClient.start()` rejection during extension activation is observed and surfaced rather than becoming an unhandled promise rejection."
    - "Every existing case in `test/textmate-highlighting.test.ts` and `test/textmate-bbx-highlighting.test.ts` still passes."
  artifacts:
    - bbj-vscode/test/extension-activation.test.ts
    - bbj-vscode/test/language-configuration.test.ts
  key_links:
    - "bbj.tmLanguage.json is shared by both IDEs — the IntelliJ plugin bundles the same TextMate grammar, so a scope change here reaches IntelliJ users with no Java-side edit."
    - "P62-D5-004's three named assertions are the same three behaviours P62-D2-007/008/009 fix; sequencing D5-004 last is what keeps it a genuine no-red D5 row per D-13."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a regression test as fail-before unless the red commit exists in git history and the test was observed failing on the pre-fix tree — no vacuous test, no attested-but-unobserved red"
    - requirement_id: FIX-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship a syntax-highlighting change without recording it as user-facing — a scope change is visible in both IDEs on the next reload and is exactly the class of change FIX-04 exists to capture"
---

<objective>
Apply the 6 remaining Phase-62 findings: 3 TextMate grammar defects plus the coverage row that names
them, the extension-activation unhandled rejection, and the non-strict language-configuration JSON.

Purpose: `bbj.tmLanguage.json` is bundled by both the VS Code extension and the IntelliJ plugin, so
these three scope fixes are the phase's most directly user-visible changes — a BBj developer sees
them the next time either editor reloads. That makes them the clearest FIX-04 rows in the ledger.

Output: 10–11 commits, 6 ledger rows closed, two new test modules.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each record
with the awk one-liner against `.planning/reviews/62-COVERAGE.md`. BBj is case-insensitive — every
TextMate pattern in this file uses the `[rR][eE][mM]` character-class idiom, and any edit must keep
that property.
</phase_conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The three TextMate grammar defects and the coverage row that names them</name>
  <files>
    bbj-vscode/syntaxes/bbj.tmLanguage.json,
    bbj-vscode/test/textmate-highlighting.test.ts
  </files>
  <read_first>
    - bbj-vscode/syntaxes/bbj.tmLanguage.json — the whole file (74 lines). The three targets are
      line `15` (the `keyword.control.bbj` match, whose tail is the `IOL=`/`LEN=` alternation),
      lines `18-25` and `27-35` (the two string rules and their `patterns` arrays), and lines
      `41-50` (the `comments` repository rule, whose `begin` requires a trailing space or tab).
    - Finding records `P62-D2-007`, `P62-D2-008`, `P62-D2-009`, `P62-D5-004` via the awk extractor
    - bbj-vscode/test/textmate-highlighting.test.ts — the existing harness and assertion style
    - bbj-vscode/test/textmate-bbx-highlighting.test.ts — must keep passing unchanged
    - bbj-vscode/package.json `contributes.grammars` — confirms which files ship this grammar
  </read_first>
  <behavior>
    - P62-D2-007 red: a character inside a double-quoted string is scoped as a character escape as
      well as string content. The test asserts the string scope is present and the escape scope is
      absent, so it fails pre-fix.
    - P62-D2-007 green: only the string scope applies, in both the double-quoted and single-quoted
      rules.
    - P62-D2-008 red: a bare `REM` at end of line, with no trailing space or tab, is not scoped as a
      comment.
    - P62-D2-008 green: bare `REM` at end of line is a comment, and `REM ` followed by text scopes
      exactly as before.
    - P62-D2-009 red: `IOL=` immediately followed by a value is not scoped as a keyword, because the
      trailing word-boundary assertion cannot hold before a digit.
    - P62-D2-009 green: `IOL=` and `LEN=` with a value attached are scoped as keywords.
  </behavior>
  <action>
    Four findings, in this order. `P62-D5-004` must be last, because the three assertions it names
    are the same three behaviours the D2 fixes deliver.

    Before the first edit, record in each row's `notes:` the pre-fix length of both string
    `patterns` arrays and the pre-fix `begin` and `match` strings you are about to change — these are
    the values the acceptance criteria compare against.

    1. **P62-D2-007** (`bbj.tmLanguage.json:18-25,27-35,68-72`, D2, medium, effort 2). Exact edit per
       the record's test-5 clause: remove the `#string-character-escape` include entry from **both**
       string `patterns` arrays; the now-unused repository rule at `68-72` may be dropped too. The
       record's test-4 clause names the assertion: a token inside a string carries
       `string.quoted.double.bbj` and no character-escape scope. Commits:
       `test(P62-D2-007): add failing test for escape scope leaking into string content`, then
       `fix(P62-D2-007): drop the character-escape include from both string rules`.
    2. **P62-D2-008** (`:41-50`, D2, low, effort 2). Exact edit per the test-5 clause: make the
       trailing whitespace optional in the comment `begin` pattern, e.g. `[rR][eE][mM]([ \t]|(?=$))`.
       Keep the `beginCaptures` mapping working for the space/tab branch. Commits:
       `test(P62-D2-008): add failing test for a bare REM at end of line`, then
       `fix(P62-D2-008): accept a bare REM with no trailing whitespace`.
    3. **P62-D2-009** (`:15`, D2, low, effort 2). Exact edit per the test-5 clause: drop the trailing
       `\B` after the `IOL=`/`LEN=` alternation, or replace it with a lookahead for a value such as
       `(?=\d)`. State which branch you took in the ledger row's `notes:`. Do not touch the long
       keyword alternation that precedes it. Commits:
       `test(P62-D2-009): add failing test for IOL= and LEN= with a value attached`, then
       `fix(P62-D2-009): stop requiring a word boundary after IOL= and LEN=`.
    4. **P62-D5-004** (`test/textmate-highlighting.test.ts`, D5, low, effort 4). Its test-5 clause
       names three assertions: string-content scope purity, bare-REM recognition, and `IOL=`/`LEN=`
       with a value attached. Compare those three against the three regression tests just committed.
       Where a named assertion is already covered, this row is closed by that commit; where the
       record asks for more than the regression test asserts, add the delta. If all three are
       covered, record `verdict: no-op` with
       `commit: none — the three assertions this record names were landed as the regression tests for
       P62-D2-007 <sha>, P62-D2-008 <sha>, P62-D2-009 <sha>`, quoting the record's own test-5 clause.
       Otherwise commit the delta as `test(P62-D5-004): complete the TextMate highlighting assertions`.
       Either outcome is recorded; neither is silently dropped.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npx vitest run test/textmate-highlighting.test.ts test/textmate-bbx-highlighting.test.ts</automated>
    <automated>cd bbj-vscode &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('syntaxes/bbj.tmLanguage.json','utf8'));console.log('TMGRAMMAR_JSON_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -6` shows `test(P62-D2-007):`→`fix(P62-D2-007):`→`test(P62-D2-008):`→`fix(P62-D2-008):`→`test(P62-D2-009):`→`fix(P62-D2-009):`
    - Checking out each of the three red commits and running
      `npx vitest run test/textmate-highlighting.test.ts` exits non-zero; each observed failure is
      recorded in the matching `fail_before:` field
    - `node -e "const g=JSON.parse(require('fs').readFileSync('bbj-vscode/syntaxes/bbj.tmLanguage.json','utf8'));console.log(g.patterns.filter(p=>p.name&&p.name.startsWith('string.quoted')).map(p=>(p.patterns||[]).length).join(','))"`
      prints two lengths, each one less than the pre-fix lengths recorded in the `P62-D2-007` row
    - The grammar file still parses as strict JSON (the `TMGRAMMAR_JSON_OK` check above)
    - Every case-insensitive character-class pattern touched still uses the `[rR][eE][mM]` idiom —
      no pattern was rewritten to a case-sensitive literal
    - `cd bbj-vscode && npx vitest run test/textmate-highlighting.test.ts test/textmate-bbx-highlighting.test.ts` exits 0
    - The `P62-D5-004` row's `verdict:` is `applied` or `no-op`, never `pending`, and if `no-op` its
      `commit:` names all three resolving shas
    - All four rows have `user_facing: yes` — a scope change is visible in both IDEs on next reload
  </acceptance_criteria>
  <done>
    Strings, bare `REM` comments and `IOL=`/`LEN=` keywords highlight correctly in both IDEs, each
    proven by a test observed red on the pre-fix grammar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extension activation rejection and the non-strict language-configuration JSON</name>
  <files>
    bbj-vscode/src/extension.ts,
    bbj-vscode/test/extension-activation.test.ts,
    bbj-vscode/bbj-language-configuration.json,
    bbj-vscode/test/language-configuration.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/extension.ts — the range around line `892` (the `client.start()` call site) plus
      the surrounding activation function and however the file already reports errors to the user
      (an output channel, a `showErrorMessage`, or neither)
    - bbj-vscode/bbj-language-configuration.json — lines `50-56` and `96-102`; both targets are a
      trailing comma that makes the file JSONC-only rather than strict JSON
    - Finding records `P62-D2-004` and `P62-D2-006` via the awk extractor
    - bbj-vscode/test/composer-commands.test.ts — the closest existing example of testing
      extension-host code in this repo; mirror its mocking approach
  </read_first>
  <behavior>
    - P62-D2-004 red: a mock `LanguageClient` whose `start()` rejects produces an unobserved
      rejection; the test asserts the rejection is observed (the error reaches the file's reporting
      path) and fails pre-fix.
    - P62-D2-004 green: the rejection is caught and surfaced; a successful `start()` activates
      exactly as before.
    - P62-D2-006 red: `JSON.parse` on the raw text of `bbj-language-configuration.json` throws.
    - P62-D2-006 green: `JSON.parse` succeeds and the parsed object still contains every pair the
      pre-fix JSONC parse contained — a deleted comma must not become a deleted entry.
  </behavior>
  <action>
    Two findings.

    1. **P62-D2-004** (`extension.ts:892`, D2, low, effort 2). Exact edit per the record's test-5
       clause: attach a `.catch()` to `client.start()` that logs and surfaces the failure — via the
       file's existing output channel if it has one, otherwise `showErrorMessage`. Read the file
       first and use whichever reporting path already exists; do not introduce a second one. The
       record's failure scenario is that every command stays registered as though the server had
       started, so the surfaced message must say the server did not start. Create
       `bbj-vscode/test/extension-activation.test.ts` with a mock `LanguageClient` whose `start()`
       rejects. Commits: `test(P62-D2-004): add failing test for an unobserved client.start rejection`,
       then `fix(P62-D2-004): surface a language-server start failure`.
    2. **P62-D2-006** (`bbj-language-configuration.json:54-55,100-101`, D2, low, effort 2). Exact
       edit per the test-5 clause: delete the comma at line 54 and at line 100. Both are trailing
       commas before a closing bracket or brace. Create
       `bbj-vscode/test/language-configuration.test.ts` asserting the file parses with strict
       `JSON.parse` and that the parsed `comments`, `brackets`, `autoClosingPairs` and
       `surroundingPairs` collections have the same entry counts as before — record those pre-fix
       counts in the ledger row's `notes:` before editing. Commits:
       `test(P62-D2-006): add failing test for non-strict language-configuration JSON`, then
       `fix(P62-D2-006): remove the two trailing commas from the language configuration`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/extension-activation.test.ts test/language-configuration.test.ts test/composer-commands.test.ts</automated>
    <automated>cd bbj-vscode &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('bbj-language-configuration.json','utf8'));console.log('LANGCONFIG_JSON_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` shows `test(P62-D2-004):`→`fix(P62-D2-004):`→`test(P62-D2-006):`→`fix(P62-D2-006):`
    - Checking out each red commit and running its test file exits non-zero; each observed failure is
      recorded in the matching `fail_before:`
    - `cd bbj-vscode && node -e "JSON.parse(require('fs').readFileSync('bbj-language-configuration.json','utf8'))"` exits 0
    - The parsed `comments`, `brackets`, `autoClosingPairs` and `surroundingPairs` entry counts match
      the pre-fix counts recorded in the `P62-D2-006` row's `notes:`
    - `bbj-vscode/test/extension-activation.test.ts` uses a mock `LanguageClient` and does not launch
      a real language-server process — verify the test file completes in under 10 seconds
    - `git show --stat` for `fix(P62-D2-004):` touches only `bbj-vscode/src/extension.ts`
    - `cd bbj-vscode && npm run build` exits 0 and `npm run lint` exits 0 with zero warnings
  </acceptance_criteria>
  <done>
    A failed language-server start is now visible to the user, and the language configuration is
    strict JSON with every pair intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the six ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the six rows for this plan)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09, §D-14 (FIX-04 discretion)
  </read_first>
  <action>
    Complete every field of the rows `P62-D2-007`, `P62-D2-008`, `P62-D2-009`, `P62-D5-004`,
    `P62-D2-004`, `P62-D2-006`. These are the phase's clearest user-facing rows — three of them
    change what a BBj developer sees on screen in both IDEs. Write `failure_scenario:` and
    `fix_applied:` so Phase 68's DOC-01 can lift them verbatim into `EASY-FIXES.md`, and set
    `user_facing: yes` on the three grammar rows and on `P62-D2-004`.

    Then run the plan-level baseline delta (D-09): from `bbj-vscode/`, `npm test` and `npm run lint`.
    Compare failing-test NAMES against the baseline gate set; identical-or-smaller. Exclude
    `beforeAll` hook-timeout suites as flaky per D-08 with the exclusion argued per occurrence.
    Append `### Plan 67-09 delta`. `./gradlew build` is not re-run — the IntelliJ plugin bundles this
    grammar but no `bbj-intellij/` file changed, and the grammar reaches it as a bundled resource.
    Note that fact in the delta subsection so the close-out does not have to re-derive it.
    Commit: `docs(67-09): close TextMate, extension and language-config rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - None of the six rows contains `TBD` or `pending` in any field
    - Every applied row's `commit:` sha resolves via `git cat-file -e`
    - The five `test_required: yes` rows have `fail_before:` beginning `observed at ` with a
      resolvable sha
    - The three grammar rows and `P62-D2-004` have `user_facing: yes`
    - `67-BASELINE.md` contains `### Plan 67-09 delta` with a verdict of `identical` or `smaller`, a
      `npm run lint` exit code of 0, and the note that the grammar reaches IntelliJ as a bundled
      resource with no Java-side change
  </acceptance_criteria>
  <done>Six rows closed; the phase's most visible changes are recorded as user-facing for DOC-01.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New file paths created by this plan (both test modules):
- `bbj-vscode/test/extension-activation.test.ts`
- `bbj-vscode/test/language-configuration.test.ts`

New symbols in source: none exported. The edits are a `.catch()` handler on `client.start()` in
`extension.ts`, two removed trailing commas in `bbj-language-configuration.json`, and three pattern
edits in `bbj-vscode/syntaxes/bbj.tmLanguage.json` (two removed `patterns` entries, one relaxed
comment `begin` pattern, one dropped trailing word-boundary assertion). Possibly one removed
repository rule (`string-character-escape`) if the record's optional clause is taken. No public API,
grammar-rule, or LSP contract change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.bbj` source text → TextMate tokenizer | arbitrary file content is matched against the grammar's regexes in the editor's tokenizer thread |
| extension host → language-server child process | `client.start()` launches the server; its failure path is what P62-D2-004 fixes |
| grammar file → both IDEs | `bbj.tmLanguage.json` is bundled by the VS Code extension and by the IntelliJ plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-09-01 | Denial of Service | catastrophic backtracking in an edited TextMate pattern | high | mitigate | Both edited patterns get *simpler*: `P62-D2-008` adds one alternation of two fixed-length branches (`[ \t]` or a zero-width end-of-line lookahead), and `P62-D2-009` removes an assertion. Neither introduces a nested quantifier or an unanchored greedy `.*` inside a repeated group — the shapes Phase 61's D1 sweep checked for. Confirm by inspection before committing each pattern. |
| T-67-09-02 | Tampering | a grammar edit reaching IntelliJ users with no Java-side review | medium | mitigate | Recorded explicitly: the grammar is a bundled resource shared by both IDEs, so these three rows are `user_facing: yes` and the plan delta states that no `bbj-intellij/` file changed yet IntelliJ behaviour does. |
| T-67-09-03 | Repudiation | a silent language-server start failure (P62-D2-004) | medium | mitigate | Attach the `.catch()` and surface the failure through the file's existing reporting path, so a non-starting server is reported rather than presenting as a fully-registered but inert extension. |
| T-67-09-04 | Tampering | a deleted trailing comma silently deleting an entry (P62-D2-006) | medium | mitigate | The regression test asserts strict-JSON parseability AND that every collection's entry count matches the pre-fix count recorded in the ledger row. |
| T-67-09-05 | Information Disclosure | the new activation error message echoing a local path or environment detail | low | mitigate | Surface the failure reason without interpolating environment variables or absolute paths beyond what the existing reporting path already prints. |
| T-67-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install and no dependency change; INVENTORY §3c test 3 guarantees it for every easy-fix record. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0 and `npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && node -e "JSON.parse(require('fs').readFileSync('syntaxes/bbj.tmLanguage.json','utf8'))"` exits 0
- `cd bbj-vscode && node -e "JSON.parse(require('fs').readFileSync('bbj-language-configuration.json','utf8'))"` exits 0
- `cd bbj-vscode && npx vitest run test/textmate-highlighting.test.ts test/textmate-bbx-highlighting.test.ts test/extension-activation.test.ts test/language-configuration.test.ts test/composer-commands.test.ts` exits 0
- Six ledger rows closed with no `pending`/`TBD`
- `### Plan 67-09 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- 6 rows dispositioned: 5 applied with red-proven tests, 1 (`P62-D5-004`) applied or argued no-op
- Both JSON artefacts parse strictly after the edits
- All grammar rows are recorded `user_facing: yes` for Phase 68's DOC-01
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-09-SUMMARY.md` when done
</output>
</content>
</invoke>
