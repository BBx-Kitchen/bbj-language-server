---
phase: 67-apply-easy-fixes
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - bbj-vscode/src/language/java-interop.ts
  - bbj-vscode/test/java-interop-service.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Two concurrent callers of `JavaInteropService.connect()` that both observe no existing connection open exactly one socket between them, and the second caller receives the same connection object as the first."
    - "After the peer drops an established connection, the next `connect()` call opens a fresh socket instead of returning the dead reference."
    - "A raced connect promise that rejects does not surface an unhandled rejection."
    - "A classpath response that omits `fields` or `methods` yields a JavaClass whose `fields` and `methods` are empty arrays, not undefined, and no consumer throws on reading them."
    - "`clearCache()` leaves the complete-class index empty, so a class resolved before the clear is re-fetched from the peer afterwards."
    - "`_resolvedClasses` never exceeds its configured size cap; resolving more distinct classes than the cap evicts and does not grow without bound."
    - "Every existing test in `test/method-return-java-type.test.ts` and `test/linking.test.ts` behaves exactly as it did on the phase-start baseline."
  artifacts:
    - bbj-vscode/test/java-interop-service.test.ts
  key_links:
    - "java-interop.ts is the only path that populates `JavaMethod.resolvedReturnType`; plan 67-01's type-inferer fallback depends on this file's behaviour staying unchanged for the resolved case."
    - "bbj-vscode/out/language/main.cjs is the single LS binary both IDEs consume — `npm run build` must pass on every commit in this plan, not just vitest."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a regression test as fail-before unless the red commit exists in git history and the test was observed failing on the pre-fix tree — no vacuous test, no attested-but-unobserved red"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT make the suite look better by deleting, skipping, weakening or timeout-tuning any existing test or test config — every change to a test's status must trace to a finding ID"
---

<objective>
Apply the 7 easy-fix findings on `bbj-vscode/src/language/java-interop.ts` — the single file
carrying the most fixes in the corpus.

Purpose: this file is the trust boundary between the language server and the java-interop socket
peer. Four of its findings are D2 correctness defects in connection lifecycle and cache
invalidation; all seven serialize into one plan because they share a file and must not be applied
by two agents concurrently.

Output: 11–12 commits, one ledger row closed per finding, a new
`bbj-vscode/test/java-interop-service.test.ts` unit-test home for this service.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
This plan follows the conventions established in `67-01-PLAN.md` `<phase_conventions>` and the
`<ledger_row_shape>` block in the same file. Read both before starting. In particular: extract a
finding record with the `awk -v ID=… ` one-liner, never `cat` a COVERAGE file; the
`classification:` field's test (5) clause names the exact edit; per-commit verification is
`npm run build` plus `npx vitest run <touched test files>` from `bbj-vscode/`.

**Test home for this plan.** `java-interop.ts` has no dedicated unit-test module today; the existing
interop coverage lives in `test/linking.test.ts > Linking Tests > Interop related tests`, whose 11
tests are in the baseline's deterministic-failure gate set because the peer at port 5008 is
unreachable and cannot be made to work (D-07). Create `bbj-vscode/test/java-interop-service.test.ts`
and drive `JavaInteropService` directly with a **mock socket** — `P61-D2-001`'s own record blesses
this approach in its test-4 clause ("regression-testable with vitest (mock socket)"). Never target
the live peer.
</phase_conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four D2 correctness fixes — connection lifecycle and cache invalidation</name>
  <files>
    bbj-vscode/src/language/java-interop.ts,
    bbj-vscode/test/java-interop-service.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/java-interop.ts — the whole file (955 lines). Read it once, in full,
      before the first edit; all four fixes land in it and re-reading per fix wastes context.
    - The four finding records, each via the awk extractor against `.planning/reviews/61-COVERAGE.md`:
      `P61-D2-001`, `P61-D2-002`, `P61-D2-003`, `P61-D2-004`
    - bbj-vscode/test/bbj-test-module.ts (`createBBjTestServices`, `JavaInteropTestService`)
    - bbj-vscode/test/linking.test.ts:1-30 (Shared Pattern B — the import and harness shape to match)
    - bbj-vscode/test/test-helper.ts
  </read_first>
  <behavior>
    - P61-D2-001 red: two `connect()` calls issued in the same tick while disconnected each construct
      their own socket (assert the socket factory was invoked twice — it should be once), and after
      the established connection emits `close`, the next `connect()` returns the same dead object
      instead of a fresh one.
    - P61-D2-001 green: the socket factory is invoked exactly once for two same-tick callers, both
      receive the identical connection, and after a `close`/`error` event the next `connect()`
      invokes the factory again.
    - P61-D2-002 red: a raced connect promise that rejects produces an unhandled rejection observable
      via a `process.on('unhandledRejection')` spy (or vitest's unhandled-rejection reporting).
    - P61-D2-002 green: no unhandled rejection; the rejection is still surfaced to the awaiting caller.
    - P61-D2-003 red: a classpath response omitting `fields`/`methods` yields a JavaClass whose
      `fields` and `methods` are `undefined`.
    - P61-D2-003 green: both are `[]`, matching the existing `classes`/`constructors` defaults.
    - P61-D2-004 red: a class resolved, then `clearCache()`, is served from the complete-class index
      rather than re-fetched.
    - P61-D2-004 green: after `clearCache()` the complete-class index is empty and the class is re-fetched.
  </behavior>
  <action>
    Apply four findings, each as a red commit immediately followed by its green commit, in this
    order. Create `bbj-vscode/test/java-interop-service.test.ts` in the first red commit, following
    Shared Pattern B's imports and `beforeAll`/`initializeWorkspace` shape, with a mock-socket
    factory injected by subclassing `JavaInteropService` and overriding `createSocket()` — do not
    reach the network. Each subsequent red commit adds only its own test to that file; every test
    added earlier must still pass in each later red run.

    1. **P61-D2-001** (`java-interop.ts:91-108`, D2, medium, effort 4). Exact edit per the record's
       test-5 clause: add an **in-flight connect promise** so concurrent callers share one
       `createSocket()`, and attach `'close'`/`'error'` listeners that clear `this.connection` so a
       dropped peer forces a reconnect instead of handing back the dead reference. The file's
       established error-narrowing idiom is `const detail = e instanceof Error ? e.message : String(e);`
       followed by `notifyJavaConnectionError(detail)` and `console.error` — match it; do not
       introduce a second error-reporting style. Commits:
       `test(P61-D2-001): add failing test for concurrent connect and dropped-peer recovery`, then
       `fix(P61-D2-001): guard connect with an in-flight promise and clear on close/error`.
    2. **P61-D2-002** (`:176-181`, D2, medium, effort 2). Exact edit: attach a no-op `.catch` to the
       raced promise. Commits: `test(P61-D2-002): ...`, `fix(P61-D2-002): attach a no-op catch to the raced connect promise`.
    3. **P61-D2-003** (`:576-585`, D2, medium, effort 2; D1 secondary). Exact edit: add
       `javaClass.fields ??= []` and `javaClass.methods ??= []` alongside the existing
       `classes`/`constructors` defaults at that site. Commits: `test(P61-D2-003): ...`,
       `fix(P61-D2-003): default fields and methods to empty arrays`.
    4. **P61-D2-004** (`:761-790`, D2, low, effort 2). Exact edit: call
       `this.clearCompleteClassIndex()` inside `clearCache()`. Commits: `test(P61-D2-004): ...`,
       `fix(P61-D2-004): clear the complete-class index in clearCache`.

    After each green commit, run `npm run build` and
    `npx vitest run test/java-interop-service.test.ts test/method-return-java-type.test.ts` and
    confirm green before moving to the next finding. Record each red sha in the corresponding
    ledger row's `fail_before:` field as you go — do not batch the ledger updates to the end of the
    task, because a lost red sha cannot be recovered.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/java-interop-service.test.ts test/method-return-java-type.test.ts</automated>
    <automated>git log --format=%s -8 | grep -cE '^(test|fix)\(P61-D2-00[1234]\):' | grep -x 8</automated>
  </verify>
  <acceptance_criteria>
    - `bbj-vscode/test/java-interop-service.test.ts` exists and imports `createBBjTestServices` (or
      constructs `JavaInteropService` directly); it contains no reference to port 5008 and opens no
      real socket — verify with `cd bbj-vscode && npx vitest run test/java-interop-service.test.ts`
      succeeding with the java-interop process not running
    - `git log --format=%s -8` shows four `test(P61-D2-00N):` commits each immediately followed by
      its `fix(P61-D2-00N):` commit, in the order 001, 002, 003, 004
    - For each of the four, checking out the red commit and running
      `npx vitest run test/java-interop-service.test.ts` exits non-zero, and the observed failure
      message is recorded in that finding's ledger row `fail_before:` field
    - `cd bbj-vscode && npx vitest run test/java-interop-service.test.ts` exits 0 at HEAD
    - `cd bbj-vscode && npm run build` exits 0 at HEAD
    - `git diff 67-01-HEAD..HEAD --name-only` lists only `bbj-vscode/src/language/java-interop.ts`,
      `bbj-vscode/test/java-interop-service.test.ts` and `.planning/phases/67-apply-easy-fixes/`
    - Four ledger rows (`P61-D2-001`..`P61-D2-004`) have `verdict: applied`, a `red-sha + green-sha`
      `commit:` value, and `user_facing:` set from each record's `failure_scenario`
  </acceptance_criteria>
  <done>
    The four connection-lifecycle and cache-invalidation defects are fixed, each proven by a test
    that was observed red on the pre-fix tree, and `java-interop.ts` has a unit-test home that does
    not depend on the unreachable peer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The unbounded cache (D3), the duplicated request path (D4), and the stale comment (D8)</name>
  <files>
    bbj-vscode/src/language/java-interop.ts,
    bbj-vscode/test/java-interop-service.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/java-interop.ts as it stands after Task 1 — read only the ranges
      `40-48` (`_resolvedClasses`), `175-314` (the request paths) and `757-790` (the comment plus the
      `clearCache()` body Task 1 edited)
    - The three finding records via the awk extractor: `P61-D3-001`, `P61-D4-003`, `P61-D8-001`
      (all in `.planning/reviews/61-COVERAGE.md`)
    - bbj-vscode/test/java-interop-service.test.ts as written in Task 1
  </read_first>
  <behavior>
    - P61-D3-001 red: resolving more distinct classes than the intended cap leaves every entry
      present in `_resolvedClasses` — the map grows without bound.
    - P61-D3-001 green: the map size never exceeds the cap; the least-recently-used entry is evicted
      and a subsequent lookup of an evicted class re-fetches rather than returning stale data.
  </behavior>
  <action>
    Three findings, in this order.

    1. **P61-D3-001** (`java-interop.ts:40-48`, D3, low, effort 4) — D3 requires a regression test
       per D-11. Exact edit per the record's test-5 clause: bound `_resolvedClasses` with an
       LRU/size cap. Choose the cap value the record names; if the record names no number, pick one,
       state it in the ledger row's `notes:` as a discretion choice, and make it a named constant in
       the file rather than a bare literal. Commits: `test(P61-D3-001): add failing test for unbounded
       resolved-class cache`, then `fix(P61-D3-001): bound _resolvedClasses with an LRU size cap`.
    2. **P61-D4-003** (`:175-314`, D4, low, effort 4) — no regression test per D-11 (no behaviour
       change). Exact edit per the record's test-5 clause: extract a private
       `sendRequestSafe(request, params, fallback)` helper and route the duplicated request paths in
       that range through it. Preserve the file's JSDoc convention — every method in this file is
       preceded by a `/** … */` block; the new helper gets one. The extraction must be
       behaviour-preserving: the existing tests in `test/java-interop-service.test.ts` and
       `test/method-return-java-type.test.ts` are the evidence. Single commit:
       `fix(P61-D4-003): extract sendRequestSafe helper for the duplicated request paths`.
    3. **P61-D8-001** (`:757-760`, D8, low, effort 2) — comment accuracy. The record's test-5 clause
       offers two resolutions: fix the comment, **or** fix the code per `P61-D2-004` so the comment
       becomes true. `P61-D2-004` landed in Task 1, so read the comment at `:757-760` as it now
       stands against the post-fix `clearCache()` body. If the comment is now accurate, this row is a
       genuine no-op: set `verdict: no-op` in the ledger with `notes:` quoting the record's own
       "or fix the code per P61-D2-004 so the comment becomes true" wording and naming
       `P61-D2-004`'s green sha as the resolution, and land **no** commit for it. If any clause of
       the comment is still wrong, correct exactly that clause and commit
       `docs(P61-D8-001): correct the clearCache comment`. Either outcome is recorded; neither is
       silently dropped.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/java-interop-service.test.ts test/method-return-java-type.test.ts test/imports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` contains `test(P61-D3-001):` immediately followed by `fix(P61-D3-001):`
    - Checking out the `P61-D3-001` red commit and running
      `npx vitest run test/java-interop-service.test.ts` exits non-zero
    - `grep -c 'sendRequestSafe' bbj-vscode/src/language/java-interop.ts` is at least 2 (the
      declaration plus at least one call site), and the declaration is preceded by a `/**` line
    - `git log --format=%s -2` contains a `fix(P61-D4-003):` commit whose `git show --stat` touches
      only `bbj-vscode/src/language/java-interop.ts`
    - The `P61-D8-001` ledger row has `verdict:` set to either `applied` (with a `docs(P61-D8-001):`
      commit sha) or `no-op` (with `commit: none — <reason>` quoting the record's own wording and
      naming P61-D2-004's sha); it is not `pending`
    - `cd bbj-vscode && npm run build` exits 0 and
      `npx vitest run test/java-interop-service.test.ts test/method-return-java-type.test.ts test/imports.test.ts` exits 0
    - The LRU cap is a named constant: `grep -nE 'const [A-Z_]+ *= *[0-9]+' bbj-vscode/src/language/java-interop.ts`
      returns a line whose identifier appears in the `_resolvedClasses` bounding logic
  </acceptance_criteria>
  <done>
    All 7 java-interop findings are dispositioned: 5 applied with tests, 1 applied without (D4), and
    the D8 comment row closed as either applied or an argued no-op.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the seven ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the seven java-interop rows)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md (`### Deterministic failures (gate set)`)
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09
  </read_first>
  <action>
    Complete every field of the seven rows `P61-D2-001`, `P61-D2-002`, `P61-D2-003`, `P61-D2-004`,
    `P61-D3-001`, `P61-D4-003`, `P61-D8-001`. `failure_scenario:` and `fix_applied:` are the fields
    Phase 68's DOC-01 lifts directly — write them as standalone prose that reads correctly without
    the COVERAGE record beside it. Set `user_facing: yes` for any fix whose `failure_scenario`
    describes something a BBj developer would observe in the editor (a hang, a missing completion, a
    missing diagnostic), `no` otherwise; this is the FIX-04 routing signal and it must be decided per
    row, not left blank.

    Then run the plan-level baseline delta (D-09). From `bbj-vscode/`: `npm test` and `npm run lint`.
    Compare the failing-test NAME set against `67-BASELINE.md`'s `### Deterministic failures (gate
    set)`; the gate is identical-or-smaller. Exclude any suite marked failed purely by a `beforeAll`
    hook timeout in `WorkspaceManager.initializeWorkspace()` as flaky per D-08, naming the suite,
    quoting the timeout, and noting whether it reproduced on a re-run — append each exclusion to
    `## Flaky exclusions (D-08)`. Append `### Plan 67-02 delta` with the verdict, the compared name
    sets, and the lint exit code. `./gradlew build` is not re-run — no `bbj-intellij/` file changed.
    Commit: `docs(67-02): close java-interop apply-set rows and record baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
    <automated>grep -c 'pending' .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md</automated>
  </verify>
  <acceptance_criteria>
    - None of the seven java-interop rows contains the token `TBD` or `pending` in any field
    - Every applied row's `commit:` sha resolves via `git cat-file -e <sha>`
    - Every row with `test_required: yes (D-11 D2)` or `yes (D-11 D3)` has a `fail_before:` value
      beginning `observed at ` followed by a resolvable sha
    - Every row has `user_facing:` set to exactly `yes` or `no`
    - `67-BASELINE.md` contains a `### Plan 67-02 delta` subsection whose verdict line reads
      `identical` or `smaller`
    - `cd bbj-vscode && npm run lint` exit code is recorded in that subsection
  </acceptance_criteria>
  <done>
    Seven rows closed with real shas and DOC-01-ready prose; the suite is no worse than the baseline.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
New file paths created by this plan:
- `bbj-vscode/test/java-interop-service.test.ts` — vitest unit-test module for `JavaInteropService`,
  driven by a mock socket; no exported symbols beyond the vitest `describe`/`test` blocks

New symbols in source (`bbj-vscode/src/language/java-interop.ts`), all private/internal — no export,
no public API, no LSP contract change (INVENTORY §3c test 2 guarantees this for every easy fix):
- an in-flight connect promise field guarding `connect()` (P61-D2-001)
- `'close'` / `'error'` listeners registered on the established connection (P61-D2-001)
- a named size-cap constant bounding `_resolvedClasses` (P61-D3-001)
- a private `sendRequestSafe(request, params, fallback)` helper (P61-D4-003)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| java-interop socket peer (127.0.0.1:5008) → language server | classpath responses (class/field/method shapes) are parsed and assigned into the AST without schema validation |
| language server → local socket | outbound JSON-RPC requests carrying workspace-derived class names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-02-01 | Denial of Service | `connect()` socket leak (P61-D2-001) | medium | mitigate | The in-flight connect promise ensures one socket per disconnected window; the `close`/`error` listeners release the dead reference so file descriptors are not retained. The regression test asserts the socket factory is invoked exactly once for two same-tick callers. |
| T-67-02-02 | Denial of Service | unbounded `_resolvedClasses` (P61-D3-001) | medium | mitigate | Bound the map with a named LRU size cap; the regression test asserts the map never exceeds the cap across more distinct resolutions than the cap. |
| T-67-02-03 | Tampering | peer-supplied class shape assigned into the AST (P61-D2-003, D1 secondary) | high | transfer | The `??= []` default removes the undefined-dereference crash but does NOT validate peer data. The underlying unvalidated-peer-data finding is `P61-D1-002`, classified `major-refactor` by INVENTORY §3c test 6 and routed to Phase 68 / Phase 69. Phase 67 must not widen what peer data can reach — the fix adds defaults only. Transferred, with the routing recorded in the ledger row's `notes:`. |
| T-67-02-04 | Spoofing | the socket peer's identity is not authenticated | high | transfer | Pre-existing and out of scope: no `easy-fix` record covers peer authentication (Phase 65 produced zero easy-fix records; all D1 findings are `major-refactor`). Routed to Phase 68's MAJOR-REFACTORS.md. |
| T-67-02-05 | Information Disclosure | `console.error` on connect failure may print peer path/host detail | low | accept | The existing `e instanceof Error ? e.message : String(e)` narrowing is preserved unchanged; the fix introduces no new logging of peer content. |
| T-67-02-SC | Tampering | npm/pip/cargo installs | high | accept | This plan runs no package-manager install and adds no dependency; INVENTORY §3c test 3 guarantees every easy-fix record adds none. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0
- `cd bbj-vscode && npx vitest run test/java-interop-service.test.ts test/method-return-java-type.test.ts test/imports.test.ts test/linking.test.ts` — the only failures are the baseline's named interop failures
- `git log --format=%s` for this plan shows every commit scoped to a `P61-*` finding ID
- Seven ledger rows closed with no `pending`/`TBD` field
- `### Plan 67-02 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- All 7 `java-interop.ts` easy-fix rows dispositioned (5 applied with tests, 1 applied without, 1 applied-or-no-op)
- Each of the 5 test-bearing fixes has a red commit that was observed failing and is the direct parent of its green commit
- No file outside `java-interop.ts`, the new test file, and `.planning/` is touched
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-02-SUMMARY.md` when done
</output>
</content>
</invoke>
