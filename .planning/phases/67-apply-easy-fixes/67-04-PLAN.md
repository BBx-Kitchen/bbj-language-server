---
phase: 67-apply-easy-fixes
plan: 04
type: execute
wave: 4
depends_on: [67-03]
files_modified:
  - bbj-vscode/src/language/bbj-completion-provider.ts
  - bbj-vscode/src/language/bbj-document-symbol-provider.ts
  - bbj-vscode/src/language/bbj-token-builder.ts
  - bbj-vscode/test/completion-test.test.ts
  - bbj-vscode/test/document-symbol.test.ts
  - bbj-vscode/test/lexer.test.ts
  - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md
  - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A completion request cancelled mid-flight stops at the next await boundary in `getFieldCompletion`, `getFilePathCompletion` and `completeAutoImportClasses` instead of running to completion."
    - "Repeated class-candidate lookups for the same prefix hit the memoization cache instead of re-running `findClassCandidatesByPrefix`, and return the same candidate set the uncached path returns."
    - "Two sibling AST nodes that share a start position but differ in end position produce two distinct document symbols, not one."
    - "A token stream in which the expected next token is absent raises instead of splicing at index -1."
    - "`cd bbj-vscode && npm run lint` exits 0 and reports zero warnings — the two pre-existing warnings recorded in the phase baseline are gone."
  artifacts:
    - bbj-vscode/src/language/bbj-completion-provider.ts
    - bbj-vscode/src/language/bbj-document-symbol-provider.ts
    - bbj-vscode/src/language/bbj-token-builder.ts
  key_links:
    - "P61-D4-010 is the only finding whose evidence is the two `npm run lint` warnings; applying it is what makes lint literally clean (D-10). No untracked suppression edit may substitute for it."
    - "P61-D2-014 and P61-D4-010 touch the same file and must be serialized; read the full getSymbol/collectPositions pair before either edit."
  prohibitions:
    - requirement_id: FIX-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT include in any commit an edit that the cited finding record does not name — no opportunistic cleanup, no scope-widening riding along under a finding ID"
    - requirement_id: FIX-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit, re-classify or re-triage any finding record, INVENTORY.md, or any closed 6N-COVERAGE.md — the easy/major classification is applied as given, never re-litigated to make a fix fit or to dodge one"
    - requirement_id: FIX-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT record a regression test as fail-before unless the red commit exists in git history and the test was observed failing on the pre-fix tree — no vacuous test, no attested-but-unobserved red"
    - requirement_id: FIX-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT reach a clean lint result by adding a suppression, widening an ignore list, or relaxing a rule — lint cleanliness must come from applying P61-D4-010's named edit and nothing else"
---

<objective>
Apply the 6 easy-fix findings on the three LSP-facing providers: `bbj-completion-provider.ts` (2),
`bbj-document-symbol-provider.ts` (2), `bbj-token-builder.ts` (2).

Purpose: these three are what a BBj developer touches on every keystroke — completion, outline,
tokenization. This plan also carries the phase's lint gate: `P61-D4-010` is the finding whose
evidence *is* the two standing `npm run lint` warnings, so lint reaches literal cleanliness here,
via a finding, not via housekeeping (D-10).

Output: 10 commits, 6 ledger rows closed, and a lint run that exits 0 with zero warnings.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apply-easy-fixes/67-CONTEXT.md
@.planning/phases/67-apply-easy-fixes/67-PATTERNS.md
@.planning/phases/67-apply-easy-fixes/67-01-PLAN.md
@CLAUDE.md
</context>

<phase_conventions>
Follow `67-01-PLAN.md` `<phase_conventions>` and `<ledger_row_shape>` exactly. Extract each record
with the awk one-liner. The `classification:` test-5 clause names the exact edit.
</phase_conventions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bbj-completion-provider.ts — cancellation threading and prefix memoization</name>
  <files>
    bbj-vscode/src/language/bbj-completion-provider.ts,
    bbj-vscode/test/completion-test.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-completion-provider.ts — ranges `90-116` and `154-200` plus the
      surrounding method signatures; the file is 818 lines, so read those ranges and the class
      declaration rather than the whole file
    - Finding records `P61-D2-013` and `P61-D3-004` via the awk extractor against `61-COVERAGE.md`
    - bbj-vscode/test/completion-test.test.ts — the existing completion-test harness to extend
    - bbj-vscode/test/file-path-completion.test.ts — the existing coverage for `getFilePathCompletion`
    - bbj-vscode/test/bbj-test-module.ts
  </read_first>
  <behavior>
    - P61-D2-013 red: issue a completion request with a cancellation token that is cancelled before
      the first await resolves; assert the provider returns early. Pre-fix the token is not consulted,
      so the provider runs to completion and the assertion fails.
    - P61-D2-013 green: each of `getFieldCompletion`, `getFilePathCompletion` and
      `completeAutoImportClasses` checks `cancelToken.isCancellationRequested` at each await
      boundary and returns early; an uncancelled request produces exactly the completions it
      produced before.
    - P61-D3-004 red: call the class-candidate lookup twice with the same prefix; assert
      `findClassCandidatesByPrefix` is invoked once. Pre-fix it is invoked twice.
    - P61-D3-004 green: the second call is served from cache and returns a candidate set equal to
      the uncached result; a different prefix still triggers a real lookup.
  </behavior>
  <action>
    Two findings, in this order, each red-then-green.

    1. **P61-D2-013** (`bbj-completion-provider.ts:154-200`, D2, medium, effort 4). Exact edit per
       the record's test-5 clause: thread `cancelToken` through `getFieldCompletion`,
       `getFilePathCompletion` and `completeAutoImportClasses`, and check
       `cancelToken.isCancellationRequested` at each await boundary. Add the tests to the existing
       `bbj-vscode/test/completion-test.test.ts`. Commits: `test(P61-D2-013): add failing test for
       uncancellable completion request`, then `fix(P61-D2-013): thread cancelToken through the
       completion await boundaries`.
    2. **P61-D3-004** (`:90-116`, D3, medium, effort 4) — D3 requires a regression test per D-11.
       Exact edit per the test-5 clause: add a small prefix-keyed memoization/debounce cache around
       `findClassCandidatesByPrefix`. Assert both the invocation-count reduction and that the cached
       result equals the uncached result — a cache that returns a different set is a regression.
       State the cache's invalidation trigger in the ledger row's `notes:`; a cache with no
       invalidation story is a stale-completion bug waiting to be filed. Commits:
       `test(P61-D3-004): add failing test for repeated class-candidate prefix lookups`, then
       `fix(P61-D3-004): memoize findClassCandidatesByPrefix by prefix`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/completion-test.test.ts test/file-path-completion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -4` shows `test(P61-D2-013):`→`fix(P61-D2-013):`→`test(P61-D3-004):`→`fix(P61-D3-004):`
    - Checking out each red commit and running `npx vitest run test/completion-test.test.ts` exits
      non-zero; the observed failure is recorded in the matching `fail_before:`
    - `grep -c 'isCancellationRequested' bbj-vscode/src/language/bbj-completion-provider.ts` is at least 3
    - The `P61-D3-004` test asserts both a reduced invocation count AND set equality between the
      cached and uncached results
    - `cd bbj-vscode && npx vitest run test/completion-test.test.ts test/file-path-completion.test.ts` exits 0
    - `cd bbj-vscode && npm run build` exits 0
    - The `P61-D3-004` ledger row's `notes:` names the cache's invalidation trigger
  </acceptance_criteria>
  <done>
    Cancelled completions stop early, repeated prefix lookups are served from a cache proven to
    return the same candidates, and both are locked by tests observed red beforehand.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: symbol-provider position collisions, the lint gate, and the token-builder splice guard</name>
  <files>
    bbj-vscode/src/language/bbj-document-symbol-provider.ts,
    bbj-vscode/src/language/bbj-token-builder.ts,
    bbj-vscode/test/document-symbol.test.ts,
    bbj-vscode/test/lexer.test.ts
  </files>
  <read_first>
    - bbj-vscode/src/language/bbj-document-symbol-provider.ts — the whole file (183 lines),
      specifically the full `getSymbol`/`collectPositions` pair. The bug is about position
      collisions across sibling nodes sharing a start offset, encoded as
      `line * 100_000 + character`; do not edit either half without having read both.
    - bbj-vscode/src/language/bbj-token-builder.ts — the whole file (182 lines)
    - Finding records `P61-D2-014`, `P61-D4-010`, `P61-D2-008`, `P61-D4-005` via the awk extractor
    - bbj-vscode/test/document-symbol.test.ts and bbj-vscode/test/lexer.test.ts — the existing homes
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md §`## npm run lint` — the two warnings this
      task must clear, with their `file:line`
  </read_first>
  <behavior>
    - P61-D2-014 red: parse a program containing two sibling nodes that share a start position but
      differ in end position; assert the outline contains two distinct symbols. Pre-fix
      `coveredPositions` keys on start only, so one is dropped and the assertion fails.
    - P61-D2-014 green: both symbols appear; every existing `document-symbol.test.ts` case is unchanged.
    - P61-D2-008 red: feed the token post-processing a stream in which the expected next token is
      absent; assert it raises a clear error. Pre-fix `nextTokenIndex === -1` reaches the splice and
      corrupts the stream silently.
    - P61-D2-008 green: the absent-token case raises before splicing; every valid stream tokenizes
      exactly as before.
  </behavior>
  <action>
    Four findings, in this order — `P61-D2-014` before `P61-D4-010` because both touch the
    symbol-provider file and the lint gate must be measured against the finished file.

    1. **P61-D2-014** (`bbj-document-symbol-provider.ts:155,173-182`, D2, low, effort 2). Exact edit
       per the test-5 clause: key `coveredPositions` on the full range — start **and** end — not just
       start. Commits: `test(P61-D2-014): add failing test for sibling symbols sharing a start
       position`, then `fix(P61-D2-014): key coveredPositions on the full node range`.
    2. **P61-D4-010** (`:75,149`, D4, low, effort **1** — off-scale, annotated in-record as rounded
       down for ISSUE-03; carry that annotation into the ledger row's `notes:`, do not re-round).
       Exact edit per the test-5 clause: delete both suppression comments at lines 75 and 149. They
       guard a `(astNode as any).name` read that no longer trips `@typescript-eslint/no-explicit-any`.
       No test per D-11 (D4, no behaviour change) — the verification is `npm run lint`. Single
       commit: `fix(P61-D4-010): drop the two unused lint suppression directives`.
       After this commit `npm run lint` must exit 0 with zero warnings (D-10). If it does not,
       do not add a suppression and do not relax a rule — investigate and record what else is
       warning, because the baseline recorded exactly two warnings and both are this finding.
    3. **P61-D2-008** (`bbj-token-builder.ts:67-71`, D2, low, effort 2). Exact edit per the test-5
       clause: throw when `nextTokenIndex === -1` before splicing. Add the test to
       `bbj-vscode/test/lexer.test.ts`. Commits: `test(P61-D2-008): add failing test for splice at a
       missing token index`, then `fix(P61-D2-008): throw before splicing at a missing token index`.
    4. **P61-D4-005** (`:7-64`, D4, low, effort 4) — no test per D-11. Exact edit per the test-5
       clause: extract `reorderTokenPriorities(tokens)` out of `buildTokens()`. Behaviour-preserving:
       the existing `test/lexer.test.ts` and `test/parser.test.ts` cases are the evidence. Remember
       BBj is case-insensitive — the custom token patterns
       (`ASTERISK_STANDALONE`, `RELEASE_NL`, `KEYWORD_STANDALONE`, …) and their ordering must survive
       the extraction unchanged. Single commit:
       `fix(P61-D4-005): extract reorderTokenPriorities from buildTokens`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run build &amp;&amp; npx vitest run test/document-symbol.test.ts test/lexer.test.ts test/parser.test.ts test/example-files.test.ts</automated>
    <automated>cd bbj-vscode &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -6` shows, in order, `test(P61-D2-014):`, `fix(P61-D2-014):`,
      `fix(P61-D4-010):`, `test(P61-D2-008):`, `fix(P61-D2-008):`, `fix(P61-D4-005):`
    - Checking out the `P61-D2-014` and `P61-D2-008` red commits and running their test files each
      exits non-zero; the observed failures are recorded in the matching `fail_before:` fields
    - `cd bbj-vscode && npm run lint` exits 0 AND its output contains no line matching
      `[0-9]+ (problem|warning)` — a clean run, not a run with tolerated warnings
    - `git show --stat` for `fix(P61-D4-010):` touches only
      `bbj-vscode/src/language/bbj-document-symbol-provider.ts` and changes exactly two lines
    - No file added to any eslint ignore list and no eslint config file modified anywhere in this
      plan: `git diff 67-03-HEAD..HEAD --name-only` contains no path matching `eslint`
    - `grep -c 'reorderTokenPriorities' bbj-vscode/src/language/bbj-token-builder.ts` is at least 2
    - `cd bbj-vscode && npx vitest run test/document-symbol.test.ts test/lexer.test.ts test/parser.test.ts test/example-files.test.ts` exits 0
    - The `P61-D4-010` ledger row's `notes:` carries the in-record off-scale-effort annotation verbatim
  </acceptance_criteria>
  <done>
    Sibling symbols no longer collide, the token splice guard is in place, `buildTokens` is
    decomposed without changing tokenization, and `npm run lint` is genuinely clean via a finding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the six ledger rows and run the plan baseline delta</name>
  <files>
    .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md,
    .planning/phases/67-apply-easy-fixes/67-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md (the six rows for this plan)
    - .planning/phases/67-apply-easy-fixes/67-BASELINE.md
    - .planning/phases/67-apply-easy-fixes/67-CONTEXT.md §D-08, §D-09, §D-10
  </read_first>
  <action>
    Complete every field of the rows `P61-D2-013`, `P61-D3-004`, `P61-D2-014`, `P61-D4-010`,
    `P61-D2-008`, `P61-D4-005`. `failure_scenario:` and `fix_applied:` must read as standalone prose
    for Phase 68's DOC-01. Set `user_facing: yes` for the completion, outline and tokenization fixes
    whose failure scenarios describe editor-visible symptoms.

    Then run the plan-level baseline delta (D-09), and record the lint milestone explicitly: this is
    the plan after which `npm run lint` is expected to be literally clean per D-10. Append
    `### Plan 67-04 delta` to `67-BASELINE.md` including the lint exit code and the statement that
    the two baseline warnings at `bbj-document-symbol-provider.ts:75` and `:149` are cleared by
    `P61-D4-010` — naming the finding, not describing it as housekeeping. Compare failing-test NAMES
    against the baseline gate set; identical-or-smaller. Exclude hook-timeout suites as flaky per
    D-08 with the exclusion argued per occurrence. `./gradlew build` is not re-run.
    Commit: `docs(67-04): close provider rows, record lint-clean milestone and baseline delta`.
  </action>
  <verify>
    <automated>cd bbj-vscode &amp;&amp; npm run lint &amp;&amp; npm test 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - None of the six rows contains `TBD` or `pending` in any field
    - Every applied row's `commit:` sha resolves via `git cat-file -e`
    - Every `test_required: yes` row has `fail_before:` beginning `observed at ` with a resolvable sha
    - `67-BASELINE.md` contains `### Plan 67-04 delta` recording `npm run lint` exit code 0 and zero
      warnings, and naming `P61-D4-010` as the cause
    - The delta verdict line reads `identical` or `smaller`
    - Every row has `user_facing:` set to exactly `yes` or `no`
  </acceptance_criteria>
  <done>Six rows closed; lint is clean and the reason is a finding ID, not a suppression.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new file paths in this plan — every test lands in an existing module
(`test/completion-test.test.ts`, `test/document-symbol.test.ts`, `test/lexer.test.ts`).

New symbols in source, all private/internal — no export, no public API, no LSP contract change:
- `cancelToken` parameters threaded through `getFieldCompletion`, `getFilePathCompletion` and
  `completeAutoImportClasses` (P61-D2-013) — internal signatures only, the LSP completion contract
  is unchanged
- a prefix-keyed memoization cache around `findClassCandidatesByPrefix` (P61-D3-004)
- a full-range key for `coveredPositions` in `collectPositions` (P61-D2-014)
- a `reorderTokenPriorities(tokens)` function extracted from `buildTokens()` (P61-D4-005)

Deletions: the two lint suppression directives at `bbj-document-symbol-provider.ts:75` and `:149`
(P61-D4-010).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LSP client → completion/symbol providers | request params (position, cancellation token, prefix) arrive from the editor |
| `.bbj` source text → token builder | arbitrary workspace file content is tokenized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-04-01 | Denial of Service | uncancellable completion work (P61-D2-013) | medium | mitigate | Checking `cancelToken.isCancellationRequested` at each await boundary bounds the work a rapid-typing client can queue; the regression test asserts early return on a pre-cancelled token. |
| T-67-04-02 | Denial of Service | unbounded prefix memoization cache (P61-D3-004) | medium | mitigate | The cache introduced by the fix must itself be bounded or invalidated; the ledger row records the invalidation trigger, and an unbounded map would reintroduce the class of defect P61-D3-001 fixed in plan 67-02. |
| T-67-04-03 | Tampering | token stream spliced at index -1 (P61-D2-008) | medium | mitigate | Throwing before the splice turns silent stream corruption on malformed input into a visible failure; the regression test drives the missing-token case. |
| T-67-04-04 | Information Disclosure | a completion cache serving one document's candidates to another | medium | mitigate | The cache is keyed on prefix; the ledger row must state whether the key is document-scoped. If it is not, scope it — cross-document candidate bleed is a correctness and disclosure defect, not an acceptable cache trade. |
| T-67-04-05 | Denial of Service | catastrophic backtracking in a custom token pattern reordered by P61-D4-005 | low | accept | Phase 61's D1 sweep checked every custom pattern in `bbj-token-builder.ts` for catastrophic-backtracking shapes and found none; the extraction reorders nothing and adds no pattern. Recorded, not separately mitigated. |
| T-67-04-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install and no dependency change; INVENTORY §3c test 3 guarantees it for every easy-fix record. |
</threat_model>

<verification>
- `cd bbj-vscode && npm run build` exits 0
- `cd bbj-vscode && npm run lint` exits 0 with zero warnings
- `cd bbj-vscode && npx vitest run test/completion-test.test.ts test/file-path-completion.test.ts test/document-symbol.test.ts test/lexer.test.ts test/parser.test.ts test/example-files.test.ts` exits 0
- Six ledger rows closed with no `pending`/`TBD`
- `### Plan 67-04 delta` verdict is `identical` or `smaller`
</verification>

<success_criteria>
- 6 rows applied: 4 with red-proven regression tests, 2 D4 fixes without
- `npm run lint` reaches literal cleanliness and the ledger attributes it to `P61-D4-010`
- No eslint config, ignore list, or suppression is added anywhere in this plan
</success_criteria>

<output>
Create `.planning/phases/67-apply-easy-fixes/67-04-SUMMARY.md` when done
</output>
</content>
</invoke>
