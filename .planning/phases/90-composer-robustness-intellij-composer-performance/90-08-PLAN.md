---
phase: 90-composer-robustness-intellij-composer-performance
plan: 08
type: execute
wave: 4
depends_on: ["90-01", "90-02", "90-03", "90-04", "90-05", "90-06", "90-07"]
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09, DISC-10, DISC-11]
files_modified:
  - bbj-vscode/test/functional/installed-extension-e2e.test.ts
  - QA/FULL-TEST-CHECKLIST.md
  - .planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md
  - .planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'The language server inside the reinstalled VS Code bundle decodes `x = MSGBOX(` at character 11 as found true and incomplete true, with span 4..11, an empty message and no `replace`, and decodes `x = MSGBOX("Hi",` as incomplete with message `"Hi"` (D-04).'
    - 'The same installed server returns `valid: false` with `xError` `Not a number — remove the quotes: 10` for an addWindow preview whose x is `"10"`, and `valid: true` with statement `sysgui!.addWindow(0, 0, 0, 0, "", $00000000$)` when every free-text field is blank (D-06, D-07).'
    - 'The IntelliJ plugin zip, rebuilt after every code plan in this phase, lists a `language-server/main.cjs` entry from this build.'
    - 'QA/FULL-TEST-CHECKLIST.md gains VS Code rows 22 and 23 and IntelliJ rows 28, 29 and 30, covering unfinished-call completion, wizard target safety, field validation, the debounced preview and a composer reopen across a server restart. No existing row changes.'
    - 'The folded todo about MSGBOX compose-new nesting moves to `.planning/todos/completed/`, with a resolution note naming the plans that fixed it.'
    - statement: "In IntelliJ, against a plugin zip rebuilt from the final tree, typing a burst in the MSGBOX, addWindow and addChildWindow dialogs updates the preview once after typing stops with OK re-enabled only then; a malformed field shows a red label and keeps OK disabled; an unfinished MSGBOX( call opens Complete MSGBOX call and applies one complete call; a second composer open is visibly faster and still works after Restart Language Server (D-05, D-08, D-12, D-15)"
      verification: backstop
    - statement: "In VS Code, against the reinstalled VSIX, the cue and the context menu on an unfinished MSGBOX( call open Complete MSGBOX call and leave one complete call, an Insert after the line changed is refused with the warning, and a malformed addWindow or addChildWindow field disables Insert with the message under the field (D-02, D-04, D-08)"
      verification: backstop
  prohibitions:
    - 'No source change under `bbj-vscode/src` or `bbj-intellij/src`: this plan proves, documents and stages, and the code plans own every behaviour.'
    - 'Existing QA rows keep their wording; new rows are only appended at the end of the VS Code composer table and of the IntelliJ composer table.'
  artifacts:
    - path: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      provides: "installed-bundle assertions for the unfinished MSGBOX decode and the addWindow `valid` verdict"
    - path: "QA/FULL-TEST-CHECKLIST.md"
      provides: "VS Code rows 22-23 and IntelliJ rows 28-30"
    - path: ".planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md"
      provides: "the closed folded todo with its resolution"
  key_links:
    - from: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      to: "bbj-vscode/src/msgbox-composer.ts"
      via: "`bbj/composer/msgbox/decodeCall` sent to the installed bundle's server returns the `incomplete` outcome"
      pattern: "bbj/composer/msgbox/decodeCall"
    - from: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      to: "bbj-vscode/src/addwindow-composer.ts"
      via: "`bbj/composer/addwindow/preview` sent to the installed server returns `valid` and `xError`"
      pattern: "bbj/composer/addwindow/preview"
---

<objective>
Close Phase 90 at the level users experience it, for DISC-07 through DISC-11. This plan:
- proves the rebuilt and reinstalled VS Code bundle serves the new MSGBOX `incomplete` outcome and the addWindow `valid` verdict end to end, and rebuilds the IntelliJ zip;
- adds QA checklist rows for every behaviour this phase changed, in both IDEs;
- closes the folded MSGBOX nesting todo;
- stages the manual-only checks from the validation strategy (the Swing debounce feel, and reopening across a server restart), plus the completion and validation flows, as end-of-phase human checks against artifacts rebuilt from the final tree.

Purpose: the phase is done only when the artifacts a tester installs carry every fix, and a human confirms the live behaviour.
Output: installed-bundle e2e assertions, QA rows, a closed todo, and two human checks.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-CONTEXT.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-VALIDATION.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-01-SUMMARY.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-02-SUMMARY.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-07-SUMMARY.md

<interfaces>
From the code plans of this phase:
- `bbj/composer/msgbox/decodeCall` with `{ line: 'x = MSGBOX(', character: 11 }` returns `{"found":true,"incomplete":true,"edit":{"callStart":4,"callEnd":11},"trailingArgs":[],"initial":{"message":"","title":"","buttonSet":0,"icon":0,"defaultButton":0,"flags":[],"customButtons":[]},"hasOptions":false}`. With `{ line: 'x = MSGBOX("Hi",', character: 16 }` it returns incomplete with message `"Hi"`.
- `bbj/composer/addwindow/preview` with `{ input: { flags: [], eventMaskEnabled: false, eventMask: [], receiver: '', sysgui: '', x: '"10"', y: '', width: '', height: '', title: '' } }` returns `valid: false` and `xError: 'Not a number — remove the quotes: 10'`. With `x: ''` it returns `valid: true` and statement `sysgui!.addWindow(0, 0, 0, 0, "", $00000000$)`.
- VS Code labels:
  - the MSGBOX panel title is `Complete MSGBOX call`, and the lightbulb action is `Complete MSGBOX call…`;
  - the stale warning is `The MSGBOX() call changed since the composer opened; nothing was applied.`;
  - `Compose MSGBOX (visual)…` and `Compose MSGBOX…` are position-aware from the palette and the editor context menu.
- IntelliJ labels:
  - the MSGBOX dialog title is `Complete MSGBOX call` with OK `Apply`;
  - the Alt+Enter intention stays `Configure MSGBOX options…`;
  - each window dialog shows red error labels under its statement fields.

Verified in the tree during planning:
- `bbj-vscode/test/functional/installed-extension-e2e.test.ts`:
  - 993-1066 hold the describe `new decode payloads on the installed bundle`, gated by `describe.skipIf(!installPresent)`; it spawns `install!.serverPath` over IPC, sends `initialize`/`initialized`, and opens the cue fixture;
  - the CVS tests at 1052-1087 send literal line texts with `connection.sendRequest(…)` and a 30-second timeout, with the stderr message on the first expectation;
  - the skip-notice test follows at 1090.
- `QA/FULL-TEST-CHECKLIST.md`:
  - rows use `| N | Name | numbered steps joined by <br> | expected | [ ] |`;
  - the VS Code composer table ends with row 21 (MSGBOX compose-and-replace), followed by `---`;
  - the IntelliJ composer table ends with row 27 (MSGBOX compose-and-replace), followed by `---` and `## VS Code - Run Commands`;
  - both tables number independently, so VS Code already has no rows 22-23 but IntelliJ already has a row 22.
- Build and install, from plans 89-13 and 89-16:
  - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (at `/usr/local/bin/bbj-ext-install`), which writes `/tmp/bbj-lang.vsix` and installs it into `~/.ext-test`;
  - `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --offline --console=plain -q` produces `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`. Use `clean`, because Gradle once judged the zip up to date after a source change;
  - a Swing stack trace during `buildSearchableOptions` is headless noise when the exit code is 0.
- The folded todo is `.planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md`. Plan 86-04 closed a folded todo by moving it into `.planning/todos/completed/` in a docs commit.
- The workflow's human checks live in `<verify><human-check>` blocks and are harvested into UAT, as in plan 89-16.

Shell rules:
- Absolute paths only. Never chain `cd` with grep/find/cat/sed/head/tail; use `git -C /home/coder/repos/bbj-language-server` for git (including `git mv`).
- Run vitest as `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run <file> --root /home/coder/repos/bbj-language-server/bbj-vscode`; gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`.
- Before reinstalling, stop any competing Gradle or e2e run.
- Stage exact paths only.

Comment discipline: test comments, test names and assertion messages must not contain planning register ids — plan numbers such as 90-08, decision ids, threat ids, gap ids, or C-/CR-/WR-/IN- ids. GitHub issue numbers are fine. Planning files such as the todo may name plans.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- Two tests in the `new decode payloads on the installed bundle` describe:
  - `bbj/composer/msgbox/decodeCall on an unfinished call returns the incomplete outcome with the call span`;
  - `bbj/composer/addwindow/preview reports a malformed field as invalid and blank fields as valid`.
- `QA/FULL-TEST-CHECKLIST.md` VS Code rows:
  - 22 `MSGBOX unfinished call and wizard target safety`;
  - 23 `addWindow and addChildWindow field validation`.
- `QA/FULL-TEST-CHECKLIST.md` IntelliJ rows:
  - 28 `MSGBOX dialog completes an unfinished call with a debounced preview`;
  - 29 `addWindow and addChildWindow dialog field validation`;
  - 30 `Composer reopen across a language-server restart`.
- `.planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md`, with a `## Resolution` section.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the rebuilt, installed bundles carry the MSGBOX completion outcome and the addWindow validity verdict end to end</name>
  <files>bbj-vscode/test/functional/installed-extension-e2e.test.ts</files>
  <precondition>`bbj-ext-install` is on PATH, `/home/coder/.ext-test/extensions/extensions.json` exists, and the commits of plans 90-01 through 90-07 are on the current branch.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/installed-extension-e2e.test.ts lines 1-100 and 985-1100 — install discovery, `findPosition`, the decode-payload describe and the skip notice
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/msgbox-composer.ts — `decodeMsgboxCall`, as plan 90-01 left it
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/addwindow-composer.ts — `addwindowPreview`, as plan 90-02 left it
  </read_first>
  <action>
    In the describe `new decode payloads on the installed bundle`, add two tests after the existing CVS tests. Keep the 30-second timeout and the stderr message on each test's first expectation. Use literal line texts; do not change the fixture.

    - `bbj/composer/msgbox/decodeCall on an unfinished call returns the incomplete outcome with the call span`:
      - `{ line: 'x = MSGBOX(', character: 11 }` must return `found` true, `incomplete` true, `edit` `{ callStart: 4, callEnd: 11 }`, `initial.message` empty, `replace` undefined and `hasOptions` false;
      - `{ line: 'x = MSGBOX("Hi",', character: 16 }` must return `incomplete` true and `initial.message` `"Hi"`.
    - `bbj/composer/addwindow/preview reports a malformed field as invalid and blank fields as valid`:
      - the `<interfaces>` input with `x: '"10"'` must return `valid` false and `xError` `Not a number — remove the quotes: 10`;
      - the same input with `x: ''` must return `valid` true and statement `sysgui!.addWindow(0, 0, 0, 0, "", $00000000$)`.

    Then run, in order:
    1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`
    2. `bbj-ext-install`
    3. the e2e file
    4. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --offline --console=plain -q`
    5. `unzip -l` on the zip
    6. `sha256sum /tmp/bbj-lang.vsix /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`

    Record in the SUMMARY:
    - the installed extension version and `installedTimestamp`;
    - both sha256 digests;
    - the zip path;
    - the `language-server/main.cjs` entry timestamp from the listing.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/functional/installed-extension-e2e.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, a failed test, or every test in the file reported as skipped (the install was not refreshed)</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --offline --console=plain -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>unzip -l /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip</automated>
    <fails_when>non-zero exit, or no listed entry ends in "language-server/main.cjs"</fails_when>
  </verify>
  <acceptance_criteria>
    - Both new tests ran against a bundle reinstalled after this plan's build (the describe reports a non-zero pass count) and passed, together with the existing MSGBOX and CVS tests in that describe.
    - The SUMMARY records the extension version, `installedTimestamp`, both sha256 digests, the zip path and the `main.cjs` entry timestamp.
  </acceptance_criteria>
  <done>The artifacts a tester installs decode an unfinished MSGBOX call as completable and refuse a malformed addWindow field, and the IntelliJ zip carries the same freshly built server.</done>
</task>

<task type="auto">
  <name>Task 2: QA rows for every Phase 90 behaviour in both IDEs, the closed MSGBOX todo, and the end-of-phase human checks</name>
  <files>QA/FULL-TEST-CHECKLIST.md, .planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md, .planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md lines 30-90 — both composer tables, rows 17-27, and the row format
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md — the whole file
    - /home/coder/repos/bbj-language-server/.planning/phases/90-composer-robustness-intellij-composer-performance/90-VALIDATION.md — the manual-only verifications table
  </read_first>
  <action>
    Append VS Code row 22 to the VS Code composer table, directly after row 21 and before its `---`, in the existing format:
    - name `MSGBOX unfinished call and wizard target safety`;
    - steps:
      1. on a new line type `x = MSGBOX(` and click the `Compose MSGBOX` cue above it;
      2. type `"Saved"` as the message, choose the Information icon and press Insert;
      3. on a new line type `y = MSGBOX("Hi",` and run `Compose MSGBOX (visual)…` from the editor context menu; while the panel is open, type more characters at the end of that line, then press Insert;
      4. on a new line type `z = MSGBOX(` and run `Compose MSGBOX…` from the editor context menu;
    - expected:
      - Steps 1-2: the panel is titled `Complete MSGBOX call`, shows no compose-and-replace banner and no assign field, and the line becomes `x = MSGBOX("Saved", 64)`.
      - Step 3: the context menu opens the same completion panel with `"Hi"` prefilled; Insert after the line changed shows `The MSGBOX() call changed since the composer opened; nothing was applied.` and leaves the line unchanged.
      - Step 4: the completion panel opens instead of the QuickPick wizard, and nothing is inserted inside the call.

    Append VS Code row 23:
    - name `addWindow and addChildWindow field validation`;
    - steps:
      1. on a blank line run `Compose addWindow (visual)…`;
      2. type `"10"` into x, then `caption` into Title expr, then `getGui(` into SysGui expr, checking the panel after each;
      3. try Insert;
      4. clear every statement field and press Insert;
      5. repeat steps 1-3 in the addChildWindow composer, with `"101"` in ID;
    - expected:
      - Step 2: each malformed field gets a red border with its message underneath (`Not a number — remove the quotes: 10`, `Not a string — quote it as "caption"…`, `Unbalanced parentheses`), and Insert is disabled.
      - Step 3: nothing is inserted.
      - Step 4: every error clears, Insert is enabled, and the inserted statement uses the defaults `sysgui!.addWindow(0, 0, 0, 0, "", $00010003$)`.
      - Step 5: the addChildWindow composer behaves the same, with `Not a number — remove the quotes: 101` under ID.

    Append IntelliJ row 28 directly after row 27, before its `---`:
    - name `MSGBOX dialog completes an unfinished call with a debounced preview`;
    - steps:
      1. on a new line type `x = MSGBOX(` (the editor adds the closing parenthesis), press Alt+Enter and choose `Configure MSGBOX options…`;
      2. type `"Saved"` quickly into the message field while watching the generated statement and the OK button;
      3. choose the Information icon and apply;
      4. place the caret on the fixture's literal `36` MSGBOX call and open the dialog;
    - expected:
      - Step 1: a `Complete MSGBOX call` dialog opens with no banner and no assign field.
      - Step 2: the generated statement updates once shortly after typing stops, and OK stays disabled until it does.
      - Step 3: the line becomes `x = MSGBOX("Saved", 64)`, never a nested `MSGBOX(MSGBOX(`.
      - Step 4: `Configure MSGBOX` opens pre-filled exactly as before.

    Append IntelliJ row 29:
    - name `addWindow and addChildWindow dialog field validation`;
    - steps:
      1. open the addWindow composer dialog on a blank line from the editor context menu;
      2. quickly type `"10"` into x, then `caption` into Title expr, then `getGui(` into SysGui expr;
      3. clear every statement field;
      4. repeat in the addChildWindow dialog, with `"101"` in ID;
    - expected:
      - Step 2: after typing stops, each malformed field shows its message in a red label directly under it, OK stays disabled, and the preview updates once per pause, not per keystroke.
      - Step 3: the labels clear and OK is enabled.
      - Step 4: the addChildWindow dialog behaves the same, with `Not a number — remove the quotes: 101` under ID.

    Append IntelliJ row 30:
    - name `Composer reopen across a language-server restart`;
    - steps:
      1. open a `.bbj` file and wait for the BBj status-bar widget to read started;
      2. open the MSGBOX dialog, cancel it, and open it again;
      3. restart the language server from the status-bar widget and wait for started;
      4. open the MSGBOX dialog again;
    - expected:
      - Step 2: the second open is noticeably faster than the first.
      - Step 4: the dialog opens normally, with no error balloon and no request-failed notice.

    Change no other row or cell.

    Move the folded todo with `git -C /home/coder/repos/bbj-language-server mv .planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md .planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md`. Then append a `## Resolution` section saying it was fixed in Phase 90:
    - an unfinished MSGBOX call now decodes as a composable `incomplete` outcome;
    - every VS Code and IntelliJ entry point completes it in place through a guarded replace (plans 90-01 and 90-07);
    - QA rows VS Code 22 and IntelliJ 28 cover it.

    Also apply the deviation rule from `<interfaces>`.
  </action>
  <verify>
    <automated>grep -c "Complete MSGBOX call" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit, or a count below 2 (VS Code row 22 and IntelliJ row 28)</fails_when>
    <automated>grep -n "Composer reopen across a language-server restart" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit (IntelliJ row 30 is missing)</fails_when>
    <automated>test -f /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md</automated>
    <fails_when>non-zero exit</fails_when>
    <human-check>
      <test>Rebuild both distributables from the final tree, after any code-review fixes:
      - VS Code: `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install` (→ `/tmp/bbj-lang.vsix`);
      - IntelliJ: `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --console=plain -q` (→ `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`).
      Install the zip in IntelliJ and run QA rows 28, 29 and 30.</test>
      <expected>
        - An unfinished `x = MSGBOX(` opens `Complete MSGBOX call` with no banner and no assign field, and applying leaves one complete call.
        - In the MSGBOX, addWindow and addChildWindow dialogs a typing burst updates the preview once after typing stops, with OK disabled until then.
        - A malformed window field shows its message in a red label under the field and keeps OK disabled; clearing the fields enables OK.
        - The second composer open is noticeably faster than the first, and a composer still opens normally after Restart Language Server.
      </expected>
      <why_human>Swing timing, the modal dialog layout and a live LSP4IJ server restart all need a running IntelliJ, which this devcontainer cannot drive headlessly.</why_human>
    </human-check>
    <human-check>
      <test>Install the rebuilt `/tmp/bbj-lang.vsix` in VS Code and run QA rows 22 and 23.</test>
      <expected>
        - The cue and the context menu on an unfinished MSGBOX call open `Complete MSGBOX call`; Insert yields `x = MSGBOX("Saved", 64)`.
        - Insert after the line changed shows the "changed since the composer opened" warning and writes nothing.
        - `Compose MSGBOX…` on an unfinished call opens the completion panel instead of the QuickPick wizard.
        - Malformed addWindow and addChildWindow fields show their messages under the field with a red border, and Insert stays disabled until they are fixed.
      </expected>
      <why_human>The cue click, the context menu, the webview panel and a live edit made beside the non-modal panel all need a running VS Code.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md` shows only added lines: two rows in the VS Code composer table and three in the IntelliJ composer table.
    - The todo no longer exists under `pending/`, exists under `completed/`, and ends with a `## Resolution` section naming Phase 90.
    - `git -C /home/coder/repos/bbj-language-server diff HEAD --stat -- bbj-vscode/src bbj-intellij/src` prints nothing.
  </acceptance_criteria>
  <done>The manual checklist and the phase's UAT both carry every behaviour Phase 90 changed, in both IDEs, and the folded MSGBOX todo is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → phase closure claim | Unit tests alone cannot show that the installed artifacts carry the fixes |
| rebuilt artifact → tester install | A stale VSIX or zip would make UAT test the old behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-22 | Repudiation | a phase closed on source-tree tests while the installed bundle still ships the old decode or preview behaviour | medium | mitigate | installed-bundle e2e assertions over IPC against the reinstalled server; `clean buildPlugin`, the `main.cjs` listing and sha256 digests recorded in the SUMMARY |
| T-90-23 | Tampering | UAT run against artifacts built before code-review fixes | low | mitigate | the human checks require rebuilding both distributables from the final tree first |
| T-90-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. Before Task 1, record `BASE` = the output of `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`.
2. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, `bbj-ext-install`, then `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/functional/installed-extension-e2e.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode`
3. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --offline --console=plain -q`, then `unzip -l /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`
4. `git -C /home/coder/repos/bbj-language-server diff $BASE..HEAD --stat -- bbj-vscode/src bbj-intellij/src` prints nothing.
5. The two human checks are harvested into UAT and run against artifacts rebuilt from the final tree.
</verification>

<success_criteria>
- The reinstalled VS Code bundle serves the MSGBOX `incomplete` outcome and the addWindow `valid` verdict, and the IntelliJ zip carries the same freshly built server.
- QA rows exist for every Phase 90 behaviour in both IDEs, and the folded todo is closed.
- The manual-only checks from the validation strategy are staged as human checks against rebuilt artifacts.
</success_criteria>

<output>
Create `.planning/phases/90-composer-robustness-intellij-composer-performance/90-08-SUMMARY.md` when done. Record:
- the extension version and `installedTimestamp`;
- both sha256 digests and the zip `main.cjs` timestamp;
- the new QA row numbers and names;
- any deviation.
</output>
