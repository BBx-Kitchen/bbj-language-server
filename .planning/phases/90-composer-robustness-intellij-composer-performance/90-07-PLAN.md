---
phase: 90-composer-robustness-intellij-composer-performance
plan: 07
type: execute
wave: 3
depends_on: ["90-01", "90-03", "90-04", "90-06"]
autonomous: true
requirements: [DISC-08]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposeMode.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/MsgboxComposeModeTest.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'Alt+Enter, the editor context menu or a Code Vision cue on an unfinished MSGBOX call — `x = MSGBOX(`, `x = MSGBOX()` or `x = MSGBOX("Hi",` — opens a `Complete MSGBOX call` dialog instead of composing a new statement inside the call (D-04).'
    - 'In complete-the-call mode the typed message is prefilled, no compose-and-replace banner is shown and the assign-to row is hidden; OK reads `Apply` and stays disabled until a valid preview arrives (D-05).'
    - 'Apply replaces exactly the decoded call span through the one existing guarded MSGBOX write — `StaleEditGuard.applyIfUnchanged` with a re-issued decode request and the MSGBOX comparator — under the write-command name `Complete MSGBOX call`. Compose-new at the caret runs only when no MSGBOX call is under the caret (D-04).'
    - '`DecodeEquality.sameMsgbox` compares `incomplete`, so if the unfinished call grew or changed while the dialog was open the write aborts with the stale-document notice and never follows a moved call (D-02, D-03).'
    - 'Decodable, add-options and compose-and-replace MSGBOX calls still open `Configure MSGBOX` exactly as before, and the banner appears only in compose-and-replace mode.'
    - '`MsgboxComposeMode.of` is a plain-Java routing seam: null or not found gives COMPOSE_NEW; `incomplete` gives COMPLETE_CALL ahead of every other check; a present `replace` gives REPLACE_OPTIONS; anything else gives EDIT_IN_PLACE.'
  prohibitions:
    - 'MUST NOT write a completed MSGBOX call anywhere but the span the re-decode still confirms: no relocation, and no caret insert for a call that was found.'
    - 'No second MSGBOX write site: `openMsgbox` keeps exactly one guarded apply, one replace and one caret insert, and the whole-file counts in `ComposerApplyGuardSourceGuardTest` are unchanged.'
    - 'No IntelliJ-side banner prose: the dialog still renders only the server `replace.banner`.'
    - 'No change under `bbj-vscode/`.'
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposeMode.java"
      provides: "plain-Java routing from a MSGBOX decode to COMPOSE_NEW, EDIT_IN_PLACE, REPLACE_OPTIONS or COMPLETE_CALL"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java"
      provides: "the COMPLETE_CALL dialog mode"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      provides: "`openMsgbox` routed through `MsgboxComposeMode`, with one guarded replace for every replace mode"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java"
      provides: "`sameMsgbox` comparing `incomplete`"
  key_links:
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposeMode.java"
      via: "`openMsgbox` branches on `MsgboxComposeMode.of(decoded)`"
      pattern: "MsgboxComposeMode\\.of\\("
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java"
      via: "`sameMsgbox` compares `a.incomplete == b.incomplete`"
      pattern: "a\\.incomplete == b\\.incomplete"
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java"
      to: "bbj-vscode/src/msgbox-composer.ts"
      via: "`MsgboxDecodeResult.incomplete` mirrors the server decode outcome"
      pattern: "public boolean incomplete;"
---

<objective>
Complete an unfinished MSGBOX call in place in IntelliJ, instead of nesting a second call inside it (the folded MSGBOX todo, alongside DISC-08's re-resolve rule).

The language server's `incomplete` decode outcome comes from plan 90-01, against the wire contract below. This plan:
- adds the DTO field;
- adds a plain-Java `MsgboxComposeMode` routing seam;
- adds a COMPLETE_CALL dialog mode (no banner, no assign-to row);
- routes `openMsgbox` through the one existing guarded MSGBOX write;
- pins the field in `sameMsgbox`, at the Gson boundary and in the apply-guard source guard;
- updates the intention preview and description.

Purpose: Alt+Enter on `x = MSGBOX(` in IntelliJ produces one complete call, and a stale completion can never pass the guard.
Output: the mode seam and its test; the updated dialog, launcher, DTO and equality; their tests; intention wording.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-CONTEXT.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-RESEARCH.md
@.planning/phases/90-composer-robustness-intellij-composer-performance/90-01-SUMMARY.md
@.planning/todos/pending/2026-09-12-msgbox-compose-new-nests-inside-an-unfinished-msgbox-call.md

<interfaces>
Wire contract, shared verbatim with plan 90-01 (which produces it):

`MsgboxDecodeCallResult` gains `incomplete?: boolean`. A MSGBOX call with no message yet (`MSGBOX(`, `MSGBOX()`), or whose options slot is open but empty (`MSGBOX("Hi",`), decodes as found true, incomplete true, with `edit`, `initial`, `trailingArgs` and `hasOptions: false`, and never with `replace`:
- `{ line: 'x = MSGBOX(', character: 11 }` → `{"found":true,"incomplete":true,"edit":{"callStart":4,"callEnd":11},"trailingArgs":[],"initial":{"message":"","title":"","buttonSet":0,"icon":0,"defaultButton":0,"flags":[],"customButtons":[]},"hasOptions":false}`
- `{ line: 'x = MSGBOX()', character: 11 }` → the same, with `callEnd` 12
- `{ line: 'x = MSGBOX("Hi",', character: 16 }` → the same shape, `callEnd` 16, `message` `"Hi"`
- `{ line: 'x = MSGBOX("Hi", , "T")' }` → incomplete, `message` `"Hi"`, `title` `"T"`
- `r = MSGBOX("Hi")`, `r = MSGBOX("Hi", 36, "T")` and `r = MSGBOX("Hi", flags%, "T")` carry no `incomplete` key; their payloads are unchanged.

Labels: VS Code panel title and IntelliJ dialog title `Complete MSGBOX call`; IntelliJ OK text `Apply`; write-command name `Complete MSGBOX call`.

Verified in the tree during planning:
- `composer/ComposerModels.java:236-250` `MsgboxDecodeResult { boolean found; MsgboxEdit edit; List<String> trailingArgs; MsgboxPreviewInput initial; MsgboxReplace replace; Boolean hasOptions; }`.
- `composer/DecodeEquality.java:55-65` `sameMsgbox` compares found, edit, trailingArgs, initial, replace and hasOptions.
- `composer/ComposerLauncher.java`:
  - 180-188 the MSGBOX launch branch, whose `staleForCue` gate fires only when nothing is found;
  - 286-321 `openMsgbox`:
    - `boolean edit = decoded != null && decoded.found;`
    - the dialog is built as `new MsgboxComposerDialog(project, server, catalogs, decoded.initial, true, decoded.trailingArgs, decoded.replace)` or `(…, null, false, null, null)`;
    - the guarded replace uses `WriteCommandAction.runWriteCommandAction(project, "Configure MSGBOX", …)`, `applyIfUnchanged`, the re-issued decode request, the MSGBOX comparator and `replaceString(ls + ed.callStart, ls + ed.callEnd, text)`;
    - compose-new is `insertAtCaret(project, editor, text, "Compose MSGBOX")`;
  - 323 `private static StaleEditGuard.DocumentView documentViewOf(` follows `openMsgbox`;
  - 649-693 `openCvs` is the routing precedent.
  - A found incomplete decode passes `staleForCue`, so a cue click on an unfinished call opens COMPLETE_CALL with no launcher branch change.
- `composer/MsgboxComposerDialog.java`, as plan 90-04 left it:
  - constructor `(Project, BbjComposerServer, MsgboxCatalogs, @Nullable MsgboxPreviewInput initial, boolean editMode, @Nullable List<String> trailingArgs, @Nullable MsgboxReplace replace)`;
  - title `Configure MSGBOX` / `Compose MSGBOX` and OK `Apply` / `Insert`;
  - the banner is built inside `if (replace != null)`;
  - `assignToRow.setVisible(!editMode)`;
  - `refresh()` sets `input.assignTo = assignTo.getText()` and `input.editMode = editMode`;
  - the debounce wiring (`scheduleRefresh`, `previewDebouncer`) stays untouched.
- `composer/CvsComposeMode.java` and `CvsComposeModeTest.java` are the routing-seam precedent; `CvsComposerDialog.java:79-114` is the mode-constructor precedent.
- `composer/DecodeEqualityTest.java`:
  - 43-70 `baseMsgbox()` and 72-100 `copyOfMsgbox(src)`;
  - 233-262 `changingAnySingleComparedMsgboxFieldBreaksTheMatch` with its mutator list.
- `composer/ComposerModelsJsonBoundaryTest.java:168-212` holds the msgbox decodeCall envelopes.
- `composer/ComposerApplyGuardSourceGuardTest.java`:
  - comment-stripped counts: six `applyIfUnchanged(`, six `replaceString(`, one `insertString(`;
  - raw-text counts, comments included: one `DecodeEquality::sameMsgbox` and two `msgboxDecodeCall(`, so a Javadoc or comment writing either literal breaks them;
  - 156-176 `theCvsCompletionReusesTheSingleGuardedCvsReplacement` is the scoped-guard precedent.
- `composer/ComposerReplaceBannerSourceGuardTest.java`:
  - the dialog must declare a `MsgboxReplace replace` parameter;
  - `replace.banner` and `replace.originalOptions` must come after `if (replace != null)`;
  - `originalOptionsField.setEditable(false)`;
  - no `Messages.`;
  - the launcher contains exactly one `decoded.replace`.
- `composer/ConfigureMsgboxIntention.java:46-50` builds one `new IntentionPreviewInfo.Html(`; `intentionDescriptions/ConfigureMsgboxIntention/description.html` has a tooltip paragraph, `<!-- tooltip end -->` and two paragraphs mentioning #426. `IntentionDescriptionResourcesTest` and `ComposerIntentionPreviewSourceGuardTest` pin that structure.

Shell rules:
- Absolute paths only. Never chain `cd` with grep/find/cat/sed/head/tail; use `git -C /home/coder/repos/bbj-language-server` for git.
- Gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew … --offline`.
- Plain JUnit 5 only.
- Stage exact paths only.

Comment discipline: source comments, test comments, test names, assertion messages and description.html must not contain planning register ids — plan numbers such as 90-07, decision ids such as D-04, threat ids, gap ids, Pitfall numbers, or C-/CR-/WR-/IN- ids. GitHub issue numbers such as #426 and #648 are fine.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- `composer/MsgboxComposeMode` — plain-Java `enum` with `COMPOSE_NEW, EDIT_IN_PLACE, REPLACE_OPTIONS, COMPLETE_CALL` and `static MsgboxComposeMode of(MsgboxDecodeResult decoded)`. Test: `MsgboxComposeModeTest`.
- `ComposerModels.MsgboxDecodeResult.incomplete` (`boolean`).
- `MsgboxComposerDialog` constructor `(Project, BbjComposerServer, MsgboxCatalogs, @Nullable MsgboxPreviewInput initial, @NotNull MsgboxComposeMode mode, @Nullable List<String> trailingArgs, @Nullable MsgboxReplace replace)`, with title `Complete MSGBOX call` in COMPLETE_CALL.
- `ComposerLauncher.openMsgbox` routed through `MsgboxComposeMode.of`, with write-command name `Complete MSGBOX call` for a completion.
- `DecodeEquality.sameMsgbox` comparing `incomplete`.
- Tests:
  - `DecodeEqualityTest`: `twoIncompleteMsgboxDecodesOfTheSameUnfinishedCallMatch`, `anIncompleteMsgboxDecodeWhoseCallGrewDoesNotMatch`;
  - `ComposerModelsJsonBoundaryTest`: `anIncompleteMsgboxDecodeCallResponseParsesThroughTheLsp4jGson`;
  - `ComposerApplyGuardSourceGuardTest`: `theMsgboxCompletionReusesTheSingleGuardedMsgboxReplacement`.
- Reworded `ConfigureMsgboxIntention.generatePreview` and `description.html`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Alt+Enter on an unfinished `MSGBOX(` opens `Complete MSGBOX call` and replaces the call through the existing MSGBOX guard</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposeMode.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/MsgboxComposeModeTest.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java</files>
  <reversibility rating="costly">`MsgboxDecodeResult.incomplete` mirrors a cross-host wire field; renaming it later needs a lockstep change with the language server.</reversibility>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java lines 162-330 and 641-725 — `launchAt`, `openMsgbox`, `openCvs`, `insertAt`
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java — the whole file, as plan 90-04 left it
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java lines 213-252 — decodeCall DTOs
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposeMode.java — routing precedent
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/CvsComposeModeTest.java — test precedent
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerReplaceBannerSourceGuardTest.java — banner pins that must keep passing
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java lines 100-195 — counts that must keep passing
  </read_first>
  <behavior>
    - `MsgboxComposeMode.of(null)` is COMPOSE_NEW, and a result with `found` false is COMPOSE_NEW.
    - `found` true and `incomplete` true is COMPLETE_CALL, even when `replace` is also set (a decode the server never sends).
    - `found` true, `incomplete` false, `replace` present is REPLACE_OPTIONS.
    - `found` true, `incomplete` false, `replace` null is EDIT_IN_PLACE, for both a decodable options call and a bare add-options call (`hasOptions` false).
    - The `MsgboxComposeMode.java` source contains no `import com.intellij`.
  </behavior>
  <action>
    Write `MsgboxComposeModeTest` first: plain JUnit 5 mirroring `CvsComposeModeTest`, building results with `new ComposerModels.MsgboxDecodeResult()`.

    `ComposerModels.MsgboxDecodeResult`: add `public boolean incomplete;`. Javadoc: true when the call has no message yet or its options slot is open but empty; `edit`, `initial` and `trailingArgs` describe the call a composer replaces; `replace` is never present with it; `hasOptions` is false.

    Create `composer/MsgboxComposeMode.java`:
    - a plain-Java `public enum MsgboxComposeMode { COMPOSE_NEW, EDIT_IN_PLACE, REPLACE_OPTIONS, COMPLETE_CALL }` with `public static MsgboxComposeMode of(MsgboxDecodeResult decoded)`:
      - null or not found → COMPOSE_NEW;
      - else `incomplete` → COMPLETE_CALL;
      - else `replace != null` → REPLACE_OPTIONS;
      - otherwise → EDIT_IN_PLACE.
    - No IntelliJ import. Javadoc: it is designed against the MSGBOX outcomes, which have no not-editable verdict, and the incomplete check deliberately comes first.

    `MsgboxComposerDialog` (D-05):
    - Replace the `boolean editMode` constructor parameter with `@NotNull MsgboxComposeMode mode`, and throw `IllegalArgumentException` for COMPOSE_NEW combined with a non-null `initial`.
    - Derive the private flags `editMode` (EDIT_IN_PLACE or REPLACE_OPTIONS) and `completing` (COMPLETE_CALL).
    - Store `this.replace = completing ? null : replace`, so a completion never shows the banner.
    - Title: `Configure MSGBOX` when editing, `Complete MSGBOX call` when completing, `Compose MSGBOX` otherwise. OK text: `Apply` when editing or completing, `Insert` otherwise.
    - `assignToRow` is visible only when neither editing nor completing.
    - `refresh()` sets `input.editMode = editMode`, and `input.assignTo` to the field text only when neither editing nor completing, `null` otherwise. The server still validates the message as required.
    - Prefill from `initial` whenever it is non-null.
    - Keep the `if (replace != null)` banner block, the read-only original-options field and the whole debounce wiring unchanged.
    - Update the class Javadoc to name the modes.

    `ComposerLauncher.openMsgbox` (D-04):
    - After the null-catalogs check, compute `MsgboxComposeMode mode = MsgboxComposeMode.of(decoded)` and `boolean replacing = mode != MsgboxComposeMode.COMPOSE_NEW`.
    - Build the dialog with `decoded.initial`, `mode`, `decoded.trailingArgs` and `decoded.replace` when replacing (the only `decoded.replace` in the file), or with nulls and `mode` otherwise.
    - After OK with a non-empty statement:
      - every replace mode runs the ONE existing guarded block, unchanged except that its write-command name is `Complete MSGBOX call` for COMPLETE_CALL and `Configure MSGBOX` otherwise;
      - COMPOSE_NEW keeps `insertAtCaret(project, editor, text, "Compose MSGBOX")`.
    - Do not add a second guarded call or replace call.
    - Rewrite the `openMsgbox` Javadoc to cover the three replace modes. In it, and in any new comment, do not write the decode-request name followed by an opening parenthesis, or the fully qualified MSGBOX comparator method reference.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew compileJava compileTestJava --offline</automated>
    <fails_when>non-zero exit or any "error:" compiler line</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.MsgboxComposeModeTest' --tests 'com.basis.bbj.intellij.composer.ComposerReplaceBannerSourceGuardTest' --tests 'com.basis.bbj.intellij.composer.ComposerApplyGuardSourceGuardTest' --tests 'com.basis.bbj.intellij.composer.ComposerDialogRefreshSourceGuardTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "public boolean incomplete;" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` prints at least one line inside `MsgboxDecodeResult`.
    - `grep -n "MsgboxComposeMode.of(" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` prints exactly one line.
    - `grep -n "Complete MSGBOX call" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` prints at least one line per file.
    - The four test classes pass with their counts unchanged.
  </acceptance_criteria>
  <done>IntelliJ opens the MSGBOX composer in complete-the-call mode on a half-typed call and writes one complete call in place under the stale-edit guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the incomplete verdict across the Gson boundary, the stale-edit comparison and the single guarded MSGBOX write</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java lines 1-100 — `sameMsgbox`
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java lines 1-110 and 200-305 — MSGBOX fixtures and tests
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java lines 1-66 and 166-215 — the `parse` helper and msgbox decodeCall envelopes
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java — `openMsgbox`, as Task 1 left it
  </read_first>
  <behavior>
    - Flipping `incomplete` on the copy breaks the match in `changingAnySingleComparedMsgboxFieldBreaksTheMatch`; this is red before `sameMsgbox` compares the field.
    - Two independently built incomplete decodes of `x = MSGBOX(` compare equal. Each has found true, incomplete true, edit 4..11, message and title empty, zero options, empty flags, customButtons and trailingArgs, null replace and hasOptions false.
    - That decode does not equal the incomplete decode of `x = MSGBOX("Hi",` (edit 4..16, message `"Hi"`).
    - The first contract envelope parses through the LSP4J Gson with incomplete true, replace null, hasOptions false, edit 4..11, an empty message, and empty flags and customButtons lists.
    - The existing msgbox decodeCall envelope, which has no `incomplete` key, parses with incomplete false.
    - A new source-guard test works on the comment-stripped text of the `openMsgbox` body, from `private static void openMsgbox(` up to `private static StaleEditGuard.DocumentView documentViewOf(`. That body contains exactly one `applyIfUnchanged(`, one `replaceString(`, one `insertAtCaret(` and one `MsgboxComposeMode.of(`, and at least one `COMPLETE_CALL`.
    - The whole-file counts stay at six `applyIfUnchanged(`, six `replaceString(`, one `insertString(`, one MSGBOX comparator reference and two MSGBOX decode requests.
  </behavior>
  <action>
    Write the test changes first; the flip mutator must fail before the production change.

    `DecodeEqualityTest`:
    - `copyOfMsgbox` copies `incomplete`.
    - Add `d -> d.incomplete = !d.incomplete` to the MSGBOX mutator list.
    - Add a private fixture builder for an incomplete decode with parameters `callEnd` and `message`.
    - Add `twoIncompleteMsgboxDecodesOfTheSameUnfinishedCallMatch` and `anIncompleteMsgboxDecodeWhoseCallGrewDoesNotMatch`.

    `DecodeEquality.sameMsgbox`: add `a.incomplete == b.incomplete` to the field-wise comparison, and name `incomplete` in its Javadoc (D-02, D-03).

    `ComposerModelsJsonBoundaryTest`:
    - Add `anIncompleteMsgboxDecodeCallResponseParsesThroughTheLsp4jGson`, using the first contract envelope inside the standard `jsonrpc` result wrapper.
    - Add `assertFalse(result.incomplete)` to the existing msgbox decodeCall test.

    `ComposerApplyGuardSourceGuardTest`:
    - Keep every existing count.
    - Reword the MSGBOX comparator message so it says the one MSGBOX call site serves edit-in-place, compose-and-replace and completing an unfinished call.
    - Add `theMsgboxCompletionReusesTheSingleGuardedMsgboxReplacement`, as described in `<behavior>`, using the existing `withoutCommentLines` and `countOccurrences` helpers. Fail with a named message when either method marker is missing.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.DecodeEqualityTest' --tests 'com.basis.bbj.intellij.composer.ComposerModelsJsonBoundaryTest' --tests 'com.basis.bbj.intellij.composer.ComposerApplyGuardSourceGuardTest' --tests 'com.basis.bbj.intellij.composer.MsgboxComposeModeTest' --tests 'com.basis.bbj.intellij.composer.StaleEditGuardTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "a.incomplete == b.incomplete" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java` prints at least one line (`sameMsgbox`; `sameCvs` already has one).
    - `grep -n "theMsgboxCompletionReusesTheSingleGuardedMsgboxReplacement" /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java` prints one line.
    - `grep -n "assertEquals(6, applyIfUnchanged.size()" /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java` still prints one line.
    - The five test classes pass.
  </acceptance_criteria>
  <done>A stale MSGBOX completion can never get past the IntelliJ guard, and the launcher's single guarded MSGBOX write is pinned for every replace mode.</done>
</task>

<task type="auto">
  <name>Task 3: The MSGBOX intention preview and description tell the user an unfinished call is completed in place</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html — completion wording precedent
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/IntentionDescriptionResourcesTest.java lines 120-190 — resource assertions
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java lines 90-150 — preview assertions
  </read_first>
  <action>
    `ConfigureMsgboxIntention.generatePreview`:
    - Keep exactly one `new IntentionPreviewInfo.Html(`.
    - Its text says the composer opens for the `MSGBOX(...)` call under the caret, prefilled from its current arguments, and that on a call still being typed it completes that call in place.
    - `getText()` stays `Configure MSGBOX options…`, and `isAvailable` is unchanged.

    `description.html`:
    - Keep the structure and the `<!-- tooltip end -->` separator; the first paragraph stays the tooltip.
    - Add a paragraph: on a call that is still being typed — `MSGBOX(`, `MSGBOX()` with the editor's auto-inserted parenthesis, or `MSGBOX("Hi",` — the composer opens as `Complete MSGBOX call`. It is prefilled with what was already typed and has no assignment field. Applying replaces the unfinished call with one complete `MSGBOX(...)` call, and nothing is written if the line changed meanwhile.
    - Keep the language-server sentence and #426.

    Then run the whole IntelliJ suite.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.IntentionDescriptionResourcesTest' --tests 'com.basis.bbj.intellij.composer.ComposerIntentionPreviewSourceGuardTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline</automated>
    <fails_when>non-zero exit or "BUILD FAILED"</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-intellij/src | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|\bG-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>non-zero exit (a planning register id appears on an added line)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "Complete MSGBOX call" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html` prints at least one line.
    - `grep -c "tooltip end" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html` prints 1.
    - `grep -c "new IntentionPreviewInfo.Html(" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java` prints 1.
    - The whole IntelliJ suite passes, and the register scan exits 0.
  </acceptance_criteria>
  <done>The Alt+Enter preview and the intention description match what the MSGBOX composer now does on an unfinished call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language-server decode verdict → IntelliJ dialog mode | The server's flags decide whether a MSGBOX call is edited, completed or composed anew |
| modal dialog OK → document write | A guarded replace of the call span, or a caret insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-19 | Tampering | COMPLETE_CALL write in `ComposerLauncher.openMsgbox` after the unfinished call grew or changed | high | mitigate | The single MSGBOX `applyIfUnchanged` re-issues the decode request and compares with `sameMsgbox`, which now includes `incomplete` alongside the span and initial fields; the stamp is re-checked inside the write command; pinned by `DecodeEqualityTest` |
| T-90-20 | Tampering | the completion path writing through `insertAtCaret` or a second unguarded write site, nesting a call | high | mitigate | an `openMsgbox`-scoped source guard (one guarded call, one replace, one caret insert, one `MsgboxComposeMode.of`) plus the unchanged whole-file counts |
| T-90-21 | Tampering | plugin and server version skew in which the plugin ignores `incomplete` | low | accept | A found incomplete decode still carries `edit`, so an older plugin opens `Configure MSGBOX` and writes through the same guard, which fails closed on any change |
| T-90-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. Before Task 1, record `BASE` = the output of `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`.
2. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline`
3. Register scan: `git -C /home/coder/repos/bbj-language-server diff $BASE..HEAD -- bbj-intellij | grep '^+' | grep -nE '\b9[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-9[0-9]-[0-9]+\b|\bG-9[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'` prints nothing.
4. `git -C /home/coder/repos/bbj-language-server diff $BASE..HEAD --stat -- bbj-vscode` prints nothing.
5. The live dialog behaviour is staged as an end-of-phase human check by plan 90-08, against a rebuilt plugin zip.
</verification>

<success_criteria>
- Alt+Enter, the context menu and the Code Vision cue on an unfinished MSGBOX call open `Complete MSGBOX call` with no banner and no assign-to row, and applying replaces the partial call through the single guarded MSGBOX write (D-04, D-05).
- `sameMsgbox` compares `incomplete`, so a changed or grown call aborts the write (D-02, D-03).
- Existing MSGBOX edit, add-options and compose-and-replace flows are unchanged, and no guard count was loosened.
</success_criteria>

<output>
Create `.planning/phases/90-composer-robustness-intellij-composer-performance/90-07-SUMMARY.md` when done. Record:
- the dialog title and OK text per mode;
- the final guard counts, whole-file and `openMsgbox`-scoped;
- the exact intention preview sentence;
- the IntelliJ test count;
- any deviation.
</output>
