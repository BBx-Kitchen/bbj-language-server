---
phase: 61-language-core-review
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - .planning/reviews/61-COVERAGE.md
autonomous: true
requirements: [RVW-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-61-01` (grammar & lexing, 5 files / 1,340 LOC) has all 6 live dimensions recorded in `61-COVERAGE.md` — D1, D2, D3 at evidence tier `repro` and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-06, D-07)"
    - "All 5 files — `bbj.langium`, `java-types.langium`, `bbj-lexer.ts`, `bbj-token-builder.ts`, `bbj-value-converter.ts` — are named at least once inside the `## RU-61-01` section, so coverage is file-granular and not merely unit-granular (ROADMAP Phase 61 criterion 1)"
    - "The routing-table item this unit owns — the 3 disabled `test/parser.test.ts` assertions (DEBT-02) — is recorded as a `P61-D5-*` finding with full evidence and a `dedup:` field naming the owning DEBT requirement, so Phase 66 re-triages rather than re-derives (D-14)"
    - "Every recorded finding carries `file:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason rather than vanishing (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues in INVENTORY.md (RVW-07, ROADMAP Phase 61 criterion 4)"
    - "The unit's 2 `n/a` cells still carry INVENTORY's marker text verbatim and the phase-wide carried-forward `n/a` line count is still 38 — this plan reworded nothing (D-03)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): a finding's `location:` decides which unit owns it, not which unit discovered it. A defect found while reading grammar or lexer code whose `file:line` belongs to another unit goes into `### Cross-unit referrals` naming the target unit; it is never recorded here and never written into another unit's section. Every referral already addressed to `RU-61-01` by an earlier plan is either promoted to a finding here or dismissed here with a written reason"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): a dimension that produced zero findings records `pass` plus a written line naming the concrete checks applied, never a bare `pass` (D-06); a sub-block with nothing in it keeps the literal text `_(none recorded)_` rather than being deleted"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): this plan appends only inside `## RU-61-01`, in wave 2 behind `depends_on: [61-01]` so the shared-file order is structural rather than conventional (D-04), and it continues finding sequence numbers from the highest `P61-D{n}-{seq}` already present for each dimension rather than restarting at 001"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): owned by `RU-61-06` in plan 61-01; not re-derived here. Where a grammar or lexer defect is reachable only through peer-supplied Java type text, it is cross-referenced to that unit rather than re-assessed"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): surfaced and dismissed in plan 61-01 — a rounding/tie-breaking contract has no referent in a review sweep, and its one applicable remnant (numeric coercion of `interopPort`) belongs to `RU-61-06`"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-01 — Grammar & lexing` — 6 live cells recorded, 2 `n/a` cells untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-61-01` cells -> the phase-wide D-17 cell-total gate re-run by plan 61-07"
    - "`RU-61-01` D5 finding on the 3 disabled `parser.test.ts` assertions -> DEBT-02 in Phase 66"
    - "`RU-61-01` findings -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records; Phase 67 fixes and Phase 66 resolves DEBT"
    - "MUST NOT propose or record a grammar redesign to remove parser ambiguity — that is an explicit Out-of-Scope row, and v3.3 established that all 47 Chevrotain ambiguities resolve correctly"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` or inside `bbj-vscode/src/language/generated/` — the first is excluded by D-13, the second is machine-generated and out of scope; the grammar source is reviewed instead"
    - "MUST NOT read any file under `.planning/codebase/` — superseded by INVENTORY.md, and `CONCERNS.md` invites false findings (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — immutable for v4.0 (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-01`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-61-01` — Grammar & lexing** (`bbj.langium` 1,036, `java-types.langium` 68,
`bbj-lexer.ts` 37, `bbj-token-builder.ts` 182, `bbj-value-converter.ts` 17 — 5 files, 1,340 LOC,
INVENTORY risk rank 2 of 7) across its 6 live dimensions and record the result in the `## RU-61-01`
section of `.planning/reviews/61-COVERAGE.md`, using the shape plan 61-01 established and the developer
approved at its D-05 checkpoint.

Purpose: this is the widest blast radius in the phase — every other Phase 61 unit and every downstream
consumer in Phases 62-63 sits on top of this grammar/lexer pipeline.

Output: 6 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals this unit produces. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**. This plan writes into exactly one
existing artifact.

**Modified**
- `.planning/reviews/61-COVERAGE.md` — section `## RU-61-01 — Grammar & lexing` only. The file, its
  grid, its D-17 gate and all 38 carried-forward `n/a` lines were created by plan 61-01.

**Finding-ID ranges.** `P61-D{dimension}-{seq}`, zero-padded to 3 digits, allocated monotonically in
discovery order within each `(61, dimension)` pair **across the whole phase**. This plan continues from
the highest sequence already present for each dimension after plan 61-01; it does not restart at 001.
`P00-*` is never allocated.

**Plan → review-unit map.** This plan owns `RU-61-01`. The full 7-plan map, the routing-table ownership
map and the section shape live in `61-01-PLAN.md` §"Artifacts this phase produces".

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption | `must_haves.truths`, this plan |
| 2 | RVW-01 | empty | authored as flagged assumption | `must_haves.truths`, this plan |
| 3 | RVW-01 | ordering | authored as flagged assumption | `must_haves.truths`, this plan |
| 4 | SEC-06 | boundary | authored as flagged assumption | plan 61-01 (`RU-61-06` owns SEC-06) |
| 5 | SEC-06 | precision | flagged, dismissed with written reason | plan 61-01 |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-61-01 at evidence tier `repro` — D1, D2, D3</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists, contains a `## RU-61-01 — ` heading whose
  6 live cell lines are still unrecorded, and the D-05 format checkpoint in plan 61-01 was resolved.
  Halt and report if the file is absent or the section is already filled.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — the header, the stopping rule, the write contract, and the
      completed `## RU-61-06` section, which is the shape to copy; plus any entry in another unit's
      `### Cross-unit referrals` addressed to `RU-61-01`
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot" (the 15-issue dedup list), §"Phase 61 review units" (`RU-61-01` block), §"Test & Build
      Baseline" including the 3 disabled `parser.test.ts` assertions and §"Routing table (D-06)", and
      §"Pinned Baseline Range" for the `a7e1b53` do-not-re-report rule
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3 "what counts as a finding" wording; RVW-01,
      RVW-06, RVW-07; the Out of Scope table (grammar redesign)
    - **Review targets, read in full:** `bbj-vscode/src/language/bbj.langium` (1,036),
      `bbj-vscode/src/language/java-types.langium` (68), `bbj-vscode/src/language/bbj-lexer.ts` (37),
      `bbj-vscode/src/language/bbj-token-builder.ts` (182),
      `bbj-vscode/src/language/bbj-value-converter.ts` (17)
    - `CLAUDE.md` §Architecture — its Langium-pipeline description, including the claim that
      `bbj-lexer.ts` is a custom lexer with line-continuation handling via `prepareLineSplitter`
    - `bbj-vscode/langium-config.json` — read only to confirm which grammar files are entry points; no
      finding is located there (it belongs to `RU-64-02`)
  </read_first>
  <action>
Record the D1, D2 and D3 cell lines of `## RU-61-01 — Grammar & lexing` in
`.planning/reviews/61-COVERAGE.md`, replacing the placeholder verdict on each with `pass` or `fail`
followed by a written line naming the concrete checks applied. Append any resulting finding records
under that section's `### Findings`. Touch no other section.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted input,
secret exposure, integrity gaps, privilege/trust-boundary errors. The lexer's input is fully untrusted:
any `.bbj` or `.bbx` file a user opens, including a file inside a cloned repository. Concrete checks:
every terminal regular expression in `bbj.langium` and every pattern constructed in
`bbj-token-builder.ts` for catastrophic backtracking on a crafted line — nested quantifiers, alternation
over overlapping character classes, unanchored `.*` inside a repeated group; whether the
line-continuation splitter in `bbj-lexer.ts` can be driven to unbounded memory or unbounded token count
by a pathological input; whether any converter in `bbj-value-converter.ts` evaluates, unescapes or
interpolates input rather than converting it; and whether `java-types.langium` accepts type text that
originates from the java-interop peer (if so, record the connection as a cross-unit referral to
`RU-61-06`, not as a finding here).

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource leaks.
Concrete checks: the line-continuation splitter for off-by-one behavior at CRLF versus LF line endings,
at a continuation on the final line with no trailing newline, and on an empty or whitespace-only input;
`bbj-token-builder.ts` for errors caught and discarded rather than surfaced as a diagnostic, and for
token-order or longest-match assumptions that break on adjacent tokens; `bbj-value-converter.ts` for
null and undefined propagation on values the grammar permits but the converter does not expect. For
each claim, either produce a runnable reproduction — a `.bbj` fixture plus the exact `npx vitest run`
command that exhibits it — or a line-by-line trace naming the concrete input and the exact `file:line`
where behavior diverges. A bare assertion is not a finding.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks, missing
caches, redundant traversals and unbounded memory growth. This is the hottest path in the product: the
lexer runs on every document change. Concrete checks: whether regular expressions are compiled once or
rebuilt per call in `bbj-token-builder.ts`; whether the line splitter is linear or quadratic in line
count and in continuation-run length; and whether any grammar construct forces backtracking
proportional to file size.

For every claim that clears tier `repro`, write one record under this unit's `### Findings` using
INVENTORY's 13-field template verbatim, one field per line, inside a fenced block. `unit:` is
`RU-61-01`. `classification:` records pass/fail for all six Phase 60 D-13 classification tests. `dedup:` is checked against the
frozen 15 — issues #83, #90 and #381 are the plausible neighbours for this unit; a match records
`#NNN duplicate` or `#NNN partial-overlap` with what the finding adds, and no match records `none`. The
field is never blank. Any claim that does not clear tier `repro` goes under
`### Not-reproducible dispositions` with the tier it failed and why. Any defect whose `file:line`
belongs to another unit goes under `### Cross-unit referrals` naming that unit.

Do not propose a grammar redesign to remove parser ambiguity: it is an explicit Out-of-Scope row and
v3.3 established that all 47 Chevrotain ambiguities resolve correctly.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier cells are recorded: `awk '/^## RU-61-01 /,/^## RU-61-03 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[123] .* — (pass|fail) — '` prints `3`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `9` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `41`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - Finding IDs are unique and well-formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing, and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - Every finding still carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0` and `grep -cE '^location:[[:space:]]+bbj-vscode/src/language/generated/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-01 /,/^## RU-61-03 /" "$f" > /tmp/p61ru01.txt; test "$(grep -cE "^- D[123] .* — (pass|fail) — " /tmp/p61ru01.txt)" = 3; test "$(grep -cE " — (pass|fail) — " "$f")" = 9; test "$(grep -c " — pending" "$f")" = 41; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/src/language/generated/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_01_REPRO_OK'</automated>
  </verify>
  <done>`RU-61-01`'s D1, D2 and D3 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — regex backtracking and untrusted-input handling for D1, line-continuation
  edge cases and swallowed errors for D2, hot-path cost for D3. Every promoted claim is a complete
  13-field record with a dedup check against the frozen 15; every unpromoted claim is visible under
  `### Not-reproducible dispositions`.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-01 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`## RU-61-01`'s D1, D2 and D3 cells already carry verdicts — Task 1 committed. Halt
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` §`## RU-61-01` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`, so a missing
      test is `trace`-evidenced), §3c, §"Finding Record Template", §"Frozen Open-Issue Snapshot",
      §"Test & Build Baseline" — specifically the 3 disabled `test/parser.test.ts` assertions at
      approximately lines 530, 811 and 860 and the reason they are disabled — and §"Routing table (D-06)"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; DEBT-02
    - `.planning/STATE.md` §"Active Constraints" — the recorded reason the 3 assertions are disabled
    - **Review targets:** `bbj-vscode/src/language/bbj.langium`,
      `bbj-vscode/src/language/java-types.langium`, `bbj-vscode/src/language/bbj-lexer.ts`,
      `bbj-vscode/src/language/bbj-token-builder.ts`, `bbj-vscode/src/language/bbj-value-converter.ts`
    - **Test surface:** `bbj-vscode/test/parser.test.ts`,
      `bbj-vscode/test/functional/chevrotain-tokens.test.ts`,
      `bbj-vscode/test/example-files.test.ts` and the `bbj-vscode/test/test-data/` corpus
    - `CLAUDE.md` §Architecture and §Testing Pattern — D8 targets: the Langium-pipeline description, the
      `prepareLineSplitter` claim, the `npm run langium:generate` instruction, and the claim that every
      `.bbj` file in `test/test-data/` must produce zero lexer/parser errors
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-61-01` and append any resulting records. Touch no other
section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead code,
tangled coupling, inconsistent patterns and missing abstractions. Concrete checks: rule duplication
inside `bbj.langium` at 1,036 lines and overlap between it and `java-types.langium`; grammar rules with
no reachable reference; whether `bbj-value-converter.ts` at 17 lines and `bbj-lexer.ts` at 37 lines are
coherent modules or fragments of `bbj-token-builder.ts`; and inconsistent naming or structure between
sibling terminal definitions. The code shape is the evidence — name it and the `file:line`.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). This unit owns the
routing-table item **3 disabled `test/parser.test.ts` assertions** — commented-out expectations rather
than `test.skip` calls, which is why vitest's skipped total does not count them. Record it as a
`P61-D5-{seq}` finding with full evidence: the three approximate line anchors, what each assertion
asserted, and the stated blocker (they need a Java classpath unavailable under `EmptyFileSystem`).
Per D-14 this phase records and Phase 66 resolves, so the `dedup:` field names **DEBT-02** as the owning
requirement alongside the frozen-snapshot check result; the field is never blank. Then assess the rest
of the unit's coverage: which grammar constructs and which token-builder branches have no regression
test, whether `example-files.test.ts`'s corpus exercises line continuation and CRLF inputs, and whether
`chevrotain-tokens.test.ts` covers the token-builder paths D2 flagged. Do not record the `beforeAll`
hookTimeout flakiness here — its root cause is `WorkspaceManager.initializeWorkspace()` and it is owned
by `RU-61-05` in plan 61-06; if this unit's tests are among the suites it intermittently strikes, note
that under `### Cross-unit referrals` instead.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md or
docs-site claims contradicted by code. Concrete checks: every comment in the five target files against
what the code does; CLAUDE.md's Langium-pipeline description, its `prepareLineSplitter` claim about
`bbj-lexer.ts`, its `npm run langium:generate` instruction, and its statement that every `.bbj` file in
`test/test-data/` must produce zero lexer/parser errors — each verified against the code and the tree,
with the exact CLAUDE.md line recorded for any divergence.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 6 live cells now carry a verdict: `awk '/^## RU-61-01 /,/^## RU-61-03 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `6`
    - The 2 `n/a` cells are untouched: `awk '/^## RU-61-01 /,/^## RU-61-03 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[67] .*n/a — '` prints `2`
    - Every file in the unit is named inside its own section: `awk '/^## RU-61-01 /,/^## RU-61-03 /' .planning/reviews/61-COVERAGE.md > /tmp/ru01.txt; for f in bbj.langium java-types.langium bbj-lexer.ts bbj-token-builder.ts bbj-value-converter.ts; do grep -q "$f" /tmp/ru01.txt || echo "MISSING $f"; done` prints nothing
    - The routed DEBT-02 item is recorded as a D5 finding naming its owner: `/tmp/ru01.txt` contains `parser.test.ts` and `DEBT-02`, and `grep -cE '^id:[[:space:]]+P61-D5-[0-9]{3}' /tmp/ru01.txt` ≥ 1
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `12` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `38`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - All three sub-blocks are present for this unit: `/tmp/ru01.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-01 /,/^## RU-61-03 /" "$f" > /tmp/ru01.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/ru01.txt)" = 6; test "$(grep -cE "^- D[67] .*n/a — " /tmp/ru01.txt)" = 2; for x in bbj.langium java-types.langium bbj-lexer.ts bbj-token-builder.ts bbj-value-converter.ts parser.test.ts DEBT-02 "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/ru01.txt; done; test "$(grep -cE " — (pass|fail) — " "$f")" = 12; test "$(grep -c " — pending" "$f")" = 38; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_01_COMPLETE'</automated>
  </verify>
  <done>`RU-61-01` is complete under the stopping rule: all 6 live cells carry a verdict plus a written
  check line, all 5 files are named inside the section, the 3 disabled `parser.test.ts` assertions are
  recorded as a D5 finding cross-referencing DEBT-02, CLAUDE.md's pipeline claims are checked against the
  code, and every unpromoted claim is visible under `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any opened `.bbj`/`.bbx` file → lexer and parser | The complete untrusted-input surface of the language server: a file inside a cloned repository is parsed before any user action. |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed D1 finding becomes world-readable at commit time. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `61-COVERAGE.md` → the other six plans | One file, seven writers; a write outside this plan's own section destroys another unit's recorded coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P02-S1 | Denial of Service | terminal regexes in `bbj.langium` and patterns built in `bbj-token-builder.ts` | medium | transfer | Task 1's D1 check tests every terminal pattern for catastrophic backtracking on a crafted line, and the D3 check tests the line splitter for quadratic behavior. Recorded as `P61-D1-*`/`P61-D3-*` findings where evidence clears tier `repro`; remediation transfers to Phase 67. |
| T-61-P02-S2 | Tampering | `bbj-value-converter.ts` value conversion | low | transfer | Task 1's D1 check asks whether any converter evaluates, unescapes or interpolates input rather than converting it. |
| T-61-P02-01 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | high | mitigate | D-12 two-tier disclosure: the record names the surface, the problem class and the impact, states that the required runnable repro exists and what it establishes, and omits the trigger sequence, the payload and any proof-of-concept. |
| T-61-P02-02 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; every record writes all six tests pass/fail and the field-count criterion requires a `classification:` line on every finding. |
| T-61-P02-03 | Tampering | seven plans writing one `61-COVERAGE.md` | medium | mitigate | Wave 2 behind `depends_on: [61-01]` makes ordering structural (D-04); both tasks assert the exact global counts of recorded verdicts (9, then 12), remaining placeholders (41, then 38) and carried-forward `n/a` lines (38), so any write outside this section fails the verify gate. |
| T-61-P02-04 | Repudiation | a coverage claim with no evidence behind it | medium | mitigate | RVW-06 drop-vs-disposition plus D-06's written-check-line requirement; the acceptance criteria reject bare `pass` verdicts by line length and require file-granular naming of all 5 unit files. |
| T-61-P02-05 | Tampering | re-reporting an already-fixed defect | low | mitigate | Explicit prohibitions: `a7e1b53`'s cyclic-inheritance fix is not a live finding, `.planning/codebase/*.md` is unreadable for this phase, and no `location:` may resolve inside `src/language/generated/`. |
| T-61-P02-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and the already-installed `npx vitest run` harness. The package legitimacy gate is not applicable. |
</threat_model>

<verification>
- `## RU-61-01`'s 6 live cells each carry `pass` or `fail` plus a written check line ≥ 80 characters.
- Its 2 `n/a` cells are byte-identical to what plan 61-01 carried forward; the phase-wide `n/a` line
  count is still 38.
- All 5 unit files are named inside the section.
- The 3 disabled `parser.test.ts` assertions are recorded as a D5 finding naming DEBT-02.
- Phase-wide verdict count is 12 and placeholder count is 38, proving only this unit was written.
- Every finding has all 13 fields, a unique well-formed `P61-D{n}-{seq}` ID and a non-blank `dedup:`.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-01 advances: 2 of 7 units swept, 12 of 50 `applies` cells recorded.
- ROADMAP Phase 61 criteria 3 and 4 hold for every record written here.
- Phase 66 can re-triage DEBT-02 from this record without re-deriving it.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-02-SUMMARY.md` when done.
</output>
