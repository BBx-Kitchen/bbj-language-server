---
phase: 61-language-core-review
plan: 07
type: execute
wave: 7
depends_on: [61-06]
files_modified:
  - .planning/reviews/61-COVERAGE.md
autonomous: true
requirements: [RVW-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-61-07` (builtin catalogs, 8 files / 3,752 LOC — the largest unit by LOC in the entire inventory) has all 6 live unit-row dimensions recorded plus the 8 `applies` cells of its 4 `.bbl` file-exception rows (D2 and D4 each), each as `pass` or `fail` plus a written line naming the concrete checks applied (D-06, D-07)"
    - "The sweep method is recorded honestly per D-08: `RU-61-07` is swept **mechanically**, and each cell names the method used — a programmatic `.ts`-versus-`.bbl` diff for D4, and a stated sampling protocol for D2 with its sample size, the sampled indices and the source consulted — so no cell implies a line-by-line read of 3,752 lines that did not happen"
    - "All 8 files — `lib/events.ts`, `lib/functions.ts`, `lib/labels.ts`, `lib/variables.ts` and their four `.bbl` siblings — are named at least once inside the `## RU-61-07` section, so coverage is file-granular (ROADMAP Phase 61 criterion 1)"
    - "The phase closes with the D-17 hard countable gate satisfied: `61-COVERAGE.md` contains exactly 50 recorded `applies` verdicts, 38 verbatim `n/a` carry-forwards and 88 cell lines, and the totals are re-derived from INVENTORY.md at close rather than restated — a disagreement between the re-derivation and the stated totals is itself recorded as a defect (D-17)"
    - "The phase closes with every one of the 53 hand-written files under `bbj-vscode/src/language/` named somewhere in `61-COVERAGE.md`, enumerated from the tree rather than from a list typed into a plan (ROADMAP Phase 61 criterion 1)"
    - "Every recorded finding carries `file:line`, a primary dimension, evidence clearing its tier, and a non-blank `dedup:` checked against the 15 frozen open issues (RVW-06, RVW-07)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): translated into a concrete D2/D4 check for this unit — each `.ts` catalog and its `.bbl` sibling are near-identical content in two formats (734 vs 732, 995 vs 993, 67 vs 61, 86 vs 84 lines), so the question is exactly what the probe asks: where the two are equal do they merge, collide, or stay separate, and is any divergence intentional or drift? The mechanical diff answers it with evidence rather than by eye. At the unit boundary, `lib/fs-provider.ts` serves these catalogs but belongs to `RU-61-06`, so a defect in the serving path is a referral, not a finding here"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — what does a catalog entry with an empty description, a zero-argument signature, or a duplicated name produce downstream? Recording-wise, a dimension with zero findings records `pass` plus a written line naming the concrete checks applied and the sampling protocol used (D-06); a `.bbl` row's 6 `n/a` cells stay verbatim and are not converted to passes"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — is catalog entry order meaningful to any consumer, and would two equal-named entries resolve deterministically? Recording-wise, this plan appends only inside `## RU-61-07`, in wave 7 behind `depends_on: [61-06]` (D-04), continuing finding sequence numbers from the highest already present per dimension, and it is the last writer, so it also closes the D-17 count gate"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): owned by `RU-61-06` in plan 61-01; not re-derived here. These catalogs are static repository content and do not cross the peer boundary"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): surfaced and dismissed in plan 61-01 — no rounding/tie-breaking contract exists in a review sweep; its applicable remnant belongs to `RU-61-06` and `RU-61-05`"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-07 — Builtin catalogs` — 6 live unit-row cells and 8 file-exception cells recorded, 26 `n/a` cells untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"
    - ".planning/reviews/61-COVERAGE.md §`## D-17 Cell-Total Gate` — closing re-derivation appended: the command, its output, the stated totals, and an explicit agree/disagree verdict"
    - ".planning/reviews/61-COVERAGE.md §`## Phase 61 Close-Out` — the 53-file coverage enumeration result and the phase-wide finding count by dimension"

  key_links:
    - "`61-COVERAGE.md` (complete) -> Phase 68 DOC-03, which concatenates the five `{NN}-COVERAGE.md` files against INVENTORY.md's grid"
    - "`61-COVERAGE.md` finding dispositions -> Phase 68's EASY-FIXES / MAJOR-REFACTORS assembly, which does not re-triage"
    - "`61-COVERAGE.md` findings -> Phase 67's apply path and Phase 69's issue filing, gated on ISSUE-01"
    - "`RU-61-07` D4 duplication verdict -> the `.ts`/`.bbl` dual-format decision Phase 67 or a future phase would have to change"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records; Phase 67 fixes and Phase 66 resolves DEBT"
    - "MUST NOT claim a line-by-line read of the catalogs — D-08 licenses a mechanical sweep for this unit only on condition that each cell names the method actually used"
    - "MUST NOT convert any `.bbl` row's `n/a` cell into a verdict — only D2 and D4 apply to those rows, and the other 6 per row stay verbatim (INVENTORY file-exception rows)"
    - "MUST NOT adopt the stated 50/38/88 totals if the closing re-derivation disagrees with them — the disagreement is itself a defect to surface (D-17)"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` or inside `bbj-vscode/src/language/generated/`"
    - "MUST NOT read any file under `.planning/codebase/` — superseded by INVENTORY.md (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — immutable for v4.0 (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-07`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-61-07` — Builtin catalogs** (`lib/events.ts` 734, `lib/functions.ts` 995,
`lib/labels.ts` 67, `lib/variables.ts` 86, `lib/events.bbl` 732, `lib/functions.bbl` 993,
`lib/labels.bbl` 61, `lib/variables.bbl` 84 — 8 files, 3,752 LOC, INVENTORY risk rank 7 of 7) and then
**close the phase**.

Two jobs. First, the unit: 6 live unit-row cells plus the 8 `applies` cells of the four `.bbl`
file-exception rows, swept **mechanically** per D-08 — these are static data catalogs with no dynamic
behavior, and a claimed line-by-line read of 3,752 lines would be a false coverage claim. The method is
recorded in each cell: a programmatic `.ts`-versus-`.bbl` diff for the D4 duplication question, and a
stated sampling protocol for D2 value correctness.

Second, the close-out: this plan is the last writer, so it re-runs the D-17 cell-total gate against
INVENTORY, enumerates the 53 review-target files from the tree and confirms each is named in the
coverage file, and records the phase-wide finding count by dimension.

Output: 14 recorded cells, the closing D-17 gate, a `## Phase 61 Close-Out` section, and a complete
`61-COVERAGE.md` with 50 verdicts, 38 verbatim `n/a` carry-forwards and 0 placeholders. No source file
changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**.

**Modified**
- `.planning/reviews/61-COVERAGE.md` — section `## RU-61-07 — Builtin catalogs`, the closing
  re-derivation appended to `## D-17 Cell-Total Gate`, and a new `## Phase 61 Close-Out` section at the
  end of the file.

**Finding-ID ranges.** `P61-D{dimension}-{seq}`, zero-padded to 3 digits, allocated monotonically in
discovery order within each `(61, dimension)` pair **across the whole phase**. This plan continues from
the highest sequence already present for each dimension after plans 61-01 through 61-06 and closes the
allocation. `P00-*` is never allocated.

**Plan → review-unit map.** This plan owns `RU-61-07` and the phase close-out. The full 7-plan map, the
routing-table ownership map and the section shape live in `61-01-PLAN.md` §"Artifacts this phase
produces".

**Sampling protocol for D2 (planner's discretion under D-08), binding on Task 1.** For each of the four
catalogs: sample **20 entries** — the first 5 in file order, the last 5, and 10 at evenly spaced indices
computed as `floor(i * N / 11)` for `i` in 1..10 where `N` is the entry count of that catalog. Check each
sampled entry against a named source, in this precedence order: (1) `bbj-vscode/VERBs.md` where it
carries a status or signature claim for that verb or function; (2) the `.bbl` sibling, as the
cross-format consistency check; (3) `bbj-vscode/src/language/bbj.langium` terminal and keyword lists for
symbolic-label and reserved-variable names. Record in the cell: the sample size, the sampled indices,
the source consulted per catalog, and either "no mismatch" or each mismatch named by catalog and entry.
No live BASIS documentation source is assumed available; if none of the three sources can adjudicate an
entry, that entry is recorded as unadjudicated rather than silently passed.

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption, translated into the `.ts`-vs-`.bbl` equality question | `must_haves.truths`, this plan |
| 2 | RVW-01 | empty | authored as flagged assumption, translated into an empty/duplicate-entry check | `must_haves.truths`, this plan |
| 3 | RVW-01 | ordering | authored as flagged assumption, translated into a catalog-order check | `must_haves.truths`, this plan |
| 4 | SEC-06 | boundary | authored as flagged assumption | plan 61-01 (`RU-61-06` owns SEC-06) |
| 5 | SEC-06 | precision | flagged, dismissed with written reason | plan 61-01 |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-61-07 at evidence tier `repro` — D1, D2, D3 plus the four `.bbl` D2 cells</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists, contains a `## RU-61-07 — ` heading with 6
  unrecorded live unit-row cells and 8 unrecorded file-exception cells, and plan 61-06 has committed
  (phase-wide verdict count is 36). Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — the header, the stopping rule, the write contract, the
      completed `## RU-61-06` section as the shape to copy, the `### File-exception cells` block in this
      unit's own section, and any `### Cross-unit referrals` entry addressed to `RU-61-07`
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Phase 61 review units" (`RU-61-07` block and its D4 duplication callout),
      §"What counts as an in-scope file" — the row restricting `.bbl` catalogs to D2 and D4 —
      §"File-exception rows" and §"Exclusion reasons" R-BBL-STATIC and R-BBL-NODOC
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3 wording; RVW-01, RVW-06, RVW-07
    - **Review targets, read structurally rather than line by line (D-08):**
      `bbj-vscode/src/language/lib/events.ts`, `lib/functions.ts`, `lib/labels.ts`, `lib/variables.ts`
      and their four `.bbl` siblings — read each file's head and tail to establish the wrapper and the
      entry format, then work by sampling and by diff
    - **Consumers, read to establish what a bad catalog value would do — no finding is located here:**
      `bbj-vscode/src/language/lib/fs-provider.ts` (`RU-61-06`), and whatever
      `grep -rn "builtinEvents\|builtinFunctions\|builtinSymbolicLabels\|builtinVariables" bbj-vscode/src`
      returns
    - `bbj-vscode/VERBs.md` — the BBj verb implementation-status table, the primary adjudication source
      for the D2 sampling protocol
  </read_first>
  <action>
Record the D1, D2 and D3 unit-row cell lines of `## RU-61-07 — Builtin catalogs`, plus the D2 cell of
each of the four `.bbl` file-exception rows — 7 cells in total for this task. Replace the placeholder
verdict on each with `pass` or `fail` plus a written line naming the concrete checks applied **and the
method used** (D-08). Append any resulting finding records under this section's `### Findings`. Touch no
other section, and do not touch any `.bbl` row cell other than D2.

*D1 Security, unit row* (tier `repro`). Assess against injection, untrusted input, secret exposure,
integrity gaps and privilege/trust-boundary errors. These are static repository-controlled data files
exported as template literals from `.ts` modules. Concrete checks: whether any catalog string can break
out of its template literal — an unescaped backtick or a `${` sequence inside the content would be a
real integrity defect, and it is mechanically checkable with a grep over the four `.ts` files rather
than by reading them; whether any entry embeds a URL, path or command that a consumer would act on; and
whether the content is repository-controlled end to end or can be regenerated from an external source.
State the method: mechanical grep plus structural read.

*D2 Correctness & error handling, unit row and the four `.bbl` rows* (tier `repro`). This is the value
correctness question, and it is answered by the sampling protocol fixed in §"Artifacts this phase
produces" — 20 entries per catalog, sampled at stated indices, adjudicated against `VERBs.md`, the
`.bbl` sibling, and `bbj.langium`'s terminal and keyword lists in that precedence order. Run it for each
of the four catalogs and record, in each cell, the sample size, the sampled indices, the source
consulted, and either "no mismatch" or each mismatch named by catalog and entry. Also check, across the
whole catalog rather than the sample because these are mechanically checkable: duplicate entry names,
entries with an empty description, and malformed entries that do not match the format the head of the
file establishes. Write the unit-row D2 cell for the four `.ts` files, and write one D2 cell per `.bbl`
row — the `.bbl` files' D2 verdict is the same sampling result viewed from the data side, so each `.bbl`
cell states its own file's sample outcome rather than pointing at the unit row.

*D3 Performance & resource use, unit row* (tier `repro`). Assess against hot-path cost and unbounded
memory growth. Concrete checks: these catalogs are module-level template literals totalling roughly
1,880 lines of `.ts` string content, so they are loaded into memory at import time for the life of the
process — record whether they are parsed once and cached or re-parsed per consumer call, and whether any
consumer scans a catalog linearly on a per-keystroke path. Trace it through the consumers found in
`read_first`. Where the cost is in a consumer rather than in the catalog, record a cross-unit referral
to that consumer's unit instead of a finding here.

For every claim that clears tier `repro`, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block. `unit:` is `RU-61-07`.
`classification:` records pass/fail for all six Phase 60 D-13 classification tests. `dedup:` is checked against all 15 frozen
issues — this unit has no strong neighbour among them, so `none` is the expected result and must still
be written explicitly; #65 (tokenized BBj files) and #475 (SETOPTS decode hovers) are the only two worth
checking for partial overlap. The field is never blank.

Any claim that does not clear tier `repro` goes under `### Not-reproducible dispositions` with the tier
it failed and why — including any sampled entry that none of the three sources could adjudicate, which
is recorded as unadjudicated rather than silently passed.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier unit-row cells are recorded: `awk '/^## RU-61-07 /,0' .planning/reviews/61-COVERAGE.md > /tmp/ru07.txt; grep -cE '^- D[123] .* — (pass|fail) — ' /tmp/ru07.txt` prints `3`
    - All four `.bbl` D2 cells are recorded: `grep -cE '^- \[file-exception\] .*· D2 — (pass|fail) — ' /tmp/ru07.txt` prints `4`
    - No `.bbl` `n/a` cell was converted: `grep -cE '^- \[file-exception\] .*n/a — ' /tmp/ru07.txt` prints `24`
    - The sampling protocol is recorded, not just asserted: the D2 cell text or an adjacent block states a sample size and the sources consulted — `grep -c 'VERBs.md' /tmp/ru07.txt` ≥ 1 and `grep -cE '20|sample' /tmp/ru07.txt` ≥ 1
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `43` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `7`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Every `pass` line is a written line, not a bare verdict: `awk '/ — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-07 /,0" "$f" > /tmp/ru07.txt; test "$(grep -cE "^- D[123] .* — (pass|fail) — " /tmp/ru07.txt)" = 3; test "$(grep -cE "^- \[file-exception\] .*· D2 — (pass|fail) — " /tmp/ru07.txt)" = 4; test "$(grep -cE "^- \[file-exception\] .*n/a — " /tmp/ru07.txt)" = 24; grep -q "VERBs.md" /tmp/ru07.txt; test "$(grep -cE " — (pass|fail) — " "$f")" = 43; test "$(grep -c " — pending" "$f")" = 7; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_07_REPRO_OK'</automated>
  </verify>
  <done>`RU-61-07`'s D1, D2 and D3 unit-row cells and all four `.bbl` D2 cells carry `pass` or `fail` plus
  a written line that names both the checks applied and the method used — mechanical grep for D1, the
  20-entry stratified sampling protocol with its indices and sources for D2, and a consumer trace for D3.
  No `.bbl` `n/a` cell was converted, and any unadjudicated sample entry is visible under
  `### Not-reproducible dispositions`.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-07 at tier `trace` (D4, D5, D8 plus the four `.bbl` D4 cells) and close the phase</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`## RU-61-07`'s D1, D2 and D3 unit-row cells and the four `.bbl` D2 cells already carry
  verdicts — Task 1 committed (phase-wide verdict count is 43). Halt otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — this unit's section, the `## D-17 Cell-Total Gate` section
      written by plan 61-01, and the whole file's structure, since this task appends the close-out
    - `.planning/reviews/INVENTORY.md` §3b, §3c, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Phase 61 review units" (`RU-61-07`'s D4 duplication callout naming the four
      near-duplicate pairs and their line counts), §"Applicability Grid" and §"File-exception rows" —
      the rows the closing gate re-derives from
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-01
    - `.planning/ROADMAP.md` §"Phase 61: Language Core Review" — the four success criteria the close-out
      is measured against
    - **Review targets:** the four `.ts` catalogs and their four `.bbl` siblings
    - `bbj-vscode/VERBs.md` — relevant to D5 and D8 claims about catalog completeness and status
  </read_first>
  <action>
Two jobs: finish the unit, then close the phase.

**A. Finish `RU-61-07`.** Record the D4, D5 and D8 unit-row cells plus the D4 cell of each of the four
`.bbl` rows — 7 cells. Touch no other unit's section, and do not touch any `.bbl` cell other than D4.

*D4 Maintainability & code smells, unit row and the four `.bbl` rows* (tier `trace`). This is
INVENTORY's flagged duplication callout, and per D-08 it is answered by a **programmatic diff**, not by
eye. For each of the four pairs, run a diff of the `.ts` file with its first and last lines removed
against its `.bbl` sibling — the `.ts` files wrap the `.bbl` content in an exported template literal, so
stripping the wrapper lines is what makes the two comparable — and record the exact number of differing
lines for each pair. Then judge: content that is byte-identical after the wrapper is stripped is
maintained-in-two-places duplication and is a D4 finding with a named remediation shape (generate one
from the other, or import the `.bbl` at build time); content that diverges is worse, because it means
the two formats have drifted, and each divergence is named. Record the four diff results in the unit-row
D4 cell and the per-file result in each `.bbl` row's D4 cell. Beyond duplication: whether the four `.ts`
modules follow one consistent export shape, and whether any is dead — check consumption with a grep over
`bbj-vscode/src`.

*D5 Test coverage gaps, unit row* (tier `inherited`; a missing test is `trace`-evidenced). Concrete
checks: whether any test asserts catalog content, entry count, or the `.ts`-versus-`.bbl` equivalence
this task just measured; whether the `bbjlib` virtual-filesystem path that serves these catalogs is
covered anywhere; and whether a regression that silently emptied or truncated a catalog would be caught
by the existing suite. Record gaps as `P61-D5-{seq}` findings naming the missing assertion. The four
`.bbl` rows are `n/a` for D5 and stay verbatim.

*D8 Comment & doc accuracy, unit row* (tier `trace`). Concrete checks: the comments inside the `.ts`
wrappers and any header documentation against the content they wrap; and `bbj-vscode/VERBs.md`'s
implementation-status claims against what the catalogs actually contain — a verb VERBs.md marks
implemented that has no catalog entry, or vice versa, is a D8 finding with the exact VERBs.md line in
`location:`. The four `.bbl` rows are `n/a` for D8 under R-BBL-NODOC and stay verbatim.

**B. Close the phase.** Append two things.

*Closing D-17 gate.* Append to the existing `## D-17 Cell-Total Gate` section a closing sub-block that
(1) re-runs the same awk re-derivation over `.planning/reviews/INVENTORY.md` that plan 61-01 recorded,
and records the command and its literal output; (2) counts what `61-COVERAGE.md` itself now contains —
recorded verdicts, verbatim `n/a` lines, and total cell lines — and records those three numbers; and
(3) states an explicit agree-or-disagree verdict against the stated totals of 50 `applies`, 38 `n/a` and
88 cells. If the numbers disagree, do not adopt the stated totals: record the disagreement as a defect,
state which of the three sources disagrees, and stop rather than reconciling silently (D-17).

*`## Phase 61 Close-Out`.* A new final section recording: the 53-file coverage enumeration — enumerate
the hand-written files from the tree with `ls` over `bbj-vscode/src/language/*.ts`,
`bbj-vscode/src/language/*.langium`, `bbj-vscode/src/language/validations/*.ts` and
`bbj-vscode/src/language/lib/*`, confirm the count is 53, and confirm each basename appears somewhere in
`61-COVERAGE.md`, listing any that do not; the phase-wide finding count broken down by dimension and by
disposition; the count of not-reproducible dispositions and of cross-unit referrals, with confirmation
that every referral was resolved by its owning unit; and a one-line statement of the four ROADMAP Phase
61 success criteria with the evidence location for each. State that Phase 61 files no GitHub issue —
ISSUE-01 is a Phase 69 gate — and that `.planning/reviews/INVENTORY.md` was not edited.
  </action>
  <acceptance_criteria>
    - All 6 live unit-row cells carry a verdict: `awk '/^## RU-61-07 /,0' .planning/reviews/61-COVERAGE.md > /tmp/ru07.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/ru07.txt` prints `6`
    - All 8 file-exception `applies` cells carry a verdict and the 24 `n/a` cells are untouched: `grep -cE '^- \[file-exception\] .*· D[24] — (pass|fail) — ' /tmp/ru07.txt` prints `8` and `grep -cE '^- \[file-exception\] .*n/a — ' /tmp/ru07.txt` prints `24`
    - Every file in the unit is named inside its own section: `for f in events.ts functions.ts labels.ts variables.ts events.bbl functions.bbl labels.bbl variables.bbl; do grep -q "$f" /tmp/ru07.txt || echo "MISSING $f"; done` prints nothing
    - The four diffs were actually run and their results recorded: the D4 cell text records a differing-line count for each pair, and re-running them reproduces those counts — `for p in events functions labels variables; do diff <(sed -e '1d' -e '$d' bbj-vscode/src/language/lib/$p.ts) bbj-vscode/src/language/lib/$p.bbl | wc -l; done` prints four numbers, each of which appears in `/tmp/ru07.txt`
    - **Phase D-17 gate — the hard countable close:** `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `50`, `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`, `grep -cE '^- (D[1-8]|\[file-exception\])' .planning/reviews/61-COVERAGE.md` prints `88`, and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `0`
    - The gate was re-derived, not restated: `awk '/^\| \`RU-61-0[1-7]\` \|/ || /^\| \`lib\/[a-z]+\.bbl\` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md` prints `50 38 88`, and that output plus an explicit agree/disagree verdict appears in the `## D-17 Cell-Total Gate` section
    - **All 53 review-target files are named in the coverage file:** `ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium bbj-vscode/src/language/validations/*.ts bbj-vscode/src/language/lib/* | xargs -n1 basename | wc -l` prints `53`, and `for f in $(ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium bbj-vscode/src/language/validations/*.ts bbj-vscode/src/language/lib/* | xargs -n1 basename); do grep -q "$f" .planning/reviews/61-COVERAGE.md || echo "MISSING $f"; done` prints nothing
    - The close-out section exists and records the four success criteria: `grep -c '## Phase 61 Close-Out' .planning/reviews/61-COVERAGE.md` prints `1`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - Finding IDs unique, phase-scoped, no blank dedup, no excluded location: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing; `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`; `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`; `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-07 /,0" "$f" > /tmp/ru07.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/ru07.txt)" = 6; test "$(grep -cE "^- \[file-exception\] .*· D[24] — (pass|fail) — " /tmp/ru07.txt)" = 8; test "$(grep -cE "^- \[file-exception\] .*n/a — " /tmp/ru07.txt)" = 24; test "$(grep -cE " — (pass|fail) — " "$f")" = 50; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test "$(grep -cE "^- (D[1-8]|\[file-exception\])" "$f")" = 88; test "$(grep -c " — pending" "$f")" = 0; test "$(awk "/^\\| .RU-61-0[1-7]. \\|/ || /^\\| .lib\\/[a-z]+\\.bbl. \\|/ {a+=gsub(/applies/,\"applies\"); n+=gsub(/n\\/a/,\"n\\/a\")} END{print a, n, a+n}" .planning/reviews/INVENTORY.md)" = "50 38 88"; test "$(ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium bbj-vscode/src/language/validations/*.ts bbj-vscode/src/language/lib/* | xargs -n1 basename | wc -l)" = 53; for x in $(ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium bbj-vscode/src/language/validations/*.ts bbj-vscode/src/language/lib/* | xargs -n1 basename); do grep -q "$x" "$f"; done; grep -q "## Phase 61 Close-Out" "$f"; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo PHASE61_COVERAGE_COMPLETE'</automated>
  </verify>
  <done>`RU-61-07` is complete and the phase is closed: 50 recorded verdicts, 38 verbatim `n/a`
  carry-forwards, 88 cell lines and 0 placeholders in `61-COVERAGE.md`; the D-17 gate re-derived from
  INVENTORY and explicitly agreeing with the stated totals; all 53 hand-written files under
  `bbj-vscode/src/language/` named in the coverage file, enumerated from the tree rather than from a
  list; every cross-unit referral resolved by its owning unit; and a `## Phase 61 Close-Out` section
  recording the finding counts by dimension and disposition and the evidence location for each of the
  four ROADMAP success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog string content → the exported template literal in each `.ts` module | An unescaped backtick or interpolation sequence inside catalog content would break out of the literal — a repository-controlled but mechanically checkable integrity surface. |
| catalog content → completion, hover and the `bbjlib` virtual filesystem | What these files contain is presented to the user as authoritative BBj builtin documentation. |
| `61-COVERAGE.md` → Phase 68's DOC-03 concatenation | This plan is the last writer; the counts it closes on are what DOC-03 consumes as the phase's coverage claim. |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| shared `61-COVERAGE.md` → the six earlier plans | One file, seven writers; this plan must not disturb six already-recorded sections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P07-S1 | Tampering | template-literal escaping in the four `.ts` catalogs | low | transfer | Task 1's D1 check greps the four `.ts` files for an unescaped backtick or `${` sequence inside the content rather than reading 1,880 lines by eye; recorded as a `P61-D1-*` finding if present. |
| T-61-P07-S2 | Tampering | drift between each `.ts` catalog and its `.bbl` sibling | medium | transfer | Task 2's D4 check runs a programmatic diff per pair and records the exact differing-line count; identical content is duplication with a named remediation shape, divergent content is drift with each divergence named. Remediation transfers to Phase 67. |
| T-61-P07-S3 | Repudiation | a coverage claim implying a read that did not happen | high | mitigate | D-08 licenses a mechanical sweep for this unit **only** on condition that each cell names the method used; the acceptance criteria require the sampling protocol, its sources and the four diff counts to be present and reproducible, and an explicit prohibition forbids claiming a line-by-line read. Without this, the largest-LOC unit in the inventory would be the easiest place to fake coverage. |
| T-61-P07-01 | Repudiation | the phase's own arithmetic silently becoming the contract | high | mitigate | The D-17 closing gate re-derives 50/38/88 from INVENTORY with a recorded command and its output, counts the coverage file's own three totals, and states an explicit agree/disagree verdict. A disagreement is recorded as a defect and stops the close rather than being reconciled silently. |
| T-61-P07-02 | Repudiation | a file swept by no unit | high | mitigate | The close-out enumerates the 53 review-target files from the tree with `ls`, not from a list typed into a plan, and confirms every basename appears in the coverage file. A file missing from every unit's section surfaces here rather than in Phase 68. |
| T-61-P07-03 | Tampering | this plan disturbing six already-recorded sections | medium | mitigate | Wave 7 behind `depends_on: [61-06]` makes ordering structural (D-04); both tasks assert exact global counts (43/7, then 50/0) plus the invariant 38 carried-forward `n/a` lines and the 24 untouched `.bbl` `n/a` cells. |
| T-61-P07-04 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | medium | mitigate | D-12 two-tier disclosure applies unchanged; this unit is static data, so a `critical`/`high` D1 finding is unlikely but the rule stands. |
| T-61-P07-05 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; every record writes all six tests pass/fail. |
| T-61-P07-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk`, `sed`, `diff` and `ls`. |
</threat_model>

<verification>
- `## RU-61-07`'s 6 live unit-row cells and all 8 file-exception `applies` cells carry a verdict plus a
  written check line naming the method used; its 24 `.bbl` `n/a` cells are untouched.
- The four `.ts`-versus-`.bbl` diff counts recorded in the D4 cells reproduce when re-run.
- The D2 sampling protocol's size, indices and sources are recorded per catalog.
- **D-17 gate:** the coverage file contains exactly 50 verdicts, 38 verbatim `n/a` lines, 88 cell lines
  and 0 placeholders; the re-derivation from INVENTORY prints `50 38 88` and is recorded with an explicit
  agree/disagree verdict.
- **53-file gate:** the tree enumeration yields 53 files and every basename appears in the coverage file.
- `## Phase 61 Close-Out` records finding counts by dimension and disposition, referral resolution, and
  the evidence location for each of the four ROADMAP success criteria.
- Every finding has all 13 fields, a unique well-formed ID, a non-blank `dedup:` and a `location:`
  outside `java-interop/`.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-01 is satisfied: all 7 units swept, 50 of 50 `applies` cells recorded, all 53 hand-written files
  under `bbj-vscode/src/language/` named in the coverage file with a recorded verdict against every
  applicable dimension.
- ROADMAP Phase 61 criterion 1 is countable rather than assumed, and criteria 3 and 4 hold for every
  record in the file.
- Phase 68 can concatenate `61-COVERAGE.md` against INVENTORY's grid without re-deriving scope, and
  Phase 67 and Phase 69 can act on the dispositions without re-triaging them.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-07-SUMMARY.md` when done.
</output>
