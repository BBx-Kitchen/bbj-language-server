---
phase: 61-language-core-review
plan: 04
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - .planning/reviews/61-COVERAGE.md
autonomous: true
requirements: [RVW-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-61-02` (scope, linking & type inference, 8 files / 1,601 LOC) has all 6 live dimensions recorded in `61-COVERAGE.md` — D1, D2, D3 at evidence tier `repro` and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-06, D-07)"
    - "All 8 files — `bbj-scope.ts`, `bbj-scope-local.ts`, `bbj-linker.ts`, `bbj-index-manager.ts`, `bbj-nodedescription-provider.ts`, `bbj-type-inferer.ts`, `bbj-overload-selector.ts`, `assertions.ts` — are named at least once inside the `## RU-61-02` section, so coverage is file-granular (ROADMAP Phase 61 criterion 1)"
    - "The routing-table item this unit owns — #232 CPU stability in multi-project workspaces (DEBT-01) — is recorded as a `P61-D3-*` finding against the current code with full evidence, its `dedup:` field stating that no frozen open issue matches and naming DEBT-01 as the owning requirement, so Phase 66 re-triages rather than re-derives (D-14)"
    - "Every recorded finding carries `file:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #83, #90 and #466 checked explicitly as this unit's plausible neighbours (RVW-07, ROADMAP Phase 61 criterion 4)"
    - "The unit's 2 `n/a` cells still carry INVENTORY's marker text verbatim and the phase-wide carried-forward `n/a` line count is still 38 (D-03)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — when two candidate symbols are exactly equally applicable, does the resolver merge them, collide, or pick one? Specifically: overload selection when two signatures score equal, scope shadowing when a local and a member share a name in the same position, and cross-file linking when two documents export the same qualified name. Separately, at the unit boundary: a finding's `location:` decides ownership, so the 11 `test/linking.test.ts` interop failures — already owned by `RU-61-06` in plan 61-01 — are NOT re-recorded here, and any defect whose `file:line` belongs to another unit goes into `### Cross-unit referrals`"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — what does scope resolution return for an empty document, a single-declaration document, and a reference whose target document failed to load? Recording-wise, a dimension that produced zero findings records `pass` plus a written line naming the concrete checks applied, never a bare `pass` (D-06); `assertions.ts` at 4 lines is still swept and still named, not skipped for being small"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — when candidates compare equal, is the resolver's output order specified and stable across runs, or does it depend on index insertion order? Recording-wise, this plan appends only inside `## RU-61-02`, in wave 4 behind `depends_on: [61-03]` so the shared-file order is structural (D-04), and continues finding sequence numbers from the highest already present per dimension"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): owned by `RU-61-06` in plan 61-01. Where an unresolved reference here is caused by the java-interop peer being unavailable rather than by a resolver defect, that is stated in the cell text and not recorded as a `RU-61-02` finding"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): surfaced and dismissed in plan 61-01 — no rounding/tie-breaking contract exists in a review sweep; its applicable remnant belongs to `RU-61-06`. The tie-breaking question that *does* exist in this unit is overload-selection ordering and is authored into the ordering row above"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-02 — Scope, linking & type inference` — 6 live cells recorded, 2 `n/a` cells untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-61-02` cells -> the phase-wide D-17 cell-total gate re-run by plan 61-07"
    - "`RU-61-02` D3 finding on #232 CPU stability -> DEBT-01 in Phase 66"
    - "`RU-61-02` type-inference findings -> DEBT-03 in Phase 66 where they restate the static-return-type gap"
    - "`RU-61-02` findings -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records; Phase 67 fixes and Phase 66 resolves DEBT"
    - "MUST NOT re-record the 11 `test/linking.test.ts` interop failures — `RU-61-06` owns them and recorded them in plan 61-01; note the ownership under `### Cross-unit referrals` instead"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53` as a live finding (D-16)"
    - "MUST NOT record a shipped v3.9 behavior as a defect merely because it looks unusual — the two-phase `resolveClass`, the `isClassRef` static-only completion filter, and the empty-CompletionList `(` trigger are recorded decisions, not bugs; a finding against any of them must carry independent evidence of wrong behavior"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` or inside `bbj-vscode/src/language/generated/`"
    - "MUST NOT read any file under `.planning/codebase/` — superseded by INVENTORY.md (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — immutable for v4.0 (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-02`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-61-02` — Scope, linking & type inference** (`bbj-scope.ts` 578,
`bbj-scope-local.ts` 408, `bbj-linker.ts` 229, `bbj-index-manager.ts` 29,
`bbj-nodedescription-provider.ts` 131, `bbj-type-inferer.ts` 107, `bbj-overload-selector.ts` 115,
`assertions.ts` 4 — 8 files, 1,601 LOC, INVENTORY risk rank 4 of 7) across its 6 live dimensions and
record the result in the `## RU-61-02` section of `.planning/reviews/61-COVERAGE.md`.

Purpose: this is the semantic core — name resolution, cross-file linking and type inference. It is also
where #232's CPU-stability tech debt (DEBT-01) is routed for triage against the current code.

Output: 6 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**.

**Modified**
- `.planning/reviews/61-COVERAGE.md` — section `## RU-61-02 — Scope, linking & type inference` only.

**Finding-ID ranges.** `P61-D{dimension}-{seq}`, zero-padded to 3 digits, allocated monotonically in
discovery order within each `(61, dimension)` pair **across the whole phase**. This plan continues from
the highest sequence already present for each dimension after plans 61-01, 61-02 and 61-03. `P00-*` is
never allocated.

**Plan → review-unit map.** This plan owns `RU-61-02`. The full 7-plan map, the routing-table ownership
map and the section shape live in `61-01-PLAN.md` §"Artifacts this phase produces".

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption, translated into a D2 tie/shadowing check | `must_haves.truths`, this plan |
| 2 | RVW-01 | empty | authored as flagged assumption, translated into a D2 empty/single-element check | `must_haves.truths`, this plan |
| 3 | RVW-01 | ordering | authored as flagged assumption, translated into a D2 stable-order check | `must_haves.truths`, this plan |
| 4 | SEC-06 | boundary | authored as flagged assumption | plan 61-01 (`RU-61-06` owns SEC-06) |
| 5 | SEC-06 | precision | flagged, dismissed with written reason | plan 61-01 |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-61-02 at evidence tier `repro` — D1, D2, D3</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists, contains a `## RU-61-02 — ` heading whose 6
  live cell lines are still unrecorded, and plan 61-03 has committed (phase-wide verdict count is 18).
  Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — the header, the stopping rule, the write contract, the
      completed `## RU-61-06` section as the shape to copy, and every `### Cross-unit referrals` entry
      addressed to `RU-61-02` — including the note from plan 61-01 that the 11 `test/linking.test.ts`
      interop failures are already owned by `RU-61-06`
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Phase 61 review units" (`RU-61-02` block), §"Routing table (D-06)"
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3 wording; RVW-01, RVW-06, RVW-07; DEBT-01,
      DEBT-03, DEBT-04
    - `.planning/STATE.md` §"Tech Debt" and §"Decisions" — #232's documented-but-unimplemented CPU
      mitigations, and the Phase 59 decisions that are shipped behavior rather than defects
    - **Review targets, read in full:** `bbj-vscode/src/language/bbj-scope.ts` (578),
      `bbj-vscode/src/language/bbj-scope-local.ts` (408), `bbj-vscode/src/language/bbj-linker.ts` (229),
      `bbj-vscode/src/language/bbj-index-manager.ts` (29),
      `bbj-vscode/src/language/bbj-nodedescription-provider.ts` (131),
      `bbj-vscode/src/language/bbj-type-inferer.ts` (107),
      `bbj-vscode/src/language/bbj-overload-selector.ts` (115),
      `bbj-vscode/src/language/assertions.ts` (4)
    - `CLAUDE.md` §Architecture — the Scope/Linking and Type-inference bullets, and the AST Type
      Constants section
  </read_first>
  <action>
Record the D1, D2 and D3 cell lines of `## RU-61-02 — Scope, linking & type inference`, replacing the
placeholder verdict on each with `pass` or `fail` plus a written line naming the concrete checks
applied. Append any resulting finding records under that section's `### Findings`. Touch no other
section.

*D1 Security* (tier `repro`). Assess against injection, untrusted input, secret exposure, integrity gaps
and privilege/trust-boundary errors. Concrete checks: whether cross-file linking in `bbj-linker.ts` and
`bbj-scope.ts` resolves document URIs derived from `USE` statements, PREFIX values or configured paths
without constraining them to the workspace — a reference that can pull in a file outside the workspace
root is an integrity/trust-boundary question, not merely a correctness one; whether
`bbj-nodedescription-provider.ts` copies peer-supplied Java descriptions into the global index without
validation, which if so is a cross-unit referral to `RU-61-06` rather than a finding here; and whether
`bbj-index-manager.ts` at 29 lines admits entries from any source it does not control.

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource leaks.
Three checks are required by name, because they are the phase's translated edge probes:
  - **Ties.** In `bbj-overload-selector.ts`, when two candidate signatures score exactly equal, what
    happens — first wins, last wins, or an arbitrary index order? Is that behavior specified anywhere,
    and is it stable across runs? Do the same for `bbj-scope.ts` scope shadowing when a local and a
    member share a name at the same position, and for `bbj-linker.ts` when two documents export the same
    qualified name.
  - **Empty and single-element inputs.** What does scope computation return for an empty document, a
    document with exactly one declaration, and a reference whose target document failed to load or was
    deleted mid-session?
  - **Ordering stability.** Is the order in which `bbj-index-manager.ts` yields descriptions
    insertion-dependent, and can that change a resolution result between two runs over the same tree?
Beyond those three: null propagation in `bbj-type-inferer.ts` on expressions the grammar permits but the
inferer does not handle — including the DEBT-03 case where a static method call assigns no type;
swallowed exceptions in the scope providers; and whether an unresolved reference caused by an
unavailable java-interop peer is distinguishable in code from a genuine resolution failure. Each claim
needs a runnable reproduction — a fixture plus the exact `npx vitest run` command — or a line-by-line
trace naming the concrete input and the exact `file:line` where behavior diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks, missing
caches, redundant AST traversals and unbounded memory growth. This unit owns the routing-table item
**#232 CPU stability in multi-project workspaces (DEBT-01)**. Re-triage it against the current code
rather than restating the issue: identify the scope-walk or index-rebuild path that drives CPU in a
multi-project workspace, name it by `file:line`, and record what the current code does — whether the
documented mitigations are present, partially present, or absent. Record it as a `P61-D3-{seq}` finding
with that evidence. Its `dedup:` field states that #232 is not in the frozen 15-issue snapshot because
it is not an open issue, and names **DEBT-01** as the owning requirement so Phase 66 re-triages rather
than re-derives; the field is never blank. Beyond #232: whether scope computation is cached per document
and invalidated correctly; whether the linker re-resolves references already resolved; and whether the
index grows without bound as documents are opened and closed.

For every claim that clears tier `repro`, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block. `unit:` is `RU-61-02`.
`classification:` records pass/fail for all six Phase 60 D-13 classification tests. `dedup:` is checked against the frozen 15 —
#83 (project-wide `USE` statements, labelled scoping), #90 (opting files or regions out of linking,
labelled linking) and #466 (sibling-type method return mismatches, labelled types and validation) are
this unit's plausible neighbours and must all three be checked explicitly. A match records
`#NNN duplicate` or `#NNN partial-overlap` with what the finding adds; no match records `none`.

Do not re-record the 11 `test/linking.test.ts` interop failures — `RU-61-06` owns them. Do not record a
shipped Phase 59 decision as a defect without independent evidence of wrong behavior. Any claim that
does not clear tier `repro` goes under `### Not-reproducible dispositions` with the tier it failed and
why. Any defect whose `file:line` belongs to another unit goes under `### Cross-unit referrals`.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier cells are recorded: `awk '/^## RU-61-02 /,/^## RU-61-04 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[123] .* — (pass|fail) — '` prints `3`
    - The routed DEBT-01 item is recorded as a D3 finding naming its owner: `awk '/^## RU-61-02 /,/^## RU-61-04 /' .planning/reviews/61-COVERAGE.md > /tmp/ru02.txt; grep -c 'DEBT-01' /tmp/ru02.txt` ≥ 1 and `grep -cE '^id:[[:space:]]+P61-D3-[0-9]{3}' /tmp/ru02.txt` ≥ 1
    - The three translated edge-probe checks are visible in the D2 cell text or in a finding: `/tmp/ru02.txt` contains `bbj-overload-selector.ts` and `bbj-index-manager.ts`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `21` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `29`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0` and `grep -cE '^location:[[:space:]]+bbj-vscode/src/language/generated/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-02 /,/^## RU-61-04 /" "$f" > /tmp/ru02.txt; test "$(grep -cE "^- D[123] .* — (pass|fail) — " /tmp/ru02.txt)" = 3; grep -q "DEBT-01" /tmp/ru02.txt; grep -q "bbj-overload-selector.ts" /tmp/ru02.txt; grep -q "bbj-index-manager.ts" /tmp/ru02.txt; test "$(grep -cE " — (pass|fail) — " "$f")" = 21; test "$(grep -c " — pending" "$f")" = 29; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_02_REPRO_OK'</automated>
  </verify>
  <done>`RU-61-02`'s D1, D2 and D3 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — URI/index trust for D1; overload ties, shadowing, empty/single-element inputs
  and index ordering stability for D2; #232's CPU path re-triaged against the current code for D3. The
  DEBT-01 finding names its owning requirement so Phase 66 does not re-derive it.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-02 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`## RU-61-02`'s D1, D2 and D3 cells already carry verdicts — Task 1 committed. Halt
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` §`## RU-61-02` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b, §3c, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Test & Build Baseline"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; DEBT-03, DEBT-04
    - **Review targets:** the same 8 files as Task 1
    - **Test surface:** `bbj-vscode/test/linking.test.ts` (read for coverage assessment only — its 11
      interop failures are owned by `RU-61-06`), `bbj-vscode/test/variable-scoping.test.ts`,
      `bbj-vscode/test/parser.test.ts`, plus whatever
      `grep -rl 'scope\|linker\|inferer\|overload' bbj-vscode/test/` returns
    - `CLAUDE.md` §Architecture Scope/Linking and Type-inference bullets, and §AST Type Constants — D8
      targets checkable against the code just read
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-61-02` and append any resulting records. Touch no other
section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead code,
tangled coupling, inconsistent patterns and missing abstractions. Concrete checks: overlap between
`bbj-scope.ts` at 578 lines and `bbj-scope-local.ts` at 408 lines — whether the same traversal or
name-matching logic exists in both; function length and branch depth in the scope providers; whether
`assertions.ts` at 4 lines is live, and where its export is consumed — confirm or dismiss a
dead/vestigial-module finding with evidence rather than asserting one; whether
`bbj-index-manager.ts` at 29 lines is a meaningful override or a pass-through; and whether the linker,
scope and inferer share one convention for reporting an unresolvable reference or three. The code shape
is the evidence — name it and the `file:line`.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). Concrete checks: which
of the eight files have a dedicated test and which are exercised only through end-to-end linking tests;
whether overload selection has any test covering an exact-tie case; whether scope shadowing precedence
is asserted anywhere; whether the type inferer's untyped-static-call gap (DEBT-03) has a regression test
or only a prose record; and whether cross-file linking has coverage that does not depend on java-interop
being reachable. Record gaps as `P61-D5-{seq}` findings naming the untested branch by `file:line`, with
`dedup:` naming DEBT-03 or DEBT-04 where the gap restates one of them. Do **not** re-record the 11
`test/linking.test.ts` interop failures — `RU-61-06` owns them; state that ownership in this unit's
`### Cross-unit referrals` so a reader of this section is not left thinking they were missed. Do not
record the `beforeAll` hookTimeout flakiness either — `RU-61-05` owns it.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
claims contradicted by code. Concrete checks: every JSDoc block across the eight files against what the
code does; CLAUDE.md's Scope/Linking bullet — which assigns name provider and scope provider to
`bbj-scope.ts`, scope computation and LocalSymbols to `bbj-scope-local.ts`, and cross-file reference
linking to `bbj-linker.ts` — verified file by file; CLAUDE.md's Type-inference bullet naming
`bbj-type-inferer.ts`; and CLAUDE.md's §AST Type Constants claim that Langium 4.x uses the string type
constant for `$type` checks and that `isXxx()` guards come from `generated/ast.ts`, checked against how
this unit's code actually performs type checks. Record a divergence as a finding with the exact
CLAUDE.md line in `location:`.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 6 live cells now carry a verdict: `awk '/^## RU-61-02 /,/^## RU-61-04 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `6`
    - The 2 `n/a` cells are untouched: `awk '/^## RU-61-02 /,/^## RU-61-04 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[67] .*n/a — '` prints `2`
    - Every file in the unit is named inside its own section: `awk '/^## RU-61-02 /,/^## RU-61-04 /' .planning/reviews/61-COVERAGE.md > /tmp/ru02.txt; for f in bbj-scope.ts bbj-scope-local.ts bbj-linker.ts bbj-index-manager.ts bbj-nodedescription-provider.ts bbj-type-inferer.ts bbj-overload-selector.ts assertions.ts; do grep -q "$f" /tmp/ru02.txt || echo "MISSING $f"; done` prints nothing
    - The cross-unit ownership of the interop test failures is stated rather than silently omitted: `/tmp/ru02.txt` contains `RU-61-06`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `24` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `26`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - All three sub-blocks are present for this unit: `/tmp/ru02.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-02 /,/^## RU-61-04 /" "$f" > /tmp/ru02.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/ru02.txt)" = 6; test "$(grep -cE "^- D[67] .*n/a — " /tmp/ru02.txt)" = 2; for x in bbj-scope.ts bbj-scope-local.ts bbj-linker.ts bbj-index-manager.ts bbj-nodedescription-provider.ts bbj-type-inferer.ts bbj-overload-selector.ts assertions.ts RU-61-06 "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/ru02.txt; done; test "$(grep -cE " — (pass|fail) — " "$f")" = 24; test "$(grep -c " — pending" "$f")" = 26; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_02_COMPLETE'</automated>
  </verify>
  <done>`RU-61-02` is complete under the stopping rule: all 6 live cells carry a verdict plus a written
  check line, all 8 files including the 4-line `assertions.ts` are named inside the section, the
  `RU-61-06` ownership of the interop test failures is stated rather than silently omitted, CLAUDE.md's
  Scope/Linking, Type-inference and AST-constant claims are checked, and every unpromoted claim is
  visible under `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `USE` statements, PREFIX values and configured paths → document resolution | Cross-file linking resolves URIs derived from document content and workspace configuration; a reference that escapes the workspace root crosses a trust boundary. |
| java-interop peer descriptions → the global symbol index | `bbj-nodedescription-provider.ts` and `bbj-index-manager.ts` admit descriptions whose origin is the unauthenticated peer reviewed at `RU-61-06`. |
| any opened `.bbj` file → scope computation and type inference | Untrusted document content drives every walk in this unit. |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `61-COVERAGE.md` → the other six plans | One file, seven writers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P04-S1 | Denial of Service | scope walks and index rebuilds in a multi-project workspace (#232, DEBT-01) | high | transfer | Task 1's D3 check re-triages #232 against the current code, names the driving path by `file:line`, and records whether the documented mitigations are present, partial or absent. Recorded as a `P61-D3-*` finding naming DEBT-01; remediation transfers to Phase 66/67. |
| T-61-P04-S2 | Tampering | document-URI resolution from `USE`/PREFIX/configured paths | medium | transfer | Task 1's D1 check asks whether resolution is constrained to the workspace root or can pull in a file outside it. |
| T-61-P04-S3 | Tampering | peer-supplied descriptions entering the global index | medium | transfer | Task 1's D1 check asks whether `bbj-nodedescription-provider.ts` validates what it indexes; where the defect is on the peer side it becomes a cross-unit referral to `RU-61-06` rather than a finding here. |
| T-61-P04-S4 | Repudiation | unspecified tie-breaking in overload selection and scope shadowing | medium | transfer | Task 1's D2 check requires a stated answer for equal-scoring overloads, same-position shadowing, duplicate exported qualified names, and insertion-order dependence in the index — the phase's translated adjacency and ordering edge probes. |
| T-61-P04-01 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | high | mitigate | D-12 two-tier disclosure: surface, problem class and impact only; no trigger sequence, no payload, no proof-of-concept. |
| T-61-P04-02 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; every record writes all six tests pass/fail. |
| T-61-P04-03 | Tampering | seven plans writing one `61-COVERAGE.md` | medium | mitigate | Wave 4 behind `depends_on: [61-03]` makes ordering structural (D-04); both tasks assert exact global counts (21/29, then 24/26) plus the invariant 38 carried-forward `n/a` lines. |
| T-61-P04-04 | Repudiation | double-recording an item another unit already owns | medium | mitigate | Explicit prohibition on re-recording the 11 `test/linking.test.ts` interop failures, plus an acceptance criterion requiring this section to name `RU-61-06` as their owner, so the item is neither duplicated nor silently dropped. |
| T-61-P04-05 | Repudiation | recording shipped v3.9 behavior as a defect | low | mitigate | Explicit prohibition naming the three Phase 59 decisions; a finding against any of them must carry independent evidence of wrong behavior. |
| T-61-P04-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and the already-installed `npx vitest run` harness. |
</threat_model>

<verification>
- `## RU-61-02`'s 6 live cells each carry `pass` or `fail` plus a written check line ≥ 80 characters.
- All 8 unit files, including the 4-line `assertions.ts`, are named inside the section.
- #232 is re-triaged as a D3 finding whose `dedup:` names DEBT-01 and states why no frozen issue matches.
- The section names `RU-61-06` as owner of the interop test failures.
- Phase-wide verdict count is 24 and placeholder count is 26; the carried-forward `n/a` count is 38.
- Every finding has all 13 fields, a unique well-formed ID and a non-blank `dedup:` that explicitly
  covered #83, #90 and #466.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-01 advances: 4 of 7 units swept, 24 of 50 `applies` cells recorded.
- DEBT-01 arrives in Phase 66 as a re-triaged finding against current code rather than a prose carry-over.
- ROADMAP Phase 61 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-04-SUMMARY.md` when done.
</output>
