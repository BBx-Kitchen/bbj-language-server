---
phase: 61-language-core-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/61-COVERAGE.md
  - .planning/BACKLOG.md
autonomous: false
requirements: [RVW-01, SEC-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths (phase goal: every hand-written file in src/language/ swept across all 8 dimensions)
    - "`.planning/reviews/61-COVERAGE.md` exists and carries the complete Phase 61 slice of INVENTORY.md's applicability grid as a skeleton: 7 unit rows + 4 `.bbl` file-exception rows = 88 cell lines, of which all 38 `n/a` cells already carry their INVENTORY marker text verbatim (D-03)"
    - "The tree the sweep reads is stated once, unambiguously: branch `v4.0-stability-and-quality` at a recorded 40-character commit SHA, so the coverage claim anchors to one tree for the whole phase (D-15)"
    - "`RU-61-06` (java-interop client, 4 files / 1,255 LOC) has all 6 live dimensions recorded — D1, D2, D3 at evidence tier `repro` and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-06, D-07)"
    - "Every one of the 4 `RU-61-06` files is named at least once inside the `## RU-61-06` section, so coverage is file-granular and not merely unit-granular (ROADMAP Phase 61 criterion 1)"
    - "SEC-06 is discharged as a written `### SEC-06 Trust Boundary` subsection answering six named questions — what the peer controls, the authentication posture as a factual statement, where host/port come from and who can set them, malicious-peer behavior, unresponsive-peer behavior at each timeout threshold, and blast radius (D-10, ROADMAP Phase 61 criterion 2)"
    - "Every recorded finding carries `file:line`, a primary dimension, and evidence that clears its tier — a runnable reproduction or a line-by-line trace naming concrete inputs/state and the exact `file:line` where behavior diverges; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason rather than vanishing (RVW-06, INVENTORY §3b)"
    - "Every recorded finding carries a non-blank `dedup:` field checked against the 15 frozen open issues in INVENTORY.md (RVW-07, ROADMAP Phase 61 criterion 4)"
    - "A `critical` or `high` D1 finding additionally carries a runnable reproduction, and its committed record names the surface, the problem class and the impact without a trigger sequence, a payload or a proof-of-concept (D-11, D-12)"
    - "The per-unit section shape a downstream plan must copy is visible in one worked unit before any other unit is swept, and the developer has approved that shape (D-01, D-05)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> authored as explicit flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): a finding's `location:` decides which unit owns it, not which unit discovered it. Where a `RU-61-06` sweep surfaces a defect whose `file:line` belongs to another unit, it is written into `### Cross-unit referrals` naming the target unit and the `file:line`; it is never recorded as an `RU-61-06` finding and never written into another unit's section. Where two units' concerns touch exactly — the 11 `test/linking.test.ts` interop failures, whose subject is the linker (`RU-61-02`) but whose cause is this unit's unreachable peer — the item is recorded exactly once, here, and named in `### Cross-unit referrals` as already-owned so `RU-61-02` does not re-record it"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): a dimension that produced zero findings records `pass` plus a written line naming the concrete checks applied, never a bare `pass` — an unswept unit must be distinguishable from a clean one (D-06). A unit with zero findings records the literal text `_(none recorded)_` under `### Findings` rather than leaving the block absent, and `.planning/BACKLOG.md` is created only if at least one D-13 Java-side observation exists"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): append order into the single shared `61-COVERAGE.md` is structural, not conventional — one plan per wave, waves 1-7, each plan's `depends_on` naming its predecessor (D-04) — and finding sequence numbers are allocated monotonically in discovery order within each `(61, dimension)` pair, so a later plan continues from the highest `P61-D{n}-{seq}` already present rather than restarting at 001. No `P00-*` ID is ever allocated"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): the SEC-06 write-up states, for each timeout threshold in the client, what happens exactly at the threshold and one step either side — the 10-second socket connect timeout, the 10-second per-request race on class resolution, and every request path where no timeout wraps the await at all — plus what a peer that accepts the TCP connection and then never answers costs the language server"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): a rounding/tie-breaking contract has no referent in a review sweep — this phase computes no numeric result, so there is no half-up-versus-half-to-even decision to state. The one numerically-shaped question the probe does reach — whether a non-integer, negative, out-of-range or string-typed `interopPort` arriving through `initializationOptions` is validated or silently coerced — is folded into the SEC-06/boundary truth above and is therefore recorded, not dropped"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md — created by this plan: header with the recorded sweep SHA, the Phase 61 applicability grid, the D-17 cell-total gate, the 4 verbatim exclusion-reason blocks, and 7 stubbed per-unit sections"
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-06 — Java interop client` — filled end to end: 8 cell lines, `### SEC-06 Trust Boundary`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`"
    - ".planning/BACKLOG.md §`## FUT-01 — java-interop/ (Java service) observations` — created only if at least one Java-side observation arose while reading the service as reference material (D-13)"

  key_links:
    - "`61-COVERAGE.md` per-unit section shape -> plans 61-02..61-07, which each fill exactly one inherited section (D-03, one-way once expansion starts, gated by the Task 3 checkpoint)"
    - "`61-COVERAGE.md` -> Phase 68 DOC-03, which concatenates the five `{NN}-COVERAGE.md` files against INVENTORY.md's grid without re-deriving scope (INVENTORY §Recording protocol)"
    - "`RU-61-06` finding `classification: easy|major` -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split (INVENTORY §3c)"
    - "`### SEC-06 Trust Boundary` -> ROADMAP Phase 61 success criterion 2 and REQUIREMENTS SEC-06"
    - "`### Cross-unit referrals` -> the owning expansion plan, which must promote or dismiss each referral addressed to it"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` — that service is read as reference material only and no coverage cell covers it (D-13)"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are superseded by INVENTORY.md and `CONCERNS.md` lists FIXMEs already resolved in v3.8 (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53` as a live finding — it is fixed on this branch and merely unreleased (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-06`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding in this public repository (D-12)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Create `.planning/reviews/61-COVERAGE.md` — Phase 61's sole deliverable — and prove the whole recording
pipeline end to end on **one** review unit before the other six are swept.

The tracer unit is **`RU-61-06`, the java-interop client** (`java-interop.ts`, `java-javadoc.ts`,
`lib/bbj-api.ts`, `lib/fs-provider.ts` — 4 files, 1,255 LOC). It is chosen deliberately (D-01): it is
INVENTORY's risk rank 1 of 7, it is the entire SEC-06 trust boundary and therefore front-loads the
phase's only non-RVW-01 requirement, it is the smallest of the six behavioral units so the shape is
proven cheaply, and it is the only unit that exercises every hard part of the standard at once — the
`repro` evidence tier, the `critical`/`high` D1 runnable-repro rule (D-11), the public-repo redaction
rule (D-12), a narrative subsection with no prior instance to copy (D-10), and a cross-unit referral.
If the recording shape is wrong, it is wrong here first, at the only point where correcting it is cheap.

Purpose: six downstream plans and Phase 68's DOC-03 concatenation inherit this file's section shape.
D-03 makes that shape costly to change once expansion starts, so the Task 3 checkpoint approves it
before plan 61-02 runs.

Output: `.planning/reviews/61-COVERAGE.md` with the grid, the D-17 cell-total gate, the 38 verbatim
`n/a` carry-forwards, 7 stubbed unit sections, and `RU-61-06` filled in full including its
`### SEC-06 Trust Boundary` write-up.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact. Drift verification must treat these as new.

**New files**
- `.planning/reviews/61-COVERAGE.md` — created by this plan (skeleton + `RU-61-06`), appended by plans
  61-02..61-07, one section each.
- `.planning/BACKLOG.md` — created by this plan **only if** at least one D-13 Java-side observation
  arose. Carries a single `## FUT-01 — java-interop/ (Java service) observations` section. This is the
  planner's discretion call on "which GSD backlog mechanism carries the FUT-01 observations": a
  standalone append-only file keeps `.planning/reviews/` purely about in-scope findings (D-13's own
  requirement), leaves the immutable INVENTORY untouched, and does not mutate ROADMAP's phase list
  mid-milestone.

**Plan → review-unit map (the plan number and the unit number deliberately do not match; D-02 orders
by INVENTORY risk rank)**

| Plan | Unit | Subject | Files / LOC | Wave |
|---|---|---|---|---|
| 61-01 | `RU-61-06` | Java interop client | 4 / 1,255 | 1 |
| 61-02 | `RU-61-01` | Grammar & lexing | 5 / 1,340 | 2 |
| 61-03 | `RU-61-03` | Validation & BBjCPL diagnostics | 8 / 2,542 | 3 |
| 61-04 | `RU-61-02` | Scope, linking & type inference | 8 / 1,601 | 4 |
| 61-05 | `RU-61-04` | LSP feature providers | 11 / 1,825 | 5 |
| 61-06 | `RU-61-05` | Server lifecycle, DI wiring & workspace | 9 / 1,433 | 6 |
| 61-07 | `RU-61-07` | Builtin catalogs | 8 / 3,752 | 7 |
| | | **Total** | **53 / 13,748** | |

**New section anchors inside `61-COVERAGE.md`** (created by this plan, filled by the owning plan)
`## Applicability Grid — Phase 61 slice`, `### File-exception rows`, `## D-17 Cell-Total Gate`,
`## Exclusion reasons carried forward`, then `## RU-61-06 …` through `## RU-61-07 …`, each with
`### Cells`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`.
`RU-61-06` additionally carries `### SEC-06 Trust Boundary`. `RU-61-07` additionally carries
`### File-exception cells`.

**Finding-ID ranges.** Token shape `P61-D{dimension}-{seq}` (INVENTORY §3a), zero-padded to three
digits, allocated monotonically in discovery order within each `(61, dimension)` pair **across the
whole phase, not per plan** — plan 61-0N continues from the highest sequence already present for that
dimension. This plan opens the phase and therefore allocates from `001` in each dimension it uses.
`P00-*` is reserved for INVENTORY's template illustration and is never allocated.

**Routing-table ownership map (D-14 inheritance, binding on all 7 plans)**

| Routed item (INVENTORY §"Routing table (D-06)") | Owning unit | Owning plan | Dimension |
|---|---|---|---|
| 11 `test/linking.test.ts` "Interop related tests" failures | `RU-61-06` | **61-01 (this plan)** | D5, cross-ref D2 |
| 3 disabled `test/parser.test.ts` assertions (DEBT-02) | `RU-61-01` | 61-02 | D5 |
| `beforeAll` `WorkspaceManager.initializeWorkspace()` hookTimeout flakiness | `RU-61-05` | 61-06 | D5 |
| TEST-03 `test/completion-test.test.ts` skip (DEBT-02) | `RU-61-04` | 61-05 | D5 |
| `bbj-document-symbol-provider.ts` 2 unused eslint-disable warnings | `RU-61-04` | 61-05 | D4 |
| #232 CPU stability in multi-project workspaces (DEBT-01) | `RU-61-02` | 61-04 | D3, cross-ref D4 |

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

No SPEC.md exists for this phase, so neither `## Edge Coverage` nor `## Prohibitions` was supplied.
All 5 probe rows came back `unresolved`; none is auto-resolved with a backstop and none is dropped.

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption | `must_haves.truths` of all 7 plans |
| 2 | RVW-01 | empty | authored as flagged assumption | `must_haves.truths` of all 7 plans |
| 3 | RVW-01 | ordering | authored as flagged assumption | `must_haves.truths` of all 7 plans |
| 4 | SEC-06 | boundary | authored as flagged assumption | `must_haves.truths` of this plan (SEC-06 is `RU-61-06`-only) |
| 5 | SEC-06 | precision | flagged, dismissed with written reason; applicable remnant folded into row 4 | `must_haves.truths` of this plan |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<task type="tracer">
  <name>Task 1: Create 61-COVERAGE.md and sweep RU-61-06 at evidence tier `repro` (D1, D2, D3) including the SEC-06 trust boundary</name>
  <files>.planning/reviews/61-COVERAGE.md, .planning/BACKLOG.md</files>
  <precondition>The working tree is on branch `v4.0-stability-and-quality` — `git rev-parse --abbrev-ref HEAD`
  prints exactly that — and `.planning/reviews/INVENTORY.md` exists. Halt and report if either fails; the
  sweep target and the finding standard both depend on it (D-15, Phase 60 D-09).</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the immutable contract. Read §"Frozen Open-Issue Snapshot"
      (the 15-issue dedup list), §"Finding Standard" 3a-3d, §"Finding Record Template", §"Phase 61
      review units" (`RU-61-06` block), §"Applicability Grid" rows `RU-61-01`..`RU-61-07` and the four
      `lib/*.bbl` file-exception rows, §"Exclusion reasons" (markers R-D6-CENTRAL, R-D7-SHARED-LS,
      R-BBL-STATIC, R-BBL-NODOC), §"Recording protocol (D-09)", §"Test & Build Baseline" including
      §"Routing table (D-06)", and the `RU-62-04` block plus its `P00-D1-001` worked record — that block
      is the literal shape this section copies
    - `.planning/REQUIREMENTS.md` lines 14-23 — the D1-D8 table. Every `pass` line is phrased against
      that dimension's own "what counts as a finding" wording. Also RVW-01, RVW-06, RVW-07, SEC-06
    - `.planning/ROADMAP.md` §"Phase 61: Language Core Review" — the four success criteria
    - `.planning/phases/61-language-core-review/61-CONTEXT.md` — D-01..D-17 are binding
    - **Review targets, read in full:** `bbj-vscode/src/language/java-interop.ts` (955),
      `bbj-vscode/src/language/java-javadoc.ts` (219), `bbj-vscode/src/language/lib/bbj-api.ts` (12),
      `bbj-vscode/src/language/lib/fs-provider.ts` (69)
    - **Reference only, no finding may be located here (D-13):** `java-interop/` — read the Java
      service far enough to establish the wire contract: which requests exist, what each response is
      entitled to contain, and whether any handshake or authentication step exists on the server side
    - **Reference only, findings belong to `RU-61-05` via `### Cross-unit referrals`:**
      `bbj-vscode/src/language/bbj-ws-manager.ts` lines 40-60 and
      `bbj-vscode/src/language/main.ts` lines 145-165 — the two call sites that supply
      `interopHost`/`interopPort` to the client
    - `bbj-vscode/test/linking.test.ts` — the 11 "Interop related tests" failures routed to this unit
  </read_first>
  <action>
Create `.planning/reviews/61-COVERAGE.md`. Write it in this order.

**A. Header.** Title it as Phase 61's coverage record for `bbj-vscode/src/language/` (RVW-01, SEC-06).
Record, as four labelled lines: the swept tree — branch `v4.0-stability-and-quality` at the full
40-character SHA from `git rev-parse HEAD`, stated once for the whole phase and not re-anchored per
plan (D-15); the governing standard, `.planning/reviews/INVENTORY.md`, named as immutable and not
edited by this phase; the dedup source, INVENTORY's frozen 15-issue snapshot, with the note that
Phase 69 re-queries live; and the slice size — 7 unit rows plus 4 `.bbl` file-exception rows, 11 rows
x 8 dimensions = 88 cells, 50 `applies` and 38 `n/a`.

**B. `## Applicability Grid — Phase 61 slice`.** The file's layout is grid table on top, per-unit detail
below (D-09), so Phase 68 can concatenate mechanically without re-deriving scope, and each unit's
evidence stays adjacent to its own coverage claim rather than being flattened into an ID-ordered queue.
A markdown table using INVENTORY's exact column shape
`Unit | D1 Security | D2 Correctness | D3 Performance | D4 Maintainability | D5 Test coverage | D6 Dependency health | D7 Cross-IDE parity | D8 Doc accuracy`, with one row per unit in D-02's risk-rank
order: `RU-61-06`, `RU-61-01`, `RU-61-03`, `RU-61-02`, `RU-61-04`, `RU-61-05`, `RU-61-07`. Then a
`### File-exception rows` sub-table with the same columns plus a parent-unit column, one row each for
`lib/events.bbl`, `lib/functions.bbl`, `lib/labels.bbl`, `lib/variables.bbl`. In both tables an `n/a`
cell carries its INVENTORY marker name; an `applies` cell carries the verdict once the owning plan has
recorded it. Fill only the `RU-61-06` row's verdicts in this plan.

**C. `## D-17 Cell-Total Gate`.** State the expected totals — 50 `applies`, 38 `n/a`, 88 total — and
immediately re-derive them from INVENTORY rather than restating them, by running and recording the
output of a single awk pass over `.planning/reviews/INVENTORY.md` that selects the rows matching the
Phase 61 unit rows and the four `lib/*.bbl` file-exception rows and counts `applies` and `n/a`
occurrences in them. Record the command and its literal output. State explicitly that if the
re-derivation ever disagrees with the stated totals, the disagreement is itself a defect to surface,
not a number to quietly adopt (D-17). Note that plan 61-07 re-runs this gate as the phase's closing
check.

**D. `## Exclusion reasons carried forward`.** Four blocks, one per marker, each carrying the sentence
from INVENTORY §"Exclusion reasons" **copied verbatim, not reworded and not re-derived**, plus the
count of cells in this slice that resolve to it: R-D6-CENTRAL (11 cells — 7 unit rows plus 4 `.bbl`
rows), R-D7-SHARED-LS (11 cells — same distribution), R-BBL-STATIC (12 cells — D1, D3, D5 on each of
the 4 `.bbl` rows), R-BBL-NODOC (4 cells — D8 on each `.bbl` row). 11+11+12+4 = 38; state that identity.

**E. Seven stubbed unit sections.** One `## RU-61-0N — <name>` heading per unit, in D-02's order, each
carrying: a `**Files (<count> / <LOC> LOC):**` line listing every file path with its individual LOC
from INVENTORY's per-unit table; a `**Risk rank:**` / `**Sweep method (D-08):**` / `**Owning plan:**`
line — sweep method is `full read` for ranks 1-6 and `mechanical` for `RU-61-07`; then a `### Cells`
block with exactly 8 lines, one per dimension, each beginning `- D{n} <dimension name> — `. For the
two `n/a` dimensions (D6, D7) the line is completed now with `n/a — ` followed by the verbatim marker
text from section D. For the six live dimensions the line ends with the single placeholder word that
section H defines, to be replaced by the owning plan. Then three empty blocks — `### Findings`,
`### Not-reproducible dispositions`, `### Cross-unit referrals` — each holding the literal text
`_(none recorded)_`. Give `RU-61-06` an additional `### SEC-06 Trust Boundary` block. Give `RU-61-07`
an additional `### File-exception cells` block with 32 lines, 8 per `.bbl` file, each beginning
`- [file-exception] ` then the file path, ` · D{n} — `; complete the 6 `n/a` lines per file now with
the verbatim marker text (D1, D3, D5 → R-BBL-STATIC; D6 → R-D6-CENTRAL; D7 → R-D7-SHARED-LS;
D8 → R-BBL-NODOC) and leave D2 and D4 for plan 61-07.

**F. The stopping rule and the write contract**, stated once near the top so every downstream plan
inherits them. Stopping rule: a unit's sweep is complete when (i) each of its 6 live `applies` cells
carries a verdict plus a written line naming the concrete checks applied, (ii) every file in the
unit's file list is named at least once inside that unit's section — in a check line or in a finding's
`location:` — so coverage is file-granular, and (iii) every candidate claim raised during the sweep is
either promoted to a finding record clearing its tier or written under `### Not-reproducible
dispositions` with its reason. Once (i)-(iii) hold the unit is done; no further reading is licensed.
Write contract: plans 61-02..61-07 each fill exactly one unit section and touch nothing else — no
fragment files, no assembly plan, no whole-file rewrite, no rewording of a carried-forward `n/a`
(D-03), and ordering is enforced by the wave chain rather than by executor behavior (D-04).

**G. Sweep `RU-61-06` at tier `repro` — D1, D2, D3.** Read all four target files in full, then record
those three cell lines and the SEC-06 subsection.

  *D1 Security.* Assess against REQUIREMENTS.md's D1 wording — injection, untrusted input, secret
  exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks: whether the
  `net.Socket` channel carries any handshake, token, peer identity check or transport security;
  whether every field of every JSON-RPC response is validated before it becomes a `JavaClass`,
  `JavaField`, `JavaMethod`, `JavaPackage` or `DocumentationInfo` node and before it reaches hover and
  completion markdown; whether a response can influence a filesystem path, a URI or a spawned process;
  what `JavadocProvider` does with peer- or disk-supplied documentation content; whether
  `lib/fs-provider.ts`'s `bbjlib` virtual filesystem can serve anything beyond its four fixed catalog
  paths; and whether the `interopHost`/`interopPort` values reaching `setConnectionConfig` are
  validated for host and port range at all, including the non-integer, negative, out-of-range and
  string-typed cases.

  *D2 Correctness & error handling.* Assess against null/undefined safety, unhandled rejections,
  swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource leaks. The
  concrete checks: what happens to the cached `connection` field when the socket errors or closes after
  a successful connect; whether a rejected connect leaves a half-open socket or an un-cleared timer;
  whether concurrent callers of the connect path can race into two sockets; whether a request that
  loses a timeout race leaves an un-awaited promise or an unhandled rejection; how a malformed,
  truncated or type-unexpected response is handled on each request path; and whether `clearCache`
  actually disconnects and releases everything it claims to.

  *D3 Performance & resource use.* Assess against hot-path cost, unbounded memory growth, missing
  caches and redundant work. The concrete checks: whether resolved-class and javadoc caches are bounded
  or grow without limit for the process lifetime; whether an unavailable peer causes repeated
  reconnect attempts per unresolved reference on every keystroke; and the cost of the implicit-import
  preload against the size of the package list.

  *`### SEC-06 Trust Boundary`.* A narrative subsection — this is a boundary map, not a list of
  defects, and it is a deliverable in its own right (D-10). It must answer, under six labelled
  headings, each answer anchored to a `file:line`:
  (1) **What the peer controls** — every response field that crosses into the AST, hover or completion,
  and what the client does with each without validation.
  (2) **Authentication posture** — a flat factual statement of whether the channel is authenticated,
  encrypted, or identity-checked in either direction, with the `file:line` that establishes it. State
  the fact even when it is not a finding; it becomes a finding only if it enables something.
  (3) **Who can set the destination** — where `interopHost` and `interopPort` come from
  (`initializationOptions` at `bbj-ws-manager.ts`, the settings-change path at `main.ts`), what the
  defaults are, and specifically whether a workspace-scoped IDE setting shipped inside a cloned
  repository can redirect the client off loopback to an attacker-chosen host, and whether the port
  value is validated or coerced.
  (4) **A malicious peer that answers** — what an attacker who owns the listening socket can do:
  oversized or deeply nested payloads, hostile strings reaching IDE-rendered markdown, wrong-typed or
  missing fields, protocol-level abuse.
  (5) **An unresponsive peer** — behavior exactly at and one step either side of each threshold: the
  socket connect timeout, the per-request timeout on class resolution, and every request path where no
  timeout wraps the await at all; plus the cost of a peer that completes the TCP handshake and then
  never answers.
  (6) **Blast radius** — whether the consequences are confined to the language-server process or reach
  the IDE host, the filesystem, or a spawned process.

  *Findings.* For every claim that clears tier `repro` — a runnable reproduction, or a line-by-line
  trace naming the concrete inputs/state and the exact `file:line` where behavior diverges — write one
  record under `### Findings` using INVENTORY's 13-field template verbatim, one field per line, inside
  a fenced block, with `id` allocated as `P61-D{dimension}-{seq}` from `001` upward in discovery order
  per dimension. `classification:` records pass/fail for all six Phase 60 D-13 classification tests. `dedup:` is checked against
  the frozen 15 and is never blank. A `critical` or `high` D1 finding must additionally carry a
  runnable reproduction (D-11) and its record names the surface, the problem class and the impact only
  — it states that the reproduction exists and what it establishes, without the trigger sequence, the
  payload or a proof-of-concept (D-12). All D2 and D3 findings, and D1 findings rated `medium` or
  `low`, carry full concrete detail. A claim that does not clear its tier goes under
  `### Not-reproducible dispositions` with the tier it failed and why.

  *Cross-unit referrals.* Any D1/D2/D3 defect discovered here whose `file:line` belongs to another unit
  — in particular anything in `bbj-ws-manager.ts` or `main.ts`, which belong to `RU-61-05` — is written
  under `### Cross-unit referrals` naming the target unit, the `file:line` and the one-line observation.
  Do not write it as an `RU-61-06` finding and do not write into another unit's section.

**H. The placeholder.** Every not-yet-recorded live-dimension cell line ends with the single lowercase
word that plans 61-02..61-07 replace with a verdict. Use exactly the word used in this plan's
acceptance criteria for the remaining-cell count, so the count is mechanically checkable at every wave.
It is: pending.

**I. `.planning/BACKLOG.md` (D-13).** If, and only if, reading `java-interop/` as reference material
surfaced an observation about the Java service itself, create `.planning/BACKLOG.md` with a single
`## FUT-01 — java-interop/ (Java service) observations` section and one bullet per observation, each
carrying a `java-interop/<path>:<line>` anchor and a one-line description. These are **not** findings,
carry no `P61-*` ID, and never appear in `61-COVERAGE.md`. If no such observation arose, do not create
the file, and record that fact in one sentence at the end of the `### SEC-06 Trust Boundary` block so a
reader knows the question was asked. Note the accepted tradeoff explicitly there: a reader of
`61-COVERAGE.md` alone will not learn these observations exist.
  </action>
  <reversibility rating="costly">The per-unit section shape is inherited by six downstream plans and by
  Phase 68's DOC-03 concatenation; changing it after any expansion plan has run forces those units to be
  re-recorded (D-03). Gated by the Task 3 checkpoint, which is placed at the only point where correcting
  it is cheap (D-05).</reversibility>
  <acceptance_criteria>
    - The file exists: `test -f .planning/reviews/61-COVERAGE.md` exits 0
    - The sweep tree is recorded as a full SHA on the right branch: `git rev-parse --abbrev-ref HEAD` prints `v4.0-stability-and-quality`, and `grep -cE '[0-9a-f]{40}' .planning/reviews/61-COVERAGE.md` ≥ 1
    - All 7 unit sections are stubbed: `grep -cE '^## RU-61-0[1-7] — ' .planning/reviews/61-COVERAGE.md` prints `7`
    - The cell-line inventory is complete: `grep -cE '^- D[1-8] ' .planning/reviews/61-COVERAGE.md` prints `56` and `grep -cE '^- \[file-exception\] ' .planning/reviews/61-COVERAGE.md` prints `32` (56 + 32 = 88)
    - All 38 `n/a` cells are already carried forward: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Exactly this task's 3 `repro`-tier cells are recorded and 47 remain: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `3` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `47`
    - The four exclusion-reason texts are verbatim: `for s in 'No distinct third-party dependency of its own' 'This code is part of the single language-server binary' 'static builtin-verb/function/label/variable data catalogs with no executable logic' 'raw data files with no comments or docstrings to go stale'; do grep -qF "$s" .planning/reviews/61-COVERAGE.md || echo "MISSING $s"; done` prints nothing
    - The D-17 gate re-derives rather than restates: `awk '/^\| \`RU-61-0[1-7]\` \|/ || /^\| \`lib\/[a-z]+\.bbl\` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md` prints `50 38 88`, and both the command and that output appear in the `## D-17 Cell-Total Gate` section
    - Every `RU-61-06` file is named inside its own section (file-granular coverage): `awk '/^## RU-61-06 /,/^## RU-61-01 /' .planning/reviews/61-COVERAGE.md > /tmp/ru06.txt; for f in java-interop.ts java-javadoc.ts bbj-api.ts fs-provider.ts; do grep -q "$f" /tmp/ru06.txt || echo "MISSING $f"; done` prints nothing
    - The `RU-61-06` section carries all 8 cell lines with a verdict or a verbatim `n/a`: `awk '/^## RU-61-06 /,/^## RU-61-01 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] '` prints `8`, of which `grep -cE '^- D[123] .* — (pass|fail) — '` prints `3`
    - Every recorded `pass` cell carries a written line, not a bare verdict: every line matching `^- D[1-8] .* — pass — ` is at least 80 characters long (`awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT: " $0}' .planning/reviews/61-COVERAGE.md` prints nothing)
    - The SEC-06 subsection exists and answers all six named questions: `grep -c '### SEC-06 Trust Boundary' .planning/reviews/61-COVERAGE.md` prints `1`, and within it each of the six headings — what the peer controls, authentication posture, who can set the destination, malicious peer, unresponsive peer, blast radius — appears, and the block contains at least 6 `file:line` anchors matching `[a-z-]+\.ts:[0-9]+`
    - Every finding record is complete: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` inside the file are all equal
    - Finding IDs are well-formed, unique, allocated from this phase only: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing, and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - No `dedup:` is blank: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is anchored in the excluded Java service: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified: `git status --porcelain bbj-vscode bbj-intellij java-interop` prints nothing
    - INVENTORY was not edited: `git status --porcelain .planning/reviews/INVENTORY.md` prints nothing
    - The three empty blocks exist in every stubbed section: `grep -c '### Findings' .planning/reviews/61-COVERAGE.md` prints `7`, `grep -c '### Not-reproducible dispositions' .planning/reviews/61-COVERAGE.md` prints `7`, `grep -c '### Cross-unit referrals' .planning/reviews/61-COVERAGE.md` prints `7`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; test -f "$f"; test "$(git rev-parse --abbrev-ref HEAD)" = v4.0-stability-and-quality; test "$(grep -cE "^## RU-61-0[1-7] — " "$f")" = 7; test "$(grep -cE "^- D[1-8] " "$f")" = 56; test "$(grep -cE "^- \[file-exception\] " "$f")" = 32; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test "$(grep -cE " — (pass|fail) — " "$f")" = 3; test "$(grep -c " — pending" "$f")" = 47; test "$(awk "/^\\| .RU-61-0[1-7]. \\|/ || /^\\| .lib\\/[a-z]+\\.bbl. \\|/ {a+=gsub(/applies/,\"applies\"); n+=gsub(/n\\/a/,\"n\\/a\")} END{print a, n, a+n}" .planning/reviews/INVENTORY.md)" = "50 38 88"; grep -q "### SEC-06 Trust Boundary" "$f"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop)"; test -z "$(git status --porcelain .planning/reviews/INVENTORY.md)"; echo TRACER_OK'</automated>
  </verify>
  <done>`.planning/reviews/61-COVERAGE.md` exists with the full 88-cell skeleton, all 38 `n/a` reasons
  carried forward verbatim, the D-17 gate re-derived from INVENTORY, and `RU-61-06`'s three `repro`-tier
  cells recorded with written check lines. The SEC-06 trust boundary is documented as a narrative
  answering six named questions with `file:line` anchors. Every finding that cleared tier `repro` is a
  complete 13-field record with a non-blank dedup check against the frozen 15; every claim that did not
  is visible under `### Not-reproducible dispositions`. No source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-06 at evidence tier `trace` (D4, D5, D8)</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists and its `## RU-61-06` section already carries
  verdicts for D1, D2 and D3 — Task 1 committed. Halt if the section is still fully unrecorded.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` §`## RU-61-06` — the section this task completes; match the
      cell-line and finding-record shape Task 1 established
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Test & Build Baseline" — the 11 `test/linking.test.ts` failures
      with their per-test evidence column, and §"Routing table (D-06)"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; DEBT-01..DEBT-06
    - **Review targets:** `bbj-vscode/src/language/java-interop.ts`,
      `bbj-vscode/src/language/java-javadoc.ts`, `bbj-vscode/src/language/lib/bbj-api.ts`,
      `bbj-vscode/src/language/lib/fs-provider.ts`
    - **Test surface:** `bbj-vscode/test/linking.test.ts`,
      `bbj-vscode/test/functional/issue447-real-interop.test.ts`,
      `bbj-vscode/test/functional/issue440-real-interop.test.ts`,
      `bbj-vscode/test/bbj-test-module.ts` — the `JavaInteropTestService` double
    - `CLAUDE.md` §Architecture — its claims about `java-interop.ts` are a D8 target checkable against
      the code just read
  </read_first>
  <action>
Fill the remaining three `RU-61-06` cell lines and append any resulting records. Touch no other section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead code,
tangled coupling, inconsistent patterns and missing abstractions. Concrete checks: the size and
responsibility count of `java-interop.ts` at 955 lines against the four-file unit; whether the
request-sending paths repeat the same connect/await/error shape; whether `java-javadoc.ts`'s singleton
`JavadocProvider` is reachable and testable; whether `lib/bbj-api.ts` at 12 lines earns its own module;
and whether the two hardcoded package lists (implicit imports, auto-import candidates) belong in this
file.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). This unit owns the
routing-table item **11 `test/linking.test.ts` "Interop related tests" failures**, per the ownership map
in §"Artifacts this phase produces". Record it as a `P61-D5-{seq}` finding with full evidence: the test
names, the shared root cause established in INVENTORY's baseline, and the established environment fact
that bringing a java-interop peer up on port 5008 does **not** fix them — a D5 finding here must not
propose starting the service as the fix. Cross-reference D2 in `secondary:`. Its `dedup:` field records
that no frozen open issue matches and names the owning DEBT requirement where one applies, so Phase 66
re-triages rather than re-derives; the field is never blank. Also assess what is genuinely untested:
socket error and close paths, timeout paths, malformed-response handling, and whether the
`JavaInteropTestService` double diverges from the real client in ways that would hide a defect. Note in
`### Cross-unit referrals` that the linker is the tests' subject but this unit owns the item, so
`RU-61-02` (plan 61-04) does not re-record it.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
claims contradicted by code. Concrete checks: every JSDoc block in the four files against what the code
does — including the doc comment on the connection-config setter and the comments describing the
unresolved-reference-means-interop-unavailable contract; and CLAUDE.md's §Architecture description of
`java-interop.ts` and its DI service-group list against the code just read.

For each of the three cells write `pass` plus a written line naming the concrete checks applied, or
`fail` plus the IDs of the findings recorded. Continue finding-ID allocation monotonically from the
highest sequence already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 6 live `RU-61-06` cells now carry a verdict: `awk '/^## RU-61-06 /,/^## RU-61-01 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `6`
    - The two `n/a` cells are untouched and still verbatim: `awk '/^## RU-61-06 /,/^## RU-61-01 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[67] .*n/a — '` prints `2`
    - No other unit's section was filled: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `6` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `44`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - The routed 11-failure item is recorded as a D5 finding in this unit: `awk '/^## RU-61-06 /,/^## RU-61-01 /' .planning/reviews/61-COVERAGE.md | grep -cE '^id:[[:space:]]+P61-D5-[0-9]{3}'` ≥ 1 and that section contains the string `linking.test.ts`
    - Finding IDs remain unique and correctly formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing
    - Every finding still carries every required field: the counts of `^id:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is located in the excluded Java service: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0`
    - Every `pass` line remains a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - No source file was modified: `git status --porcelain bbj-vscode bbj-intellij java-interop` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-06 /,/^## RU-61-01 /" "$f" > /tmp/p61ru06.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p61ru06.txt)" = 6; test "$(grep -cE "^- D[67] .*n/a — " /tmp/p61ru06.txt)" = 2; test "$(grep -cE " — (pass|fail) — " "$f")" = 6; test "$(grep -c " — pending" "$f")" = 44; test "$(grep -cE "^- .*n/a — " "$f")" = 38; grep -q "linking.test.ts" /tmp/p61ru06.txt; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop)"; echo RU61_06_COMPLETE'</automated>
  </verify>
  <done>`RU-61-06` is complete under the stopping rule: all 6 live cells carry a verdict plus a written
  check line, all 4 files are named inside the section, the routed 11-failure item is recorded with
  evidence and a DEBT cross-reference, and every unpromoted claim is visible under
  `### Not-reproducible dispositions`.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the recording shape before expansion (D-05)</name>
  <decision>
    The shape of a `61-COVERAGE.md` unit section — cell-line format, the written-pass-line wording, the
    `n/a` carry-forward presentation, the finding-record rendering, and the four sub-blocks
    (`### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`, and for this unit
    `### SEC-06 Trust Boundary`). This is a review of the **shape**, not of the findings — finding-level
    triage stays in Phases 67-69.
  </decision>
  <context>
    Six downstream plans and Phase 68's DOC-03 concatenation inherit this shape (D-03). Changing it after
    plan 61-02 has run forces every already-recorded unit to be re-recorded, so this checkpoint sits at
    the only point where correcting it is cheap (D-05). Read the rendered `## RU-61-06` section of
    `.planning/reviews/61-COVERAGE.md` and the grid above it.
  </context>
  <options>
    <option id="approve">
      <name>Approve the shape as rendered — plans 61-02..61-07 copy it unchanged</name>
      <pros>No re-recording cost; DOC-03 gets one uniform shape across all 7 units.</pros>
      <cons>Any wording weakness is inherited by six more units.</cons>
    </option>
    <option id="revise">
      <name>Revise the shape, then re-render `RU-61-06` before plan 61-02 runs</name>
      <pros>Corrections cost one unit instead of seven; the grid header and the 38 `n/a` carry-forwards are unaffected by wording changes below them.</pros>
      <cons>Costs one extra pass over the already-swept unit.</cons>
    </option>
  </options>
  <resume-signal>Select: approve, or revise and describe the change</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| java-interop peer on `interopHost:interopPort` → language-server process | An unauthenticated JSON-RPC channel over a raw TCP socket; every response field crosses into the AST, hover text and completion items. This is the SEC-06 boundary the plan exists to document. |
| IDE workspace settings → the socket destination | `initializationOptions.interopHost`/`interopPort` (`bbj-ws-manager.ts`) and the settings-change path (`main.ts`) choose where the client connects; workspace-scoped settings travel inside a cloned repository. |
| `java-interop/` Java service → this review's scope | Read as reference material to establish the wire contract; excluded from all `location:` fields and from every coverage cell (D-13, FUT-01). |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed D1 finding becomes world-readable the moment the record lands. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `61-COVERAGE.md` → six downstream plans | One file, seven writers; a rewrite outside a plan's own section silently destroys another unit's recorded coverage. |

## STRIDE Threat Register

Rows `T-61-P01-S*` are **subject-matter** threats — the boundary this plan must document. Their
disposition is `transfer`: Phase 61 records and evidences, Phase 67 fixes and Phase 69 files. Rows
`T-61-P01-0*` are threats to this plan's own execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P01-S1 | Spoofing | `net.Socket` channel to `interopHost:interopPort` (`java-interop.ts` connect path) | high | transfer | Task 1 §SEC-06 heading (2) states the authentication posture as a flat fact with its `file:line` — whether any handshake, token, TLS or peer identity check exists in either direction. Any process able to bind the port before the real service impersonates it; ASVS L1 expects the posture to be known, not assumed. Remediation transfers to Phase 67/69. |
| T-61-P01-S2 | Tampering | JSON-RPC response fields → `JavaClass`/`JavaField`/`JavaMethod`/`JavaPackage`/`DocumentationInfo` AST nodes and hover/completion markdown | high | transfer | Task 1 §SEC-06 headings (1) and (4) enumerate every field the peer controls and what the client does with it unvalidated, including hostile strings reaching IDE-rendered markdown. Recorded as `P61-D1-*` findings where evidence clears tier `repro`. |
| T-61-P01-S3 | Elevation of Privilege | `setConnectionConfig` fed from `initializationOptions` / `didChangeConfiguration` | high | transfer | Task 1 §SEC-06 heading (3) must answer whether a workspace-scoped IDE setting shipped inside a cloned repository can redirect the client off loopback, and whether the port value is validated or coerced (non-integer, negative, out-of-range, string-typed). Merely opening a hostile repository must not silently become an outbound connection to an attacker-chosen host. |
| T-61-P01-S4 | Denial of Service | connect timeout, per-request timeout race, and request paths with no timeout at all | medium | transfer | Task 1 §SEC-06 heading (5) records behavior exactly at and one step either side of each threshold, plus the cost of a peer that completes the TCP handshake and never answers. Task 1 D2/D3 cells cover leaked sockets, un-cleared timers and unbounded caches. |
| T-61-P01-S5 | Information Disclosure | `lib/fs-provider.ts` `bbjlib` virtual filesystem and `JavadocProvider` content loading | medium | transfer | Task 1 D1 checks whether the provider can serve anything beyond its four fixed catalog paths and what `JavadocProvider` accepts from disk or peer. |
| T-61-P01-01 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | high | mitigate | D-12 two-tier disclosure: the committed record names the surface, the problem class and the impact, and states that the required runnable repro (D-11) exists and what it establishes — no trigger sequence, no payload, no proof-of-concept. Everything else carries full detail. Phase 69 decides disclosure at filing time. |
| T-61-P01-02 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or any D1 primary dimension regardless of edit size. Every record writes all six tests pass/fail, and the acceptance criteria require a `classification:` line on every finding. |
| T-61-P01-03 | Tampering | seven plans writing one `61-COVERAGE.md` | medium | mitigate | Ordering is structural, not conventional: one plan per wave, waves 1-7, `depends_on` chained (D-04). Each plan asserts the exact global counts of recorded verdicts, remaining placeholders and carried-forward `n/a` lines, so any write outside its own section fails its own verify gate. |
| T-61-P01-04 | Repudiation | a coverage claim with no evidence behind it | medium | mitigate | RVW-06 drop-vs-disposition: an unverifiable claim is never recorded as a finding but is written under `### Not-reproducible dispositions` with its reason. D-06 requires every `pass` cell to carry a written line naming the concrete checks applied; the acceptance criteria reject bare verdicts by length. |
| T-61-P01-05 | Tampering | reading `java-interop/` as reference material | low | mitigate | D-13 prohibition plus an anchored acceptance grep: no `location:` field may resolve inside `java-interop/`. Java-side observations route to `.planning/BACKLOG.md` under `FUT-01`, never into `61-COVERAGE.md`. |
| T-61-P01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and `diff` only. The package legitimacy gate is not applicable and no RESEARCH.md audit table is required. |
</threat_model>

<verification>
- `.planning/reviews/61-COVERAGE.md` exists, records the swept branch and full SHA, and carries all 88
  cell lines (56 unit-row + 32 file-exception).
- All 38 `n/a` cells carry INVENTORY's marker text verbatim; the four anchor sentences grep clean.
- The D-17 gate command re-derives `50 38 88` from INVENTORY, and both the command and its output are
  recorded in the file.
- `RU-61-06`'s 6 live cells all carry a verdict plus a written check line; its 2 `n/a` cells are
  untouched; all 4 of its files are named inside its section.
- `### SEC-06 Trust Boundary` answers all six named questions with `file:line` anchors.
- Every finding record has all 13 fields, a unique well-formed `P61-D{n}-{seq}` ID, a non-blank
  `dedup:`, and a `location:` outside `java-interop/`.
- Exactly 6 verdicts are recorded phase-wide and 44 placeholders remain, proving no other section was
  written.
- `git status --porcelain bbj-vscode bbj-intellij java-interop` is empty and INVENTORY.md is unmodified.
</verification>

<success_criteria>
- RVW-01 is opened: 1 of 7 units is swept across all 6 live dimensions with file-granular coverage.
- SEC-06 is satisfied: `java-interop.ts`'s trust boundary is documented — what a malicious or
  unresponsive peer on the configured host/port can do to the language server, and whether the channel
  is authenticated (ROADMAP Phase 61 criterion 2).
- ROADMAP Phase 61 criteria 3 and 4 hold for every record written here: `file:line`, dimension, verified
  failure scenario, and a dedup check against the frozen 15.
- A plan 61-02..61-07 executor can fill its unit section by copying this one, with no clarifying question.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-01-SUMMARY.md` when done.
</output>
