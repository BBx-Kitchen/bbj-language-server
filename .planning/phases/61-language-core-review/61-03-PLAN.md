---
phase: 61-language-core-review
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - .planning/reviews/61-COVERAGE.md
autonomous: true
requirements: [RVW-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-61-03` (validation & BBjCPL diagnostics, 8 files / 2,542 LOC — the largest unit by LOC in Phase 61) has all 6 live dimensions recorded in `61-COVERAGE.md` — D1, D2, D3 at evidence tier `repro` and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-06, D-07)"
    - "All 8 files — `bbj-validator.ts`, `bbj-document-validator.ts`, `validations/check-classes.ts`, `validations/check-function-calls.ts`, `validations/check-variable-scoping.ts`, `validations/line-break-validation.ts`, `bbj-cpl-service.ts`, `bbj-cpl-parser.ts` — are named at least once inside the `## RU-61-03` section, so coverage is file-granular (ROADMAP Phase 61 criterion 1)"
    - "The external-process boundary in `bbj-cpl-service.ts` is assessed under D1 as part of this unit — whether user-controlled paths, classpath values or config settings reach the spawned BBjCPL compiler as arguments without quoting or validation — and any finding is cross-referenced to SEC-05, which Phase 65 owns, rather than deferred to it"
    - "Every recorded finding carries `file:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #466 and #90 checked explicitly as this unit's plausible neighbours (RVW-07, ROADMAP Phase 61 criterion 4)"
    - "The unit's 2 `n/a` cells still carry INVENTORY's marker text verbatim and the phase-wide carried-forward `n/a` line count is still 38 (D-03)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): a finding's `location:` decides which unit owns it, not which unit discovered it. Validation code reads scope, linking and type-inference results owned by `RU-61-02`; where the defect is in the consumed service rather than in the check, it goes into `### Cross-unit referrals` naming `RU-61-02`, never recorded here and never written into another unit's section. Every referral already addressed to `RU-61-03` is promoted or dismissed here with a written reason"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): a dimension that produced zero findings records `pass` plus a written line naming the concrete checks applied, never a bare `pass` (D-06); a sub-block with nothing in it keeps the literal text `_(none recorded)_`"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): this plan appends only inside `## RU-61-03`, in wave 3 behind `depends_on: [61-02]` so the shared-file order is structural rather than conventional (D-04), and it continues finding sequence numbers from the highest `P61-D{n}-{seq}` already present for each dimension"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): owned by `RU-61-06` in plan 61-01. The BBjCPL process boundary assessed here is a **different** boundary and is recorded on its own terms under D1, not folded into SEC-06"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): surfaced and dismissed in plan 61-01 — no rounding/tie-breaking contract exists in a review sweep; its applicable remnant belongs to `RU-61-06`"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-03 — Validation & BBjCPL diagnostics` — 6 live cells recorded, 2 `n/a` cells untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-61-03` cells -> the phase-wide D-17 cell-total gate re-run by plan 61-07"
    - "`RU-61-03` D1 findings on the BBjCPL spawn -> SEC-05, which Phase 65 audits across both IDEs"
    - "`RU-61-03` findings -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records; Phase 67 fixes and Phase 66 resolves DEBT"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53` as a live finding — the fix lands in this unit's visibility-hierarchy walk and is already on this branch, merely unreleased (D-16)"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` or inside `bbj-vscode/src/language/generated/`"
    - "MUST NOT read any file under `.planning/codebase/` — `CONCERNS.md` lists FIXMEs already resolved in v3.8 and would produce false findings against exactly this unit (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — immutable for v4.0 (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-03`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding in this public repository (D-12)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-61-03` — Validation & BBjCPL diagnostics** (`bbj-validator.ts` 566,
`bbj-document-validator.ts` 271, `validations/check-classes.ts` 549,
`validations/check-function-calls.ts` 196, `validations/check-variable-scoping.ts` 343,
`validations/line-break-validation.ts` 318, `bbj-cpl-service.ts` 236, `bbj-cpl-parser.ts` 63 — 8 files,
2,542 LOC, INVENTORY risk rank 3 of 7) across its 6 live dimensions and record the result in the
`## RU-61-03` section of `.planning/reviews/61-COVERAGE.md`.

Purpose: this is the largest unit by LOC in the phase and the only one besides `RU-61-06` that crosses a
process boundary — `bbj-cpl-service.ts` spawns the external BBjCPL compiler. Its diagnostics are what a
user sees on every save.

Output: 6 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**.

**Modified**
- `.planning/reviews/61-COVERAGE.md` — section `## RU-61-03 — Validation & BBjCPL diagnostics` only.

**Finding-ID ranges.** `P61-D{dimension}-{seq}`, zero-padded to 3 digits, allocated monotonically in
discovery order within each `(61, dimension)` pair **across the whole phase**. This plan continues from
the highest sequence already present for each dimension after plans 61-01 and 61-02. `P00-*` is never
allocated.

**Plan → review-unit map.** This plan owns `RU-61-03`. The full 7-plan map, the routing-table ownership
map and the section shape live in `61-01-PLAN.md` §"Artifacts this phase produces".

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption | `must_haves.truths`, this plan |
| 2 | RVW-01 | empty | authored as flagged assumption | `must_haves.truths`, this plan |
| 3 | RVW-01 | ordering | authored as flagged assumption | `must_haves.truths`, this plan |
| 4 | SEC-06 | boundary | authored as flagged assumption | plan 61-01 (`RU-61-06` owns SEC-06) |
| 5 | SEC-06 | precision | flagged, dismissed with written reason | plan 61-01 |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<!-- planner-discipline-allow: bbj-cpl-service.ts -->
<!-- `bbj-cpl-service.ts` is asserted POSITIVELY, not negatively: the D1 criterion requires the D1 cell
     line in `61-COVERAGE.md` to name this file (count == 1), proving the process boundary was assessed
     rather than skipped. The plan's <action> must name the file for the executor to review it, and the
     file this plan writes is the coverage record, not this plan. No negative gate exists on this literal. -->
<task type="auto">
  <name>Task 1: Sweep RU-61-03 at evidence tier `repro` — D1, D2, D3</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists, contains a `## RU-61-03 — ` heading whose 6
  live cell lines are still unrecorded, and plan 61-02 has committed (phase-wide verdict count is 12).
  Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — the header, the stopping rule, the write contract, the
      completed `## RU-61-06` section as the shape to copy, and any `### Cross-unit referrals` entry
      addressed to `RU-61-03`
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Phase 61 review units" (`RU-61-03` block), §"Routing table (D-06)", and
      §"Pinned Baseline Range" — the `a7e1b53` do-not-re-report rule matters most in this unit
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3 wording; RVW-01, RVW-06, RVW-07; SEC-05,
      which Phase 65 owns and this unit cross-references
    - **Review targets, read in full:** `bbj-vscode/src/language/bbj-validator.ts` (566),
      `bbj-vscode/src/language/bbj-document-validator.ts` (271),
      `bbj-vscode/src/language/validations/check-classes.ts` (549),
      `bbj-vscode/src/language/validations/check-function-calls.ts` (196),
      `bbj-vscode/src/language/validations/check-variable-scoping.ts` (343),
      `bbj-vscode/src/language/validations/line-break-validation.ts` (318),
      `bbj-vscode/src/language/bbj-cpl-service.ts` (236),
      `bbj-vscode/src/language/bbj-cpl-parser.ts` (63)
    - `CLAUDE.md` §Architecture — the Validation bullet, which names only three of the four
      `validations/` modules and omits `check-function-calls.ts`; treat as a D8 candidate to confirm in
      Task 2, not as an asserted finding
  </read_first>
  <action>
Record the D1, D2 and D3 cell lines of `## RU-61-03 — Validation & BBjCPL diagnostics`, replacing the
placeholder verdict on each with `pass` or `fail` plus a written line naming the concrete checks
applied. Append any resulting finding records under that section's `### Findings`. Touch no other
section.

*D1 Security* (tier `repro`). Assess against injection, untrusted input, secret exposure, integrity gaps
and privilege/trust-boundary errors. `bbj-cpl-service.ts` spawns the external BBjCPL compiler process,
which makes this unit the only Phase 61 surface besides the java-interop client that crosses a process
boundary. Concrete checks: how the command line is assembled — whether a user-controlled document path,
a workspace path, a classpath value or a configured setting reaches the spawn as an argument without
quoting, validation or an argument-array form; whether a shell is interposed; whether the spawned
process inherits environment values that carry secrets; whether temporary files written for the
compiler are created with predictable names or in a world-writable location; and whether
`bbj-cpl-parser.ts` can be driven by hostile compiler output into producing diagnostics that carry
attacker-chosen text into the IDE. Record any such finding here — the file belongs to this unit — and
name SEC-05 in the record so Phase 65's cross-cutting audit can pick it up rather than re-derive it.

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource leaks.
Concrete checks: `bbj-cpl-parser.ts` at 63 lines against malformed, truncated, empty and
unexpected-locale compiler output, including the case where the compiler writes nothing at all; the
lifecycle in `bbj-cpl-service.ts` when the process fails to start, exits non-zero, or never exits —
whether the promise settles, whether the child is reaped, whether a second validation of the same
document races the first; `bbj-document-validator.ts` for exceptions caught and discarded rather than
surfaced; each of the four `validations/*` modules for null propagation on partially-linked ASTs, where
a reference is unresolved because java-interop is unavailable rather than because the code is wrong; and
`line-break-validation.ts` for off-by-one behavior at CRLF, at the final line, and on an empty document.
Each claim needs a runnable reproduction — a fixture plus the exact `npx vitest run` command — or a
line-by-line trace naming the concrete input and the exact `file:line` where behavior diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks, missing
caches or debounces, redundant AST traversals and unbounded memory growth. Validation runs on every
document change. Concrete checks: whether each registered check re-walks the AST independently rather
than sharing one traversal; whether `check-classes.ts` at 549 lines walks the class hierarchy once per
member or once per class; whether any validation is quadratic in symbol count; whether BBjCPL runs are
debounced or one process is spawned per keystroke; and whether diagnostics or parsed compiler output
accumulate without bound across a long session.

For every claim that clears tier `repro`, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block. `unit:` is `RU-61-03`.
`classification:` records pass/fail for all six Phase 60 D-13 classification tests — a `critical`/`high` severity or a D1
primary dimension forces `major`. `dedup:` is checked against the frozen 15 — #466 (sibling-type method
return mismatches, labelled types and validation) and #90 (opting files or regions out of linking) are
this unit's plausible neighbours and must both be checked explicitly; a match records `#NNN duplicate`
or `#NNN partial-overlap` with what the finding adds, no match records `none`, and the field is never
blank. A `critical`/`high` D1 finding additionally carries a runnable reproduction (D-11) and its
committed record names the surface, the problem class and the impact only, stating that the repro exists
and what it establishes without the trigger sequence, the payload or a proof-of-concept (D-12).

Do not re-report the cyclic-inheritance hang: `a7e1b53` already terminates the visibility hierarchy walk
on cyclic inheritance on this branch, and that walk lives in this unit. Confirm the fix is present in
the code you are reading, and if a *different* unbounded walk exists, record that one on its own
evidence rather than restating the fixed issue.

Any claim that does not clear tier `repro` goes under `### Not-reproducible dispositions` with the tier
it failed and why. Any defect whose `file:line` belongs to another unit goes under
`### Cross-unit referrals` naming that unit.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier cells are recorded: `awk '/^## RU-61-03 /,/^## RU-61-02 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[123] .* — (pass|fail) — '` prints `3`
    - The D1 cell names the process boundary it assessed: `awk '/^## RU-61-03 /,/^## RU-61-02 /' .planning/reviews/61-COVERAGE.md | grep -E '^- D1 ' | grep -c 'bbj-cpl-service.ts'` prints `1`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `15` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `35`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0` and `grep -cE '^location:[[:space:]]+bbj-vscode/src/language/generated/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-03 /,/^## RU-61-02 /" "$f" > /tmp/ru03.txt; test "$(grep -cE "^- D[123] .* — (pass|fail) — " /tmp/ru03.txt)" = 3; test "$(grep -E "^- D1 " /tmp/ru03.txt | grep -c "bbj-cpl-service.ts")" = 1; test "$(grep -cE " — (pass|fail) — " "$f")" = 15; test "$(grep -c " — pending" "$f")" = 35; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/src/language/generated/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_03_REPRO_OK'</automated>
  </verify>
  <done>`RU-61-03`'s D1, D2 and D3 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — the BBjCPL spawn's argument handling and hostile-output handling for D1,
  compiler-output parsing and process-lifecycle edge cases for D2, per-keystroke validation cost for D3.
  Every promoted claim is a complete 13-field record whose dedup check explicitly covers #466 and #90.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-03 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`## RU-61-03`'s D1, D2 and D3 cells already carry verdicts — Task 1 committed. Halt
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` §`## RU-61-03` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot", §"Test & Build Baseline"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows
    - **Review targets:** the same 8 files as Task 1
    - **Test surface:** `bbj-vscode/test/validation.test.ts`,
      `bbj-vscode/test/variable-scoping.test.ts`, and any `bbj-vscode/test/` file exercising the CPL
      service or line-break validation — enumerate with `ls bbj-vscode/test/` and
      `grep -rl 'check-classes\|check-function-calls\|check-variable-scoping\|line-break\|cpl' bbj-vscode/test/`
    - `CLAUDE.md` §Architecture — the Validation bullet lists `check-classes.ts`,
      `check-variable-scoping.ts` and `line-break-validation.ts` but not `check-function-calls.ts`; this
      is the named D8 candidate to confirm or dismiss with evidence
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-61-03` and append any resulting records. Touch no other
section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead code,
tangled coupling, inconsistent patterns and missing abstractions. Concrete checks: repeated
hierarchy-walk, symbol-lookup and diagnostic-emission code across the four `validations/*` modules;
whether `bbj-validator.ts` at 566 lines is a registration surface or has grown behavior of its own;
function length and branch depth in `check-classes.ts` at 549 lines; whether the two CPL modules share a
coherent boundary or leak parsing concerns into the service; and whether the four checks follow one
consistent registration and severity-selection pattern or three different ones. The code shape is the
evidence — name it and the `file:line`.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). Concrete checks: which
of the four `validations/*` modules have a dedicated test file and which are only exercised
incidentally; whether `bbj-cpl-service.ts` and `bbj-cpl-parser.ts` have any test at all, given they
require an external compiler; whether malformed-compiler-output handling is tested; and whether each
diagnostic this unit can emit has a regression test naming it. Record gaps as `P61-D5-{seq}` findings
with the untested branch named by `file:line`. Do not record the `beforeAll` hookTimeout flakiness here
— it is owned by `RU-61-05` in plan 61-06; if a suite covering this unit is among those it intermittently
strikes, note it under `### Cross-unit referrals` instead. Do not propose "run the BBj service" or "start
java-interop" as the fix for an environment-dependent failure: the established environment fact is that
bringing a peer up on port 5008 does not resolve those failures.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
claims contradicted by code. Concrete checks: every comment and JSDoc block across the 8 files against
what the code does, with particular attention to comments describing suppression or severity rules that
may have moved; and CLAUDE.md's §Architecture Validation bullet — it names three `validations/` modules
and omits `check-function-calls.ts`, and it names `bbj-document-validator.ts` as "document-level
validation with BBjCPL compiler integration". Verify both against the tree and the code. Record a
divergence as a finding with the exact CLAUDE.md line in `location:`; record the absence of a divergence
as part of the D8 written pass line. Do not assert the omission as a finding without confirming it
against the current file list.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 6 live cells now carry a verdict: `awk '/^## RU-61-03 /,/^## RU-61-02 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `6`
    - The 2 `n/a` cells are untouched: `awk '/^## RU-61-03 /,/^## RU-61-02 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[67] .*n/a — '` prints `2`
    - Every file in the unit is named inside its own section: `awk '/^## RU-61-03 /,/^## RU-61-02 /' .planning/reviews/61-COVERAGE.md > /tmp/ru03.txt; for f in bbj-validator.ts bbj-document-validator.ts check-classes.ts check-function-calls.ts check-variable-scoping.ts line-break-validation.ts bbj-cpl-service.ts bbj-cpl-parser.ts; do grep -q "$f" /tmp/ru03.txt || echo "MISSING $f"; done` prints nothing
    - The named D8 candidate was resolved rather than ignored: `/tmp/ru03.txt` contains `CLAUDE.md`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `18` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `32`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - All three sub-blocks are present for this unit: `/tmp/ru03.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-03 /,/^## RU-61-02 /" "$f" > /tmp/ru03.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/ru03.txt)" = 6; test "$(grep -cE "^- D[67] .*n/a — " /tmp/ru03.txt)" = 2; for x in bbj-validator.ts bbj-document-validator.ts check-classes.ts check-function-calls.ts check-variable-scoping.ts line-break-validation.ts bbj-cpl-service.ts bbj-cpl-parser.ts CLAUDE.md "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/ru03.txt; done; test "$(grep -cE " — (pass|fail) — " "$f")" = 18; test "$(grep -c " — pending" "$f")" = 32; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_03_COMPLETE'</automated>
  </verify>
  <done>`RU-61-03` is complete under the stopping rule: all 6 live cells carry a verdict plus a written
  check line, all 8 files are named inside the section, CLAUDE.md's Validation claims are checked against
  the tree, and every unpromoted claim is visible under `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server → spawned BBjCPL compiler process | `bbj-cpl-service.ts` crosses a process boundary with an assembled command line; user-controlled document paths, workspace paths, classpath values and configured settings are candidate inputs to it. |
| BBjCPL process output → IDE diagnostics | `bbj-cpl-parser.ts` turns external process output into diagnostics rendered in the editor. |
| any opened `.bbj` file → the four `validations/*` checks | Validation runs on untrusted document content on every change. |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `61-COVERAGE.md` → the other six plans | One file, seven writers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P03-S1 | Elevation of Privilege | command-line assembly in `bbj-cpl-service.ts` | high | transfer | Task 1's D1 check asks whether a user-controlled path, classpath value or configured setting reaches the spawn without quoting, validation or an argument-array form, and whether a shell is interposed. Recorded as a `P61-D1-*` finding where evidence clears tier `repro`; SEC-05 named in the record so Phase 65 picks it up. Remediation transfers to Phase 67. |
| T-61-P03-S2 | Tampering | compiler output → diagnostics via `bbj-cpl-parser.ts` | medium | transfer | Task 1's D1 and D2 checks cover hostile, malformed, truncated and empty output, including attacker-chosen text reaching the IDE as a diagnostic. |
| T-61-P03-S3 | Denial of Service | validation cost per keystroke and per-change BBjCPL spawns | medium | transfer | Task 1's D3 check asks whether checks share one AST traversal, whether the hierarchy walk is quadratic, and whether BBjCPL runs are debounced or spawned per keystroke. |
| T-61-P03-S4 | Information Disclosure | environment and temporary files handed to the spawned compiler | medium | transfer | Task 1's D1 check covers inherited environment values carrying secrets and predictable or world-writable temporary file paths. |
| T-61-P03-01 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | high | mitigate | D-12 two-tier disclosure: surface, problem class and impact only; the required runnable repro (D-11) exists and is described by what it establishes, without the trigger sequence, the payload or a proof-of-concept. |
| T-61-P03-02 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; every record writes all six tests pass/fail. |
| T-61-P03-03 | Tampering | seven plans writing one `61-COVERAGE.md` | medium | mitigate | Wave 3 behind `depends_on: [61-02]` makes ordering structural (D-04); both tasks assert exact global counts (15/35, then 18/32) plus the invariant 38 carried-forward `n/a` lines. |
| T-61-P03-04 | Repudiation | re-reporting the already-fixed cyclic-inheritance hang | medium | mitigate | Explicit prohibition plus an action instruction to confirm `a7e1b53`'s termination guard is present in the code being read; a different unbounded walk may be recorded only on its own evidence. `.planning/codebase/CONCERNS.md`, which lists v3.8-resolved FIXMEs against exactly this unit, is unreadable for this phase (D-16). |
| T-61-P03-05 | Repudiation | a coverage claim with no evidence behind it | medium | mitigate | RVW-06 drop-vs-disposition plus D-06's written-check-line requirement; acceptance criteria reject bare `pass` verdicts by length and require all 8 unit files to be named. |
| T-61-P03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk`, `ls` and the already-installed `npx vitest run` harness. |
</threat_model>

<verification>
- `## RU-61-03`'s 6 live cells each carry `pass` or `fail` plus a written check line ≥ 80 characters.
- The D1 cell names `bbj-cpl-service.ts` explicitly, so the process boundary was assessed rather than
  skipped.
- All 8 unit files are named inside the section; CLAUDE.md's Validation claims are addressed.
- Phase-wide verdict count is 18 and placeholder count is 32; the carried-forward `n/a` count is 38.
- Every finding has all 13 fields, a unique well-formed ID, and a non-blank `dedup:` that explicitly
  covered #466 and #90.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-01 advances: 3 of 7 units swept, 18 of 50 `applies` cells recorded.
- The BBjCPL process boundary has a recorded D1 verdict with named checks, giving Phase 65's SEC-05 audit
  a reviewed baseline instead of a blank surface.
- ROADMAP Phase 61 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-03-SUMMARY.md` when done.
</output>
