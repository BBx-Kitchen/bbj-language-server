---
phase: 61-language-core-review
plan: 06
type: execute
wave: 6
depends_on: [61-05]
files_modified:
  - .planning/reviews/61-COVERAGE.md
autonomous: true
requirements: [RVW-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-61-05` (server lifecycle, DI wiring & workspace management, 9 files / 1,433 LOC) has all 6 live dimensions recorded in `61-COVERAGE.md` — D1, D2, D3 at evidence tier `repro` and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-06, D-07)"
    - "All 9 files — `main.ts`, `bbj-module.ts`, `bbj-ws-manager.ts`, `bbj-document-builder.ts`, `bbj-notifications.ts`, `logger.ts`, `constants.ts`, `utils.ts`, `composer-commands.ts` — are named at least once inside the `## RU-61-05` section, including the 1-line `constants.ts` and the 0-line `utils.ts`, so coverage is file-granular (ROADMAP Phase 61 criterion 1)"
    - "The routing-table item this unit owns — the `beforeAll` `WorkspaceManager.initializeWorkspace()` hookTimeout flakiness that strikes a different suite on each run — is recorded as a `P61-D5-*` finding against the initialization path by `file:line`, not merely as a test-runner observation (D-14)"
    - "Every cross-unit referral addressed to `RU-61-05` by an earlier plan — in particular anything plan 61-01 raised about `bbj-ws-manager.ts` or `main.ts` while documenting the SEC-06 boundary — is either promoted to a finding here or dismissed here with a written reason; none is silently dropped"
    - "The INVENTORY-flagged dead/vestigial-module candidate on `constants.ts` and `utils.ts` is confirmed or dismissed with evidence, not asserted and not ignored"
    - "Every recorded finding carries `file:line`, a primary dimension, evidence clearing its tier, and a non-blank `dedup:` checked against the 15 frozen open issues, with #33, #231, #385, #485 and #486 checked explicitly as this unit's plausible neighbours (RVW-06, RVW-07)"
    - "The unit's 2 `n/a` cells still carry INVENTORY's marker text verbatim and the phase-wide carried-forward `n/a` line count is still 38 (D-03)"

    # ~~ spec-less edge probe (no SPEC.md; all 5 rows unresolved -> flagged assumptions, never auto-resolved, never dropped)
    - "EDGE-PROBE [RVW-01/adjacency] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — when two workspace folders' PREFIX paths exactly coincide, or a document is reachable through two roots at once, does the workspace manager merge them, collide, or keep them separate? Separately, at the unit boundary: this unit owns the two call sites that configure the java-interop client, so a defect in how host/port are read and validated is a finding here, while a defect in what the client does with them is `RU-61-06`'s and stays a referral"
    - "EDGE-PROBE [RVW-01/empty] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — what happens on initialization with no workspace folder, with an empty `initializationOptions`, with a `bbjdir` that does not exist, and with a workspace containing zero `.bbj` files? Recording-wise, a dimension with zero findings records `pass` plus a written line naming the concrete checks applied (D-06), and the 0-line `utils.ts` is still swept and still named rather than skipped for being empty"
    - "EDGE-PROBE [RVW-01/ordering] (flagged assumption — no SPEC.md): translated into a concrete D2 check for this unit — is the order in which the document builder processes a batch specified and stable, and can two documents that compare equal be built in an order that changes the result? Recording-wise, this plan appends only inside `## RU-61-05`, in wave 6 behind `depends_on: [61-05]` (D-04), continuing finding sequence numbers from the highest already present per dimension"
    - "EDGE-PROBE [SEC-06/boundary] (flagged assumption — no SPEC.md): owned by `RU-61-06` in plan 61-01. This unit's share of that boundary is the input side — whether `interopHost` and `interopPort` arriving through `initializationOptions` and the settings-change path are validated before they are handed to the client — and it is recorded here as a D1 check rather than re-deriving the peer-side analysis"
    - "EDGE-PROBE [SEC-06/precision] (flagged assumption — no SPEC.md, DISMISSED-WITH-REASON): surfaced and dismissed in plan 61-01 — no rounding/tie-breaking contract exists in a review sweep. Its one applicable remnant, whether a non-integer, negative, out-of-range or string-typed `interopPort` is validated or silently coerced, is checked at the input side here and at the client side in plan 61-01, so it is recorded twice over rather than dropped"

  artifacts:
    - ".planning/reviews/61-COVERAGE.md §`## RU-61-05 — Server lifecycle, DI wiring & workspace management` — 6 live cells recorded, 2 `n/a` cells untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-61-05` cells -> the phase-wide D-17 cell-total gate re-run by plan 61-07"
    - "`RU-61-05` D5 finding on the initialization hookTimeout -> FIX-03's `npm test` clean gate in Phase 67"
    - "`RU-61-05` D1 checks on `initializationOptions` -> the SEC-06 boundary documented at `RU-61-06`"
    - "`RU-61-05` findings -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, path traversal,
    # injection, GDPR, generic fairness) deliberately dropped — they are review *subjects* here, not plan prohibitions.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 61 records; Phase 67 fixes and Phase 66 resolves DEBT"
    - "MUST NOT propose merging, inlining or removing `bbj-notifications.ts` — it is a deliberate isolation module, and importing `main.ts` from shared services crashes the test suite; that constraint is recorded in STATE.md and any D4 finding must respect it"
    - "MUST NOT assert the `constants.ts` / `utils.ts` dead-module candidate as a finding without evidence that its export is unreferenced — INVENTORY flags it for confirmation, not as an asserted finding"
    - "MUST NOT re-derive the java-interop peer-side analysis owned by `RU-61-06` — this unit records the input side only"
    - "MUST NOT record a finding whose `location:` is inside `java-interop/` or inside `bbj-vscode/src/language/generated/`"
    - "MUST NOT read any file under `.planning/codebase/` — superseded by INVENTORY.md (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — immutable for v4.0 (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-61-0N` section other than `RU-61-05`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-06)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-61-05` — Server lifecycle, DI wiring & workspace management** (`main.ts` 189,
`bbj-module.ts` 210, `bbj-ws-manager.ts` 293, `bbj-document-builder.ts` 412, `bbj-notifications.ts` 52,
`logger.ts` 68, `constants.ts` 1, `utils.ts` 0, `composer-commands.ts` 208 — 9 files, 1,433 LOC,
INVENTORY risk rank 6 of 7) across its 6 live dimensions and record the result in the `## RU-61-05`
section of `.planning/reviews/61-COVERAGE.md`.

Purpose: this unit is where every externally-supplied setting enters the language server —
`initializationOptions`, the settings-change path, `bbjdir`, classpath, `configPath` and the
interop host/port. It also owns the initialization path behind the phase's most-cited test flakiness,
and it is the unit every cross-unit referral about configuration lands in.

Output: 6 recorded cells with written check lines, every referral addressed to this unit resolved, plus
any finding records and not-reproducible dispositions. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/61-language-core-review/61-CONTEXT.md
@.planning/phases/61-language-core-review/61-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 61 creates **no source symbol and modifies no source file**.

**Modified**
- `.planning/reviews/61-COVERAGE.md` — section
  `## RU-61-05 — Server lifecycle, DI wiring & workspace management` only.

**Finding-ID ranges.** `P61-D{dimension}-{seq}`, zero-padded to 3 digits, allocated monotonically in
discovery order within each `(61, dimension)` pair **across the whole phase**. This plan continues from
the highest sequence already present for each dimension after plans 61-01 through 61-05. `P00-*` is
never allocated.

**Plan → review-unit map.** This plan owns `RU-61-05`. The full 7-plan map, the routing-table ownership
map and the section shape live in `61-01-PLAN.md` §"Artifacts this phase produces".

## Spec-less Edge-Probe Ledger (phase-level, 5 rows, 0 dropped)

| # | Requirement | Category | Disposition | Where |
|---|---|---|---|---|
| 1 | RVW-01 | adjacency | authored as flagged assumption, translated into a D2 coinciding-root check | `must_haves.truths`, this plan |
| 2 | RVW-01 | empty | authored as flagged assumption, translated into a D2 empty-workspace check | `must_haves.truths`, this plan |
| 3 | RVW-01 | ordering | authored as flagged assumption, translated into a D2 batch-order check | `must_haves.truths`, this plan |
| 4 | SEC-06 | boundary | authored as flagged assumption; input side checked here, peer side in plan 61-01 | `must_haves.truths`, this plan and plan 61-01 |
| 5 | SEC-06 | precision | flagged, dismissed with written reason; its applicable remnant (port coercion) checked here and in plan 61-01 | `must_haves.truths`, this plan and plan 61-01 |

Accounting: 4 authored + 1 flagged-and-dismissed = 5 surfaced = 5 probe rows. No silent drop.

<tasks>

<!-- planner-discipline-allow: bbj-ws-manager.ts -->
<!-- `bbj-ws-manager.ts` is asserted POSITIVELY, not negatively: the D1 criterion requires the D1 cell
     line in `61-COVERAGE.md` to name this file (count == 1), proving the settings-input surface was
     assessed rather than skipped. The plan's <action> must name the file for the executor to review it,
     and the file this plan writes is the coverage record, not this plan. No negative gate exists here. -->
<task type="auto">
  <name>Task 1: Sweep RU-61-05 at evidence tier `repro` — D1, D2, D3</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`.planning/reviews/61-COVERAGE.md` exists, contains a `## RU-61-05 — ` heading whose 6
  live cell lines are still unrecorded, and plan 61-05 has committed (phase-wide verdict count is 30).
  Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` — the header, the stopping rule, the write contract, the
      completed `## RU-61-06` section including its `### SEC-06 Trust Boundary`, and **every**
      `### Cross-unit referrals` entry addressed to `RU-61-05` across all previously filled sections
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot" — #33, #231, #385, #485 and #486 in particular, §"Phase 61 review units" (`RU-61-05`
      block, including the `constants.ts` / `utils.ts` note), §"Routing table (D-06)", §"Test & Build
      Baseline" §"Flaky suite-level failures"
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3 wording; RVW-01, RVW-06, RVW-07
    - `.planning/STATE.md` §"Active Constraints" — the `bbj-notifications.ts` isolation constraint
    - **Review targets, read in full:** `bbj-vscode/src/language/main.ts` (189),
      `bbj-vscode/src/language/bbj-module.ts` (210), `bbj-vscode/src/language/bbj-ws-manager.ts` (293),
      `bbj-vscode/src/language/bbj-document-builder.ts` (412),
      `bbj-vscode/src/language/bbj-notifications.ts` (52), `bbj-vscode/src/language/logger.ts` (68),
      `bbj-vscode/src/language/constants.ts` (1), `bbj-vscode/src/language/utils.ts` (0),
      `bbj-vscode/src/language/composer-commands.ts` (208)
    - `CLAUDE.md` §Architecture §"DI Module Pattern" — the `createBBjServices()` description and the
      four named custom service groups, a D8 target checkable against `bbj-module.ts`
  </read_first>
  <action>
Record the D1, D2 and D3 cell lines of `## RU-61-05 — Server lifecycle, DI wiring & workspace
management`, replacing the placeholder verdict on each with `pass` or `fail` plus a written line naming
the concrete checks applied. Resolve every cross-unit referral addressed to this unit. Append any
resulting finding records under that section's `### Findings`. Touch no other section.

**First, resolve inherited referrals.** Read every `### Cross-unit referrals` block already written in
`61-COVERAGE.md` and take every entry addressed to `RU-61-05` — in particular anything plan 61-01 raised
about `bbj-ws-manager.ts` or `main.ts` while documenting the SEC-06 boundary. Each is either promoted to
a finding in this unit's `### Findings` with its own evidence, or dismissed in this unit's
`### Cross-unit referrals` with a written reason. None may be left unaddressed.

*D1 Security* (tier `repro`). Assess against injection, untrusted input, secret exposure, integrity gaps
and privilege/trust-boundary errors. This unit is the input side of every externally-supplied setting.
Concrete checks: how `bbj-ws-manager.ts` handles `initializationOptions` — whether `bbjdir`,
`classpath`, `configPath`, `interopHost` and `interopPort` are validated for type, shape and range
before use, and specifically whether a non-integer, negative, out-of-range or string-typed port is
rejected or silently coerced; whether a workspace-scoped setting can direct the server to read a config
file or resolve a PREFIX outside the workspace root; how the settings-change path in `main.ts`
revalidates on update; whether `composer-commands.ts` builds any command, path or argument from
user-controlled values; whether `logger.ts` can emit a classpath, a token, a path or document content to
a user-visible or on-disk log at its default level; and whether `bbj-document-builder.ts` admits
documents from outside the workspace. Where a defect concerns what the java-interop client then does
with the host/port, that is `RU-61-06`'s and stays a referral — record the input side here.

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource leaks.
Three checks are required by name, because they are the phase's translated edge probes:
  - **Coinciding roots.** When two workspace folders' PREFIX paths coincide, or one document is
    reachable through two roots at once, does the manager merge, collide, or keep them separate? Note
    that issue #33 reports multi-root/workspace usage as broken — check the code against that report
    without assuming the report is accurate.
  - **Empty inputs.** Initialization with no workspace folder, with empty `initializationOptions`, with
    a `bbjdir` that does not exist, and with a workspace containing zero `.bbj` files.
  - **Batch order.** Whether `bbj-document-builder.ts` processes a batch in a specified, stable order,
    and whether two documents that compare equal can be built in an order that changes the result.
Beyond those three: unhandled rejections and swallowed exceptions on the initialization path; whether a
failed initialization leaves the server in a half-configured state that later requests silently use; and
whether `clearCache`-style reconfiguration paths in `main.ts` are safe to run concurrently with an
in-flight build. Each claim needs a runnable reproduction or a line-by-line trace naming the concrete
input and the exact `file:line` where behavior diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks, missing
caches, redundant traversals and unbounded memory growth. Concrete checks: the cost profile of
`WorkspaceManager.initializeWorkspace()`, which is the path behind the routed hookTimeout flakiness —
what it does synchronously, what it awaits, and what scales with workspace size; whether the workspace
scan is bounded or walks node_modules-scale trees; whether `bbj-document-builder.ts` rebuilds documents
that did not change; and whether any lifecycle structure — cached settings, document handles, listeners
— accumulates without bound across a long session.

For every claim that clears tier `repro`, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block. `unit:` is `RU-61-05`.
`classification:` records pass/fail for all six Phase 60 D-13 classification tests. `dedup:` is checked against the frozen 15 —
#33 (VS Code workspaces reported broken), #231 (custom classpath and command-line settings), #385
(launching the Graffiti Composer), #485 (custom-named or relocated config files) and #486 (watching
`config.bbx` to re-apply PREFIX/USE) are this unit's plausible neighbours and must all five be checked
explicitly; #231, #385, #485 and #486 are feature requests, so a finding about existing behavior is
`partial-overlap` at most with what it adds stated. No match records `none`; the field is never blank.

Any claim that does not clear tier `repro` goes under `### Not-reproducible dispositions` with the tier
it failed and why.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier cells are recorded: `awk '/^## RU-61-05 /,/^## RU-61-07 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[123] .* — (pass|fail) — '` prints `3`
    - The settings-input surface was assessed by name: `awk '/^## RU-61-05 /,/^## RU-61-07 /' .planning/reviews/61-COVERAGE.md > /tmp/ru05.txt; grep -E '^- D1 ' /tmp/ru05.txt | grep -c 'bbj-ws-manager.ts'` prints `1`
    - Every referral addressed to this unit is resolved: for each line elsewhere in the file that names `RU-61-05` inside a `### Cross-unit referrals` block, `/tmp/ru05.txt` contains either a finding whose `location:` names the same file or a written dismissal naming it — verified by listing referral targets with `grep -n 'RU-61-05' .planning/reviews/61-COVERAGE.md` and checking each against the section
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `33` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `17`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 80) print "SHORT"}' .planning/reviews/61-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/61-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+java-interop/' .planning/reviews/61-COVERAGE.md` prints `0` and `grep -cE '^location:[[:space:]]+bbj-vscode/src/language/generated/' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-05 /,/^## RU-61-07 /" "$f" > /tmp/ru05.txt; test "$(grep -cE "^- D[123] .* — (pass|fail) — " /tmp/ru05.txt)" = 3; test "$(grep -E "^- D1 " /tmp/ru05.txt | grep -c "bbj-ws-manager.ts")" = 1; test "$(grep -cE " — (pass|fail) — " "$f")" = 33; test "$(grep -c " — pending" "$f")" = 17; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+java-interop/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_05_REPRO_OK'</automated>
  </verify>
  <done>`RU-61-05`'s D1, D2 and D3 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — `initializationOptions` validation, PREFIX/config path scoping, logger
  exposure and command construction for D1; coinciding roots, empty workspaces and batch ordering for D2;
  the initialization path's cost profile and lifecycle accumulation for D3. Every referral addressed to
  this unit is promoted or dismissed with a written reason.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-61-05 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/61-COVERAGE.md</files>
  <precondition>`## RU-61-05`'s D1, D2 and D3 cells already carry verdicts — Task 1 committed. Halt
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/61-COVERAGE.md` §`## RU-61-05` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b, §3c, §"Finding Record Template", §"Frozen Open-Issue
      Snapshot", §"Test & Build Baseline" §"Flaky suite-level failures" — the three suites the
      `beforeAll` hook timeout struck across two runs and the 10-second default `hookTimeout` — and
      §"Routing table (D-06)"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows
    - `.planning/STATE.md` §"Active Constraints" — the `bbj-notifications.ts` isolation constraint,
      restated because a D4 duplication finding could otherwise propose exactly the merge that breaks it
    - **Review targets:** the same 9 files as Task 1
    - **Test surface:** `bbj-vscode/test/test-helper.ts` — the `initializeWorkspace()` helper —
      `bbj-vscode/test/functional/chevrotain-tokens.test.ts`,
      `bbj-vscode/test/run-call-file-resolution.test.ts`, `bbj-vscode/test/variable-scoping.test.ts`,
      and `bbj-vscode/vitest.config.ts` for the configured timeouts
    - Consumption evidence for the dead-module candidate: run
      `grep -rn "from './constants.js'\|from './utils.js'\|constants\.js\|utils\.js" bbj-vscode/src bbj-vscode/test`
      and record the result — this is the evidence that confirms or dismisses the candidate
    - `CLAUDE.md` §"DI Module Pattern" — the `createBBjServices()` claim and the four named custom
      service groups, checkable against `bbj-module.ts`
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-61-05` and append any resulting records. Touch no other
section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead code,
tangled coupling, inconsistent patterns and missing abstractions. This unit carries INVENTORY's flagged
**dead/vestigial-module candidate**: `constants.ts` at 1 line and `utils.ts` at 0 lines. Resolve it with
the consumption evidence gathered in `read_first` — if either module's export is referenced, dismiss the
candidate in the D4 written line and say what references it; if neither is, record a `P61-D4-{seq}`
finding naming both files, the reference count, and the exact edit that removes them. Do not assert it
without that evidence. Beyond that: whether `bbj-document-builder.ts` at 412 lines is one responsibility
or several; whether `main.ts` and `bbj-ws-manager.ts` duplicate settings-reading logic — both compute an
interop host and port with the same defaults at two call sites, which is a concrete duplication
candidate to assess; whether `bbj-module.ts`'s DI registration follows one consistent pattern; and
whether `composer-commands.ts` belongs in `src/language/` at all given the composers live in `src/`.
`bbj-notifications.ts` must not be proposed for merging or inlining — it is a deliberate isolation
module and importing `main.ts` from shared services crashes the test suite; if its existence looks like
a smell, record that constraint as the reason it is not one.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). This unit owns the
routing-table item **`beforeAll` `WorkspaceManager.initializeWorkspace()` hookTimeout flakiness**, which
struck `chevrotain-tokens.test.ts` and `run-call-file-resolution.test.ts` in one baseline run and
`variable-scoping.test.ts` in the next, marking whole suites failed with their tests reported skipped.
Record it as a `P61-D5-{seq}` finding against the initialization path itself, not merely as a
test-runner observation: name what `initializeWorkspace()` does that can exceed 10 seconds under load,
by `file:line`, and state both candidate remediations — reducing the work, or configuring the timeout —
without choosing one. Note that this flakiness is why the phase's skip counts vary between runs while
the 886 test total stays stable. Beyond it: whether `main.ts`, `bbj-module.ts`, `bbj-ws-manager.ts` and
`bbj-document-builder.ts` have direct tests or are exercised only incidentally through helpers; and
whether `composer-commands.ts` and `logger.ts` have any coverage. Do not propose starting the
java-interop service as a fix for an environment-dependent failure — that has been tried and does not
resolve those failures.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
claims contradicted by code. Concrete checks: every JSDoc block across the nine files against what the
code does; and CLAUDE.md's §"DI Module Pattern", which claims services are wired in `bbj-module.ts` via
`createBBjServices()` and names exactly four custom service groups — verify the function name, the group
names and the count against `bbj-module.ts`, and record any divergence as a finding with the exact
CLAUDE.md line in `location:`.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 6 live cells now carry a verdict: `awk '/^## RU-61-05 /,/^## RU-61-07 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `6`
    - The 2 `n/a` cells are untouched: `awk '/^## RU-61-05 /,/^## RU-61-07 /' .planning/reviews/61-COVERAGE.md | grep -cE '^- D[67] .*n/a — '` prints `2`
    - Every file in the unit is named inside its own section, including the 1-line and 0-line files: `awk '/^## RU-61-05 /,/^## RU-61-07 /' .planning/reviews/61-COVERAGE.md > /tmp/ru05.txt; for f in main.ts bbj-module.ts bbj-ws-manager.ts bbj-document-builder.ts bbj-notifications.ts logger.ts constants.ts utils.ts composer-commands.ts; do grep -q "$f" /tmp/ru05.txt || echo "MISSING $f"; done` prints nothing
    - The routed flakiness item is recorded as a D5 finding against the initialization path: `/tmp/ru05.txt` contains `initializeWorkspace` and `grep -cE '^id:[[:space:]]+P61-D5-[0-9]{3}' /tmp/ru05.txt` ≥ 1
    - The dead-module candidate is resolved with evidence: `/tmp/ru05.txt` contains both `constants.ts` and `utils.ts` in the D4 written line or in a finding, and the recorded reference count matches `grep -rc "constants\.js\|utils\.js" bbj-vscode/src bbj-vscode/test | awk -F: '{s+=$2} END{print s}'`
    - Phase-wide counts prove no other section was written: `grep -cE ' — (pass|fail) — ' .planning/reviews/61-COVERAGE.md` prints `36` and `grep -c ' — pending' .planning/reviews/61-COVERAGE.md` prints `14`
    - The 38 carried-forward `n/a` lines are intact: `grep -cE '^- .*n/a — ' .planning/reviews/61-COVERAGE.md` prints `38`
    - All three sub-blocks are present for this unit: `/tmp/ru05.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P61-D[1-8]-[0-9]{3}' .planning/reviews/61-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/61-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/61-COVERAGE.md; awk "/^## RU-61-05 /,/^## RU-61-07 /" "$f" > /tmp/ru05.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/ru05.txt)" = 6; test "$(grep -cE "^- D[67] .*n/a — " /tmp/ru05.txt)" = 2; for x in main.ts bbj-module.ts bbj-ws-manager.ts bbj-document-builder.ts bbj-notifications.ts logger.ts constants.ts utils.ts composer-commands.ts initializeWorkspace "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/ru05.txt; done; test "$(grep -cE " — (pass|fail) — " "$f")" = 36; test "$(grep -c " — pending" "$f")" = 14; test "$(grep -cE "^- .*n/a — " "$f")" = 38; test -z "$(grep -oE "^id:[[:space:]]+P61-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU61_05_COMPLETE'</automated>
  </verify>
  <done>`RU-61-05` is complete under the stopping rule: all 6 live cells carry a verdict plus a written
  check line, all 9 files including the 1-line `constants.ts` and 0-line `utils.ts` are named inside the
  section, the initialization hookTimeout is recorded as a D5 finding against the code rather than
  against the test runner, the dead-module candidate is confirmed or dismissed with reference-count
  evidence, CLAUDE.md's DI claims are checked, and every unpromoted claim is visible under
  `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE client → language server `initializationOptions` and `didChangeConfiguration` | Every externally-supplied setting — `bbjdir`, `classpath`, `configPath`, `interopHost`, `interopPort` — enters here, and workspace-scoped settings travel inside a cloned repository. |
| workspace configuration → filesystem reads (PREFIX, `config.bbx`) | Configured paths decide which files the server opens outside the documents the user opened. |
| language server → user-visible log output | `logger.ts` decides what leaves the process and at what level. |
| `composer-commands.ts` → command/argument construction | A command surface registered by the language server. |
| `61-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| shared `61-COVERAGE.md` → the other six plans | One file, seven writers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-P06-S1 | Elevation of Privilege | `initializationOptions` handling in `bbj-ws-manager.ts` and the settings-change path in `main.ts` | high | transfer | Task 1's D1 check asks whether `bbjdir`, `classpath`, `configPath`, `interopHost` and `interopPort` are validated for type, shape and range before use, including the non-integer, negative, out-of-range and string-typed port cases, and whether a workspace-scoped setting can redirect the server. This is the input side of the SEC-06 boundary documented at `RU-61-06`; remediation transfers to Phase 67. |
| T-61-P06-S2 | Information Disclosure | `logger.ts` output at its default level | medium | transfer | Task 1's D1 check asks whether a classpath, a token, a path or document content can reach a user-visible or on-disk log. |
| T-61-P06-S3 | Tampering | PREFIX and `config.bbx` path resolution | medium | transfer | Task 1's D1 check asks whether a configured path can direct the server to read outside the workspace root. |
| T-61-P06-S4 | Denial of Service | `WorkspaceManager.initializeWorkspace()` cost under load | medium | transfer | Task 1's D3 check profiles the initialization path; Task 2 records it as a D5 finding against the code, naming both candidate remediations without choosing one. |
| T-61-P06-S5 | Elevation of Privilege | command/argument construction in `composer-commands.ts` | medium | transfer | Task 1's D1 check asks whether any command, path or argument is built from user-controlled values. |
| T-61-P06-01 | Information Disclosure | an unfixed `critical`/`high` D1 finding committed to a public repository | high | mitigate | D-12 two-tier disclosure: surface, problem class and impact only; no trigger sequence, no payload, no proof-of-concept. |
| T-61-P06-02 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67 apply path | high | mitigate | INVENTORY §3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; every record writes all six tests pass/fail. |
| T-61-P06-03 | Tampering | seven plans writing one `61-COVERAGE.md` | medium | mitigate | Wave 6 behind `depends_on: [61-05]` makes ordering structural (D-04); both tasks assert exact global counts (33/17, then 36/14) plus the invariant 38 carried-forward `n/a` lines. |
| T-61-P06-04 | Repudiation | a cross-unit referral addressed to this unit going unanswered | high | mitigate | This is the unit most referrals land in, so Task 1 opens by resolving every entry addressed to `RU-61-05` — each promoted to a finding or dismissed with a written reason — and the acceptance criteria check each referral target against this section. Without this, a defect discovered by plan 61-01 could vanish between units. |
| T-61-P06-05 | Repudiation | a D4 finding that proposes the merge STATE.md forbids | medium | mitigate | Explicit prohibition on proposing to merge, inline or remove `bbj-notifications.ts`; the action instructs that the isolation constraint be recorded as the reason it is not a smell. |
| T-61-P06-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and the already-installed test harness. |
</threat_model>

<verification>
- `## RU-61-05`'s 6 live cells each carry `pass` or `fail` plus a written check line ≥ 80 characters.
- The D1 cell names `bbj-ws-manager.ts`, so the settings-input surface was assessed rather than skipped.
- All 9 unit files are named inside the section, including the 1-line `constants.ts` and 0-line `utils.ts`.
- Every cross-unit referral addressed to `RU-61-05` is promoted or dismissed with a written reason.
- The initialization hookTimeout is recorded as a D5 finding against `initializeWorkspace()` by `file:line`.
- The dead-module candidate is resolved against an actual reference count.
- Phase-wide verdict count is 36 and placeholder count is 14; the carried-forward `n/a` count is 38.
- Every finding has all 13 fields, a unique well-formed ID and a non-blank `dedup:` that explicitly
  covered #33, #231, #385, #485 and #486.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-01 advances: 6 of 7 units swept, 36 of 50 `applies` cells recorded.
- The input side of the SEC-06 boundary has a recorded D1 verdict, completing the boundary documented at
  `RU-61-06` from the other direction.
- FIX-03's `npm test` clean gate in Phase 67 has an evidenced finding to work from for the flakiness that
  currently makes the suite's skip count non-deterministic.
</success_criteria>

<output>
Create `.planning/phases/61-language-core-review/61-06-SUMMARY.md` when done.
</output>
