---
phase: 86-intellij-interop-settings-targeted-refresh
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md
  - QA/FULL-TEST-CHECKLIST.md
  - documentation/docs/intellij/configuration.md
  - .planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md
  - .planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md
autonomous: true
requirements: [CFG-04, CFG-05]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The phase's hand-verifiable surface is written down before the code is verified: the one live-IDE property no automated test in this repo can reach — that diagnostics, completion, hover and the Structure View keep answering while a refresh runs — has a checklist row naming exact steps and an exact expected result (D-18)."
    - "The port behaviour that only a real BBjServices install can show — detection from the live BBj.properties and an explicitly confirmed 5008 surviving a properties file that names something else — has its own checklist row (D-06, D-18)."
    - "The IntelliJ configuration documentation describes the Auto-detect checkbox, the exact key it reads, the hint line and the explicit-port rule; today's text advertises auto-detection that never worked (D-19)."
    - "The documentation records that only the port is detected and that the host stays a manually edited field (D-08), and that a port change takes effect at the next language-server start because applying settings restarts it (D-10, D-17)."
    - "The seal-time API-coverage gate has a reasoned declaration to read rather than a missing file, because this phase adds one in-plugin request on an existing interface and one local properties read, not an external service integration."
    - "The folded todo about the Enterprise Manager config sentinel is closed as already delivered, with a note pointing at the Phase 84 guards, and no code is written for it (86-CONTEXT Folded Todos)."
  artifacts:
    - path: .planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md
      provides: "The reasoned no-external-API declaration the seal gate reads"
      contains: "No external API integration:"
    - path: .planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md
      provides: "The folded todo, closed with a resolution note naming the phase that delivered it"
  key_links:
    - from: .planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md
      to: "the api-coverage.verify-pre seal gate"
      via: "a reasoned no-external-API-integration declaration is accepted in place of a capability matrix"
    - from: QA/FULL-TEST-CHECKLIST.md
      to: "the UAT run that closes CFG-04 and CFG-05"
      via: "each new row is a hand-executable check for a property no JUnit or vitest test in this repo can reach"
    - from: documentation/docs/intellij/configuration.md
      to: bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjInteropPortDetector.java
      via: "the documented key and default port are the detector's ADDR_KEY and DEFAULT_PORT"
  prohibitions:
    - statement: "COVERAGE.md MUST be a single line under 200 characters beginning `No external API integration: ` followed by a one-line reason — no capability matrix, since this phase integrates no external service, and no fabricated rows for capabilities that do not exist."
      status: resolved
      verification: test
    - statement: "Existing QA checklist rows MUST NOT be renumbered, reworded or removed; append inside the correct section only."
      status: resolved
      verification: test
    - statement: "The folded todo MUST NOT be implemented — Phase 84 already delivered both guards, and this phase's deliverable for it is the closure note alone. No production file may be changed by this plan."
      status: resolved
      verification: test
    - statement: "No plan identifiers, D-xx, C-xx, COMP or CR-xx tokens in the QA rows or in the user-facing documentation — those are read by a human tester and by users, not by the planner. Planning-directory notes such as the todo closure may reference a phase and its decision id."
      status: resolved
      verification: judgment
    - statement: "No `git add -A` / `git add .` — exact-path staging only, on the current branch, no new branch."
      status: resolved
      verification: judgment
---

<objective>
Write down everything about this phase a human has to check or read: the two QA checklist rows for the properties no automated test in this repo can reach, the one-line API-coverage declaration the seal gate reads, the rewritten IntelliJ Port documentation, and the closure note for the folded Enterprise Manager sentinel todo.

Purpose: Phase 84 left three UAT-covered behaviours out of the checklist and that became a recorded follow-up; writing the rows in the same phase that creates the behaviour is what stops it repeating. The refresh property in success criterion 2 is only observable in a running IDE, so its row is not optional documentation — it is the phase's only evidence for that property. The Port documentation currently promises auto-detection that never matched a real installation, which is a user-visible falsehood this phase must not leave behind.
Output: `.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md`, two new rows in `QA/FULL-TEST-CHECKLIST.md`, a rewritten Java Interop section in `documentation/docs/intellij/configuration.md`, and the folded todo moved to the completed directory with a resolution note.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<executor_rules>
**Shell and file-access rules (project CLAUDE.md — follow verbatim):**
- Search and read with the built-in tools first: `Grep` for content search, `Glob` for file discovery, `Read` for file contents. Fall back to shell `grep`/`find`/`cat` only when a built-in tool genuinely cannot do the job.
- Every shell path is absolute and complete, named from the repo root (`/home/coder/repos/bbj-language-server/...`). Never use `.`, `..` or `~` as a search target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`; use `git -C /home/coder/repos/bbj-language-server ...` for every git command, including the file move.
- No blind recursive scans. Never point a read at `.env*`, `*.pem`, `*.key` or a credential store.
- Stage commits with `git add <exact path>` only — never `git add -A` or `git add .`. Commit on the current branch; do not create or switch branches.

**Scope rule for this plan:** it writes documentation, QA rows and planning files only. It must not modify any file under `bbj-intellij/src`, `bbj-vscode/src` or `java-interop`.
</executor_rules>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-intellij-interop-settings-targeted-refresh/86-CONTEXT.md
@.planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md
@QA/FULL-TEST-CHECKLIST.md
@documentation/docs/intellij/configuration.md
@.planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md
</context>

<settled_facts>
- **The QA checklist's IntelliJ LSP Features section currently ends at row 15** ("Save burst: a `.bbj` file and the config file together yields one clean restart"), immediately before a horizontal rule and the `## VS Code - Run Commands` heading. The new rows are 16 and 17 in that section. No other section of the file reaches row 16, so a row-number match on 16 and 17 is unambiguous across the whole file.
- **The table columns are `#`, `Feature`, `Steps`, `Expected`, `Pass/Fail`**, steps are numbered inside one cell separated by `<br>`, and the last cell is an empty checkbox. Rows 10-15 of that section are the closest style models.
- **The API-coverage detector already returned a negative verdict** for this phase's roadmap section. The declaration is written anyway because the seal-time gate has false-positived in this repository on the prose word "API" — Phase 85 wrote the same declaration for the same reason, and its file is the format model: exactly one line, nothing else.
- **The completed-todo directory is `.planning/todos/completed/`, not `done/`, and it is currently empty.** The move is the first entry in it.
- **The folded todo is closed, not built.** Phase 84 delivered both halves: `ConfigPaths.configPathArg` refuses the Enterprise Manager sentinel on the IntelliJ side, and the VS Code side strips it from the active config path. The todo's own "Solution" section asked for exactly those two guards.
- **`documentation/docs/intellij/configuration.md` has a `## Java Interop` section at line 96** with `### Host` and `### Port` subsections; the Port subsection's "Features" list is the text that currently claims auto-detection from `BBj.properties` works when BBj Home is set.
</settled_facts>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the phase's hand-checked surface is written down" — checklist rows and the coverage declaration</name>
  <files>.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md, QA/FULL-TEST-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/85-config-hot-reload-with-restart-coalescing/COVERAGE.md (the exact one-line declaration format the seal gate accepts)
    - QA/FULL-TEST-CHECKLIST.md lines 38-58 (the IntelliJ LSP Features table: its header, the `<br>`-separated numbered steps, the expected-result voice and the trailing empty checkbox; rows 13-15 are the closest models)
    - .planning/phases/86-intellij-interop-settings-targeted-refresh/86-CONTEXT.md (the two hand checks named in its Specific Ideas section: the large-classpath refresh with completion and hover invoked mid-refresh, and the two halves of the port regression pair)
  </read_first>
  <action>
Create `.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md` containing exactly one line and nothing else: `No external API integration: ` followed by a one-line reason naming what the phase touches instead — one in-plugin JSON-RPC request added to the existing composer-server interface, plus a read of the local BBj.properties file. Keep the whole line under 200 characters. No heading, no table, no trailing prose.

Append two rows to the `## IntelliJ IDEA - LSP Features` table in `QA/FULL-TEST-CHECKLIST.md`, directly after the existing row 15 and before the horizontal rule that ends the section. Do not touch any existing row, its number or its wording, and do not add rows to any other section.

Row 16, feature name `Refresh Java Classes keeps language features online`. Steps: open a project whose BBj Home has a large Java classpath and open a `.bbj` file that uses Java classes; wait for the BBj status-bar widget to read started; run Refresh Java Classes from the Tools menu; while the "Refreshing Java classes…" progress task is still visible, invoke code completion, hover a variable and open the Structure view; watch the status-bar widget throughout; after the progress task disappears, check the BBj Language Server tool window. Expected: completion answers, hover answers and the Structure view populates while the refresh is running; the status widget never leaves started and no message about the connection to the server being closed appears; exactly one console line reports the refresh finished and no balloon is raised.

Row 17, feature name `Java-interop port auto-detects, and an explicitly confirmed 5008 is kept`. Steps: in BBj settings open the Java Interop section and confirm Auto-detect is checked, the Port field is greyed out, and it shows the port from the `com.basis.languageServer.addr` line of the BBj Home's `cfg/BBj.properties`, with a hint line under the field naming where the value came from; edit that properties line to a different port, close and reopen the settings dialog, and confirm the greyed field now shows the new port; uncheck Auto-detect, confirm the field is editable and pre-filled with the value that was showing, type 5008, and apply; reopen the dialog and confirm Auto-detect is still unchecked and the field still reads 5008 even though the properties file names a different port; re-check Auto-detect and confirm the field returns to the detected value. Expected: the greyed field and the hint track the properties file while Auto-detect is on; an explicitly applied 5008 survives a reopen unchanged with the checkbox still off; re-checking restores the detected value; no dialog, warning or balloon appears at any point.

Write both rows in the voice the surrounding rows use — imperative numbered steps in one cell, an observable expected result in the next, an empty checkbox last — and keep them free of plan identifiers and decision ids.
  </action>
  <verify>
    <automated>awk 'NR==1 && /^No external API integration: ./ && length($0) < 200 {ok=1} END{exit !(ok && NR<=1)}' /home/coder/repos/bbj-language-server/.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md</automated>
    <fails_when>non-zero exit — the file is missing, its first line does not begin with the declaration prefix followed by a reason, the line is 200 characters or longer, or the file has more than one line</fails_when>
    <automated>test "$(grep -cE '^\| 1[67] \| ' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md)" = "2"</automated>
    <fails_when>non-zero exit — fewer or more than two rows numbered 16 and 17 exist in the checklist</fails_when>
    <automated>grep -q 'Refresh Java Classes keeps language features online' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md && grep -q 'com.basis.languageServer.addr' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit — either the refresh row's feature name or the properties key named by the port row is absent</fails_when>
    <automated>grep -q '^| 14 | Config file outside' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md && grep -q '^| 15 | Save burst:' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit — the two rows that previously ended the IntelliJ LSP Features section are no longer present under their original numbers and wording, meaning an existing row was renumbered or reworded instead of appended to</fails_when>
  </verify>
  <acceptance_criteria>
    - `COVERAGE.md` exists, is exactly one line, begins with `No external API integration: ` followed by a reason, and is under 200 characters.
    - `QA/FULL-TEST-CHECKLIST.md` contains exactly two rows numbered 16 and 17, both inside the `## IntelliJ IDEA - LSP Features` table and both before the horizontal rule that ends that section.
    - Row 16's feature cell reads `Refresh Java Classes keeps language features online`, its steps name invoking completion, hover and the Structure view while the progress task is visible, and its expected cell requires the status widget never to leave started, exactly one console line, and no balloon.
    - Row 17's feature cell names both halves — auto-detection and an explicitly confirmed 5008 — its steps name the `com.basis.languageServer.addr` key and the uncheck-type-5008-apply-reopen sequence, and its expected cell requires the applied 5008 to survive a reopen with the checkbox still unchecked.
    - Rows 14 and 15 of the IntelliJ LSP Features section are still present under their original numbers and opening wording, proving the new rows were appended rather than inserted over existing ones.
    - Neither new row contains a plan identifier or a decision id.
  </acceptance_criteria>
  <done>The two properties this phase cannot prove with a test are written down as hand-executable rows with exact steps and exact expected results, and the seal-time coverage gate has a reasoned one-line declaration to read.</done>
</task>

<task type="auto">
  <name>Task 2: The Port documentation tells the truth, and the folded todo is closed</name>
  <files>documentation/docs/intellij/configuration.md, .planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md, .planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md</files>
  <read_first>
    - documentation/docs/intellij/configuration.md lines 90-130 (the `## Java Interop` section with its `### Host` and `### Port` subsections, the Default/Features formatting convention used throughout the file, and the neighbouring Enterprise Manager section for voice)
    - .planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md (whole file — its front matter shape, its Problem and Solution sections, and the `audit_acknowledged` block)
    - .planning/phases/84-config-path-resolution-discoverability-foundation/84-06-SUMMARY.md (the plan that delivered the sentinel guards — the closure note must name what it actually shipped)
    - .planning/phases/86-intellij-interop-settings-targeted-refresh/86-CONTEXT.md (the Folded Todos section: this todo is folded to close it, not to build it)
  </read_first>
  <action>
Rewrite the `### Port` subsection of `documentation/docs/intellij/configuration.md` inside `## Java Interop`, keeping the file's existing Default/Features formatting and heading levels. It must state:
- the default is 5008;
- there is an **Auto-detect** checkbox, on by default; while it is on the port is read from the BBj Home's `cfg/BBj.properties`, specifically the `com.basis.languageServer.addr` key whose value has the form host, port and enabled flag separated by colons, and the Port field is greyed out showing the detected value;
- a hint line under the field says where the value came from, or that 5008 is being used because no port was found in `BBj.properties`;
- when that key reports the java-interop service as disabled in BBjServices, the port is still used and the hint says so;
- unchecking Auto-detect pre-fills the field with the value that was in effect and makes it editable; whatever is applied — including 5008 — is kept and is never replaced by auto-detection; re-checking discards the edit and returns to the detected value;
- the range 1-65535 is validated, and the value must match the port BBjServices uses for the java-interop service;
- every consumer of the setting — the language server's startup options, the java-interop health indicator and the settings dialog itself — uses the same effective value;
- a port change takes effect at the next language-server start, and applying settings restarts the server.
Also add one sentence to the `### Host` subsection recording that auto-detection never changes the host: it stays a manually edited field, defaulting to `localhost`. Remove the current Features bullet claiming the port is auto-detected from `BBj.properties` when BBj Home is set, since that is the text this phase replaces. Keep every other section of the file untouched and write no plan identifier or decision id into it.

Close the folded todo without writing any code. Move it with `git -C /home/coder/repos/bbj-language-server mv .planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md .planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md` so the deletion and the addition are staged together, then append a `## Resolution` section to the moved file recording: that it was reviewed while gathering context for phase 86 and folded in to be closed rather than built; that Phase 84 already delivered both halves the Solution section asked for — the IntelliJ config-path argument builder refuses the Enterprise Manager sentinel, and the VS Code side strips the sentinel from the active config path — naming the phase, its decision id and the plan that shipped them; that phase 86 therefore wrote no code for it; and the date. Add a `completed:` key with today's date to the file's front matter, leaving every existing key and the whole Problem section byte-identical.
  </action>
  <verify>
    <automated>grep -q 'Auto-detect' /home/coder/repos/bbj-language-server/documentation/docs/intellij/configuration.md && grep -q 'com.basis.languageServer.addr' /home/coder/repos/bbj-language-server/documentation/docs/intellij/configuration.md</automated>
    <fails_when>non-zero exit — either the checkbox name or the detected key is missing from the configuration documentation</fails_when>
    <automated>test -f /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md && test ! -e /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md</automated>
    <fails_when>non-zero exit — the todo is absent from the completed directory, or a copy is still sitting in the pending directory</fails_when>
    <automated>grep -q '## Resolution' /home/coder/repos/bbj-language-server/.planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md</automated>
    <fails_when>non-zero exit — the moved todo carries no Resolution section</fails_when>
    <automated>test -z "$(git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-intellij/src bbj-vscode/src java-interop)"</automated>
    <fails_when>non-zero exit — this plan touched a production source tree, which its scope rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - The `### Port` subsection names the Auto-detect checkbox, the `com.basis.languageServer.addr` key, the greyed field, the hint line, the disabled-service case, the uncheck-and-keep rule including 5008 explicitly, the re-check discard, the 1-65535 range, the shared effective value across all consumers, and the next-start propagation with the restart on Apply.
    - The `### Port` subsection no longer contains the old bullet claiming auto-detection from `BBj.properties` when BBj Home is set.
    - The `### Host` subsection records that the host is never auto-detected and defaults to `localhost`.
    - No other section of `configuration.md` is changed, and the file contains no plan identifier or decision id.
    - The folded todo exists only under `.planning/todos/completed/`, carries a `## Resolution` section naming Phase 84's two delivered guards and the plan that shipped them, and has a `completed:` front-matter key; its Problem section is byte-identical to before the move.
    - `git status --porcelain` for `bbj-intellij/src`, `bbj-vscode/src` and `java-interop` is empty — no production code was written for the folded todo.
  </acceptance_criteria>
  <done>A user reading the IntelliJ Port documentation gets the behaviour this phase actually ships, including that an explicitly chosen 5008 is kept, and the folded todo is closed with a note pointing at the phase that already delivered it.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new, not drift):

- `.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md` — new file; one-line `No external API integration: …` declaration
- `QA/FULL-TEST-CHECKLIST.md` — new row 16 `Refresh Java Classes keeps language features online` and new row 17 `Java-interop port auto-detects, and an explicitly confirmed 5008 is kept`, both in the IntelliJ LSP Features table
- `documentation/docs/intellij/configuration.md` — rewritten `### Port` subsection under `## Java Interop`; one new sentence in `### Host`
- `.planning/todos/completed/2026-08-22-strip-em-config-sentinel-in-getconfigpatharg-and-commands-cj.md` — the folded todo moved out of `pending/` with a new `## Resolution` section and a `completed:` front-matter key
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project → published documentation | The Docusaurus site is public; text written here describes where a configuration value is read from. |
| planning directory → future phases | The QA checklist and the closed todo are the record a later UAT run and a later audit read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-17 | Information disclosure | published documentation naming a configuration file path and key | low | accept | `BBj.properties` and the `com.basis.languageServer.addr` key are part of BBjServices' own documented configuration surface; naming them discloses nothing that is not already in the product's own configuration files, and no credential, token or secret is named. |
| T-86-18 | Repudiation | a hand check recorded as passed without exact steps, leaving the phase's only evidence for its live-IDE property unauditable | high | mitigate | Each row states numbered steps and an observable expected result, so a later reader can tell what was actually exercised; the refresh row explicitly requires the widget never to leave started and exactly one console line, which are falsifiable rather than impressionistic. |
| T-86-19 | Tampering | an existing QA row silently reworded or renumbered while appending | medium | mitigate | A verify command asserts that rows 14 and 15 are still present under their original numbers and opening wording, so renumbering or rewording the rows the new ones sit behind fails the task. |
| T-86-20 | Tampering | closing a todo that was not actually delivered | medium | mitigate | The closure note must name the delivering phase, its decision id and the plan that shipped each guard, and a verify command asserts no production tree was modified — so the closure is either evidenced or visibly a code change that does not belong to this plan. |
| T-86-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager install task and no dependency of any kind; it writes markdown only, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
- `awk 'NR==1 && /^No external API integration: ./ && length($0) < 200 {ok=1} END{exit !(ok && NR<=1)}' /home/coder/repos/bbj-language-server/.planning/phases/86-intellij-interop-settings-targeted-refresh/COVERAGE.md` exits 0.
- `test "$(grep -cE '^\| 1[67] \| ' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md)" = "2"` exits 0.
- `grep -q '^| 14 | Config file outside' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md && grep -q '^| 15 | Save burst:' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md` exits 0.
- `test -z "$(git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-intellij/src bbj-vscode/src java-interop)"` exits 0.
</verification>

<success_criteria>
- The two properties this phase cannot prove with an automated test have hand-executable checklist rows with exact steps and falsifiable expected results, appended without disturbing any existing row.
- The seal-time API-coverage gate has a reasoned one-line declaration instead of a missing file or a fabricated matrix.
- The IntelliJ Port documentation describes the checkbox, the key, the hint, the explicit-port rule and the restart-on-Apply behaviour, and no longer promises detection that never worked.
- The folded todo is closed in the completed directory with a resolution note naming what already delivered it, and no production code was written for it.
</success_criteria>

<output>
Create `.planning/phases/86-intellij-interop-settings-targeted-refresh/86-04-SUMMARY.md` when done.
</output>
