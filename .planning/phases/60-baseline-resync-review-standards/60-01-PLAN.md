---
phase: 60-baseline-resync-review-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/INVENTORY.md
autonomous: false
requirements: [BASE-03, RVW-06, RVW-07]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`.planning/reviews/INVENTORY.md` exists and declares itself the immutable authority on review scope, structure and file counts for v4.0; no later phase edits it (D-09, D-16)"
    - "A reader can take any single review unit and read, in one place, its file list, its D1-D8 applicability with a written reason for every n/a cell, and the exact record shape a finding against it must have (D-07, D-08)"
    - "Finding IDs are allocatable by Phases 61, 62, 63 and 64 concurrently with zero collision risk, and the sweep plus violated dimension are readable from the ID alone (D-11)"
    - "A reviewer can decide, without asking, whether a given claim clears the evidence bar for its dimension and whether it is easy or major (D-12, D-13)"
    - "The 15 GitHub issues open on 2026-08-17 are frozen in the document as the authoritative dedup list, with number, title, labels and a one-line summary (D-14, RVW-07)"
    - "The measured test and build state at plan time is frozen in the document, with environment-dependent failures distinguished from genuine defects and each routed to a target review phase and dimension (D-05, D-06)"
    - "Every stale planning-document claim corrected by this phase is logged with its prior text, its corrected text, and the command whose output establishes the truth (D-15)"

    # ~~ edge probe: BASE-01 (resolved, explicit)
    - "BASE-01 boundary: the baseline range is `2194616..v0.12.0` — `2194616` itself is excluded, `v0.12.0` (`1b86a6b`) is included — and `git rev-list --count 2194616..v0.12.0` prints exactly `153`; every commit reachable only from `v0.12.0..HEAD` is outside the baseline, and `a7e1b53` is named as an unreleased in-flight fix (D-01)"
    - "BASE-01 precision: the four per-release commit counts are exact integers from `git rev-list --count <prev>..<tag>` and sum to exactly 153 (93 + 38 + 9 + 13); no count is rounded or approximated (D-01, D-04)"

    # ~~ edge probe: RVW-06 / RVW-07 (resolved, explicit)
    - "RVW-06 adjacency: a finding spanning two dimensions declares exactly one primary dimension (which supplies its ID) and lists the others in a `secondary` field; where the two dimensions demand different D-12 tiers the finding must clear the stricter tier (D-11, D-12)"
    - "RVW-07 adjacency: a finding that partially overlaps a frozen open issue is recorded as `partial-overlap` with a one-line statement of what it adds beyond that issue; only an exact-subject match is `duplicate`, and a duplicate is dispositioned rather than filed (D-14)"

    # ~~ edge probe: RVW-06 / RVW-07 (resolved, backstop — Phase 60 states the rule; Phases 61-64 exercise it)
    - { statement: "RVW-06 empty: a finding that does not fully clear its D-12 tier is recorded in the sweep's coverage file under disposition `not-reproducible` with its reason rather than silently dropped", verification: backstop }
    - { statement: "RVW-06 ordering: finding sequence numbers are zero-padded three digits allocated monotonically in discovery order within each (phase, dimension) pair, so ties cannot occur", verification: backstop }
    - { statement: "RVW-07 empty: a finding with no matching frozen issue records `dedup: none` explicitly; the dedup field is never left blank", verification: backstop }
    - { statement: "RVW-07 ordering: the frozen issue snapshot is stored in ascending issue-number order, and a finding matching two issues records both with the lower number first", verification: backstop }

  artifacts:
    - ".planning/reviews/ (directory, created by this plan)"
    - ".planning/reviews/INVENTORY.md with sections: Status & Authority, Frozen Open-Issue Snapshot, Finding Standard, Finding Record Template, Review Units, Test & Build Baseline, D-15 Correction Log"
    - "Review unit RU-62-04 (composer webview HTML generators), fully worked as the shape every other unit follows"

  key_links:
    - "INVENTORY.md finding-ID scheme -> Phase 67 commit messages and Phase 69 GitHub issue bodies (D-11, one-way)"
    - "INVENTORY.md frozen issue snapshot -> RVW-07 dedup check in Phases 61-65 and the Phase 69 re-check (D-14)"
    - "INVENTORY.md easy-vs-major rule -> the Phase 67 apply path and the Phase 68 EASY-FIXES / MAJOR-REFACTORS split (D-13)"
    - "INVENTORY.md test/build baseline -> FIX-03's `npm test` clean gate in Phase 67 (D-05, D-06)"
    - "INVENTORY.md D-15 correction log -> the in-place ROADMAP/REQUIREMENTS amendments applied by plan 60-04 (D-15)"

  prohibitions:
    - "MUST NOT weaken any ROADMAP or REQUIREMENTS success criterion, or soften a recorded test/build baseline, in the direction that makes the v4.0 milestone easier to pass — D-15 corrections are factual corrections only"
    - "MUST NOT draw a review-unit boundary or write an n/a reason that excludes a genuinely applicable dimension in order to reduce Phase 61-64 sweep cost"
    - "MUST NOT record a reconstructed capability or accomplishment claim that cannot be traced to a specific commit inside `2194616..v0.12.0`"
---

<objective>
Create `.planning/reviews/INVENTORY.md` and establish, in one immutable document, every fact and every
rule that Phases 61-69 inherit: the finding-ID scheme (D-11), the tiered evidence bar (D-12), the
easy-vs-major classification rule (D-13), the finding record template, the frozen 15-issue dedup list
(D-14), the measured test/build baseline (D-05, D-06), the pinned baseline range (D-01), and the D-15
correction log with evidence.

The document's shape is proven end-to-end on **one** review unit before any of the other ~20 are
written (plan 60-02 does that expansion).

Purpose: Phases 61, 62, 63, 64 and 66 all depend on Phase 60 alone and may run concurrently. Every
standard they are held to, and every number their success criteria quote, must be frozen and
collision-free before any of them starts.

Output: `.planning/reviews/` directory and `.planning/reviews/INVENTORY.md` containing the Status &
Authority header, the frozen open-issue snapshot, the finding standard, the finding record template,
review unit RU-62-04 worked end-to-end, the test & build baseline, and the D-15 correction log.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Everything below is **created by Phase 60** and does not exist in the tree today. Drift verification
must treat these as new, not as references to existing symbols.

**New directories**
- `.planning/reviews/`

**New files**
- `.planning/reviews/INVENTORY.md` (this plan + plan 60-02)

**New naming conventions defined here, consumed by later phases**
- Finding ID token shape: `P{phase}-D{dimension}-{seq}` — e.g. `P62-D1-003`. Phase `00` is reserved
  for non-allocatable template examples so no real sweep can collide with an illustration.
- Per-phase coverage file: `.planning/reviews/{NN}-COVERAGE.md` — created by Phases 61, 62, 63, 64,
  65 (e.g. `.planning/reviews/61-COVERAGE.md`). Not created by Phase 60.
- Later deliverables (Phase 68): `.planning/reviews/EASY-FIXES.md`, `.planning/reviews/MAJOR-REFACTORS.md`.

**New review-unit identifiers** — token shape `RU-{phase}-{seq}`. Defined in this plan:
- `RU-62-04` — Composer webview HTML generators.
Plan 60-02 defines the remaining ~20 (`RU-61-01`..`RU-61-07`, `RU-62-01`..`RU-62-05`,
`RU-63-01`..`RU-63-05`, `RU-64-01`..`RU-64-03`, `RU-D8-01`).

**New applicability-grid vocabulary**
- Columns: `D1` Security, `D2` Correctness & error handling, `D3` Performance & resource use,
  `D4` Maintainability & code smells, `D5` Test coverage gaps, `D6` Dependency health,
  `D7` Cross-IDE parity, `D8` Comment & doc accuracy — verbatim from `.planning/REQUIREMENTS.md`.
- Cell values: `applies` | `n/a — <written reason>`. No other value is legal; a blank cell is a defect.
- Row types: `unit` (one per review unit) and `file-exception` (one per file whose applicability
  differs from its unit's).

**New finding-record field vocabulary**
`id`, `unit`, `location`, `dimension`, `secondary`, `severity`, `evidence_tier`, `evidence`,
`failure_scenario`, `classification`, `effort`, `dedup`, `disposition`.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the finding-ID scheme (D-11)</name>
  <decision>
    The token shape for every finding ID produced by Phases 61-65, embedded in Phase 67 commit
    messages and in Phase 69 GitHub issue bodies.
  </decision>
  <context>
    This is a one-way decision. Once Phase 67 commits reference an ID and Phase 69 files a public
    GitHub issue whose body carries it (ISSUE-05 cross-references the number back into
    MAJOR-REFACTORS.md), renumbering requires rewriting git history and editing live tracker issues.
    Phases 61, 62, 63 and 64 depend only on Phase 60, so they may run concurrently and must be able
    to allocate IDs without coordinating with each other.
  </context>
  <options>
    <option id="phase-dimension-seq">
      <name>`P{phase}-D{dimension}-{seq}` — e.g. `P62-D1-003` (D-11, the CONTEXT.md decision)</name>
      <pros>Sweep and violated dimension readable from the ID with no lookup; each of Phases 61-64
      owns a disjoint ID space so concurrent allocation cannot collide; sorts naturally by phase then
      dimension.</pros>
      <cons>A finding spanning two dimensions must nominate one primary dimension for its ID; if a
      finding is later reclassified to a different dimension its ID becomes mildly misleading (the
      record's `secondary` field absorbs this).</cons>
    </option>
    <option id="global-counter">
      <name>Global `F-{seq}` counter</name>
      <pros>Shortest form; no primary-dimension nomination needed.</pros>
      <cons>Concurrent phases cannot allocate without a shared counter — guaranteed collisions given
      Phases 61-64 run in parallel. Carries no information.</cons>
    </option>
    <option id="module-seq">
      <name>`{module}-{seq}` — e.g. `LANG-003`, `WEBVIEW-011`</name>
      <pros>Names the affected area directly.</pros>
      <cons>Module names are not stable (this phase is redrawing the unit boundaries); loses the
      dimension; a module reviewed by two phases reintroduces collisions.</cons>
    </option>
  </options>
  <resume-signal>Select: phase-dimension-seq, global-counter, or module-seq</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Create INVENTORY.md and carry one review unit through the whole contract</name>
  <files>.planning/reviews/INVENTORY.md</files>
  <precondition>The `gh` CLI is authenticated against this repository — `gh auth status` exits 0 and
  `gh issue list --state open --limit 60 --json number` returns JSON. Halt and report if not.</precondition>
  <read_first>
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` — decisions D-07 through
      D-14 are the authority for this task; the `### Claude's Discretion` block lists what is yours
      to settle
    - `.planning/REQUIREMENTS.md` — the D1-D8 review dimensions table (lines 14-23) is **locked**;
      copy the dimension names and the "what counts as a finding" wording verbatim. Also RVW-06,
      RVW-07, DOC-01..DOC-04, ISSUE-02, ISSUE-03
    - `.planning/ROADMAP.md` §Phase 62, §Phase 65, §Phase 67, §Phase 68, §Phase 69 — the success
      criteria this standard has to make satisfiable
    - `bbj-vscode/src/msgbox-composer-webview.ts`, `bbj-vscode/src/addwindow-composer-webview.ts`,
      `bbj-vscode/src/addchildwindow-composer-webview.ts`, `bbj-vscode/src/setopts-composer-webview.ts`
      — read enough of each to write a defensible applicability row; do **not** review them
  </read_first>
  <action>
Create the directory `.planning/reviews/` and the file `.planning/reviews/INVENTORY.md`.

Write these sections, in this order:

**1. Status & Authority.** Dated 2026-08-17. State that this document is written once and is
immutable for the v4.0 milestone: no phase edits it after Phase 60 completes (D-09). State that it,
not `.planning/codebase/*.md`, is the authority on review scope, structure and file counts for this
milestone (D-16). State that each review phase records its results in its own
`.planning/reviews/{NN}-COVERAGE.md` and that Phase 68 assembles DOC-03 from that set (D-09).

**2. Frozen Open-Issue Snapshot (D-14, RVW-07).** Run
`gh issue list --state open --limit 60 --json number,title,labels,createdAt` and transcribe the
result into a markdown table with columns `#`, `Title`, `Labels`, `Area`, `One-line summary`, sorted
ascending by issue number. Exactly these 15 issues must appear: 33, 65, 83, 90, 108, 231, 381, 385,
410, 466, 472, 475, 476, 485, 486. Record the query command and the date above the table. State that
this is the authoritative dedup list for Phases 61-65 and that Phase 69 re-queries the tracker
immediately before filing to catch anything opened mid-milestone. Record `Area` from the existing
label set only — the repository's labels are `grammar`, `scoping`, `types`, `library`, `validation`,
`linking`, `CUI`, `vscode`, `intellij`, `BBj integration and infrastructure`,
`missing verb/parameter`, `common pattern`, `dependencies`, `javascript`, `documentation`; where an
issue carries none, write `— unlabelled` and infer nothing.

**3. Finding Standard.** Four sub-sections:

  *3a. Finding IDs (D-11).* The token shape locked in Task 1, with `P62-D1-003` as the worked
  example, and the rules: `{phase}` is the sweep phase number, `{dimension}` is the primary
  dimension digit 1-8, `{seq}` is zero-padded to three digits and allocated monotonically in
  discovery order within each (phase, dimension) pair. A finding spanning two dimensions declares one
  primary dimension for its ID and lists the rest in `secondary`. Reserve phase number `00` for
  non-allocatable template examples so an illustration can never collide with a real allocation.

  *3b. Evidence tiers (D-12).* A table with one row per dimension:
  D4 and D8 → tier `trace` (a written trace naming the code shape or the stale text is sufficient;
  there is nothing to run). D1, D2 and D3 → tier `repro` (either a runnable reproduction, or a
  line-by-line trace naming the concrete inputs/state and the exact `file:line` where behaviour
  diverges — a bare assertion is not a finding). D5, D6 and D7 → tier `inherited` (follow the tier of
  whatever the finding asserts: a missing test is `trace`, a CVE claim needs the advisory reference).
  State the two adjacency rules: a finding spanning two dimensions must clear the **stricter** of the
  two tiers; a finding that does not fully clear its tier is not recorded as a finding but is written
  into the sweep's coverage file under disposition `not-reproducible` with its reason, so it stays
  visible (RVW-06, DOC-04).

  *3c. Easy-vs-major classification (D-13).* Six numbered tests. A finding is `easy` only if **all
  six** pass; failing any one makes it `major`:
  (1) the fix touches at most one file;
  (2) it changes no public API, no rule in `bbj-vscode/src/language/bbj.langium`, and no LSP
      protocol contract;
  (3) it adds or upgrades no dependency in `bbj-vscode/package.json` or
      `bbj-intellij/build.gradle.kts`;
  (4) it is regression-testable with the existing harness — vitest for TypeScript, Gradle for the
      IntelliJ plugin — with no new test infrastructure;
  (5) the reviewer can name the exact edit in the finding record;
  (6) its `severity` is neither `critical` nor `high`, **and** its primary dimension is not D1.
  Test (6) is a deliberate safety gate: it guarantees that no high-impact or security finding reaches
  Phase 67's apply path without first being recorded in MAJOR-REFACTORS.md. Record that rationale
  next to the test.

  *3d. Severity and effort scales.* Severity is a four-point scale `critical` | `high` | `medium` |
  `low`, mapped to the repository's existing priority labels for ISSUE-03: `critical` and `high` →
  `PRIO 1`, `medium` → `PRIO 2`, `low` → `PRIO 3`. Effort is measured in hours and takes exactly one
  of the three values `2`, `4`, `8` — the literal label names already present in the repository —
  so DOC-02's effort estimate and ISSUE-03's effort label are the same value with no translation
  step.

**4. Finding Record Template.** A single markdown block listing every field, its legal values, and
whether it is required. Fields, in order: `id`, `unit`, `location` (`path:line`, required — RVW-06,
DOC-01, DOC-02, ISSUE-02), `dimension` (primary), `secondary` (optional list), `severity`,
`evidence_tier`, `evidence` (the reproduction or the line-by-line trace), `failure_scenario`
(inputs/state → wrong behaviour), `classification` (`easy` | `major`, with the six D-13 tests
recorded pass/fail), `effort` (`2` | `4` | `8`), `dedup` (`none` | `#NNN duplicate` |
`#NNN partial-overlap — <what this adds>`, checked against the frozen snapshot; never blank), and
`disposition` (`easy-fix` | `major-refactor` | `duplicate` | `wontfix` | `already-covered` |
`not-reproducible`, with a reason for the last four). State explicitly that DOC-04 dispositions are
captured **at sweep time** in the finding record — Phase 68 assembles and does not re-triage.

**5. Review Units — RU-62-04 (worked in full).** Introduce the `RU-{phase}-{seq}` identifier shape
and the applicability-grid vocabulary (`applies` | `n/a — <reason>`; blank is a defect), then write
one complete unit:

  - **`RU-62-04` — Composer webview HTML generators.** Review phase 62 (RVW-03); cross-referenced by
    Phase 65 (SEC-01). Files: `bbj-vscode/src/msgbox-composer-webview.ts` (373 lines),
    `addwindow-composer-webview.ts` (408), `addchildwindow-composer-webview.ts` (431),
    `setopts-composer-webview.ts` (321) — 1,533 lines, 4 files. Risk rank 1 of the Phase 62 units:
    largest LOC and the entire SEC-01 attack surface. Record LOC and risk rank as columns so the
    sweeps can be ordered by them.
  - **D1-D8 applicability with a written reason for every n/a.** D1 `applies` (these four modules are
    the complete SEC-01 webview HTML-generation surface; `package.json` declares no `customEditors`
    contribution, so there is no fifth generator). D2 `applies`. D3 `applies`. D4 `applies` — call
    out that the four generators are near-duplicates, which is the duplication callout ROADMAP
    §Phase 62 criterion 2 requires. D5 `applies`. D6 `n/a` with the reason that these modules import
    only `vscode` and sibling project modules and introduce no third-party dependency, so dependency
    health is assessed once at `RU-64-02`. D7 `applies` — VS Code renders these webviews while
    IntelliJ presents native Swing dialogs (`RU-63-04`), so the parity question is equivalence of the
    generated BBj code, not of the UI. D8 `applies`.
  - **One `file-exception` row.** `setopts-composer-webview.ts` has no `setopts-composer.ts` sibling;
    its codegen logic lives in `setopts-catalog.ts` (`RU-62-03`), so its D4 duplication assessment
    runs against three `-composer.ts` files, not four. Any template that assumes a uniform
    `-composer`/`-ui`/`-webview` triple is wrong (D-15).
  - **Dedup neighbours from the frozen snapshot.** #475 (SETOPTS assistance: decode hovers +
    tri-state composer) and #385 (launch the Graffiti Composer in the VS Code extension) — record
    both as `partial-overlap` candidates a Phase 62 reviewer must check against, with a one-line note
    on what each covers.
  - **One worked finding record, ID `P00-D1-001`**, explicitly labelled *template illustration — not
    an allocated finding; Phase 62 allocates the real `P62-D1-001`*. Populate every field of the
    template so the shape is unambiguous, and demonstrate the tier-`repro` D1 evidence rule and the
    D-13 test-(6) outcome (a D1 finding is `major` regardless of edit size). Because this repository
    is public and these surfaces are unfixed, the illustration must name the surface and the class of
    problem only — no payload, no trigger sequence, no proof-of-concept (threat T-60-02).

Do not write any other review unit; plan 60-02 owns the remaining ~20. Do not review any source file
for defects — this phase records scope, not findings.
  </action>
  <reversibility rating="one-way">The finding-ID scheme lands in Phase 67 commit messages and Phase 69
  public issue bodies; renumbering afterwards needs a history rewrite plus live tracker edits. Gated
  by the Task 1 checkpoint.</reversibility>
  <acceptance_criteria>
    - `test -d .planning/reviews` exits 0 and `test -f .planning/reviews/INVENTORY.md` exits 0
    - All 15 frozen issue numbers resolve: `for n in 33 65 83 90 108 231 381 385 410 466 472 475 476 485 486; do grep -q "| $n |" .planning/reviews/INVENTORY.md || echo "MISSING $n"; done` prints nothing
    - The snapshot table has exactly 15 data rows: `awk '/^## Frozen Open-Issue Snapshot/,/^## Finding Standard/' .planning/reviews/INVENTORY.md | grep -cE '^\| [0-9]+ \|'` prints `15`
    - Every dimension name from REQUIREMENTS.md appears: `for d in Security Correctness Performance Maintainability 'Test coverage' 'Dependency health' 'Cross-IDE parity' 'Comment'; do grep -q "$d" .planning/reviews/INVENTORY.md || echo "MISSING $d"; done` prints nothing
    - The three evidence tier names are present: `grep -c 'repro' .planning/reviews/INVENTORY.md` ≥ 1, `grep -c 'trace' .planning/reviews/INVENTORY.md` ≥ 1, `grep -c 'inherited' .planning/reviews/INVENTORY.md` ≥ 1
    - The classification rule states six numbered tests: `awk '/Easy-vs-major/,/^## /' .planning/reviews/INVENTORY.md | grep -cE '^\s*\(?[1-6][.)]'` ≥ 6
    - The three effort values are the repository's literal label names: `grep -qE '\`2\`.*\`4\`.*\`8\`|effort' .planning/reviews/INVENTORY.md` exits 0 and `grep -c 'PRIO 1' .planning/reviews/INVENTORY.md` ≥ 1
    - All thirteen finding-record field names appear: `for f in id unit location dimension secondary severity evidence_tier evidence failure_scenario classification effort dedup disposition; do grep -q "$f" .planning/reviews/INVENTORY.md || echo "MISSING $f"; done` prints nothing
    - All six DOC-04 dispositions appear: `for d in easy-fix major-refactor duplicate wontfix already-covered not-reproducible; do grep -q "$d" .planning/reviews/INVENTORY.md || echo "MISSING $d"; done` prints nothing
    - `grep -c 'RU-62-04' .planning/reviews/INVENTORY.md` ≥ 1 and all four webview filenames are listed: `for f in msgbox-composer-webview.ts addwindow-composer-webview.ts addchildwindow-composer-webview.ts setopts-composer-webview.ts; do grep -q "$f" .planning/reviews/INVENTORY.md || echo "MISSING $f"; done` prints nothing
    - The RU-62-04 block carries eight applicability cells and at least one written n/a reason: `awk '/RU-62-04/,/^## /' .planning/reviews/INVENTORY.md | grep -cE 'D[1-8]'` ≥ 8 and `awk '/RU-62-04/,/^## /' .planning/reviews/INVENTORY.md | grep -c 'n/a —'` ≥ 1
    - The template example uses the reserved phase and is labelled as an illustration: `grep -c 'P00-D1-001' .planning/reviews/INVENTORY.md` ≥ 1
    - Both dedup neighbours are named in the RU-62-04 block: `awk '/RU-62-04/,/^## /' .planning/reviews/INVENTORY.md | grep -cE '475|385'` ≥ 2
    - The immutability and authority statements are present: `grep -ci 'immutable' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -c 'COVERAGE.md' .planning/reviews/INVENTORY.md` ≥ 1
    - No credential material is recorded: `grep -ciE 'gh_token|github_token|ghp_|Bearer ' .planning/reviews/INVENTORY.md` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/INVENTORY.md; test -f "$f"; for n in 33 65 83 90 108 231 381 385 410 466 472 475 476 485 486; do grep -q "| $n |" "$f"; done; for x in id unit location dimension secondary severity evidence_tier evidence failure_scenario classification effort dedup disposition easy-fix major-refactor duplicate wontfix already-covered not-reproducible RU-62-04 P00-D1-001 COVERAGE.md; do grep -q "$x" "$f"; done; test "$(grep -ciE "gh_token|github_token|ghp_|Bearer " "$f")" = 0; echo TRACER_OK'</automated>
  </verify>
  <done>`.planning/reviews/INVENTORY.md` exists and one review unit, RU-62-04, is readable end to end:
  its files, LOC and risk rank; its eight D1-D8 cells with a written reason for the n/a; its
  file-exception row; its dedup neighbours drawn from the frozen 15; and a fully populated finding
  record in the template shape carrying an ID allocated under the locked scheme and the tier its
  dimension demands. The standards those elements reference — ID scheme, tier table, six-test
  classification rule, severity/effort scales, record template — are all present in the same file.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the baseline range and the measured test/build state</name>
  <files>.planning/reviews/INVENTORY.md</files>
  <precondition>`bbj-vscode/node_modules` is installed, so `npm test` and `npm run lint` are runnable
  from `bbj-vscode/` without a fresh install. If absent, run `npm install` in `bbj-vscode/` first and
  say so in the recorded snapshot.</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the file this task appends to; match the section style Task 2 established
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` — decisions D-01, D-05, D-06
    - `.planning/MILESTONES.md` §"v3.9 Quick Wins" — the `**Stats:**` block carries the prior test-suite figures this snapshot is contrasted against
    - `CLAUDE.md` §"Build & Test Commands" — the exact commands to run, and the note that BBj/Java-dependent tests skip unless BBj is reachable
    - `.planning/STATE.md` §"Active Constraints" — the already-known skip/disable constraints (TEST-03, the 3 disabled `parser.test.ts` assertions)
  </read_first>
  <action>
Append two sections to `.planning/reviews/INVENTORY.md`.

**A. Pinned Baseline Range (D-01).** State that the v4.0 baseline is pinned to the `v0.12.0` tag, not
to the branch tip. Record the range `2194616..v0.12.0`, its verified size, and the four release tags
inside it with their dates and their exact per-release commit counts. Run and record the output of
each of these, and write the numbers into a table:
`git rev-list --count 2194616..v0.12.0`;
`git rev-list --count 2194616..v0.9.0`; `git rev-list --count v0.9.0..v0.10.0`;
`git rev-list --count v0.10.0..v0.11.0`; `git rev-list --count v0.11.0..v0.12.0`.
The four per-release counts must sum to the range total; state that sum in the table's footer as a
checked arithmetic identity, not as prose.

Record the tag targets and dates from
`git tag --list 'v0.9.0' 'v0.10.0' 'v0.11.0' 'v0.12.0' --sort=creatordate --format='%(refname:short) %(creatordate:short) %(objectname:short)'`
— note that `v0.10.0` and `v0.11.0` are both dated 2026-07-18 and are nevertheless separate releases
with separate rows, ordered by tag creation date.

State the endpoint semantics explicitly: the left endpoint `2194616` is excluded, the right endpoint
`v0.12.0` is included. State the attribution rule the reconstruction in plan 60-03 uses: a single
commit belongs to the first release tag that contains it (`git tag --contains <sha>`, taking the
earliest by creation date); a capability spanning several commits is attributed to the tag of its
**last** commit, and is listed exactly once.

Then record the excluded tail. Run `git log --oneline v0.12.0..HEAD` and list every commit it
returns with a one-word classification (`planning-doc` or `code-fix`). State that all of them are
outside the baseline. Call out `a7e1b53 fix(#494): terminate the visibility hierarchy walk on cyclic
inheritance` explicitly as an **unreleased, in-flight code fix**, so a Phase 61 reviewer does not
re-report the cyclic-inheritance hang as a live finding. Record that the branch this range was
measured on is `issue494-cyclic-inheritance-hang`, that v4.0 planning commits are landing on it, and
that this moving tail is precisely why the endpoint is pinned to a tag — the tail measured 4 commits
when the phase context was gathered and is larger now.

**B. Test & Build Baseline (D-05, D-06).** Run, from `bbj-vscode/`, `npm test` and `npm run lint`;
run `./gradlew build` from `bbj-intellij/`. Capture full output to a scratch file and record:

  - The headline vitest counts — test files failed/passed/total, and tests failed/passed/skipped/total.
  - A table of **every failing test**: test file, test name, one-line failure summary, and a
    `classification` column with exactly one of `environment` or `genuine`, plus the evidence for that
    call. Two environment facts are already established and must be reflected rather than
    re-litigated: (i) these failures are **not** resolved by bringing a java-interop peer up on port
    5008 — that has been tried; (ii) `test/linking.test.ts > Linking Tests > Interop related tests >
    Resolve nested class FQN` fails on unresolved `Map`/`Entry`/`getValue` references and is the
    leading environment-dependent candidate. Classify each remaining failure on its own evidence; do
    not blanket-classify.
  - A table of **every skipped test**, grouped by file with counts, and a note distinguishing skips
    that are deliberate and already documented (TEST-03's Langium grammar-follower limitation; the 3
    disabled `parser.test.ts` assertions needing a Java classpath unavailable under `EmptyFileSystem`)
    from skips introduced during the drift window with no recorded reason.
  - `npm run lint` state: exit code, error count, warning count.
  - `bbj-intellij/./gradlew build` state: exit code and the failure mode if it fails. The local JDK is
    Temurin 25.0.3 while `bbj-intellij/build.gradle.kts` sets source/target compatibility to
    `JavaVersion.VERSION_17`; a toolchain/version rejection here is an **environment** classification,
    not a code defect, and must be recorded as such with the observed JDK version as evidence.
  - The contrast row: MILESTONES.md records v3.9 as 511 passed / 4 skipped / 0 failures. State the
    delta the drift window produced in tests added, failures added, and the skip growth.
  - The routing table required by D-06: one row per failing test and one row per unexplained skip
    group, each assigned a target review phase and a target dimension (D5 test coverage, or D2
    correctness where the failure indicates a real defect). State that these are **pre-identified
    findings routed into Phases 61-64 for triage**, not an accepted known-failing allowlist, and that
    FIX-03's "`npm test` clean" gate in Phase 67 is unreachable until they are dispositioned.
  </action>
  <reversibility rating="costly">The pinned range is quoted by BASE-01 and BASE-04 and by the coverage
  statements Phases 61-64 and 68 inherit; moving it later invalidates every reconstructed entry's
  release attribution (D-01).</reversibility>
  <acceptance_criteria>
    - The range and its size are recorded literally: `grep -c '2194616..v0.12.0' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -c '153' .planning/reviews/INVENTORY.md` ≥ 1
    - `git rev-list --count 2194616..v0.12.0` prints `153` and that value is the range total recorded in the table
    - The per-release counts sum correctly: the recorded values for v0.9.0/v0.10.0/v0.11.0/v0.12.0 are `93`, `38`, `9`, `13` and `93+38+9+13 = 153`
    - All four tags appear: `for t in v0.9.0 v0.10.0 v0.11.0 v0.12.0; do grep -q "$t" .planning/reviews/INVENTORY.md || echo "MISSING $t"; done` prints nothing
    - Both same-day releases have distinct rows: `grep -c '2026-07-18' .planning/reviews/INVENTORY.md` ≥ 2
    - The in-flight fix is named: `grep -c 'a7e1b53' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -ci 'unreleased' .planning/reviews/INVENTORY.md` ≥ 1
    - The measuring branch is named: `grep -c 'issue494-cyclic-inheritance-hang' .planning/reviews/INVENTORY.md` ≥ 1
    - Every failing test has a classification: within the baseline section, the count of rows containing `environment` or `genuine` equals the recorded number of failing tests
    - The build results are recorded: `grep -c 'npm run lint' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -c 'gradlew build' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -c '25.0.3' .planning/reviews/INVENTORY.md` ≥ 1
    - The v3.9 contrast is present: `grep -c '511' .planning/reviews/INVENTORY.md` ≥ 1
    - The routing table assigns a phase and a dimension to each failure: `awk '/Test & Build Baseline/,0' .planning/reviews/INVENTORY.md | grep -cE 'D[25]'` ≥ 1 and `awk '/Test & Build Baseline/,0' .planning/reviews/INVENTORY.md | grep -cE 'Phase 6[1-4]'` ≥ 1
    - The routing is stated as triage input, not an allowlist: `grep -ci 'allowlist' .planning/reviews/INVENTORY.md` ≥ 1 (in the sentence that rejects it) and `grep -c 'FIX-03' .planning/reviews/INVENTORY.md` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/INVENTORY.md; test "$(git rev-list --count 2194616..v0.12.0)" = 153; for x in "2194616..v0.12.0" a7e1b53 issue494-cyclic-inheritance-hang v0.9.0 v0.10.0 v0.11.0 v0.12.0 "gradlew build" "npm run lint" 511 FIX-03; do grep -q "$x" "$f"; done; echo BASELINE_OK'</automated>
  </verify>
  <done>A reader can see the exact commit range v4.0 is measured against, its four constituent releases
  with counts that sum to the total, the commits deliberately excluded from it and why, and the exact
  test/lint/build state on 2026-08-17 — with each failure marked environment or genuine and routed to
  a named review phase and dimension.</done>
</task>

<!-- planner-discipline-allow: customEditors -->
<!-- `customEditors` is evidence about bbj-vscode/package.json, a file no task in this plan writes;
     the zero-count assertion is a property of the tree being recorded, not a ban on plan output. -->
<task type="auto">
  <name>Task 4: Log every planning-document correction with its evidence</name>
  <files>.planning/reviews/INVENTORY.md</files>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the file this task appends to
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` §D-15 — the correction table and the list of files absent from the maps; this is the authority
    - `.planning/ROADMAP.md` — the v4.0 phase checklist under `### 🚧 v4.0 Stability and Quality`, `### Phase 60:` with its five success criteria, `### Phase 61:` with its four, `### Phase 62:` criterion 2, `### Phase 65:` criterion 1 — the exact current wording being corrected. Anchor on headings, not absolute line numbers
    - `.planning/REQUIREMENTS.md` §Baseline (BASE-01..BASE-04), §Review Coverage (RVW-03), §Security (SEC-01) — the exact current wording being corrected
    - `.planning/PROJECT.md` §`## Current Milestone: v4.0 Stability and Quality` (`**Target features:**`) and §`## Context` (the `**Current state:**` paragraph and the ⚠ Planning-drift note) — the exact current wording being corrected
    - `bbj-vscode/package.json` — the `contributes` block; the `bbx-config` entry is a language registration for `config.bbx`/`config.min`, not an editor
  </read_first>
  <action>
Append a `## D-15 Correction Log` section to `.planning/reviews/INVENTORY.md`.

This is the evidence record that plans 60-03 and 60-04 apply. Every correction gets one row with
these six columns: `Document`, `Anchor` (heading or line reference), `Claimed`, `Verified`,
`Verification command`, `Observed output`. Quote the claimed text as it stands today so a reader can
see exactly what changed and why — the point is that no edit to an approved roadmap is silent.

Cover, at minimum, these five verified discrepancies from D-15, each appearing once per document that
carries it (`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`):

1. **Drift-window size.** The documents state the count as one greater than reality. Verified by
   `git rev-list --count 2194616..v0.12.0`.
2. **Drift-window endpoint.** The documents name the branch tip as the endpoint; the verified
   endpoint is the `v0.12.0` tag. Verified by `git log --oneline v0.12.0..HEAD` showing a non-empty,
   growing tail on `issue494-cyclic-inheritance-hang`.
3. **`bbj-vscode/src/language/` file count.** The documents state 39; the verified hand-written total
   is approximately 49 — 37 top-level `.ts` plus 2 `.langium`, plus 4 under `validations/`, plus 6
   `lib/*.ts`, alongside 4 `lib/*.bbl` catalogs. Verified by
   `ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium | wc -l`,
   `ls bbj-vscode/src/language/validations/ | wc -l`,
   `ls bbj-vscode/src/language/lib/*.ts | wc -l`, `ls bbj-vscode/src/language/lib/*.bbl | wc -l`.
   Record the four numbers and the total.
4. **Webview composer file count and shape.** The documents state 13 files split uniformly across a
   `-composer`/`-ui`/`-webview` triple. The verified reality is 11 composer files plus
   `setopts-catalog.ts`: SETOPTS has only `-ui` and `-webview` and no `-composer.ts`. Verified by
   `ls bbj-vscode/src/*composer*.ts bbj-vscode/src/setopts-catalog.ts`. Record the full 12-file
   listing so the asymmetry is visible.
5. **The non-existent editor subsystem.** ROADMAP §Phase 60 criterion 1, ROADMAP §Phase 65
   criterion 1, REQUIREMENTS BASE-02 and REQUIREMENTS SEC-01 all name a shipped editor subsystem
   that does not exist. Verified reality: `bbx-config` is a **language ID** registered in
   `bbj-vscode/package.json` for `config.bbx`/`config.min`; the markup SEC-01 actually targets is
   generated by `bbj-vscode/src/setopts-composer-webview.ts` and scoped to that language ID by
   `bbj-vscode/src/setopts-composer-ui.ts`. Verified by
   `grep -n 'bbx-config' bbj-vscode/package.json` and by the absence of any `customEditors`
   contribution: `grep -c 'customEditors' bbj-vscode/package.json`. State the replacement wording
   each affected criterion should carry, so plan 60-04 applies it rather than inventing it.

Then add two further rows for discrepancies verifiable today:

6. **PROJECT.md Context test-suite claim.** The Context paragraph presents the v3.9 figures as the
   current state. Verified by the Test & Build Baseline section written in Task 3; cross-reference it
   rather than repeating the numbers.
7. **PROJECT.md carried-debt count.** The Target-features list states a carried-debt item count that
   does not match `REQUIREMENTS.md`'s DEBT-01..DEBT-06 or ROADMAP §Phase 66 criterion 5. Verified by
   `grep -c '^- \[ \] \*\*DEBT-' .planning/REQUIREMENTS.md`. Record the observed number. If the
   discrepancy turns out to be resolvable in favour of the existing text, record that outcome and
   the reason instead — but do not leave it unexamined.

Below the table, add a short `### Files absent from the 2026-02-01 maps and from the roadmap
enumeration` list, naming each file from D-15 and confirming its presence with
`ls bbj-vscode/src/language/<file>`: `bbj-code-action-provider.ts`, `bbj-inlay-hint-provider.ts`,
`bbj-overload-selector.ts`, `bbj-use-insert.ts`, `composer-commands.ts`, `bbj-definition-provider.ts`,
`bbj-document-symbol-provider.ts`, `logger.ts`, `validations/check-function-calls.ts`,
`lib/fs-provider.ts`, `lib/bbj-api.ts`.

Close the section with a one-line statement that plan 60-04 applies rows 1-5 to
`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`, and plan 60-03 applies rows 1, 3, 4, 5, 6 and
7 to `.planning/PROJECT.md`, and that no correction may be applied that does not appear in this
table.
  </action>
  <reversibility rating="costly">Once Phases 61-64 are planned against the corrected criteria,
  reverting the roadmap text desynchronises their success criteria from their coverage files (D-15).</reversibility>
  <acceptance_criteria>
    - The section exists: `grep -c 'D-15 Correction Log' .planning/reviews/INVENTORY.md` ≥ 1
    - The table has at least 7 data rows: `awk '/D-15 Correction Log/,0' .planning/reviews/INVENTORY.md | grep -cE '^\|.*\|.*\|.*\|.*\|.*\|' ` ≥ 8 (header + 7)
    - All three target documents are named in the table: `awk '/D-15 Correction Log/,0' .planning/reviews/INVENTORY.md | grep -c 'ROADMAP.md'` ≥ 1, same for `REQUIREMENTS.md` and `PROJECT.md`
    - Every row carries a verification command: `awk '/D-15 Correction Log/,0' .planning/reviews/INVENTORY.md | grep -cE 'git rev-list|ls |grep '` ≥ 7
    - The verified file counts are recorded: `ls bbj-vscode/src/language/*.ts bbj-vscode/src/language/*.langium | wc -l` prints `39` and that number appears in the log next to the corrected total; `ls bbj-vscode/src/language/validations/ | wc -l` prints `4`; `ls bbj-vscode/src/language/lib/*.ts | wc -l` prints `6`
    - The composer listing is complete: `ls bbj-vscode/src/*composer*.ts | wc -l` prints `11` and all 11 names plus `setopts-catalog.ts` appear in the log
    - The non-existent-editor row records the real target: `grep -c 'setopts-composer-webview.ts' .planning/reviews/INVENTORY.md` ≥ 1 and `grep -c 'setopts-composer-ui.ts' .planning/reviews/INVENTORY.md` ≥ 1
    - The absence of custom editors is recorded: `grep -c 'customEditors' bbj-vscode/package.json` prints `0` and that result appears as evidence
    - All 11 map-absent files are listed: `for f in bbj-code-action-provider bbj-inlay-hint-provider bbj-overload-selector bbj-use-insert composer-commands bbj-definition-provider bbj-document-symbol-provider logger check-function-calls fs-provider bbj-api; do grep -q "$f" .planning/reviews/INVENTORY.md || echo "MISSING $f"; done` prints nothing
    - The debt-count row is present: `grep -c '^- \[ \] \*\*DEBT-' .planning/REQUIREMENTS.md` prints `6` and `awk '/D-15 Correction Log/,0' .planning/reviews/INVENTORY.md | grep -c 'DEBT-'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/INVENTORY.md; grep -q "D-15 Correction Log" "$f"; for x in ROADMAP.md REQUIREMENTS.md PROJECT.md setopts-composer-webview.ts setopts-composer-ui.ts customEditors bbj-code-action-provider bbj-inlay-hint-provider bbj-overload-selector bbj-use-insert composer-commands bbj-definition-provider bbj-document-symbol-provider fs-provider bbj-api DEBT-; do grep -q "$x" "$f"; done; test "$(ls bbj-vscode/src/*composer*.ts | wc -l)" = 11; test "$(grep -c "customEditors" bbj-vscode/package.json)" = 0; echo CORRECTION_LOG_OK'</automated>
  </verify>
  <done>Every stale claim this phase corrects is in one table with its prior text, its corrected text,
  the command that establishes the truth, and that command's observed output — and plans 60-03 and
  60-04 have a row to point at for each edit they make.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated `gh` CLI → committed planning file | Tracker output fetched with the developer's credentials is transcribed into a file committed to a public repository |
| INVENTORY.md → public repository readers | Scope statements naming unfixed attack surfaces become world-readable before any fix ships |
| INVENTORY.md classification rule → Phase 67 apply path | The easy-vs-major rule decides which findings bypass MAJOR-REFACTORS.md review and land as direct commits |
| `.planning/` planning documents → Phases 61-69 success criteria | Numbers and criteria edited here become the pass/fail conditions of nine downstream phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-01 | Information Disclosure | `gh issue list` → INVENTORY.md snapshot (Task 2) | medium | mitigate | Query with `--json number,title,labels,createdAt` only. Never `--json body`, never `gh auth token`, never echo `GH_TOKEN`/`GITHUB_TOKEN`. Acceptance criterion greps the written file for token-shaped material and requires a zero count. |
| T-60-02 | Information Disclosure | RU-62-04 scope pointers and the `P00-D1-001` illustration (Task 2) | medium | mitigate | Record surface identity and dimension only — file path, why it is in scope, which requirement covers it. No payloads, no trigger sequences, no proof-of-concept. Exploit-level detail belongs in the sweep phase records and is gated behind ISSUE-01 approval before any public filing. |
| T-60-03 | Elevation of Privilege | D-13 easy-vs-major rule → Phase 67 (Task 2, sub-section 3c) | high | mitigate | Test (6) of the classification rule: a finding whose severity is `critical` or `high`, or whose primary dimension is D1, is `major` regardless of edit size. A security finding therefore cannot reach the easy-fix apply path without a MAJOR-REFACTORS.md record. Verified by the six-numbered-test acceptance criterion. |
| T-60-04 | Repudiation | D-15 in-place edits to an approved ROADMAP/REQUIREMENTS (Task 4) | medium | mitigate | Every correction is logged with prior text, corrected text, verification command and observed output before any edit is applied, and plan 60-04 may apply only rows present in that log. The amendment is auditable rather than silent. |
| T-60-05 | Tampering | INVENTORY.md immutability after Phase 60 (D-09) | low | accept | Enforced by convention plus git history, not by tooling. A tooling gate is disproportionate for a single-developer planning repository, and `git log --follow .planning/reviews/INVENTORY.md` makes any later edit visible. |
| T-60-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its only commands are `git`, `gh`, and the already-installed `npm test`/`npm run lint`/`gradlew build` harnesses. The package legitimacy gate is not applicable; no RESEARCH.md audit table is required. |
</threat_model>

<verification>
- `.planning/reviews/INVENTORY.md` exists and contains all seven top-level sections.
- The 15 frozen issue numbers resolve, and no credential-shaped string is present.
- `git rev-list --count 2194616..v0.12.0` prints `153`, and the four per-release counts recorded in
  the document sum to that value.
- The finding standard states an ID scheme, a per-dimension tier table, six numbered classification
  tests, a severity scale mapped to `PRIO 1/2/3`, and effort restricted to `2`/`4`/`8`.
- The finding record template names all thirteen fields and all six DOC-04 dispositions.
- Review unit `RU-62-04` is complete: files, LOC, risk rank, eight applicability cells with a written
  reason for each n/a, one file-exception row, dedup neighbours, and a template finding record under
  the reserved `P00` phase.
- The D-15 correction log has at least seven rows, each with a verification command and its output.
</verification>

<success_criteria>
- BASE-03 is partially satisfied: a review inventory document exists, declares its own authority and
  immutability, and defines the coverage-file convention Phases 61-65 write into.
- RVW-06 is satisfied at standard level: the evidence bar every later finding must clear is written
  down, tiered by dimension, with the drop-vs-disposition rule stated.
- RVW-07 is satisfied at standard level: the 15 open issues are frozen with the dedup rule and the
  Phase 69 re-check stated.
- A Phase 61-64 executor can record a finding without asking a single clarifying question.
</success_criteria>

<output>
Create `.planning/phases/60-baseline-resync-review-standards/60-01-SUMMARY.md` when done.
</output>
