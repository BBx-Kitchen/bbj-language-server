---
phase: 60-baseline-resync-review-standards
plan: 04
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every success criterion in `.planning/ROADMAP.md` §Phases 60-65 that quotes a figure or names a subsystem is satisfiable against the tree as it actually stands, so a phase verifier fails on a real gap rather than on a document error (D-15)"
    - "Every requirement text in `.planning/REQUIREMENTS.md` that quotes the same figures or names the same subsystem is corrected identically, so the two documents cannot disagree (D-15)"
    - "Each applied correction corresponds to a row already present in `.planning/reviews/INVENTORY.md` §\"D-15 Correction Log\"; no correction is applied that was not logged with its evidence first (D-15)"
    - "All seven `.planning/codebase/*.md` maps carry a dated banner naming what is known-wrong in that specific map, and naming `.planning/reviews/INVENTORY.md` as the authority on scope, structure and file counts for this milestone (D-16)"
    - "A Phase 61-64 reviewer who opens `CONCERNS.md` is warned, on the first screen, that it lists debt already resolved in v3.8 and cites a Langium version two majors behind the installed one — so resolved debt is not re-reported as a fresh finding (D-16)"
    - "The edit to the seven maps is additive only — no map content is deleted or rewritten, and each remains readable as historical context (D-16)"
    - "The ROADMAP Plans list and the phase checklist entries written by the planner are untouched by this plan"

  artifacts:
    - ".planning/ROADMAP.md — corrected §Phase 60, §Phase 61, §Phase 62 and §Phase 65 success criteria"
    - ".planning/REQUIREMENTS.md — corrected BASE-01, BASE-02, RVW-03 and SEC-01 texts"
    - "Seven dated staleness banners, one per `.planning/codebase/*.md` file"

  key_links:
    - "`.planning/reviews/INVENTORY.md` §D-15 Correction Log -> every edit applied by this plan (D-15)"
    - "Corrected ROADMAP §Phase 61/62 criteria -> the coverage statements Phases 61 and 62 must satisfy"
    - "Corrected ROADMAP §Phase 65 criterion 1 -> the real SEC-01 target, `setopts-composer-webview.ts`"
    - "Staleness banners -> the re-report risk Phases 61-64 would otherwise inherit from the 2026-02-01 maps (D-16)"

  prohibitions:
    - "MUST NOT weaken any ROADMAP or REQUIREMENTS success criterion, or soften a recorded test/build baseline, in the direction that makes the v4.0 milestone easier to pass — D-15 corrections are factual corrections only"
    - "MUST NOT draw a review-unit boundary or write an n/a reason that excludes a genuinely applicable dimension in order to reduce Phase 61-64 sweep cost"
    - "MUST NOT record a reconstructed capability or accomplishment claim that cannot be traced to a specific commit inside `2194616..v0.12.0`"
---

<objective>
Apply the logged corrections in place to `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`, and
mark the seven 2026-02-01 codebase maps as superseded with a banner that names what is wrong in each.

Purpose: several Phase 61-65 success criteria quote figures and name a subsystem that do not match
the tree. Left uncorrected, a phase verifier fails on a document error instead of on a real gap, and
`.planning/codebase/CONCERNS.md` actively misleads a reviewer into re-reporting debt resolved in v3.8.

This is a deliberate mid-milestone edit to an approved roadmap. It is accepted only because every edit
was logged with its evidence first (plan 60-01), and only factual corrections are applied — no
criterion is weakened.

Output: corrected `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`, and seven additive banners
under `.planning/codebase/`.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md
@.planning/phases/60-baseline-resync-review-standards/60-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Everything below is **created by Phase 60** and does not exist in the tree today. Drift verification
must treat these as new, not as references to existing symbols.

**New directories:** `.planning/reviews/`

**New files:** `.planning/reviews/INVENTORY.md`

**New naming conventions defined by Phase 60, consumed by later phases**
- Finding ID token shape: `P{phase}-D{dimension}-{seq}` (phase `00` reserved for template examples).
- Per-phase coverage file: `.planning/reviews/{NN}-COVERAGE.md` — created by Phases 61-65.
- Phase 68 deliverables: `.planning/reviews/EASY-FIXES.md`, `.planning/reviews/MAJOR-REFACTORS.md`.

**New review-unit identifiers** — token shape `RU-{phase}-{seq}`: `RU-61-01`..`RU-61-07`,
`RU-62-01`..`RU-62-05`, `RU-63-01`..`RU-63-05`, `RU-64-01`..`RU-64-03`, `RU-D8-01`.

**New applicability-grid vocabulary:** columns `D1`..`D8`; cell values `applies` | `n/a — <reason>`;
row types `unit` and `file-exception`.

**New finding-record field vocabulary:** `id`, `unit`, `location`, `dimension`, `secondary`,
`severity`, `evidence_tier`, `evidence`, `failure_scenario`, `classification`, `effort`, `dedup`,
`disposition`.

<tasks>

<!-- planner-discipline-allow: 153 -->
<!-- `153` is asserted PRESENT in both documents (the verified commit count this task writes in);
     the zero-count criteria in this task target the stale count and the stale subsystem name, which
     are different literals and appear nowhere in any <action> body. -->
<task type="auto">
  <name>Task 1: Apply the logged corrections to ROADMAP.md and REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/reviews/INVENTORY.md` §"D-15 Correction Log" — the authority. Every edit made here must match a row; if a needed edit has no row, stop and report rather than inventing one
    - `.planning/reviews/INVENTORY.md` §"Pinned Baseline Range" — the verified range, endpoint and counts
    - `.planning/ROADMAP.md` — the v4.0 phase checklist under `### 🚧 v4.0 Stability and Quality`, then `### Phase 60:` with its five success criteria, `### Phase 61:` with its four, `### Phase 62:` criterion 2, and `### Phase 65:` criterion 1. Anchor every edit on those headings; do not rely on absolute line numbers, they shift as the file is edited
    - `.planning/REQUIREMENTS.md` §Baseline (BASE-01..BASE-04), §Review Coverage (RVW-03), §Security (SEC-01)
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` §D-15 — the correction table and its rationale
    - `bbj-vscode/package.json` — the `contributes` block; `bbx-config` is a language registration for `config.bbx`/`config.min`, and there is no `customEditors` contribution
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` in place with **scoped replacements only**.
Never rewrite either file wholesale — a whole-file write destroys every entry outside the diff window.
Make one focused edit per correction.

Apply exactly these corrections, each of which has a row in the correction log:

**A. Drift-window size.** Three places in ROADMAP.md state the window's commit count one greater than
reality (the Phase 60 checklist line, the Phase 60 Goal paragraph, and Phase 60 criterion 1); one
place in REQUIREMENTS.md does the same (BASE-01). Set every one of them to the verified `153`.

**B. Drift-window endpoint.** The ROADMAP Phase 60 Goal and criterion 3, and REQUIREMENTS BASE-01,
name the moving branch tip as the window's endpoint. The verified endpoint is the `v0.12.0` tag.
Rewrite each so the range reads `2194616` → `v0.12.0`. ROADMAP criterion 3 in particular is the
criterion the new MILESTONES.md entry is checked against, so its wording and that entry's title must
name the same two endpoints.

**C. Language-core file count.** The ROADMAP Phase 61 checklist line, the Phase 61 Goal paragraph and
Phase 61 criterion 1 all state a file count for `bbj-vscode/src/language/` that is understated.
Replace it with the verified hand-written total from the correction log: 37 top-level `.ts` plus 2
`.langium`, plus 4 under `validations/`, plus 6 `lib/*.ts` — approximately 49 — alongside 4
`lib/*.bbl` catalogs. Criterion 1's enumeration also omits eleven files that exist in the tree; extend
it so the enumeration and the count agree. While editing the Phase 61 Goal, check its LOC figure
against `find bbj-vscode/src/language -maxdepth 2 -name '*.ts' -not -path '*/generated/*' | xargs wc -l`
and correct it if the measurement contradicts it — record the measured value in the summary either way.

**D. Composer file count and shape.** ROADMAP Phase 62 criterion 2 and REQUIREMENTS RVW-03 both
describe a uniform three-way `-composer`/`-ui`/`-webview` split across a stated number of files. The
verified reality is 11 composer files plus `setopts-catalog.ts`: SETOPTS has only `-ui` and
`-webview`, and no `-composer.ts`. Rewrite both so the count is 11 plus `setopts-catalog.ts` and the
asymmetry is explicit — any later template that assumes a uniform triple is wrong.

**E. The subsystem that does not exist.** Four texts name a shipped editor subsystem that is not in
the tree: ROADMAP Phase 60 criterion 1, ROADMAP Phase 65 criterion 1, REQUIREMENTS BASE-02 and
REQUIREMENTS SEC-01. `bbx-config` is a **language ID** registered in `bbj-vscode/package.json` for
`config.bbx`/`config.min`. The markup SEC-01 actually targets is generated by
`bbj-vscode/src/setopts-composer-webview.ts` and scoped to that language ID by
`bbj-vscode/src/setopts-composer-ui.ts`. In each of the four places, replace the reference to the
nonexistent subsystem with the real target, phrased so the language-ID relationship is visible rather
than implied. Use the replacement wording recorded in the correction log rather than inventing new
wording, so all four read consistently.

**Do not touch** the ROADMAP `**Plans:**` count line or the `Plans:` checklist under §Phase 60 — those
are written by the planner and are correct. Do not renumber any criterion, do not delete any
criterion, and do not soften any criterion: every edit replaces a false figure or a false subsystem
name with a verified one, and nothing else. If applying a correction would make a criterion easier to
satisfy rather than merely truer, stop and report it.

Finally, add a one-line dated amendment note at the foot of each file — beneath ROADMAP.md's
`*Roadmap last updated: ...*` line and REQUIREMENTS.md's `*Last updated: ...*` line — recording that
Phase 60 amended the document in place under D-15 and pointing at
`.planning/reviews/INVENTORY.md` §"D-15 Correction Log" for the evidence.
  </action>
  <reversibility rating="costly">Once Phases 61-64 are planned against the corrected criteria, reverting
  this text desynchronises their success criteria from their coverage files (D-15).</reversibility>
  <acceptance_criteria>
    - The stale window size is gone from both files: `grep -c '154' .planning/ROADMAP.md` prints `0` and `grep -c '154' .planning/REQUIREMENTS.md` prints `0`
    - The verified size is present in both: `grep -c '153' .planning/ROADMAP.md` ≥ 3 and `grep -c '153' .planning/REQUIREMENTS.md` ≥ 1
    - The endpoint is pinned in the Phase 60 section: `awk '/^### Phase 60:/,/^### Phase 61:/' .planning/ROADMAP.md | grep -c 'v0.12.0'` ≥ 2 and `awk '/^### Phase 60:/,/^### Phase 61:/' .planning/ROADMAP.md | grep -c 'HEAD'` prints `0`
    - The stale language-core count is gone: `grep -c '39 files' .planning/ROADMAP.md` prints `0`
    - The verified count is present in the Phase 61 section: `awk '/^### Phase 61:/,/^### Phase 62:/' .planning/ROADMAP.md | grep -c '49'` ≥ 1
    - The stale composer count is gone: `grep -c '13 webview composer files' .planning/ROADMAP.md` prints `0`
    - The composer asymmetry is stated in both files: `grep -c 'setopts-catalog.ts' .planning/ROADMAP.md` ≥ 1 and `grep -c 'setopts-catalog.ts' .planning/REQUIREMENTS.md` ≥ 1
    - The nonexistent subsystem is gone from both: `grep -ciE 'bbx-config[ -]editor' .planning/ROADMAP.md` prints `0` and `grep -ciE 'bbx-config[ -]editor' .planning/REQUIREMENTS.md` prints `0`
    - The real SEC-01 target is named in both: `grep -c 'setopts-composer-webview.ts' .planning/ROADMAP.md` ≥ 1 and `grep -c 'setopts-composer-webview.ts' .planning/REQUIREMENTS.md` ≥ 1
    - Nothing was structurally removed: `grep -c '^### Phase 6' .planning/ROADMAP.md` prints `10` and `grep -cE '^- \[ \] \*\*(BASE|RVW|SEC|DEBT|FIX|DOC|ISSUE)-' .planning/REQUIREMENTS.md` prints `38`
    - The planner-owned Plans block is untouched: `awk '/^### Phase 60:/,/^### Phase 61:/' .planning/ROADMAP.md | grep -c 'PLAN.md'` ≥ 4
    - Both amendment notes exist: `tail -n 5 .planning/ROADMAP.md | grep -c 'D-15'` ≥ 1 and `tail -n 5 .planning/REQUIREMENTS.md | grep -c 'D-15'` ≥ 1
    - Every applied edit has a logged row: each grep above that changed a value pairs with a row in `.planning/reviews/INVENTORY.md` naming `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; r=.planning/ROADMAP.md; q=.planning/REQUIREMENTS.md; test "$(grep -c "154" $r)" = 0; test "$(grep -c "154" $q)" = 0; test "$(grep -c "39 files" $r)" = 0; test "$(grep -c "13 webview composer files" $r)" = 0; test "$(grep -ciE "bbx-config[ -]editor" $r)" = 0; test "$(grep -ciE "bbx-config[ -]editor" $q)" = 0; test "$(grep -c "153" $r)" -ge 3; for x in setopts-catalog.ts setopts-composer-webview.ts; do grep -q "$x" $r; grep -q "$x" $q; done; test "$(grep -c "^### Phase 6" $r)" = 10; test "$(grep -cE "^- \[ \] \*\*(BASE|RVW|SEC|DEBT|FIX|DOC|ISSUE)-" $q)" = 38; echo AMEND_OK'</automated>
  </verify>
  <done>Every figure and subsystem name in ROADMAP.md §Phases 60-65 and in REQUIREMENTS.md's BASE/RVW/SEC
  texts matches the tree; all ten phase sections and all 38 requirement rows survive; the planner-owned
  Plans block is unchanged; and both files carry a dated note pointing at the correction log.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the seven codebase maps superseded with a specific staleness banner</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - All seven `.planning/codebase/*.md` files — read each one; the banner must name what is wrong in **that** map, and you cannot write that without knowing what it claims. Each opens with a title line and an `**Analysis Date:** 2026-02-01` line
    - `.planning/reviews/INVENTORY.md` §"Review Units", §"Surface Accounting & Named Exclusions" — the document the banners name as the authority
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` §D-16 and §`<specifics>` — the banner must name what is known-wrong, not merely say the map may be outdated
    - `bbj-vscode/package.json` — the installed `langium` version, for the version claims
  </read_first>
  <action>
Insert a staleness banner into each of the seven `.planning/codebase/*.md` files, immediately after
its `**Analysis Date:** 2026-02-01` line. The edit is **additive only** — do not delete, reword or
reorder any existing map content. The maps stay readable as historical context; they simply stop being
authoritative.

Every banner carries the same three fixed elements:
1. A `> ⚠ SUPERSEDED — 2026-08-17` opening line.
2. One sentence naming `.planning/reviews/INVENTORY.md` as the authority on review scope, structure
   and file counts for the v4.0 milestone, and stating that this map was not regenerated (D-16).
3. A short `Known-wrong in this map:` list — **specific to that file**, at least two bullets, each one
   you have verified against the tree.

A generic "may be outdated" banner fails this task. The point is to stop a Phase 61-64 reviewer from
re-reporting resolved debt as a fresh finding.

Per-file starting points — verify each before writing it, and add anything else you find:

- **CONCERNS.md — the highest re-report risk; say so in the banner.** It lists FIXMEs already resolved
  in v3.8 (Phase 56 resolved four production FIXMEs; the linker receiver ref, scope orphaned AST,
  javadoc cancellation and InteropService inner-class items are the ones to check) and it cites a
  Langium version two majors behind the installed one. Verify the installed version with
  `grep '"langium"' bbj-vscode/package.json` and verify the FIXME state with
  `grep -rn 'FIXME' bbj-vscode/src --include='*.ts' | grep -v generated`. Name both findings
  explicitly.
- **STRUCTURE.md** — predates `bbj-intellij/` entirely, and its file counts and directory tree omit
  the drift window's additions: the four webview composers, `setopts-catalog.ts`, inlay hints,
  `Commands/CompilerOptions.ts`, the document formatter and line numbering. Verify with
  `ls bbj-intellij` and `ls bbj-vscode/src`.
- **STACK.md** — its Langium version claim is wrong against the installed version; it also predates
  the IntelliJ toolchain and LSP4IJ entries. Verify each version claim you name.
- **ARCHITECTURE.md** — predates the BBjCPL compiler integration, the logger singleton, the composer
  subsystem, and the whole IntelliJ/LSP4IJ side of the architecture. Verify by checking for
  `bbj-cpl-service.ts`, `logger.ts` and `bbj-intellij/`.
- **CONVENTIONS.md** — predates the `-composer`/`-ui`/`-webview` module convention (and its SETOPTS
  exception), and the logger-over-console convention established in v3.3.
- **INTEGRATIONS.md** — predates the configurable java-interop host and port, EM token-based
  authentication, and the BBjCPL compiler integration.
- **TESTING.md** — its suite-state assumptions predate the drift window; the current measured state is
  in `.planning/reviews/INVENTORY.md` §"Test & Build Baseline" and includes failing and skipped tests
  that did not exist when the map was written. Point at the inventory section rather than restating
  numbers that will age.

Do not regenerate any map. Regeneration was considered and rejected for this phase: Phases 61-64 are
about to read every one of these files directly, and their output will be far better material than a
fresh mapping pass.
  </action>
  <acceptance_criteria>
    - All seven files carry the banner: `grep -L 'SUPERSEDED' .planning/codebase/*.md` prints nothing
    - All seven are dated: `grep -l '2026-08-17' .planning/codebase/*.md | wc -l` prints `7`
    - All seven name the authority: `grep -L 'reviews/INVENTORY.md' .planning/codebase/*.md` prints nothing
    - All seven name at least two specific wrongness bullets: `for f in .planning/codebase/*.md; do n=$(sed -n '/SUPERSEDED/,/^## /p' "$f" | grep -cE '^> *[-*] '); test "$n" -ge 2 || echo "THIN $f"; done` prints nothing
    - CONCERNS.md flags the two named traps: `grep -ci 'FIXME' .planning/codebase/CONCERNS.md` ≥ 1 within its banner block, and `sed -n '/SUPERSEDED/,/^## /p' .planning/codebase/CONCERNS.md | grep -c '4.1.3'` ≥ 1
    - STRUCTURE.md flags the missing IntelliJ side: `sed -n '/SUPERSEDED/,/^## /p' .planning/codebase/STRUCTURE.md | grep -c 'bbj-intellij'` ≥ 1
    - The edit is additive only: `git diff -- .planning/codebase/ | grep -cE '^-[^-]'` prints `0`
    - No map was regenerated: `git diff --stat -- .planning/codebase/ | tail -1` shows insertions and zero deletions
    - The installed Langium version claim is verified, not assumed: `grep '"langium"' bbj-vscode/package.json` output is recorded in the plan summary
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -z "$(grep -L "SUPERSEDED" .planning/codebase/*.md)"; test -z "$(grep -L "reviews/INVENTORY.md" .planning/codebase/*.md)"; test "$(grep -l "2026-08-17" .planning/codebase/*.md | wc -l)" = 7; test "$(git diff -- .planning/codebase/ | grep -cE "^-[^-]")" = 0; sed -n "/SUPERSEDED/,/^## /p" .planning/codebase/CONCERNS.md | grep -qi FIXME; sed -n "/SUPERSEDED/,/^## /p" .planning/codebase/STRUCTURE.md | grep -q bbj-intellij; echo BANNERS_OK'</automated>
  </verify>
  <done>All seven maps open with a dated superseded banner that names `.planning/reviews/INVENTORY.md`
  as the authority and lists at least two verified, map-specific wrong claims; CONCERNS.md's banner
  names both the v3.8-resolved debt trap and the wrong Langium version; and the git diff over
  `.planning/codebase/` contains insertions only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INVENTORY.md correction log → approved ROADMAP/REQUIREMENTS | An in-place edit to an approved milestone contract; anything applied without a logged row is an unauditable change to the pass conditions of nine phases |
| Corrected success criteria → Phase 61-65 verifiers | Criterion text is the pass/fail condition; an edit that makes a criterion easier to satisfy silently reduces the milestone's scope |
| Superseded maps → Phase 61-64 reviewers | A reviewer trusting a stale map re-reports resolved debt, consuming sweep budget and polluting the finding set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-13 | Tampering | Scope reduction disguised as correction (Task 1) | high | mitigate | Only corrections with a row in `.planning/reviews/INVENTORY.md` §"D-15 Correction Log" may be applied, and each replaces a false figure or a false subsystem name with a verified one. The task states explicitly that an edit which makes a criterion easier to satisfy rather than truer must be reported instead of applied. Structural acceptance criteria assert all ten phase sections and all 38 requirement rows survive. |
| T-60-14 | Repudiation | In-place amendment of an approved roadmap (Task 1) | medium | mitigate | Both files gain a dated foot note recording the amendment and pointing at the correction log, so the change is discoverable from the document itself rather than only from git history. |
| T-60-15 | Denial of Service | Whole-file rewrite of ROADMAP.md or REQUIREMENTS.md (Task 1) | medium | mitigate | Scoped replacement only, one focused edit per correction; a whole-file write destroys every entry outside the diff window. Acceptance criteria count the surviving phase sections and requirement rows. |
| T-60-16 | Tampering | Deletion of map content while bannering (Task 2) | medium | mitigate | The banner is inserted after the existing analysis-date line and nothing else changes; `git diff -- .planning/codebase/ | grep -cE '^-[^-]'` must print `0`. |
| T-60-17 | Information Disclosure | Banner text naming known-wrong claims | low | accept | The banners name stale internal documentation claims and version numbers already visible in `package.json`; they add no non-public information and describe no attack surface. |
| T-60-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `grep`, `sed`, `ls`, `find` and `git diff`. The package legitimacy gate is not applicable; no RESEARCH.md audit table is required. |
</threat_model>

<verification>
- `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` contain no stale window size, no stale
  language-core file count, no stale composer file count and no reference to the nonexistent editor
  subsystem; the verified figures and the real SEC-01 target appear in both.
- All ten `### Phase 6N` sections and all 38 requirement rows survive; the planner-owned Plans block
  under §Phase 60 is unchanged.
- Both documents carry a dated amendment note pointing at the correction log.
- All seven `.planning/codebase/*.md` files carry a dated superseded banner naming the inventory as
  the authority and at least two verified, file-specific wrong claims.
- `git diff -- .planning/codebase/` contains insertions only.

</verification>

<success_criteria>
- Every Phase 61-65 success criterion is satisfiable against the tree, so a verifier fails on a real
  gap rather than a document error (D-15).
- The two milestone-contract documents agree with each other and with the inventory.
- A Phase 61-64 reviewer opening any codebase map is told, on the first screen, that it is superseded
  and exactly what it gets wrong (D-16).
</success_criteria>

<output>
Create `.planning/phases/60-baseline-resync-review-standards/60-04-SUMMARY.md` when done.
</output>
