---
phase: 60-baseline-resync-review-standards
plan: 03
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-04]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PROJECT.md's Validated list names every user-visible capability shipped in `2194616..v0.12.0`, one line per capability in the existing one-line style, with issue numbers appended inline where the underlying commit references one (BASE-01, D-03)"
    - "Every reconstructed Validated entry is labelled by release tag — `— 0.9.0`, `— 0.10.0`, `— 0.11.0`, `— 0.12.0` — not by GSD milestone, with a one-line note in the document saying why the convention differs for this block (D-02)"
    - "PROJECT.md's Context, Constraints and Key Decisions read as true on the day of writing, not as of v3.9 (BASE-02, D-17)"
    - "MILESTONES.md contains one entry for the untracked release window, titled for the range `2194616` → `v0.12.0` at 153 commits, carrying a per-release breakdown table for 0.9.0, 0.10.0, 0.11.0 and 0.12.0 (BASE-04, D-04)"
    - "Project history contains no unexplained gap between the v3.9 entry and the start of v4.0 (BASE-04)"

    # ~~ edge probe: BASE-01 (resolved, explicit)
    - "BASE-01 adjacency: a single commit is attributed to the first release tag that contains it (`git tag --contains <sha>`, earliest by creation date); a capability spanning several commits is attributed to the tag of its last commit and is listed exactly once, so a capability straddling a tag boundary neither duplicates nor disappears (D-02, D-03)"
    - "BASE-01 empty: a release inside the window with no user-visible capability still appears — in MILESTONES.md's breakdown table with its commit count and an explicit `— none user-visible` cell — rather than being omitted (D-03, D-04)"
    - "BASE-01 ordering: reconstructed Validated entries are appended as one release-grouped block per tag in ascending release order 0.9.0 → 0.12.0, and within a release in ascending commit-date order; entries sharing a date keep `git log` order (D-02, D-03)"

    # ~~ edge probe: BASE-02 (resolved, explicit)
    - "BASE-02 adjacency: a reconstructed Key Decisions row that restates an existing row is not appended; the existing row is left untouched and only decisions not already present are added (D-17)"
    - "BASE-02 empty: PROJECT.md sections with nothing demonstrably false are left byte-identical, and the ⚠ Planning-drift note is retained as evidence of how this milestone arose rather than deleted (D-17)"
    - "BASE-02 ordering: new Key Decisions rows are appended as one contiguous block after the existing final row, in release order, and no existing row changes position (D-17)"

    # ~~ edge probe: BASE-04 (resolved, explicit)
    - "BASE-04 adjacency: 0.10.0 and 0.11.0, both tagged 2026-07-18, get separate rows ordered by tag creation date rather than being merged into one (D-04)"
    - "BASE-04 empty: each of the four tags has exactly one row in the breakdown table even when its headline-capabilities cell is empty of user-visible work (D-04)"
    - "BASE-04 ordering: the new entry is appended after the v3.9 entry at the end of MILESTONES.md, and the breakdown table rows run in ascending tag-creation order (D-04)"

  artifacts:
    - ".planning/PROJECT.md — reconstructed Validated block (approximately 15-25 entries) plus corrected Context, Constraints and Key Decisions"
    - ".planning/MILESTONES.md — one new entry for the untracked release window with a four-row per-release breakdown table"

  key_links:
    - "Release-tag attribution -> the per-release breakdown table in MILESTONES.md (D-02 ↔ D-04 must agree)"
    - "`.planning/reviews/INVENTORY.md` §Pinned Baseline Range -> the commit counts and tag dates both documents quote (D-01)"
    - "`.planning/reviews/INVENTORY.md` §Test & Build Baseline -> PROJECT.md's Context test-suite state (D-05)"
    - "`.planning/reviews/INVENTORY.md` §D-15 Correction Log -> every PROJECT.md correction applied here must have a logged row (D-15)"

  prohibitions:
    - "MUST NOT weaken any ROADMAP or REQUIREMENTS success criterion, or soften a recorded test/build baseline, in the direction that makes the v4.0 milestone easier to pass — D-15 corrections are factual corrections only"
    - "MUST NOT draw a review-unit boundary or write an n/a reason that excludes a genuinely applicable dimension in order to reduce Phase 61-64 sweep cost"
    - "MUST NOT record a reconstructed PROJECT.md Validated entry or MILESTONES.md accomplishment that cannot be traced to a specific commit inside `2194616..v0.12.0`"
---

<objective>
Resync the two historical planning documents with what actually shipped. Reconstruct the user-visible
capabilities of the 153-commit window between the end of v3.9 and the `v0.12.0` tag into PROJECT.md's
Validated list, correct PROJECT.md's Context, Constraints and Key Decisions to read as true today,
and add the single MILESTONES.md entry that closes the unexplained gap in project history.

Purpose: four public releases shipped without going through a GSD milestone. Until that work is
recorded, the Validated list understates the product, the tech-debt list carries items already fixed,
and MILESTONES.md shows a six-month hole.

Output: an updated `.planning/PROJECT.md` and a new entry at the end of `.planning/MILESTONES.md`.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md
@.planning/phases/60-baseline-resync-review-standards/60-01-SUMMARY.md
</context>

## Artifacts this phase produces

Everything below is **created by Phase 60** and does not exist in the tree today. Drift verification
must treat these as new, not as references to existing symbols.

**New directories:** `.planning/reviews/`

**New files:** `.planning/reviews/INVENTORY.md`

**New naming conventions defined by Phase 60, consumed by later phases**
- Finding ID token shape: `P{phase}-D{dimension}-{seq}` (phase `00` reserved for template examples).
- Per-phase coverage file: `.planning/reviews/{NN}-COVERAGE.md` — created by Phases 61-65.
- Phase 68 deliverables: `.planning/reviews/EASY-FIXES.md`, `.planning/reviews/MAJOR-REFACTORS.md`.

**New review-unit identifiers** — token shape `RU-{phase}-{seq}`: `RU-61-01`..`RU-61-07`,
`RU-62-01`..`RU-62-05`, `RU-63-01`..`RU-63-05`, `RU-64-01`..`RU-64-03`, `RU-D8-01`.

**New applicability-grid vocabulary:** columns `D1`..`D8`; cell values `applies` | `n/a — <reason>`;
row types `unit` and `file-exception`.

**New finding-record field vocabulary:** `id`, `unit`, `location`, `dimension`, `secondary`,
`severity`, `evidence_tier`, `evidence`, `failure_scenario`, `classification`, `effort`, `dedup`,
`disposition`.

**New Validated-list label form introduced by this plan:** release-tag suffixes `— 0.9.0`,
`— 0.10.0`, `— 0.11.0`, `— 0.12.0`, deliberately distinct from the existing `— vN.M` GSD-milestone
suffix.

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct the shipped capabilities into PROJECT.md's Validated list</name>
  <files>.planning/PROJECT.md</files>
  <precondition>The full git history is available locally — `git rev-list --count 2194616..v0.12.0`
  succeeds and the tags `v0.9.0`, `v0.10.0`, `v0.11.0`, `v0.12.0` all resolve. Halt if the clone is
  shallow.</precondition>
  <read_first>
    - `.planning/PROJECT.md` §`### Validated` (from that heading down to `### Active`) — the existing list; the one-line-per-capability convention with an inline `(#issue)` and a trailing `— vN.M` suffix is the format this block extends, not replaces. Anchor on headings, not absolute line numbers
    - `.planning/reviews/INVENTORY.md` §"Pinned Baseline Range" — the authoritative range, the four tags with dates and targets, and the per-release commit counts; do not re-derive them, quote them
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` §D-01, §D-02, §D-03
    - `.planning/ROADMAP.md` §Phase 60 criterion 1 — the subsystems it names as needing to appear
  </read_first>
  <action>
Reconstruct the user-visible capabilities of the range `2194616..v0.12.0` and append them to
`.planning/PROJECT.md`'s `### Validated` list, immediately after the final v3.9 entry and before the
`### Active` heading.

**Derive the capability set.** Walk the range release by release:
`git log --oneline --no-merges 2194616..v0.9.0`, then `v0.9.0..v0.10.0`, then `v0.10.0..v0.11.0`,
then `v0.11.0..v0.12.0`. For any commit whose subject is ambiguous, read the diff stat with
`git show --stat <sha>` to decide what it shipped. Group the commits into user-visible capabilities —
things a BBj developer using VS Code or IntelliJ can now do that they could not before.

**Granularity is one entry per capability (D-03), not one per issue reference and not one per
release.** The four webview composers alone span issues #474 and #483; splitting by issue number
fragments a single capability across rows, and one block per release breaks the flat list format and
makes coverage unverifiable claim by claim. Expect roughly 15-25 entries in total.

**Format.** Match the existing convention exactly: `- ✓ <capability> (#issue) — <release>`, with the
issue reference present only where the underlying commit cites one, and multiple issues
comma-separated inside the single parenthesis. The release suffix is the **release tag**, written
without a leading `v`: `— 0.9.0`, `— 0.10.0`, `— 0.11.0`, `— 0.12.0`.

**Attribution.** Attribute a capability to the tag containing its **last** commit — that is the
release under which it became shippable. For a single-commit capability that is the same as the first
tag containing it, which you can confirm with `git tag --contains <sha> --sort=creatordate | head -1`.
A capability is listed exactly once regardless of how many commits or how many tag boundaries it
crosses.

**Ordering.** Append as four release-grouped blocks in ascending release order, separated by a blank
line the way the existing per-milestone blocks are. Within a block, order by ascending commit date;
where two capabilities share a date, keep `git log` order. If a release contains no user-visible
capability, write no entries for it here — its presence is recorded in the MILESTONES.md breakdown
table instead (Task 3), and add a one-line note in this block saying so.

**Label-convention note.** Immediately above the reconstructed block, add a single italic line stating
that these entries are labelled by release tag rather than by GSD milestone because the work never
went through a GSD milestone, and that labelling it as if it had would be false.

**Coverage floor.** The subsystems ROADMAP §Phase 60 criterion 1 names must all be represented:
the four webview composers (msgbox, addwindow, addchildwindow, SETOPTS), inlay hints,
`Commands/CompilerOptions.ts`, the document formatter, and line numbering. Criterion 1 also names an
editor subsystem that does not exist — `bbx-config` is a language ID registered in
`bbj-vscode/package.json` for `config.bbx`/`config.min`, and the markup involved is generated by
`bbj-vscode/src/setopts-composer-webview.ts`. Do **not** invent a Validated entry for a subsystem
that is not in the tree; record the real capability instead, and confirm the correction is already
logged in `.planning/reviews/INVENTORY.md` §"D-15 Correction Log".

Every entry must be traceable to at least one commit inside the range. If you cannot name the commit,
do not write the entry.
  </action>
  <acceptance_criteria>
    - Entry count is in range: `awk '/^- ✓ .*— 0\.(9|10|11|12)\.0$/' .planning/PROJECT.md | wc -l` is between 15 and 25
    - All four release labels are used: `for r in '— 0.9.0' '— 0.10.0' '— 0.11.0' '— 0.12.0'; do grep -q -- "$r" .planning/PROJECT.md || echo "MISSING $r"; done` prints nothing
    - Every reconstructed entry uses the existing one-line format: `grep -cE '^- ✓ .+ — 0\.(9|10|11|12)\.0$' .planning/PROJECT.md` equals the entry count above (no multi-line or bulleted sub-entries)
    - The named subsystems all appear: `for s in msgbox addwindow addchildwindow SETOPTS 'inlay hint' CompilerOptions formatter 'line number'; do grep -qi "$s" .planning/PROJECT.md || echo "MISSING $s"; done` prints nothing
    - The block sits before the Active heading: the line number of the last `— 0.12.0` entry is less than the line number of `### Active`
    - The label-convention note is present: `grep -ci 'release tag' .planning/PROJECT.md` ≥ 1
    - No entry claims a subsystem absent from the tree: `grep -ci 'bbx-config editor' .planning/PROJECT.md` prints `0`
    - Existing entries are untouched: `git diff -- .planning/PROJECT.md | grep -cE '^-- ✓ .*— v[0-9]'` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/PROJECT.md; n=$(grep -cE "^- ✓ .+ — 0\.(9|10|11|12)\.0$" "$f"); test "$n" -ge 15 -a "$n" -le 25; for r in "— 0.9.0" "— 0.10.0" "— 0.11.0" "— 0.12.0"; do grep -q -- "$r" "$f"; done; for s in msgbox addwindow addchildwindow SETOPTS "inlay hint" CompilerOptions formatter; do grep -qi "$s" "$f"; done; test "$(grep -ci "bbx-config editor" "$f")" = 0; a=$(grep -n "^### Active" "$f" | head -1 | cut -d: -f1); l=$(grep -n -- "— 0.12.0" "$f" | tail -1 | cut -d: -f1); test "$l" -lt "$a"; echo VALIDATED_OK'</automated>
  </verify>
  <done>Between 15 and 25 new one-line Validated entries, each labelled with its release tag, each
  traceable to a commit inside the pinned range, covering every subsystem the roadmap names — and no
  existing entry moved or rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Correct PROJECT.md's Context, Constraints and Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` §`## Current Milestone: v4.0 Stability and Quality` (including its `**Target features:**` list), §`## Context`, §`## Constraints` and §`## Key Decisions` — the current text being corrected. Anchor on headings, not absolute line numbers: Task 1 shifts every line after `### Validated`
    - `.planning/reviews/INVENTORY.md` §"D-15 Correction Log" — the authority for every correction applied here; no correction may be applied that has no row
    - `.planning/reviews/INVENTORY.md` §"Test & Build Baseline" — the measured figures replacing the stale test-suite claim
    - `.planning/reviews/INVENTORY.md` §"Pinned Baseline Range" — the verified commit count, endpoint and per-release counts
    - `.planning/STATE.md` §"Active Constraints", §"Tech Debt" — the constraint and debt state currently believed true
    - `.planning/REQUIREMENTS.md` §Debt Re-triage — the authoritative carried-debt item list
  </read_first>
  <action>
Update `.planning/PROJECT.md` by **appending new rows and correcting only what is demonstrably false
today** (D-17). This is not a rewrite and not a full re-audit of the roughly 110-row Key Decisions
table — that would be a review sweep in its own right and duplicates what Phases 61-64 do against the
code. Leave anything you cannot demonstrate to be false byte-identical.

**A. Current Milestone / Target features** (§`## Current Milestone: v4.0 Stability and Quality`).
Two claims here are wrong and both have a
row in the correction log: the drift-window commit count is one greater than the verified value, and
the `bbj-vscode/src/language/` file count is understated. Correct both to the verified figures from
the log. Also reconcile the carried-debt item count with `.planning/REQUIREMENTS.md`'s DEBT-01..DEBT-06
and ROADMAP §Phase 66 criterion 5, per the log's debt-count row.

**B. Context** (§`## Context`).
- The `**Current state:**` paragraph presents the v3.9 test-suite figures as though they were the
  current state. They are not. Replace the parenthetical with the measured state recorded in
  `.planning/reviews/INVENTORY.md` §"Test & Build Baseline", keep the v3.9 figures as an explicitly
  labelled historical reference, and cross-reference the inventory section rather than duplicating
  the whole snapshot.
- The `**⚠ Planning drift**` note is **retained** — it is the evidence of how this milestone arose and
  must not be deleted (D-17). Correct its three false elements: the commit count, the endpoint (it
  names the moving branch tip; the verified endpoint is the `v0.12.0` tag), and the list of drifted
  subsystems, which names an editor subsystem that does not exist. Then append one sentence recording
  that Phase 60 resolved the drift, with the date.
- Add the unreleased in-flight fix to Context: `a7e1b53 fix(#494): terminate the visibility hierarchy
  walk on cyclic inheritance` is committed on branch `issue494-cyclic-inheritance-hang` but is outside
  the `v0.12.0` tag and therefore unreleased. State it plainly so a Phase 61 reviewer does not report
  the cyclic-inheritance hang as a live finding.
- Update `**Tech stack:**` only where a version is demonstrably wrong against the tree; verify each
  version claim you touch with a command and note nothing you did not verify.

**C. Known tech debt** (the `**Known tech debt:**` list at the foot of §`## Context`). Check each of the eight bullets against the current tree and
against `.planning/REQUIREMENTS.md`'s DEBT-01..DEBT-06. Remove only a bullet you can demonstrate is
resolved — cite the commit or the code state that resolves it. Keep the rest verbatim. Where a bullet
survives, append its DEBT-NN identifier so Phase 66's re-triage has a direct handle. Record in the
plan summary which bullets you checked and what evidence each check produced, including the ones you
kept.

**D. Constraints** (§`## Constraints`). Correct only what is false. The four current constraints concern
Community Edition support, the Node.js runtime dependency, leaving the language server unchanged for
IntelliJ, and deferring Langium 4 features — check each against the tree before touching it.

**E. Key Decisions.** Append a single contiguous block of new rows after the existing final row, in
release order, in the existing three-column `| Decision | Rationale | Outcome |` format with the
`✓ Good — <release> shipped` outcome style. Reconstruct the decisions behind the capabilities Task 1
recorded — at minimum the composer architecture (its `-composer`/`-ui`/`-webview` split and the
SETOPTS deviation from it), inlay hints, the document formatter, line numbering, overload selection,
and code actions. Derive each from the commits, not from imagination: if you cannot name the commit
that embodies the decision, do not write the row. Do not append a row that restates an existing row —
scan the table first and leave the existing row untouched instead. No existing row changes position.
  </action>
  <acceptance_criteria>
    - The stale drift-window count is gone from the file: `grep -c '154' .planning/PROJECT.md` prints `0`
    - The verified count is present: `grep -c '153' .planning/PROJECT.md` ≥ 1
    - The stale file count claim is gone: `grep -c '39 files' .planning/PROJECT.md` prints `0`
    - The stale test-suite claim is gone: `grep -c '511 passed' .planning/PROJECT.md` prints `0`
    - The drift note is retained: `grep -c 'Planning drift' .planning/PROJECT.md` ≥ 1
    - The nonexistent subsystem is gone: `grep -ci 'bbx-config editor' .planning/PROJECT.md` prints `0`
    - The unreleased in-flight fix is named: `grep -c 'a7e1b53' .planning/PROJECT.md` ≥ 1
    - The inventory is cross-referenced: `grep -c 'reviews/INVENTORY.md' .planning/PROJECT.md` ≥ 1
    - Surviving debt bullets carry a DEBT identifier: `awk '/Known tech debt/,/^## Constraints/' .planning/PROJECT.md | grep -c 'DEBT-'` ≥ 1
    - New Key Decisions rows were appended and no existing row was removed: `git diff -- .planning/PROJECT.md | grep -c '^+|'` ≥ 6 and `git diff -- .planning/PROJECT.md | grep -c '^-|'` prints `0`
    - Every applied correction has a logged row: for each `grep -c` assertion above that changed a value, the corresponding `Document = .planning/PROJECT.md` row exists in `.planning/reviews/INVENTORY.md` §"D-15 Correction Log"
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/PROJECT.md; test "$(grep -c "154" "$f")" = 0; test "$(grep -c "39 files" "$f")" = 0; test "$(grep -c "511 passed" "$f")" = 0; test "$(grep -ci "bbx-config editor" "$f")" = 0; for x in "153" "Planning drift" a7e1b53 reviews/INVENTORY.md; do grep -q "$x" "$f"; done; test "$(git diff -- "$f" | grep -c "^-|")" = 0; test "$(git diff -- "$f" | grep -c "^+|")" -ge 6; echo PROJECT_OK'</automated>
  </verify>
  <done>PROJECT.md's Context, Constraints and Key Decisions read as true on the day of writing: the
  stale commit count, file count and test-suite claim are gone, the drift note survives as corrected
  evidence with a resolution line, the unreleased in-flight fix is visible, resolved debt bullets are
  removed with cited evidence while survivors carry their DEBT identifier, and the new decision rows
  sit in one appended block with no existing row moved or deleted.</done>
</task>

<!-- planner-discipline-allow: 2026-07-18 -->
<!-- `2026-07-18` is asserted PRESENT (the two same-day release rows), never absent; the adjacent
     zero-count criterion asserts the diff is pure addition and is unrelated to this literal. -->
<task type="auto">
  <name>Task 3: Add the MILESTONES.md entry for the untracked release window</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - `.planning/MILESTONES.md` lines 443-481 — the v3.9 entry; its section shape (`**Delivered:**`, `**Phases completed:**`, `**Key accomplishments:**`, `**Stats:**`, `**Git range:**`, `**Tech debt accepted:**`, `**What's next:**`) is the format this entry follows, and its `**Stats:**` block carries the prior test-suite figures used for contrast
    - `.planning/MILESTONES.md` lines 3-27 — the v3.4 entry, as a second example of the same shape
    - `.planning/reviews/INVENTORY.md` §"Pinned Baseline Range" — the four tags, their dates, their targets and their exact per-release commit counts
    - `.planning/reviews/INVENTORY.md` §"Test & Build Baseline" — the measured end-of-window test state
    - `.planning/PROJECT.md` §Validated — the reconstructed capability entries written in Task 1; the headline features in the breakdown table must agree with them
    - `.planning/phases/60-baseline-resync-review-standards/60-CONTEXT.md` §D-04
    - `.planning/ROADMAP.md` §Phase 60 criterion 3 — the criterion this entry satisfies
  </read_first>
  <action>
Append one new entry to the end of `.planning/MILESTONES.md`, after the v3.9 entry, following the
existing entry shape exactly.

**One entry, not four.** Three of the four releases shipped within 48 hours of each other; four
separate entries would produce thin entries that overshoot the criterion. The per-release detail lives
in a breakdown table inside this single entry (D-04).

**Title and range.** Title the entry for the drift window and give it a `**Git range:**` line reading
`` `2194616` → `v0.12.0` (153 commits) ``. The literal tokens `2194616`, `v0.12.0` and `153` must all
appear, because ROADMAP §Phase 60 criterion 3 is checked against them.

**Sections, in the existing order:**
- `**Delivered:**` — one paragraph naming what the window shipped, in the voice the other entries use.
- `**Phases completed:**` — state plainly that this window went through **no GSD phases**; it is
  reconstructed after the fact by Phase 60 of v4.0. Do not invent phase numbers.
- `**Key accomplishments:**` — one bullet per capability, agreeing with the Validated entries Task 1
  wrote. Where they disagree, the Validated list is wrong or this list is wrong; fix whichever is not
  supported by a commit.
- **Per-release breakdown table** — this is the required addition. Columns: `Release`, `Tag date`,
  `Tag target`, `Commits`, `Headline capabilities`. Exactly four rows: 0.9.0, 0.10.0, 0.11.0, 0.12.0,
  in ascending tag-creation order. 0.10.0 and 0.11.0 are both dated 2026-07-18 and stay as two rows.
  A release whose window contains no user-visible capability keeps its row with its commit count and
  a `— none user-visible` headline cell. Add a totals row whose `Commits` cell equals the range total,
  so the arithmetic is visible on the page.
- `**Stats:**` — files changed and line delta across the range (`git diff --shortstat 2194616 v0.12.0`),
  the commit count, the calendar span, and the end-of-window test-suite state taken from the inventory
  baseline. Note the contrast with the v3.9 entry's recorded suite state, which is the last GSD-tracked
  measurement.
- `**Tech debt accepted:**` — the failing and skipped tests the window introduced, cross-referenced to
  the inventory's routing table rather than restated in full.
- `**What's next:**` — that v4.0 Stability and Quality reviews this window's output.

Do not modify any existing entry.
  </action>
  <acceptance_criteria>
    - The entry exists at the end of the file: the last `^## ` heading in `.planning/MILESTONES.md` is the new entry, not `## v3.9 Quick Wins`
    - The three criterion tokens are present in the new entry: `tail -n 60 .planning/MILESTONES.md | grep -c '2194616'` ≥ 1, `... | grep -c 'v0.12.0'` ≥ 1, `... | grep -c '153'` ≥ 1
    - The breakdown table has exactly four release rows plus a totals row: `awk '/^## /{s=0} /Tag date/{s=1} s' .planning/MILESTONES.md | grep -cE '^\| *0\.(9|10|11|12)\.0 *\|'` prints `4`
    - Both same-day releases have their own row: `tail -n 60 .planning/MILESTONES.md | grep -c '2026-07-18'` ≥ 2
    - The per-release commit counts sum to the range total: the four `Commits` cells are `93`, `38`, `9`, `13` and the totals row reads `153`
    - No existing entry changed — the diff is pure addition: `git diff -- .planning/MILESTONES.md | grep -cE '^-[^-]'` prints `0`
    - The entry follows the existing section shape: `tail -n 60 .planning/MILESTONES.md | grep -c '\*\*Delivered:\*\*'` ≥ 1, same for `**Key accomplishments:**`, `**Stats:**` and `**Git range:**`
    - The no-GSD-phases fact is stated rather than fabricated: `tail -n 60 .planning/MILESTONES.md | grep -ciE 'no GSD phases|reconstructed'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/MILESTONES.md; test "$(grep -c "^## " "$f")" -ge 20; tail -n 80 "$f" > /tmp/ms.$$; for x in 2194616 v0.12.0 153 "**Delivered:**" "**Key accomplishments:**" "**Stats:**" "**Git range:**"; do grep -q -- "$x" /tmp/ms.$$; done; test "$(grep -cE "^\| *0\.(9|10|11|12)\.0 *\|" /tmp/ms.$$)" = 4; test "$(grep -c "2026-07-18" /tmp/ms.$$)" -ge 2; test "$(git diff -- "$f" | grep -cE "^-[^-]")" = 0; rm -f /tmp/ms.$$; echo MILESTONES_OK'</automated>
  </verify>
  <done>MILESTONES.md ends with one entry titled for the `2194616` → `v0.12.0` range at 153 commits,
  carrying a four-row per-release breakdown whose commit counts sum to the range total, following the
  existing entry shape, with no prior entry modified — and project history no longer shows an
  unexplained gap between v3.9 and v4.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → PROJECT.md Validated claims | Reconstructed capability claims become the product's own record of what it does; an unsupported claim is indistinguishable from a shipped feature |
| INVENTORY.md correction log → PROJECT.md edits | Corrections applied here must all be pre-logged; an unlogged edit to an approved document is unauditable |
| PROJECT.md Known tech debt → Phase 66 re-triage scope | Removing a debt bullet removes it from DEBT-01..DEBT-06's re-triage obligation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-09 | Spoofing | Reconstructed Validated entries and MILESTONES accomplishments (Tasks 1, 3) | medium | mitigate | Every entry must be traceable to a named commit inside `2194616..v0.12.0`; if the commit cannot be named the entry is not written. The Task 1 and Task 3 lists must agree, and disagreement is resolved by whichever side a commit supports. Enforced by the plan-wide prohibition. |
| T-60-10 | Repudiation | Unlogged corrections to PROJECT.md (Task 2) | medium | mitigate | Task 2 may apply only corrections that already have a row in `.planning/reviews/INVENTORY.md` §"D-15 Correction Log"; an acceptance criterion pairs each changed value with its logged row. The drift note is retained rather than deleted so the origin of the milestone stays on the record. |
| T-60-11 | Tampering | Removal of a Known-tech-debt bullet (Task 2C) | high | mitigate | A bullet may be removed only with a cited commit or code state demonstrating it is resolved; surviving bullets are annotated with their DEBT-NN identifier so Phase 66's obligation is explicit. The summary must record what was checked and what evidence each check produced, including the bullets kept. Removing a live debt item would silently shrink DEBT-06's "none remain as prose only" gate. |
| T-60-12 | Information Disclosure | Reconstructed entries naming security-relevant subsystems | low | accept | The Validated list and MILESTONES entry record capability names and issue numbers already public in the tracker and the release tags; they add no non-public information. No mitigation warranted. |
| T-60-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git log`, `git show`, `git diff` and `grep`. The package legitimacy gate is not applicable; no RESEARCH.md audit table is required. |
</threat_model>

<verification>
- PROJECT.md's Validated list gained between 15 and 25 one-line entries, each with a release-tag
  suffix, covering every subsystem ROADMAP §Phase 60 criterion 1 names that actually exists.
- No existing Validated entry, Key Decisions row, or Constraint was removed or moved.
- The stale commit count, file count and test-suite claim no longer appear anywhere in PROJECT.md;
  the drift note survives, corrected, with a resolution line.
- MILESTONES.md ends with one new entry carrying `2194616`, `v0.12.0` and `153`, a four-row
  per-release breakdown whose counts sum to the range total, and the existing section shape.
- Every value changed in PROJECT.md has a matching row in the INVENTORY.md correction log.
</verification>

<success_criteria>
- BASE-01: PROJECT.md's Validated requirements cover everything shipped in the pinned range.
- BASE-02: PROJECT.md's Context, Constraints and Key Decisions reflect the subsystems the window
  added and read as true today.
- BASE-04: MILESTONES.md records the interval, so project history has no unexplained gap.
- ROADMAP §Phase 60 criteria 1, 2 and 3 are satisfiable against the resulting documents.
</success_criteria>

<output>
Create `.planning/phases/60-baseline-resync-review-standards/60-03-SUMMARY.md` when done.
</output>
