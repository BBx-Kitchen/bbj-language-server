---
phase: 68-deliverable-documents
plan: 07
type: execute
wave: 7
depends_on: ["68-06"]
files_modified:
  - .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  - .planning/reviews/EASY-FIXES.md
  - .planning/reviews/MAJOR-REFACTORS.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`EASY-FIXES.md` §\"Close-out\" states in plain words that FIX-04 becomes true with this document: it names the requirement, names Phase 67 as the phase that deliberately left it open, names the 29 `user_facing: yes` rows that now carry it, and does not restate FIX-04's wording as if it had always been met."
    - "`MAJOR-REFACTORS.md` §\"Close-out\" states the Phase 69 handoff: which fields ISSUE-02, ISSUE-03 and ISSUE-05 each read, that the `issue:` slot is empty and awaiting ISSUE-05, and that regenerating the document after Phase 69 has written issue numbers would clobber them, with the guard that prevents it named."
    - "Both documents carry a Discrepancy Register listing every departure this phase found between a source artifact's claim and the corpus — reported, never silently absorbed — including that `68-CONTEXT.md` D-02 describes the two `effort: 1` records as already annotated in-record when neither carries such an annotation, and that D-09 describes four appended fields when the frozen order already carries `effort:` so only three are genuinely appended."
    - "`node derive-review-docs.mjs check` exits 0 with every placeholder census at zero, and a remaining placeholder is now a hard failure rather than an informational count."
    - "`git status --porcelain .planning/reviews/` lists exactly two entries, both new files, and `git status --porcelain` shows no modification to `INVENTORY.md`, any `6N-COVERAGE.md`, `.planning/REQUIREMENTS.md`, or any file under `bbj-vscode/`, `bbj-intellij/`, `java-interop/`, `documentation/` or `examples/` (D-12)."
    - "No GitHub tracker write occurred in this phase; the tracker was read at most, and `gh issue create`, `gh issue edit` and `gh issue comment` appear nowhere in the phase's committed artifacts."
    - "The four requirements DOC-01, DOC-02, DOC-03 and DOC-04 are each stated as met with the evidence naming the section that meets it, or stated as partially met with the shortfall and its cause."
  artifacts:
    - path: .planning/reviews/EASY-FIXES.md
      provides: "The FIX-04 discharge statement and the easy-fix side's discrepancy register"
      contains: "## Close-out"
    - path: .planning/reviews/MAJOR-REFACTORS.md
      provides: "The Phase 69 handoff statement, the regeneration warning and the discrepancy register"
      contains: "## Close-out"
  key_links:
    - from: ".planning/reviews/EASY-FIXES.md"
      to: ".planning/REQUIREMENTS.md FIX-04"
      via: "the close-out names the requirement and the 29 user_facing rows that discharge it; no requirement text is edited"
      pattern: "FIX-04"
    - from: ".planning/reviews/MAJOR-REFACTORS.md"
      to: "Phase 69 ISSUE-02 / ISSUE-03 / ISSUE-05"
      via: "the close-out names which field each requirement reads and leaves `issue:` empty for the write-back"
      pattern: "ISSUE-05"
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT restate a requirement's wording as if it had been met — a requirement that became true two phases after it was written says so, naming the phase that deferred it and the rows that now carry it"
    - requirement_id: DOC-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT absorb a discrepancy between a source artifact's claim and the derived corpus into a quiet correction — every such departure is written into the Discrepancy Register with both numbers"
    - requirement_id: DOC-02
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT write to the GitHub tracker in this phase under any circumstances — filing is Phase 69's, gated on ISSUE-01's single approval, and reading the tracker is the only permitted interaction"
---

<objective>
Close both documents honestly: what became true, what is handed to Phase 69, and what did not line up.

Purpose: three things are still open. FIX-04 was deliberately left un-discharged at the end of Phase
67 and becomes true with `EASY-FIXES.md` existing — a reader should not have to notice that on their
own (D-11). `MAJOR-REFACTORS.md` is Phase 69's input and needs its handoff and its regeneration hazard
stated (D-09). And this phase found several places where a source artifact's claim does not match the
corpus; D-02's rule is that those are findings to report, not adjustments to absorb.

Output: a `## Close-out` section in each document, the placeholder census promoted to a hard gate, and
the D-12 write-boundary check run and recorded.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-deliverable-documents/68-CONTEXT.md
@.planning/phases/68-deliverable-documents/68-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `68-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `68-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds: document headings
`## Close-out`, `### FIX-04 verdict` (EASY-FIXES.md), `### Phase 69 handoff` (MAJOR-REFACTORS.md),
`### Discrepancy Register` (both), `### Write-boundary check` (both).
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: gh issue create -->
<!-- planner-discipline-allow: gh issue edit -->
<!-- planner-discipline-allow: gh issue comment -->

<tasks>

<task type="auto">
  <name>Task 1: EASY-FIXES.md close-out — FIX-04 discharged and said so</name>
  <precondition>`node derive-review-docs.mjs check` exits 0 with every placeholder census at zero before this task begins.</precondition>
  <files>
    .planning/reviews/EASY-FIXES.md
  </files>
  <read_first>
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 1884-1903 only (§"FIX-04 verdict" — the statement this close-out answers, and the honesty-pattern shape to mirror)
    - `.planning/REQUIREMENTS.md` lines 66-70 only (FIX-01..FIX-04's exact wording) and lines 73-77 only (DOC-01..DOC-04's exact wording)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-11 and §`<specifics>`
    - `.planning/reviews/EASY-FIXES.md` — its `## Reconciliation` section only
  </read_first>
  <action>
Add a `## Close-out` section at the end of `EASY-FIXES.md` with three sub-sections.

`### FIX-04 verdict` — mirror the shape `67-APPLY-SET.md` §"FIX-04 verdict" used, inverted to the
positive. State that FIX-04 becomes true with this document; that Phase 67's close-out recorded
`.planning/reviews/EASY-FIXES.md` as absent and named the requirement as becoming true when Phase 68
assembles it; that 29 of the 77 rows carry `user_facing: yes` and those are the rows that record a
user-facing behaviour change against its fix and commit; and that the remaining 48 rows are
internal-only edits recorded with the same fields. Name the requirement by ID and quote its wording
once, so a reader can check the claim against the requirement rather than against a paraphrase. Do not
edit `REQUIREMENTS.md`; the traceability matrix is not this phase's to tick.

`### Discrepancy Register` — every departure this phase found between a source artifact's claim and
the derived corpus, each with both numbers and no adjustment. At minimum:
`68-CONTEXT.md` D-02 states the two off-scale `effort: 1` values are "already annotated in-record as
rounded down", and neither `P61-D4-010` nor `P61-D8-005` carries such an annotation — Phase 67's
close-out recorded the same absence; the values are carried unrounded and the claim is corrected here
rather than an annotation being fabricated. `67-CONTEXT.md`'s per-phase figures of 73/34/65/45/37/18
findings are counts of items enumerated or swept, not `disposition:` record counts, which are
73/34/62/44/3/8; its totals and per-phase easy-fix counts are correct and match. Add any further
discrepancy carried in the SUMMARY files of plans `68-02` through `68-06`.

`### Write-boundary check` — record the output of `git status --porcelain .planning/reviews/` verbatim,
and state that D-12's boundary held: two new files, no modification to `INVENTORY.md` or any
`6N-COVERAGE.md`, and no tracker write.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^## Close-out' .planning/reviews/EASY-FIXES.md && grep -q '^### FIX-04 verdict' .planning/reviews/EASY-FIXES.md && grep -q '^### Discrepancy Register' .planning/reviews/EASY-FIXES.md && grep -Fq 'P61-D4-010' .planning/reviews/EASY-FIXES.md && grep -Fq '29' .planning/reviews/EASY-FIXES.md && git diff --quiet .planning/REQUIREMENTS.md && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `## Close-out` exists at the end of `EASY-FIXES.md` with `### FIX-04 verdict`, `### Discrepancy Register` and `### Write-boundary check` sub-sections.
    - The FIX-04 sub-section names the requirement ID, quotes its wording once, names Phase 67 as the phase that deferred it, and states the counts `29` and `77`.
    - The Discrepancy Register contains at least the two named discrepancies, each stating both the claimed and the derived value.
    - The Write-boundary sub-section contains the literal output of `git status --porcelain .planning/reviews/`, showing exactly two entries.
    - `git diff --quiet .planning/REQUIREMENTS.md` exits 0 — no requirement text was edited.
    - `check` exits 0.
  </acceptance_criteria>
  <done>A reader of EASY-FIXES.md learns that FIX-04 became true here, why it was open, and every place a source claim did not match the corpus.</done>
</task>

<task type="auto">
  <name>Task 2: MAJOR-REFACTORS.md close-out — the Phase 69 handoff and the regeneration hazard</name>
  <files>
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 79-84 only (ISSUE-01..ISSUE-05's exact wording)
    - `.planning/ROADMAP.md` §"Phase 69: GitHub Issue Filing" (the consumer's own success criteria)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-09, D-10, D-12 and §`<deferred>`
    - `.planning/reviews/MAJOR-REFACTORS.md` — its `## Reconciliation` and `## Other Dispositions` census lines only
  </read_first>
  <action>
Add a `## Close-out` section at the end of `MAJOR-REFACTORS.md` with three sub-sections.

`### Phase 69 handoff` — state, field by field, what each of Phase 69's requirements reads from this
document. ISSUE-02 reads `failure_scenario:` and `proposed_approach:`, each written to stand alone so
an issue body needs neither this document nor a COVERAGE file open; `location:` supplies the
`file:line` evidence. ISSUE-03 reads `proposed_labels:`, whose area comes from the repository's
existing fifteen-label set, whose `PRIO` is INVENTORY §3d's mapping of `severity:`, and whose effort is
the same `{2,4,8}` value as `effort:` with no translation step. ISSUE-05 writes the filed issue number
into the empty `issue:` slot on each block. ISSUE-04's dedup is served by the `dedup:` field, which
carries `none` on 210 of the 224 records and a named overlap on the other 14. Point Phase 69 at the
severity-sorted index as its filing order and state that the 1 `critical` and 16 `high` records lead it.

State plainly, in the same sub-section, that this document records 144 proposed refactors and that
implementing them is FUT-04 — out of scope for this milestone, each to be resolved in its own
milestone or PR — so a reader does not mistake a proposed approach for scheduled work.

`### Regeneration hazard` — state that once Phase 69 writes issue numbers under ISSUE-05, re-running
`derive-review-docs.mjs` to regenerate this document would clobber every one of them; that the field
set therefore had to be right before Phase 69 ran, which is why the `issue:` slot exists now rather
than being added later; and that the script refuses to emit when any `issue:` value is non-empty unless
an explicit override flag is passed. Name the flag so a future maintainer who genuinely needs it can
find it, and name what they must preserve first.

`### Discrepancy Register` — the same register as `EASY-FIXES.md`'s, plus the two that belong to this
document: `68-CONTEXT.md` D-09 describes four appended Phase-69-facing fields while INVENTORY's frozen
order already carries `effort:`, so three fields are genuinely appended and `effort:` is retained in
place; and the open-gap referrals from plan `68-06`'s census, listed by source anchor, which are the
only places this milestone's review handed something over and cannot show where it landed.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^## Close-out' .planning/reviews/MAJOR-REFACTORS.md && grep -q '^### Phase 69 handoff' .planning/reviews/MAJOR-REFACTORS.md && grep -q '^### Regeneration hazard' .planning/reviews/MAJOR-REFACTORS.md && for s in ISSUE-02 ISSUE-03 ISSUE-04 ISSUE-05 FUT-04 --force; do grep -Fq "$s" .planning/reviews/MAJOR-REFACTORS.md || exit 1; done && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `## Close-out` exists at the end of `MAJOR-REFACTORS.md` with `### Phase 69 handoff`, `### Regeneration hazard` and `### Discrepancy Register` sub-sections.
    - The handoff names ISSUE-02, ISSUE-03, ISSUE-04 and ISSUE-05 and, for each, the field it reads or writes.
    - The handoff names FUT-04 and states that implementing the 144 refactors is out of this milestone's scope.
    - The regeneration sub-section names the override flag and states what must be preserved before it is used.
    - The Discrepancy Register names the `effort:` field-count reconciliation and lists every open-gap referral by source anchor.
    - `check` exits 0.
  </acceptance_criteria>
  <done>Phase 69 can pick this document up knowing exactly which field feeds which requirement and what would destroy its own output.</done>
</task>

<task type="auto">
  <name>Task 3: Placeholder census promoted to a hard gate and the phase's final verification run</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/EASY-FIXES.md,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after plan `68-04`)
    - `.planning/reviews/EASY-FIXES.md` and `.planning/reviews/MAJOR-REFACTORS.md` — their heading sequences only, via `grep -n '^#'`
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-12
  </read_first>
  <action>
Promote the placeholder census from informational output to a hard assertion: `check` now exits
non-zero when either document contains any placeholder marker of any family, naming the marker, the
document and the block it sits in. This is the gate that makes "no judgment field was left unfilled" a
machine fact rather than a claim.

Add a final DOC-completeness assertion group to `check`, one assertion per requirement, each naming the
document section that satisfies it: DOC-01 — every one of the 77 easy rows carries a non-empty
finding ID, `location:`, `dimension:`, `failure_scenario:`, `fix_applied:` and `commit:` or an inline
verdict reason; DOC-02 — every one of the 144 major blocks carries a non-empty `id:`, `location:`,
`dimension:`, `failure_scenario:`, `proposed_approach:`, `effort:` and `proposed_labels:`; DOC-03 —
both documents carry an identical `## Coverage` block containing `### Scope` and `### Gaps`; DOC-04 —
`## Other Dispositions` holds the reconciled 3 + 24 + 0 + 14 populations and 30 resolved referrals.

Add a write-boundary assertion to `check`: it shells out to `git status --porcelain .planning/reviews/`
and fails if the output is anything other than exactly two entries naming `EASY-FIXES.md` and
`MAJOR-REFACTORS.md`, or if `git status --porcelain` reports any modification under `bbj-vscode/`,
`bbj-intellij/`, `java-interop/`, `documentation/` or `examples/`, or to `.planning/reviews/INVENTORY.md`,
any `6N-COVERAGE.md`, or `.planning/REQUIREMENTS.md`. This is the only place the script invokes git
beyond the sha-resolvability check, and it is read-only.

Run the full verification and record its output in both documents' `### Write-boundary check`
sub-sections, updating `EASY-FIXES.md`'s from Task 1 with the final state.

Finally, confirm no GitHub tracker write occurred anywhere in the phase: grep the phase directory and
both documents for `gh issue create`, `gh issue edit` and `gh issue comment` and record the zero result
in the phase SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/.planning/phases/68-deliverable-documents && node derive-review-docs.mjs check && cd /home/coder/repos/bbj-language-server && test "$(git status --porcelain .planning/reviews/ | wc -l)" -eq 2 && git diff --quiet .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md .planning/reviews/66-COVERAGE.md .planning/REQUIREMENTS.md && test "$(git status --porcelain bbj-vscode bbj-intellij java-interop documentation examples | wc -l)" -eq 0 && test "$(grep -rc 'gh issue create' .planning/reviews/EASY-FIXES.md .planning/reviews/MAJOR-REFACTORS.md | grep -v ':0' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `node derive-review-docs.mjs check` exits 0 and reports a DOC-completeness group with one assertion per requirement, each naming its satisfying section.
    - Inserting any placeholder marker into either document makes `check` exit non-zero and name the marker, the document and the block; the insertion is reverted and `check` returns to exit 0.
    - `git status --porcelain .planning/reviews/` outputs exactly two lines.
    - `git diff --quiet` over `INVENTORY.md`, all six COVERAGE files and `REQUIREMENTS.md` exits 0.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop documentation examples` outputs nothing.
    - Neither document contains `gh issue create`, `gh issue edit` or `gh issue comment`.
    - Both documents' `### Write-boundary check` sub-sections contain the final `git status --porcelain .planning/reviews/` output.
  </acceptance_criteria>
  <done>Every judgment field is filled as a machine fact, all four requirements are asserted against a named section, and the D-12 write boundary is proven rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase → `.planning/REQUIREMENTS.md` and the traceability matrix | a requirement's status is named in prose here; editing the matrix is another phase's work |
| this phase → the GitHub tracker | reading is permitted, writing is Phase 69's under ISSUE-01's approval gate |
| `MAJOR-REFACTORS.md` → Phase 69 | the handoff statement is what a filer acts on; an inaccurate one misroutes five requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-24 | Elevation of privilege | a tracker write escaping this phase ahead of ISSUE-01's approval gate | high | mitigate | Authored prohibition; Task 3 greps both documents and the phase directory for `gh issue create`/`edit`/`comment` and records the zero result |
| T-68-01 | Tampering | a write outside the two permitted files | high | mitigate | Task 3's write-boundary assertion inside `check` fails on any entry other than the two new files or any modification under the five source directories, `INVENTORY.md`, a COVERAGE file or `REQUIREMENTS.md` |
| T-68-25 | Repudiation | a judgment field left unfilled and shipped as complete | high | mitigate | The placeholder census is promoted from informational to a hard failure naming the marker, document and block |
| T-68-26 | Repudiation | a requirement declared met without evidence | medium | mitigate | The DOC-completeness assertion group names the satisfying section per requirement and re-derives its population |
| T-68-27 | Tampering | a source-artifact discrepancy quietly corrected instead of reported | medium | mitigate | Both Discrepancy Registers require both the claimed and the derived value; the authored prohibition forbids absorption |
| T-68-03 | Tampering | regenerating after Phase 69's ISSUE-05 write | high | mitigate | The `### Regeneration hazard` sub-section documents the guard and the override flag, and the guard itself remains in the script |
| T-68-SC | Tampering | npm/pip/cargo installs | low | accept | no package-manager install task in this plan |
</threat_model>

<verification>
- `node derive-review-docs.mjs check` exits 0 with every placeholder census at zero and the DOC-completeness group reported.
- `git status --porcelain .planning/reviews/` outputs exactly two lines, both new files.
- `git diff --quiet` over `INVENTORY.md`, all six COVERAGE files and `REQUIREMENTS.md` exits 0.
- `git status --porcelain bbj-vscode bbj-intellij java-interop documentation examples` outputs nothing.
</verification>

<success_criteria>
- `EASY-FIXES.md` §"Close-out" discharges FIX-04 by name, with the phase that deferred it and the 29 rows that carry it.
- `MAJOR-REFACTORS.md` §"Close-out" names which field each Phase 69 requirement reads or writes, and documents the regeneration hazard and its guard.
- Both documents carry a Discrepancy Register with both values for every departure found.
- Every judgment field is filled, proven by a hard gate, and the D-12 write boundary is proven by git.
</success_criteria>

<output>
Create `.planning/phases/68-deliverable-documents/68-07-SUMMARY.md` when done, recording: the final
`check` output in full, the `git status --porcelain .planning/reviews/` output, the zero-result tracker
grep, the complete Discrepancy Register, and a per-requirement statement for DOC-01, DOC-02, DOC-03 and
DOC-04 naming the section that satisfies each — or the shortfall and its cause where one is only
partially met.
</output>
