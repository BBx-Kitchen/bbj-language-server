---
phase: 68-deliverable-documents
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  - .planning/reviews/EASY-FIXES.md
  - .planning/reviews/MAJOR-REFACTORS.md
autonomous: true
requirements: [DOC-01, DOC-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 77 EASY-FIXES.md rows carries DOC-01's six required fields non-empty: `finding_id:`, `location:` (path:line), `dimension:`, `failure_scenario:`, `fix_applied:` and `commit:` — and the 7 rows whose verdict is not `applied` carry their verdict and reason in the `commit:` field inline, so no row sends the reader to another file."
    - "Every one of the 144 MAJOR-REFACTORS.md blocks carries DOC-02's required fields non-empty: `id:`, `location:`, `dimension:`, `failure_scenario:`, `proposed_approach:` and `effort:`, plus `proposed_labels:` and an `issue:` key that is present and empty."
    - "`proposed_labels:` on every block names an area drawn only from INVENTORY's fifteen-label area set, a `PRIO` value that is the INVENTORY §3d mapping of that block's severity (critical/high to PRIO 1, medium to PRIO 2, low to PRIO 3), and an effort value in {2,4,8} that is the leading token of the block's own `effort:` field."
    - "118 of the 144 blocks carry a `proposed_approach:` lifted from their own `classification:` test-(5) named-edit clause; the remaining 26 carry an explicit placeholder marker and are named in this plan's SUMMARY, because their test-(5) clause names no edit."
    - "The severity-sorted index lists exactly 144 rows and its first 17 rows are the 1 `critical` and 16 `high` records, so Phase 69 reads the highest-severity work first rather than whatever phase it came from."
    - "Two records whose `effort:` or `evidence_tier:` field carries in-record annotation prose keep that prose verbatim in the assembled block; only the derived label value uses the leading token."
    - "The four Phase 67 close-out corrections are carried into the block of the record they correct — `P61-D2-002` and `P64-D6-013` in EASY-FIXES.md, `P63-D2-013` and `P64-D3-002` in MAJOR-REFACTORS.md — so no disproven or superseded claim is transcribed as established fact."
  artifacts:
    - path: .planning/reviews/EASY-FIXES.md
      provides: "77 complete rows lifted from 67-APPLY-SET.md, plus the verdict Index table"
      contains: "user_facing:"
    - path: .planning/reviews/MAJOR-REFACTORS.md
      provides: "144 complete blocks in INVENTORY frozen field order plus proposed_approach / proposed_labels / issue, and the severity-sorted filing index"
      contains: "proposed_labels:"
  key_links:
    - from: ".planning/reviews/EASY-FIXES.md"
      to: ".planning/phases/67-apply-easy-fixes/67-APPLY-SET.md"
      via: "row content lifted field-for-field, not re-derived from source or COVERAGE (D-04)"
      pattern: "^fix_applied: "
    - from: ".planning/reviews/MAJOR-REFACTORS.md"
      to: ".planning/reviews/INVENTORY.md"
      via: "block field order is INVENTORY's frozen Finding Record Template; PRIO and effort labels come from INVENTORY §3d"
      pattern: "^proposed_labels: "
  prohibitions:
    - requirement_id: DOC-01
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT restate as established fact any finding-record claim that a later phase empirically falsified — a lifted claim that was disproven carries the correction alongside it or is not lifted"
    - requirement_id: DOC-02
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT re-classify, re-triage, re-word or re-score any finding record while assembling — the documents transcribe verdicts they did not make, and a disagreement is reported as a discrepancy rather than absorbed into the transcription"
    - requirement_id: DOC-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT invent a proposed approach the reviewing phase did not name — where the record's own classification clause names no edit, the field carries a placeholder for a later authored entry rather than a plausible-sounding guess"
---

<objective>
Fill both documents' record blocks with the content DOC-01 and DOC-02 actually require.

Purpose: plan `68-01` proved the spine and left every judgment field unfilled. DOC-01 needs the fix
applied and its commit hash on every easy row; DOC-02 needs a proposed approach, an effort estimate
and proposed labels on every major block. Both are lifts, not re-derivations: `67-APPLY-SET.md`
already carries the completed easy-fix ledger (D-04), and each major record's own `classification:`
test-(5) clause already names the exact edit its reviewer could see. Lifting is what keeps the
documents from disagreeing with the corpus they claim to summarise.

Output: `EASY-FIXES.md` with 77 complete rows and a verdict index; `MAJOR-REFACTORS.md` with 144
complete blocks, proposed labels, an empty `issue:` slot per block, and the severity-sorted filing
index Phase 69 works down.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-deliverable-documents/68-CONTEXT.md
@.planning/phases/68-deliverable-documents/68-PATTERNS.md
@.planning/phases/68-deliverable-documents/68-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `68-01-PLAN.md` §`<phase_conventions>` — the awk record extractors, the D-12
write boundary, the `docs(68):` commit shape, phase-local decision IDs, the honesty gate, and the
no-re-triage rule. Read that block before starting; it is not restated here.
</phase_conventions>

<artifacts_this_phase_produces>
See `68-01-PLAN.md` §`<artifacts_this_phase_produces>` for the full list. The symbols this plan adds
to it: script functions `renderEasyRow`, `renderMajorBlock`, `proposedLabels`, `areaForRecord`,
`approachSeed`, `severityIndex`; document headings `## Index`, `## Rows`, `## Records`,
`## Index (severity-sorted, for Phase 69 filing order)`; record field keys `proposed_approach:`,
`proposed_labels:`, `issue:`; placeholder markers `PENDING-APPROACH` and `PENDING-AREA`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EASY-FIXES.md rows lifted whole from the Phase 67 ledger</name>
  <precondition>`.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` exists and contains exactly 77 lines beginning `finding_id:` and exactly 77 lines beginning `verdict:`.</precondition>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/EASY-FIXES.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after plan `68-01`)
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 1-40 and 116-175 only (the Derivation/Reconciliation/Index shape and three completed row blocks — enough to see the field set and the column-20 alignment; do not read the whole file)
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 1904-1984 only (§"Recorded departures" — the four corrections this phase must carry)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-03, D-04, D-11
  </read_first>
  <action>
Extend `emit-easy` so it stops re-deriving from the COVERAGE files and instead lifts each row whole
from `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md`, per D-04. Parse that file's fenced row
blocks with the same `parseFields` continuation-line parser, keyed on `finding_id:`. Emit, per row, in
this order at column-20 alignment: `row:`, `finding_id:`, `unit:`, `location:`, `dimension:`,
`severity:`, `effort:`, `verdict:`, `test_required:`, `fail_before:`, `failure_scenario:`,
`fix_applied:`, `user_facing:`, `verification:`, `commit:`, `notes:`. Every value is carried verbatim;
`notes:` is emitted as a present-but-empty key when the ledger row's own `notes:` is empty, never
dropped. Cross-check the lifted finding-ID set against the corpus easy-fix selection and hard-fail
with the mismatched IDs named if the two sets differ — the ledger is the content source, the corpus is
still the denominator (D-02).

Add a `## Index` table above `## Rows` in the same four-column shape `67-APPLY-SET.md` uses —
`| # | finding_id | verdict | commit |` — with all 77 rows. For the 7 rows whose verdict is not
`applied`, the `commit` cell carries the inline reason rather than a hash, so the table alone answers
why a row has no commit (D-03): the four `no-op` rows name the resolving finding ID and its commit;
`P64-D8-003` and `P64-D8-004` name INVENTORY immutability under Phase 60 D-09 and Phase 67 D-03; and
`P63-D7-004` names the absent JDK 17 under Phase 67 D-15.

Carry the two Phase 67 close-out corrections that land on easy-fix rows, each appended to that row's
own `notes:` value so it travels with the row rather than sitting in a separate section:
`P61-D2-002` — its record's claim that `Promise.race`'s losing branch leaks an unhandled rejection was
empirically falsified against the real `vscode-jsonrpc` library, the fix was applied verbatim anyway,
and `fail_before:` reads `inapplicable` with that reason; the row must not restate the original claim
as established fact. `P64-D6-013` — the record named six moderate-severity packages but the approved
`npm audit fix --package-lock-only` closed all 19 pre-existing advisories (7 moderate, 11 high,
1 critical) including an `@azure/msal-node` 3.8.6 to 5.6.0 major-version jump, with
`bbj-vscode/package.json` provably untouched; the row records the actual 19-advisory scope, not the
record's original 6-package estimate.

Regenerate `.planning/reviews/EASY-FIXES.md` by concatenating its existing `## Derivation`,
`## Reconciliation` and any already-authored prose sections with the new `## Index` and `## Rows`
output, using shell redirection so the row content never passes through the agent's context.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && test "$(grep -c '^finding_id:' .planning/reviews/EASY-FIXES.md)" -eq 77 && test "$(grep -c '^fix_applied:' .planning/reviews/EASY-FIXES.md)" -eq 77 && test "$(grep -c '^commit:' .planning/reviews/EASY-FIXES.md)" -eq 77 && test "$(grep -c '^user_facing: *yes' .planning/reviews/EASY-FIXES.md)" -eq 29 && test "$(grep -c '^verdict: *applied' .planning/reviews/EASY-FIXES.md)" -eq 70 && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^finding_id:' .planning/reviews/EASY-FIXES.md` outputs `77`; the same count holds for `^location:`, `^dimension:`, `^failure_scenario:`, `^fix_applied:`, `^commit:`, `^user_facing:` and `^notes:`.
    - `grep -c '^user_facing: *yes'` outputs `29`; `grep -c '^verdict: *applied'` outputs `70`; `grep -c '^verdict: *no-op'` outputs `4`; `grep -c '^verdict: *excluded'` outputs `2`; `grep -c '^verdict: *deferred'` outputs `1`.
    - The `## Index` table has 77 data rows, and the four `no-op` rows plus `P64-D8-003`, `P64-D8-004` and `P63-D7-004` each have a non-empty `commit` cell that is prose, not a hex sha.
    - `awk` extraction of the `P61-D4-010` row shows `effort:` with the value `1` carried through unrounded.
    - The `P61-D2-002` row's `notes:` names the empirical falsification and its `fail_before:` reads `inapplicable`; the `P64-D6-013` row's `notes:` states the 19-advisory actual scope.
    - `node derive-review-docs.mjs check` exits 0, including the commit-sha resolvability group.
  </acceptance_criteria>
  <done>All 77 easy-fix rows carry DOC-01's six required fields plus the Phase 67 verdict, and the document answers on its own why any row has no commit hash.</done>
</task>

<task type="auto">
  <name>Task 2: MAJOR-REFACTORS.md blocks in frozen field order with approach, labels and issue slot</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after Task 1)
    - `.planning/reviews/INVENTORY.md` lines 121-160 only (§3d severity/effort scales and §"Finding Record Template")
    - `.planning/reviews/INVENTORY.md` lines 34-41 only (the fifteen existing area labels, quoted in the Frozen Open-Issue Snapshot preamble)
    - Two real major records for field shape, via the awk extractor: `P65-D1-001` from `65-COVERAGE.md` (a passing test-(5) clause) and `P64-D3-002` from `64-COVERAGE.md` (a failing one)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-09, D-10 and §`<specifics>`
  </read_first>
  <action>
Extend `emit-major` to render each of the 144 blocks in INVENTORY's frozen field order verbatim —
`id:`, `unit:`, `location:`, `dimension:`, `secondary:`, `severity:`, `evidence_tier:`, `evidence:`,
`failure_scenario:`, `classification:`, `effort:`, `dedup:`, `disposition:` — with every value carried
through unmodified, including the in-record annotation prose on `P63-D3-005`'s, `P66-D4-001`'s and
`P66-D2-002`'s `effort:` values and on the three annotated `evidence_tier:` values. Then append three
fields, at the same column-20 alignment:

`proposed_approach:` — seeded from the record's own `classification:` field. Extract the substring
that starts at the `(5)` marker and ends before the `(6)` marker, and take the named edit from inside
that clause. When the clause records `: pass` the named edit exists and is lifted as the approach
value, rewritten only enough to read as a standalone imperative sentence with no "reviewer can name"
framing — 118 of the 144 records are in this class. When the clause records `FAIL`, `n/a` or `moot`,
no edit was named; emit the literal placeholder marker for a later authored entry instead of a guess.
26 records are in that class: `P61-D5-001`, `P61-D5-002`, `P61-D5-003`, `P61-D5-010`, `P61-D5-013`,
`P61-D5-014`, `P63-D7-001`, `P63-D7-002`, `P63-D7-003`, `P63-D7-005`, `P63-D7-006`, `P63-D2-010`,
`P63-D6-002`, `P63-D4-010`, `P64-D1-002`, `P64-D1-003`, `P64-D2-005`, `P64-D2-006`, `P64-D3-002`,
`P64-D4-003`, `P64-D5-001`, `P64-D6-002`, `P64-D6-003`, `P64-D6-005`, `P66-D5-001`, `P66-D5-002`.

`proposed_labels:` — written as `area=<label>; PRIO <n>; effort <n>`. The area comes only from
INVENTORY's fifteen-label set, resolved by this rule in order: `dimension:` is `D6` gives
`dependencies`; else `dimension:` is `D8` gives `documentation`; else the longest matching
`location:` path prefix, where `bbj-intellij/` gives `intellij`, `bbj-vscode/src/` gives `vscode`,
`bbj-vscode/tools/` gives `BBj integration and infrastructure`, `bbj-vscode/test/` gives `javascript`,
`.github/` gives `BBj integration and infrastructure`, and `java-interop/` gives
`BBj integration and infrastructure`; else a placeholder marker naming the unresolved area. `PRIO` is
INVENTORY §3d's mapping of this block's own `severity:` — `critical` and `high` give `PRIO 1`,
`medium` gives `PRIO 2`, `low` gives `PRIO 3`. Effort is the leading `{2,4,8}` token of the block's
own `effort:` field, so the label and the estimate are the same value with no translation step.

`issue:` — emitted as a present key with an empty value, the slot Phase 69 fills under ISSUE-05.

Carry the two Phase 67 close-out corrections that land on major records, appended to that block's own
`proposed_approach:` value: `P63-D2-013` — the "zero call sites anywhere in the codebase" premise for
`scheduleRestart()` is false, `BbjSettingsConfigurable.apply():83` has called it since commit
`35c916b`, predating the Phase 63 review; the block must not restate that premise as fact, while the
record's broader conclusion (most restart triggers bypass the debounce) stands and was independently
reverified. `P64-D3-002` — its paired `P64-D4-004` already landed in Phase 67 as a recorded D-06
departure, so this block carries the reciprocal note that its `build.yml` `on:`-block sibling is
already applied and whoever implements this record is not surprised by it.

Note in the block-shape comment at the head of `renderMajorBlock` that D-09 describes four appended
fields while the frozen order already carries `effort:`, so three fields are genuinely appended and
`effort:` is retained in place — the reconciliation is stated rather than the discrepancy absorbed.

Regenerate `.planning/reviews/MAJOR-REFACTORS.md` via shell redirection, preserving its existing
`## Derivation` and `## Reconciliation` sections.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && test "$(grep -c '^proposed_approach:' .planning/reviews/MAJOR-REFACTORS.md)" -eq 144 && test "$(grep -c '^proposed_labels:' .planning/reviews/MAJOR-REFACTORS.md)" -eq 144 && test "$(grep -c '^issue:' .planning/reviews/MAJOR-REFACTORS.md)" -eq 144 && test "$(grep -c '^secondary:' .planning/reviews/MAJOR-REFACTORS.md)" -eq 144 && test "$(grep -c 'PRIO 1' .planning/reviews/MAJOR-REFACTORS.md)" -ge 17 && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - Each of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^secondary:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:`, `^proposed_approach:`, `^proposed_labels:`, `^issue:` occurs exactly 144 times in `.planning/reviews/MAJOR-REFACTORS.md`.
    - `grep -c 'PRIO 1' .planning/reviews/MAJOR-REFACTORS.md` outputs `17`, `PRIO 2` outputs `70`, `PRIO 3` outputs `57` — matching the derived severity distribution 1 critical + 16 high, 70 medium, 57 low.
    - Every `proposed_labels:` area value is one of the fifteen INVENTORY area labels; a script assertion in `check` reports zero values outside that set.
    - Exactly 26 blocks carry the approach placeholder marker, and their finding-ID set equals the 26 IDs named in this task's action.
    - `awk` extraction of `P63-D3-005` shows its `effort:` annotation prose intact, and its `proposed_labels:` effort component is `2`.
    - `awk` extraction of `P63-D2-013` shows a `proposed_approach:` that states the `BbjSettingsConfigurable.apply():83` call site and does not assert the zero-call-sites premise; `awk` extraction of `P64-D3-002` shows the reciprocal note naming `P64-D4-004`.
    - `node derive-review-docs.mjs check` exits 0, including the required-field-presence group for MAJOR-REFACTORS.md.
  </acceptance_criteria>
  <done>All 144 major blocks carry DOC-02's required fields, labels drawn only from the repository's existing set, and an empty issue slot for Phase 69.</done>
</task>

<task type="auto">
  <name>Task 3: Severity-sorted filing index above the phase-then-ID order</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after Task 2)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-10
    - `.planning/phases/68-deliverable-documents/68-PATTERNS.md` §"Analog C" (the index-table shape; note that this table has no existing analog and is constructed from the same parsed field data)
  </read_first>
  <action>
Add a `## Index (severity-sorted, for Phase 69 filing order)` section to `MAJOR-REFACTORS.md`,
positioned after `## Reconciliation` and before the `## Records` blocks, per D-10. Columns:
`| # | severity | PRIO | effort | finding_id | location | area |`. All 144 records appear.

Sort key, in order: severity rank with `critical` first, then `high`, `medium`, `low`; then the `PRIO`
value; then effort ascending (2, 4, 8); then originating phase; then finding ID. The last two
components make the order total — no two records can tie — so the table is stable across
re-derivations and diffable against the phase-then-ID record order below it.

The `location:` cell carries only the first `path:line` token of a multi-location record, with the
full value remaining in the record block; the `area` cell repeats the area component of that record's
`proposed_labels:` so a Phase 69 filer can batch by area without opening each block.

State in one line above the table that this index exists because Phase 69 files in severity order
while the record blocks below stay in originating-phase order to keep the document diffable against
`67-APPLY-SET.md`, so the 1 critical and 16 high records surface first rather than being buried at
whatever phase they came from.

Extend `check` with an assertion that the index row count is 144, that its finding-ID set equals the
record-block finding-ID set, and that the rows are in non-decreasing sort-key order under the full
five-component key.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^## Index (severity-sorted' .planning/reviews/MAJOR-REFACTORS.md && test "$(awk '/^## Index \(severity-sorted/,/^## Records/' .planning/reviews/MAJOR-REFACTORS.md | grep -c '^| [0-9]')" -eq 144 && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - The section heading `## Index (severity-sorted, for Phase 69 filing order)` exists and sits between `## Reconciliation` and `## Records`.
    - The table has exactly 144 numbered data rows.
    - The first data row's severity cell is `critical`; rows 2 through 17 are `high`; row 18 onward begin `medium`.
    - The index finding-ID set equals the record-block finding-ID set, asserted by `check` rather than by eye.
    - `check` reports the index as sorted under the five-component key and exits 0.
    - Re-running `emit-major` twice produces byte-identical index output.
  </acceptance_criteria>
  <done>Phase 69 has a filing order that starts at the highest severity, and the record body keeps the ledger-diffable phase-then-ID order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `67-APPLY-SET.md` → `EASY-FIXES.md` | completed ledger prose, including commit shas, crosses into a deliverable |
| six COVERAGE files → `MAJOR-REFACTORS.md` | reviewer prose crosses into a document Phase 69 turns into public issues |
| `MAJOR-REFACTORS.md` → Phase 69 tracker writes | the `issue:` slot is the write-back surface ISSUE-05 uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-05 | Spoofing | `commit:` values lifted into EASY-FIXES.md | medium | mitigate | `check`'s sha-resolvability group runs `git cat-file -e <sha>^{commit}` on every unique sha; Task 1's verify runs it |
| T-68-02 | Information disclosure | `evidence:` and `failure_scenario:` prose bound for public Phase 69 issues | medium | mitigate | `check`'s credential-shaped-literal scan runs over both documents after every regeneration in this plan |
| T-68-03 | Tampering | regenerating `MAJOR-REFACTORS.md` after Phase 69 wrote `issue:` numbers | high | mitigate | the `--force`-gated regeneration guard from plan `68-01` Task 2 blocks every `emit-*` in this plan once any `issue:` is non-empty |
| T-68-08 | Tampering | a fabricated `proposed_approach:` for a record whose reviewer named no edit | medium | mitigate | the 26 no-named-edit records emit a placeholder marker and are enumerated by ID in the action, in `check`'s placeholder census, and in this plan's SUMMARY |
| T-68-09 | Repudiation | a `proposed_labels:` area invented outside the repository's label set, unfilable in Phase 69 | medium | mitigate | area values are drawn from INVENTORY's fifteen-label set by a stated resolution rule; `check` reports zero out-of-set values |
| T-68-06 | Denial of service | regex parsing over the 17,730-line corpus plus the 1,995-line ledger | low | accept | fixed, local, read-only inputs already parsed by `derive-apply-set.mjs` |
| T-68-SC | Tampering | npm/pip/cargo installs | low | accept | this plan adds no package-manager install task; the script imports only Node built-ins |
</threat_model>

<verification>
- `node derive-review-docs.mjs check` exits 0.
- `git status --porcelain .planning/reviews/` still lists exactly two entries and no COVERAGE or INVENTORY modification.
- `git diff --stat .planning/phases/67-apply-easy-fixes/` is empty — the ledger is read, never written.
- The placeholder census printed by `check` reports exactly 26 approach placeholders and 0 area placeholders.
</verification>

<success_criteria>
- 77 EASY-FIXES.md rows carry DOC-01's six required fields; the 7 non-applied rows explain themselves inline.
- 144 MAJOR-REFACTORS.md blocks carry DOC-02's required fields plus labels and an empty `issue:` slot.
- The severity-sorted index lists all 144 records, highest severity first, under a total sort key.
- The four Phase 67 close-out corrections travel with the records they correct.
</success_criteria>

<output>
Create `.planning/phases/68-deliverable-documents/68-02-SUMMARY.md` when done, recording: the per-field
counts asserted, the 26 finding IDs left carrying an approach placeholder, the area-label distribution
across the 144 blocks, and any record whose `classification:` clause could not be parsed into either
class (reported as a discrepancy, never silently defaulted).
</output>
