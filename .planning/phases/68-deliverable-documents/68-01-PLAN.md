---
phase: 68-deliverable-documents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  - .planning/reviews/EASY-FIXES.md
  - .planning/reviews/MAJOR-REFACTORS.md
autonomous: true
requirements: [DOC-01, DOC-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node derive-review-docs.mjs check` exits 0 against the assembled documents, and exits non-zero with a written breakdown when any derived count departs from 224 records = 144 major-refactor + 77 easy-fix + 3 wontfix."
    - "`.planning/reviews/EASY-FIXES.md` contains exactly 77 record blocks and `.planning/reviews/MAJOR-REFACTORS.md` exactly 144, and the set of finding IDs in each equals the set the corpus selection produces — no extra ID, no absent ID."
    - "EASY-FIXES.md contains exactly 77 record blocks — one per easy-fix record, never merged: `P61-D2-011` and `P66-D2-001` each keep their own block and both cite the same commit pair, because they are two records of one defect."
    - "MAJOR-REFACTORS.md keeps one block per record even where two records name the same location or one supersedes another: `P66-D4-001` and `P63-D4-010` each keep their own block and the supersedes relation is stated in `dedup:`, not collapsed."
    - "Every zero-count bucket is rendered with the literal count 0 and a written reason rather than an omitted section — Phase 65 contributes 0 easy-fix records and that zero is stated."
    - "Every empty optional field is written explicitly (`secondary: none`, `notes:` present-but-empty) rather than dropped from the block, matching INVENTORY's rule that optional fields are never left blank."
    - "Running `node derive-review-docs.mjs emit-easy` and `emit-major` twice on an unchanged corpus produces byte-identical output; ordering is originating phase then finding ID and finding IDs are unique, so no ordering tie exists."
    - "The severity-sorted index in MAJOR-REFACTORS.md sorts by severity rank (critical, high, medium, low), then PRIO, then effort, with ties broken by originating phase then finding ID, so the order is total and stable, and it lists exactly 144 rows."
    - "All corpus reads and document writes use UTF-8, and each lifted `failure_scenario:` is identical to its COVERAGE source after collapsing runs of whitespace to single spaces; no lifted field introduces a triple-backtick sequence that would terminate a record fence."
    - "`derive-review-docs.mjs` exits non-zero without emitting when `.planning/reviews/MAJOR-REFACTORS.md` already carries a non-empty `issue:` value, unless `--force` is passed, so a re-run after Phase 69's ISSUE-05 write cannot clobber filed issue numbers."
    - statement: "An interrupted derivation never leaves a partially written deliverable: each document is written to a sibling temporary path and renamed into place, so a killed run leaves the previously committed content intact."
      verification: backstop
  artifacts:
    - path: .planning/phases/68-deliverable-documents/derive-review-docs.mjs
      provides: "Corpus parsing, three-way disposition selection, hard-fail count gates, emit-easy / emit-major / check subcommands"
    - path: .planning/reviews/EASY-FIXES.md
      provides: "DOC-01 deliverable — 77 easy-fix record blocks with derivation and reconciliation"
      contains: "finding_id:"
    - path: .planning/reviews/MAJOR-REFACTORS.md
      provides: "DOC-02 deliverable — 144 major-refactor record blocks with derivation and reconciliation"
      contains: "proposed_approach:"
  key_links:
    - from: ".planning/phases/68-deliverable-documents/derive-review-docs.mjs"
      to: ".planning/reviews/61-COVERAGE.md .. 66-COVERAGE.md"
      via: "read-only readFileSync of exactly six paths resolved from the script's own directory"
      pattern: "COVERAGE\\.md"
    - from: ".planning/phases/68-deliverable-documents/derive-review-docs.mjs"
      to: ".planning/phases/67-apply-easy-fixes/67-APPLY-SET.md"
      via: "read-only lift of the 77 completed ledger rows (D-04)"
      pattern: "APPLY-SET\\.md"
    - from: ".planning/reviews/EASY-FIXES.md"
      to: "git object store"
      via: "every commit: sha in an applied row resolves via git cat-file -e"
      pattern: "^commit: "
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT state any count, coverage claim or completeness statement whose derivation is not written down and re-runnable — no total may appear in either document without the selection rule that produced it"
    - requirement_id: DOC-02
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT restate as established fact any finding-record claim that a later phase empirically falsified — a lifted claim that was disproven carries the correction alongside it or is not lifted"
    - requirement_id: DOC-02
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT re-classify, re-triage, re-word or re-score any finding record while assembling — the documents transcribe verdicts they did not make, and a disagreement is reported as a discrepancy rather than absorbed into the transcription"
---

<objective>
Stand up the derivation apparatus and prove it end-to-end: corpus in, both deliverable documents out,
verified by a re-runnable check.

Purpose: Phase 68 assembles 224 finding records into two documents. Before any judgment content is
written, the mechanical spine must exist and be provable — the script that selects records by
`disposition:` (D-01), the hard-fail gate that turns a wrong denominator into a reported finding
rather than a silent adjustment (D-02), and both documents actually existing on disk with every
record block present. This plan walks that entire path once, thinly, and leaves a `check` subcommand
that every later plan verifies against.

Output: `derive-review-docs.mjs`, `.planning/reviews/EASY-FIXES.md` (77 blocks) and
`.planning/reviews/MAJOR-REFACTORS.md` (144 blocks), each opening with a Derivation section, with
judgment fields carrying explicit placeholder markers.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-deliverable-documents/68-CONTEXT.md
@.planning/phases/68-deliverable-documents/68-PATTERNS.md
@CLAUDE.md
</context>

<phase_conventions>
These conventions bind every plan in Phase 68 (68-01 … 68-07). This plan establishes them; later
plans cite this block.

**Never `cat` a COVERAGE file.** They are 1.8k–3.9k lines / 140–480 KB each. Extract exactly one
finding record with:

    awk -v ID=<FINDING-ID> '$0 ~ "^id: +"ID"$"{f=1} f{print} f&&/^```$/{exit}' .planning/reviews/<NN>-COVERAGE.md

**Never `cat` `67-APPLY-SET.md`** (1,995 lines). Extract one ledger row with:

    awk -v ID=<FINDING-ID> '$0 ~ "^finding_id: +"ID"$"{f=1} f{print} f&&/^```$/{exit}' .planning/phases/67-apply-easy-fixes/67-APPLY-SET.md

**The write boundary (D-12).** Exactly two files are created under `.planning/reviews/`:
`EASY-FIXES.md` and `MAJOR-REFACTORS.md`. `derive-review-docs.mjs`, PLAN files and SUMMARY files land
in `.planning/phases/68-deliverable-documents/`. Nothing else in the tree is written — no source, no
`INVENTORY.md`, no `6N-COVERAGE.md`, no `REQUIREMENTS.md`, no `ROADMAP.md` phase body, no GitHub
tracker. `git status --porcelain .planning/reviews/` must show exactly two entries and no
modification to any other file in that directory.

**Commit subject shape:** `docs(68): <what changed>`, matching Phase 67's ledger commits.

**Phase-local decision IDs.** `D-01`..`D-12` in this phase mean `68-CONTEXT.md`'s decisions. Any
reference to another phase's decision is written in full as "Phase 6N D-nn", in the plans and in
both documents.

**The honesty gate.** Whenever a derived number departs from the expected one, the script prints the
breakdown and exits non-zero, and the executor reports the departure. A derived number is never
adjusted to match an expectation, and an expectation is never edited to match a derived number
without the discrepancy being written down first.

**This phase re-triages nothing.** INVENTORY §3c is the routing rule and every record already carries
its six-test result. These documents transcribe the verdict; they never revise it. If assembly
surfaces something the sweeps missed, it is reported in the phase close-out as a discrepancy, not
written into the corpus as a 225th record.
</phase_conventions>

<flagged_assumptions>
The edge probe surfaced 11 items across DOC-01..DOC-04. Nine are authored as `must_haves.truths`
above (eight explicit, one structured `verification: backstop` marker). Two are `unclassified` and
stay unresolved; they are surfaced here rather than dropped, and are repeated in the plan that owns
the requirement:

1. **DOC-03 / unclassified.** The probe could not classify DOC-03 ("both documents state review
   coverage explicitly") into an edge category, so no boundary predicate was derived for it. Planner
   assumption carried into plan `68-04`: the preamble's correctness is a completeness-and-honesty
   property, checkable only against INVENTORY's named sections and the recorded environment gaps —
   not against a boundary condition. Flagged, not resolved.
2. **DOC-04 / unclassified.** Same for DOC-04 ("findings that are neither easy nor major are
   recorded with disposition and reason"). Planner assumption carried into plans `68-03` and `68-06`:
   the checkable form is a count equality against the corpus sub-blocks (3 + 24 + 0 + 14 + 30), not a
   boundary condition. Flagged, not resolved.

**Count reconciliation (no silent drops):** 11 probe-surfaced edges = 9 authored into `must_haves`
+ 2 surfaced as flagged assumptions. Prohibition recall surfaced 3 kept must-NOTs (all authored
descriptor-less into `must_haves.prohibitions` above); one further candidate — path traversal in the
script's file reads — is canon, covered by `/gsd-secure-phase` and eslint, and is dropped with this
breadcrumb rather than minted.
</flagged_assumptions>

<artifacts_this_phase_produces>
New symbols created by Phase 68. Every name below is net-new and does not exist anywhere in the tree
before this phase:

- **Files:** `.planning/phases/68-deliverable-documents/derive-review-docs.mjs`,
  `.planning/reviews/EASY-FIXES.md`, `.planning/reviews/MAJOR-REFACTORS.md`
- **Script subcommands:** `emit-easy`, `emit-major`, `emit-other`, `check`
- **Script flags:** `--force`
- **Script exported/module functions:** `extractRecords`, `parseFields`, `joined`, `fullJoined`,
  `selectByDisposition`, `renderEasyRow`, `renderMajorBlock`, `proposedLabels`, `areaForRecord`,
  `writeAtomic`, `assertCounts`, `runCheck`
- **Document section headings (both documents unless noted):** `## Derivation`, `## Coverage`,
  `## Reconciliation`, `## Index`, `## Rows` (EASY-FIXES.md), `## Records` (MAJOR-REFACTORS.md),
  `## Index (severity-sorted, for Phase 69 filing order)` (MAJOR-REFACTORS.md),
  `## Other Dispositions` (MAJOR-REFACTORS.md), `## Close-out`
- **Record field keys new to this phase:** `proposed_approach:`, `proposed_labels:`, `issue:`
- **Placeholder markers:** `PENDING-APPROACH`, `PENDING-RESOLUTION`, `PENDING-AREA`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end slice — corpus parsed, three-way selection gated, both documents on disk</name>
  <precondition>The six files `.planning/reviews/61-COVERAGE.md` … `66-COVERAGE.md` exist and together contain exactly 224 lines beginning `disposition:` (verify with `cat .planning/reviews/6[1-6]-COVERAGE.md | grep -c '^disposition:'`).</precondition>
  <reversibility rating="reversible">The script and both documents are net-new planning files; nothing outside `.planning/` reads them until Phase 69.</reversibility>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/EASY-FIXES.md,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/67-apply-easy-fixes/derive-apply-set.mjs` (149 lines, read in full — this is the template to fork per D-01, not merely a reference; its `extractRecords`, `parseFields`, `joined`, phase-then-ID sort and hard-fail gate are already proven against this exact corpus)
    - `.planning/phases/68-deliverable-documents/68-PATTERNS.md` (the pattern map; §`derive-review-docs.mjs` names every function to carry over and every divergence)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-01, D-02, D-12
    - `.planning/reviews/INVENTORY.md` lines 136-160 only (§"Finding Record Template" — the frozen field order)
    - One real record to see the field shape, extracted with the awk recipe in `<phase_conventions>`: `P65-D1-001` from `.planning/reviews/65-COVERAGE.md`
  </read_first>
  <action>
Fork `derive-apply-set.mjs` into `.planning/phases/68-deliverable-documents/derive-review-docs.mjs`.
Keep its header-comment style, its `SCRIPT_DIR` / `REVIEWS_DIR` path resolution
(`join(SCRIPT_DIR, '..', '..', 'reviews')` — unchanged, both phase directories are siblings), its
`extractRecords` fence regex, its `parseFields` continuation-line parser, its `joined` helper, and
its phase-then-ID sort. Add a `fullJoined` helper that preserves every continuation line joined by a
single space — `derive-apply-set.mjs`'s `firstThreeLines` truncation is not carried over, because
D-04 and ISSUE-02 require `failure_scenario:` to stand alone in full.

Selection is three-way on the leading token of the `disposition:` field, per D-01: `major-refactor`,
`easy-fix`, `wontfix`. Records are read from exactly the six paths
`.planning/reviews/{61,62,63,64,65,66}-COVERAGE.md`, read-only, and from nowhere else.

Hard-fail gate, per D-02 and the pattern `derive-apply-set.mjs` established: expected total 224;
expected disposition split major-refactor 144 / easy-fix 77 / wontfix 3; expected per-phase record
totals 61=73, 62=34, 63=62, 64=44, 65=3, 66=8; expected per-phase major-refactor split 61=29, 62=20,
63=52, 64=34, 65=3, 66=6; expected per-phase easy-fix split 61=44, 62=14, 63=10, 64=8, 65=0, 66=1;
expected wontfix IDs exactly `P64-D8-002`, `P64-D6-012`, `P66-D5-003`. On any departure, print the
full derived breakdown to stderr, state that the departure is to be treated as a finding and not
silently adjusted, and set a non-zero exit code without emitting anything.

Add subcommands dispatched from `process.argv[2]`: `emit-easy` and `emit-major` write their rendered
blocks to stdout; `check` validates the two assembled documents (this task's `check` covers only
counts and finding-ID set equality — later tasks extend it). Add a `writeAtomic(path, text)` helper
used for any file this script itself writes: write to `<path>.tmp` in the same directory, then
`renameSync` into place, so an interrupted run cannot leave a half-written deliverable.

For this task the emitted block is the thin slice: for `emit-easy`, a fenced block per record
carrying `row:`, `finding_id:`, `unit:`, `location:`, `dimension:`, `severity:`, `effort:` (verbatim,
annotation preserved) and `failure_scenario:` (full); for `emit-major`, a fenced block per record
carrying INVENTORY's frozen field order verbatim. Judgment fields are not emitted yet — plan `68-02`
adds them. Field values are written with the key left-aligned and the value starting at column 20,
matching `67-APPLY-SET.md`'s row shape.

Then compose both deliverables without paging their content through the agent's context, using shell
redirection: write a short header file containing the `# ` title and a `## Derivation` section, then
concatenate the header with the script's stdout into the target path. `## Derivation` states, in
prose: the selection rule (records selected by the leading token of `disposition:` across the six
closed COVERAGE files), the script name and how to re-run it, the ordering rule (originating phase
then finding ID, per D-10), and the fact that the script emits the mechanical scaffold while judgment
fields are authored in the document — so a reader knows re-running `emit-*` regenerates the scaffold
only and is not a safe overwrite of the assembled file.

Do not write to `INVENTORY.md`, any `6N-COVERAGE.md`, `67-APPLY-SET.md`, any source file, or the
GitHub tracker.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/.planning/phases/68-deliverable-documents && node derive-review-docs.mjs emit-major > /tmp/m1.txt && node derive-review-docs.mjs emit-major > /tmp/m2.txt && cmp /tmp/m1.txt /tmp/m2.txt && test "$(grep -c '^finding_id:' /home/coder/repos/bbj-language-server/.planning/reviews/EASY-FIXES.md)" -eq 77 && test "$(grep -c '^id:' /home/coder/repos/bbj-language-server/.planning/reviews/MAJOR-REFACTORS.md)" -eq 144 && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `node derive-review-docs.mjs check` exits 0.
    - `grep -c '^finding_id:' .planning/reviews/EASY-FIXES.md` outputs exactly `77`.
    - `grep -c '^id:' .planning/reviews/MAJOR-REFACTORS.md` outputs exactly `144`.
    - `grep -c '^disposition:' .planning/reviews/MAJOR-REFACTORS.md` outputs exactly `144`.
    - Two consecutive `emit-major` runs produce byte-identical output (`cmp` exits 0); same for `emit-easy`.
    - Temporarily changing the expected total from 224 to 223 in the script makes every subcommand exit non-zero and print the derived breakdown; the change is reverted before commit and `git diff` on the script is empty with respect to that constant.
    - `grep -c 'renameSync' .planning/phases/68-deliverable-documents/derive-review-docs.mjs` is at least 1.
    - `grep -Eic 'child_process|https?://|fetch\(' .planning/phases/68-deliverable-documents/derive-review-docs.mjs` outputs `0`.
    - `git status --porcelain .planning/reviews/` lists exactly two entries, both untracked-new, and neither is a COVERAGE file nor `INVENTORY.md`.
    - Both documents open with a `# ` title line followed by a `## Derivation` section that names `derive-review-docs.mjs`, the `disposition:` selection rule, and the phase-then-ID ordering rule.
  </acceptance_criteria>
  <done>Both deliverable documents exist on disk with every record block present, produced end-to-end by a committed, re-runnable, deterministic script whose count gate fails loudly on a wrong denominator.</done>
</task>

<task type="auto">
  <name>Task 2: Check subcommand hardened into the phase's standing gate</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (as written by Task 1 — the current state, not an assumption about it)
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 1985-1995 only (§"Close-out review" — records that Phase 67's orchestrator verified all 98 ledger commit shas with `git cat-file -e`; this task re-uses that check)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-09 (the `issue:` slot and its `costly` rating)
  </read_first>
  <action>
Extend the `check` subcommand so every later plan in this phase has one command that proves its work.
`check` reads the two assembled documents from `.planning/reviews/` and asserts, printing one line
per assertion and exiting non-zero on the first failure with the offending finding IDs named:

1. **Counts and ID-set equality** (already present from Task 1) — 77 and 144 blocks, and the finding
   ID set in each document equals the set the corpus selection produces, reported as
   "in document not in corpus" and "in corpus not in document" lists rather than a bare inequality.
2. **Required-field presence per block.** Every EASY-FIXES.md block carries `row:`, `finding_id:`,
   `unit:`, `location:`, `dimension:`, `severity:`, `effort:`, `verdict:`, `failure_scenario:`,
   `fix_applied:`, `user_facing:`, `verification:`, `commit:`, `notes:`. Every MAJOR-REFACTORS.md
   block carries INVENTORY's thirteen frozen keys (`id:`, `unit:`, `location:`, `dimension:`,
   `secondary:`, `severity:`, `evidence_tier:`, `evidence:`, `failure_scenario:`, `classification:`,
   `effort:`, `dedup:`, `disposition:`) plus `proposed_approach:`, `proposed_labels:` and `issue:`.
   A key present with an empty value is reported distinctly from a key that is absent.
3. **Verbatim fidelity.** For each record, the document's `failure_scenario:` equals the COVERAGE
   source's `failure_scenario:` after collapsing runs of whitespace to a single space, and no field
   value contains a triple-backtick sequence that would terminate a fence.
4. **Commit-sha resolvability.** Every whitespace- or punctuation-delimited 7-to-40-character
   lowercase hex token in an EASY-FIXES.md `commit:` field resolves via `git cat-file -e <sha>^{commit}`,
   run once per unique sha. Rows whose `commit:` field carries a prose reason instead of a sha (the
   `no-op`, `excluded` and `deferred` verdicts) are exempted by verdict, not by pattern.
5. **Credential-shaped-literal scan.** Neither document contains `ghp_`, `github_pat_`, `AKIA`,
   `xox`, or a PEM private-key header. This closes T-68-02 before Phase 69 lifts this prose into
   public GitHub issues.
6. **Placeholder inventory.** Report the count of each placeholder marker still present in each
   document, grouped by marker, as informational output — this is a census, not a failure, until
   plan `68-07` turns it into a hard gate.
7. **Determinism.** Two in-process renders of each emit produce identical strings.

Add the regeneration guard, mitigating T-68-03 and discharging D-09's `costly` rating: before
`emit-easy` or `emit-major` writes or emits, if `.planning/reviews/MAJOR-REFACTORS.md` exists and
contains any `issue:` line whose value is non-empty, print that Phase 69 has already written filed
issue numbers into the document and that regenerating would clobber them, and exit non-zero — unless
`--force` was passed on the command line.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/.planning/phases/68-deliverable-documents && node derive-review-docs.mjs check && cp /home/coder/repos/bbj-language-server/.planning/reviews/MAJOR-REFACTORS.md /tmp/mr.bak && sed -i '0,/^issue: *$/s//issue:             #999/' /home/coder/repos/bbj-language-server/.planning/reviews/MAJOR-REFACTORS.md && ! node derive-review-docs.mjs emit-major > /dev/null 2>&1 && node derive-review-docs.mjs emit-major --force > /dev/null && cp /tmp/mr.bak /home/coder/repos/bbj-language-server/.planning/reviews/MAJOR-REFACTORS.md && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `node derive-review-docs.mjs check` exits 0 and prints one PASS line per assertion group, including the ID-set equality result for both documents.
    - With a single `issue:` value set to `#999` in MAJOR-REFACTORS.md, `node derive-review-docs.mjs emit-major` exits non-zero and emits nothing to stdout; adding `--force` makes the same command exit 0.
    - Deleting one `unit:` line from any MAJOR-REFACTORS.md block makes `check` exit non-zero and name that block's finding ID; the line is restored and `check` returns to exit 0.
    - Replacing one document `failure_scenario:` value with different prose makes `check` exit non-zero and name that finding ID; the value is restored and `check` returns to exit 0.
    - Inserting the literal `AKIA` into either document makes `check` exit non-zero; it is removed and `check` returns to exit 0.
    - `check` prints a placeholder census naming each marker and its per-document count.
    - The commit-sha assertion reports the number of unique shas checked, and that number is greater than 90 (Phase 67's close-out recorded 98 unique shas across its 77 rows).
  </acceptance_criteria>
  <done>One command, `node derive-review-docs.mjs check`, proves counts, ID-set equality, field presence, verbatim fidelity, sha resolvability and secret-freedom, and the regeneration guard makes a post-Phase-69 clobber impossible without an explicit override.</done>
</task>

<task type="auto">
  <name>Task 3: Reconciliation sections state the arithmetic in both documents</name>
  <files>
    .planning/reviews/EASY-FIXES.md,
    .planning/reviews/MAJOR-REFACTORS.md,
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  </files>
  <read_first>
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 16-33 only (§"Reconciliation" — the prose-arithmetic pattern this section copies)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-01, D-02, D-03, D-06
    - `.planning/reviews/EASY-FIXES.md` and `.planning/reviews/MAJOR-REFACTORS.md` (current state after Task 2)
  </read_first>
  <action>
Add a `## Reconciliation` section immediately after `## Derivation` in each document, written as prose
arithmetic in the shape `67-APPLY-SET.md` §"Reconciliation" uses — state the selection rule, state the
count, argue every exclusion in writing, and make the derivation re-runnable.

`MAJOR-REFACTORS.md` records: 224 records selected across the six closed COVERAGE files, splitting
144 major-refactor + 77 easy-fix + 3 wontfix = 224, with the per-phase major split 61=29, 62=20,
63=52, 64=34, 65=3, 66=6 = 144. It states that the 77 easy-fix records live in `EASY-FIXES.md` and
the 3 wontfix records live in this document's `## Other Dispositions` section, so no row of the 224
is absent from the pair of documents. It states the severity distribution of the 144 (1 critical,
16 high, 70 medium, 57 low) and the effort distribution after INVENTORY §3d normalisation (40 × 2,
59 × 4, 45 × 8), naming that three records — `P63-D3-005`, `P66-D4-001`, `P66-D2-002` — carry an
in-record annotation on the `effort:` value that is carried through verbatim rather than stripped to
the bare number.

`EASY-FIXES.md` records: 77 easy-fix records selected, splitting 70 `applied` + 4 `no-op` +
2 `excluded` + 1 `deferred` = 77 (D-03), naming the four `no-op` records `P61-D8-001`, `P61-D8-002`,
`P61-D8-006`, `P62-D5-004`; the two `excluded` records `P64-D8-003`, `P64-D8-004`; and the one
`deferred` record `P63-D7-004`. It states that 29 of the 77 rows carry `user_facing: yes` and that
those rows are what discharges FIX-04 under D-11. It states that two records — `P61-D4-010` and
`P61-D8-005` — carry `effort: 1`, off INVENTORY §3d's locked `{2,4,8}` scale, and that Phase 67's
close-out §"Recorded departures" found no in-record annotation for either despite `68-CONTEXT.md`
D-02 asserting one exists; the value is carried through unrounded and the discrepancy is stated here
rather than an annotation being fabricated. It closes with the one-line pointer required by D-06:
findings that are neither easy-fix nor major-refactor are recorded in `MAJOR-REFACTORS.md`
§"Other Dispositions", and this document is not duplicated there.

Extend `check` with one further assertion group: every integer appearing in a Reconciliation section's
arithmetic that names a derived quantity is re-derived from the corpus at check time and compared, so
a Reconciliation number that drifts from the corpus fails the gate rather than sitting stale.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^## Reconciliation' .planning/reviews/EASY-FIXES.md && grep -q '^## Reconciliation' .planning/reviews/MAJOR-REFACTORS.md && grep -Fq 'P61-D4-010' .planning/reviews/EASY-FIXES.md && grep -Fq 'P63-D7-004' .planning/reviews/EASY-FIXES.md && grep -Fq 'Other Dispositions' .planning/reviews/EASY-FIXES.md && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - Both documents contain a `## Reconciliation` heading positioned after `## Derivation` and before the record blocks.
    - `MAJOR-REFACTORS.md` §Reconciliation contains the literals `224`, `144`, `77` and `3`, and the per-phase major split `61=29`-equivalent prose naming all six phases.
    - `EASY-FIXES.md` §Reconciliation contains the literals `70`, `4`, `2`, `1` and `29`, and names all seven non-applied finding IDs: `P61-D8-001`, `P61-D8-002`, `P61-D8-006`, `P62-D5-004`, `P64-D8-003`, `P64-D8-004`, `P63-D7-004`.
    - `grep -F 'Other Dispositions' .planning/reviews/EASY-FIXES.md` returns at least one line, and `grep -c '^## Other Dispositions' .planning/reviews/EASY-FIXES.md` outputs `0` — the pointer exists, the section is not duplicated.
    - `node derive-review-docs.mjs check` exits 0 with the Reconciliation-arithmetic assertion group reported.
    - Editing `224` to `225` in `MAJOR-REFACTORS.md` §Reconciliation makes `check` exit non-zero; the edit is reverted and `check` returns to exit 0.
  </acceptance_criteria>
  <done>Both documents state their own arithmetic in prose, every exclusion is argued in writing, and a stale Reconciliation number fails the standing gate instead of sitting unnoticed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| six COVERAGE files → `derive-review-docs.mjs` | review prose authored by earlier phases crosses into a generated document; treated as data, never as instruction |
| `derive-review-docs.mjs` → `.planning/reviews/` | the only write boundary this phase may cross (D-12) |
| these documents → Phase 69 GitHub issues | lifted `failure_scenario:` / `proposed_approach:` prose becomes public tracker content under ISSUE-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Tampering | `derive-review-docs.mjs` write paths | medium | mitigate | Output paths are two constants resolved from `SCRIPT_DIR`; Task 1 asserts `git status --porcelain .planning/reviews/` lists exactly two entries and no COVERAGE/INVENTORY modification |
| T-68-02 | Information disclosure | lifted `evidence:` / `failure_scenario:` prose reaching public issues in Phase 69 | medium | mitigate | Task 2 `check` assertion 5 fails on `ghp_`, `github_pat_`, `AKIA`, `xox` or a PEM private-key header in either document |
| T-68-03 | Tampering | `MAJOR-REFACTORS.md` after Phase 69 writes `issue:` numbers (ISSUE-05) | high | mitigate | Task 2 regeneration guard: `emit-*` refuses to run and emits nothing when any `issue:` value is non-empty, unless `--force` |
| T-68-04 | Repudiation | counts stated without a derivation a reader can re-run | low | mitigate | `## Derivation` names the selection rule and the script; `## Reconciliation` states the arithmetic; Task 3 re-derives every stated number at check time |
| T-68-05 | Spoofing | a fabricated or mistyped `commit:` sha misattributing an applied fix | medium | mitigate | Task 2 `check` assertion 4 resolves every unique sha with `git cat-file -e <sha>^{commit}` |
| T-68-06 | Denial of service | fence regex backtracking over the 17,730-line corpus | low | accept | The pattern is lazily bounded by a literal fence and the corpus is fixed, local and read-only; `derive-apply-set.mjs` has run it on this exact corpus |
| T-68-07 | Elevation of privilege | script execution | low | accept | Pure Node built-ins (`node:fs`, `node:path`, `node:url`); no `child_process`, no network, asserted by Task 1's acceptance criteria |
| T-68-SC | Tampering | npm/pip/cargo installs | low | accept | This phase adds no package-manager install task and the script imports only Node built-ins, so the Package Legitimacy Gate does not fire; no `[ASSUMED]`/`[SUS]` package exists to check |
</threat_model>

<verification>
- `node derive-review-docs.mjs check` exits 0 from `.planning/phases/68-deliverable-documents/`.
- `git status --porcelain .planning/reviews/` lists exactly two entries: `EASY-FIXES.md` and `MAJOR-REFACTORS.md`, both new.
- `git status --porcelain` shows no modification to `.planning/reviews/INVENTORY.md`, any `6N-COVERAGE.md`, `.planning/REQUIREMENTS.md`, or any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/`.
- Two consecutive `emit-easy` runs and two consecutive `emit-major` runs are byte-identical.
</verification>

<success_criteria>
- `derive-review-docs.mjs` is committed, runs from its own directory, reads only the six COVERAGE files and `67-APPLY-SET.md`, and exits non-zero with a printed breakdown on any count departure.
- `.planning/reviews/EASY-FIXES.md` holds 77 record blocks; `.planning/reviews/MAJOR-REFACTORS.md` holds 144.
- Both documents open with `## Derivation` then `## Reconciliation`.
- The regeneration guard blocks an `emit-*` that would clobber a filed `issue:` number.
</success_criteria>

<output>
Create `.planning/phases/68-deliverable-documents/68-01-SUMMARY.md` when done, recording: the derived
counts as printed by the script, any departure from the expected denominators (reported, never
adjusted), the unique-sha count checked, and the placeholder census both documents end this plan with.
</output>
