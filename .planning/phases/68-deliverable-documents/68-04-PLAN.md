---
phase: 68-deliverable-documents
plan: 04
type: execute
wave: 4
depends_on: ["68-03"]
files_modified:
  - .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  - .planning/reviews/EASY-FIXES.md
  - .planning/reviews/MAJOR-REFACTORS.md
autonomous: true
requirements: [DOC-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both documents open with the same `## Coverage` block, positioned after the title and before `## Derivation`, and the two copies are byte-identical — asserted by `check`, not by eye."
    - "The scope half names 21 review units plus 8 file-exception rows across four sweep phases, the eight dimensions D1 Security through D8 Doc accuracy, the grid totals 148 `applies` and 84 `n/a` summing to 232 cells, and the evidence-tier distribution 108 `repro` / 80 `trace` / 36 `inherited` across the 224 records."
    - "The named exclusions are listed with their reasons: `java-interop/` (scope decision at milestone start; the TypeScript client at `RU-61-06` is reviewed), `bbj-vscode/src/language/generated/` (17.5k LOC, machine-generated from `bbj.langium`), `bbj-vscode-deprecated/` (stale `.vsix`, no source), grammar redesign, wholesale test authoring, implementing the major refactors, and new features of any kind."
    - "The gap half states four gaps as plainly as the scope: no IntelliJ fix was compiled or tested because the only installed JDK is Temurin 25.0.3 while `bbj-intellij/build.gradle.kts` targets Java 17, leaving nine applied fixes review-verified only and one deferred; 11 deterministic `npm test` failures in `test/linking.test.ts > Linking Tests > Interop related tests` that opening a listener on port 5008 does not fix; 24 not-reproducible candidate claims; and Phase 65's shape, where roughly 36 enumerated items yielded 3 recorded findings because the rest were settled by direct code trace."
    - "The preamble summarises INVENTORY rather than restating its grid verbatim, and cites `INVENTORY.md` §\"Applicability Grid\", §\"Surface Accounting & Named Exclusions\" and §\"Test & Build Baseline (D-05, D-06)\" by section name so an auditor has the path."
    - "No requirement's wording is restated as though it had been met — the gap half names each shortfall and its cause, following the honesty pattern Phase 66 D-02 and Phase 67 D-07/D-14 established."
    - statement: "A reader who opens only one of the two documents sees the same coverage picture as a reader who opens the other, including the same gaps."
      verification: backstop
  artifacts:
    - path: .planning/reviews/EASY-FIXES.md
      provides: "The shared DOC-03 coverage preamble, first section after the title"
      contains: "## Coverage"
    - path: .planning/reviews/MAJOR-REFACTORS.md
      provides: "The identical DOC-03 coverage preamble"
      contains: "## Coverage"
  key_links:
    - from: ".planning/reviews/EASY-FIXES.md"
      to: ".planning/reviews/MAJOR-REFACTORS.md"
      via: "the `## Coverage` block is generated once and emitted into both documents; `check` compares the two extracted blocks byte for byte"
      pattern: "^## Coverage"
    - from: ".planning/reviews/MAJOR-REFACTORS.md"
      to: ".planning/reviews/INVENTORY.md"
      via: "the preamble cites INVENTORY section names for the full grid and surface accounting rather than restating them"
      pattern: "Applicability Grid"
  prohibitions:
    - requirement_id: DOC-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT write a coverage statement that reads as complete when it is not — the gaps are stated as plainly as the scope, and a preamble listing only what was reviewed does not discharge DOC-03"
    - requirement_id: DOC-03
      category: integrity
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT describe a fix as verified when no compiler, test or runtime confirmed it — the nine IntelliJ fixes are named as review-verified only, and no stronger word is used for them"
    - requirement_id: DOC-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT restate a requirement's wording as if it had been met — where a requirement is partially met the shortfall and its cause are named in the same sentence"
---

<objective>
Give both documents the same opening coverage statement, with the gaps stated as plainly as the scope.

Purpose: DOC-03's wording is "so coverage gaps are visible to a reader". A preamble that lists only
what was reviewed does not discharge it — it produces exactly the impression the requirement exists to
prevent, that everything was checked. This plan writes one block, half scope and half gap, and emits
it into both documents identically so neither can drift into a rosier version of the other.

Output: a `## Coverage` section opening both `EASY-FIXES.md` and `MAJOR-REFACTORS.md`, byte-identical,
generated once by `derive-review-docs.mjs` and asserted identical by `check`.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-deliverable-documents/68-CONTEXT.md
@.planning/phases/68-deliverable-documents/68-PATTERNS.md
@.planning/phases/68-deliverable-documents/68-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `68-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.

**Final section order in both documents**, fixed by this plan: the `# ` title, then `## Coverage`,
then `## Derivation`, then `## Reconciliation`, then the index tables, then the record blocks, then
`## Other Dispositions` (MAJOR-REFACTORS.md only), then `## Close-out`. Plan `68-01` created
`## Derivation` as the first section; this plan moves `## Coverage` above it.
</phase_conventions>

<flagged_assumptions>
DOC-03's edge-probe row is `unclassified` and stays unresolved (see `68-01-PLAN.md`
§`<flagged_assumptions>`, item 1). The planner assumption this plan carries: the preamble's
correctness is a completeness-and-honesty property checkable against INVENTORY's named sections and
the recorded environment gaps, not against a boundary condition. Flagged, not resolved.
</flagged_assumptions>

<artifacts_this_phase_produces>
See `68-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds: script function
`renderCoveragePreamble`; document headings `## Coverage`, `### Scope`, `### Gaps`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The scope half — units, dimensions, grid totals, evidence tiers, named exclusions</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/EASY-FIXES.md,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after plan `68-03`)
    - `.planning/reviews/INVENTORY.md` lines 782-800 only (§"Applicability Grid" header and the 21-unit table's first rows — enough to see the dimension column names)
    - `.planning/reviews/INVENTORY.md` lines 816-833 only (§"File-exception rows" — the 8 rows)
    - `.planning/reviews/INVENTORY.md` lines 905-916 only (§"Grid totals")
    - `.planning/reviews/INVENTORY.md` lines 916-944 only (§"Surface Accounting & Named Exclusions" — the top-level disposition table)
    - `.planning/reviews/INVENTORY.md` lines 84-102 only (§3b "Evidence Tiers")
    - `.planning/REQUIREMENTS.md` lines 100-113 only (§"Out of Scope" — the seven-row exclusion table)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-08
  </read_first>
  <action>
Add a `renderCoveragePreamble` function to `derive-review-docs.mjs` that returns one string, used for
both documents. Give it a `### Scope` sub-section containing:

The review surface: 21 review units across the four sweep phases — `RU-61-01`..`RU-61-07`
(`bbj-vscode/src/language/`), `RU-62-01`..`RU-62-05` (extension host, composers, TextMate),
`RU-63-01`..`RU-63-05` (`bbj-intellij/`, 61 Java files), `RU-64-01`..`RU-64-03` (build, CI,
dependencies and tools) and the cross-cutting `RU-D8-01` documentation unit — plus 8 file-exception
rows for files whose applicability differs from their unit's.

The dimensions: D1 Security, D2 Correctness, D3 Performance, D4 Maintainability, D5 Test coverage,
D6 Dependency health, D7 Cross-IDE parity, D8 Doc accuracy, applied cell by cell. The grid totals:
29 rows x 8 dimensions = 232 cells, of which 148 are `applies` and 84 are `n/a` with a written
exclusion marker. State the totals and cite `INVENTORY.md` §"Applicability Grid" and §"Grid totals"
for the cell-by-cell detail rather than restating the grid — a reader gets a truthful picture without
a second file open, an auditor gets the path.

The evidence standard: every one of the 224 records clears an evidence tier, distributed 108 `repro`,
80 `trace` and 36 `inherited`, defined in `INVENTORY.md` §3b. State that this means no record in
either document is an unverified claim, and that the separate population of claims that could not
clear a tier is recorded in `MAJOR-REFACTORS.md` §"Other Dispositions" rather than dropped.

The named exclusions, each with its reason: `java-interop/`'s Java service (excluded by scope decision
at milestone start, FUT-01; the TypeScript-side client is reviewed at `RU-61-06`, and
`java-interop/build.gradle` is read once by `RU-64-02` only as a dependency-tree source, not as a code
review); `bbj-vscode/src/language/generated/` (17.5k LOC machine-generated from `bbj.langium`, whose
grammar source is reviewed instead); `bbj-vscode-deprecated/` (a stale `bbj-vscode-0.1.999.vsix`
artifact only, no source, flagged for removal); grammar redesign to remove parser ambiguity (v3.3
established all 47 ambiguities resolve correctly; a language-level project); wholesale test-suite
authoring (coverage gaps are reported as findings, only regression tests for applied fixes are
written); implementing the major refactors (the milestone deliverable is detailed issues for separate
resolution); and new features of any kind. Cite `REQUIREMENTS.md` §"Out of Scope" and `INVENTORY.md`
§"Surface Accounting & Named Exclusions" for the full per-directory accounting, and name
`documentation/` as scoped-but-D8-only and `README.md` as an excluded candidate for a future D8 unit,
so the two boundary cases are not read as oversights.

The corpus size: 224 recorded findings, split 144 major-refactor, 77 easy-fix and 3 wontfix, derived
by the selection rule stated in `## Derivation`.

Emit the rendered block into both documents as a `## Coverage` section positioned immediately after
the `# ` title line and before `## Derivation`, moving `## Derivation` down. Regenerate both
documents via shell redirection so their record content never passes through the agent's context.
</action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^## Coverage' .planning/reviews/EASY-FIXES.md && grep -q '^## Coverage' .planning/reviews/MAJOR-REFACTORS.md && grep -q '^### Scope' .planning/reviews/MAJOR-REFACTORS.md && for s in 'Applicability Grid' 'Surface Accounting' 'java-interop/' 'bbj-vscode-deprecated/' 'generated/'; do grep -Fq "$s" .planning/reviews/EASY-FIXES.md || exit 1; done && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `## Coverage` is the first `##` heading in both documents, appearing before `## Derivation` in each.
    - The `### Scope` sub-section names the literals `21`, `8`, `232`, `148`, `84`, `224`, `108`, `80` and `36`.
    - All eight dimension names appear: `D1 Security`, `D2 Correctness`, `D3 Performance`, `D4 Maintainability`, `D5 Test coverage`, `D6 Dependency health`, `D7 Cross-IDE parity`, `D8 Doc accuracy`.
    - All seven `REQUIREMENTS.md` §"Out of Scope" exclusions appear with a reason, and `documentation/` (D8-only) and `README.md` (excluded, future-D8 candidate) are each named.
    - The block cites `INVENTORY.md` §"Applicability Grid" and §"Surface Accounting & Named Exclusions" by section name rather than restating the grid rows.
    - Both documents' `## Coverage` sections extract to identical text at this point (`diff <(awk ...) <(awk ...)` is empty).
    - `node derive-review-docs.mjs check` exits 0.
  </acceptance_criteria>
  <done>Both documents open with the same statement of what was reviewed, at what granularity, against which dimensions, and what was deliberately not reviewed and why.</done>
</task>

<task type="auto">
  <name>Task 2: The gap half — what was not checked, stated as plainly as what was</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs,
    .planning/reviews/EASY-FIXES.md,
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after Task 1)
    - `.planning/reviews/INVENTORY.md` lines 1079-1140 only (§"Test & Build Baseline (D-05, D-06)" — the headline counts, the 11 named failing tests, and the flaky suite-level variance; this is the sole authority for these numbers, do not restate suite numbers from anywhere else)
    - `.planning/reviews/INVENTORY.md` lines 1162-1182 only (§"`bbj-intellij/./gradlew build`" and §"Contrast with v3.9")
    - `.planning/phases/67-apply-easy-fixes/67-APPLY-SET.md` lines 1751-1810 only (§"Close-out" and §"Denominator" — the source for the nine review-verified IntelliJ rows)
    - `.planning/phases/68-deliverable-documents/68-CONTEXT.md` §`<decisions>` D-08 (the gap half in full) and §`<code_context>` "Environment facts"
  </read_first>
  <action>
Add a `### Gaps` sub-section to `renderCoveragePreamble`, written in the honesty pattern Phase 66 D-02
and Phase 67 D-07/D-14 established: say what is true at phase end, name the shortfall and its cause,
cite the record IDs, and never restate a requirement's wording as if it had been met. Four gaps, each
a named paragraph:

**No IntelliJ fix was compiled or tested.** The only installed JDK is Temurin 25.0.3 at
`/opt/java/default` and `bbj-intellij/build.gradle.kts` targets Java 17, so `./gradlew build` cannot
run in this environment. Nine applied `bbj-intellij/` fixes are review-verified only under Phase 67
D-14, and one further easy fix — `P63-D7-004` — is deferred for the same reason under Phase 67 D-15.
State that "review-verified" means a human-readable statement-by-statement check and nothing stronger;
no compiler and no test confirmed any of the ten.

**11 deterministic `npm test` failures.** All 11 are in
`test/linking.test.ts > Linking Tests > Interop related tests` and reproduce identically across runs,
caused by an unreachable java-interop peer. Opening a listener on port 5008 does not fix them (Phase
64 D-06) — that has been tried. State separately that the suite-level file-failure and skip counts
vary between otherwise identical runs because a `beforeAll` hook
(`WorkspaceManager.initializeWorkspace()`) intermittently exceeds vitest's default 10-second
`hookTimeout` under load, so a reader comparing two runs is not misled into reading the variance as a
regression. Cite `INVENTORY.md` §"Test & Build Baseline (D-05, D-06)" as the authority for these
numbers and take no suite counts from any other source.

**24 not-reproducible candidate claims.** Each is an area a reviewer looked at and could not settle
within a read-only sweep — a runtime measurement, a BBj interpreter or repository-settings access it
did not have. They are coverage gaps by definition. Link to `MAJOR-REFACTORS.md`
§"Other Dispositions" §"not-reproducible", which enumerates all 24 with the tier each failed and why.

**Phase 65's shape.** Its cross-cutting security audit enumerated roughly 36 items and recorded 3 as
findings, because the remainder were settled by direct code trace rather than left unexamined. State
this plainly so a reader does not infer that the security audit found almost nothing.

Regenerate both documents. The `### Gaps` sub-section is part of the same single rendered block as
`### Scope`, so it is emitted identically into both files by construction.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && grep -q '^### Gaps' .planning/reviews/EASY-FIXES.md && grep -q '^### Gaps' .planning/reviews/MAJOR-REFACTORS.md && for s in 'Temurin 25.0.3' 'P63-D7-004' '5008' 'linking.test.ts' 'hookTimeout'; do grep -Fq "$s" .planning/reviews/EASY-FIXES.md || exit 1; done && diff <(awk '/^## Coverage/,/^## Derivation/' .planning/reviews/EASY-FIXES.md) <(awk '/^## Coverage/,/^## Derivation/' .planning/reviews/MAJOR-REFACTORS.md) && cd .planning/phases/68-deliverable-documents && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `### Gaps` exists in both documents with four named gap paragraphs.
    - The IntelliJ gap names `Temurin 25.0.3`, `bbj-intellij/build.gradle.kts`, Java 17, the nine review-verified fixes under Phase 67 D-14, and `P63-D7-004` deferred under Phase 67 D-15.
    - The test gap names the literal `11`, the suite path `test/linking.test.ts`, the test group `Interop related tests`, port `5008`, Phase 64 D-06, and the `hookTimeout` cause of the run-to-run variance.
    - The not-reproducible gap names the literal `24` and links to the `## Other Dispositions` section by name.
    - The Phase 65 paragraph names both the approximate `36` enumerated items and the `3` recorded findings.
    - The word "verified" is never applied unqualified to an IntelliJ fix; every occurrence in that paragraph is qualified as review-verified.
    - `diff` of the extracted `## Coverage` sections from the two documents is empty.
  </acceptance_criteria>
  <done>A reader sees what was not checked as clearly as what was, with the cause of each gap and the record IDs to follow.</done>
</task>

<task type="auto">
  <name>Task 3: Preamble identity and section order added to the standing check</name>
  <files>
    .planning/phases/68-deliverable-documents/derive-review-docs.mjs
  </files>
  <read_first>
    - `.planning/phases/68-deliverable-documents/derive-review-docs.mjs` (current state after Task 2)
    - `.planning/reviews/EASY-FIXES.md` — the `## Coverage` section only, extracted with `awk '/^## Coverage/,/^## Derivation/'`
  </read_first>
  <action>
Extend the `check` subcommand with a DOC-03 assertion group:

1. Both documents contain exactly one `## Coverage` heading, and it is the first `##` heading in each.
2. The text between `## Coverage` and the following `## Derivation` heading is byte-identical between
   the two documents; on mismatch, print the first differing line number and both lines rather than a
   bare inequality.
3. That text equals the string `renderCoveragePreamble` produces at check time, so a hand edit to one
   document's copy fails the gate instead of silently forking the two.
4. Both `### Scope` and `### Gaps` exist inside it, in that order.
5. The section order in each document matches the order fixed in this plan's `<phase_conventions>`
   block, reported as the observed heading sequence when it does not.

Report this group in the same one-line-per-assertion style as the existing groups.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/.planning/phases/68-deliverable-documents && node derive-review-docs.mjs check && cp /home/coder/repos/bbj-language-server/.planning/reviews/EASY-FIXES.md /tmp/ef.bak && sed -i '0,/^### Gaps/s//### Gaps EDITED/' /home/coder/repos/bbj-language-server/.planning/reviews/EASY-FIXES.md && ! node derive-review-docs.mjs check && cp /tmp/ef.bak /home/coder/repos/bbj-language-server/.planning/reviews/EASY-FIXES.md && node derive-review-docs.mjs check</automated>
  </verify>
  <acceptance_criteria>
    - `node derive-review-docs.mjs check` exits 0 and prints a DOC-03 assertion group with five named assertions.
    - Editing one document's `## Coverage` text makes `check` exit non-zero and print the first differing line number and both lines; restoring it returns `check` to exit 0.
    - Moving `## Coverage` below `## Derivation` in either document makes `check` exit non-zero and print the observed heading sequence; the move is reverted before commit.
    - `check` reports the preamble length in lines, so a future reader can see at a glance whether the block shrank.
  </acceptance_criteria>
  <done>The two coverage statements cannot drift apart, and a hand edit to either fails the standing gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `INVENTORY.md` + `REQUIREMENTS.md` → the preamble | scope and exclusion facts owned by other documents cross into a summary that a reader may trust without opening them |
| environment facts → the gap half | the JDK, port-5008 and test-suite facts are the difference between an honest and a misleading coverage claim |
| one document's preamble → the other's | two copies of the same block are two places a rosier version can appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-13 | Repudiation | a coverage statement that lists scope but not gaps, misleading a reader into believing everything was checked | high | mitigate | `### Gaps` is a required sub-section asserted by Task 3's `check` group; its four paragraphs are pinned by named literals in Task 2's acceptance criteria |
| T-68-14 | Tampering | the two preamble copies drifting apart by hand edit | medium | mitigate | one `renderCoveragePreamble` producer, two emissions; `check` compares both copies against the live render, not merely against each other |
| T-68-15 | Spoofing | test or build numbers taken from a source other than the pinned baseline | medium | mitigate | the action names `INVENTORY.md` §"Test & Build Baseline (D-05, D-06)" as the sole authority and forbids restating suite numbers from elsewhere |
| T-68-16 | Repudiation | describing a review-verified IntelliJ fix as verified without qualification | high | mitigate | the prohibition is authored into `must_haves.prohibitions`, and Task 2's acceptance criteria require every occurrence in that paragraph to be qualified |
| T-68-02 | Information disclosure | preamble prose reaching public issues in Phase 69 | medium | mitigate | `check`'s credential-shaped-literal scan runs over both documents after every regeneration |
| T-68-SC | Tampering | npm/pip/cargo installs | low | accept | no package-manager install task in this plan |
</threat_model>

<verification>
- `node derive-review-docs.mjs check` exits 0 with the DOC-03 assertion group reported.
- `diff` of the extracted `## Coverage` sections from the two documents is empty.
- `git status --porcelain .planning/reviews/` still lists exactly two entries; `INVENTORY.md` and `REQUIREMENTS.md` are unmodified.
</verification>

<success_criteria>
- Both documents open with the same `## Coverage` block, `### Scope` then `### Gaps`.
- The scope half states units, dimensions, grid totals, evidence tiers and every named exclusion with its reason, citing INVENTORY by section rather than restating it.
- The gap half states the JDK-17 gap, the 11 interop test failures, the 24 not-reproducible claims and Phase 65's shape, each with its cause.
- A hand edit to either copy fails the standing gate.
</success_criteria>

<output>
Create `.planning/phases/68-deliverable-documents/68-04-SUMMARY.md` when done, recording: the preamble's
final length in lines, the identity-check result, and any INVENTORY or REQUIREMENTS fact the preamble
had to omit for length (reported, so the omission is a recorded choice rather than an accident).
</output>
