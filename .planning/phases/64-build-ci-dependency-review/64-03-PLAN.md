---
phase: 64-build-ci-dependency-review
plan: 03
type: execute
wave: 3
depends_on: [64-02]
files_modified:
  - .planning/reviews/64-COVERAGE.md
autonomous: true
requirements: [RVW-05, SEC-08]

estimate:
  tokens: 175000
  raw_tokens: 175000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-64-02` (build, packaging & dependency manifests, 14 files / 9,208 lines, INVENTORY risk rank 3 — the largest unit by LOC in the whole inventory) has all 7 live unit-row dimensions recorded in `64-COVERAGE.md`, plus the `bbj-vscode/package-lock.json` file-exception row's single live D6 cell and its 7 verbatim `n/a` carry-forwards under `R-LOCKFILE`"
    - "`### SEC-08 Dependency Triage` exists under `## RU-64-02` and holds the ROADMAP criterion-3 table: **every** npm and Gradle dependency with a known vulnerability, enumerated and triaged as `fix-now`, `file-issue` or `accepted-with-reason`, each row carrying a resolvable advisory reference, a `classification:` per D-09's mapping, and the finding ID that records it (D-06, D-09)"
    - "The npm half is enumerated with a **live `npm audit` run whose output and run date are pinned into the file**, because `npm audit` is a live query whose result changes as advisories are published — a later reader must be able to tell what was true on the day of the sweep rather than re-run it and get a different number (D-07)"
    - "The Gradle half is enumerated **statically** — every version coordinate declared in `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties` and `gradle/wrapper/gradle-wrapper.properties`, plus `java-interop/build.gradle` and `settings.gradle` read once as an additional dependency-tree source — and the fact that transitive Gradle dependencies are **not enumerable in this environment** is recorded as a stated coverage limitation inside the D6 cell with the failing command and its literal output, not left implicit (D-10)"
    - "`P63-D6-002` — the `bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch at `bbj-intellij/build.gradle.kts:12-13`, the phase's single inherited item — is re-triaged under `### Inherited item triage` with a written disposition (promoted / dismissed-with-evidence / merged / not-reproducible), noting that fixing it belongs to Phase 66/67 and would retroactively close the D-10 enumeration gap (D-17)"
    - "INVENTORY's stale `node_modules/` exclusion note — which states 0 packages present in this checkout while the tree now holds a populated `node_modules/` — is recorded as a **D8 finding against INVENTORY.md** on `RU-64-02`'s row: the exclusion still stands, but the stated reason no longer describes the tree, and INVENTORY is immutable so drift is recorded rather than corrected (D-08)"
    - "Every D6 finding carries **both** `triage:` and `classification:` with D-09's stated mapping, and every `accepted-with-reason` carries a written reachability argument naming the code path that would have to exist for the vulnerability to matter and showing it does not — a bare dev-dependency or not-shipped claim is not an accepted reason under RVW-06 (D-09)"
    - "`## Phase 64 Close-Out` carries all seven lettered sections **plus `### Milestone coverage position (last sweep phase)`**, and **both D-18 gates are re-run live with their literal outputs recorded**: the cell gate re-derives `7 27 29 56` from INVENTORY with the **leading row count of 7** proving the four file-exception rows were counted, and the file gate enumerates **28** files from the live tree with `.github/dependabot.yml`'s adoption named at the point of the count and every basename confirmed present in the coverage file (D-18, D-19)"
    - "The close-out states the milestone-level coverage position, re-derived live across `61`/`62`/`63`/`64-COVERAGE.md` rather than restated: 147 of INVENTORY's 148 `applies` cells are now recorded and 77 of its 84 `n/a` cells carried forward, and **`RU-D8-01`/D8 is the sole remaining unrecorded row** — the input Phase 68's DOC-03 full-coverage claim consumes directly (D-18)"
    - "No `P64-*` finding is located in `CLAUDE.md`, `bbj-vscode/VERBs.md` or anything under `documentation/`, and the close-out states plainly that `RU-D8-01` is not Phase 64's rather than leaving a reader to assume the phase forgot it (D-18)"
    - "Every recorded finding carries `path:line`, a primary dimension, evidence clearing its tier, and a non-blank `dedup:` checked against the frozen 15 open issues; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06, RVW-07)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P64-D{dimension}-{seq}` continue the monotonic per-`(64, dimension)` sequence plans `64-01` and `64-02` opened — they do not restart at 001 — so the phase-wide ID set stays collision-free and ordered by discovery (INVENTORY 3a)"
    - "All three plans append to the single `.planning/reviews/64-COVERAGE.md`; concurrency is excluded structurally by the dependency graph (wave 3, `depends_on: [64-02]`), not by an assumption about the executor (D-04). This plan is the only one authorized to write outside a unit section, and only into the close-out and the ledger's disposition column"
    - "Boundary case: `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` (43,583 bytes) exists in the tree and appears in **no** INVENTORY file list — it is recorded as a D8 drift finding and its integrity is assessed through the two files INVENTORY **does** assign (`gradlew` and `gradle-wrapper.properties`), but it is **not** adopted as a 29th file, because D-18/D-19 pin the file gate at 28; the non-adoption is stated as a deliberate documented decision rather than left as a miscount"
    - "The SEC-08 triage states the **empty case**: what `npm audit` reports for a dependency tree with no vulnerable package, and what the recorded totals mean if a later re-run returns a different number — which is precisely why the run date is pinned"
    - "The SEC-08 triage states the **ordering** fact that decides what is reachable: which of the flagged packages are production dependencies of the shipped extension versus dev-only, established from `package.json` and the lockfile's own dependency paths rather than asserted, since that distinction is the input to every `accepted-with-reason` reachability argument"
    - "The D6 cell states what the Gradle wrapper does on a **cold or tampered** distribution fetch — the `distributionUrl` scheme, whether `validateDistributionUrl` is set, and whether any `distributionSha256Sum` pins the downloaded distribution — because that is the one Gradle-side integrity question answerable without a working build"
    - "The D5 cell states the **adjacency** case: where `vitest.config.ts`, `tsconfig.test.json` and `package.json`'s scripts disagree about which files constitute the suite, the record names the disagreement rather than reporting whichever source is most convenient"

  artifacts:
    - ".planning/reviews/64-COVERAGE.md §`## RU-64-02 — Build, packaging & dependency manifests` — 7 live unit-row cells recorded, 1 `n/a` unit-row cell carried forward verbatim, 8 `### File-exception cells` lines for the lockfile row (1 verdict, 7 `n/a`), plus `### SEC-08 Dependency Triage`, `### Inherited item triage`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals` and `### Unit closure`"
    - ".planning/reviews/64-COVERAGE.md §`## Phase 64 Close-Out` — sections A-G filled (file gate, cell-total gate, finding accounting, inherited-item accounting, scope-fidelity note, ROADMAP success criteria, closing confirmations) plus `### Milestone coverage position (last sweep phase)`, and the header's `## Inherited item ledger (D-17)` disposition column resolved"

  key_links:
    - "`### SEC-08 Dependency Triage` -> ROADMAP Phase 64 criterion 3, answered in the close-out; SEC-08 closes with this phase and does not flow to Phase 65 as open work"
    - "`triage: fix-now` -> Phase 67's apply path as `classification: easy`; `triage: file-issue` -> Phase 68's `MAJOR-REFACTORS.md` and Phase 69's issue drafting (D-09)"
    - "`P63-D6-002`'s disposition -> Phase 66's DEBT re-triage if it names a `DEBT-*` requirement, and Phase 67 if it classifies `easy`"
    - "`### Milestone coverage position (last sweep phase)` -> Phase 68's DOC-03 full-coverage statement, which reads the 147-of-148 arithmetic and the `RU-D8-01` remainder rather than re-deriving scope"
    - "The `## Phase 64 Close-Out` gates -> Phase 68's DOC-03 concatenation of all four `{NN}-COVERAGE.md` files against INVENTORY's 232-cell grid"

  prohibitions:
    - "MUST NOT run any command that mutates the working tree — no dependency install, no lockfile regeneration, no automated vulnerability remediation, no Gradle build. Phase 64 records; Phase 67 is the only phase that applies fixes. Enforced by an acceptance criterion asserting `git status --porcelain` is empty across every reviewed tree, which a remediation command would break by rewriting `package.json` or `package-lock.json`"
    - "MUST NOT assert a dependency-vulnerability claim without a resolvable advisory reference — D6's `inherited` tier resolves to repro-equivalent for a CVE claim, so a bare version-looks-old assertion is not a finding (INVENTORY 3b, RVW-06)"
    - "MUST NOT present the Gradle half as fully enumerated — transitive Gradle dependencies are not reachable in this environment, and the D6 cell states that limitation with the failing command and its literal output rather than hiding it behind a confident-sounding cell (D-10)"
    - "MUST NOT record `triage: accepted-with-reason` on the strength of a bare dev-dependency or not-shipped claim — a written reachability argument naming the code path that would have to exist is required (D-09)"
    - "MUST NOT repurpose or replace the `classification:` field with `triage:` — `classification:` drives Phases 67 and 68 milestone-wide and both fields are carried on every D6 finding with D-09's mapping stated (D-09)"
    - "MUST NOT declare either completion gate satisfied by restating a number from `64-CONTEXT.md` or from the skeleton — both are re-run at execution time and their literal outputs recorded; a stated total that was never re-derived is the false-assurance failure the gates exist to catch (D-18)"
    - "MUST NOT adopt a re-derived number that disagrees with the stated totals — a disagreement between INVENTORY's re-derivation and this file's own content is itself a defect to surface in the close-out, not a number to quietly adopt (D-18)"
    - "MUST NOT let the cell gate's re-derivation report fewer than 7 rows — a 3-row result means the four file-exception rows were silently dropped, which is the specific failure mode D-17 names as this phase's most likely (D-18.1)"
    - "MUST NOT adopt `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` as a 29th reviewed file — D-18/D-19 pin the file gate at 28; the drift is recorded as a D8 finding and the non-adoption stated as a deliberate documented decision, so a later reader sees a considered boundary rather than an omission"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md`, `61-COVERAGE.md`, `62-COVERAGE.md` or `63-COVERAGE.md` — INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and all three prior sweeps are closed and verified; every drift claim is recorded as a D8 finding whose `location:` points into the stale document"
    - "MUST NOT record a finding whose `location:` is `CLAUDE.md`, `bbj-vscode/VERBs.md` or anything under `documentation/` — that is `RU-D8-01`'s surface, owned by no sweep phase, and the close-out says so out loud (D-18)"
    - "MUST NOT record a finding whose `location:` is inside `bbj-vscode/src/` or `bbj-intellij/src/`, and MUST NOT add any `java-interop/` source file to this unit's file list — `java-interop/build.gradle` and `settings.gradle` are read **once** as an additional dependency-tree source only, per INVENTORY's own carve-out"
    - "MUST NOT re-record the 11 known-failing `linking.test.ts` tests or the 3 disabled `parser.test.ts` assertions — `RU-61-06` and DEBT-02 own them respectively; cross-reference by ID and add only what is new here (D-15)"
    - "MUST NOT issue a `P64-D7-*` finding ID or mark any D7 cell live — `R-D7-CI` retires the dimension for all 7 rows (D-14)"
    - "MUST NOT publish a working exploitation path against a shipped artifact — D-16's two-tier rule applies to any SEC-08 finding that would amount to one; open such an `evidence:` field with the exact phrase `Disclosure-limited per D-16`"
    - "MUST NOT add a grid row, a cell line or a unit section beyond the 56 cells INVENTORY's Phase 64 slice defines, and MUST NOT reword or merge any carried-forward `n/a` marker"
    - "MUST NOT reword or overwrite anything plans `64-01` and `64-02` recorded — this plan fills `## RU-64-02`, the close-out, and the ledger's disposition column, and nothing else"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier, MUST NOT leave a `dedup:` field blank, MUST NOT leave a `triage:` field off any D6 finding, and MUST NOT allocate an ID in the reserved `P00-*` namespace"
    - "MUST NOT open, comment on or file any GitHub issue, and MUST NOT draft one into this file — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-64-02` — Build, packaging & dependency manifests** (`package.json` 694,
`package-lock.json` 7,894, `esbuild.mjs` 28, `eslint.config.js` 18, `langium-config.json` 22,
`tsconfig.json` 25, `tsconfig.test.json` 13, `vitest.config.ts` 30, `build.gradle.kts` 135,
`settings.gradle.kts` 5, `gradle.properties` 1, `gradlew` 244, `gradlew.bat` 92,
`gradle/wrapper/gradle-wrapper.properties` 7 — 14 files, 9,208 lines, INVENTORY risk rank 3 of 3)
across its 7 live unit-row dimensions plus the `package-lock.json` file-exception row, and then
**close Phase 64**: fill `## Phase 64 Close-Out` sections A-G, write
`### Milestone coverage position (last sweep phase)`, and re-run both D-18 completion gates live.

Purpose: this unit carries the npm and Gradle halves of SEC-08 — the ROADMAP criterion-3 obligation
to enumerate and triage every vulnerable dependency in both trees — which is why D-03 gives that
work its own task and its own commit rather than folding it into the repro-tier sweep. It also
carries the phase's single inherited item (`P63-D6-002`) and the second of the phase's two
INVENTORY-drift records (D-08's stale `node_modules/` note). And because **Phase 64 is the last
sweep phase**, its close-out is the one that states the milestone-level coverage position: a cell
missed here is a permanent hole in the milestone's coverage statement, because there is no fifth
sweep phase to catch it.

Output: 8 recorded cells with written check lines, the criterion-3 triage table with its pinned
audit run, a dispositioned inherited item, a complete eight-section close-out with both gates re-run
live, and the milestone coverage position Phase 68 consumes. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md
@.planning/phases/64-build-ci-dependency-review/64-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 64 creates **no code symbol and modifies no source file**. `bbj-vscode/`, `bbj-intellij/`,
`.github/` and `java-interop/` are read-only review subject matter.

**Modified by this plan**

- `.planning/reviews/64-COVERAGE.md` — section `## RU-64-02 — Build, packaging & dependency
  manifests` (Tasks 1-3), and `## Phase 64 Close-Out` plus the header's
  `## Inherited item ledger (D-17)` disposition column (Task 4). **Task 4 is the one task in the
  phase authorized to write outside a unit section**: it is the closing task, and the close-out and
  the ledger's disposition column are precisely its scope. It rewords nothing already recorded by
  plans `64-01` and `64-02` and does not touch their unit sections.

**Named subsection anchors created here** (Phase 68 consumes them by name, so the literal headings
matter — D-06, D-18):

- `### SEC-08 Dependency Triage` — under `## RU-64-02`, Task 2. Holds the ROADMAP criterion-3
  triage table.
- `### Inherited item triage` — under `## RU-64-02`, Task 2. Holds `P63-D6-002`'s disposition.
- `### Milestone coverage position (last sweep phase)` — in the close-out, Task 4. New to Phase 64
  because Phase 64 is the last sweep phase.

**Cell accounting for this plan.** `RU-64-02`'s unit row is 7 `applies` + 1 `n/a` (D7 under
`R-D7-CI`); the `bbj-vscode/package-lock.json` file-exception row is 1 `applies` (D6) + 7 `n/a`
(under `R-LOCKFILE`). Task 1 records 3 verdicts (phase-wide 19 → 22), Task 2 records 2 (22 → 24),
Task 3 records 3 (24 → **27**, zero placeholders). Task 4 records no cell and asserts 27 / 0 / 29 /
56.

**Three-way task split (D-03).** Phases 61-63 used a two-task evidence-tier split per plan, and
plans `64-01` and `64-02` follow it. `64-03` splits three ways because `RU-64-02`/D6 is the single
largest discrete unit of work in this phase — the entire npm tree, the entire Gradle tree, and the
criterion-3 triage — so it gets its own task and its own commit. The close-out is the fourth task.

<!-- The literals below are section headings and one required marker phrase the executor must write
     verbatim into the coverage file; every one is asserted POSITIVELY by the acceptance criteria,
     never negatively, so their appearance in the action text is intended rather than
     self-invalidating. -->
<!-- planner-discipline-allow: Disclosure-limited per D-16 -->
<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### File-exception cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Unit closure -->
<!-- planner-discipline-allow: ### SEC-08 Dependency Triage -->
<!-- planner-discipline-allow: ### Inherited item triage -->
<!-- planner-discipline-allow: ### Milestone coverage position (last sweep phase) -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-64-02 at evidence tier `repro` — D1, D2, D3 — over the 14 build, packaging and Gradle manifests</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>`.planning/reviews/64-COVERAGE.md` exists with `## RU-64-03` and `## RU-64-01` complete —
  the phase-wide verdict count is exactly 19 and the placeholder count is exactly 8 — and plan `64-02` has
  committed. Halt and report otherwise; this plan appends into a frozen skeleton and does not create
  one.</precondition>
  <read_first>
    - `.planning/reviews/64-COVERAGE.md` — §header, §`## Applicability Grid — Phase 64 slice`
      including its `### File-exception rows` sub-table, both gate sections,
      §`## Stopping Rule & Write Contract`, §`## Exclusion reasons carried forward`,
      §`## Inherited item ledger (D-17)`, and the stubbed `## RU-64-02` section this task fills.
      Read `## RU-64-03`'s `### Cells` block as the check-line register to match; do **not** re-read
      the rest of the completed unit sections
    - `.planning/reviews/INVENTORY.md` §"Finding Standard" 3a-3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Phase 64 review units …" → the `RU-64-02` block with its
      14-file table and its coverage-denominator note, §"Applicability Grid" → the `RU-64-02` row,
      §"File-exception rows" → the `bbj-vscode/package-lock.json` row, §"Exclusion reasons" →
      **R-LOCKFILE** and **R-D7-CI**, §"Surface Accounting" → the `bbj-vscode/` and `bbj-intellij/`
      breakdowns and the `java-interop/` and `node_modules/` exclusion rows
    - `.planning/REQUIREMENTS.md` lines 14-23 — the D1-D8 table; also RVW-05, RVW-06, RVW-07
    - `.planning/ROADMAP.md` §"Phase 64: Build, CI & Dependency Review" — the four success criteria
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — read in full; D-10, D-15 and
      D-18 govern this plan
    - **Review targets, read in full:** `bbj-vscode/package.json` (694),
      `bbj-vscode/esbuild.mjs` (28), `bbj-vscode/eslint.config.js` (18),
      `bbj-vscode/langium-config.json` (22), `bbj-vscode/tsconfig.json` (25),
      `bbj-vscode/tsconfig.test.json` (13), `bbj-vscode/vitest.config.ts` (30),
      `bbj-intellij/build.gradle.kts` (135), `bbj-intellij/settings.gradle.kts` (5),
      `bbj-intellij/gradle.properties` (1), `bbj-intellij/gradlew` (244),
      `bbj-intellij/gradlew.bat` (92), `bbj-intellij/gradle/wrapper/gradle-wrapper.properties` (7)
    - **`bbj-vscode/package-lock.json` is NOT read as source** — per INVENTORY's
      coverage-denominator convention it is in scope for D6 only, as the dependency-tree source
      Task 2 audits. Query it rather than reading it end to end
    - **Enumerations, run rather than assumed** — record each command and its literal output: the
      full contents of `bbj-intellij/gradle/wrapper/`; every lifecycle script in `package.json`
      (`preinstall`, `postinstall`, `prepare`, `prepublish` and friends); every `repositories`
      declaration and every URL in the Gradle files; and `sha256sum` on
      `bbj-intellij/gradle/wrapper/gradle-wrapper.jar`
    - **Do NOT read** any file under `.planning/codebase/`, and do **not** run any Gradle task,
      dependency install, lockfile regeneration or automated remediation
  </read_first>
  <action>
Record `RU-64-02`'s D1, D2 and D3 cell lines, replacing the placeholder on each with `pass` or
`fail` plus a written line naming the concrete checks applied. Enumerate the dimensions explicitly
so none is absorbed by whichever is loudest: this task covers **D1 Security, D2 Correctness & error
handling, and D3 Performance & resource use** — and no others. D6 is Task 2's whole subject; D4, D5
and D8 are Task 3's; D7 is `n/a` here and everywhere in this phase.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
input, secret exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks:
**(a) lifecycle scripts** — enumerate every npm lifecycle hook declared in `package.json` and say
what each executes, because a lifecycle hook runs on every contributor's and CI runner's machine at
install time; **(b) the Gradle wrapper chain** — `gradlew`/`gradlew.bat` bootstrap a distribution
named by `gradle/wrapper/gradle-wrapper.properties`, so record the `distributionUrl` scheme and
host, whether `validateDistributionUrl` is set, and **whether any `distributionSha256Sum` pins the
downloaded distribution**; this is the one Gradle-side integrity question answerable without a
working build, and its answer is a fact to state either way; **(c) the wrapper JAR** — the
bootstrap executes `gradle/wrapper/gradle-wrapper.jar`, a 43,583-byte binary committed to this
repository. Record its `sha256sum` and byte size as evidence and assess the integrity chain through
the two files INVENTORY **does** assign to this unit (`gradlew` and `gradle-wrapper.properties`).
**Do not adopt the JAR as a 29th reviewed file** — D-18/D-19 pin the file gate at 28 and the
adoption question is not this plan's to decide; the fact that it appears in no INVENTORY file list
is a D8 drift record Task 3 writes, and the non-adoption is stated as a deliberate documented
decision in the close-out; **(d) build and bundling configuration** — what `esbuild.mjs` marks
external, what it inlines, whether any value from the environment reaches the bundle, and what
`langium-config.json` points at; **(e) declared repositories and plugin sources** in the Gradle
files — every host, every scheme, and whether any resolves over plaintext; and **(f) secrets and
paths** — whether any manifest declares a credential, a token, an absolute developer-machine path,
or a value that is later executed. Record concrete findings where evidence clears the tier; a bare
assertion is not a finding.

*D2 Correctness & error handling* (tier `repro`). Assess against wrong edge-case behaviour, swallowed
failures, inconsistent configuration and resource leaks — read into configuration terms. The
concrete checks: whether `tsconfig.json` and `tsconfig.test.json` agree on the settings that matter
(target, module resolution, strictness, include/exclude), and where they diverge, whether the
divergence is deliberate or accidental; whether `package.json`'s scripts do what their names claim
and whether any of them can report success while its underlying step failed — a pipeline whose exit
code is masked, a chained command where only the last status survives; whether `esbuild.mjs` handles
its own build failure by exiting non-zero; whether `eslint.config.js` and `langium-config.json`
point at paths that exist in this checkout; whether `build.gradle.kts`'s declared
`sourceCompatibility`/`targetCompatibility` are consistent with everything else the build assumes
(this is the file `P63-D6-002` is anchored in — record correctness observations here and leave the
toolchain-health triage to Task 2 so the two are not double-counted); and what each of `gradlew`
and `gradlew.bat` does when the wrapper properties file is missing or malformed. Each claim needs a
line-by-line trace naming the concrete input or state and the exact `file:line` where behaviour
diverges.

*D3 Performance & resource use* (tier `repro`). Assess against redundant work, missing caches and
unbounded growth, in build terms. The concrete checks: whether the build performs the same
compilation twice — `tsc` plus `esbuild` over the same sources — and whether that is necessary or
redundant; whether `vitest.config.ts` declares include/exclude globs narrow enough that the suite
does not walk the whole tree, and whether coverage collection is scoped; whether the esbuild
configuration produces a sourcemap and a bundle size proportionate to what ships; and whether any
Gradle configuration forces a full re-resolve or disables caching. Where a cost is latent rather
than active, say so and do not promote it on volume alone.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order. `unit:` is `RU-64-02`. `evidence:` states in one clause why it carries no runnable
reproduction where none is possible — the Gradle build does not run in this environment and no
command in this phase mutates the tree. `classification:` records pass/FAIL for all six INVENTORY
3c tests inline; test (6) independently forces `major` for any `critical`/`high` severity and for
any D1 primary dimension. `dedup:` is never blank and is checked against the frozen 15. Any claim
that does not clear its tier goes under `### Not-reproducible dispositions` with the tier it failed
and why.
  </action>
  <acceptance_criteria>
    - The three `repro`-tier unit-row cells are recorded: with the section extracted by `sed -n '/^## RU-64-02 /,/^## Phase 64 Close-Out/p' .planning/reviews/64-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — ` and `^- D3 .* — (pass|fail) — ` each print `1`
    - D7 is untouched and the lockfile file-exception row is present and still stubbed for D6: within the extracted section, `grep -cE '^- D7 .* — n/a — '` prints `1`, `grep -cE '^- \[file-exception\] '` prints `8`, and `grep -cE '^- \[file-exception\] .* — n/a — '` prints `7`
    - The Gradle wrapper chain was assessed with the wrapper JAR named as evidence: the extracted section contains `distributionUrl`, `gradle-wrapper.jar` and `sha256`
    - The wrapper JAR was not adopted as a reviewed file: the file gate still enumerates 28 — `ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties | wc -l` prints `28`
    - Phase-wide counts prove nothing else was written: `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `22` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `5`
    - The grid is unchanged in size and all 29 `n/a` carry-forwards are intact: `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of the same pattern with `.{120,}` appended, and the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no `P64-D7-` ID, no blank `dedup:`, every required field count equals the `^id:` count, and the `^triage:` count equals the `^dimension:[[:space:]]+D6` count
    - No finding is anchored in a tree owned by a closed phase or in the unowned documentation unit: the counts of `^location:[[:space:]]+bbj-vscode/src/`, `^location:[[:space:]]+bbj-intellij/src/`, `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; sed -n "/^## RU-64-02 /,/^## Phase 64 Close-Out/p" "$f" > /tmp/p64ru02.txt; for d in 1 2 3; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p64ru02.txt)" = 1; done; test "$(grep -cE "^- D7 .* — n/a — " /tmp/p64ru02.txt)" = 1; test "$(grep -cE "^- \[file-exception\] " /tmp/p64ru02.txt)" = 8; test "$(grep -cE "^- \[file-exception\] .* — n/a — " /tmp/p64ru02.txt)" = 7; for s in "distributionUrl" "gradle-wrapper.jar" "sha256"; do grep -qF "$s" /tmp/p64ru02.txt; done; test "$(ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties | wc -l)" = 28; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 22; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 5; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "bbj-vscode/src/" "bbj-intellij/src/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_03_RU02_REPRO_OK'</automated>
  </verify>
  <done>`RU-64-02`'s D1, D2 and D3 cells each carry `pass` or `fail` plus a written check line naming the
  concrete checks applied, including the npm lifecycle-hook enumeration, the Gradle wrapper integrity
  chain with the `distributionUrl`/`distributionSha256Sum` question answered either way and the wrapper
  JAR's hash recorded as evidence without adopting it as a 29th file, the declared repository hosts and
  schemes, and the tsconfig/script consistency traces — with the toolchain-health triage deliberately
  left to Task 2 so it is not double-counted.</done>
</task>

<task type="auto">
  <name>Task 2: The SEC-08 dependency audit — D6 for RU-64-02 and for the package-lock.json file-exception row — with the criterion-3 triage table and the P63-D6-002 re-triage</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>`## RU-64-02`'s D1, D2 and D3 cells already carry verdicts — Task 1 committed — the
  phase-wide verdict count is exactly 22 with 5 placeholders remaining, and `bbj-vscode/node_modules/`
  is populated (`ls bbj-vscode/node_modules | wc -l` prints a non-zero count) so `npm audit` can resolve
  the installed tree. If `node_modules/` is empty or the npm registry is unreachable, do **not** install
  anything: halt and report, because the alternative — a lockfile-only hand audit presented as an
  enumeration — would misrepresent this cell's coverage.</precondition>
  <reversibility rating="costly">D-09's `triage:` vocabulary (`fix-now` | `file-issue` |
  `accepted-with-reason`) is first written at scale here and is read by Phase 68's
  `MAJOR-REFACTORS.md` generation and Phase 69's issue drafting; changing the vocabulary after
  `64-COVERAGE.md` is written means re-triaging every D6 finding by hand. The vocabulary and its
  mapping to `classification:` are fixed by D-09 and are not to be re-derived here.</reversibility>
  <read_first>
    - `.planning/reviews/64-COVERAGE.md` §`## RU-64-02` — the cells Task 1 recorded and the stubbed
      `### SEC-08 Dependency Triage`, `### Inherited item triage` and `### File-exception cells`
      blocks this task fills; also §`## Inherited item ledger (D-17)` in the header
    - `.planning/reviews/INVENTORY.md` §3b (D6 is tier `inherited`, and a CVE claim needs the
      advisory reference — repro-equivalent), §3c, §3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Exclusion reasons" → **R-LOCKFILE**, §"Surface Accounting" →
      the `node_modules/` exclusion row (its parenthetical is Task 3's D8 target, noted here but not
      recorded here) and the `java-interop/` row with its read-once-as-a-dependency-tree-source
      carve-out
    - `.planning/REQUIREMENTS.md` — **SEC-08** verbatim, plus D6's row and RVW-06/RVW-07
    - `.planning/ROADMAP.md` §"Phase 64" **criterion 3** — the enumerate-and-triage obligation this
      task discharges
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — D-07, D-08, D-09, D-10 and
      D-17 are the decisions this task turns on
    - `.planning/reviews/63-COVERAGE.md` — **only** the `P63-D6-002` finding record and the
      close-out row that routes it here (locate both by searching for `P63-D6-002`); do not read the
      file broadly
    - `.planning/reviews/64-COVERAGE.md` §`## RU-64-03`'s `### Vendored Binary Provenance` and
      §`## RU-64-01`'s D6 cell — the two SEC-08 contributions already recorded, which this task
      consolidates **by reference** rather than re-auditing
    - **Dependency-tree sources:** `bbj-vscode/package.json` (already read in Task 1) and
      `bbj-vscode/package-lock.json` (queried, not read end to end); `bbj-intellij/build.gradle.kts`,
      `settings.gradle.kts`, `gradle.properties`, `gradle/wrapper/gradle-wrapper.properties`; and
      `java-interop/build.gradle` and `java-interop/settings.gradle`, read **once** as an additional
      dependency-tree source only — this adds no `java-interop/` source file to any unit's file list
    - **Do NOT read** any file under `.planning/codebase/`
  </read_first>
  <action>
Record `RU-64-02`'s D6 cell and the `bbj-vscode/package-lock.json` file-exception row's D6 cell, then
write `### SEC-08 Dependency Triage` and `### Inherited item triage`. This task covers **D6
Dependency health** and no other dimension. It is its own task and its own commit because it is the
single largest discrete unit of work in this phase (D-03).

**The npm half — enumerated live, with the output pinned (D-07).** Run `npm audit` from
`bbj-vscode/` and record the run date, the command, and its summary totals verbatim into the file.
Then run `npm audit --json` and use it as the machine-readable enumeration. **The output must be
pinned with its run date** because `npm audit` is a live query whose result changes as advisories are
published: a later reader must be able to tell what was true on the day of the sweep rather than
re-run it and get a different number. Whether the JSON is embedded verbatim, summarised into a table,
or both is left to judgement, provided the run date and the totals are pinned and every claim traces
to an advisory. For each vulnerable package: name the package, the installed version range, the
advisory identifier and a resolvable reference, the severity the advisory assigns, and the dependency
path that brings it in — direct dependency, direct dev-dependency, or transitive through a named
parent. Establish **from `package.json` and the lockfile's own dependency paths, not by assertion**,
which flagged packages are production dependencies of the shipped extension and which are dev-only;
that distinction is the input to every reachability argument below. Note as a fact what the totals
would mean for a tree with no vulnerable package, so the recorded numbers are interpretable rather
than merely present.

**The Gradle half — enumerated statically, with the gap stated (D-10).** `./gradlew dependencies`
does not run in this environment. Confirm that now rather than trusting it: run
`cd bbj-intellij && ./gradlew --offline -q dependencies`, record the command and its literal failure
output, and state the cause. Then enumerate the Gradle tree **statically**: every version coordinate
declared in `build.gradle.kts` (including the IntelliJ Platform version and the LSP4IJ version that
Phase 63 explicitly left to this cell), `settings.gradle.kts` (including the settings-plugin
version), `gradle.properties`, and `gradle/wrapper/gradle-wrapper.properties` (the Gradle
distribution version), plus the coordinates declared in `java-interop/build.gradle` and
`settings.gradle`, read once as an additional dependency-tree source. Check each against published
advisories by web lookup, citing a resolvable reference per claim — GitHub Advisory, OSV or a vendor
advisory, your choice, provided the citation resolves. **Then state the coverage limitation
explicitly inside the D6 cell**: transitive Gradle dependencies are not enumerable in this
environment, so the Gradle half covers declared coordinates only. The cell still records `pass` or
`fail`, but its text names exactly what was and was not reachable. Cross-reference `RU-64-01`'s D6
finding that `.github/dependabot.yml` declares no `gradle` ecosystem: the two compose into a
materially stronger statement than either half alone — the `bbj-intellij` tree is both unscanned by
tooling and unenumerable by hand here — and say so in one sentence rather than leaving the reader to
join them.

**The lockfile file-exception cell.** `bbj-vscode/package-lock.json` is in scope for **D6 only**
under `R-LOCKFILE`, as the dependency-tree source SEC-08 audits. Its cell records what the lockfile
establishes — its `lockfileVersion`, the total number of resolved packages, and how many of the
audit's flagged packages resolve through it — with each figure produced by a recorded query rather
than by reading 7,894 lines. Its other seven cells were completed verbatim in the skeleton and are
not touched.

**`### SEC-08 Dependency Triage`** — the ROADMAP criterion-3 table. One row per dependency with a
known vulnerability, across **both** trees, with columns: package/coordinate, ecosystem, installed
or declared version, advisory reference, advisory severity, `triage:`, `classification:`, and the
`P64-D6-*` finding ID that records it. **Every vulnerable dependency gets a row** — criterion 3
demands enumeration, not spot-checks. Apply D-09's vocabulary and mapping exactly, without
re-deriving it:

| `triage:` | Meaning | Maps to `classification:` |
|---|---|---|
| `fix-now` | A version bump with no API change, applicable in Phase 67 | `easy` |
| `file-issue` | Needs a breaking upgrade, a replacement, or upstream action | `major` |
| `accepted-with-reason` | Not reachable in this product's usage; reason stated in full | `major` (documented, not filed as a fix) |

`triage:` is a **second, additive field** — it does not replace `classification:`, which drives
Phases 67 and 68 milestone-wide, so every D6 finding carries both. **`accepted-with-reason` requires
a written reachability argument** naming the code path that would have to exist for the
vulnerability to matter and showing it does not; a bare dev-dependency or not-shipped claim is
**not** an accepted reason under RVW-06. Beneath the table, consolidate the phase's other two SEC-08
contributions **by reference, not by re-audit**: `RU-64-03`'s `### Vendored Binary Provenance` (the
three vendored JARs, including the one that cannot be triaged at all) and `RU-64-01`'s D6 records
(action pinning and the dependabot ecosystem gap), so criterion 3's answer is complete in one place
and Phase 68 does not have to assemble it. Any finding here that would amount to a working
exploitation path against a shipped artifact follows D-16's two-tier rule and opens its `evidence:`
with the exact phrase `Disclosure-limited per D-16`.

**`### Inherited item triage`** — the phase's single inherited item. `P63-D6-002` is the
`bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch recorded by Phase 63 at
`bbj-intellij/build.gradle.kts:12-13`, a `location:` Phase 63 flagged as its one deliberate exception
because INVENTORY assigns that file to `RU-64-02` for every dimension. Re-triage it here with a
**written disposition** — promoted (with the `P64-*` ID that carries it forward),
dismissed-with-evidence (with the citation), merged (into a named `P64-*` record), or
not-reproducible (with the reason) — exactly as Phases 62 and 63 dispositioned their inherited
referrals. Note in the disposition that fixing it belongs to Phase 66/67 and that doing so would
retroactively close the D-10 enumeration gap this very cell records, which is worth stating because
it makes one fix close two records. State also that Phase 63 left `RU-64-02`/SEC-08 owning every
other dependency version it did not touch — the IntelliJ Platform version, LSP4IJ, and the Gradle
wrapper version — and that those are covered by this cell's static enumeration rather than by a
separate ledger entry (D-17).

Write each finding under `### Findings` using INVENTORY's 13-field template verbatim, with `triage:`
added immediately after `classification:` on every record in this task. `unit:` is `RU-64-02` for
every record here, including the lockfile row's. Any claim that does not clear its tier — in
particular any Gradle-side claim that cannot be pinned to a resolvable advisory — goes under
`### Not-reproducible dispositions` with the tier it failed and why, visible rather than silently
dropped.
  </action>
  <acceptance_criteria>
    - Both D6 cells are recorded: with the section extracted by `sed -n '/^## RU-64-02 /,/^## Phase 64 Close-Out/p' .planning/reviews/64-COVERAGE.md`, `grep -cE '^- D6 .* — (pass|fail) — '` prints `1` and `grep -cE '^- \[file-exception\] .* · D6 — (pass|fail) — '` prints `1`, while `grep -cE '^- \[file-exception\] .* — n/a — '` still prints `7`
    - The npm audit was run live and pinned: the extracted section contains `npm audit` and contains `vulnerabilities`
    - The Gradle limitation was re-derived rather than asserted: the extracted section contains `gradlew`, `dependencies` and `transitive`
    - The criterion-3 table exists with D-09's full vocabulary: the extracted section contains `### SEC-08 Dependency Triage`, `fix-now`, `file-issue` and `accepted-with-reason`
    - The other two SEC-08 contributions are consolidated by reference: the extracted section contains `Vendored Binary Provenance` and `RU-64-01`
    - The inherited item is dispositioned: the extracted section contains `### Inherited item triage`, `P63-D6-002` and `build.gradle.kts:12-13`, and matches at least one of `promoted`, `dismissed-with-evidence`, `merged` or `not-reproducible`
    - Every D6 finding carries the dual field: phase-wide, the count of `^triage:` equals the count of `^dimension:[[:space:]]+D6`
    - Phase-wide counts prove nothing else was written: `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `24` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `3`
    - The grid is unchanged in size and all 29 `n/a` carry-forwards are intact: `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of the same pattern with `.{120,}` appended, and the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no `P64-D7-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in a tree owned by a closed phase or in the unowned documentation unit: the counts of `^location:[[:space:]]+bbj-vscode/src/`, `^location:[[:space:]]+bbj-intellij/src/`, `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - **The audit did not mutate the tree** — the decisive check that no automated remediation ran: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; sed -n "/^## RU-64-02 /,/^## Phase 64 Close-Out/p" "$f" > /tmp/p64ru02.txt; test "$(grep -cE "^- D6 .* — (pass|fail) — " /tmp/p64ru02.txt)" = 1; test "$(grep -cE "^- \[file-exception\] .* · D6 — (pass|fail) — " /tmp/p64ru02.txt)" = 1; test "$(grep -cE "^- \[file-exception\] .* — n/a — " /tmp/p64ru02.txt)" = 7; for s in "npm audit" "vulnerabilities" "gradlew" "dependencies" "transitive" "### SEC-08 Dependency Triage" "fix-now" "file-issue" "accepted-with-reason" "Vendored Binary Provenance" "RU-64-01" "### Inherited item triage" "P63-D6-002" "build.gradle.kts:12-13"; do grep -qF "$s" /tmp/p64ru02.txt; done; grep -qE "promoted|dismissed-with-evidence|merged|not-reproducible" /tmp/p64ru02.txt; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 24; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 3; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "bbj-vscode/src/" "bbj-intellij/src/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_03_SEC08_OK'</automated>
  </verify>
  <done>SEC-08 is enumerated across both trees: the npm half by a live `npm audit` whose run date, command
  and totals are pinned into the file with every flagged package carrying an advisory reference and a
  production-versus-dev dependency path; the Gradle half by static coordinate enumeration with the
  transitive-tree gap stated and evidenced by the re-run failing command; `### SEC-08 Dependency Triage`
  holds a criterion-3 row for every vulnerable dependency with both `triage:` and `classification:` per
  D-09's mapping and a written reachability argument behind every `accepted-with-reason`; the vendored
  binaries and the action/dependabot records are consolidated by reference; and `P63-D6-002` carries a
  written disposition noting that fixing it would also close the D-10 gap.</done>
</task>

<task type="auto">
  <name>Task 3: Complete RU-64-02 at evidence tier `trace` — D4, D5, D8 — including the INVENTORY node_modules and wrapper-JAR drift records — and close the unit</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>`## RU-64-02`'s D1, D2, D3 and both D6 cells already carry verdicts — Task 2 committed —
  and the phase-wide verdict count is exactly 24 with 3 placeholders remaining. Halt and report
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/64-COVERAGE.md` §`## RU-64-02` — everything Tasks 1 and 2 recorded, plus
      §`## Stopping Rule & Write Contract`
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and
      follows whatever it asserts), §3c, §3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Test & Build Baseline" → `### Headline vitest counts`,
      `### Every failing test`, `### Flaky suite-level failures` and
      `### Every skipped test, grouped by file`, §"Routing table (D-06)" — which sends the test
      failures and the disabled assertions to **Phase 61**, not here — and §"Surface Accounting" →
      the `node_modules/` exclusion row, whose parenthetical about 0 packages present in this
      checkout is this task's D8 target, and the `bbj-intellij/` breakdown, whose file list omits
      `gradle/wrapper/gradle-wrapper.jar`. Record the exact line numbers you cite
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-05, RVW-06, RVW-07
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — D-08 (the stale exclusion
      note), D-15 (what D5 means here and what it must not re-record) and D-18 (`RU-D8-01` is not
      this phase's)
    - `.planning/STATE.md` §"Active Constraints" — the 11 known-failing tests and the 3 disabled
      `parser.test.ts` assertions, both already owned elsewhere
    - **Review targets:** the same 13 readable manifests as Task 1, re-read only where a specific
      claim needs a line anchor
    - **Enumerations, run rather than assumed** — record each command and its literal output:
      `ls bbj-vscode/node_modules | wc -l`; the test-file count the vitest globs actually match
      versus the test files present in the tree; and the full contents of
      `bbj-intellij/gradle/wrapper/`
    - `CLAUDE.md` (repo root) — its Build & Test Commands section names `npm install`,
      `npm run langium:generate`, `npm run build`, `npm run watch`, `npm test`, `npm run test:bbj`,
      `npm run test:watch`, `npm run test:coverage`, `npm run lint` and `./gradlew build`, every one
      of which is checkable against `package.json`'s own scripts. **Read as reference only** — it is
      `RU-D8-01`'s file and no `P64-*` finding may be located in it (D-18)
    - **Do NOT read** any file under `.planning/codebase/`, and do **not** run any Gradle task,
      dependency install or lockfile regeneration
  </read_first>
  <action>
Complete `## RU-64-02` by recording its D4, D5 and D8 cell lines, then close the unit against the
four-part stopping rule. Enumerate the dimensions explicitly: this task covers **D4 Maintainability
& code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no others. Touch no
other unit section; the close-out is Task 4's.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, dead configuration,
tangled coupling, inconsistent patterns and missing abstractions. The concrete checks, with a
mechanical basis wherever one exists: `package.json` is 694 lines — establish by a recorded count
where its bulk sits (contributed commands, configuration blocks, dependency lists, scripts) and say
whether any block has grown past the point where a reader can find things in it; whether the script
list contains dead or duplicated entries, or entries that differ only in a flag and could be
parameterised; whether `tsconfig.json` and `tsconfig.test.json` duplicate settings that one could
inherit from the other; whether `eslint.config.js` at 18 lines actually covers the tree the project
lints; whether the two Gradle build files and `gradle.properties` agree on a single convention or
each carry a slice of the same concern; and whether `gradlew`/`gradlew.bat` are the stock generated
wrapper scripts or have been hand-edited — a recorded diff-shaped check, not an impression, because
a hand-edited wrapper is a maintenance and integrity fact rather than a style one.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **In this phase D5 means something
specific (D-15): not is this config file tested, but does the test configuration this unit owns
accurately describe and actually run the suite it claims to run.** The concrete checks: what
`vitest.config.ts`'s include/exclude globs actually match, counted against the test files present in
the tree, with both counts recorded — if they disagree, the disagreement is the finding; whether
`tsconfig.test.json` covers the same set those globs match; whether `package.json`'s `test`,
`test:bbj`, `test:watch` and `test:coverage` scripts run what their names claim and whether the
BBj-gated script's environment variable is the one the suite actually reads; and **where the three
sources disagree about what constitutes the suite, name the disagreement rather than reporting
whichever source is most convenient**. **Do not re-record the 11 known-failing `linking.test.ts`
tests or the 3 disabled `parser.test.ts` assertions** — INVENTORY's routing table sends the former
to Phase 61 (`RU-61-06` recorded them) and DEBT-02 owns the latter; where this cell touches them,
cross-reference by finding ID or requirement ID and add only what is new, which here is whether the
**configuration** conceals or surfaces them. State that cross-reference explicitly rather than
leaving the overlap unexplained.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments and claims contradicted by
code. The concrete checks: every comment in the 13 readable manifests against what the configuration
beneath it does; whether `package.json`'s contributed command titles, categories and `when` clauses
describe what those commands actually do; and `CLAUDE.md`'s Build & Test Commands section checked
line by line against `package.json`'s own scripts. **Where a manifest is the stale side, the finding
is located in the manifest. Where `CLAUDE.md` is the stale side, it is recorded as a written
observation naming `RU-D8-01` as the row that owns it and stating that no Phase 64 plan may allocate
a finding there (D-18) — never as a `P64-*` finding located in `CLAUDE.md`.** Then the two
INVENTORY-drift records, both located **in INVENTORY** because Phase 60 D-09 makes it immutable and
drift is recorded rather than corrected:

1. **The stale `node_modules/` exclusion note (D-08).** INVENTORY's Surface Accounting records
   `node_modules/` as excluded with a parenthetical stating 0 packages are present in this checkout.
   That was true when INVENTORY was written; it is not true now — record the live count from the
   enumeration you ran. **The exclusion itself still stands** — Phase 64 does not review installed
   package source, and dependency health is assessed from the manifest and lockfile — but the stated
   reason no longer describes the tree. Record it as a D8 finding with the exact line cited, and say
   in the record that only the reason is stale, not the exclusion, so a later reader does not read
   this as licence to review `node_modules/`. This is exactly the claim-versus-reality drift D8
   exists to catch, and the milestone has a precedent for it in Phase 60's own D-15 correction log.
2. **The unaccounted Gradle wrapper JAR.** `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` (43,583
   bytes) is committed to this repository and appears in **no** INVENTORY file list: the
   `bbj-intellij/` breakdown names `gradle/wrapper/gradle-wrapper.properties` and its five siblings
   but not the JAR that `gradlew` actually executes. Record that as a second D8 drift finding
   against INVENTORY, citing the exact line, with the directory enumeration and the JAR's
   `sha256sum` (recorded in Task 1) as evidence. **State explicitly that Phase 64 does not adopt it
   as a 29th reviewed file** — unlike `.github/dependabot.yml`, which D-19 adopted — because
   D-18/D-19 pin the file gate at 28 and the adoption question belongs to whoever revisits the
   inventory; state instead that its integrity was assessed in Task 1 through the two files
   INVENTORY does assign, so the risk is covered even though the file is not counted. A reader must
   see a deliberate documented boundary here, not an omission.

A D8 fix that changes only comment or document text changes no runtime behaviour, so it may be
`easy` if the other five INVENTORY 3c tests pass; each record states which reading was applied.

**Then close the unit** by writing `### Unit closure`, making the four-part closure visible in the
section text: confirm that all 7 live unit-row cells and the lockfile row's single live cell carry a
verdict plus a written check line; that all 14 files are named at least once inside `## RU-64-02`, in
a check line or a finding `location:`; that every candidate claim raised during the three tasks is
either a finding record or a written `### Not-reproducible dispositions` entry with its reason; and
that the phase's one inherited item was dispositioned in Task 2. State also that D7 is `n/a` under
`R-D7-CI` and that no parity work of any kind was performed, so the absence does not read as an
oversight.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells are recorded and the unit row is fully covered: with the section extracted by `sed -n '/^## RU-64-02 /,/^## Phase 64 Close-Out/p' .planning/reviews/64-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The lockfile row is complete: within the extracted section, `grep -cE '^- \[file-exception\] .* — (pass|fail) — '` prints `1` and `grep -cE '^- \[file-exception\] .* — n/a — '` prints `7`
    - Both INVENTORY drift records exist and point into INVENTORY: `grep -cE '^location:[[:space:]]+\.planning/reviews/INVENTORY\.md:'` prints at least `2`, and the extracted section contains `node_modules` and `gradle-wrapper.jar`
    - The wrapper-JAR non-adoption is stated as a decision: the extracted section contains `28`
    - The already-owned test debt is cross-referenced rather than re-recorded: the extracted section contains `DEBT-02` and contains `RU-61-06`
    - `RU-D8-01`'s boundary is respected and stated: the extracted section contains `RU-D8-01`, and phase-wide the counts of `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - Coverage is file-granular across all 14 files: the extracted section contains each of `package.json`, `package-lock.json`, `esbuild.mjs`, `eslint.config.js`, `langium-config.json`, `tsconfig.json`, `tsconfig.test.json`, `vitest.config.ts`, `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`, `gradlew`, `gradlew.bat` and `gradle-wrapper.properties`
    - The unit is closed with all six sub-blocks present: the extracted section contains `### Unit closure`, `### SEC-08 Dependency Triage`, `### Inherited item triage`, `### File-exception cells`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - **The whole grid is now recorded with nothing outstanding:** `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `27`, `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `0`, `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`, and `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of the same pattern with `.{120,}` appended, and the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no `P64-D7-` ID, no blank `dedup:`, every required field count equals the `^id:` count, and the `^triage:` count equals the `^dimension:[[:space:]]+D6` count
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; sed -n "/^## RU-64-02 /,/^## Phase 64 Close-Out/p" "$f" > /tmp/p64ru02.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p64ru02.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p64ru02.txt)" = 7; test "$(grep -cE "^- \[file-exception\] .* — (pass|fail) — " /tmp/p64ru02.txt)" = 1; test "$(grep -cE "^- \[file-exception\] .* — n/a — " /tmp/p64ru02.txt)" = 7; test "$(grep -cE "^location:[[:space:]]+\.planning/reviews/INVENTORY\.md:" "$f")" -ge 2; for s in "node_modules" "gradle-wrapper.jar" "28" "DEBT-02" "RU-61-06" "RU-D8-01" "package.json" "package-lock.json" "esbuild.mjs" "eslint.config.js" "langium-config.json" "tsconfig.json" "tsconfig.test.json" "vitest.config.ts" "build.gradle.kts" "settings.gradle.kts" "gradle.properties" "gradlew" "gradlew.bat" "gradle-wrapper.properties" "### Unit closure" "### SEC-08 Dependency Triage" "### Inherited item triage" "### File-exception cells" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -qF "$s" /tmp/p64ru02.txt; done; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 27; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 0; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_03_RU02_COMPLETE'</automated>
  </verify>
  <done>`RU-64-02` is complete under the four-part stopping rule and **all 27 live cells in the phase now
  carry a verdict with zero placeholders remaining**: D4, D5 and D8 each carry a written check line, D5
  names where `vitest.config.ts`, `tsconfig.test.json` and `package.json`'s scripts agree or disagree
  about the suite while cross-referencing the already-owned test debt by ID, and D8 records both
  INVENTORY drift claims — the stale `node_modules/` reason with the exclusion itself explicitly still
  standing, and the unaccounted wrapper JAR with its non-adoption stated as a deliberate decision that
  keeps the file gate at 28.</done>
</task>

<task type="auto">
  <name>Task 4: Close Phase 64 — fill the eight close-out sections, re-run both D-18 gates live, and state the milestone coverage position</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>Every live cell in `.planning/reviews/64-COVERAGE.md` carries a verdict — the phase-wide
  verdict count is exactly 27 and the placeholder count is exactly 0 — and Task 3 has committed. Halt and
  report otherwise; the close-out's accounting is derived from the three completed unit sections.</precondition>
  <read_first>
    - `.planning/reviews/64-COVERAGE.md` — **the whole file**, since the close-out's accounting is
      derived from it: the header and its `## Inherited item ledger (D-17)`, the grid and its
      `### File-exception rows` sub-table, both gate sections, the stopping rule, all four exclusion
      blocks, and all three completed `## RU-64-0N` sections including every `### Cells` and
      `### File-exception cells` line, every finding record's `dimension:`, `severity:`,
      `classification:`, `triage:`, `effort:`, `dedup:` and `disposition:` field, every
      `### Not-reproducible dispositions` entry, the `### Inherited item triage` disposition and
      every `### Cross-unit referrals` entry
    - `.planning/reviews/63-COVERAGE.md` `## Phase 63 Close-Out` sections A-G — the shape to mirror,
      including its downstream-inheritance table. Note the structural difference: Phase 63 closed
      with **no** file-exception rows and said so; Phase 64 closes with **four**, so its §A and §B
      must account for them
    - `.planning/reviews/INVENTORY.md` §"Applicability Grid" (the three `RU-64-*` rows and the four
      Phase 64 file-exception rows the cell gate re-derives from), §"File-exception rows",
      §"Grid totals" (the milestone-wide 148 / 84 / 232 figures §`### Milestone coverage position`
      is measured against), §"Routing table (D-06)", §"Recording protocol (D-09)", and the
      `RU-D8-01` block with its `R-D8-SCOPE` row
    - `.planning/ROADMAP.md` §"Phase 64: Build, CI & Dependency Review" — the four success criteria
      answered in §F
    - `.planning/REQUIREMENTS.md` — RVW-05, SEC-07 and SEC-08, all three of which close with this
      phase
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — D-14, D-16, D-17, D-18, D-19
    - **Do NOT read** any file under `.planning/codebase/`, and do **not** run any Gradle task,
      dependency install or lockfile regeneration
  </read_first>
  <action>
Fill `## Phase 64 Close-Out` with seven lettered sections mirroring `63-COVERAGE.md`'s A-G, plus
`### Milestone coverage position (last sweep phase)`, and resolve the header ledger's disposition
column. **Every gate below is re-run at execution time and its literal output recorded — none is
restated from `64-CONTEXT.md` or from the skeleton (D-18).**

**A. File gate (D-18.2).** Enumerate the in-scope files from the tree, not from a list typed into a
plan. Run the enumeration command the skeleton records and capture both the command and its literal
output, which must be **28**. State the composition at the point of the count: 27 files from
INVENTORY's `RU-64-01`..`RU-64-03` lists, plus `.github/dependabot.yml` — **named explicitly as the
D-19 adoption, with its justification restated in one sentence**, so a reader diffing against
INVENTORY sees a deliberate documented addition rather than a miscount. Then, for each of the 28
basenames, confirm it appears somewhere in `64-COVERAGE.md` — run a shell loop over the enumeration
that greps each basename and reports any that is absent, and record the command and its result.
State the unit distribution (7 + 7 + 14 = 28) and confirm that no file is named inside two unit
sections. Finally, name the one file present in the tree that this phase deliberately did **not**
count: `bbj-intellij/gradle/wrapper/gradle-wrapper.jar`, recorded as a D8 drift finding in Task 3
and assessed for integrity through the two assigned files, but not adopted, because D-18/D-19 pin
this gate at 28.

**B. Cell-total gate (D-18.1), three sources.** Build a three-source agreement table: the **stated**
totals (27 `applies`, 29 `n/a`, 56 total across 7 rows), the **re-derivation from INVENTORY** via the
pipeline the skeleton records, with its literal output captured — and state that the pipeline's
**leading row count must be 7, not 3**: a 3-row result means the four file-exception rows were
silently dropped, which is the specific failure mode D-17 names as this phase's most likely — and
this file's **own grep-counted content**: the count of recorded verdicts, the count of `n/a` lines,
and the total cell-line count across both the unit-row and file-exception line shapes. All three
must agree. **If any disagrees, that disagreement is itself a defect to surface in this section, not
a number to quietly adopt** — write it up rather than reconciling silently. Note that Phase 63 closed
with no file-exception rows at all and explicitly recorded that adding one would have broken its
gate; Phase 64 is the mirror image, and omitting one would break this gate.

**C. Finding accounting.** Tables counting every recorded finding by primary dimension (D1-D8), by
`severity`, by `classification` (`easy` versus `major`), by `effort` (2 / 4 / 8) and by
`disposition`. **Then a `triage:` distribution table** — how many `fix-now`, how many `file-issue`,
how many `accepted-with-reason` — with a stated check that every row obeys D-09's mapping
(`fix-now` → `easy`; `file-issue` and `accepted-with-reason` → `major`) and that every
`accepted-with-reason` carries a written reachability argument rather than a bare not-shipped claim.
Then a not-reproducible-by-unit table. Then a dedup-resolution paragraph stating how many findings
resolved to `none`, how many to a duplicate and how many to a partial overlap, naming the issue
numbers touched from the frozen 15 and restating the header's fact about how few of the 15 touch
this phase's subject matter. **Then two explicit assertions**: that **zero** `P64-D7-*` IDs were
allocated, since D7 is `n/a` in all 7 rows (D-14); and that every `critical`/`high` finding in
`## RU-64-01` opens its `evidence:` with the D-16 disclosure marker, with the two counts printed.

**D. Inherited-item accounting.** Three parts, none of them optional. **(1)** The ledger's single
row — `P63-D6-002` — with its disposition from Task 2's `### Inherited item triage`, and **resolve
the header's `## Inherited item ledger (D-17)` disposition column to match**, so the ledger and this
table agree. **(2)** The two Phase 62 body-level deferrals addressed to `RU-64-03`
(`62-COVERAGE.md:1489` and `:1833`), with the dispositions plan `64-01` recorded — stated here so
the accounting is complete even though they are not ledger rows and are not counted by any gate.
**(3)** The three verified negative facts: INVENTORY's routing table has no Phase 64 row,
`61-COVERAGE.md` names no Phase 64 obligation, and `62-COVERAGE.md`'s close-out inheritance table
has no Phase 64 row — each re-confirmed rather than restated. **Zero inherited items may be silently
dropped.**

**E. Scope-fidelity note (D-18).** State plainly that **all 28 files were swept** although ROADMAP
criterion 1 names only a subset, and name the surfaces swept beyond the criterion's wording
explicitly — `.github/dependabot.yml`, the interop test harness, and the eight non-Gradle packaging
manifests. Then the boundaries this phase held, each stated as a fact: no `P64-*` finding is located
inside `bbj-vscode/src/` or `bbj-intellij/src/` (both owned by closed phases); `java-interop/`
contributed only `build.gradle` and `settings.gradle`, read **once** as an additional
dependency-tree source per INVENTORY's own carve-out, adding no `java-interop/` source file to any
unit's list; `node_modules/` was not reviewed as source, only queried as the installed tree behind
`npm audit`, and INVENTORY's exclusion of it still stands even though its stated reason is now
recorded as stale; the three vendored JARs were assessed by manifest and hash with no decompilation
and no execution; and `bbj-intellij/gradle/wrapper/gradle-wrapper.jar` was assessed but deliberately
not adopted as a counted file. **Then the `RU-D8-01` statement, which D-18 requires out loud:**
`RU-D8-01` (`CLAUDE.md`, `bbj-vscode/VERBs.md`, `documentation/`) is scoped by INVENTORY as
cross-cutting and owned by none of Phases 61-64; no `P64-*` finding is located in any of its files;
and it therefore remains **the milestone's one unrecorded row** — stated here rather than leaving a
reader to assume Phase 64 forgot it.

**F. ROADMAP success criteria.** Answer each of the four per-criterion, in Phase 63's shape, citing
where in this file the evidence sits. Criterion 1: all six workflows, the Gradle build,
esbuild/packaging config and the three `tools/*.bbj` scripts have a recorded pass/fail against
D1-D8 — cite §A and §B. Criterion 2: every workflow's secret handling, `GITHUB_TOKEN` permission
scope, third-party action pinning and exposure to untrusted PR-controlled input is documented — cite
`RU-64-01`'s `### SEC-07 Workflow Security Posture` and state that **SEC-07 closes with this phase**,
so nothing on it flows to Phase 65 as open work. Criterion 3: every npm and Gradle dependency with a
known vulnerability is enumerated and triaged — cite `RU-64-02`'s `### SEC-08 Dependency Triage`,
state the pinned `npm audit` run date, **state the Gradle coverage limitation honestly** (declared
coordinates enumerated; transitive dependencies not reachable in this environment, with the failing
command recorded), and state that **SEC-08 closes with this phase**. Criterion 4: every recorded
finding carries `file:line`, a dimension and a verified failure scenario per the Phase 60 standard —
cite §C and state the evidence bases: workflow findings are trace-evidenced and name their
triggering event because Actions cannot be executed here (D-12), and JAR findings rest on manifest,
hash and distribution reasoning because no JAR was decompiled or run (D-11).

**G. Closing confirmations.** Mirror Phase 63's, evidencing each with a command and its output:
ISSUE-01 not triggered (no issue opened, commented on or drafted); `INVENTORY.md` not edited —
`git log --oneline -- .planning/reviews/INVENTORY.md` shows no commit since Phase 60 and
`git status --porcelain` on it is empty, **even though this phase recorded two D8 findings against
it**, which is precisely the immutability-plus-drift pattern D-08 and D-19 prescribe;
`61`/`62`/`63-COVERAGE.md` not reopened or edited; and **no source file modified and no working-tree
mutation of any kind** — `git status --porcelain bbj-vscode bbj-intellij java-interop .github`
returns nothing, which is also the evidence that no automated dependency remediation was run during
the SEC-08 audit, since any such command would have rewritten `package.json` or `package-lock.json`.
Confirm in one line that the swept-tree SHA recorded in the header by plan `64-01` is still the tree
these three sweeps describe. Then a downstream-inheritance table naming what each later phase
consumes: **Phase 65** — nothing from this phase by design, since SEC-01/02/04/05 are cross-cutting
over Phases 61-63's units, with the note that SEC-07 and SEC-08 both close here so Phase 65's scope
carries no CI or dependency component (state also which `RU-64-03` D1 records feed its SEC-04/SEC-05
picture as context rather than as open work); **Phase 66** — every finding whose `dedup:` names a
`DEBT-*` requirement, `P63-D6-002`'s re-triage in particular; **Phase 67** — the
`classification: easy` set and the `triage: fix-now` bucket for its apply path; **Phase 68** — this
whole file for the DOC-03 concatenation, plus the milestone coverage position below; **Phase 69** —
every `dedup:` verdict for issue drafting, gated on ISSUE-01, under D-16's disclosure limits for the
`RU-64-01` records.

**`### Milestone coverage position (last sweep phase)`.** Phase 64 is the last sweep phase, so this
subsection states where the milestone's coverage stands — **re-derived live, not restated**. Loop
over the four coverage files — `61`, `62`, `63` and `64` — and count each one's recorded-verdict cell
lines using the **identical cell-line pattern this file's own gate uses**, covering both the
unit-row and the file-exception line shapes so no earlier phase's file-exception rows are dropped the
way this phase's could be. Repeat the loop twice more, once with the `n/a` cell-line pattern and once
with the bare total-cell-line pattern, and record all three commands with their literal outputs. The
patterns are the ones already written into this file's gate sections and its unit `### Cells` blocks;
do not invent a third shape. Then build a per-phase table — phase, rows in its slice, `applies` recorded, `n/a` carried forward, total cells —
covering `61`, `62`, `63`, `64` and the unrecorded `RU-D8-01` row, and reconcile it against
INVENTORY's own §"Grid totals" figures of **148 `applies`, 84 `n/a`, 232 cells across 29 rows**.
State the arithmetic explicitly: **147 of 148 `applies` cells are now recorded and 77 of 84 `n/a`
cells carried forward — 224 of 232 cells — and the entire remainder is `RU-D8-01`'s single row**
(1 `applies` D8 cell plus 7 `n/a` cells under `R-D8-SCOPE`), which INVENTORY scopes as cross-cutting
and assigns to no sweep phase. If the live re-derivation disagrees with that arithmetic, **surface
the disagreement here as a defect rather than adjusting the claim**. Close with one sentence stating
that Phase 68's DOC-03 full-coverage statement reads this subsection directly and does not need to
re-derive scope.
  </action>
  <acceptance_criteria>
    - All seven close-out sections and the milestone subsection exist: with the close-out extracted by `sed -n '/^## Phase 64 Close-Out/,$p' .planning/reviews/64-COVERAGE.md`, the strings `### A.`, `### B.`, `### C.`, `### D.`, `### E.`, `### F.`, `### G.` and `### Milestone coverage position (last sweep phase)` each appear
    - The file gate was re-run live and agrees with the tree: the close-out contains `28` and contains `.github/dependabot.yml`, and independently the enumeration command still prints `28`
    - Every one of the 28 basenames from the live enumeration appears somewhere in the coverage file — verified by looping the enumeration output and asserting zero absent basenames
    - The cell gate was re-run live and all three sources agree, with the row count part of the gate: the close-out contains the literal `7 27 29 56`, and independently the INVENTORY pipeline still prints `7 27 29 56`
    - The deliberately-uncounted file is named: the close-out contains `gradle-wrapper.jar`
    - The inherited-item accounting is complete: the close-out contains `P63-D6-002`, `62-COVERAGE.md:1489`, `62-COVERAGE.md:1833` and `routing table`, and matches at least one of `promoted`, `dismissed-with-evidence`, `merged` or `not-reproducible`
    - The ledger's disposition column is resolved: `grep -cE '^\| .*P63-D6-002.*\|' .planning/reviews/64-COVERAGE.md` prints at least `1` and `grep -c 'pending' .planning/reviews/64-COVERAGE.md` prints `0`
    - The D7 and disclosure assertions are present: the close-out contains `P64-D7` and contains `Disclosure-limited per D-16`
    - `RU-D8-01` is stated as the milestone's sole remainder: the close-out contains `RU-D8-01` and contains `R-D8-SCOPE`
    - All four ROADMAP criteria are answered and both requirements close: the close-out contains `SEC-07 Workflow Security Posture`, `SEC-08 Dependency Triage`, `criterion 2`, `criterion 3` and `Met`
    - The milestone position was re-derived live and reconciles against INVENTORY: the close-out contains `148`, `232` and `147`, and independently the per-phase verdict loop still prints `50 35 35 27`
    - The whole grid is recorded with nothing outstanding: `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `27`, `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `0`, `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`, and `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of the same pattern with `.{120,}` appended, and the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding records are complete and well-formed phase-wide: IDs unique, no `P00-` ID, no `P64-D7-` ID, no blank `dedup:`, every required field count equals the `^id:` count, and the `^triage:` count equals the `^dimension:[[:space:]]+D6` count
    - No finding is anchored in a tree owned by a closed phase or in the unowned documentation unit: the counts of `^location:[[:space:]]+bbj-vscode/src/`, `^location:[[:space:]]+bbj-intellij/src/`, `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; sed -n "/^## Phase 64 Close-Out/,\$p" "$f" > /tmp/p64co.txt; for s in "### A." "### B." "### C." "### D." "### E." "### F." "### G." "### Milestone coverage position (last sweep phase)" "28" ".github/dependabot.yml" "7 27 29 56" "gradle-wrapper.jar" "P63-D6-002" "62-COVERAGE.md:1489" "62-COVERAGE.md:1833" "routing table" "P64-D7" "Disclosure-limited per D-16" "RU-D8-01" "R-D8-SCOPE" "SEC-07 Workflow Security Posture" "SEC-08 Dependency Triage" "criterion 2" "criterion 3" "Met" "148" "232" "147"; do grep -qF "$s" /tmp/p64co.txt; done; grep -qE "promoted|dismissed-with-evidence|merged|not-reproducible" /tmp/p64co.txt; test "$(ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties | wc -l)" = 28; miss=0; for p in $(ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties); do b=$(basename "$p"); grep -qF "$b" "$f" || { echo "ABSENT: $b"; miss=1; }; done; test "$miss" = 0; test "$(grep -E "^\| .(RU-64-0[1-3]|bbj-vscode/package-lock\.json|tools/formatter/[^|]*). \|" .planning/reviews/INVENTORY.md | grep -E "applies|n/a" | awk "{a+=gsub(/applies/,\"x\"); n+=gsub(/n\/a/,\"y\")} END{print NR, a, n, a+n}")" = "7 27 29 56"; test "$(grep -cE "^\| .*P63-D6-002.*\|" "$f")" -ge 1; test "$(grep -c "pending" "$f")" = 0; agg=""; for c in 61 62 63 64; do agg="$agg$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " .planning/reviews/$c-COVERAGE.md) "; done; test "$agg" = "50 35 35 27 "; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 27; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 0; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "bbj-vscode/src/" "bbj-intellij/src/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_CLOSED'</automated>
  </verify>
  <done>Phase 64 is closed in the record: `## Phase 64 Close-Out` carries all seven lettered sections plus
  `### Milestone coverage position (last sweep phase)`; the file gate was re-run live and enumerates 28
  files with `.github/dependabot.yml` named as the D-19 adoption at the point of the count and every
  basename confirmed present; the cell gate was re-run live and all three sources agree at 7 rows /
  27 / 29 / 56, with the row count proving the four file-exception rows were counted; the finding
  accounting reports the `triage:` distribution against D-09's mapping and asserts zero `P64-D7-*` IDs
  and full D-16 marker coverage; the single inherited item and both Phase 62 body-level deferrals carry
  written dispositions with the ledger column resolved and no placeholder left anywhere in the file; all
  four success criteria are answered with the Gradle coverage limitation stated honestly; `RU-D8-01` is
  named out loud as the milestone's sole unrecorded row; and the milestone coverage position is
  re-derived live at 147 of 148 `applies` cells across the four coverage files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are false-assurance, record-integrity and — uniquely for this plan, which declares the phase
and the milestone's sweep coverage complete — **completion-claim** threats. Rows marked
*subject-matter* are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| npm lifecycle scripts → every contributor and CI runner at install time (*subject-matter*) | A lifecycle hook declared in `package.json` executes on install, before any review of what it does. |
| `gradlew` → `gradle-wrapper.jar` → the distribution named by `distributionUrl` (*subject-matter*) | A three-step bootstrap chain that fetches and executes a Gradle distribution; only one of the three links is a file INVENTORY assigns. |
| 19 flagged npm packages → the shipped extension or the dev toolchain (*subject-matter*) | Whether a vulnerability matters turns entirely on which side of that line the package sits. |
| the `bbj-intellij` Gradle tree → no scanner and no local enumeration (*subject-matter*) | Dependabot declares no `gradle` ecosystem and `./gradlew dependencies` fails on the local toolchain. |
| the reviewed working tree → any command this plan runs | An automated remediation invoked during the audit would rewrite `package.json`/`package-lock.json` and silently change the artifact under review. |
| the close-out's gate outputs → Phase 68's DOC-03 and this phase's own verification | A gate declared satisfied without being re-run is an unfalsifiable completion claim. |
| a re-derived number that disagrees with a stated one → the record | Silently adopting the convenient number hides a real defect. |
| the four file-exception rows → the cell gate's row count | A 3-row re-derivation looks superficially fine and under-counts by 32 cells. |
| `RU-D8-01` → the milestone's coverage statement | If the close-out is silent about it, a reader assumes Phase 64 forgot a row rather than that no phase owns it. |
| `64-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; git history preserves an over-disclosed supply-chain record. |
| recorded `classification:`/`triage:` → Phase 67's apply path and Phase 68's document split | An `easy`/`fix-now` pair routes a dependency change past `MAJOR-REFACTORS.md` review. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-P03-01 | Tampering | an automated dependency remediation run during the SEC-08 audit, mutating the tree under review | high | mitigate | Prohibition plus an acceptance criterion on **every** task asserting `git status --porcelain` is empty across `bbj-vscode`, `bbj-intellij`, `java-interop` and `.github` — any such command would have rewritten `package.json` or `package-lock.json` and broken it. §G records the same evidence in the close-out. |
| T-64-P03-02 | Repudiation | a completion gate declared satisfied without being re-run | high | mitigate | D-18 prohibition plus acceptance criteria that re-run the `ls`-based file gate and the INVENTORY cell-gate pipeline **independently of the file's own text**, and require both the command and its literal output to appear in the close-out. |
| T-64-P03-03 | Repudiation | the four file-exception rows dropped from the cell gate's re-derivation, under-counting by 32 cells | high | mitigate | D-17/D-18.1 prohibition plus the gate's **leading row count** being part of the asserted literal `7 27 29 56`, and separate acceptance counts for unit-row and file-exception cell lines throughout. |
| T-64-P03-04 | Repudiation | adopting a re-derived number that disagrees with the stated totals | high | mitigate | D-18 prohibition: a disagreement is written up in §B or in the milestone subsection as a defect to surface. The independent re-derivations in the verify block make silent adoption detectable. |
| T-64-P03-05 | Repudiation | a dependency-vulnerability claim asserted without a resolvable advisory reference | high | mitigate | INVENTORY 3b's repro-equivalent bar for a CVE claim, enforced by the criterion-3 table's advisory-reference column and by RVW-06's drop-vs-disposition rule for anything that cannot be pinned. |
| T-64-P03-06 | Repudiation | the Gradle half presented as fully enumerated when only declared coordinates are reachable | high | mitigate | D-10 prohibition: the failing `./gradlew --offline -q dependencies` command is re-run and its literal output recorded, and the cell text names exactly what was and was not reachable; §F restates the limitation in criterion 3's answer. |
| T-64-P03-07 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | Prohibition plus acceptance criteria requiring every `pass` cell line phase-wide to be at least 120 bytes of written checks naming concrete anchors. |
| T-64-P03-08 | Elevation of Privilege | `triage: accepted-with-reason` granted on a bare dev-dependency claim, retiring a real vulnerability without argument | high | mitigate | D-09 prohibition requiring a written reachability argument naming the code path, plus §C's stated check that every `accepted-with-reason` carries one and maps to `major`. |
| T-64-P03-09 | Tampering | correcting INVENTORY's stale claims in place rather than recording the drift | high | mitigate | D-08/D-19/Phase 60 D-09 prohibition plus acceptance criteria requiring at least two findings whose `location:` resolves into INVENTORY **and** asserting `git status --porcelain .planning/reviews/INVENTORY.md` is empty. |
| T-64-P03-10 | Repudiation | the inherited item or either Phase 62 body-level deferral silently dropped from the accounting | high | mitigate | D-17 prohibition plus acceptance criteria asserting `P63-D6-002`, both `62-COVERAGE.md` line anchors and a disposition token all appear in §D, and that no placeholder word remains anywhere in the file. |
| T-64-P03-11 | Repudiation | `RU-D8-01` left unmentioned, so a reader reads a deliberate scope boundary as a forgotten row | high | mitigate | D-18 requirement plus acceptance criteria asserting both `RU-D8-01` and `R-D8-SCOPE` appear in the close-out and that the milestone subsection reconciles 147 of 148 against INVENTORY's own totals. |
| T-64-P03-12 | Information Disclosure | a supply-chain or workflow finding written as a working exploitation path and committed to a public repository | high | mitigate | D-16's two-tier rule with the `Disclosure-limited per D-16` marker, and §C asserting the `critical`/`high` count equals the marker count for `RU-64-01`. |
| T-64-P03-13 | Elevation of Privilege | `classification:` on a D1 or D6 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; §C reports the `easy`/`major` and `triage:` splits explicitly so the routing is auditable. |
| T-64-P03-14 | Tampering | adopting the unaccounted `gradle-wrapper.jar` as a 29th file, breaking the pinned 28-file gate | medium | mitigate | Prohibition plus §A naming it as the deliberately-uncounted file with its rationale, and an acceptance criterion asserting the enumeration still prints 28. |
| T-64-P03-15 | Repudiation | re-recording the 11 known-failing tests or the 3 disabled assertions as fresh Phase 64 findings | medium | mitigate | D-15 prohibition plus acceptance criteria asserting `RU-61-06` and `DEBT-02` are cross-referenced inside the `RU-64-02` section. |
| T-64-P03-16 | Tampering | three plans and four tasks writing one `64-COVERAGE.md` | medium | mitigate | Wave 3 behind `depends_on: [64-02]` makes ordering structural (D-04); the four tasks assert exact global counts (22/5, 24/3, 27/0, then 27/0) plus the invariant 29 `n/a` lines and 56 total cell lines. |
| T-64-P03-17 | Repudiation | a `P64-D7-*` ID issued against a dimension that is `n/a` in all 7 rows | medium | mitigate | D-14 prohibition plus an acceptance criterion asserting zero such IDs, and §C asserting it in the record itself. |
| T-64-P03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager **install** task. It runs `npm audit`, a read-only query against the already-installed tree and the advisory registry, guarded by a precondition that halts rather than installing if `node_modules/` is empty — so INVENTORY's Package Legitimacy Gate has no install to audit. |
</threat_model>

<verification>
- `## RU-64-02`'s 7 live unit-row cells and the lockfile row's single live D6 cell each carry
  `pass` or `fail` plus a written check line of at least 120 bytes; its D7 cell and the lockfile
  row's other seven cells carry `R-D7-CI` and `R-LOCKFILE` verbatim.
- `### SEC-08 Dependency Triage` holds a criterion-3 row per vulnerable dependency across both
  trees, with `fix-now`, `file-issue` and `accepted-with-reason` all present in the vocabulary and
  the vendored-binary and action-pinning contributions consolidated by reference.
- The `npm audit` run is pinned with its date and totals; the failing
  `./gradlew --offline -q dependencies` command and its output are recorded, and the transitive gap
  is stated in the cell.
- `P63-D6-002` carries a written disposition and the header ledger's column is resolved; no
  placeholder word remains anywhere in the file.
- Both INVENTORY drift records exist with `location:` resolving into `INVENTORY.md`, and
  `git status --porcelain .planning/reviews/INVENTORY.md` is empty.
- Phase-wide: 27 verdicts, 0 placeholders, 29 `n/a` lines, 56 cell lines.
- The close-out carries sections A-G plus `### Milestone coverage position (last sweep phase)`.
- The file gate re-runs live and prints 28, with every basename present in the coverage file and
  `gradle-wrapper.jar` named as the deliberately-uncounted file.
- The cell gate re-runs live and prints `7 27 29 56`, the leading row count included.
- The per-phase verdict loop across the four coverage files prints `50 35 35 27`, and the close-out
  reconciles 147 of 148 against INVENTORY's stated totals with `RU-D8-01` named as the remainder.
- Every finding phase-wide has all 13 template fields plus `triage:` on every D6 record, a unique
  well-formed non-`P00-`, non-`P64-D7-` ID and a non-blank `dedup:`.
- Zero `^location:` lines resolve into `bbj-vscode/src/`, `bbj-intellij/src/`, `CLAUDE.md`,
  `VERBs.md` or `documentation/`.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- **RVW-05 is complete**: all 28 in-scope files have a recorded pass/fail against every dimension
  that applies to them, across all 3 units, all 4 file-exception rows and all 27 live cells, with
  zero placeholders remaining.
- **SEC-08 is complete**: every npm and Gradle dependency with a known vulnerability is enumerated
  and triaged as `fix-now`, `file-issue` or `accepted-with-reason`, with the npm audit pinned to its
  run date, the Gradle coverage limitation stated rather than hidden, and the vendored binaries and
  action pinning consolidated into one criterion-3 answer.
- **SEC-07 is complete** and answered in the close-out from `RU-64-01`'s posture table, so neither
  security requirement flows to Phase 65 as open work.
- Both D-18 gates pass on live re-derivation — including the row count that proves the four
  file-exception rows were counted — and any disagreement would have been surfaced as a defect
  rather than adopted.
- No inherited work is left outstanding: the one ledger item and both Phase 62 body-level deferrals
  carry written dispositions, and the three verified negative facts are re-confirmed.
- **The milestone's sweep coverage is stated and reconciled**: 147 of INVENTORY's 148 `applies`
  cells recorded across `61`/`62`/`63`/`64-COVERAGE.md`, with `RU-D8-01`/D8 named as the sole
  remainder — the exact claim Phase 68's DOC-03 needs and no longer has to derive.
- Phases 65-69 inherit a complete, gate-verified coverage file: two closed security requirements,
  the `easy`/`fix-now` set for Phase 67, the `DEBT-*` re-triage input for Phase 66, the DOC-03 input
  for Phase 68, and the issue-draft candidates for Phase 69 under D-16's disclosure limits.
</success_criteria>

<output>
Create `.planning/phases/64-build-ci-dependency-review/64-03-SUMMARY.md` when done.
</output>
