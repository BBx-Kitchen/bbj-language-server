---
phase: 64-build-ci-dependency-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/64-COVERAGE.md
autonomous: true
requirements: [RVW-05, SEC-08]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`.planning/reviews/64-COVERAGE.md` exists and carries the complete Phase 64 skeleton: header with the swept-tree SHA recorded once, the **7-row** applicability grid (3 unit rows **plus the 4 file-exception rows**), the `27 / 29 / 56` cell gate re-derived live from INVENTORY, the 28-file gate with `.github/dependabot.yml`'s adoption justified in writing at the point of the count, all 29 verbatim `n/a` carry-forwards, the inherited-item ledger, 3 stubbed unit sections and a stubbed `## Phase 64 Close-Out` — so plans `64-02` and `64-03` append into a frozen shape rather than re-deriving one (D-01, D-05, D-18, D-19)"
    - "`RU-64-03` (BBj tool scripts, vendored JARs & interop test harness, 4 readable files / 1,240 LOC + 3 binary JARs / 112,361 bytes, INVENTORY risk rank 1) has all 7 live unit-row dimensions recorded — D1, D2, D3, D6 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording"
    - "All **3 JAR file-exception rows** are recorded cell-by-cell inside `## RU-64-03` under `### File-exception cells` — 6 live cells (D1 and D6 on each JAR) carrying verdicts and 18 `n/a` cells carrying `R-JAR-BINARY`/`R-D7-CI` verbatim — because Phase 64 is the only sweep phase besides Phase 61 with live file-exception rows and omitting them is the single most likely way this phase under-counts (D-17, D-18.1)"
    - "All 7 files of this unit — `web.bbj`, `em-login.bbj`, `em-validate-token.bbj`, `run-tests.ts`, `BBjCFCli.jar`, `BBjCodeFomatter.jar`, `jcommander-1.71.jar` — are named at least once inside the `## RU-64-03` section, so coverage is file-granular (ROADMAP Phase 64 criterion 1)"
    - "`### Vendored Binary Provenance` exists under `## RU-64-03` and states as facts — not as findings — what is shipped, from where, verified how, and wired to what: each JAR's manifest fields, its SHA-256 and byte size, the `Class-Path: lib/jcommander-1.71.jar lib/BBjCodeFomatter.jar` wiring, the absence of any in-repo build script producing them, and the single call site that executes `BBjCFCli.jar` (D-06, D-11)"
    - "`BBjCodeFomatter.jar`'s **unidentifiability is itself recorded as the D6 finding** — a 38,078-byte binary shipped inside the published extension whose manifest carries `Manifest-Version: 1.0` and nothing else cannot be vulnerability-triaged at all — with `triage: file-issue` asking for provenance rather than a version bump, and not softened into a could-not-determine-version note (D-11)"
    - "`jcommander-1.71.jar` is dated from its own manifest (`Bnd-LastModified: 1493325683414` → 2017-04-27) and checked against a **resolvable advisory reference** for version 1.71, so INVENTORY's `notably old` risk-rank note is verified from the artifact rather than from the filename (D-11)"
    - "Every D6 finding in this plan carries **both** `triage:` (`fix-now` | `file-issue` | `accepted-with-reason`) and `classification:` (`easy` | `major`) with D-09's stated mapping, and any `accepted-with-reason` carries a written reachability argument naming the code path that would have to exist — a bare not-shipped claim is not an accepted reason (D-09)"
    - "The two Phase 62 **body-level deferrals** addressed to `RU-64-03` — the `'java'`-resolved-from-`PATH` provenance boundary noted at `62-COVERAGE.md:1489` and the not-reproducible `BBjCFCli.jar` `-i`-path-versus-stdin precedence question at `62-COVERAGE.md:1833` — each carry a written disposition here, recorded as sweep inputs rather than as ledger rows, so the inherited-item ledger stays at exactly one row and D-18's arithmetic is unchanged (D-17)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier, with each `repro`-tier record stating in one clause why it carries no runnable reproduction; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason rather than being silently dropped (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against INVENTORY's frozen 15-issue snapshot, and the header states once — re-derived, not asserted — that no issue in that snapshot carries the repository's `dependencies` area label or names CI, workflows, build configuration or a vendored binary (RVW-07)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P64-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(64, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop. Plan execution order is fixed by D-02 (`RU-64-03` → `RU-64-01` → `RU-64-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All three plans append to the single `.planning/reviews/64-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one plan per wave, waves 1-3, each plan's `depends_on` naming its predecessor in D-02's order (D-04). If a plan is interrupted mid-append, every unrecorded live cell still carries the literal placeholder `pending`, which is mechanically countable at every wave"
    - "Boundary case: `### File-exception cells` lines use a distinct line shape from unit-row `### Cells` lines, so the two are separately countable — 24 unit-row cell lines plus 32 file-exception cell lines is exactly 56, and a file-exception row folded into a unit row would show up immediately as a short count"
    - "`### Vendored Binary Provenance` states what happens when the JAR the formatter invokes is **absent or replaced** — `document-formatter.ts:10` resolves `${__dirname}/../tools/formatter/BBjCFCli.jar` as a compile-time constant with no existence check, no hash check and no signature check before `java -jar` runs it"
    - "`### Vendored Binary Provenance` states whether anything in this repository **produces** the three JARs — no build script, no CI step, no `package.json` script, no Gradle task — so the only provenance signal is the manifest bytes themselves, and it says so rather than implying a build exists"
    - "`### Vendored Binary Provenance` states the **ordering** fact that decides reachability: `BBjCFCli.jar`'s manifest `Class-Path` names both `lib/` JARs, so executing the CLI loads all three, and the two library JARs are reachable through the first rather than independently invoked"
    - "`### Vendored Binary Provenance` states what an **empty or truncated** manifest leaves unknowable — `BBjCodeFomatter.jar` has no version, no vendor, no build date and no source reference — and names that as a strictly worse posture than a known-vulnerable dependency, which is the point D-11 makes"

  artifacts:
    - ".planning/reviews/64-COVERAGE.md — created by this plan: header (with the swept-tree SHA), `## Applicability Grid — Phase 64 slice` with its `### File-exception rows` sub-table, `## Cell-Total Gate (D-18.1)`, `## File Gate (D-18.2)`, `## Stopping Rule & Write Contract`, `## Exclusion reasons carried forward` (four markers), `## Inherited item ledger (D-17)`, three stubbed `## RU-64-0N` sections, and a stubbed `## Phase 64 Close-Out`"
    - ".planning/reviews/64-COVERAGE.md §`## RU-64-03 — BBj tool scripts, vendored JARs & interop test harness` — 7 live unit-row cells recorded, 1 `n/a` unit-row cell carried forward verbatim, 24 `### File-exception cells` lines for the 3 JAR rows (6 verdicts, 18 `n/a`), plus `### Vendored Binary Provenance`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"

  key_links:
    - "`### Vendored Binary Provenance` -> `RU-64-02`'s `### SEC-08 Dependency Triage` in plan `64-03`, which consolidates the criterion-3 answer by reference rather than re-auditing the binaries (D-02's ordering rationale)"
    - "The skeleton this plan writes -> plans `64-02` and `64-03`, which append into it, and Phase 68's DOC-03 concatenation against INVENTORY's grid"
    - "The `triage:` field this plan first renders -> Phase 67's `fix-now` apply path and Phase 68's `MAJOR-REFACTORS.md` generation (D-09)"
    - "`RU-64-03`'s D1 records on the `.bbj` tool scripts -> Phase 65's SEC-04 (EM token lifecycle) and SEC-05 (process spawning) synthesis, which owns the end-to-end picture this unit only supplies one leg of"
    - "The two dispositioned Phase 62 body-level deferrals -> Phase 68's DOC-04 intent that nothing raised in an earlier sweep is silently dropped"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, supply-chain integrity, path traversal, GDPR) deliberately DROPPED as prohibitions —
    # in this phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT run any command that mutates the working tree — no dependency install, no lockfile regeneration, no automated vulnerability remediation, no formatter invocation. Phase 64 records; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items. Enforced by an acceptance criterion asserting `git status --porcelain` is empty across every reviewed tree"
    - "MUST NOT decompile, disassemble, unpack beyond `META-INF/MANIFEST.MF`, or execute any of the three vendored JARs — D-11 satisfies D1 on the JAR rows by distribution-integrity reasoning and `Class-Path` wiring, consistent with Phase 63 D-13's prohibition on constructing an exploit to confirm a finding"
    - "MUST NOT publish a trigger sequence, a payload or a fork-and-run procedure for any SEC-08 finding that would amount to a working exploitation path against a shipped artifact — this repository is public, git history retains what is committed, and the gap would be unfixed at commit time; name the surface, the problem class and the impact, and stop there (D-16)"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence"
    - "MUST NOT issue a `P64-D7-*` finding ID or mark any D7 cell live — `R-D7-CI` retires the dimension for all 7 rows of this phase, and a finding that seems to want one belongs to a different dimension or a different unit (D-14)"
    - "MUST NOT record a finding whose `location:` is inside `bbj-vscode/src/` or `bbj-intellij/src/` — those trees belong to Phases 61-63, which are closed; a Phase 64 cell claiming them would double-count against INVENTORY's 232-cell total"
    - "MUST NOT record a finding whose `location:` is `CLAUDE.md`, `bbj-vscode/VERBs.md` or anything under `documentation/` — that is `RU-D8-01`'s surface, owned by no sweep phase (D-18)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md`, `61-COVERAGE.md`, `62-COVERAGE.md` or `63-COVERAGE.md` to make a gate agree — INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and all three prior sweeps are closed and verified; a disagreement between a stated total and its re-derivation is itself a defect to surface (D-18)"
    - "MUST NOT add a grid row, a file-exception row, a cell line or a unit section beyond the 56 cells INVENTORY's Phase 64 slice defines, and MUST NOT omit any of the 4 file-exception rows (D-17, D-18.1)"
    - "MUST NOT reword, paraphrase, merge or re-derive any carried-forward `n/a` marker — `R-D7-CI`, `R-D5-CI`, `R-LOCKFILE` and `R-JAR-BINARY` are quoted verbatim from INVENTORY"
    - "MUST NOT add `R-D6-CENTRAL` to this phase's carried-forward list — all seven D6 cells in this slice are live, so the marker three prior phases carried appears zero times here; its absence is stated as a fact, not filled in by reflex"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason, visible rather than silently dropped (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, MUST NOT leave a `triage:` field off any D6 finding, and MUST NOT allocate an ID in INVENTORY's reserved `P00-*` template namespace"
    - "MUST NOT write a credential, an EM token, a session value or an absolute developer-machine path into any record — every citation is a repository-relative `path:line` anchor"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
    - "MUST NOT write into `## RU-64-01` or `## RU-64-02`, and MUST NOT record a cell verdict for a file the sweep did not open — a unit-level generalization over an unread file is a bare verdict wearing a written line"
---

<objective>
Create `.planning/reviews/64-COVERAGE.md` — the phase's sole deliverable — with its complete
skeleton, and sweep review unit **`RU-64-03` — BBj tool scripts, vendored JARs & interop test
harness** (`web.bbj` 97, `em-login.bbj` 51, `em-validate-token.bbj` 34,
`interop-test-harness/run-tests.ts` 1,058 — 4 readable files, 1,240 LOC — plus
`formatter/BBjCFCli.jar` 6,780 B, `formatter/lib/BBjCodeFomatter.jar` 38,078 B and
`formatter/lib/jcommander-1.71.jar` 67,503 B, INVENTORY risk rank 1 of 3) end to end across its 7
live unit-row dimensions **and the 6 live cells of its three JAR file-exception rows**.

Purpose: this plan carries the tracer responsibility (D-01). With only three units, a standalone
skeleton plan would produce a skeleton and nothing else, so `64-01` renders the recording shape on
real content and sweeps its unit in the same pass. It front-loads everything structurally new about
Phase 64: the **first live file-exception rows since Phase 61** (three of the four), the
**vendored-binary half of SEC-08** (D-06's `### Vendored Binary Provenance`), the first rendering of
D-09's dual `triage:`/`classification:` fields, and the phase-wide facts that **D7 is dead in all 7
rows** and **`R-D6-CENTRAL` is carried zero times**. Per D-02 this unit runs first so that
`RU-64-02`'s npm/Gradle sweep in `64-03` can reference an established binary posture rather than
guess at it.

Output: a created coverage file with a frozen skeleton, 13 recorded cells with written check lines,
the vendored-binary provenance narrative, and any finding records, not-reproducible dispositions and
cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 64 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact. `.github/`, `bbj-vscode/`, `bbj-intellij/` and `java-interop/` are read-only review
subject matter for all three plans.

**Created by this plan**

- `.planning/reviews/64-COVERAGE.md` — the phase's sole deliverable. This plan writes the whole
  skeleton plus the `## RU-64-03` section; plan `64-02` appends `## RU-64-01`, plan `64-03` appends
  `## RU-64-02` and fills the close-out.

**Named subsection anchors** (downstream phases consume these by name, so the literal headings
matter — D-06):

- `### Vendored Binary Provenance` — under `## RU-64-03`, this plan. The direct analogue of
  `63-COVERAGE.md`'s `### SEC-03 Integrity Posture`.
- `### SEC-07 Workflow Security Posture` — under `## RU-64-01`, plan `64-02` (D-11/D-13).
- `### SEC-08 Dependency Triage` — under `## RU-64-02`, plan `64-03`, holding the ROADMAP
  criterion-3 triage table (D-09).
- `### File-exception cells` — under `## RU-64-03` (3 JAR rows, this plan) and under `## RU-64-02`
  (the lockfile row, plan `64-03`). Carried forward from `61-COVERAGE.md`, the only prior sweep
  with live file-exception rows.
- Per-unit, carried forward unchanged from `63-COVERAGE.md`: `### Cells`, `### Findings`,
  `### Not-reproducible dispositions`, `### Cross-unit referrals`, `### Unit closure`.

**Finding-ID namespace.** `P64-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(64, dimension)` pair **across the whole phase**.
Phase 64 owns the `P64-*` namespace outright. `P00-*` is INVENTORY's reserved template namespace and
is never allocated. **`P64-D7-*` is never allocated at all** — D7 is `n/a` in all 7 rows (D-14).

**Plan -> review-unit map (D-01, D-02, D-04).** One plan per unit, one wave per plan, in INVENTORY's
risk-rank order:

| Plan | Wave | `depends_on` | Unit | Files | Live cells recorded |
|---|---|---|---|---|---|
| `64-01` (this plan) | 1 | — | `RU-64-03` BBj tool scripts, vendored JARs & interop harness + **3 JAR file-exception rows** | 7 (4 readable / 1,240 LOC + 3 JARs) | 13 |
| `64-02` | 2 | `64-01` | `RU-64-01` GitHub Actions workflows + `.github/dependabot.yml` (D-19) | 7 (587 lines) | 6 |
| `64-03` | 3 | `64-02` | `RU-64-02` Build, packaging & dependency manifests + **the lockfile file-exception row**, **plus the close-out** | 14 (9,208 lines) | 8 |
| | | | **Total** | **28** | **27** |

**Inherited work.** INVENTORY's routing table (D-06) has **zero** Phase 64 rows, `61-COVERAGE.md`
has no downstream-inheritance table, and `62-COVERAGE.md`'s close-out inheritance table names
Phases 63, 65, 66, 67, 68 and 69 — no Phase 64 row. The ledger therefore holds exactly **one** row:
`P63-D6-002`, routed by Phase 63's close-out to `RU-64-02` and re-triaged by plan `64-03` (D-17).
Separately, `62-COVERAGE.md` carries **two body-level deferrals to `RU-64-03`** — not ledger rows,
but sweep inputs this plan must dispose of.

<!-- The literals below are section headings the executor must create verbatim in the coverage
     file; they are asserted positively by the acceptance criteria. -->
<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### File-exception cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Unit closure -->
<!-- planner-discipline-allow: ### Vendored Binary Provenance -->
<!-- planner-discipline-allow: ### File-exception rows -->

<tasks>

<task type="auto">
  <name>Task 1: Create 64-COVERAGE.md and sweep RU-64-03 at evidence tier `repro`/repro-equivalent — D1, D2, D3, D6 plus the three JAR rows — including the Vendored Binary Provenance subsection</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>The working tree is on branch `v4.0-stability-and-quality` — `git rev-parse --abbrev-ref HEAD`
  prints exactly that — `.planning/reviews/INVENTORY.md` exists, and `.planning/reviews/64-COVERAGE.md`
  does not yet exist. `unzip` is on `PATH` (`command -v unzip` succeeds), since every JAR provenance fact
  is read with it. Halt and report if any of the four fails.</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the immutable contract. Read §"Frozen Open-Issue Snapshot"
      (the 15-issue dedup list — check the `Area` column for the repository's `dependencies` label
      before asserting anything about neighbours), §"Finding Standard" 3a-3d (note D6 is tier
      `inherited`, resolving to repro-equivalent for a version or advisory claim — it needs the
      advisory reference), §"Finding Record Template" (the exact 13 fields, in order),
      §"Phase 64 review units …" → the ranking basis and **all three** unit blocks with their file
      lists and LOC (needed for the stubs), §"Applicability Grid" rows `RU-64-01`..`RU-64-03`,
      §"File-exception rows" → **all four** Phase 64 rows, §"Exclusion reasons" → **R-D7-CI**,
      **R-D5-CI**, **R-LOCKFILE**, **R-JAR-BINARY** (and confirm for yourself that `R-D6-CENTRAL`
      applies to **no** Phase 64 cell), §"Recording protocol (D-09)", §"Grid totals",
      §"Surface Accounting" → the `bbj-vscode/` and `.github/` breakdowns, and
      §"Routing table (D-06)" — confirm there is **no** Phase 64 row
    - `.planning/REQUIREMENTS.md` lines 14-23 — the D1-D8 table. Every `pass` line is phrased against
      that dimension's own "what counts as a finding" wording. Also RVW-05, RVW-06, RVW-07, and
      **SEC-08**, whose vendored-binary half this plan discharges
    - `.planning/ROADMAP.md` §"Phase 64: Build, CI & Dependency Review" — the goal and the four
      success criteria, criteria 1 and 3 being this plan's
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — D-01..D-19 are binding, read
      in full
    - `.planning/reviews/63-COVERAGE.md` lines 1-130 — the frozen shape to copy (header, grid, cell
      gate, stopping rule and write contract, placeholder convention, exclusion carry-forward with
      its identity check, inherited ledger, and the opening of `## RU-63-03` with its `### Cells`
      line format and its `### SEC-03 Integrity Posture` — the structural mirror for
      `### Vendored Binary Provenance`). Also read lines 400-435 (the `P63-D6-002` record, the
      13-field rendering) and lines 597-610 (`### Unit closure`). Do **not** read the remaining
      ~2,500 lines
    - `.planning/reviews/61-COVERAGE.md` lines 21-101 and lines 3226-3236 — **the only prior
      precedent for file-exception rows**: the `### File-exception rows` sub-table under the grid,
      the cell-gate wording that counts unit rows plus file-exception rows together, and the
      `### File-exception cells` line shape used inside the parent unit's section
    - `.planning/reviews/62-COVERAGE.md` **lines 1489 and 1833 only** — the two body-level deferrals
      to `RU-64-03`: the `'java'`-resolved-from-`PATH` provenance boundary Phase 62 explicitly left
      to this unit, and the not-reproducible `BBjCFCli.jar` `-i`-path-versus-stdin precedence
      question. Do not read the rest of that file
    - **Review targets, read in full:** `bbj-vscode/tools/web.bbj` (97),
      `bbj-vscode/tools/em-login.bbj` (51), `bbj-vscode/tools/em-validate-token.bbj` (34),
      `bbj-vscode/tools/interop-test-harness/run-tests.ts` (1,058)
    - **Binary provenance, read by manifest and hash only:** `unzip -p <jar> META-INF/MANIFEST.MF`
      and `sha256sum <jar>` for each of `bbj-vscode/tools/formatter/BBjCFCli.jar`,
      `bbj-vscode/tools/formatter/lib/BBjCodeFomatter.jar`,
      `bbj-vscode/tools/formatter/lib/jcommander-1.71.jar`. Nothing else is extracted, nothing is
      decompiled, nothing is executed (D-11)
    - **Context only, no finding may be located in them:** `bbj-vscode/src/document-formatter.ts`
      (the single call site — line 10 resolves the CLI JAR path) and `bbj-vscode/.vscodeignore`
      (the packaging-exclusion list, which decides whether `tools/` reaches the published `.vsix`)
    - **Do NOT read** any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and
      INVENTORY supersedes them
  </read_first>
  <action>
Create `.planning/reviews/64-COVERAGE.md` and write it in the order below, then record `RU-64-03`'s
four `repro`-tier unit-row cells and the six live cells of its three JAR file-exception rows. Copy
`63-COVERAGE.md`'s frozen shape unchanged (D-05) — Phase 61's D-05 checkpoint froze it and Phases 62
and 63 each confirmed it transfers, so **no new format checkpoint is spent**. Phase 64 adds exactly
three named subsections (D-06) and reinstates one element Phase 63 did not need: file-exception
rows, whose rendering comes from `61-COVERAGE.md`.

**A. Header.** Title it as Phase 64's coverage record for the build, CI and dependency surface
(RVW-05, SEC-07, SEC-08). Record, as labelled lines: the **swept tree** — branch
`v4.0-stability-and-quality` at the full 40-character SHA obtained by running `git rev-parse HEAD`
**now, at execution time**, stated once for the whole phase and never re-anchored per plan (HEAD
advances with every v4.0 planning commit, including the one that committed these plans); the
**governing standard**, `.planning/reviews/INVENTORY.md`, named as immutable and not edited by this
phase; the **dedup source**, INVENTORY's frozen 15-issue snapshot queried 2026-08-17, noting that
Phase 69 re-queries live before filing, and stating — **after checking the snapshot's `Area` column
yourself rather than assuming** — how many of the 15 carry the repository's `dependencies` area
label and how many name CI, a workflow, build configuration or a vendored binary, so a reader knows
why most `dedup:` verdicts in this file resolve the way they do; the **slice size** — 3 unit rows
plus 4 file-exception rows = **7 rows × 8 dimensions = 56 cells**, **27** `applies` and **29** `n/a`;
and the **file gate** — **28 files**, being the 27 INVENTORY assigns to `RU-64-01`..`RU-64-03` plus
`.github/dependabot.yml`, adopted into `RU-64-01` by D-19 and named here as a **documented
addition** rather than folded in silently, with its one-sentence justification (it is committed,
functional `.github/` CI configuration; `RU-64-01` is the unit that owns `.github/`; and it is the
milestone's only dependency-automation config, so leaving it unreviewed in the phase that owns
SEC-08 would be exactly the invisible hole the gates exist to prevent). Then state in one sentence
that the recording shape is inherited unchanged from `63-COVERAGE.md` (D-05) rather than re-derived,
and name the three Phase 64 additions by their literal headings: `### Vendored Binary Provenance`
under `RU-64-03`, `### SEC-07 Workflow Security Posture` under `RU-64-01`, and
`### SEC-08 Dependency Triage` under `RU-64-02`.

**B. `## Applicability Grid — Phase 64 slice`.** Grid table on top, per-unit detail below, so
Phase 68 can concatenate mechanically and each unit's evidence stays adjacent to its own coverage
claim. Use INVENTORY's exact column shape
`Unit | D1 Security | D2 Correctness | D3 Performance | D4 Maintainability | D5 Test coverage | D6 Dependency health | D7 Cross-IDE parity | D8 Doc accuracy`,
one row per unit in D-02's risk-rank order: `RU-64-03`, `RU-64-01`, `RU-64-02`. Then a
`### File-exception rows` sub-table in INVENTORY's own column shape
(`File | Parent unit | D1 | … | D8`) carrying **all four** Phase 64 file-exception rows transcribed
verbatim: `bbj-vscode/package-lock.json` (parent `RU-64-02`), `tools/formatter/BBjCFCli.jar`,
`tools/formatter/lib/BBjCodeFomatter.jar` and `tools/formatter/lib/jcommander-1.71.jar` (parent
`RU-64-03`). Use INVENTORY's row labels character-for-character so the gate below re-derives against
the same strings. Note beneath the two tables the three things that make this slice structurally
unlike Phase 63's: **(1) Phase 64 is the only sweep phase that owns file-exception rows in a live
capacity besides Phase 61** — all four remaining rows in INVENTORY belong to Phase 64 units, and
omitting one is the single most likely way this phase silently under-counts (D-17); **(2) D7 is
dead across the entire phase** — all 7 rows carry `n/a — R-D7-CI`, no plan performs parity work of
any kind, and no `P64-D7-*` ID is ever issued (D-14); **(3) D6 is live in bulk for the first time**
— seven cells, three unit rows plus all four file-exception rows, carrying the whole of SEC-08,
where Phase 63 had exactly one live D6 cell.

**C. `## Cell-Total Gate (D-18.1)`.** State the expected totals — **27 `applies`, 29 `n/a`, 56
total, across 7 rows** — and immediately **re-derive them from INVENTORY rather than restating
them**, by running this exact pipeline and recording both the command and its literal output in the
file:

    grep -E '^\| .(RU-64-0[1-3]|bbj-vscode/package-lock\.json|tools/formatter/[^|]*). \|' .planning/reviews/INVENTORY.md | grep -E 'applies|n/a' | awk '{a+=gsub(/applies/,"x"); n+=gsub(/n\/a/,"y")} END{print NR, a, n, a+n}'

The four fields it prints are rows, `applies`, `n/a`, total. State explicitly that the leading row
count is part of the gate: **7**, not 3 — a re-derivation that prints 3 rows has silently dropped
the file-exception rows, which is the specific failure mode D-17 names. State that if this
re-derivation ever disagrees with the stated totals, the disagreement is itself a defect to surface,
not a number to quietly adopt. Note that plan `64-03` re-runs this gate at close-out as one of the
phase's two hard gates.

**D. `## File Gate (D-18.2)`.** State the expected count — **28 files** — and its composition: 6
workflow files (568 lines) + `.github/dependabot.yml` (19 lines, adopted by D-19) for `RU-64-01`;
4 readable tool files (1,240 lines) + 3 vendored JARs (112,361 bytes) for `RU-64-03`; 14 manifest
files (9,208 lines) for `RU-64-02`. Record the enumeration command and its literal output:

    ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties | wc -l

Restate at the point of the count that 27 of the 28 come from INVENTORY's own file lists and the
28th is the D-19 adoption, so a reader diffing this file against INVENTORY sees a deliberate
documented addition rather than a miscount. Note that plan `64-03` re-runs this gate at close-out
and additionally confirms every one of the 28 basenames appears somewhere in this file.

**E. `## Stopping Rule & Write Contract`.** Four-part stopping rule. A unit's sweep is complete
when: (i) **every live `applies` cell it owns** — in its own unit row **and in every file-exception
row parented to it** — carries a verdict (`pass`/`fail`) plus a written line naming the concrete
checks applied; (ii) every file in the unit's file list is named at least once inside that unit's
own section, in a check line or in a finding's `location:`, so coverage is file-granular rather than
unit-granular; (iii) every candidate claim raised during the sweep is either promoted to a finding
record clearing its evidence tier, or written under that unit's `### Not-reproducible dispositions`
with its reason; **and (iv) every inherited item addressed to that unit carries a written
disposition** — promoted, dismissed-with-evidence, merged, or not-reproducible. Once (i)-(iv) hold,
the unit is done and no further reading is licensed. Write contract: plans `64-02` and `64-03` each
fill exactly one unit section and touch nothing else — no fragment files, no assembly plan, no
whole-file rewrite, no rewording of a carried-forward `n/a` reason — with one scoped exception, that
`64-03` additionally fills the close-out and resolves the inherited-item ledger's disposition
column, which is precisely its role as the closing plan. Ordering across this shared file is
enforced structurally by the wave dependency chain (D-04), not by an assumption about executor
behaviour: one plan per wave, waves 1-3, each plan's `depends_on` naming its predecessor in D-02's
order. Placeholder: every not-yet-recorded live cell line ends with the single lowercase word
`pending`, so remaining work is mechanically countable at every wave. Close this section with one
paragraph stating the four environment constraints that shape every record in this file, each
**re-derived at execution time rather than trusted from the plan**: (a) **the working tree is never
mutated** — this phase records and Phase 67 applies, so no dependency install, no lockfile
regeneration and no automated remediation is run anywhere in this phase; (b) **the vendored JARs are
assessed by manifest and hash only** — no decompilation, no execution (D-11); (c) **GitHub Actions
cannot be executed here**, so every `RU-64-01` finding is trace-evidenced and names its triggering
event (D-12), which plan `64-02` renders; (d) **the Gradle dependency tree cannot be enumerated
locally** — `./gradlew --offline -q dependencies` fails on the local JDK before doing any work — so
plan `64-03` enumerates Gradle statically and records that as a stated coverage limitation rather
than hiding it (D-10).

**F. `## Exclusion reasons carried forward`.** Four blocks, each copied **verbatim from INVENTORY
§"Exclusion reasons" — not reworded, not paraphrased, not merged**: **R-D7-CI** (6 cells — the D7
cell of `RU-64-03`, `RU-64-01`, `RU-64-02` and of all three JAR rows), **R-D5-CI** (1 cell —
`RU-64-01`/D5), **R-LOCKFILE** (7 cells — every dimension of `bbj-vscode/package-lock.json` except
D6), and **R-JAR-BINARY** (15 cells — D2, D3, D4, D5 and D8 on each of the three JAR rows). State
the identity check: 6 + 1 + 7 + 15 = 29, matching the 29 `n/a` cells in this slice. Then state, as a
fact rather than an omission, that **`R-D6-CENTRAL` — the marker Phases 61, 62 and 63 all carried —
appears zero times in this slice**, because all seven D6 cells here are live: `R-D6-CENTRAL`'s own
text says dependency-tree health is assessed once at `RU-64-02` and vendored-binary provenance at
`RU-64-03`, and this is that phase.

**G. `## Inherited item ledger (D-17)`.** A one-row table with columns *source / from / to /
subject / disposition*, the disposition left as the placeholder word `pending` for plan `64-03` to
resolve: `P63-D6-002`, from `63-COVERAGE.md`'s close-out inheritance table, to `RU-64-02`/D6 — the
`bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch recorded at
`bbj-intellij/build.gradle.kts:12-13`, a `location:` Phase 63 flagged as its one deliberate
exception. Beneath it, state three facts **each verified now rather than asserted**: INVENTORY's
routing table (D-06) contains no Phase 64 row; `61-COVERAGE.md` has no downstream-inheritance table
and names no Phase 64 obligation; and `62-COVERAGE.md`'s close-out inheritance table names
Phases 63, 65, 66, 67, 68 and 69 and contains no Phase 64 row. Then add a clearly-labelled second
block, **not** a ledger row and **not** counted by any gate, recording the **two body-level
deferrals `62-COVERAGE.md` addresses to `RU-64-03`**: at `62-COVERAGE.md:1489`, Phase 62's D1 cell
for `RU-62-02` states that `document-formatter.ts`'s `jarPath` provenance and pinning are
"`RU-64-03`'s territory (Phase 64), noted as a boundary here, not recorded as a finding"; and at
`62-COVERAGE.md:1833`, a not-reproducible disposition defers whether `BBjCFCli.jar` honours its
`-i` path argument or its stdin content, explicitly because "the jar itself is `RU-64-03`'s surface
(Phase 64)". State that both are dispositioned by this plan inside `## RU-64-03` and that they are
recorded here as sweep inputs rather than as ledger rows precisely so D-18's arithmetic stays
unchanged.

**H. Three stubbed unit sections**, in D-02's order. One `## RU-64-0N — <name>` heading per unit,
each carrying a `**Files (<count> / <LOC>):**` line listing every file path with its individual LOC
or byte size from INVENTORY's per-unit table, then `**Risk rank:**`, `**Sweep method:**` and
`**Owning plan:**` lines. Sweep method is `full read` for the readable files of every unit;
`RU-64-03` adds `manifest and hash only, no decompilation and no execution (D-11)` for its three
JARs; `RU-64-01` adds `plus .github/dependabot.yml, adopted by D-19`; `RU-64-02` adds
`full read except package-lock.json, which is read as the D6 dependency-tree source only
(R-LOCKFILE)`. Then a `### Cells` block with exactly 8 lines, one per dimension, each beginning
`- D{n} <dimension name> — ` using REQUIREMENTS.md's dimension names verbatim (`D1 Security`,
`D2 Correctness & error handling`, `D3 Performance & resource use`,
`D4 Maintainability & code smells`, `D5 Test coverage gaps`, `D6 Dependency health`,
`D7 Cross-IDE parity`, `D8 Comment & doc accuracy`). Each live cell line ends with the single
lowercase word `pending`; each `n/a` cell line is completed immediately as
`- D{n} <name> — n/a — "<verbatim marker text>"`. Then, for `RU-64-03` and `RU-64-02` only, a
`### File-exception cells` block using `61-COVERAGE.md`'s line shape —
`- [file-exception] <INVENTORY row label> · D{n} — <verdict or n/a> — <text>` — with 8 lines per
owned file-exception row: 24 lines under `RU-64-03` (three JARs) and 8 lines under `RU-64-02` (the
lockfile). Complete every `n/a` file-exception line immediately with its verbatim marker text;
every live one ends with `pending`. Then empty `### Findings`,
`### Not-reproducible dispositions`, `### Cross-unit referrals` and `### Unit closure` sub-blocks
for every unit, an empty `### Vendored Binary Provenance` for `RU-64-03`, an empty
`### SEC-07 Workflow Security Posture` for `RU-64-01`, and an empty `### SEC-08 Dependency Triage`
plus an empty `### Inherited item triage` for `RU-64-02`. **Counting check before moving on:**
exactly 24 lines match `- D{n} ` at line start, exactly 32 match the file-exception line shape,
56 in total, exactly 29 of the 56 carry `n/a`, and exactly 27 carry `pending`.

**I. Stubbed `## Phase 64 Close-Out`** with seven empty lettered subsections mirroring
`63-COVERAGE.md`'s A-G — file gate, cell-total gate, finding accounting, inherited-item accounting,
scope-fidelity note, ROADMAP success criteria, closing confirmations — plus an eighth stubbed
subsection `### Milestone coverage position (last sweep phase)`, which exists because Phase 64 is
the last sweep phase and Phase 68's DOC-03 full-coverage claim reads it directly. Note that plan
`64-03` fills all of it.

---

**Now sweep `RU-64-03`.** Record its D1, D2, D3 and D6 unit-row cell lines and the D1 and D6 lines
of each of the three JAR file-exception rows, replacing the placeholder on each with `pass` or
`fail` plus a written line naming the concrete checks applied. Append any resulting finding records
under `### Findings`. Enumerate the dimensions explicitly so none is absorbed by whichever is
loudest: this task covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource
use, and D6 Dependency health** — and no others. D6 is in this task because a version or advisory
claim is repro-equivalent under INVENTORY 3b; D7 is excluded because it is `n/a` here and is `n/a`
everywhere in this phase.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
input, secret exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks, on the
four readable files first: what `em-login.bbj` and `em-validate-token.bbj` do with an Enterprise
Manager credential and the token it returns — where the token comes from, whether it is passed as a
process argument (visible in a process table), written to a file, echoed to stdout, or logged, and
what happens when validation fails or the endpoint is unreachable; what `web.bbj` accepts as input
and what it constructs from it — any URL, path, command string or classpath value built from a
value the script did not itself produce; and for
`bbj-vscode/tools/interop-test-harness/run-tests.ts` (1,058 lines, the largest readable file in this
phase) what it spawns, what it reads from the environment, what it writes to disk, and what it opens
over the network — in particular any connection to the java-interop socket service and whether the
host/port it targets is fixed or configurable. State plainly which of these belong to Phase 65's
cross-cutting SEC-04 (EM token lifecycle) and SEC-05 (process spawning) synthesis: record this
unit's leg with full evidence and cite the boundary, do **not** attempt the end-to-end lifecycle
here. Then the three JAR rows' D1, satisfied by distribution-integrity reasoning rather than
bytecode analysis (D-11): whether `tools/` is excluded from the published extension — read
`bbj-vscode/.vscodeignore` and say what it does and does not exclude, so "shipped inside the
published extension" is a verified claim rather than an assumption; the single call site, which
resolves the CLI JAR as a compile-time constant path with no existence check, no hash check and no
signature check before it is executed (cite `document-formatter.ts:10` in `evidence:` — its
`location:` stays on the JAR row, because that file is `RU-62-02`'s and is closed); the
`Class-Path: lib/jcommander-1.71.jar lib/BBjCodeFomatter.jar` declaration in `BBjCFCli.jar`'s
manifest, which makes both library JARs reachable through the first rather than independently
invoked; and whether anything in this repository **produces** these JARs — search for a build
script, a CI step, an npm script or a Gradle task that emits them, and record the search you ran and
its result, because "no in-repo producer" is a provenance fact that must be established rather than
inferred. Record concrete findings where evidence clears the tier — a bare assertion is not a
finding.

*D2 Correctness & error handling* (tier `repro`; the JAR rows are `n/a — R-JAR-BINARY` here and
below). Assess against null/undefined safety, unhandled rejections, swallowed exceptions, async
races, off-by-one, wrong edge-case behaviour and resource leaks, across the four readable files. The
concrete checks: what each `.bbj` script does on an error path — a non-zero exit, an unreachable
endpoint, a malformed response, an empty or missing argument — and whether the caller can
distinguish failure from an empty success; and in `run-tests.ts`, whether every asynchronous
operation is awaited and every rejection handled, whether spawned processes are reaped and file
handles and sockets closed on every exit path including the failure path, whether a test-case loop
can mis-report a failure as a pass, and what the harness returns as its process exit code when a
case fails. Each claim needs a line-by-line trace naming the concrete input or state and the exact
`file:line` where behaviour diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks,
missing caches or debounces, redundant work and unbounded memory growth. The concrete checks:
whether `run-tests.ts` accumulates unbounded output from spawned processes or holds every test
result in memory, whether it re-reads the same file or re-establishes the same connection per case
rather than once, and whether any loop over test cases is quadratic in the corpus; and whether the
`.bbj` scripts perform per-invocation work that is repeated rather than cached. Where a cost is
latent rather than active, say so and do not promote it to a finding on volume alone.

*D6 Dependency health* (tier `inherited`, resolving to repro-equivalent — a version or advisory
claim needs the advisory reference). This is the **vendored-binary half of SEC-08** and it carries
four live cells: the `RU-64-03` unit row plus one on each JAR row. On the unit row: what
third-party code the four readable files depend on — every import, require or external tool
`run-tests.ts` and the three `.bbj` scripts reach for — and whether any of it is declared anywhere
that a dependency scanner could see. On the JAR rows, one cell each, each opening with the artifact
identity established by `sha256sum` and byte size, since for two of the three that hash is the only
stable identifier available:

- **`jcommander-1.71.jar`** (67,503 bytes) — its manifest identifies it: `Bundle-SymbolicName:
  jcommander`, `Bundle-Version: 1.71`, `Bnd-LastModified: 1493325683414`, `Created-By: 1.8.0_111
  (Oracle Corporation)`, `Tool: Bnd-2.4.0.201411031534`. Convert the `Bnd-LastModified` epoch
  milliseconds to a date and record both the raw value and the date, so INVENTORY's `notably old`
  risk-rank note is verified from the artifact itself rather than from the filename. Then check
  version 1.71 against a **resolvable advisory reference** — GitHub Advisory, OSV or the vendor's
  own advisory, your choice, provided the citation resolves — and record the reference, the outcome,
  and how far behind the current stable line 1.71 is. If no advisory applies, say so with the source
  you checked and the date you checked it; a clean result is a finding-free `pass` with a written
  line, not silence.
- **`BBjCFCli.jar`** (6,780 bytes) — first-party, and its build provenance is readable:
  `Created-By: 11.0.10+9 (AdoptOpenJDK)`, `Ant-Version: Apache Ant 1.10.8`,
  `Main-Class: BBjCFCli.BBjCFCli`, `Class-Path: lib/jcommander-1.71.jar lib/BBjCodeFomatter.jar`,
  and an `X-COMMENT` line stating the main class is added by the build. Record what that establishes
  — which JDK and which build tool produced it, and therefore roughly when — and what it does not:
  no version, no source revision, and no build script in this repository that reproduces it.
- **`BBjCodeFomatter.jar`** (38,078 bytes) — its entire manifest is `Manifest-Version: 1.0` and
  nothing else: no version, no vendor, no build date, no source reference. **This unidentifiability
  is the finding, and it is not to be softened into a could-not-determine-version note.** A binary
  shipped inside a published marketplace extension that no manifest, lockfile or build script
  identifies cannot be vulnerability-triaged at all, which is a strictly worse posture than a
  known-vulnerable dependency — record that as a supply-chain posture statement with `triage:
  file-issue`, and state that the fix it asks for is provenance, not a version bump. Note its
  filename typo (`Fomatter`) as corroborating evidence of a hand-copied vendored binary rather than
  a build-produced one.

Every D6 finding in this plan carries **both** `triage:` and `classification:`, per D-09's mapping —
`fix-now` maps to `easy` (a version bump with no API change, applicable in Phase 67), `file-issue`
maps to `major` (needs a breaking upgrade, a replacement, or upstream action), and
`accepted-with-reason` maps to `major` (documented, not filed as a fix) and requires a written
reachability argument naming the code path that would have to exist for the vulnerability to matter
and showing it does not. A bare not-shipped or dev-dependency claim is **not** an accepted reason
under RVW-06.

**Then write `### Vendored Binary Provenance`** under `## RU-64-03`. It is a narrative subsection in
the register of `63-COVERAGE.md`'s `### SEC-03 Integrity Posture` — numbered facts stated against the
actual artifacts, closing with a blast-radius statement and a what-was-read-versus-what-was-asserted
disclaimer. It reads like a map, not a defect list: "there is no checksum" is a fact to state; it
becomes a finding only when the record says what it enables. State: **(1) what is shipped and how it
reaches a user** — the three files, their byte sizes, their SHA-256 hashes, and what
`.vscodeignore` does and does not exclude, so the packaging claim is verified; **(2) provenance per
artifact** — the manifest facts above, one block each, with the identifiable one, the first-party
one and the unidentifiable one clearly distinguished; **(3) pinning and update mechanism** — that no
lockfile entry, no checksum, no signature and no in-repo build script covers any of the three, and
whether any dependency-automation config in this repository could ever see them (plan `64-02` sweeps
`.github/dependabot.yml`; state the boundary rather than pre-empting its finding); **(4) reachability
and wiring** — the `Class-Path` chain, the single call site that executes the CLI JAR, and the
already-recorded Phase 62 boundary that the `java` executable itself is resolved from `PATH` with no
absolute-path pinning (`62-COVERAGE.md:1489`), cited rather than re-recorded, since
`document-formatter.ts` is `RU-62-02`'s file; and **(5) what was deliberately not done** — no
decompilation, no disassembly, no execution (D-11) — and precisely what that leaves unknowable,
including the deferred `-i`-path-versus-stdin precedence question from `62-COVERAGE.md:1833`, which
is dispositioned as **not-reproducible** here with that reason unless it can be settled from the
manifest alone. Close with the blast radius: what an attacker who controls any one of these three
artifacts ends up controlling, given that the CLI JAR is executed with the user's own document
content on the formatter path. End with one line stating which artifacts were read, by what method,
and that nothing here is asserted as a defect purely by virtue of appearing in this subsection —
discrete `P64-D1-*`/`P64-D6-*` records are allocated only where a concrete evidence-clearing defect
exists.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order: `id`, `unit`, `location`, `dimension`, `secondary`, `severity`, `evidence_tier`, `evidence`,
`failure_scenario`, `classification`, `effort`, `dedup`, `disposition` — with `triage:` added as a
14th line immediately after `classification:` on **every D6 finding** (D-09). `unit:` is `RU-64-03`
for every record in this section, including those whose `location:` is one of the three JAR paths.
`evidence:` opens with the basis and states in one clause why it carries no runnable reproduction
where none is possible. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline;
test (6) independently forces `major` for any `critical`/`high` severity and for any D1 primary
dimension. `dedup:` is never blank and is checked against the frozen 15. Any finding that would
amount to a working exploitation path against a shipped artifact follows D-16's two-tier rule
strictly — surface, problem class and impact; no trigger sequence, no payload, no fork-and-run
procedure — and opens its `evidence:` field with the exact phrase `Disclosure-limited per D-16` so
the redaction is auditable rather than indistinguishable from thin evidence. Everything else gets
full concrete detail per the finding standard as written. Any claim that does not clear its tier
goes under `### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The coverage file exists and carries the swept-tree SHA recorded at execution time: `test -f .planning/reviews/64-COVERAGE.md` succeeds and `grep -c "$(git rev-parse HEAD)" .planning/reviews/64-COVERAGE.md` prints at least `1`
    - The skeleton is complete and correctly sized, with unit rows and file-exception rows separately countable: `grep -cE '^- D[1-8] '` prints `24`, `grep -cE '^- \[file-exception\] '` prints `32`, `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56`, `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`, and `grep -c '^## RU-64-0'` prints `3`
    - The cell gate was re-derived live and the row count is part of it: the file contains the literal `7 27 29 56`, and independently the INVENTORY pipeline still prints `7 27 29 56`
    - The file gate is stated with the D-19 adoption named at the point of the count: the file contains `28` and contains `.github/dependabot.yml`, and independently the enumeration command still prints `28`
    - All four `n/a` markers are carried forward verbatim and unmerged: the file contains `governs build/CI/packaging output, not end-user-observable IDE runtime behavior`, `Workflow YAML orchestrates test execution but is not itself unit-testable code`, `machine-generated lockfile, never hand-edited`, and `its bytecode cannot be read or diffed in this review`
    - The `R-D6-CENTRAL` non-carry is stated rather than left silent: the file contains `R-D6-CENTRAL`
    - The four `repro`-tier unit-row cells of this unit are recorded: with the section extracted by `sed -n '/^## RU-64-03 /,/^## RU-64-01 /p' .planning/reviews/64-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D6 .* — (pass|fail) — ` each print `1`
    - D7 is untouched in this unit and carries its marker: within the extracted section, `grep -cE '^- D7 .* — n/a — '` prints `1`
    - The three JAR file-exception rows are recorded cell-by-cell: within the extracted section, `grep -cE '^- \[file-exception\] '` prints `24`, `grep -cE '^- \[file-exception\] .* · D1 — (pass|fail) — '` prints `3`, `grep -cE '^- \[file-exception\] .* · D6 — (pass|fail) — '` prints `3`, and `grep -cE '^- \[file-exception\] .* — n/a — '` prints `18`
    - Coverage is file-granular across all 7 files of the unit: the extracted section contains each of `web.bbj`, `em-login.bbj`, `em-validate-token.bbj`, `run-tests.ts`, `BBjCFCli.jar`, `BBjCodeFomatter.jar` and `jcommander-1.71.jar`
    - The provenance subsection exists and states the facts D-11 names: the extracted section contains `### Vendored Binary Provenance`, and within it the strings `Manifest-Version`, `Class-Path`, `Bnd-LastModified`, `sha256`, `.vscodeignore` and `document-formatter.ts` all appear
    - Both Phase 62 body-level deferrals are dispositioned: the file contains `62-COVERAGE.md:1489` and contains `62-COVERAGE.md:1833`
    - Every D6 finding carries the dual field: the count of `^triage:` equals the count of `^dimension:[[:space:]]+D6`
    - Phase-wide counts prove nothing else was written: `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `10` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `17`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding IDs are unique, well-formed, never from the reserved namespace and never D7: `grep -oE '^id:[[:space:]]+P64-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing, `grep -cE '^id:[[:space:]]+P00-'` prints `0`, and `grep -cE '^id:[[:space:]]+P64-D7-'` prints `0`
    - Every finding carries every required field: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$'` prints `0`
    - No finding is anchored in a tree owned by a closed phase or in the unowned documentation unit: the counts of `^location:[[:space:]]+bbj-vscode/src/`, `^location:[[:space:]]+bbj-intellij/src/`, `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; test -f "$f"; grep -q "$(git rev-parse HEAD)" "$f"; test "$(grep -cE "^- D[1-8] " "$f")" = 24; test "$(grep -cE "^- \[file-exception\] " "$f")" = 32; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -c "^## RU-64-0" "$f")" = 3; grep -qF "7 27 29 56" "$f"; test "$(grep -E "^\| .(RU-64-0[1-3]|bbj-vscode/package-lock\.json|tools/formatter/[^|]*). \|" .planning/reviews/INVENTORY.md | grep -E "applies|n/a" | awk "{a+=gsub(/applies/,\"x\"); n+=gsub(/n\/a/,\"y\")} END{print NR, a, n, a+n}")" = "7 27 29 56"; grep -qF ".github/dependabot.yml" "$f"; test "$(ls .github/workflows/*.yml .github/dependabot.yml bbj-vscode/tools/*.bbj bbj-vscode/tools/interop-test-harness/run-tests.ts bbj-vscode/tools/formatter/*.jar bbj-vscode/tools/formatter/lib/*.jar bbj-vscode/package.json bbj-vscode/package-lock.json bbj-vscode/esbuild.mjs bbj-vscode/eslint.config.js bbj-vscode/langium-config.json bbj-vscode/tsconfig.json bbj-vscode/tsconfig.test.json bbj-vscode/vitest.config.ts bbj-intellij/build.gradle.kts bbj-intellij/settings.gradle.kts bbj-intellij/gradle.properties bbj-intellij/gradlew bbj-intellij/gradlew.bat bbj-intellij/gradle/wrapper/gradle-wrapper.properties | wc -l)" = 28; for s in "governs build/CI/packaging output, not end-user-observable IDE runtime behavior" "Workflow YAML orchestrates test execution but is not itself unit-testable code" "machine-generated lockfile, never hand-edited" "its bytecode cannot be read or diffed in this review" "R-D6-CENTRAL" "62-COVERAGE.md:1489" "62-COVERAGE.md:1833"; do grep -qF "$s" "$f"; done; sed -n "/^## RU-64-03 /,/^## RU-64-01 /p" "$f" > /tmp/p64ru03.txt; for d in 1 2 3 6; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p64ru03.txt)" = 1; done; test "$(grep -cE "^- D7 .* — n/a — " /tmp/p64ru03.txt)" = 1; test "$(grep -cE "^- \[file-exception\] " /tmp/p64ru03.txt)" = 24; test "$(grep -cE "^- \[file-exception\] .* · D1 — (pass|fail) — " /tmp/p64ru03.txt)" = 3; test "$(grep -cE "^- \[file-exception\] .* · D6 — (pass|fail) — " /tmp/p64ru03.txt)" = 3; test "$(grep -cE "^- \[file-exception\] .* — n/a — " /tmp/p64ru03.txt)" = 18; for s in "web.bbj" "em-login.bbj" "em-validate-token.bbj" "run-tests.ts" "BBjCFCli.jar" "BBjCodeFomatter.jar" "jcommander-1.71.jar" "### Vendored Binary Provenance" "Manifest-Version" "Class-Path" "Bnd-LastModified" "sha256" ".vscodeignore" "document-formatter.ts"; do grep -qF "$s" /tmp/p64ru03.txt; done; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 10; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 17; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "bbj-vscode/src/" "bbj-intellij/src/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_01_SKELETON_AND_RU03_REPRO_OK'</automated>
  </verify>
  <done>`64-COVERAGE.md` exists with the full frozen skeleton — 24 unit-row cell lines plus 32
  file-exception cell lines totalling 56, 29 verbatim `n/a` carry-forwards under four markers with the
  `R-D6-CENTRAL` non-carry stated, the live-re-derived `7 27 29 56` cell gate, the 28-file gate with
  `.github/dependabot.yml`'s adoption justified at the point of the count, the four-part stopping rule,
  the one-row inherited-item ledger plus the two Phase 62 body-level deferrals, three stubbed unit
  sections and a stubbed close-out — and `RU-64-03`'s D1, D2, D3 and D6 cells plus the D1 and D6 cells
  of all three JAR rows each carry `pass` or `fail` with a written check line, with
  `### Vendored Binary Provenance` stating shipping, provenance, pinning, reachability and the
  deliberate limits of the method, and `BBjCodeFomatter.jar`'s unidentifiability recorded as the D6
  finding it is.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-64-03 at evidence tier `trace` — D4, D5, D8 — and close the unit against the four-part stopping rule</name>
  <files>.planning/reviews/64-COVERAGE.md</files>
  <precondition>`## RU-64-03`'s D1, D2, D3 and D6 unit-row cells and the D1/D6 cells of all three JAR rows
  already carry verdicts — Task 1 committed — and the phase-wide verdict count is exactly 10 with 17
  placeholders remaining. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/64-COVERAGE.md` §`## RU-64-03` — the section this task completes, including
      the `### Vendored Binary Provenance` text Task 1 wrote, plus
      §`## Stopping Rule & Write Contract`
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot", §"Test & Build Baseline" →
      `### Headline vitest counts`, `### Every failing test` and `### Every skipped test, grouped by
      file`, and §"Routing table (D-06)" — which sends the 11 `linking.test.ts` failures and the 3
      disabled `parser.test.ts` assertions to **Phase 61**, not here
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-05, RVW-06, RVW-07
    - `.planning/phases/64-build-ci-dependency-review/64-CONTEXT.md` — D-15 (what D5 means in this
      phase and what it must not re-record) and D-18 (`RU-D8-01` is not this phase's)
    - `.planning/STATE.md` §"Active Constraints" — the 11 known-failing tests and the 3 disabled
      `parser.test.ts` assertions, both already owned elsewhere
    - **Review targets:** the same four readable files as Task 1, re-read only where a specific claim
      needs a line anchor
    - **Test-surface enumeration, run rather than assumed:** whether any test in the repository
      exercises `tools/interop-test-harness/run-tests.ts` or the three `.bbj` scripts, and how the
      harness is invoked (a named grep over `bbj-vscode/package.json` scripts and the test tree, with
      the command and its literal output recorded)
    - `bbj-vscode/vitest.config.ts` and `bbj-vscode/package.json` — read as **context only** for the
      D5 claim about what the suite covers; both belong to `RU-64-02` and are swept in plan `64-03`,
      so no finding here may be located in either
    - `CLAUDE.md` (repo root) — its Build & Test Commands section names `npm run test:bbj` and the
      BBj-side tests, and its IDE-integration section names `web.bbj` and `em-login.bbj` as shared
      run tools; both are checkable against the files just read. **Read as reference only** — it is
      `RU-D8-01`'s file and no `P64-*` finding may be located in it (D-18)
    - **Do NOT read** any file under `.planning/codebase/`
  </read_first>
  <action>
Complete `## RU-64-03` by recording its D4, D5 and D8 unit-row cell lines, then close the unit
against the four-part stopping rule. Enumerate the dimensions explicitly: this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. The three JAR rows are `n/a — R-JAR-BINARY` for all three of these dimensions and were
completed in the skeleton; do not touch them. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks:
`run-tests.ts` at 1,058 lines is the largest readable file in this phase — establish mechanically
where its bulk sits (a named count of functions and their line spans, not an eyeball impression) and
say whether any single function has crossed into god-function territory, whether test cases are
expressed as data or as copy-pasted blocks, and whether setup/teardown is factored once or repeated;
whether the three `.bbj` scripts share a connection, argument-parsing or error-reporting shape that
is copy-pasted rather than factored, and whether any endpoint, path or constant is duplicated across
them; whether anything in the four files is dead — unreferenced helpers, unreachable branches,
commented-out blocks; and whether the `tools/` directory layout itself couples the formatter JARs to
a hardcoded relative path that any consumer must reproduce. Use a mechanical basis wherever one
exists — a named grep or a line-anchored comparison.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **In this phase D5 means something
specific (D-15): not "is this file covered by a test" in the abstract, but whether the test surface
this unit owns is real and reachable.** For `RU-64-03` that is: whether
`tools/interop-test-harness/run-tests.ts` — a test harness that is itself 1,058 lines of untested
code — is exercised by anything in the vitest suite or only ever run by hand; how it is invoked
(which script, which environment variable, which precondition) and whether that invocation path is
documented and reachable in this checkout; and whether the three `.bbj` scripts have any regression
surface at all. Establish each by enumeration with the command and its literal output recorded, not
by assumption. **Do not re-record the 11 known-failing `linking.test.ts` tests or the 3 disabled
`parser.test.ts` assertions** — INVENTORY's routing table sends both to Phase 61 and DEBT-02 owns
the latter; where this unit's D5 sweep touches them, cross-reference by finding ID or by requirement
ID and add only what is new here. State that cross-reference explicitly rather than leaving the
overlap unexplained.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: every header comment,
function comment and inline explanation in the four readable files against what the code beneath it
actually does — in particular any comment claiming a validation, a default, an ordering or a
precondition; whether `run-tests.ts`'s own usage/help text matches its actual arguments and
behaviour; and `CLAUDE.md`'s two claims that touch this unit — that `web.bbj` and `em-login.bbj` are
shared run tools consumed by both IDEs, and that `npm run test:bbj` runs the BBj-side tests needing
java-interop on port 5008 — checked against the files just read. **Where the code is the stale side,
the finding is located in the code file. Where `CLAUDE.md` is the stale side, it is recorded as a
written observation naming `RU-D8-01` as the row that owns it and stating that no Phase 64 plan may
allocate a finding there (D-18) — never as a `P64-*` finding located in `CLAUDE.md`.** Where
`CLAUDE.md` is simply silent about a surface, note the silence as an observation rather than
promoting it to a finding; silence contradicts nothing. A D8 fix that changes only comment text
changes no runtime behaviour, so it may be `easy` if the other five INVENTORY 3c tests pass; the
record states which reading was applied and why.

**Then close the unit** by writing `### Unit closure`, making the four-part closure visible in the
section text: confirm that all 7 live unit-row cells and all 6 live file-exception cells carry a
verdict plus a written check line; that all seven files — the four readable and the three JARs — are
named at least once inside `## RU-64-03`, in a check line or a finding `location:`; that every
candidate claim raised during either task is either a finding record or a written
`### Not-reproducible dispositions` entry with its reason; and that **both** Phase 62 body-level
deferrals addressed to this unit carry a written disposition, naming them by their
`62-COVERAGE.md` line anchors. If the sweep produced no not-reproducible candidates and no
cross-unit referrals, state those sub-blocks as explicitly empty rather than omitting them. Finally,
state in the section text that this unit's D7 cell and all three JAR rows' D7 cells are `n/a` under
`R-D7-CI` and that no parity work of any kind was performed here, so a reader does not read the
absence of a parity claim as an oversight.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier unit-row cells are recorded: with the section extracted by `sed -n '/^## RU-64-03 /,/^## RU-64-01 /p' .planning/reviews/64-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The unit's `n/a` cell and its three file-exception rows are intact: within the extracted section, `grep -cE '^- D7 .* — n/a — '` prints `1`, `grep -cE '^- \[file-exception\] '` prints `24`, and `grep -cE '^- \[file-exception\] .* — (pass|fail) — '` prints `6`
    - The already-owned test debt is cross-referenced rather than re-recorded: the extracted section contains `DEBT-02` and contains `RU-61-06`
    - The unit is closed with its four parts visible: the extracted section contains `### Unit closure`, `62-COVERAGE.md:1489`, `62-COVERAGE.md:1833` and `R-D7-CI`
    - `RU-D8-01`'s boundary is respected and stated: the extracted section contains `RU-D8-01`, and phase-wide the counts of `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - All five per-unit sub-blocks exist in the section: `### Vendored Binary Provenance`, `### File-exception cells`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear
    - Phase-wide counts prove no other unit section was written: `grep -cE '^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — '` prints `13` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — pending$'` prints `14`
    - The grid is unchanged in size and all 29 `n/a` carry-forwards are intact: `grep -cE '^- (D[1-8]|\[file-exception\]) '` prints `56` and `grep -cE '^- (D[1-8]|\[file-exception\]) .* — n/a — '` prints `29`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- (D[1-8]|\[file-exception\]) .* — pass — ` equals the count of the same pattern with `.{120,}` appended, and the count of `^- (D[1-8]|\[file-exception\]) .* — fail — ` equals the count of `^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no `P64-D7-` ID, no blank `dedup:`, every required field count equals the `^id:` count, and the `^triage:` count equals the `^dimension:[[:space:]]+D6` count
    - No finding is anchored in a tree owned by a closed phase: the counts of `^location:[[:space:]]+bbj-vscode/src/` and `^location:[[:space:]]+bbj-intellij/src/` each print `0`
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/64-COVERAGE.md; sed -n "/^## RU-64-03 /,/^## RU-64-01 /p" "$f" > /tmp/p64ru03.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p64ru03.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p64ru03.txt)" = 7; test "$(grep -cE "^- D7 .* — n/a — " /tmp/p64ru03.txt)" = 1; test "$(grep -cE "^- \[file-exception\] " /tmp/p64ru03.txt)" = 24; test "$(grep -cE "^- \[file-exception\] .* — (pass|fail) — " /tmp/p64ru03.txt)" = 6; for s in "DEBT-02" "RU-61-06" "### Unit closure" "62-COVERAGE.md:1489" "62-COVERAGE.md:1833" "R-D7-CI" "RU-D8-01" "### Vendored Binary Provenance" "### File-exception cells" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -qF "$s" /tmp/p64ru03.txt; done; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — (pass|fail) — " "$f")" = 13; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pending$" "$f")" = 14; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) " "$f")" = 56; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — n/a — " "$f")" = 29; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — " "$f")" = "$(grep -cE "^- (D[1-8]|\[file-exception\]) .* — fail — .*P64-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P64-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^id:[[:space:]]+P64-D7-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^triage:" "$f")" = "$(grep -cE "^dimension:[[:space:]]+D6" "$f")"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; for p in "bbj-vscode/src/" "bbj-intellij/src/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f")" = 0; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md)"; echo P64_01_RU03_COMPLETE'</automated>
  </verify>
  <done>`RU-64-03` is complete under the four-part stopping rule: all 7 live unit-row cells and all 6 live
  JAR file-exception cells carry a verdict plus a written check line, all seven files are named inside the
  section, the already-owned test debt is cross-referenced by ID rather than re-recorded, `CLAUDE.md`
  staleness is routed to `RU-D8-01` as an observation rather than allocated as a Phase 64 finding, both
  Phase 62 body-level deferrals carry a written disposition, and the `R-D7-CI` non-parity fact is stated
  so its absence does not read as an oversight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| three vendored JAR binaries → the published `.vsix` → `java -jar` on a user's machine (*subject-matter*) | `tools/` is not excluded by `.vscodeignore`, and `document-formatter.ts:10` executes `BBjCFCli.jar` with the user's document content. |
| `BBjCFCli.jar`'s `Class-Path` → both `lib/` JARs (*subject-matter*) | Executing the CLI loads all three artifacts; two of them are reachable only through the first. |
| an unidentifiable binary → any vulnerability-triage process (*subject-matter*) | `BBjCodeFomatter.jar`'s manifest carries no version, vendor, date or source, so no scanner and no reviewer can triage it. |
| Enterprise Manager credentials and tokens → `em-login.bbj` / `em-validate-token.bbj` (*subject-matter*) | Token acquisition and validation cross a trust boundary this unit owns one leg of; Phase 65's SEC-04 owns the lifecycle. |
| the interop test harness → spawned processes, the filesystem and the java-interop socket (*subject-matter*) | `run-tests.ts` is 1,058 lines of untested code that starts, reads and connects. |
| `64-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed supply-chain record is world-readable on landing and git history preserves it. |
| code and manifests read during the sweep → the committed record | Credentials, tokens or absolute developer-machine paths encountered while reading can be copied verbatim into a record. |
| a written check line → a reader who cannot re-run the check | Phase 68's DOC-03 and Phase 67's apply path consume `pass` verdicts as evidence. |
| `bbj-vscode/src/`, `CLAUDE.md` and `documentation/` → this phase's finding namespace | Read as context only; a `location:` inside any of them would double-count a closed phase's cell or claim `RU-D8-01`'s unowned row. |
| recorded `classification:` and `triage:` → Phase 67's apply path and Phase 68's document split | An `easy`/`fix-now` pair routes a change past `MAJOR-REFACTORS.md` review. |
| shared `64-COVERAGE.md` → the other two plans | One file, three writers. |
| the reviewed working tree → any command this plan runs | A remediation or install command would silently change the artifact under review mid-sweep. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-P01-S1 | Tampering | the three vendored JARs shipped inside the published extension and executed on the formatter path | high | transfer | Task 1's D1 checks and `### Vendored Binary Provenance` facts (1), (3) and (4) state what ships, what pins it, what verifies it and what executes it, against `.vscodeignore`, the manifests and the single call site; remediation transfers to Phase 67, disclosure to Phase 69. |
| T-64-P01-S2 | Spoofing | `BBjCodeFomatter.jar`, whose manifest carries no identifying field at all | high | transfer | Task 1's D6 cell records the unidentifiability itself as the finding with `triage: file-issue`, and provenance fact (2) records the SHA-256 as the only stable identifier available. |
| T-64-P01-S3 | Information Disclosure | EM credentials and tokens handled by `em-login.bbj` and `em-validate-token.bbj` | high | transfer | Task 1's D1 check asks where the token comes from, whether it reaches a process argument, a file, stdout or a log, and what happens on failure; the end-to-end lifecycle transfers to Phase 65's SEC-04 with the boundary stated. |
| T-64-P01-01 | Information Disclosure | a supply-chain finding written as a working exploitation path against a shipped artifact, committed to a public repository | high | mitigate | D-16's two-tier rule applied strictly, with the exact phrase `Disclosure-limited per D-16` opening any redacted `evidence:` field so the redaction is auditable rather than indistinguishable from thin evidence. Enforced as a `must_haves` prohibition. |
| T-64-P01-02 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | Prohibition plus acceptance criteria requiring every `pass` cell line — unit-row and file-exception alike — to be at least 120 bytes of written checks naming concrete anchors; RVW-06's drop-vs-disposition rule sends unclearing claims to `### Not-reproducible dispositions`. |
| T-64-P01-03 | Repudiation | the four file-exception rows omitted or folded into their parent unit rows, silently under-counting the slice | high | mitigate | D-17/D-18.1 prohibition plus acceptance criteria counting unit-row and file-exception cell lines **separately** (24 / 32 / 56) and asserting the INVENTORY re-derivation prints a leading row count of `7`, not `3`. |
| T-64-P01-04 | Tampering | a command that mutates the reviewed tree — a dependency install, a lockfile regeneration or an automated remediation — run during the sweep | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty across `bbj-vscode`, `bbj-intellij`, `java-interop` and `.github` at task end. |
| T-64-P01-05 | Elevation of Privilege | `classification:`/`triage:` on a D1 or D6 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; D-09's mapping is stated per record and an acceptance criterion asserts every D6 finding carries `triage:`. |
| T-64-P01-06 | Information Disclosure | a credential, EM token or absolute developer-machine path copied into a record while reading the tool scripts | high | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value and no absolute path is written into any field. |
| T-64-P01-07 | Tampering | editing `INVENTORY.md` or a closed phase's coverage file so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for INVENTORY and all three prior coverage files; D-18 makes a disagreement a defect to surface rather than a number to adopt. |
| T-64-P01-08 | Tampering | a finding relocated into `bbj-vscode/src/`, `CLAUDE.md`, `VERBs.md` or `documentation/` to dodge a boundary | medium | mitigate | D-18 prohibitions plus anchored acceptance greps asserting zero `^location:` lines resolve into any of the five forbidden prefixes. |
| T-64-P01-09 | Repudiation | a `P64-D7-*` ID issued against a dimension that is `n/a` in all 7 rows | medium | mitigate | D-14 prohibition plus an acceptance criterion asserting `grep -cE '^id:[[:space:]]+P64-D7-'` prints `0`. |
| T-64-P01-10 | Tampering | three plans writing one `64-COVERAGE.md` | medium | mitigate | Wave 1 with an empty `depends_on` and a structural wave chain thereafter (D-04); both tasks assert exact global counts (10/17, then 13/14) plus the invariant 29 `n/a` lines and 56 total cell lines. |
| T-64-P01-11 | Repudiation | a claim asserting a JAR behaviour that could only be confirmed by executing or decompiling it | medium | mitigate | D-11 prohibition: manifest and hash only. The deferred `-i`-versus-stdin question is dispositioned as not-reproducible with that reason rather than answered by assertion. |
| T-64-P01-12 | Denial of Service | the D6 cells expanding into the npm/Gradle audit that `RU-64-02` owns | low | accept | D-02's ordering bounds this plan to the vendored-binary half; `64-03` owns the trees. Accepted because the boundary is stated in the cell text and in provenance fact (3). |
| T-64-P01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed`, `awk`, `unzip -p` and `sha256sum` only, so INVENTORY's Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `.planning/reviews/64-COVERAGE.md` exists, records the swept-tree SHA obtained at execution time,
  and carries exactly 24 unit-row cell lines plus 32 file-exception cell lines — 56 total, 29 of
  them `n/a` under four verbatim markers, with the `R-D6-CENTRAL` non-carry stated as a fact.
- The cell gate re-derives `7 27 29 56` live from INVENTORY, leading row count included.
- The file gate states 28 with `.github/dependabot.yml` named and justified at the point of the
  count, and the enumeration command independently prints 28.
- `## RU-64-03`'s 7 live unit-row cells and all 6 live JAR file-exception cells carry `pass` or
  `fail` plus a written check line of at least 120 bytes; its D7 cell and all three JAR D7 cells
  carry `R-D7-CI` verbatim.
- `### Vendored Binary Provenance` names `Manifest-Version`, `Class-Path`, `Bnd-LastModified`,
  a SHA-256, `.vscodeignore` and `document-formatter.ts`.
- Both Phase 62 body-level deferrals (`62-COVERAGE.md:1489`, `:1833`) carry written dispositions.
- Every D6 finding carries `triage:`; the `^triage:` count equals the `^dimension: D6` count.
- Phase-wide verdict count is 13 and placeholder count is 14; 13 + 14 + 29 = 56.
- Every finding has all 13 template fields, a unique well-formed non-`P00-`, non-`P64-D7-` ID and a
  non-blank `dedup:`.
- Zero `^location:` lines resolve into `bbj-vscode/src/`, `bbj-intellij/src/`, `CLAUDE.md`,
  `VERBs.md` or `documentation/`.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- The recording shape is proven on real content and frozen: plans `64-02` and `64-03` append into a
  skeleton that already carries both completion gates, all four `n/a` markers, the inherited-item
  ledger and three stubbed unit sections — and no format checkpoint is spent (D-01, D-05).
- The four file-exception rows are present in the grid from the first commit of the file, three of
  them recorded cell-by-cell here, so the failure mode D-17 names cannot occur silently later.
- SEC-08's vendored-binary half is discharged: what ships, from where, verified how, wired to what,
  and — for the one artifact nothing identifies — that the unidentifiability is itself the finding.
- RVW-05 advances: 7 of the phase's 28 files have a recorded pass/fail against every dimension that
  applies to them, and 13 of the 27 live cells are filled.
- ROADMAP Phase 64 criteria 1 and 4 hold for every record written here, and criterion 3's
  vendored-binary contribution is in place for `64-03` to consolidate by reference.
</success_criteria>

<output>
Create `.planning/phases/64-build-ci-dependency-review/64-01-SUMMARY.md` when done.
</output>
