---
phase: 97-release-0-16-0-milestone-close
plan: 05
type: execute
wave: 3
depends_on: ["97-01", "97-02", "97-03", "97-04"]
files_modified:
  - .planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md
autonomous: false
requirements: [REL-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The whole IntelliJ JUnit suite runs with `--rerun-tasks` and is green, and the whole Vitest suite reports `numFailedTests: 0` with `RUN_BBJ_TESTS=0`, before the landing PR is allowed to open (D-01 step 1's gate)."
    - "No planning identifier (plan number, `D-xx`, `C-xx`, `CR-xx`, or a requirement id such as `COMP-`/`PLAT-`/`REL-`) appears in the source or test diff against `origin/main` (D-03)."
    - "Both distributables are built from the final code-wave tree and their sha256, byte size and source commit are recorded before the hand UAT is requested (D-07)."
    - "A maintainer has killed the language-server process (or dropped the connection) in a running IDE and confirmed the crash banner and restart behaviour, against those exact artifacts (D-07)."
    - statement: "If a code-review fix lands after the UAT, both distributables are rebuilt from the final tree and the UAT record names the rebuilt artifacts."
      verification: backstop
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md
  key_links:
    - "`bbj-vscode/out/language/main.cjs` -> `bbj-intellij`'s `verifyLanguageServerBundle` task, which fails fast if the bundle is missing — the VS Code build must run before the IntelliJ build."
    - "The recorded sha256 of each distributable -> the UAT verdict sentence, so the verdict names the bytes it was given."
  prohibitions:
    - "No `git push`, `gh pr create` or any outward-facing command in this plan — the landing PR is plan 97-06's work, behind its own checkpoint."
    - "No `/gsd-ship`, no `windows waive` — WINDOWS.md entry 1 stays open and untouched (D-05)."
    - "No git branch creation, switching, renaming or `git stash`; no `git add -A` or `git add .`."
    - "No whole-suite failure may be waved through on a 'known flaky' assertion without naming it against the recorded baseline."
---

<objective>
Close the code wave: prove both suites green on the final tree, prove the diff carries no planning
identifiers, build both distributables from that tree and record their identity, then get the
maintainer's hand UAT of the crash-detection rework in a running IDE.

Purpose: D-01 fixes this as step 1's gate — the landing PR does not open until the folded-todo code
wave is complete, verified and hand-UAT'd. D-07 sets the bar: plain-JUnit seam tests, a whole-file
source guard, an LSP4IJ coupling canary (all delivered by 97-01/97-02/97-04) **and** a maintainer
hand UAT.
Output: a green two-suite gate, a clean register check, two identified artifacts, one recorded UAT
verdict.

## Artifacts this phase produces (plan 97-05)

- `.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md` — the artifact-identity and hand-UAT record (sha256, byte size, source commit, verdict, verbatim maintainer reply).
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-VALIDATION.md
@.planning/phases/97-release-0-16-0-milestone-close/97-01-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-02-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-04-SUMMARY.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`; `cd` only
  directly in front of a build tool. Use `git -C /home/coder/repos/bbj-language-server …`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.

**Recorded baselines the executor must judge against, not re-derive.**
- The IntelliJ whole-suite gate runs with `--rerun-tasks`; without it Gradle can report an
  UP-TO-DATE `:test` and mask a stale green (recorded standing practice from phases 94 and 96).
- The Vitest whole-suite verdict is `numFailedTests: 0` with `RUN_BBJ_TESTS=0` and `--maxWorkers=2`,
  run with cwd = `bbj-vscode`. Suites reported as "failed" while `numFailedTests` is 0 are
  `initializeWorkspace` `beforeAll` timeouts from worker contention, not regressions — judge on
  `numFailedTests`.
- With `RUN_BBJ_TESTS` unset and a live service on `:5008`, the documented local baseline is 12
  failures (11 in `linking.test.ts` plus 1 issue447 capability test). Plan 97-03 fixed the issue447
  one and either fixed or re-filed the eleven. Read `97-03-SUMMARY.md` first and state the expected
  number before running.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-suite gate and the register check on the final code-wave tree</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md (which linking branch was taken — it sets the expected failure count)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-VALIDATION.md (§ Sampling Rate — the after-the-code-wave contract this task satisfies)
    - /home/coder/repos/bbj-language-server/.planning/DEBT.md (the documented local baseline)
  </read_first>
  <action>
Run both whole suites on the current tree and record the numbers, then run the register check.

1. IntelliJ: `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks`.
   Record the test count and the result. A red run stops the plan — report it, do not re-run hoping
   for a different answer.
2. Vitest: `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`.
   Judge on `numFailedTests`. If any suite is reported failed while `numFailedTests` is 0, name those
   suites in the record and state that they are `beforeAll` contention timeouts, not regressions.
3. Register check of the source/test diff against `origin/main` (this is the standing per-commit
   check, applied once here over the whole code wave): run the diff restricted to
   `bbj-intellij bbj-vscode documentation .github` and grep it for planning-identifier tokens —
   plan numbers, `D-` followed by digits, `C-`/`CR-` followed by digits, and requirement prefixes
   `COMP-`, `PLAT-`, `EM-`, `IOP-`, `REL-`. GitHub issue numbers (`#123`) are fine and must not be
   flagged. If the grep finds anything, fix the offending comment in place, re-run, and record what
   was found.

Start `.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md` with a "Suite gate"
section holding: the two commands as run, their observed counts, the expected-vs-observed statement
for the Vitest baseline, and the register-check result.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>the run summary reports a non-zero failed-test count (a failed *suite* with zero failed tests is a contention timeout and is not a failure here)</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff origin/main...HEAD -- bbj-intellij bbj-vscode documentation .github | grep -nE '(^\+.*)(\b(D|C|CR)-[0-9]+\b|\b(COMP|PLAT|EM|IOP|REL)-[0-9]+\b|\b9[0-7]-[0-9]{2}\b)'</automated>
    <fails_when>any line is printed — an added source or test line carries a planning identifier, which must be removed before the PR opens</fails_when>
  </verify>
  <acceptance_criteria>
    - The IntelliJ suite reports BUILD SUCCESSFUL under `--rerun-tasks`.
    - The Vitest run reports `numFailedTests: 0`; any "failed suite" lines are named and classified as contention timeouts in the record.
    - The register grep prints nothing, or the offending comments were fixed and the re-run prints nothing.
    - `97-UAT-ARTIFACTS.md` exists and contains a "Suite gate" section with both commands, both observed counts and the register-check result.
  </acceptance_criteria>
  <done>Both suites are green on the final code-wave tree and the diff is free of planning identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Build both distributables from the final tree and record their identity</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/build.gradle.kts (the `verifyLanguageServerBundle` task — it fails fast when `bbj-vscode/out/language/main.cjs` is absent, so the VS Code build must run first)
    - /home/coder/repos/bbj-language-server/bbj-vscode/package.json (the `build` script and the current version string, which becomes part of the artifact names)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-PATTERNS.md § "96-08 UAT-record shape" (the verdict + artifact identity sentence shape to copy)
  </read_first>
  <action>
Build in this order — the IntelliJ build fails fast without the language-server bundle:

1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build`, then package the VSIX with
   the pinned `vsce` from devDependencies (`npx vsce package`). If packaging trips a dependency
   resolution problem in this workspace, add `--no-dependencies` and record that you did.
2. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin` — the zip lands
   under `bbj-intellij/build/distributions/`.

Then record, in `97-UAT-ARTIFACTS.md` under an "Artifacts under test" section, for **each** of the
two files: the exact filename, the sha256, the byte size, and the source commit
(`git -C /home/coder/repos/bbj-language-server rev-parse HEAD`). Use the sentence shape this project
already uses for UAT records: verdict tied to an artefact identity (name, sha256, size) and a source
commit, with the counterpart artefact named in the same sentence.

Do not install anything into the maintainer's IDEs yourself and do not publish anything. State in
the record where both files are on disk, with absolute paths, so the maintainer can install them.

Record, in the same section, the standing rebuild rule: if a code-review fix lands after this
build, both distributables are rebuilt from the final tree and this section is re-filled with the
new hashes before the UAT verdict is considered current.
  </action>
  <verify>
    <automated>ls -1 /home/coder/repos/bbj-language-server/bbj-vscode/*.vsix /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-*.zip</automated>
    <fails_when>non-zero exit, or fewer than two paths printed — one of the distributables was not produced</fails_when>
    <automated>grep -cE '^[0-9a-f]{64}' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</automated>
    <fails_when>the printed count is fewer than 2 — the record does not carry a sha256 line for each artifact</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one `.vsix` exists under `bbj-vscode/` and exactly one `bbj-intellij-*.zip` under `bbj-intellij/build/distributions/`.
    - `97-UAT-ARTIFACTS.md` names both files with their sha256, byte size and the same source commit SHA.
    - The record names the absolute on-disk path of each artifact and states the rebuild-after-code-review rule.
    - No publish, push or install command was run.
  </acceptance_criteria>
  <done>Both distributables exist, are identified by hash, and are traceable to one source commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Maintainer hand UAT of the crash-detection rework</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md (as filled by tasks 1 and 2 — the maintainer installs exactly these files)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-07 — what this UAT must cover, and that no Windows re-attestation is required)
  </read_first>
  <what-built>Both distributables built from the final code-wave tree and recorded by sha256, byte size and source commit in `97-UAT-ARTIFACTS.md`: the VS Code `.vsix` under `/home/coder/repos/bbj-language-server/bbj-vscode/` and the IntelliJ plugin zip under `/home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/`. They carry the crash-detection status-feed move, the corrected classifier from-state, the BBjCPL-availability no-op handler and the Node.js progress-indicator fix.</what-built>
  <how-to-verify>
1. Install the recorded `bbj-intellij-<version>.zip` via **Settings > Plugins > gear > Install Plugin from Disk...** and restart the IDE. Install the `.vsix` in VS Code too if you want that side exercised in the same pass; the behaviour under test is IntelliJ-side.
2. Open a `.bbj` file from `examples/` and wait until the status-bar widget shows the server started.
3. Kill the language server from outside the IDE — terminate the `out/language/main.cjs` process on Linux/macOS, or the matching `node` process on Windows — or drop the connection the same way the original Windows session did.
4. Expected: a crash banner appears, the status widget changes, and the server auto-restarts on the first kill. Kill it a second time within 30 seconds — expected: auto-restart stops and the crash notification appears with its **Show Log** and **Restart** actions.
5. Open `Help > Show Log in Files`. Expected: no `Unsupported notification method: bbj/bbjcplAvailability` warning from this session, and status transition lines that name a plausible immediately-preceding state.
6. Optional, if a Node.js download is convenient to trigger: expected no `IllegalStateException` for `setFraction` during the download.

A "the banner never appeared" result is a blocking finding — report it and the landing PR does not open.
  </how-to-verify>
  <resume-signal>Reply with pass/fail per step, or "approved" if every expectation held. Any failure text is recorded verbatim.</resume-signal>
  <action>
Ask the maintainer to run the hand UAT, then record the verdict. No later plan starts until this
checkpoint passes (D-01).

Present these steps, numbered, naming the absolute artifact paths and their sha256 from the record:

1. Install `bbj-intellij-<version>.zip` in the usual IDE via **Settings > Plugins > gear > Install
   Plugin from Disk...**, and restart the IDE. (Install the `.vsix` in VS Code too if the
   maintainer wants the VS Code side exercised in the same pass — the crash behaviour under test is
   IntelliJ-side.)
2. Open a `.bbj` file from `examples/` and wait for the language server to reach a started state
   (status-bar widget).
3. Kill the language-server process from outside the IDE — on Linux/macOS find the
   `out/language/main.cjs` process and terminate it; on Windows terminate the matching `node`
   process. Alternatively drop the connection the same way the original Windows debug session did.
4. Observe and report: does the crash banner appear? Does the status widget change? Does the server
   auto-restart on the first kill, and does a second kill within 30 seconds stop auto-restarting and
   raise the crash notification with its **Show Log** and **Restart** actions?
5. Open `Help > Show Log in Files` and confirm there is **no** `Unsupported notification method:
   bbj/bbjcplAvailability` warning from this session, and that the status transition lines name a
   plausible from-state rather than a two-transitions-old one.
6. Optional, if a Node.js download is convenient to trigger: confirm no `IllegalStateException` for
   `setFraction` appears during the download.

Expected outcome: the crash banner appears on an unexpected stop, which is exactly what did not
happen before this phase. A "the banner never appeared" result is a blocking finding — report it and
stop; do not open the landing PR.

Record the maintainer's reply verbatim in `97-UAT-ARTIFACTS.md` under "Hand UAT verdict", tied to
both artifact sha256 values and the source commit, in the project's established verdict sentence
shape. If a code-review fix lands afterwards, rebuild both distributables and re-run this checkpoint
against the new hashes.
  </action>
  <verify>
    <human-check>The maintainer has installed the recorded IntelliJ zip, forced an unexpected server stop, and reported what the IDE did — including whether the crash banner appeared and whether the BBjCPL-availability warning is gone.</human-check>
  </verify>
  <acceptance_criteria>
    - `97-UAT-ARTIFACTS.md` contains a "Hand UAT verdict" section with the maintainer's verbatim reply.
    - The verdict sentence names both artifact filenames, both sha256 values and the source commit.
    - The reply explicitly covers: crash banner on an unexpected stop, auto-restart on the first crash, the stop-after-second-crash notification, and the absence of the unsupported-notification warning.
    - No `git push`, `gh pr create` or publish command has been run anywhere in this plan.
  </acceptance_criteria>
  <done>The crash-detection rework is confirmed to work in a real IDE against identified artifacts, and the code wave's gate (D-01 step 1) is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build output -> maintainer's IDE | Artifacts installed by hand must be the ones that were verified, identified by hash. |
| Planning workspace -> public git history | A planning identifier leaked into a source comment becomes permanent public history at the landing PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-14 | Tampering | UAT artifact identity | medium | mitigate | The verdict is recorded against sha256, byte size and source commit for both artifacts, and the record states the rebuild-after-code-review rule, so a verdict cannot silently attach to different bytes. |
| T-97-15 | Information Disclosure | Public source comments | high | mitigate | The register check greps the added lines of the whole diff against `origin/main` for planning identifiers and blocks on any hit — this runs before any push exists. |
| T-97-16 | Repudiation | Whole-suite green claim | medium | mitigate | `--rerun-tasks` forbids an UP-TO-DATE `:test` no-op; the Vitest verdict is `numFailedTests`, with contention timeouts named explicitly rather than waved through. |

No package-manager install occurs in this plan (`npm ci`/`npm install` are not run; the build uses
the existing lockfile-installed tree), so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- IntelliJ suite green under `--rerun-tasks`; Vitest `numFailedTests: 0` under `RUN_BBJ_TESTS=0 --maxWorkers=2`.
- Register grep over the added diff lines prints nothing.
- Both distributables exist with recorded sha256, size and source commit.
- The hand-UAT verdict is recorded verbatim against those hashes.
</verification>

<success_criteria>
- The code wave is provably complete, green and identifier-clean.
- A maintainer has seen the crash banner appear on an unexpected stop, in a real IDE.
- Nothing outward-facing has happened yet.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-05-SUMMARY.md` when done. Record both
suite counts, the register-check result, both artifact identities and the verbatim UAT verdict.
</output>
