---
phase: 97-release-0-16-0-milestone-close
plan: 05
type: execute
wave: 3
depends_on: ["97-01", "97-02", "97-03", "97-04"]
files_modified:
  - .planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md
autonomous: false
requirements: [REL-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The revert of the crash-detection status-feed move and the from-state change is provably complete: `BbjServerService.java`, `BbjLanguageServerFactory.java` and `ExpectedStopGuard.java` are byte-identical to their pre-code-wave state (D-22)."
    - "Both whole suites are re-run on the post-revert tree — the IntelliJ JUnit suite with `--rerun-tasks`, the Vitest suite reporting `numFailedTests: 0` with `RUN_BBJ_TESTS=0` — before the landing PR is allowed to open (D-01 step 1's gate as narrowed by D-22)."
    - "No planning identifier (plan number, `D-xx`, `C-xx`, `CR-xx`, or a requirement id such as `COMP-`/`PLAT-`/`REL-`) appears in the source or test diff against `origin/main` (D-03)."
    - "Both distributables are rebuilt from the post-revert tree, the IntelliJ one with `clean buildPlugin` so an UP-TO-DATE task cannot hand back a stale zip, and each archive's bundled `main.cjs` is proven byte-identical to `bbj-vscode/out/language/main.cjs` (D-22)."
    - "The rebuilt artifacts' sha256, byte size and source commit are recorded under a Round 2 heading in `97-UAT-ARTIFACTS.md`, leaving the Round 1 record intact as history (D-22)."
    - "A maintainer has installed the rebuilt IntelliJ zip, opened a `.bbj` file, seen the server reach a started state with working diagnostics and completion, and confirmed that session's `idea.log` carries no `Unsupported notification method: bbj/bbjcplAvailability` WARN (D-22's Round 2 hand check)."
    - statement: "If a code-review fix lands after the UAT, both distributables are rebuilt from the final tree and the UAT record names the rebuilt artifacts."
      verification: backstop
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md
  key_links:
    - "`bbj-vscode/out/language/main.cjs` -> `bbj-intellij`'s `verifyLanguageServerBundle` task, which fails fast if the bundle is missing — the VS Code build must run before the IntelliJ build."
    - "`bbj-vscode/out/language/main.cjs` -> the copy inside each archive (`bbj-intellij/lib/language-server/main.cjs` in the zip, `extension/out/language/main.cjs` in the VSIX); a byte comparison is what proves the archives carry the post-revert bundle."
    - "The recorded sha256 of each distributable -> the Round 2 verdict sentence, so the verdict names the bytes it was given."
  prohibitions:
    - "No `git push`, `gh pr create` or any outward-facing command in this plan — the landing PR is plan 97-06's work, behind its own checkpoint."
    - "No `/gsd-ship`, no `windows waive` — WINDOWS.md entry 1 stays open and untouched (D-05)."
    - "No git branch creation, switching, renaming or `git stash`; no `git add -A` or `git add .`."
    - "No whole-suite failure may be waved through on a 'known flaky' assertion without naming it against the recorded baseline."
    - "No source file may be modified in this plan — the revert stands as committed; this plan only measures, builds and records (D-22)."
    - "The Round 1 record in `97-UAT-ARTIFACTS.md` is not deleted, rewritten or softened; Round 2 is appended beneath it (D-22)."
    - "No expectation of a crash banner, an auto-restart or a status-log from-state may appear anywhere in this plan's asks or record — that work was reverted (D-22)."
---

<objective>
Close the code wave **on the post-revert tree**: prove the revert is complete, prove both suites
green, prove the diff carries no planning identifiers, rebuild both distributables from that tree
and prove each archive carries the freshly built language-server bundle, then get the maintainer's
short Round 2 hand check of what is actually left in 0.16.0.

Purpose: D-01 fixes this as step 1's gate — the landing PR does not open until the folded-todo code
wave is complete, verified and hand-checked. **D-22 replaces the original bar.** The Round 1 hand
UAT of the crash-detection rework failed (`97-UAT-ARTIFACTS.md` § Hand UAT verdict, Round 1): with
the full status feed a killed server arrives as `started -> stopping -> stopped`, which the
expected-stop classifier never treats as a crash, and status alone cannot separate a kill from
LSP4IJ's own deliberate stops. The maintainer pulled that work out of 0.16.0; it was reverted
(`8fe7cb72`, `a22b78ad`) and its two todo files stay pending. D-22 supersedes D-07 and D-08, so
this plan's gate is now: revert complete, both suites green on the post-revert tree, both
distributables rebuilt and proven fresh, and a short maintainer hand check that the server starts
and works with no `bbj/bbjcplAvailability` WARN.

**This plan is being re-run.** Its Tasks 1 and 2 already ran once, against the pre-revert tree, and
their docs commits (`25821695`, `a7adea54`) stand as history. Round 2 re-runs both tasks against the
post-revert tree and adds new commits; it appends to `97-UAT-ARTIFACTS.md` under a "Round 2"
heading and leaves every Round 1 section exactly as written.

Output: a proven-complete revert, a green two-suite gate, a clean register check, two freshly built
and identified artifacts, one recorded Round 2 verdict.

## What is actually left in this phase's code wave

After the revert the tree carries, from this phase, only:
- the no-op `bbj/bbjcplAvailability` `@JsonNotification` handler plus its source-guard assertion;
- `setIndeterminate(false)` before the first `setFraction` in `BbjNodeDownloader`, plus its
  `BbjNodeDownloaderSourceGuardTest` assertion;
- the rewritten `bbj-vscode/test/functional/issue447-real-interop.test.ts`;
- the already-green gradle-wrapper-hygiene fixture (bookkeeping only, no code change).

Nothing about crash detection, auto-restart, crash banners or status-log from-states is in the tree.

## Artifacts this phase produces (plan 97-05)

- `.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md` — the artifact-identity and hand-check record (sha256, byte size, source commit, verdict, verbatim maintainer reply), with Round 1 preserved and Round 2 appended.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-VALIDATION.md
@.planning/phases/97-release-0-16-0-milestone-close/97-01-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-02-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md
@.planning/phases/97-release-0-16-0-milestone-close/97-04-SUMMARY.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`; `cd` only
  directly in front of a build tool. Use `git -C /home/coder/repos/bbj-language-server …`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.

**Read `97-01-SUMMARY.md` and `97-02-SUMMARY.md` as history only.** They describe the
crash-detection status-feed move and the from-state change, which were reverted (`8fe7cb72`,
`a22b78ad`) after the Round 1 hand UAT failed. None of that work is in the tree and none of it ships
in 0.16.0 (D-22). Do not use those SUMMARYs to predict suite counts, artifact contents or what the
maintainer should be asked to check.

**Recorded baselines the executor must judge against, not re-derive.**
- The IntelliJ whole-suite gate runs with `--rerun-tasks`; without it Gradle can report an
  UP-TO-DATE `:test` and mask a stale green (recorded standing practice from phases 94 and 96).
- The Vitest whole-suite verdict is `numFailedTests: 0` with `RUN_BBJ_TESTS=0` and `--maxWorkers=2`,
  run with cwd = `bbj-vscode`. Suites reported as "failed" while `numFailedTests` is 0 are
  `initializeWorkspace` `beforeAll` timeouts from worker contention, not regressions — judge on
  `numFailedTests`.
- With `RUN_BBJ_TESTS` unset and a live service on `:5008`, the documented local baseline is 12
  failures (11 in `linking.test.ts` plus 1 issue447 capability test). Plan 97-03 fixed the issue447
  one and either fixed or re-filed the eleven. Read `97-03-SUMMARY.md` first and state the expected
  number before running.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Round 2): Revert assertion, two-suite gate and the register check on the post-revert tree</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-22 — what was pulled out of 0.16.0 and what the gate is now)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md (the Round 1 record — read it, append beneath it, change none of it)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md (which linking branch was taken — it sets the expected failure count)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-VALIDATION.md (§ Sampling Rate — the after-the-code-wave contract this task satisfies)
    - /home/coder/repos/bbj-language-server/.planning/DEBT.md (the documented local baseline)
  </read_first>
  <action>
This task already ran once against the pre-revert tree; its docs commit `25821695` stands as
history. Run it again against the current, post-revert tree and append the new numbers — do not edit
or delete anything already in the file under a Round 1 heading.

1. Assert the revert is complete before measuring anything. The three files the pulled-out work
   touched must be byte-identical to their pre-code-wave state at `bdc024dc` (D-22). If this diff is
   non-empty, stop and report: the tree still carries part of the reverted rework and every number
   below would describe the wrong code.
2. IntelliJ: `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks`.
   Record the test count and the result. A red run stops the plan — report it, do not re-run hoping
   for a different answer. Expect the suite to be smaller than Round 1's 1101: the reverted work's
   own tests went away with it.
3. Vitest: `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`.
   Judge on `numFailedTests`. If any suite is reported failed while `numFailedTests` is 0, name those
   suites in the record and state that they are `beforeAll` contention timeouts, not regressions.
   Round 1 saw exactly two such files; name whichever appear this time rather than copying that list.
4. Register check of the source/test diff against `origin/main` (this is the standing per-commit
   check, applied once here over the whole code wave): run the diff restricted to
   `bbj-intellij bbj-vscode documentation .github` and grep it for planning-identifier tokens —
   plan numbers, `D-` followed by digits, `C-`/`CR-` followed by digits, and requirement prefixes
   `COMP-`, `PLAT-`, `EM-`, `IOP-`, `REL-`. GitHub issue numbers (`#123`) are fine and must not be
   flagged. If the grep finds anything, fix the offending comment in place, re-run, and record what
   was found.

Append to `.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md` a top-level
"Round 2" section holding a "Suite gate" subsection with: the revert assertion and its result, the
two suite commands as run, their observed counts, the expected-vs-observed statement for the Vitest
baseline, and the register-check result. State in it that Round 1's numbers describe the pre-revert
tree and are kept only as history.
  </action>
  <verify>
    <automated>git -C /home/coder/repos/bbj-language-server diff --exit-code bdc024dc -- bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java bbj-intellij/src/main/java/com/basis/bbj/intellij/concurrency/ExpectedStopGuard.java</automated>
    <fails_when>non-zero exit — one of the three files still differs from its pre-code-wave state, so the revert of the pulled-out crash-detection work is incomplete</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks</automated>
    <fails_when>non-zero exit, or "BUILD FAILED" in the output</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>the run summary reports a non-zero failed-test count (a failed *suite* with zero failed tests is a contention timeout and is not a failure here)</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff origin/main...HEAD -- bbj-intellij bbj-vscode documentation .github | grep -nE '(^\+.*)(\b(D|C|CR)-[0-9]+\b|\b(COMP|PLAT|EM|IOP|REL)-[0-9]+\b|\b9[0-7]-[0-9]{2}\b)'</automated>
    <fails_when>any line is printed — an added source or test line carries a planning identifier, which must be removed before the PR opens</fails_when>
  </verify>
  <acceptance_criteria>
    - The three-file diff against `bdc024dc` exits 0, proving the pulled-out work is fully reverted.
    - The IntelliJ suite reports BUILD SUCCESSFUL under `--rerun-tasks`.
    - The Vitest run reports `numFailedTests: 0`; any "failed suite" lines are named and classified as contention timeouts in the record.
    - The register grep prints nothing, or the offending comments were fixed and the re-run prints nothing.
    - `97-UAT-ARTIFACTS.md` carries a "Round 2" section whose "Suite gate" subsection holds the revert assertion, both commands, both observed counts and the register-check result.
    - Every Round 1 section in `97-UAT-ARTIFACTS.md` is unchanged.
  </acceptance_criteria>
  <done>The revert is provably complete, both suites are green on the post-revert tree, and the diff is free of planning identifiers.</done>
</task>

<task type="auto">
  <name>Task 2 (Round 2): Rebuild both distributables from the post-revert tree and prove they are fresh</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-22 — why both artifacts must be rebuilt rather than reused)
    - /home/coder/repos/bbj-language-server/bbj-intellij/build.gradle.kts (the `verifyLanguageServerBundle` task — it fails fast when `bbj-vscode/out/language/main.cjs` is absent, so the VS Code build must run first; and the `copyTextMateBundle`/bundle tasks that place `main.cjs` inside the zip)
    - /home/coder/repos/bbj-language-server/bbj-vscode/package.json (the `build` script and the current version string, which becomes part of the artifact names)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-PATTERNS.md § "96-08 UAT-record shape" (the verdict + artifact identity sentence shape to copy)
  </read_first>
  <action>
The Round 1 artifacts were built from the pre-revert tree and must not be reused; both are rebuilt
from the current tree. Build in this order — the IntelliJ build fails fast without the
language-server bundle:

1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build`, then package the VSIX with
   the pinned `vsce` from devDependencies (`npx vsce package`). If packaging trips a dependency
   resolution problem in this workspace, add `--no-dependencies` and record that you did.
2. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin` — `clean`
   is mandatory here (D-22): without it Gradle can report the bundling tasks UP-TO-DATE and hand
   back the Round 1 zip, which carries the reverted code. The zip lands under
   `bbj-intellij/build/distributions/`.
3. Prove each archive carries the bundle that step 1 just produced, rather than a stale copy:
   extract `bbj-intellij/lib/language-server/main.cjs` from the zip and
   `extension/out/language/main.cjs` from the VSIX, and compare each byte-for-byte against
   `/home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs`. Both comparisons must
   report no difference. Record both commands and their results.

Then record, in `97-UAT-ARTIFACTS.md` under the "Round 2" section in an "Artifacts under test
(Round 2)" subsection, for **each** of the two files: the exact filename, the sha256, the byte size,
and the source commit (`git -C /home/coder/repos/bbj-language-server rev-parse HEAD`). Use the
sentence shape this project already uses for UAT records: verdict tied to an artefact identity
(name, sha256, size) and a source commit, with the counterpart artefact named in the same sentence.
State explicitly that these hashes supersede Round 1's for the purpose of the current verdict, and
leave Round 1's hashes in place as the record of what failed.

Do not install anything into the maintainer's IDEs yourself and do not publish anything. State in
the record where both files are on disk, with absolute paths, so the maintainer can install them.

Record, in the same subsection, the standing rebuild rule: if a code-review fix lands after this
build, both distributables are rebuilt from the final tree and this subsection is re-filled with the
new hashes before the hand-check verdict is considered current.
  </action>
  <verify>
    <automated>ls -1 /home/coder/repos/bbj-language-server/bbj-vscode/*.vsix /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-*.zip</automated>
    <fails_when>non-zero exit, or fewer than two paths printed — one of the distributables was not produced</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-*.zip bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs</automated>
    <fails_when>non-zero exit or any "differ" line — the IntelliJ zip carries a language-server bundle that is not the one just built, i.e. it is a stale artifact from before the revert</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-vscode/bbj-lang-*.vsix extension/out/language/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs</automated>
    <fails_when>non-zero exit or any "differ" line — the VSIX carries a language-server bundle that is not the one just built</fails_when>
    <automated>grep -cE '^[0-9a-f]{64}' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md</automated>
    <fails_when>the printed count is fewer than 4 — Round 1's two sha256 lines plus Round 2's two are not all present, so either a Round 1 line was deleted or a Round 2 artifact is unrecorded</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one `.vsix` exists under `bbj-vscode/` and exactly one `bbj-intellij-*.zip` under `bbj-intellij/build/distributions/`, both produced by this task's run.
    - Both `cmp` comparisons report no difference, proving neither archive is a stale UP-TO-DATE artifact.
    - The "Round 2" section names both files with their sha256, byte size and the same source commit SHA, and that commit is on the post-revert tree.
    - The record names the absolute on-disk path of each artifact and states the rebuild-after-code-review rule.
    - Round 1's artifact identities are still present in the file, unedited.
    - No publish, push or install command was run.
  </acceptance_criteria>
  <done>Both distributables are rebuilt from the post-revert tree, provably carry the freshly built bundle, and are identified by hash against one source commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (Round 2): Short maintainer hand check of what 0.16.0 actually ships</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-UAT-ARTIFACTS.md (the "Round 2" section as filled by tasks 1 and 2 — the maintainer installs exactly those files; Round 1 above it records why the crash-detection work is gone)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-22 — the narrowed gate this check satisfies, superseding D-07 and D-08)
  </read_first>
  <what-built>Both distributables rebuilt from the post-revert tree with `clean buildPlugin`, proven to carry the freshly built language-server bundle, and recorded by sha256, byte size and source commit under "Round 2" in `97-UAT-ARTIFACTS.md`: the VS Code `.vsix` under `/home/coder/repos/bbj-language-server/bbj-vscode/` and the IntelliJ plugin zip under `/home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/`. From this phase they carry exactly two IDE-visible changes — the no-op `bbj/bbjcplAvailability` handler and the Node.js download progress-indicator fix — plus test-only housekeeping. The crash-detection status-feed rework and the status-log from-state change were reverted after the Round 1 UAT failed and are **not** in these builds (D-22).</what-built>
  <how-to-verify>
1. Install the recorded `bbj-intellij-<version>.zip` via **Settings > Plugins > gear > Install Plugin from Disk...** and restart the IDE.
2. Open a `.bbj` file from `examples/`. Expected: the server reaches a started state, diagnostics appear for the file, and completion works.
3. Open `Help > Show Log in Files` and search this session's `idea.log`. Expected: no `Unsupported notification method: bbj/bbjcplAvailability` WARN line for this session (older dated lines from previous sessions do not count).
4. Optional, only if a Node.js download is convenient to trigger: expected no `IllegalStateException` for `setFraction` during the download.

Nothing about crash banners, auto-restart or status-log from-states is under test — that work is out of 0.16.0. A server that will not start, missing diagnostics or completion, or a `bbj/bbjcplAvailability` WARN in this session is a blocking finding: report it and the landing PR does not open.
  </how-to-verify>
  <resume-signal>Reply with pass/fail per step, or "approved" if every expectation held. Any failure text is recorded verbatim.</resume-signal>
  <action>
Ask the maintainer to run the short Round 2 hand check, then record the verdict. No later plan
starts until this checkpoint passes (D-01 step 1, as narrowed by D-22).

Say up front what changed since Round 1: the crash-detection rework and the status-log from-state
change failed their hand UAT and were pulled out of 0.16.0 by the maintainer's own decision; both
are reverted and both todo files stay pending. This check covers only what is left.

Present these steps, numbered, naming the absolute artifact paths and their Round 2 sha256 values
from the record:

1. Install `bbj-intellij-<version>.zip` in the usual IDE via **Settings > Plugins > gear > Install
   Plugin from Disk...**, and restart the IDE. (Install the `.vsix` in VS Code too if the
   maintainer wants that side exercised in the same pass.)
2. Open a `.bbj` file from `examples/` and confirm the language server reaches a started state, that
   diagnostics appear for the file, and that completion works.
3. Open `Help > Show Log in Files` and confirm this session's `idea.log` carries **no**
   `Unsupported notification method: bbj/bbjcplAvailability` WARN. Older dated lines from earlier
   sessions are expected and do not count.
4. Optional, if a Node.js download is convenient to trigger: confirm no `IllegalStateException` for
   `setFraction` appears during the download.

Do not ask about crash banners, auto-restart behaviour or status-log from-states — that work is not
in these builds. A server that will not start, missing diagnostics or completion, or a
`bbj/bbjcplAvailability` WARN from this session is a blocking finding: report it and stop; do not
open the landing PR.

Record the maintainer's reply verbatim in `97-UAT-ARTIFACTS.md` under "Round 2", in a
"Hand check verdict" subsection, tied to both Round 2 artifact sha256 values and the source commit,
in the project's established verdict sentence shape. Leave the Round 1 verdict above it untouched.
If a code-review fix lands afterwards, rebuild both distributables and re-run this checkpoint
against the new hashes.
  </action>
  <verify>
    <human-check>The maintainer has installed the rebuilt IntelliJ zip, opened a `.bbj` file, and reported whether the server started with working diagnostics and completion and whether this session's idea.log is free of the `bbj/bbjcplAvailability` WARN.</human-check>
  </verify>
  <acceptance_criteria>
    - `97-UAT-ARTIFACTS.md`'s "Round 2" section contains a "Hand check verdict" subsection with the maintainer's verbatim reply.
    - The verdict sentence names both Round 2 artifact filenames, both Round 2 sha256 values and the post-revert source commit.
    - The reply explicitly covers: the server reaching a started state with working diagnostics and completion, and the absence of the `bbj/bbjcplAvailability` WARN in that session.
    - No expectation about crash banners, auto-restart or status-log from-states was put to the maintainer, and none appears in the recorded verdict (D-22).
    - The Round 1 verdict section is still present and unedited.
    - No `git push`, `gh pr create` or publish command has been run anywhere in this plan.
  </acceptance_criteria>
  <done>A maintainer has confirmed, against identified post-revert artifacts, that 0.16.0's remaining IntelliJ changes behave, and the code wave's gate (D-01 step 1, narrowed by D-22) is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build output -> maintainer's IDE | Artifacts installed by hand must be the ones that were verified, identified by hash. |
| Planning workspace -> public git history | A planning identifier leaked into a source comment becomes permanent public history at the landing PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-14 | Tampering | UAT artifact identity | medium | mitigate | The verdict is recorded against sha256, byte size and source commit for both artifacts, and the record states the rebuild-after-code-review rule, so a verdict cannot silently attach to different bytes. |
| T-97-15 | Information Disclosure | Public source comments | high | mitigate | The register check greps the added lines of the whole diff against `origin/main` for planning identifiers and blocks on any hit — this runs before any push exists. |
| T-97-16 | Repudiation | Whole-suite green claim | medium | mitigate | `--rerun-tasks` forbids an UP-TO-DATE `:test` no-op; the Vitest verdict is `numFailedTests`, with contention timeouts named explicitly rather than waved through. |
| T-97-16a | Tampering | Rebuilt distributable freshness | high | mitigate | `clean buildPlugin` plus a byte comparison of each archive's bundled `main.cjs` against the freshly built `bbj-vscode/out/language/main.cjs` makes it impossible to hand the maintainer a pre-revert artifact (D-22). |
| T-97-16b | Repudiation | Reverted-work claim | high | mitigate | An exit-code diff of the three touched files against `bdc024dc` proves the pulled-out crash-detection work is wholly absent before any suite number or artifact hash is recorded (D-22). |

No package-manager install occurs in this plan (`npm ci`/`npm install` are not run; the build uses
the existing lockfile-installed tree), so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- The three-file diff against `bdc024dc` exits 0 — the pulled-out crash-detection work is fully reverted (D-22).
- IntelliJ suite green under `--rerun-tasks`; Vitest `numFailedTests: 0` under `RUN_BBJ_TESTS=0 --maxWorkers=2`, both on the post-revert tree.
- Register grep over the added diff lines prints nothing.
- Both distributables were rebuilt with `clean buildPlugin` (IntelliJ) and each archive's bundled `main.cjs` compares equal to the freshly built `bbj-vscode/out/language/main.cjs`.
- Round 2 records both artifact identities (sha256, size, source commit) beneath an intact Round 1.
- The Round 2 hand-check verdict is recorded verbatim against those Round 2 hashes.
</verification>

<success_criteria>
- The code wave, as it actually stands after the revert, is provably complete, green and identifier-clean.
- A maintainer has confirmed in a real IDE that the server starts and works and that the `bbj/bbjcplAvailability` WARN is gone — and was asked nothing about crash banners, auto-restart or status-log from-states, because that work is out of 0.16.0 (D-22).
- Round 1's failed verdict survives in the record as history.
- Nothing outward-facing has happened yet.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-05-SUMMARY.md` when done. Record the
revert assertion, both Round 2 suite counts, the register-check result, both Round 2 artifact
identities with their `cmp` freshness proofs, and the verbatim Round 2 hand-check verdict. Note that
Tasks 1 and 2 ran once before against the pre-revert tree (commits `25821695`, `a7adea54`) and that
Round 2 adds new commits rather than replacing those.
</output>
