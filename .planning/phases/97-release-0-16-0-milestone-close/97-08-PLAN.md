---
phase: 97-release-0-16-0-milestone-close
plan: 08
type: execute
wave: 6
depends_on: ["97-07"]
files_modified:
  - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md
  - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md
autonomous: false
requirements: [REL-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Before the release is dispatched, a written precondition report confirms each of the seven items: landing PR merged; Preview run green; hand check passed; `origin/main` HEAD is the expected SHA; `package.json` on `main` is a 0.15.x lower than 0.16.0 so the version validator passes; no `v0.16.0` tag or release exists; the runbook is written (D-11)."
    - "The maintainer dispatches Manual Release with version `0.16.0` from the Actions UI; Claude never runs the workflow-dispatch command (D-11)."
    - "One verification job runs green before anything is published, tagged or pushed, and no publish job starts ahead of it — read back from the run's own job graph, not assumed (ROADMAP criterion 1)."
    - "After the run, the released commit SHA, the workflow run id, the tag, and the sha256 and byte size of the `.vsix` and the `.zip` attached to the v0.16.0 GitHub Release are recorded (D-16)."
    - "If any job fails, the tiered failure rule applies and the runbook is followed; Claude reports and waits, performing no autonomous retry, not even for a plainly transient failure (D-12)."
    - statement: "A JetBrains upload accepted by the marketplace counts as live for this phase; the review queue is not waited on (D-14)."
      verification: backstop
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md
    - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md
  key_links:
    - "`origin/main` HEAD -> the commit the release run checks out -> the commit every closing comment cites."
    - "The run's `verify` job -> the `needs:` edges that make it the single gate for both publishes, the tag and the release."
    - "The release assets' sha256 -> the smoke verdict recorded in plan 97-10."
  prohibitions:
    - "Claude must never run `gh workflow run` or otherwise dispatch the release — the dispatch is the maintainer's act, from the Actions UI."
    - "No re-run, re-dispatch or retry of any failed job by Claude; report and wait."
    - "No version bump to paper over a half-release."
    - "No edit to any file under .github/workflows/."
    - "No `git stash`, no branch creation or switching, no `git add -A`."
  flagged_assumptions:
    - "Edge-coverage probe row for REL-01 came back `unclassified` (\"review manually\") and is surfaced rather than resolved: the release's edge behaviour is the partial-failure matrix, which plan 97-07's runbook enumerates as four named paths. If the maintainer sees a fifth failure shape during the run, it is a runbook gap to record, not a silent case."
---

<objective>
Get release 0.16.0 dispatched by the maintainer and record exactly what came out of it.

Purpose: D-01 step 4 and D-11. This is the first real exercise of the verify-before-publish gate, and
it is one-way — a version published to the VS Code Marketplace cannot be replaced or re-published
under the same number, and the JetBrains upload enters a review queue. Everything Claude does here is
confirm, watch and record; the button is pressed by a human.
Output: one precondition report, one dispatched run, one evidence record.

## Artifacts this phase produces (plan 97-08)

- `.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md` — the seven-item report with each item's command and observed output.
- `.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md` — run id, job conclusions, released commit SHA, tag, and per-asset sha256/size.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RESEARCH.md
@.planning/phases/97-release-0-16-0-milestone-close/97-LANDING-PR.md
@.planning/phases/97-release-0-16-0-milestone-close/97-PREVIEW-GATE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RECONCILIATION-RUNBOOK.md
@.github/workflows/manual-release.yml

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`. Use
  `git -C /home/coder/repos/bbj-language-server …`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.
- Never write a token value anywhere.

**The one hard rule of this plan.** Claude does not dispatch the release. No `gh workflow run`, no
API call that creates a workflow dispatch, under any circumstance — including "it obviously would
have worked" and "the maintainer is away". The dispatch happens in the Actions UI, by the
maintainer, after the precondition report.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the release precondition report</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-11's seven items, verbatim)
    - /home/coder/repos/bbj-language-server/.github/workflows/manual-release.yml (the version validator's two rules: the `x.y.0` shape and strictly-greater-than the current `package.json` value)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-LANDING-PR.md and 97-PREVIEW-GATE.md (the merge and preview facts to quote)
  </read_first>
  <action>
Write `97-RELEASE-PRECONDITIONS.md` as a checklist where every line carries the command that produced
its answer and the observed output — not a claim. Cover exactly these seven, in this order:

1. **Landing PR merged.** `gh pr view --repo BBx-Kitchen/bbj-language-server <number> --json
   number,state,mergedAt,mergeCommit` — quote state, mergedAt and the merge commit SHA.
2. **Preview run green.** `gh run view <run-id> --repo BBx-Kitchen/bbj-language-server --json
   conclusion,headSha,jobs` — quote the conclusion and the head SHA, and confirm it equals the merge
   commit.
3. **Maintainer hand check passed.** Quote the verbatim reply recorded in `97-PREVIEW-GATE.md`.
4. **`origin/main` HEAD is the expected SHA.** `git -C /home/coder/repos/bbj-language-server fetch
   origin` then `git -C … rev-parse origin/main` — quote it and state that it equals the merge
   commit, or explain what landed since (a preview version bump commit is expected and is fine; say
   so and name it).
5. **The version validator will pass.** `git -C /home/coder/repos/bbj-language-server show
   origin/main:bbj-vscode/package.json` — quote the version field, state that it is a 0.15.x and
   therefore strictly lower than 0.16.0, and that `0.16.0` matches the required `x.y.0` shape.
6. **No `v0.16.0` tag or release exists.** `git -C /home/coder/repos/bbj-language-server ls-remote
   --tags origin v0.16.0` (expected: no output) and `gh release view v0.16.0 --repo
   BBx-Kitchen/bbj-language-server` (expected: a not-found error). Quote both.
7. **The runbook is written.** Name the runbook path and list its five labelled recovery paths.

Close the report with the exact dispatch instruction for the maintainer: the Actions tab, the
**Manual Release** workflow, **Run workflow**, branch `main`, version input exactly `0.16.0`. Add a
one-line reminder that if anything after `verify` goes red, the runbook governs, nothing is retried
automatically, and the version is never bumped to paper over a half-release.

If any of the seven items does not hold, write it down as not holding and stop the plan — do not
present a partially-true report as a go.
  </action>
  <verify>
    <automated>git -C /home/coder/repos/bbj-language-server ls-remote --tags origin v0.16.0</automated>
    <fails_when>any line is printed — a v0.16.0 tag already exists on origin, so the release cannot be dispatched as planned</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server show origin/main:bbj-vscode/package.json | grep -m1 '"version"'</automated>
    <fails_when>the printed version is not a 0.15.x value — the validator's strictly-greater rule would then behave differently than the report claims</fails_when>
    <automated>grep -cE '^[0-9]\.' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md</automated>
    <fails_when>the printed count is fewer than 7 — the report does not carry all seven numbered precondition items</fails_when>
  </verify>
  <acceptance_criteria>
    - `97-RELEASE-PRECONDITIONS.md` contains all seven items, each with its command and the observed output quoted.
    - `git ls-remote --tags origin v0.16.0` produced no output and `gh release view v0.16.0` reported not found.
    - `origin/main:bbj-vscode/package.json` carries a 0.15.x version.
    - The report ends with the exact Actions-UI dispatch instruction and the version string `0.16.0`.
    - No workflow-dispatch command was run.
  </acceptance_criteria>
  <done>Every precondition is confirmed by an observed command output, written down where the maintainer can read it.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Maintainer decides and dispatches Manual Release 0.16.0 from the Actions UI</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-PRECONDITIONS.md (the report to present)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RECONCILIATION-RUNBOOK.md (what happens if a job goes red)
  </read_first>
  <decision>Dispatch Manual Release with version `0.16.0` now, or hold. This is the one-way door of the phase: the maintainer both decides and performs it, from the Actions UI.</decision>
  <context>
All seven preconditions are reported with their observed command outputs in
`97-RELEASE-PRECONDITIONS.md`. Dispatching runs one `verify` job (VS Code build, test and package
plus IntelliJ build and plugin verification) first; only if it is green do `publish-vscode` and
`publish-intellij` run, in parallel; only if both are green does the tag job commit the version bump,
tag `v0.16.0` and push; only then is the GitHub Release created with both artifacts attached.

A version published to the VS Code Marketplace cannot be replaced or re-published under the same
number, and the JetBrains upload enters a review queue. If anything after `verify` goes red, the
written runbook governs: the missing side is published from the failed run's own verified artifact,
nothing is retried automatically, and the version is never bumped to paper over a half-release.

Claude does not dispatch the workflow under any circumstance.
  </context>
  <options>
    <option id="dispatch">
      <name>Dispatch 0.16.0 now (recommended if all seven preconditions hold)</name>
      <pros>Everything 93-96 and this phase's fixes reach users behind the single verification gate; the tagged release is also the trigger the maintainer has been waiting for on the separate publication decision.</pros>
      <cons>Irreversible: the published version number cannot be reused, and a single-sided publish failure means following the reconciliation runbook by hand inside the one-day artifact retention window.</cons>
    </option>
    <option id="hold">
      <name>Hold</name>
      <pros>Nothing becomes public; any precondition that does not hold can be fixed first and the same version dispatched later, unchanged.</pros>
      <cons>The phase stalls at step 4 and milestone #7 stays open.</cons>
    </option>
  </options>
  <resume-signal>Dispatch from GitHub -> **Actions** -> **Manual Release** -> **Run workflow** -> branch `main` -> version `0.16.0`, then reply with the run id or run URL. Or reply "hold" with what is missing.</resume-signal>
  <action>
Present the precondition report and ask the maintainer to dispatch the release. Then wait. Claude
performs no dispatch.

Give the maintainer exactly this:
1. The seven-item precondition report, each item with its observed output.
2. The dispatch steps: GitHub -> **Actions** -> **Manual Release** -> **Run workflow** -> branch
   `main` -> version `0.16.0` -> **Run workflow**.
3. What will happen: one `verify` job (VS Code build/test/package plus IntelliJ build and plugin
   verification) runs first; only if it is green do `publish-vscode` and `publish-intellij` run, in
   parallel; only if both are green does the tag job commit the version bump, tag `v0.16.0` and push;
   only then is the GitHub Release created with both artifacts attached.
4. The one-way warning: a version published to the VS Code Marketplace cannot be replaced or
   re-published under the same number, and the JetBrains upload enters a review queue.
5. What Claude will do next: watch the run and report; follow the runbook if anything goes red; retry
   nothing.

Ask for the run id or the run URL once dispatched. Record it.
  </action>
  <verify>
    <human-check>The maintainer has dispatched the Manual Release workflow from the Actions UI with version 0.16.0 and reported the run id or URL.</human-check>
  </verify>
  <acceptance_criteria>
    - A run id for the Manual Release workflow exists and was supplied by the maintainer.
    - `gh run view <run-id> --repo BBx-Kitchen/bbj-language-server --json event,displayTitle` reports event `workflow_dispatch`.
    - Claude's command history for this plan contains no `gh workflow run` invocation.
  </acceptance_criteria>
  <done>The release run exists, started by the maintainer, with its id recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Watch the run and record the release evidence</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.github/workflows/manual-release.yml (the job names and their `needs:` edges, so the read-back proves the gate rather than assuming it)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RECONCILIATION-RUNBOOK.md (the path to follow if a job is red)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-PATTERNS.md § "96-08 UAT-record shape" (the artifact-identity sentence shape)
  </read_first>
  <action>
Watch the run to completion, then write the evidence record.

1. `gh run watch <run-id> --repo BBx-Kitchen/bbj-language-server --exit-status`.
2. Read back the job graph rather than assuming it: `gh run view <run-id> --repo
   BBx-Kitchen/bbj-language-server --json jobs,conclusion,headSha` and record, per job, its name,
   conclusion, and its start time — so the record shows `verify` completed before either publish job
   started. This is the evidence for the "one verification job runs green before anything is
   published" criterion.
3. Confirm the release exists and capture its identity: `gh release view v0.16.0 --repo
   BBx-Kitchen/bbj-language-server --json tagName,targetCommitish,publishedAt,assets`. Download both
   assets to a scratch directory and compute the sha256 and byte size of each. Record: the tag, the
   released commit SHA, the run id, and for each asset the filename, sha256 and size, in the
   project's verdict-with-artifact-identity sentence shape.
4. Confirm the tag is on the remote: `git -C /home/coder/repos/bbj-language-server ls-remote --tags
   origin v0.16.0`.
5. Record explicitly that a green JetBrains publish job counts as live for this phase and that the
   review queue is not waited on, so the IntelliJ smoke in the next plan runs against the release's
   own zip rather than a marketplace install.

**If any job is red:** stop. Write what failed and where into the evidence record, open the runbook
at the matching path, and report to the maintainer with the recommended path. Do not re-run the job,
do not re-dispatch, do not bump the version. Every recovery step is a human checkpoint.
  </action>
  <verify>
    <automated>gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json tagName,targetCommitish,assets</automated>
    <fails_when>non-zero exit, or the assets array does not contain both a .vsix and a .zip — the release is incomplete and the runbook applies</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server ls-remote --tags origin v0.16.0</automated>
    <fails_when>no line printed — the tag never reached the remote, which is the tagging-failed path in the runbook</fails_when>
    <automated>grep -cE '^[0-9a-f]{64}' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md</automated>
    <fails_when>the printed count is fewer than 2 — the record does not carry a sha256 line for each released asset</fails_when>
  </verify>
  <acceptance_criteria>
    - `97-RELEASE-EVIDENCE.md` records the run id, every job's name and conclusion, and start times showing `verify` finished before either publish job started.
    - It records the tag `v0.16.0`, the released commit SHA, and for each of the two assets the filename, sha256 and byte size.
    - `git ls-remote --tags origin v0.16.0` prints a ref.
    - The record states the JetBrains "upload accepted counts as live" position and that the IntelliJ smoke will use the release's zip.
    - No job was re-run or re-dispatched by Claude; no version was bumped.
  </acceptance_criteria>
  <reversibility rating="one-way">This task observes and records an act that cannot be undone: the published marketplace version cannot be replaced or re-published under the same number, and the JetBrains upload has entered a review queue. The preceding decision checkpoint is the confirmation before that door opened.</reversibility>
  <done>0.16.0 is published to both marketplaces behind one verification gate, tagged and released, with every identity fact written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer -> public marketplaces | The dispatch is the moment private work becomes a public, irrevocable version. |
| CI run -> recorded evidence | The claim "0.16.0 shipped" must rest on read-back facts, not on a green-looking UI. |
| Failure handling -> automation | An autonomous retry after a partial publish is how a half-release becomes unrecoverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-25 | Elevation of Privilege | Release dispatch | high | mitigate | Claude is forbidden from dispatching; the acceptance criterion asserts the run's event is `workflow_dispatch` and that no dispatch command was issued by Claude. |
| T-97-26 | Repudiation | Release completeness claim | high | mitigate | The evidence record is built from `gh run view` job data and `gh release view` asset data, including per-asset sha256 and the remote tag ref — not from a summary claim. |
| T-97-27 | Denial of Service | Half-released state | high | mitigate | Red-job handling routes to the written runbook with no autonomous retry and no version bump; every recovery step is a human checkpoint. |
| T-97-28 | Information Disclosure | Publishing tokens | high | accept | Tokens live only in GitHub Actions secrets for this plan; no hand-run publish occurs unless the runbook path is entered, where the token rules apply. |

No package-manager install occurs in this plan, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- The precondition report carries all seven items with observed outputs.
- The run was dispatched by the maintainer (`event: workflow_dispatch`), not by Claude.
- `verify` completed before either publish job started, read back from the run's job data.
- `gh release view v0.16.0` reports both assets; the remote tag exists; both asset hashes are recorded.
</verification>

<success_criteria>
- Release 0.16.0 is published to both marketplaces through the single verification gate, with tag and GitHub Release present.
- No half-release, no orphaned tag — or, if one occurred, the runbook path is open and reported with no autonomous action taken.
- The artifact identity needed by the smoke verdict and the closing comments is on record.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-08-SUMMARY.md` when done. Record the
run id, every job conclusion, the released commit SHA, the tag, both asset hashes, and — if anything
went red — which runbook path was opened and what the maintainer decided.
</output>
