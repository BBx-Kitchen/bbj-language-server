---
phase: 97-release-0-16-0-milestone-close
plan: 10
type: execute
wave: 8
depends_on: ["97-09"]
files_modified:
  - QA/test-runs/
  - .planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md
autonomous: false
requirements: [REL-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The maintainer runs `QA/SMOKE-TEST-CHECKLIST.md` by hand, in a clean profile of each IDE, on their usual machine — no Playwright-driven VS Code pass and no Windows pass (D-15)."
    - "The VS Code side is installed from the Marketplace as the roadmap criterion states; the IntelliJ side is installed from `bbj-intellij-0.16.0.zip` on the v0.16.0 GitHub Release, and that override of the criterion's \"install from their marketplaces\" wording is recorded explicitly rather than reported as met verbatim (D-14)."
    - "A green `publish-intellij` job is what counts as live for the JetBrains side; the review queue is not waited on (D-14)."
    - "The verdict is recorded against the released assets' sha256, the workflow run id and the released commit SHA — not against a version string alone (D-16)."
    - "A smoke finding is classified by the maintainer, not by Claude: blocking findings keep milestone #7 open and ship in the next version; non-blocking findings become issues for the next milestone and 0.16.0 stands (D-17)."
    - statement: "No download-and-compare of the Marketplace VSIX is performed; the job graph plus the recorded asset hashes are the artifact-identity argument (D-16)."
      verification: backstop
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md
    - QA/test-runs/ (a dated copy of the smoke checklist, renamed with its PASS or FAIL suffix)
  key_links:
    - "The v0.16.0 release assets' sha256 (recorded in 97-RELEASE-EVIDENCE.md) -> the identity the smoke verdict is tied to."
    - "The checklist's \"Test Run Result\" and \"Test Information\" blocks -> the pre-filled fields Claude prepares and the maintainer completes."
  prohibitions:
    - "No Claude-run smoke pass, no Playwright automation of the checklist, no Windows attestation — this is a hand run by the maintainer."
    - "No classification of a finding as blocking or non-blocking by Claude; the maintainer classifies."
    - "No edit to QA/SMOKE-TEST-CHECKLIST.md itself — the filled-in copy lives under QA/test-runs/."
    - "No issue closure in this plan; closure happens only after the smoke verdict is in."
    - "No `git stash`, no branch creation or switching, no `git add -A`."
---

<objective>
Get the released artifacts smoke-tested by hand, in clean IDE profiles, and record the verdict
against the exact bytes that were published.

Purpose: D-01 step 6, and the roadmap's third success criterion. D-14 records one deliberate
override of that criterion's wording for the JetBrains side only: an accepted upload counts as live,
so the IntelliJ smoke runs against the release's own zip rather than a marketplace install.
Output: one pre-filled checklist copy, one hand run, one recorded verdict with its classification.

## Artifacts this phase produces (plan 97-10)

- `QA/test-runs/<date>-smoke-test.md` — a copy of the smoke checklist with its Test Information block pre-filled, renamed with a `-PASS` or `-FAIL` suffix after the run.
- `.planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md` — the verdict tied to asset hashes, run id and commit, plus the maintainer's classification of any finding.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md
@QA/SMOKE-TEST-CHECKLIST.md
@QA/TESTING-GUIDE.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.

**The checklist, read at planning time.** `QA/SMOKE-TEST-CHECKLIST.md` has ten numbered rows: VS
Code extension loads; syntax highlighting; BBj completion; Java completion; diagnostics; IntelliJ
basic; run program in VS Code; run program in IntelliJ; language server exits on quit; no runaway
CPU. It closes with a "Test Run Result" block (PASS / FAIL) and a "Test Information" block (Date,
Tester, OS, VS Code version, IntelliJ version, Extension version). Its own instructions say to copy
it to `QA/test-runs/YYYY-MM-DD-smoke-test.md`, mark each row, and rename the file with a `-PASS` or
`-FAIL` suffix.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prepare the filled-in smoke checklist copy against the released artifacts</name>
  <files>QA/test-runs/</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/SMOKE-TEST-CHECKLIST.md (whole file — the ten rows and the two closing blocks that get pre-filled)
    - /home/coder/repos/bbj-language-server/QA/TESTING-GUIDE.md (clean-IDE setup guidance to quote into the preamble)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md (the asset names, sha256 values, run id and released commit to embed)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <action>
Copy `QA/SMOKE-TEST-CHECKLIST.md` to `QA/test-runs/<YYYY-MM-DD>-smoke-test.md` using today's date,
then pre-fill everything that does not require running the tests. Do not edit the original checklist.

Pre-fill:
1. A short preamble above the table, stating: the version under test (0.16.0); where each side comes
   from — VS Code **from the Marketplace** (`basis-intl.bbj-lang`), IntelliJ from
   `bbj-intellij-0.16.0.zip` attached to the v0.16.0 GitHub Release via **Settings > Plugins > gear >
   Install Plugin from Disk...**; and one sentence recording plainly that the IntelliJ side is
   deliberately not installed from the JetBrains Marketplace because an accepted upload counts as
   live for this release and the review queue is not being waited on. Keep the wording free of
   planning identifiers.
2. The artifact identity block: both asset filenames with their sha256 and byte size, the workflow
   run id, and the released commit SHA, all copied from `97-RELEASE-EVIDENCE.md`.
3. The "Test Information" block: Date (today), Extension Version `0.16.0`; leave Tester, OS, VS Code
   Version and IntelliJ Version as blanks for the maintainer to fill.
4. A clean-profile reminder drawn from `QA/TESTING-GUIDE.md`: how to start each IDE with a clean
   profile, and that a locally installed development build must be uninstalled first so the run
   exercises the released artifact.
5. Leave all ten `[ ]` result cells and the "Test Run Result" block untouched — those are the
   maintainer's to mark.

Also confirm, and note in the preamble, that the JetBrains publish job for this release was green —
read it back from `97-RELEASE-EVIDENCE.md` rather than asserting it.
  </action>
  <verify>
    <automated>ls -1 /home/coder/repos/bbj-language-server/QA/test-runs/*-smoke-test.md</automated>
    <fails_when>non-zero exit, or no file printed — the dated checklist copy was not created</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --exit-code -- QA/SMOKE-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit — the source checklist was edited instead of copied</fails_when>
    <automated>grep -cE '^[0-9a-f]{64}|[0-9a-f]{64}' /home/coder/repos/bbj-language-server/QA/test-runs/*-smoke-test.md</automated>
    <fails_when>the printed count is fewer than 2 — the copy does not carry a sha256 for each released asset</fails_when>
  </verify>
  <acceptance_criteria>
    - `QA/test-runs/<date>-smoke-test.md` exists and contains all ten original checklist rows unmodified, with their result cells still empty.
    - Its preamble names the VS Code Marketplace install path and the GitHub-Release zip install path, and records the JetBrains-side override in plain language.
    - It carries both asset sha256 values, both byte sizes, the run id and the released commit SHA.
    - The Test Information block has Date and Extension Version filled and the remaining fields blank.
    - `QA/SMOKE-TEST-CHECKLIST.md` is unmodified.
  </acceptance_criteria>
  <done>The maintainer can start the smoke run without looking anything up, and the run is already tied to the bytes under test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer runs the smoke checklist in clean profiles of both IDEs</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/test-runs/ (the prepared copy — this is the file the maintainer fills in)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-15, D-17)
  </read_first>
  <what-built>A dated copy of `QA/SMOKE-TEST-CHECKLIST.md` under `QA/test-runs/`, pre-filled with: the version under test (0.16.0), both install paths, both released assets' sha256 and byte size, the workflow run id, the released commit SHA, a clean-profile reminder, and the Date and Extension Version fields. All ten result cells and the Test Run Result block are left empty for the maintainer.</what-built>
  <how-to-verify>
1. Open the prepared checklist copy at the absolute path given in chat.
2. Install VS Code side from the Marketplace (`basis-intl.bbj-lang`, version 0.16.0). Install the IntelliJ side from `bbj-intellij-0.16.0.zip` attached to the v0.16.0 GitHub Release, via **Settings > Plugins > gear > Install Plugin from Disk...**.
3. Use a clean profile of each IDE and uninstall any development build first, so the run exercises what users get.
4. Work through the ten rows in order, marking each `[x]` for pass or `[FAIL]` for a failure.
5. Fill Tester, OS, VS Code Version and IntelliJ Version, then mark the Test Run Result block PASS or FAIL.
6. For any failure, write one sentence on what happened and classify it as **blocking** (extension will not activate, server will not start, data loss) or **non-blocking**. The classification is yours; Claude records it and does not argue it.
  </how-to-verify>
  <resume-signal>Reply "PASS" or "FAIL" with the per-row marks and, for any failure, the one-sentence description plus its blocking / non-blocking classification.</resume-signal>
  <action>
Hand the prepared checklist to the maintainer and wait for the completed run. Issue closure does not
start until this verdict is in (D-18).

Present, briefly:
1. The absolute path of the prepared checklist copy.
2. The two install paths: VS Code from the Marketplace (`basis-intl.bbj-lang`, version 0.16.0);
   IntelliJ from `bbj-intellij-0.16.0.zip` on the v0.16.0 GitHub Release, installed from disk.
3. The clean-profile reminder — uninstall any development build first, so the run exercises what
   users get.
4. The ten rows to work through in order, marking each `[x]` or `[FAIL]`.
5. The ask: mark the rows, fill Tester / OS / VS Code Version / IntelliJ Version, mark the Test Run
   Result block, and — for any failure — say in one sentence what happened and whether it is
   **blocking** (extension will not activate, server will not start, data loss) or **non-blocking**.
   The classification is the maintainer's; Claude records it and does not argue it.

When the run comes back, rename the file to `QA/test-runs/<date>-smoke-test-PASS.md` or
`…-FAIL.md` to match the marked result, exactly as the checklist's own instructions say.
  </action>
  <verify>
    <human-check>The maintainer has installed both released artifacts in clean profiles, worked through all ten checklist rows, marked the Test Run Result block, and classified any failure as blocking or non-blocking.</human-check>
  </verify>
  <acceptance_criteria>
    - The checklist copy has all ten result cells marked and the Test Information block fully filled.
    - The Test Run Result block is marked PASS or FAIL.
    - The file is renamed with a matching `-PASS` or `-FAIL` suffix.
    - Every failure, if any, carries the maintainer's one-sentence description and their blocking / non-blocking classification.
  </acceptance_criteria>
  <done>The released artifacts have been exercised by a human in clean IDEs, with a recorded result per row.</done>
</task>

<task type="auto">
  <name>Task 3: Record the smoke verdict against artifact identity and act on the classification</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/test-runs/ (the completed, renamed checklist)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md (the identities to tie the verdict to)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-PATTERNS.md § "96-08 UAT-record shape" (the verdict sentence shape to copy)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-17's two branches)
  </read_first>
  <action>
Write `97-SMOKE-VERDICT.md` containing:

1. **The verdict sentence**, in this project's established shape: the outcome, tied to both released
   assets by filename, sha256 and byte size, plus the workflow run id and the released commit SHA,
   with the maintainer's verbatim wording quoted.
2. **The per-row result table** — the ten checklist rows with their marks, copied from the completed
   file, and the path of that file.
3. **The recorded criterion override**: the roadmap's third success criterion says the published
   artifacts install from *their marketplaces*; state plainly that the VS Code side met that as
   written and the IntelliJ side was installed from the GitHub Release zip instead, that this was a
   deliberate decision, and that the phase's verification must report it as an override rather than
   as the criterion met verbatim. This sentence is what `/gsd-verify-work` will read.
4. **The classification and its consequence**, taking exactly one branch:
   - *No findings, or only non-blocking findings:* 0.16.0 stands and the milestone may close. For
     each non-blocking finding, open a GitHub issue for the next milestone — title, body with the
     reproduction, no milestone-#7 assignment — and record the issue number here. Note explicitly
     that these new issues are **not** added to milestone #7, which must stay at exactly 21 issues.
   - *Any blocking finding:* 0.16.0 does not stand as shippable-without-follow-up. Record the
     finding, state that a fix lands via PR and ships in the next preview immediately with stable
     users getting it in the following version, and that milestone #7 stays open until then. Stop
     the phase here and report — do not proceed to issue closure.
5. **What was deliberately not done**: no download-and-compare of the Marketplace VSIX; the job
   graph plus the recorded asset hashes are the artifact-identity argument.
  </action>
  <verify>
    <automated>grep -cE '[0-9a-f]{64}' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md</automated>
    <fails_when>the printed count is fewer than 2 — the verdict is not tied to both released assets by hash</fails_when>
    <automated>ls -1 /home/coder/repos/bbj-language-server/QA/test-runs/ | grep -E '\-smoke-test-(PASS|FAIL)\.md$'</automated>
    <fails_when>no line printed — the completed checklist was never renamed with its result suffix</fails_when>
    <automated>gh api repos/BBx-Kitchen/bbj-language-server/milestones/7 --jq '.open_issues'</automated>
    <fails_when>the printed number is anything other than 21 — a new issue was attached to milestone #7, or an issue was closed before the closure plan</fails_when>
  </verify>
  <acceptance_criteria>
    - `97-SMOKE-VERDICT.md` carries the verdict sentence with both asset filenames, both sha256 values, both byte sizes, the run id and the released commit SHA.
    - It carries the ten-row result table and the path of the renamed checklist file.
    - It states the roadmap-criterion override for the JetBrains side in explicit, verifier-readable language.
    - Exactly one classification branch is taken and recorded; any new issues carry their numbers and are not on milestone #7.
    - Milestone #7 still reports 21 open issues.
  </acceptance_criteria>
  <done>The smoke verdict exists, is tied to the published bytes, and its consequence is decided and written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketplace / GitHub Release -> maintainer's clean IDE profile | The artifacts a user would get are exercised, not a local build. |
| Smoke finding -> release standing | A misclassified blocking finding would let a broken version stand with its milestone closed. |
| New issue creation -> milestone #7 | An issue attached to milestone #7 would break the exact-21 criterion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-32 | Tampering | Artifact under test | medium | mitigate | The prepared checklist embeds both asset sha256 values and the released commit, and the verdict sentence repeats them, so a verdict cannot detach from the bytes it judged. |
| T-97-33 | Repudiation | Criterion 3 compliance claim | medium | mitigate | The JetBrains-side install override is written into the verdict file in explicit language, so verification reports it as an override rather than as the criterion met verbatim. |
| T-97-34 | Elevation of Privilege | Finding classification | medium | mitigate | Claude records the maintainer's classification and takes the matching branch; it does not classify, and a blocking finding halts the phase before issue closure. |
| T-97-35 | Tampering | Milestone composition | high | mitigate | A milestone read-back asserts exactly 21 open issues after any new issue is filed, so follow-up issues cannot silently join the milestone being closed. |

No package-manager install occurs in this plan, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- The dated checklist copy exists, is fully marked, and is renamed with its result suffix.
- The verdict file ties the outcome to both asset hashes, the run id and the released commit.
- The criterion-3 override for the JetBrains side is stated explicitly.
- Milestone #7 still reports 21 open issues.
</verification>

<success_criteria>
- The published artifacts were exercised in clean IDEs by the maintainer, row by row.
- The verdict and its classification are recorded, with the consequence decided.
- Nothing was closed, and no follow-up issue joined milestone #7.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-10-SUMMARY.md` when done. Record the
verdict, the classification branch taken, any new issue numbers, and the explicit criterion-3
override sentence.
</output>
