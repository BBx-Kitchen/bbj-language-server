---
phase: 97-release-0-16-0-milestone-close
plan: 09
type: execute
wave: 7
depends_on: ["97-08"]
files_modified:
  - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md
autonomous: false
requirements: [REL-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The GitHub Release is first created by the workflow untouched — auto-generated notes plus the install block — and is only edited afterwards (D-13)."
    - "The curated summary covers the eleven behaviour fixes in plain user-facing language, the ten consolidations as one line, the Color Scheme page removal, the Node.js diagnosis and fallback change, and the user-visible folded todos (D-13)."
    - "The maintainer approves the wording before anything public changes; only then is it applied (D-13)."
    - "The install block from the workflow's own release body survives the edit verbatim (D-13)."
    - "No planning identifier appears anywhere in the published notes (D-20's no-identifier rule applies to every public word this phase writes)."
    - statement: "The folded todos appear in the release notes and the landing PR body only; none of them gets a GitHub issue, so milestone #7 stays at exactly 21 issues (D-21)."
      verification: backstop
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md
  key_links:
    - "The workflow's `create-release` install block -> the `## Installation` section that must reappear unchanged after `gh release edit`."
    - "REQUIREMENTS.md's v4.4 list plus STATE.md's phase 93-96 decisions -> the plain-language fix list."
  prohibitions:
    - "No `gh release edit` before the maintainer's explicit approval."
    - "No planning identifiers (plan numbers, D-xx, requirement ids such as COMP-/PLAT-/EM-/IOP-/REL-) in the published notes."
    - "No GitHub issue created for any folded todo."
    - "No removal or rewording of the install block the workflow generated."
    - "No `git stash`, no branch creation or switching, no `git add -A`."
---

<objective>
Turn the release's auto-generated notes into something a BBj developer can read: what changed for
them in 0.16.0, in plain language, approved by the maintainer before it goes public.

Purpose: D-01 step 5 and D-13. The workflow's own notes are a commit list; the milestone shipped
eleven behaviour fixes, ten behaviour-preserving consolidations and six folded todos, and none of
that is legible from commit subjects.
Output: one draft, one approval, one applied edit.

## Artifacts this phase produces (plan 97-09)

- `.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md` — the draft, the maintainer's edits, and the final applied text.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-LANDING-PR.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Capture the released notes as published, then draft the curated summary</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/REQUIREMENTS.md (the v4.4 requirement list — the source for the eleven behaviour fixes and the ten consolidations)
    - /home/coder/repos/bbj-language-server/.planning/STATE.md § Decisions (the phase 93/95/96 entries, which say what shipped and, for six issues, what shipped instead of the literal wording)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-13's exact content list, and D-21 on the folded todos)
    - /home/coder/repos/bbj-language-server/.github/workflows/manual-release.yml (the `create-release` step's install block, which must survive verbatim)
  </read_first>
  <action>
This is the thin end-to-end step: read the live release body, keep it, and write the replacement
around it.

1. Capture the release body exactly as the workflow created it:
   `gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json body` and save it into
   `97-RELEASE-NOTES.md` under a heading "As published by the workflow (do not lose this)". The
   `## Installation` block inside it is the part that must reappear unchanged.
2. Draft the curated summary under a heading "Draft for approval". Structure:
   - A two-sentence opener: this release is the v4.4 IntelliJ-focused milestone; what a user gets.
   - **Fixes** — the eleven behaviour fixes, one line each, each written from the user's side
     ("the composer no longer throws when the server returns an incomplete catalog", not "added a
     null guard in a dialog"). Reference the GitHub issue number in plain text where it helps
     (`#607`); never a requirement id or a plan number.
   - **Under the hood** — one single line saying that ten internal consolidations landed with no
     change in behaviour.
   - **Removed** — the Color Scheme customization page, with one sentence on why (its keys were
     never consulted by the TextMate-driven highlighter) and what a user should do instead.
   - **Node.js** — the diagnosis and fallback change: the banner now names the actual reason a
     configured or cached Node.js could not be used, and drops the download action in the case where
     retrying is hopeless.
   - **Also in this release** — the user-visible folded todos in plain language: a lost
     language-server connection is now detected as a crash (banner, auto-restart); the status log
     names the real previous state; two sources of log noise are gone.
   - Keep the whole thing short — a user should read it in under a minute.
3. Append the workflow's `## Installation` block verbatim at the end of the draft, byte-identical to
   what step 1 captured.
4. Re-read the draft and grep it for planning identifiers before presenting it. Requirement ids,
   plan numbers and decision ids are all forbidden in public text; GitHub issue numbers are fine.

Apply nothing yet.
  </action>
  <verify>
    <automated>gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json body</automated>
    <fails_when>non-zero exit — the release does not exist, so there is nothing to curate and the previous plan's evidence is wrong</fails_when>
    <automated>grep -nE '\b(D|C|CR)-[0-9]+\b|\b(COMP|PLAT|EM|IOP|REL)-[0-9]+\b|\b9[0-7]-[0-9]{2}\b' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md</automated>
    <fails_when>any line inside the "Draft for approval" section is printed — a planning identifier would become public text</fails_when>
    <automated>grep -c '## Installation' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md</automated>
    <fails_when>the printed count is fewer than 2 — the captured original and the draft do not both carry the install block</fails_when>
  </verify>
  <acceptance_criteria>
    - `97-RELEASE-NOTES.md` contains the workflow's published body captured verbatim under its own heading.
    - The draft covers all six required content blocks: eleven fixes, the one-line consolidation statement, the Color Scheme removal, the Node.js change, the folded todos, and the install block.
    - The draft's install block is byte-identical to the captured one.
    - No planning identifier appears in the draft section.
    - Nothing public has been changed.
  </acceptance_criteria>
  <done>The published body is preserved and a readable replacement exists, ready for approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer approves the release-notes wording</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md (the draft to present)
  </read_first>
  <what-built>The release body as the workflow published it is captured verbatim in `97-RELEASE-NOTES.md`, and a curated user-facing v4.4 summary is drafted beneath it — the eleven behaviour fixes in plain language, one line for the ten behaviour-preserving consolidations, the Color Scheme page removal, the Node.js diagnosis and fallback change, the user-visible folded todos, and the workflow's own install block reproduced byte-identically at the end.</what-built>
  <how-to-verify>
1. Read the "Draft for approval" section in `97-RELEASE-NOTES.md` — it is exactly what would appear on the v0.16.0 release page.
2. Check that each of the eleven fix lines reads from a user's point of view and that nothing overstates what shipped.
3. Check that the six cited-reasoning items are described honestly (what shipped, not what the issue asked for word-for-word).
4. Note what the edit will do: it replaces the release body, keeping the install block verbatim; the auto-generated commit list is replaced by the curated text, not appended to. The tag and both release assets are untouched.
5. Expected: nothing on the release page has changed yet — `gh release view v0.16.0 --json body` still returns the workflow's original body.
  </how-to-verify>
  <resume-signal>Reply "approved" to apply as written, or give edits; substantial edits are folded in, re-checked and re-presented.</resume-signal>
  <action>
Show the maintainer the draft in full and ask for approval or edits. Nothing public changes until an
explicit go.

Present:
1. The draft, complete, as it would appear on the release page.
2. A one-line statement of what the edit will do: replace the release body, keeping the install block
   verbatim; the auto-generated commit list is replaced by the curated text, not appended to.
3. A note that the release assets and the tag are untouched by this edit.

Ask: approve as written, or give edits. Apply any edits into the file under a heading "Approved
text", re-run the planning-identifier grep over it, and re-present if the edits were substantial.
Record the verbatim approval.
  </action>
  <verify>
    <human-check>The maintainer has read the full draft and either approved it verbatim or supplied edits that were folded in and re-checked.</human-check>
  </verify>
  <acceptance_criteria>
    - `97-RELEASE-NOTES.md` contains an "Approved text" section that is exactly what will be applied.
    - The maintainer's approval is recorded verbatim.
    - The planning-identifier grep over the approved text prints nothing.
    - `gh release view v0.16.0 --json body` still returns the workflow's original body — nothing was applied before approval.
  </acceptance_criteria>
  <done>The public wording is the maintainer's, not Claude's guess at it.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the approved notes to the release and read them back</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-NOTES.md (the "Approved text" section — this and nothing else is what gets applied)
  </read_first>
  <action>
Write the approved text to a scratch file under the session scratchpad directory and apply it with
`gh release edit v0.16.0 --repo BBx-Kitchen/bbj-language-server --notes-file <that file>`. Use a
notes file rather than an inline `--notes` string so no shell quoting can mangle the body.

Then read it back — `gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json
body,tagName,assets` — and confirm three things, recording each in the file under "Applied":
  - the body now equals the approved text;
  - the `## Installation` block is present and byte-identical to the captured original;
  - the tag and both assets are unchanged from the identities recorded in `97-RELEASE-EVIDENCE.md`
    (same asset names, same count).

If the read-back does not match, do not retry blindly: record the difference and report it.
  </action>
  <verify>
    <automated>gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json body --jq '.body' | grep -c '## Installation'</automated>
    <fails_when>the printed count is 0 — the install block was lost by the edit</fails_when>
    <automated>gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json assets --jq '.assets | length'</automated>
    <fails_when>the printed number is not 2 — an asset was lost or added by the edit</fails_when>
    <automated>gh release view v0.16.0 --repo BBx-Kitchen/bbj-language-server --json body --jq '.body' | grep -nE '\b(D|C|CR)-[0-9]+\b|\b(COMP|PLAT|EM|IOP|REL)-[0-9]+\b'</automated>
    <fails_when>any line is printed — a planning identifier reached the public release page</fails_when>
  </verify>
  <acceptance_criteria>
    - The live release body equals the approved text.
    - The `## Installation` block is present in the live body and matches the captured original.
    - The release still has exactly two assets, with the names recorded in `97-RELEASE-EVIDENCE.md`.
    - The live body contains no planning identifier.
    - `97-RELEASE-NOTES.md` has an "Applied" section recording the read-back result.
  </acceptance_criteria>
  <done>The public release page reads like release notes, with its install instructions intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning workspace -> public release page | Every word becomes public under the maintainer's repository. |
| Release edit -> release assets | A body edit must not disturb the tag or the attached artifacts users download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-29 | Information Disclosure | Public release notes | high | mitigate | A planning-identifier grep runs over the draft, over the approved text, and again over the live body after the edit. |
| T-97-30 | Tampering | Release assets and install instructions | medium | mitigate | The read-back asserts two assets and a present, byte-identical install block; the original body is captured before any edit so it can be restored. |
| T-97-31 | Repudiation | Public wording authorship | medium | mitigate | The edit runs only after a recorded, verbatim maintainer approval of the exact text applied. |

No package-manager install occurs in this plan, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- The workflow's original body is captured before any edit.
- The approved text carries no planning identifier and includes the install block verbatim.
- The live release body after the edit equals the approved text, with two assets and the install block intact.
</verification>

<success_criteria>
- Users reading the v0.16.0 release page learn what changed for them, in plain language.
- The install instructions survived.
- Nothing public was written without the maintainer's explicit approval.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-09-SUMMARY.md` when done. Record the
maintainer's verbatim approval and the post-edit read-back result.
</output>
