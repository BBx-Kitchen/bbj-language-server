---
phase: 97-release-0-16-0-milestone-close
plan: 11
type: execute
wave: 9
depends_on: ["97-10"]
files_modified:
  - .planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md
  - .planning/todos/pending/
  - .planning/todos/completed/
autonomous: false
requirements: [REL-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No issue closes until the release run is green and the smoke verdict is in, so every closing comment names a version users can install (D-18)."
    - "All 21 closing comments are drafted into one review file in the phase directory, read and edited by the maintainer, and posted only after an explicit go — one approval gate for the whole batch (D-19)."
    - "Each comment is two to four sentences: fixed in 0.16.0 with the release link, what the user now sees (or that there is no behaviour change, for the consolidations), and the fixing commit SHA on `main` (D-20)."
    - "The six issues closed on cited reasoning rather than as literally written — #616, #618, #620, #622, #594, #593 — additionally say plainly what was done instead and why (D-20)."
    - "No planning identifier appears in any comment; GitHub issue numbers are fine (D-20)."
    - "The folded todos get no GitHub issues; milestone #7 stays at exactly 21 issues and the six todo files move from pending to completed (D-21)."
    - "Issues are closed strictly one at a time, in ascending issue number, and milestone #7 is closed only after the last issue closes — never before."
    - "An issue that is already closed when the pass reaches it is skipped and recorded as already-closed, not re-closed or re-commented; a retry never posts a second comment on an issue that already carries this release's closing comment."
    - "If milestone #7 already reports zero open issues when the pass begins, the comment pass is a no-op and only the milestone close runs — that outcome is recorded rather than treated as an error."
  artifacts:
    - .planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md
  key_links:
    - "The squash commit SHA from 97-LANDING-PR.md -> the SHA every comment cites."
    - "The v0.16.0 release URL -> the installable version every comment names."
    - "`gh api repos/BBx-Kitchen/bbj-language-server/milestones/7` -> the 21/0 counter that proves REL-02."
  prohibitions:
    - "No `gh issue close` or `gh issue comment` before the maintainer's explicit go on the batch."
    - "No bulk close loop that continues past an error — one issue at a time, checked between each."
    - "No GitHub issue created for any folded todo."
    - "No planning identifiers (plan numbers, D-xx, requirement ids such as COMP-/PLAT-/EM-/IOP-/REL-) in any posted comment."
    - "No milestone close while any of the 21 issues is still open."
    - "No `git stash`, no branch creation or switching, no `git add -A`."
---

<objective>
Close the 21 issues on GitHub milestone #7 with a useful comment each, then close the milestone —
after the maintainer has read and approved every word.

Purpose: D-01 step 7 and REL-02. D-18 puts this last so each comment can name a version users can
install. D-19 rates it **costly**: 21 public comments under the maintainer's identity, editable
afterwards but already delivered to watchers.
Output: one review file, one approval, 21 closed issues and a closed milestone.

## Artifacts this phase produces (plan 97-11)

- `.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md` — the 21 drafted comments, the maintainer's edits, and the posting log.
- Six todo files moved from `.planning/todos/pending/` to `.planning/todos/completed/`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md
@.planning/phases/97-release-0-16-0-milestone-close/97-LANDING-PR.md
@.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md
@.planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md

**Shell and file-access rules (from CLAUDE.md — mandatory, inherited by every command in this plan):**
- Read with the `Read` tool; search with `grep` scoped to an absolute path. Every shell path
  absolute and complete. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`.
- `git add <exact path>` only. No `git stash`. Do not create or switch branches.

**Milestone #7 as read at planning time.** Title "v4.4 IntelliJ Focus", 21 open, 0 closed. The 21
issue numbers: 587, 588, 589, 590, 591, 592, 593, 594, 607, 609, 613, 614, 615, 616, 617, 618, 619,
620, 621, 622, 630. Re-read the live list before drafting rather than trusting this snapshot.

**The six cited-reasoning closures.** #616, #618, #620, #622, #594 and #593 shipped differently from
their literal wording. The reasoning is already recorded in `STATE.md`'s phase 93/95/96 decision
entries and in `REQUIREMENTS.md`'s entry for the Java-interop poll gating. Each of those six comments
must state, in plain language, what was done instead and why — without quoting a planning identifier.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Draft all 21 closing comments into one review file</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CONTEXT.md (D-19, D-20, D-21 — the review-file shape, the comment shape, the six special cases, the no-issues-for-todos rule)
    - /home/coder/repos/bbj-language-server/.planning/STATE.md § Decisions (the phase 93/95/96 entries carrying the cited reasoning for #616, #618, #620, #622, #594)
    - /home/coder/repos/bbj-language-server/.planning/REQUIREMENTS.md (the v4.4 requirement list, including the cited-reasoning closure text for the Java-interop poll gating issue #593)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-LANDING-PR.md (the squash commit SHA and PR number every comment cites)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-RELEASE-EVIDENCE.md (the release URL and released commit SHA)
  </read_first>
  <action>
Write the whole batch once, as one reviewable document — this is the thin end-to-end step: one
comment drafted completely to the final shape, then the other twenty to the same shape.

1. Re-read the live milestone membership rather than trusting the snapshot:
   `gh issue list --repo BBx-Kitchen/bbj-language-server --milestone "v4.4 IntelliJ Focus" --state all
   --limit 40 --json number,title,state`. Record the list and each issue's current state. If the
   count is not 21, stop and report — the exact-21 criterion is part of REL-02.
2. For each issue, draft a comment of two to four sentences:
   - **Sentence 1** — fixed in 0.16.0, with the release link.
   - **Sentence 2** — what the user now sees, in their words. For a behaviour-preserving
     consolidation, say plainly that there is no behaviour change and what was consolidated.
   - **Sentence 3** — the fixing commit on `main`: since the landing PR was squash-merged, cite the
     squash commit SHA and the PR number rather than per-fix SHAs.
   - For **#616, #618, #620, #622, #594 and #593** only, add a fourth sentence saying what was done
     instead of the issue's literal wording and why — e.g. a shared abstract base with thin
     subclasses rather than a single data-driven registration, because the platform instantiates
     these through a no-argument constructor with no registration identity, so one class cannot vary
     its text and kind and a mistake would become a silent runtime no-op instead of a compile error.
     Write the reasoning from the issue's own subject matter; do not paste a decision-log line.
   - Never use a planning identifier. GitHub issue numbers are fine.
3. Lay the file out so it is easy to read and edit: one section per issue, ascending by issue
   number, headed with the number and the issue title, then the comment body in a fenced block so
   the maintainer can edit it in place.
4. Add a header section stating: the batch is posted only after an explicit go; comments are posted
   and issues closed one at a time in ascending number order; the milestone is closed last; and any
   issue already closed when the pass reaches it is skipped and recorded rather than re-commented.
5. Add a footer section listing the six folded todos with a line each saying they are covered by the
   release notes and the landing PR body and deliberately get no GitHub issue, so the milestone stays
   at exactly 21.
6. Grep the whole file for planning identifiers before presenting it.
  </action>
  <verify>
    <automated>gh issue list --repo BBx-Kitchen/bbj-language-server --milestone "v4.4 IntelliJ Focus" --state all --limit 40 --json number,state --jq 'length'</automated>
    <fails_when>the printed number is anything other than 21 — the milestone's membership changed and the closure plan's premise no longer holds</fails_when>
    <automated>grep -cE '^#+ *#?[0-9]{3}' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md</automated>
    <fails_when>the printed count is fewer than 21 — the review file does not carry a section per issue</fails_when>
    <automated>grep -nE '\b(D|C|CR)-[0-9]+\b|\b(COMP|PLAT|EM|IOP|REL)-[0-9]+\b|\b9[0-7]-[0-9]{2}\b' /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md</automated>
    <fails_when>any line inside a comment body is printed — a planning identifier would be posted publicly</fails_when>
  </verify>
  <acceptance_criteria>
    - The live milestone list reports exactly 21 issues, and the file has one section per issue in ascending number order.
    - Every comment body is 2-4 sentences and contains the v0.16.0 release link and the squash commit SHA plus the PR number.
    - The sections for #616, #618, #620, #622, #594 and #593 each carry the additional what-was-done-instead sentence.
    - No comment body contains a planning identifier.
    - The header states the one-at-a-time, ascending-order, milestone-last and skip-already-closed rules; the footer records that the folded todos get no issues.
    - Nothing has been posted.
  </acceptance_criteria>
  <done>Every public word of the closure batch exists in one file, in final shape, unposted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer approves the batch of 21 closing comments</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md (the batch to present)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-SMOKE-VERDICT.md (the verdict that authorises closure at all)
  </read_first>
  <what-built>All 21 closing comments drafted into `97-CLOSING-COMMENTS.md`, one section per issue in ascending number order, each 2-4 sentences naming 0.16.0 with the release link, what the user now sees (or that there is no behaviour change), and the squash commit SHA plus the PR number. The sections for #616, #618, #620, #622, #594 and #593 carry an extra sentence saying what was done instead of the issue's literal wording and why. No planning identifiers anywhere. Nothing is posted.</what-built>
  <how-to-verify>
1. Open `97-CLOSING-COMMENTS.md` and read all 21 comment bodies; edit any of them in place.
2. Check the six special cases first — #616, #618, #620, #622, #594, #593 — where the extra sentence has to explain honestly what shipped instead.
3. Read the quoted smoke verdict and its classification in the header: closure is authorised only because the release is green and the verdict is in. If the verdict recorded a blocking finding, the milestone stays open and nothing is posted.
4. Note what happens on a go: comments posted and issues closed one at a time, ascending by number, each verified closed before the next is touched; milestone #7 closed last; the six folded-todo files moved from pending to completed. An already-closed issue is skipped and logged, never re-commented.
5. Expected right now: `gh issue list --milestone "v4.4 IntelliJ Focus" --state open` still reports 21 — nothing has been posted or closed.
  </how-to-verify>
  <resume-signal>Reply "go" to post the batch as written, or give edits; substantial edits are folded in, re-checked for planning identifiers and re-presented.</resume-signal>
  <action>
Present the batch and wait for one explicit go. No comment is posted and no issue is closed before
it.

Present:
1. The path of the review file and a statement that it contains all 21 comments in final form.
2. The smoke verdict and its classification, quoted — closure is authorised only because the release
   is green and the verdict is in. If the verdict recorded a blocking finding, say so and stop: the
   milestone stays open.
3. The six issues whose comments carry the extra what-was-done-instead sentence, listed by number, so
   the maintainer can check those first.
4. What will happen on a go: comments posted and issues closed one at a time, ascending by number,
   each verified closed before the next is touched; milestone #7 closed last; the six folded-todo
   files moved from pending to completed.

Ask for edits or an explicit go. Fold any edits back into the file, re-run the planning-identifier
grep, and re-present if the edits were substantial. Record the verbatim go.
  </action>
  <verify>
    <human-check>The maintainer has read the 21 comments, made any edits, and given an explicit go (or withheld it).</human-check>
  </verify>
  <acceptance_criteria>
    - The maintainer's verbatim go is recorded in the review file.
    - Any edits are folded into the file and re-checked for planning identifiers.
    - `gh issue list --repo BBx-Kitchen/bbj-language-server --milestone "v4.4 IntelliJ Focus" --state open --json number --jq 'length'` still reports 21 — nothing was posted or closed before the go.
  </acceptance_criteria>
  <reversibility rating="costly">21 public comments under the maintainer's identity; editable after the fact but already delivered to every watcher.</reversibility>
  <done>The batch is approved, word by word, before a single comment exists publicly.</done>
</task>

<task type="auto">
  <name>Task 3: Post, close one at a time, close the milestone, and move the todo files</name>
  <files>.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md, .planning/todos/pending/, .planning/todos/completed/</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-CLOSING-COMMENTS.md (the approved text — this and nothing else is posted)
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/ (the six files to move: the four dated 2026-09-20, plus the gradle-wrapper-hygiene one and the live-interop one)
    - /home/coder/repos/bbj-language-server/.planning/phases/97-release-0-16-0-milestone-close/97-03-SUMMARY.md (whether the linking investigation re-filed a new pending todo — if it did, that file stays in pending)
  </read_first>
  <action>
Work the batch deliberately, one issue at a time, in ascending issue number. Never loop blindly.

For each issue, in order:
1. Re-read its current state: `gh issue view <n> --repo BBx-Kitchen/bbj-language-server --json
   number,state,comments`. If it is already CLOSED, skip it and record "already closed, skipped" in
   the posting log — do not re-close it and do not post a comment. If its comment list already
   contains this release's closing comment (match on the v0.16.0 release link plus the squash SHA),
   skip it too and record "comment already present, skipped": a retry must never post twice.
2. Otherwise post the approved comment and close in one step:
   `gh issue close <n> --repo BBx-Kitchen/bbj-language-server --comment "<approved text>"`. Pass the
   comment through a file or a quoted variable so no shell interpretation mangles it.
3. Verify it: re-read the issue and confirm state CLOSED and the comment present. Only then move to
   the next number. If any step errors, stop the pass, record where it stopped, and report — do not
   continue past an error.
4. Append each result to a "Posting log" section in the review file: issue number, action taken
   (posted+closed / skipped-already-closed / skipped-comment-present), and the comment URL.

When and only when all 21 are closed:
5. Confirm the count first: `gh api repos/BBx-Kitchen/bbj-language-server/milestones/7 --jq
   '.open_issues, .closed_issues'` must report 0 open and 21 closed. If `open_issues` is already 0
   when the pass begins — every issue having been closed beforehand — record that the comment pass
   was a no-op for those issues and continue to the milestone close; that is a valid outcome, not an
   error.
6. Close the milestone: `gh api -X PATCH repos/BBx-Kitchen/bbj-language-server/milestones/7 -f
   state=closed`, then read it back and record its state.

Finally, the todo bookkeeping (no GitHub issues, by decision):
7. Move each of the six folded-todo files from `.planning/todos/pending/` to
   `.planning/todos/completed/` with `git -C /home/coder/repos/bbj-language-server mv <exact source>
   <exact destination>`, one exact path at a time. If plan 97-03's linking investigation filed a new
   pending todo, that one stays in `pending` — it is a live residual, not a completed item. Record
   which six moved and which, if any, stayed.
  </action>
  <verify>
    <automated>gh api repos/BBx-Kitchen/bbj-language-server/milestones/7 --jq '.open_issues'</automated>
    <fails_when>the printed number is anything other than 0 — at least one of the 21 issues is still open</fails_when>
    <automated>gh api repos/BBx-Kitchen/bbj-language-server/milestones/7 --jq '.closed_issues'</automated>
    <fails_when>the printed number is anything other than 21 — the milestone does not hold exactly the 21 issues it is supposed to close with</fails_when>
    <automated>gh api repos/BBx-Kitchen/bbj-language-server/milestones/7 --jq '.state'</automated>
    <fails_when>the printed value is not "closed" — the milestone itself was not closed</fails_when>
    <automated>ls -1 /home/coder/repos/bbj-language-server/.planning/todos/completed/ | grep -cE '2026-09-20-(lost-language-server-connection|status-transition-log|intellij-client-has-no-handler|node-download-progress)|2026-09-05-gradle-wrapper-hygiene|2026-09-03-update-live-interop-tests'</automated>
    <fails_when>the printed count is fewer than 6 — one or more folded-todo files did not reach the completed directory</fails_when>
  </verify>
  <acceptance_criteria>
    - Every one of the 21 issues is CLOSED, each having been verified individually before the next was touched.
    - The posting log records an action and a comment URL per issue, including any skips with their reason.
    - Milestone #7 reports 0 open, 21 closed, state closed.
    - All six folded-todo files are in `.planning/todos/completed/`; any residual todo filed by the linking investigation is still in `pending` and is named in the log.
    - No GitHub issue was created for any folded todo; no posted comment contains a planning identifier.
  </acceptance_criteria>
  <done>The 21 issues and milestone #7 are closed, each comment naming a version users can install, and the todo bookkeeping matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning workspace -> 21 public comments under the maintainer's identity | Every posted sentence is public and notifies watchers immediately. |
| Retry / resume -> issue comment stream | A re-run of the pass must not double-post on an issue it already handled. |
| Milestone state -> REL-02 evidence | The 0-open / 21-closed / closed-state read-back is the requirement's proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-36 | Information Disclosure | Public issue comments | high | mitigate | Planning-identifier greps run over the draft, over the approved text, and the batch posts only after a recorded verbatim go. |
| T-97-37 | Tampering | Issue comment stream on retry | medium | mitigate | Each issue's live state and existing comments are re-read before posting; an already-closed or already-commented issue is skipped and logged, so a retry cannot double-post. |
| T-97-38 | Repudiation | REL-02 completion claim | medium | mitigate | The claim rests on a milestone API read-back of open/closed counts and state, not on the pass having appeared to run. |
| T-97-39 | Denial of Service | Partial closure pass | medium | mitigate | One issue at a time with verification between each, and a hard stop on the first error, so a half-finished pass is visible and resumable rather than silently partial. |
| T-97-40 | Tampering | Milestone composition | high | mitigate | No issue is created for any folded todo, and the closed count is asserted to be exactly 21. |

No package-manager install occurs in this plan, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
- Milestone #7: 0 open, 21 closed, state `closed`.
- Every issue carries exactly one closing comment naming 0.16.0, the release link and the squash commit.
- No posted comment contains a planning identifier.
- Six folded-todo files are in `completed/`; any residual stays in `pending` and is named.
</verification>

<success_criteria>
- REL-02 is met and provable from the GitHub API, not from a narrative.
- Every closing comment names a version a user can install, and the six cited-reasoning closures explain themselves.
- The batch went out only after the maintainer read it.
</success_criteria>

<output>
Create `.planning/phases/97-release-0-16-0-milestone-close/97-11-SUMMARY.md` when done. Record the
posting log summary (posted / skipped counts), the milestone read-back, the six moved todo files,
and any residual todo left in pending.
</output>
