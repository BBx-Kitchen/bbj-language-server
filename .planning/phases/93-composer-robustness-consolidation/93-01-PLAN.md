---
phase: 93-composer-robustness-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxSchematicPanel.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/WindowSchematicPanel.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ChildWindowSchematicPanel.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java
autonomous: true
requirements: [COMP-07]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`clip`, `labeled`, `labeledWithError`, `errorLabel`, `setEnabledRecursive` and the unavailable-preview label each have exactly one definition in the source tree, so a fix to any of those shapes is written once (COMP-07)."
    - "The consolidation covers the whole present-day family — six dialogs and three schematic panels — not the three helpers and three files #619 names (D-01)."
    - "Error text renders in a theme-aware error colour rather than one hardcoded RGB value, so it is correct in Darcula as well as Light (D-02)."
    - "A stalled preview renders in that same error colour rather than default gray, so a dialog that cannot preview is visibly distinct from one that can (D-04)."
    - "Each dialog keeps its own one-line wrapper that refuses OK after delegating the label update, so the OK-gating discipline stays visible per dialog and the existing three-disable count per dialog is unchanged."
    - "`clip` is reconciled deliberately to the cached-`FontMetrics` variant rather than whichever copy an extraction happened to keep first; the three copies are behaviourally identical but not textually identical."
    - "`errorLabel`'s import style is normalised to the plain `UIUtil` form already imported by three of the five dialogs, rather than leaving two fully-qualified call sites behind."
  artifacts:
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java
  key_links:
    - "All six dialogs and all three schematic panels must reach the shared home; a file left behind keeps a second definition alive and defeats the requirement."
    - "`ComposerDialogRefreshSourceGuardTest` counts the unavailable-preview prefix per dialog file today and must be re-pointed to the shared body plus a per-dialog delegation pin (D-11)."
  prohibitions:
    - "No helper is left duplicated in any dialog or panel; a private copy retained anywhere defeats the requirement."
    - "The hardcoded red is not merely moved into the shared home — it is replaced by a theme-aware colour, which is the point of D-02."
    - "`setOKActionEnabled(false)` does not move into the shared helper; the helper owns text and colour only, never a dialog's OK state."
    - "No dialog's user-visible wording changes: the unavailable-preview prefix and every field label keep their exact current text."
    - "The new guard imports no shared test utility; it carries its own private helper copies (D-12)."
    - "No planning identifier (COMP-07, D-01, D-02, D-04, D-11, D-12, plan numbers, C-xx, CR-xx) appears in any source or test file."
  flagged_assumptions:
    - "COMP-07's probe row is classified `unclassified` by the engine and is largely inapplicable: these are pure Swing rendering helpers with no length, equality, ordering or concurrency semantics of their own. The only behavioural deltas are the two deliberate colour changes in D-02 and D-04, which are covered by the truths above and must be called out at UAT. Flagged rather than silently dropped."
---

<objective>
Give the composer surface one home for its shared Swing helpers, so the six dialogs and three
schematic panels stop carrying private copies of the same six methods.

Purpose: COMP-07 (#619). `labeled()` is duplicated across all six dialogs, `errorLabel()` across
five, the unavailable-preview label across six, `clip()` across three panels,
`setEnabledRecursive()` across two dialogs, and `labeledWithError()` across two — the last of which
#619 does not name but D-01's "whole present-day family" covers. Two of these families are only
behaviourally identical, not textually identical, so this is an extract-and-reconcile job, not a
cut-paste. Two deliberate colour changes ride along: D-02 makes the error red theme-aware, and D-04
makes a stalled preview render in that same error colour instead of gray.
Output: `ComposerSwingHelpers`, nine call-site files, one new guard, one re-pointed guard.
</objective>

<shell_rules>
The permission hooks block opaque shell commands. Every task below must obey these:
- Use absolute paths from `/home/coder/repos/bbj-language-server/...`. Never `.`, `..` or `~`.
- Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`. `cd` is allowed ONLY directly in
  front of a build tool that needs its project directory
  (`cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test`).
- Use `git -C /home/coder/repos/bbj-language-server <subcommand>` for git.
- Prefer the Read tool over shell `cat`; scope any `grep` with `--include` to the exact subtree.
- Never read `.env*`, `*.pem`, `*.key`. Stage with `git add <exact path>`, never `git add -A`.
</shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-composer-robustness-consolidation/93-CONTEXT.md
@.planning/phases/93-composer-robustness-consolidation/93-PATTERNS.md
@.planning/phases/93-composer-robustness-consolidation/93-RESEARCH.md
</context>

<interface_context>
Every duplicate, with its exact current location:

| Helper | Copies | Locations |
|---|---|---|
| `private static JPanel labeled(String, JComponent)` | 6 | `MsgboxComposerDialog:345`, `AddWindowComposerDialog:349`, `AddChildWindowComposerDialog:363`, `SetoptsComposerDialog:318`, `SetoptsTriStateComposerDialog:265`, `CvsComposerDialog:264` |
| `private static JBLabel errorLabel()` | 5 | `MsgboxComposerDialog:338`, `AddWindowComposerDialog:356`, `AddChildWindowComposerDialog:370`, `SetoptsComposerDialog:311`, `CvsComposerDialog:257` |
| `private static JPanel labeledWithError(String, JComponent, JBLabel)` | 2 | `AddWindowComposerDialog:364`, `AddChildWindowComposerDialog:378` |
| `private static void setEnabledRecursive(JComponent, boolean)` | 2 | `AddWindowComposerDialog:215`, `AddChildWindowComposerDialog:222` |
| `private static String clip(Graphics2D, String, int)` | 3 | `MsgboxSchematicPanel:166`, `WindowSchematicPanel:118`, `ChildWindowSchematicPanel:145` |
| `private void previewUnavailable(String)` | 6 | `MsgboxComposerDialog:300`, `AddWindowComposerDialog:303`, `AddChildWindowComposerDialog:315`, `SetoptsComposerDialog:277`, `SetoptsTriStateComposerDialog:253`, `CvsComposerDialog:238` |

**Byte-identical, safe to cut-paste:** `labeled` (all six), `labeledWithError` (both),
`setEnabledRecursive` (both).

**Behaviourally identical but NOT textually identical — reconcile deliberately:**
- `clip`: `MsgboxSchematicPanel`'s copy caches `g.getFontMetrics()` into a local `FontMetrics fm` and
  reuses it; the other two call `g.getFontMetrics()` fresh on every reference and hoist the ellipsis
  into a local `String ell = "…"`. All three produce the same output. Keep the cached variant.
- `errorLabel`: `AddWindowComposerDialog` and `AddChildWindowComposerDialog` reference
  `com.intellij.util.ui.UIUtil.ComponentStyle.SMALL` fully-qualified; `MsgboxComposerDialog`,
  `SetoptsComposerDialog` and `CvsComposerDialog` use the imported `UIUtil.ComponentStyle.SMALL`.
  Normalise to the imported form. All five bodies then read:
  `JBLabel label = new JBLabel(" "); label.setComponentStyle(UIUtil.ComponentStyle.SMALL);
  label.setForeground(new Color(0xC0392B)); return label;` — and that hardcoded foreground is what
  D-02 replaces.

**`previewUnavailable` targets three different fields.** Each copy is two statements: set a label's
text to the prefix `"Preview unavailable — "` concatenated with the reason, then
`setOKActionEnabled(false)`. Targets: `summary` (`MsgboxComposerDialog`, `SetoptsComposerDialog`,
`CvsComposerDialog`), `flagsSummary` (`AddWindowComposerDialog`, `AddChildWindowComposerDialog`),
`blockPreview` (`SetoptsTriStateComposerDialog`). Five targets are `JBLabel`; `blockPreview` is a
`JBTextArea`, so the shared helper needs two overloads rather than an `instanceof` dispatch.

**The guard that breaks and must be re-pointed**
(`ComposerDialogRefreshSourceGuardTest:143-150`):
`eachDialogLabelsAFailureOnAFailedPreviewExactlyOnce()` asserts the literal
`"Preview unavailable — "` appears exactly once in each of the six dialog files. Once the prefix
lives in the shared home, every dialog file holds zero.

**Guards that must stay green without modification** —
`ComposerDialogRefreshSourceGuardTest:170-190` asserts `setOKActionEnabled(false)` appears exactly 3
times in each dialog. Keeping a one-line `previewUnavailable(String)` wrapper in each dialog, which
delegates the label update and then calls `setOKActionEnabled(false)` itself, preserves that count
and keeps the OK-gating discipline visible per dialog.
`SetoptsComposerDialogSourceGuardTest:195-208` asserts ordering around `previewUnavailable` inside
`SetoptsComposerDialog`'s `refresh()` body, which the retained wrapper also preserves.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Shared Swing home carrying one helper across all six dialogs</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java
  </files>
  <read_first>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java lines 345-370 (the `labeled`, `errorLabel` and `labeledWithError` bodies together),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java lines 310-324 (the imported-`UIUtil` variant of the same two helpers),
    bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmTokenTrustWindowSourceGuardTest.java lines 36-101 (the private `extractMethodBody`, `readGuardedSource`, `countOccurrences`, and the delegation-pin shape)
  </read_first>
  <action>
Create `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java` in
package `com.basis.bbj.intellij.composer` as `public final class ComposerSwingHelpers` with a private
constructor. Its javadoc states that it is the one home for composer Swing rendering helpers, so a
fix to any of these shapes is written once.

Give it one method in this task: `static JPanel labeled(String label, JComponent field)`, its body
copied byte-for-byte from `AddWindowComposerDialog:349-354` — a `JPanel` with a
`BorderLayout(0, JBUI.scale(2))`, a `JBLabel` in `BorderLayout.NORTH` and the field in
`BorderLayout.CENTER`. This helper is byte-identical in all six dialogs, which makes it the right
first slice: it proves the shared home, the import set and the call-site rewrite end-to-end with no
reconciliation risk.

In all six dialogs, delete the private `labeled` definition and rewrite every call site to
`ComposerSwingHelpers.labeled(...)`. Do not change any label string. `SetoptsTriStateComposerDialog`
passes a `JBScrollPane` as the field argument — that is already a `JComponent`, so the signature
needs no widening.

Create
`bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java`,
a plain JUnit 5 guard with its OWN private copies of `countOccurrences`, `extractMethodBody`,
`withoutCommentLines`, `readSource` and `UncheckedIOExceptionForTest` — never a shared test helper,
so a single bad edit can never weaken every guard at once. Apply `withoutCommentLines` before every
count-based assertion. Assert: `ComposerSwingHelpers.java` declares `static JPanel labeled(` exactly
once; each of the six dialog files declares it exactly zero times; and each of the six dialog files
calls `ComposerSwingHelpers.labeled(` at least once. Also assert `bbj-intellij/build.gradle.kts`
contains zero `TestFrameworkType` and zero `BasePlatformTestCase`.

Cite `#619` in rationale comments. Never write a requirement id, a decision id or a plan number into
source or test files.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.ComposerSwingHelpersSourceGuardTest" --tests "*.ComposerDialogRefreshSourceGuardTest" --tests "*.SetoptsComposerDialogSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output, or "cannot find symbol" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `ComposerSwingHelpers.java` exists and declares `static JPanel labeled(` exactly once.
    - Across the six dialog files combined, `private static JPanel labeled(` appears zero times.
    - Each of the six dialog files contains at least one `ComposerSwingHelpers.labeled(`.
    - `ComposerSwingHelpersSourceGuardTest.java` declares its own `private static int countOccurrences(` and `private static String extractMethodBody(`, and imports no shared test utility.
    - `ComposerDialogRefreshSourceGuardTest` and `SetoptsComposerDialogSourceGuardTest` both still pass unmodified.
  </acceptance_criteria>
  <done>The shared home exists and one helper is defined exactly once, reached by all six dialogs, with every existing guard still green.</done>
</task>

<task type="auto">
  <name>Task 2: Theme-aware error label, plus the error-row and enablement helpers</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java
  </files>
  <read_first>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java lines 80-92 (the nine `errorLabel()` field initialisers) and lines 211-230 (`updateEventEnabled` and `setEnabledRecursive`),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java lines 83-92 and lines 218-230 (the matching initialisers and the byte-identical `setEnabledRecursive`),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java lines 255-270 (the imported-`UIUtil` `errorLabel` variant)
  </read_first>
  <action>
Add three more methods to `ComposerSwingHelpers`:

`static Color errorForeground()` — the single definition of the composer's error colour, returning a
theme-aware colour rather than a fixed RGB triple. Use `NamedColorUtil.getErrorForeground()` from
`com.intellij.util.ui`; if that symbol does not resolve against the platform version this module
compiles against, fall back to a `JBColor.namedColor` lookup with an explicit light/dark pair. Record
in the method's javadoc which of the two was used and why, and cite `#619`. This method is the reason
the colour is now correct in Darcula as well as Light — previously every copy carried the same
hardcoded light-theme red.

`static JBLabel errorLabel()` — the body of the five existing copies, normalised to the imported
`UIUtil.ComponentStyle.SMALL` form, with the foreground taken from `errorForeground()` instead of a
constructed colour. Keep the `new JBLabel(" ")` single-space initial text: it is what holds the
label's height stable so the form does not jump when an error appears or clears.

`static JPanel labeledWithError(String label, JComponent field, JBLabel error)` — copied
byte-for-byte from `AddWindowComposerDialog:364-370`, a `labeled`-shaped panel with the error label
added in `BorderLayout.SOUTH`.

`static void setEnabledRecursive(JComponent c, boolean enabled)` — copied byte-for-byte from
`AddWindowComposerDialog:215-222`, including its recursive descent over `getComponents()`. It is
byte-identical in both dialogs today.

Delete all five `errorLabel` definitions, both `labeledWithError` definitions and both
`setEnabledRecursive` definitions, and rewrite every call site to the `ComposerSwingHelpers.` form.
Remove the now-unused `java.awt.Color` import from any dialog that no longer references it, and
remove any `UIUtil` import left unused. Do not change any field name, label string or panel layout.

Extend `ComposerSwingHelpersSourceGuardTest` with: each of the four new methods declared exactly once
in the shared home; zero private definitions of any of them across the six dialog files; at least one
delegating call site per dialog that used the helper; exactly one call to the theme-aware colour
lookup inside the extracted body of `errorForeground(`; and — swept at full breadth across all six
dialog files and all three schematic panel files — zero occurrences of the hardcoded-red constructor
expression those copies used. Apply `withoutCommentLines` before that negative sweep, and describe
the forbidden expression in prose in this test's own comments rather than restating it, so a
rationale sentence can never satisfy the assertion it guards.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.ComposerSwingHelpersSourceGuardTest" --tests "*.ComposerFieldValidationSourceGuardTest" --tests "*.ComposerDialogRefreshSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output, or "cannot find symbol" / "NamedColorUtil" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `ComposerSwingHelpers.java` declares `errorForeground(`, `errorLabel(`, `labeledWithError(` and `setEnabledRecursive(` exactly once each.
    - Across the six dialog files and three panel files combined, `new Color(0xC0392B)` appears exactly zero times.
    - Across the six dialog files combined, `private static JBLabel errorLabel(`, `private static JPanel labeledWithError(` and `private static void setEnabledRecursive(` each appear zero times.
    - `ComposerSwingHelpers.java` contains zero occurrences of `com.intellij.util.ui.UIUtil.ComponentStyle` — the fully-qualified form is normalised away in favour of the import.
    - `ComposerFieldValidationSourceGuardTest` still passes: each addWindow dialog still reads each `p.*Error` field exactly once and still holds no validation message of its own.
  </acceptance_criteria>
  <done>Error styling has one theme-aware definition, and the error-row and enablement helpers exist exactly once.</done>
</task>

<task type="auto">
  <name>Task 3: Shared clip and unavailable-preview label, with the refresh guard re-pointed</name>
  <files>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerSwingHelpers.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxSchematicPanel.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/WindowSchematicPanel.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ChildWindowSchematicPanel.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/AddChildWindowComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java,
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerSwingHelpersSourceGuardTest.java,
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java
  </files>
  <read_first>
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxSchematicPanel.java lines 160-180 (the cached-`FontMetrics` clip variant to keep),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/WindowSchematicPanel.java lines 114-132 (the repeated-call variant to discard),
    bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java lines 246-263 (the `JBTextArea` unavailable-preview target, the only non-`JBLabel` one),
    bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java lines 140-190 (the per-file prefix count to re-point, and the three-disable count that must stay intact)
  </read_first>
  <action>
Add `static String clip(Graphics2D g, String s, int maxWidth)` to `ComposerSwingHelpers`, using
`MsgboxSchematicPanel`'s variant verbatim — the one that caches `g.getFontMetrics()` into a local
`FontMetrics fm` and reuses it across the width comparisons. This is a deliberate pick between two
behaviourally equivalent copies: the cached variant avoids a repeated accessor call inside a
per-glyph loop. Note that choice in the method's javadoc so a later reader does not mistake it for an
accident. Delete all three private `clip` definitions and rewrite the four call sites
(`MsgboxSchematicPanel:67` and `:119`, `WindowSchematicPanel:74`, `ChildWindowSchematicPanel:118`) to
`ComposerSwingHelpers.clip(...)`.

Add two overloads for the stalled-preview label, so no `instanceof` dispatch is needed:
`static void previewUnavailable(JBLabel target, String reason)` and
`static void previewUnavailable(JBTextArea target, String reason)`. Both delegate to one private
`previewUnavailableText(String reason)` holding the prefix literal exactly once, and both set the
target's foreground from `errorForeground()`. That foreground is the D-04 change: a stalled preview
now renders in the error colour instead of default gray, making an un-previewable dialog visibly
distinct.

In each of the six dialogs, keep the existing `private void previewUnavailable(String reason)`
method but reduce its body to two statements: delegate to the shared overload with that dialog's own
target field, then `setOKActionEnabled(false)`. Do NOT move `setOKActionEnabled(false)` into the
shared helper — the helper owns text and colour only, and keeping the OK refusal in each dialog
preserves both the visible per-dialog gating discipline and the exactly-three
`setOKActionEnabled(false)` count that `ComposerDialogRefreshSourceGuardTest:170-190` pins. Targets:
`summary` for `MsgboxComposerDialog`, `SetoptsComposerDialog` and `CvsComposerDialog`; `flagsSummary`
for `AddWindowComposerDialog` and `AddChildWindowComposerDialog`; `blockPreview` for
`SetoptsTriStateComposerDialog`.

Each dialog's `apply(...)` method must clear the error colour when a successful preview arrives,
restoring the target's default foreground — otherwise the label stays red after recovery. Capture
each target's default foreground once at construction, as `CvsComposerDialog` already does for
`charsFieldDefaultForeground` at `:160-161`, and restore it in `apply`.

Re-point `ComposerDialogRefreshSourceGuardTest.eachDialogLabelsAFailureOnAFailedPreviewExactlyOnce()`:
assert the prefix literal appears exactly once inside the extracted body of the shared home's
`previewUnavailableText(` method, exactly zero times across all six dialog files, and that each of the
six dialog files calls `ComposerSwingHelpers.previewUnavailable(` exactly once — a delegation pin per
dialog, sweeping all six at full breadth. Leave every other test in that class untouched.

Extend `ComposerSwingHelpersSourceGuardTest` with: `clip(` declared exactly once in the shared home
and zero times across the three panel files; each panel calling `ComposerSwingHelpers.clip(` at least
once; and the two `previewUnavailable` overloads declared exactly once each.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test</automated>
    <fails_when>non-zero exit, or "tests completed" with a non-zero "failed" count in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `ComposerSwingHelpers.java` declares `clip(` exactly once and contains `FontMetrics fm` exactly once.
    - Across the three schematic panel files combined, `private static String clip(` appears zero times, and each panel contains at least one `ComposerSwingHelpers.clip(`.
    - Across the six dialog files combined, the literal `Preview unavailable — ` appears zero times; `ComposerSwingHelpers.java` contains it exactly once.
    - Each of the six dialog files still contains `setOKActionEnabled(false)` exactly three times.
    - Each of the six dialog files contains `ComposerSwingHelpers.previewUnavailable(` exactly once.
    - `./gradlew test` exits 0 across the whole IntelliJ suite.
  </acceptance_criteria>
  <done>All six helpers exist exactly once, the two deliberate colour changes are in place, and the whole IntelliJ suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server preview response → composer dialog labels | server-supplied reason text is rendered into Swing labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-13 | Spoofing | stalled-preview label | medium | mitigate | A stalled preview now renders in the error colour rather than default gray, so a dialog showing a stale or failed preview cannot be mistaken for one showing a current, valid one |
| T-93-14 | Tampering | per-dialog OK gating | high | mitigate | `setOKActionEnabled(false)` deliberately stays in each dialog's own wrapper rather than moving into the shared helper, so consolidating the label update cannot accidentally drop a dialog's refusal to accept an un-previewed selection; pinned by the exactly-three count per dialog |
| T-93-15 | Information Disclosure | reason text rendered into a label | low | accept | The reason comes from `ComposerNotices.shortReason`, which already collapses and truncates a throwable's message to 80 characters; this plan changes the colour, not the content |
| T-93-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan adds zero packages to `bbj-intellij/build.gradle.kts`; no install task exists, so the package-legitimacy gate has nothing to clear |
</threat_model>

<artifacts_this_phase_produces>
Phase 93 creates these symbols across all eight plans. This plan (93-01) produces the shared Swing
home; the rest are listed so no plan re-creates or renames another's artifact.

**New shared classes (4):** `composer/ComposerSwingHelpers.java` (this plan),
`composer/AddWindowFamilyComposerDialogBase.java` (93-06), `composer/ComposerIntentionBase.java`
(93-04), `actions/BbjComposeActionBase.java` (93-07).

**New plain-Java seams (2):** `composer/ComposerEditRanges.java` (93-03),
`composer/ComposerCatalogsCheck.java` (93-05).

**Thin subclasses (11, existing files, names unchanged):**
`composer/Configure{Msgbox,AddWindow,AddChildWindow,SetoptsInCode,Cvs}Intention.java` (93-04);
`actions/BbjCompose{Msgbox,AddWindow,AddChildWindow,Setopts,SetoptsInCode,Cvs}Action.java` (93-07).

**New enum constant:** `ComposerNotices.Reason.MALFORMED_EDIT` + `malformedEdit(String)` (93-03).

**New DTO fields:** `valid` and `rawTailError` on `SetOptsPreview` (TypeScript) and
`ComposerModels.SetoptsPreview` (Java); `valid` on the composeTriState result on both hosts (93-02).

**New source-guard test classes (6):** `ComposerSwingHelpersSourceGuardTest` (this plan),
`ComposerEditRangesTest` + `ComposerLauncherRangeGuardSourceGuardTest` (93-03),
`ComposerIntentionBaseSourceGuardTest` (93-04), `ComposerCatalogsCheckTest` +
`ComposerCatalogsShapeSourceGuardTest` (93-05),
`AddWindowFamilyComposerDialogBaseSourceGuardTest` (93-06), `BbjComposeActionBaseSourceGuardTest`
(93-07).
</artifacts_this_phase_produces>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test` exits 0.
- `ComposerFieldValidationSourceGuardTest`, `SetoptsComposerDialogSourceGuardTest` and
  `ComposerApplyGuardSourceGuardTest` all pass — proof the extraction did not disturb the validation,
  SETOPTS-refresh or write-path invariants already pinned over these dialogs.
- The two deliberate visual changes must be reported at UAT as intended deltas in BOTH Light and
  Darcula, never as no-observable-delta: error text renders a theme-aware red, and a stalled preview
  renders red instead of gray.
</verification>

<success_criteria>
`clip`, `labeled`, `labeledWithError`, `errorLabel`, `setEnabledRecursive` and the
unavailable-preview label each have exactly one definition in the source tree; all six dialogs and
all three schematic panels reach them; the error colour is theme-aware and a stalled preview is
visibly red; every dialog still refuses OK exactly as before; the whole IntelliJ JUnit suite is green.
</success_criteria>

<output>
Create `.planning/phases/93-composer-robustness-consolidation/93-01-SUMMARY.md` when done. Record
which theme-aware colour lookup compiled (`NamedColorUtil.getErrorForeground()` or the
`JBColor.namedColor` fallback), state plainly that `clip` was reconciled to the cached-`FontMetrics`
variant as a deliberate pick between two equivalent copies, and list the two visual changes for the
UAT round so they are reported as intended rather than as regressions.
</output>
</content>
