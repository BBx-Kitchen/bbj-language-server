---
phase: 89-cvs-composer-msgbox-expressions-composer-discoverability
plan: 12
type: execute
wave: 6
depends_on: ["89-08", "89-10", "89-11"]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLensKindsTest.java
  - bbj-intellij/src/main/resources/META-INF/plugin.xml
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeRegistrationTest.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeSetoptsAction.java
autonomous: true
requirements: [DISC-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Clicking a `Compose CVS()` cue in IntelliJ opens the CVS() composer for that call, because `ComposerLensKinds` maps `cvs` to `ComposerLauncher.Kind.CVS`; the existing cross-language contract test confirms the `cvs` kind string exists in the server's cue contract."
    - "The `BBx Config` language is mapped to the BBj language server under language id `bbx-config` — never `bbj`. LSP4IJ therefore renders the server's config-file `Compose SETOPTS` cue as Code Vision, and a click opens the existing config SETOPTS dialog through `ComposerLauncher.Kind.SETOPTS` (D-02, D-07's server-data rule)."
    - "Phase 84's invariant is re-pinned in refined form: exactly two LSP4IJ language mappings exist — BBj as `bbj`, BBx Config as `bbx-config` — so a config file is never sent as BBj source and still gets no BBj parsing or diagnostics."
    - "The config mapping runs only when 89-06-SUMMARY.md records both `Code Vision spike: GO` and `Config routing decision: route`. On NO-GO the D-07 gutter fallback is planned via gap closure first."
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java"
      provides: "cvs mapped to Kind.CVS"
    - path: "bbj-intellij/src/main/resources/META-INF/plugin.xml"
      provides: "BBx Config languageMapping with languageId bbx-config"
    - path: "bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeRegistrationTest.java"
      provides: "refined two-mapping invariant"
  key_links:
    - from: "bbj-intellij/src/main/resources/META-INF/plugin.xml"
      to: "bbj-vscode/src/language/composer-codelens-handler.ts"
      via: "LSP4IJ sends config documents with languageId bbx-config, which the handler answers from raw text"
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      via: "cvs → Kind.CVS and setopts-config → Kind.SETOPTS"
---

<objective>
Finish the IntelliJ side of the shared cue (DISC-01, #650). This plan makes two changes:
- CVS cue clicks now route to the CVS() composer built in plans 89-08 and 89-10;
- the `BBx Config` language is mapped to the BBj language server under its own `bbx-config` id, so LSP4IJ
  renders the server's config-file SETOPTS cue as Code Vision (D-02).
The server side and VS Code side of config routing landed in plan 89-11, per the human decision recorded in
plan 89-06.

Phase 84's registration guard is refined rather than removed: a config file still never reaches the server
as BBj source.

Purpose: all five composer kinds, including config.bbx SETOPTS, get a clickable cue in IntelliJ.
Output: the `cvs` kind mapping, the BBx Config language mapping, the refined registration test, and a
corrected Javadoc.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-06-SUMMARY.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-11-SUMMARY.md

<interfaces>
Verified during planning.
- `ComposerLensKinds` (plan 89-04): an immutable map of five wire kinds, `launcherKindOf`, `mappedWireKinds()`;
  `ComposerLensKindsTest` asserts `cvs` is empty today. `ComposerLauncher.Kind.CVS` exists after plan 89-08.
  `ComposerLensCommandContractTest` asserts every mapped wire kind appears quoted in `composer-lens-contract.ts`,
  so adding `cvs` needs no change there.
- plugin.xml's LSP4IJ block has exactly one `<languageMapping language="BBj" serverId="bbjLanguageServer" languageId="bbj"/>`.
  The config file type is `<fileType name="BBx Config" … language="BBx Config" fileNamesCaseInsensitive="config.bbx;config.min"/>`,
  with runtime override by `BbjConfigFileTypeOverrider`.
- `BbjConfigFileTypeRegistrationTest`:
  - class Javadoc: "exactly one LSP4IJ language mapping -- naming BBj -- so a config file never reaches the language server";
  - test `exactlyOneLanguageMappingExistsAndItNamesBbj`;
  - constants `CONFIG_LANGUAGE_ID = "BBx Config"` and `BBJ_LANGUAGE_ID = "BBj"`;
  - helpers `readPluginXml()` and `elementsByTagName(document, tag)`.
- `BbjComposeSetoptsAction.java` Javadoc says the config language "has no parser and is unmapped to the server".
- The server answers `bbx-config` codeLens requests from raw text and never builds such documents (plan 89-11).
  LSP4IJ renders code lenses through its own globally registered Code Vision provider, and the cue click
  reaches `BbjOpenComposerAtAction`, which maps `setopts-config` to `Kind.SETOPTS` (plan 89-04).

Shell rules: absolute paths only; never chain `cd` with grep/find/cat/sed/head/tail/git; use
`git -C /home/coder/repos/bbj-language-server`; Gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew … --offline`.
Comment discipline: no plan ids, decision ids or threat ids in source or test comments; #650 is fine.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates:
- `ComposerLensKinds`: mapping `cvs` → `ComposerLauncher.Kind.CVS` (six wire kinds mapped).
- plugin.xml: `<languageMapping language="BBx Config" serverId="bbjLanguageServer" languageId="bbx-config"/>`.
- `BbjConfigFileTypeRegistrationTest`: `theBbjLanguageIsMappedAsBbjAndTheConfigLanguageOnlyAsBbxConfig`, replacing the single-mapping test.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A CVS cue click in IntelliJ opens the CVS() composer</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLensKindsTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLensKindsTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLensCommandContractTest.java — the kind-literal check this mapping must satisfy
  </read_first>
  <behavior>
    - `ComposerLensKinds.launcherKindOf("cvs")` returns `ComposerLauncher.Kind.CVS`.
    - `mappedWireKinds()` returns exactly `msgbox`, `addwindow`, `addchildwindow`, `cvs`, `setopts-in-code` and `setopts-config`.
    - `ComposerLensCommandContractTest` still passes, finding `'cvs'` quoted in `composer-lens-contract.ts`.
  </behavior>
  <action>
    Update `ComposerLensKindsTest` first: the `cvs` expectation changes from empty to `Kind.CVS`, and the
    mapped set has six entries. Then add `cvs` → `ComposerLauncher.Kind.CVS` to the map in
    `ComposerLensKinds.java`. The action class needs no change — it already routes every mapped kind through
    `launchAt(..., true)`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerLensKindsTest' --tests 'com.basis.bbj.intellij.composer.ComposerLensCommandContractTest' --tests 'com.basis.bbj.intellij.actions.BbjOpenComposerAtActionSourceGuardTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "Kind.CVS" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLensKinds.java` prints one line.
    - All three test classes pass.
  </acceptance_criteria>
  <done>Every `.bbj` composer cue, CVS included, opens its composer when clicked in IntelliJ.</done>
</task>

<task type="auto">
  <name>Task 2: Map BBx Config to the server as `bbx-config` and re-pin Phase 84's invariant in refined form</name>
  <reversibility rating="costly">Changes a pinned Phase 84 registration invariant; undoing it must happen together with plan 89-11's server filter and VS Code selector, or the config cue disappears in one IDE only.</reversibility>
  <precondition>/home/coder/repos/bbj-language-server/.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-06-SUMMARY.md contains a line starting `Code Vision spike: GO` and the line `Config routing decision: route`</precondition>
  <files>bbj-intellij/src/main/resources/META-INF/plugin.xml, bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeRegistrationTest.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeSetoptsAction.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/config/BbjConfigFileTypeRegistrationTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml lines 150-170 and 279-295 — the config file type and the LSP4IJ block
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeSetoptsAction.java — the Javadoc to correct
    - /home/coder/repos/bbj-language-server/.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-11-SUMMARY.md — the server-side filter rule this mapping depends on
  </read_first>
  <action>
    In `BbjConfigFileTypeRegistrationTest` replace `exactlyOneLanguageMappingExistsAndItNamesBbj` with
    `theBbjLanguageIsMappedAsBbjAndTheConfigLanguageOnlyAsBbxConfig`, asserting:
    - exactly two `languageMapping` elements exist;
    - the one with `language` equal to `BBJ_LANGUAGE_ID` has `languageId` `bbj`;
    - the one with `language` equal to `CONFIG_LANGUAGE_ID` has `languageId` `bbx-config`;
    - both name `serverId` `bbjLanguageServer`;
    - no mapping pairs the config language with `languageId` `bbj`.
    Rewrite the class Javadoc's last clause to: exactly two LSP4IJ language mappings — BBj as BBj source, and
    the config language only under its own `bbx-config` id, which the language server answers for the
    composer cue and never parses or diagnoses (#650).

    In plugin.xml add `<languageMapping language="BBx Config" serverId="bbjLanguageServer" languageId="bbx-config"/>`
    directly after the BBj mapping.

    In `BbjComposeSetoptsAction.java` correct the Javadoc phrase "unmapped to the server": the config language
    reaches the server only under the `bbx-config` language id, which the server never parses. The class still
    scans no syntax tree. Change no code in that class.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.config.*'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline</automated>
    <fails_when>non-zero exit or "BUILD FAILED" — another plugin.xml guard needs reconciling, never weakening</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "languageId=\"bbx-config\"" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml` prints one line.
    - `grep -c "<languageMapping" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml` prints 2.
    - The refined registration test and the full IntelliJ run pass.
  </acceptance_criteria>
  <done>IntelliJ receives the server's config-file SETOPTS cue as Code Vision, and the config file still never reaches the server as BBj source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ LSP4IJ → language server | Config documents now cross under `bbx-config` |
| cue click → config SETOPTS dialog | The existing guarded config composer is reached from a cue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-38 | Tampering | a config file mapped as BBj source, gaining BBj diagnostics and edits | high | mitigate | The mapping uses `languageId="bbx-config"`; the refined registration test forbids any config→`bbj` pairing; the server filter (plan 89-11) never builds `bbx-config` documents |
| T-89-39 | Denial of Service | LSP4IJ features on config files waiting on the server | medium | mitigate | Unknown-document requests reject immediately server-side, codeAction is bounded, and the precondition requires the recorded Code Vision GO; plan 89-13's human check watches the Language Servers console for errors on a config file |
| T-89-40 | Repudiation | shipping the config mapping on a NO-GO or non-route decision | medium | mitigate | Task 2's precondition checks both recorded lines verbatim and halts otherwise |
| T-89-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline`
2. Register check: inspect `git -C /home/coder/repos/bbj-language-server diff` for plan, decision or threat ids in source/test comments.
3. Visible config-file Code Vision in a running IntelliJ is covered by plan 89-13's end-of-phase human checks.
</verification>

<success_criteria>
- CVS cues click through in IntelliJ.
- Config files are mapped to the server only as `bbx-config`, so their SETOPTS cue renders (D-02).
- Phase 84's protection is re-pinned in refined form.
</success_criteria>

<output>
Create `.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-12-SUMMARY.md` when done.
Record both precondition lines as found, the refined invariant wording, and any other guard touched.
</output>
