---
phase: 89-cvs-composer-msgbox-expressions-composer-discoverability
plan: 10
type: execute
wave: 5
depends_on: ["89-08"]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/before.bbj.template
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/after.bbj.template
  - bbj-intellij/src/main/resources/META-INF/plugin.xml
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeCvsAction.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/BbjComposeCvsActionSourceGuardTest.java
autonomous: true
requirements: [DISC-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With the caret on a `CVS(` call in a `.bbj` file, Alt+Enter offers `Configure CVS() options…`. It opens the CVS dialog through `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.CVS)`: edit-in-place for a literal-mask call, the server's reason for any other mask (D-14)."
    - "The intention ships `intentionDescriptions/ConfigureCvsIntention/` with a non-blank `description.html` and before/after templates, returns exactly one HTML intention preview, and does not start in a write action, so opening the lightbulb never throws `Intention Description Dir URL is null`."
    - "The editor context menu offers `Compose CVS()…` (action id `bbj.composeCvs`, the same identifier as the VS Code command), reaching the same launcher kind, with no keyboard shortcut."
    - "The lightbulb check is a synchronous caret-line text heuristic with no LSP round trip; whether a call is editable is always decided by the server's `bbj/composer/cvs/decodeCall`, with no Java-side CVS parsing."
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java"
      provides: "Alt+Enter entry into the CVS() composer"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeCvsAction.java"
      provides: "editor context-menu entry into the CVS() composer"
    - path: "bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html"
      provides: "intention description resource"
  key_links:
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      via: "ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.CVS)"
    - from: "bbj-intellij/src/main/resources/META-INF/plugin.xml"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java"
      via: "intentionAction registration for language BBj"
---

<objective>
Give IntelliJ users two direct ways into the CVS() composer built in plan 89-08 (DISC-03, #649):
- an Alt+Enter intention on a `CVS(` call, modelled exactly on `ConfigureMsgboxIntention`, shipping its
  description resources so the lightbulb preview never fails (the Phase 82 G-82-6 lesson);
- an editor context-menu action, modelled on `BbjComposeMsgboxAction`.

Both call the same launcher kind. The server's decode decides edit versus reason (D-14). The always-visible
cue click for CVS is wired in plan 89-12.

Purpose: DISC-03 reachability in IntelliJ.
Output: `ConfigureCvsIntention` with its resources, `BbjComposeCvsAction`, the plugin.xml registrations, and updated or new source guards.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-08-SUMMARY.md

<interfaces>
Verified during planning.
- `ConfigureMsgboxIntention.java` (template):
  - `getText()` returns `Configure MSGBOX options…`; `getFamilyName()` returns `BBj visual composer`;
  - `isAvailable` returns `editor != null && ComposerLauncher.isCaretOnCall(editor, "msgbox")`;
  - `invoke` calls `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.MSGBOX)`;
  - `startInWriteAction()` returns false;
  - `generatePreview` returns one `IntentionPreviewInfo.Html`.
- `intentionDescriptions/ConfigureMsgboxIntention/` holds `description.html` (with a `<!-- tooltip end -->`
  marker), `before.bbj.template` and `after.bbj.template`. `IntentionDescriptionResourcesTest` enumerates
  every `<intentionAction>` in plugin.xml and requires this directory, including a non-blank `description.html`.
- `ComposerLauncher.isCaretOnCall(editor, keyword)` does a lower-cased caret-line scan and requires a
  non-identifier character before the keyword; keywords ending in `(` get a trailing boundary for free.
  `ComposerLauncher.Kind.CVS` exists after plan 89-08.
- `ComposerIntentionPreviewSourceGuardTest` names four intention source paths and asserts HTML previews,
  `startInWriteAction` false, unchanged `invoke`/`isAvailable`, and plugin.xml registering all four
  intentions exactly once.
- `BbjComposeMsgboxAction.java` (template): `AnAction`; `actionPerformed` resolves project and editor and
  calls `ComposerLauncher.launch`; `update` uses `setEnabledAndVisible(project != null && editor != null)`;
  `getActionUpdateThread()` returns BGT.
- `BbjComposeSetoptsInCodeActionSourceGuardTest` is the action source-guard template (comment stripping,
  counts, plugin.xml element slice).
- plugin.xml: the intentions sit in one block (MSGBOX, addWindow, addChildWindow, SETOPTS-in-code); the
  composer actions sit in one block ending with `bbj.composeSetoptsInCode` and `bbj.openComposerAt`.

Shell rules: absolute paths only; never chain `cd` with grep/find/cat/sed/head/tail/git; use
`git -C /home/coder/repos/bbj-language-server`; Gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew … --offline`.
Comment discipline: no plan ids, decision ids or threat ids in source, test or resource comments; #649 is fine.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates:
- `composer/ConfigureCvsIntention implements IntentionAction` — text `Configure CVS() options…`, family `BBj visual composer`.
- Resources `intentionDescriptions/ConfigureCvsIntention/description.html`, `before.bbj.template` and `after.bbj.template`.
- `actions/BbjComposeCvsAction extends AnAction` — IntelliJ action id `bbj.composeCvs`, text `Compose CVS()…`, in `EditorPopupMenu`.
- Test `actions/BbjComposeCvsActionSourceGuardTest`; `ComposerIntentionPreviewSourceGuardTest` extended to five intentions.

<tasks>

<task type="tracer">
  <name>Task 1: Alt+Enter on a CVS( call opens the CVS() composer, with description resources shipped</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/before.bbj.template, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/after.bbj.template, bbj-intellij/src/main/resources/META-INF/plugin.xml</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html — the resource format
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/before.bbj.template and after.bbj.template
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/IntentionDescriptionResourcesTest.java — what it requires
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml lines 124-150 — the intention block
  </read_first>
  <action>
    Create `ConfigureCvsIntention.java` as a copy of `ConfigureMsgboxIntention`'s structure:
    - `getText()` returns `Configure CVS() options…`;
    - `getFamilyName()` returns `BBj visual composer`;
    - `isAvailable` returns `editor != null && ComposerLauncher.isCaretOnCall(editor, "cvs(")`;
    - `invoke` calls `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.CVS)` when the editor is non-null;
    - `startInWriteAction()` returns false, with the same comment reason;
    - `generatePreview` returns one `IntentionPreviewInfo.Html` saying it opens the BBj visual composer for
      the `CVS(...)` call under the caret, pre-filled when its mask is a sum of integer literals.
    Javadoc names #649 and states that the server's decode decides whether the call is editable.

    Create `intentionDescriptions/ConfigureCvsIntention/`:
    - `description.html`, with the MSGBOX file's structure and a `<!-- tooltip end -->` marker, explaining:
      - pre-fill from the current mask and chars argument;
      - the string argument is kept verbatim;
      - a mask using a variable or expression cannot be edited in place;
      - the language server must be running.
    - `before.bbj.template`: `trimmed$ = cvs("  name  ", 3)`.
    - `after.bbj.template`: `trimmed$ = cvs("  name  ", 7)`.

    Register the intention in plugin.xml right after `ConfigureSetoptsInCodeIntention`, as an
    `<intentionAction>` with `<language>BBj</language>`,
    `<className>com.basis.bbj.intellij.composer.ConfigureCvsIntention</className>` and `<category>BBj</category>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.IntentionDescriptionResourcesTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew compileJava --offline</automated>
    <fails_when>non-zero exit or any "error:" compiler line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "com.basis.bbj.intellij.composer.ConfigureCvsIntention" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml` prints one line.
    - `grep -n "Kind.CVS" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java` prints one line.
    - All three resource files exist and are non-empty; `IntentionDescriptionResourcesTest` passes.
  </acceptance_criteria>
  <done>An IntelliJ user can press Alt+Enter on a CVS() call and open the composer, and the lightbulb preview renders without an exception.</done>
</task>

<task type="auto">
  <name>Task 2: Editor context-menu entry for the CVS() composer, and source guards over both entry points</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeCvsAction.java, bbj-intellij/src/main/resources/META-INF/plugin.xml, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/BbjComposeCvsActionSourceGuardTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjComposeMsgboxAction.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/BbjComposeSetoptsInCodeActionSourceGuardTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml lines 60-110 — the composer action block
  </read_first>
  <action>
    Create `BbjComposeCvsAction.java` as a copy of `BbjComposeMsgboxAction`'s structure:
    - `actionPerformed` calls `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.CVS)`;
    - `update` calls `setEnabledAndVisible(project != null && editor != null)` once;
    - `getActionUpdateThread()` returns BGT.
    Its Javadoc names #649 and the position-aware behaviour.

    Register it in plugin.xml after `bbj.composeSetoptsInCode` as
    `<action id="bbj.composeCvs" class="com.basis.bbj.intellij.actions.BbjComposeCvsAction" text="Compose CVS()…" description="Visually compose or edit a CVS(...) call">`
    with `<add-to-group group-id="EditorPopupMenu" anchor="last"/>` and no keyboard shortcut.

    Extend `ComposerIntentionPreviewSourceGuardTest` to the fifth intention:
    - add the `ConfigureCvsIntention.java` path to every per-intention check;
    - rename `pluginXmlStillRegistersAllFourIntentionsUntouched` to `pluginXmlRegistersAllFiveIntentionsExactlyOnce`,
      adding the CVS class count of exactly one;
    - keep every existing assertion.

    Create `BbjComposeCvsActionSourceGuardTest`, reusing the sibling guard's helpers, pinning:
    - `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.CVS)` appears exactly once;
    - `update` calls `setEnabledAndVisible` exactly once and never `setEnabled(`;
    - `getActionUpdateThread` returns `ActionUpdateThread.BGT`;
    - the action contains no `Pattern.compile(` — the decode is the server's job;
    - the plugin.xml slice of the `bbj.composeCvs` element names this class, sits in `EditorPopupMenu`, and
      has no `keyboard-shortcut`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerIntentionPreviewSourceGuardTest' --tests 'com.basis.bbj.intellij.actions.BbjComposeCvsActionSourceGuardTest' --tests 'com.basis.bbj.intellij.composer.IntentionDescriptionResourcesTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline</automated>
    <fails_when>non-zero exit or "BUILD FAILED" — an existing plugin.xml-counting guard needs reconciling, never weakening</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "id=\"bbj.composeCvs\"" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml` prints one line.
    - `ComposerIntentionPreviewSourceGuardTest` covers five intentions and passes; the new action guard passes.
    - The full IntelliJ test run succeeds.
  </acceptance_criteria>
  <done>An IntelliJ user can open the CVS() composer from Alt+Enter on a call or from the editor context menu, and both doors are pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ intention search → `isAvailable` | Runs inside IntelliJ's single modal, EDT-blocking intention computation |
| intention/action → `ComposerLauncher` | New doors into the write-capable CVS composer flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-31 | Denial of Service | a slow `isAvailable` freezing the intention dialog | medium | mitigate | `isAvailable` uses only the synchronous caret-line heuristic `isCaretOnCall`, with no LSP call; the context-menu action provides a second door that bypasses intention search entirely |
| T-89-32 | Denial of Service | a missing description resource throwing on every lightbulb preview (the G-82-6 class) | medium | mitigate | Resources ship with the intention and `IntentionDescriptionResourcesTest` derives its subjects from plugin.xml; `generatePreview` returns HTML |
| T-89-33 | Tampering | a non-literal CVS mask rewritten from the lightbulb | medium | mitigate | The launcher opens a dialog only on the server's `editable: true` and renders the reason otherwise; the action contains no Java-side parsing, pinned by its source guard |
| T-89-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline`
2. Register check: inspect `git -C /home/coder/repos/bbj-language-server diff` for plan, decision or threat ids in source, test or resource comments.
</verification>

<success_criteria>
- The CVS() composer is reachable in IntelliJ from Alt+Enter and the editor context menu.
- Description resources ship with the intention.
- Editability stays a server decision.
</success_criteria>

<output>
Create `.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-10-SUMMARY.md` when done.
Record the intention text, the action id and label, and the renamed guard test.
</output>
