---
phase: 89-cvs-composer-msgbox-expressions-composer-discoverability
plan: 13
type: execute
wave: 7
depends_on: ["89-05", "89-08", "89-10", "89-11", "89-12"]
files_modified:
  - examples/issue650-composer-cues.bbj
  - bbj-vscode/test/functional/installed-extension-e2e.test.ts
  - QA/FULL-TEST-CHECKLIST.md
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On `examples/issue650-composer-cues.bbj`, the installed VS Code bundle serves cues for every `.bbj` composer kind — MSGBOX literal, constant-sum and expression calls; addWindow; addChildWindow; an editable CVS call; editable in-code SETOPTS — and none for the non-literal CVS call, the unsafe SETOPTS shape, the REM line or the string decoy."
    - "The installed bundle answers `textDocument/codeLens` on a `bbx-config` document with one `Compose SETOPTS` cue per SETOPTS line, settles a hover on that document within the codeLens budget without hanging, and publishes no non-empty diagnostics for it."
    - "Against the installed bundle, `bbj/composer/msgbox/decodeCall` on the fixture's expression-options line returns `replace` carrying that expression, and `bbj/composer/cvs/decodeCall` on the literal-mask line returns `editable: true` with the string argument verbatim."
    - "Both distributables are rebuilt from the final tree: the VSIX installed in the ext-test rig, and the IntelliJ zip carrying a freshly built `main.cjs`."
    - "`QA/FULL-TEST-CHECKLIST.md` has hand-check rows in both IDE sections for: the cue on all five composer kinds, including config.bbx; the CVS() composer (compose, edit in place, not-editable mask, disabled chars field); and MSGBOX expression handling (constant-sum pre-fill, compose-and-replace banner). The Phase 84 config rows describe config files reaching the server only as `bbx-config`."
    - statement: "Typing in a large .bbj file full of composer calls shows no added input lag after the cues ship, in either IDE (the perceptual half of Roadmap Success Criterion 5)"
      verification: backstop
  artifacts:
    - path: "examples/issue650-composer-cues.bbj"
      provides: "fixture covering every composer kind, including non-editable and decoy shapes"
    - path: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      provides: "installed-bundle assertions for all cue kinds, config documents and the new decode payloads"
    - path: "QA/FULL-TEST-CHECKLIST.md"
      provides: "manual rows for cues, the CVS composer and MSGBOX expressions in both IDEs"
  key_links:
    - from: "bbj-vscode/test/functional/installed-extension-e2e.test.ts"
      to: "installed bundle (~/.ext-test)"
      via: "spawned server over IPC, proving the shipped artifact rather than the source tree"
---

<objective>
Close the phase: prove the shipped VS Code bundle serves every cue and the new decode payloads, rebuild both
distributables from the final tree, and record the hand checks both IDEs need, in the project's QA checklist
and as end-of-phase human checks harvested into UAT.

This covers DISC-01 (#650), DISC-02 (#648) and DISC-03 (#649) together, including the perceptual half of
Roadmap Success Criterion 5, which only a human can observe.

Purpose: phase-level evidence against the shipped artifacts.
Output: an extended fixture, installed-bundle e2e assertions, rebuilt artifacts, and QA checklist rows.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-06-SUMMARY.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-11-SUMMARY.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-12-SUMMARY.md
@QA/FULL-TEST-CHECKLIST.md

<interfaces>
Verified during planning.
- `installed-extension-e2e.test.ts` has install resolution, a skip gate, a spawn/initialize/didOpen pattern and
  `findPosition`, and plan 89-06 added the `composer cues on the installed bundle` describe.
  `SETOPTS_DECODE_IN_CODE_METHOD` shows how custom requests are sent over the live connection.
- `examples/issue650-composer-cues.bbj` (plan 89-06) has addWindow lines, a shared-line pair, a REM-commented
  call and a string decoy. `examples/issue475-setopts-in-code.bbj` holds canonical safe and unsafe SETOPTS
  shapes to copy.
- Build and install:
  - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install`
    (writes `/tmp/bbj-lang.vsix` and installs into `~/.ext-test`);
  - `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q`
    → `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip`.
- `QA/FULL-TEST-CHECKLIST.md`:
  - "VS Code - LSP Features" rows run 1-16; "IntelliJ IDEA - LSP Features" rows run 1-22;
  - row format is `| N | Name | numbered steps with <br> | expected | [ ] |`;
  - VS Code row 10 mentions "the SETOPTS CodeLens"; IntelliJ rows 10-11 describe the config file type and
    it "not being sent to the language server as BBj source".
- The workflow's `human_verify_mode` is end-of-phase: human checks live in `<verify><human-check>` blocks,
  harvested into UAT.

Shell rules: absolute paths only; never chain `cd` with grep/find/cat/sed/head/tail/git; use
`git -C /home/coder/repos/bbj-language-server`; build tools only as `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode …`
and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`.
Comment discipline: no plan ids, decision ids or threat ids in source, test or fixture comments; issue numbers are fine.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates:
- The extended `examples/issue650-composer-cues.bbj`, covering MSGBOX (literal, `BBjMsgBox.*` sum, `flags%`
  expression), addWindow, addChildWindow, CVS (`1+4` editable, `n%` not editable), SETOPTS (absolute
  literal, canonical safe chain, an unsafe interrupted chain), a REM line and a string decoy.
- Installed-bundle e2e describe blocks: `every composer kind carries its cue`, `config documents are
  text-only`, and `new decode payloads on the installed bundle`.
- QA rows: VS Code rows 17-21 and IntelliJ rows 23-27, plus reworded VS Code row 10 and IntelliJ rows 10-11.

<tasks>

<task type="tracer">
  <name>Task 1: Prove every cue kind, text-only config documents and the new decode payloads on the rebuilt, installed bundle</name>
  <files>examples/issue650-composer-cues.bbj, bbj-vscode/test/functional/installed-extension-e2e.test.ts</files>
  <precondition>`bbj-ext-install` is on PATH and `/home/coder/.ext-test/extensions/extensions.json` exists.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/examples/issue650-composer-cues.bbj — the fixture from plan 89-06
    - /home/coder/repos/bbj-language-server/examples/issue475-setopts-in-code.bbj — safe and unsafe SETOPTS shapes to copy
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/installed-extension-e2e.test.ts — the whole file, including plan 89-06's describe
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/composer-lens-contract.ts — kinds, titles, config language id
  </read_first>
  <action>
    Extend `examples/issue650-composer-cues.bbj`, keeping its existing lines, with one line each for:
    - `r = MSGBOX("Save?", 36, "Confirm")`
    - `r = MSGBOX("Save?", BBjMsgBox.MSGBOX_BUTTONS_YES_NO+BBjMsgBox.MSGBOX_ICON_QUESTION, "Confirm")`
    - `r = MSGBOX("Save?", flags%, "Confirm")`
    - a `window!.addChildWindow(101, "Child", 10, 10, 200, 150, $00000000$)` call
    - `trimmed$ = CVS(name$, 1+4)`
    - `trimmed$ = CVS(name$, mode%)`
    - an absolute `SETOPTS $04$`
    - a canonical safe `b$=OPTS` / `b$=IOR(b$,$01$)` / `SETOPTS b$` chain
    - an interrupted chain copied from the #475 fixture (an `IF`/`FI` between the reassignment and SETOPTS)
    Keep the program valid BBj.

    In `installed-extension-e2e.test.ts` add three describes, gated like the others, each with its own spawned
    server killed in teardown and the generous timeouts the file already uses.

    1. `every composer kind carries its cue`: after `didOpen`, `textDocument/codeLens` returns cues whose
       `(kind, line)` pairs include exactly:
       - `msgbox` on the three MSGBOX lines;
       - `addwindow` on the addWindow lines;
       - `addchildwindow` on its line;
       - `cvs` on the `1+4` line only;
       - `setopts-in-code` on the absolute SETOPTS line and the safe chain's `SETOPTS b$` line only.
       No cue sits on the `mode%` CVS line, the interrupted chain's SETOPTS line, the REM line or the string
       decoy. Locate every line with `findPosition` by content.
    2. `config documents are text-only`:
       - register a `publishDiagnostics` listener before `didOpen`;
       - `didOpen` a uri built with `pathToFileURL` from `os.tmpdir()` for `issue650-config.bbx`, with
         `languageId: 'bbx-config'` and text `PREFIX "/tmp/"\nSETOPTS 00000080\n`;
       - `textDocument/codeLens` returns exactly one cue, kind `setopts-config`, on line 1;
       - a `textDocument/hover` on line 1 settles (resolved or rejected) within 6000 ms, measured with a
         `Promise.race` against a timer;
       - wait 3000 ms, then assert no `publishDiagnostics` for that uri carried a non-empty diagnostics array.
    3. `new decode payloads on the installed bundle`:
       - `bbj/composer/msgbox/decodeCall` with the `flags%` line text returns `found: true` and
         `replace.originalOptions === 'flags%'`;
       - `bbj/composer/cvs/decodeCall` with the `1+4` line text returns `editable: true`, `initial.str === 'name$'` and `initial.bits` `[1, 4]`.

    Then run, in order:
    - `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`;
    - `bbj-ext-install`;
    - the e2e file;
    - `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q`.
    Record the extension version, `installedTimestamp`, the zip path and the `main.cjs` timestamp.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build</automated>
    <fails_when>non-zero exit or any `error TS` line</fails_when>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/functional/installed-extension-e2e.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode</automated>
    <fails_when>non-zero exit, a failed test, or every test in the file reported as skipped</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>unzip -l /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip</automated>
    <fails_when>non-zero exit, or no listed entry ends in `language-server/main.cjs`</fails_when>
  </verify>
  <acceptance_criteria>
    - The fixture contains every shape listed under Artifacts.
    - The three new describes ran (non-zero pass count) against a bundle reinstalled after this plan's build, and passed.
    - The SUMMARY records the extension version, `installedTimestamp`, zip path and `main.cjs` timestamp.
  </acceptance_criteria>
  <done>The artifacts users will install are proven to serve every cue, keep config documents text-only, and carry the new MSGBOX and CVS decode payloads.</done>
</task>

<task type="auto">
  <name>Task 2: QA checklist rows for cues, the CVS() composer and MSGBOX expressions in both IDEs, with end-of-phase human checks</name>
  <files>QA/FULL-TEST-CHECKLIST.md</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md — the whole file; row format and existing SETOPTS/config rows
    - /home/coder/repos/bbj-language-server/.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-06-SUMMARY.md — the recorded spike outcome and routing decision
  </read_first>
  <action>
    Append to "VS Code - LSP Features", in the existing format:
    - Row 17 — Composer cues on every composer line: open `examples/issue650-composer-cues.bbj` without clicking into it. Expected:
      - `Compose MSGBOX`, `Compose addWindow`, `Compose addChildWindow`, `Compose CVS()` and `Compose SETOPTS` appear above the matching lines;
      - none appear on the `mode%` CVS line, the interrupted chain, the REM line or the string decoy;
      - clicking each opens its composer for that call, and cancel leaves the file unchanged.
    - Row 18 — Config-file SETOPTS cue: open the resolved config file. Expected:
      - exactly one `Compose SETOPTS` cue per SETOPTS line and no BBj diagnostics;
      - clicking it opens the SETOPTS composer.
    - Row 19 — CVS() composer compose and edit:
      - compose a new call with bits 1 and 4 plus a chars value and confirm the call is inserted;
      - edit the `1+4` call, confirm the string argument is untouched and only the call changes;
      - confirm the chars field is disabled while only bit 4 is checked;
      - confirm the `mode%` call offers no lightbulb.
    - Row 20 — MSGBOX constant-sum pre-fill: on the `BBjMsgBox.*` sum line the composer opens pre-filled
      (Yes/No, Question) with no banner.
    - Row 21 — MSGBOX compose-and-replace: on the `flags%` line the composer shows the
      "Could not decode this options expression — composing will replace it." banner and the original
      `flags%`. Apply replaces the options with no extra confirmation, and the message and title are preserved.

    Append to "IntelliJ IDEA - LSP Features", in the same format:
    - Row 23 — Code Vision composer cues: cues on the same lines as VS Code row 17, rendered as Code Vision, and each click opens its dialog.
    - Row 24 — Config-file SETOPTS Code Vision cue: one cue per SETOPTS line; a click opens the SETOPTS dialog;
      the Language Servers console shows no errors for the config file.
    - Row 25 — CVS() dialog via Alt+Enter and the context menu: compose, edit in place, disabled chars field,
      and the reason notice on `mode%`.
    - Row 26 — MSGBOX constant-sum pre-fill in the dialog.
    - Row 27 — MSGBOX compose-and-replace banner with read-only original in the dialog.

    Reword VS Code row 10's "the SETOPTS CodeLens" to "the `Compose SETOPTS` cue". Reword IntelliJ rows 10-11
    so the expected result says the config file is never treated as BBj source and gets no BBj diagnostics,
    and that it may appear under the BBj server in the Language Servers window with language id `bbx-config`.
    Change no other row.
  </action>
  <verify>
    <automated>grep -n "Compose CVS()" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit or fewer than 2 matching lines (one per IDE section)</fails_when>
    <automated>grep -n "composing will replace it" /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>non-zero exit or fewer than 2 matching lines</fails_when>
    <human-check>
      <test>Install `/tmp/bbj-lang.vsix` in VS Code and `bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip` in IntelliJ 2024.2 or later, then run VS Code rows 17-21 and IntelliJ rows 23-27 of QA/FULL-TEST-CHECKLIST.md.</test>
      <expected>Every row's expected result holds in both IDEs; every cue appears without placing the caret and opens the matching composer for its own call.</expected>
      <why_human>Code Vision and CodeLens rendering, dialog layout and click-through happen in running IDEs this devcontainer cannot drive headlessly.</why_human>
    </human-check>
    <human-check>
      <test>In both IDEs, open a copy of a large .bbj file (several thousand lines) containing many MSGBOX, addWindow and CVS calls, and type steadily for 30 seconds in the middle of the file.</test>
      <expected>No visible input lag or stutter compared with the same file before the cue feature, and cues settle after typing pauses.</expected>
      <why_human>Roadmap Success Criterion 5's no-added-typing-lag half is perceptual; the automated structural test only proves no re-parse per request.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - VS Code rows 17-21 and IntelliJ rows 23-27 exist in the checklist's row format, with numbered steps and explicit expected results.
    - VS Code row 10 and IntelliJ rows 10-11 carry the reworded config wording; no other existing row changed (`git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md` shows only those edits plus the new rows).
  </acceptance_criteria>
  <done>The project's manual checklist and the phase's UAT carry every hand check the new cues, the CVS composer and MSGBOX expressions need in both IDEs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → installed artifacts | Evidence must come from what users install |
| automated evidence → human UAT | Perceptual and rendering claims need human confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-41 | Repudiation | phase closed on source-tree tests while the shipped bundle lacks a change | high | mitigate | Task 1 rebuilds and reinstalls, records the artifact identities and asserts cues, config text-only behaviour and decode payloads over IPC against the installed bundle |
| T-89-42 | Denial of Service | a config document hanging requests in the shipped server | medium | mitigate | The e2e hover on a `bbx-config` document must settle within 6000 ms |
| T-89-43 | Repudiation | no-typing-lag claimed from a structural test alone | medium | mitigate | A backstop truth plus a human check on a large file in both IDEs |
| T-89-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then `bbj-ext-install`
2. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run test/functional/installed-extension-e2e.test.ts --root /home/coder/repos/bbj-language-server/bbj-vscode` — confirm a non-zero pass count
3. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline --console=plain -q` and `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline`
4. Whole VS Code suite judged on `numFailedTests: 0`: `RUN_BBJ_TESTS=0 npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode exec -- vitest run --root /home/coder/repos/bbj-language-server/bbj-vscode --maxWorkers=2`
5. Phase register check: inspect `git -C /home/coder/repos/bbj-language-server diff origin/main -- bbj-vscode/src bbj-vscode/test bbj-intellij/src examples` for plan ids, decision ids or threat ids in comments.
</verification>

<success_criteria>
- The installed bundle serves every composer cue, keeps config documents text-only, and returns the MSGBOX replace and CVS editable payloads.
- Both artifacts are rebuilt.
- The QA checklist and end-of-phase human checks cover cues, the CVS() composer and MSGBOX expressions in both IDEs.
</success_criteria>

<output>
Create `.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-13-SUMMARY.md` when done.
Record the artifact identities, the e2e pass count, the new QA row numbers, and the whole-suite `numFailedTests`.
</output>
