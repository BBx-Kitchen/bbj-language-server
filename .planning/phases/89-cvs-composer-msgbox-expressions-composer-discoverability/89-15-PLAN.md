---
phase: 89-cvs-composer-msgbox-expressions-composer-discoverability
plan: 15
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-89-3]
autonomous: true
requirements: [DISC-03]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposeMode.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/CvsComposeModeTest.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Pressing Alt+Enter, or using the editor context menu, with the caret in `a$ = CVS(`, `a$ = CVS()`, `a$ = CVS(a$` or `a$ = CVS(a$)` opens the CVS() composer titled `Complete CVS() call` instead of an error notice (DISC-03)."
    - "In complete-the-call mode the string field is prefilled from the call's first argument and stays editable, and the assign-to row is hidden. The eight operations remain one flat checkbox list, and the chars field stays visible but greyed while no chars-customizable bit is checked. OK stays disabled until a valid preview arrives, so an empty string can never be applied (D-11, D-12, D-13)."
    - "Apply replaces exactly the decoded call span. It uses the same single guarded CVS write that edit-in-place uses: `StaleEditGuard.applyIfUnchanged` with a re-issued `cvsDecodeCall` and `DecodeEquality::sameCvs`. So `a$ = CVS(` becomes `a$ = CVS(name$, 5)`, never a nested `CVS(CVS(`. Compose-new at the caret runs only when no CVS call is under the caret."
    - "A non-literal (`mode%`) or undocumented-bit mask still opens no dialog and shows the server's reason (D-14), and an editable literal-sum call still opens `Configure CVS()` edit-in-place unchanged."
    - "`sameCvs` compares `incomplete`. If the unfinished call's span or string grew while the dialog was open, the re-decode fails that comparison and the write aborts with the stale-document notice. Typed characters are never left behind after the composed call."
    - "The intention preview and `description.html` describe completing an unfinished call, and the not-editable paragraph matches what actually happens: a notice, and no dialog."
  prohibitions:
    - "No second CVS write site. Completion reuses the one CVS `applyIfUnchanged(` body, and `insertAtCaret(` in `openCvs` stays the compose-new write only."
    - "No loosened source guard. `ComposerApplyGuardSourceGuardTest` keeps exactly six `applyIfUnchanged(`, six `replaceString(`, one `insertString(`, one `DecodeEquality::sameCvs` and two `cvsDecodeCall(`, and gains an `openCvs`-scoped assertion."
    - "No change under `bbj-vscode/`. The server outcome comes from the parallel plan 89-14, and this plan is pinned to the wire contract below."
    - "`ConfigureCvsIntention.isAvailable` keeps its text-only `isCaretOnCall` heuristic; no LSP round trip runs inside `isAvailable`."
  artifacts:
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java"
      provides: "`CvsDecodeResult.incomplete`"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposeMode.java"
      provides: "plain-Java route from a CVS decode to COMPOSE_NEW, EDIT_IN_PLACE, COMPLETE_CALL or NOT_EDITABLE"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java"
      provides: "the COMPLETE_CALL dialog mode"
    - path: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      provides: "`openCvs` routed through `CvsComposeMode`, with one guarded replace for both edit and completion"
  key_links:
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposeMode.java"
      via: "`openCvs` branches on `CvsComposeMode.of(decoded)`"
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/StaleEditGuard.java"
      via: "one `applyIfUnchanged` with `sameCvs` for both EDIT_IN_PLACE and COMPLETE_CALL"
    - from: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java"
      to: "bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java"
      via: "`sameCvs` compares `incomplete`"
---

<objective>
Close UAT gap G-89-3 in IntelliJ (DISC-03, #649). Alt+Enter on an unfinished `CVS(` call must open the CVS() composer instead of the not-editable notice, and apply must replace that partial call through the existing stale-edit guard. The composer must never insert a second call at the caret.

The language server's new `incomplete` decode outcome arrives from the parallel plan 89-14, against the wire contract below. This plan:
- adds the DTO field;
- adds a plain-Java routing seam (`CvsComposeMode`) that decides new / edit / complete / not-editable;
- adds a COMPLETE_CALL dialog mode;
- routes `openCvs` through the one existing guarded CVS write;
- pins the new field in `sameCvs`, the JSON boundary test and the apply-guard source guard;
- corrects the intention wording.

Purpose: the tester's exact flow (type `CVS(`, press Alt+Enter) reaches a working composer.
Output: `CvsComposeMode` and its test, the updated dialog, launcher, DTO and equality, their tests, and the intention preview and description.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-CONTEXT.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-UAT.md
@.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-14-PLAN.md
@.planning/debug/g-89-3-cvs-composer-unfinished-call.md

<interfaces>
Wire contract, shared verbatim with plan 89-14. It is the source of truth for this plan's tests; this plan runs in parallel, so do not wait for the server change.

The server's `CvsDecodeCallResult` gains `incomplete?: boolean`. A CVS call with no mask yet (argument-less, still being typed, or closed without a mask) decodes as `found: true`, `editable: false`, `incomplete: true`, carrying `edit`, `initial` and `trailingArgs`, with no `reason`:
- `a$ = CVS(` at character 9 → `{"found":true,"editable":false,"incomplete":true,"edit":{"callStart":5,"callEnd":9},"initial":{"str":"","bits":[],"chars":""},"trailingArgs":[]}`
- `a$ = CVS(name$` at 14 → `callEnd` 14, `str` `name$`
- `x$ = CVS(a$)` → `callEnd` 12, `str` `a$`
- `x$ = CVS(a$, n%)` → `{"found":true,"editable":false,"reason":"The mask argument is not a sum of integer literals, so it cannot be safely decoded.","edit":{"callStart":5,"callEnd":16}}` — no `incomplete`
- The server retires the missing-mask reason, so no IntelliJ source may keep a copy of its sentence. The only copy is the fixture at `DecodeEqualityTest.java:687`.

Verified in the tree during planning:
- `ComposerModels.java:535-542` defines `CvsDecodeResult { boolean found; boolean editable; String reason; CvsEdit edit; CvsInitial initial; List<String> trailingArgs; }`.
- `DecodeEquality.java:289-299`: `sameCvs` compares found, editable, reason, edit (`callStart`/`callEnd`), initial (`str`, order-sensitive `bits`, `chars`) and `trailingArgs`.
- `ComposerLauncher.java:237-245` is the CVS launch branch. It issues `server.cvsDecodeCall(new DecodeCallParams(lineText, col))`, and its `staleForCue(fromCue, found)` gate only fires when nothing is found.
- `ComposerLauncher.java:648-690`, `openCvs`:
  - 650-653: null catalogs → `notReady`;
  - 654-660: `found && !editable` → `ComposerNotices.requestFailed(labelOf(Kind.CVS), reason)` and return;
  - 661-664: dialog construction;
  - 672-686: guarded edit — `StaleEditGuard`, `applyIfUnchanged`, a re-issued `cvsDecodeCall`, `DecodeEquality::sameCvs`, and a `replaceString` over `[ls + callStart, ls + callEnd)`;
  - 688: `insertAtCaret(project, editor, text, "Compose CVS()")`.
- `CvsComposerDialog.java`:
  - 78-108: constructor `(Project, BbjComposerServer, CvsCatalogs, @Nullable CvsInitial initial, boolean editMode, @Nullable List<String> trailingArgs)`;
  - 97-98: title and OK text;
  - 121-127: string field editability, label, and assign-to row visibility;
  - 190-218: `refresh()` sends `input.assignTo` and `input.editMode`.
- `StaleEditGuard.applyIfUnchanged` (lines 76-108) re-reads the captured line's current text, re-decodes at the captured column, compares with `sameDecode`, and re-checks the modification stamp as the first statement inside the write command.
- `ComposerApplyGuardSourceGuardTest`:
  - comment-stripped counts: six `applyIfUnchanged(`, six `replaceString(`, one `insertString(`;
  - raw-text counts, comments included: one `DecodeEquality::sameCvs`, two `cvsDecodeCall(`. A Javadoc or comment that writes either literal breaks those two counts.
- `DecodeEqualityTest.java:594-695` holds the CVS fixtures `baseCvs`/`copyOfCvs`, the per-field mutator test, the null test and the not-editable fixture.
- `ComposerModelsJsonBoundaryTest.java:336-381` holds the CVS decode envelopes and the `parse(method, resultType, envelope, paramsType)` helper.
- `IntentionDescriptionResourcesTest` requires `description.html` to be non-empty and to contain `<!-- tooltip end -->`, with non-empty and different before/after templates.
- `ComposerIntentionPreviewSourceGuardTest` requires, per intention:
  - exactly one `new IntentionPreviewInfo.Html(`;
  - a `startInWriteAction` that returns false;
  - one `ComposerLauncher.launch(project, editor, ComposerLauncher.Kind.`;
  - one `ComposerLauncher.isCaretOnCall(editor,`.
- `ComposerDialogRefreshSourceGuardTest` lists `CvsComposerDialog.java` as a debounced dialog (`scheduleRefresh()`, `new PreviewDebouncer(`). Those must stay.

Gap decisions this plan implements (full record in 89-14-PLAN.md):
- The closed `CVS(a$)` and the auto-closed `CVS()` are composable, because IntelliJ's closing-parenthesis insertion makes them the state the tester actually reaches.
- There is one guarded CVS write, so the whole-file source-guard counts stay the same by design, and the guard gains an `openCvs`-scoped assertion instead.
- `isAvailable` stays a text heuristic.

Shell rules:
- Use absolute paths only.
- Never chain `cd` with grep/find/cat/sed/head/tail; use `git -C /home/coder/repos/bbj-language-server`.
- Gradle only as `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew … --offline`.
- Stage exact paths only.

Comment discipline:
- Source comments, test comments, test names, assertion messages and description.html must not contain planning register ids: plan numbers such as 89-15, decision ids such as D-14, gap ids such as G-89-3, threat ids, and C-/CR-/WR-/IN- ids.
- GitHub issue numbers such as #649 are fine.

Deviations: record resolved deviations in this plan's SUMMARY.md only, never in a WINDOWS.md ledger. Do not edit REQUIREMENTS.md.
</interfaces>
</context>

## Artifacts this plan produces

- `composer/CvsComposeMode` — a plain-Java enum `COMPOSE_NEW, EDIT_IN_PLACE, COMPLETE_CALL, NOT_EDITABLE` with `static CvsComposeMode of(CvsDecodeResult decoded)`. Test: `CvsComposeModeTest`.
- `ComposerModels.CvsDecodeResult.incomplete`.
- `CvsComposerDialog`:
  - constructor `(Project, BbjComposerServer, CvsCatalogs, @Nullable CvsInitial, CvsComposeMode, @Nullable List<String> trailingArgs)`;
  - title `Complete CVS() call` in COMPLETE_CALL.
- `ComposerLauncher.openCvs` routed through `CvsComposeMode.of`, with write-command name `Complete CVS() call` for a completion.
- `DecodeEquality.sameCvs` comparing `incomplete`.
- Updated `DecodeEqualityTest`, `ComposerModelsJsonBoundaryTest` and `ComposerApplyGuardSourceGuardTest` (new `theCvsCompletionReusesTheSingleGuardedCvsReplacement`).
- Reworded `ConfigureCvsIntention.generatePreview` and `description.html`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Alt+Enter on an unfinished `CVS(` opens `Complete CVS() call` and replaces the call through the existing CVS guard</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposeMode.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/CvsComposeModeTest.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java, bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java lines 162-247 and 641-714 — `launchAt`, `openCvs`, `insertAt`
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java lines 496-569 — the CVS DTOs
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/StaleEditGuard.java lines 56-108 — the guard contract
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java lines 100-162 — the counts this task must keep
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java lines 594-634 — the plain JUnit 5 CVS fixture style to follow
  </read_first>
  <behavior>
    - `CvsComposeMode.of(null)` is COMPOSE_NEW, and a result with `found` false is COMPOSE_NEW.
    - `found` true with `incomplete` true and `editable` false is COMPLETE_CALL.
    - `found` true with `editable` true and `incomplete` false is EDIT_IN_PLACE.
    - `found` true with `editable` false and `incomplete` false (the non-literal-mask verdict, reason set) is NOT_EDITABLE.
    - `found` true with both `incomplete` and `editable` true (never sent by the server) is COMPLETE_CALL, so an incomplete call is never opened with a read-only string field.
    - The `CvsComposeMode.java` source contains no IntelliJ platform import.
  </behavior>
  <action>
    Write `CvsComposeModeTest` first, covering the behaviour bullets. It is plain JUnit 5 in package `com.basis.bbj.intellij.composer`, builds results with `new ComposerModels.CvsDecodeResult()`, and reads the enum's source the way `ComposerApplyGuardSourceGuardTest.readSource` does to assert that no platform import is present.

    `ComposerModels.CvsDecodeResult`:
    - Add `public boolean incomplete;`. Javadoc: true when the call has no mask argument yet (argument-less, still being typed, or closed without one); `editable` is then false, `reason` is absent, and `edit`, `initial` and `trailingArgs` describe the call a composer replaces.
    - Reword the class Javadoc so it no longer implies that every non-editable result carries a reason.

    New `CvsComposeMode.java`:
    - An enum with COMPOSE_NEW, EDIT_IN_PLACE, COMPLETE_CALL and NOT_EDITABLE, and `static CvsComposeMode of(CvsDecodeResult decoded)`:
      - null or not found → COMPOSE_NEW;
      - else `incomplete` → COMPLETE_CALL;
      - else `editable` → EDIT_IN_PLACE;
      - otherwise → NOT_EDITABLE.
    - Plain Java like `StaleEditGuard`, with no annotation or platform imports. Its Javadoc names #649 and explains that the incomplete check deliberately precedes the editable check.

    `CvsComposerDialog`:
    - Replace the `boolean editMode` constructor parameter with `CvsComposeMode mode`, and throw `IllegalArgumentException` for NOT_EDITABLE.
    - Derive the private flags `editMode` (EDIT_IN_PLACE) and `completing` (COMPLETE_CALL).
    - Title: `Configure CVS()` for edit, `Complete CVS() call` for completing, `Compose CVS()` for new. OK text: `Apply` for edit and completing, `Insert` for new.
    - The string field is editable unless editing, and is labelled `String expression (kept verbatim)` only when editing.
    - The assign-to row is visible only in COMPOSE_NEW, because in both replace modes any assignment sits outside the replaced call span.
    - `refresh()` sets `input.editMode = editMode` — false while completing, so the server validates the string as required — and sets `input.assignTo` to the assign field's text only in COMPOSE_NEW, `null` otherwise.
    - Prefill from `initial` in both EDIT_IN_PLACE and COMPLETE_CALL; a completion's bits are empty.
    - The flat operations list, `PreviewDebouncer`, `scheduleRefresh()`, OK gating and the greyed, never-hidden chars field are untouched (D-11, D-12, D-13).
    - Update the class Javadoc to name the three modes.

    `ComposerLauncher.openCvs`:
    - After the null-catalog check, compute `CvsComposeMode mode = CvsComposeMode.of(decoded)`.
    - NOT_EDITABLE keeps today's `requestFailed` notice with the server's reason and its fallback sentence, then returns (D-14).
    - Otherwise build the dialog with `decoded.initial` and `decoded.trailingArgs` for EDIT_IN_PLACE and COMPLETE_CALL, or with nulls for COMPOSE_NEW, passing `mode`.
    - After OK with a non-empty statement:
      - EDIT_IN_PLACE and COMPLETE_CALL both run the ONE existing guarded block, unchanged except for the write-command name: `Configure CVS()` for edit, `Complete CVS() call` for completing. It keeps the `StaleEditGuard`, the re-issued decode request, the `sameCvs` comparator and the replace body over `[lineStart + edit.callStart, lineStart + edit.callEnd)`.
      - COMPOSE_NEW keeps `insertAtCaret(project, editor, text, "Compose CVS()")`.
    - Do not add a second guarded call or replace call.
    - Rewrite `openCvs`'s Javadoc to say that an unfinished call is completed in place through the guard and that a non-literal or undocumented mask shows the reason. In that Javadoc and any new comment, do not write the decode-request name followed by an opening parenthesis, or the fully qualified comparator method reference, because the source guard counts both literals in raw text.

    `ConfigureCvsIntention.isAvailable` and the CVS branch of `launchAt` stay unchanged. A found incomplete call passes `staleForCue`, so a cue click on a call that has since lost its mask opens COMPLETE_CALL, matching the VS Code cue dispatch.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew compileJava compileTestJava --offline</automated>
    <fails_when>non-zero exit or any "error:" compiler line</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "public boolean incomplete;" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` prints exactly one line.
    - `grep -n "CvsComposeMode.of(" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` prints exactly one line.
    - `grep -n "Complete CVS() call" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/CvsComposerDialog.java /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` prints at least one line per file.
    - `CvsComposeModeTest` passes, and the whole composer test package passes, including the unchanged `ComposerApplyGuardSourceGuardTest`, `ComposerDialogRefreshSourceGuardTest` and `ComposerLauncherChainSourceGuardTest` counts.
  </acceptance_criteria>
  <done>IntelliJ opens the CVS() composer on a call the user is still typing, and completes that call in place under the stale-edit guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the incomplete verdict across the wire, the stale-edit comparison and the launcher's single guarded CVS write</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java lines 283-316 — `sameCvs`
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/DecodeEqualityTest.java lines 594-696 — the CVS fixtures and tests
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerModelsJsonBoundaryTest.java lines 1-60 and 334-381 — the `parse` helper and the CVS envelopes
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java — `openCvs` as Task 1 left it
  </read_first>
  <behavior>
    - Flipping `incomplete` on the copy breaks the match inside `changingAnySingleComparedCvsFieldBreaksTheMatch`. This is red before `sameCvs` compares the field.
    - Two independently built incomplete decodes of `a$ = CVS(` compare equal. Each has found true, editable false, incomplete true, edit 5..9, initial str empty, bits empty, chars empty, and empty trailingArgs.
    - That decode does not equal the decode after the user typed `name$` (edit 5..14, str `name$`).
    - The not-editable fixture now carries `The mask argument is not a sum of integer literals, so it cannot be safely decoded.` as its reason, and still equals its copy.
    - The envelope `{"found":true,"editable":false,"incomplete":true,"edit":{"callStart":5,"callEnd":9},"initial":{"str":"","bits":[],"chars":""},"trailingArgs":[]}` parses through the LSP4J Gson with incomplete true, editable false, reason null, an empty bits list, an empty `str` and an empty trailingArgs list.
    - The existing not-editable envelope, which has no `incomplete` key, parses with incomplete false.
    - A new source-guard test works on the comment-stripped text of the `openCvs` method body, from `private static void openCvs(` up to `private static void insertAt(`. That body contains exactly one `applyIfUnchanged(`, exactly one `replaceString(`, exactly one `insertAtCaret(`, exactly one `CvsComposeMode.of(`, and at least one `COMPLETE_CALL`.
    - The whole-file counts stay at six `applyIfUnchanged(`, six `replaceString(`, one `insertString(`, one `DecodeEquality::sameCvs` and two `cvsDecodeCall(`.
  </behavior>
  <action>
    Write the test changes first; the flip mutator must fail before the production change.

    `DecodeEqualityTest`:
    - `copyOfCvs` copies `incomplete`.
    - Add `d -> d.incomplete = !d.incomplete` to the CVS mutator list.
    - Add `twoIncompleteCvsDecodesOfTheSameUnfinishedCallMatch` and `anIncompleteCvsDecodeWhoseCallGrewDoesNotMatch`.
    - In `notEditableCvsDecodesWithNoInitialOnBothSidesCompareEqual`, replace the retired reason sentence with the non-literal-mask sentence quoted in the behaviour list.

    `DecodeEquality.sameCvs`:
    - Add `a.incomplete == b.incomplete` to the field-wise comparison, and name the field in its Javadoc (#649).

    `ComposerModelsJsonBoundaryTest`:
    - Add `anIncompleteCvsDecodeCallResponseParsesThroughTheLsp4jGson`.
    - Add `assertFalse(result.incomplete)` to the existing not-editable CVS test.

    `ComposerApplyGuardSourceGuardTest`:
    - Keep every existing count. A second CVS write does not exist, by design.
    - Reword the six-call, six-write and CVS comparator messages to say that the CVS() replacement serves both edit-in-place and completing an unfinished call (#649).
    - Add `theCvsCompletionReusesTheSingleGuardedCvsReplacement` as described in the behaviour list, using the class's existing `withoutCommentLines` and `countOccurrences` helpers. Fail with a named message when either method marker is missing.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.DecodeEqualityTest' --tests 'com.basis.bbj.intellij.composer.ComposerModelsJsonBoundaryTest' --tests 'com.basis.bbj.intellij.composer.ComposerApplyGuardSourceGuardTest' --tests 'com.basis.bbj.intellij.composer.CvsComposeModeTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>! grep -rn --include='*.java' 'nothing to compose from' /home/coder/repos/bbj-language-server/bbj-intellij/src</automated>
    <fails_when>non-zero exit (a copy of the retired sentence remains in the IntelliJ tree)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "a.incomplete == b.incomplete" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/DecodeEquality.java` prints exactly one line.
    - `grep -n "theCvsCompletionReusesTheSingleGuardedCvsReplacement" /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java` prints one line.
    - `grep -n "assertEquals(6, applyIfUnchanged.size()" /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java` still prints one line.
    - The four test classes pass, and the retired-sentence grep exits 0.
  </acceptance_criteria>
  <done>A stale unfinished-call completion can never get past the IntelliJ guard, and the launcher's single guarded CVS write is pinned for both edit and completion.</done>
</task>

<task type="auto">
  <name>Task 3: Intention preview and description tell the user an unfinished CVS() call is completed in place</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html</files>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html — the whole file
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/IntentionDescriptionResourcesTest.java lines 125-190 — resource assertions
    - /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java lines 95-150 — preview assertions
  </read_first>
  <action>
    `ConfigureCvsIntention.generatePreview`:
    - Keep exactly one `new IntentionPreviewInfo.Html(`.
    - Its text says the composer opens for the `CVS(...)` call under the caret. It is prefilled when the mask is a sum of integer literals, or from the string argument when the call has no mask yet; in that case applying replaces the unfinished call.
    - `getText()` stays `Configure CVS() options…`, and `isAvailable` stays unchanged (gap decision 8 in 89-14-PLAN.md).

    `description.html`:
    - Keep the structure and the `<!-- tooltip end -->` separator. The first paragraph stays the tooltip.
    - Add a paragraph: on a call that is still being typed — `CVS(`, `CVS()` with the editor's auto-inserted parenthesis, `CVS(name$` or `CVS(name$)` — the composer opens as `Complete CVS() call`. The string argument is prefilled and editable and there is no assignment field. Applying replaces the unfinished call with one complete `CVS(...)` call, and nothing is written if the line changed meanwhile.
    - Correct the not-editable paragraph: a mask built from a variable or an expression, or one that uses undocumented bits, opens no dialog; a notice shows the language server's reason.
    - Keep the language-server-running sentence and #649.
    - The before/after templates stay unchanged.

    Also apply the comment discipline and deviation rules from `<interfaces>`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.IntentionDescriptionResourcesTest' --tests 'com.basis.bbj.intellij.composer.ComposerIntentionPreviewSourceGuardTest'</automated>
    <fails_when>non-zero exit, "BUILD FAILED", or "No tests found for given includes"</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline</automated>
    <fails_when>non-zero exit or "BUILD FAILED"</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-intellij/src | grep '^+' | grep -nE '\b8[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-8[0-9]-[0-9]+\b|\bG-8[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'</automated>
    <fails_when>non-zero exit (a planning register id appears on an added line)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "Complete CVS() call" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html` prints at least one line.
    - `grep -c "tooltip end" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureCvsIntention/description.html` prints 1.
    - `grep -c "new IntentionPreviewInfo.Html(" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureCvsIntention.java` prints 1.
    - The whole IntelliJ test run succeeds, and the register-id scan exits 0.
  </acceptance_criteria>
  <done>The Alt+Enter preview and the intention description match what the composer now does on unfinished and non-literal CVS calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language-server decode verdict → IntelliJ dialog mode | The server's flags decide whether a call is edited, completed, refused or composed anew |
| modal dialog OK → document write | A guarded replace of the call span, or a caret insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-48 | Tampering | COMPLETE_CALL write in `ComposerLauncher.openCvs` after the line changed (the span grew, or the string was edited) | high | mitigate | The single CVS `applyIfUnchanged` re-issues the decode request and compares with `sameCvs`, which now includes `incomplete` alongside the span and initial fields; the stamp is re-checked inside the write command; pinned by `DecodeEqualityTest` |
| T-89-49 | Tampering | routing precedence that lets a non-literal mask open a dialog, or opens an incomplete call with a read-only verbatim string | high | mitigate | Plain-Java `CvsComposeMode.of` with behavioural tests: NOT_EDITABLE opens no dialog, and incomplete wins over editable |
| T-89-50 | Tampering | the completion path writing through `insertAtCaret` or an unguarded second write site | high | mitigate | An `openCvs`-scoped source guard (one guarded call, one replace, one caret insert) plus the unchanged whole-file counts |
| T-89-51 | Tampering | a plugin/server version skew in which the plugin ignores `incomplete` | low | accept | The server keeps `editable: false` on the incomplete verdict, so a plugin unaware of the field shows a notice instead of writing — fail closed |
| T-89-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added |
</threat_model>

<verification>
1. Before Task 1, record `BASE` = the output of `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`.
2. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*' --tests 'com.basis.bbj.intellij.concurrency.*'`
3. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline`
4. Register scan over the plan's added lines, including the new files once committed: `git -C /home/coder/repos/bbj-language-server diff $BASE..HEAD -- bbj-intellij/src | grep '^+' | grep -nE '\b8[0-9]-[0-9]{2}\b|\bD-[0-9]{2}\b|\bT-8[0-9]-[0-9]+\b|\bG-8[0-9]-[0-9]+\b|Pitfall [0-9]|\b(C|CR|WR|IN)-[0-9]{2}\b'` prints nothing.
5. `git -C /home/coder/repos/bbj-language-server diff $BASE..HEAD --stat -- bbj-vscode` prints nothing.
6. The live dialog behaviour is staged as an end-of-phase human check by plan 89-16, against a rebuilt plugin zip.
</verification>

<success_criteria>
- Alt+Enter and the context menu on an unfinished CVS call open the `Complete CVS() call` dialog. Its string field is editable and required and its layout rules are unchanged (D-11, D-12, D-13). Applying replaces the partial call through the single guarded CVS write.
- Non-literal and undocumented-bit masks still show only the reason (D-14), and edit-in-place on a literal-sum call is unchanged.
- The new field is pinned in `sameCvs`, at the JSON boundary and in the apply-guard source guard, and no guard count was loosened.
</success_criteria>

<output>
Create `.planning/phases/89-cvs-composer-msgbox-expressions-composer-discoverability/89-15-SUMMARY.md` when done. Record:
- the dialog title and OK text per mode;
- the final guard counts (whole-file and `openCvs`-scoped);
- the exact intention preview sentence;
- the IntelliJ test count;
- any deviation.
</output>
