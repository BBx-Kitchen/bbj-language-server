---
phase: 63-intellij-plugin-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/63-COVERAGE.md
autonomous: true
requirements: [RVW-04, SEC-03]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`.planning/reviews/63-COVERAGE.md` exists and carries the complete Phase 63 skeleton: header with the swept-tree SHA recorded once (D-18), the 5-row applicability grid, the cell-total gate re-derived from INVENTORY, the stopping rule and write contract, both `n/a` markers carried forward verbatim, the inherited-referral ledger, five stubbed `## RU-63-0N` sections and a stubbed `## Phase 63 Close-Out` — so plans `63-02`..`63-05` append into a frozen shape rather than re-deriving one (D-01, D-03)"
    - "`RU-63-03` (settings & runtime acquisition, 6 files / 1,097 LOC, INVENTORY risk rank 1) has all 7 live dimensions recorded — D1, D2, D3, D6 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-14, D-15)"
    - "All 6 files — `BbjSettings.java`, `BbjSettingsComponent.java`, `BbjSettingsConfigurable.java`, `BbjHomeDetector.java`, `BbjNodeDetector.java`, `BbjNodeDownloader.java` — are named at least once inside the `## RU-63-03` section, so coverage is file-granular (ROADMAP Phase 63 criterion 1)"
    - "`### SEC-03 Integrity Posture` exists under `## RU-63-03` and states as facts — not as findings — the four things ROADMAP criterion 2 names against the actual code: transport security, checksum-or-signature verification, archive extraction path safety (zip-slip), and cache trust, plus the extracted-binary path resolution and `setExecutable(true)` landmark (D-11)"
    - "`RU-63-03`/D6 — the phase's only live D6 cell — covers exactly two things and no more: the pinned `NODE_VERSION = \"v20.18.1\"` runtime at `BbjNodeDownloader.java:34-35`, and INVENTORY's one routed item, the `bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch, whose finding carries the phase's single deliberate `location:` exception at `bbj-intellij/build.gradle.kts:12-13` with the exception stated in the record (D-10)"
    - "The systemic absence of any test source set in `bbj-intellij` is recorded exactly once, as `P63-D5-001` against `RU-63-03`, with `ls bbj-intellij/src/` and the `build.gradle.kts` evidence — so the other four units cross-reference it by ID rather than restating it (D-08)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier via INVENTORY 3b's line-by-line-trace branch, and each record states in one clause why it carries no runnable reproduction (D-07); a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #410 (Zed Editor support) and #476 (starter programs via IntelliJ File and Code Templates) checked explicitly as this unit's plausible neighbours (RVW-07)"
    - "The classification of every finding logs all six INVENTORY 3c tests pass/FAIL inline and states which reading of test (4) was applied — `fail, no src/test/ source set exists (D-08)` for any behaviour-changing fix, or satisfied vacuously where no runtime behaviour can regress (D-09)"
    - "The unit's 1 `n/a` cell carries INVENTORY's `R-VSCODE-NO-DOWNLOAD` text verbatim — the marker no earlier phase has carried — the phase-wide carried-forward `n/a` count is 5 under two distinct markers, and the phase-wide cell-line count is 40 (D-03, D-17)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "Two findings sharing one root cause are recorded ONCE with a cross-reference by finding ID, never restated per layer (D-08 for the D5 systemic absence; D-06 for the two SETOPTS referrals that describe the same absence from two Phase 62 vantage points). Boundary case: the 24 files directly under `com/basis/bbj/intellij/` split 6 (`RU-63-03`) / 18 (`RU-63-02`) — each of the 61 files appears under exactly one unit section, never two"
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording (D-14) — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P63-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(63, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop, so two findings can never tie. Plan execution order is fixed by D-02 (`RU-63-03` -> `RU-63-01` -> `RU-63-04` -> `RU-63-05` -> `RU-63-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All five plans append to the single `.planning/reviews/63-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one wave per plan (waves 1-5), each plan's `depends_on` naming its predecessor in D-02's order (D-04). Phase 63 and Phase 64 may run concurrently but write disjoint `{NN}-COVERAGE.md` files, so no cross-phase collision exists. If a plan is interrupted mid-append, the partially-filled unit section still carries the literal placeholder `pending` on every unrecorded live-dimension cell, which is mechanically checkable at every wave"
    - "The `### SEC-03 Integrity Posture` subsection states, as a fact, what happens when the download's target path is ALREADY OCCUPIED — the cache-hit path (`BbjNodeDownloader.java:50`) and the post-extract move to the same `nodeDataDir` target (`:148`) resolve the same file, so the record says whether an existing, partial, or foreign file at that path is detected, overwritten, or trusted"
    - "The `### SEC-03 Integrity Posture` subsection states whether an empty, truncated, or entry-less archive is detected before the extracted binary is marked executable — `BbjNodeDownloader.java:137-139` resolves `extractedNode` and `:148-153` moves it and calls `setExecutable(true)`, so the record says what happens when no `node`/`node.exe` entry was extracted at all"
    - "The `### SEC-03 Integrity Posture` subsection states whether archive entry NAMES are validated BEFORE being passed to `destDir.resolve(...)` in the `ZipInputStream` loop (`BbjNodeDownloader.java:169-176`) — ROADMAP Phase 63 criterion 2's zip-slip clause — and what happens when two entries resolve to the same target path (last-write-wins vs rejected)"
    - "The `### SEC-03 Integrity Posture` subsection states what a CANCELLED or CONCURRENT download leaves behind and whether the cache-trust path can observe it — the download runs as a cancellable `Task.Backgroundable` (`BbjNodeDownloader.java:76`) writing through a temp file (`:110-115`) into the shared plugin data dir (`:244`), and the cache-hit check at `:50` is what every subsequent launch trusts. This is ROADMAP criterion 2's cache-trust clause"

  artifacts:
    - ".planning/reviews/63-COVERAGE.md — created by this plan: header (with the D-18 SHA), `## Applicability Grid — Phase 63 slice`, `## Cell-Total Gate (D-17.1)`, `## Stopping Rule & Write Contract`, `## Exclusion reasons carried forward` (two markers), `## Inherited referral ledger (D-06)`, five stubbed `## RU-63-0N` sections, and a stubbed `## Phase 63 Close-Out`"
    - ".planning/reviews/63-COVERAGE.md §`## RU-63-03 — Settings & runtime acquisition` — 7 live cells recorded, 1 `n/a` cell carried forward verbatim, plus `### SEC-03 Integrity Posture`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"

  key_links:
    - "`### SEC-03 Integrity Posture` -> ROADMAP Phase 63 criterion 2, answered in the close-out plan `63-05` writes; SEC-03 closes here and does not flow onward as open work (D-11)"
    - "`RU-63-03` D1 records on the runtime-download path -> Phase 65's SEC-04/SEC-05 synthesis as the IntelliJ half of the picture (D-12), and Phase 67's apply path via `classification:`"
    - "`P63-D5-001` (no test source set) -> the D5 cells of `RU-63-01`, `RU-63-04`, `RU-63-05`, `RU-63-02`, which cross-reference it by ID rather than restating it (D-08)"
    - "`RU-63-03`/D6's routed-item finding at `bbj-intellij/build.gradle.kts:12-13` -> Phase 64's `RU-64-02`, which owns that file for every other dimension and re-triages rather than re-reports (D-10)"
    - "The skeleton this plan writes -> plans `63-02`..`63-05`, which append into it, and Phase 68's DOC-03 concatenation against INVENTORY's grid"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, zip-slip, path traversal, supply-chain integrity, GDPR) deliberately DROPPED as
    # prohibitions — in this phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding on the Node.js runtime-download path — this repository is public, the gap would be unfixed at commit time, and the binary is fetched over the network, executed, and trusted from cache on every later launch; name the surface, the problem class and the impact, state that the evidence exists and what it establishes, and stop there (D-13)"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence (D-14)"
    - "MUST NOT modify any file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` — Phase 63 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` or `.planning/reviews/62-COVERAGE.md` to make a gate agree — INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and Phase 62 is closed and verified 13/13; a disagreement between a stated total and its re-derivation is itself a defect to surface (D-17)"
    - "MUST NOT record a finding whose `location:` is inside `bbj-vscode/` — that tree is D7 comparison material only, Phase 62 owns its rows, and a Phase 63 cell claiming them would double-count against INVENTORY's 232-cell total (D-05)"
    - "MUST NOT record a finding whose `location:` is `bbj-intellij/src/main/resources/META-INF/plugin.xml` — it is read as context only and no cell covers it (D-16)"
    - "MUST NOT schedule, run or claim a Gradle build, compile, test run or build-driven static analysis — `./gradlew --offline -q tasks` fails in 5s on the local Temurin 25.0.3 toolchain before task listing, so a finding asserting a runnable reproduction would be asserting something that did not happen (D-07)"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and predate `bbj-intellij/` entirely, so for this phase they are blank on the subject matter, and INVENTORY supersedes them (D-19)"
    - "MUST NOT add a grid row, a file-exception row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 63 slice defines (D-17)"
    - "MUST NOT reword, paraphrase, merge or re-derive either carried-forward `n/a` marker — `R-D6-CENTRAL` and `R-VSCODE-NO-DOWNLOAD` are quoted verbatim from INVENTORY and are not merged into a single bullet the way Phase 62's uniform single-marker slice allowed (D-03)"
    - "MUST NOT extend `RU-63-03`'s D6 cell beyond the pinned Node.js runtime and the one routed toolchain item — every other Gradle, IntelliJ-Platform and LSP4IJ version question belongs to `RU-64-02`/SEC-08 and a second dependency audit here would duplicate it (D-10)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason, visible rather than silently dropped (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT allocate an ID in INVENTORY's reserved `P00-*` template namespace"
    - "MUST NOT write a credential, an EM token, a session value or an absolute developer-machine path into any record — every citation is a repository-relative `path:line` anchor"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
    - "MUST NOT write into any `## RU-63-0N` section other than `RU-63-03`, and MUST NOT record a cell verdict for a file the sweep did not open — a unit-level generalization over an unread file is a bare verdict wearing a written line"
---

<objective>
Create `.planning/reviews/63-COVERAGE.md` — the phase's sole deliverable — with its complete
skeleton, and sweep review unit **`RU-63-03` — Settings & runtime acquisition**
(`BbjSettings.java` 152, `BbjSettingsComponent.java` 333, `BbjSettingsConfigurable.java` 161,
`BbjHomeDetector.java` 91, `BbjNodeDetector.java` 70, `BbjNodeDownloader.java` 290 — 6 files,
1,097 LOC, all directly under `com/basis/bbj/intellij/`, INVENTORY risk rank 1 of 5) end to end
across its 7 live dimensions.

Purpose: this is the tracer (D-01). It proves the whole recording shape on real content before four
more units are appended, and it front-loads everything asymmetric about Phase 63 into one plan —
the phase's only named security requirement (**SEC-03**, `### SEC-03 Integrity Posture`, D-11), its
only live **D6** cell (D-10), its only `R-VSCODE-NO-DOWNLOAD` `n/a` carry-forward, and the
systemic zero-test-source-set fact all five D5 cells sit over (`P63-D5-001`, D-08). Per D-15 this
unit is also the one whose Task A carries **5** dimensions rather than 4: D1, D2, D3 **and D6**,
with D7 excluded because it is `n/a` here.

Output: a created coverage file with a frozen skeleton, 7 recorded cells with written check lines,
the SEC-03 narrative subsection, and any finding records, not-reproducible dispositions and
cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/63-intellij-plugin-review/63-CONTEXT.md
@.planning/phases/63-intellij-plugin-review/63-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 63 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact.

**Created by this plan**

- `.planning/reviews/63-COVERAGE.md` — the phase's sole deliverable. This plan writes the whole
  skeleton plus the `## RU-63-03` section; plans `63-02`..`63-05` append their own unit sections
  and, in `63-05`, the close-out.

**Named subsection anchors** (downstream phases consume these by name, so the literal headings
matter):

- `### SEC-03 Integrity Posture` — under `## RU-63-03`, this plan. New to Phase 63; structurally
  mirrors `62-COVERAGE.md`'s `### SEC-01/SEC-02 Surface Handoff` and `61-COVERAGE.md`'s
  `### SEC-06 Trust Boundary` (D-03, D-11).
- `### Inherited referral triage` — under each unit owning one of the 7 inherited Phase 62
  referrals (`RU-63-01`, `RU-63-04`, `RU-63-02`). New to Phase 63 (D-03, D-06). `RU-63-03` and
  `RU-63-05` own none.
- `### Cross-phase observations (VS Code side)` — in `## Phase 63 Close-Out`. New to Phase 63
  (D-03, D-05). Stubbed by this plan so any plan that finds a VS Code-side observation has a place
  to append it; the accounting is confirmed by `63-05`.
- Per-unit, carried forward unchanged from `62-COVERAGE.md`: `### Cells`, `### Findings`,
  `### Not-reproducible dispositions`, `### Cross-unit referrals`.

**Finding-ID namespace.** `P63-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(63, dimension)` pair **across the whole phase**.
Phase 63 owns the `P63-*` namespace outright — the concurrent Phase 64 sweep owns `P64-*`, so no
collision is possible (INVENTORY 3a). This plan opens the phase and allocates the real first
sequence for each dimension it touches. `P00-*` is INVENTORY's reserved template namespace and is
never allocated.

**Plan -> review-unit map (D-01, D-02, D-04).** One plan per unit, one wave per plan, in
INVENTORY's risk-rank order:

| Plan | Wave | `depends_on` | Unit | Files / LOC |
|---|---|---|---|---|
| `63-01` (this plan, tracer) | 1 | — | `RU-63-03` Settings & runtime acquisition | 6 / 1,097 |
| `63-02` | 2 | `63-01` | `RU-63-01` Run, compile & EM actions | 11 / 1,260 |
| `63-03` | 3 | `63-02` | `RU-63-04` Composer dialogs & bridge | 13 / 2,067 |
| `63-04` | 4 | `63-03` | `RU-63-05` LSP wiring, lifecycle & status UI | 13 / 1,297 |
| `63-05` | 5 | `63-04` | `RU-63-02` Language registration & editor support, **plus the close-out** | 18 / 888 |

**Inherited routing.** INVENTORY's routing table (D-06) has **exactly one** Phase 63 row — the
`bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch, targeted at D6. This plan owns it,
because `RU-63-03`/D6 is the phase's only live D6 cell (D-10).

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-04 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-04 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-04 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-04 | concurrency | all 5 plans, incl. this one | explicit |
| 5 | SEC-03 | adjacency | `63-01` (this plan) — target-path-already-occupied | explicit |
| 6 | SEC-03 | empty | `63-01` (this plan) — entry-less/truncated archive | explicit |
| 7 | SEC-03 | ordering | `63-01` (this plan) — entry-name validation before `resolve` | explicit |
| 8 | SEC-03 | concurrency | `63-01` (this plan) — cancelled/concurrent download | explicit |

Accounting: 8 surfaced = 8 authored = 0 dropped. No edge needed a backstop and none was dismissed.

<!-- The literals below are section headings the executor must create verbatim in the coverage
     file; they are asserted positively by the acceptance criteria. -->
<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### SEC-03 Integrity Posture -->
<!-- planner-discipline-allow: ### Inherited referral triage -->
<!-- planner-discipline-allow: ### Cross-phase observations (VS Code side) -->

<tasks>

<task type="tracer">
  <name>Task 1: Create 63-COVERAGE.md and sweep RU-63-03 at evidence tier `repro` — D1, D2, D3, D6 — including the SEC-03 Integrity Posture</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>The working tree is on branch `v4.0-stability-and-quality` — `git rev-parse --abbrev-ref HEAD`
  prints exactly that — `.planning/reviews/INVENTORY.md` exists, and `.planning/reviews/63-COVERAGE.md`
  does not yet exist. Halt and report if any of the three fails; the sweep target, the finding standard
  and the append contract all depend on them (D-18, Phase 60 D-09).</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the immutable contract. Read §"Frozen Open-Issue Snapshot"
      (the 15-issue dedup list; **#410** and **#476** are this unit's named neighbours),
      §"Finding Standard" 3a-3d (note D6 is tier `inherited`, resolving to repro-equivalent for a
      version or advisory claim — it needs the advisory reference), §"Finding Record Template" (the
      exact 13 fields, in order), §"Phase 63 review units …" → the ranking basis and **all five**
      unit blocks with their file lists and LOC (needed for the stubs), §"Applicability Grid" rows
      `RU-63-01`..`RU-63-05`, §"Exclusion reasons" → **R-D6-CENTRAL** and **R-VSCODE-NO-DOWNLOAD**,
      §"Recording protocol (D-09)", §"Surface Accounting" → the `bbj-intellij/` breakdown with its
      `src/main/resources/` exclusion, §"Test & Build Baseline" → `### bbj-intellij/./gradlew build`,
      and §"Routing table (D-06)" — confirm there is exactly one Phase 63 row
    - `.planning/REQUIREMENTS.md` lines 14-23 — the D1-D8 table. Every `pass` line is phrased against
      that dimension's own "what counts as a finding" wording. Also RVW-04, RVW-06, RVW-07, and
      **SEC-03**, which this plan discharges in full
    - `.planning/ROADMAP.md` §"Phase 63: IntelliJ Plugin Review" (lines ~354-380) — the four success
      criteria, and criterion 2's explicit `BbjNodeDownloader.java` integrity requirement
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-01..D-19 are binding, read in full
    - `.planning/phases/63-intellij-plugin-review/63-PATTERNS.md` — the literal shape to copy, with
      line-anchored excerpts and the three new shape elements spelled out
    - `.planning/reviews/62-COVERAGE.md` — the frozen precedent (D-03). Read lines 1-63 (header,
      grid, cell-total gate, stopping rule and write contract, placeholder convention, exclusion
      carry-forward with its identity check) and lines 65-324 (`## RU-62-04` — the approved per-unit
      rendering: `### Cells` line format, `### SEC-01/SEC-02 Surface Handoff` as the structural
      mirror for this unit's SEC-03 subsection, the 13-field finding records, and the
      `### Not-reproducible dispositions` / `### Cross-unit referrals` sub-block shapes). Also read
      lines 1957-1977 (§D, the 7-row inherited-referral ledger). Do **not** read the remaining
      ~1,600 lines
    - **Review targets, read in full:**
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjNodeDownloader.java` (290),
      `BbjSettings.java` (152), `BbjSettingsComponent.java` (333), `BbjSettingsConfigurable.java` (161),
      `BbjHomeDetector.java` (91), `BbjNodeDetector.java` (70) — all six directly under
      `com/basis/bbj/intellij/`
    - `bbj-intellij/build.gradle.kts` — lines 12-13 (`sourceCompatibility`/`targetCompatibility` set to
      `JavaVersion.VERSION_17`), read as context for D6's routed item only; `RU-64-02` owns this file
      for every other dimension (D-10)
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Create `.planning/reviews/63-COVERAGE.md` and write it in the order below, then record
`RU-63-03`'s four `repro`-tier cells. Copy `62-COVERAGE.md`'s frozen shape unchanged (D-03) —
Phase 63 adds exactly three shape elements, all defined here rather than discovered at a
checkpoint, and spends no new format checkpoint on a question Phases 61 and 62 already answered.

**A. Header.** Title it as Phase 63's coverage record for
`bbj-intellij/src/main/java/` (RVW-04, SEC-03). Record, as four labelled lines: the **swept tree** —
branch `v4.0-stability-and-quality` at the full 40-character SHA obtained by running
`git rev-parse HEAD` **now, at execution time**, stated once for the whole phase and never
re-anchored per plan (D-18 — no SHA is pinned in any plan text, because HEAD advances with every
v4.0 planning commit including the one that committed these plans); the **governing standard**,
`.planning/reviews/INVENTORY.md`, named as immutable and not edited by this phase; the **dedup
source**, INVENTORY's frozen 15-issue snapshot queried 2026-08-17, noting that Phase 69 re-queries
live before filing and naming the Phase 63-plausible neighbours #65, #231, #381, #385, #410, #476,
#485; and the **slice size** — 5 unit rows x 8 dimensions = 40 cells, 35 `applies` and 5 `n/a`.
Then state in one sentence that the recording shape is inherited unchanged from `62-COVERAGE.md`
(D-03) rather than re-derived, and name the three Phase 63 additions by their literal headings:
`### SEC-03 Integrity Posture` under `RU-63-03`, `### Inherited referral triage` under each unit
owning one of the 7 inherited referrals, and `### Cross-phase observations (VS Code side)` in the
close-out.

**B. `## Applicability Grid — Phase 63 slice`.** The file's layout is grid table on top, per-unit
detail below, so Phase 68 can concatenate mechanically and each unit's evidence stays adjacent to
its own coverage claim. Use INVENTORY's exact column shape
`Unit | D1 Security | D2 Correctness | D3 Performance | D4 Maintainability | D5 Test coverage | D6 Dependency health | D7 Cross-IDE parity | D8 Doc accuracy`,
one row per unit in D-02's risk-rank order: `RU-63-03`, `RU-63-01`, `RU-63-04`, `RU-63-05`,
`RU-63-02`. Transcribe applicability exactly as INVENTORY's grid states it — **the `n/a` set is not
uniform in this phase**: `RU-63-03` is `applies` in D6 and `n/a — R-VSCODE-NO-DOWNLOAD` in D7, while
the other four units are `n/a — R-D6-CENTRAL` in D6 and `applies` in D7. Note beneath the table that
Phase 63 has **no file-exception rows** — all 8 in INVENTORY belong to `RU-61-07`, `RU-64-02` and
`RU-64-03`, and adding one here would break the cell gate below. Note also the two structural firsts
this phase carries relative to Phases 61 and 62: D6 is live for the first time in any sweep phase
(exactly one cell), and D7's comparison direction is reversed — Phase 63 owns the IntelliJ rows and
reads `bbj-vscode/` as reference material only (D-05).

**C. `## Cell-Total Gate (D-17.1)`.** State the expected totals — 35 `applies`, 5 `n/a`, 40 total
(5 unit rows x 8 dimensions, no file-exception rows) — and immediately **re-derive them from
INVENTORY rather than restating them**, by running this exact command and recording both the command
and its literal output in the file:

    awk '/^\| `RU-63-0[1-5]` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md

State explicitly that if this re-derivation ever disagrees with the stated totals, that disagreement
is itself a defect to surface, not a number to quietly adopt (D-17). Note that plan `63-05` re-runs
this gate together with the 61-file tree enumeration and the referral gate as the phase's closing
check.

**D. `## Stopping Rule & Write Contract`.** Carry forward Phase 62's rendered three-part stopping
rule, extended to **four** parts by D-06's inherited-referral clause (the planner confirms this
extension, which Claude's Discretion in `63-CONTEXT.md` flagged): a unit's sweep is complete when
(i) each of its 7 live `applies` cells carries a verdict plus a written line naming the concrete
checks applied; (ii) every file in the unit's file list is named at least once inside that unit's own
section — in a check line or in a finding's `location:` — so coverage is file-granular, not merely
unit-granular; (iii) every candidate claim raised during the sweep is either promoted to a finding
record clearing its evidence tier, or written under that unit's `### Not-reproducible dispositions`
with its reason; **and (iv) every inherited Phase 62 referral addressed to that unit carries a
written disposition** — promoted, dismissed-with-evidence, or not-reproducible. Once (i)-(iv) hold,
the unit is done and no further reading is licensed. Write contract: plans `63-02`..`63-05` each fill
exactly one unit section and touch nothing else — no fragment files, no assembly plan, no whole-file
rewrite, no rewording of a carried-forward `n/a` reason (D-03) — with **one narrowly scoped
exception**: any plan may *append* a bullet to `### Cross-phase observations (VS Code side)` in the
close-out, never rewording an earlier bullet, because Phase 62 is closed and there is no downstream
phase to route such an observation to (D-05). Ordering across this shared file is enforced
structurally by the wave dependency chain (D-04), not by an assumption about executor behaviour: one
plan per wave, waves 1-5, each plan's `depends_on` naming its predecessor in D-02's risk-rank order.
Placeholder: every not-yet-recorded live-dimension cell line ends with the single lowercase word
`pending`, so the remaining work is mechanically countable at every wave. State finally, in one
paragraph, the environment constraint that shapes every record in this file (D-07): the Gradle build
does not run here — `./gradlew --offline -q tasks` fails in 5s with `* What went wrong: 25.0.3`
because the local JDK is Temurin 25.0.3 while `build.gradle.kts:12-13` targets
`JavaVersion.VERSION_17` — so every D1/D2/D3 finding clears its `repro` tier via INVENTORY 3b's
**second** branch, a line-by-line trace naming the concrete inputs/state and the exact `file:line`
where behaviour diverges, and every record says in one clause why it carries no runnable
reproduction.

**E. `## Exclusion reasons carried forward`.** Two blocks, each copied **verbatim from INVENTORY
§"Exclusion reasons", not reworded, not paraphrased and not merged into one bullet** the way Phase
62's uniform single-marker slice allowed. Block one: **R-D6-CENTRAL** (4 cells in this slice — the
D6 cell of `RU-63-01`, `RU-63-04`, `RU-63-05` and `RU-63-02`). Block two: **R-VSCODE-NO-DOWNLOAD**
(1 cell — `RU-63-03`/D7), a marker no previous phase has carried. State the identity check:
4 + 1 = 5, matching the 5 `n/a` cells in this slice.

**F. `## Inherited referral ledger (D-06)`.** Reproduce the 7-row table from `62-COVERAGE.md` §D
Group 2 with a disposition column left as the placeholder word `pending` for each row: 3 rows owned
by `RU-63-01` (the `BbjCompileAction.java` TODO stub; the six VS Code commands with no IntelliJ
counterpart; `bbj.refreshJavaClasses` restarting the whole LS), 2 by `RU-63-04` (the SETOPTS absence
from `RU-62-04`, and its logic/UI-layer confirmation from `RU-62-03`), 2 by `RU-63-02` (TextMate
`filenames` honoring plus `.bbl` registration; the four-feature editor-support absence). State the
two things that make this ledger countable: referrals **#4 and #5 describe the same absence from two
Phase 62 vantage points and are triaged once**, as a single disposition on `RU-63-04` naming both
sources — recording two findings for one absence would double-count; and `RU-63-03` and `RU-63-05`
own **zero** inherited referrals, which is stated rather than left as silence. Add an eighth row for
INVENTORY's one routed item — the `bbj-intellij` Gradle build JDK 17-vs-25.0.3 toolchain mismatch,
owned by `RU-63-03`/D6 — and note that this ledger's completion is the phase's third hard gate
(D-17.3), the one inherited work makes easiest to under-serve.

**G. Five stubbed unit sections**, in D-02's order. One `## RU-63-0N — <name>` heading per unit,
each carrying a `**Files (<count> / <LOC> LOC):**` line listing every file path with its individual
LOC from INVENTORY's per-unit table, then `**Risk rank:**`, `**Sweep method:**` and
`**Owning plan:**` lines. Sweep method is `full read` for `RU-63-03`, `RU-63-01`, `RU-63-04` and
`RU-63-05`; for `RU-63-02` write `full read, in three sub-clusters (registration & file type / PSI,
lexer & editor plumbing / notification providers & presentation)` — 18 files at the smallest average
LOC in the phase, so the planner has confirmed sub-clustering rather than a flat pass, with every
file still named individually. Then a `### Cells` block with exactly 8 lines, one per dimension, each
beginning `- D{n} <dimension name> — ` using REQUIREMENTS.md's dimension names verbatim
(`D1 Security`, `D2 Correctness & error handling`, `D3 Performance & resource use`,
`D4 Maintainability & code smells`, `D5 Test coverage gaps`, `D6 Dependency health`,
`D7 Cross-IDE parity`, `D8 Comment & doc accuracy`). Each live cell line ends with the single
lowercase word `pending`; each `n/a` cell line is completed immediately as
`- D{n} <name> — n/a — "<verbatim marker text>"`. Then empty `### Findings`,
`### Not-reproducible dispositions` and `### Cross-unit referrals` sub-blocks for every unit, an
empty `### Inherited referral triage` sub-block for `RU-63-01`, `RU-63-04` and `RU-63-02` only, and
an empty `### SEC-03 Integrity Posture` sub-block for `RU-63-03` only. **Counting check before
moving on:** exactly 40 lines match `- D{n} `, exactly 5 of them carry `n/a`, and exactly 35 carry
`pending`.

**H. Stubbed `## Phase 63 Close-Out`** with seven empty lettered subsections mirroring
`62-COVERAGE.md`'s A-G — file gate, cell-total gate, finding accounting, cross-unit referral
accounting (inbound this time, with a disposition column), scope-fidelity note, ROADMAP success
criteria, closing confirmations — plus the stubbed `### Cross-phase observations (VS Code side)`
subsection any plan may append to. Note that plan `63-05` fills all of it.

---

**Now sweep `RU-63-03`** and record its D1, D2, D3 and D6 cell lines, replacing the placeholder on
each with `pass` or `fail` plus a written line naming the concrete checks applied. Append any
resulting finding records under that section's `### Findings`. Enumerate the dimensions explicitly so
none is absorbed by whichever is loudest (D-15): this task covers **D1 Security, D2 Correctness &
error handling, D3 Performance & resource use, and D6 Dependency health** — and no others. **This
unit is the phase's only 5-dimension Task A shape** — D6 is included because a version or advisory
claim is repro-equivalent under INVENTORY 3b, and D7 is excluded because it is `n/a` here. A uniform
4/3 template is wrong for this unit.

*D1 Security* (tier `repro`, satisfied by trace per D-07). Assess against REQUIREMENTS.md's D1
wording — injection, untrusted input, secret exposure, integrity gaps, privilege/trust-boundary
errors. The concrete checks, on `BbjNodeDownloader.java` first: the scheme and host of the
constructed download URL, traced from `DOWNLOAD_BASE_URL` and `NODE_VERSION` (`:34-35`) through
`downloadAndExtractNode`'s filename/URL construction (`:99-104`) to the `HttpRequests.request(...)`
`.connect(...)` / `request.saveToFile(...)` call (`:110-117`), asking whether any redirect, plaintext
fallback or certificate-handling override is configured; whether the archive written to the
`Files.createTempFile` temp file (`:110`) is verified against any checksum, signature or expected
size before extraction, and what the code does with the archive if it is not; whether the archive
entry names are validated before being passed to `destDir.resolve(...)` in the `ZipInputStream` loop
(`extractZip`, `:167-188`) and what the fixed-name resolve at `:174` implies for an entry whose name
merely ends with the expected suffix, plus the separate `extractTarGz` path (`:190-217`) which shells
out to a `tar` `ProcessBuilder` and therefore delegates entry-path safety to the system tar — state
what each of the two extraction paths does, not what one of them does; whether an empty, truncated or
entry-less archive is detected at `:135-144` before `Files.copy` to the target and
`setExecutable(true)` at `:148-153`; what happens when the target path is already occupied, given
that `Files.copy` uses `REPLACE_EXISTING` (`:149`) and the cache-hit check at `:47-52` trusts any
existing executable file at that same resolved path on every subsequent launch; and what a cancelled
or concurrent download leaves behind, given the cancellable `Task.Backgroundable` at `:76`, the
`DOWNLOAD_IN_PROGRESS_KEY` `PropertiesComponent` flag at `:70-74`/`:79`/`:88`, and the shared plugin
data directory at `:243-246`. Then on the other five files: what `BbjSettings.java` persists and
whether any value is a credential or a path that is later executed; whether
`BbjSettingsComponent.java` / `BbjSettingsConfigurable.java` validate a user-entered BBj home or Node
path before it is stored or used; and what `BbjHomeDetector.java` and `BbjNodeDetector.java` accept
from the environment (environment variables, `PATH` lookups, filesystem probes) as a trusted source.
Record concrete findings where evidence clears the tier — a bare assertion is not a finding.

*D2 Correctness & error handling* (tier `repro`, satisfied by trace per D-07). Assess against
null/undefined safety, unhandled rejections, swallowed exceptions, async races, off-by-one, wrong
edge-case behaviour and resource leaks. The concrete checks: whether the `catch (Exception e)` in the
background task (`:83-86`) can leave the plugin believing Node is available when it is not, and
whether the `finally` at `:87-92` restores the in-progress flag on every path including cancellation;
whether the temp file and temp extraction directory are removed on every exit path
(`:157-164`, `deleteDirectory` at `:277`) including the throw at `:142-144`; whether
`getCachedNodePath()`'s swallowed `IOException` (`:48`, comment at `:56`) is distinguishable by the
caller from "no cached Node"; what `extractTarGz` does with a non-zero `tar` exit code, an
interrupted wait, or unbounded process output (`:198-217`); what the settings round-trip does with an
absent, empty or malformed stored value; and what `BbjHomeDetector` / `BbjNodeDetector` return when
nothing is found versus when detection itself fails. Each claim needs a line-by-line trace naming the
concrete input or state and the exact `file:line` where behaviour diverges.

*D3 Performance & resource use* (tier `repro`, satisfied by trace per D-07). Assess against hot-path
cost, quadratic walks, missing caches or debounces, redundant work and unbounded memory growth. The
concrete checks: whether `getCachedNodePath()` is called on a hot path and whether its two filesystem
stats (`Files.exists` / `Files.isExecutable`, `:52`) plus the `Files.createDirectories` inside
`getNodeDataDirectory()` (`:245`) run per call rather than being cached; whether detection in
`BbjHomeDetector` / `BbjNodeDetector` walks the filesystem or spawns processes on every invocation;
whether the 8 KiB copy loop in `extractZip` (`:177-181`) or the `tar` output accumulation in
`extractTarGz` can grow unbounded; and whether the settings UI rebuilds its model per interaction.

*D6 Dependency health* (tier `inherited`, resolving to repro-equivalent — a version or advisory claim
needs the advisory reference). **This is Phase 63's only live D6 cell and it covers exactly two
things (D-10).** First, the **pinned Node.js runtime**: `NODE_VERSION = "v20.18.1"` at `:34`, fetched
from `DOWNLOAD_BASE_URL = "https://nodejs.org/dist/"` at `:35`. Assess its currency and support
status against Node.js's published release schedule and state, as evidence, what release line
`v20.18.1` belongs to, whether that line is still supported at sweep time, and whether any advisory
applies to it — this is a third-party runtime the plugin downloads and then executes, which is
precisely why INVENTORY marks this row `applies` where every other Phase 63 row carries
`R-D6-CENTRAL`. If network access is unavailable at execution time, say so in the cell text and
record the claim under `### Not-reproducible dispositions` rather than asserting an unreferenced
advisory. Second, the **routed item**: INVENTORY's routing table sends the `bbj-intellij` Gradle
build JDK 17-vs-25.0.3 toolchain mismatch to Phase 63, D6, and this is the only D6 cell that can
hold it. Record it with the evidence already established — `build.gradle.kts:12-13` sets
`sourceCompatibility`/`targetCompatibility` to `JavaVersion.VERSION_17` while the local JDK is
Temurin 25.0.3, and `./gradlew --offline -q tasks` fails in 5s with `* What went wrong: 25.0.3`
before task listing. Its `location:` is `bbj-intellij/build.gradle.kts:12-13`, a file INVENTORY
assigns to `RU-64-02` — **this is the phase's one deliberate location exception and the record must
say so explicitly**, with its `dedup:` field noting that `RU-64-02` owns the file for all other
dimensions so Phase 64 re-triages rather than re-reports (D-10, and Phase 61 D-14's precedent that a
routed item is recorded with full evidence by the sweep phase and resolved by a later one). Do not
extend this cell to any other Gradle, IntelliJ-Platform (`2024.2`) or LSP4IJ (`0.19.0`) version
question — those stay with `RU-64-02`/SEC-08.

**Then write `### SEC-03 Integrity Posture`** under `## RU-63-03`. It is a narrative subsection in
the register of `62-COVERAGE.md`'s `### SEC-01/SEC-02 Surface Handoff` and `61-COVERAGE.md`'s
`### SEC-06 Trust Boundary` — numbered facts stated against the actual code, closing with a
blast-radius statement and a what-was-read-versus-what-was-asserted disclaimer. It reads like a map,
not a defect list: "there is no checksum" is a fact to state; it becomes a finding only when the
record says what it enables. State the four things ROADMAP criterion 2 names, plus the fifth
structural landmark D-11 names alongside them: **(1) transport security** — the scheme, host and
request configuration behind `:34-35` and `:110-117`; **(2) checksum or signature verification** —
whether the archive at the temp path is verified before use, stated as a fact either way;
**(3) archive extraction path safety (zip-slip)** — both extraction paths, the in-process
`ZipInputStream` loop at `:167-188` with its `destDir.resolve(...)` at `:174` and the shelled-out
`tar` at `:190-217`, including whether entry names are validated before resolution and what happens
when two entries resolve to the same target; **(4) cache trust** — the cache location at `:243-246`
(`PathManager.getPluginsPath()` / `bbj-intellij-data` / `nodejs`) and the cache-hit path at `:47-52`
that every subsequent launch trusts, including what an existing, partial or foreign file at that path
produces; and **(5) extracted-binary path resolution and the executable bit** — `:135-144`'s
`extractedNode` resolution, `:148-149`'s `REPLACE_EXISTING` copy to the `nodeDataDir` target, and
`:151-153`'s `setExecutable(true)`. Close with the blast radius: what an attacker who controls any
one of these steps ends up controlling, given that the resulting binary is executed as the language
server host. End with one line stating which files were read in full for this subsection and that
nothing here is asserted as a defect purely by virtue of appearing in it — discrete `P63-D1-*`
records are allocated only where a concrete evidence-clearing defect exists.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order: `id`, `unit`, `location`, `dimension`, `secondary`, `severity`, `evidence_tier`, `evidence`,
`failure_scenario`, `classification`, `effort`, `dedup`, `disposition`. `unit:` is `RU-63-03`.
`evidence:` opens with the trace basis and states in one clause why no runnable reproduction
accompanies it (D-07) — silence there reads as an omission; a stated environment constraint reads as
method. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline and **states which
reading of test (4) was applied** (D-09): a fix that changes runtime behaviour fails test (4)
because no `src/test/` source set exists and adding one is new test infrastructure by definition, so
the finding is `major` regardless of edit size; a fix that changes no runtime behaviour at all
satisfies test (4) vacuously and may be `easy` if the other five tests pass. Test (6) independently
forces `major` for any `critical`/`high` severity and for any D1 primary dimension. `dedup:` is never
blank and is checked against the frozen 15, with **#410** (Zed Editor support — a second
non-VS-Code host for the same language server) and **#476** (starter programs surfaced through
IntelliJ's File and Code Templates) checked explicitly by number as this unit's plausible neighbours.
A `critical` or `high` D1 finding follows D-13's two-tier disclosure rule strictly: name the surface,
the problem class and the impact, state that the evidence exists and what it establishes, and publish
no trigger sequence, no payload and no proof-of-concept — this repository is public and this gap
would be unfixed at commit time. Everything else — all D2-D8 findings and D1 findings rated
`medium`/`low` — gets full concrete detail per the finding standard as written. Any claim that does
not clear its tier goes under `### Not-reproducible dispositions` with the tier it failed and why.
Any observation whose defect is plainly on the VS Code side goes into
`### Cross-phase observations (VS Code side)` in the close-out, never as a `P63-*` finding (D-05).
  </action>
  <acceptance_criteria>
    - The coverage file exists and carries the swept-tree SHA recorded at execution time: `test -f .planning/reviews/63-COVERAGE.md` succeeds and `grep -cE "$(git rev-parse HEAD)" .planning/reviews/63-COVERAGE.md` prints at least `1`
    - The skeleton is complete and correctly sized: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40`, `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`, and `grep -c '^## RU-63-0' .planning/reviews/63-COVERAGE.md` prints `5`
    - Both `n/a` markers are carried forward verbatim and unmerged: the file contains `No distinct third-party dependency of its own` and contains `never perform a Node.js runtime download/detection step`
    - The four `repro`-tier cells of this unit are recorded: with the unit section extracted by `sed -n '/^## RU-63-03 /,/^## RU-63-01 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D6 .* — (pass|fail) — ` each print `1`
    - D7 is untouched and carries its `n/a` marker in this unit: within the same extracted section, `grep -cE '^- D7 .* — n/a — '` prints `1`
    - The SEC-03 subsection exists and answers all four ROADMAP criterion 2 clauses: the extracted section contains `### SEC-03 Integrity Posture`, and within it the strings `BbjNodeDownloader.java`, `nodejs.org`, `ZipInputStream`, `setExecutable` and `PathManager.getPluginsPath` all appear
    - The D6 cell covers both D-10 items: the extracted section contains `v20.18.1` and contains `build.gradle.kts:12-13`
    - Phase-wide counts prove nothing else was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `4` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `31`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding IDs are unique, well-formed and never from the reserved namespace: `grep -oE '^id:[[:space:]]+P63-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/63-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only VS Code tree or in the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; test -f "$f"; grep -q "$(git rev-parse HEAD)" "$f"; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -c "^## RU-63-0" "$f")" = 5; grep -q "No distinct third-party dependency of its own" "$f"; grep -q "never perform a Node.js runtime download/detection step" "$f"; sed -n "/^## RU-63-03 /,/^## RU-63-01 /p" "$f" > /tmp/p63ru03.txt; for d in 1 2 3 6; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru03.txt)" = 1; done; test "$(grep -cE "^- D7 .* — n/a — " /tmp/p63ru03.txt)" = 1; for s in "### SEC-03 Integrity Posture" "BbjNodeDownloader.java" "nodejs.org" "ZipInputStream" "setExecutable" "PathManager.getPluginsPath" "v20.18.1" "build.gradle.kts:12-13"; do grep -qF "$s" /tmp/p63ru03.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 4; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 31; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_01_TRACER_OK'</automated>
  </verify>
  <done>`63-COVERAGE.md` exists with the full frozen skeleton — 40 cell lines, 5 verbatim `n/a`
  carry-forwards under two distinct markers, the re-derived cell-total gate, the four-part stopping
  rule, the 7+1 inherited-referral ledger, five stubbed unit sections and a stubbed close-out — and
  `RU-63-03`'s D1, D2, D3 and D6 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied, with `### SEC-03 Integrity Posture` stating transport, checksum/signature,
  zip-slip (both extraction paths), cache trust and the executable-bit landmark as facts against the
  code, and D6 covering the `v20.18.1` pin plus the routed `build.gradle.kts:12-13` toolchain item
  with its location exception stated.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-63-03 at evidence tier `trace` — D4, D5, D8 — including the systemic P63-D5-001 test-infrastructure absence</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`## RU-63-03`'s D1, D2, D3 and D6 cells already carry verdicts — Task 1 committed — and the
  phase-wide verdict count is exactly 4. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` §`## RU-63-03` — the section this task completes, including the
      `### SEC-03 Integrity Posture` text Task 1 wrote, and §`## Stopping Rule & Write Contract`
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot",
      §"Test & Build Baseline" → `### bbj-intellij/./gradlew build`
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-04, RVW-06, RVW-07
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` D-08, D-09, D-14, D-15, D-16 — the
      recording rules this task turns on
    - **Review targets:** the same 6 files as Task 1, plus `bbj-intellij/build.gradle.kts` read for its
      test configuration only
    - **Test-surface enumeration, run rather than assumed:** `ls bbj-intellij/src/` and
      `grep -rn "test" bbj-intellij/build.gradle.kts` — the two commands that establish D-08's fact
    - `CLAUDE.md` (repo root) — its IDE-integration section makes positive claims about
      `bbj-intellij/` (both IDEs consume `out/language/main.cjs`; the plugin bundles the compiled LS
      and the TextMate grammar and connects via LSP4IJ) that are checkable against the code just read;
      a candidate D8 target, not an asserted finding
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Complete `## RU-63-03` by recording its D4, D5 and D8 cell lines, then confirm the unit against the
four-part stopping rule. Enumerate the dimensions explicitly (D-15): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks:
whether the platform/architecture branching is expressed once or repeated — the
`SystemInfo.isWindows` ternaries at `BbjNodeDownloader.java:50`, `:103`, `:136-139`, `:148` and
`:152` resolve the same platform question at five separate sites, so state whether that is a genuine
missing abstraction or an unavoidable per-call-site branch; whether `getPlatformName()` (`:220`) and
`getArchitecture()` (`:231`) duplicate logic that `SystemInfo` already provides; whether
`downloadAndExtractNode` (`:97-165`) has crossed into god-function territory given that it builds a
URL, downloads, dispatches on archive type, extracts, resolves, copies, chmods and cleans up;
whether `BbjHomeDetector.java` and `BbjNodeDetector.java` share a detection shape that is
copy-pasted rather than factored; whether `BbjSettings.java`, `BbjSettingsComponent.java` and
`BbjSettingsConfigurable.java` agree on a single settings-access convention; and whether any
constant, default or path string is duplicated across the six files rather than declared once. Use a
mechanical basis where one exists — a named grep or a line-anchored comparison — not an eyeball
impression.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **This cell carries the phase's
single systemic finding and it is recorded here exactly once (D-08).** Establish the fact by
enumeration, not assumption: `ls bbj-intellij/src/` prints only `main`, and
`grep -rn "test" bbj-intellij/build.gradle.kts` returns no matches — the module declares no test
dependency and configures no test task, so **`bbj-intellij` has no test source set at all**. Record
this as **`P63-D5-001`**, allocated against `RU-63-03`, with both commands and their literal outputs
as evidence, its `location:` anchored at `bbj-intellij/build.gradle.kts` (the file that would declare
the missing test configuration), and its `failure_scenario:` naming what a regression in this unit
would do undetected — a silently broken download, a wrong cached path, a settings round-trip that
loses a value. Its `classification:` is `major` by D-09's primary reading: any fix requires adding a
test source set, which is new test infrastructure by definition, so test (4) fails; log all six tests
inline. Its `dedup:` is checked against the frozen 15 and states the outcome. Then state this unit's
**own specific consequence** in the cell text — which behaviours in `RU-63-03` are consequently
untested and what a first test would have to cover — so the cell is genuinely filled rather than a
pointer. State in one clause that the other four units' D5 cells will cross-reference `P63-D5-001` by
ID rather than restating the systemic fact.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong Javadoc and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: every class-level and
method-level Javadoc in the six files against the code just read — in particular
`BbjNodeDownloader`'s class comment claiming it "Handles platform detection, download, extraction,
and caching in plugin data directory" (`:29-31`), `getCachedNodePath()`'s Javadoc claiming the
method is "fast and synchronous — safe to call from any thread" (`:42-45`) against what it actually
does at `:48-52`, and the inline comments at `:109` ("Download to temp file"), `:146`
("Copy to plugin data directory"), `:151` ("Set executable permission (important for Unix-like
systems)"), `:158` and `:162` (cleanup) and `:172` ("We only want node.exe from the archive")
against the code beneath each; every comment in the settings and detector files that claims a
validation, a default or an ordering; and `CLAUDE.md`'s IDE-integration claims about the IntelliJ
plugin against what these six files actually do. Where `CLAUDE.md` is simply silent about a surface,
note the silence as an observation rather than promoting it to a finding — silence contradicts
nothing. Per D-09, a D8 fix that changes only comment or Javadoc text changes no runtime behaviour,
so it satisfies test (4) vacuously and may be `easy` if the other five tests pass; the record states
which reading was applied and why.

**Then close the unit against the four-part stopping rule** and make the closure visible in the
section text: confirm that all 7 live cells carry a verdict plus a written check line; that all six
file names appear at least once inside `## RU-63-03` — in a check line or a finding `location:`;
that every candidate claim raised during either task is either a finding record or a written
`### Not-reproducible dispositions` entry with its reason; and that `RU-63-03` owns **zero** inherited
Phase 62 referrals, stated explicitly rather than left as silence, while INVENTORY's one routed item
was dispositioned in Task 1's D6 cell. If the sweep produced no not-reproducible candidates and no
cross-unit referrals, state those sub-blocks as explicitly empty rather than omitting them (D-14's
register). Finally, state in the section text that all six files were swept even though ROADMAP
criterion 1 names only `BbjSettingsComponent.java` and `BbjNodeDownloader.java` from this unit — the
grid is the contract and the criteria are a subset of it (D-16) — so the extra coverage reads as
deliberate.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells of this unit are recorded: with the unit section extracted by `sed -n '/^## RU-63-03 /,/^## RU-63-01 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The unit's `n/a` cell is intact: within the extracted section, `grep -cE '^- D7 .* — n/a — '` prints `1`
    - The systemic test-infrastructure absence is recorded once with its enumerated evidence: the file contains `P63-D5-001` and the extracted section contains both `ls bbj-intellij/src/` and `build.gradle.kts`
    - Coverage is file-granular: each of the six basenames `BbjSettings.java`, `BbjSettingsComponent.java`, `BbjSettingsConfigurable.java`, `BbjHomeDetector.java`, `BbjNodeDetector.java`, `BbjNodeDownloader.java` appears at least once in the extracted section
    - All four per-unit sub-blocks exist in the section: `### SEC-03 Integrity Posture`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear
    - The scope-fidelity statement is present: the extracted section contains the string `criterion 1`
    - Phase-wide counts prove no other unit section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `7` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `28`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names its findings: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-03 /,/^## RU-63-01 /p" "$f" > /tmp/p63ru03.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru03.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p63ru03.txt)" = 7; test "$(grep -cE "^- D7 .* — n/a — " /tmp/p63ru03.txt)" = 1; grep -qF "P63-D5-001" "$f"; for s in "ls bbj-intellij/src/" "build.gradle.kts" "BbjSettings.java" "BbjSettingsComponent.java" "BbjSettingsConfigurable.java" "BbjHomeDetector.java" "BbjNodeDetector.java" "BbjNodeDownloader.java" "### SEC-03 Integrity Posture" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals" "criterion 1"; do grep -qF "$s" /tmp/p63ru03.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 7; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 28; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_01_RU03_COMPLETE'</automated>
  </verify>
  <done>`RU-63-03` is complete under the four-part stopping rule: all 7 live cells carry a verdict plus a
  written check line, all 6 files are named inside the section, `P63-D5-001` records the zero-test-source-set
  fact once with `ls bbj-intellij/src/` and the `build.gradle.kts` grep as evidence plus this unit's own
  untested-behaviour consequence, `CLAUDE.md`'s IntelliJ claims are checked against the code, the
  zero-inherited-referrals fact is stated explicitly, and the D-16 scope-fidelity note explains why all six
  files were swept though ROADMAP criterion 1 names only two of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| `nodejs.org` over the network → the downloaded archive → the extracted, chmod'd, executed binary (*subject-matter*) | `BbjNodeDownloader.java` fetches a third-party runtime, extracts it, marks it executable and the plugin then runs the language server on it. |
| archive entry names → `destDir.resolve(...)` and the shelled-out `tar` (*subject-matter*) | Two extraction paths, `extractZip` (`:167-188`) and `extractTarGz` (`:190-217`), decide where archive content lands. |
| the shared plugin data dir cache → every subsequent IDE launch (*subject-matter*) | `getCachedNodePath()` (`:47-52`) trusts any existing executable file at the resolved path without re-verification. |
| user-entered settings and environment detection → stored BBj/Node paths (*subject-matter*) | `BbjSettingsComponent`/`BbjSettingsConfigurable`/`BbjHomeDetector`/`BbjNodeDetector` accept values that are later used as executable or filesystem paths. |
| `63-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record on a runtime-download integrity gap is world-readable on landing, and git history preserves it. |
| code read during the sweep → the committed record | Credentials, tokens or absolute developer-machine paths encountered while reading can be copied verbatim into a record. |
| a written check line → a reader who cannot re-run the check | Phase 68's DOC-03 and Phase 67's apply path consume `pass` verdicts as evidence. |
| `bbj-vscode/` and `plugin.xml` → this phase's finding namespace | Read as reference/context only; a `location:` inside either would double-count or invent a cell. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past `MAJOR-REFACTORS.md` review. |
| shared `63-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-P01-S1 | Tampering | the Node.js archive fetched from `nodejs.org` and executed after extraction | high | transfer | Task 1's D1 check and `### SEC-03 Integrity Posture` facts (1) and (2) state transport configuration and whether any checksum or signature verification exists, against `:34-35` and `:110-117`; remediation transfers to Phase 67, disclosure to Phase 69. |
| T-63-P01-S2 | Tampering | archive entry names reaching `destDir.resolve(...)` (`:174`) and the shelled-out `tar` (`:190-217`) | high | transfer | `### SEC-03 Integrity Posture` fact (3) covers **both** extraction paths, including whether entry names are validated before resolution and what two entries resolving to one target produce. |
| T-63-P01-S3 | Spoofing | the trusted cache at `PathManager.getPluginsPath()/bbj-intellij-data/nodejs` read by `getCachedNodePath()` (`:47-52`) | high | transfer | `### SEC-03 Integrity Posture` fact (4) states what an existing, partial or foreign file at that path produces, and Task 1's D1 check covers the cancelled/concurrent-download remnant question via `:76`, `:110-115` and `:88`. |
| T-63-P01-S4 | Elevation of Privilege | `setExecutable(true)` at `:151-153` applied to whatever was resolved at `:135-144` | high | transfer | `### SEC-03 Integrity Posture` fact (5) plus Task 1's D1 empty/truncated-archive check state what is marked executable and under what preconditions. |
| T-63-P01-01 | Information Disclosure | an unfixed `critical`/`high` D1 record on the runtime-download path, committed to a public repository | high | mitigate | D-13's two-tier rule applied strictly: surface, problem class and impact; state that the evidence exists and what it establishes; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-63-P01-02 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | D-14 plus an acceptance criterion requiring every `pass` cell line to be at least 120 bytes of written checks naming concrete `file:line` anchors; RVW-06's drop-vs-disposition rule sends unclearing claims to `### Not-reproducible dispositions`. |
| T-63-P01-03 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; D-09 requires the test-(4) reading to be stated inline in every record. |
| T-63-P01-04 | Information Disclosure | a credential, token or absolute developer-machine path copied into a record while reading settings and detector code | high | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value and no absolute path is written into any field. |
| T-63-P01-05 | Tampering | editing `INVENTORY.md` or `62-COVERAGE.md` so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for both files; D-17 makes a disagreement a defect to surface rather than a number to adopt. |
| T-63-P01-06 | Repudiation | a claim asserting a runnable Gradle reproduction that could not have been produced | medium | mitigate | D-07 prohibition: no build, compile or test task is scheduled; every `repro`-tier record clears its tier by trace and says in one clause why it carries no runnable reproduction. |
| T-63-P01-07 | Tampering | a finding relocated into `bbj-vscode/` or `plugin.xml` to dodge a boundary | medium | mitigate | D-05/D-16 prohibitions plus anchored acceptance greps asserting zero `^location:` lines resolve into either. |
| T-63-P01-08 | Tampering | five plans writing one `63-COVERAGE.md` | medium | mitigate | Wave 1 with an empty `depends_on` and a structural wave chain thereafter (D-04); both tasks assert exact global counts (4/31, then 7/28) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-63-P01-09 | Denial of Service | the D6 cell expanding into a second full dependency audit | low | accept | D-10 bounds it to the `v20.18.1` pin and the one routed toolchain item; `RU-64-02`/SEC-08 owns everything else. Accepted because the boundary is stated in the cell text and re-triage is Phase 64's. |
| T-63-P01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed` and `awk` only, so INVENTORY's Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `.planning/reviews/63-COVERAGE.md` exists, records the swept-tree SHA obtained at execution time,
  and carries exactly 40 cell lines with exactly 5 `n/a` lines under two distinct verbatim markers.
- `## RU-63-03`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; its D7 cell carries INVENTORY's `R-VSCODE-NO-DOWNLOAD` text verbatim.
- `### SEC-03 Integrity Posture` names `BbjNodeDownloader.java`, `nodejs.org`, `ZipInputStream`,
  `setExecutable` and `PathManager.getPluginsPath`, covering all four ROADMAP criterion 2 clauses
  plus the executable-bit landmark.
- The D6 cell names both `v20.18.1` and `build.gradle.kts:12-13`, and no other dependency question.
- `P63-D5-001` exists once, with `ls bbj-intellij/src/` and the `build.gradle.kts` grep as evidence.
- All 6 unit files are named inside `## RU-63-03`.
- Phase-wide verdict count is 7 and placeholder count is 28; 7 + 28 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed non-`P00-` ID and a non-blank `dedup:`.
- Zero `^location:` lines resolve inside `bbj-vscode/` or `plugin.xml`.
- `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- SEC-03 is discharged in full and closes here: ROADMAP Phase 63 criterion 2's four named
  facts — transport security, checksum or signature verification, archive extraction path safety
  (zip-slip), and cache trust — are documented against the actual code, so Phase 65 inherits a closed
  requirement rather than open work.
- RVW-04 advances: 6 of 61 files have a recorded pass/fail against every dimension that applies to
  them, and the phase's only live D6 cell is filled with both of D-10's items.
- The recording shape is proven on real content: plans `63-02`..`63-05` append into a frozen
  skeleton rather than re-deriving one, and no format checkpoint is spent (D-03).
- Phase 64 inherits a fully-evidenced routed toolchain item to re-triage rather than re-derive.
- ROADMAP Phase 63 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/63-intellij-plugin-review/63-01-SUMMARY.md` when done.
</output>
