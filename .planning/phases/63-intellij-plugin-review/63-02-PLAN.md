---
phase: 63-intellij-plugin-review
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - .planning/reviews/63-COVERAGE.md
autonomous: true
requirements: [RVW-04]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-63-01` (run, compile & EM actions, 11 files / 1,260 LOC, INVENTORY risk rank 2) has all 7 live dimensions recorded in `63-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-14, D-15)"
    - "All 11 files under `com/basis/bbj/intellij/actions/` — `BbjRunActionBase.java`, `BbjEMLoginAction.java`, `BbjRunBuiAction.java`, `BbjRunDwcAction.java`, `BbjEMTokenStore.java`, `BbjCompileAction.java`, `BbjRunGuiAction.java`, `BbjRefreshJavaClassesAction.java`, `BbjComposeAddChildWindowAction.java`, `BbjComposeAddWindowAction.java`, `BbjComposeMsgboxAction.java` — are named at least once inside the `## RU-63-01` section, so coverage is file-granular (ROADMAP Phase 63 criterion 1)"
    - "The D1 cell is filled with real `file:line` findings against `BbjEMTokenStore.java`, `BbjEMLoginAction.java`, `BbjRunActionBase.java` and the three run subclasses — the EM token lifecycle and the process-spawning surface are recorded here as concrete verdicts, never as a pointer to Phase 65's later cross-cutting synthesis; a D1 cell filled with a pointer instead of a verdict is the hole D-12 exists to prevent"
    - "All 3 inherited Phase 62 referrals addressed to `RU-63-01` are triaged under `### Inherited referral triage` and each ends in exactly one of promoted / dismissed-with-evidence / not-reproducible (D-06): the `BbjCompileAction.java` TODO stub, the six VS Code commands with no IntelliJ counterpart, and `bbj.refreshJavaClasses` restarting the whole language server rather than issuing a targeted request"
    - "Referral #3 (`bbj.refreshJavaClasses`) is dispositioned here, under `RU-63-01`, because `BbjRefreshJavaClassesAction.java` lives in this unit — location decides ownership — with a written note pointing at `RU-63-05`'s `BbjServerService.restart()` as the mechanism side, so `RU-63-05` re-triages rather than re-reports"
    - "The D7 parity cell reads `bbj-vscode/` as reference material only: every divergence is recorded against the IntelliJ file that is missing or differs, with the VS Code side cited in `evidence:` as the comparison point, and no `P63-*` finding carries a `location:` inside `bbj-vscode/` (D-05)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier via INVENTORY 3b's line-by-line-trace branch, and each record states in one clause why it carries no runnable reproduction (D-07); a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #231 (custom classpath and command-line settings for starting BBj programs) and #385 (launching the Graffiti Composer) checked explicitly as this unit's plausible neighbours (RVW-07)"
    - "The D5 cell records `fail` by cross-referencing `P63-D5-001` by ID rather than restating the systemic zero-test-source-set fact, plus this unit's own specific consequence — which run, compile and EM behaviours are consequently untested and what a first test would have to cover (D-08)"
    - "The unit's 1 `n/a` cell carries INVENTORY's `R-D6-CENTRAL` text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-17)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "Two findings sharing one root cause are recorded ONCE with a cross-reference by finding ID, never restated per layer (D-08 for the D5 systemic absence; D-06 for the two SETOPTS referrals that describe the same absence from two Phase 62 vantage points). Boundary case: the 24 files directly under `com/basis/bbj/intellij/` split 6 (`RU-63-03`) / 18 (`RU-63-02`) — each of the 61 files appears under exactly one unit section, never two"
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording (D-14) — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P63-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(63, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop, so two findings can never tie. Plan execution order is fixed by D-02 (`RU-63-03` -> `RU-63-01` -> `RU-63-04` -> `RU-63-05` -> `RU-63-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All five plans append to the single `.planning/reviews/63-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one wave per plan (waves 1-5), each plan's `depends_on` naming its predecessor in D-02's order (D-04). Phase 63 and Phase 64 may run concurrently but write disjoint `{NN}-COVERAGE.md` files, so no cross-phase collision exists. If a plan is interrupted mid-append, the partially-filled unit section still carries the literal placeholder `pending` on every unrecorded live-dimension cell, which is mechanically checkable at every wave"

  artifacts:
    - ".planning/reviews/63-COVERAGE.md §`## RU-63-01 — Run, compile & EM actions` — 7 live cells recorded, 1 `n/a` cell carried forward verbatim, plus `### Inherited referral triage` (3 referrals), `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"

  key_links:
    - "`RU-63-01`'s D1 records on `BbjEMTokenStore.java` and the EM login flow -> Phase 65's SEC-04 EM token lifecycle synthesis across `BbjEMTokenStore.java` + `em-login.bbj` + `em-validate-token.bbj` (D-12)"
    - "`RU-63-01`'s D1 records on `GeneralCommandLine` construction -> Phase 65's SEC-05 process-spawning picture across both IDEs, where Phase 62's `P62-D7-001` already established the `addParameter()`-versus-`child_process.exec()` divergence from the VS Code side (D-12)"
    - "The 3 dispositioned inherited referrals -> `## Phase 63 Close-Out` §D's inbound referral accounting, re-counted by plan `63-05` (D-17.3)"
    - "Referral #3's mechanism-side note -> `RU-63-05`'s sweep in plan `63-04`, which re-triages `BbjServerService.restart()` rather than re-reporting it"
    - "`RU-63-01` cells -> the phase-wide cell, file and referral gates re-run by plan `63-05`"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, command injection, credential-handling checklists, GDPR) deliberately DROPPED as
    # prohibitions — in this phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding on the process-spawn or EM-credential path — this repository is public and the gap would be unfixed at commit time; name the surface, the problem class and the impact, state that the evidence exists and what it establishes, and stop there (D-13)"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence (D-14)"
    - "MUST NOT silently drop any of the 3 inherited referrals addressed to this unit — each carries a written disposition, because inherited work is easier to skip than a blank cell and D-17.3 counts it (D-06)"
    - "MUST NOT relocate a Phase 63 finding into `bbj-vscode/` to sidestep the IntelliJ-side verdict — that tree is D7 comparison material only, Phase 62 owns its rows and is closed, and a Phase 63 cell claiming them would double-count against INVENTORY's 232-cell total (D-05)"
    - "MUST NOT defer this unit's D1 verdict to Phase 65 — Phase 65 owns only the cross-cutting SEC-04/SEC-05 synthesis a per-unit sweep structurally cannot produce, and a cell filled with a pointer instead of a verdict is the hole D-12 exists to prevent"
    - "MUST NOT modify any file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` — Phase 63 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` or `.planning/reviews/62-COVERAGE.md` to make a gate agree — INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and Phase 62 is closed and verified 13/13 (D-05)"
    - "MUST NOT schedule, run or claim a Gradle build, compile, test run or build-driven static analysis — the local Temurin 25.0.3 toolchain fails `build.gradle.kts:12-13`'s `JavaVersion.VERSION_17` target before task listing, so a finding asserting a runnable reproduction would be asserting something that did not happen (D-07)"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and predate `bbj-intellij/` entirely (D-19)"
    - "MUST NOT write into any `## RU-63-0N` section other than `RU-63-01`, MUST NOT reword or re-derive the carried-forward `R-D6-CENTRAL` text, and MUST NOT add a grid row, a cell line or a unit section beyond the 40 cells INVENTORY's Phase 63 slice defines (D-03, D-17)"
    - "MUST NOT restate the systemic zero-test-source-set fact in this unit's D5 cell — cross-reference `P63-D5-001` by ID and add only this unit's own consequence (D-08)"
    - "MUST NOT write a credential, an EM token, a username, a password or an absolute developer-machine path into any record — every citation is a repository-relative `path:line` anchor, and the EM login flow handles secret material that must never be quoted"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason, visible rather than silently dropped (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, MUST NOT allocate an ID in INVENTORY's reserved `P00-*` template namespace, and MUST NOT record a cell verdict for a file the sweep did not open"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-63-01` — Run, compile & EM actions**
(`actions/BbjRunActionBase.java` 423, `BbjEMLoginAction.java` 169, `BbjRunBuiAction.java` 142,
`BbjRunDwcAction.java` 142, `BbjEMTokenStore.java` 89, `BbjCompileAction.java` 71,
`BbjRunGuiAction.java` 62, `BbjRefreshJavaClassesAction.java` 48,
`BbjComposeAddChildWindowAction.java` 38, `BbjComposeAddWindowAction.java` 38,
`BbjComposeMsgboxAction.java` 38 — 11 files, 1,260 LOC, all under
`com/basis/bbj/intellij/actions/`, INVENTORY risk rank 2 of 5) across its 7 live dimensions, and
record the result in the `## RU-63-01` section of `.planning/reviews/63-COVERAGE.md`.

Purpose: every run and compile action in the plugin spawns a process through this unit, and
`BbjEMTokenStore.java` is the IntelliJ side of the EM token lifecycle. Per D-12 this is where
SEC-04 and SEC-05 get **concrete** IntelliJ-side records — Phase 65 adds only the cross-cutting
synthesis a per-unit sweep structurally cannot produce, so a D1 cell filled with a pointer instead
of a verdict would leave exactly the hole that decision exists to prevent. It is also the unit
carrying the largest share of inherited work: **3 of the 7 Phase 62 referrals** are addressed here
(D-06).

Output: 7 recorded cells with written check lines, 3 dispositioned inherited referrals, and any
finding records, not-reproducible dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/63-intellij-plugin-review/63-CONTEXT.md
@.planning/phases/63-intellij-plugin-review/63-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 63 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/63-COVERAGE.md` — section `## RU-63-01 — Run, compile & EM actions` only, plus
  the one narrowly scoped exception the write contract allows: appending a bullet to
  `### Cross-phase observations (VS Code side)` in the close-out if a VS Code-side observation
  surfaces during the D7 read (D-05). The file's header, grid, cell-total gate, stopping rule,
  exclusion-reason blocks, inherited-referral ledger, the other four unit sections and the rest of
  `## Phase 63 Close-Out` are inherited from plan `63-01` and are not touched.

**Section anchors this plan fills.** `## RU-63-01 — Run, compile & EM actions` → its `### Cells`,
`### Inherited referral triage`, `### Findings`, `### Not-reproducible dispositions`,
`### Cross-unit referrals`. Downstream phases consume these anchors by name: Phase 65 (the D1
records), Phase 66 (any `dedup:` naming a `DEBT-*` requirement), Phase 67 (`classification:`),
Phase 68 (DOC-03 concatenation), Phase 69 (issue drafting, gated on ISSUE-01).

**Finding-ID namespace.** `P63-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(63, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plan `63-01` — it
does not restart per plan. `P00-*` is INVENTORY's reserved template namespace and is never
allocated.

**Plan -> review-unit map.** This plan owns `RU-63-01`. The full 5-plan map, the wave chain
(waves 1-5 in D-02's risk-rank order) and the section shape live in `63-01-PLAN.md`
§"Artifacts this phase produces".

**Inherited work owned here (D-06, 3 of 7).**

| # | From unit | Subject |
|---|---|---|
| 1 | `RU-62-01` | `BbjCompileAction.java` is an unimplemented `TODO` stub, never invokes `bbjcpl` |
| 2 | `RU-62-01` | Six VS Code commands (`configureCompileOptions`, `denumber`, `decompile`, `decompileReadonly`, `em`) have no IntelliJ action counterpart |
| 3 | `RU-62-01` | `bbj.refreshJavaClasses` restarts the whole LS on IntelliJ vs. a targeted LSP request on VS Code |

Referral #3's ownership was open at discussion time (Phase 62 flagged it "secondary interest to
`RU-63-05`"). **The planner confirms `RU-63-01`** — `BbjRefreshJavaClassesAction.java` lives in this
unit and location decides ownership — with a written note naming `RU-63-05`'s
`BbjServerService.restart()` as the mechanism side so plan `63-04` re-triages rather than
re-reports.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-04 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-04 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-04 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-04 | concurrency | all 5 plans, incl. this one | explicit |
| 5-8 | SEC-03 | adjacency / empty / ordering / concurrency | `63-01`, which owns `### SEC-03 Integrity Posture` | explicit |

Accounting: 8 surfaced = 8 authored = 0 dropped. No edge needed a backstop and none was dismissed.

<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Inherited referral triage -->
<!-- planner-discipline-allow: ### Cross-phase observations (VS Code side) -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-63-01 at evidence tier `repro` — D1, D2, D3, D7 — and triage the 3 inherited referrals</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`.planning/reviews/63-COVERAGE.md` exists, contains a `## RU-63-01 — ` heading whose 7
  live cell lines still end with the placeholder word `pending`, and plan `63-01` has committed — the
  phase-wide verdict count is exactly 7. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      `## Inherited referral ledger (D-06)`, and the completed `## RU-63-03` section as the shape to
      copy: its `### Cells` line format, its 13-field finding records, and the disclosure rendering
      plan `63-01` applied under D-13
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-01` → its `### Cross-unit referrals` sub-block
      (around lines 798-803) — the **full text** of the 3 referrals addressed to `RU-63-01`,
      including the commands Phase 62 already ran and their outputs. Do not re-derive them
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-01` §D7 cell and finding `P62-D7-001` — establishes
      the `GeneralCommandLine.addParameter()`-versus-`child_process.exec()` spawning divergence from
      the VS Code side, with IntelliJ line anchors already located
      (`BbjRunActionBase.java:144-169`, `:282-322`, `BbjRunGuiAction.java:27-52`,
      `BbjRunBuiAction.java:115-129`, `BbjRunDwcAction.java:115-129`, `BbjEMLoginAction.java:94-152`).
      This unit's D1 and D7 cells start here rather than from zero
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent — it needs the concrete divergent behaviour in both IDEs),
      §"Finding Record Template", §"Frozen Open-Issue Snapshot", §"Phase 63 review units …" →
      `### RU-63-01`, §"Applicability Grid" row `RU-63-01`, §"Exclusion reasons" → **R-D6-CENTRAL**
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-04, RVW-06, RVW-07; and
      SEC-04/SEC-05, whose cross-cutting synthesis is Phase 65's and whose concrete per-unit findings
      are this plan's (D-12)
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-05, D-06, D-07, D-09, D-12,
      D-13, D-14, D-15 are the decisions this task turns on
    - **Review targets, read in full:** all 11 files under
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/`
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-vscode/package.json` `contributes.commands` (the VS Code command surface referral #2
      compares against), `bbj-vscode/src/extension.ts` and `bbj-vscode/src/Commands/Commands.cjs`
      grepped for the run/compile/EM/denumber/decompile command registrations and their spawn call
      sites rather than read in full
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-63-01 — Run, compile & EM actions`, replacing the
placeholder on each with `pass` or `fail` plus a written line naming the concrete checks applied.
Append any resulting finding records under that section's `### Findings`, and fill its
`### Inherited referral triage` sub-block. Touch no other unit section. Enumerate the dimensions
explicitly so none is absorbed by whichever is loudest (D-15): this task covers **D1 Security,
D2 Correctness & error handling, D3 Performance & resource use, and D7 Cross-IDE parity** — and no
others. Note the asymmetry against plan `63-01`: this unit's Task A carries **4** dimensions, not 5
— D6 is `n/a — R-D6-CENTRAL` here and D7 is live.

*D1 Security* (tier `repro`, satisfied by trace per D-07). Assess against REQUIREMENTS.md's D1
wording — injection, untrusted input, secret exposure, integrity gaps, privilege/trust-boundary
errors. **Record concrete findings here; do not defer this cell (D-12).** The concrete checks, by
surface. **Process spawning:** every `GeneralCommandLine` construction in the unit traced from the
executable path and each `addParameter` argument back to the origin of that value — the shared
`buildCommandLine` contract at `BbjRunActionBase.java:414` and its three subclass implementations,
the base's own command construction around `:60` and `:298-303`, and `BbjEMLoginAction.java:98-112`
— asking for each interpolated segment whether a settings value, a workspace path, a detected BBj
home, a filename or a user-entered value reaches an argument without validation, and whether any
path is constrained to the project or workspace. State explicitly how IntelliJ's
`addParameter()`-based construction differs in exposure from the VS Code side's shell-string
`child_process.exec()` pattern that Phase 62's `P62-D7-001` documented — that difference is a fact
about the surface, and a finding only where the record says what it enables. **EM credential and
token handling:** what `BbjEMLoginAction.java:98-112` passes as process arguments, given that
`:103` supplies the password and `BbjRunActionBase.java:302` supplies the token, and whether either
value is observable to another local process, written to a log, an output channel or a temp file —
name the surface by `file:line` and never quote a value; what `BbjEMTokenStore.java:25-46` actually
guarantees by storing the JWT through `PasswordSafe`/`CredentialAttributes` rather than in
plaintext settings, and what its `generateServiceName` key scoping means for isolation; what the
`exp`-claim expiry decoder from `:50` onward does with a malformed, unsigned, or `exp`-less token
and whether expiry is *checked* or merely *decoded*; and whether `deleteToken()` (`:44-46`) is
reached on every failure path. **Temp files:** the temp file paths passed as arguments at
`BbjRunActionBase.java:303` and `BbjEMLoginAction.java:104` — how they are created, with what
permissions, what is written into them, and whether they are removed. **Output handling:** what the
process listeners around `BbjRunActionBase.java:74-95` do with process output, and whether anything
sensitive can reach a persistent sink.

*D2 Correctness & error handling* (tier `repro`, satisfied by trace per D-07). Assess against
null/undefined safety, unhandled rejections, swallowed exceptions, async races, off-by-one, wrong
edge-case behaviour and resource leaks. The concrete checks: what each `actionPerformed` does when
no project is open, no editor is active, the file is not a BBj file, or the configured BBj home is
missing or wrong; whether `update()` and `getActionUpdateThread()` agree across all eleven actions
about when an action is enabled, and whether any action performs blocking work on the EDT; whether
a failed or non-zero-exit process is distinguishable by the user from a spawn failure and from
empty output (`BbjRunActionBase.java:74-95`, `:93` `processTerminated`); whether temp files and
process handles are released on every exit path including exceptions; whether concurrent
invocations of the same action can collide on a shared temp path or a shared token; what
`BbjEMTokenStore`'s expiry decoder returns when it cannot determine expiry and whether callers
treat "unknown" as "valid"; and what the three `BbjCompose*Action` classes do when the composer
service is unavailable. Each claim needs a line-by-line trace naming the concrete input or state
and the exact `file:line` where behaviour diverges.

*D3 Performance & resource use* (tier `repro`, satisfied by trace per D-07). Assess against hot-path
cost, quadratic walks, missing caches or debounces, redundant work and unbounded memory growth. The
concrete checks: what each `update()` does per menu/toolbar repaint — these run frequently, so any
filesystem probe, settings read or path resolution there is hot-path cost; whether
`getEmValidateBbjPath()` (`BbjRunActionBase.java:257`) and `getEMLoginBbjPath()`
(`BbjEMLoginAction.java:158`) re-resolve their paths on every invocation rather than caching;
whether process output accumulates unbounded in the listeners around `:74-95`; and whether repeated
runs leak processes, listeners or temp files.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). **The direction is reversed relative to Phase 62: this
phase owns the IntelliJ rows and reads `bbj-vscode/` as reference material only (D-05).** The
concrete checks: enumerate IntelliJ's action surface from these eleven files, set it against the VS
Code command surface from `bbj-vscode/package.json`'s `contributes.commands` and the
`registerCommand` calls in `extension.ts`, and name every capability present on one side and absent
or behaviourally different on the other; compare, for the run and compile paths, which settings each
side reads, how it assembles arguments, and what it does when BBj home is unset; and compare the EM
login and token-handling flows at the level of what is passed where, without reproducing any value.
Record a divergence whose defect is on the **IntelliJ** side as a `P63-D7-{seq}` finding whose
`location:` is the IntelliJ file that is missing the behaviour or differs, citing the
`bbj-vscode/` `file:line` in `evidence:` as the comparison point only. Where the defect is plainly
on the **VS Code** side, record nothing in `### Findings` — Phase 62 is closed and is not reopened —
and instead append a bullet to `### Cross-phase observations (VS Code side)` in the close-out
stating the surface, the evidence, and which Phase 62 unit would have owned it (D-05).

**Then fill `### Inherited referral triage`** with the 3 referrals addressed to this unit,
**disposed one bullet each**, using the full referral text read from `62-COVERAGE.md` rather than a
re-derivation. **Referral #1** — `BbjCompileAction.java` is an unimplemented `TODO` stub that never
invokes `bbjcpl`: verify the stub against the code (`BbjCompileAction.java:25-40`, whose
`actionPerformed` body carries the TODO comment at `:35` referring the work to a language-server
custom notification) and dispose it as promoted to a `P63-D{n}-{seq}` finding clearing its tier, or
dismissed with the document or code that establishes it as a deliberate scope decision, or
not-reproducible with its reason. **Referral #2** — six VS Code commands with no IntelliJ action
counterpart: enumerate both surfaces before deciding, and if it is promoted, allocate **one**
finding for the categorical absence rather than one per command, so the same gap is not counted six
times. **Referral #3** — `bbj.refreshJavaClasses` restarting the whole language server: check
`BbjRefreshJavaClassesAction.java:22-32`, whose `actionPerformed` calls
`BbjServerService.getInstance(project).restart()` at `:30`, against what the VS Code side does for
the same command, and dispose it here (location decides ownership), adding a written note that
`RU-63-05` owns `BbjServerService.restart()` as the mechanism side so plan `63-04` re-triages
rather than re-reports. Each of the three bullets names its source referral, states its disposition
in the vocabulary promoted / dismissed-with-evidence / not-reproducible, and — where promoted —
names the finding ID it became; where not-reproducible, is also written under this unit's
`### Not-reproducible dispositions` with its reason.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order. `unit:` is `RU-63-01`. `evidence:` opens with the trace basis and states in one clause why no
runnable reproduction accompanies it (D-07). `classification:` records pass/FAIL for all six
INVENTORY 3c tests inline and **states which reading of test (4) was applied** (D-09): a fix that
changes runtime behaviour fails test (4) because no `src/test/` source set exists, so the finding is
`major` regardless of edit size; a fix that changes no runtime behaviour at all satisfies test (4)
vacuously and may be `easy` if the other five tests pass. Test (6) independently forces `major` for
any `critical`/`high` severity and for any D1 primary dimension. `dedup:` is never blank and is
checked against the frozen 15, with **#231** (custom classpath and command-line settings for
starting BBj programs) and **#385** (launching the Graffiti Composer) checked explicitly by number
as this unit's plausible neighbours. A `critical` or `high` D1 finding follows D-13's two-tier
disclosure rule strictly: name the surface, the problem class and the impact, state that the
evidence exists and what it establishes, and publish no trigger sequence, no payload and no
proof-of-concept. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-01 /,/^## RU-63-04 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D7 .* — (pass|fail) — ` each print `1`
    - The unit's `n/a` cell is intact and unreworded: within the extracted section, `grep -cE '^- D6 .* — n/a — '` prints `1`
    - The D1 cell is a verdict on the real surface, not a deferral: the extracted section contains `GeneralCommandLine`, `BbjEMTokenStore.java`, `BbjEMLoginAction.java` and `PasswordSafe`
    - The D7 cell was filled from the VS Code comparison side: the extracted section contains `package.json` and contains `P62-D7-001`
    - All 3 inherited referrals are dispositioned: the extracted section contains `### Inherited referral triage`, and the count of lines matching `promoted|dismissed-with-evidence|not-reproducible` within that sub-block is at least `3`; the section also contains `BbjCompileAction.java`, `refreshJavaClasses` and `RU-63-05`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `11` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `24`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding IDs unique, well-formed, never from the reserved namespace: `grep -oE '^id:[[:space:]]+P63-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`, and `grep -cE '^dedup:[[:space:]]*$'` prints `0`
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-01 /,/^## RU-63-04 /p" "$f" > /tmp/p63ru01.txt; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru01.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p63ru01.txt)" = 1; for s in "GeneralCommandLine" "BbjEMTokenStore.java" "BbjEMLoginAction.java" "PasswordSafe" "package.json" "P62-D7-001" "### Inherited referral triage" "BbjCompileAction.java" "refreshJavaClasses" "RU-63-05"; do grep -qF "$s" /tmp/p63ru01.txt; done; test "$(grep -cE "promoted|dismissed-with-evidence|not-reproducible" /tmp/p63ru01.txt)" -ge 3; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 11; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 24; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_02_REPRO_OK'</automated>
  </verify>
  <done>`RU-63-01`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — every `GeneralCommandLine` argument traced to its origin and the EM
  credential/token surface recorded by `file:line` without quoting a value for D1; action-enablement,
  process-termination and temp-file release paths for D2; per-repaint `update()` cost and path
  re-resolution for D3; and IntelliJ's action surface set against VS Code's command surface for D7 with
  IntelliJ-side defects recorded here and VS Code-side observations routed to the close-out — and all 3
  inherited Phase 62 referrals carry a written disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-63-01 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`## RU-63-01`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed — and the
  phase-wide verdict count is exactly 11. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` §`## RU-63-01` — the section this task completes — and
      §`## RU-63-03`'s D5 cell plus the `P63-D5-001` record, which this unit cross-references by ID
      rather than restating (D-08)
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-04, RVW-06, RVW-07
    - `.planning/ROADMAP.md` §"Phase 63" success criterion 1 — note that it names
      `BbjRunActionBase.java` and its GUI/BUI/DWC subclasses and `BbjEMTokenStore.java` but **not**
      the three `BbjCompose*Action` files, `BbjCompileAction.java` or
      `BbjRefreshJavaClassesAction.java`, which are swept in full anyway (D-16)
    - **Review targets:** the same 11 files as Task 1
    - `CLAUDE.md` (repo root) — its IDE-integration and run-tools claims (`web.bbj`, `em-login.bbj`
      shared between both IDEs) are checkable against these actions; a candidate D8 target, not an
      asserted finding
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Complete `## RU-63-01` by recording its D4, D5 and D8 cell lines, then confirm the unit against the
four-part stopping rule. Enumerate the dimensions explicitly (D-15): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks, on a
mechanical basis rather than an eyeball impression: `BbjRunBuiAction.java` and
`BbjRunDwcAction.java` are both 142 LOC — run a line-anchored comparison of the two (a `diff` of the
two files is non-build tooling and is available) and state how much of each is shared and whether
the shared part belongs in `BbjRunActionBase`; do the same for the three `BbjCompose*Action` files,
all exactly 38 LOC, which is a strong duplication signal to confirm or refute mechanically; assess
whether `BbjRunActionBase.java` at 423 LOC has crossed into god-class territory given that it
handles action enablement, command construction, process launch, output listening, EM token
validation and temp-file management, and name the seams if it has; check whether the abstract
`buildCommandLine` contract at `:414` is the only extension point or whether subclasses also
override behaviour the base assumes; and check for duplicated constants, path strings or option
flags across the eleven files. Where a duplication is real, allocate **one** finding for it rather
than one per copy.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **Cross-reference `P63-D5-001` by
ID; do not restate the systemic fact (D-08).** Record `fail` with a line that names `P63-D5-001` as
the systemic absence of any test source set in `bbj-intellij`, then state **this unit's own specific
consequence**: which run, compile and EM behaviours are consequently untested — command-line
construction for each of the three run modes, the EM login and token-validation flows, the expiry
decoder, action enablement — and what a first test would have to cover for this unit specifically.
If a distinct, unit-specific D5 claim exists beyond the systemic absence, allocate its own finding;
otherwise the cross-reference plus the consequence is the record.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong Javadoc and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: every class-level and
method-level Javadoc across the eleven files against the code just read — in particular
`BbjEMTokenStore.java:16-18`'s claim that it stores and retrieves EM JWT tokens via IntelliJ
`PasswordSafe`, and its `:50-54` Javadoc claiming the expiry check "Returns true if token is
expired, false otherwise or if unable to determine", against what the decoder actually does;
`BbjEMLoginAction.java:23`'s class comment describing the prompt/launch/store sequence against the
code at `:44-152`; and `BbjCompileAction.java:35`'s TODO comment, checking whether it still
describes the actual gap after referral #1's triage — a TODO that has drifted from reality is a D8
finding independent of the D7 gap it names. Also check `CLAUDE.md`'s claims about the shared run
tools (`web.bbj`, `em-login.bbj`) and IDE integration against how these actions actually resolve and
invoke them; where `CLAUDE.md` is simply silent about a surface, note the silence as an observation
rather than promoting it to a finding. Per D-09, a D8 fix that changes only comment or Javadoc text
changes no runtime behaviour, satisfies test (4) vacuously, and may be `easy` if the other five
tests pass; the record states which reading was applied.

**Then close the unit against the four-part stopping rule** and make the closure visible in the
section text: all 7 live cells carry a verdict plus a written check line; all eleven file names
appear at least once inside `## RU-63-01`; every candidate claim raised during either task is either
a finding record or a written `### Not-reproducible dispositions` entry with its reason; and all 3
inherited referrals carry a disposition. If the sweep produced no not-reproducible candidates and no
new outbound cross-unit referrals, state those sub-blocks as explicitly empty rather than omitting
them (D-14's register). Finally, state in the section text that all eleven files were swept even
though ROADMAP criterion 1 names only `BbjRunActionBase.java`, its GUI/BUI/DWC subclasses and
`BbjEMTokenStore.java` from this unit — the grid is the contract and the criteria are a subset of it
(D-16) — naming the three `BbjCompose*Action` files, `BbjCompileAction.java` and
`BbjRefreshJavaClassesAction.java` as the extra coverage, so it reads as deliberate.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-01 /,/^## RU-63-04 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The D5 cell cross-references rather than restates: the extracted section contains `P63-D5-001`
    - Coverage is file-granular: each of the eleven basenames `BbjRunActionBase.java`, `BbjEMLoginAction.java`, `BbjRunBuiAction.java`, `BbjRunDwcAction.java`, `BbjEMTokenStore.java`, `BbjCompileAction.java`, `BbjRunGuiAction.java`, `BbjRefreshJavaClassesAction.java`, `BbjComposeAddChildWindowAction.java`, `BbjComposeAddWindowAction.java`, `BbjComposeMsgboxAction.java` appears at least once in the extracted section
    - All five per-unit sub-blocks exist: `### Cells`, `### Inherited referral triage`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear in the extracted section
    - The scope-fidelity statement is present: the extracted section contains the string `criterion 1`
    - Phase-wide counts prove no other unit section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `14` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `21`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`, and the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-01 /,/^## RU-63-04 /p" "$f" > /tmp/p63ru01.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru01.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p63ru01.txt)" = 7; for s in "P63-D5-001" "BbjRunActionBase.java" "BbjEMLoginAction.java" "BbjRunBuiAction.java" "BbjRunDwcAction.java" "BbjEMTokenStore.java" "BbjCompileAction.java" "BbjRunGuiAction.java" "BbjRefreshJavaClassesAction.java" "BbjComposeAddChildWindowAction.java" "BbjComposeAddWindowAction.java" "BbjComposeMsgboxAction.java" "### Cells" "### Inherited referral triage" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals" "criterion 1"; do grep -qF "$s" /tmp/p63ru01.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 14; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 21; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_02_RU01_COMPLETE'</automated>
  </verify>
  <done>`RU-63-01` is complete under the four-part stopping rule: all 7 live cells carry a verdict plus a
  written check line, all 11 files are named inside the section, the BUI/DWC and three-way
  `BbjCompose*Action` duplication questions are settled on a mechanical basis, the D5 cell
  cross-references `P63-D5-001` and adds this unit's own untested-behaviour consequence, `CLAUDE.md`'s
  run-tools and IDE-integration claims are checked against the actions, all 3 inherited referrals carry a
  disposition, and the D-16 scope-fidelity note explains why the compose/compile/refresh actions were
  swept though ROADMAP criterion 1 omits them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's and the cross-cutting synthesis is
Phase 65's.

| Boundary | Description |
|----------|-------------|
| settings values, detected BBj home, workspace paths → `GeneralCommandLine` arguments (*subject-matter*) | `BbjRunActionBase.java:414`'s `buildCommandLine` contract, its three subclass implementations, and the base's own construction at `:60` and `:298-303` assemble the process that runs the user's program. |
| EM username, password and JWT → process arguments and temp files (*subject-matter*) | `BbjEMLoginAction.java:98-112` passes the password at `:103` and a temp path at `:104`; `BbjRunActionBase.java:302` passes the token. Process argument lists are observable to other local processes. |
| EM JWT at rest → IntelliJ `PasswordSafe` / OS keychain (*subject-matter*) | `BbjEMTokenStore.java:25-46` stores through `CredentialAttributes`; `:50` onward decodes the `exp` claim. What that guarantees is this unit's D1 work. |
| process output → listeners and any persistent sink (*subject-matter*) | `BbjRunActionBase.java:74-95` receives all child-process output. |
| `63-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record on a spawn or credential path is world-readable on landing, and git history preserves it. |
| code read during the sweep → the committed record | An EM token, username, password or absolute developer-machine path encountered while reading can be copied verbatim into a record. |
| a written check line → a reader who cannot re-run the check | Phase 68's DOC-03 and Phase 67's apply path consume `pass` verdicts as evidence. |
| `bbj-vscode/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 62's already-filled rows. |
| inherited referrals → the phase's completion claim | An untriaged referral is countable work silently skipped (D-17.3). |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past `MAJOR-REFACTORS.md` review. |
| shared `63-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-P02-S1 | Elevation of Privilege | `GeneralCommandLine` argument construction across `BbjRunActionBase.java` and the three run subclasses | high | transfer | Task 1's D1 check traces every `addParameter` argument back to its origin and asks what validation stands between a settings/workspace/user value and the spawned process; recorded as `P63-D1-*` where evidence clears tier `repro` by trace. Cross-cutting SEC-05 synthesis transfers to Phase 65; remediation to Phase 67. |
| T-63-P02-S2 | Information Disclosure | EM password at `BbjEMLoginAction.java:103` and JWT at `BbjRunActionBase.java:302` passed as process arguments | high | transfer | Task 1's D1 check asks whether either value is observable to another local process, logged, or written to a temp file — stated by `file:line`, never by quoting the value. |
| T-63-P02-S3 | Spoofing | the `exp`-claim expiry decoder in `BbjEMTokenStore.java` from `:50` | medium | transfer | Task 1's D1 check asks what the decoder does with a malformed, unsigned or `exp`-less token and whether callers treat "unable to determine" as valid. |
| T-63-P02-S4 | Information Disclosure | temp files passed as arguments at `BbjRunActionBase.java:303` and `BbjEMLoginAction.java:104` | medium | transfer | Task 1's D1 check covers creation, permissions, contents and removal on every exit path. |
| T-63-P02-01 | Information Disclosure | an unfixed `critical`/`high` D1 record on a spawn or credential path, committed to a public repository | high | mitigate | D-13's two-tier rule: surface, problem class and impact; state that the evidence exists and what it establishes; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-63-P02-02 | Information Disclosure | an EM token, username, password or absolute developer path copied into a record while reading | high | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value is written into any field. |
| T-63-P02-03 | Repudiation | a D1 cell filled with a Phase 65 pointer instead of a verdict | high | mitigate | D-12 prohibition plus acceptance criteria asserting the D1 cell names `GeneralCommandLine`, `BbjEMTokenStore.java`, `BbjEMLoginAction.java` and `PasswordSafe`; a pointer would leave those strings absent. |
| T-63-P02-04 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | D-14 plus an acceptance criterion requiring every `pass` cell line to be at least 120 bytes of written checks; RVW-06 sends unclearing claims to `### Not-reproducible dispositions`. |
| T-63-P02-05 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; D-09 requires the test-(4) reading stated inline. |
| T-63-P02-06 | Repudiation | one or more of the 3 inherited referrals silently skipped | high | mitigate | D-06 prohibition plus an acceptance criterion counting at least 3 disposition tokens inside `### Inherited referral triage` and asserting `BbjCompileAction.java`, `refreshJavaClasses` and `RU-63-05` all appear. |
| T-63-P02-07 | Tampering | a finding relocated into `bbj-vscode/` to dodge the IntelliJ-side verdict | medium | mitigate | D-05 prohibition plus an anchored acceptance grep asserting zero `^location:` lines resolve into `bbj-vscode/`; VS Code-side observations route to the close-out subsection instead. |
| T-63-P02-08 | Tampering | editing `INVENTORY.md` or `62-COVERAGE.md` so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for both. |
| T-63-P02-09 | Repudiation | a claim asserting a runnable Gradle reproduction that could not have been produced | medium | mitigate | D-07 prohibition: no build, compile or test task is scheduled; every `repro`-tier record clears its tier by trace and says why it carries no runnable reproduction. |
| T-63-P02-10 | Tampering | five plans writing one `63-COVERAGE.md` | medium | mitigate | Wave 2 behind `depends_on: [63-01]` makes ordering structural (D-04); both tasks assert exact global counts (11/24, then 14/21) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-63-P02-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed`, `diff` and `awk` only. |
</threat_model>

<verification>
- `## RU-63-01`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's `R-D6-CENTRAL` text verbatim.
- All 11 unit files are named inside the section.
- The D1 cell names `GeneralCommandLine`, `BbjEMTokenStore.java`, `BbjEMLoginAction.java` and
  `PasswordSafe` — a verdict on the real surface, not a deferral.
- The D7 cell cites `bbj-vscode/package.json` and Phase 62's `P62-D7-001` as the comparison basis;
  no `location:` resolves inside `bbj-vscode/`.
- `### Inherited referral triage` disposes all 3 referrals and names `RU-63-05` for referral #3's
  mechanism side.
- The D5 cell cross-references `P63-D5-001` and adds this unit's own consequence.
- The D-16 scope-fidelity statement is present.
- Phase-wide verdict count is 14 and placeholder count is 21; 14 + 21 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed non-`P00-` ID and a non-blank `dedup:` that
  explicitly covered #231 and #385.
- `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- RVW-04 advances: 11 more of the 61 files (17 cumulative) have a recorded pass/fail against every
  dimension that applies to them.
- Phase 65 inherits concrete, evidenced IntelliJ-side D1 records on the process-spawn and EM
  token-lifecycle paths rather than a deferral pointer, completing the second half of the picture
  Phase 62's `P62-D7-001` opened from the VS Code side.
- The 3 inherited Phase 62 referrals stop being outstanding work: each carries a written
  disposition, which is what D-17's third gate counts.
- ROADMAP Phase 63 criteria 1, 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/63-intellij-plugin-review/63-02-SUMMARY.md` when done.
</output>
