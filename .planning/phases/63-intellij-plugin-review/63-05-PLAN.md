---
phase: 63-intellij-plugin-review
plan: 05
type: execute
wave: 5
depends_on: [63-04]
files_modified:
  - .planning/reviews/63-COVERAGE.md
autonomous: true
requirements: [RVW-04, SEC-03]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-63-02` (language registration, editor support & notifications, 18 files / 888 LOC — the largest file count in the phase at the smallest average LOC, INVENTORY risk rank 5) has all 7 live dimensions recorded in `63-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-14, D-15)"
    - "All 18 files are named at least once inside the `## RU-63-02` section, swept in three named sub-clusters — registration & file type (7), PSI/lexer & editor plumbing (7), notification providers & presentation (4) — so no small file is absorbed into a group verdict and coverage stays file-granular (ROADMAP Phase 63 criterion 1)"
    - "The 6/18 split of the 24 files directly under `com/basis/bbj/intellij/` is honoured exactly: the six settings/detection/download files belong to `RU-63-03` and appear only in its section, and the other eighteen appear only here — a globbed sweep of that directory would double-count and break the file gate"
    - "Both inherited Phase 62 referrals addressed to `RU-63-02` are triaged under `### Inherited referral triage` and each ends in exactly one of promoted / dismissed-with-evidence / not-reproducible (D-06): the TextMate `filenames`-honoring plus `.bbl` registration question, and the four-feature editor-support absence (format, denumber, tokenized detection, decompile)"
    - "`plugin.xml` is read as context for the registration sweep and never as a finding surface: no cell covers it and no `P63-*` finding carries a `location:` inside it (D-16)"
    - "`63-COVERAGE.md` carries a complete `## Phase 63 Close-Out` with all seven lettered sections, including `### Cross-phase observations (VS Code side)` — the record of every VS Code-side observation Phase 63 could not route anywhere else, because Phase 62 is closed and is not reopened (D-05)"
    - "All three D-17 completion gates are **re-run live and recorded with their literal outputs**, not restated: the cell gate re-derives `35 5 40` from INVENTORY via the awk pass, the file gate enumerates `61` files from the tree with each basename confirmed present in the coverage file, and the referral gate confirms all 7 inherited referrals plus the 1 routed item carry a written disposition"
    - "The close-out states plainly that all 61 files were swept even though ROADMAP criterion 1 names only a subset, naming the extra surfaces explicitly — the five notification providers, `BbjColorSettingsPage.java`, `BbjSpellcheckingStrategy.java`, `BbjTextMateBundleProvider.java`, `BbjCommenter.java` and the composer bridge files — so the extra coverage reads as deliberate (D-16)"
    - "All four ROADMAP Phase 63 success criteria are answered per-criterion in the close-out, with criterion 2 answered by `RU-63-03`'s `### SEC-03 Integrity Posture` — SEC-03 closes in this phase and nothing on it flows to Phase 65 as open work (D-11)"
    - "Every recorded finding carries `path:line`, a primary dimension, evidence clearing its tier via INVENTORY 3b's line-by-line-trace branch with a clause stating why it carries no runnable reproduction (D-07), and a non-blank `dedup:` checked against the 15 frozen open issues — with #65 (tokenized BBj files), #381 (config.bbx highlighting) and #476 (starter programs via IntelliJ File and Code Templates) checked explicitly as this unit's plausible neighbours (RVW-06, RVW-07)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "Two findings sharing one root cause are recorded ONCE with a cross-reference by finding ID, never restated per layer (D-08 for the D5 systemic absence; D-06 for the two SETOPTS referrals that describe the same absence from two Phase 62 vantage points). Boundary case: the 24 files directly under `com/basis/bbj/intellij/` split 6 (`RU-63-03`) / 18 (`RU-63-02`) — each of the 61 files appears under exactly one unit section, never two"
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording (D-14) — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P63-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(63, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop, so two findings can never tie. Plan execution order is fixed by D-02 (`RU-63-03` -> `RU-63-01` -> `RU-63-04` -> `RU-63-05` -> `RU-63-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All five plans append to the single `.planning/reviews/63-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one wave per plan (waves 1-5), each plan's `depends_on` naming its predecessor in D-02's order (D-04). Phase 63 and Phase 64 may run concurrently but write disjoint `{NN}-COVERAGE.md` files, so no cross-phase collision exists. If a plan is interrupted mid-append, the partially-filled unit section still carries the literal placeholder `pending` on every unrecorded live-dimension cell, which is mechanically checkable at every wave"

  artifacts:
    - ".planning/reviews/63-COVERAGE.md §`## RU-63-02 — Language registration, editor support & notifications` — 7 live cells recorded, 1 `n/a` cell carried forward verbatim, plus `### Inherited referral triage` (2 referrals), `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"
    - ".planning/reviews/63-COVERAGE.md §`## Phase 63 Close-Out` — sections A-G filled (file gate, cell-total gate, finding accounting, inbound cross-unit referral accounting, scope-fidelity note, ROADMAP success criteria, closing confirmations) plus `### Cross-phase observations (VS Code side)`, and the header's `## Inherited referral ledger (D-06)` disposition column resolved from the five unit sections"

  key_links:
    - "`## Phase 63 Close-Out` -> Phase 68's DOC-03 concatenation of all five coverage files against INVENTORY's grid, which reads the gates and the accounting rather than re-deriving scope"
    - "`### Cross-phase observations (VS Code side)` -> Phase 68's DOC-04 intent (nothing silently dropped) and Phase 67's classification reading — the only durable home for a VS Code-side observation, since Phase 62 is closed (D-05)"
    - "The close-out's answer to ROADMAP criterion 2 -> SEC-03 marked complete; `RU-63-03`'s `### SEC-03 Integrity Posture` is the evidence and nothing on SEC-03 flows to Phase 65 as open work (D-11)"
    - "The 8 resolved referral/routing dispositions -> Phase 64's `RU-64-02` re-triage of the routed toolchain item, and Phase 69's issue drafting from the promoted ones"
    - "`RU-63-02`'s D7 TextMate records -> Phase 62's `RU-62-05` D7 cell as the already-established comparison basis, cited in `evidence:` and never re-recorded there"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, injection, path traversal, GDPR) deliberately DROPPED as prohibitions — in this
    # phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT declare a completion gate satisfied by restating a number from `63-CONTEXT.md` — all three D-17 gates are re-run live at execution time and their literal outputs recorded; a stated total that was never re-derived is the false-assurance failure this gate exists to catch"
    - "MUST NOT adopt a re-derived number that disagrees with the stated totals — a disagreement between INVENTORY's re-derivation and the coverage file's own totals is itself a defect to surface in the close-out, not a number to quietly adopt (D-17.1)"
    - "MUST NOT silently drop any of the 7 inherited referrals or the 1 routed item — each carries a written disposition and the close-out counts all 8; inherited work is easier to skip than a blank cell, which is exactly why D-17.3 counts it"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence (D-14)"
    - "MUST NOT reopen or edit `.planning/reviews/62-COVERAGE.md` to record a VS Code-side observation found now — Phase 62 is closed and verified 13/13, and D-05 routes such observations to `### Cross-phase observations (VS Code side)` in this phase's own close-out instead"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` to make a gate agree — it is immutable for the v4.0 milestone (Phase 60 D-09)"
    - "MUST NOT record a finding whose `location:` is inside `bbj-vscode/` or inside `bbj-intellij/src/main/resources/META-INF/plugin.xml` — the first is D7 comparison material owned by Phase 62, the second is context-only with no cell covering it (D-05, D-16)"
    - "MUST NOT sweep the 24 files directly under `com/basis/bbj/intellij/` as one directory glob — the 6/18 split between `RU-63-03` and `RU-63-02` is a unit boundary, and ignoring it double-counts files across two units and breaks the file gate"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and the gap would be unfixed at commit time (D-13)"
    - "MUST NOT modify any file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` — Phase 63 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT schedule, run or claim a Gradle build, compile, test run or build-driven static analysis — the local Temurin 25.0.3 toolchain fails `build.gradle.kts:12-13`'s `JavaVersion.VERSION_17` target before task listing (D-07)"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and predate `bbj-intellij/` entirely (D-19)"
    - "MUST NOT reword or re-derive either carried-forward `n/a` marker, and MUST NOT add a grid row, a cell line or a unit section beyond the 40 cells INVENTORY's Phase 63 slice defines (D-03, D-17)"
    - "MUST NOT restate the systemic zero-test-source-set fact in this unit's D5 cell — cross-reference `P63-D5-001` by ID and add only this unit's own consequence (D-08)"
    - "MUST NOT re-record Phase 62's `RU-62-05` TextMate findings as Phase 63 findings — they are cited in `evidence:` as the comparison basis; only an IntelliJ-side divergence is recorded here"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier, MUST NOT leave a `dedup:` field blank, MUST NOT allocate an ID in the reserved `P00-*` namespace, and MUST NOT record a cell verdict for a file the sweep did not open (RVW-06)"
    - "MUST NOT open, comment on or file any GitHub issue, and MUST NOT draft one into this file — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-63-02` — Language registration, editor support & notifications**
(18 files, 888 LOC, all directly under `com/basis/bbj/intellij/` excluding the six files
`RU-63-03` claims, INVENTORY risk rank 5 of 5) across its 7 live dimensions, then **close
Phase 63**: fill `## Phase 63 Close-Out` sections A-G, write
`### Cross-phase observations (VS Code side)`, and re-run all three D-17 completion gates live.

Purpose: this is the phase's last unit and its closing plan. The unit itself is the largest file
count at the smallest average LOC — 18 declarative registration, PSI/lexer and notification files —
so it is swept in three named sub-clusters with every file still named individually, which is the
planner's resolution of the Claude's-Discretion question `63-CONTEXT.md` raised. It owns **2 of the
7 inherited Phase 62 referrals** (D-06), both on the editor-support parity surface. The close-out
then answers all four ROADMAP success criteria, records that all 61 files were swept though
criterion 1 names only a subset (D-16), and re-runs the cell, file and referral gates rather than
restating numbers established at discussion time (D-17).

Output: 7 recorded cells with written check lines, 2 dispositioned inherited referrals, a complete
seven-section close-out with three live gate outputs, and the phase's cross-phase observation
record. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/63-intellij-plugin-review/63-CONTEXT.md
@.planning/phases/63-intellij-plugin-review/63-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 63 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/63-COVERAGE.md` — section `## RU-63-02 — Language registration, editor support
  & notifications` (Tasks 1-2), and `## Phase 63 Close-Out` plus the header's
  `## Inherited referral ledger (D-06)` disposition column (Task 3). **Task 3 is the one plan
  authorized to write outside a unit section**: it is the closing plan, and the close-out and the
  ledger's disposition column are precisely its scope. It still rewords nothing already recorded by
  plans `63-01`..`63-04` and does not touch their unit sections.

**Section anchors this plan fills.** `## RU-63-02 — Language registration, editor support &
notifications` → its `### Cells`, `### Inherited referral triage`, `### Findings`,
`### Not-reproducible dispositions`, `### Cross-unit referrals`. Then `## Phase 63 Close-Out` →
**A** file gate, **B** cell-total gate, **C** finding accounting, **D** inbound cross-unit referral
accounting, **E** scope-fidelity note, **F** ROADMAP success criteria, **G** closing confirmations,
plus `### Cross-phase observations (VS Code side)`.

**Finding-ID namespace.** `P63-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(63, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `63-01`
through `63-04` — it does not restart per plan. `P00-*` is INVENTORY's reserved template namespace
and is never allocated.

**Plan -> review-unit map.** This plan owns `RU-63-02` and the close-out. The full 5-plan map, the
wave chain (waves 1-5 in D-02's risk-rank order) and the section shape live in `63-01-PLAN.md`
§"Artifacts this phase produces".

**Sub-clusters for this unit (planner's resolution of Claude's Discretion — 18 files, 7 + 7 + 4).**

| Cluster | Files | LOC |
|---|---|---|
| A. Registration & file type | `BbjLanguage.java` 11, `BbjFileType.java` 36, `BbjFile.java` 21, `BbjIcons.java` 19, `BbjTokenTypes.java` 23, `BbjParserDefinition.java` 79, `BbjPsiElement.java` 15 | 204 |
| B. Lexer & editor plumbing | `BbjWordLexer.java` 105, `BbjPairedBraceMatcher.java` 39, `BbjCommenter.java` 36, `BbjColorSettingsPage.java` 157, `BbjLanguageCodeStyleSettingsProvider.java` 31, `BbjSpellcheckingStrategy.java` 16, `BbjTextMateBundleProvider.java` 49 | 433 |
| C. Notification providers & presentation | `BbjJavaInteropNotificationProvider.java` 57, `BbjMissingHomeNotificationProvider.java` 55, `BbjMissingNodeNotificationProvider.java` 76, `BbjWelcomeNotification.java` 63 | 251 |

7 + 7 + 4 = 18 files; 204 + 433 + 251 = 888 LOC, matching INVENTORY exactly. Clusters organise the
sweep; **every file is still named individually in the section** so no small file is absorbed into
a group verdict.

**Inherited work owned here (D-06, 2 of 7).**

| # | From unit | Subject |
|---|---|---|
| 6 | `RU-62-05` | Whether IntelliJ's TextMate importer honors `filenames`; whether LSP4IJ registration independently covers `.bbl` |
| 7 | `RU-62-02` | None of format / denumber / tokenized-detection / decompile has any IntelliJ counterpart (four features in one bullet); #65 already checked by number as the tokenized-detection dedup neighbour |

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-04 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-04 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-04 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-04 | concurrency | all 5 plans, incl. this one | explicit |
| 5-8 | SEC-03 | adjacency / empty / ordering / concurrency | `63-01`, which owns `### SEC-03 Integrity Posture` | explicit |

Accounting: 8 surfaced = 8 authored = 0 dropped. No edge needed a backstop and none was dismissed.

<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Inherited referral triage -->
<!-- planner-discipline-allow: ### Cross-phase observations (VS Code side) -->
<!-- Task 2 negative-greps `RU-63-03`'s six basenames out of the `## RU-63-02` section only. Task 3's
     close-out legitimately names `BbjNodeDownloader.java` in §E/§F, a different section extracted by a
     different range, so the two do not collide. -->
<!-- planner-discipline-allow: BbjNodeDownloader.java -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-63-02 at evidence tier `repro` — D1, D2, D3, D7 — and triage the 2 inherited referrals</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`.planning/reviews/63-COVERAGE.md` exists, contains a `## RU-63-02 — ` heading whose 7
  live cell lines still end with the placeholder word `pending`, and plan `63-04` has committed — the
  phase-wide verdict count is exactly 28. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      `## Inherited referral ledger (D-06)`, and the four completed unit sections as the shape to
      copy, including the `### Inherited referral triage` renderings plans `63-02` and `63-03`
      established
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-05` → its `### Cross-unit referrals` sub-block
      (around lines 1454-1475) and `## RU-62-02` → its `### Cross-unit referrals` sub-block (around
      lines 1835-1838) — the **full text** of referrals #6 and #7, including the commands Phase 62
      already ran and their outputs. Do not re-derive them
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-05` §D7 cell — the four-file byte-identical
      `copyTextMateBundle` diff, `BbjTextMateBundleProvider.java:17-23`'s `BUNDLE_FILES` list
      including its hand-authored fifth file, and the confirmation that #381 is fixed symmetrically.
      This unit's D7 cell starts here rather than from zero
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot" (**#65**, **#381** and **#476** are this unit's named
      neighbours), §"Phase 63 review units …" → `### RU-63-02` including its named-file note about
      `BbjTextMateBundleProvider.java`, §"Applicability Grid" row `RU-63-02`, §"Exclusion reasons" →
      **R-D6-CENTRAL**, §"Surface Accounting" → the `bbj-intellij/` breakdown's `src/main/resources/`
      exclusion with its `plugin.xml` cross-reference
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-04, RVW-06, RVW-07
    - `.planning/PROJECT.md` §"Known tech debt" — the entry stating the IntelliJ TextMate bundle
      cannot exclude `config.bbx` at filename level, which referral #6 sits directly on
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-05, D-06, D-07, D-09, D-13,
      D-14, D-15, D-16 are the decisions this task turns on
    - **Review targets, read in full:** all 18 files listed in the three sub-clusters above.
      **Apply the 6/18 split** — `BbjSettings.java`, `BbjSettingsComponent.java`,
      `BbjSettingsConfigurable.java`, `BbjHomeDetector.java`, `BbjNodeDetector.java` and
      `BbjNodeDownloader.java` sit in the same directory but belong to `RU-63-03` and are **not**
      part of this unit
    - `bbj-intellij/src/main/resources/META-INF/plugin.xml` (247 lines) — **context only (D-16)**:
      its extension-point declarations are the manifest counterpart to this unit's Java registration
      classes. No cell covers it and no finding is located in it
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-vscode/package.json`'s `contributes.languages` / `grammars` / `commands` entries and
      `bbj-vscode/language-configuration.json`, plus `bbj-vscode/src/extension.ts` grepped for the
      format, denumber, tokenized-detection and decompile command registrations referral #7 names
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-63-02 — Language registration, editor support &
notifications`, replacing the placeholder on each with `pass` or `fail` plus a written line naming
the concrete checks applied. Append any resulting finding records under that section's
`### Findings`, and fill its `### Inherited referral triage` sub-block. Touch no other unit section.
Enumerate the dimensions explicitly so none is absorbed by whichever is loudest (D-15): this task
covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource use, and
D7 Cross-IDE parity** — and no others. D6 is `n/a — R-D6-CENTRAL` here and stays untouched.

Sweep in the three named sub-clusters — **A** registration & file type, **B** lexer & editor
plumbing, **C** notification providers & presentation — and name every one of the 18 files
individually in the section text, in a check line or a finding `location:`. A cluster is an
organising device, not a unit of verdict: a per-cluster generalisation over an unread file is a bare
verdict wearing a written line.

*D1 Security* (tier `repro`, satisfied by trace per D-07). Assess against REQUIREMENTS.md's D1
wording — injection, untrusted input, secret exposure, integrity gaps, privilege/trust-boundary
errors. This unit is largely declarative, so the honest D1 answer may well be `pass` — but it must
be a *written* pass. The concrete checks: what `BbjTextMateBundleProvider.java` copies and from
where, whether its `BUNDLE_FILES` list at `:17-23` is validated before files are read or written,
and where the destination path comes from — this is the one file in the unit that touches the
filesystem with a path; whether any notification provider (`BbjJavaInteropNotificationProvider`,
`BbjMissingHomeNotificationProvider`, `BbjMissingNodeNotificationProvider`,
`BbjWelcomeNotification`) interpolates a settings value, a filesystem path, a detected BBj home or a
server message into notification text or into an action it offers, and whether any of those actions
executes something; whether `BbjColorSettingsPage.java`'s demo text or attribute descriptors carry
anything external; and whether `BbjWordLexer.java` or `BbjParserDefinition.java` can be driven into
unbounded work or a crash by hostile document content — a document-content trust boundary, since any
opened file reaches them. State the conclusion as a fact whether or not it produces a finding.

*D2 Correctness & error handling* (tier `repro`, satisfied by trace per D-07). Assess against
null/undefined safety, unhandled rejections, swallowed exceptions, async races, off-by-one, wrong
edge-case behaviour and resource leaks. The concrete checks: `BbjWordLexer.java`'s tokenisation
boundaries — what it does at end-of-buffer, on an empty buffer, on a lone continuation character,
and on a mixed line-ending document (the same class of invariant Phase 61 found broken on the
TypeScript lexer side, so check it here rather than assume symmetry); `BbjParserDefinition.java`'s
element-type and whitespace/comment token-set declarations against `BbjTokenTypes.java` — a missing
or mismatched token type is a silent editor-behaviour defect; `BbjPairedBraceMatcher.java`'s pair
table and its `isPairedBracesAllowedBeforeType` behaviour at end-of-file; `BbjCommenter.java`'s
line/block comment prefixes against BBj's actual comment syntax, including `REM`; whether
`BbjFileType.java` / `BbjLanguage.java` / `BbjFile.java` agree on one language instance and one
extension set; each notification provider's behaviour when the condition it reports flips while the
notification is showing, and whether any can be produced repeatedly for the same editor; and
`BbjTextMateBundleProvider.java`'s behaviour when a bundle file is missing, unreadable or already
present at the destination. Each claim needs a line-by-line trace naming the concrete input or state
and the exact `file:line` where behaviour diverges.

*D3 Performance & resource use* (tier `repro`, satisfied by trace per D-07). Assess against hot-path
cost, quadratic walks, missing caches or debounces, redundant work and unbounded memory growth. The
concrete checks: `BbjWordLexer.java` is on the per-keystroke editor path, so state its per-character
cost and whether it allocates or rescans; whether `BbjColorSettingsPage.java` rebuilds its attribute
descriptor array per call; whether the four notification providers do filesystem or settings work per
editor-notification pass (these run often); whether `BbjSpellcheckingStrategy.java` adds per-token
cost; and whether `BbjTextMateBundleProvider.java`'s copy work runs once or on every startup.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). **The direction is reversed relative to Phase 62: this
phase owns the IntelliJ rows and reads `bbj-vscode/` as reference material only (D-05).** Start from
Phase 62's `RU-62-05` D7 cell rather than from zero — the byte-identical bundle diff and the
`BUNDLE_FILES` list are already established, and #381 is already confirmed fixed symmetrically; cite
them in `evidence:` rather than re-deriving them. The concrete checks: compare the file
extensions, language IDs and grammar registrations IntelliJ declares (across `BbjFileType.java`,
`BbjLanguage.java`, `BbjTextMateBundleProvider.java` and `plugin.xml` read as context) against
VS Code's `contributes.languages` and `grammars`, and name every extension or file name recognised
on one side and not the other — `.bbl` and `config.bbx` are the two known pressure points; compare
comment, brace-matching and code-style behaviour against
`bbj-vscode/language-configuration.json`; and compare the editor-command surface for the four
features referral #7 names. Record a divergence whose defect is on the **IntelliJ** side as a
`P63-D7-{seq}` finding whose `location:` is the IntelliJ file that is missing the behaviour or
differs — never `plugin.xml` (D-16) — citing the `bbj-vscode/` `file:line` in `evidence:` as the
comparison point only. Where the defect is plainly on the **VS Code** side, record nothing in
`### Findings` and instead append a bullet to `### Cross-phase observations (VS Code side)` in the
close-out stating the surface, the evidence, and which Phase 62 unit would have owned it (D-05).

**Then fill `### Inherited referral triage`** with the 2 referrals addressed to this unit, one
bullet each, using the full referral text read from `62-COVERAGE.md` rather than a re-derivation.
**Referral #6** — whether IntelliJ's TextMate importer honors `filenames` and whether LSP4IJ
registration independently covers `.bbl`: answer both halves against the actual registration code
and `plugin.xml` (context only), noting PROJECT.md's known-debt entry that the IntelliJ TextMate
bundle cannot exclude `config.bbx` at filename level, and dispose it as promoted (naming the finding
ID and checking **#381** by number in `dedup:`), dismissed with the code or document that
establishes the limitation as a deliberate, documented platform constraint, or not-reproducible with
its reason. **Referral #7** — none of format, denumber, tokenized-detection or decompile has any
IntelliJ counterpart: this is four features in one bullet, so enumerate all four on both sides
before disposing, and if promoted, allocate **one** finding for the categorical absence rather than
four, with **#65** checked by number in `dedup:` as the tokenized-detection neighbour. Each bullet
names its source referral, states its disposition in the vocabulary promoted /
dismissed-with-evidence / not-reproducible, and — where promoted — names the finding ID it became;
where not-reproducible, is also written under this unit's `### Not-reproducible dispositions` with
its reason.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order. `unit:` is `RU-63-02`. `evidence:` opens with the trace basis and states in one clause why no
runnable reproduction accompanies it (D-07). `classification:` records pass/FAIL for all six
INVENTORY 3c tests inline and **states which reading of test (4) was applied** (D-09). `dedup:` is
never blank and is checked against the frozen 15, with **#65**, **#381** and **#476** checked
explicitly by number. A `critical` or `high` D1 finding follows D-13's two-tier disclosure rule
strictly. Any claim that does not clear its tier goes under `### Not-reproducible dispositions` with
the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-02 /,/^## Phase 63 Close-Out/p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D7 .* — (pass|fail) — ` each print `1`
    - The unit's `n/a` cell is intact and unreworded: within the extracted section, `grep -cE '^- D6 .* — n/a — '` prints `1`
    - The D7 cell starts from Phase 62's established basis: the extracted section contains `RU-62-05`, `BbjTextMateBundleProvider.java` and `.bbl`
    - Both inherited referrals are dispositioned: the extracted section contains `### Inherited referral triage` and exactly `2` lines beginning `- **Referral`, and the section contains `#65` and `denumber`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `32` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `3`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding IDs unique, well-formed, never from the reserved namespace: `grep -oE '^id:[[:space:]]+P63-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field and no blank dedup: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`, and `grep -cE '^dedup:[[:space:]]*$'` prints `0`
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-02 /,/^## Phase 63 Close-Out/p" "$f" > /tmp/p63ru02.txt; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru02.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p63ru02.txt)" = 1; for s in "RU-62-05" "BbjTextMateBundleProvider.java" ".bbl" "### Inherited referral triage" "#65" "denumber"; do grep -qF "$s" /tmp/p63ru02.txt; done; test "$(grep -c "^- \*\*Referral" /tmp/p63ru02.txt)" = 2; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 32; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 3; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_05_REPRO_OK'</automated>
  </verify>
  <done>`RU-63-02`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied across the three sub-clusters — the bundle-copy path and the notification
  providers' interpolated content for D1; lexer boundaries, token-set agreement and comment/brace tables
  for D2; per-keystroke lexer cost and per-pass notification work for D3; and the IntelliJ extension /
  language-ID / grammar registration set against VS Code's for D7 starting from `RU-62-05`'s established
  basis — and both inherited referrals carry a written disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-63-02 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`## RU-63-02`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed — and the
  phase-wide verdict count is exactly 32. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` §`## RU-63-02` — the section this task completes — and
      §`## RU-63-03`'s D5 cell plus the `P63-D5-001` record, which this unit cross-references by ID
      rather than restating (D-08)
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-04, RVW-06, RVW-07
    - `.planning/ROADMAP.md` §"Phase 63" success criterion 1 — it names "lexer/parser definitions"
      but not the five notification providers, `BbjColorSettingsPage.java`,
      `BbjSpellcheckingStrategy.java`, `BbjTextMateBundleProvider.java` or `BbjCommenter.java`, all
      of which are swept in full anyway (D-16)
    - **Review targets:** the same 18 files as Task 1, in the same three sub-clusters
    - `CLAUDE.md` (repo root) — its claims about the shared TextMate grammar
      (`syntaxes/bbj.tmLanguage.json`) and about the IntelliJ plugin bundling it are checkable
      against `BbjTextMateBundleProvider.java`; a candidate D8 target, not an asserted finding
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Complete `## RU-63-02` by recording its D4, D5 and D8 cell lines, then confirm the unit against the
four-part stopping rule. Enumerate the dimensions explicitly (D-15): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks, on a
mechanical basis: the four notification providers (57 / 55 / 76 / 63 LOC) are the strongest
duplication signal in the unit — compare them pairwise with `diff` and state the actual overlap and
whether a shared base or factory is absent; check whether the three tiny registration classes
(`BbjLanguage.java` 11, `BbjPsiElement.java` 15, `BbjIcons.java` 19) are each pulling their weight
or are indirection without purpose; check `BbjTokenTypes.java` against `BbjParserDefinition.java`
and `BbjColorSettingsPage.java` for token or attribute keys declared in more than one place;
check whether `BbjColorSettingsPage.java` (157, the largest file in the unit) is a data table or has
accreted logic; and look for dead code — any registration class, token type or icon that nothing
references. Where a duplication is real, allocate **one** finding rather than one per copy. Per
D-09, a dead-code or unused-import removal changes no runtime behaviour and may therefore satisfy
test (4) vacuously and be `easy`; a change to a token set, a comment prefix or a brace table does
change behaviour and is `major`. The record states which reading was applied.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **Cross-reference `P63-D5-001` by
ID; do not restate the systemic fact (D-08).** Record `fail` with a line naming `P63-D5-001` as the
systemic absence of any test source set in `bbj-intellij`, then state **this unit's own specific
consequence**: which registration and editor behaviours are consequently untested — extension and
language-ID registration, the lexer's tokenisation boundaries, the token-set declarations, the
comment and brace tables, the bundle-copy path, and each notification provider's trigger condition
— and what a first test would have to cover for this unit specifically. Note the parity-relevant
asymmetry in one clause: the VS Code side has grammar and language-configuration tests (Phase 62
exercised them via live tokenization) while the IntelliJ side has none for the same registration
contract.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong Javadoc and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: every class-level and
method-level comment across the eighteen files against the code just read — in particular any
comment on `BbjTextMateBundleProvider.java` describing what is copied and why, checked against its
`BUNDLE_FILES` list at `:17-23` including the hand-authored fifth file Phase 62 identified; any
comment in `BbjParserDefinition.java` or `BbjWordLexer.java` claiming a tokenisation or
whitespace-handling behaviour; and any comment asserting a registration is handled elsewhere. Then
check `CLAUDE.md`'s claims that both IDEs share `syntaxes/bbj.tmLanguage.json` and that the IntelliJ
plugin bundles the compiled LS and the TextMate grammar, against this unit's actual bundle provider.
Where `CLAUDE.md` is simply silent about a surface, note the silence as an observation rather than
promoting it to a finding. Per D-09, a D8 fix that changes only comment or Javadoc text satisfies
test (4) vacuously and may be `easy` if the other five tests pass.

**Then close the unit against the four-part stopping rule** and make the closure visible in the
section text: all 7 live cells carry a verdict plus a written check line; **all eighteen file names
appear at least once inside `## RU-63-02`**, and the unit boundary is stated *without listing the
other unit's basenames* — write it as "the six settings, detection and runtime-download files in
this directory are claimed by `RU-63-03` and are swept there", never as an enumeration, so the
6/18 split stays mechanically checkable by the absence of those names from this section;
every candidate claim raised during either task is either a finding record or a written
`### Not-reproducible dispositions` entry with its reason; and both inherited referrals carry a
disposition. If the sweep produced no not-reproducible candidates and no new outbound cross-unit
referrals, state those sub-blocks as explicitly empty rather than omitting them (D-14's register).
State in the section text that `plugin.xml` was read as context for the registration sweep and
carries no cell and no finding location (D-16), and that all eighteen files were swept even though
ROADMAP criterion 1 names only "lexer/parser definitions" from this unit — the grid is the contract
and the criteria are a subset of it.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-02 /,/^## Phase 63 Close-Out/p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The D5 cell cross-references rather than restates: the extracted section contains `P63-D5-001`
    - Coverage is file-granular across all three sub-clusters: each of the eighteen basenames `BbjColorSettingsPage.java`, `BbjWordLexer.java`, `BbjParserDefinition.java`, `BbjMissingNodeNotificationProvider.java`, `BbjWelcomeNotification.java`, `BbjJavaInteropNotificationProvider.java`, `BbjMissingHomeNotificationProvider.java`, `BbjTextMateBundleProvider.java`, `BbjPairedBraceMatcher.java`, `BbjCommenter.java`, `BbjFileType.java`, `BbjLanguageCodeStyleSettingsProvider.java`, `BbjTokenTypes.java`, `BbjFile.java`, `BbjIcons.java`, `BbjSpellcheckingStrategy.java`, `BbjPsiElement.java`, `BbjLanguage.java` appears at least once in the extracted section
    - The unit boundary is honoured: none of `BbjSettings.java`, `BbjSettingsComponent.java`, `BbjSettingsConfigurable.java`, `BbjHomeDetector.java`, `BbjNodeDetector.java`, `BbjNodeDownloader.java` appears inside the extracted `## RU-63-02` section
    - All five per-unit sub-blocks exist: `### Cells`, `### Inherited referral triage`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear in the extracted section
    - The context-only and scope-fidelity statements are present: the extracted section contains `plugin.xml` and contains `criterion 1`
    - Phase-wide counts prove every live cell is now recorded: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `35` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `0`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`, and the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-02 /,/^## Phase 63 Close-Out/p" "$f" > /tmp/p63ru02.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru02.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p63ru02.txt)" = 7; for s in "P63-D5-001" "BbjColorSettingsPage.java" "BbjWordLexer.java" "BbjParserDefinition.java" "BbjMissingNodeNotificationProvider.java" "BbjWelcomeNotification.java" "BbjJavaInteropNotificationProvider.java" "BbjMissingHomeNotificationProvider.java" "BbjTextMateBundleProvider.java" "BbjPairedBraceMatcher.java" "BbjCommenter.java" "BbjFileType.java" "BbjLanguageCodeStyleSettingsProvider.java" "BbjTokenTypes.java" "BbjFile.java" "BbjIcons.java" "BbjSpellcheckingStrategy.java" "BbjPsiElement.java" "BbjLanguage.java" "### Cells" "### Inherited referral triage" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals" "plugin.xml" "criterion 1"; do grep -qF "$s" /tmp/p63ru02.txt; done; for s in BbjSettings.java BbjSettingsComponent.java BbjSettingsConfigurable.java BbjHomeDetector.java BbjNodeDetector.java BbjNodeDownloader.java; do test "$(grep -cF "$s" /tmp/p63ru02.txt)" = 0; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 35; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 0; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_05_RU02_COMPLETE'</automated>
  </verify>
  <done>`RU-63-02` is complete under the four-part stopping rule and every one of the phase's 35 live cells
  now carries a verdict with zero placeholders remaining: all 18 files are named inside the section across
  the three sub-clusters, none of `RU-63-03`'s six files is claimed here, the four notification providers'
  duplication question is settled on a mechanical `diff` basis, the D5 cell cross-references `P63-D5-001`
  with this unit's own consequence, `plugin.xml`'s context-only status is stated, both inherited referrals
  carry a disposition, and the D-16 scope-fidelity note explains why all 18 files were swept though
  ROADMAP criterion 1 names only lexer/parser definitions.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 63 — fill the seven close-out sections, record the cross-phase observations, and re-run all three D-17 gates live</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>Every live cell in `.planning/reviews/63-COVERAGE.md` carries a verdict — the phase-wide
  verdict count is exactly 35 and the placeholder count is exactly 0 — and Task 2 has committed. Halt and
  report otherwise; the close-out's accounting is derived from the five completed unit sections.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` — **the whole file**, since the close-out's accounting is
      derived from it: the header and its `## Inherited referral ledger (D-06)`, the grid, the
      cell-total gate, the stopping rule, both exclusion blocks, and all five completed
      `## RU-63-0N` sections including every `### Cells` line, every finding record's `dimension:`,
      `severity:`, `classification:`, `effort:`, `dedup:` and `disposition:` field, every
      `### Not-reproducible dispositions` entry, every `### Inherited referral triage` disposition
      and every `### Cross-unit referrals` entry
    - `.planning/reviews/62-COVERAGE.md` `## Phase 62 Close-Out` sections A-G (lines 1839-2076) —
      the shape to mirror. Note the one structural difference: Phase 62's §D referral table was
      entirely **outbound**; Phase 63's Group 2 is **inbound** and carries a disposition column
    - `.planning/reviews/INVENTORY.md` §"Applicability Grid" (the five `RU-63-*` rows the cell gate
      re-derives from), §"Grid totals", §"Routing table (D-06)" (the one Phase 63 row),
      §"Recording protocol (D-09)"
    - `.planning/ROADMAP.md` §"Phase 63: IntelliJ Plugin Review" — the four success criteria answered
      in §F, criterion 2 being the `BbjNodeDownloader.java` integrity requirement
    - `.planning/REQUIREMENTS.md` — RVW-04 and SEC-03, both of which close with this phase
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-05, D-11, D-16, D-17, D-18
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Fill `## Phase 63 Close-Out` with seven lettered sections mirroring `62-COVERAGE.md`'s A-G, plus
`### Cross-phase observations (VS Code side)`, and resolve the header ledger's disposition column.
**Every gate below is re-run at execution time and its literal output recorded — none is restated
from `63-CONTEXT.md` (D-17).**

**A. File gate (D-17.2).** Enumerate the in-scope files from the tree, not from a list typed into a
plan. Run `find bbj-intellij/src/main/java -name '*.java' | wc -l` and record the command and its
literal output, which must be `61`. Then, for each of the 61 basenames, confirm it appears somewhere
in `63-COVERAGE.md` — run a shell loop over the `find` output that greps each basename and reports
any that is absent, and record the command and its result. State the unit distribution
(6 + 11 + 13 + 13 + 18 = 61) and confirm the 6/18 split of the 24 files directly under
`com/basis/bbj/intellij/` held: no file is named inside two unit sections.

**B. Cell-total gate (D-17.1), three sources.** Build the three-source agreement table Phase 62
used: the **stated** totals (35 `applies`, 5 `n/a`, 40 total), the **re-derivation from INVENTORY**
via this exact command with its literal output recorded —

    awk '/^\| `RU-63-0[1-5]` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md

— and this file's **own grep-counted content**: the count of lines matching `- D{n} ` with a verdict,
the count with `n/a`, and the total. All three must agree. **If any disagrees, that disagreement is
itself a defect to surface in this section, not a number to quietly adopt** — write it up rather
than reconciling silently.

**C. Finding accounting.** Tables counting every recorded finding by primary dimension (D1-D8), by
`severity`, by `classification` (`easy` versus `major`), by `effort` (2 / 4 / 8) and by
`disposition`. Then a not-reproducible-by-unit table. Then a dedup-resolution paragraph stating how
many findings resolved to `none`, how many to a `#NNN duplicate` and how many to a
`#NNN partial-overlap`, naming the issue numbers touched from the frozen 15. State the D-09 split
explicitly: how many findings were classified `major` because test (4) failed on the absent test
source set, and how many were `easy` because their fix changes no runtime behaviour and satisfied
test (4) vacuously — this split is what Phase 67 and Phase 68 consume.

**D. Cross-unit referral accounting.** Two groups. **Group 1 — referrals addressed to another
Phase 63 unit**: count them and list them, or state zero as a fact. **Group 2 — the inbound
inherited work**, an 8-row table with columns *from unit / to unit / subject / disposition*: the 7
Phase 62 referrals plus INVENTORY's 1 routed item, each carrying `promoted` (with the finding ID),
`dismissed-with-evidence` (with the citation) or `not-reproducible` (with the reason). Note the
merge explicitly: referrals #4 and #5 are one absence seen twice and resolve to **one** disposition,
so 8 rows resolve to 7 distinct dispositions. Then **resolve the header's
`## Inherited referral ledger (D-06)` disposition column** to match, so the ledger and this table
agree. This is the referral gate (D-17.3): **zero of the 8 may be silently dropped.**

**E. Scope-fidelity note (D-16).** State plainly that **all 61 files were swept** although ROADMAP
criterion 1 names only a subset, and name the extra surfaces explicitly: the five notification
providers (`BbjJavaInteropNotificationProvider.java`, `BbjMissingHomeNotificationProvider.java`,
`BbjMissingNodeNotificationProvider.java`, `BbjServerCrashNotificationProvider.java`,
`BbjWelcomeNotification.java`), `BbjColorSettingsPage.java`, `BbjSpellcheckingStrategy.java`,
`BbjTextMateBundleProvider.java`, `BbjCommenter.java`, and the composer bridge
(`BbjComposerServer.java`, `BbjComposerService.java`, `ComposerLauncher.java`, `ComposerModels.java`,
the three `Configure*Intention.java` files and the three `*SchematicPanel.java` files). Also state
the two boundaries this phase held: `plugin.xml` was read as context for `RU-63-02` and carries no
cell and no finding location, and `bbj-vscode/` was read as D7 reference material only with no
`P63-*` finding located inside it. Name the one deliberate exception: `RU-63-03`/D6's routed-item
finding at `bbj-intellij/build.gradle.kts:12-13`, a file INVENTORY assigns to `RU-64-02`, recorded
here because INVENTORY's own routing table names Phase 63 as the target phase.

**F. ROADMAP success criteria.** Answer each of the four per-criterion, in Phase 62's "Met." shape,
citing where in this file the evidence sits. Criterion 1: all 61 files have a recorded pass/fail
against D1-D8 — cite §A and §B. Criterion 2: `BbjNodeDownloader.java`'s integrity posture is
documented — transport security, checksum or signature verification, and archive extraction path
safety — cite `RU-63-03`'s `### SEC-03 Integrity Posture` and state that **SEC-03 closes with this
phase** (D-11), so nothing on it flows to Phase 65 as open work. Criterion 3: every recorded finding
carries `file:line`, a dimension and a verified failure scenario per the Phase 60 standard — cite
§C and state the D-07 evidence basis, that every `repro`-tier record clears its tier by
line-by-line trace because the Gradle build does not run in this environment. Criterion 4: every
finding was checked against the 15 open issues — cite the dedup paragraph in §C.

**G. Closing confirmations.** Mirror Phase 62's: ISSUE-01 not triggered (no issue opened,
commented on or drafted); INVENTORY not edited; `62-COVERAGE.md` not reopened or edited; no source
file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` modified — evidence each with
`git status --porcelain` output. Then a downstream-inheritance table naming what each later phase
consumes: **Phase 64** — the routed toolchain item at `build.gradle.kts:12-13` to re-triage, and
`RU-64-02`/SEC-08's continuing ownership of all other dependency versions; **Phase 65** —
`RU-63-01`'s D1 records as the IntelliJ half of the SEC-04/SEC-05 synthesis, plus the fact that
SEC-03 is already closed; **Phase 66** — every finding whose `dedup:` names a `DEBT-*` requirement,
DEBT-05 in particular; **Phase 67** — the `classification: easy` set for its apply path;
**Phase 68** — this whole file for the DOC-03 concatenation, including
`### Cross-phase observations (VS Code side)`; **Phase 69** — the promoted findings for issue
drafting, gated on ISSUE-01.

**`### Cross-phase observations (VS Code side)`.** Confirm this subsection's contents against the
five unit sections: every VS Code-side observation any plan appended is present, each stating the
surface (a `bbj-vscode/` `file:line`), the evidence observed during a D7 read, the Phase 62 unit
that would have owned it, and one sentence explaining why it is recorded here rather than as a
`P63-D7-*` finding — because no `P63-*` finding may be located inside `bbj-vscode/` and Phase 62 is
closed and is not reopened (D-05). **If no plan appended one, state that as a fact** — "zero VS
Code-side observations arose during Phase 63's D7 reads" — rather than deleting the subsection;
Phase 68 reads it either way (DOC-04's intent).

Finally, confirm in one line that the swept-tree SHA recorded in the header by plan `63-01` is still
the tree these five sweeps describe (D-18), and that RVW-04 and SEC-03 are both answered by this
file.
  </action>
  <acceptance_criteria>
    - All seven close-out sections and the cross-phase subsection exist: with the close-out extracted by `sed -n '/^## Phase 63 Close-Out/,$p' .planning/reviews/63-COVERAGE.md`, the strings `### A.`, `### B.`, `### C.`, `### D.`, `### E.`, `### F.`, `### G.` and `### Cross-phase observations (VS Code side)` each appear
    - The file gate was re-run live and agrees with the tree: the close-out contains `find bbj-intellij/src/main/java -name` and contains the literal `61`, and independently `find bbj-intellij/src/main/java -name '*.java' | wc -l` still prints `61`
    - Every one of the 61 basenames from the tree appears somewhere in the coverage file — verified by looping the live `find` output and asserting zero absent basenames
    - The cell gate was re-run live and all three sources agree: the close-out contains the `RU-63-0[1-5]` awk line and the literal `35 5 40`, and independently the awk pass over INVENTORY still prints `35 5 40`
    - The referral gate is complete: the close-out contains a Group 2 table naming all four source units `RU-62-01`, `RU-62-02`, `RU-62-03`, `RU-62-04` and `RU-62-05`, contains `build.gradle.kts`, and the count of lines in the close-out matching `promoted|dismissed-with-evidence|not-reproducible` is at least `7`
    - The scope-fidelity note names the extra surfaces: the close-out contains `BbjSpellcheckingStrategy.java`, `BbjColorSettingsPage.java`, `BbjWelcomeNotification.java` and `ComposerModels.java`
    - All four ROADMAP criteria are answered and SEC-03 is closed: the close-out contains `SEC-03 Integrity Posture`, `criterion 2` and `Met`
    - The whole grid is recorded with nothing outstanding: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `35`, `grep -cE '^- D[1-8] .* — pending$'` prints `0`, `grep -cE '^- D[1-8] .* — n/a — '` prints `5`, and `grep -cE '^- D[1-8] '` prints `40`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`, and the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records are complete and well-formed phase-wide: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## Phase 63 Close-Out/,\$p" "$f" > /tmp/p63co.txt; for s in "### A." "### B." "### C." "### D." "### E." "### F." "### G." "### Cross-phase observations (VS Code side)" "find bbj-intellij/src/main/java -name" "61" "RU-63-0[1-5]" "35 5 40" "RU-62-01" "RU-62-02" "RU-62-03" "RU-62-04" "RU-62-05" "build.gradle.kts" "BbjSpellcheckingStrategy.java" "BbjColorSettingsPage.java" "BbjWelcomeNotification.java" "ComposerModels.java" "SEC-03 Integrity Posture" "criterion 2" "Met"; do grep -qF "$s" /tmp/p63co.txt; done; test "$(grep -cE "promoted|dismissed-with-evidence|not-reproducible" /tmp/p63co.txt)" -ge 7; test "$(find bbj-intellij/src/main/java -name "*.java" | wc -l)" = 61; miss=0; for p in $(find bbj-intellij/src/main/java -name "*.java"); do b=$(basename "$p"); grep -qF "$b" "$f" || { echo "ABSENT: $b"; miss=1; }; done; test "$miss" = 0; test "$(awk "/^\\| \`RU-63-0[1-5]\` \\|/ {a+=gsub(/applies/,\"applies\"); n+=gsub(/n\\/a/,\"n\\/a\")} END{print a, n, a+n}" .planning/reviews/INVENTORY.md)" = "35 5 40"; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 35; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 0; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_CLOSED'</automated>
  </verify>
  <done>Phase 63 is closed in the record: `## Phase 63 Close-Out` carries all seven lettered sections plus
  `### Cross-phase observations (VS Code side)`; the file gate was re-run live and enumerates 61 files
  from the tree with every basename confirmed present; the cell gate was re-run live and all three sources
  agree at 35 / 5 / 40; the referral gate shows all 7 inherited Phase 62 referrals plus the 1 routed item
  carrying a written disposition, with #4 and #5 resolving to one; the scope-fidelity note names every
  surface swept beyond ROADMAP criterion 1; all four success criteria are answered per-criterion with
  criterion 2 satisfied by `RU-63-03`'s `### SEC-03 Integrity Posture`; and the closing confirmations
  evidence that no source file, `INVENTORY.md` or `62-COVERAGE.md` was modified and no GitHub issue was
  touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats — and, uniquely for this plan,
**completion-claim** threats, because it is the plan that declares the phase done. Rows marked
*subject-matter* are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| document content → `BbjWordLexer.java` / `BbjParserDefinition.java` (*subject-matter*) | Any opened file's bytes reach the lexer and parser definition on the editor path. |
| the TextMate bundle copy path in `BbjTextMateBundleProvider.java` (*subject-matter*) | The one file in `RU-63-02` that reads and writes filesystem paths, with a `BUNDLE_FILES` list at `:17-23`. |
| settings values, detected paths and server messages → notification text and actions (*subject-matter*) | The four notification providers surface externally-sourced content and may offer actions. |
| `63-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record is world-readable on landing. |
| the close-out's gate outputs → Phase 68's DOC-03 and the phase's own verification | A gate declared satisfied without being re-run is an unfalsifiable completion claim. |
| a re-derived number that disagrees with a stated one → the record | Silently adopting the convenient number hides a real defect. |
| inherited referrals and the routed item → the phase's completion claim | An untriaged referral is countable work silently skipped (D-17.3). |
| VS Code-side observations → nowhere, if not recorded here | Phase 62 is closed; without `### Cross-phase observations (VS Code side)` such an observation is dropped. |
| `bbj-vscode/` and `plugin.xml` → this phase's finding namespace | Read as reference/context only; a `location:` inside either would double-count or invent a cell. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past `MAJOR-REFACTORS.md` review. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-P05-S1 | Denial of Service | `BbjWordLexer.java` on hostile or pathological document content | medium | transfer | Task 1's D1 and D2 checks cover end-of-buffer, empty-buffer, lone-continuation and mixed-line-ending inputs and per-character cost; remediation transfers to Phase 67. |
| T-63-P05-S2 | Tampering | the bundle copy path and `BUNDLE_FILES` list in `BbjTextMateBundleProvider.java` | medium | transfer | Task 1's D1 check asks what is copied, from where, to where, and whether the list is validated before files are read or written. |
| T-63-P05-S3 | Information Disclosure | externally-sourced content interpolated into notification text or actions | medium | transfer | Task 1's D1 check covers all four notification providers and whether any offered action executes something. |
| T-63-P05-01 | Repudiation | a completion gate declared satisfied without being re-run | high | mitigate | Explicit prohibition plus acceptance criteria that re-run the `find`-based file gate and the INVENTORY awk pass **independently of the file's own text** and require both the command and its literal output to appear in the close-out. |
| T-63-P05-02 | Repudiation | adopting a re-derived number that disagrees with the stated totals | high | mitigate | D-17.1 prohibition: a disagreement is written up in §B as a defect to surface. The independent re-derivation in the verify block makes silent adoption detectable. |
| T-63-P05-03 | Repudiation | one of the 8 inherited/routed items silently dropped from the accounting | high | mitigate | D-17.3 prohibition plus an acceptance criterion counting at least 7 disposition tokens in the close-out and asserting all five Phase 62 source units and `build.gradle.kts` are named in §D. |
| T-63-P05-04 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | D-14 plus an acceptance criterion requiring every `pass` cell line phase-wide to be at least 120 bytes of written checks. |
| T-63-P05-05 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-13's two-tier rule: surface, problem class and impact; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-63-P05-06 | Repudiation | a VS Code-side observation dropped because Phase 62 is closed | high | mitigate | D-05 prohibition against reopening `62-COVERAGE.md`, plus the requirement that `### Cross-phase observations (VS Code side)` exists and states zero as a fact if empty rather than being deleted. |
| T-63-P05-07 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; §C reports the D-09 easy/major split explicitly so the routing is auditable. |
| T-63-P05-08 | Tampering | editing `INVENTORY.md` or `62-COVERAGE.md` so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for both, evidenced inside §G as well. |
| T-63-P05-09 | Tampering | a globbed sweep of `com/basis/bbj/intellij/` double-counting the 6/18 split | medium | mitigate | Prohibition plus an acceptance criterion asserting none of `RU-63-03`'s six basenames appears inside the `## RU-63-02` section. |
| T-63-P05-10 | Tampering | a finding relocated into `bbj-vscode/` or `plugin.xml` to dodge a boundary | medium | mitigate | D-05/D-16 prohibitions plus anchored acceptance greps asserting zero `^location:` lines resolve into either. |
| T-63-P05-11 | Repudiation | a claim asserting a runnable Gradle reproduction that could not have been produced | medium | mitigate | D-07 prohibition: no build, compile or test task is scheduled; §F's criterion-3 answer states the trace-based evidence basis explicitly. |
| T-63-P05-12 | Tampering | five plans writing one `63-COVERAGE.md` | medium | mitigate | Wave 5 behind `depends_on: [63-04]` makes ordering structural (D-04); the three tasks assert exact global counts (32/3, then 35/0, then 35/0) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-63-P05-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `find`, `ls`, `grep`, `sed`, `diff` and `awk` only. |
</threat_model>

<verification>
- `## RU-63-02`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's `R-D6-CENTRAL` text verbatim.
- All 18 unit files are named inside `## RU-63-02`, and none of `RU-63-03`'s six files is.
- Both inherited referrals are dispositioned; `### Inherited referral triage` holds exactly two
  bullets.
- Phase-wide: 35 verdicts, 0 placeholders, 5 `n/a` lines, 40 cell lines total.
- The close-out carries sections A-G plus `### Cross-phase observations (VS Code side)`.
- The file gate re-runs live: `find bbj-intellij/src/main/java -name '*.java' | wc -l` prints `61`
  and every one of those 61 basenames is present in `63-COVERAGE.md`.
- The cell gate re-runs live: the awk pass over INVENTORY prints `35 5 40` and the close-out records
  the same three-source agreement.
- The referral gate: all 5 Phase 62 source units and the routed `build.gradle.kts` item appear in
  §D with at least 7 distinct dispositions.
- Every finding phase-wide has all 13 fields, a unique well-formed non-`P00-` ID and a non-blank
  `dedup:`; zero `^location:` lines resolve inside `bbj-vscode/` or `plugin.xml`.
- `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- **RVW-04 is complete**: all 61 `bbj-intellij/src/main/java/` files (18 more here, 61 cumulative)
  have a recorded pass/fail against every dimension that applies to them, across all 5 units and all
  35 live cells, with zero placeholders remaining.
- **SEC-03 is complete**: ROADMAP Phase 63 criterion 2 is answered in the close-out by
  `RU-63-03`'s `### SEC-03 Integrity Posture`, and nothing on SEC-03 flows to Phase 65 as open work.
- All three D-17 gates pass on live re-derivation, not on restated numbers, and any disagreement
  would have been surfaced as a defect rather than adopted.
- No inherited work is left outstanding: all 7 Phase 62 referrals and INVENTORY's 1 routed item
  carry a written disposition, and every VS Code-side observation is recorded where Phase 68 will
  read it.
- Phases 64-69 inherit a complete, gate-verified coverage file: the routed toolchain item to
  re-triage, the IntelliJ half of the SEC-04/SEC-05 picture, the DEBT-05 evidence, the `easy`/`major`
  split, the DOC-03 input and the issue-draft candidates.
</success_criteria>

<output>
Create `.planning/phases/63-intellij-plugin-review/63-05-SUMMARY.md` when done.
</output>