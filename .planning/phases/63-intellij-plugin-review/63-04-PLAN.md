---
phase: 63-intellij-plugin-review
plan: 04
type: execute
wave: 4
depends_on: [63-03]
files_modified:
  - .planning/reviews/63-COVERAGE.md
autonomous: true
requirements: [RVW-04]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-63-05` (LSP wiring, server lifecycle & status UI, 13 files / 1,297 LOC — 4 under `lsp/` and 9 under `ui/`, INVENTORY risk rank 4) has all 7 live dimensions recorded in `63-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-14, D-15)"
    - "All 13 files — `lsp/BbjLanguageServer.java`, `lsp/BbjLanguageServerFactory.java`, `lsp/BbjLanguageClient.java`, `lsp/BbjCompletionFeature.java`, `ui/BbjServerService.java`, `ui/BbjJavaInteropService.java`, `ui/BbjStatusBarWidget.java`, `ui/BbjStatusBarWidgetFactory.java`, `ui/BbjJavaInteropStatusBarWidget.java`, `ui/BbjJavaInteropStatusBarWidgetFactory.java`, `ui/BbjRestartServerAction.java`, `ui/BbjServerCrashNotificationProvider.java`, `ui/BbjServerLogToolWindowFactory.java` — are named at least once inside the `## RU-63-05` section, so coverage is file-granular (ROADMAP Phase 63 criterion 1)"
    - "The DEBT-05 surface is recorded with evidence and bounded, not resolved here: the count basis is stated as measured — zero `@ApiStatus.Experimental` annotations exist in this repository's own source and 20 `com.redhat.devtools.lsp4ij` references span 10 files, while PROJECT.md's \"19 experimental API usages\" figure counts annotations on the LSP4IJ side which cannot be grepped from this tree — and any finding on it names DEBT-05 in its `dedup:` field so Phase 66 re-triages rather than re-derives"
    - "`BbjCompletionFeature`'s dependence on LSP4IJ's `LSPCompletionFeature` API is recorded as a concrete D4 coupling observation against `lsp/BbjCompletionFeature.java`, matching PROJECT.md's known-debt entry, rather than left as a general statement about experimental APIs"
    - "The mechanism side of Phase 62's referral #3 — `BbjServerService.restart()` versus a targeted LSP request — is re-triaged here against the disposition plan `63-02` already recorded under `RU-63-01`, cross-referenced by finding ID rather than re-reported as a second finding for the same divergence (D-06)"
    - "The D7 parity cell reads `bbj-vscode/` as reference material only: every divergence is recorded against the IntelliJ file that is missing or differs, with the VS Code side cited in `evidence:` as the comparison point, and no `P63-*` finding carries a `location:` inside `bbj-vscode/` (D-05)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier via INVENTORY 3b's line-by-line-trace branch, and each record states in one clause why it carries no runnable reproduction (D-07); a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #410 (Zed Editor support) and #231 (custom classpath and command-line settings) checked explicitly as this unit's plausible neighbours (RVW-07)"
    - "The D5 cell records `fail` by cross-referencing `P63-D5-001` by ID rather than restating the systemic zero-test-source-set fact, plus this unit's own specific consequence — which lifecycle, status-propagation and interop-probe behaviours are consequently untested and what a first test would have to cover (D-08)"
    - "The unit's 1 `n/a` cell carries INVENTORY's `R-D6-CENTRAL` text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-17)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "Two findings sharing one root cause are recorded ONCE with a cross-reference by finding ID, never restated per layer (D-08 for the D5 systemic absence; D-06 for the two SETOPTS referrals that describe the same absence from two Phase 62 vantage points). Boundary case: the 24 files directly under `com/basis/bbj/intellij/` split 6 (`RU-63-03`) / 18 (`RU-63-02`) — each of the 61 files appears under exactly one unit section, never two"
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording (D-14) — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P63-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(63, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop, so two findings can never tie. Plan execution order is fixed by D-02 (`RU-63-03` -> `RU-63-01` -> `RU-63-04` -> `RU-63-05` -> `RU-63-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All five plans append to the single `.planning/reviews/63-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one wave per plan (waves 1-5), each plan's `depends_on` naming its predecessor in D-02's order (D-04). Phase 63 and Phase 64 may run concurrently but write disjoint `{NN}-COVERAGE.md` files, so no cross-phase collision exists. If a plan is interrupted mid-append, the partially-filled unit section still carries the literal placeholder `pending` on every unrecorded live-dimension cell, which is mechanically checkable at every wave"

  artifacts:
    - ".planning/reviews/63-COVERAGE.md §`## RU-63-05 — LSP wiring, server lifecycle & status UI` — 7 live cells recorded, 1 `n/a` cell carried forward verbatim, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`. This unit owns no inherited referral, so it has no `### Inherited referral triage` sub-block — a fact the section states explicitly rather than leaving as silence"

  key_links:
    - "Any finding whose `dedup:` names DEBT-05 -> Phase 66's LSP4IJ experimental-API and `BbjCompletionFeature`-coupling re-triage; Phase 63 records with evidence, Phase 66 resolves (Phase 61 D-14's pattern)"
    - "The `BbjServerService.restart()` mechanism note -> `RU-63-01`'s already-recorded disposition of Phase 62's referral #3, cross-referenced by ID rather than re-reported (D-06)"
    - "`RU-63-05`'s D1 records on the language-server launch path and the java-interop TCP probe -> Phase 65's SEC-05 process-spawning synthesis across both IDEs (D-12)"
    - "`RU-63-05`'s D7 records -> Phase 67's apply path via `classification:`, and Phase 69's issue drafting alongside #410"
    - "`RU-63-05` cells -> the phase-wide cell, file and referral gates re-run by plan `63-05`"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, network-exposure checklists, supply-chain integrity, GDPR) deliberately DROPPED as
    # prohibitions — in this phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT attempt to resolve DEBT-05's \"19 experimental API usages\" figure by inspecting LSP4IJ's own annotations — they are not greppable from this tree, the count basis is stated as measured instead, and Phase 66 owns the re-triage; asserting a number that was not measured here would be a fabricated count"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence (D-14)"
    - "MUST NOT re-report the `bbj.refreshJavaClasses` restart-versus-targeted-request divergence as a new finding — plan `63-02` already dispositioned Phase 62's referral #3 under `RU-63-01`; this unit re-triages the mechanism side and cross-references by ID (D-06)"
    - "MUST NOT relocate a Phase 63 finding into `bbj-vscode/` to sidestep the IntelliJ-side verdict — that tree is D7 comparison material only, Phase 62 owns its rows and is closed, and a Phase 63 cell claiming them would double-count against INVENTORY's 232-cell total (D-05)"
    - "MUST NOT record a finding whose `location:` is `bbj-intellij/src/main/resources/META-INF/plugin.xml`, even though `BbjLanguageServerFactory.java` is registered through it — it is read as context only and no cell covers it (D-16)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and the gap would be unfixed at commit time; name the surface, the problem class and the impact and stop there (D-13)"
    - "MUST NOT modify any file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` — Phase 63 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` or `.planning/reviews/62-COVERAGE.md` to make a gate agree — INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and Phase 62 is closed and verified 13/13 (D-05)"
    - "MUST NOT schedule, run or claim a Gradle build, compile, test run or build-driven static analysis — the local Temurin 25.0.3 toolchain fails `build.gradle.kts:12-13`'s `JavaVersion.VERSION_17` target before task listing (D-07)"
    - "MUST NOT start, connect to or depend on the `java-interop` socket service to produce evidence — its availability is environment-dependent and INVENTORY already classifies the 11 dependent test failures as `environment`; the probe path is assessed by reading it, not by running it"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and predate `bbj-intellij/` entirely (D-19)"
    - "MUST NOT write into any `## RU-63-0N` section other than `RU-63-05`, MUST NOT reword or re-derive the carried-forward `R-D6-CENTRAL` text, and MUST NOT add a grid row, a cell line or a unit section beyond the 40 cells INVENTORY's Phase 63 slice defines (D-03, D-17)"
    - "MUST NOT extend this unit's cells into a dependency-version audit of LSP4IJ `0.19.0` or IntelliJ Platform `2024.2` — D6 is `n/a — R-D6-CENTRAL` here and `RU-64-02`/SEC-08 owns those versions (D-10)"
    - "MUST NOT restate the systemic zero-test-source-set fact in this unit's D5 cell — cross-reference `P63-D5-001` by ID and add only this unit's own consequence (D-08)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier, MUST NOT leave a `dedup:` field blank, MUST NOT allocate an ID in the reserved `P00-*` namespace, and MUST NOT record a cell verdict for a file the sweep did not open (RVW-06)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-63-05` — LSP wiring, server lifecycle & status UI**
(`ui/BbjServerService.java` 244, `ui/BbjJavaInteropService.java` 206, `ui/BbjStatusBarWidget.java`
167, `ui/BbjJavaInteropStatusBarWidget.java` 151, `lsp/BbjLanguageServer.java` 97,
`lsp/BbjCompletionFeature.java` 77, `lsp/BbjLanguageServerFactory.java` 66,
`ui/BbjServerCrashNotificationProvider.java` 63, `lsp/BbjLanguageClient.java` 50,
`ui/BbjServerLogToolWindowFactory.java` 47, `ui/BbjJavaInteropStatusBarWidgetFactory.java` 43,
`ui/BbjRestartServerAction.java` 43, `ui/BbjStatusBarWidgetFactory.java` 43 — 13 files, 1,297 LOC,
4 under `lsp/` and 9 under `ui/`, INVENTORY risk rank 4 of 5) across its 7 live dimensions, and
record the result in the `## RU-63-05` section of `.planning/reviews/63-COVERAGE.md`.

Purpose: this unit is how the plugin actually starts, watches and restarts the shared language
server, and how it tells the user what state that server is in. It is also the phase's **DEBT-05
surface** — the LSP4IJ experimental-API exposure and the `BbjCompletionFeature`/`LSPCompletionFeature`
coupling PROJECT.md names as known debt. Phase 63 **records** that with evidence and a stated count
basis; Phase 66 resolves it. This unit owns **zero** inherited Phase 62 referrals, but it does carry
the mechanism side of referral #3, whose disposition plan `63-02` already recorded under `RU-63-01`.

Output: 7 recorded cells with written check lines, a bounded and evidenced DEBT-05 record, and any
finding records, not-reproducible dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/reviews/INVENTORY.md
@.planning/phases/63-intellij-plugin-review/63-CONTEXT.md
@.planning/phases/63-intellij-plugin-review/63-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 63 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/63-COVERAGE.md` — section `## RU-63-05 — LSP wiring, server lifecycle & status
  UI` only, plus the one narrowly scoped exception the write contract allows: appending a bullet to
  `### Cross-phase observations (VS Code side)` in the close-out if a VS Code-side observation
  surfaces during the D7 read (D-05). Everything else in the file is inherited from plans `63-01`,
  `63-02` and `63-03` and is not touched.

**Section anchors this plan fills.** `## RU-63-05 — LSP wiring, server lifecycle & status UI` → its
`### Cells`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`. This
unit has **no** `### Inherited referral triage` sub-block, because the D-06 ledger addresses zero
referrals to it — the section states that explicitly rather than leaving it as silence. Downstream
phases consume these anchors by name: Phase 65 (D1 records), **Phase 66 (any `dedup:` naming
DEBT-05 — the likely hit for this unit)**, Phase 67 (`classification:`), Phase 68 (DOC-03
concatenation), Phase 69 (issue drafting, gated on ISSUE-01).

**Finding-ID namespace.** `P63-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(63, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `63-01`,
`63-02` and `63-03` — it does not restart per plan. `P00-*` is INVENTORY's reserved template
namespace and is never allocated.

**Plan -> review-unit map.** This plan owns `RU-63-05`. The full 5-plan map, the wave chain
(waves 1-5 in D-02's risk-rank order) and the section shape live in `63-01-PLAN.md`
§"Artifacts this phase produces".

**Inherited work owned here (D-06): none.** Zero of the 7 Phase 62 referrals are addressed to
`RU-63-05`. Referral #3 (`bbj.refreshJavaClasses`) was dispositioned under `RU-63-01` by plan
`63-02` on the location-decides-ownership rule; this unit re-triages the `BbjServerService.restart()`
mechanism side and cross-references that disposition by ID rather than re-reporting it.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-04 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-04 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-04 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-04 | concurrency | all 5 plans, incl. this one | explicit |
| 5-8 | SEC-03 | adjacency / empty / ordering / concurrency | `63-01`, which owns `### SEC-03 Integrity Posture` | explicit |

Accounting: 8 surfaced = 8 authored = 0 dropped. No edge needed a backstop and none was dismissed.

<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Cross-phase observations (VS Code side) -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-63-05 at evidence tier `repro` — D1, D2, D3, D7</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`.planning/reviews/63-COVERAGE.md` exists, contains a `## RU-63-05 — ` heading whose 7
  live cell lines still end with the placeholder word `pending`, and plan `63-03` has committed — the
  phase-wide verdict count is exactly 21. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, and the
      completed `## RU-63-03`, `## RU-63-01` and `## RU-63-04` sections as the shape to copy, plus
      `## RU-63-01`'s disposition of Phase 62's referral #3, which this unit cross-references
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent), §"Finding Record Template", §"Frozen Open-Issue Snapshot" (**#410** and
      **#231** are this unit's named neighbours), §"Phase 63 review units …" → `### RU-63-05`
      including its DEBT-05 note, §"Applicability Grid" row `RU-63-05`, §"Exclusion reasons" →
      **R-D6-CENTRAL**
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-04, RVW-06, RVW-07; and
      DEBT-05, whose re-triage is Phase 66's and whose evidence is recorded here
    - `.planning/PROJECT.md` §"Known tech debt" — the three IntelliJ-relevant entries (19 LSP4IJ
      experimental API usages; `BbjCompletionFeature` depending on an `LSPCompletionFeature` API
      that may change; the TextMate bundle filename-exclusion limitation, which belongs to
      `RU-63-02` not here)
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-05, D-06, D-07, D-09, D-10,
      D-13, D-14, D-15, D-16 are the decisions this task turns on, plus Claude's Discretion on how
      far to pursue the LSP4IJ experimental-API question, resolved in the action below
    - **Review targets, read in full:** all 13 files under
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/` and
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/`
    - `bbj-intellij/src/main/resources/META-INF/plugin.xml` — **context only (D-16)**, read for the
      `com.redhat.devtools.lsp4ij.server` extension-point registration that
      `lsp/BbjLanguageServerFactory.java:19` names; no cell covers it and no finding is located in it
    - **Reference only for D7, no finding may be located here (D-05):** `bbj-vscode/src/extension.ts`
      grepped for the language-client construction, restart handling, server status surfacing and
      java-interop connection handling, rather than read in full
    - **Do NOT read** any file under `.planning/codebase/` (D-19), do NOT run any Gradle task (D-07),
      and do NOT start or connect to the `java-interop` socket service
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-63-05 — LSP wiring, server lifecycle & status UI`,
replacing the placeholder on each with `pass` or `fail` plus a written line naming the concrete
checks applied. Append any resulting finding records under that section's `### Findings`. Touch no
other unit section. Enumerate the dimensions explicitly so none is absorbed by whichever is loudest
(D-15): this task covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource
use, and D7 Cross-IDE parity** — and no others. D6 is `n/a — R-D6-CENTRAL` here and stays untouched;
LSP4IJ `0.19.0` and IntelliJ Platform `2024.2` version questions belong to `RU-64-02` (D-10).

*D1 Security* (tier `repro`, satisfied by trace per D-07). Assess against REQUIREMENTS.md's D1
wording — injection, untrusted input, secret exposure, integrity gaps, privilege/trust-boundary
errors. The concrete checks. **The language-server launch path:** `lsp/BbjLanguageServer.java`'s
`OSProcessStreamConnectionProvider` configuration — what executable is launched, what arguments and
environment it receives, where the Node runtime path comes from (it is the artifact `RU-63-03`'s
downloader produces and caches), where `out/language/main.cjs` is resolved from, and whether any of
those values is validated before being used to start a process. **The java-interop probe:**
`ui/BbjJavaInteropService.java` opens a TCP `Socket` to a host and port read from settings at check
time (`:114-135` region) — state what host/port values are accepted, whether anything is sent or
read on the socket, whether a failed or hostile peer can influence plugin state, and what the probe
discloses about the local machine. Do not start the service to test this; read the path. **Status
and log surfaces:** what `ui/BbjServerLogToolWindowFactory.java` and the two status-bar widgets
display, and whether anything from the server's stderr/stdout or from a crash report can carry
sensitive content into a persistent UI or log sink. **Trust in server-reported state:**
`lsp/BbjLanguageClient.java` and `ui/BbjServerService.java` consume LSP4IJ `ServerStatus` and
server-sent messages — state what the plugin does on the strength of them.

*D2 Correctness & error handling* (tier `repro`, satisfied by trace per D-07). Assess against
null/undefined safety, unhandled rejections, swallowed exceptions, async races, off-by-one, wrong
edge-case behaviour and resource leaks. The concrete checks: `ui/BbjServerService.java`'s
start/stop/restart lifecycle against LSP4IJ's `LanguageServerManager` — what a restart during an
in-flight request does, whether two concurrent restarts can interleave, and what state the status
bar shows in between; whether `ui/BbjJavaInteropService.java`'s `Alarm`-driven polling is cancelled
on dispose (it implements `Disposable`) and whether a check in flight at dispose time can touch a
disposed project; whether the socket is closed on every path including timeout and refusal; whether
the status listeners published on the `Topic` are removed, and whether a listener can be invoked off
the EDT and then touch UI; what `ui/BbjServerCrashNotificationProvider.java` does on repeated
crashes and whether it can loop; whether `ui/BbjRestartServerAction.java` is enabled when no server
exists; and what each widget factory does when the project is closing. Each claim needs a
line-by-line trace naming the concrete input or state and the exact `file:line` where behaviour
diverges.

*D3 Performance & resource use* (tier `repro`, satisfied by trace per D-07). Assess against hot-path
cost, quadratic walks, missing caches or debounces, redundant work and unbounded memory growth. The
concrete checks: the java-interop poll interval and what each poll costs — a socket connect attempt
per tick, with settings re-read every time (the code notes the values are deliberately not cached so
the user may change them, so state the trade-off rather than assuming it is a defect); whether the
poll continues while no BBj file is open or while the project is in the background; whether the
status-bar widgets repaint or recompute per status event or per editor event; whether the server log
tool window accumulates unbounded output; and whether `lsp/BbjCompletionFeature.java` does per-item
work that scales with completion-list size.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). **The direction is reversed relative to Phase 62: this
phase owns the IntelliJ rows and reads `bbj-vscode/` as reference material only (D-05).** The
concrete checks: compare how each IDE starts the shared language server — what runtime it uses, how
it locates `out/language/main.cjs`, what it does when the runtime is missing — and name every
behaviour present on one side and absent or different on the other; compare server-restart handling,
including the `bbj.refreshJavaClasses` mechanism (`ui/BbjServerService.java`'s `restart()`) against
the VS Code side's targeted LSP request — **cross-reference `RU-63-01`'s already-recorded
disposition of Phase 62's referral #3 by ID rather than allocating a second finding for the same
divergence (D-06)**, adding only what the mechanism side contributes; compare what server status,
crash and log surfaces each IDE offers the user; and compare java-interop connection handling
between the two. Record a divergence whose defect is on the **IntelliJ** side as a `P63-D7-{seq}`
finding whose `location:` is the IntelliJ file that is missing the behaviour or differs, citing the
`bbj-vscode/` `file:line` in `evidence:` as the comparison point only. Where the defect is plainly
on the **VS Code** side, record nothing in `### Findings` and instead append a bullet to
`### Cross-phase observations (VS Code side)` in the close-out stating the surface, the evidence,
and which Phase 62 unit would have owned it (D-05).

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order. `unit:` is `RU-63-05`. `evidence:` opens with the trace basis and states in one clause why no
runnable reproduction accompanies it (D-07). `classification:` records pass/FAIL for all six
INVENTORY 3c tests inline and **states which reading of test (4) was applied** (D-09). `dedup:` is
never blank, is checked against the frozen 15 with **#410** and **#231** checked explicitly by
number, and **names DEBT-05 explicitly wherever the finding touches the LSP4IJ experimental-API or
`BbjCompletionFeature` coupling surface**, so Phase 66 re-triages rather than re-derives. A
`critical` or `high` D1 finding follows D-13's two-tier disclosure rule strictly. Any claim that
does not clear its tier goes under `### Not-reproducible dispositions` with the tier it failed and
why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-05 /,/^## RU-63-02 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D7 .* — (pass|fail) — ` each print `1`
    - The unit's `n/a` cell is intact and unreworded: within the extracted section, `grep -cE '^- D6 .* — n/a — '` prints `1`
    - The launch and probe paths were actually traced: the extracted section contains `OSProcessStreamConnectionProvider`, `BbjJavaInteropService.java` and `Socket`
    - The D7 cell cross-references rather than re-reports referral #3: the extracted section contains `RU-63-01` and contains `restart`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `25` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `10`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding IDs unique, well-formed, never from the reserved namespace: `grep -oE '^id:[[:space:]]+P63-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field and no blank dedup: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`, and `grep -cE '^dedup:[[:space:]]*$'` prints `0`
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-05 /,/^## RU-63-02 /p" "$f" > /tmp/p63ru05.txt; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru05.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p63ru05.txt)" = 1; for s in "OSProcessStreamConnectionProvider" "BbjJavaInteropService.java" "Socket" "RU-63-01" "restart"; do grep -qF "$s" /tmp/p63ru05.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 25; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 10; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_04_REPRO_OK'</automated>
  </verify>
  <done>`RU-63-05`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — the language-server launch configuration and the java-interop TCP probe traced
  by reading rather than running for D1; lifecycle, disposal, threading and crash-loop paths for D2; poll
  cost, repaint frequency and log growth for D3; and the two IDEs' server-start, restart, status and
  interop handling compared for D7, with referral #3's mechanism side cross-referencing `RU-63-01`'s
  existing disposition rather than re-reporting it.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-63-05 at evidence tier `trace` — D4, D5, D8 — including the bounded DEBT-05 record</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`## RU-63-05`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed — and the
  phase-wide verdict count is exactly 25. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` §`## RU-63-05` — the section this task completes — and
      §`## RU-63-03`'s D5 cell plus the `P63-D5-001` record, which this unit cross-references by ID
      rather than restating (D-08)
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-04, RVW-06, RVW-07, DEBT-05
    - `.planning/PROJECT.md` §"Known tech debt" — the LSP4IJ experimental-API and
      `BbjCompletionFeature` entries this cell records with evidence
    - `.planning/ROADMAP.md` §"Phase 63" success criterion 1 — it names "LSP wiring" and "status bar
      widgets" but not `BbjServerCrashNotificationProvider.java` or
      `BbjServerLogToolWindowFactory.java`, which are swept in full anyway (D-16)
    - **Review targets:** the same 13 files as Task 1
    - **Count basis, measured rather than assumed:**
      `grep -rn "ApiStatus.Experimental\|@Experimental" bbj-intellij/src/main/java` and
      `grep -rn "com.redhat.devtools.lsp4ij" bbj-intellij/src/main/java` — run both and record their
      literal outputs
    - `CLAUDE.md` (repo root) §"IDE Integration" — its claims that both IDEs consume
      `out/language/main.cjs` and that the IntelliJ plugin bundles the compiled LS and connects via
      LSP4IJ are checkable against this unit's code; a candidate D8 target, not an asserted finding
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Complete `## RU-63-05` by recording its D4, D5 and D8 cell lines, then confirm the unit against the
four-part stopping rule. Enumerate the dimensions explicitly (D-15): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. **This is where DEBT-05's
evidence is recorded, bounded as follows.** State the count basis as *measured*, with both commands
and their literal outputs: `grep -rn "ApiStatus.Experimental\|@Experimental" bbj-intellij/src/main/java`
returns **zero** annotations in this repository's own source, and
`grep -rn "com.redhat.devtools.lsp4ij" bbj-intellij/src/main/java` returns **20** references across
**10** files. State plainly that PROJECT.md's "19 experimental API usages" figure counts APIs marked
experimental *inside LSP4IJ*, which is not greppable from this tree — so the number is neither
confirmed nor refuted here, and Phase 66 owns its resolution (DEBT-05). **Do not assert a count that
was not measured.** Then record the concrete, checkable half: enumerate which LSP4IJ types this unit
depends on and where — `LSPCompletionFeature` (`lsp/BbjCompletionFeature.java`),
`LanguageServerFactory` / `LSPClientFeatures` / `LSPDocumentLinkFeature` / `StreamConnectionProvider`
(`lsp/BbjLanguageServerFactory.java`), `LanguageClientImpl` (`lsp/BbjLanguageClient.java`),
`OSProcessStreamConnectionProvider` (`lsp/BbjLanguageServer.java`), `LanguageServerManager` and
`ServerStatus` (`ui/BbjServerService.java`, `ui/BbjJavaInteropService.java`,
`ui/BbjStatusBarWidget.java`) — and assess the coupling shape: which of these are extension points
LSP4IJ intends to be subclassed versus internals the plugin reaches into, and what breaks at which
call site if a signature changes. Any finding here names **DEBT-05** in its `dedup:` field. Beyond
DEBT-05, run the ordinary D4 checks: whether `ui/BbjStatusBarWidget.java` (167) and
`ui/BbjJavaInteropStatusBarWidget.java` (151) duplicate a widget shape that belongs in a shared base
— compare them mechanically with `diff`, and likewise the two 43-line widget factories; whether
`ui/BbjServerService.java` (244) has crossed into god-service territory; and whether status
representation is expressed once or re-derived in each consumer.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **Cross-reference `P63-D5-001` by
ID; do not restate the systemic fact (D-08).** Record `fail` with a line naming `P63-D5-001` as the
systemic absence of any test source set in `bbj-intellij`, then state **this unit's own specific
consequence**: which lifecycle and status behaviours are consequently untested — server start/stop/
restart sequencing, status propagation to both widgets, the java-interop probe's state machine and
its dispose path, crash-notification behaviour on repeated crashes, and the completion-feature
override — and what a first test would have to cover for this unit specifically. Note in one clause
that this is also the unit whose LSP4IJ coupling would most benefit from a contract test, which is
the concrete cost DEBT-05 carries.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong Javadoc and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: `ui/BbjJavaInteropService.java`'s
class comment (`:19-28`) explaining *why* the plugin probes the TCP port independently — that the
language server connects to java-interop but does not expose connection status over LSP, so an
independent probe is the only way to show status without LS changes — checked against what the code
actually does and against whether that constraint still holds; `lsp/BbjLanguageServerFactory.java:19`'s
comment naming the `com.redhat.devtools.lsp4ij.server` extension point, checked against
`plugin.xml`'s actual registration (read as context only — no finding is located in `plugin.xml`,
D-16); every class-level and method-level Javadoc across the other eleven files; and `CLAUDE.md`
§"IDE Integration"'s claims that both IDEs consume `out/language/main.cjs` and that the plugin
bundles the compiled LS and the TextMate grammar and connects via LSP4IJ, checked against this
unit's launch configuration. Where `CLAUDE.md` is simply silent about a surface, note the silence as
an observation rather than promoting it to a finding. Per D-09, a D8 fix that changes only comment
or Javadoc text changes no runtime behaviour, satisfies test (4) vacuously, and may be `easy` if the
other five tests pass; the record states which reading was applied.

**Then close the unit against the four-part stopping rule** and make the closure visible in the
section text: all 7 live cells carry a verdict plus a written check line; all thirteen file names
appear at least once inside `## RU-63-05`; every candidate claim raised during either task is either
a finding record or a written `### Not-reproducible dispositions` entry with its reason; and **state
explicitly that this unit owns zero inherited Phase 62 referrals**, so the absence of an
`### Inherited referral triage` sub-block is a recorded fact rather than an omission. If the sweep
produced no not-reproducible candidates and no new outbound cross-unit referrals, state those
sub-blocks as explicitly empty rather than omitting them (D-14's register). Finally, state in the
section text that all thirteen files were swept even though ROADMAP criterion 1 names only "LSP
wiring" and "status bar widgets" from this unit — the grid is the contract and the criteria are a
subset of it (D-16) — naming `BbjServerCrashNotificationProvider.java` and
`BbjServerLogToolWindowFactory.java` as the extra coverage, so it reads as deliberate.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-05 /,/^## RU-63-02 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The DEBT-05 record is present, bounded and evidenced by measurement: the extracted section contains `DEBT-05`, `LSPCompletionFeature`, `ApiStatus.Experimental` and `com.redhat.devtools.lsp4ij`
    - The D5 cell cross-references rather than restates: the extracted section contains `P63-D5-001`
    - Coverage is file-granular: each of the thirteen basenames `BbjServerService.java`, `BbjJavaInteropService.java`, `BbjStatusBarWidget.java`, `BbjJavaInteropStatusBarWidget.java`, `BbjLanguageServer.java`, `BbjCompletionFeature.java`, `BbjLanguageServerFactory.java`, `BbjServerCrashNotificationProvider.java`, `BbjLanguageClient.java`, `BbjServerLogToolWindowFactory.java`, `BbjJavaInteropStatusBarWidgetFactory.java`, `BbjRestartServerAction.java`, `BbjStatusBarWidgetFactory.java` appears at least once in the extracted section
    - All four per-unit sub-blocks exist and the zero-referral fact is stated: `### Cells`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear, and the extracted section contains the string `zero inherited`
    - The scope-fidelity statement is present: the extracted section contains the string `criterion 1`
    - Phase-wide counts prove no other unit section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `28` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `7`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`, and the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-05 /,/^## RU-63-02 /p" "$f" > /tmp/p63ru05.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru05.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p63ru05.txt)" = 7; for s in "DEBT-05" "LSPCompletionFeature" "ApiStatus.Experimental" "com.redhat.devtools.lsp4ij" "P63-D5-001" "BbjServerService.java" "BbjJavaInteropService.java" "BbjStatusBarWidget.java" "BbjJavaInteropStatusBarWidget.java" "BbjLanguageServer.java" "BbjCompletionFeature.java" "BbjLanguageServerFactory.java" "BbjServerCrashNotificationProvider.java" "BbjLanguageClient.java" "BbjServerLogToolWindowFactory.java" "BbjJavaInteropStatusBarWidgetFactory.java" "BbjRestartServerAction.java" "BbjStatusBarWidgetFactory.java" "### Cells" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals" "zero inherited" "criterion 1"; do grep -qF "$s" /tmp/p63ru05.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 28; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 7; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_04_RU05_COMPLETE'</automated>
  </verify>
  <done>`RU-63-05` is complete under the four-part stopping rule: all 7 live cells carry a verdict plus a
  written check line, all 13 files are named inside the section, DEBT-05 is recorded with a measured count
  basis (0 own-source experimental annotations, 20 LSP4IJ references across 10 files) and an explicit
  statement that PROJECT.md's 19-usage figure is not greppable from this tree so Phase 66 owns it, the
  `BbjCompletionFeature`/`LSPCompletionFeature` coupling is recorded against a concrete file, the D5 cell
  cross-references `P63-D5-001` with this unit's own consequence, the zero-inherited-referral fact is
  stated, and the D-16 scope-fidelity note explains why the crash-notification and log tool-window files
  were swept though ROADMAP criterion 1 omits them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's and DEBT-05's re-triage is
Phase 66's.

| Boundary | Description |
|----------|-------------|
| the downloaded Node runtime and `out/language/main.cjs` → the spawned language-server process (*subject-matter*) | `lsp/BbjLanguageServer.java`'s `OSProcessStreamConnectionProvider` decides what executable runs with what arguments — the consumer of the artifact `RU-63-03`'s downloader produces. |
| a settings-supplied host and port → a TCP `Socket` connect (*subject-matter*) | `ui/BbjJavaInteropService.java` probes java-interop on values re-read from settings at each check. |
| language-server stdout/stderr and crash reports → the log tool window and status widgets (*subject-matter*) | `ui/BbjServerLogToolWindowFactory.java` and the two widgets surface server-originated content in the IDE. |
| LSP4IJ's own API surface → this plugin's subclasses and call sites (*subject-matter*) | DEBT-05: `LSPCompletionFeature`, `LanguageServerFactory`, `LanguageClientImpl`, `LanguageServerManager` and `ServerStatus` are consumed across 10 files. |
| `63-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record is world-readable on landing. |
| a written check line → a reader who cannot re-run the check | Phase 68's DOC-03 and Phase 67's apply path consume `pass` verdicts as evidence. |
| a stated count → a downstream phase that trusts it | DEBT-05's "19 usages" figure is not measurable from this tree; asserting it would manufacture evidence. |
| `bbj-vscode/` and `plugin.xml` → this phase's finding namespace | Read as reference/context only; a `location:` inside either would double-count or invent a cell. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past `MAJOR-REFACTORS.md` review. |
| shared `63-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-P04-S1 | Elevation of Privilege | the language-server launch configuration in `lsp/BbjLanguageServer.java` | high | transfer | Task 1's D1 check traces what executable, arguments and environment are used and where the runtime and script paths come from; recorded as `P63-D1-*` where evidence clears tier `repro` by trace. Cross-cutting SEC-05 synthesis transfers to Phase 65; remediation to Phase 67. |
| T-63-P04-S2 | Information Disclosure | the java-interop TCP probe in `ui/BbjJavaInteropService.java` | medium | transfer | Task 1's D1 check asks what host/port values are accepted, what is sent or read, and what a hostile or absent peer can influence — assessed by reading the path, never by starting the service. |
| T-63-P04-S3 | Information Disclosure | server stdout/stderr and crash content surfaced in the log tool window and widgets | medium | transfer | Task 1's D1 check asks whether server-originated content can carry sensitive material into a persistent UI or log sink. |
| T-63-P04-S4 | Denial of Service | repeated crash notifications and uncancelled `Alarm` polling at dispose | medium | transfer | Task 1's D2 check covers crash-loop behaviour, disposal of the polling alarm, socket closure on every path, and listener removal. |
| T-63-P04-01 | Repudiation | asserting DEBT-05's 19-usage figure as if it had been measured here | high | mitigate | Explicit prohibition plus an acceptance criterion requiring both grep commands and their literal outputs in the cell, and an explicit statement that the LSP4IJ-side annotation count is not greppable from this tree. |
| T-63-P04-02 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | D-14 plus an acceptance criterion requiring every `pass` cell line to be at least 120 bytes of written checks; RVW-06 sends unclearing claims to `### Not-reproducible dispositions`. |
| T-63-P04-03 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-13's two-tier rule: surface, problem class and impact; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-63-P04-04 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; D-09 requires the test-(4) reading stated inline. |
| T-63-P04-05 | Repudiation | re-reporting referral #3's divergence as a second finding | medium | mitigate | D-06 prohibition plus an acceptance criterion asserting the section names `RU-63-01` and `restart`, so the cross-reference is present rather than a duplicate record. |
| T-63-P04-06 | Tampering | a finding relocated into `bbj-vscode/` or `plugin.xml` to dodge a boundary | medium | mitigate | D-05/D-16 prohibitions plus anchored acceptance greps asserting zero `^location:` lines resolve into either; `plugin.xml` is read as context for the extension-point registration only. |
| T-63-P04-07 | Tampering | editing `INVENTORY.md` or `62-COVERAGE.md` so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for both. |
| T-63-P04-08 | Repudiation | a claim asserting a runnable Gradle reproduction or a live java-interop probe that could not have been produced | medium | mitigate | D-07 prohibition plus an explicit prohibition on starting or connecting to the java-interop service; every `repro`-tier record clears its tier by trace and says why it carries no runnable reproduction. |
| T-63-P04-09 | Tampering | five plans writing one `63-COVERAGE.md` | medium | mitigate | Wave 4 behind `depends_on: [63-03]` makes ordering structural (D-04); both tasks assert exact global counts (25/10, then 28/7) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-63-P04-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed`, `diff` and `awk` only. |
</threat_model>

<verification>
- `## RU-63-05`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's `R-D6-CENTRAL` text verbatim.
- All 13 unit files are named inside the section.
- The D1 cell names `OSProcessStreamConnectionProvider`, `BbjJavaInteropService.java` and `Socket`.
- The D7 cell names `RU-63-01` and `restart`, cross-referencing referral #3's disposition rather
  than re-reporting it; no `location:` resolves inside `bbj-vscode/` or `plugin.xml`.
- The D4 cell records DEBT-05 with both grep commands and their outputs, names
  `LSPCompletionFeature`, and states that the LSP4IJ-side annotation count is not greppable here.
- The D5 cell cross-references `P63-D5-001` and adds this unit's own consequence.
- The zero-inherited-referral fact and the D-16 scope-fidelity statement are both present.
- Phase-wide verdict count is 28 and placeholder count is 7; 28 + 7 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed non-`P00-` ID and a non-blank `dedup:` that
  explicitly covered #410 and #231.
- `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- RVW-04 advances: 13 more of the 61 files (43 cumulative) have a recorded pass/fail against every
  dimension that applies to them.
- Phase 66 inherits a DEBT-05 record with a measured count basis and a concrete per-call-site
  coupling map, so it re-triages rather than re-derives — and inherits an honest statement of what
  could not be measured from this tree rather than an invented number.
- Phase 65 inherits IntelliJ-side D1 records on the language-server launch path and the java-interop
  probe.
- ROADMAP Phase 63 criteria 1, 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/63-intellij-plugin-review/63-04-SUMMARY.md` when done.
</output>
