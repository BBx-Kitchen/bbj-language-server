---
phase: 63-intellij-plugin-review
plan: 03
type: execute
wave: 3
depends_on: [63-02]
files_modified:
  - .planning/reviews/63-COVERAGE.md
autonomous: true
requirements: [RVW-04]

estimate:
  tokens: 135000
  raw_tokens: 135000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-63-04` (composer dialogs & bridge, 13 files / 2,067 LOC — the largest unit by LOC in the phase, INVENTORY risk rank 3) has all 7 live dimensions recorded in `63-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-14, D-15)"
    - "All 13 files under `com/basis/bbj/intellij/composer/` — the three `*ComposerDialog.java`, the three `*SchematicPanel.java`, the three `Configure*Intention.java`, `ComposerModels.java`, `ComposerLauncher.java`, `BbjComposerServer.java` and `BbjComposerService.java` — are named at least once inside the `## RU-63-04` section, so coverage is file-granular (ROADMAP Phase 63 criterion 1)"
    - "The D7 parity question is assessed as equivalence of the generated BBj code, not of the UI toolkit (IntelliJ native Swing versus VS Code webview) — INVENTORY's own framing for this unit — with the shared language-server preview/decode handlers established as the single source of truth where they are shared, and every divergence recorded against the IntelliJ file that is missing or differs (D-05)"
    - "Inherited referrals #4 (`RU-62-04`) and #5 (`RU-62-03`) describe the SAME SETOPTS absence from two Phase 62 vantage points and are triaged ONCE, as a single disposition naming both source referrals — recording two findings for one absence would double-count (D-06)"
    - "The single SETOPTS disposition ends in exactly one of promoted / dismissed-with-evidence / not-reproducible, and where dismissed it cites the document or code that establishes the absence as a deliberate scope decision rather than asserting it (D-06)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier via INVENTORY 3b's line-by-line-trace branch, and each record states in one clause why it carries no runnable reproduction (D-07); a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #385 (launching the Graffiti Composer) and #475 (SETOPTS assistance — decode hovers plus a tri-state composer with IOR/AND-aware codegen) checked explicitly as this unit's plausible neighbours (RVW-07)"
    - "The D5 cell records `fail` by cross-referencing `P63-D5-001` by ID rather than restating the systemic zero-test-source-set fact, plus this unit's own specific consequence — which dialog, schematic-panel and bridge behaviours are consequently untested and what a first test would have to cover (D-08)"
    - "No `P63-*` finding carries a `location:` inside `bbj-vscode/`: the VS Code composer webviews are cited in `evidence:` as the comparison point only, and a defect plainly on the VS Code side is appended to `### Cross-phase observations (VS Code side)` in the close-out rather than recorded as a finding, because Phase 62 is closed and `62-COVERAGE.md` is not reopened (D-05)"
    - "The unit's 1 `n/a` cell carries INVENTORY's `R-D6-CENTRAL` text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-17)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "Two findings sharing one root cause are recorded ONCE with a cross-reference by finding ID, never restated per layer (D-08 for the D5 systemic absence; D-06 for the two SETOPTS referrals that describe the same absence from two Phase 62 vantage points). Boundary case: the 24 files directly under `com/basis/bbj/intellij/` split 6 (`RU-63-03`) / 18 (`RU-63-02`) — each of the 61 files appears under exactly one unit section, never two"
    - "A no-finding `applies` cell records `pass` PLUS a written line naming the concrete checks applied, phrased against that dimension's REQUIREMENTS.md wording (D-14) — never a bare `pass`, never blank, never the placeholder `pending`. A unit whose sweep produced no not-reproducible candidates and no cross-unit referrals states those sub-blocks as explicitly empty rather than omitting them"
    - "Finding IDs `P63-D{dimension}-{seq}` allocate `{seq}` zero-padded to three digits, monotonically in discovery order within each `(63, dimension)` pair — INVENTORY 3a's RVW-06 ordering backstop, so two findings can never tie. Plan execution order is fixed by D-02 (`RU-63-03` -> `RU-63-01` -> `RU-63-04` -> `RU-63-05` -> `RU-63-02`), so the append order into the shared coverage file is deterministic across runs"
    - "All five plans append to the single `.planning/reviews/63-COVERAGE.md`; concurrency is excluded structurally by the dependency graph, not by an assumption about the executor — one wave per plan (waves 1-5), each plan's `depends_on` naming its predecessor in D-02's order (D-04). Phase 63 and Phase 64 may run concurrently but write disjoint `{NN}-COVERAGE.md` files, so no cross-phase collision exists. If a plan is interrupted mid-append, the partially-filled unit section still carries the literal placeholder `pending` on every unrecorded live-dimension cell, which is mechanically checkable at every wave"

  artifacts:
    - ".planning/reviews/63-COVERAGE.md §`## RU-63-04 — Composer dialogs & bridge` — 7 live cells recorded, 1 `n/a` cell carried forward verbatim, plus `### Inherited referral triage` (referrals #4 and #5 merged into one disposition), `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`"

  key_links:
    - "The single SETOPTS disposition -> `## Phase 63 Close-Out` §D's inbound referral accounting, where 2 of the 7 inherited referrals resolve to one disposition (D-06, D-17.3)"
    - "`RU-63-04`'s D7 records on generated-BBj-code equivalence -> Phase 67's apply path via `classification:`, and Phase 69's issue drafting alongside #475 and #385"
    - "`RU-63-04`'s D1 records on the composer bridge and document-edit path -> Phase 65's SEC-05 process/bridge picture across both IDEs (D-12)"
    - "Any VS Code-side observation found during the D7 read -> `### Cross-phase observations (VS Code side)` in the close-out, which Phase 68 reads for DOC-03/DOC-04 (D-05)"
    - "`RU-63-04` cells -> the phase-wide cell, file and referral gates re-run by plan `63-05`"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance candidates (OWASP
    # taxonomies, injection, input-validation checklists, GDPR) deliberately DROPPED as prohibitions —
    # in this phase they are the review *subject matter*, not constraints on the plan.
    - "MUST NOT record two findings for the one SETOPTS absence — referrals #4 and #5 are two Phase 62 vantage points on the same gap and are triaged once, in a single disposition naming both sources; merging them is the point of the ledger (D-06)"
    - "MUST NOT record `pass` on any cell whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 68's DOC-03 and Phase 67's apply path both consume as if it were evidence (D-14)"
    - "MUST NOT relocate a Phase 63 finding into `bbj-vscode/` to sidestep the IntelliJ-side verdict — that tree is D7 comparison material only, Phase 62 owns its rows and is closed, and a Phase 63 cell claiming them would double-count against INVENTORY's 232-cell total (D-05)"
    - "MUST NOT reopen or edit `.planning/reviews/62-COVERAGE.md`, and MUST NOT edit `.planning/reviews/INVENTORY.md` to make a gate agree — Phase 62 is closed and verified 13/13 and INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09, D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and the gap would be unfixed at commit time; name the surface, the problem class and the impact and stop there (D-13)"
    - "MUST NOT modify any file under `bbj-intellij/`, `bbj-vscode/` or `java-interop/` — Phase 63 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT schedule, run or claim a Gradle build, compile, test run or build-driven static analysis — the local Temurin 25.0.3 toolchain fails `build.gradle.kts:12-13`'s `JavaVersion.VERSION_17` target before task listing (D-07)"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and predate `bbj-intellij/` entirely (D-19)"
    - "MUST NOT write into any `## RU-63-0N` section other than `RU-63-04`, MUST NOT reword or re-derive the carried-forward `R-D6-CENTRAL` text, and MUST NOT add a grid row, a cell line or a unit section beyond the 40 cells INVENTORY's Phase 63 slice defines (D-03, D-17)"
    - "MUST NOT restate the systemic zero-test-source-set fact in this unit's D5 cell — cross-reference `P63-D5-001` by ID and add only this unit's own consequence (D-08)"
    - "MUST NOT re-record the generator-layer or logic/UI-layer composer duplication Phase 62 already owns on the VS Code side — this unit records the IntelliJ-side shape and cross-references rather than restating"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason, visible rather than silently dropped (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, MUST NOT allocate an ID in INVENTORY's reserved `P00-*` template namespace, and MUST NOT record a cell verdict for a file the sweep did not open"
    - "MUST NOT write a credential, a session value or an absolute developer-machine path into any record — every citation is a repository-relative `path:line` anchor"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-63-04` — Composer dialogs & bridge**
(`composer/AddChildWindowComposerDialog.java` 315, `AddWindowComposerDialog.java` 306,
`MsgboxComposerDialog.java` 273, `ComposerModels.java` 245, `ComposerLauncher.java` 224,
`MsgboxSchematicPanel.java` 180, `ChildWindowSchematicPanel.java` 159,
`WindowSchematicPanel.java` 132, `BbjComposerServer.java` 54, `ConfigureAddChildWindowIntention.java`
50, `ConfigureAddWindowIntention.java` 50, `ConfigureMsgboxIntention.java` 49,
`BbjComposerService.java` 30 — 13 files, 2,067 LOC, the largest unit by LOC in Phase 63, INVENTORY
risk rank 3 of 5) across its 7 live dimensions, and record the result in the `## RU-63-04` section
of `.planning/reviews/63-COVERAGE.md`.

Purpose: these dialogs are IntelliJ's native-Swing counterpart to VS Code's webview composers, and
INVENTORY frames the genuine D7 comparison precisely — **equivalence of the generated BBj code, not
of the UI toolkit**. The bridge files (`BbjComposerServer.java`, `BbjComposerService.java`,
`ComposerLauncher.java`) are where the plugin reaches the shared language-server handlers and writes
the result into the user's document. This unit also owns the phase's one merged referral: **#4 and
#5 are the same SETOPTS absence seen from two Phase 62 vantage points and are triaged once** (D-06).

Output: 7 recorded cells with written check lines, one merged SETOPTS disposition naming both source
referrals, and any finding records, not-reproducible dispositions and cross-unit referrals. No
source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/63-intellij-plugin-review/63-CONTEXT.md
@.planning/phases/63-intellij-plugin-review/63-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 63 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/63-COVERAGE.md` — section `## RU-63-04 — Composer dialogs & bridge` only, plus
  the one narrowly scoped exception the write contract allows: appending a bullet to
  `### Cross-phase observations (VS Code side)` in the close-out if a VS Code-side observation
  surfaces during the D7 read (D-05). Everything else in the file is inherited from plans `63-01`
  and `63-02` and is not touched.

**Section anchors this plan fills.** `## RU-63-04 — Composer dialogs & bridge` → its `### Cells`,
`### Inherited referral triage`, `### Findings`, `### Not-reproducible dispositions`,
`### Cross-unit referrals`. Downstream phases consume these anchors by name: Phase 65 (D1 records),
Phase 66 (any `dedup:` naming a `DEBT-*` requirement), Phase 67 (`classification:`), Phase 68
(DOC-03 concatenation), Phase 69 (issue drafting, gated on ISSUE-01).

**Finding-ID namespace.** `P63-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(63, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `63-01` and
`63-02` — it does not restart per plan. `P00-*` is INVENTORY's reserved template namespace and is
never allocated.

**Plan -> review-unit map.** This plan owns `RU-63-04`. The full 5-plan map, the wave chain
(waves 1-5 in D-02's risk-rank order) and the section shape live in `63-01-PLAN.md`
§"Artifacts this phase produces".

**Inherited work owned here (D-06, 2 of 7 — merged into 1 disposition).**

| # | From unit | Subject |
|---|---|---|
| 4 | `RU-62-04` | SETOPTS has no IntelliJ composer dialog at all |
| 5 | `RU-62-03` | Independent logic/UI-layer confirmation of the same SETOPTS absence |

These are **one absence seen twice**. They are triaged in a single disposition naming both source
referrals. Two records for one gap would double-count against the phase's finding accounting.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-04 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-04 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-04 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-04 | concurrency | all 5 plans, incl. this one | explicit |
| 5-8 | SEC-03 | adjacency / empty / ordering / concurrency | `63-01`, which owns `### SEC-03 Integrity Posture` | explicit |

Accounting: 8 surfaced = 8 authored = 0 dropped. No edge needed a backstop and none was dismissed.

<!-- planner-discipline-allow: ### Cells -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->
<!-- planner-discipline-allow: ### Inherited referral triage -->
<!-- planner-discipline-allow: ### Cross-phase observations (VS Code side) -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-63-04 at evidence tier `repro` — D1, D2, D3, D7 — and triage the merged SETOPTS referral</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`.planning/reviews/63-COVERAGE.md` exists, contains a `## RU-63-04 — ` heading whose 7
  live cell lines still end with the placeholder word `pending`, and plan `63-02` has committed — the
  phase-wide verdict count is exactly 14. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      `## Inherited referral ledger (D-06)`, and the completed `## RU-63-03` and `## RU-63-01`
      sections as the shape to copy, including the `### Inherited referral triage` rendering plan
      `63-02` established
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-04` → its `### Cross-unit referrals` sub-block
      (around lines 321-323) and `## RU-62-03` → its `### Cross-unit referrals` sub-block (around
      lines 1076-1079) — the **full text** of referrals #4 and #5, including the commands Phase 62
      already ran (`grep -c setopts` on the LS command registrations, the `ls` of the IntelliJ
      composer directory, the `ComposerLauncher.java` grep for `Setopts`/`SetOpts`) and their
      outputs. Do not re-derive them
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-04` §D7 cell — establishes that msgbox, addwindow
      and addchildwindow previews are LS-shared between both IDEs, with the IntelliJ call sites
      already located (`MsgboxComposerDialog.java:209`, `AddWindowComposerDialog.java:238`,
      `AddChildWindowComposerDialog.java:247`) and `ComposerModels.java:1-14`'s own doc comment
      naming the TypeScript side as the single source of truth. This unit's D7 cell starts here
    - `.planning/reviews/INVENTORY.md` §3a-3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot" (**#385** and **#475** are this unit's named neighbours),
      §"Phase 63 review units …" → `### RU-63-04` including its risk-rank note about the bridge,
      §"Applicability Grid" row `RU-63-04`, §"Exclusion reasons" → **R-D6-CENTRAL**
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-04, RVW-06, RVW-07
    - `.planning/phases/63-intellij-plugin-review/63-CONTEXT.md` — D-05, D-06, D-07, D-09, D-13,
      D-14, D-15 are the decisions this task turns on, plus Claude's Discretion on
      `ComposerModels.java` read depth, resolved in the action below
    - **Review targets, read in full:** all 13 files under
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/`. `ComposerModels.java` (245
      lines) is read in full — it is short — but **assessed as a field-for-field shape comparison
      against the language-server param/result types, not as a logic trace**, because its own doc
      comment declares the TypeScript side the single source of truth for the flag and hex
      arithmetic and the class contains no logic to trace
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-vscode/src/language/composer-commands.ts` (the LS-side handler registrations and their
      param/result types — the comparison basis for the `ComposerModels.java` shape check), and
      `bbj-vscode/src/setopts-composer-webview.ts` plus `bbj-vscode/src/language/setopts-catalog.ts`
      grepped for the SETOPTS surface referrals #4 and #5 describe, rather than read in full
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-63-04 — Composer dialogs & bridge`, replacing the
placeholder on each with `pass` or `fail` plus a written line naming the concrete checks applied.
Append any resulting finding records under that section's `### Findings`, and fill its
`### Inherited referral triage` sub-block. Touch no other unit section. Enumerate the dimensions
explicitly so none is absorbed by whichever is loudest (D-15): this task covers **D1 Security,
D2 Correctness & error handling, D3 Performance & resource use, and D7 Cross-IDE parity** — and no
others. D6 is `n/a — R-D6-CENTRAL` here and stays untouched.

*D1 Security* (tier `repro`, satisfied by trace per D-07). Assess against REQUIREMENTS.md's D1
wording — injection, untrusted input, secret exposure, integrity gaps, privilege/trust-boundary
errors. The concrete checks. **The bridge:** establish first what the bridge actually is — read
`BbjComposerServer.java` (an LSP4J `@JsonRequest` interface extending `LanguageServer`) and
`BbjComposerService.java` (which resolves it through LSP4IJ's `LanguageServerManager`) and state
whether any external process is spawned by this unit at all, or whether every call rides the
existing language-server connection; INVENTORY's risk-rank note describes these files as bridging
"to an external composer process", so state what the code actually does rather than inheriting the
description. **The document-edit path:** trace `ComposerLauncher.java`'s `WriteCommandAction` /
`Document` edit sites from the value that is written back to its origin, asking whether a
dialog-entered value, a decoded catalog value, or an LS-returned preview string is validated or
escaped before it is inserted into the user's BBj source, and what range it replaces. **Dialog input
handling:** for each of `MsgboxComposerDialog.java`, `AddWindowComposerDialog.java` and
`AddChildWindowComposerDialog.java`, what the dialog accepts from the user (title, geometry, flags,
event mask, assign-to target), whether any field is range- or type-checked before it reaches the
preview call or the emitted statement, and whether the emitted statement can be driven to something
syntactically invalid or semantically surprising. **The intention actions:** what
`ConfigureAddChildWindowIntention.java`, `ConfigureAddWindowIntention.java` and
`ConfigureMsgboxIntention.java` accept from the PSI element under the caret before opening a dialog,
and what happens on a malformed or partially typed statement. **DTO trust:** whether
`ComposerModels.java`'s public mutable fields are populated from an LS response without null or
shape checks, and what a missing or unexpected field produces downstream.

*D2 Correctness & error handling* (tier `repro`, satisfied by trace per D-07). Assess against
null/undefined safety, unhandled rejections, swallowed exceptions, async races, off-by-one, wrong
edge-case behaviour and resource leaks. The concrete checks: what `BbjComposerService.server(...)`
returns when no language server is running — its own Javadoc warns callers must handle the null
case, so trace every call site and confirm each does; how every `CompletableFuture` returned by
`BbjComposerServer`'s request methods handles completion exceptions, timeouts and cancellation, and
what the user sees when a preview or decode call fails; whether any LS response is consumed on a
background thread and then used to touch the UI or the document without the required threading
guarantee (`ApplicationManager` / `ModalityState` / `WriteCommandAction`); whether the dialog's
apply path can run against a document that changed after the dialog opened, and whether the captured
range is revalidated; whether the three schematic panels handle a zero, negative or oversized
geometry without an off-by-one or a divide-by-zero in their paint logic; and what each decode path
does with an unparseable existing statement. Each claim needs a line-by-line trace naming the
concrete input or state and the exact `file:line` where behaviour diverges.

*D3 Performance & resource use* (tier `repro`, satisfied by trace per D-07). Assess against
hot-path cost, quadratic walks, missing caches or debounces, redundant work and unbounded memory
growth. The concrete checks: whether a preview request is issued per keystroke or per field change
without debouncing, and what the round-trip cost is over LSP4IJ; whether the catalogs are fetched
once per dialog open or repeatedly; whether the three schematic panels repaint proportionally to the
change or rebuild their whole model; whether any listener, future or dialog disposable accumulates
across repeated open/close cycles; and whether `ComposerLauncher.java` re-resolves the server or the
editor on every call.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). **The parity question here is equivalence of the
generated BBj code, not of the UI toolkit** — INVENTORY's own framing for this unit. **The direction
is reversed relative to Phase 62: this phase owns the IntelliJ rows and reads `bbj-vscode/` as
reference material only (D-05).** The concrete checks: for msgbox, addwindow and addchildwindow,
confirm against the code whether both IDEs call the same language-server preview and decode handlers
— Phase 62 established that they do, with the IntelliJ call sites already located — and state
whether the generated statement can differ between the two sides given the same inputs; compare
`ComposerModels.java`'s DTO field set field-for-field against the LS-side param and result types in
`bbj-vscode/src/language/composer-commands.ts`, and record any field present on one side and absent
or differently named on the other, since a silent shape drift is a parity defect the compiler cannot
catch; and check whether every composer capability reachable from the VS Code side is reachable from
IntelliJ, and vice versa. Record a divergence whose defect is on the **IntelliJ** side as a
`P63-D7-{seq}` finding whose `location:` is the IntelliJ file that is missing the behaviour or
differs, citing the `bbj-vscode/` `file:line` in `evidence:` as the comparison point only. Where the
defect is plainly on the **VS Code** side, record nothing in `### Findings` — Phase 62 is closed and
is not reopened — and instead append a bullet to `### Cross-phase observations (VS Code side)` in
the close-out stating the surface, the evidence, and which Phase 62 unit would have owned it (D-05).

**Then fill `### Inherited referral triage`** with **one** bullet covering referrals #4 and #5
together, naming both source referrals (`RU-62-04`'s and `RU-62-03`'s) and stating that they are two
Phase 62 vantage points on the same absence — SETOPTS has no IntelliJ composer dialog, and the
logic/UI layer independently confirms it. Use the full referral text and the commands Phase 62
already ran rather than re-deriving them; then verify the absence against the current tree (no
`SetoptsComposerDialog.java` under `composer/`, no SETOPTS reference in `ComposerLauncher.java`'s
dispatch, no `bbj/composer/setopts/*` LS command) and dispose it as exactly one of: **promoted** to
a `P63-D7-{seq}` finding clearing its tier, whose `location:` is the IntelliJ file or directory
where the counterpart would live and whose `dedup:` names **#475** explicitly; **dismissed with
evidence**, citing the document or code that establishes the absence as a deliberate, documented
scope decision — an assertion that it "seems intentional" is not evidence; or **not-reproducible**,
written also under this unit's `### Not-reproducible dispositions` with its reason. **Do not render
two entries and do not allocate two findings** — merging them is the point of the ledger (D-06).

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's
13-field template verbatim, one field per line, inside a fenced block, in the template's exact field
order. `unit:` is `RU-63-04`. `evidence:` opens with the trace basis and states in one clause why no
runnable reproduction accompanies it (D-07). `classification:` records pass/FAIL for all six
INVENTORY 3c tests inline and **states which reading of test (4) was applied** (D-09): a fix that
changes runtime behaviour fails test (4) because no `src/test/` source set exists, so the finding is
`major` regardless of edit size; a fix that changes no runtime behaviour at all satisfies test (4)
vacuously and may be `easy` if the other five tests pass. Test (6) independently forces `major` for
any `critical`/`high` severity and for any D1 primary dimension. `dedup:` is never blank and is
checked against the frozen 15, with **#385** (launching the Graffiti Composer) and **#475** (SETOPTS
assistance — decode hovers plus a tri-state composer with IOR/AND-aware codegen) checked explicitly
by number. A `critical` or `high` D1 finding follows D-13's two-tier disclosure rule strictly. Any
claim that does not clear its tier goes under `### Not-reproducible dispositions` with the tier it
failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-04 /,/^## RU-63-05 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D1 .* — (pass|fail) — `, `^- D2 .* — (pass|fail) — `, `^- D3 .* — (pass|fail) — ` and `^- D7 .* — (pass|fail) — ` each print `1`
    - The unit's `n/a` cell is intact and unreworded: within the extracted section, `grep -cE '^- D6 .* — n/a — '` prints `1`
    - The bridge was actually characterised: the extracted section contains `BbjComposerServer.java`, `BbjComposerService.java` and `LanguageServerManager`
    - The D7 cell compares generated code rather than UI toolkits: the extracted section contains `composer-commands.ts` and `ComposerModels.java`
    - The merged SETOPTS referral is dispositioned exactly once: the extracted section contains `### Inherited referral triage`, contains both `RU-62-04` and `RU-62-03`, contains `SETOPTS`, and `grep -c 'SETOPTS'` inside the `### Inherited referral triage` sub-block shows a single bullet — verified by asserting the sub-block contains exactly one line beginning `- **Referral`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `18` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `17`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`
    - Every `fail` line names the findings it recorded: the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding IDs unique, well-formed, never from the reserved namespace: `grep -oE '^id:[[:space:]]+P63-D[1-8]-[0-9]{3}' | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field and no blank dedup: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` each equal the count of `^id:`, and `grep -cE '^dedup:[[:space:]]*$'` prints `0`
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-04 /,/^## RU-63-05 /p" "$f" > /tmp/p63ru04.txt; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru04.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p63ru04.txt)" = 1; for s in "BbjComposerServer.java" "BbjComposerService.java" "LanguageServerManager" "composer-commands.ts" "ComposerModels.java" "### Inherited referral triage" "RU-62-04" "RU-62-03" "SETOPTS"; do grep -qF "$s" /tmp/p63ru04.txt; done; sed -n "/^### Inherited referral triage/,/^### /p" /tmp/p63ru04.txt > /tmp/p63ru04ref.txt; test "$(grep -c "^- \*\*Referral" /tmp/p63ru04ref.txt)" = 1; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 18; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 17; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_03_REPRO_OK'</automated>
  </verify>
  <done>`RU-63-04`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — what the bridge actually is and what reaches the user's document for D1;
  null-server, future-failure and threading paths for D2; per-keystroke preview cost and disposable
  accumulation for D3; and generated-BBj-code equivalence plus the `ComposerModels.java`-versus-LS DTO
  shape comparison for D7 — and referrals #4 and #5 carry a single merged disposition naming both source
  referrals.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-63-04 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/63-COVERAGE.md</files>
  <precondition>`## RU-63-04`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed — and the
  phase-wide verdict count is exactly 18. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/63-COVERAGE.md` §`## RU-63-04` — the section this task completes — and
      §`## RU-63-03`'s D5 cell plus the `P63-D5-001` record, which this unit cross-references by ID
      rather than restating (D-08)
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c, §3d,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-04, RVW-06, RVW-07
    - `.planning/ROADMAP.md` §"Phase 63" success criterion 1 — it names "composer dialogs" but not the
      three `*SchematicPanel.java` files, the three `Configure*Intention.java` files,
      `ComposerModels.java`, `ComposerLauncher.java`, `BbjComposerServer.java` or
      `BbjComposerService.java`, all of which are swept in full anyway (D-16)
    - `.planning/reviews/62-COVERAGE.md` `## RU-62-04` §D4 cell — Phase 62's mechanical duplication
      finding across the VS Code generators, for cross-reference only; this unit records the
      IntelliJ-side shape and does not restate the VS Code-side record
    - **Review targets:** the same 13 files as Task 1
    - `CLAUDE.md` (repo root) and `.planning/PROJECT.md` §"Known tech debt" — checkable claims about
      the composer subsystem and the IDE split; candidate D8 targets, not asserted findings
    - **Do NOT read** any file under `.planning/codebase/` (D-19), and do NOT run any Gradle task (D-07)
  </read_first>
  <action>
Complete `## RU-63-04` by recording its D4, D5 and D8 cell lines, then confirm the unit against the
four-part stopping rule. Enumerate the dimensions explicitly (D-15): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others. Touch no other unit section.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. This is the phase's largest
unit by LOC and its duplication signal is strong, so use a **mechanical basis**: run a line-anchored
comparison (`diff` is non-build tooling and is available) between
`AddWindowComposerDialog.java` (306) and `AddChildWindowComposerDialog.java` (315), and between each
of those and `MsgboxComposerDialog.java` (273), and record the actual overlap rather than an
impression; do the same across the three `*SchematicPanel.java` files (132 / 159 / 180) and across
the three `Configure*Intention.java` files (49 / 50 / 50, a near-identical triple that is the
strongest signal in the unit). State whether a shared base class or helper is absent where one is
clearly warranted, and name the seam. Then assess `ComposerLauncher.java` (224) for god-function
shape given that it dispatches, calls the server, decodes, opens dialogs and applies edits; assess
whether `ComposerModels.java`'s public mutable fields are the right abstraction for a DTO carrier or
a smell; and check for duplicated catalog keys, flag constants or statement templates across the
thirteen files. Where a duplication is real, allocate **one** finding for it rather than one per
copy, and cross-reference Phase 62's VS Code-side composer duplication record by ID rather than
restating it — the two are separate layers on separate sides, and the record says so.

*D5 Test coverage gaps* (tier `inherited`, resolving to `trace`). **Cross-reference `P63-D5-001` by
ID; do not restate the systemic fact (D-08).** Record `fail` with a line naming `P63-D5-001` as the
systemic absence of any test source set in `bbj-intellij`, then state **this unit's own specific
consequence**: which composer behaviours are consequently untested — the decode path for an existing
statement, the emitted-statement construction for each of the three dialogs, the DTO round-trip
against the LS response shape, the schematic-panel geometry logic, and the document-edit apply path
— and what a first test would have to cover for this unit specifically. Note in one clause the
asymmetry worth recording: the VS Code side has composer tests (Phase 62 enumerated them) while the
IntelliJ side has none for the same generated-code contract, which is a parity-relevant consequence
of the same systemic absence. If a distinct, unit-specific D5 claim exists beyond the systemic
absence, allocate its own finding.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong Javadoc and
`CLAUDE.md`/docs-site claims contradicted by code. The concrete checks: `ComposerModels.java:1-14`'s
own doc comment declaring the TypeScript side the single source of truth for the flag and hex
arithmetic and describing these classes as only carrying JSON across LSP4IJ — check that claim
against the DTO field set and against whether any arithmetic actually happens on the Java side;
`BbjComposerService.java`'s Javadoc warning that the returned future may resolve to nothing when no
server is running and that callers must handle the null case — check every call site actually does,
and if not, the mismatch is a D2 finding while the comment itself may still be accurate; every
class-level comment on the three dialogs and the three intentions against what the class does; and
`CLAUDE.md`'s claims about the composer subsystem and the shared language server against this code.
Where `CLAUDE.md` is simply silent about a surface, note the silence as an observation rather than
promoting it to a finding. Per D-09, a D8 fix that changes only comment or Javadoc text changes no
runtime behaviour, satisfies test (4) vacuously, and may be `easy` if the other five tests pass; the
record states which reading was applied.

**Then close the unit against the four-part stopping rule** and make the closure visible in the
section text: all 7 live cells carry a verdict plus a written check line; all thirteen file names
appear at least once inside `## RU-63-04`; every candidate claim raised during either task is either
a finding record or a written `### Not-reproducible dispositions` entry with its reason; and the
merged SETOPTS referral carries its single disposition. If the sweep produced no not-reproducible
candidates and no new outbound cross-unit referrals, state those sub-blocks as explicitly empty
rather than omitting them (D-14's register). Finally, state in the section text that all thirteen
files were swept even though ROADMAP criterion 1 names only "composer dialogs" from this unit — the
grid is the contract and the criteria are a subset of it (D-16) — naming the three schematic panels,
the three intention actions and the four bridge/model files as the extra coverage, so it reads as
deliberate.
  </action>
  <acceptance_criteria>
    - All three `trace`-tier cells are recorded: with the unit section extracted by `sed -n '/^## RU-63-04 /,/^## RU-63-05 /p' .planning/reviews/63-COVERAGE.md`, the counts of `^- D4 .* — (pass|fail) — `, `^- D5 .* — (pass|fail) — ` and `^- D8 .* — (pass|fail) — ` each print `1`, and the total count of `^- D[1-8] .* — (pass|fail) — ` in the section prints `7`
    - The D5 cell cross-references rather than restates: the extracted section contains `P63-D5-001`
    - Coverage is file-granular: each of the thirteen basenames `AddChildWindowComposerDialog.java`, `AddWindowComposerDialog.java`, `MsgboxComposerDialog.java`, `ComposerModels.java`, `ComposerLauncher.java`, `MsgboxSchematicPanel.java`, `ChildWindowSchematicPanel.java`, `WindowSchematicPanel.java`, `BbjComposerServer.java`, `ConfigureAddChildWindowIntention.java`, `ConfigureAddWindowIntention.java`, `ConfigureMsgboxIntention.java`, `BbjComposerService.java` appears at least once in the extracted section
    - All five per-unit sub-blocks exist: `### Cells`, `### Inherited referral triage`, `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` each appear in the extracted section
    - The scope-fidelity statement is present: the extracted section contains the string `criterion 1`
    - Phase-wide counts prove no other unit section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/63-COVERAGE.md` prints `21` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/63-COVERAGE.md` prints `14`
    - The grid is unchanged in size and both `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/63-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/63-COVERAGE.md` prints `5`
    - Every `pass` line is a written line and every `fail` line names its findings: the count of `^- D[1-8] .* — pass — ` equals the count of `^- D[1-8] .* — pass — .{120,}`, and the count of `^- D[1-8] .* — fail — ` equals the count of `^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}`
    - Finding records remain complete and well-formed: IDs unique, no `P00-` ID, no blank `dedup:`, and every required field count equals the `^id:` count
    - No finding is anchored in the read-only VS Code tree or the context-only manifest: `grep -cE '^location:[[:space:]]+bbj-vscode/'` prints `0` and `grep -cE '^location:.*plugin\.xml'` prints `0`
    - No source file was modified and neither immutable record was touched: `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/63-COVERAGE.md; sed -n "/^## RU-63-04 /,/^## RU-63-05 /p" "$f" > /tmp/p63ru04.txt; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p63ru04.txt)" = 1; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p63ru04.txt)" = 7; for s in "P63-D5-001" "AddChildWindowComposerDialog.java" "AddWindowComposerDialog.java" "MsgboxComposerDialog.java" "ComposerModels.java" "ComposerLauncher.java" "MsgboxSchematicPanel.java" "ChildWindowSchematicPanel.java" "WindowSchematicPanel.java" "BbjComposerServer.java" "ConfigureAddChildWindowIntention.java" "ConfigureAddWindowIntention.java" "ConfigureMsgboxIntention.java" "BbjComposerService.java" "### Cells" "### Inherited referral triage" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals" "criterion 1"; do grep -qF "$s" /tmp/p63ru04.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 21; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 14; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — pass — " "$f")" = "$(grep -cE "^- D[1-8] .* — pass — .{120,}" "$f")"; test "$(grep -cE "^- D[1-8] .* — fail — " "$f")" = "$(grep -cE "^- D[1-8] .* — fail — .*P63-D[1-8]-[0-9]{3}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P63-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; n=$(grep -cE "^id:" "$f"); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f")" = "$n"; done; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-vscode/" "$f")" = 0; test "$(grep -cE "^location:.*plugin\.xml" "$f")" = 0; test -z "$(git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md)"; echo P63_03_RU04_COMPLETE'</automated>
  </verify>
  <done>`RU-63-04` is complete under the four-part stopping rule: all 7 live cells carry a verdict plus a
  written check line, all 13 files are named inside the section, the dialog / schematic-panel /
  intention-action duplication questions are settled on a mechanical `diff` basis with one finding per
  duplication rather than one per copy, the D5 cell cross-references `P63-D5-001` and adds this unit's own
  untested-behaviour consequence including the VS-Code-has-tests asymmetry, `ComposerModels.java`'s
  single-source-of-truth doc claim is checked against the DTOs, the merged SETOPTS referral carries its
  single disposition, and the D-16 scope-fidelity note explains why the panels, intentions and bridge
  files were swept though ROADMAP criterion 1 names only "composer dialogs".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| dialog-entered values and LS-returned preview strings → the user's BBj document (*subject-matter*) | `ComposerLauncher.java`'s `WriteCommandAction`/`Document` edit sites write generated statements into live source. |
| language-server responses → `ComposerModels.java` DTOs (*subject-matter*) | Public mutable fields populated from a JSON-RPC response cross a trust boundary into UI and codegen paths. |
| PSI element under the caret → the three `Configure*Intention` actions (*subject-matter*) | A malformed or partially typed statement is decoded before a dialog opens. |
| LSP4IJ connection → `BbjComposerServer` / `BbjComposerService` (*subject-matter*) | The bridge's actual shape — LSP4IJ request versus spawned process — is what this unit must establish rather than inherit from a description. |
| `63-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record is world-readable on landing. |
| a written check line → a reader who cannot re-run the check | Phase 68's DOC-03 and Phase 67's apply path consume `pass` verdicts as evidence. |
| `bbj-vscode/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 62's already-filled rows. |
| the merged SETOPTS referral → the phase's finding accounting | Two records for one absence would inflate the count and misrepresent the gap. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past `MAJOR-REFACTORS.md` review. |
| shared `63-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-P03-S1 | Tampering | generated statements written into the user's document via `ComposerLauncher.java`'s `WriteCommandAction` | high | transfer | Task 1's D1 check traces every written value back to its origin and asks what validation or escaping stands between a dialog field, a catalog value or an LS preview string and the inserted text; recorded as `P63-D1-*` where evidence clears tier `repro` by trace. Remediation transfers to Phase 67. |
| T-63-P03-S2 | Tampering | LS-response fields populating `ComposerModels.java`'s public mutable DTOs | medium | transfer | Task 1's D1 check asks whether responses are null- or shape-checked before use and what a missing or unexpected field produces downstream. |
| T-63-P03-S3 | Denial of Service | `BbjComposerService.server(...)` resolving to nothing, and unhandled `CompletableFuture` failures | medium | transfer | Task 1's D2 check traces every call site against the service's own null-case warning and covers completion exceptions, timeouts and cancellation. |
| T-63-P03-S4 | Tampering | a stale captured document range applied after the document changed under an open dialog | medium | transfer | Task 1's D2 check asks whether the apply path revalidates the captured range. |
| T-63-P03-01 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-13's two-tier rule: surface, problem class and impact; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-63-P03-02 | Repudiation | a `pass` verdict recorded for checks that were never run | high | mitigate | D-14 plus an acceptance criterion requiring every `pass` cell line to be at least 120 bytes of written checks; RVW-06 sends unclearing claims to `### Not-reproducible dispositions`. |
| T-63-P03-03 | Repudiation | the two SETOPTS referrals rendered as two dispositions or two findings | high | mitigate | D-06 prohibition plus an acceptance criterion asserting exactly one `- **Referral` bullet inside `### Inherited referral triage` while both `RU-62-04` and `RU-62-03` are named in it. |
| T-63-P03-04 | Repudiation | the SETOPTS absence dismissed without evidence | high | mitigate | D-06 requires a dismissal to cite the document or code establishing the deliberate scope decision; "seems intentional" is explicitly not evidence, and the alternative dispositions (promoted / not-reproducible) are both available. |
| T-63-P03-05 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or D1 primary dimension; D-09 requires the test-(4) reading stated inline. |
| T-63-P03-06 | Tampering | a finding relocated into `bbj-vscode/` to dodge the IntelliJ-side verdict | medium | mitigate | D-05 prohibition plus an anchored acceptance grep asserting zero `^location:` lines resolve into `bbj-vscode/`; VS Code-side observations route to the close-out subsection instead. |
| T-63-P03-07 | Tampering | editing `INVENTORY.md` or `62-COVERAGE.md` so a gate agrees | high | mitigate | Prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for both. |
| T-63-P03-08 | Repudiation | a claim asserting a runnable Gradle reproduction that could not have been produced | medium | mitigate | D-07 prohibition: no build, compile or test task is scheduled; every `repro`-tier record clears its tier by trace and says why it carries no runnable reproduction. |
| T-63-P03-09 | Tampering | five plans writing one `63-COVERAGE.md` | medium | mitigate | Wave 3 behind `depends_on: [63-02]` makes ordering structural (D-04); both tasks assert exact global counts (18/17, then 21/14) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-63-P03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed`, `diff` and `awk` only. |
</threat_model>

<verification>
- `## RU-63-04`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's `R-D6-CENTRAL` text verbatim.
- All 13 unit files are named inside the section.
- The D1 cell characterises the bridge for what it is, naming `BbjComposerServer.java`,
  `BbjComposerService.java` and `LanguageServerManager`.
- The D7 cell compares generated BBj code and DTO shapes, naming `composer-commands.ts` and
  `ComposerModels.java`; no `location:` resolves inside `bbj-vscode/`.
- `### Inherited referral triage` contains exactly one `- **Referral` bullet, naming both `RU-62-04`
  and `RU-62-03`, with a disposition from the three-way vocabulary.
- The D5 cell cross-references `P63-D5-001` and adds this unit's own consequence.
- The D-16 scope-fidelity statement is present.
- Phase-wide verdict count is 21 and placeholder count is 14; 21 + 14 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed non-`P00-` ID and a non-blank `dedup:` that
  explicitly covered #385 and #475.
- `git status --porcelain bbj-intellij bbj-vscode java-interop .planning/reviews/INVENTORY.md .planning/reviews/62-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- RVW-04 advances: 13 more of the 61 files (30 cumulative) have a recorded pass/fail against every
  dimension that applies to them — the largest unit by LOC in the phase is closed.
- The SETOPTS gap Phase 62 referred twice reaches exactly one written disposition, so the phase's
  finding accounting reflects one absence rather than two.
- The D7 question is answered as INVENTORY frames it — equivalence of generated BBj code, including
  a field-for-field DTO shape comparison that no compiler on either side can catch.
- ROADMAP Phase 63 criteria 1, 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/63-intellij-plugin-review/63-03-SUMMARY.md` when done.
</output>
