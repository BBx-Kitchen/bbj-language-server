---
phase: 69-github-issue-filing
plan: 13
type: execute
wave: 8
depends_on: ["69-12"]
files_modified:
  - .planning/reviews/MAJOR-REFACTORS.md
  - .planning/phases/69-github-issue-filing/69-CLOSE-OUT.md
autonomous: true
requirements: [ISSUE-05, ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 144 `issue:` slots in `MAJOR-REFACTORS.md` are non-empty and each names exactly one tracker entry: an issue number for the 135 public records and a GHSA ID annotated as a draft advisory for the 9 advisory records (ISSUE-05, D-15)."
    - "The write-back changed nothing but the 144 `issue:` lines: `git diff -U0` over `MAJOR-REFACTORS.md` shows exactly 144 removed and 144 added lines, every one of them matching `^[+-]issue:`, and the record-block order and every other byte of the 578 KB document are preserved (ISSUE-05 / edge: ordering)."
    - "The write-back is re-runnable from the ledger and idempotent: running it a second time produces no diff, and no `issue:` slot ends up holding two values or a duplicated entry (ISSUE-05 / edge: adjacency)."
    - "The write-back halts rather than writing partially if any of the 144 finding IDs has no ledger row — no slot is left half-written and no slot is filled with a guessed value (ISSUE-05 / edge: empty)."
    - "`derive-review-docs.mjs` was not invoked by this phase and `--force` was not passed: the diff shape proves it, because a regeneration would rewrite the whole document rather than 144 lines (D-16)."
    - "`69-CLOSE-OUT.md` states the D-17 carve-out in the honesty pattern this project uses: it names ISSUE-03, names the nine advisory records, and states that a GitHub advisory carries no labels so the area/PRIO/effort triad cannot be applied to them — without rewording the requirement and without restating it as if it had been met."
    - "`git status --porcelain .planning/reviews/` shows exactly one modified file — `MAJOR-REFACTORS.md` — and nothing is modified under `bbj-vscode/`, `bbj-intellij/`, `java-interop/`, `documentation/` or `examples/`, and `INVENTORY.md`, every `6N-COVERAGE.md`, `EASY-FIXES.md` and `REQUIREMENTS.md` are untouched."
  artifacts:
    - path: .planning/reviews/MAJOR-REFACTORS.md
      provides: "144 filled `issue:` slots — every documented finding traceable to exactly one tracker entry"
      contains: "GHSA-"
    - path: .planning/phases/69-github-issue-filing/69-CLOSE-OUT.md
      provides: "The ISSUE-01..ISSUE-05 verdicts, the D-17 advisory carve-out, the phase reconciliation, the write-boundary check and the discrepancy register"
      contains: "## Advisory carve-out"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-FILING-LEDGER.md `## Rows`"
      to: "the 144 `issue:` lines in `.planning/reviews/MAJOR-REFACTORS.md`"
      via: "a finding_id → entry map built from the ledger drives a targeted per-line replacement; nothing else in the document is read or written"
      pattern: "^issue:"
    - from: ".planning/phases/69-github-issue-filing/69-CLOSE-OUT.md §## Advisory carve-out"
      to: ".planning/REQUIREMENTS.md ISSUE-03"
      via: "the close-out names the requirement and the shortfall in writing; no requirement text is edited (D-17)"
      pattern: "ISSUE-03"
  prohibitions:
    - requirement_id: ISSUE-05
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT restate a requirement's wording as if it had been met — ISSUE-03 is not fully true of the nine advisory records, and the close-out says so plainly, names the nine, and names the cause, instead of reporting a clean sweep"
---

<objective>
Write the 144 tracker entries back into `MAJOR-REFACTORS.md`, then close the phase honestly.

Purpose: ISSUE-05 is what makes the other four requirements auditable a year from now — without the
`issue:` slots filled, a reader holding either document cannot get from a finding to its tracker entry. The
write-back is also the event Phase 68 rated `costly` in advance: once these 144 values are in the document,
it can no longer be regenerated from its own derivation script without preserving them by hand first. That
hazard is respected here rather than worked around.

Output: 144 filled `issue:` slots in one commit, `69-CLOSE-OUT.md`, and the write-boundary check.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
@.planning/phases/69-github-issue-filing/69-12-SUMMARY.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting — in
particular the rule that `derive-review-docs.mjs` is never invoked by this phase and `--force` is never
passed.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file,
`.planning/phases/69-github-issue-filing/69-CLOSE-OUT.md`, and modifies
`.planning/reviews/MAJOR-REFACTORS.md`'s 144 `issue:` lines and nothing else.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->
<!-- planner-discipline-allow: derive-review-docs -->

<tasks>

<task type="auto">
  <name>Task 1: Write the 144 tracker entries into the `issue:` slots, in one commit</name>
  <reversibility rating="costly">Once these 144 values are written, `MAJOR-REFACTORS.md` can no longer be regenerated from its own derivation script without manually preserving every value first — the regenerated scaffold emits `issue:` always empty and carries nothing forward. This is the hazard Phase 68 D-09 rated `costly` in advance, and this task is the event it predicted.</reversibility>
  <precondition>`.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` §`## Rows` holds exactly 144 rows whose `finding_id:` set equals the 144 `^id:` values in `MAJOR-REFACTORS.md` §`## Records`, and every row carries a non-empty `entry:` value. Halt if any finding ID is missing a row — a partial write-back is worse than none.</precondition>
  <files>
    .planning/reviews/MAJOR-REFACTORS.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` — the whole file; it is the write-back's only source (D-16)
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 258-320 only — three record blocks, to confirm the `issue:` line's exact shape before writing: `issue:` followed by 13 spaces, giving a 19-character line with the value starting at column 20, uniform across all 144
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 3394-3408 only (§"Close-out" §"Regeneration hazard") — why the derivation script must not be run and what a future maintainer must preserve first
    - `.planning/phases/69-github-issue-filing/69-PATTERNS.md` §"Write-back into `MAJOR-REFACTORS.md`" — the `^issue: *(.*)$` line regex Phase 68 already anticipated, and the atomic temp-file-then-rename write approach to copy
  </read_first>
  <action>
Write an ephemeral script under the session's temporary directory — not into the repository, this phase
commits no script — that performs the write-back and nothing else:

1. Parse `69-FILING-LEDGER.md` §`## Rows` into a `finding_id → {route, entry}` map. Assert the map has 144
   members and that every `entry` is non-empty.
2. Read `MAJOR-REFACTORS.md` once. For each of the 144 fenced record blocks, located with the proven
   `` /```\n(id: *P\d+-D\d+-\d+[\s\S]*?)\n``` / `` extraction shape, find the block's `issue:` line with
   `/^issue: *(.*)$/m` and replace it with `issue:` plus 13 spaces plus the value, so the file's monospace
   alignment is preserved exactly. For a `route: issue` record the value is the issue number prefixed with
   `#`. For a `route: advisory` record the value is the GHSA ID followed by ` (draft advisory)` — D-15's
   convention, so no new field is added and Phase 68's frozen field set stays closed.
3. **Idempotence.** If a block's `issue:` value is already non-empty, assert it equals the value the ledger
   would write and leave the line untouched; if it differs, stop and report both values. Re-running the
   script must produce no diff.
4. Assert before writing that the replacement count is exactly 144 and that the only difference between the
   old and new file content is on lines beginning `issue:`. Write via a temporary file and `rename`, so a
   crash mid-write cannot leave a truncated 578 KB document.
5. Never invoke `derive-review-docs.mjs` and never pass `--force`. The script overwrites this document
   wholesale from a fresh render whose `issue:` field is always emitted empty, with no merge step — running
   it now would destroy exactly the values this task writes (D-16).

Then verify the diff shape and commit once, subject
`docs(69): write 144 filed tracker entries into MAJOR-REFACTORS.md issue slots`. One commit, not 144:
the ledger is already the crash-safe record, so 144 separate edits to a 578 KB file would produce an
unreadable history for no additional safety, and the write-back is re-runnable from the ledger if it fails
(D-16).
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && M=.planning/reviews/MAJOR-REFACTORS.md && [ "$(grep -cE '^issue: +$' $M)" = 0 ] && [ "$(grep -cE '^issue:' $M)" = 144 ] && [ "$(grep -cE '^issue: {13}(#[0-9]+|GHSA-[a-zA-Z0-9-]+ \(draft advisory\))$' $M)" = 144 ] && [ "$(grep -cE '^issue: {13}GHSA-' $M)" = 9 ] && [ "$(grep -cE '^issue: {13}#[0-9]+$' $M)" = 135 ] && [ "$(grep -E '^issue:' $M | sort -u | wc -l)" = 144 ] && git diff -U0 -- $M | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | grep -cvE '^[+-]issue:' | grep -qx 0 && [ "$(git diff --numstat -- $M | awk '{print $1"/"$2}')" = "144/144" ] && diff <(grep -E '^issue:' $M | sed -E 's/^issue: +//;s/ \(draft advisory\)$//;s/^#//' | sort) <(grep -E '^entry:' .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md | awk '{print $2}' | tr -d '#' | sort) && [ "$(git status --porcelain .planning/reviews/ | wc -l)" = 1 ] && git diff --quiet .planning/reviews/EASY-FIXES.md .planning/reviews/INVENTORY.md .planning/REQUIREMENTS.md && echo WRITEBACK-144-OK</automated>
  </verify>
  <acceptance_criteria>
    - `MAJOR-REFACTORS.md` contains 0 lines matching `^issue: +$` and 144 lines matching `^issue:`.
    - Every one of the 144 `issue:` lines matches `^issue: {13}(#[0-9]+|GHSA-[a-zA-Z0-9-]+ \(draft advisory\))$` — 135 issue numbers and 9 GHSA IDs, with the original 13-space padding preserved.
    - All 144 `issue:` line values are distinct — no two findings point at the same tracker entry.
    - The sorted set of entry values in the document equals the sorted set of `entry:` values in the ledger, with an empty `diff`.
    - `git diff --numstat` over `MAJOR-REFACTORS.md` reports exactly `144` added and `144` removed lines, and `git diff -U0` shows zero changed lines that do not begin `issue:` — so the record-block order and every other byte of the document are preserved.
    - Re-running the write-back script produces no diff at all.
    - `git status --porcelain .planning/reviews/` lists exactly one entry, and `EASY-FIXES.md`, `INVENTORY.md`, every `6N-COVERAGE.md` and `REQUIREMENTS.md` are unmodified.
    - The change lands as exactly one commit whose subject names the write-back, and no commit in the phase touches `derive-review-docs.mjs`.
  </acceptance_criteria>
  <done>Every one of the 144 documented findings resolves to exactly one tracker entry from inside the document, and the diff proves that nothing else in the 578 KB corpus moved.</done>
</task>

<task type="auto">
  <name>Task 2: 69-CLOSE-OUT.md — the requirement verdicts and the D-17 advisory carve-out</name>
  <files>
    .planning/phases/69-github-issue-filing/69-CLOSE-OUT.md
  </files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 80-84 only — the exact wording of ISSUE-01 through ISSUE-05, to quote once each rather than paraphrase
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` §`## Filing reconciliation` and §`## Dedup re-query` — the measured results the verdicts cite
    - `.planning/phases/69-github-issue-filing/69-08-SUMMARY.md` — the verbatim approval reply and the integrity report, as ISSUE-01's evidence
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Discrepancies` and §`## Routing decisions` — the departures already recorded, to carry into the register
    - `.planning/reviews/EASY-FIXES.md` §"Close-out" §"FIX-04 verdict" — the honesty-pattern shape to mirror: name the requirement, quote it once, name the shortfall and its cause, never reword it
  </read_first>
  <action>
Create `69-CLOSE-OUT.md` with five sections.

`## Requirement verdicts` — one entry per requirement, each quoting the requirement once and stating Met or
Partially Met with the evidence that settles it:

- **ISSUE-01** — met. The complete 144-entry list was rendered into one committed
  `69-ISSUE-DRAFT.md`, integrity-gated, and presented; the developer's approval is quoted verbatim in the
  `69-08` SUMMARY; the advisory count was 0 and the issue count unchanged at the moment approval was
  requested. Name the approved draft's blob SHA.
- **ISSUE-02** — met for the 135 public issues and for the 9 advisory descriptions: every body carries the
  five elements in a fixed order, with `failure_scenario:` and `proposed_approach:` lifted verbatim, and
  the region-scoped gate proved zero pointers into the planning directory.
- **ISSUE-03** — **partially met, and say so plainly.** Met for all 135 public issues: one area label, one
  `PRIO`, one effort value, all from the repository's existing set, nothing created. Not applicable to the
  nine advisory records — see the carve-out below. Do not report this as a clean sweep.
- **ISSUE-04** — met, on the widened reading: the live re-query covered open and closed issues immediately
  before the first write, all 144 records were screened, and every candidate carries a recorded
  disposition. State the widening explicitly — the requirement says "existing open issue" and the check
  covered closed issues too, on D-07's authority — and state the planner assumption that a match halts for
  the developer, which was flagged in the plans rather than resolved.
- **ISSUE-05** — met. All 144 `issue:` slots are non-empty and each names exactly one tracker entry; the
  diff shape shows 144 changed lines and nothing else.

`## Advisory carve-out` (D-17) — the honesty statement. Name ISSUE-03. Quote its wording once. State that a
GitHub security advisory carries no labels at all, so the area / `PRIO` / effort triad cannot be applied to
the nine records routed to advisories under D-01, and that their severity and effort remain recorded in
their advisory bodies and in `MAJOR-REFACTORS.md`'s own `severity:` and `effort:` fields. List the nine
finding IDs with their GHSA IDs. State that no requirement text was edited to accommodate this and that
`REQUIREMENTS.md` is unmodified — the shortfall is named, not smoothed away.

`## Reconciliation` — the phase's arithmetic as equations with their members named:
`144 = 135 public issues + 9 private draft advisories`; `144 = 17 wave-1 + 127 wave-2`;
`17 = 9 advisories + 8 public issues`; `144 = 1 critical + 16 high + 70 medium + 57 low`;
`11 dedup-annotated records filed + 0 skipped as duplicates = 11`; `144 issue: slots filled, 0 empty`.

`## Discrepancy Register` — every departure this phase found between a source artifact's claim and what is
true, each stated with both sides and no adjustment:

1. **D-10's cited title shape.** D-10 says titles are `<area>: <problem>` and cites `#497`–`#500` as the
   matched shape; those four use a component prefix (`java-interop:`, `Completion:`, `Formatter:`,
   `Decompile:`), not an area-label prefix. Titles were filed per D-10's literal rule. State how the
   developer ruled on it at the `69-08` gate and at the `69-10` pause.
2. **The 35-character area prefix.** The 16 `area=BBj integration and infrastructure` records carry a long
   title prefix under D-10's literal rule.
3. **Any stale `dedup:` annotation** the `69-09` re-query found — a named issue whose state changed since
   the frozen snapshot — recorded as a discrepancy, with the annotation left unrevised.
4. Every skip, rejection or halt recorded in the ledger during the filing run, and how it was resolved.
5. **The regeneration hazard is now live.** `MAJOR-REFACTORS.md` holds 144 filed values and its derivation
   script emits `issue:` always empty with no merge step; a future maintainer who genuinely needs to
   regenerate must preserve all 144 values first. Name the guard and the override flag so it is findable,
   and state that this phase invoked neither.

`## Write-boundary check` — filled in by Task 3.

If any equation does not balance or any verdict cannot be supported by a measured result, state the
departure rather than adjusting either side.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && C=.planning/phases/69-github-issue-filing/69-CLOSE-OUT.md && for s in '## Requirement verdicts' '## Advisory carve-out' '## Reconciliation' '## Discrepancy Register' '## Write-boundary check'; do grep -Fq "$s" $C || { echo "MISSING $s"; exit 1; }; done && for r in ISSUE-01 ISSUE-02 ISSUE-03 ISSUE-04 ISSUE-05; do grep -Fq "$r" $C || { echo "MISSING $r"; exit 1; }; done && grep -Eq 'Partially Met|partially met' $C && [ "$(grep -c 'GHSA-' $C)" -ge 9 ] && for i in P62-D1-003 P64-D1-004 P61-D1-003 P63-D1-007 P64-D1-006 P63-D1-001 P63-D1-003 P64-D1-002 P64-D1-003; do grep -Fq "$i" $C || { echo "MISSING advisory id $i"; exit 1; }; done && grep -Eq '144.*=.*135.*\+.*9' $C && grep -Eq '144.*=.*17.*\+.*127' $C && grep -Eq '144.*=.*1.*\+.*16.*\+.*70.*\+.*57' $C && grep -Fq '#497' $C && git diff --quiet .planning/REQUIREMENTS.md && echo CLOSEOUT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `69-CLOSE-OUT.md` contains all five sections.
    - Each of ISSUE-01 through ISSUE-05 has a verdict that quotes the requirement once and cites a measured result; ISSUE-03's verdict says partially met and does not report a clean sweep.
    - `## Advisory carve-out` names ISSUE-03, quotes its wording, states that advisories carry no labels, lists all nine finding IDs with their GHSA IDs, and states that no requirement text was edited.
    - ISSUE-04's verdict states the widening to closed issues explicitly and repeats the flagged planner assumption that a match halts for the developer rather than being auto-skipped.
    - `## Reconciliation` states all six equations with their members named.
    - `## Discrepancy Register` contains at least the five named items, including the `#497`–`#500` title-shape departure and the now-live regeneration hazard with its guard and override flag named.
    - `git diff --quiet .planning/REQUIREMENTS.md` exits 0 — no requirement text was edited (D-17).
  </acceptance_criteria>
  <done>A reader of the close-out learns which requirements are fully true, which is only partly true and why, and every place a source claim did not match what happened.</done>
</task>

<task type="auto">
  <name>Task 3: The write-boundary check and the phase's final assertions</name>
  <files>
    .planning/phases/69-github-issue-filing/69-CLOSE-OUT.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-CLOSE-OUT.md` — the four sections Task 2 wrote and the empty write-boundary section to fill
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 3443-3465 only (§"Close-out" §"Write-boundary check") — Phase 68's own boundary record, as the shape to mirror
  </read_first>
  <action>
Fill `## Write-boundary check` in `69-CLOSE-OUT.md` with the literal output of each command below and a
one-line verdict per assertion. This mirrors Phase 68 D-12's discipline: cheap, checkable, recorded.

1. `git status --porcelain .planning/reviews/` — must show exactly one entry, ` M
   .planning/reviews/MAJOR-REFACTORS.md`. Record the output verbatim.
2. `git status --porcelain -- bbj-vscode/ bbj-intellij/ java-interop/ documentation/ examples/` — must be
   empty. This phase changed no source, ran no build and executed no test.
3. `git diff --quiet` over `.planning/reviews/INVENTORY.md`, `.planning/reviews/EASY-FIXES.md`, every
   `.planning/reviews/6N-COVERAGE.md` and `.planning/REQUIREMENTS.md` — all must exit 0; those records are
   immutable to this phase.
4. `git diff --numstat -- .planning/reviews/MAJOR-REFACTORS.md` — must read `144 144`, and `git diff -U0`
   over the same file must show zero changed lines that do not begin `issue:`. Record both.
5. `git log --oneline --name-only <phase-range> -- .planning/phases/68-deliverable-documents/derive-review-docs.mjs`
   — must be empty: no commit in this phase touched the derivation script. Record the empty result, and
   state that the 144-line diff shape in assertion 4 is the positive proof, since a regeneration would have
   rewritten the whole document.
6. Tracker final state: `gh issue list --state all --limit 1000 --json number --jq 'length'` and
   `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq '[.[]|select(.state=="draft")]|length'`
   — the issue count must exceed the `## Dedup re-query` `total_count:` figure by exactly 135, and the draft
   advisory count must be 9. Record both numbers and the arithmetic.
7. `gh label list --limit 100 | wc -l` — must equal the phase-start count. No label was created.

Then commit `69-CLOSE-OUT.md` with subject
`docs(69): phase close-out — requirement verdicts, advisory carve-out and write-boundary check`.

If any assertion fails, record the failure with its literal output and stop; the boundary record's value is
that it is true, not that it is clean.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && C=.planning/phases/69-github-issue-filing/69-CLOSE-OUT.md && M=.planning/reviews/MAJOR-REFACTORS.md && grep -Fq '## Write-boundary check' $C && grep -Fq 'MAJOR-REFACTORS.md' $C && [ "$(git status --porcelain .planning/reviews/ | wc -l)" = 1 ] && [ -z "$(git status --porcelain -- bbj-vscode/ bbj-intellij/ java-interop/ documentation/ examples/)" ] && git diff --quiet .planning/reviews/INVENTORY.md .planning/reviews/EASY-FIXES.md .planning/REQUIREMENTS.md && for f in .planning/reviews/6*-COVERAGE.md; do git diff --quiet "$f" || { echo "MODIFIED $f"; exit 1; }; done && [ "$(git diff --numstat -- $M | awk '{print $1" "$2}')" = "144 144" ] && git diff -U0 -- $M | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | grep -cvE '^[+-]issue:' | grep -qx 0 && [ "$(gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq '[.[]|select(.state=="draft")]|length')" = 9 ] && grep -Eq '135' $C && grep -Fq 'derive-review-docs' $C && echo BOUNDARY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Write-boundary check` records all seven assertions with their literal command output and a one-line verdict each.
    - `git status --porcelain .planning/reviews/` shows exactly one entry, `MAJOR-REFACTORS.md`.
    - Nothing is modified under `bbj-vscode/`, `bbj-intellij/`, `java-interop/`, `documentation/` or `examples/`.
    - `INVENTORY.md`, `EASY-FIXES.md`, every `6N-COVERAGE.md` and `REQUIREMENTS.md` are unmodified.
    - `git diff --numstat` over `MAJOR-REFACTORS.md` reads `144 144`, and `git diff -U0` shows zero changed lines not beginning `issue:`.
    - No commit in this phase touched the derivation script, and the close-out states that the 144-line diff shape is the positive proof.
    - The final issue count exceeds the recorded pre-filing `total_count:` by exactly 135, the draft advisory count is 9, and the arithmetic is written out.
    - `gh label list --limit 100` returns the phase-start label count.
    - `69-CLOSE-OUT.md` is committed.
  </acceptance_criteria>
  <done>The phase's write boundary is recorded as a set of literal command outputs a reader can re-run, and the milestone's last deliverable is closed with its shortfall named rather than smoothed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `69-FILING-LEDGER.md` → `.planning/reviews/MAJOR-REFACTORS.md` | the ledger becomes the authority for a `costly`-to-undo edit of a 578 KB document |
| this phase → every immutable record in `.planning/reviews/` | `INVENTORY.md` and the six COVERAGE files are read-only to this phase; the boundary check proves it rather than asserting it |
| the filed tracker state → the close-out's requirement verdicts | what is claimed met must be what is measured, including where it is only partly met |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-08 | Tampering | a whole-file rewrite clobbering 578 KB of records, or a regeneration emptying all 144 slots | high | mitigate | The write-back is a targeted per-line replacement written via temp-file-then-rename; `git diff --numstat` must read exactly `144 144` and `git diff -U0` must show zero non-`issue:` changed lines; the derivation script is never invoked and `--force` is never passed, and the diff shape is the positive proof of both |
| T-69-28 | Tampering | a partial write-back leaving some slots filled and some empty after a crash | high | mitigate | Task 1's `<precondition>` requires a complete 144-row ledger before anything is written; the replacement count is asserted to be 144 before the file is written; the write is atomic via rename; the script is idempotent so a re-run produces no diff |
| T-69-27 | Repudiation | two findings pointing at the same tracker entry | medium | mitigate | All 144 `issue:` values asserted distinct, and the document's entry set is `diff`ed against the ledger's |
| T-69-29 | Tampering | an immutable record modified as a side effect | medium | mitigate | Task 3 asserts `git diff --quiet` over `INVENTORY.md`, `EASY-FIXES.md`, every `6N-COVERAGE.md` and `REQUIREMENTS.md`, and that `git status --porcelain .planning/reviews/` lists exactly one entry |
| T-69-30 | Repudiation | a requirement reported met when it is only partly true | high | mitigate | The transparency prohibition in `must_haves.prohibitions`; ISSUE-03's verdict must say partially met and the `## Advisory carve-out` must name the requirement, the nine records and the cause; no requirement text is edited |
| T-69-20 | Information disclosure | a credential reaching the corpus through a written-back value | low | accept | The only written values are issue numbers and GHSA IDs, both matched against a strict line-shape regex; no free text is written |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 144 non-empty `issue:` slots, 135 issue numbers and 9 GHSA IDs, all distinct, all matching the strict
  line shape with the original padding.
- `git diff --numstat` reads `144 144` and `git diff -U0` shows no non-`issue:` change.
- Re-running the write-back produces no diff.
- `69-CLOSE-OUT.md` carries five sections, verdicts for all five requirements with ISSUE-03 partially met,
  the D-17 carve-out naming the nine records, six balanced equations, a discrepancy register and seven
  literal boundary assertions.
- Exactly one file modified under `.planning/reviews/`; nothing under the four source trees; no requirement
  text edited; no label created; nine advisories still drafts.
</verification>

<success_criteria>
Every one of the 144 documented findings is traceable to exactly one tracker entry from inside the
document, the 578 KB corpus is otherwise byte-identical, and the phase closes stating what is true —
including the one requirement that is only partly true and exactly why.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-13-SUMMARY.md` when done, including the diff-shape
output, the six reconciliation equations and the seven boundary assertions.
</output>
</content>
