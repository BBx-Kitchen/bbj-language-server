---
phase: 69-github-issue-filing
plan: 08
type: execute
wave: 3
depends_on: ["69-01", "69-02", "69-03", "69-04", "69-05", "69-06", "69-07"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md
  - .planning/phases/69-github-issue-filing/69-BODIES-01.md
  - .planning/phases/69-github-issue-filing/69-BODIES-02.md
  - .planning/phases/69-github-issue-filing/69-BODIES-03.md
  - .planning/phases/69-github-issue-filing/69-BODIES-04.md
  - .planning/phases/69-github-issue-filing/69-BODIES-05.md
  - .planning/phases/69-github-issue-filing/69-BODIES-06.md
  - .planning/phases/69-github-issue-filing/69-BODIES-07.md
autonomous: false
requirements: [ISSUE-01, ISSUE-02, ISSUE-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`69-ISSUE-DRAFT.md` is a single committed file holding every one of the 144 rendered bodies in full, in filing order, with their titles and labels — the one approval artifact D-05 requires, and the file the filing run consumes rather than re-rendering from the corpus."
    - "The set of finding IDs carrying a `BODY-BEGIN` marker in the assembled draft is exactly equal to the set of 144 `id:` values in `MAJOR-REFACTORS.md` §`## Records` — no record is missing and none appears twice."
    - "Exactly 9 entries are routed `private draft advisory` and exactly 135 `public issue`, and the 9 are exactly those records whose `dimension:` is `D1` and whose `severity:` is `critical` or `high` (D-01/D-02)."
    - "All 144 titles are distinct. The filing run's resume check matches on the title, so a duplicate title would let a real record be silently skipped as already-filed (D-14)."
    - "The seven render shards are removed in the same commit that adds the assembled draft, so exactly one file holds the approved content and approval cannot be given against a stale copy (D-05)."
    - "No tracker write has occurred at the moment the approval is requested: the count returned by `gh issue list --state all --json number --jq 'length'` equals the pre-phase count, and `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq 'length'` returns 0 (ISSUE-01)."
    - "The complete drafted list is presented and the approval is explicitly requested before any create is attempted; an empty, absent, or non-affirmative response results in zero tracker writes, and `gh issue list --state all --search` for any drafted title continues to return no match (ISSUE-01 / edge: empty)."
    - "The filing step reads the approved committed draft as its only source, and the draft's committed git blob SHA is recorded so that a draft edited after approval is detectable by comparison rather than by trust (ISSUE-01 / edge: adjacency)."
    - "A single approval releases both the issue run and the advisory run; there is no second approval gate, and the wave-1 pause in `69-09` is a checkpoint inside the approved run rather than a second gate (D-06, D-13)."
    - "Every one of the 144 rendered bodies contains all five ISSUE-02 elements non-empty; a body with an empty section fails the gate rather than reaching the tracker (ISSUE-02 / edge: empty)."
    - "Every index row's three label values are byte-identical to that record's `proposed_labels:` triad and to a name in `gh label list` output, including the spaces inside `BBj integration and infrastructure`; no label is created (ISSUE-03 / edges: empty, encoding)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md
      provides: "The complete, committed, approval-ready filing list — 144 bodies, titles, labels, routes, plus the derivation, reconciliation, routing decisions, discrepancies, dedup baseline and D1 grouping"
      contains: "## Bodies"
  key_links:
    - from: "the seven 69-BODIES-0N.md shards"
      to: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md"
      via: "shell concatenation in shard order, which is filing order, replacing the two assembly markers; shard contents are never read into context"
      pattern: "<!-- ASSEMBLE: BODIES -->"
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md"
      to: "the GitHub tracker (plans 69-09, 69-10, 69-11)"
      via: "the approved committed file is the filing run's only source; bodies are sliced out by their BODY-BEGIN/BODY-END anchors, never re-rendered"
      pattern: "BODY-BEGIN"
  prohibitions:
    - requirement_id: ISSUE-01
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT treat any non-human signal as the ISSUE-01 approval — a yolo-mode config, an auto-advance setting, a planner or executor self-assessment, or the absence of an objection must never discharge this gate; only the developer's own explicit affirmation does, and it is quoted verbatim in the SUMMARY"
    - requirement_id: ISSUE-02
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT put a step-by-step reproduction or a runnable attack sequence into a body destined for the PUBLIC tracker for any unfixed security path — the 24 `medium`/`low` D1-primary records are the exposure this gate exists to let a human rule on before it becomes public and permanent"
    - requirement_id: ISSUE-02
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT let the authored acceptance-criteria field read as a design commitment, an accepted implementation plan, or a schedule promise — FUT-04 defers every one of these 144 refactors"
---

<objective>
Assemble the seven shards into the single approval artifact, prove it mechanically, and put it in front
of the developer.

Purpose: this is the ISSUE-01 gate and the one-way door. Every constraint in this phase exists because a
GitHub issue on a public repository is indexed, emailed to watchers and mirrored by third parties within
minutes, and closing it afterwards does not retract it (D-04). Nothing may be created on the tracker
before the developer has read this file and said yes.

Output: one committed `69-ISSUE-DRAFT.md` holding all 144 bodies, the seven shards removed, an integrity
report in the SUMMARY, and an explicit approval decision recorded verbatim.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan produces no new file; it assembles
`69-ISSUE-DRAFT.md` from the seven shards and deletes the shards.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Assemble the seven shards into one draft and remove them</name>
  <files>
    .planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md
    .planning/phases/69-github-issue-filing/69-BODIES-01.md
    .planning/phases/69-github-issue-filing/69-BODIES-02.md
    .planning/phases/69-github-issue-filing/69-BODIES-03.md
    .planning/phases/69-github-issue-filing/69-BODIES-04.md
    .planning/phases/69-github-issue-filing/69-BODIES-05.md
    .planning/phases/69-github-issue-filing/69-BODIES-06.md
    .planning/phases/69-github-issue-filing/69-BODIES-07.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` — the frame only, down to and including the two assembly-marker lines; do not read past them
    - `.planning/phases/69-github-issue-filing/69-01-SUMMARY.md` … `69-07-SUMMARY.md` — the per-shard row counts and any departure each render plan reported
    - the seven shard files' structure only, via `grep -n '^## '` on each — their `## Index rows A-B` and `## Bodies rows A-B` heading line numbers. Do not read shard bodies into context; they total roughly 250 KB and the assembly is a byte operation, not an editorial one
  </read_first>
  <action>
Replace the two assembly markers in `69-ISSUE-DRAFT.md` with the concatenated shard content, using shell
text processing so no shard body ever enters context.

Replace the line `<!-- ASSEMBLE: INDEX -->` with, in shard order 01 through 07: a single five-column
table header (`| # | finding_id | route | title | labels |` plus its separator row) followed by every
index data row from every shard's `## Index rows A-B` section, in shard order. The result is one table of
144 data rows in filing order 1 through 144. Extract each shard's data rows with an `awk` range bounded
by its `## Index rows` heading and its `## Bodies rows` heading, keeping only lines that begin with a
pipe and whose second field is a bare integer, so the per-shard table header and separator rows are
dropped rather than repeated.

Replace the line `<!-- ASSEMBLE: BODIES -->` with every shard's `## Bodies rows A-B` section, in shard
order, heading included — so the assembled `## Bodies` section reads as seven labelled sub-blocks whose
row ranges are contiguous from 1 to 144.

Then `git rm` the seven shard files. The assembled draft and the shard removal land in one commit,
subject `docs(69): assemble the 144-entry issue draft from the seven render shards`, so exactly one file
holds the approved content and no stale copy can be approved against or filed from (D-05).

Do not re-render, re-wrap, re-order or normalise any byte inside a `BODY-BEGIN` / `BODY-END` region.
Whitespace normalisation is precisely the divergence D-05 exists to prevent: the developer must approve
the same bytes the filing run sends.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && F=$D/69-ISSUE-DRAFT.md && ! grep -Fq '<!-- ASSEMBLE:' $F && [ "$(grep -c 'BODY-BEGIN' $F)" = 144 ] && [ "$(grep -c 'BODY-END' $F)" = 144 ] && [ "$(ls $D/69-BODIES-0*.md 2>/dev/null | wc -l)" = 0 ] && awk '/^## Index/{f=1;next}/^## Bodies/{f=0}f' $F | grep -cE '^\| *[0-9]+ *\|' | grep -qx 144 && awk '/^## Index/{f=1;next}/^## Bodies/{f=0}f' $F | awk -F'|' '/^\| *[0-9]+ *\|/{gsub(/ /,"",$2);print $2}' | sort -n | uniq | wc -l | grep -qx 144 && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo ASSEMBLED-144</automated>
  </verify>
  <acceptance_criteria>
    - `69-ISSUE-DRAFT.md` contains neither assembly marker.
    - `69-ISSUE-DRAFT.md` contains exactly 144 `BODY-BEGIN` markers and 144 matching `BODY-END` markers.
    - The `## Index` section contains exactly 144 data rows whose row numbers are the integers 1 through 144, each appearing once, in ascending order, with exactly one table header and one separator row.
    - The `## Bodies` section contains seven sub-headings whose row ranges are contiguous and disjoint and together span 1-144.
    - Zero files matching `69-BODIES-0*.md` remain in the phase directory, and their removal is in the same commit as the assembled draft.
    - For each of three spot-checked records (`P62-D1-003`, `P66-D4-001`, `P64-D4-001`), the bytes between its `BODY-BEGIN` and `BODY-END` markers in the assembled draft are identical to the bytes between the same markers in the pre-assembly shard as recorded by that shard's git blob.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>One file holds all 144 bodies in filing order, byte-identical to what the shards rendered, and no second copy exists to be approved by mistake.</done>
</task>

<task type="auto">
  <name>Task 2: The integrity gate — prove the draft mechanically, then commit it</name>
  <files>
    .planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation`, §`## Reconciliation`, §`## Routing decisions` and §`## Discrepancies` — the contract the gate checks against; do not read the `## Bodies` section into context, check it with shell assertions
    - `.planning/REQUIREMENTS.md` lines 80-84 only — the exact wording of ISSUE-01..ISSUE-05
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 107-255 only — the filing-order Index, as the authority for row order and finding-ID membership
  </read_first>
  <action>
Write an ephemeral gate script under the session's temporary directory — not into the repository, this
phase commits no script — and run it against the assembled draft. It asserts, printing a labelled
pass/fail line per assertion and exiting non-zero on any failure:

1. **Membership.** The sorted set of finding IDs appearing as `BODY-BEGIN <id>` equals the sorted set of
   `^id:` values in `MAJOR-REFACTORS.md` §`## Records`. Both sets have 144 members. Report any ID present
   in one and not the other by name.
2. **Route split.** Exactly 9 `**Route:** private draft advisory` and exactly 135 `**Route:** public
   issue`. The 9 advisory finding IDs equal the set computed from the corpus by the two-field predicate
   (`dimension: D1` and `severity:` in `critical`, `high`). Report a mismatch by name.
3. **Label fidelity.** For each of the 144 index rows, the three label values equal that record's
   `proposed_labels:` triad with the `area=` prefix stripped, and each value is present in the output of
   `gh label list --limit 100`. No fourth value, no missing value, no label absent from the repository.
4. **Title determinism and uniqueness.** All 144 titles are distinct after trimming; each begins with its
   record's area label followed by a colon and a space. A duplicate title is a hard failure — the filing
   run's resume check matches on the title, so two identical titles would let a real record be silently
   skipped as already-filed.
5. **Body completeness.** Inside each of the 144 delimited regions, each of the six section headings
   appears exactly once, and no section is empty — in particular no `## Acceptance criteria` section is
   empty or consists only of a heading.
6. **Verbatim fidelity.** For each of the 144 records, the first 80 characters of its
   `failure_scenario:` and the first 80 characters of its `proposed_approach:` appear verbatim inside
   that record's delimited region.
7. **Self-containedness.** The concatenation of all 144 delimited regions contains zero occurrences of
   `.planning/`, `MAJOR-REFACTORS`, `INVENTORY.md`, a COVERAGE-file name, an `RU-nn-nn` unit identifier
   or a `§` section pointer.
8. **Secret scan.** The whole draft contains no `ghp_`, `github_pat_`, `AKIA` or `xox` token prefix and
   no PEM private-key header.
9. **No tracker write yet.** `gh issue list --state all --limit 1000 --json number --jq 'length'` equals
   the count recorded in `## Dedup baseline`, and
   `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq 'length'` returns `0`.

Paste the script's full labelled output into the plan SUMMARY — the integrity report is the evidence the
approval is given against, not a claim about it.

If any assertion fails, fix the draft and re-run rather than adjusting the assertion. If an assertion
cannot be made to pass because the corpus itself disagrees with an expectation, stop and report the
departure as a discrepancy; a derived number is never adjusted to match an expectation, and an
expectation is never edited to match a derived number without the discrepancy being written down first.

Then commit the draft with subject `docs(69): integrity-gate the 144-entry issue draft ahead of the
ISSUE-01 approval`, and record the committed blob SHA of the draft
(`git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md`) in the SUMMARY. That
SHA is what plans `69-09`, `69-10` and `69-11` compare against before every filing batch, so an edit
between approval and filing is detected rather than trusted away.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && F=$D/69-ISSUE-DRAFT.md && M=.planning/reviews/MAJOR-REFACTORS.md && diff <(grep -o 'BODY-BEGIN [A-Za-z0-9-]*' $F | awk '{print $2}' | sort) <(grep '^id: ' $M | awk '{print $2}' | sort) && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 9 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 135 ] && node -e 'const fs=require("fs");const m=fs.readFileSync(".planning/reviews/MAJOR-REFACTORS.md","utf8");const re=/```\n(id: *P\d+-D\d+-\d+[\s\S]*?)\n```/g;let x;const adv=[];while((x=re.exec(m))!==null){const b=x[1];const g=f=>{const t=b.match(new RegExp("^"+f+": *(.*)$","m"));return t?t[1].trim():""};if(g("dimension")==="D1"&&["critical","high"].includes(g("severity")))adv.push(g("id"));}if(adv.length!==9){console.error("advisory predicate selected "+adv.length+", expected 9");process.exit(1);}const d=fs.readFileSync(process.argv[1],"utf8");for(const id of adv){const i=d.indexOf("BODY-BEGIN "+id);if(i<0){console.error("absent "+id);process.exit(1);}const h=d.lastIndexOf("**Route:**",i);if(!/private draft advisory/.test(d.slice(h,h+60))){console.error("misrouted "+id);process.exit(1);}}console.log("ROUTE-OK 9 "+adv.join(","));' $F && T=$(awk '/^## Index/{f=1;next}/^## Bodies/{f=0}f' $F | awk -F'|' '/^\| *[0-9]+ *\|/{gsub(/^ +| +$/,"",$5);print $5}') && [ "$(printf '%s\n' "$T" | wc -l)" = 144 ] && [ "$(printf '%s\n' "$T" | sort -u | wc -l)" = 144 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 144 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && ! grep -Eq 'ghp_|github_pat_|AKIA|xox|BEGIN [A-Z ]*PRIVATE KEY' $F && [ "$(gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq 'length')" = 0 ] && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - The membership diff between draft `BODY-BEGIN` IDs and corpus `id:` values is empty — 144 on both sides.
    - Exactly 9 entries are routed `private draft advisory`, exactly 135 `public issue`, and every one of the 9 is a record whose `dimension:` is `D1` and `severity:` is `critical` or `high`.
    - All 144 titles are distinct, and each begins with its record's area label followed by `: `.
    - Each of the six body section headings appears exactly 144 times inside the delimited regions, and no `## Acceptance criteria` section is empty.
    - For all 144 records, the first 80 characters of `failure_scenario:` and of `proposed_approach:` appear verbatim inside that record's delimited region.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - The draft contains no `ghp_`, `github_pat_`, `AKIA` or `xox` prefix and no PEM private-key header.
    - Every label value across all 144 rows is present in `gh label list --limit 100` output, and `gh label list` returns the same label count as at phase start.
    - `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq 'length'` returns 0 and the total issue count is unchanged from the `## Dedup baseline` figure — no tracker write has happened.
    - The SUMMARY contains the gate script's full labelled output and the draft's committed blob SHA.
  </acceptance_criteria>
  <done>The draft is proved rather than asserted, and the exact bytes the developer is about to approve are committed and addressable by SHA.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: ISSUE-01 approval — the one-way door</name>
  <reversibility rating="one-way">Creating an issue on a public repository is indexed, emailed to watchers and mirrored by third parties within minutes; closing or deleting it afterwards removes the page, not the notification, the index entry or any mirror. This gate is the only point at which the decision is still free.</reversibility>
  <decision>Approve the 144-entry filing list in `69-ISSUE-DRAFT.md` for execution — 135 public GitHub issues and 9 private draft security advisories — or withhold approval and name what must change first.</decision>
  <context>
This is the ISSUE-01 gate and the only approval in the phase (D-06): one decision releases both the
issue run and the advisory run. The wave-1 pause that follows in `69-09` is a checkpoint inside the
approved run, not a second gate.

The whole milestone has been holding tracker writes closed for this moment. Phase 66 D-02 froze them so
this phase could open them once, deliberately, behind a human decision. Nothing has been written to the
tracker: the integrity gate in Task 2 confirmed the issue count is unchanged and the advisory endpoint
returns 0.

**What to read.** `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md`, committed at the blob SHA
recorded in the Task 2 SUMMARY. Its `## Reconciliation` gives the arithmetic, `## D1 security surface`
puts all 33 security findings in one block, `## Index` lists all 144 titles and label triads on one
screen, and `## Bodies` holds every body in full. Editing the file in place before approving is expected
and supported — approval is given against the file's committed state, and an edit after approval is
detected by SHA comparison at filing time rather than trusted away.

**Five things this gate is specifically asking you to rule on:**

1. **The 9/135 split.** Nine D1-primary `critical`/`high` findings go to private draft advisories and
   stay off the public tracker; everything else is public. In particular the 24 `medium`/`low` D1-primary
   findings describe security paths that are still unfixed and will be publicly readable. They are
   grouped under `## D1 security surface` so you can rule on the set rather than on 24 scattered entries.
2. **The four `high` dependency findings** — `P64-D6-002`, `P64-D6-006`, `P64-D6-007`, `P64-D6-008` —
   stay public, on the reasoning that they concern already-published CVEs in third-party packages and
   privatizing our tracking of a public CVE protects nothing. Two of them carry D1 as a secondary
   dimension, so this is the boundary Claude drew under D-02 and it is correctable here.
3. **The supersedes routing.** `P66-D4-001` supersedes `P63-D4-010` and both are among the 144. Both are
   filed as separate issues, each body naming the other and stating the direction. The alternative —
   filing only the superseding record and pointing both `issue:` slots at it — would drop a documented
   finding from the 144 denominator. `## Routing decisions` entry 2 records the reasoning.
4. **The title shape.** D-10 says `<area>: <problem>` and cites `#497`–`#500` as the matched shape, but
   those four titles actually use a component prefix (`java-interop:`, `Completion:`, `Formatter:`,
   `Decompile:`). Titles are rendered per D-10's literal rule, which gives the 16
   `BBj integration and infrastructure` records a 35-character prefix. `## Discrepancies` records this.
   Changing it after filing means editing 135 public titles.
5. **The acceptance criteria.** These are the one field with no source in the corpus; Claude authored all
   144 from `failure_scenario:` plus `proposed_approach:` under D-12's rule. They are the most likely
   place for something to have been asserted that the review never established.
  </context>
  <options>
    <option id="approve">
      <name>Approve as committed — file all 144</name>
      <pros>Releases the run against exactly the bytes reviewed; the filing step consumes this file rather than re-rendering, so approval and execution cannot diverge; the milestone's last deliverable proceeds.</pros>
      <cons>135 public issues become permanent public artifacts within minutes. Withdrawal is not available afterwards.</cons>
    </option>
    <option id="approve-after-edit">
      <name>Edit the draft in place, then approve the edited state</name>
      <pros>Any title shape, any body wording, any routing call can be corrected while it is still free; the edited file is re-committed and its new blob SHA becomes the approved one.</pros>
      <cons>Requires a re-run of the Task 2 integrity gate against the edited file before filing, so the approval costs one extra cycle.</cons>
    </option>
    <option id="withhold">
      <name>Withhold approval</name>
      <pros>Zero tracker writes; the phase stops with the draft committed and reviewable, and nothing about the corpus is lost.</pros>
      <cons>ISSUE-01 through ISSUE-05 stay open and the milestone cannot close.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve`, `approve-after-edit` (and say what to change), or `withhold`. This gate is never auto-approved: a yolo-mode config, an auto-advance setting, an executor self-assessment, or the mere absence of an objection does not discharge it. Quote the reply verbatim into the SUMMARY, because plans 69-09 through 69-11 assert against that record before they write anything.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the seven render shards → the single assembled draft | a byte concatenation; any normalisation here means the developer approves bytes the filing run will not send |
| the developer's approval → the filing run's authority to write | the phase's central control; everything downstream asserts against the record this gate produces |
| the assembled draft → the public GitHub tracker | the one-way door; this plan stops immediately before it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-02 | Elevation of privilege | the ISSUE-01 approval gate under `mode: yolo` | high | mitigate | `gate="blocking-human"`; the resume-signal states explicitly that no config setting, self-assessment or absence of objection discharges it; the human-approval prohibition in `must_haves.prohibitions`; downstream plans carry a precondition asserting a verbatim-quoted approval exists |
| T-69-03 | Tampering | draft edited between approval and filing | high | mitigate | Task 2 records the draft's committed git blob SHA; plans 69-09/10/11 re-derive it and halt on mismatch before every batch, so a post-approval edit is detected rather than trusted |
| T-69-01 | Information disclosure | route assignment across all 144 entries | high | mitigate | Gate assertion 2 recomputes the 9 advisory IDs from the corpus by the two-field predicate and compares to the draft's routes by name; a misrouted D1-primary `critical`/`high` record fails the gate before the developer sees it |
| T-69-09 | Information disclosure | the 24 `medium`/`low` D1-primary records becoming publicly readable | high | mitigate | `## D1 security surface` groups all 33 D1 records into one block and the checkpoint names them as decision item 1, so the developer rules on the exposure as a set instead of discovering it entry by entry |
| T-69-19 | Spoofing | two identical titles letting a real record be skipped by the resume check | high | mitigate | Gate assertion 4 requires all 144 titles distinct and hard-fails on a duplicate |
| T-69-20 | Information disclosure | a credential or token pasted into a body during rendering | medium | mitigate | Gate assertion 8 scans the whole draft for `ghp_`, `github_pat_`, `AKIA`, `xox` and PEM private-key headers |
| T-69-10 | Tampering | shard content normalised during assembly | medium | mitigate | Assembly is shell text processing with no re-render; Task 1's acceptance criteria compare three spot-checked regions byte-for-byte against their pre-assembly git blobs |
| T-69-21 | Tampering | approval given against a stale shard copy | medium | mitigate | The seven shards are `git rm`'d in the same commit that adds the assembled draft, so exactly one copy of the content exists |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- `69-ISSUE-DRAFT.md` holds 144 bodies, 144 index rows numbered 1-144, no assembly markers, and the
  seven shards are gone.
- The nine-assertion integrity gate exits 0 and its labelled output is in the SUMMARY.
- The issue count and the advisory count are unchanged from phase start: nothing has been filed.
- An explicit approval decision is recorded verbatim, or the phase is stopped with zero tracker writes.
</verification>

<success_criteria>
The developer has read the complete filing list in one file, ruled on the five decisions this gate
exists to route to a human, and either released the run against a SHA-addressable set of bytes or
stopped it — and in the withheld case the tracker is exactly as it was before the phase started.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-08-SUMMARY.md` when done, including the integrity
report, the draft's committed blob SHA, and the approval reply quoted verbatim.
</output>
</content>
