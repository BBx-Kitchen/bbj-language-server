---
phase: 69-github-issue-filing
plan: 04
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-04.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-04.md` holds 23 index rows and 23 delimited body blocks for filing-order rows 65-87 — the last of the 70 `medium` records — in filing order, all routed `public issue`."
    - "The two `dedup:`-annotated records in this band, `P61-D2-018` (`#486` partial-overlap) and `P61-D5-010` (`DEBT-02`), are filed with the annotation carried into the body naming what the finding adds beyond the existing item; neither is skipped (D-08)."
    - "`P66-D2-002`'s three labels are `vscode`, `PRIO 2`, `8`, parsed from `proposed_labels:` — its `effort:` field carries a prose cross-repo-scope note that is not a valid label value and is never read as one (D-09)."
    - "Every body carries all five ISSUE-02 elements in the fixed six-section order, with `failure_scenario:` and `proposed_approach:` lifted byte-identically (D-11)."
    - "The four records in this band whose lifted text carries a review-internal reference — `P62-D5-002`, `P63-D5-001`, `P63-D6-002`, `P64-D6-010` — keep the verbatim sentence and gain a bracketed gloss naming the referenced thing in outside-reader terms; the reference is neither deleted nor left dangling."
    - "The concatenated delimited regions of `69-BODIES-04.md` contain zero pointers into `.planning/`, no reference to the review corpus by filename, no `RU-nn-nn` unit identifier and no `§` section pointer (D-11)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-04.md
      provides: "Rendered index rows and body blocks for filing-order rows 65-87"
      contains: "BODY-BEGIN P61-D2-012"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md §## Derivation"
      to: ".planning/phases/69-github-issue-filing/69-BODIES-04.md"
      via: "the gloss rule and the six-section order are lifted from the contract, not re-derived per shard"
      pattern: "## Acceptance criteria"
---

<objective>
Render filing-order rows 65-87 — the closing 23 `medium` records — into shard `69-BODIES-04.md`.

Purpose: this band holds four of the seventeen records whose lifted prose refers to the review's own
internal structure. Those sentences are the ones most likely to reach a public tracker meaning nothing
to the reader, so the gloss rule gets its heaviest exercise here.

Output: `69-BODIES-04.md`, holding 23 index rows and 23 delimited body blocks in filing order.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-04.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 65-76 (12 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-04.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract, including the gloss rule and its list of affected finding IDs
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 177-188 only — Index rows 65-76
    - the twelve records, each extracted with the single-record awk from `<phase_conventions>`: `P61-D2-012`, `P61-D2-018`, `P61-D4-001`, `P61-D5-001`, `P61-D5-002`, `P61-D5-010`, `P61-D5-013`, `P61-D5-014`, `P62-D4-002`, `P62-D5-002`, `P62-D7-001`, `P63-D2-010`
  </read_first>
  <action>
Create `69-BODIES-04.md` with a `## Index rows 65-87` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 65-87` heading, then render the
twelve records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` (D-10); parse the three label values from
`proposed_labels:` alone (D-09); evaluate the route predicate from the record's own `dimension:` and
`severity:` — this band has no D1-primary record and every severity is `medium`, so every route is
`public issue`, but evaluate rather than assume and report a disagreement instead of resolving it; write
the six body sections in the fixed order between the `BODY-BEGIN` / `BODY-END` markers; lift
`failure_scenario:` and `proposed_approach:` byte-identically; author `## Acceptance criteria` per D-12
and nothing beyond it.

Three records need specific handling:

- **`P61-D2-018`** — `dedup:` names `#486` as a partial overlap. Its `## Traceability` section carries
  one line stating what this finding adds beyond `#486` (D-08). It is filed, not skipped.
- **`P61-D5-010`** — `dedup:` names `DEBT-02`, an internal debt item rather than a tracker issue. Its
  `## Traceability` line states the relationship in outside-reader terms without using the internal
  identifier as if a reader could look it up.
- **`P62-D5-002`** — its lifted text carries a review-internal reference. Lift verbatim and append the
  bracketed gloss naming the referenced thing in terms of real source files or real behaviour.

Several records in this band are D5 (test coverage) findings. Their `## Acceptance criteria` state the
observable condition under which the failure scenario stops being possible — a named behaviour covered
by a test that fails on the current code — and do not name a coverage percentage, a test framework
migration, or any scope the `proposed_approach:` does not already imply.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-04.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 12 ] && [ "$(grep -c 'BODY-END' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 12 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P61-D2-012 P61-D2-018 P61-D4-001 P61-D5-001 P61-D5-002 P61-D5-010 P61-D5-013 P61-D5-014 P62-D4-002 P62-D5-002 P62-D7-001 P63-D2-010; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && printf '%s' "$B" | grep -Fq '#486' && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-04.md` contains exactly 12 `BODY-BEGIN` markers, 12 matching `BODY-END` markers and 12 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 12 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - `P61-D2-018`'s `## Traceability` section names `#486` and states what the finding adds beyond it.
    - `P61-D5-010`'s `## Traceability` line describes the related work without using an internal debt identifier as a lookup key.
    - `P62-D5-002`'s body contains the verbatim source sentence plus a bracketed gloss naming the referenced thing in terms of real source files or real behaviour.
    - Every `**Labels:**` line carries exactly three values, each byte-identical to a name in `gh label list` output and traceable to that record's `proposed_labels:`.
    - No `## Acceptance criteria` section names a coverage percentage, a framework migration, a date, an owner, or any scope absent from that record's `proposed_approach:`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 65-76 are rendered and the shard's structure is established for Task 2 to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 77-87 (11 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-04.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-BODIES-04.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 189-199 only — Index rows 77-87
    - the eleven records, each extracted with the single-record awk: `P63-D2-015`, `P63-D5-001`, `P63-D6-002`, `P63-D7-001`, `P64-D2-005`, `P64-D4-003`, `P64-D4-005`, `P64-D5-001`, `P64-D6-010`, `P66-D2-002`, `P66-D5-002`
  </read_first>
  <action>
Append the remaining eleven entries of this band to `69-BODIES-04.md` — index rows into the existing
table in filing order, body blocks into `## Bodies rows 65-87` in filing order — so the shard ends
holding all 23 rows and the 70-record `medium` block is complete across shards 02, 03 and 04.

Four records need specific handling:

- **`P66-D2-002`** carries prose inside `effort:` — a cross-repo-scope note. Its labels are `vscode`,
  `PRIO 2`, `8`, read from `proposed_labels:`; reading the label from `effort:` would produce a
  malformed value (D-09).
- **`P63-D5-001`**, **`P63-D6-002`** and **`P64-D6-010`** carry review-internal references in their
  lifted text. Lift verbatim and append the bracketed gloss naming the referenced thing in
  outside-reader terms.

`P64-D6-010` has the second-longest `failure_scenario:` in the corpus at 1,097 characters. Lift the
whole of it; do not summarise, truncate or split it — D-11's verbatim rule has no length exception, and
Phase 68 wrote these fields long precisely so an issue body would stand alone.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-04.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 23 ] && [ "$(grep -c 'BODY-END' $F)" = 23 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 23 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 23 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 23 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P63-D2-015 P63-D5-001 P63-D6-002 P63-D7-001 P64-D2-005 P64-D4-003 P64-D4-005 P64-D5-001 P64-D6-010 P66-D2-002 P66-D5-002; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && SRC=$(awk -v ID=P64-D6-010 '$0 ~ "^id: +"ID"$"{f=1} f{print} f&&/^```$/{exit}' .planning/reviews/MAJOR-REFACTORS.md | sed -n 's/^failure_scenario: *//p') && awk '/BODY-BEGIN P64-D6-010/{f=1}f{print}/BODY-END P64-D6-010/{exit}' $F | grep -Fq "${SRC:0:120}" && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-04.md` contains exactly 23 `BODY-BEGIN` markers, 23 matching `BODY-END` markers and 23 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 23 times inside the delimited regions.
    - `P66-D2-002`'s index row carries exactly the three labels `vscode`, `PRIO 2`, `8`, and no fragment of its prose `effort:` value appears in any label position.
    - `P63-D5-001`, `P63-D6-002` and `P64-D6-010` each contain the verbatim source sentence plus a bracketed gloss; the concatenated delimited regions contain zero `RU-nn-nn` identifiers and zero `§` pointers.
    - `P64-D6-010`'s `## Failure scenario` section reproduces its source field in full — the first 120 characters of the source value are found verbatim inside that body's delimited region, and the section is not summarised or truncated.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 23 records of filing-order rows 65-87 are rendered, and the 70-record `medium` block is complete across shards 02, 03 and 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-10`) | the one-way boundary; this plan populates what will cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-13 | Information disclosure | four records whose lifted prose names review units and section pointers | medium | mitigate | Gloss rule applied per record; both tasks negative-grep the delimited regions for `RU-nn-nn` and `§` and require zero hits |
| T-69-10 | Tampering | verbatim lift of a 1,097-character `failure_scenario:` | medium | mitigate | Task 2 asserts the first 120 source characters appear verbatim inside the rendered region, so a summarised or truncated lift fails the gate |
| T-69-15 | Tampering | a malformed label from `P66-D2-002`'s prose `effort:` value | medium | mitigate | Labels parsed from `proposed_labels:` only; acceptance criteria assert the exact triad and that no fragment of the prose value reaches a label position |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-16 | Repudiation | a D5 acceptance criterion inventing scope the record never proposed | low | mitigate | Acceptance criteria forbid a coverage percentage, a framework migration or any scope absent from `proposed_approach:` |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 23 index rows and 23 delimited body blocks, all `public issue`.
- The four gloss records carry a verbatim sentence plus a gloss, and the region-scoped gate is clean.
- `P64-D6-010`'s long failure scenario is present in full.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
Filing-order rows 65-87 exist as approval-ready entries, and every sentence that referred to the
review's own internal structure now means something to a reader who has never seen it.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-04-SUMMARY.md` when done.
</output>
</content>
