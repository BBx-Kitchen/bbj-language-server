---
phase: 69-github-issue-filing
plan: 06
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-06.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-06.md` holds 18 index rows and 18 delimited body blocks for filing-order rows 108-125, in filing order, all routed `public issue`."
    - "The 3 D1-primary records in this band are `low` and therefore route `public issue` under the two-field predicate, filed with no difference in template, labels, order or batch (D-03)."
    - "`P64-D8-001` — the second of the corpus's two `area=documentation` records — carries the existing `documentation` label; no label is created for it (D-09, ISSUE-03)."
    - "`P63-D3-006`, the one record in this band whose lifted text carries a review-internal reference, keeps the verbatim sentence and gains a bracketed gloss naming the referenced thing in outside-reader terms."
    - "No record in this band carries a `dedup:` annotation; the absence is confirmed per record while extracting, not assumed from the band."
    - "Every body carries all five ISSUE-02 elements in the fixed six-section order, with `failure_scenario:` and `proposed_approach:` lifted byte-identically (D-11)."
    - "The concatenated delimited regions of `69-BODIES-06.md` contain zero pointers into `.planning/`, no reference to the review corpus by filename, no `RU-nn-nn` unit identifier and no `§` section pointer (D-11)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-06.md
      provides: "Rendered index rows and body blocks for filing-order rows 108-125"
      contains: "BODY-BEGIN P64-D4-002"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md §## Derivation"
      to: ".planning/phases/69-github-issue-filing/69-BODIES-06.md"
      via: "the six-section order and the D-12 acceptance-criteria rule are lifted from the contract, not re-derived per shard"
      pattern: "## Acceptance criteria"
---

<objective>
Render filing-order rows 108-125 — 18 `low` records — into shard `69-BODIES-06.md`.

Purpose: the middle of the `low` block. Nothing exotic lives here beyond one glossed reference and the
corpus's second documentation record, which is exactly why it is the band most at risk of a shard that
quietly drifts from the template while nobody is watching a special case.

Output: `69-BODIES-06.md`, holding 18 index rows and 18 delimited body blocks in filing order.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-06.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 108-116 (9 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-06.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 220-228 only — Index rows 108-116
    - the nine records, each extracted with the single-record awk from `<phase_conventions>`: `P64-D4-002`, `P64-D4-006`, `P64-D8-001`, `P61-D4-004`, `P61-D4-015`, `P62-D1-001`, `P62-D1-006`, `P62-D4-003`, `P63-D1-006`
  </read_first>
  <action>
Create `69-BODIES-06.md` with a `## Index rows 108-125` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 108-125` heading, then render the
nine records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` (D-10); parse the three label values from
`proposed_labels:` alone (D-09); evaluate the route predicate from the record's own `dimension:` and
`severity:` — every record here is `low`, so every route is `public issue`, but evaluate rather than
assume and report a disagreement instead of resolving it; write the six body sections in the fixed order
between the `BODY-BEGIN` / `BODY-END` markers; lift `failure_scenario:` and `proposed_approach:`
byte-identically; author `## Acceptance criteria` per D-12 and nothing beyond it.

Two records need specific handling:

- **`P64-D8-001`** is the second of the corpus's two `area=documentation` records. The `documentation`
  label exists in the repository and is applied as-is — nothing is created. Its `## Acceptance criteria`
  state the observable condition under which the documented claim matches the code, not a docs-site
  restructuring, and do not stray into the editorial review that is explicitly out of scope.
- **`P62-D1-001`**, **`P62-D1-006`** and **`P63-D1-006`** are D1-primary at `low` severity and file
  publicly with the standard template (D-03). Their `## Evidence` sections state surface, problem class
  and impact; `low` severity is not a licence to include a command line, a payload or an ordered step
  sequence that would reproduce the unfixed path.

`P64-D4-006` has the third-longest `failure_scenario:` in the corpus at 1,072 characters. Lift the whole
of it — D-11's verbatim rule has no length exception.

Every `dedup:` value in this band reads `none`. Confirm that per record while extracting and state the
confirmation in the plan SUMMARY rather than silently omitting a relationship line.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-06.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 9 ] && [ "$(grep -c 'BODY-END' $F)" = 9 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 9 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 9 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P64-D4-002 P64-D4-006 P64-D8-001 P61-D4-004 P61-D4-015 P62-D1-001 P62-D1-006 P62-D4-003 P63-D1-006; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && SRC=$(awk -v ID=P64-D4-006 '$0 ~ "^id: +"ID"$"{f=1} f{print} f&&/^```$/{exit}' .planning/reviews/MAJOR-REFACTORS.md | sed -n 's/^failure_scenario: *//p') && awk '/BODY-BEGIN P64-D4-006/{f=1}f{print}/BODY-END P64-D4-006/{exit}' $F | grep -Fq "${SRC:0:120}" && grep -Fq 'documentation' $F && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-06.md` contains exactly 9 `BODY-BEGIN` markers, 9 matching `BODY-END` markers and 9 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 9 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - `P64-D8-001`'s index row carries the `documentation` area label, and `gh label list` returns the same label count before and after this task.
    - `P64-D4-006`'s `## Failure scenario` reproduces its source field in full — the first 120 source characters appear verbatim inside its delimited region.
    - No `## Evidence` section of the three D1-primary records contains a command line, a payload, or an ordered step sequence that would reproduce the unfixed path.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`, each byte-identical to a name in `gh label list` output.
    - The SUMMARY records that all nine `dedup:` values read `none`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 108-116 are rendered and the shard's structure is established for Task 2 to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 117-125 (9 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-06.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-BODIES-06.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 229-237 only — Index rows 117-125
    - the nine records, each extracted with the single-record awk: `P63-D2-002`, `P63-D2-008`, `P63-D2-011`, `P63-D3-003`, `P63-D3-004`, `P63-D3-006`, `P63-D4-003`, `P63-D4-004`, `P63-D4-005`
  </read_first>
  <action>
Append the remaining nine entries of this band to `69-BODIES-06.md` — index rows into the existing table
in filing order, body blocks into `## Bodies rows 108-125` in filing order — so the shard ends holding
all 18 rows.

**`P63-D3-006`** carries a review-internal reference in its lifted text. Lift verbatim and append the
bracketed gloss naming the referenced thing in terms of real source files or real behaviour, per the
gloss rule in `69-ISSUE-DRAFT.md` §`## Derivation`.

Two of this band's records name a location with no line number — `P63-D4-005` names a Java file with no
range. Its `## Evidence` section reproduces the `location:` value exactly as recorded, in backticks, and
does not invent a line range to make the evidence look more precise than the record is. ISSUE-02 asks
for `file:line` evidence; where the record has only a file, the body carries the file and says so.

Every `dedup:` value in this band reads `none`. Confirm per record and state the confirmation in the
SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-06.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 18 ] && [ "$(grep -c 'BODY-END' $F)" = 18 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 18 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 18 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 18 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P63-D2-002 P63-D2-008 P63-D2-011 P63-D3-003 P63-D3-004 P63-D3-006 P63-D4-003 P63-D4-004 P63-D4-005; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && LOC=$(awk -v ID=P63-D4-005 '$0 ~ "^id: +"ID"$"{f=1} f{print} f&&/^```$/{exit}' .planning/reviews/MAJOR-REFACTORS.md | sed -n 's/^location: *//p') && awk '/BODY-BEGIN P63-D4-005/{f=1}f{print}/BODY-END P63-D4-005/{exit}' $F | grep -Fq "$LOC" && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-06.md` contains exactly 18 `BODY-BEGIN` markers, 18 matching `BODY-END` markers and 18 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 18 times inside the delimited regions.
    - `P63-D3-006` contains the verbatim source sentence plus a bracketed gloss; the concatenated delimited regions contain zero `RU-nn-nn` identifiers and zero `§` pointers.
    - `P63-D4-005`'s `## Evidence` section contains its `location:` value byte-identically and contains no line number or line range absent from that value.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`.
    - The SUMMARY records that all nine `dedup:` values read `none`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 18 records of filing-order rows 108-125 are rendered, and no body claims evidence more precise than its record recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-11`) | the one-way boundary; this plan populates what will cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Information disclosure | public bodies for the 3 `low` D1-primary records in this band | high | mitigate | `## Evidence` states surface, problem class and impact only; Task 1's acceptance criteria forbid a command line, a payload or an ordered step sequence |
| T-69-17 | Spoofing | a fabricated line range making evidence look more precise than the record | medium | mitigate | Task 2 asserts `P63-D4-005`'s rendered evidence contains its `location:` value byte-identically and no line number absent from it |
| T-69-10 | Tampering | verbatim lift of a 1,072-character `failure_scenario:` | medium | mitigate | Task 1 asserts the first 120 source characters appear verbatim inside the rendered region, so a summarised lift fails the gate |
| T-69-13 | Information disclosure | `P63-D3-006`'s review-internal reference reaching a public body | low | mitigate | Gloss rule applied; both tasks negative-grep the delimited regions for `RU-nn-nn` and `§` |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 18 index rows and 18 delimited body blocks, all `public issue`.
- The documentation record carries the existing label and no label was created.
- Region-scoped self-containedness gate returns zero hits.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
Filing-order rows 108-125 exist as approval-ready entries whose evidence is exactly as precise as the
records behind them, and no more.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-06-SUMMARY.md` when done.
</output>
</content>
