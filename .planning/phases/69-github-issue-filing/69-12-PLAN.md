---
phase: 69-github-issue-filing
plan: 12
type: execute
wave: 7
depends_on: ["69-11"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03, ISSUE-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 57 `low` records — filing-order rows 88-144 — exist as public issues, each with a title byte-identical to its drafted title, a body byte-identical to the approved draft's delimited region for that finding, and exactly three labels drawn from the repository's existing label set (ISSUE-02, ISSUE-03)."
    - "The ledger holds exactly 144 rows at the end of this plan — 9 with `route: advisory` and 135 with `route: issue` — and the set of ledger `finding_id` values equals the set of 144 `id:` values in `MAJOR-REFACTORS.md` §`## Records` exactly (D-14)."
    - "The 4 `dedup:`-annotated records in this range — `P61-D4-007`, `P63-D7-002`, `P63-D7-005`, `P63-D7-006` — are filed with their annotations in the body; none is skipped, so no finding anywhere in the corpus was dropped as a duplicate (D-08)."
    - "`P63-D7-002` and `P63-D7-006` are filed as two separate issues against the same existing issue `#65`, each stating a different part of its scope, so the tracker gains two distinct additions rather than two copies (ISSUE-02 / edge: adjacency)."
    - "The filing run closes with the derived denominator proven by measurement rather than by summing plans: 144 = 9 advisories + 135 public issues, and the ledger-versus-corpus set difference is empty in both directions."
    - "No label is created across the whole 135-issue run: `gh label list --limit 100` returns the same label count at the end of the run as at phase start (ISSUE-03 / edge: encoding)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
      provides: "The complete 144-row filing ledger and the closing reconciliation — the write-back source for plan 69-13"
      contains: "## Filing reconciliation"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-FILING-LEDGER.md `## Rows`"
      to: ".planning/reviews/MAJOR-REFACTORS.md's 144 `issue:` slots (plan 69-13)"
      via: "the ledger is the write-back's only source; every row carries the finding ID and the one tracker entry it maps to"
      pattern: "finding_id:"
---

<objective>
Close the filing run: file the 57 `low` public issues — filing-order rows 88-144 — and prove the
denominator.

Purpose: this is the last plan that writes to the tracker. After it, every one of the 144 records maps to
exactly one tracker entry, and the ledger is the file plan `69-13` writes back from. Proving that mapping
by set comparison against the corpus, rather than by adding up what each plan claimed, is what makes
ISSUE-05 checkable.

Output: 57 public issues, a 144-row ledger, and a closing reconciliation.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
@.planning/phases/69-github-issue-filing/69-11-SUMMARY.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<reversibility_gate>
The `one-way` ratings on the filing tasks below carry no in-plan `checkpoint:decision`, and that is
deliberate rather than an omission. D-06 locks this phase to a **single** approval gate, discharged at
`69-08` Task 3 — a `checkpoint:decision` framing the public-write door with its options and its undo cost
— and D-13 states that the wave-1 pause in `69-10` is a checkpoint inside the already-approved run, not a
second gate. Re-asking a door the developer has already walked through would be checkpoint fatigue, and
the rating is kept for the record exactly as the reversibility taxonomy prescribes. A structural-validator
warning about a missing preceding checkpoint is therefore expected on this plan.
</reversibility_gate>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan produces no new file; it appends 57 rows
and one reconciliation section to `69-FILING-LEDGER.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: File filing-order rows 88-115 (28 issues)</name>
  <reversibility rating="one-way">Every create here is a public, indexed, watcher-notified artifact that closing does not retract. The `69-08` checkpoint authorised the run and the `69-10` pause released it; no further approval exists and none is sought.</reversibility>
  <precondition>`69-FILING-LEDGER.md` holds exactly 87 rows and its `draft_blob_sha:` equals the current output of `git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md`. Halt on either mismatch.</precondition>
  <files>
    .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Index` rows 88-115 only — the 28 finding IDs, their exact titles and their label triads. Do not read the `## Bodies` section into context at any point
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` — its `## Rows` table, as the resume-check source
    - `.planning/phases/69-github-issue-filing/69-11-SUMMARY.md` — the batch driver as it ran, and any skip or rejection recorded
  </read_first>
  <action>
File filing-order rows 88 through 115 as public issues, in Index order, using the same batch driver as plan
`69-11`: read the draft's Index rows into a tab-separated worklist; snapshot the tracker once with
`gh issue list --state all --limit 1000 --json number,title`; then per finding, in order — resume check
against the ledger and that snapshot; `awk`-slice the body between its `BODY-BEGIN` and `BODY-END` markers
into a temp file, asserted non-empty and six-section-complete; `gh issue create --title "$TITLE"
--body-file "$BODYFILE" --label "$AREA" --label "$PRIO" --label "$EFFORT"`; append the ledger row
immediately; pace before the next.

Batches of about ten creates per shell invocation. Stop on the first rejection of any kind, record the
finding ID and the error in the ledger, and do not continue. Do not retry a create whose outcome is unknown
without first re-listing the tracker and matching the title.

Every record in this range is `low` severity and carries `PRIO 3`. `P61-D4-007` — `dedup:` naming `#466` —
is filed like everything else; its annotation is already in the approved body.

**Create no label.** All twelve values already exist. An unknown-label rejection is a discrepancy to
report, not a label to invent.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && L=$D/69-FILING-LEDGER.md && [ "$(grep -c '^finding_id:' $L)" = 115 ] && [ "$(grep -c '^route: *issue' $L)" = 106 ] && [ "$(grep -c '^route: *advisory' $L)" = 9 ] && IDS=$(awk '/^## Index/{f=1;next}/^## Bodies/{f=0}f' $D/69-ISSUE-DRAFT.md | awk -F'|' '/^\| *[0-9]+ *\|/{gsub(/ /,"",$2);gsub(/ /,"",$3); if($2+0>=88 && $2+0<=115) print $3}') && [ "$(printf '%s\n' "$IDS" | wc -l)" = 28 ] && for i in $IDS; do grep -Fq "$i" $L || { echo "MISSING ledger row $i"; exit 1; }; done && N=$(awk '/^entry: *#?[0-9]+/{gsub(/#/,"",$2);print $2}' $L) && for n in $(printf '%s\n' "$N" | tail -10); do C=$(gh issue view "$n" --json labels --jq '[.labels[].name]|length'); [ "$C" = 3 ] || { echo "issue #$n has $C labels"; exit 1; }; P=$(gh issue view "$n" --json labels --jq '[.labels[].name|select(.=="PRIO 3")]|length'); [ "$P" = 1 ] || { echo "issue #$n PRIO 3 count $P"; exit 1; }; done && [ "$(grep '^draft_blob_sha:' $L | awk '{print $2}')" = "$(git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md)" ] && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo ROWS-88-115-FILED</automated>
  </verify>
  <acceptance_criteria>
    - `69-FILING-LEDGER.md` holds exactly 115 rows: 9 `route: advisory` and 106 `route: issue`.
    - Every finding ID from the draft's Index rows 88-115 has exactly one ledger row, in ascending Index order with non-decreasing timestamps.
    - For the last ten issues created in this batch, `gh issue view <n> --json labels` returns exactly 3 label names, exactly one of which is `PRIO 3` and exactly one of which is a bare `2`, `4` or `8`.
    - For three spot-checked findings in this batch, the created issue's title equals its drafted title byte-for-byte and its body's first 200 characters equal the first 200 characters of the body sliced from the approved draft.
    - Every body was passed via `--body-file` from a slice asserted non-empty and six-section-complete before posting.
    - `gh label list --limit 100` returns the same label count as at phase start.
    - Any skip is recorded with its reason and any rejection with the finding ID and the error, with no create attempted after a rejection.
    - `draft_blob_sha:` still equals the current committed blob SHA of `69-ISSUE-DRAFT.md`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Filing-order rows 88-115 are on the tracker with correct titles, bodies and labels, and 29 entries remain.</done>
</task>

<task type="auto">
  <name>Task 2: File filing-order rows 116-144 (29 issues) and prove the 144 denominator</name>
  <reversibility rating="one-way">Same one-way public boundary; authorised by the `69-08` gate and released by the `69-10` pause.</reversibility>
  <precondition>`69-FILING-LEDGER.md` holds exactly 115 rows and its `draft_blob_sha:` still equals the current output of `git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md`. Halt on either mismatch.</precondition>
  <files>
    .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Index` rows 116-144 only, and §`## Reconciliation` — the last 29 finding IDs and the arithmetic this task closes against. Do not read the `## Bodies` section into context
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` — the 115 rows, as the resume-check source
  </read_first>
  <action>
File filing-order rows 116 through 144 using the same batch driver, resume check, slice-create-ledger-pace
sequence and stop-on-first-rejection rule as Task 1.

Three `dedup:`-annotated records fall in this range — `P63-D7-002`, `P63-D7-005` and `P63-D7-006`. They are
filed like everything else. `P63-D7-002` and `P63-D7-006` both reference the same existing issue `#65` from
different directions and their approved bodies already state which part of `#65`'s scope each covers;
record both new issue numbers together in the reconciliation section so the pair is traceable from the
ledger.

Then add a `## Filing reconciliation` section to the ledger and prove the denominator by measurement, not
by addition:

- **Set equality.** Extract the sorted set of `finding_id:` values from `## Rows` and the sorted set of
  `^id:` values from `MAJOR-REFACTORS.md` §`## Records`, and `diff` them. Record the literal diff output;
  it must be empty in both directions. Report any ID present in one set and not the other by name.
- **Route split.** `144 = 9 draft advisories + 135 public issues`, with the nine advisory IDs listed and
  the equation stated with its members named.
- **Uniqueness.** Every `entry:` value appears exactly once across the 144 rows — no two findings map to
  the same tracker entry — with the single exception being nothing: the supersedes pair has two distinct
  issue numbers, and that is stated explicitly so a reader does not expect a shared one.
- **Severity accounting.** `144 = 1 critical + 16 high + 70 medium + 57 low`, matching the corpus.
- **Label boundary.** `gh label list --limit 100` returns the phase-start label count; record it literally.
- **Advisory containment.** `gh issue list --state all --search <finding_id>` returns zero for each of the
  nine advisory finding IDs; record the nine results.
- **Dedup accounting.** All 11 `dedup:`-annotated records were filed and none skipped; the corpus's
  `duplicate` count of 0 still holds. List the eleven IDs with their new tracker entries.

If any set difference is non-empty or any equation fails to balance, stop and report the departure. Do not
create a missing entry as part of the reconciliation task and do not delete an extra one — either is a
decision for the developer, and a public tracker is not the place to improvise.

Commit the ledger with subject `docs(69): file the 57 low issues and close the 144-entry filing run`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && L=$D/69-FILING-LEDGER.md && M=.planning/reviews/MAJOR-REFACTORS.md && [ "$(grep -c '^finding_id:' $L)" = 144 ] && [ "$(grep -c '^route: *issue' $L)" = 135 ] && [ "$(grep -c '^route: *advisory' $L)" = 9 ] && grep -Fq '## Filing reconciliation' $L && diff <(grep '^finding_id:' $L | awk '{print $2}' | sort) <(grep '^id: ' $M | awk '{print $2}' | sort) && [ "$(grep -c '^entry:' $L)" = 144 ] && [ "$(grep '^entry:' $L | awk '{print $2}' | sort -u | wc -l)" = 144 ] && grep -Eq '144.*=.*9.*\+.*135' $L && grep -Eq '144.*=.*1.*\+.*16.*\+.*70.*\+.*57' $L && for i in P61-D1-007 P61-D2-018 P61-D4-007 P61-D5-003 P61-D5-010 P62-D1-003 P63-D4-010 P63-D7-002 P63-D7-005 P63-D7-006 P66-D4-001; do grep -Fq "$i" $L || { echo "MISSING dedup-annotated $i"; exit 1; }; done && for i in P62-D1-003 P64-D1-004 P61-D1-003 P63-D1-007 P64-D1-006 P63-D1-001 P63-D1-003 P64-D1-002 P64-D1-003; do [ "$(gh issue list --state all --limit 1000 --search "$i" --json number --jq 'length')" = 0 ] || { echo "LEAKED: $i"; exit 1; }; done && [ "$(gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq '[.[]|select(.state=="draft")]|length')" = 9 ] && git diff --quiet $M && echo FILING-RUN-COMPLETE-144</automated>
  </verify>
  <acceptance_criteria>
    - `69-FILING-LEDGER.md` holds exactly 144 rows: 9 `route: advisory` and 135 `route: issue`.
    - The `diff` between the ledger's sorted `finding_id:` set and the corpus's sorted `^id:` set is empty in both directions, and its literal output is recorded in `## Filing reconciliation`.
    - All 144 `entry:` values are distinct — no two findings map to the same tracker entry — and the supersedes pair's two distinct issue numbers are named explicitly.
    - `## Filing reconciliation` states `144 = 9 + 135` and `144 = 1 + 16 + 70 + 57`, each with its members named, and records the label-boundary count, the nine advisory-containment results and the eleven dedup-annotated IDs with their tracker entries, all with literal output.
    - `gh api .../security-advisories` reports 9 drafts, and `gh issue list --state all --search <finding_id>` returns zero for each of the nine advisory finding IDs.
    - `gh label list --limit 100` returns the phase-start label count — no label was created anywhere in the 135-issue run.
    - For three spot-checked findings in this batch, the created issue's title and body prefix match the approved draft byte-for-byte.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty — the write-back has not started.
  </acceptance_criteria>
  <done>Every one of the 144 records maps to exactly one tracker entry, the mapping is proved by set comparison against the corpus rather than by adding up plan claims, and the ledger is ready to be written back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the approved draft → the public GitHub tracker | the final 57 crossings of the one-way boundary |
| the ledger → `MAJOR-REFACTORS.md`'s `issue:` slots (plan 69-13) | the ledger becomes the authority for a `costly`-to-undo document edit, so its completeness is proved here, not assumed there |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-05 | Repudiation | a create succeeding with no ledger row, leaving a finding with no traceable entry for the write-back | high | mitigate | The row is appended immediately after each create; Task 2 proves ledger-versus-corpus set equality by `diff` and reports any missing ID by name rather than filling a gap silently |
| T-69-25 | Tampering | a blind retry producing two copies on a public tracker | high | mitigate | Retry forbidden without re-listing and title-matching; run stops on the first rejection |
| T-69-27 | Repudiation | two findings mapping to the same tracker entry, breaking ISSUE-05's one-entry-per-record property | medium | mitigate | Task 2 asserts all 144 `entry:` values distinct and states the supersedes pair's two separate numbers explicitly |
| T-69-03 | Tampering | the draft edited mid-run | high | mitigate | Both tasks carry a `<precondition>` and a verify re-deriving `draft_blob_sha:` |
| T-69-07 | Tampering | record prose containing shell metacharacters altering a `gh issue create` invocation | medium | mitigate | Bodies are `awk` slices passed with `--body-file`; titles are quoted shell variables; no record text is interpolated into a command string |
| T-69-01 | Information disclosure | an advisory-routed finding reaching the public tracker | high | mitigate | The worklists are Index rows 88-144, which contain no advisory-routed record; Task 2 asserts zero public search hits for all nine advisory finding IDs |
| T-69-06 | Denial of service | GitHub secondary rate limits mid-run | low | mitigate | Creates are paced and batched at about ten per invocation; on rejection the run stops with the finding ID and error recorded |
| T-69-23 | Tampering | an unknown `--label` value creating a fabricated taxonomy | medium | mitigate | No label is created; an unknown-label rejection stops the run and is reported as a discrepancy |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 144 ledger rows, 9 advisory and 135 issue, every `entry:` distinct.
- Ledger-versus-corpus set difference empty in both directions, proved by `diff` with literal output.
- Nine advisories still drafts, none of their finding IDs public, no label created.
- `MAJOR-REFACTORS.md` untouched — the write-back is plan `69-13`'s.
</verification>

<success_criteria>
The tracker holds 135 public issues and 9 private draft advisories, every one of the 144 documented
findings maps to exactly one of them, and that claim is a measurement a reader can re-run rather than a
sum of what twelve plans said about themselves.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-12-SUMMARY.md` when done, including the set-equality
diff output and the closing reconciliation equations.
</output>
</content>
