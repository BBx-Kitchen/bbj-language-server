---
phase: 69-github-issue-filing
plan: 05
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-05.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-05.md` holds 20 index rows and 20 delimited body blocks for filing-order rows 88-107 — the first 20 of the 57 `low` records — in filing order, all routed `public issue`."
    - "`P63-D3-005`'s three labels are `intellij`, `PRIO 3`, `2`, parsed from `proposed_labels:` — its `effort:` field carries a prose rounding annotation that is not a valid label value and is never read as one (D-09)."
    - "The 5 D1-primary records in this band are `low` and therefore route `public issue` under the two-field predicate, filed with no difference in template, labels, order or batch (D-03)."
    - "No record in this band carries a `dedup:` annotation, so no `## Traceability` section in this shard claims a relationship to an existing tracker item; the absence is recorded rather than assumed."
    - "Every body carries all five ISSUE-02 elements in the fixed six-section order, with `failure_scenario:` and `proposed_approach:` lifted byte-identically (D-11)."
    - "The concatenated delimited regions of `69-BODIES-05.md` contain zero pointers into `.planning/`, no reference to the review corpus by filename, no `RU-nn-nn` unit identifier and no `§` section pointer (D-11)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-05.md
      provides: "Rendered index rows and body blocks for filing-order rows 88-107"
      contains: "BODY-BEGIN P61-D1-009"
  key_links:
    - from: ".planning/reviews/MAJOR-REFACTORS.md `proposed_labels:`"
      to: "the `labels` column of every row in this shard"
      via: "the triad is split on `;` and used verbatim; `P63-D3-005` proves that `effort:` is never the source"
      pattern: "PRIO 3"
---

<objective>
Render filing-order rows 88-107 — the first 20 `low` records — into shard `69-BODIES-05.md`.

Purpose: the `low` block is where the label parse is most easily got wrong, because one of its records
carries a prose rounding annotation inside `effort:` that reads like a number and is not one. This band
holds it.

Output: `69-BODIES-05.md`, holding 20 index rows and 20 delimited body blocks in filing order.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-05.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 88-97 (10 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-05.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 200-209 only — Index rows 88-97
    - the ten records, each extracted with the single-record awk from `<phase_conventions>`: `P61-D1-009`, `P61-D4-011`, `P61-D4-013`, `P61-D4-014`, `P61-D4-016`, `P62-D1-002`, `P62-D1-007`, `P63-D1-002`, `P63-D1-008`, `P63-D2-001`
  </read_first>
  <action>
Create `69-BODIES-05.md` with a `## Index rows 88-107` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 88-107` heading, then render the
ten records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` (D-10); parse the three label values from
`proposed_labels:` alone (D-09) — every record in this band carries `PRIO 3`; evaluate the route
predicate from the record's own `dimension:` and `severity:` — every record here is `low`, so every
route is `public issue`, but evaluate rather than assume and report a disagreement instead of resolving
it; write the six body sections in the fixed order between the `BODY-BEGIN` / `BODY-END` markers; lift
`failure_scenario:` and `proposed_approach:` byte-identically; author `## Acceptance criteria` per D-12
and nothing beyond it.

Five records in this band are D1-primary (`P61-D1-009`, `P62-D1-002`, `P62-D1-007`, `P63-D1-002`,
`P63-D1-008`) — four of them in this task's ten. All are `low`, so all file publicly with the standard
template and no special handling (D-03). Their `## Evidence` sections state surface, problem class and
impact; a `low` severity does not license a step-by-step reproduction of an unfixed path in a public
body any more than a `medium` one does.

No record in this band carries a `dedup:` annotation — every `dedup:` value reads `none`. Confirm that
per record while extracting, and state the confirmation in the plan SUMMARY rather than silently
omitting a relationship line.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-05.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 10 ] && [ "$(grep -c 'BODY-END' $F)" = 10 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 10 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 10 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P61-D1-009 P61-D4-011 P61-D4-013 P61-D4-014 P61-D4-016 P62-D1-002 P62-D1-007 P63-D1-002 P63-D1-008 P63-D2-001; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && [ "$(grep -c 'PRIO 3' $F)" -ge 10 ] && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-05.md` contains exactly 10 `BODY-BEGIN` markers, 10 matching `BODY-END` markers and 10 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 10 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - Every `**Labels:**` line carries exactly three values including `PRIO 3`, each byte-identical to a name in `gh label list` output and traceable to that record's `proposed_labels:`.
    - No `## Traceability` section in this shard claims a relationship to an existing tracker issue, and the SUMMARY records that all ten `dedup:` values read `none`.
    - No `## Evidence` section of the four D1-primary records contains a command line, a payload, or an ordered step sequence that would reproduce the unfixed path.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 88-97 are rendered and the shard's structure is established for Task 2 to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 98-107 (10 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-05.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-BODIES-05.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 210-219 only — Index rows 98-107
    - the ten records, each extracted with the single-record awk: `P63-D2-005`, `P63-D2-006`, `P63-D2-009`, `P63-D2-014`, `P63-D3-005`, `P63-D4-002`, `P63-D8-004`, `P64-D2-002`, `P64-D2-006`, `P64-D3-003`
  </read_first>
  <action>
Append the remaining ten entries of this band to `69-BODIES-05.md` — index rows into the existing table
in filing order, body blocks into `## Bodies rows 88-107` in filing order — so the shard ends holding
all 20 rows.

Two records need specific handling:

- **`P63-D3-005`** carries prose inside `effort:` — a rounding annotation recording that the reviewer's
  original value was off the locked `{2,4,8}` scale and was rounded down. Its labels are `intellij`,
  `PRIO 3`, `2`, read from `proposed_labels:`. The rounding annotation is review bookkeeping and does
  not belong in the issue body at all; do not transcribe it into `## Traceability`, and do not read a
  label from it (D-09).
- **`P63-D8-004`** is one of the corpus's two `area=documentation` records. The `documentation` label
  exists in the repository's set and needs no special handling; it is called out only so nobody treats
  it as a missing label. Its `## Acceptance criteria` state the observable condition under which the
  documented claim matches the code, not a docs-site restructuring.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-05.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 20 ] && [ "$(grep -c 'BODY-END' $F)" = 20 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 20 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 20 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 20 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P63-D2-005 P63-D2-006 P63-D2-009 P63-D2-014 P63-D3-005 P63-D4-002 P63-D8-004 P64-D2-002 P64-D2-006 P64-D3-003; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && ! awk '/BODY-BEGIN P63-D3-005/{f=1}f{print}/BODY-END P63-D3-005/{exit}' $F | grep -Eqi 'rounded|locked \{2,4,8\}' && grep -Fq 'documentation' $F && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-05.md` contains exactly 20 `BODY-BEGIN` markers, 20 matching `BODY-END` markers and 20 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 20 times inside the delimited regions.
    - `P63-D3-005`'s index row carries exactly the three labels `intellij`, `PRIO 3`, `2`, and its delimited region contains no fragment of the prose rounding annotation from its `effort:` field.
    - `P63-D8-004`'s index row carries the `documentation` area label, and its `## Acceptance criteria` name a code-versus-docs agreement condition rather than a docs-site restructuring.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 20 records of filing-order rows 88-107 are rendered, with the prose-`effort:` record proving the label rule under the condition designed to break it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-11`) | the one-way boundary; this plan populates what will cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Information disclosure | public bodies for the 5 `low` D1-primary records in this band | high | mitigate | `## Evidence` states surface, problem class and impact only; Task 1's acceptance criteria forbid a command line, a payload or an ordered step sequence, and a `low` severity is explicitly not a licence to include one |
| T-69-15 | Tampering | a malformed label from `P63-D3-005`'s prose `effort:` value | medium | mitigate | Labels parsed from `proposed_labels:` only; Task 2 asserts the exact triad and that no fragment of the rounding annotation reaches the body or a label position |
| T-69-10 | Tampering | verbatim lift of `failure_scenario:` / `proposed_approach:` | medium | mitigate | Byte-identity asserted against the source field; the lift is a file write, never a shell interpolation |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 20 index rows and 20 delimited body blocks, all `public issue`, all carrying `PRIO 3`.
- `P63-D3-005` carries the clean triad and no rounding-annotation text.
- Region-scoped self-containedness gate returns zero hits.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
Filing-order rows 88-107 exist as approval-ready entries, and the one record whose `effort:` field would
have produced a malformed label is labelled correctly and provably so.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-05-SUMMARY.md` when done.
</output>
</content>
