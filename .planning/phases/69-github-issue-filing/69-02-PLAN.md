---
phase: 69-github-issue-filing
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-02.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-02.md` holds 23 index rows and 23 delimited body blocks for filing-order rows 18-40, in filing order, all routed `public issue`."
    - "Every body carries all five ISSUE-02 elements in the fixed six-section order established in `69-ISSUE-DRAFT.md` §`## Derivation`, with `failure_scenario:` and `proposed_approach:` lifted byte-identically (D-11)."
    - "Every entry's three labels come from `proposed_labels:` alone — `effort:` and `severity:` are never read as a label source (D-09) — and all three values exist in `gh label list` output."
    - "The two `dedup:`-annotated records in this band, `P61-D1-007` (`#485` partial-overlap) and `P61-D5-003` (`DEBT-02`), are filed with the annotation carried into the body naming what the finding adds beyond the existing item; neither is skipped (D-08)."
    - "All 8 D1-primary records in this band are `medium` and therefore route `public issue` under the two-field predicate, filed with no difference in template, labels, order or batch from any other record (D-03)."
    - "The concatenated delimited regions of `69-BODIES-02.md` contain zero pointers into `.planning/`, no reference to the review corpus by filename, and no `§` section pointer (D-11)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-02.md
      provides: "Rendered index rows and body blocks for filing-order rows 18-40"
      contains: "BODY-BEGIN P61-D1-001"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md §## Derivation"
      to: ".planning/phases/69-github-issue-filing/69-BODIES-02.md"
      via: "the shard is rendered against the contract plan 69-01 wrote, not against a re-derived one"
      pattern: "## Acceptance criteria"
---

<objective>
Render filing-order rows 18-40 — the first 23 `medium` records — into shard `69-BODIES-02.md`.

Purpose: 127 records remain after the wave-1 cut. They are rendered in six independent shards so no
executor holds more than its own band, and so a template error in one band cannot silently propagate
through a shared append target. This is the first of the six.

Output: `69-BODIES-02.md`, holding 23 index rows and 23 delimited body blocks in filing order.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting — it
carries the single-record extraction command, the route predicate, the label rule, the title rule, the
six-section body order, the verbatim-lift rule and the review-internal-reference gloss rule.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-02.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 18-29 (12 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-02.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract; follow it exactly rather than inferring one
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 130-141 only — Index rows 18-29
    - the twelve records, each extracted with the single-record awk from `<phase_conventions>`: `P61-D1-001`, `P61-D1-006`, `P61-D1-007`, `P62-D2-002`, `P63-D2-012`, `P64-D2-001`, `P64-D2-007`, `P64-D2-008`, `P64-D2-009`, `P64-D3-001`, `P64-D5-002`, `P64-D6-001`
  </read_first>
  <action>
Create `69-BODIES-02.md` with a `## Index rows 18-40` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 18-40` heading, then render the
twelve records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` where `<area>` is the bare area label from
`proposed_labels:` (D-10); parse the three label values from `proposed_labels:` alone (D-09); evaluate
the route predicate — every record in this band is `medium`, so every route is `public issue`, but
evaluate it from the record's own `dimension:` and `severity:` rather than assuming, and report a
disagreement rather than resolving it; write the six body sections in the fixed order between the
`BODY-BEGIN` / `BODY-END` markers; lift `failure_scenario:` and `proposed_approach:` byte-identically;
author `## Acceptance criteria` per D-12 — the observable condition under which the failure scenario no
longer occurs, plus whatever regression coverage the proposed approach implies, and nothing beyond it.

`P61-D1-007` carries a `dedup:` annotation naming `#485` as a partial overlap. Its `## Traceability`
section carries one line stating what this finding adds beyond `#485` (D-08). It is filed, not skipped.

Eight of this band's records are D1-primary (`P61-D1-001`, `P61-D1-006`, `P61-D1-007`, `P61-D1-002`,
`P61-D1-004`, `P61-D1-005`, `P61-D1-008`, `P62-D1-004`); four of them are in this task's twelve. All
are `medium` and file publicly with no special handling (D-03) — and their `## Evidence` sections state
surface, problem class and impact without a step-by-step reproduction of the unfixed path.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-02.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 12 ] && [ "$(grep -c 'BODY-END' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 12 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md')" = 0 ] && for i in P61-D1-001 P61-D1-006 P61-D1-007 P62-D2-002 P63-D2-012 P64-D2-001 P64-D2-007 P64-D2-008 P64-D2-009 P64-D3-001 P64-D5-002 P64-D6-001; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && printf '%s' "$B" | grep -Fq '#485' && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-02.md` contains exactly 12 `BODY-BEGIN` markers, 12 matching `BODY-END` markers and 12 index rows, all carrying route `public issue`.
    - Each of the six body section headings appears exactly 12 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md` or `INVENTORY.md`.
    - `P61-D1-007`'s `## Traceability` section names `#485` and states what the finding adds beyond it.
    - Every entry's `**Labels:**` line carries exactly three values, each byte-identical to a name in `gh label list` output, and each traceable to that record's `proposed_labels:` value.
    - Every `## Failure scenario` and `## Proposed approach` section is byte-identical to its source field apart from an appended bracketed gloss.
    - No `## Acceptance criteria` section contains a date, a milestone name, an owner, or a sentence committing anyone to do the work.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 18-29 are rendered under the same contract as the wave-1 shard, and the shard's structure is established for Task 2 to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 30-40 (11 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-02.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-BODIES-02.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 142-152 only — Index rows 30-40
    - the eleven records, each extracted with the single-record awk: `P64-D6-011`, `P66-D2-003`, `P61-D1-002`, `P61-D1-004`, `P61-D1-005`, `P61-D1-008`, `P61-D2-007`, `P61-D5-003`, `P62-D1-004`, `P62-D2-001`, `P62-D2-003`
  </read_first>
  <action>
Append the remaining eleven entries of this band to `69-BODIES-02.md` — index rows into the existing
table in filing order, body blocks into `## Bodies rows 18-40` in filing order — so the shard ends
holding all 23 rows of the band.

Two records need specific handling:

- **`P61-D1-004`** — its lifted text carries a review-internal reference. Lift verbatim and append the
  bracketed gloss naming the referenced review unit in terms an outside reader can use, per the gloss
  rule in `69-ISSUE-DRAFT.md` §`## Derivation`. Do not delete the reference and do not leave it
  dangling.
- **`P61-D5-003`** — its `dedup:` names `DEBT-02`, an internal debt item rather than a tracker issue.
  Its `## Traceability` line states the relationship in outside-reader terms: name what the finding
  adds and what the related work is, without pointing at a planning document or using the `DEBT-02`
  identifier as if a reader could look it up.

The four remaining D1-primary records of this band (`P61-D1-002`, `P61-D1-004`, `P61-D1-005`,
`P61-D1-008`) plus `P62-D1-004` are all `medium` and file publicly with the same template as everything
else, with their `## Evidence` sections stating surface, problem class and impact only.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-02.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 23 ] && [ "$(grep -c 'BODY-END' $F)" = 23 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 23 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 23 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 23 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P64-D6-011 P66-D2-003 P61-D1-002 P61-D1-004 P61-D1-005 P61-D1-008 P61-D2-007 P61-D5-003 P62-D1-004 P62-D2-001 P62-D2-003; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-02.md` contains exactly 23 `BODY-BEGIN` markers, 23 matching `BODY-END` markers and 23 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 23 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` unit identifier, or a `§` section pointer — every review-internal reference has been glossed away or replaced by outside-reader terms.
    - `P61-D1-004`'s body contains the verbatim source sentence plus a bracketed gloss naming the referenced layer in terms of real source files.
    - `P61-D5-003`'s `## Traceability` line describes the related work without using an internal debt identifier as a lookup key.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 23 records of filing-order rows 18-40 are rendered and the shard is complete and ready for assembly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-10`) | the one-way boundary; this plan populates what will cross it and crosses nothing itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Information disclosure | public bodies for the 8 D1-primary records in this band | high | mitigate | `## Evidence` states surface, problem class and impact only; the exploit-recipe prohibition carried from `69-01`; the acceptance criteria forbid a step-by-step reproduction of an unfixed path |
| T-69-10 | Tampering | verbatim lift of `failure_scenario:` / `proposed_approach:` | medium | mitigate | Byte-identity asserted against the source field; the lift is a file write, never a shell interpolation, because these values contain backticks, `$`, `"` and `\` |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-13 | Information disclosure | review-internal references leaking planning-document structure into a public body | low | mitigate | Task 2's acceptance criteria negative-grep the delimited regions for `RU-nn-nn` and `§` and require zero hits |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 23 index rows and 23 delimited body blocks in `69-BODIES-02.md`, all `public issue`.
- Six section headings, 23 occurrences each, inside the delimited regions.
- Region-scoped self-containedness gate returns zero hits.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
Filing-order rows 18-40 exist as approval-ready entries indistinguishable in shape from the wave-1
shard, and plan `69-08` can concatenate this shard without touching its contents.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-02-SUMMARY.md` when done.
</output>
</content>
