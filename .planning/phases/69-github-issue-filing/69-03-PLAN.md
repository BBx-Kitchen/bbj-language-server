---
phase: 69-github-issue-filing
plan: 03
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-03.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-03.md` holds 24 index rows and 24 delimited body blocks for filing-order rows 41-64, in filing order, all routed `public issue`."
    - "The `supersedes` pair is rendered as two entries, not one and not two silent ones: `P63-D4-010` (row 53) and `P66-D4-001` (row 63) are both present, and each body's `## Traceability` section names the other finding and states the direction of the relationship — exactly as recorded in `69-ISSUE-DRAFT.md` §`## Routing decisions` (ISSUE-02 / edge: adjacency)."
    - "`P66-D4-001`'s three labels are `intellij`, `PRIO 2`, `4`, parsed from `proposed_labels:` — its `effort:` field carries a prose no-departure note that is not a valid label value and is never read as one (D-09)."
    - "Every body carries all five ISSUE-02 elements in the fixed six-section order, with `failure_scenario:` and `proposed_approach:` lifted byte-identically (D-11)."
    - "All 6 D1-primary records in this band are `medium` and therefore route `public issue` under the two-field predicate, filed with no difference in template, labels, order or batch (D-03)."
    - "The concatenated delimited regions of `69-BODIES-03.md` contain zero pointers into `.planning/`, no reference to the review corpus by filename, no `RU-nn-nn` unit identifier and no `§` section pointer (D-11)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-03.md
      provides: "Rendered index rows and body blocks for filing-order rows 41-64, including both halves of the supersedes pair"
      contains: "BODY-BEGIN P66-D4-001"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md §## Routing decisions"
      to: "the `## Traceability` sections of `P63-D4-010` and `P66-D4-001`"
      via: "the recorded supersedes routing decision is what those two bodies state; neither body invents its own version of the relationship"
      pattern: "supersede"
---

<objective>
Render filing-order rows 41-64 — 24 `medium` records including both halves of the only `supersedes`
pair in the corpus — into shard `69-BODIES-03.md`.

Purpose: this band carries the one relationship in the 144 that could put two tracker entries on one
item without a reader being able to tell. `69-01` settled how that is expressed; this plan is where the
settlement becomes two actual bodies.

Output: `69-BODIES-03.md`, holding 24 index rows and 24 delimited body blocks in filing order.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-03.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 41-52 (12 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-03.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 153-164 only — Index rows 41-52
    - the twelve records, each extracted with the single-record awk from `<phase_conventions>`: `P62-D2-005`, `P62-D4-001`, `P62-D4-004`, `P63-D1-004`, `P63-D1-005`, `P63-D2-003`, `P63-D2-007`, `P63-D2-013`, `P63-D2-016`, `P63-D3-001`, `P63-D3-002`, `P63-D3-007`
  </read_first>
  <action>
Create `69-BODIES-03.md` with a `## Index rows 41-64` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 41-64` heading, then render the
twelve records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` (D-10); parse the three label values from
`proposed_labels:` alone (D-09); evaluate the route predicate from the record's own `dimension:` and
`severity:` — every record in this band is `medium`, so every route is `public issue`, but evaluate it
rather than assume it and report a disagreement instead of resolving it; write the six body sections in
the fixed order between the `BODY-BEGIN` / `BODY-END` markers; lift `failure_scenario:` and
`proposed_approach:` byte-identically; author `## Acceptance criteria` per D-12 and nothing beyond it.

`P63-D1-004` and `P63-D1-005` are D1-primary at `medium` severity: they file publicly with the same
template as every other record (D-03), and their `## Evidence` sections state surface, problem class
and impact without a step-by-step reproduction of the unfixed path.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-03.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 12 ] && [ "$(grep -c 'BODY-END' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 12 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 12 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P62-D2-005 P62-D4-001 P62-D4-004 P63-D1-004 P63-D1-005 P63-D2-003 P63-D2-007 P63-D2-013 P63-D2-016 P63-D3-001 P63-D3-002 P63-D3-007; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-03.md` contains exactly 12 `BODY-BEGIN` markers, 12 matching `BODY-END` markers and 12 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 12 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - Every `**Labels:**` line carries exactly three values, each byte-identical to a name in `gh label list` output and traceable to that record's `proposed_labels:`.
    - Every `## Failure scenario` and `## Proposed approach` section is byte-identical to its source field apart from an appended bracketed gloss.
    - No `## Acceptance criteria` section contains a date, a milestone name, an owner, or a sentence committing anyone to do the work.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 41-52 are rendered and the shard's structure is established for Task 2 to extend.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 53-64, including both halves of the supersedes pair</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-03.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Routing decisions` entry 2 — the settled supersedes routing, which is what the two bodies must state
    - `.planning/phases/69-github-issue-filing/69-BODIES-03.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 165-176 only — Index rows 53-64
    - the twelve records, each extracted with the single-record awk: `P63-D4-010`, `P63-D6-001`, `P64-D1-001`, `P64-D1-005`, `P64-D2-003`, `P64-D3-002`, `P64-D6-003`, `P64-D6-005`, `P65-D1-002`, `P65-D1-003`, `P66-D4-001`, `P66-D5-001`
  </read_first>
  <action>
Append the remaining twelve entries of this band to `69-BODIES-03.md` — index rows into the existing
table in filing order, body blocks into `## Bodies rows 41-64` in filing order — so the shard ends
holding all 24 rows.

Four records need specific handling:

- **`P63-D4-010` (row 53) and `P66-D4-001` (row 63)** — the corpus's only `supersedes` pair. Both are
  filed as separate public issues, per the settled routing decision. Row 53's `## Traceability` section
  states that a later finding supersedes this one, names that finding's ID, and states what remains
  true of row 53 on its own. Row 63's `## Traceability` section names the finding it supersedes and
  states what it adds beyond it. Neither body may be written as if the other did not exist, and neither
  finding may be dropped — the corpus's `duplicate` count is 0 and no finding is skipped as a duplicate
  anywhere in this phase (D-08).
- **`P66-D4-001`** additionally carries prose inside its `effort:` field. Its labels are `intellij`,
  `PRIO 2`, `4`, read from `proposed_labels:` — reading the label from `effort:` would produce a
  malformed value (D-09).
- **`P64-D1-005`** is D1-primary at `medium` and carries **no** redaction limit — the two-tier
  redaction instruction attaches to `P64-D1-004` alone, which is not in this band. Render
  `P64-D1-005`'s evidence normally; do not over-redact it by association.
- **`P64-D3-002`** and **`P64-D6-005`** carry review-internal references in their lifted text. Lift
  verbatim and append the bracketed gloss naming the referenced thing in outside-reader terms.

The remaining D1-primary records of this band (`P64-D1-001`, `P65-D1-002`, `P65-D1-003`) are `medium`
and file publicly with the standard template, evidence limited to surface, problem class and impact.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-03.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 24 ] && [ "$(grep -c 'BODY-END' $F)" = 24 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 24 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 24 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 24 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P63-D4-010 P63-D6-001 P64-D1-001 P64-D1-005 P64-D2-003 P64-D3-002 P64-D6-003 P64-D6-005 P65-D1-002 P65-D1-003 P66-D4-001 P66-D5-001; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && awk '/BODY-BEGIN P63-D4-010/{f=1}f{print}/BODY-END P63-D4-010/{exit}' $F | grep -Fq 'P66-D4-001' && awk '/BODY-BEGIN P66-D4-001/{f=1}f{print}/BODY-END P66-D4-001/{exit}' $F | grep -Fq 'P63-D4-010' && awk -F'|' '/^\| *63 *\|/{print $6}' $F | grep -Eq 'intellij' && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-03.md` contains exactly 24 `BODY-BEGIN` markers, 24 matching `BODY-END` markers and 24 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 24 times inside the delimited regions.
    - `P63-D4-010`'s delimited region contains the string `P66-D4-001` and a sentence stating that a later finding supersedes it and what remains true of it.
    - `P66-D4-001`'s delimited region contains the string `P63-D4-010` and a sentence stating what it supersedes and what it adds.
    - `P66-D4-001`'s index row carries exactly the three labels `intellij`, `PRIO 2`, `4`, and no fragment of its prose `effort:` value appears in any label position.
    - `P64-D1-005`'s `## Evidence` section is rendered at the same detail level as the other `medium` D1 records in this band — it is not truncated to a bare surface statement.
    - `P64-D3-002` and `P64-D6-005` each contain the verbatim source sentence plus a bracketed gloss; the concatenated delimited regions contain zero `RU-nn-nn` identifiers and zero `§` pointers.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 24 records of filing-order rows 41-64 are rendered, and a triager reading either half of the supersedes pair can see the relationship without opening anything else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-10`) | the one-way boundary; this plan populates what will cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Information disclosure | public bodies for the 6 D1-primary records in this band | high | mitigate | `## Evidence` states surface, problem class and impact only; the exploit-recipe prohibition carried from `69-01`; acceptance criteria forbid a step-by-step reproduction of an unfixed path |
| T-69-14 | Repudiation | the supersedes pair filed without the relationship stated, leaving a triager two overlapping issues and no way to tell | medium | mitigate | Task 2's acceptance criteria require each half's delimited region to contain the other's finding ID and a direction-stating sentence, verified by a per-region grep |
| T-69-10 | Tampering | verbatim lift of `failure_scenario:` / `proposed_approach:` | medium | mitigate | Byte-identity asserted against the source field; the lift is a file write, never a shell interpolation |
| T-69-15 | Tampering | a malformed label from `P66-D4-001`'s prose `effort:` value | medium | mitigate | Labels are parsed from `proposed_labels:` only; the acceptance criteria assert the exact triad and that no fragment of the prose `effort:` value appears in a label position |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 24 index rows and 24 delimited body blocks, all `public issue`.
- Both halves of the supersedes pair name each other inside their own delimited regions.
- `P66-D4-001` carries the clean triad from `proposed_labels:`.
- Region-scoped self-containedness gate returns zero hits.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
Filing-order rows 41-64 exist as approval-ready entries, and the corpus's only supersedes relationship
is visible from either issue without opening a review document.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-03-SUMMARY.md` when done.
</output>
</content>
