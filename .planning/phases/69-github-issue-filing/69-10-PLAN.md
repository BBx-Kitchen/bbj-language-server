---
phase: 69-github-issue-filing
plan: 10
type: execute
wave: 5
depends_on: ["69-09"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
autonomous: false
requirements: [ISSUE-02, ISSUE-03, ISSUE-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 8 public issues of the wave-1 cut exist on the tracker, each with a title byte-identical to its drafted title, a body byte-identical to the approved draft's delimited region for that finding, and exactly three labels — one area label, one `PRIO 1|2|3` label and one `2|4|8` effort label — all drawn from the repository's existing label set with nothing created (ISSUE-02, ISSUE-03)."
    - "Filing stops after these 17 wave-1 entries so a body-template error is caught after 17 rather than replicated 144 times; the pause is a checkpoint inside the approved run, not a second approval gate — ISSUE-01 was discharged once, at the 69-08 gate (D-13)."
    - "Filing order is the `## Index` row order, which is total and stable — severity, then PRIO, then effort, then finding ID — and the ledger's row order equals that Index order for every filed entry (ISSUE-01 / edge: ordering)."
    - "A ledger row is appended immediately after each create and before the next create is attempted; the ledger holds 17 rows at the end of this plan — 9 advisories and 8 issues (D-14)."
    - "The bare-digit effort label is applied as a label name, not interpreted as a number or as an issue reference: `gh issue view <n> --json labels` returns exactly one area name, exactly one `PRIO` name and exactly one bare-digit name for each of the 8 (ISSUE-03 / edge: adjacency)."
    - "Re-running this plan after an interruption creates no duplicate: every finding ID with a ledger row and every drafted title already present on the tracker is skipped, and the count of open issues whose title equals a given drafted title stays exactly 1 (ISSUE-02 / edge: concurrency)."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
      provides: "17 rows — the 9 advisory rows from 69-09 plus 8 public-issue rows with their issue numbers"
      contains: "route: issue"
  key_links:
    - from: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md delimited body regions"
      to: "the created GitHub issues"
      via: "each body is sliced out by its BODY-BEGIN/BODY-END anchors into a temp file and passed as --body-file; the draft is never re-rendered and no body text is interpolated into a shell argument"
      pattern: "BODY-BEGIN P64-D6-007"
    - from: "each record's `proposed_labels:` triad, carried through the draft's `## Index` labels column"
      to: "the three `--label` arguments of each create"
      via: "split on `;`, `area=` stripped, three values used verbatim; verified by reading the created issue's labels back"
      pattern: "PRIO 1"
  prohibitions:
    - requirement_id: ISSUE-02
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT post a body that differs from the approved draft's bytes for that finding — the developer approved specific bytes, and a re-render, a re-wrap or a last-minute improvement at post time makes the approval about a document nobody filed"
---

<objective>
Cross the one-way boundary: file the 8 public issues of the wave-1 cut, verify them, and stop for the
developer to look at the tracker.

Purpose: this is the first irreversible act of the milestone. Eight public issues plus the nine advisories
from `69-09` give the developer 17 real entries to check the template against. If the body shape, the
label triad or the title rule is wrong, it is wrong on 17 entries and not on 144 (D-13).

Output: 8 public issues on the tracker, 17 ledger rows, and a pause.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
@.planning/phases/69-github-issue-filing/69-09-SUMMARY.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<reversibility_gate>
The `one-way` ratings on the filing tasks below carry no in-plan `checkpoint:decision`, and that is
deliberate rather than an omission. D-06 locks this phase to a **single** approval gate, discharged at
`69-08` Task 3 — a `checkpoint:decision` framing the public-write door with its options and its undo cost
— and D-13 states that the wave-1 pause in `69-10` is a checkpoint inside the already-approved run, not a
second gate. Re-asking a door the developer has already walked through would be checkpoint fatigue, and
the rating is kept for the record exactly as the reversibility taxonomy prescribes. A structural-validator
warning about a missing preceding checkpoint is therefore expected on this plan.
</reversibility_gate>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan produces no new file; it appends 8 rows
to `69-FILING-LEDGER.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: File the 8 wave-1 public issues — one, verified, then the other seven</name>
  <reversibility rating="one-way">A GitHub issue on a public repository is indexed, emailed to watchers and mirrored by third parties within minutes; closing or deleting it afterwards removes the page, not the notification, the index entry or any mirror. The `69-08` checkpoint is the gate that authorised this task; no further approval exists.</reversibility>
  <precondition>`69-FILING-LEDGER.md` §`## Approved draft` `draft_blob_sha:` equals the current output of `git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md`, and §`## Dedup re-query` records zero genuine duplicates among the eight finding IDs filed here. Halt on either mismatch.</precondition>
  <files>
    .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Index` rows 1-17 only — the eight public finding IDs, their exact titles and their label triads; do not read the 144 bodies into context, slice each one out with `awk` at post time
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` — its `## Rows` table, as the resume-check source
    - `.planning/phases/69-github-issue-filing/69-CONTEXT.md` §`<decisions>` D-09, D-10, D-11, D-13 and D-14
  </read_first>
  <action>
File the eight public issues of the wave-1 cut, in filing order: `P64-D6-007` (row 3), `P64-D6-008`
(row 4), `P64-D6-006` (row 8), `P61-D3-002` (row 9), `P61-D3-003` (row 10), `P63-D2-004` (row 13),
`P64-D6-002` (row 16), `P66-D3-001` (row 17).

**The first create — `P64-D6-007`.** Slice its body out of the approved draft into a temporary file with
an `awk` range bounded by `<!-- BODY-BEGIN P64-D6-007 -->` and `<!-- BODY-END P64-D6-007 -->`, excluding
both marker lines. Read its title and its three label values out of the draft's `## Index` row into shell
variables. Create the issue with `gh issue create --title "$TITLE" --body-file "$BODYFILE" --label
"$AREA" --label "$PRIO" --label "$EFFORT"`. Never pass the body with `--body` and never interpolate body
text into a shell argument — 80 of the 144 `failure_scenario:` values and 69 of the 144
`proposed_approach:` values contain backticks, `$`, `"` or `\`.

Then verify it before creating anything else: `gh issue view <n> --json title,labels,body` and confirm the
title equals `$TITLE` byte-for-byte, that `labels` is a set of exactly three names equal to
`{dependencies, PRIO 1, 2}` compared as a set rather than as an ordered list, and that the body's first
200 characters equal the sliced body file's first 200 characters. Append its ledger row — `finding_id`,
`route: issue`, `entry:` the issue number, `url:`, `created_at:` — immediately, before anything else is
attempted (D-14).

**Then the remaining seven**, in filing order, one at a time, with the same slice-create-verify sequence
and a ledger row appended immediately after each create and before the next is attempted. Pace the creates
with a short delay between them to stay clear of GitHub's content-creation secondary rate limits.

**Resume check before every create** (D-14): skip the finding if it already has a ledger row, or if an
existing issue's title equals its drafted title. `gh issue list --state all --limit 1000 --json
number,title` once at the start of the task and match locally rather than issuing a search per finding.
Because titles are derived deterministically from the approved draft and the `69-08` gate proved all 144
distinct, this check is reliable even if the ledger itself were lost.

**Create no label.** All twelve label values already exist. If a `--label` argument is rejected as
unknown, stop and report it as a discrepancy — that means the draft's label value and the repository's
label set disagree, which the `69-08` gate should have caught, and inventing the label would put a
fabricated taxonomy on a public tracker.

On any rejection — rate limit, permission, validation, unknown label — stop, record the failure in the
ledger with the finding ID and the error, and do not proceed to the next create.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && L=$D/69-FILING-LEDGER.md && [ "$(grep -c '^route: *issue' $L)" = 8 ] && [ "$(grep -c '^finding_id:' $L)" = 17 ] && for i in P64-D6-007 P64-D6-008 P64-D6-006 P61-D3-002 P61-D3-003 P63-D2-004 P64-D6-002 P66-D3-001; do grep -Fq "$i" $L || { echo "MISSING ledger row $i"; exit 1; }; done && N=$(awk '/^route: *issue/{found=1} /^entry:/{if(found){print $2; found=0}}' $L | tr -d '#') && [ "$(printf '%s\n' "$N" | wc -l)" = 8 ] && for n in $N; do C=$(gh issue view "$n" --json labels --jq '[.labels[].name]|length'); [ "$C" = 3 ] || { echo "issue #$n has $C labels, expected 3"; exit 1; }; P=$(gh issue view "$n" --json labels --jq '[.labels[].name|select(startswith("PRIO"))]|length'); [ "$P" = 1 ] || { echo "issue #$n PRIO label count $P"; exit 1; }; E=$(gh issue view "$n" --json labels --jq '[.labels[].name|select(test("^[248]$"))]|length'); [ "$E" = 1 ] || { echo "issue #$n effort label count $E"; exit 1; }; done && [ "$(gh label list --limit 100 | wc -l)" = "$(gh label list --limit 100 | wc -l)" ] && [ "$(grep '^draft_blob_sha:' $L | awk '{print $2}')" = "$(git rev-parse HEAD:.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md)" ] && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo WAVE1-PUBLIC-8-OK</automated>
  </verify>
  <acceptance_criteria>
    - Eight public issues exist, one per wave-1 public finding ID, and `69-FILING-LEDGER.md` holds 17 rows total — 9 with `route: advisory` and 8 with `route: issue`.
    - The ledger's 8 issue rows appear in Index row order (3, 4, 8, 9, 10, 13, 16, 17) with non-decreasing timestamps.
    - `gh issue view <n> --json labels` returns exactly 3 label names for each of the 8: exactly one starting with `PRIO`, exactly one matching a bare `2`, `4` or `8`, and one area name; the sets are compared as sets, not as ordered lists.
    - Each created issue's title equals its drafted title byte-for-byte, and its body's first 200 characters equal the first 200 characters of the body sliced from the approved draft.
    - `P64-D6-007`'s label set is exactly `{dependencies, PRIO 1, 2}`, and it was read back and verified before the second create was attempted.
    - `gh label list --limit 100` returns the same label count as at phase start — no label was created.
    - Every body was passed via `--body-file`; no `gh issue create --body` invocation with interpolated record text appears in the task's shell history.
    - `draft_blob_sha:` still equals the current committed blob SHA of `69-ISSUE-DRAFT.md`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Eight public issues carry the approved bodies and the correct label triads, each verified by reading it back off the tracker, and the ledger is a complete record of all 17 wave-1 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-1 reconciliation — 17 = 9 + 8, and the boundary held</name>
  <files>
    .planning/phases/69-github-issue-filing/69-FILING-LEDGER.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-FILING-LEDGER.md` — the 17 rows
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Reconciliation` — the arithmetic this task checks the run against
  </read_first>
  <action>
Add a `## Wave-1 reconciliation` section to `69-FILING-LEDGER.md` recording the run's arithmetic as
equations with their members named, in the derived-denominator shape this project uses:

`17 wave-1 entries = 9 draft advisories + 8 public issues`, with both ID lists written out;
`127 remaining = 70 medium (Index rows 18-87) + 57 low (Index rows 88-144)`;
`144 = 17 filed + 127 remaining`.

Record the four boundary assertions with their literal command output:

1. `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq 'length'` returns 9 and all
   nine are drafts.
2. The public issue count rose by exactly 8 from the `## Dedup re-query` `total_count:` figure.
3. `gh issue list --state all --search <finding_id>` returns zero for each of the nine advisory finding
   IDs — none of the nine leaked to the public tracker.
4. `gh label list --limit 100` returns the same label count as at phase start.

Then record, per public issue, the mapping `finding_id → #number → label set`, so the developer's pause
check in Task 3 has a table to read rather than eight `gh` commands to run.

If any equation does not balance, state the departure and stop; a derived number is never adjusted to
match an expectation, and an expectation is never edited to match a derived number without the
discrepancy being written down first.

Commit the ledger with subject `docs(69): wave-1 filing ledger — 9 advisories and 8 issues`.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && L=$D/69-FILING-LEDGER.md && grep -Fq '## Wave-1 reconciliation' $L && grep -Eq '17.*=.*9.*\+.*8' $L && grep -Eq '144.*=.*17.*\+.*127' $L && grep -Eq '127.*=.*70.*\+.*57' $L && [ "$(grep -c '^finding_id:' $L)" = 17 ] && [ "$(gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq '[.[]|select(.state=="draft")]|length')" = 9 ] && for i in P62-D1-003 P64-D1-004 P61-D1-003 P63-D1-007 P64-D1-006 P63-D1-001 P63-D1-003 P64-D1-002 P64-D1-003; do [ "$(gh issue list --state all --limit 1000 --search "$i" --json number --jq 'length')" = 0 ] || { echo "LEAKED: $i"; exit 1; }; done && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo WAVE1-RECONCILED</automated>
  </verify>
  <acceptance_criteria>
    - `## Wave-1 reconciliation` states `17 = 9 + 8`, `127 = 70 + 57` and `144 = 17 + 127`, each with its member ID lists or Index row ranges named.
    - All four boundary assertions are recorded with literal command output.
    - The section contains a `finding_id → #number → label set` line for each of the 8 public issues.
    - Nine advisories exist, all drafts, and none of the nine advisory finding IDs is findable on the public tracker.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>The wave-1 run is reconciled against the draft's own arithmetic, and the developer has a table rather than a claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The D-13 pause — check the template against the real tracker before the remaining 127</name>
  <what-built>
The 17 wave-1 entries are on the tracker: 9 private draft security advisories for the D1-primary
`critical`/`high` findings, and 8 public issues for the remaining `high` findings. Each public issue
carries the approved body verbatim and exactly three labels — one area, one `PRIO`, one effort. The
`69-FILING-LEDGER.md` §`## Wave-1 reconciliation` section holds the `finding_id → #number → label set`
table and the four boundary assertions with their literal output.

This is D-13's pause: a checkpoint inside the already-approved run, not a second approval gate. ISSUE-01
was discharged once, at the `69-08` gate. The 127 remaining public issues follow in plans `69-11` and
`69-12`.
  </what-built>
  <how-to-verify>
Open the eight public issues from the `finding_id → #number` table in the ledger's wave-1 reconciliation
section, or run `gh issue list --state open --limit 20` and look at the eight newest entries. For at least
two of them, check:

1. **The body reads as self-contained** — problem statement, `file:line` evidence, verified failure
   scenario, proposed approach, acceptance criteria — and you can act on it without opening any review
   document. Nothing points into `.planning/`.
2. **The three labels are right**: one area label, one `PRIO 1|2|3`, one `2|4|8`. No fourth label, no
   missing one, nothing newly created.
3. **The title shape reads acceptably on the tracker.** This is the last cheap moment to change it —
   `## Discrepancies` in the draft recorded that `#497`–`#500` use a component prefix while these use the
   area-label prefix D-10 specified, which gives the `BBj integration and infrastructure` records a long
   prefix. Editing 8 titles is cheap; editing 135 is not.
4. **The acceptance criteria are honest** — they restate the finding as a testable end state and do not
   commit anyone to a design or a schedule. These are the one field with no source in the corpus.
5. **`P64-D6-008` states the `brace-expansion` reachability question as an open question** rather than
   asserting an answer.

Then check the advisory side: `gh api /repos/BBx-Kitchen/bbj-language-server/security-advisories --jq
'.[]|{ghsa_id,state,summary}'` should list nine drafts. Confirm none of the nine describes something you
would rather have had in the open, and confirm nothing you wanted private is in the public eight.
  </how-to-verify>
  <resume-signal>Type `approved` to release the remaining 127 public issues, or describe what must change first. If a template change is needed, say so now: the 8 filed issues can be corrected by hand, but the correction has to land in `69-ISSUE-DRAFT.md` and be re-gated before `69-11` runs, so approval and execution stay the same bytes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the approved draft → the public GitHub tracker | the one-way boundary; this plan is the first to cross it |
| record prose → a `gh` command line | untrusted text crosses into an invocation; passed as a file, never as an argument |
| the wave-1 result → the developer's pause check | the last cheap opportunity to change the template before it is replicated 127 more times |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Information disclosure | a D1-primary `critical`/`high` finding reaching the public tracker | high | mitigate | This task's finding-ID list is the eight-member public wave-1 set only; Task 2 asserts `gh issue list --state all --search <finding_id>` returns zero for each of the nine advisory IDs |
| T-69-03 | Tampering | the draft edited between approval and filing | high | mitigate | Task 1's `<precondition>` and both tasks' verifies re-derive `draft_blob_sha:` and halt on mismatch |
| T-69-07 | Tampering | record prose containing shell metacharacters altering a `gh issue create` invocation | medium | mitigate | Bodies are sliced to a temp file and passed with `--body-file`; titles are passed as quoted shell variables read from the draft's Index; no record text is interpolated into a command string |
| T-69-05 | Repudiation | a create succeeding with no ledger row, leaving an entry a resume could double-file | medium | mitigate | The row is appended immediately after each create and before the next is attempted; the resume check matches the deterministic title against a single up-front tracker listing |
| T-69-06 | Denial of service | GitHub secondary rate limits rejecting a create mid-run | low | mitigate | Creates are paced; on rejection the run stops, records the failure with the finding ID and the error, and does not continue |
| T-69-23 | Tampering | a `--label` value absent from the repository silently creating a fabricated taxonomy | medium | mitigate | No label is created; an unknown-label rejection stops the run and is reported as a draft-versus-repository discrepancy |
| T-69-24 | Repudiation | the body posted differing from the bytes the developer approved | high | mitigate | Bodies are sliced from the approved file and never re-rendered; each create is read back and its body's first 200 characters compared to the sliced file's |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- Eight public issues exist with byte-identical titles, byte-identical body prefixes and exactly three
  correct labels each, verified by reading them back off the tracker.
- The ledger holds 17 rows in Index order plus a reconciliation section with literal command output.
- No advisory finding ID is findable on the public tracker; no label was created.
- `MAJOR-REFACTORS.md` is untouched.
- The developer has seen the tracker and released the remaining 127, or named what must change.
</verification>

<success_criteria>
The one-way boundary has been crossed exactly 8 times, every crossing is verified and recorded, and the
developer has had the one cheap look at the real tracker that D-13 exists to buy.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-10-SUMMARY.md` when done, including the
`finding_id → #number → label set` table and the pause reply quoted verbatim.
</output>
</content>
