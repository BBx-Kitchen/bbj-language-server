---
phase: 69-github-issue-filing
plan: 07
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/phases/69-github-issue-filing/69-BODIES-07.md
autonomous: true
requirements: [ISSUE-02, ISSUE-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`69-BODIES-07.md` holds 19 index rows and 19 delimited body blocks for filing-order rows 126-144 — the final band, closing the corpus at 144 — in filing order, all routed `public issue`."
    - "The four `dedup:`-annotated records in this band — `P61-D4-007` (`#466`), `P63-D7-002` (`#65`), `P63-D7-005` (`#475`) and `P63-D7-006` (`#65`) — are each filed with the annotation carried into the body naming what that finding adds beyond the existing issue; none is skipped (D-08)."
    - "`P63-D7-002` and `P63-D7-006` both partially overlap the same existing issue `#65`, and each body states which distinct part of `#65`'s scope its own finding covers, so a triager reading both sees two non-identical additions rather than two copies."
    - "The 2 D1-primary records in this band are `low` and therefore route `public issue` under the two-field predicate, filed with no difference in template, labels, order or batch (D-03)."
    - "The five records in this band whose lifted text carries a review-internal reference — `P63-D4-008`, `P62-D1-005`, `P62-D5-003`, `P63-D7-003`, `P63-D7-006` — keep the verbatim sentence and gain a bracketed gloss naming the referenced thing in outside-reader terms."
    - "Across the seven shards the rendered entry count is exactly 144 with no gap and no overlap: 17 + 23 + 24 + 23 + 20 + 18 + 19 = 144."
  artifacts:
    - path: .planning/phases/69-github-issue-filing/69-BODIES-07.md
      provides: "Rendered index rows and body blocks for filing-order rows 126-144, closing the 144-record denominator"
      contains: "BODY-BEGIN P64-D4-001"
  key_links:
    - from: "the seven shard files 69-BODIES-01.md … 69-BODIES-07.md"
      to: ".planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md"
      via: "plan 69-08 concatenates them in shard order, which is filing order, into the single approval artifact D-05 requires"
      pattern: "BODY-BEGIN"
---

<objective>
Render filing-order rows 126-144 — the final 19 `low` records — into shard `69-BODIES-07.md`, closing
the 144-record denominator.

Purpose: this band carries four of the eleven dedup-annotated records, including two that overlap the
same existing issue from different directions. Getting those two right is what stops the tracker from
gaining two entries that read as duplicates of each other.

Output: `69-BODIES-07.md`, holding 19 index rows and 19 delimited body blocks in filing order, after
which all 144 records are rendered.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-github-issue-filing/69-CONTEXT.md
@.planning/phases/69-github-issue-filing/69-01-PLAN.md
</context>

<phase_conventions>
This plan is bound by `69-01-PLAN.md` §`<phase_conventions>`. Read that block before starting.
</phase_conventions>

<artifacts_this_phase_produces>
See `69-01-PLAN.md` §`<artifacts_this_phase_produces>`. This plan adds one file:
`.planning/phases/69-github-issue-filing/69-BODIES-07.md`.
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: .planning/ -->
<!-- planner-discipline-allow: MAJOR-REFACTORS -->

<tasks>

<task type="auto">
  <name>Task 1: Render filing-order rows 126-135 (10 records)</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-07.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-ISSUE-DRAFT.md` §`## Derivation` and §`## Routing decisions` — the rendering contract, including the gloss rule and the D-08 annotation-carry rule
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` — its first three body blocks only, as the shape to match
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 238-247 only — Index rows 126-135
    - the ten records, each extracted with the single-record awk from `<phase_conventions>`: `P63-D4-006`, `P63-D4-008`, `P63-D4-009`, `P63-D4-011`, `P63-D4-012`, `P63-D4-013`, `P65-D1-001`, `P61-D4-002`, `P61-D4-007`, `P62-D1-005`
  </read_first>
  <action>
Create `69-BODIES-07.md` with a `## Index rows 126-144` heading holding a five-column table
(`| # | finding_id | route | title | labels |`) and a `## Bodies rows 126-144` heading, then render the
ten records above into both, in filing order.

Per record: derive the title as `<area>: <problem>` (D-10); parse the three label values from
`proposed_labels:` alone (D-09); evaluate the route predicate from the record's own `dimension:` and
`severity:` — every record here is `low`, so every route is `public issue`, but evaluate rather than
assume and report a disagreement instead of resolving it; write the six body sections in the fixed order
between the `BODY-BEGIN` / `BODY-END` markers; lift `failure_scenario:` and `proposed_approach:`
byte-identically; author `## Acceptance criteria` per D-12 and nothing beyond it.

Three records need specific handling:

- **`P61-D4-007`** — `dedup:` names `#466` as a partial overlap and states that this finding's subject
  (a class's responsibility count) does not duplicate `#466`'s request. Its `## Traceability` section
  carries one line stating what this finding adds beyond `#466` (D-08). It is filed, not skipped.
- **`P63-D4-008`** and **`P62-D1-005`** carry review-internal references in their lifted text. Lift
  verbatim and append the bracketed gloss naming the referenced thing in terms of real source files or
  real behaviour.
- **`P65-D1-001`** and **`P62-D1-005`** are D1-primary at `low` severity and file publicly with the
  standard template (D-03). Their `## Evidence` sections state surface, problem class and impact; `low`
  severity is not a licence to include a command line, a payload or an ordered step sequence that would
  reproduce the unfixed path.

Six of this task's ten records are D4 (maintainability) findings against `bbj-intellij/` Java files.
Their `## Acceptance criteria` state the observable condition under which the failure scenario stops
being possible and stay inside what `proposed_approach:` already names — no architecture proposal, no
package reorganisation, no refactor scope the record did not put on the table.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && F=.planning/phases/69-github-issue-filing/69-BODIES-07.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 10 ] && [ "$(grep -c 'BODY-END' $F)" = 10 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 10 ] && [ "$(grep -c '^\*\*Route:\*\* private draft advisory' $F)" = 0 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 10 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P63-D4-006 P63-D4-008 P63-D4-009 P63-D4-011 P63-D4-012 P63-D4-013 P65-D1-001 P61-D4-002 P61-D4-007 P62-D1-005; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && awk '/BODY-BEGIN P61-D4-007/{f=1}f{print}/BODY-END P61-D4-007/{exit}' $F | grep -Fq '#466' && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-07.md` contains exactly 10 `BODY-BEGIN` markers, 10 matching `BODY-END` markers and 10 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 10 times inside the delimited regions.
    - The concatenated delimited regions contain zero occurrences of `.planning/`, `MAJOR-REFACTORS`, `COVERAGE.md`, `INVENTORY.md`, an `RU-nn-nn` identifier or a `§` pointer.
    - `P61-D4-007`'s delimited region contains `#466` and a sentence stating what this finding adds beyond it.
    - `P63-D4-008` and `P62-D1-005` each contain the verbatim source sentence plus a bracketed gloss naming the referenced thing in terms of real source files or real behaviour.
    - No `## Evidence` section of the two D1-primary records contains a command line, a payload, or an ordered step sequence that would reproduce the unfixed path.
    - No `## Acceptance criteria` section names a package reorganisation, an architecture proposal, or any scope absent from that record's `proposed_approach:`.
    - Every `**Labels:**` line carries exactly three values drawn from that record's `proposed_labels:`.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0.
  </acceptance_criteria>
  <done>Rows 126-135 are rendered and the shard's structure is established for Task 2 to close the corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Render filing-order rows 136-144 and close the 144-record denominator</name>
  <files>
    .planning/phases/69-github-issue-filing/69-BODIES-07.md
  </files>
  <read_first>
    - `.planning/phases/69-github-issue-filing/69-BODIES-07.md` — the shard Task 1 created, as the shape to extend
    - `.planning/reviews/MAJOR-REFACTORS.md` lines 248-256 only — Index rows 136-144
    - the nine records, each extracted with the single-record awk: `P62-D5-001`, `P62-D5-003`, `P62-D5-005`, `P63-D4-007`, `P63-D7-002`, `P63-D7-003`, `P63-D7-005`, `P63-D7-006`, `P64-D4-001`
    - `.planning/phases/69-github-issue-filing/69-BODIES-01.md` … `69-BODIES-06.md` — their `BODY-BEGIN` marker counts only, obtained with `grep -c`, for the closing reconciliation; do not read their contents
  </read_first>
  <action>
Append the remaining nine entries of this band to `69-BODIES-07.md` — index rows into the existing table
in filing order, body blocks into `## Bodies rows 126-144` in filing order — so the shard ends holding
all 19 rows and all 144 records of the corpus are rendered across the seven shards.

Four records need specific handling:

- **`P63-D7-002`** and **`P63-D7-006`** both carry a `dedup:` annotation naming the same existing issue
  `#65` (tokenized BBj file support) as a partial overlap, from different directions: one concerns the
  tokenized/line-numbered file capability, the other the tokenized-detection quarter of that scope. Each
  body's `## Traceability` section names `#65` and states which distinct part of its scope this finding
  covers, so the two issues read as two non-identical additions rather than two copies. Do not merge
  them and do not let either body's wording be a paraphrase of the other's.
- **`P63-D7-005`** — `dedup:` names `#475` as a partial overlap, which requests a new BBj-code-scoped
  SETOPTS capability. Its `## Traceability` line states what this finding adds beyond `#475`.
- **`P62-D5-003`**, **`P63-D7-003`** and **`P63-D7-006`** carry review-internal references in their
  lifted text. Lift verbatim and append the bracketed gloss.

Then write the closing reconciliation into the SUMMARY: the per-shard `BODY-BEGIN` counts obtained by
`grep -c` across all seven shards, their sum, and the statement that the sum is 144 with no gap and no
overlap because the seven row bands 1-17, 18-40, 41-64, 65-87, 88-107, 108-125 and 126-144 are
contiguous and disjoint. If the sum is anything other than 144, stop and report the departure rather
than adjusting a band boundary — that is the derived-denominator discipline this project uses, and a
missing entry here becomes a missing issue later.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server && D=.planning/phases/69-github-issue-filing && F=$D/69-BODIES-07.md && [ "$(grep -c 'BODY-BEGIN' $F)" = 19 ] && [ "$(grep -c 'BODY-END' $F)" = 19 ] && [ "$(grep -c '^\*\*Route:\*\* public issue' $F)" = 19 ] && [ "$(grep -c '^| *[0-9]* *|' $F)" = 19 ] && B=$(awk '/BODY-BEGIN/{f=1;next}/BODY-END/{f=0}f' $F) && for s in '## Problem' '## Evidence' '## Failure scenario' '## Proposed approach' '## Acceptance criteria' '## Traceability'; do [ "$(printf '%s' "$B" | grep -Fc "$s")" = 19 ] || { echo "section count wrong: $s"; exit 1; }; done && [ "$(printf '%s' "$B" | grep -Ec '\.planning/|MAJOR-REFACTORS|COVERAGE\.md|INVENTORY\.md|RU-[0-9][0-9]-[0-9][0-9]|§')" = 0 ] && for i in P62-D5-001 P62-D5-003 P62-D5-005 P63-D4-007 P63-D7-002 P63-D7-003 P63-D7-005 P63-D7-006 P64-D4-001; do grep -Fq "BODY-BEGIN $i" $F || { echo "MISSING $i"; exit 1; }; done && awk '/BODY-BEGIN P63-D7-002/{f=1}f{print}/BODY-END P63-D7-002/{exit}' $F | grep -Fq '#65' && awk '/BODY-BEGIN P63-D7-006/{f=1}f{print}/BODY-END P63-D7-006/{exit}' $F | grep -Fq '#65' && awk '/BODY-BEGIN P63-D7-005/{f=1}f{print}/BODY-END P63-D7-005/{exit}' $F | grep -Fq '#475' && TOTAL=$(cat $D/69-BODIES-0[1-7].md | grep -c 'BODY-BEGIN') && [ "$TOTAL" = 144 ] && git diff --quiet .planning/reviews/MAJOR-REFACTORS.md && echo "RENDER-COMPLETE 144"</automated>
  </verify>
  <acceptance_criteria>
    - `69-BODIES-07.md` contains exactly 19 `BODY-BEGIN` markers, 19 matching `BODY-END` markers and 19 index rows, all routed `public issue`.
    - Each of the six body section headings appears exactly 19 times inside the delimited regions.
    - The concatenated `BODY-BEGIN` count across all seven shard files is exactly 144.
    - `P63-D7-002`'s and `P63-D7-006`'s delimited regions each contain `#65` and each state a different part of `#65`'s scope; neither `## Traceability` line is a verbatim copy of the other.
    - `P63-D7-005`'s delimited region contains `#475` and a sentence stating what the finding adds beyond it.
    - `P62-D5-003`, `P63-D7-003` and `P63-D7-006` each contain the verbatim source sentence plus a bracketed gloss; the concatenated delimited regions contain zero `RU-nn-nn` identifiers and zero `§` pointers.
    - The SUMMARY states the seven per-shard counts, their sum, and that the seven bands are contiguous and disjoint.
    - `git diff --quiet .planning/reviews/MAJOR-REFACTORS.md` exits 0 and `git status --porcelain .planning/reviews/` is empty.
  </acceptance_criteria>
  <done>All 144 records are rendered across seven shards, the count is proved by grep rather than by summing plans, and the two findings that overlap the same existing issue are visibly different from each other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MAJOR-REFACTORS.md` record prose → rendered body | review text crosses into content destined for a public tracker; data, never instruction |
| rendered body → the public GitHub tracker (in `69-11`) | the one-way boundary; this plan populates what will cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-14 | Repudiation | two findings overlapping the same existing issue `#65` filed as indistinguishable near-duplicates | medium | mitigate | Task 2's acceptance criteria require each body to name a different part of `#65`'s scope and forbid one `## Traceability` line being a verbatim copy of the other |
| T-69-09 | Information disclosure | public bodies for the 2 `low` D1-primary records in this band | high | mitigate | `## Evidence` states surface, problem class and impact only; acceptance criteria forbid a command line, a payload or an ordered step sequence |
| T-69-18 | Repudiation | a rendered-entry count asserted by summing plan claims rather than measured | medium | mitigate | Task 2 derives the total with `grep -c` across all seven shards and hard-fails unless it is exactly 144; a departure is reported, never absorbed by moving a band boundary |
| T-69-13 | Information disclosure | five records whose lifted prose names review units and section pointers | low | mitigate | Gloss rule applied per record; both tasks negative-grep the delimited regions for `RU-nn-nn` and `§` |
| T-69-12 | Tampering | accidental write to `MAJOR-REFACTORS.md` while extracting records | medium | mitigate | Extraction is read-only `awk`; both tasks assert `git diff --quiet` on the corpus |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; the Package Legitimacy Gate does not fire |
</threat_model>

<verification>
- 19 index rows and 19 delimited body blocks in this shard, all `public issue`.
- 144 `BODY-BEGIN` markers across the seven shards, measured.
- The two `#65` overlaps read as distinct additions.
- No write to `.planning/reviews/` and no tracker write.
</verification>

<success_criteria>
All 144 records exist as approval-ready entries, the count is measured rather than claimed, and the
corpus is ready for assembly into the single approval artifact.
</success_criteria>

<output>
Create `.planning/phases/69-github-issue-filing/69-07-SUMMARY.md` when done.
</output>
</content>
