---
phase: 66-known-debt-re-triage
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - .planning/reviews/66-COVERAGE.md
autonomous: true
requirements: [DEBT-04, DEBT-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DEBT-04's verdict rests on a written line-by-line trace naming concrete inputs and the exact `file:line` where completion filtering diverges from expectation, and the trace states explicitly that no live reproduction was attempted and why"
    - "DEBT-04's record names the JAR-redeployment dependency as its stated blocker and records that the USE-alias path works while the MemberCall `isClassRef` path is the gap"
    - "DEBT-05's verdict cites annotations read off the locally cached LSP4IJ 0.19.0 jar for each of the nine classes and methods our code actually touches — not off documentation, a changelog, or a remembered fact"
    - "PROJECT.md's '19 experimental API usages' figure is either settled with a measured count or explicitly retired, with the evidence that settles it named"
    - "63-COVERAGE.md's two baselines are re-derived live with their literal outputs printed, and any drift from `0` and from `11 files / 20 references` is reported as a finding here rather than corrected in 63-COVERAGE.md"
    - "Zero GitHub tracker writes occur: no issue is created, commented on, labelled, reopened or closed by this plan"
    - "The jar inspection is read-only and all-or-nothing per class: each of the nine enumerated classes is either individually cited with what was read off it, or explicitly recorded as unreadable with the reason — a partial or interrupted inspection never produces a DEBT-05 annotation verdict covering classes that were not actually read"
  prohibitions:
    - statement: "MUST NOT write to the GitHub tracker at any point — no `gh issue create`, `gh issue comment`, `gh issue edit`, `gh issue close/reopen`, and no label or state change. Only read-only `gh issue view` / `gh issue list` are permitted."
      status: resolved
      verification: judgment
    - statement: "MUST NOT present DEBT-04's static trace as a live reproduction, and MUST NOT write a DEBT-05 annotation verdict for any class whose bytes were not actually read from the cached jar in this task. A measurement the phase did not take, asserted as one it did, makes the evidence base unfalsifiable for Phases 67-69 and is worse than an honestly-labelled trace."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/reviews/66-COVERAGE.md"
      provides: "Appended `## DEBT-04` and `## DEBT-05` sections with the live-investigation evidence this phase produces that no prior sweep could"
      contains: "## DEBT-05"
  key_links:
    - from: ".planning/reviews/66-COVERAGE.md"
      to: ".planning/reviews/63-COVERAGE.md"
      via: "DEBT-05 cites P63-D4-010 as the designated inherited record and re-derives its two baselines"
      pattern: "P63-D4-010"
    - from: ".planning/reviews/66-COVERAGE.md"
      to: "bbj-intellij/build.gradle.kts"
      via: "the LSP4IJ 0.19.0 pin the cached jar is measured against"
      pattern: "0\\.19\\.0"
    - from: ".planning/reviews/66-COVERAGE.md"
      to: "bbj-vscode/src/language/bbj-scope.ts"
      via: "DEBT-04's isClassRef / isJavaClass static-filtering trace anchor"
      pattern: "isClassRef"
---

<objective>
Close the two debt items that arrive with **no usable inherited verdict** and require real
investigative work in this phase: DEBT-04 by static trace (CONTEXT.md D-09) and DEBT-05 by direct
measurement against the locally cached LSP4IJ 0.19.0 artifact (D-10).

Purpose: DEBT-05 is the one item where Phase 66 can do something no sweep could — 63-COVERAGE.md
explicitly could not resolve PROJECT.md's "19" from this tree and said so, and the cached jar makes it
answerable. DEBT-04 is the one item no sweep touched at all, so it starts from zero inherited
evidence. Together these are where this phase earns its keep.

Output: `## DEBT-04` and `## DEBT-05` appended to `.planning/reviews/66-COVERAGE.md`, each with
currency/measurement evidence, a verdict, a `P66-*` finding record and an issue-ready draft; the
register rows at PROJECT.md lines 250 and 256 filled.

**This plan modifies zero source files and writes to the GitHub tracker zero times** (D-01, D-02).
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-known-debt-re-triage/66-CONTEXT.md
@.planning/phases/66-known-debt-re-triage/66-PATTERNS.md
@.planning/phases/66-known-debt-re-triage/66-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase 66 produces markdown artifacts and identifiers, not code symbols. Everything below is newly
created by this phase and has no pre-existing definition anywhere in the tree — source-grounding
drift verification must exclude these identifiers.

| Artifact / identifier | Created by | Kind |
|---|---|---|
| `.planning/reviews/66-COVERAGE.md` | 66-01 (created), 66-02 + 66-03 (appended) | new file |
| `P66-D3-001` | 66-01 | new finding ID (DEBT-01) |
| `P66-D5-001`, `P66-D5-002` | 66-01 | new finding IDs (DEBT-02) |
| `P66-D2-001` | 66-01 | new finding ID (DEBT-03) |
| **`P66-D2-002`** | **66-02** | new finding ID (DEBT-04) |
| **`P66-D4-001`** | **66-02** | new finding ID (DEBT-05) |
| `P66-D2-003`, `P66-D5-003` | 66-03 | new finding IDs (DEBT-07, DEBT-08) |
| `DEBT-07`, `DEBT-08` | 66-03 | new requirement IDs in `.planning/REQUIREMENTS.md` |

## Flagged planner assumptions (edge-probe rows the deterministic probe could not classify)

One of the phase's three `unclassified` edge rows belongs to this plan:

- **DEBT-04 / `unclassified` / `unresolved`.** The probe could not classify "FQN path static-only
  completion filtering re-triaged — fixed or filed, with the JAR-redeployment dependency stated" into
  its shape taxonomy. **Planner assumption:** the operative edge is *what counts as established* when
  the item's own blocker makes live reproduction impossible. This plan reads it as D-09 does — a
  static trace clearing INVENTORY §3b's second evidentiary form, with the non-attempt of a live repro
  stated as a limitation rather than hidden. The reason is recorded honestly: this environment has
  known pre-existing test failures that an open java-interop port does not resolve, so a failed setup
  would be indistinguishable from a failed repro, and would be a *worse* evidence record than an
  honest trace. If a reader wants a live repro, the disagreement lands on this assumption.

**Section arithmetic for this plan:** 1 edge row authored into `must_haves.truths` as an explicit
string (DEBT-05 `concurrency`, reinterpreted as "what is guaranteed if the jar inspection is
interrupted mid-read") + 1 surfaced as a flagged assumption above = 2 of the phase's 9 rows accounted
for here. 66-01 accounted for 4, 66-03 accounts for 3. Phase total 9 = 9.

**Prohibition arithmetic for this plan:** Stage-1 adversarial recall over DEBT-04/DEBT-05 produced ~9
raw must-NOT candidates; Stage-2 precision dropped the routine-engineering ones (must not mis-name a
class FQN, must not use an off-scale effort value, must not leave a field blank — all owned by this
plan's `<acceptance_criteria>`), and canon-referral dropped one: *executing untrusted code from a
downloaded artifact* is canon supply-chain security — covered by `/gsd-secure-phase`; not minted here
(and inapplicable regardless: `javap` and `unzip` decode class bytes, they do not run them). **2**
bespoke prohibitions kept and authored descriptor-less. 2 kept = 2 authored, 0 dropped silently.

## Planner reconciliation note — D-09's "trace tier" and D2's `repro` bar

CONTEXT.md D-09 says DEBT-04 is "established by static trace to INVENTORY's `trace` tier". DEBT-04's
concern is completion-filtering **correctness**, which D-16's follow-the-concern rule assigns
`dimension: D2`, and INVENTORY §3b sets D2's bar at `repro`. These are not in conflict: §3b's `repro`
tier is satisfied by **either** a runnable reproduction **or** "a line-by-line trace naming the
concrete inputs/state and the exact `file:line` where behaviour diverges" — which is precisely the
artifact D-09 prescribes. The record therefore carries `evidence_tier: repro`, cleared by §3b's
second form, and says so in the record rather than leaving a reader to reconcile it. Recording
`trace` on a D2 finding would understate the tier the record actually clears.

<tasks>

<task type="auto">
  <name>Task 1: DEBT-05 — measure LSP4IJ 0.19.0 experimental-API exposure against the cached jar</name>
  <precondition>The locally cached LSP4IJ 0.19.0 artifact is present and readable: `test -r ~/.gradle/caches/8.13/transforms/adf3542fc53c5acc20d2eaa00b91d526/transformed/com.redhat.devtools.lsp4ij-0.19.0/lsp4ij/lib/lsp4ij-0.19.0.jar` exits 0, and `javap -version` exits 0. Without both, D-10's measurement cannot be taken and the task must halt rather than fall back to an asserted verdict.</precondition>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — 66-01's header, register and section shape; match it exactly
    - `.planning/reviews/63-COVERAGE.md` line 1932 (the D4 cell narrative) and lines 2236-2290 (the
      `P63-D4-010` record) — the designated DEBT-05 evidence record, including its measured `0` and
      `11 files / 20 refs` correction and its explicit routing of the "19" to Phase 66
    - `.planning/phases/66-known-debt-re-triage/66-CONTEXT.md` — D-10 in full, including the enumerated
      list of nine classes and methods our code touches
    - `bbj-intellij/build.gradle.kts` lines 22-32 — the `plugin("com.redhat.devtools.lsp4ij:0.19.0")` pin
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjCompletionFeature.java` — the subclass
      of `LSPCompletionFeature`, PROJECT.md's named coupling of concern
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServerFactory.java` — the
      `LanguageServerFactory` implementation returning an anonymous `LSPClientFeatures` with a nested
      `LSPDocumentLinkFeature` override
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageClient.java` — the
      `LanguageClientImpl` subclass overriding `createSettings()` / `handleServerStatusChanged()`
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java` — the
      `OSProcessStreamConnectionProvider` subclass
    - `.planning/reviews/INVENTORY.md` lines 61-160 — record template, §3b tiers, §3c tests, §3d scales
  </read_first>
  <action>
Write `## DEBT-05` into `66-COVERAGE.md` in 66-01's five-subsection shape, with the measurement as its
core.

**Subsection `### Baseline re-derivation`.** Re-run 63-COVERAGE.md's own two counts and print each
command with its literal output:
`grep -rn "ApiStatus.Experimental\|@Experimental" bbj-intellij/src/main/java | wc -l` — expected `0`;
`grep -rln "com.redhat.devtools.lsp4ij" bbj-intellij/src/main/java | wc -l` — expected `11`;
`grep -rn "com.redhat.devtools.lsp4ij" bbj-intellij/src/main/java | wc -l` — expected `20`.
Any drift from `0` / `11` / `20` is reported **here as a finding**, with the evidence that settles it,
and never as an edit to 63-COVERAGE.md (D-08; the `P63-D1-002` withdrawal is the precedent). Note that
the first count is about **our** source carrying the annotation; the annotations that matter are on
**LSP4IJ's** classes, which is what the next subsection measures — 63 could not do this and said so.

**Subsection `### Jar measurement (D-10)`.** Set the jar path to
`~/.gradle/caches/8.13/transforms/adf3542fc53c5acc20d2eaa00b91d526/transformed/com.redhat.devtools.lsp4ij-0.19.0/lsp4ij/lib/lsp4ij-0.19.0.jar`
and record it in the file, together with the `bbj-intellij/build.gradle.kts` line pinning `0.19.0`, so
a reader can see the artifact matches the shipped version.

Read `@ApiStatus.Experimental` and `@ApiStatus.Internal` off the exact classes and methods our code
touches. The nine targets, with their fully-qualified names as they exist in the jar:
`com.redhat.devtools.lsp4ij.client.features.LSPCompletionFeature` (subclassed by
`BbjCompletionFeature`), `com.redhat.devtools.lsp4ij.LanguageServerFactory`,
`com.redhat.devtools.lsp4ij.client.features.LSPClientFeatures`,
`com.redhat.devtools.lsp4ij.client.features.LSPDocumentLinkFeature`,
`com.redhat.devtools.lsp4ij.server.StreamConnectionProvider`,
`com.redhat.devtools.lsp4ij.client.LanguageClientImpl` (subclassed, overriding `createSettings()` and
`handleServerStatusChanged()`), `com.redhat.devtools.lsp4ij.server.OSProcessStreamConnectionProvider`
(subclassed), `com.redhat.devtools.lsp4ij.LanguageServerManager`, and
`com.redhat.devtools.lsp4ij.ServerStatus`.

Tool choice is discretionary provided the result is citable. The verified-working recipe is
`javap -v -cp "$JAR" <FQN>` — these annotations have CLASS retention and surface as
`RuntimeInvisibleAnnotations:` blocks in `javap -v` output, attached to the enclosing class or member.
Reading the annotation **names** out of the constant pool alone is not sufficient: it proves the class
file references the annotation somewhere but not **which** member carries it. Attribute each hit to
its enclosing class or method by reading the `RuntimeInvisibleAnnotations:` block in position.

Record, per target: whether the **type** carries `@ApiStatus.Experimental` or `@ApiStatus.Internal`;
which of the specific **members our code overrides or calls** carry either annotation; and, for
`LanguageServerManager`, whether `start(String)` / `stop(String)` carry either. Present the result as
a table with one row per target and a column for the annotation found.

**All-or-nothing rule.** If any target cannot be read — class absent from the jar under that FQN,
`javap` failure, truncated output — record that target as `unreadable` with the reason, and do not
let the verdict cover it. A DEBT-05 annotation verdict that silently spans classes whose bytes were
never read is the failure mode this rule exists to prevent.

**Subsection `### The "19", settled or retired`.** PROJECT.md and DEBT-01..06's wording carry a figure
of 19 experimental API usages whose provenance 63-COVERAGE.md could not confirm or refute. State
plainly which it is: settled with the measured count from the table above (giving the count and how
it was derived), or **retired** as an unsourced figure with the evidence that retires it. Do not
restate `19` as if it were measured.

**Subsection `### Verdict`.** The question the measurement answers is whether
`BbjCompletionFeature`'s subclassing of `LSPCompletionFeature` is a genuine upgrade hazard or an
accepted, stable extension point — answer it from the table, not from prior prose. Choose a
disposition from INVENTORY line 154: `major-refactor` if the coupling is a real, still-open upgrade
hazard drafted for Phase 69; `wontfix` **with its unblocking condition stated** if it is an accepted
upstream limitation; `already-covered` or `not-reproducible` if the measurement retires the concern,
with the settling evidence named.

**Subsection `### Finding record`.** `id: P66-D4-001`; `unit: DEBT-05`; `location:` the
`BbjCompletionFeature.java` anchor plus `bbj-intellij/build.gradle.kts` at its `0.19.0` line;
`dimension: D4` (maintainability, matching `P63-D4-010`, per D-16); `evidence_tier: trace` — D4's tier
per INVENTORY §3b, cleared by the annotation table; `evidence:` the jar path, the commands run, and
the table; `failure_scenario:` what a breaking change to an annotated member would do at plugin-update
time across the 11 referencing files, given no regression test exists in that module (`P63-D5-001`);
`classification:` all six §3c tests recorded individually; `effort:` `2`, `4` or `8` and nothing else
— `P63-D4-010` recorded `4`, and any departure from it is stated with its reason; `dedup:` checked
against INVENTORY's frozen 15-issue snapshot, naming `P63-D4-010` as the inherited record this
supersedes and stating explicitly that #410 and #231 were checked and are unrelated to LSP4IJ API
coupling; `disposition:` from line 154.

**Subsection `### Issue-ready draft`** (omit only if the verdict is `already-covered` or
`not-reproducible`, in which case say so explicitly). Problem statement, `file:line` evidence, verified
failure scenario, proposed approach, acceptance criteria, and proposed labels — area from the
repository's existing set (`intellij` and/or `dependencies` are the candidates), `PRIO 1|2|3` mapped
from severity per INVENTORY §3d, effort `2`/`4`/`8`.

Update the register row at PROJECT.md line 250. Run no `gh` write subcommand.
  </action>
  <verify>
    <automated>grep -c '^## DEBT-05$' .planning/reviews/66-COVERAGE.md; grep -c 'P66-D4-001' .planning/reviews/66-COVERAGE.md</automated>
    <automated>sed -n '/^## DEBT-05/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'lsp4ij-0.19.0.jar'</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEBT-05$' .planning/reviews/66-COVERAGE.md` outputs `1` and `grep -c 'P66-D4-001' .planning/reviews/66-COVERAGE.md` outputs at least `1`.
    - The `## DEBT-05` range prints the literal outputs of all three baseline commands. At plan-write time those outputs are `0`, `11` and `20`; a different value is present **with** a written drift report naming the settling evidence.
    - The range names the cached jar path and contains the string `lsp4ij-0.19.0.jar`.
    - The range contains an annotation table naming all nine targets: `sed -n '/^## DEBT-05/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -cE 'LSPCompletionFeature|LanguageServerFactory|LSPClientFeatures|LSPDocumentLinkFeature|StreamConnectionProvider|LanguageClientImpl|OSProcessStreamConnectionProvider|LanguageServerManager|ServerStatus'` outputs at least `9`.
    - Every one of the nine targets carries either an annotation result or the literal word `unreadable` with a reason on its row; no row is blank.
    - The range contains a subsection heading matching `^### The "19"` and states either a measured count or an explicit retirement — the bare figure is not restated as if measured.
    - The `P66-D4-001` record carries `dimension: D4`, `evidence_tier: trace`, and an `effort:` matching `^effort: *(2|4|8)$` after whitespace stripping.
    - The record's `dedup:` names `P63-D4-010` and states that #410 and #231 were checked and are unrelated.
    - `grep -c 'RU-66-' .planning/reviews/66-COVERAGE.md` outputs `0` and `grep -cE '^triage:' .planning/reviews/66-COVERAGE.md` outputs `0`. <!-- planner-discipline-allow: RU-66- --> <!-- planner-discipline-allow: ^triage: -->
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    `## DEBT-05` carries the three re-derived baselines with literal outputs, a nine-row annotation
    table read off the cached 0.19.0 jar, an explicit settle-or-retire statement for the "19", a
    verdict, `P66-D4-001` and (unless resolved) an issue-ready draft. The register row at PROJECT.md
    line 250 is filled.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 — static trace of the FQN static-only completion filtering gap</name>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — 66-01's and Task 1's section shape; match it
    - `bbj-vscode/src/language/bbj-scope.ts` lines 190-240 — the `isClassRef` / `isJavaClass` branch,
      the static-members-only path, and what it does and does not filter
    - `bbj-vscode/src/language/bbj-completion-provider.ts` — the completion path that consumes the
      scope decision
    - `.planning/reviews/61-COVERAGE.md` line 1862 — where the Phase 61 D5 sweep explicitly noted that
      DEBT-04 has only a prose record and no dedicated regression test, and declined to file a second
      overlapping finding
    - `.planning/reviews/INVENTORY.md` lines 61-160 — record template, §3b tiers, §3c tests, §3d scales
    - `.planning/phases/66-known-debt-re-triage/66-CONTEXT.md` — D-09 in full
  </read_first>
  <action>
Write `## DEBT-04` into `66-COVERAGE.md` in the established shape. This is the one item with **zero
inherited evidence** — no sweep recorded a finding for it — so its section leads with the trace rather
than with a citation.

**Subsection `### Why no live reproduction was attempted`.** State the reason before the evidence, so
nobody reads the trace as a substitute that was never explained: this environment carries known
pre-existing test failures that an open java-interop port does not resolve, so a failed setup would be
indistinguishable from a failed reproduction and would be a worse evidence record than an honest
trace (D-09). Record that Phase 61's D5 sweep at 61-COVERAGE.md:1862 independently observed that
DEBT-04 has only a prose record and no dedicated regression test.

**Subsection `### Static trace`.** Read the actual path and record **line by line** what the code does
and does not filter, naming concrete inputs and the exact `file:line` where behaviour diverges from
the expectation. Cover, at minimum: `bbj-vscode/src/language/bbj-scope.ts:199-213`'s detection of a
class reference — `isSymbolRef(receiver)` followed by `isJavaClass(receiver.symbol.ref)` inside a
try/catch that swallows cyclic-reference errors — and the `isJavaClass(receiverType)` branch that
follows it, in which the `isClassRef` path is supposed to expose static members only. Then follow into
`bbj-vscode/src/language/bbj-completion-provider.ts` and record what the completion list actually
contains for the two input shapes.

The two input shapes to name concretely, because the item's own wording distinguishes them:
1. the **USE-alias path** — a `USE java.lang.String` followed by `String.` — which DEBT-04's bullet
   states **works**; record what in the trace makes it work;
2. the **MemberCall `isClassRef` path** — a fully-qualified reference — which DEBT-04's bullet states
   is the gap; record the exact `file:line` where it diverges and what the user sees instead.

Record both results. A working path recorded as working is what keeps checked-and-clean distinguishable
from unchecked (the Phase 64 D-12 positive-results pattern).

**Subsection `### Stated blocker`.** DEBT-04's own wording requires the JAR-redeployment dependency to
be stated: record that closing the MemberCall `isClassRef` gap requires a redeployment on the
java-interop side, and that `java-interop/` is outside this milestone's review boundary under FUT-01 —
so the blocker is named, not worked around.

**Subsection `### Verdict`.** Choose from INVENTORY line 154 per D-06's mapping: a still-real item
drafted for Phase 69 is `major-refactor`; if the trace shows the MemberCall path now filters
correctly, `already-covered` or `not-reproducible` with the settling evidence named.

**Subsection `### Finding record`.** `id: P66-D2-002`; `unit: DEBT-04`; `location:`
`bbj-vscode/src/language/bbj-scope.ts` at its current line range plus
`bbj-vscode/src/language/bbj-completion-provider.ts`; `dimension: D2` (correctness — DEBT-04 has no
inherited finding to inherit a dimension from, so D-16's follow-the-concern rule applies and the
concern is completion-filtering correctness); `evidence_tier: repro`, cleared by INVENTORY §3b's
**second** form (a line-by-line trace naming concrete inputs/state and the exact `file:line` where
behaviour diverges) — state that in the record, because a reader comparing D-09's phrase "static
trace" against D2's `repro` bar needs to see the reconciliation rather than reconstruct it;
`evidence:` the trace itself; `failure_scenario:` the concrete input (a fully-qualified MemberCall)
and the wrong behaviour (instance members offered where only statics are valid, or the reverse);
`classification:` all six §3c tests recorded individually — note that test (4), regression-testable
with the existing harness, must account for the java-interop dependency; `effort:` `2`, `4` or `8`;
`dedup:` checked against INVENTORY's frozen 15-issue snapshot, with #466 (sibling-type method return
mismatches) explicitly considered and resolved as duplicate, partial-overlap or unrelated;
`disposition:` from line 154.

**Subsection `### Issue-ready draft`** (omit only if the verdict resolves the item, in which case say
so explicitly). Problem statement, `file:line` evidence, verified failure scenario **labelled as
traced rather than executed**, proposed approach, acceptance criteria including the regression test
that does not exist today, the JAR-redeployment dependency stated in the body, and proposed labels —
area from the repository's existing set (`types`, `scoping` and `vscode` are the candidates),
`PRIO 1|2|3` mapped from severity per INVENTORY §3d, effort `2`/`4`/`8`.

Update the register row at PROJECT.md line 256. Then add a `## Plan 66-02 accounting` section stating
which register rows this plan verdicted, which finding IDs it allocated, and the literal output of
`git status --porcelain bbj-vscode bbj-intellij java-interop .github`.

Run no `gh` write subcommand.
  </action>
  <verify>
    <automated>grep -c '^## DEBT-04$' .planning/reviews/66-COVERAGE.md; grep -c 'P66-D2-002' .planning/reviews/66-COVERAGE.md</automated>
    <automated>sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-02'</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEBT-04$' .planning/reviews/66-COVERAGE.md` outputs `1` and `grep -c 'P66-D2-002' .planning/reviews/66-COVERAGE.md` outputs at least `1`.
    - The `## DEBT-04` range contains a subsection heading matching `^### Why no live reproduction` — the non-attempt is stated, not implied.
    - The range names both input shapes: `sed -n '/^## DEBT-04/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'USE'` and the same range's `grep -c 'MemberCall'` each output at least `1`, and the range records the USE-alias path as working.
    - The range names `isClassRef`, `isJavaClass`, `bbj-scope.ts` and `bbj-completion-provider.ts`, each at least once.
    - The range states the JAR-redeployment dependency: its `grep -ci 'redeploy'` outputs at least `1`.
    - The `P66-D2-002` record carries `dimension: D2`, `evidence_tier: repro`, and text naming which of INVENTORY §3b's two forms clears the tier.
    - The record's `effort:` matches `^effort: *(2|4|8)$` after whitespace stripping and its `disposition:` is one of INVENTORY line 154's six values, with a reason present for the last four.
    - `sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-02'` outputs `0` — this plan owed two rows and both are now filled.
    - `sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-03'` outputs `3` — the three rows 66-03 still owes, with no blank cells.
    - `grep -c '^## Plan 66-02 accounting$' .planning/reviews/66-COVERAGE.md` outputs `1`.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    `## DEBT-04` carries an explicit statement of why no live reproduction was attempted, a
    line-by-line static trace covering both the working USE-alias path and the diverging MemberCall
    path with exact `file:line` anchors, the JAR-redeployment blocker stated, a verdict,
    `P66-D2-002` and (unless resolved) an issue-ready draft. Five of eight register rows are
    verdicted; the three 66-03 owes remain explicitly pending.
  </done>
</task>

</tasks>

<threat_model>
Same surface as 66-01 — evidence integrity and boundary violation, not application security. This plan
adds one component: read-only inspection of a locally cached third-party artifact. ASVS L1; block on
`high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → GitHub tracker | Reads permitted; a write is irreversible and reserved to Phase 69 (D-02, ISSUE-01). |
| executor → immutable review records | `INVENTORY.md` and the five closed `6N-COVERAGE.md` files are frozen inputs; 63-COVERAGE.md is this plan's most likely edit temptation because DEBT-05 re-derives its baselines. |
| executor → source trees | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` are read-only evidence anchors (D-01). |
| cached jar → executor | A third-party artifact under `~/.gradle/caches/`. It is decoded, never executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | GitHub tracker (`gh` CLI) | high | mitigate | 66-01 Task 1's `checkpoint:decision` gates the one-way door for the whole phase; both tasks' `<action>` end "run no `gh` write subcommand"; 66-03's D-15 gate 4 asserts no tracker write occurred. |
| T-66-02 | Tampering | `63-COVERAGE.md` specifically, plus `INVENTORY.md` and the other four closed records | high | mitigate | DEBT-05 re-derives 63's own baselines and is the plan most likely to want to "fix" a stale number; the `<action>` states drift is recorded here as a finding, never as an edit; both tasks' `<acceptance_criteria>` require `git status --porcelain` over all six files to print nothing. |
| T-66-03 | Tampering | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` | medium | mitigate | Verdict-only phase (D-01); both tasks' `<verify>` and `<acceptance_criteria>` run `git status --porcelain` over the four trees and require empty output; `files_modified` names one `.planning/` file. |
| T-66-04 | Repudiation | `P66-D4-001` and `P66-D2-002` evidence fields | medium | mitigate | The `no-fabricated-measurement` prohibition; DEBT-05's all-or-nothing rule marks any unreadable class `unreadable` rather than covering it; DEBT-04's section leads with "why no live reproduction was attempted" and labels its failure scenario as traced rather than executed; every re-derived count is printed with its literal command output. |
| T-66-05 | Elevation of privilege | `javap` / `unzip` over the cached LSP4IJ jar | low | accept | Both tools decode class bytes; neither loads or runs the classes. The artifact is a Gradle-resolved cache entry already on disk for the normal build, not newly fetched by this plan. No network call, no install, no execution. |
| T-66-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs **zero** package-manager install tasks — no `npm install`, `pip install` or `cargo add` appears in any `<action>`, and no artifact is downloaded. INVENTORY's package-legitimacy gate has nothing to gate; no RESEARCH.md audit table is required. Verifiable: `grep -cE 'npm install\|pip install\|cargo add' .planning/phases/66-known-debt-re-triage/66-02-PLAN.md` outputs `0`. |
</threat_model>

<verification>
- `## DEBT-05` and `## DEBT-04` both exist in `.planning/reviews/66-COVERAGE.md` with the five-subsection
  shape 66-01 established.
- Two `P66-*` records exist: `P66-D4-001` (D4, tier `trace`) and `P66-D2-002` (D2, tier `repro` via
  §3b's second form).
- All three DEBT-05 baseline commands are printed with literal outputs; drift from `0` / `11` / `20`
  is reported as a finding, not corrected upstream.
- The nine-target annotation table covers every target with a result or an `unreadable` reason.
- PROJECT.md's "19" is explicitly settled or retired.
- DEBT-04 records both the working USE-alias path and the diverging MemberCall path, and states the
  JAR-redeployment blocker.
- Every `effort:` matches `^effort: *(2|4|8)$`; every `disposition:` is one of INVENTORY line 154's six
  values.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
- `git status --porcelain` over `INVENTORY.md` and all five closed `6N-COVERAGE.md` files prints nothing.
- No `gh` write subcommand ran.
</verification>

<success_criteria>
Five of the eight denominator rows are verdicted. DEBT-05 carries the measurement no prior sweep could
take, against the version actually shipped, discharging ROADMAP criterion 4. DEBT-04 carries an
honest static trace with its non-attempt of live reproduction stated rather than hidden, discharging
half of ROADMAP criterion 3. The three rows 66-03 owes remain explicitly pending, never blank.
</success_criteria>

<output>
Create `.planning/phases/66-known-debt-re-triage/66-02-SUMMARY.md` when done.
</output>
