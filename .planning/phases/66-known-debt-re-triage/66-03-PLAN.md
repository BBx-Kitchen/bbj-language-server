---
phase: 66-known-debt-re-triage
plan: 03
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified:
  - .planning/reviews/66-COVERAGE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DEBT-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`DEBT-07` and `DEBT-08` exist in `.planning/REQUIREMENTS.md` as `- [ ] **DEBT-0N**:` bullets in the Debt Re-triage section and as `| DEBT-0N | Phase 66 | Pending |` coverage-matrix rows, closing the 8-vs-6 drift INVENTORY.md line 1220 recorded"
    - "`.planning/reviews/INVENTORY.md` is unchanged by this phase — the drift it recorded is closed by acting on it, never by editing it"
    - "Every surviving bullet in `.planning/PROJECT.md` §\"Known tech debt\" carries its `P66-*` finding ID, its `DEBT-NN` requirement and its disposition, so the prose is a pointer into the evidence base rather than the record itself"
    - "All four D-15 gates are re-derived live in the close-out with each command's literal output printed, none restated from CONTEXT.md or from an earlier plan"
    - "The close-out states plainly that DEBT-06's \"represented by a GitHub issue\" is not literally true at the end of Phase 66 and becomes true when Phase 69 files under ISSUE-01"
    - "The close-out states what Phases 67, 68 and 69 each inherit from this phase, in the inheritance-table shape every prior phase used"
    - "Zero GitHub tracker writes occur: no issue is created, commented on, labelled, reopened or closed by this plan"
    - "Two PROJECT.md bullets that map to the same requirement — lines 253 and 254, both DEBT-02 — stay separate register rows with separate verdicts and separate finding IDs; the denominator counts bullets, not requirement IDs, so equal-mapping rows never merge and never collide"
    - "A denominator that re-derives to any count other than 8 — including zero — is reported with its cause in the Denominator gate and is never silently adopted; DEBT-06 is not declared complete on a drifted denominator"
    - "The 8-row enumeration's output order is PROJECT.md source order, which is deterministic across re-derivations, and each row records its PROJECT.md line number so a reader can re-run the extraction and check the rows one by one in the same sequence"
  prohibitions:
    - statement: "MUST NOT write to the GitHub tracker at any point — no `gh issue create`, `gh issue comment`, `gh issue edit`, `gh issue close/reopen`, and no label or state change. Only read-only `gh issue view` / `gh issue list` are permitted."
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim that any debt item is represented by a GitHub issue at the end of Phase 66, and MUST NOT mark DEBT-06 or ROADMAP criterion 5 fully discharged on the strength of a draft. The close-out states the Phase 69 dependency plainly rather than claiming tracker state this phase does not create."
      status: resolved
      verification: judgment
    - statement: "MUST NOT resolve a disagreement with a closed record — `INVENTORY.md` or any of the five closed `6N-COVERAGE.md` files — by editing that record or by silently adopting the newer number. A disagreement is recorded as a finding in `66-COVERAGE.md` with the evidence that settles it."
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete a PROJECT.md debt bullet that turned out resolved or not-reproducible. It is struck through in place with its settling evidence named, so a later reader can tell checked-and-clean apart from never-checked."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/reviews/66-COVERAGE.md"
      provides: "`## DEBT-07`, `## DEBT-08`, `## DEBT-06 closure` and `## Phase 66 Close-Out` with D-15's four gates and the downstream inheritance table"
      contains: "## Phase 66 Close-Out"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEBT-07 and DEBT-08 requirement bullets plus their coverage-matrix rows mapped to Phase 66"
      contains: "**DEBT-08**"
    - path: ".planning/PROJECT.md"
      provides: "§\"Known tech debt\" rewritten so each bullet points into the evidence base by finding ID, requirement and disposition"
      contains: "P66-"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/reviews/66-COVERAGE.md"
      via: "each surviving debt bullet carries the P66-* finding ID that verdicts it"
      pattern: "P66-D[0-9]-00[0-9]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "DEBT-07 and DEBT-08 coverage-matrix rows map to Phase 66"
      pattern: "\\| DEBT-0[78] \\| Phase 66 \\|"
    - from: ".planning/reviews/66-COVERAGE.md"
      to: ".planning/ROADMAP.md"
      via: "the Criterion gate answers each of Phase 66's five success criteria and names the section discharging it"
      pattern: "criterion [1-5]"
---

<objective>
Close Phase 66: verdict the two orphan bullets, add `DEBT-07`/`DEBT-08` to `REQUIREMENTS.md`, rewrite
`PROJECT.md`'s debt list so every bullet points into the evidence base, discharge `DEBT-06`, and
re-derive D-15's four completion gates live.

Purpose: DEBT-06 is the milestone's closure gate, and it is only provable inside the phase that owns
it. Doing the ID pass here rather than depending on Phase 69 keeps "none remain recorded only as prose
in PROJECT.md" checkable now. The four gates are what make D-01 (verdict-only) and D-02 (zero tracker
writes) — this phase's two most consequential decisions — *checkable* rather than asserted, in the
first phase of the milestone where writing code was even on the table.

Output: `## DEBT-07`, `## DEBT-08`, `## DEBT-06 closure` and `## Phase 66 Close-Out` appended to
`.planning/reviews/66-COVERAGE.md`; two new requirement bullets plus two coverage-matrix rows in
`.planning/REQUIREMENTS.md`; `.planning/PROJECT.md` §"Known tech debt" rewritten in place.

**This plan modifies zero source files and writes to the GitHub tracker zero times** (D-01, D-02).
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-known-debt-re-triage/66-CONTEXT.md
@.planning/phases/66-known-debt-re-triage/66-PATTERNS.md
@.planning/phases/66-known-debt-re-triage/66-01-SUMMARY.md
@.planning/phases/66-known-debt-re-triage/66-02-SUMMARY.md
</context>

## Artifacts this phase produces

Phase 66 produces markdown artifacts and identifiers, not code symbols. Everything below is newly
created by this phase and has no pre-existing definition anywhere in the tree — source-grounding drift
verification must exclude these identifiers.

| Artifact / identifier | Created by | Kind |
|---|---|---|
| `.planning/reviews/66-COVERAGE.md` | 66-01 (created), 66-02 + 66-03 (appended) | new file |
| `P66-D3-001` | 66-01 | new finding ID (DEBT-01) |
| `P66-D5-001`, `P66-D5-002` | 66-01 | new finding IDs (DEBT-02) |
| `P66-D2-001` | 66-01 | new finding ID (DEBT-03) |
| `P66-D2-002` | 66-02 | new finding ID (DEBT-04) |
| `P66-D4-001` | 66-02 | new finding ID (DEBT-05) |
| **`P66-D2-003`** | **66-03** | new finding ID (DEBT-07) |
| **`P66-D5-003`** | **66-03** | new finding ID (DEBT-08) |
| **`DEBT-07`** | **66-03** | new requirement ID in `.planning/REQUIREMENTS.md` |
| **`DEBT-08`** | **66-03** | new requirement ID in `.planning/REQUIREMENTS.md` |

## Flagged planner assumptions

This plan's three edge-probe rows (DEBT-06 `adjacency`, `empty`, `ordering`) were all resolvable to
explicit acceptance criteria and are authored as plain `must_haves.truths` strings above — none is
carried as an assumption. The phase's three `unclassified` rows are surfaced as assumptions in 66-01
(DEBT-01, DEBT-02) and 66-02 (DEBT-04).

**Section arithmetic for this plan:** 3 edge rows authored into `must_haves.truths` as explicit strings
+ 0 flagged assumptions = 3 of the phase's 9 rows accounted for here. 66-01 accounted for 4, 66-02 for
2. **Phase total: 9 probe-surfaced rows in → 6 authored into `must_haves.truths` + 3 surfaced as
flagged assumptions = 9 accounted for. No silent drops.**

**Prohibition arithmetic for this plan:** Stage-1 adversarial recall over DEBT-06 plus the two orphans
produced ~12 raw must-NOT candidates; Stage-2 precision dropped the routine-engineering ones (must not
break the coverage-matrix table shape, must not misnumber a requirement ID, must not leave a gate
command unprinted — all owned by this plan's `<acceptance_criteria>`), and canon-referral dropped one:
*credential exposure via `gh` authentication* is canon security — covered by `/gsd-secure-phase`; not
minted here. **4** bespoke prohibitions kept and authored descriptor-less. 4 kept = 4 authored, 0
dropped silently. **Phase-wide distinct kept prohibitions: 5** — tracker-write (all three plans),
fabricated-evidence (66-01, 66-02), overclaim-DEBT-06, silent-edit-of-closed-record, and
silent-deletion-of-a-resolved-bullet (66-03).

<tasks>

<task type="auto">
  <name>Task 1: Verdict the two orphan bullets and add DEBT-07 / DEBT-08 to REQUIREMENTS.md</name>
  <reversibility rating="costly">Phase 68's coverage arithmetic and the requirement matrix both pick up DEBT-07 and DEBT-08; removing them later means re-deriving Phase 68's requirement-coverage statement (CONTEXT.md D-05).</reversibility>
  <files>.planning/reviews/66-COVERAGE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — the register and the six sections 66-01 and 66-02 wrote;
      match their five-subsection shape exactly
    - `.planning/reviews/INVENTORY.md` line 1220 — the recorded 8-vs-6 drift, verbatim, and the two
      legal resolutions it names
    - `.planning/REQUIREMENTS.md` lines 53-61 — the `### Debt Re-triage` heading and the six existing
      `- [ ] **DEBT-0N**: <text>` bullets whose shape the new bullets copy
    - `.planning/REQUIREMENTS.md` lines 135-160 — the coverage-matrix rows and the `**Coverage:**`
      totals block beneath them
    - `.planning/PROJECT.md` lines 249-257 — bullet 252 (DEBT-07's carried text) and bullet 255
      (DEBT-08's carried text)
    - `bbj-vscode/src/language/bbj-document-validator.ts` lines 27-120 — the diagnostic-hierarchy tier
      enum and `applyDiagnosticHierarchy`, DEBT-07's trace anchor for the BBjCPL suppression path
    - `bbj-intellij/src/main/resources/textmate/bbj-bundle/package.json` — the `"BBx Config"` language
      entry using a `filenames` array rather than `extensions`, DEBT-08's trace anchor
    - `bbj-intellij/build.gradle.kts` lines 11-14 — the Java 17 source/target compatibility that
      `P64-D6-010` records as mismatched against the local JDK, DEBT-08's blocker
    - `.planning/reviews/64-COVERAGE.md` — the `P64-D6-010` record (Gradle JDK toolchain mismatch,
      `P63-D6-002` merged in), cited as DEBT-08's blocker and **not** re-triaged here
  </read_first>
  <action>
Write `## DEBT-07` and `## DEBT-08` into `66-COVERAGE.md` in the established five-subsection shape,
then add both requirements to `REQUIREMENTS.md`.

**`## DEBT-07` — CPL-06 hierarchy suppression timing nuance (PROJECT.md line 252).** The carried
bullet states the suppression takes one extra build cycle after the BBjCPL merge and that the end
state is correct. Trace the actual path in `bbj-vscode/src/language/bbj-document-validator.ts`: the
`DiagnosticTier` ordering in which `BBjCPL` outranks `Parse` which outranks `Link` which outranks
`Warning`, and `applyDiagnosticHierarchy`'s Rule 0 (BBjCPL errors present suppress redundant Langium
parse errors). Record line by line what happens on the first build cycle versus the second, naming the
concrete inputs and the exact `file:line` where the one-cycle lag arises.

Verdict per D-06: if the lag is real and still present, it is a still-real item and verdicts
`major-refactor` or `easy-fix` per the six §3c tests — evaluate and record each test, do not assume.
If the end state is correct and the transient is an accepted consequence of the merge ordering,
`wontfix` **with its unblocking condition stated** is the honest disposition. If the trace cannot
reach INVENTORY §3b's `repro` bar for a D2 finding — neither a runnable reproduction nor a line-by-line
trace naming concrete inputs/state and the exact divergence point — then per INVENTORY's
drop-vs-disposition rule no finding is minted: record the row under `not-reproducible` with its reason,
note in the register that `P66-D2-003` was **not allocated**, and do not reassign that ID.

Where a finding is minted: `id: P66-D2-003`; `unit: DEBT-07`; `location:` the validator anchor;
`dimension: D2`; `evidence_tier: repro` with the clearing form named; the full field set; `effort:`
`2`, `4` or `8`; `dedup:` against INVENTORY's frozen 15-issue snapshot; `disposition:` from line 154.
Add an issue-ready draft unless the verdict resolves the item.

**`## DEBT-08` — IntelliJ TextMate bundle filename registration (PROJECT.md line 255).** The carried
bullet states that filename-based `config.bbx` / `config.min` registration was added to the bundle in
commit `2489001` for issue #381, mirroring the VS Code approach, but that whether JetBrains' TextMate
plugin honors `filenames` rather than `extensions` is unverified. Trace what is actually in the tree:
`bbj-intellij/src/main/resources/textmate/bbj-bundle/package.json` declares a `"BBx Config"` language
whose registration uses a `filenames` array of `config.bbx` / `Config.bbx` / `config.min` /
`Config.min`, alongside a `"BBj"` language registered by `extensions`. Record that both forms are
present in the same bundle, which is what makes the question well-posed.

State the blocker explicitly and as a dependency on another finding, not as a workaround: verification
requires running the plugin, and `./gradlew build` fails locally on the JDK toolchain mismatch
`P64-D6-010` records (`bbj-intellij/build.gradle.kts` pins Java 17 source/target against a local JDK
that does not match). DEBT-08's unblocking condition **is another finding's fix**. Do not re-triage
`P64-D6-010` here — CONTEXT.md keeps it deliberately outside this phase's denominator; cite it.

Finding record: `id: P66-D5-003`; `unit: DEBT-08`; `location:` the bundle `package.json` plus the
`build.gradle.kts` toolchain lines; `dimension: D5` — the assertion is a missing verification, and
INVENTORY §3b puts D5 on the `inherited` tier meaning it follows what the finding asserts, so a
missing verification is `trace`-evidenced; record `evidence_tier: trace` and state that reasoning in
the record; `evidence:` the bundle contents plus the blocked-build citation; `failure_scenario:` what
a user sees if the TextMate plugin ignores `filenames` — `config.bbx` rendering as plain text, the
exact symptom issue #381 reported; `classification:` all six §3c tests recorded individually;
`effort:` `2`, `4` or `8`; `dedup:` **#381 is in INVENTORY's frozen 15-issue snapshot** — check it
explicitly and record whether this is a duplicate of #381, a partial overlap stating what it adds, or
distinct because #381 reported the VS Code-side regression; `disposition:` from line 154 —
`wontfix` with the unblocking condition stated is legitimate here provided the condition names
`P64-D6-010`. Add an issue-ready draft; per D-07 a blocked item still gets one, with the blocker as
part of the body.

**`.planning/REQUIREMENTS.md` edit.** Two scoped changes, both appending in the file's own existing
shape — do not rewrite the file:

1. Append two bullets immediately after the `DEBT-06` bullet in the `### Debt Re-triage` section,
   in the identical `- [ ] **DEBT-0N**: <text>` shape. `DEBT-07`'s text carries PROJECT.md line 252's
   subject: CPL-06 hierarchy suppression taking one extra build cycle after the BBjCPL merge, a timing
   nuance with a correct end state, re-triaged against current code. `DEBT-08`'s text carries line
   255's subject: the IntelliJ TextMate bundle's filename-based `config.bbx` / `config.min`
   registration (commit `2489001`, issue #381), whose honoring by JetBrains' TextMate plugin is
   unverified, re-triaged against current code.
2. Append two rows to the requirement coverage matrix immediately after the `DEBT-06` row, in the
   identical `| ID | Phase N | Status |` pipe-table shape: `| DEBT-07 | Phase 66 | Pending |` and
   `| DEBT-08 | Phase 66 | Pending |`. Then update the `**Coverage:**` block beneath the matrix so its
   totals arithmetic still adds up — the v4.0 requirement total rises from 38 to 40, mapped rises to
   40, unmapped stays 0.

Record in `66-COVERAGE.md` that `INVENTORY.md` was **not** edited: the drift it recorded at line 1220
is closed by taking the second of the two resolutions it itself named (added as new requirements),
never by changing the record. Print `git status --porcelain .planning/reviews/INVENTORY.md` with its
literal output as the evidence.

Update the register rows at PROJECT.md lines 252 and 255. Run no `gh` write subcommand.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*DEBT-' .planning/REQUIREMENTS.md</automated>
    <automated>grep -cE '^\| DEBT-0[78] \| Phase 66 \|' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c '^## DEBT-07$' .planning/reviews/66-COVERAGE.md; grep -c '^## DEBT-08$' .planning/reviews/66-COVERAGE.md</automated>
    <automated>git status --porcelain .planning/reviews/INVENTORY.md bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*DEBT-' .planning/REQUIREMENTS.md` outputs `8` (it output `6` before this task).
    - `grep -c '\*\*DEBT-07\*\*' .planning/REQUIREMENTS.md` and `grep -c '\*\*DEBT-08\*\*' .planning/REQUIREMENTS.md` each output at least `1`.
    - `grep -cE '^\| DEBT-0[78] \| Phase 66 \|' .planning/REQUIREMENTS.md` outputs `2`.
    - The `**Coverage:**` block beneath the matrix reads `40 total`, `Mapped to phases: 40`, `Unmapped: 0` — the arithmetic still closes after the append.
    - `grep -c '^## DEBT-07$' .planning/reviews/66-COVERAGE.md` and `grep -c '^## DEBT-08$' .planning/reviews/66-COVERAGE.md` each output `1`.
    - The `## DEBT-08` range cites its blocker: `sed -n '/^## DEBT-08/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'P64-D6-010'` outputs at least `1`.
    - The `## DEBT-08` range names the bundle anchor and the registration key: the same range's `grep -c 'textmate/bbj-bundle/package.json'` and `grep -c 'filenames'` each output at least `1`.
    - The `## DEBT-08` record's `dedup:` explicitly names `#381` and states duplicate, partial-overlap, or distinct.
    - Where `P66-D2-003` or `P66-D5-003` is minted, its `effort:` matches `^effort: *(2|4|8)$` after whitespace stripping and its `disposition:` is one of INVENTORY line 154's six values, with a reason for the last four. Where an ID is not minted, the register row states `not allocated` with the disposition that replaced it.
    - `git status --porcelain .planning/reviews/INVENTORY.md` prints nothing, and its literal (empty) output is recorded in `66-COVERAGE.md`.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    Both orphan bullets carry verdicts in `66-COVERAGE.md`; `REQUIREMENTS.md` holds eight `DEBT-*`
    bullets and eight `DEBT-*` coverage-matrix rows with closing arithmetic; `INVENTORY.md` is
    untouched and evidenced as untouched; seven of eight register rows are verdicted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite PROJECT.md's debt list so every bullet points into the evidence base</name>
  <reversibility rating="reversible">The file is planning prose under version control; the rewrite can be reverted with a single commit and nothing downstream reads it as a machine contract (CONTEXT.md D-13).</reversibility>
  <files>.planning/PROJECT.md, .planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 249-258 — the header parenthetical and the eight bullets being
      rewritten, in their current exact wording
    - `.planning/reviews/66-COVERAGE.md` — every `### Verdict` and `### Finding record` subsection
      written by 66-01, 66-02 and Task 1; the rewrite copies their finding IDs and dispositions and
      invents nothing
    - `.planning/phases/66-known-debt-re-triage/66-PATTERNS.md` §".planning/PROJECT.md §\"Known tech
      debt\" (MODIFY)" — the current literal bullet formatting and the target rewrite shape
  </read_first>
  <action>
Rewrite `.planning/PROJECT.md` §"Known tech debt" in place, with a scoped edit to that section only.
Do not rewrite the whole file and do not touch any other section.

**Preserve the bullet count at 8.** Every bullet stays a `^- ` list item so the D-04 denominator
command still extracts eight rows after this edit. Resolved and not-reproducible items are **struck
through in place** — `~~<original bullet text>~~ — resolved, see <P66 ID or disposition>, evidence:
<what settles it>` — never deleted. A deleted bullet destroys the distinction between checked-and-clean
and never-checked, which is the exact distinction the Phase 64 D-12 positive-results pattern exists to
preserve, and it would silently shrink the denominator Phase 68 audits against.

**Each surviving bullet** keeps its existing text and gains a suffix carrying three things: its `P66-*`
finding ID, its `DEBT-NN` requirement, and its disposition — for example a bullet ending
`— DEBT-01, P66-D3-001, major-refactor (drafted; issue number backfilled by Phase 69)`. Take the ID
and the disposition from that item's `### Finding record` in `66-COVERAGE.md`; do not re-derive or
re-decide either here. Where an item resolved without a minted finding, the suffix carries the
disposition and the settling evidence in place of an ID.

**The two orphan bullets** — PROJECT.md lines 252 and 255 — lose their "not yet mapped to a DEBT-NN
item" tail and gain `DEBT-07` and `DEBT-08` respectively, alongside the same ID-and-disposition suffix.
After this edit no bullet in the section describes itself as unmapped.
<!-- planner-discipline-allow: not yet mapped -->

**The header parenthetical** is rewritten too. It currently describes a 2026-08-17 survival check
against `DEBT-01..DEBT-06` and records the 8-vs-6 enumeration gap. Replace it with a parenthetical
that points at `.planning/reviews/66-COVERAGE.md` as the evidence base, names the re-triage date, and
states that `DEBT-01..DEBT-08` now enumerate all eight bullets — so the recorded gap is described as
closed rather than left standing. Remove the unmapped-item wording entirely; leaving it as a quoted
reference would keep a stale claim in the file.

Then record in `66-COVERAGE.md`, under a `## PROJECT.md rewrite (D-13)` section, the before/after
bullet count with its literal command output, and a table of the eight bullets with the suffix each
one received, so the rewrite is auditable without diffing.

State in that section what Phase 69 still owes: backfilling the filed issue number into each bullet.
Phase 66 supplies the ID-and-disposition pointer; Phase 69 supplies the tracker number. Run no `gh`
write subcommand.
  </action>
  <verify>
    <automated>sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '</automated>
    <automated>sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c 'P66-'</automated>
    <automated>sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -cE 'DEBT-0[78]'</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '` outputs `8` — the same value as before the edit; no bullet was deleted or added.
    - `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -cE 'DEBT-0[78]'` outputs at least `2` — both orphans now carry their new requirement ID.
    - The extracted section contains no self-describing unmapped-item wording: `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -ci 'not yet mapped'` outputs `0`.
    - Every one of the eight extracted bullets carries a `DEBT-0[1-8]` token: `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep '^- ' | grep -cE 'DEBT-0[1-8]'` outputs `8`.
    - Every surviving (non-struck) bullet carries a `P66-` token or, where no finding was minted, a disposition value from INVENTORY line 154 plus its settling evidence.
    - Any struck bullet uses `~~` strikethrough and retains its original text between the markers — `grep -c '~~' .planning/PROJECT.md` is `0` only if nothing resolved.
    - The header parenthetical names `66-COVERAGE.md`: `sed -n '/^\*\*Known tech debt:/p' .planning/PROJECT.md | grep -c '66-COVERAGE.md'` outputs `1`.
    - `grep -c '^## PROJECT.md rewrite (D-13)$' .planning/reviews/66-COVERAGE.md` outputs `1`, and that section contains an eight-row table of bullet-to-suffix assignments.
    - `git diff --name-only .planning/PROJECT.md` shows the file changed, and `git diff -U0 .planning/PROJECT.md | grep -cE '^[+-]' ` shows changes confined to the debt-list region — no hunk outside lines 249-258 of the pre-edit file.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    PROJECT.md's debt list still holds eight bullets, each carrying its requirement ID and either a
    `P66-*` finding ID with disposition or a strikethrough with settling evidence; the header points at
    `66-COVERAGE.md`; the rewrite is recorded as an auditable table in `66-COVERAGE.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: DEBT-06 closure and the four D-15 completion gates, re-derived live</name>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — everything the three plans wrote; the close-out reads from
      it and re-derives, it does not restate CONTEXT.md
    - `.planning/reviews/65-COVERAGE.md` lines 1444-1843 — `## Phase 65 Close-Out` sections A/B/C/F/G:
      the gate-per-section shape, the literal-output discipline, the three-command boundary-evidence
      block, and the downstream inheritance table
    - `.planning/ROADMAP.md` §"Phase 66: Known Debt Re-triage" — the five success criteria, verbatim,
      that the Criterion gate answers
    - `.planning/ROADMAP.md` §"Phase 67" / §"Phase 68" / §"Phase 69" — what each downstream phase
      consumes, for the inheritance table
    - `.planning/REQUIREMENTS.md` lines 53-61 and 135-160 — `DEBT-01`..`DEBT-08` and their matrix rows,
      for the Requirement gate
    - `.planning/phases/66-known-debt-re-triage/66-CONTEXT.md` — D-02's honesty consequence and D-15's
      four gates
  </read_first>
  <action>
Write `## DEBT-06 closure` and `## Phase 66 Close-Out` into `66-COVERAGE.md`.

**`## DEBT-06 closure`.** DEBT-06 asks that every carried debt item ends the milestone either fixed or
represented by a GitHub issue, with none remaining recorded only as prose in PROJECT.md. State how
Phase 66 discharges each half, and state plainly where it does not:

- The prose half **is** discharged here: Task 2's rewrite makes every PROJECT.md bullet a pointer into
  `66-COVERAGE.md` carrying a finding ID, a requirement and a disposition. Cite the acceptance
  arithmetic.
- The tracker half is **not literally true at the end of Phase 66**. Every unresolved item has an
  issue-ready draft; none is filed, because D-02 reserves every tracker write to Phase 69 under
  ISSUE-01's single approval gate. Say this in plain words. Do not write that any item "is represented
  by a GitHub issue" and do not mark DEBT-06 complete on the strength of a draft. State what makes it
  true: Phase 69 filing the drafts and backfilling the issue numbers into the PROJECT.md bullets.
- List every draft produced by the phase, one row per draft, with its item, its finding ID, its
  proposed area / `PRIO` / effort labels, and its `dedup:` result — this is the handoff Phase 69 acts
  on.

**`## Phase 66 Close-Out`.** Open with the same discipline statement 65-COVERAGE.md's close-out uses:
every gate below was re-run at execution time and its literal output recorded; none is restated from
`66-CONTEXT.md`, from an item section, or from an earlier plan. Where a live re-derivation disagrees
with a figure already in this file, the disagreement is surfaced here as a defect rather than
reconciled by editing the section.

**`### A. Denominator gate.`** Run `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '`
and print the command with its literal output. Then show every row carrying a verdict: reproduce the
Debt Denominator Register as a closing table with one row per PROJECT.md bullet — line number, owner
requirement, finding ID or `not allocated`, disposition — and confirm no cell is blank and no row
reads `pending`. State that the register's row order is PROJECT.md source order and therefore
reproducible by re-running the extraction. If the count is not `8`, report the drift with its cause
and do **not** proceed to declare DEBT-06 discharged on it.

**`### B. Criterion gate.`** Answer each of ROADMAP's five Phase 66 success criteria **Met**,
**Partially Met** or **Not Met**, naming the section of this file that discharges it — the same
per-criterion citation-plus-verdict shape 65-COVERAGE.md `### B` uses. Answer criterion 5 ("every one
of these 6 items ends the milestone represented either by a merged fix or a GitHub issue") from the
actual verdicts: where any item's representation depends on a Phase 69 filing, the honest answer is
**Partially Met** with the Phase 69 dependency named. Do not answer **Met** on the strength of a draft.

**`### C. Requirement gate.`** A table with one row per requirement `DEBT-01` through `DEBT-08`,
each marked complete or explicitly not complete, with the evidence named — the section, the finding
ID, and the disposition. Eight rows, none omitted. `DEBT-07` and `DEBT-08` are the two Task 1 created;
they are gated here like any other.

**`### D. Boundary gate.`** The gate that makes D-01 and D-02 checkable rather than asserted. Run and
print, each with its literal output:
`git status --porcelain bbj-vscode bbj-intellij java-interop .github` — expected: nothing;
`git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md`
— expected: nothing;
`git log --oneline -- .planning/reviews/INVENTORY.md | head -1` — expected: still Phase 60's commit.
Then narrate the outputs in prose the way 65-COVERAGE.md §F does, and state the tracker fact
explicitly: no GitHub issue was created, commented on, labelled, reopened or closed by any of this
phase's three plans; only read-only `gh issue view` / `gh issue list` ran. Name which read-only tracker
queries the phase actually made and why (the `#232` state check, the `dedup:` checks against
INVENTORY's frozen snapshot), so "no writes" is a positive record rather than a bare denial.

**`### E. Finding accounting.`** Every `P66-*` ID pre-allocated by the phase, with allocated-or-not and
its disposition; the per-dimension breakdown; and the classification split, stating how many findings
Phase 67 inherits as `easy` apply candidates.

**`### F. Downstream inheritance.`** A table with one row per downstream phase, in the shape every
prior phase used:
`Phase 67` — which `classification: easy` findings it inherits, and the fact that Phase 66 landed no
source change so nothing is pre-applied;
`Phase 68` — `66-COVERAGE.md` for DOC-03's concatenation, the `P66-*` findings for
`MAJOR-REFACTORS.md` / `EASY-FIXES.md` assembly, the `DOC-04` dispositions captured at triage time
here, and the new `DEBT-07` / `DEBT-08` its requirement-coverage statement must pick up;
`Phase 69` — the drafted issues to file under ISSUE-01, the `dedup:` results feeding ISSUE-04's
re-query, and the PROJECT.md issue-number backfill that finally makes DEBT-06 literally true.

**`### G. Closing confirmations.`** Short, checkable statements: ISSUE-01 not triggered; `INVENTORY.md`
not edited; the five closed `6N-COVERAGE.md` files not edited; no source file modified anywhere; the
`RU-` namespace not invented for a unit INVENTORY never defined; Phase 64's `triage:` field absent by
decision; every `effort:` on INVENTORY §3d's three-value scale; every `disposition:` from line 154.

Run no `gh` write subcommand.
  </action>
  <verify>
    <automated>grep -c '^## Phase 66 Close-Out$' .planning/reviews/66-COVERAGE.md; grep -c '^## DEBT-06 closure$' .planning/reviews/66-COVERAGE.md</automated>
    <automated>sed -n '/^## Phase 66 Close-Out/,$p' .planning/reviews/66-COVERAGE.md | grep -cE '^### [A-G]\. '</automated>
    <automated>sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-0'</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEBT-06 closure$' .planning/reviews/66-COVERAGE.md` and `grep -c '^## Phase 66 Close-Out$' .planning/reviews/66-COVERAGE.md` each output `1`.
    - `sed -n '/^## Phase 66 Close-Out/,$p' .planning/reviews/66-COVERAGE.md | grep -cE '^### [A-G]\. '` outputs `7` — gates A-D plus accounting, inheritance and closing confirmations.
    - The `### A. Denominator gate` range prints the literal output of the D-04 command; at plan-write time that output is `8`, and a different value is present **with** a written drift cause and without a DEBT-06 completion claim.
    - The closing register table in `### A` has 8 rows and `sed -n '/^### A\. Denominator gate/,/^### B\./p' .planning/reviews/66-COVERAGE.md | grep -ci 'pending'` outputs `0`.
    - `sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-0'` outputs `0` — every row the earlier plans marked owed is now filled. <!-- planner-discipline-allow: pending 66-0 -->
    - The `### B. Criterion gate` range answers all five criteria: its `grep -cE 'Met|Partially Met|Not Met'` outputs at least `5`, and each answer names a discharging section of this file.
    - The `### C. Requirement gate` range contains 8 rows matching `DEBT-0[1-8]`, none omitted.
    - The `### D. Boundary gate` range prints all three commands with their literal outputs, and states in words that no GitHub issue was created, commented on, labelled, reopened or closed.
    - The `### F. Downstream inheritance` range has one row each naming `Phase 67`, `Phase 68` and `Phase 69`.
    - The close-out states plainly that DEBT-06's tracker half completes in Phase 69: `sed -n '/^## DEBT-06 closure/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'Phase 69'` outputs at least `1`.
    - Every `effort:` line in the whole file matches `^effort: *(2|4|8)$` after whitespace stripping.
    - Every `disposition:` line in the whole file carries one of INVENTORY line 154's six values.
    - `grep -c 'RU-66-' .planning/reviews/66-COVERAGE.md` outputs `0`. <!-- planner-discipline-allow: RU-66- -->
    - `grep -cE '^triage:' .planning/reviews/66-COVERAGE.md` outputs `0`. <!-- planner-discipline-allow: ^triage: -->
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    `66-COVERAGE.md` closes with DEBT-06's two halves stated honestly, four live-re-derived gates each
    carrying its literal command output, a per-requirement table covering `DEBT-01`..`DEBT-08`, finding
    accounting, a three-row downstream inheritance table, and closing confirmations. All eight
    denominator rows carry verdicts. No source file was modified and no tracker write occurred.
  </done>
</task>

</tasks>

<threat_model>
Same surface as 66-01 and 66-02 — evidence integrity and boundary violation, not application security.
This plan adds the phase's only edits outside `.planning/reviews/`, which raises the blast radius of a
careless whole-file write. ASVS L1; block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → GitHub tracker | Reads permitted; a write is irreversible and reserved to Phase 69 (D-02, ISSUE-01). This plan writes the close-out that *asserts* no write happened, so a write here would also falsify its own evidence. |
| executor → immutable review records | `INVENTORY.md` records the very drift this plan closes, making it the plan's sharpest edit temptation. |
| executor → `REQUIREMENTS.md` / `PROJECT.md` | Living planning documents with many sections this phase does not own. A whole-file rewrite would destroy content outside the debt list and the Debt Re-triage section. |
| executor → source trees | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` are read-only evidence anchors (D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | GitHub tracker (`gh` CLI) | high | mitigate | 66-01 Task 1's `checkpoint:decision` gated the one-way door for the phase; all three tasks' `<action>` end "run no `gh` write subcommand"; Task 3's `### D. Boundary gate` records the tracker fact positively, naming which read-only queries ran and why. |
| T-66-02 | Tampering | `INVENTORY.md` and the five closed `6N-COVERAGE.md` files | high | mitigate | Task 1's `<action>` states the drift is closed by taking one of the two resolutions INVENTORY itself named, never by editing it, and requires `git status --porcelain .planning/reviews/INVENTORY.md` to be printed as evidence; all three tasks' `<acceptance_criteria>` require porcelain over all six files to print nothing. |
| T-66-06 | Tampering | `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` | high | mitigate | Both edits are specified as **scoped appends/in-place rewrites of one named section**, never whole-file writes; Task 2's acceptance criteria require the diff to be confined to the debt-list region and require the post-edit bullet count to equal the pre-edit count; Task 1's criteria require the coverage-matrix arithmetic to still close at 40/40/0. |
| T-66-03 | Tampering | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` | medium | mitigate | Verdict-only phase (D-01); every task's `<verify>` and `<acceptance_criteria>` run `git status --porcelain` over the four trees and require empty output. |
| T-66-07 | Repudiation | The close-out's own claims | medium | mitigate | The `overclaim-DEBT-06` prohibition; every gate re-derived live with its literal output printed rather than restated from CONTEXT.md; criterion 5 answered from actual verdicts with **Partially Met** where a Phase 69 filing is the missing step; the `silent-deletion` prohibition keeps a resolved bullet visible as struck rather than absent. |
| T-66-05 | Information disclosure | `gh` authenticated reads | low | accept | Read-only `gh issue view` / `gh issue list` against a repository the executor already has credentials for; no token value is written into any artifact. Credential handling itself is canon security — owned by `/gsd-secure-phase`. |
| T-66-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs **zero** package-manager install tasks — no `npm install`, `pip install` or `cargo add` appears in any `<action>`. INVENTORY's package-legitimacy gate has nothing to gate; no RESEARCH.md audit table is required. Verifiable: `grep -cE 'npm install\|pip install\|cargo add' .planning/phases/66-known-debt-re-triage/66-03-PLAN.md` outputs `0`. |
</threat_model>

<verification>
- `REQUIREMENTS.md` holds eight `- [ ] **DEBT-` bullets and eight `DEBT-*` coverage-matrix rows, with
  the `**Coverage:**` arithmetic closing at 40 total / 40 mapped / 0 unmapped.
- `PROJECT.md` §"Known tech debt" still extracts eight bullets, each carrying a `DEBT-0[1-8]` token and
  either a `P66-*` ID with disposition or a strikethrough with settling evidence; no bullet describes
  itself as unmapped; the header points at `66-COVERAGE.md`.
- `66-COVERAGE.md` contains `## DEBT-07`, `## DEBT-08`, `## PROJECT.md rewrite (D-13)`,
  `## DEBT-06 closure` and `## Phase 66 Close-Out` with seven `### A.`-`### G.` subsections.
- All four D-15 gates carry their commands with literal outputs; the register's closing table has eight
  rows and no `pending` cell.
- The close-out states that DEBT-06's tracker half completes in Phase 69, and criterion 5 is answered
  from actual verdicts rather than from drafts.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
- `git status --porcelain` over `INVENTORY.md` and all five closed `6N-COVERAGE.md` files prints nothing.
- No `gh` write subcommand ran.
</verification>

<success_criteria>
All eight denominator rows carry verdicts. `DEBT-07` and `DEBT-08` exist as requirements mapped to
Phase 66, closing the drift INVENTORY.md line 1220 recorded without editing that record. PROJECT.md's
debt prose is a pointer into the evidence base rather than the record itself. The four gates make
D-01's zero-source-change and D-02's zero-tracker-write boundaries checkable from literal command
output. Phases 67, 68 and 69 each know what they inherit, and DEBT-06's remaining half is stated
plainly rather than claimed.
</success_criteria>

<output>
Create `.planning/phases/66-known-debt-re-triage/66-03-SUMMARY.md` when done.
</output>
