---
phase: 66-known-debt-re-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/66-COVERAGE.md
autonomous: false
requirements: [DEBT-01, DEBT-02, DEBT-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A reader re-running `sed -n '/^\\*\\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '` gets the same denominator count that `66-COVERAGE.md`'s Debt Denominator Register enumerates, and the register lists one row per bullet with that bullet's PROJECT.md line number"
    - "D-03: DEBT-01, DEBT-02 and DEBT-03 each carry a written verdict in `66-COVERAGE.md`, recorded in INVENTORY's frozen finding-record format — its disposition value drawn verbatim from INVENTORY.md line 154's six-value vocabulary and its `effort:` landing on INVENTORY §3d's `{2,4,8}` scale"
    - "Every inherited finding cited by this plan (P61-D3-003, P61-D5-003, P61-D5-010, P61-D2-011, P61-D5-009) has each of its `file:line` anchors re-read against the current tree, with the currency result recorded in `66-COVERAGE.md`"
    - "DEBT-02's three `parser.test.ts` assertions and its `completion-test.test.ts` TEST-03 skip carry two separate issue-ready drafts naming two different unblocking conditions — a repo-local Java classpath under a non-EmptyFileSystem fixture, and an upstream Langium completion grammar follower"
    - "DEBT-01's draft names the exact edit for both of P61-D3-003's mechanisms — a cache shape/key/invalidation trigger for `getBBjClassesFromFile`, and an `isExternalDocument`-aware `treeIter.prune()` modelled on `bbj-linker.ts`'s `link()` — plus `isAffected()` recorded as existing partial mitigation"
    - "Zero GitHub tracker writes occur: no issue is created, commented on, labelled, reopened or closed by this plan"
    - "If DEBT-01, DEBT-02 or DEBT-03 turns out already fixed or no longer reproducing against current code, its register row still carries a verdict — `already-covered` or `not-reproducible` with the settling evidence named — and is never absent, blank, or silently deleted from the register"
    - "Currency-check equality is by named construct, not by line number: each re-read anchor records the recorded line, the current line, and whether the named identifier or construct is still present; a construct found at a shifted line is recorded `current`, a vanished construct is recorded as drift with its cause"
  prohibitions:
    - statement: "MUST NOT write to the GitHub tracker at any point — no `gh issue create`, `gh issue comment`, `gh issue edit`, `gh issue close/reopen`, and no label or state change. Only read-only `gh issue view` / `gh issue list` are permitted. An issue filed early cannot be un-filed from a public repository and permanently corrupts Phase 69's ISSUE-01 'nothing filed before approval' claim."
      status: resolved
      verification: judgment
    - statement: "MUST NOT record a verdict as evidenced by a reproduction, measurement or command that was not actually run in this plan — a cited inherited record is cited as inherited, and a re-read anchor is recorded with what was actually read. Presenting an inherited or asserted result as a fresh reproduction makes the evidence base unfalsifiable for Phases 67-69."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/reviews/66-COVERAGE.md"
      provides: "Phase 66 re-triage record — header, finding-ID namespace, dedup source, evidence rule, scope fence, 8-row Debt Denominator Register, and full sections for DEBT-01, DEBT-02, DEBT-03"
      contains: "## Debt Denominator Register"
      min_lines: 250
  key_links:
    - from: ".planning/reviews/66-COVERAGE.md"
      to: ".planning/reviews/61-COVERAGE.md"
      via: "inherited-evidence citation by finding ID (D-08) — cited, never re-recorded"
      pattern: "P61-D(2|3|5)-0(03|09|10|11)"
    - from: ".planning/reviews/66-COVERAGE.md"
      to: ".planning/PROJECT.md"
      via: "Debt Denominator Register rows keyed to PROJECT.md bullet line numbers 250-257"
      pattern: "PROJECT\\.md:25[0-7]"
    - from: ".planning/reviews/66-COVERAGE.md"
      to: ".planning/reviews/INVENTORY.md"
      via: "finding records use INVENTORY's frozen field template and line-154 disposition vocabulary"
      pattern: "disposition: +(easy-fix|major-refactor|duplicate|wontfix|already-covered|not-reproducible)"
---

<objective>
Create `.planning/reviews/66-COVERAGE.md` and close the three debt items that arrive with inherited
sweep evidence — DEBT-01, DEBT-02 and DEBT-03 — via CONTEXT.md D-08's cite-plus-currency-check,
including D-11's named-edit implementation plan for DEBT-01.

Purpose: Phases 61-65 recorded the evidence for these three items but never wrote the **verdict**.
The verdict is the thing that is actually missing, and it is what makes ROADMAP criteria 1, 2 and 3
answerable. This plan spends its effort on verdicts, not on re-deriving evidence that already exists.

Output: `.planning/reviews/66-COVERAGE.md` containing the file header (which states plainly that this
file's completeness construct is a debt denominator and not a cell grid, per D-12), the finding-ID
namespace, the dedup source, the evidence rule, the scope fence, the 8-row Debt Denominator Register
covering **every** PROJECT.md bullet (including the five this plan does not verdict — those rows are
enumerated with `verdict: pending 66-02` / `pending 66-03` placeholders that later plans fill), and
complete sections for DEBT-01, DEBT-02 and DEBT-03 with their `P66-*` finding records and
issue-ready drafts.

Plan split per D-14: this is plan 1 of 3, grouped by the kind of work each item needs rather
than by requirement number — `66-01` the inherited-evidence items, `66-02` the
live-investigation items (DEBT-04, DEBT-05), `66-03` the orphans plus the REQUIREMENTS.md
and PROJECT.md edits, DEBT-06 closure and the four gates.

**This plan modifies zero source files and writes to the GitHub tracker zero times** (D-01, D-02).
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-known-debt-re-triage/66-CONTEXT.md
@.planning/phases/66-known-debt-re-triage/66-PATTERNS.md
</context>

## Artifacts this phase produces

Phase 66 produces markdown artifacts and identifiers, not code symbols. Everything below is
**newly created by this phase** and therefore has no pre-existing definition anywhere in the tree —
source-grounding drift verification must exclude these identifiers.

| Artifact / identifier | Created by | Kind |
|---|---|---|
| `.planning/reviews/66-COVERAGE.md` | 66-01 (created), 66-02 + 66-03 (appended) | new file |
| `P66-D3-001` | 66-01 | new finding ID (DEBT-01) |
| `P66-D5-001` | 66-01 | new finding ID (DEBT-02, `parser.test.ts` trio) |
| `P66-D5-002` | 66-01 | new finding ID (DEBT-02, TEST-03 skip) |
| `P66-D2-001` | 66-01 | new finding ID (DEBT-03) |
| `P66-D2-002` | 66-02 | new finding ID (DEBT-04) |
| `P66-D4-001` | 66-02 | new finding ID (DEBT-05) |
| `P66-D2-003` | 66-03 | new finding ID (DEBT-07) |
| `P66-D5-003` | 66-03 | new finding ID (DEBT-08) |
| `DEBT-07` | 66-03 | new requirement ID in `.planning/REQUIREMENTS.md` |
| `DEBT-08` | 66-03 | new requirement ID in `.planning/REQUIREMENTS.md` |

Finding IDs are **pre-allocated** here rather than allocated at write time, so the three plans cannot
collide. INVENTORY §3a's monotonic-within-`(phase, dimension)` rule is satisfied because the plans
run in strictly sequential waves. A pre-allocated ID that ends up unused (because its item resolves
`already-covered` / `not-reproducible` and D-03 mints a finding only for **unresolved** items) is
recorded in the register as *"not allocated — disposition `<value>`"* rather than being reassigned.

## Flagged planner assumptions (edge-probe rows the deterministic probe could not classify)

The spec-less edge probe emitted 9 rows across DEBT-01..DEBT-06. Three came back `unclassified` and
are carried here as explicit assumptions rather than auto-resolved or dropped. Two belong to this
plan:

- **DEBT-01 / `unclassified` / `unresolved`.** The probe could not classify "CPU stability in
  multi-project workspaces re-triaged — mitigation implemented, or issue updated with a concrete
  implementation plan" into its shape taxonomy. **Planner assumption:** the operative edge is *what
  counts as "concrete"*. This plan reads "concrete" as D-11 defines it — a named edit per mechanism
  (cache shape + key + invalidation trigger; the `isExternalDocument`-aware prune modelled on an
  in-repo precedent) — and explicitly **not** a measured benchmark, because #232's symptom is
  macOS-specific and load-dependent and a sandbox number would mislead. If a reader disagrees with
  that reading of "concrete", the disagreement lands on this assumption, not on hidden work.
- **DEBT-02 / `unclassified` / `unresolved`.** The probe could not classify "the 3 disabled
  assertions and the skipped TEST-03 case re-triaged — enabled, or documented with the specific
  blocking limitation and what would unblock them". **Planner assumption:** "enabled" is out of reach
  in a verdict-only phase (D-01), so all four sites take the documented-blocker branch, and per D-07
  the documentation becomes an issue body rather than an alternative to filing. The two-versus-one
  drafting question (are the trio and the skip one item or two?) is resolved to **two** by their two
  different unblocking conditions.

**Section arithmetic for this plan:** 2 edge rows authored into `must_haves.truths` as explicit
strings (DEBT-03 `empty`, DEBT-03 `encoding`) + 2 surfaced as flagged assumptions above = 4 of the
phase's 9 rows accounted for here. 66-02 accounts for 2 more, 66-03 for 3 more. Phase total 9 = 9.

**Prohibition arithmetic for this plan:** Stage-1 adversarial recall over DEBT-01/02/03 produced ~11
raw must-NOT candidates; Stage-2 precision dropped the routine-engineering ones (must not leave a
required field blank, must not use an off-scale effort value, must not mis-cite a line number — all
owned by this plan's `<acceptance_criteria>`), and canon-referral dropped one: *credential exposure
via `gh` authentication* is canon security — covered by `/gsd-secure-phase`; not minted here. **2**
bespoke prohibitions kept and authored descriptor-less into `must_haves.prohibitions`. 2 kept = 2
authored, 0 dropped silently.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way tracker boundary before any issue draft is written</name>
  <read_first>
    - `.planning/phases/66-known-debt-re-triage/66-CONTEXT.md` — decision D-02 in full, including its
      one-way reversibility rating and its honesty consequence for DEBT-06
    - `.planning/REQUIREMENTS.md` line 78 — `ISSUE-01`, the gate D-02 protects
    - `.planning/ROADMAP.md` §"Phase 69: GitHub Issue Filing" — the phase that owns tracker writes
  </read_first>
  <decision>
    Phase 66 produces issue-ready **drafts** and files nothing. Confirm that no `gh` write subcommand
    runs anywhere in this phase.
  </decision>
  <context>
    D-02 rates this **one-way**. An issue created in a public repository cannot be un-created; it
    would permanently falsify Phase 69's ISSUE-01 claim that nothing was filed before the user
    approved the drafted list, and it would break Phase 69's ISSUE-04 dedup re-query, which is only
    meaningful because nothing was opened behind it. Every other decision in this phase is reversible
    or costly; this one is not. Reading the tracker (`gh issue view`, `gh issue list`) is permitted
    and expected for `dedup:` checks against INVENTORY's Frozen Open-Issue Snapshot.
  </context>
  <options>
    <option id="drafts-only">
      <name>Drafts only — file nothing (D-02 as written)</name>
      <pros>Preserves ISSUE-01's single approval gate and ISSUE-04's dedup re-query; keeps the
      milestone's one-phase-one-job shape; every draft is still reviewable before it becomes public.</pros>
      <cons>DEBT-06's "represented by a GitHub issue" is not literally true at the end of Phase 66 —
      it becomes true when Phase 69 files. The close-out must say this plainly rather than overclaim.</cons>
    </option>
    <option id="file-now">
      <name>File issues during Phase 66</name>
      <pros>DEBT-06 becomes literally true one phase earlier.</pros>
      <cons>Irreversible. Breaks ISSUE-01 (nothing filed before approval), breaks ISSUE-04's dedup
      baseline, and splits filing across two phases so no single approval moment exists. Rejected by
      D-02.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The resume signal is recorded in the plan's SUMMARY before Task 2 begins.
    - After the checkpoint resolves, `grep -rnE "gh issue (create|comment|edit|close|reopen)|gh label|gh api .*-X (POST|PATCH|PUT|DELETE)" .planning/phases/66-known-debt-re-triage/` returns no line from any executed command log or SUMMARY.
  </acceptance_criteria>
  <resume-signal>Select: drafts-only, file-now, or state an alternative</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end DEBT-01 slice — denominator row through to issue-ready draft</name>
  <precondition>The swept-tree SHA is reachable from HEAD: `git cat-file -e 1750ad749d55c3e88d74be3ac2d561d37e8170d0^{commit}` exits 0. Without it the D-08 currency check has no baseline to compare against and this task must halt.</precondition>
  <reversibility rating="costly">The 8-row denominator this task establishes becomes Phase 68's evidence that the debt list was discharged; changing its shape later means re-deriving every verdict (CONTEXT.md D-04).</reversibility>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/65-COVERAGE.md` lines 1-70 — the header, structural-break statement,
      recording-shape resolutions, finding-ID namespace, dedup source and evidence rule sections this
      file's header copies in shape (`grep -n "^## " .planning/reviews/65-COVERAGE.md` for the full
      section skeleton)
    - `.planning/reviews/INVENTORY.md` lines 24-60 — the Frozen Open-Issue Snapshot (15 issues,
      queried 2026-08-17), the `dedup:` input
    - `.planning/reviews/INVENTORY.md` lines 61-160 — §3a finding IDs, §3b evidence tiers, §3c the six
      easy-vs-major tests, §3d severity and the `2|4|8` effort scale, and the finding-record template
      with the line-154 disposition vocabulary
    - `.planning/reviews/61-COVERAGE.md` line 1517 (the D3 cell narrative) and lines 1625-1660 (the
      `P61-D3-003` record itself)
    - `.planning/PROJECT.md` lines 249-257 — the denominator being enumerated
    - `bbj-vscode/src/language/bbj-scope.ts` lines 306-333 — `getBBjClassesFromFile`, mechanism 1
    - `bbj-vscode/src/language/bbj-scope-local.ts` lines 104-116 — `collectLocalSymbols`, mechanism 2
    - `bbj-vscode/src/language/bbj-index-manager.ts` lines 12-29 — `isAffected()`, the existing
      partial mitigation
    - `bbj-vscode/src/language/bbj-linker.ts` lines 45-60 — `link()`'s `treeIter.prune()`, the in-repo
      precedent D-11's named edit copies
  </read_first>
  <action>
This is the tracer slice: one debt item carried production-quality through **every** layer the phase
uses, so the remaining seven rows expand against a proven shape rather than an invented one. Do
DEBT-01 completely, end to end, before touching anything else.

**Step A — create `.planning/reviews/66-COVERAGE.md` with its header block.** Copy the section shape
of `65-COVERAGE.md` (listed in `66-PATTERNS.md` under "Top-level section shape to copy"), renamed to
this phase's vocabulary. Required header sections, in this order:

`## The structural break — no `RU-66-*` units exist` — state that INVENTORY defines no review unit,
no applicability grid, no cell gate and no file gate for Phase 66, exactly as Phase 65 faced, and
that Phase 66 constructs its own closed denominator keyed on **debt items**.

`## Two recording-shape resolutions the structural break forces` — (1) the required `unit:` field
carries the **`DEBT-NN` requirement ID** rather than an `RU-` token, mirroring how 65-COVERAGE.md put
its surface ID there; no record in this file carries an `RU-` token in `unit:`. (2) Phase 64's
`triage:` field is absent **by decision, not oversight** (D-06) — an inapplicable field makes Phase
68's assembly ambiguous about which buckets it covers.

`## Finding-ID namespace` — `P66-D{n}-nnn`, zero-padded to three digits. State that dimensions follow
the concern rather than a phase default (D-16), unlike Phase 65's D1-throughout, and reproduce the
pre-allocation table from this plan's "Artifacts this phase produces" section so a reader can see the
whole allocation at once. State the effort scale is INVENTORY §3d's `2` | `4` | `8` and nothing else,
naming Phase 63's three off-scale values as the mistake not being repeated.

`## Dedup source` — INVENTORY's Frozen Open-Issue Snapshot, 15 issues, queried 2026-08-17. Record the
composition check you actually run against those 15 rows: which of them are topically adjacent to a
debt item and which are not, so a `dedup: none` in this file is a derived result rather than a shrug.
Record explicitly that **#232 is absent from the snapshot because it is CLOSED** — confirm with
`gh issue view 232 --json number,state,title,labels` (read-only) and record its literal state.

`## The evidence rule (D-08) — the governing rule of this file` — inherited evidence is **cited, not
re-derived**, plus a currency check against swept SHA `1750ad749d55c3e88d74be3ac2d561d37e8170d0`.
Record the phase-wide currency baseline by running `git diff --name-only 1750ad749d55c3e88d74be3ac2d561d37e8170d0..HEAD -- bbj-vscode bbj-intellij java-interop .github`
and printing its literal output. State that this baseline does not replace the per-anchor re-read —
it bounds it. State that a disagreement with a closed COVERAGE file is recorded **here as a finding**,
with the evidence that settles it, and never as a silent correction or an edit to that file (the
`P63-D1-002` withdrawal is the worked precedent).

`## The scope fence` — this file records verdicts and lands no source change (D-01); it drafts issues
and writes to the tracker zero times (D-02); `INVENTORY.md` and the five closed `6N-COVERAGE.md`
files are immutable inputs.

`## Stopping rule & write contract` — one section per denominator row; a row is closed when it
carries a disposition from INVENTORY line 154 plus, for unresolved items, a `P66-*` record and an
issue-ready draft.

**Step B — the Debt Denominator Register (D-04).** Run
`sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '` and print the command
with its literal output. If the output is not `8`, do **not** silently adopt the new count: record the
drift, identify its cause by diffing the current bullet list against the eight rows below, and
surface it in the register before continuing.

Enumerate all eight rows in PROJECT.md source order (which is deterministic and reproducible, so the
register's row order is specified and stable across re-derivations). Give each row: PROJECT.md line
number, the bullet's leading text, its `DEBT-NN` owner, its pre-allocated `P66-*` ID, and its verdict
cell. The mapping is fixed:

| PROJECT.md line | Owner | Pre-allocated ID | Verdict written by |
|---|---|---|---|
| 250 — BbjCompletionFeature extends LSPCompletionFeature | DEBT-05 | `P66-D4-001` | 66-02 |
| 251 — CPU stability mitigations documented not implemented (#232) | DEBT-01 | `P66-D3-001` | this task |
| 252 — CPL-06 hierarchy suppression timing nuance | DEBT-07 | `P66-D2-003` | 66-03 |
| 253 — TEST-03 skipped, Langium grammar follower limitation | DEBT-02 | `P66-D5-002` | Task 3 |
| 254 — 3 parser.test.ts assertions DISABLED | DEBT-02 | `P66-D5-001` | Task 3 |
| 255 — IntelliJ TextMate bundle filename registration unverified | DEBT-08 | `P66-D5-003` | 66-03 |
| 256 — FQN path static-only filtering deferred | DEBT-04 | `P66-D2-002` | 66-02 |
| 257 — Static method return type inference gap | DEBT-03 | `P66-D2-001` | Task 4 |

Rows not verdicted by this plan carry the literal cell `pending 66-02` or `pending 66-03` — never a
blank cell. Note in the register that lines 252 and 255 are the two orphans INVENTORY.md line 1220
recorded, and that 66-03 closes that drift by adding `DEBT-07`/`DEBT-08` to REQUIREMENTS.md, never by
editing INVENTORY.md.

Also record in the register the currency-equality rule this file uses: an anchor is `current` when
the named identifier or construct is still present, even at a shifted line; each anchor row records
the recorded line, the current line, and the construct name.

**Step C — `## DEBT-01` section, complete.** Subsections: `### Inherited evidence`,
`### Currency check`, `### Verdict`, `### Finding record`, `### Issue-ready draft`.

`### Inherited evidence` — cite `P61-D3-003` by ID with its 61-COVERAGE.md location. Do **not**
restate its trace; state what it established: two current-code mechanisms that scale with total
multi-project workspace size rather than the referencing file's own size, plus `isAffected()` as an
existing partial mitigation present at the index-rebuild layer and absent at both request-time paths.

`### Currency check` — re-read all three anchors and record, per anchor, the recorded line, the
current line, the construct name, and `current` or `drift`:
`bbj-vscode/src/language/bbj-scope.ts:308-331` (`getBBjClassesFromFile`, the
`this.indexManager.allElements(BbjClass.$type).filter(...)` full-index scan),
`bbj-vscode/src/language/bbj-scope-local.ts:106-114` (`collectLocalSymbols`, the
`AstUtils.streamAllContents(rootNode)` loop with per-node `await interruptAndCheck`), and
`bbj-vscode/src/language/bbj-index-manager.ts:14-27` (`isAffected()`).

`### Verdict` — a still-real item drafted for Phase 69 is `major-refactor` per D-06. If the currency
check shows a mechanism has been fixed since the swept SHA, the verdict flips to `already-covered`
with the settling evidence named; if it no longer reproduces, `not-reproducible` with its reason.
Whichever branch is taken, the register row is filled — never left blank and never deleted.

`### Finding record` — the full INVENTORY field template with no extra field:
`id: P66-D3-001`; `unit: DEBT-01`; `location:` the two mechanism anchors;
`dimension: D3` (performance and resource use, matching `P61-D3-003`, per D-16);
`severity:` carried from `P61-D3-003`; `evidence_tier: repro`, cleared by INVENTORY §3b's second form
— the cited line-by-line trace plus this task's re-read naming the concrete inputs/state and the
exact `file:line` where behaviour diverges; `evidence:` the citation plus the currency result;
`failure_scenario:` the multi-project-workspace inputs and the wrong behaviour;
`classification:` with all six §3c tests recorded pass/fail individually — note test (1) fails
because the named edit touches two files; `effort:` one of `2` | `4` | `8` and nothing else;
`dedup:` #232 is CLOSED and therefore absent from the frozen snapshot, so this is not a duplicate of
any open issue — state what it adds; `disposition:` from line 154.

`### Issue-ready draft` — per D-11 this draft is a **new issue that supersedes #232**, cross-referencing
it, because #232 is closed and cannot take an update comment. Sections: problem statement; `file:line`
evidence; verified failure scenario; proposed approach carrying the named edit for **both** mechanisms
— for `getBBjClassesFromFile` state the cache shape, its key and its invalidation trigger; for
`collectLocalSymbols` state the `isExternalDocument`-aware `treeIter.prune()` and name
`bbj-vscode/src/language/bbj-linker.ts:47-58`'s `link()` as the in-repo precedent to copy rather than a
design to invent; acceptance criteria; and proposed labels — an area label from the repository's
existing set, a `PRIO 1|2|3` label mapped from severity by INVENTORY §3d, and an effort label of
`2`, `4` or `8`. Record `isAffected()` as the existing partial mitigation and state the method for
measuring the win **without building a benchmark**, with the reason: #232's symptom is macOS-specific
and load-dependent, so a sandbox number would mislead more than it informs.

Write the draft as a draft. Do not run any `gh` write subcommand.
  </action>
  <verify>
    <automated>test -f .planning/reviews/66-COVERAGE.md; sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -cE '^\| *25[0-7] '</automated>
    <automated>grep -c 'P66-D3-001' .planning/reviews/66-COVERAGE.md</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/reviews/66-COVERAGE.md` exists and `grep -c '^## ' .planning/reviews/66-COVERAGE.md` outputs at least `8` (the header sections plus the register plus `## DEBT-01`).
    - `grep -c '^## Debt Denominator Register$' .planning/reviews/66-COVERAGE.md` outputs `1`.
    - The register enumerates 8 rows: `sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -cE '^\| *25[0-7] '` outputs `8`.
    - Each of the strings `P66-D3-001`, `P66-D5-001`, `P66-D5-002`, `P66-D2-001`, `P66-D2-002`, `P66-D4-001`, `P66-D2-003`, `P66-D5-003` appears at least once in `.planning/reviews/66-COVERAGE.md`.
    - The literal output of `sed -n '/^\*\*Known tech debt:/,/^## /p' .planning/PROJECT.md | grep -c '^- '` is printed in the register; at plan-write time that output is `8`. A different value is present **with** a written drift cause.
    - The literal output of `git diff --name-only 1750ad749d55c3e88d74be3ac2d561d37e8170d0..HEAD -- bbj-vscode bbj-intellij java-interop .github` is printed in the evidence-rule section; at plan-write time that command prints nothing.
    - `grep -c '^## DEBT-01$' .planning/reviews/66-COVERAGE.md` outputs `1`, and that section contains all five subsection headings `### Inherited evidence`, `### Currency check`, `### Verdict`, `### Finding record`, `### Issue-ready draft`.
    - `grep -c 'P61-D3-003' .planning/reviews/66-COVERAGE.md` outputs at least `1`.
    - The `P66-D3-001` record's `effort:` value matches `^effort: *(2|4|8)$` when the leading whitespace is stripped; no other value is accepted.
    - The `P66-D3-001` record's `disposition:` value is one of `easy-fix`, `major-refactor`, `duplicate`, `wontfix`, `already-covered`, `not-reproducible`; the last four carry a reason on the same record.
    - `grep -c 'RU-66-' .planning/reviews/66-COVERAGE.md` outputs `0` — no review-unit token is invented for a unit INVENTORY never defined. <!-- planner-discipline-allow: RU-66- -->
    - `grep -cE '^triage:' .planning/reviews/66-COVERAGE.md` outputs `0` — Phase 64's field is absent by decision. <!-- planner-discipline-allow: ^triage: -->
    - The draft names `bbj-vscode/src/language/bbj-linker.ts` as the prune precedent: `grep -c 'bbj-linker.ts' .planning/reviews/66-COVERAGE.md` outputs at least `1`.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    `66-COVERAGE.md` exists with its full header, an 8-row register in which every row is enumerated
    and no cell is blank, and a complete `## DEBT-01` section carrying inherited citation, currency
    check, verdict, `P66-D3-001` record and a two-mechanism named-edit issue draft. The single path
    from denominator row to issue-ready draft works end to end and is committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: DEBT-02 — the parser.test.ts trio and the TEST-03 skip, as two separate items</name>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — the file Task 2 wrote; match its `## DEBT-01` section shape
      exactly rather than inventing a second shape
    - `.planning/reviews/61-COVERAGE.md` line 661 (the D5 cell narrative) and lines 900-935 (the
      `P61-D5-003` record)
    - `.planning/reviews/61-COVERAGE.md` line 1862 (the D5 cell narrative) and lines 2139-2175 (the
      `P61-D5-010` record)
    - `bbj-vscode/test/parser.test.ts` lines 525-535, 806-816, 855-865 — the three `// DISABLED:` sites
    - `bbj-vscode/test/completion-test.test.ts` lines 180-195 — the TEST-03 `test.skip`
    - `.planning/reviews/INVENTORY.md` lines 61-160 — the record template, §3b tiers, §3c tests, §3d scales
    - `.planning/phases/66-known-debt-re-triage/66-CONTEXT.md` — D-07 and the `<specifics>` block on
      why these two must not be collapsed
  </read_first>
  <action>
Write `## DEBT-02` into `66-COVERAGE.md` using the same five-subsection shape Task 2 established, but
splitting the item into **two** finding records with two separate issue-ready drafts. D-07 governs:
DEBT-06 outranks DEBT-02's doc-only escape hatch, so the documentation becomes the issue body rather
than an alternative to filing.

**Currency check first**, recorded per site with recorded line, current line, construct name, and
`current` or `drift`:
`bbj-vscode/test/parser.test.ts:530`, `:811`, `:860` (each a `// DISABLED:` comment block — the first
and third blocked on Java types unresolvable under `EmptyFileSystem`, the second on a `BBjAPI()`
method chain), and `bbj-vscode/test/completion-test.test.ts:185`
(`test.skip('DEF FN parameters with $ suffix inside class method', ...)`). Confirm the counts by
running `grep -c 'DISABLED' bbj-vscode/test/parser.test.ts` and
`grep -c 'test.skip' bbj-vscode/test/completion-test.test.ts` and printing their literal outputs; at
plan-write time these are `3` and `1`.

**Record 1 — `P66-D5-001`, the `parser.test.ts` trio.** `unit: DEBT-02`; `location:` the three sites;
`dimension: D5` (test coverage, matching `P61-D5-003`, per D-16); `evidence_tier:` D5 follows what the
finding asserts per INVENTORY §3b — a missing test is `trace`-evidenced, so record `trace` and state
that reasoning in the record rather than leaving the tier unexplained; `evidence:` cite `P61-D5-003`
by ID plus this task's re-read; `failure_scenario:` the assertions that do not run and the regression
class they would catch; `classification:` all six §3c tests recorded individually; `effort:` `2`, `4`
or `8`; `dedup:` checked against the frozen 15-issue snapshot; `disposition:` from line 154.

**Record 2 — `P66-D5-002`, the TEST-03 skip.** Same field shape; `location:`
`bbj-vscode/test/completion-test.test.ts:185`; cite `P61-D5-010`; `dimension: D5`.

**Two drafts, two unblocking conditions — this is the point of the split.** Draft 1's unblocking
condition is repo-local and actionable in this repository: a Java classpath available under a
non-`EmptyFileSystem` test fixture, i.e. `createBBjTestServices` from `bbj-vscode/test/bbj-test-module.ts`
with real classpath data rather than the fake `JavaInteropTestService` classes. Draft 2's unblocking
condition is upstream and outside this repository: Langium's completion grammar follower resolving
inside `MethodDecl.body`. Their acceptance criteria therefore differ in shape — draft 1's is "these
three assertions run and pass under the new fixture"; draft 2's is "when the upstream follower
resolves inside a method body, this skip is removed and the test passes". Do not write one draft
covering both, and do not let draft 2's acceptance criteria imply this repository can close it alone.

Each draft carries: problem statement, `file:line` evidence, verified failure scenario, proposed
approach, acceptance criteria, and proposed labels (area label from the repository's existing set,
`PRIO 1|2|3` mapped from severity per INVENTORY §3d, effort label `2`/`4`/`8`).

Filing an issue whose resolution depends on something outside this repository is the intended outcome
per D-07, not a defect — draft 2 states the blocker and the unblocking condition, which is what
DEBT-02's own wording asked for.

Update the register rows at PROJECT.md lines 253 and 254 with their verdicts. Note in the DEBT-02
section that `bbj-vscode/test/linking.test.ts:85`'s third `test.skip` is named by no `DEBT-*` item and
is deliberately outside this phase's denominator (66-CONTEXT.md `<deferred>`), so a reader counting
three skips in the suite is not left wondering.

Run no `gh` write subcommand.
  </action>
  <verify>
    <automated>grep -c 'P66-D5-001\|P66-D5-002' .planning/reviews/66-COVERAGE.md</automated>
    <automated>grep -c '^## DEBT-02$' .planning/reviews/66-COVERAGE.md</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEBT-02$' .planning/reviews/66-COVERAGE.md` outputs `1`.
    - `grep -c 'P66-D5-001' .planning/reviews/66-COVERAGE.md` and `grep -c 'P66-D5-002' .planning/reviews/66-COVERAGE.md` each output at least `1`.
    - The `## DEBT-02` section contains exactly two blocks matching `^id: *P66-D5-00[12]$` after whitespace stripping — two records, not one merged record.
    - The section contains at least two distinct issue-draft blocks, and `sed -n '/^## DEBT-02/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'unblocking condition'` outputs at least `2`.
    - The section names both anchors: `sed -n '/^## DEBT-02/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'parser.test.ts'` and the same range's `grep -c 'completion-test.test.ts'` each output at least `1`.
    - The section cites both inherited records: the same range's `grep -c 'P61-D5-003'` and `grep -c 'P61-D5-010'` each output at least `1`.
    - The literal outputs of `grep -c 'DISABLED' bbj-vscode/test/parser.test.ts` and `grep -c 'test.skip' bbj-vscode/test/completion-test.test.ts` are printed in the currency-check subsection; at plan-write time they are `3` and `1`.
    - Every `effort:` line inside the `## DEBT-02` range matches `^effort: *(2|4|8)$` after whitespace stripping.
    - Every `disposition:` line inside the range carries one of INVENTORY line 154's six values.
    - The register rows for PROJECT.md lines 253 and 254 no longer read `pending 66-02` or `pending 66-03`.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
  </acceptance_criteria>
  <done>
    `## DEBT-02` carries two finding records (`P66-D5-001`, `P66-D5-002`) and two issue-ready drafts
    with two distinct unblocking conditions; the register's two DEBT-02 rows carry verdicts; the third
    `linking.test.ts` skip is named as deliberately out of denominator.
  </done>
</task>

<task type="auto">
  <name>Task 4: DEBT-03 — static method return-type inference gap, cited and verdicted</name>
  <files>.planning/reviews/66-COVERAGE.md</files>
  <read_first>
    - `.planning/reviews/66-COVERAGE.md` — Tasks 2 and 3's sections; match their shape
    - `.planning/reviews/61-COVERAGE.md` line 1516 (the D2 cell narrative) and lines 1527-1575 (the
      `P61-D2-011` record) — the reproduction angle
    - `.planning/reviews/61-COVERAGE.md` line 1519 (the D5 cell narrative) and lines 1797-1825 (the
      `P61-D5-009` record) — the untested-regression angle
    - `bbj-vscode/src/language/bbj-type-inferer.ts` lines 65-85 — the `isJavaMethod(member)` branch
      returning `member.resolvedReturnType?.ref`
    - `.planning/reviews/INVENTORY.md` lines 61-160 — record template, §3b tiers, §3c tests, §3d scales
  </read_first>
  <action>
Write `## DEBT-03` into `66-COVERAGE.md` in the same five-subsection shape.

**Inherited evidence** — cite **both** `P61-*` records by ID: `P61-D2-011` (the reproduction — a
`JavaMethod` `valueOf` with `returnType: 'java.lang.String'` and `resolvedReturnType` left unset
produced zero "incompatible type" diagnostics, proving `getType()` silently returned `undefined`) and
`P61-D5-009` (the untested-regression angle — the gap exists only as prose in STATE.md and ROADMAP.md
with no committed regression test). Do not restate their traces.

**Currency check** — the Phase 61 reproduction was run with a throwaway vitest file that was deleted
before commit, so it cannot be re-run by citation alone. Do **not** re-run it and do **not** describe
it as freshly reproduced. Instead satisfy INVENTORY §3b's `repro` bar by its **second** form, from
current code: re-read `bbj-vscode/src/language/bbj-type-inferer.ts` around lines 73-78 and write the
line-by-line trace naming the concrete inputs/state and the exact `file:line` where behaviour
diverges — specifically that the `isJavaMethod(member)` branch returns `member.resolvedReturnType?.ref`
with no fallback to the always-present raw `member.returnType: string`, so any path that bypasses
java-interop's async resolution yields `undefined` instead of a type. Record the recorded line, the
current line, the construct name, and `current` or `drift`.

**Verdict** — per D-06's mapping. If the fallback is now present in current code the verdict is
`already-covered` with the settling evidence named; if it is still absent the item is still real and
verdicts `major-refactor` (or `easy-fix` only if all six §3c tests pass, which requires the fix to
touch at most one file and be regression-testable with the existing vitest harness — evaluate and
record each test, do not assume).

**Finding record `P66-D2-001`** — `unit: DEBT-03`; `location: bbj-vscode/src/language/bbj-type-inferer.ts`
at its current line range; `dimension: D2` (correctness and error handling — the primary dimension of
`P61-D2-011`, per D-16); `secondary: [D5]` (the untested-regression angle `P61-D5-009` carries);
`evidence_tier: repro` — and because a `secondary` dimension is set, INVENTORY §3b's adjacency rule
requires the **stricter** of the two tiers, which `repro` already is; state that in the record;
`evidence:` the two citations plus this task's line-by-line trace; `failure_scenario:`
`String.valueOf(2)` assigns no type to the target variable, so a downstream type mismatch produces no
diagnostic; `classification:` all six §3c tests recorded individually; `effort:` `2`, `4` or `8`;
`dedup:` checked against the frozen 15-issue snapshot — note #466 (sibling-type method return
mismatches via Java class hierarchy) is topically adjacent and state whether it is a duplicate, a
partial overlap with what this adds, or unrelated; `disposition:` from line 154.

**Issue-ready draft** — problem statement, `file:line` evidence, verified failure scenario, proposed
approach naming the fallback edit concretely, acceptance criteria including the regression test
`P61-D5-009` says does not exist, and proposed labels (area from the repository's existing set, a
`PRIO 1|2|3` mapped from severity, effort `2`/`4`/`8`).

Update the register row at PROJECT.md line 257. Run no `gh` write subcommand.

Finally, add a `## Plan 66-01 accounting` section stating: which of the eight register rows this plan
verdicted (3 rows, 4 records), which finding IDs it allocated, which pre-allocated IDs it left for
66-02 and 66-03, and the literal statement that zero source files were modified and zero tracker
writes occurred — with `git status --porcelain bbj-vscode bbj-intellij java-interop .github` printed
with its literal output.
  </action>
  <verify>
    <automated>grep -c 'P66-D2-001' .planning/reviews/66-COVERAGE.md</automated>
    <automated>grep -c '^## DEBT-03$' .planning/reviews/66-COVERAGE.md; grep -c '^## Plan 66-01 accounting$' .planning/reviews/66-COVERAGE.md</automated>
    <automated>git status --porcelain bbj-vscode bbj-intellij java-interop .github</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEBT-03$' .planning/reviews/66-COVERAGE.md` outputs `1`.
    - `grep -c 'P66-D2-001' .planning/reviews/66-COVERAGE.md` outputs at least `1`.
    - The `## DEBT-03` range cites both inherited records: `sed -n '/^## DEBT-03/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'P61-D2-011'` and the same range's `grep -c 'P61-D5-009'` each output at least `1`.
    - The `P66-D2-001` record carries `dimension: D2` and a `secondary:` line naming `D5`.
    - The `P66-D2-001` record's `evidence_tier:` is `repro` and the record states which of INVENTORY §3b's two forms clears it.
    - The record's `effort:` matches `^effort: *(2|4|8)$` after whitespace stripping and its `disposition:` is one of INVENTORY line 154's six values.
    - The `## DEBT-03` range names `bbj-type-inferer.ts` and the `resolvedReturnType` identifier: the range's `grep -c 'resolvedReturnType'` outputs at least `1`.
    - The `dedup:` field explicitly names issue `#466` and states duplicate, partial-overlap, or unrelated.
    - `grep -c '^## Plan 66-01 accounting$' .planning/reviews/66-COVERAGE.md` outputs `1`, and that section states 3 verdicted rows and lists the finding IDs allocated by this plan.
    - `sed -n '/^## Debt Denominator Register/,/^## /p' .planning/reviews/66-COVERAGE.md | grep -c 'pending 66-0'` outputs `5` — the five rows 66-02 and 66-03 still owe, with no blank cells.
    - `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
    - `git status --porcelain .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md .planning/reviews/65-COVERAGE.md` prints nothing.
  </acceptance_criteria>
  <done>
    `## DEBT-03` carries both inherited citations, a current-code line-by-line trace clearing the
    `repro` bar, a verdict, `P66-D2-001`, and an issue-ready draft; the plan's accounting section
    states what it closed and what it left; three of eight register rows are verdicted.
  </done>
</task>

</tasks>

<threat_model>
This phase modifies zero source files and writes nothing to the GitHub tracker. The genuine threat
surface is **evidence integrity and boundary violation**, not application security. ASVS L1; block on
`high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → GitHub tracker | A public, append-mostly external system. Reads are safe; a write is irreversible and is Phase 69's exclusive right (D-02, ISSUE-01). |
| executor → immutable review records | `INVENTORY.md` (Phase 60 D-09) and the five closed `6N-COVERAGE.md` files are frozen inputs. An edit destroys the audit trail five phases depend on. |
| executor → source trees | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` are read-only evidence anchors in a verdict-only phase (D-01). |
| inherited record → new verdict | Evidence crosses from a closed phase into a new one by citation. An uncurrency-checked citation silently asserts a fact about code that may have moved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Tampering | GitHub tracker (`gh` CLI) | high | mitigate | Task 1 `checkpoint:decision` gates the one-way door before any draft is written; every task's `<action>` ends "run no `gh` write subcommand"; only `gh issue view`/`gh issue list` appear anywhere in the plan; 66-03's D-15 gate 4 asserts no tracker write occurred. |
| T-66-02 | Tampering | `INVENTORY.md`, `61..65-COVERAGE.md` | high | mitigate | Every task's `<acceptance_criteria>` requires `git status --porcelain` over all six files to print nothing; a disagreement with a closed record is recorded as a finding here with settling evidence (the `P63-D1-002` withdrawal precedent), never as an edit. |
| T-66-03 | Tampering | `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` | medium | mitigate | D-01 makes the phase verdict-only; every task's `<verify>` and `<acceptance_criteria>` run `git status --porcelain` over the four trees and require empty output; the plan's `files_modified` names one `.planning/` file. |
| T-66-04 | Repudiation | `P66-*` evidence fields | medium | mitigate | The `no-fabricated-evidence` prohibition; `evidence_tier:` must name which INVENTORY §3b form clears it; DEBT-03 explicitly forbids describing Phase 61's deleted throwaway test as freshly reproduced; every re-derived count is printed with its literal command output. |
| T-66-05 | Information disclosure | `gh` authenticated reads | low | accept | Read-only `gh issue view`/`list` against a repository the executor already has credentials for; no token value is written into any artifact. Credential handling itself is canon security — owned by `/gsd-secure-phase`, not re-litigated here. |
| T-66-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs **zero** package-manager install tasks — no `npm install`, `pip install` or `cargo add` appears in any `<action>`. INVENTORY's package-legitimacy gate has nothing to gate; no RESEARCH.md audit table is required. Verifiable: `grep -cE 'npm install\|pip install\|cargo add' .planning/phases/66-known-debt-re-triage/66-01-PLAN.md` outputs `0`. |
</threat_model>

<verification>
- `.planning/reviews/66-COVERAGE.md` exists and its Debt Denominator Register enumerates 8 rows, one
  per PROJECT.md bullet at lines 250-257, with no blank verdict cell.
- The D-04 denominator command's literal output is printed in the register; a value other than `8`
  carries a written drift cause.
- DEBT-01, DEBT-02 and DEBT-03 each carry a complete section; four `P66-*` records exist
  (`P66-D3-001`, `P66-D5-001`, `P66-D5-002`, `P66-D2-001`).
- Every `effort:` in the file matches `^effort: *(2|4|8)$` after whitespace stripping.
- Every `disposition:` is one of INVENTORY line 154's six values, with a reason on the last four.
- No `RU-66-*` token and no `triage:` field appears anywhere in the file.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github` prints nothing.
- `git status --porcelain` over `INVENTORY.md` and all five closed `6N-COVERAGE.md` files prints nothing.
- No `gh` write subcommand ran.
</verification>

<success_criteria>
Three of the eight denominator rows are verdicted with INVENTORY-format finding records and
issue-ready drafts; the remaining five are enumerated with explicit `pending` cells that 66-02 and
66-03 fill; the file's header states its own completeness construct; the phase's zero-source-change
and zero-tracker-write boundaries hold and are evidenced by literal command output.
</success_criteria>

<output>
Create `.planning/phases/66-known-debt-re-triage/66-01-SUMMARY.md` when done.
</output>
