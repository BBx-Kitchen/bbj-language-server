---
phase: 104-conformance-measurement-milestone-exit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/coder/repos/bbj-corpus/conformance/run.mjs
  - /home/coder/repos/bbj-corpus/conformance/worker.mts
  - /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs
  - /home/coder/repos/bbj-corpus/conformance/.gitignore
  - /home/coder/repos/bbj-corpus/README.md
  - /home/coder/repos/bbj-corpus/conformance/REPORT.md
  - /home/coder/repos/bbj-corpus/conformance/summary.json
  - /home/coder/repos/bbj-corpus/conformance/details.json
  - /home/coder/repos/bbj-corpus/conformance/history.jsonl
autonomous: true
requirements: [CONF-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node run.mjs --ls <repo> --endpoint 127.0.0.1:5008` sends every accepted corpus file and every reject to the real `parseProgram` endpoint through one `JavaInteropService` per shard, one request at a time per shard, and reports list B with the endpoint's verdict applied (D-01, D-02)."
    - "In endpoint mode every file is built with validation on the hermetic fake-interop services even when it has a syntax error, and the verdict is folded in by the language server's own `reconcileWithVerdict` then `applyDiagnosticHierarchy(…, true, 20)` — nothing is reimplemented; a failed call falls back to `applyDiagnosticHierarchy(raw, true, 20)` for that file only (D-01, D-04)."
    - "REPORT.md and summary.json in endpoint mode carry Langium's raw A, A2 and B unchanged beside the reconciled view, a row for accepted files that drew a BBj Parser error, the endpoint failure count and kinds, and a `gate` key that reads A and A2 from the raw view and B from the reconciled view (D-02, D-03, D-04)."
    - "Without `--endpoint`, the harness's classification is unchanged: a `--limit 150` sample taken before any edit and one taken after all edits produce identical REPORT-sample.md bodies and identical summaries apart from date, seconds and the language-server commit (D-01)."
    - "Endpoint-mode history lines carry an `endpoint` field (host, port, calls, failures, failure kinds, retries, `bbjVersion: null` because the endpoint reports no version); endpoint-off lines keep exactly today's shape (D-15)."
    - "`--data <dir>` measures another corpus checkout without touching corpus data; the baseline build (11,898 accepted, 1,210 rejects, compiler build of September 1 2026) is read from a detached worktree at `cdaf3761`, asserted before every run (D-05)."
    - "A full endpoint run on the baseline build has zero endpoint failures and no crash and reproduces the Phase 103 probe's list-B file set (or a re-run of the probe on the same tree), after which the probe script and its JSON are deleted and `snapshots/` is ignored by git (D-14)."
    - "The corpus README's conformance section documents endpoint mode: the flag, BBjServices with `parseProgram` (BBj 26.03 or later), the measured runtime, raw versus reconciled columns, that validation always runs in endpoint mode, and that a gate run counts only with zero endpoint failures (D-13)."
    - "Every bbj-corpus commit stages exact paths only, `eval/` is never staged or touched, and nothing is pushed — the repository has no remote (D-12)."
  artifacts:
    - path: "/home/coder/repos/bbj-corpus/conformance/worker.mts"
      provides: "Endpoint branch: per-file real parse verdict, product reconciliation, failure classification, sanity check, explicit exit"
      contains: "reconcileWithVerdict"
    - path: "/home/coder/repos/bbj-corpus/conformance/run.mjs"
      provides: "`--endpoint` and `--data` flags, reconciled classification, endpoint summary/history field, gate key"
      contains: "with the BBj verdict"
    - path: "/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs"
      provides: "Checks files bound for the public repository for corpus ids, file names, origin names and flagged source lines"
      contains: "LEAK"
    - path: "/home/coder/repos/bbj-corpus/conformance/.gitignore"
      provides: "snapshots/ and REPORT-sample.md ignored as scratch"
      contains: "snapshots/"
    - path: "/home/coder/repos/bbj-corpus/README.md"
      provides: "Endpoint-mode procedure for maintainers"
      contains: "--endpoint"
  key_links:
    - from: "/home/coder/repos/bbj-corpus/conformance/run.mjs"
      to: "/home/coder/repos/bbj-corpus/conformance/worker.mts"
      via: "spawn argv gains the endpoint string as a 5th element only when --endpoint is given"
      pattern: "worker\\.mts"
    - from: "/home/coder/repos/bbj-corpus/conformance/worker.mts"
      to: "/home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts"
      via: "createBBjServices(NodeFileSystem) then JavaInteropService.setConnectionConfig(host, port) and parseProgram per file"
      pattern: "setConnectionConfig"
    - from: "/home/coder/repos/bbj-corpus/conformance/worker.mts"
      to: "/home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts"
      via: "absolute-path dynamic import of reconcileWithVerdict, documentLineText, recallLangiumDiagnostics, isSyntaxComplaint"
      pattern: "bbj-diagnostic-reconciliation\\.ts"
    - from: "/home/coder/repos/bbj-corpus/conformance/run.mjs"
      to: "/home/coder/repos/bbj-corpus/conformance/history.jsonl"
      via: "summary object (with the endpoint key in endpoint mode) appended as one line per full run"
      pattern: "appendFileSync"
---

<objective>
Give the private conformance harness an endpoint mode, prove it end to end, and reproduce the Phase
103 probe with it on the milestone's own corpus build.

Purpose: CONF-02 — the conformance run can include the `bbj-ls` endpoint, reports list B with it, and
the way to run it is documented for maintainers. This plan also builds the two tools the rest of the
phase leans on: `--data` (so the gate reads the September 1 build from a detached worktree instead of
the rebuilt `main`) and `leak-guard.mjs` (so nothing corpus-derived reaches the public repository).
Output: endpoint mode in `run.mjs`/`worker.mts`, `--data`, `leak-guard.mjs`, `.gitignore` entries, the
README section, a reproduction run on the baseline build, the probe retired, and three bbj-corpus
commits. Nothing in `bbj-language-server` changes except this plan's SUMMARY.
</objective>

<executor_shell_rules>
Read this before running anything. The permission hooks block opaque shell reads and cannot tell a
scoped search from a secret read; a blocked command stalls a background executor on an unanswered
prompt.

- Prefer the Read tool for reading files. For content search use `grep` against an exact absolute
  file path (the Grep tool may be unavailable in this environment).
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. Use `git -C <repo> …` for
  every git command. `run.mjs` needs no working directory: call it as
  `node /home/coder/repos/bbj-corpus/conformance/run.mjs …`.
- No bare recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or any
  credential store.
- In bbj-corpus: `git add <exact path>` only — never `-A`, never `.`, never a directory. `eval/`
  belongs to another project: never read, stage, move or delete anything under it. Never `git stash`,
  never `git reset`, `git rebase` or `git checkout` of any branch in the bbj-corpus main working tree.
  Never modify corpus data (`corpus/`, `rejects/`, `manifest.jsonl`, `rejects.jsonl`,
  `build-info.json`, `sources.json`) in either the main tree or the worktree. There is no remote: never
  push.
- Commits: plain `git -C … commit -m "<subject>" -m "<body>" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"`
  (the GSD commit helper writes no trailer).
- Long runs (any full harness run, any endpoint run over more than a few hundred files): start them
  with the Bash tool's `run_in_background` (or `nohup … &` with output to a log under
  `/home/coder/repos/bbj-corpus/conformance/snapshots/`), then poll the log and the process list.
  Never let one tool call block past ten minutes.
- Nothing corpus-derived goes into this repository: this plan's SUMMARY holds counts, commit hashes
  and own-words descriptions only — no corpus id, file name, path under `corpus/` or `rejects/`, or
  source line. Run `leak-guard.mjs` over the SUMMARY before its commit.
- Record deviations in the SUMMARY only; do not add `WINDOWS.md` entries. Do not mark CONF-02 complete
  in `REQUIREMENTS.md` — plans 02 and 03 still declare it; the phase verification does that.
</executor_shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-conformance-measurement-milestone-exit/104-CONTEXT.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-RESEARCH.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-PATTERNS.md
@.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md
</context>

<artifacts_this_phase_produces>
Phase-wide list; the plan that produces each item is in brackets.

bbj-corpus (private, local-only):
- `conformance/run.mjs` [01] — flags `--endpoint <host>:<port>` (allowed only with mode `validate`;
  a present-but-malformed value or `--mode parser` exits 2 with a usage line) and `--data <dir>`
  (corpus checkout root; default stays the parent of `conformance/`). Endpoint-mode-only summary keys:
  `endpoint { host, port, calls, failures, failuresByKind, retries, retriedFiles, bbjVersion }`
  (inserted right after `mode`), `corpus.reconciled { withError, syntaxErrorRemains, drewBbjParserError }`,
  `rejects.reconciled { missed, byKind { syntax|semantic { total, error, 'warning only', missed, bbjAccepted } } }`,
  `checkExceptions { corpus, rejects, rejectsCaughtOnlyThisWay }`, `gate { A, A2, B, endpointFailures }`.
  Endpoint-mode-only details.json keys: `missedReconciled`, `acceptedWithError`,
  `acceptedDrewBbjParserError`, `endpointFailures`, `checkExceptions`. REPORT.md rows and sections
  labelled with the literal phrase "with the BBj verdict".
- `conformance/worker.mts` [01] — 5th argv `endpoint`; `APPLICATION_ERROR_KINDS`, `REQUEST_CANCELLED`,
  `classifyFailureKind(code)`, `RETRY_KINDS`, `RETRY_WAITS_MS`, `PARSE_WATCHDOG_MS`, `CHECK_EXCEPTION`,
  `reconcileFile(document, text, index)`; per-job result keys `reconciled { outcome, attempts, bbjErrors,
  errors, warnings, syntaxErrorRemains, onlyCheckExceptionErrors, first, firstBbj }` and
  `checkExceptions` (count), endpoint mode only.
- `conformance/leak-guard.mjs` [01, new] — `node leak-guard.mjs [--data <dir>] <file>…`; exit 1 and
  `LEAK <file> line <n>: <kind>` per hit, never echoing the matched text; exit 0 with `clean: <file>`.
- `conformance/.gitignore` [01] — `snapshots/`, `REPORT-sample.md`.
- `README.md` §"Conformance of the language server" [01 endpoint mode, 03 exit line].
- `conformance/REPORT.md`, `summary.json`, `details.json`, `history.jsonl` [01 commits the 98-103
  results; 03 commits the closing run].
- Scratch, untracked: `conformance/snapshots/phase-104-*` files [01-03]; the detached baseline
  worktree `/home/coder/repos/bbj-corpus-baseline` at `cdaf3761` [created 01, removed 03].

bbj-language-server (public):
- `bbj-vscode/test/test-data/conformance/README.md` [02, new] — maintainer pointer, no corpus content.
- `.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md` [02, new].
- `.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md` [03, new] — exit
  record with the fixed-format line `**Exit gate: A = …, A2 = …, B = … of 1,210 (… %, ≤ 5 %), endpoint failures = 0 — passed.**`.
- `.planning/PROJECT.md` [03] — one `**Conformance starting point:**` line under "Next Milestone Goals".
- PR #691 title and body [03].
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: with the BBj verdict -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one reject goes through the real endpoint and lands as a "with the BBj verdict" list-B row</name>
  <files>/home/coder/repos/bbj-corpus/conformance/run.mjs, /home/coder/repos/bbj-corpus/conformance/worker.mts, /home/coder/repos/bbj-corpus/conformance/REPORT.md, /home/coder/repos/bbj-corpus/conformance/summary.json, /home/coder/repos/bbj-corpus/conformance/details.json, /home/coder/repos/bbj-corpus/conformance/history.jsonl (the last four committed as they are, not edited)</files>
  <precondition>BBjServices accepts a connection on 127.0.0.1:5008 (`bash -c '</dev/tcp/127.0.0.1/5008'` exits 0) and `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx` exists.</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/run.mjs — the whole file: argument idiom (line 14), spawn call (lines 35-41), classification (lines 59-76), summary literal (lines 80-88), report helpers (lines 90-138)
    - /home/coder/repos/bbj-corpus/conformance/worker.mts — the whole file: argv, import idiom, the per-job loop and result contract, the validate-only-without-syntax-errors guard
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts — the code to promote (untracked scratch): imports (lines 26-37), real services (49-50), failure-kind table (58-70), `processFile` (74-121), sanity check (123-137)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts — `reconcileWithVerdict` (line 180), `documentLineText` (44), `isSyntaxComplaint` (83), `recallLangiumDiagnostics` (355)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-document-validator.ts — `applyDiagnosticHierarchy` (lines 106-178)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-parser-service.ts — `BBJ_PARSER_SOURCE` (16), `parseErrorsToDiagnostics` (67), and the module-private failure-code classification the harness must duplicate
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/java-interop.ts — `setConnectionConfig` (406), `parseProgram` (478-510): no client-side timeout, dedicated parse lane
    - /home/coder/repos/bbj-ls/src/main/java/bbj/interop/ParserWorker.java — lines 71-125 and 260-320: the `-3300x` codes, the 10 s per-parse timeout, and the fail-fast timeout answer while a previous parse on the same connection is still overrunning
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/conformance-harness-overwrites-details.md — snapshot discipline, never snapshot under `work/`
  </read_first>
  <action>
    Step 0 — preserve the 98-103 results before anything overwrites them (per D-12). Run
    `git -C /home/coder/repos/bbj-corpus status --short`: it must show exactly ` M` for
    `conformance/REPORT.md`, `conformance/details.json`, `conformance/history.jsonl`,
    `conformance/summary.json` and `??` for `conformance/snapshots/` and `eval/`; anything else, stop
    and report. Confirm `conformance/summary.json` reads `corpus.files` 11898, `rejects.files` 1210 and
    compiler build "build on September 1 2026". Stage those four paths by exact name and commit with
    subject "conformance: record the language-server runs through Phase 103". Write the resulting
    commit hash into the SUMMARY as the "Phase 103 endpoint-off baseline commit" — plan 03 extracts
    `details.json` from it.

    Step 1 — capture the flag-off "before" sample with the harness still unedited:
    `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --limit 150 > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-before-summary.json`,
    then copy `/home/coder/repos/bbj-corpus/conformance/REPORT-sample.md` to
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-before-REPORT-sample.md`.
    Record `git -C /home/coder/repos/bbj-corpus log -1 --format=%h -- manifest.jsonl rejects.jsonl`
    in the SUMMARY (task 2 compares against the same data).

    Step 2 — `run.mjs` (per D-01): add `const endpoint = args.endpoint ?? '';` beside the other
    arguments. If the key `endpoint` is present in `args` and its value does not match
    `^[^\s:]+:\d+$`, or if it is set while `mode` is not `validate`, print one usage line to stderr and
    exit 2. Split host and port at the last colon. Append `endpoint` to the worker's spawn argument
    array ONLY when it is non-empty, so the flag-off spawn is unchanged.

    Step 3 — `worker.mts`, promoting the probe (per D-01, D-02, D-04; nothing reimplemented):
    destructure a fifth argv `endpoint`. When it is non-empty — and only then — dynamically import, with
    the file's existing absolute-path idiom, `NodeFileSystem` from
    `${vscode}/node_modules/langium/lib/node/index.js`, `createBBjServices` from
    `${vscode}/src/language/bbj-module.ts`, `parseErrorsToDiagnostics` and `BBJ_PARSER_SOURCE` from
    `${vscode}/src/language/bbj-parser-service.ts`, `reconcileWithVerdict`, `documentLineText`,
    `recallLangiumDiagnostics` and `isSyntaxComplaint` from
    `${vscode}/src/language/bbj-diagnostic-reconciliation.ts`, and `applyDiagnosticHierarchy` from
    `${vscode}/src/language/bbj-document-validator.ts`. Create one `createBBjServices(NodeFileSystem)`
    per shard and call `.BBj.java.JavaInteropService.setConnectionConfig(host, Number(port))` once.
    Duplicate the probe's `APPLICATION_ERROR_KINDS` (-33001 parser-exception, -33002 timeout, -33003
    size-cap, -33004 service-unavailable, -33005 protected-program), `REQUEST_CANCELLED` (-32800) and
    `classifyFailureKind` exactly (the language server's own copy is module-private, and no export is
    added to the language server). Run the probe's sanity check once per shard before the job loop: a
    document built from the text `x = (1 + 2` plus a newline must give a non-empty
    `recallLangiumDiagnostics`, otherwise print a FATAL line and `process.exit(1)`.

    In the job loop, widen the validation guard to `endpoint || (mode === 'validate' && result.syntaxErrors === 0)`
    (the flag-off expression is unchanged; endpoint mode validates every file, as the product validates
    every open document). Inside that block, after the existing `validationErrors`,
    `validationWarnings` and `firstValidation` are computed exactly as today and before
    `builder.update([], [uri])`, when `endpoint` is set: `result.reconciled = await reconcileFile(document, text, index)`
    and `result.checkExceptions` = the number of `document.diagnostics` whose message matches
    `CHECK_EXCEPTION` (`/^An error occurred during validation/`, the text Langium's registry emits when
    a check throws).

    `reconcileFile`: `raw = recallLangiumDiagnostics(document) ?? []`; call `parseProgram` with `text`,
    `canonicalName` = `path.join(os.tmpdir(), 'conformance-' + process.pid + '-' + index + '.bbj')`,
    `version: 'conformance'`, `prefixes: []`, `workspaceRoots: []`, raced against a client watchdog of
    `PARSE_WATCHDOG_MS` = 60000 (failure kind `watchdog`), awaiting each call before the next — never
    `Promise.all` over a shard's jobs. Attach a no-op `.catch` to the request promise before the race
    (as `java-interop.ts` does for its class-info race, around line 471) so a rejection arriving after
    the watchdog fired cannot become an unhandled rejection that kills the shard, and clear the
    watchdog timer once the race settles. A result without an `errors` array is failure kind
    `malformed-result`. Retry transient failures: kinds in `RETRY_KINDS` (`timeout`,
    `service-unavailable`, `transport`, `watchdog`) are retried after waits of `RETRY_WAITS_MS`
    = [5000, 15000, 30000] (the server fails a request fast while an earlier parse on the same
    connection still overruns its 10 s timeout); other kinds are not retried. On a verdict:
    `parseErrorsToDiagnostics(errors, document.textDocument.lineCount, 20)` →
    `reconcileWithVerdict(raw, verdictDiagnostics, documentLineText(document.textDocument)).diagnostics`
    → `applyDiagnosticHierarchy(…, true, 20)`. On a final failure: `applyDiagnosticHierarchy(raw, true, 20)`
    and outcome `failed:<kind>`. Of the resulting list, drop diagnostics whose `data.code` is
    `'linking-error'` or whose message matches the existing `ENVIRONMENT` regex, exactly as the probe
    did. Return `{ outcome ('verdict' | 'failed:<kind>'), attempts, bbjErrors (verdict error count), errors
    (Error-severity count), warnings (Warning-severity count), syntaxErrorRemains (an Error for which
    isSyntaxComplaint is true), onlyCheckExceptionErrors (errors > 0 and every Error message matches
    CHECK_EXCEPTION), first (first Error else first Warning as { severity 'error'|'warning', message
    first line ≤ 200 chars, line 1-based, source text line ≤ 200 chars, fromBbj: d.source === BBJ_PARSER_SOURCE }),
    firstBbj (first verdict diagnostic as { message, line, source }, only when bbjErrors > 0) }`.
    After `writeFileSync(outFile, …)`, call `process.exit(0)` in endpoint mode only — the open interop
    sockets would otherwise keep the shard alive and `run.mjs` would wait forever.

    Step 4 — `run.mjs`, minimal reconciled path: `reconciledCaughtBy(entry)` = `'error'` when
    `entry.result.reconciled?.errors > 0`, else `'warning only'` when `warnings > 0`, else `'missed'`;
    `missedReconciled` = rejects whose reconciled verdict is not `'error'` (a warning-only file is missed,
    matching today's list-B rule). In endpoint mode only, insert into the summary literal right after
    `mode` (conditional spread, so the flag-off key order is untouched) an `endpoint` object with
    `host`, `port`, `calls` (files that went to the endpoint — one per job carrying a `reconciled`
    result, so a full baseline run has 13,108), `failures` (files whose final outcome is `failed:…`),
    `failuresByKind`, `retries` (attempts beyond the first, summed), `retriedFiles` (files with more
    than one attempt) and `bbjVersion: null`, and add `rejects.reconciled.missed`. In the REPORT header line, right after
    the mode, add ", endpoint" followed by the host:port in code format and "(N calls, F failed)"; and
    after the existing B row add the row `| **B with the BBj verdict.** Invalid code not flagged as an error once the endpoint's verdict is applied | n | N | share |`.

    Step 5 — prove the path: run the verify command below (a 20-per-set sample of `main`'s current
    data is enough to prove the wiring; sample runs write only REPORT-sample.md and stdout).
  </action>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --limit 20 --shards 2 > /tmp/bbj-104-01-tracer-summary.json && node -e 'const s=require("/tmp/bbj-104-01-tracer-summary.json");if(!s.endpoint||!(s.endpoint.calls>0)||s.endpoint.failures!==0||s.endpoint.bbjVersion!==null||typeof s.rejects?.reconciled?.missed!=="number"){console.error("TRACER FAIL "+JSON.stringify(s.endpoint));process.exit(1)}console.log("tracer ok: "+s.endpoint.calls+" calls, reconciled missed "+s.rejects.reconciled.missed)'</automated>
    <fails_when>non-zero exit; stderr shows `shard N exited with`, a `FATAL` sanity line or a usage line; or the printed summary has no `endpoint` key, zero calls, a non-zero failure count, a non-null `bbjVersion`, or no numeric `rejects.reconciled.missed`</fails_when>
    <automated>grep -c 'with the BBj verdict' /home/coder/repos/bbj-corpus/conformance/REPORT-sample.md</automated>
    <fails_when>prints 0 or the file is missing — the sample report carries no reconciled list-B row</fails_when>
    <automated>git -C /home/coder/repos/bbj-corpus diff --quiet HEAD -- conformance/REPORT.md conformance/summary.json conformance/details.json conformance/history.jsonl</automated>
    <fails_when>non-zero exit — the 98-103 result files were not committed in Step 0 or were changed afterwards</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-corpus log -1 --format=%s -- conformance/history.jsonl` prints "conformance: record the language-server runs through Phase 103", and `git -C /home/coder/repos/bbj-corpus show --name-only --format= <that commit>` lists exactly the four result files
    - `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-before-REPORT-sample.md` and `phase-104-flagoff-before-summary.json` exist and were written before the first edit to `run.mjs` or `worker.mts`
    - `grep -c 'reconcileWithVerdict' /home/coder/repos/bbj-corpus/conformance/worker.mts` ≥ 1 and `grep -c 'process.exit(0)' /home/coder/repos/bbj-corpus/conformance/worker.mts` ≥ 1
    - `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --mode parser --endpoint 127.0.0.1:5008 --limit 5` exits 2
    - `git -C /home/coder/repos/bbj-corpus status --porcelain -- eval` prints `?? eval/`
  </acceptance_criteria>
  <done>A sampled endpoint run finishes with zero failures and a "with the BBj verdict" list-B row, produced by the language server's own reconciliation over the real endpoint, and the 98-103 results are safe in a commit.</done>
</task>

<task type="auto">
  <name>Task 2: Both views, the disagreement row, failure kinds, --data and the leak guard — flag-off output unchanged</name>
  <files>/home/coder/repos/bbj-corpus/conformance/run.mjs, /home/coder/repos/bbj-corpus/conformance/worker.mts, /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs, /home/coder/repos/bbj-corpus/conformance/.gitignore</files>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/run.mjs — as left by task 1
    - /home/coder/repos/bbj-corpus/conformance/worker.mts — as left by task 1
    - /home/coder/repos/bbj-corpus/conformance/.gitignore
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts — lines 175-219: the probe's aggregate measures (bbjAccepted, drew-a-BBj-error, still carries a syntax error) the report must be able to reproduce
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md — the measures a reader will compare against
  </read_first>
  <action>
    Part A — `--data` (per D-05): move the `root` definition below the argument parsing as
    `args.data ? path.resolve(args.data) : path.dirname(here)`, so every read of `manifest.jsonl`,
    `rejects.jsonl`, `build-info.json`, `corpus/` and `rejects/` follows it while REPORT/summary/
    details/history are still written next to `run.mjs`. Exit 2 with a usage line when the given
    directory has no `manifest.jsonl`. The default is unchanged.

    Part B — both views in endpoint mode (per D-02, D-03, D-04, D-15), every addition guarded by
    `endpoint` so the flag-off output is untouched. Keep the raw rows (A, A2, B, crashes) and their
    computation exactly as they are; they are Langium's own view, the same meaning as the first run.
    Summary: `corpus.reconciled { withError, syntaxErrorRemains, drewBbjParserError }` (accepted files
    with a reconciled Error; with a surviving syntax Error; whose verdict had `bbjErrors > 0`),
    `rejects.reconciled.byKind` keyed by the existing `isSyntaxReject` split with
    `{ total, error, 'warning only', missed, bbjAccepted }` (bbjAccepted = outcome `verdict` and 0 BBj
    errors), `endpoint.failuresByKind`, `endpoint.retries` (extra attempts) and
    `endpoint.retriedFiles`, `checkExceptions { corpus, rejects, rejectsCaughtOnlyThisWay }` (the last
    counts rejects whose reconciled Errors are all check-exception messages), and
    `gate { A: falseRejects.length, A2: falseAlarms.length, B: missedReconciled.length, endpointFailures }`.
    REPORT.md: after the existing rows, rows for "Valid code with an error once the endpoint's verdict is
    applied (with the BBj verdict)", "Valid code where a language-server syntax error survives the
    verdict", "Valid code that drew a BBj Parser error (endpoint and compiler disagree)", "Endpoint
    calls that failed (file measured without a verdict)" with the kinds, and "Files where a
    validation check threw"; one sentence saying the unmarked rows are Langium alone, the rows marked
    "with the BBj verdict" apply the language server's own reconciliation, and the exit gate reads A
    and A2 raw, B with the verdict, and needs zero endpoint failures; a reconciled reject table
    (Compiler | Files | Error | Warning only | Nothing | BBj accepted); sections, via the existing
    `section`/`group`/`firstWord` helpers, "B with the BBj verdict. Invalid code not flagged, by first
    word of the line the compiler rejects", and — only when non-empty — "Valid code that drew a BBj
    Parser error, by first word of the BBj error line" (from `firstBbj`), "Valid code with an error
    with the BBj verdict, by message", "Endpoint failures, by kind" and "Validation checks that
    threw, by message". details.json (full runs only, as today): add `missedReconciled` (id, compiler
    error, reconciled `first`), `acceptedWithError` (id, syntaxErrorRemains, first),
    `acceptedDrewBbjParserError` (id, firstBbj), `endpointFailures` (id, kind, attempts) and
    `checkExceptions` (id, set `corpus`|`rejects`, first matching message ≤ 200 chars). Crashed entries
    carry no `reconciled`; guard every read with optional chaining.

    Part C — `conformance/leak-guard.mjs` (new; the mitigation for the phase's main threat):
    `node leak-guard.mjs [--data <dir>] <file>…`. Data root as in `run.mjs`. It collects every `id`
    and its basename (text after the last `/`) from `<root>/manifest.jsonl` and `<root>/rejects.jsonl`,
    every manifest/reject `origin` of 8 or more characters, every reject `errors[].source` and every
    string property named `source` at any depth of the `details.json` next to the script (trimmed,
    15 or more characters), plus the id-shape pattern `\b\d+-[0-9a-f]{8}\.bbj\b`. For each target file
    it prints `LEAK <file> line <n>: <kind>` per hit (kind: corpus id, corpus file name, origin name,
    id-shaped name, corpus source line) without ever printing the matched text, else `clean: <file>`;
    a final line gives the counts checked; exit 1 on any hit.

    Part D — `.gitignore`: append `snapshots/` and `REPORT-sample.md` (per D-14; the second is the
    `--limit` scratch output).

    Part E — prove it: run the three verify commands below. Then, as an audit only, run the guard
    over the four earlier public records
    (`/home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md`,
    `…/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md`,
    `…/100-parser-gaps-remaining-groups-long-tail-examples/100-CONFORMANCE.md`,
    `…/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md`) and record each hit in the
    SUMMARY by file, line number and kind only; do not edit those files here (plan 03 handles hits
    before its push).

    Part F — commit (per D-12): stage exactly `conformance/run.mjs`, `conformance/worker.mts`,
    `conformance/leak-guard.mjs`, `conformance/.gitignore`; subject "conformance: endpoint mode,
    --data and a leak guard".
  </action>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --limit 150 > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-after-summary.json && diff <(tail -n +4 /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-before-REPORT-sample.md) <(tail -n +4 /home/coder/repos/bbj-corpus/conformance/REPORT-sample.md) && node -e 'const strip=f=>{const s=require(f);delete s.date;delete s.seconds;delete s.languageServer.commit;return JSON.stringify(s)};const a=strip("/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-before-summary.json"),b=strip("/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-flagoff-after-summary.json");if(a!==b){console.error("FLAG-OFF SUMMARY DIFFERS");process.exit(1)}console.log("flag-off output unchanged")'</automated>
    <fails_when>non-zero exit, any `diff` output line, or `FLAG-OFF SUMMARY DIFFERS` — the flag-off classification or report changed (only valid if `git -C /home/coder/repos/bbj-corpus log -1 --format=%h -- manifest.jsonl rejects.jsonl` still equals the value task 1 recorded)</fails_when>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --limit 20 --shards 2 > /tmp/bbj-104-01-t2-summary.json && node -e 'const s=require("/tmp/bbj-104-01-t2-summary.json");const need=[s.gate?.A,s.gate?.A2,s.gate?.B,s.gate?.endpointFailures,s.corpus?.reconciled?.drewBbjParserError,s.corpus?.reconciled?.syntaxErrorRemains,s.rejects?.reconciled?.byKind,s.endpoint?.failuresByKind,s.endpoint?.retries,s.checkExceptions?.rejectsCaughtOnlyThisWay];if(need.some(v=>v===undefined)||s.endpoint.failures!==0||Object.keys(s)[Object.keys(s).indexOf("mode")+1]!=="endpoint"){console.error("ENDPOINT SUMMARY INCOMPLETE");process.exit(1)}console.log("endpoint summary complete")'</automated>
    <fails_when>non-zero exit, `ENDPOINT SUMMARY INCOMPLETE`, a non-zero failure count, or the `endpoint` key not directly after `mode`</fails_when>
    <automated>node -e 'require("fs").writeFileSync("/home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-clean.txt","This note names no corpus content.\n");const id=JSON.parse(require("fs").readFileSync("/home/coder/repos/bbj-corpus/rejects.jsonl","utf8").split("\n")[0]).id;require("fs").writeFileSync("/home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-dirty.txt","mentions "+id+"\n")' && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-clean.txt && ! node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-dirty.txt; R=$?; rm -f /home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-clean.txt /home/coder/repos/bbj-corpus/conformance/snapshots/leak-guard-dirty.txt; exit $R</automated>
    <fails_when>non-zero exit — the guard flags a clean file or passes a file carrying a real reject id</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-corpus show --name-only --format= HEAD` lists exactly `conformance/.gitignore`, `conformance/leak-guard.mjs`, `conformance/run.mjs`, `conformance/worker.mts`
    - `grep -c '^snapshots/$' /home/coder/repos/bbj-corpus/conformance/.gitignore` prints 1 and `grep -c '^REPORT-sample.md$' /home/coder/repos/bbj-corpus/conformance/.gitignore` prints 1
    - `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --data /nonexistent --limit 5` exits 2
    - The SUMMARY lists the leak-guard audit result for each of the four earlier CONFORMANCE.md files (clean, or hits by line and kind only)
    - `git -C /home/coder/repos/bbj-corpus status --porcelain -- eval` prints `?? eval/`
  </acceptance_criteria>
  <done>Endpoint mode reports both views, the disagreement row, failure kinds, retries and check exceptions; `--data` and the leak guard work; the flag-off output is proven unchanged; all of it is committed by exact path.</done>
</task>

<task type="auto">
  <name>Task 3: Baseline worktree, timed sample, full endpoint run reproducing the probe, probe retired, README</name>
  <files>/home/coder/repos/bbj-corpus/README.md, /home/coder/repos/bbj-corpus/conformance/REPORT.md, /home/coder/repos/bbj-corpus/conformance/summary.json, /home/coder/repos/bbj-corpus/conformance/details.json, /home/coder/repos/bbj-corpus/conformance/history.jsonl (the last four rewritten and appended by the run, left uncommitted for plan 03), /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts and phase-103-endpoint-probe.json (deleted — untracked scratch)</files>
  <reversibility rating="costly">Deleting the untracked probe script cannot be undone from git; it is deleted only after the committed endpoint mode has reproduced its list-B file set, per the user's decision.</reversibility>
  <precondition>127.0.0.1:5008 accepts a connection; `git -C /home/coder/repos/bbj-corpus cat-file -e cdaf3761^{commit}` exits 0.</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/README.md — §"Conformance of the language server" (lines 55-66), the section to extend
    - /home/coder/repos/bbj-corpus/conformance/run.mjs and worker.mts — as committed in task 2
    - /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts — `corpusRoot` (line 21) and the output path (line 222), both of which a re-run copy must repoint
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-RESEARCH.md — §"Corpus Rebuild — Blocking Finding" (why the baseline is read from a worktree) and §"Common Pitfalls"
  </read_first>
  <action>
    Step 1 — baseline worktree (per D-05 and the orchestrator's corpus-rebuild resolution):
    `git -C /home/coder/repos/bbj-corpus worktree add --detach /home/coder/repos/bbj-corpus-baseline cdaf3761`.
    Assert, and repeat the assertion immediately before every run on it in this phase:
    `/home/coder/repos/bbj-corpus-baseline/build-info.json` has `corpus_entries` 11898,
    `reject_entries` 1210 and `compiler.build` "build on September 1 2026", and
    `git -C /home/coder/repos/bbj-corpus-baseline status --porcelain` prints nothing. Never edit
    anything in the worktree.

    Step 2 — timed sample and shard choice (Claude's discretion on concurrency; zero failures matters
    more than speed): `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --data /home/coder/repos/bbj-corpus-baseline --limit 300 --shards 4`,
    wrapped with `date +%s` before and after. If `endpoint.failures` > 0, repeat with `--shards 2`,
    then `--shards 1`. Record in the SUMMARY: the chosen shard count, calls, seconds, files per
    second, retries, and the projected wall-clock time for 13,108 files and for the rebuilt `main`
    corpus (read its counts from `/home/coder/repos/bbj-corpus/build-info.json`).

    Step 3 — full reproduction run on the baseline build, in the background:
    `nohup node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server --endpoint 127.0.0.1:5008 --data /home/coder/repos/bbj-corpus-baseline --shards <N> > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-full-run.log 2>&1 &`,
    then poll until the process is gone and the log ends with the summary JSON. It rewrites
    REPORT.md, summary.json and details.json and appends a history line; that is intended (the 98-103
    versions are committed). If `endpoint.failures` > 0 or `crashes` > 0: re-run once at a lower shard
    count; if failures or crashes remain, STOP — do not delete the probe, do not change BBjServices'
    configuration, and report the failure kinds and counts (per D-04 a run with failures is not a
    measurement).

    Step 4 — reproduce the probe (per D-14): compare the set of `missedReconciled` ids in
    `details.json` with the set of reject ids whose `caught` is false in
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.json`, printing counts
    only (both sizes, left, entered). Equal: reproduced. Not equal: copy the probe to
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-probe-rerun.mts`, repoint its
    `corpusRoot` to `/home/coder/repos/bbj-corpus-baseline` and its final output path to
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-probe-rerun.json`, run it with
    `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx <copy> /home/coder/repos/bbj-language-server`
    (stop it if it has not exited a minute after writing its output — it has no explicit exit), and
    compare again. Equal to the re-run: the harness reproduces the probe's method, and the difference
    to the Phase 103 output comes from language-server commits since `311c0170`; list them with
    `git -C /home/coder/repos/bbj-language-server log --oneline 311c0170..HEAD -- bbj-vscode/src/language/bbj-diagnostic-reconciliation.ts bbj-vscode/src/language/bbj-document-validator.ts bbj-vscode/src/language/bbj-parser-service.ts`.
    Not equal to the re-run either: a harness defect — fix it in `run.mjs`/`worker.mts`, commit the fix
    by exact path, and repeat Steps 3-4. Also record the accepted-file measures against the probe's
    (`syntaxErrorRemains` against its "still carries a syntax error"; `drewBbjParserError` now covers
    every accepted file, not only the 31 the probe saw).

    Step 5 — keep the evidence for plans 02 and 03: copy `summary.json` and `details.json` to
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-endpoint-summary.json` and
    `phase-104-01-endpoint-details.json`.

    Step 6 — retire the probe (per D-14): delete
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts`,
    `…/phase-103-endpoint-probe.json` and, if created, `…/phase-104-probe-rerun.mts` and
    `…/phase-104-probe-rerun.json`.

    Step 7 — README (per D-13): extend §"Conformance of the language server" in
    `/home/coder/repos/bbj-corpus/README.md`, same terse tone. Command block gains
    `[--endpoint 127.0.0.1:5008] [--data /path/to/corpus-checkout]`. Then short paragraphs covering:
    endpoint mode sends every accepted file and every reject to the `parseProgram` endpoint of
    BBjServices (`bbj-ls`, BBj 26.03 or later; an older BBjServices answers MethodNotFound and every
    call counts as a failure), one connection per shard and one request at a time per shard, with
    retries for transient timeouts; validation always runs in endpoint mode, even for a file with a
    syntax error, because the product validates every open document — the default mode keeps
    validating only files without a syntax error so its series stays comparable; the measured runtime
    from Step 3 and the shard count; unmarked rows are Langium alone (the same meaning as the first
    run's A, A2, B) and rows marked "with the BBj verdict" apply the language server's own
    reconciliation; the exit gate reads A and A2 raw and B with the verdict, and a gate run counts only
    with zero endpoint failures and no crash; a failed call falls back to Langium's diagnostics for that
    file and is counted; endpoint-mode history lines carry an `endpoint` field, whose `bbjVersion` is
    null because the endpoint reports no version; `--data` measures another checkout, for example a
    detached worktree of an earlier corpus build, while results are still written next to `run.mjs`;
    run `node conformance/leak-guard.mjs <file>…` over anything derived from a run before it goes into a
    public repository; `snapshots/` is ignored scratch. Stage only `README.md`; subject
    "README: conformance endpoint mode". Do not commit the run's result files — plan 03 commits the
    closing run's.

    Step 8 — leave the worktree in place for plans 02 and 03 and record its path and HEAD in the
    SUMMARY.
  </action>
  <verify>
    <automated>node -e 'const s=require("/home/coder/repos/bbj-corpus/conformance/summary.json");const f=[];if(s.corpus.files!==11898)f.push("corpus.files");if(s.rejects.files!==1210)f.push("rejects.files");if(s.compiler.build!=="build on September 1 2026")f.push("compiler");if(!s.endpoint||s.endpoint.failures!==0)f.push("endpoint.failures");if(s.crashes!==0)f.push("crashes");if(s.sampled!==false)f.push("sampled");if(!(s.gate&&s.gate.B<=60))f.push("B");if(f.length){console.error("REPRODUCTION RUN INVALID: "+f.join(", "));process.exit(1)}console.log("reproduction run: A="+s.gate.A+" A2="+s.gate.A2+" B="+s.gate.B+", "+s.endpoint.calls+" calls, 0 failures")'</automated>
    <fails_when>non-zero exit or `REPRODUCTION RUN INVALID` — not a full, failure-free, crash-free endpoint run on the September 1 build, or B above 60</fails_when>
    <automated>test ! -e /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.mts && test ! -e /home/coder/repos/bbj-corpus/conformance/snapshots/phase-103-endpoint-probe.json && test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-endpoint-details.json && git -C /home/coder/repos/bbj-corpus check-ignore -q conformance/snapshots/phase-104-01-endpoint-details.json</automated>
    <fails_when>non-zero exit — the probe still exists, the evidence snapshot is missing, or snapshots/ is not ignored</fails_when>
    <automated>grep -q -- '--endpoint' /home/coder/repos/bbj-corpus/README.md && grep -q -- '--data' /home/coder/repos/bbj-corpus/README.md && grep -q '26.03' /home/coder/repos/bbj-corpus/README.md && grep -qi 'zero endpoint failures' /home/coder/repos/bbj-corpus/README.md && grep -qi 'validation always runs' /home/coder/repos/bbj-corpus/README.md</automated>
    <fails_when>non-zero exit — the README section lacks the flag, the data option, the BBj version prerequisite, the zero-failure rule or the always-validate note</fails_when>
    <automated>test "$(git -C /home/coder/repos/bbj-corpus-baseline rev-parse --short=8 HEAD)" = cdaf3761 && test -z "$(git -C /home/coder/repos/bbj-corpus-baseline status --porcelain)"</automated>
    <fails_when>non-zero exit — the baseline worktree is missing, not at cdaf3761, or not pristine</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-corpus show --name-only --format= HEAD` lists exactly `README.md`
    - The SUMMARY states the probe comparison result (equal, or equal to a same-tree re-run with the language-server commits named) with counts only
    - The SUMMARY records the chosen shard count, files per second, the measured full-run time and both projections
    - `tail -n 1 /home/coder/repos/bbj-corpus/conformance/history.jsonl` is a JSON line with an `endpoint` key and `endpoint.failures` 0
    - `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md` exits 0 before the SUMMARY is committed
    - `git -C /home/coder/repos/bbj-corpus status --porcelain -- eval` prints `?? eval/`
  </acceptance_criteria>
  <done>Endpoint mode has measured the whole September 1 build with zero failures and reproduced the Phase 103 probe, the probe is gone, snapshots are ignored, and maintainers can read how to run it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bbj-corpus working tree ↔ another project | The private clone is shared with an unrelated project that owns `eval/` and rebuilt `main` during this phase. |
| harness → BBjServices on 127.0.0.1:5008 | Local JSON-RPC; the harness is a client of the existing endpoint only. |
| this plan's SUMMARY → the public repository | The SUMMARY is committed to `bbj-language-server`, which is pushed publicly in plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-01 | Information Disclosure | 104-01-SUMMARY.md | high | mitigate | Counts, hashes and own-words only; `leak-guard.mjs` (built in task 2) must exit 0 on the SUMMARY before its commit. |
| T-104-02 | Tampering | bbj-corpus commits | high | mitigate | Exact-path `git add`; `git show --name-only` checked after each of the three commits; `eval/` status asserted unchanged; no stash, reset, rebase or branch checkout in the main tree. |
| T-104-03 | Tampering | measurement input | medium | mitigate | The baseline is read from a detached worktree at `cdaf3761` through `--data`; `build-info.json` counts and compiler build asserted before every run; the worktree is never edited. |
| T-104-04 | Denial of Service | a shard waiting on an unanswered parse | medium | mitigate | Client watchdog of 60 s per call, bounded retries for transient kinds, explicit `process.exit(0)` in endpoint mode; every final failure is counted. |
| T-104-05 | Repudiation | a run that silently measures nothing | medium | mitigate | Per-shard sanity check exits 1 on an empty remembered Langium list; zero calls or a missing `endpoint` key fails the tracer verify. |
| T-104-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; the harness uses its existing `node_modules`. |
</threat_model>

<verification>
- The tracer sample run reports calls > 0, zero failures and a "with the BBj verdict" row.
- The flag-off before/after sample comparison shows no difference.
- The full baseline-build endpoint run is failure-free and crash-free with B ≤ 60, and matches the probe's list-B file set (directly or through a same-tree re-run).
- Three bbj-corpus commits, each with exactly its intended paths; `eval/` untracked and untouched.
</verification>

<success_criteria>
- CONF-02's harness half: the run can be pointed at a BBjServices with the endpoint and reports list B with its verdicts; the way to run it is in the corpus README.
- The probe is superseded and deleted; the baseline worktree and evidence snapshots are ready for plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md` when done
(counts and own-words only; run `leak-guard.mjs` over it before committing).
</output>
