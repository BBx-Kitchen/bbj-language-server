---
phase: 104-conformance-measurement-milestone-exit
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md
  - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md
autonomous: true
requirements: [CONF-02, CONF-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A maintainer opening `bbj-vscode/test/test-data/conformance/` finds a README that says these are synthetic regression programs for constructs the compiler accepts, that `test/conformance-regressions.test.ts` parses and validates each one with no error, that the corpus measurement lives in the private `bbj-corpus` repository and runs locally only, never in CI, with the command shape `conformance/run.mjs --ls <this repo> [--endpoint host:port]`, and that endpoint mode needs a BBjServices of BBj 26.03 or later — with no corpus number, file name or content, no planning identifier, no docs-site link, and CLAUDE.md unchanged (D-09)."
    - "A pending todo records, in own words with no corpus text, that the variable-scoping check throws on a reference node that carries no symbol and is then silently skipped for that file, with a synthetic repro shape if one reproduces and otherwise the shape in words plus the candidates already tried (D-11)."
    - "With BBjServices up, the whole `bbj-vscode` suite fails only on test names inside the linking \"Interop related tests\" block or the issue447 real-interop file, and every one of those names also fails on origin/main in a scratch worktree regenerated with Node 22 — compared by name, not count (D-06)."
    - "With `RUN_BBJ_TESTS=0` (what CI sees) the whole `bbj-vscode` suite reports `numFailedTests` 0, and that run includes the conformance regression files and the examples assertions (D-06)."
    - "IntelliJ `./gradlew test --rerun-tasks` passes with zero failed or errored tests (D-06)."
    - "The VSIX and the IntelliJ plugin zip are built from the final tree, the zip carries the freshly built `main.cjs` byte for byte, and no hand UAT is staged (D-07)."
  artifacts:
    - path: "/home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md"
      provides: "Maintainer pointer from the regression files to the private harness"
      contains: "conformance-regressions.test.ts"
    - path: "/home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md"
      provides: "The Phase 103 check-exception caveat as a pending todo"
      contains: "check-variable-scoping.ts"
  key_links:
    - from: "/home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md"
      to: "/home/coder/repos/bbj-corpus/README.md"
      via: "names the private repository and the run command; the full procedure lives in the corpus README"
      pattern: "bbj-corpus"
    - from: "/home/coder/repos/bbj-language-server/bbj-vscode/test/conformance-regressions.test.ts"
      to: "/home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md"
      via: "the test collects only files ending in .bbj, so the README is never parsed"
      pattern: "endsWith\\('\\.bbj'\\)"
---

<objective>
Put the maintainer pointer next to the regression files, file the check-exception todo, and prove every
existing test suite green on the final tree, then build both distributables from it.

Purpose: CONF-02's "documented for maintainers" half on this side, and CONF-03's "with all existing
test suites passing". The README is the last change to the language-server tree in this phase, so the
suites in tasks 2 and 3 run on exactly the tree plan 03 measures (D-05).
Output: `bbj-vscode/test/test-data/conformance/README.md`, one pending todo, three recorded suite runs
compared by test name against origin/main, an IntelliJ suite run, a VSIX and a plugin zip.
</objective>

<executor_shell_rules>
Read this before running anything. The permission hooks block opaque shell reads and cannot tell a
scoped search from a secret read; a blocked command stalls a background executor on an unanswered
prompt.

- Prefer the Read tool for reading files; for content search use `grep` against an exact absolute
  file path (the Grep tool may be unavailable here).
- Every shell path is absolute and complete. Never `.`, `..` or `~` as a target.
- Never chain `cd` with `grep`, `find`, `cat`, `sed`, `head` or `tail`. `cd` is allowed only directly
  in front of a build tool that needs its project directory (`npx vitest`, `npm`, `npx vsce`,
  `./gradlew`). Use `git -C <repo> …` for every git command.
- vitest runs with `bbj-vscode` as the working directory. Never pass a reporter named `basic`; use
  `--reporter=json --outputFile=<absolute path>` when failing names are needed.
- No bare recursive `grep -r` or `find` over a directory. Never read `.env*`, `*.pem`, `*.key` or any
  credential store.
- `git add <exact path>` only — never `-A`, never `.`. Never `git stash`. Never push in this plan.
- Commits: plain `git -C /home/coder/repos/bbj-language-server commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"`
  (the GSD commit helper writes no trailer).
- No planning identifier (requirement, decision, plan or phase number) in any file under
  `bbj-vscode/`. No corpus content (id, file name, path, source line) anywhere in this repository.
- bbj-corpus is read-only for this plan except scratch files under
  `/home/coder/repos/bbj-corpus/conformance/snapshots/` (ignored). Never touch `eval/`.
- Record deviations in the SUMMARY only; no `WINDOWS.md` entries. Do not mark CONF-02 or CONF-03
  complete in `REQUIREMENTS.md` — plan 03 still declares both; the phase verification does that.
</executor_shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-conformance-measurement-milestone-exit/104-CONTEXT.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-RESEARCH.md
@.planning/phases/104-conformance-measurement-milestone-exit/104-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Phase-wide list; the plan that produces each item is in brackets.

bbj-corpus (private, local-only):
- `conformance/run.mjs` [01] — `--endpoint <host>:<port>`, `--data <dir>`; endpoint-mode summary keys
  `endpoint`, `corpus.reconciled`, `rejects.reconciled`, `checkExceptions`, `gate`; details.json keys
  `missedReconciled`, `acceptedWithError`, `acceptedDrewBbjParserError`, `endpointFailures`,
  `checkExceptions`; REPORT rows labelled "with the BBj verdict".
- `conformance/worker.mts` [01] — 5th argv `endpoint`, `reconcileFile`, `classifyFailureKind`,
  `RETRY_KINDS`, `PARSE_WATCHDOG_MS`, `CHECK_EXCEPTION`; per-job `reconciled` and `checkExceptions`.
- `conformance/leak-guard.mjs` [01, new] — `node leak-guard.mjs [--data <dir>] <file>…`.
- `conformance/.gitignore` [01] — `snapshots/`, `REPORT-sample.md`.
- `README.md` §"Conformance of the language server" [01, 03].
- `conformance/REPORT.md`, `summary.json`, `details.json`, `history.jsonl` [01, 03].
- Scratch: `conformance/snapshots/phase-104-*` [01-03]; worktree `/home/coder/repos/bbj-corpus-baseline` [01 → 03].

bbj-language-server (public):
- `bbj-vscode/test/test-data/conformance/README.md` [02, new].
- `.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md` [02, new].
- `.planning/phases/104-conformance-measurement-milestone-exit/104-CONFORMANCE.md` [03, new].
- `.planning/PROJECT.md` `**Conformance starting point:**` line [03].
- PR #691 title and body [03].
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: CONF-0|PSRV-0|D-[0-9][0-9]|[Pp]hase [0-9]|10[0-9]-[0-9][0-9]|9[89]-[0-9][0-9] -->
<!-- planner-discipline-allow: 11[,.]?898|1[,.]?210|16[,.]?884|4[,.]?615|\b(168|267|658|669)\b -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the maintainer pointer and the scoping-check todo land, and the regression suites still pass the way CI runs them</name>
  <files>/home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md, /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md</files>
  <precondition>`/home/coder/repos/bbj-corpus/conformance/leak-guard.mjs`, `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-endpoint-details.json` and the worktree `/home/coder/repos/bbj-corpus-baseline` exist (plan 01).</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/conformance-regressions.test.ts — the test that parses and validates this folder (collects only `.bbj` files)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/example-files.test.ts — reads only the flat `test-data` folder, non-recursively, so it never sees this folder
    - /home/coder/repos/bbj-corpus/README.md — §"Conformance of the language server" as extended by plan 01 (command shape to mirror)
    - /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-20-linking-interop-failures-survive-class-warmup.md — the todo template (frontmatter, Problem / Investigation / Reproduction / Fix options)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/validations/check-variable-scoping.ts — lines 55-290: `getSymbolRefName` (62-67), `checkUseBeforeAssignment` (97-) and the inline `.symbol.$refText` reads (lines 141-229)
    - /home/coder/repos/bbj-language-server/.planning/phases/104-conformance-measurement-milestone-exit/104-RESEARCH.md — §"D-11 Todo" (the hypothesis and the 13 candidates that did not reproduce)
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-CONFORMANCE.md — §"A caveat found during the run"
    - /home/coder/repos/bbj-corpus/conformance/worker.mts — the absolute-path import idiom a scratch probe script copies
  </read_first>
  <action>
    Part A — README (per D-09). Write `bbj-vscode/test/test-data/conformance/README.md`, short, in the
    corpus README's terse tone. It says: these `.bbj` files are small synthetic programs, one per
    construct the language server was changed to accept so that it agrees with the BBj compiler; each
    must parse and validate with no error, which `test/conformance-regressions.test.ts` checks (D-09's
    wording names `example-files.test.ts`, but that test reads only the flat `test-data` folder, so the
    README names the test that actually covers this folder); a new fix adds a new file here, written
    from scratch with invented names — never copied from the corpus or from any customer or BASIS
    program. The measurement itself lives in the private `bbj-corpus` repository: from a checkout,
    `npm install` in `conformance/`, then `node conformance/run.mjs --ls /path/to/bbj-language-server`,
    adding `--endpoint 127.0.0.1:5008` to include the verdict of BBj's own parser; endpoint mode needs
    a BBjServices whose `bbj-ls` offers `parseProgram` (BBj 26.03 or later); the run is local only and
    never part of CI, because the corpus holds internal and third-party code; the full procedure is in
    the corpus repository's README. It carries no corpus number, file name or content and no requirement,
    decision, plan or phase identifier (describe the fixtures in words). Do not link it from
    `documentation/` and do not change `CLAUDE.md`.

    Part B — the todo (per D-11). Find the files that threw: read only the `checkExceptions` list of
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-01-endpoint-details.json` (private;
    ids stay in your terminal). For each listed file, a scratch script at
    `/home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-scoping-probe.mts` (ignored scratch,
    same absolute-path imports as `worker.mts`, run with `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx`)
    builds the file from `/home/coder/repos/bbj-corpus-baseline/rejects/` (or `corpus/` for an entry
    whose set is `corpus`) with validation on
    `createBBjTestServices(EmptyFileSystem)`, prints the check-exception diagnostic and the stack
    frame in `check-variable-scoping.ts` that Langium writes to stderr, and walks the AST
    (`AstUtils.streamAllContents`) for nodes whose `symbol` — or whose `receiver`'s `symbol` — is
    absent, printing each such node's `$type` chain up to its statement. From that, write a synthetic
    snippet with invented names that has the same statement and container shape, and confirm in the
    same script (string-input mode) that it yields a diagnostic starting "An error occurred during
    validation". Time-box this to 45 minutes or 10 candidate snippets. Then write the todo with
    frontmatter `created: 2026-09-23`, `title:` (the check throws on a reference node without a symbol
    and is silently skipped for that file), `area: validation`, `severity: minor`, `files:` pointing at
    `bbj-vscode/src/language/validations/check-variable-scoping.ts` with the function and line the
    stack names; sections Problem (own words: where it throws, why Langium catches it, the effect — the
    check contributes nothing for that file, and the registry's own error diagnostic can be what makes
    such a file count as flagged), Investigation (how many reject files threw in the Phase 104 run,
    counts only; the research candidates that did not reproduce, in words; what the probe found),
    Reproduction (the synthetic snippet in a fenced block if one reproduced, else the shape in words
    and "no synthetic repro yet"), Fix options for the next investigator. If the Phase 104 run listed
    no check exception, file it anyway, saying it was seen in the Phase 103 probe and not in the
    Phase 104 run. No corpus id, file name, path or source line. Delete the scratch script afterwards.

    Part C — guards, targeted run, commit. Run the verify commands below. Then stage exactly the two
    files and commit with subject
    "docs(104-02): point maintainers at the private conformance harness and file the scoping-check todo".
  </action>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs --data /home/coder/repos/bbj-corpus-baseline /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md</automated>
    <fails_when>non-zero exit or any `LEAK` line — a corpus id, file name, origin name or source line is in the README or the todo</fails_when>
    <automated>! grep -nE 'CONF-0|PSRV-0|D-[0-9][0-9]|[Pp]hase [0-9]|10[0-9]-[0-9][0-9]|9[89]-[0-9][0-9]' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md && ! grep -nE '11[,.]?898|1[,.]?210|16[,.]?884|4[,.]?615|\b(168|267|658|669)\b' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md</automated>
    <fails_when>non-zero exit — the README carries a planning identifier or a corpus figure (the grep prints the offending line)</fails_when>
    <automated>grep -q 'conformance-regressions.test.ts' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md && grep -q 'bbj-corpus' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md && grep -q -- '--endpoint' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md && grep -q '26.03' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md && grep -qiE 'never[^.]*CI' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/README.md</automated>
    <fails_when>non-zero exit — the README misses the covering test, the private repository, the endpoint flag, the BBj version or the never-in-CI statement</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/conformance-regressions.test.ts test/example-files.test.ts test/examples-compile.test.ts</automated>
    <fails_when>non-zero exit or any test reported as failed — the regression folder or the examples no longer pass with the README in place</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server show --name-only --format= HEAD` lists exactly the README and the todo
    - `grep -c '^area: validation' /home/coder/repos/bbj-language-server/.planning/todos/pending/2026-09-23-use-before-assignment-check-throws-on-a-reference-without-a-symbol.md` prints 1 and the file names `check-variable-scoping.ts`
    - `test ! -e /home/coder/repos/bbj-corpus/conformance/snapshots/phase-104-scoping-probe.mts` exits 0
    - The SUMMARY states whether a synthetic snippet reproduced the exception, with counts only
  </acceptance_criteria>
  <done>The regression folder explains itself and points at the private harness, the check-exception caveat is a pending todo, both pass the leak guard, and the regression suites still pass the way CI runs them.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-suite gate in both interop modes, compared by name against origin/main</name>
  <files>none — this task changes no repository file; scratch JSON goes to /tmp and the base worktree is removed at the end</files>
  <precondition>127.0.0.1:5008 accepts a connection (`bash -c '</dev/tcp/127.0.0.1/5008'` exits 0), so the interop-up run exercises the BBj-gated tests.</precondition>
  <read_first>
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/regression-gate-compare-against-base-commit.md — scratch worktree, symlinked node_modules, Node 22 regenerate, compare names
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/whole-suite-hook-timeouts-contention.md — failed suites with zero failed tests are hook timeouts; confirm the run executed
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/vitest-cwd-relative-fixtures.md — vitest needs cwd bbj-vscode
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-helper.ts — `shouldRunBBjTests` (RUN_BBJ_TESTS=0 forces BBj tests off)
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/linking.test.ts — line 295, the "Interop related tests" block
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/issue447-real-interop.test.ts — the capability test file
  </read_first>
  <action>
    Step 1 — interop-up run (per D-06):
    `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/bbj-104-02-suite-interop-up.json`.
    The exit code is non-zero whenever anything fails; judge on the JSON. Every failing test must sit in
    `linking.test.ts` with "Interop related tests" in its full name, or in
    `issue447-real-interop.test.ts` (first verify command). A failed suite with zero failed tests is a
    hook timeout: re-run that file alone and confirm it passes.

    Step 2 — base comparison by name (per D-06). `git -C /home/coder/repos/bbj-language-server fetch origin main`,
    then `git -C /home/coder/repos/bbj-language-server worktree add --detach /home/coder/repos/bbj-ls-base-104 origin/main`,
    `ln -s /home/coder/repos/bbj-language-server/bbj-vscode/node_modules /home/coder/repos/bbj-ls-base-104/bbj-vscode/node_modules`,
    resolve Node 22 with `npx -y node@22 -p process.execPath`, and with that binary's directory first on
    `PATH`: `cd /home/coder/repos/bbj-ls-base-104/bbj-vscode && npm run langium:generate`, then
    `cd /home/coder/repos/bbj-ls-base-104/bbj-vscode && npx vitest run <each test file that failed in Step 1, relative to bbj-vscode> --maxWorkers=1 --reporter=json --outputFile=/tmp/bbj-104-02-base-failing-files.json`.
    Every failing name from Step 1 must also fail there (second verify command). A name failing only
    on HEAD is a real regression from this milestone: STOP, bisect it across the branch's source
    commits in a scratch worktree, and report it — do not relabel it as drift. Clean up in this order:
    `rm /home/coder/repos/bbj-ls-base-104/bbj-vscode/node_modules` (removes the symlink only), then
    `git -C /home/coder/repos/bbj-language-server worktree remove --force /home/coder/repos/bbj-ls-base-104`
    (the force covers the generated, ignored files) and `git -C /home/coder/repos/bbj-language-server worktree prune`.

    Step 3 — CI-mode run (per D-06):
    `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/bbj-104-02-suite-ci-mode.json`;
    `numFailedTests` must be 0 and the run must have executed (third verify command). Hook-timeout
    suites: re-run alone as in Step 1.

    Step 4 — record in the SUMMARY: passed/failed/skipped counts of both runs, the failing names of the
    interop-up run, and that each also fails on origin/main (with its short hash).
  </action>
  <verify>
    <automated>node -e 'const p=require("path");const r=require("/tmp/bbj-104-02-suite-interop-up.json");const bad=[];let n=0;for(const f of r.testResults)for(const a of f.assertionResults){if(a.status!=="failed")continue;n++;const b=p.basename(f.name);const ok=(b==="linking.test.ts"&&a.fullName.includes("Interop related tests"))||b==="issue447-real-interop.test.ts";if(!ok)bad.push(b+" > "+a.fullName)}console.log("interop-up: "+r.numPassedTests+" passed, "+n+" failed");if(r.numTotalTests<1000||bad.length){bad.forEach(x=>console.error("UNEXPECTED FAILURE: "+x));process.exit(1)}'</automated>
    <fails_when>non-zero exit — a failing test outside the known interop drift, or fewer than 1,000 tests ran (the suite did not really execute)</fails_when>
    <automated>node -e 'const p=require("path");const names=f=>require(f).testResults.flatMap(r=>r.assertionResults.filter(a=>a.status==="failed").map(a=>p.basename(r.name)+" > "+a.fullName));const head=names("/tmp/bbj-104-02-suite-interop-up.json");const base=new Set(names("/tmp/bbj-104-02-base-failing-files.json"));const only=head.filter(x=>!base.has(x));console.log("head failing "+head.length+", base failing "+base.size+", only on head "+only.length);only.forEach(x=>console.error("ONLY ON HEAD: "+x));process.exit(only.length?1:0)'</automated>
    <fails_when>non-zero exit or any `ONLY ON HEAD` line — a failing name that passes on origin/main, i.e. a regression, not drift</fails_when>
    <automated>node -e 'const r=require("/tmp/bbj-104-02-suite-ci-mode.json");console.log("ci-mode: "+r.numPassedTests+" passed, "+r.numFailedTests+" failed, "+r.numPendingTests+" skipped");if(r.numFailedTests!==0||r.numTotalTests<1000)process.exit(1)'</automated>
    <fails_when>non-zero exit — `numFailedTests` is not 0 with RUN_BBJ_TESTS=0, or the run did not execute</fails_when>
  </verify>
  <acceptance_criteria>
    - `/tmp/bbj-104-02-suite-interop-up.json`, `/tmp/bbj-104-02-base-failing-files.json` and `/tmp/bbj-104-02-suite-ci-mode.json` exist and all three verify commands exit 0
    - `git -C /home/coder/repos/bbj-language-server worktree list` no longer lists `/home/coder/repos/bbj-ls-base-104`, and `/home/coder/repos/bbj-language-server/bbj-vscode/node_modules` still exists
    - `git -C /home/coder/repos/bbj-language-server status --porcelain --untracked-files=no` prints nothing
    - The SUMMARY lists the interop-up failing names and the origin/main hash they were compared against
  </acceptance_criteria>
  <done>Both whole-suite runs are green by the phase's definition: no failure outside the named interop drift, each such name also failing on origin/main, and zero failures the way CI runs.</done>
</task>

<task type="auto">
  <name>Task 3: IntelliJ suite and both distributables from the final tree</name>
  <files>none — build outputs only (bbj-vscode/out/, the VSIX under /tmp, bbj-intellij/build/), all git-ignored</files>
  <read_first>
    - ~/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/uat-build-both-extensions-first.md — build both from the final tree; prove the zip carries the fresh `main.cjs`
    - /home/coder/repos/bbj-language-server/bbj-vscode/package.json — `build` and `vscode:prepublish` scripts
    - /home/coder/repos/bbj-language-server/bbj-intellij/build.gradle.kts — lines 110-170: the build fails fast without `bbj-vscode/out/language/main.cjs`
    - /home/coder/repos/bbj-language-server/.planning/phases/103-one-set-of-errors-diagnostic-reconciliation/103-05-PLAN.md — task 2's build and `cmp` commands, reused here
  </read_first>
  <action>
    Step 1 — language server: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build`.

    Step 2 — IntelliJ suite (per D-06; `--rerun-tasks` so an UP-TO-DATE result cannot mask a stale
    green): `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks`.
    Then confirm no JUnit result file under `bbj-intellij/build/test-results/test/` reports a failure
    or error (second verify command).

    Step 3 — distributables (per D-07, no hand UAT, nothing installed):
    `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin`, then prove the zip
    carries the fresh server:
    `unzip -p <the zip under /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/> bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs`
    (if Gradle reports UP-TO-DATE with an old zip, run `./gradlew clean buildPlugin`). Then
    `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vsce package --no-dependencies --out /tmp/bbj-104-02-final.vsix`
    (its prepublish step rebuilds and lints). Record both files' sizes and sha256 sums and the HEAD
    commit they were built from in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npm run build</automated>
    <fails_when>non-zero exit, any `error TS` line, or `/home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs` absent afterwards</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks && ! grep -lE 'failures="[1-9]|errors="[1-9]' /home/coder/repos/bbj-language-server/bbj-intellij/build/test-results/test/*.xml</automated>
    <fails_when>non-zero exit, `FAILED` in the Gradle output, or any result file listed as carrying a failure or error</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin</automated>
    <fails_when>non-zero exit, `FAILED` in the output, or no zip under `/home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/`</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vsce package --no-dependencies --out /tmp/bbj-104-02-final.vsix</automated>
    <fails_when>non-zero exit, a lint error, or `/tmp/bbj-104-02-final.vsix` absent afterwards</fails_when>
  </verify>
  <acceptance_criteria>
    - The `unzip -p … | cmp - …/out/language/main.cjs` command exits 0
    - `/tmp/bbj-104-02-final.vsix` and the plugin zip both exist with modification times later than `git -C /home/coder/repos/bbj-language-server log -1 --format=%cI -- bbj-vscode`
    - `git -C /home/coder/repos/bbj-language-server status --porcelain --untracked-files=no` prints nothing
    - The SUMMARY records sizes, sha256 sums and the commit both were built from, and states that no hand UAT applies because this phase changes no IDE behaviour
  </acceptance_criteria>
  <done>The IntelliJ suite passes on the final tree and both distributables are built from it, the plugin carrying the same language server byte for byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new files → the public repository | The README and the todo are committed here and pushed with PR #691 in plan 03. |
| scratch base worktree → the real checkout | The base worktree borrows the real `node_modules` through a symlink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-06 | Information Disclosure | test-data README, pending todo | high | mitigate | `leak-guard.mjs` against both the rebuilt `main` data and the baseline worktree; a number guard for the corpus figures; a planning-identifier guard on the README; the probe output stays in the terminal and the scratch probe is deleted. Task 1. |
| T-104-07 | Tampering | `bbj-vscode/node_modules` during base-worktree cleanup | medium | mitigate | The symlink is removed with `rm` on the link before `git worktree remove --force`, so the real directory is never traversed. Task 2. |
| T-104-08 | Repudiation | whole-suite gate | medium | mitigate | Failures are judged by name, each checked against origin/main in a scratch worktree; a head-only name stops the plan instead of being relabelled. Task 2. |
| T-104-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; `npx -y node@22` fetches the Node 22 runtime the project already uses for base comparisons, and every build uses the locked dependencies. |
</threat_model>

<verification>
- The README and the todo pass the leak guard on both data roots and the README passes the identifier and number guards.
- Interop-up suite: only named interop-drift failures, each also failing on origin/main.
- CI-mode suite: `numFailedTests` 0.
- IntelliJ `./gradlew test --rerun-tasks`: zero failures; plugin zip and VSIX built from the final tree.
</verification>

<success_criteria>
- CONF-03's suite half holds on the final tree.
- The maintainer pointer exists next to the regression files with no corpus content.
- The check-exception caveat is filed as a pending todo.
</success_criteria>

<output>
Create `.planning/phases/104-conformance-measurement-milestone-exit/104-02-SUMMARY.md` when done
(counts and own-words only; run `leak-guard.mjs` over it before committing).
</output>
