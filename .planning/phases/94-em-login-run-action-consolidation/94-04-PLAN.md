---
phase: 94-em-login-run-action-consolidation
plan: 04
type: execute
wave: 3
depends_on: [94-02, 94-03]
files_modified: []
autonomous: true
requirements: [EM-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`BbjRunBuiAction` and `BbjRunDwcAction` share the run flow through `BbjRunActionBase.buildWebRunCommandLine`, differing only in their BUI/DWC-specific literals — verified as already true and closed on cited evidence rather than reported as newly implemented (EM-04, D-05)."
    - "The two existing guards that pin that delegation still pass after this phase's edits to the shared base, so the consolidation survived the token-validation move."
    - "The whole `bbj-intellij` suite is green at the end of the phase, from the final tree."
    - "An installable IntelliJ distributable is built from the final tree, so the hand UAT exercises what a user would install rather than a dev sandbox."
  artifacts:
    - .planning/phases/94-em-login-run-action-consolidation/94-04-SUMMARY.md
  key_links:
    - "ROADMAP criterion 5 names the validation script `em-validate.bbj`; the real filename is `em-validate-token.bbj`, as are the issue texts for #617 and #614. The SUMMARY must record the corrected name so the criterion is not marked met against a file that does not exist."
    - "Both distributables are built from the FINAL tree, and again after any code-review fixes land — a UAT pass against a pre-fix build proves nothing about what ships."
  prohibitions:
    - "EM-04 is not reported as newly implemented. Commit `6a55b854` made it true; this phase confirms and records that."
    - "No third guard is added for EM-04. The invariant is already pinned twice, and a third assertion of the same fact adds nothing."
    - "The BUI and DWC subclasses are not edited. They are already down to their client-type literals."
    - "No planning identifier (EM-04, D-05, plan numbers, C-xx, CR-xx) appears in any source or test file. GitHub issue numbers such as #615 are acceptable."
  flagged_assumptions:
    - "The EM-04 probe row came back `unclassified`/`unresolved` from the deterministic edge probe and is surfaced here rather than auto-resolved. Read manually, it has no edge dimension left to resolve: the requirement is a structural duplication claim about two 31-line subclasses, already satisfied, with no runtime input space of its own. Flagged, not dismissed."
    - "Whether the two existing guards still pass is not assumed from this phase's earlier plans — it is re-verified here from the final tree, because plan 02 edited both the shared base and one of those two guards."
---

<objective>
Close EM-04 on evidence, and gate the phase: the full suite green from the final tree, plus an
installable build for the hand UAT that no CI job can perform.

Purpose: EM-04 (#615) describes `BbjRunBuiAction` and `BbjRunDwcAction` as 142-line near-duplicates
differing in 131 lines. That evidence is stale. Commit `6a55b854` extracted the shared body into
`BbjRunActionBase.buildWebRunCommandLine`, and the two subclasses are now 31 lines each differing in
exactly 7: the class name, two javadoc lines, two constructor arguments, the client-type argument,
and the run-mode literal. Two guards already pin the delegation. This plan verifies that is still
true after the phase's edits to the shared base, and records the closure — it writes no production
code.
Output: the phase's regression gate, an installable plugin build, and the recorded EM-04 evidence.
</objective>

<shell_rules>
The permission hooks block opaque shell commands. Every task below must obey these:
- Use absolute paths from `/home/coder/repos/bbj-language-server/...`. Never `.`, `..` or `~`.
- Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`. `cd` is allowed ONLY directly in
  front of a build tool that needs its project directory
  (`cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew build`).
- Use `git -C /home/coder/repos/bbj-language-server <subcommand>` for git.
- Stage with `git add <exact path>` only. Never `git add -A` or `git add .`.
- No blind recursive scans; scope every search to named files or an `--include` filter.
</shell_rules>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-em-login-run-action-consolidation/94-CONTEXT.md
@.planning/phases/94-em-login-run-action-consolidation/94-02-SUMMARY.md
@.planning/phases/94-em-login-run-action-consolidation/94-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm and record the BUI/DWC consolidation evidence</name>
  <read_first>
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjRunBuiAction.java (whole file, 31 lines)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjRunDwcAction.java (whole file, 31 lines)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmTokenTrustWindowSourceGuardTest.java (the delegation test pinning both client-type literals)
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/BbjRunActionConfigPathSourceGuardTest.java (the second delegation pin, and the sweep asserting no run-action source derives its own config default)
  </read_first>
  <files>.planning/phases/94-em-login-run-action-consolidation/94-04-SUMMARY.md</files>
  <action>
    Verify, from the final tree, that the two web-run subclasses still differ only in their
    BUI/DWC-specific literals, and that both guards pinning that delegation still pass after plan
    02's edits to the shared base.

    Diff the two subclass files against each other and confirm the divergence is confined to: the
    class name, the two javadoc description lines, the two constructor arguments carrying the action
    text and icon, the client-type argument passed to the shared helper, and the run-mode literal.
    Confirm each file is 31 lines. Report the actual count if it differs rather than restating the
    expectation.

    Record the closure in the SUMMARY: that EM-04 was already satisfied before this phase began, by
    commit `6a55b854` ("fix(84): extract shared BUI/DWC command-line builder", 2026-09-06); that the
    invariant is pinned by the delegation assertions in both named guards; that no third guard was
    added because a third assertion of the same fact adds nothing; and that the requirement closes as
    verified-already-true, not as newly implemented.

    Record one correction while writing the SUMMARY: the validation script is named
    `em-validate-token.bbj`, not `em-validate.bbj` as the phase's success criterion 5 and the issue
    texts for #617 and #614 all call it. Criterion 5 must not be marked met against a filename that
    does not exist in the tree.

    Add no test and change no production file in this task.
  </action>
  <acceptance_criteria>
    - The SUMMARY names commit `6a55b854` and both guard test classes as the EM-04 evidence.
    - The SUMMARY states EM-04 closes as already-satisfied, not as newly implemented.
    - The SUMMARY records the `em-validate-token.bbj` filename correction against criterion 5.
    - No file under `bbj-intellij/src` is modified by this task.
    - The diff between the two subclass files is confined to the enumerated literals; any wider divergence is reported rather than absorbed.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --tests "*.EmTokenTrustWindowSourceGuardTest" --tests "*.BbjRunActionConfigPathSourceGuardTest"</automated>
    <fails_when>non-zero exit, or "FAILED" appears in the Gradle output, or "no tests found for given includes" appears (either guard having been renamed away would otherwise read as green)</fails_when>
  </verify>
  <done>EM-04 is confirmed still true from the final tree and closed on cited evidence, with the script-name correction recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Phase regression gate and an installable build for UAT</name>
  <read_first>
    - .planning/phases/94-em-login-run-action-consolidation/94-VALIDATION.md (the sampling contract and the manual-only verification table this task hands to UAT)
    - bbj-intellij/build.gradle.kts lines 205-222 (the sandbox/distribution copy that places the three tool scripts, so a build failure there is recognised rather than mistaken for a test failure)
  </read_first>
  <files>.planning/phases/94-em-login-run-action-consolidation/94-04-SUMMARY.md</files>
  <action>
    Run the whole `bbj-intellij` JUnit suite from the final tree and require zero failed tests. Per
    the standing project constraint, judge on the failed-test count rather than on a failed-suite
    count: initialisation timeouts under contention can report a failed suite with zero failed tests,
    and that is contention, not a regression.

    Then build the installable IntelliJ plugin distributable from that same tree, so the hand UAT
    exercises what a user would install rather than a dev sandbox — which is exactly the distinction
    the phase's fifth success criterion turns on, since the three tool scripts only reach the plugin
    through the distribution copy.

    Record in the SUMMARY: the failed-test count, the built artefact's path, and the five UAT items
    below with their expected outcomes, so the verification step has them without re-deriving them.

    If code-review fixes land after this task, the distributable must be rebuilt from the corrected
    tree before UAT is performed — a UAT pass against a pre-fix build proves nothing about what ships.
  </action>
  <acceptance_criteria>
    - The full `bbj-intellij` suite reports zero failed tests.
    - An installable plugin distributable exists under the Gradle distribution output directory, built from the final tree.
    - The SUMMARY records the failed-test count and the artefact path.
    - The SUMMARY lists the five UAT items with their expected outcomes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test</automated>
    <fails_when>non-zero exit, or "FAILED" in the Gradle output with a non-zero failed-test count; a failed suite reporting zero failed tests is initialisation contention and is recorded, not treated as a regression</fails_when>
  </verify>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin</automated>
    <fails_when>non-zero exit, or "FAILED" in the Gradle output, or no archive is produced in the distribution output directory</fails_when>
  </verify>
  <human-check>
    Install the built distributable, then verify in a running IDE:
    1. Tools ▸ Login to Enterprise Manager with a project open → item present; with all projects
       closed → item absent (not greyed). This is the phase's one intended user-visible change.
    2. EM login end-to-end → credential prompts, success dialog, token stored; no leftover temp file.
    3. Run a BBj file as BUI and as DWC, from the editor context menu AND from the keyboard
       shortcuts → both launch in the browser.
    4. Token lifecycle: a run with no stored token prompts login; a run with an expired token
       re-prompts; a second run within five minutes hits the trust window and spawns no validation
       subprocess.
    5. Control: run a BBj file as GUI → behaves exactly as before, since it touches no EM code.
  </human-check>
  <done>The phase gate is green from the final tree, an installable build exists, and the UAT items are recorded for verification.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols and files created by phase 94 as a whole (this section exists so the source-grounding
pass excludes newly-created symbols from drift verification). This plan itself creates no source
symbol — it verifies and records.

- `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjToolScriptResolver.java` — class `BbjToolScriptResolver`, nested `@FunctionalInterface PluginPathResolver`, field `SESSION`, method `resolveToolScript` (plan 01)
- `bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/BbjToolScriptResolverTest.java` — class `BbjToolScriptResolverTest` (plan 01)
- `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/EmTokenValidator.java` — class `EmTokenValidator`, nested `@FunctionalInterface ValidationRunner`, field `SESSION`, methods `validateTokenServerSide` / `validateTokenTrusted` (plan 02)
- `bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmTokenValidatorTest.java` — class `EmTokenValidatorTest` (plan 02)
- `bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmLoginEnablementSourceGuardTest.java` — class `EmLoginEnablementSourceGuardTest` (plan 03)
- `bbj-intellij/src/test/java/com/basis/bbj/intellij/actions/EmLoginTempFileCleanupSourceGuardTest.java` — class `EmLoginTempFileCleanupSourceGuardTest` (plan 03)
- New methods on the existing `BbjEMLoginAction`: `update(AnActionEvent)` and `getActionUpdateThread()` (plan 03)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → user's IDE installation | the distributable is what a user actually installs, including the three bundled tool scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-12 | Tampering | plugin distribution copy | medium | mitigate | The tool scripts reach the plugin only through the distribution copy task, so a resolver whose relative path drifted from that task's destination would fail only in a real installation. Mitigated by building and hand-installing the distributable and exercising both login and a web run against it, rather than testing in a dev sandbox alone. |
| T-94-13 | Spoofing | UAT against a stale build | medium | mitigate | A UAT pass against a pre-code-review-fix build would certify code that does not ship. Mitigated by the explicit rebuild-after-fixes requirement recorded in Task 2 and in the SUMMARY. |
| T-94-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this phase; RESEARCH.md records the Package Legitimacy Audit as not applicable, and `build.gradle.kts` is unchanged by the phase. |
</threat_model>

<verification>
- Zero failed tests across the whole `bbj-intellij` suite, from the final tree.
- Both EM-04 delegation guards pass after plan 02's edits to the shared base.
- An installable distributable exists and the five UAT items are recorded for the verification step.
</verification>

<success_criteria>
- EM-04 closed on cited evidence, with no third duplicate guard and no production edit.
- Phase regression gate green; installable build available for hand UAT.
- The `em-validate-token.bbj` naming correction recorded against the phase's fifth success criterion.
</success_criteria>

<output>
Create `.planning/phases/94-em-login-run-action-consolidation/94-04-SUMMARY.md` when done.
</output>
