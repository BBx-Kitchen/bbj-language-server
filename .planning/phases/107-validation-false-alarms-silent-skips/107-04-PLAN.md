---
phase: 107-validation-false-alarms-silent-skips
plan: 04
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - bbj-vscode/src/language/validations/line-break-validation.ts
  - bbj-vscode/test/line-break-single-line-if.test.ts
  - bbj-vscode/test/test-data/conformance/single-line-if-forms.bbj
  - .planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md
autonomous: true
requirements: [VAL-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fresh baseline of the private harness, run against the phase base in a scratch worktree, is snapshotted under /home/coder/repos/bbj-corpus/conformance/snapshots/ before any other harness run of this phase, and the pre-existing details.json is snapshotted before that (D-13)"
    - "The VAL-01 target set is the base run's A2 files in the blank-text group ('This statement needs to start in a new line: ' with nothing after it) and the ': else' group, the two groups re-flagged at the Phase 98 close; its size is recorded (the 2026-09-23 run showed 6 + 1)"
    - "After the fixes, a per-file probe with the harness's own services shows zero line-break Errors in every target-set file (ROADMAP criterion 3; D-03)"
    - "Each target file's flagged shape is identified locally and described in own words; every shape not already covered by 107-01 gets a synthetic failing test, a fix in line-break-validation.ts and a clean fixture line (D-03)"
    - "No fix changes either line-break message text or adds a fallback for an empty range (D-04)"
    - "A quick full harness re-measure after the last VAL-01 change shows, by file set, that no line-break-message file newly entered A2 and that no file newly entered B; any movement is explained per file (D-13)"
    - "107-CONFORMANCE.md records the baseline numbers, the target set by count and group, the mechanism per shape in own words and the re-measure, with no corpus file name, id, path or source line; leak-guard.mjs reports it clean (D-13)"
    - "Edge (concurrency): only one harness run or corpus probe runs at a time; each run is snapshotted before the next one overwrites details.json"
  artifacts:
    - path: ".planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md"
      provides: "baseline, VAL-01 target set, mechanism per shape, re-measure"
      contains: "## 2. VAL-01 target set"
    - path: "/home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-base-details.json"
      provides: "fresh baseline details (outside the repository, never committed)"
  key_links:
    - from: "phase-107-base-details.json falseAlarms"
      to: "the per-file probe run on the current tree"
      via: "target-set ids read locally, never written into the repository"
      pattern: "falseAlarms"
  prohibitions:
    - requirement_id: VAL-01
      category: confidentiality
      statement: "MUST NOT write a corpus file name, id, path or source line into the repository, a commit message, a planning file or a SUMMARY; describe shapes in own words and write synthetic fixtures from scratch (D-13)"
      flagged: true
    - requirement_id: VAL-01
      category: integrity
      statement: "MUST NOT accept residue silently or re-flag valid code to protect B; an unexplained remaining shape stops the plan for a human decision"
      flagged: true
  assumptions:
    - statement: "The phase base is the parent of the first commit whose subject contains '(107-01)'; the grammar is unchanged by this phase, so the base worktree can reuse the main checkout's generated files and node_modules"
      source: "planning decision"
      flagged: true
    - statement: "Plan 107-01's fix is expected to clear the whole target set (the planning probe reproduced the blank-text group as the nested one-liner over ':'-continuation lines); this plan confirms that on the real files and handles any shape it does not cover"
      source: "107-01 assumptions"
      flagged: true
---

<objective>
Confirm on the private corpus that the files re-flagged at the Phase 98 close no longer carry the
line-break error, and investigate and fix any remaining shape (VAL-01, D-03, D-04, D-13).

Purpose: success criterion 3 is measured on real files that cannot enter this repository. Residue is
not an acceptable end state (D-03).
Output: harness snapshots outside the repository, fixes and synthetic tests for any remaining shape,
and `107-CONFORMANCE.md` sections 1-3.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-CONTEXT.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-RESEARCH.md
@.planning/phases/107-validation-false-alarms-silent-skips/107-01-SUMMARY.md
@.planning/todos/pending/2026-09-21-loosen-single-line-if-balance-rule-a2-residue.md
@.planning/milestones/v4.5-phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md
@bbj-vscode/test/test-data/conformance/README.md

<interfaces>
Harness (private repository, local only):
- `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls <repo root> [--endpoint 127.0.0.1:5008]` writes `REPORT.md`, `summary.json` and `details.json` next to run.mjs, appends to `history.jsonl`, and wipes `conformance/work/` at start. Snapshots belong in `/home/coder/repos/bbj-corpus/conformance/snapshots/` (untracked in the corpus repository), never under `work/`.
- `details.json` keys: `falseRejects`, `falseAlarms` (A2: `{ id, line, message, severity, source }`, the FIRST validation diagnostic per file only), `missed` (raw B), `missedReconciled` (B with the endpoint verdict), `checkExceptions`, and more. Corpus files are at `/home/coder/repos/bbj-corpus/corpus/<id>` for ids without the `corpus/` prefix (see run.mjs jobs: `path.join(root, 'corpus', record.id)`).
- `worker.mts` validates with `createBBjTestServices(EmptyFileSystem)`: `factory.fromString(text, uri)`, `documents.addDocument`, `builder.build([document], { validation: true })`, then `builder.update([], [uri])`; it imports the language server by absolute path (`${lsRepo}/bbj-vscode/...`) and runs under `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx`.
- `node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs <file>...` exits 0 and prints `clean: <file>` when no corpus id, name or source line appears.
Language server: `bbj-lexer.ts` `prepareLineSplitter` joins every line that starts with `:` into the previous line and leaves an empty line in its place, so CST positions of joined content sit on the first line past its physical end.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  Allowed `cd` form: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Run the harness
  and probes by absolute path (`node /home/coder/repos/bbj-corpus/conformance/run.mjs …`,
  `/home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx <script>`). Use `git -C` for git.
  Stage with `git add <exact path>` only. Never use `git stash`. Never run a path-wide `git restore --source`.
- No corpus text may leave the private repository (D-13): no corpus file name, id, path or source line
  in any tracked file, test, fixture, commit message, planning file, SUMMARY or tool output you paste
  into them. Read flagged lines locally, describe shapes in your own words, write fixtures from
  scratch. Run `leak-guard.mjs` on every file you change in this repository before committing it.
  Write markdown table separator rows as `| --- | --- |` (short dashes with spaces): a long unspaced
  dash row can equal a generic corpus line and trip the guard.
- Probe scripts and snapshots live in `/home/coder/repos/bbj-corpus/conformance/snapshots/` with a
  `phase-107-` prefix; nothing from them is copied into this repository.
- One harness run or probe at a time: before starting one, `pgrep -f "conformance/run.mjs"` and
  `pgrep -f "phase-107-.*probe"` print nothing. Snapshot `details.json` and `summary.json` before every
  full run; compare file sets, never totals alone.
- Source and test text must not contain planning identifiers (D-NN, VAL-family ids, 107-0N, WR-A,
  CR-/WR-/IN-NN, threat ids). Run vitest with cwd `bbj-vscode`, one file per invocation; never the
  `basic` reporter.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark VAL-01 complete in
  REQUIREMENTS.md.
- Escalation (D-03): if a remaining shape's mechanism is still unidentified after instrumenting it with
  the probe and three synthetic reproduction attempts, stop and return a `checkpoint:decision` with
  `gate="blocking-human"`: decision "how to handle an unexplained line-break false alarm", context (the
  shape in own words, what was tried, the counts), options "continue with a direction the user gives",
  "accept the residue for this shape with a new todo (overrides D-03)", "revert to the base behaviour for
  this shape only".

## Artifacts this phase produces

- (107-01) `elseClaims` counter in `line-break-validation.ts`; line-break tests; fixture lines.
- (107-02) `symbol?.` reads in `check-variable-scoping.ts`; guard in `bbj-scope-local.ts`; tests.
- (107-03, 107-05) `validations/check-unknown-java-member.ts` (`UNKNOWN_JAVA_MEMBER_CODE`,
  `isFullyResolvedJavaClass`, `hasCertainReceiverType`, `checkUnknownJavaMember`,
  `registerUnknownJavaMemberChecks`), `dropShadowedMemberLinkingDiagnostics`, registration, tests.
- (this plan) `107-CONFORMANCE.md` sections 1-3; outside the repository:
  `snapshots/phase-107-prior-details.json`, `phase-107-prior-summary.json`,
  `phase-107-base-details.json`, `phase-107-base-summary.json`, `phase-107-base-run.log`,
  `phase-107-val01-details.json`, `phase-107-val01-summary.json`, probe script
  `phase-107-file-probe.mts`; any further fix, tests and fixture lines for a remaining shape.
- (107-06) final measurement section, todo close-out.

<tasks>

<task type="tracer">
  <name>Task 1: Baseline on the phase base, then the target set checked file by file on the current tree</name>
  <files>.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md</files>
  <precondition>/home/coder/repos/bbj-corpus/conformance/run.mjs and /home/coder/repos/bbj-corpus/conformance/node_modules/.bin/tsx exist, and plan 107-01's commits are on HEAD</precondition>
  <read_first>
    - /home/coder/repos/bbj-corpus/conformance/run.mjs (lines 1-60 arguments, jobs, snapshot-relevant outputs; 270-289 details.json)
    - /home/coder/repos/bbj-corpus/conformance/worker.mts (lines 1-35 imports and services, 180-252 the validate loop)
    - .planning/phases/107-validation-false-alarms-silent-skips/107-01-SUMMARY.md
    - .planning/milestones/v4.5-phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md (sections 10-11)
  </read_first>
  <action>
Produce the measurement the whole phase compares against, then check the VAL-01 target set end to end (D-03, D-13).

1. Snapshot the current `details.json` and `summary.json` to `snapshots/phase-107-prior-details.json` and `phase-107-prior-summary.json`.
2. Base worktree: `B=$(git -C /home/coder/repos/bbj-language-server rev-parse "$(git -C /home/coder/repos/bbj-language-server log --reverse --format=%H --grep='(107-01)' | head -1)^")`; confirm the grammar is unchanged with `git -C /home/coder/repos/bbj-language-server diff --quiet "$B" HEAD -- bbj-vscode/src/language/bbj.langium`; `git -C /home/coder/repos/bbj-language-server worktree add --detach /tmp/phase-107-base-wt "$B"`; `ln -s /home/coder/repos/bbj-language-server/bbj-vscode/node_modules /tmp/phase-107-base-wt/bbj-vscode/node_modules`; `cp -r /home/coder/repos/bbj-language-server/bbj-vscode/src/language/generated /tmp/phase-107-base-wt/bbj-vscode/src/language/generated`.
3. If `timeout 3 bash -c '</dev/tcp/127.0.0.1/5008'` succeeds, run `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /tmp/phase-107-base-wt --endpoint 127.0.0.1:5008 > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-base-run.log 2>&1` (about 10 minutes; run it in the background and poll the log); otherwise run without `--endpoint` and record that 107-06 must use the same mode. Copy `details.json` and `summary.json` to `phase-107-base-details.json` and `phase-107-base-summary.json`. Remove the worktree with `git -C /home/coder/repos/bbj-language-server worktree remove --force /tmp/phase-107-base-wt`.
4. Compare the base A2 id set with `phase-107-prior-details.json`'s (the 2026-09-23 run) and note any difference by count.
5. Target set T: base `falseAlarms` ids whose message is exactly `This statement needs to start in a new line: ` or ends with `: else`. Also list, separately, ids whose message ends with `: fi` (same mask family; recorded, not a criterion-3 item).
6. Write `snapshots/phase-107-file-probe.mts` from worker.mts's idioms: it takes the language-server root and a file of ids, validates each corpus file with `createBBjTestServices(EmptyFileSystem)` exactly as the worker does, and prints per id every Error-severity diagnostic's code, message and 1-based line (local output only). Run it on T against the main checkout (107-01 applied).
7. If no T file carries a `bbj-line-break` Error: record that. Otherwise take the first remaining file: read its flagged line and neighbours locally, instrument the probe to print the flagged node's `$type`, the statements on its logical line and whether `:`-continuation lines are involved, reproduce the shape in a synthetic failing test in `line-break-single-line-if.test.ts` (invented names), fix it in `line-break-validation.ts` within the existing walkers (D-01 structure, D-04 text), add a clean fixture line to `single-line-if-forms.bbj`, and re-run the probe until that file is clean and all line-break suites pass.
8. Create `107-CONFORMANCE.md` with `## 1. Baseline` (date, language-server commit, mode, A, A2, raw B, reconciled B if measured, checkExceptions, and the prior-vs-base A2 difference count) and `## 2. VAL-01 target set` (size per group, and per remaining file its shape in own words and whether 107-01 already cleared it). Run the leak guard on it.
  </action>
  <verify>
    <automated>test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-base-details.json && test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-prior-details.json && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md && echo baseline-ok</automated>
    <fails_when>"baseline-ok" is not printed: a snapshot is missing or empty, or the leak guard prints a LEAK line and exits non-zero</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/line-break-single-line-if.test.ts && npx vitest run test/line-break-walk-termination.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in either summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server worktree list` shows only the main checkout
    - `grep -c "^## 1. Baseline" /home/coder/repos/bbj-language-server/.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md` prints 1, and the same for `^## 2. VAL-01 target set`
    - The SUMMARY states the target-set size per group and, for each file, clean or the shape handled (in own words)
    - `git -C /home/coder/repos/bbj-language-server status --porcelain` lists no probe or snapshot file
  </acceptance_criteria>
  <done>The baseline is snapshotted, the target set is known by group, each target file is clean on the current tree or its first remaining shape is fixed and pinned, and the record has no corpus text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every remaining shape fixed, then the VAL-01 re-measure by file set</name>
  <files>bbj-vscode/src/language/validations/line-break-validation.ts, bbj-vscode/test/line-break-single-line-if.test.ts, bbj-vscode/test/test-data/conformance/single-line-if-forms.bbj, .planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md</files>
  <read_first>
    - .planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md (after Task 1)
    - bbj-vscode/src/language/validations/line-break-validation.ts
    - bbj-vscode/test/line-break-single-line-if.test.ts
  </read_first>
  <behavior>
    - Every remaining target-set shape has a synthetic test that failed before its fix and passes after
    - The three 'stay flagged' tests, the extra-ELSE test and all of line-break-walk-termination.test.ts still pass
    - The per-file probe reports zero `bbj-line-break` Errors for every target-set file
  </behavior>
  <action>
Close the target set and re-measure (D-03, D-04, D-13).

1. Repeat Task 1 step 7 for each remaining target file until the probe reports zero `bbj-line-break` Errors for the whole set. Follow the escalation rule if a mechanism stays unidentified.
2. Snapshot `details.json`/`summary.json` (they now hold the base run; copy them only if they differ from `phase-107-base-*`), then run the harness without `--endpoint` on the main checkout: `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server > /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-val01-run.log 2>&1`. Copy the outputs to `phase-107-val01-details.json` and `phase-107-val01-summary.json`.
3. File-set comparison against the base run (same mode where possible; if the base run used the endpoint, compare A2 and raw `missed` only, which do not depend on it):
   - no target-set id appears in the new `falseAlarms` with a line-break message (the probe from step 1 is the authority for files whose first diagnostic is now something else);
   - list ids that newly entered A2 with a line-break message (must be none; otherwise investigate each by shape and fix, or escalate);
   - list ids that newly entered raw `missed` (B). For each, read locally what the compiler rejects and whether the only Langium Error the file lost was the line-break one; record the finding per file in own words. Do not re-flag valid shapes to protect B; report it for 107-06's human check.
   - Ids that newly entered A2 with the new unknown-member message are expected here (107-03 is in the tree and the harness's test double lacks many real members); count them and leave their classification to 107-06.
4. Append `## 3. VAL-01 re-measure` to 107-CONFORMANCE.md: A2 before/after, target set cleared (count), newly entered line-break A2 (count and shapes), newly entered raw B (count and per-file finding in own words), the count of unknown-member A2 entries deferred to 107-06. Run the leak guard on every changed file, then the register check.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/line-break-single-line-if.test.ts && npx vitest run test/line-break-walk-termination.test.ts && npx vitest run test/line-break-validation.test.ts && npx vitest run test/conformance-regressions.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in any summary line</fails_when>
    <automated>test -s /home/coder/repos/bbj-corpus/conformance/snapshots/phase-107-val01-details.json && node /home/coder/repos/bbj-corpus/conformance/leak-guard.mjs /home/coder/repos/bbj-language-server/.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md /home/coder/repos/bbj-language-server/bbj-vscode/test/line-break-single-line-if.test.ts /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/single-line-if-forms.bbj && echo remeasure-ok</automated>
    <fails_when>"remeasure-ok" is not printed: the re-measure snapshot is missing, or the leak guard prints a LEAK line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 3. VAL-01 re-measure" /home/coder/repos/bbj-language-server/.planning/phases/107-validation-false-alarms-silent-skips/107-CONFORMANCE.md` prints 1
    - The SUMMARY states: target set cleared N of N; newly entered line-break A2 files: 0; newly entered raw B files: count with a per-file finding in own words
    - `grep -c "This statement needs to start in a new line" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/validations/line-break-validation.ts` prints 2 (D-04)
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\bVAL-0[1-3]\b|\b107-0[1-6]\b|\bWR-A\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-107-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>Every re-flagged file is clean, no line-break file newly entered A2, every B movement is explained per file, and the record holds numbers and own-words shapes only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus → public repository | the executor reads proprietary source locally and writes records, fixtures and commits in a public repository |
| BBj source text → validators | real programs exercise the walkers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-09 | Information disclosure | 107-CONFORMANCE.md, SUMMARY, fixtures, tests, commit messages | high | mitigate | Own-words shapes and counts only; snapshots and probes stay in the corpus repository's untracked snapshots folder; `leak-guard.mjs` runs on every changed tracked file before each commit. |
| T-107-10 | Tampering (measurement integrity) | harness runs | medium | mitigate | details.json is snapshotted before every run and compared by file set; the base is measured on the phase base in a scratch worktree; only one run at a time. |
| T-107-11 | Tampering (diagnostic integrity) | line-break walkers | medium | mitigate | Each fix is driven by a synthetic failing test; every still-flagged test stays green; newly entered B files are reported per file, never hidden. |
| T-107-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- Snapshots `phase-107-prior-*`, `phase-107-base-*`, `phase-107-val01-*` exist outside the repository
- Every target-set file has zero line-break Errors in the per-file probe
- Line-break suites and conformance-regressions pass; leak guard and register check clean
</verification>

<success_criteria>
- The valid files re-flagged at the Phase 98 close no longer carry the line-break error (ROADMAP criterion 3, first part)
- No line-break file newly enters A2; every newly entered B file is explained per file
</success_criteria>

<output>
Create `.planning/phases/107-validation-false-alarms-silent-skips/107-04-SUMMARY.md` when done
</output>
