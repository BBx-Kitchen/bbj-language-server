---
phase: 108-intellij-crash-detection
plan: 04
type: execute
wave: 4
depends_on: ["108-01", "108-02", "108-03"]
files_modified:
  - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md
autonomous: false
requirements: [LIFE-01, LIFE-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A VSIX and an IntelliJ zip built from the final tree are in /tmp/phase-108-uat/. If code-review fixes land later, both are rebuilt and the affected scenarios repeated. The zip's server bundle equals bbj-vscode/out/language/main.cjs, its plugin jar contains reportUnexpectedExit, and the IntelliJ suite passes with --rerun-tasks (D-14)"
    - "On macOS, a first kill -9 is logged as a crash WARN with pid and exit code. The widget shows BBj: Crashed, the plugin restarts the server once and it returns to started, with no banner and no plugin balloon (ROADMAP criterion 1, D-14 scenario 1)"
    - "A second kill -9 within 30 s logs the crash WARN and the give-up WARN, shows the balloon and the editor banner, and schedules no plugin restart (D-14 scenario 2)"
    - "Closing the last BBj file, closing the project, Settings Apply, a manual restart, and a config reload or Refresh Java Classes fallback produce no crash WARN and no crash-triggered restart. The plugin's own restarts show its Scheduled and Restarting lines (ROADMAP criterion 2, D-14 scenarios 3-7)"
    - "A kill within 30 s after a manual restart is still classified as a crash, which proves the plugin disarms its own restart token (D-08)"
    - "Every status line in the pasted excerpts names the real previous status (ROADMAP criterion 3, LIFE-02)"
    - "LSP4IJ's own `stopped unexpectedly` notification is listed as expected in scenarios 1 and 2, not as a regression (D-12)"
    - "Every expected line in the Final UAT section is marked observed, derived or missing. Observed lines come from the maintainer's pasted `grep \"BBj language server\" idea.log` output, and no hand-derived trace counts as evidence (D-15)"
    - "Source, test and doc text changed in this phase contains no planning identifiers"
  artifacts:
    - path: ".planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md"
      provides: "final build identity, seven-scenario script with derived expectations, pasted observed excerpts, per-scenario verdicts"
      contains: "## Final UAT"
  key_links:
    - from: "108-UAT-ARTIFACTS.md Final UAT expectations"
      to: "BbjServerService / BbjLanguageServer log literals in the final tree"
      via: "each expected line quotes a literal read from the code path named beside it"
      pattern: "BBj language server"
  prohibitions:
    - requirement_id: LIFE-01
      category: transparency
      statement: "MUST NOT record a scenario as passed on a hand-derived or remembered sequence. Every observed mark cites a line in the pasted excerpt or the maintainer's own description of the screen"
      flagged: true
---

<objective>
Prove the phase in a running IntelliJ on macOS (ROADMAP criterion 4). Build both distributables from
the final tree, run the whole IntelliJ suite and the register check, write the seven-scenario script
with each expected line derived from the code, have the maintainer run it, and record every line as
observed, derived or missing (D-14, D-15).

Purpose: no CI covers IntelliJ or LSP4IJ at runtime, and Phase 97 failed exactly this check.
Output: `/tmp/phase-108-uat/`, the Final UAT section of `108-UAT-ARTIFACTS.md`, and the
maintainer's verdict.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-intellij-crash-detection/108-CONTEXT.md
@.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md
@.planning/phases/108-intellij-crash-detection/108-02-SUMMARY.md
@.planning/phases/108-intellij-crash-detection/108-03-SUMMARY.md
@bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java
@bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java

<interfaces>
- Log literals to quote (re-read them from the final code; the text below is the planned wording):
  - `BBj language server process exited unexpectedly (pid <pid>, exit code <code>); auto-restarting (1 of 1)`
  - `…; crash <n> within 30 s, not auto-restarting`
  - `BBj language server crashed <n> times within 30 s; auto-restart stopped until a manual restart`
  - `BBj language server process exited during a plugin restart (…); treated as an expected stop, not a crash`
  - `BBj language server connection stop requested (pid <pid>, process alive: <bool>)`
  - `BBj language server unexpected-stop handler registered beside LSP4IJ's own`
  - `BBj language server status: <from> -> <to>`
  - `Scheduled a BBj language server restart in <ms> ms`
  - `Restarting the BBj language server; status before the stop: <s>`
  - `Starting the BBj language server; status before the start: <s>`
  - `Launching the BBj language server: …`
- UI text: widget `BBj: Crashed` / `BBj: Ready`; balloon title `BBj Language Server crashed unexpectedly` (Show Log, Restart); banner `BBj Language Server crashed again within 30 seconds and was not restarted. …` (Restart Server, Show Log); LSP4IJ's own error notification, raised by `showNotificationStartServerError()` in LanguageServerWrapper.start (tag 0.21.0, lines 430-433) with the text `The server was stopped unexpectedly.`
- Restart delays: crash auto-restart 1000 ms (`CRASH_RESTART_DELAY_MS`); Settings Apply and config reload 500 ms (`RESTART_DEBOUNCE_MS`); manual restart, banner and balloon Restart, and the Refresh Java Classes fallback 0 ms.
- Phase 97 observation (97-UAT-ARTIFACTS.md Round 1): after a kill, LSP4IJ restarts the server on its own when the user clicks into an editor.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`.
  The only allowed `cd` form is `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew …`.
  Prefer `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`. Use
  `git -C /home/coder/repos/bbj-language-server …` for git. Stage with `git add <exact path>` only.
  Never use `git stash` or a path-wide `git restore --source`.
- A runtime sequence used as evidence comes only from the maintainer's pasted idea.log lines or the
  maintainer's own description of the screen, never from a hand-derived trace (v4.4 standing
  decision). Every expected line quotes a literal read from the code path named beside it.
- If code-review fixes land after this plan, rebuild both distributables with Task 1's commands and
  repeat the scenarios the fixes touch before the phase is verified.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark LIFE-01 or
  LIFE-02 complete in REQUIREMENTS.md; the phase verifier closes them.
- Commits: conventional subject. Put the `Co-Authored-By:` trailer in the final paragraph.

## Artifacts this phase produces

- (108-01) `BbjLanguageServer` hook and `stop()` overrides; the factory status feed; console-only
  client override; real from-state log line; canaries; `lsp/BbjStatusFeedSourceGuardTest.java`;
  `108-UAT-ARTIFACTS.md` Probe build; `/tmp/phase-108-probe/`.
- (108-02) `BbjServerService.reportUnexpectedExit`, `applyCrashPolicy`, `describeExit`,
  `isAutoRestartAbandoned`, `requestGatedRestart`, disarm after the plugin's own stop, crash and
  give-up WARN lines; `ExpectedStopGuard.classifyExit(long)`; the rewritten guards and tests.
- (108-03) `BBj: Crashed` widget state; banner gated on give-up;
  `ui/BbjServerCrashNotificationProviderSourceGuardTest.java`; the features.md lines.
- (this plan) `/tmp/phase-108-uat/bbj-lang.vsix`, `/tmp/phase-108-uat/bbj-intellij-0.1.0.zip`,
  `/tmp/phase-108-uat/plugin.jar` (outside the repository), and the `## Final UAT` section of
  `108-UAT-ARTIFACTS.md`: build identity, run order, seven scenario tables, observed excerpts, verdict.

<tasks>

<task type="tracer">
  <name>Task 1: Final tree to the maintainer, end to end: register check, both distributables, whole suite, and the seven-scenario script with derived expectations</name>
  <files>.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md</files>
  <read_first>
    - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md (Probe build section and its observed excerpt and verdict)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerService.java (final: reportUnexpectedExit, applyCrashPolicy, updateStatus, requestRestart, requestGatedRestart, doRestart, notifyCrash)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/lsp/BbjLanguageServer.java (final: onUnexpectedStop, stop)
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjStatusBarWidget.java and bbj-intellij/src/main/java/com/basis/bbj/intellij/ui/BbjServerCrashNotificationProvider.java (final UI text)
    - .planning/phases/108-intellij-crash-detection/108-02-SUMMARY.md and .planning/phases/108-intellij-crash-detection/108-03-SUMMARY.md (deviations that change a literal)
    - /home/coder/.claude-shared/dot-claude/projects/-home-coder-repos-bbj-language-server/memory/uat-build-both-extensions-first.md
  </read_first>
  <action>
Prepare the exact build and script the maintainer will run (D-14, D-15).

1. Register check. Run `grep -nE` with the pattern from the acceptance criteria over every file this
   phase changed: the four `lsp` and three `ui` main sources, `ExpectedStopGuard.java`, the seven
   test files and `documentation/docs/intellij/features.md`. It must print nothing. A hit is fixed at
   the source by rewording the comment, and recorded in the SUMMARY.
2. Builds:
   - Run `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then
     `bbj-ext-install`, then
     `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew clean buildPlugin --console=plain -q`.
   - Copy the VSIX (`/tmp/bbj-lang.vsix`) and the zip into `/tmp/phase-108-uat/`, and record
     `sha256sum` and the byte size of both.
   - Extract the plugin jar to `/tmp/phase-108-uat/plugin.jar`, and confirm with `javap` that
     `com.basis.bbj.intellij.ui.BbjServerService` declares `reportUnexpectedExit`.
3. Run the whole IntelliJ suite with `--rerun-tasks` and record the test count.
4. Append `## Final UAT` to `108-UAT-ARTIFACTS.md`. Every expected line quotes the literal as written
   in the final code. Re-read each code path first and adjust the wording from the `<interfaces>`
   list if a later plan changed it. The section has these subsections:
   - `### Build identity`: commit, both sha256 values and sizes, test count.
   - `### Run order`: scenarios 7, 5, 3, 4 (reopen the project afterwards), 6, then 1 within 30 s of
     scenario 6 reaching Ready (this also proves the plugin's restart token is disarmed), then 2
     within 30 s of scenario 1's crash. Note the time before each scenario.
   - One `### Scenario N: <title>` per scenario 1-7, each with its steps and a table
     `| Row | Expected line or UI (substring) | Derived from | Observed? |`. Rows are numbered
     `S<N>.<k>`, "Derived from" names the code path, and "Observed?" is left empty. Contents:
     - Scenario 1 (kill once): the first-crash WARN from `applyCrashPolicy`; the
       `connection stop requested (…, process alive: false)` line from LSP4IJ's handler → stop(ctx)
       → shutdownAll → our `stop()`; `status: started -> stopping` and `status: stopping -> stopped`;
       `Scheduled a BBj language server restart in 1000 ms`; the Restarting, Starting and Launching
       lines from `doRestart`; a status line ending `-> started`. UI rows: the widget shows
       `BBj: Crashed`, then `BBj: Ready`; no editor banner; no `crashed unexpectedly` balloon;
       LSP4IJ's own `The server was stopped unexpectedly.` notification may appear and is expected
       (D-12). Must-not rows: `auto-restart stopped until a manual restart`, and
       `exited during a plugin restart`.
     - Scenario 2 (second kill within 30 s): the crash WARN ending `crash 2 within 30 s, not auto-restarting`;
       the give-up WARN; no `Scheduled a BBj language server restart` line after it until the tester
       acts. UI rows: the `crashed unexpectedly` balloon with Show Log and Restart; the editor banner;
       the widget's `BBj: Crashed` with the give-up tooltip; LSP4IJ's own notification. A step: wait
       15 s without clicking into an editor and look at the banner, then press Restart Server in the
       banner and see `Scheduled a BBj language server restart in 0 ms` and the Crashed state clear.
       Note, from Phase 97's observation: clicking into an editor earlier may let LSP4IJ start the
       server by itself (`stopped -> starting` with no Scheduled line); the banner then clears on
       `started`.
     - Scenario 3 (close the last BBj file): `status: started -> stopping`,
       `connection stop requested (…, process alive: true)`, `status: stopping -> stopped`. Must-not
       rows: `exited unexpectedly`, `Scheduled a BBj language server restart`.
     - Scenario 4 (close the project with a BBj file open): a `connection stop requested` line may
       appear. Must-not row: `exited unexpectedly`.
     - Scenarios 5, 6 and 7 (Settings Apply after changing a setting; the manual Restart Server;
       config reload by editing and saving the configured config file, or the Refresh Java Classes
       fallback's `Restart language server` balloon with BBjServices stopped): `Scheduled … in 500 ms`
       for 5 and a config reload, `in 0 ms` for 6 and the fallback; the Restarting line with status
       `started`; `connection stop requested (…, process alive: true)`; the status lines; Starting;
       Launching; `-> started`. Must-not row: `exited unexpectedly`. Allowed alternative:
       `exited during a plugin restart … treated as an expected stop, not a crash`, which is the
       race the plugin's token covers.
     - One cross-scenario row per scenario: every status line's from-state equals the previous status
       line's to-state (criterion 3).
   - `### Scenario excerpts (observed)`: redaction rule, marker format
     `--- scenario N (HH:MM:SS) ---`, and an empty fenced `text` block.
   - `### Verdict`: `Pending`.
  </action>
  <verify>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip bbj-intellij/lib/language-server/main.cjs | cmp - /home/coder/repos/bbj-language-server/bbj-vscode/out/language/main.cjs && unzip -p /tmp/phase-108-uat/bbj-intellij-0.1.0.zip bbj-intellij/lib/bbj-intellij-0.1.0.jar > /tmp/phase-108-uat/plugin.jar && javap -p -classpath /tmp/phase-108-uat/plugin.jar com.basis.bbj.intellij.ui.BbjServerService | grep -c 'reportUnexpectedExit' && ls /tmp/phase-108-uat/bbj-lang.vsix /tmp/phase-108-uat/bbj-intellij-0.1.0.zip</automated>
    <fails_when>cmp prints a difference or exits non-zero, grep prints 0 (stale plugin jar), or ls reports a missing file</fails_when>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --rerun-tasks --console=plain</automated>
    <fails_when>non-zero exit or "BUILD FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b108-[0-9]{2}\b|\bD-[0-9]{2}\b|\bLIFE-0[0-9]\b|\bT-108-[0-9]+\b|\b(CR|WR|IN)-[0-9]{2}\b|Pitfall [0-9]'` over the phase's changed source, test and doc files (each named by absolute path, as listed in the action; no recursive scan) prints nothing
    - `grep -c -E '^### Scenario [1-7]: ' /home/coder/repos/bbj-language-server/.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md` prints 7, and `grep -c -E '^\| S[1-7]\.[0-9]+ ' …/108-UAT-ARTIFACTS.md` prints at least 35
    - `grep -c 'The server was stopped unexpectedly' /home/coder/repos/bbj-language-server/.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md` prints at least 2 (D-12 listed in scenarios 1 and 2)
    - `/tmp/phase-108-uat/` holds the VSIX, the zip and plugin.jar; both sha256 values are in the Build identity subsection; the Gradle run ends with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>The final tree passes the register check and the whole suite. Both distributables are built from it and the zip provably carries the crash entry. The script lists every expected line, derived from a named code path, ready for observed marks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hand UAT on macOS: the seven crash and stop scenarios</name>
  <read_first>
    - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md (Final UAT section)
  </read_first>
  <what-built>Crash detection from LSP4IJ's unexpected-stop hook. The first crash is auto-restarted once, quietly, with the widget showing BBj: Crashed. A second crash within 30 s gives up with the balloon and the editor banner. The plugin's own restarts and LSP4IJ's normal stops are never crashes. Status lines name the real previous status. Builds: `/tmp/phase-108-uat/bbj-intellij-0.1.0.zip` (and `bbj-lang.vsix`).</what-built>
  <how-to-verify>
    1. Install the zip in IntelliJ on macOS (Settings, Plugins, gear icon, Install Plugin from Disk) and restart the IDE. Open a BBj project and a `.bbj` file, and wait for `BBj: Ready`.
    2. Run the scenarios in the order given under `### Run order` in 108-UAT-ARTIFACTS.md, noting the time before each. Kill with `kill -9 $(pgrep -f 'bbj-intellij/lib/language-server/main.cjs')`.
    3. For each scenario, note what the status bar widget, balloons and editor banner show.
    4. At the end, run `grep "BBj language server" <path to idea.log>` and paste this session's lines into `### Scenario excerpts (observed)`, with a `--- scenario N (HH:MM:SS) ---` marker before each scenario's lines. Replace your home directory with `<home>` and the project directory with `<project>`.
  </how-to-verify>
  <acceptance_criteria>
    - The excerpt block holds the real grep output for all seven scenarios, with markers, and no unredacted `/Users/<name>` path
    - The maintainer's reply describes the widget, balloon and banner seen in each scenario, or lists the failing scenario numbers with what was seen
  </acceptance_criteria>
  <resume-signal>Type "uat pasted" followed by what the widget, balloons and banner showed in each scenario, or list the failing scenario numbers and what you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark every expected line observed, derived or missing, and record the verdict</name>
  <files>.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md</files>
  <read_first>
    - .planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md (Final UAT tables and the pasted excerpts)
    - the maintainer's resume message from Task 2
  </read_first>
  <action>
Per D-15, fill the `Observed?` cell of every `S<N>.<k>` row:
- `observed` when the pasted excerpt contains the line in that scenario's window (a log row), or the
  maintainer's reply describes it (a UI row). Quote the timestamp or the maintainer's words next to
  it.
- `derived` when the scenario did not exercise the row. For example, the optional LSP4IJ restart on
  an editor click, or a must-not row whose absence was simply not contradicted. Name the code path.
- `missing` when an expected line is absent, or a must-not line is present.
Then check criterion 3 across all excerpts: every status line's from-state equals the previous to-state.
Replace `### Verdict` with one line per scenario (pass or fail, with the rows that decide it), a
mapping to ROADMAP success criteria 1-4, and an overall verdict. Redact any `/Users/<name>` or project
path that slipped through. If any scenario failed, record it as failing and stop; the orchestrator
routes it to gap closure. Never turn a `missing` into a pass.
  </action>
  <verify>
    <automated>F=/home/coder/repos/bbj-language-server/.planning/phases/108-intellij-crash-detection/108-UAT-ARTIFACTS.md; test "$(grep -cE '^\| S[1-7]\.[0-9]+ ' "$F")" -ge 35 && test "$(grep -E '^\| S[1-7]\.[0-9]+ ' "$F" | grep -vcE '\| *(observed|derived|missing)[^|]*\| *$')" = 0 && test "$(grep -cE '/Users/[^<]' "$F")" = 0 && grep -c -E '^### Verdict' "$F"</automated>
    <fails_when>a test fails (fewer than 35 scenario rows, a row without an observed, derived or missing mark, or an unredacted /Users path), or the final grep prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Every `S<N>.<k>` row carries observed, derived or missing
    - `### Verdict` has seven scenario lines, the criteria 1-4 mapping and an overall verdict
    - No unredacted `/Users/<name>` path remains in the file
  </acceptance_criteria>
  <done>Every expected line is marked observed, derived or missing from the real excerpt and the maintainer's reply, and the per-scenario and overall verdicts are recorded. A failing scenario is left for gap closure, never smoothed over.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer's idea.log and screen observations → public repository | pasted log lines and descriptions enter a committed planning file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-108-12 | Information disclosure | `108-UAT-ARTIFACTS.md` scenario excerpts | medium | mitigate | The redaction rule appears in the checkpoint and the file; Task 3 redacts and its verify requires `grep -cE '/Users/[^<]'` to print 0; no BBj source text is pasted. |
| T-108-13 | Repudiation | UAT verdict | medium | mitigate | Rows are observed only when backed by a pasted line or the maintainer's own words. Unexercised rows stay `derived`, and `missing` is never converted into a pass (D-15). |
| T-108-14 | Information disclosure | planning identifiers in shipped source | low | mitigate | The Task 1 register grep over every changed source, test and doc file must print nothing before the build. |
| T-108-SC | Tampering | package installs | low | accept | No package install; the builds use the existing lockfiles and the pinned Gradle wrapper. |
</threat_model>

<verification>
- `/tmp/phase-108-uat/` zip carries the fresh server bundle and a plugin jar with `reportUnexpectedExit`; whole IntelliJ suite green with `--rerun-tasks`
- Every scenario row marked; verdict recorded against ROADMAP criteria 1-4
</verification>

<success_criteria>
- One build carrying LIFE-01 and LIFE-02 passes the hand UAT in a running IntelliJ on macOS: kills are crashes, deliberate stops and plugin restarts are not, and every status line shows the real previous status
</success_criteria>

<output>
Create `.planning/phases/108-intellij-crash-detection/108-04-SUMMARY.md` when done
</output>
