---
phase: 88-setopts-in-code-hovers-tri-state-composer
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - bbj-vscode/src/setopts-catalog.ts
  - bbj-vscode/src/language/setopts-code-scanner.ts
  - bbj-vscode/src/language/bbj-hover.ts
  - bbj-vscode/test/setopts-catalog.test.ts
  - bbj-vscode/test/setopts-code-scanner.test.ts
  - bbj-vscode/test/hover.test.ts
autonomous: true
requirements: [DISC-05, DISC-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02 shape (b): hovering `SETOPTS opts$` where `opts$` traces back through only `IOR`/`AND` reassignments to an `OPTS` assignment shows which options the block sets and which it clears, relative to the runtime options vector (DISC-05)."
    - "D-02 shape (c): hovering a single `IOR(...)` call names the options it sets; hovering a single `AND(...)` call names the options it CLEARS — the bits absent from the mask — and never prints the raw mask as a set-bit list."
    - "D-04 (the DISC-06 safe boundary): a chain interrupted by an `IfStatement`/`ElseStatement`/`IfEndStatement`/`WhileStatement`/`WhileEndStatement`/`ForStatement`/`GotoStatement`, by a reassignment of the tracked variable to anything other than an `IOR`/`AND` of itself, by an `IOR`/`AND` whose first argument is a different variable (alias), by an unparseable mask, or by exhausting the enclosing statement list without reaching `OPTS`, is reported `safe: false` with a named `unsafeReason` — never `safe: true`."
    - "EDGE/DISC-05/adjacency (explicit): a chain with zero reassignments (`opts$=OPTS` immediately followed by `SETOPTS opts$`) and a chain whose first `IOR` is the statement immediately after the `OPTS` assignment are both recognized as safe."
    - "EDGE/DISC-06/adjacency (explicit): a chain of exactly one `IOR`/`AND` reassignment is safe, and an `OPTS` origin sharing its `LetStatement` line with an unrelated comma-chained assignment (`opts$=OPTS,other$=\"x\"`) is still recognized as the origin."
    - "EDGE/DISC-05/ordering (explicit): the accumulated set/clear lists are emitted in `SETOPTS_BITS` catalog order regardless of the order the `IOR`/`AND` statements appear, and last-write-wins per bit (a later `AND` of a bit an earlier `IOR` set leaves that bit in the clear list only)."
    - "D-07: the backward walk visits only the statement array directly enclosing the target and never descends into a nested `MethodDecl`, `DefFunction` or `BbjClass`; walk cost is bounded by that array's length, not by document size."
    - "D-01: no `bbj-intellij/` file is touched — both IDEs get shapes (b) and (c) through the standard LSP hover response."
  artifacts:
    - bbj-vscode/src/language/setopts-code-scanner.ts
    - bbj-vscode/test/setopts-code-scanner.test.ts
  key_links:
    - "`setoptsHoverTarget` → `traceOptsChain(setOptsStatement)` → backward sibling walk over the enclosing `Program.statements` / `MethodDecl.body` / `DefFunction.body` / `CompoundStatement.statements` array."
    - "`traceOptsChain` → `resolveLibFunction` (exported by plan 88-01) for every `IOR`/`AND` link and for the `OPTS` `LibVariable` origin."
    - "`foldChainEffect(links)` → `SetOptsChainEffect` → `describeIorAndMask` / `describeMaskVector` → hover markdown."
  prohibitions:
    - "A shape the walk could not fully trace must never be reported as safe; ambiguity resolves to hover-only, never to an offered edit."
    - "An `AND` mask must never be rendered as the options it sets — inverting DISC-05's cleared-bits framing would tell a user the opposite of what their code does."
    - "The walk must never cross into an enclosing or nested scope to find an origin it could not reach in the directly enclosing statement array."
---

<objective>
Complete DISC-05 by adding the two remaining hover shapes on top of the tracer: the
`OPTS`→`IOR`/`AND`→`SETOPTS` chain (D-02 shape b) and a single `IOR`/`AND` call (D-02 shape c,
with AND masks rendered as the options they clear). The same backward walk is the safe/unsafe
oracle DISC-06's edit gating depends on in plan 88-03, so it is built and tested here to its
final conservative boundary, not to a provisional one.

Purpose: 88-RESEARCH.md rates the traversal boundary MEDIUM confidence and names the failure
asymmetry explicitly — a false "unsafe" only denies a user an edit action; a false "safe" lets
an edit silently rewrite a variable whose runtime value was never decidable. This plan locks the
boundary and pins every named unsafe case with an executing test.

Output: `traceOptsChain`, `foldChainEffect`, `describeIorAndMask`, `describeMaskVector`, the two
new hover branches, and the negative-case suite.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-CONTEXT.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-RESEARCH.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-PATTERNS.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-01-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section of `88-01-PLAN.md` — the same list applies to
every plan in this phase. The symbols this plan lands are: `traceOptsChain`, `foldChainEffect`,
`SetOptsChainLink`, `SetOptsChainEffect`, `SetOptsUnsafeReason`, `describeIorAndMask`,
`describeMaskVector`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `describeIorAndMask` / `describeMaskVector` — AND masks as the options they clear</name>
  <files>bbj-vscode/src/setopts-catalog.ts, bbj-vscode/test/setopts-catalog.test.ts</files>
  <read_first>
    - `bbj-vscode/src/setopts-catalog.ts` lines 58-116 (`SETOPTS_BITS`), 118-147 (`SetOptsVector`, `parseVector`, `encodeVector`), 178-239 (`knownByteMask`, `unknownBitsInByte`, `describeVector`)
    - `bbj-vscode/test/setopts-catalog.test.ts` (whole file — the existing assertion style for catalog helpers)
    - `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-RESEARCH.md` §"AND-mask decode phrasing (DISC-05's \"logical cleared bits\" requirement)"
  </read_first>
  <behavior>
    - `describeIorAndMask(1, 0x08, 'set')` returns exactly `['Console mode in public programs']`.
    - `describeIorAndMask(1, 0xF7, 'clear')` returns exactly `['Console mode in public programs']` — the one catalog bit of byte 1 that is ABSENT from the mask.
    - `describeIorAndMask(1, 0xFF, 'clear')` returns `[]` — an all-ones AND mask clears nothing in that byte.
    - `describeIorAndMask(1, 0x00, 'set')` returns `[]`.
    - `describeMaskVector(v, 'set')` where `v` spans several bytes concatenates every byte's labels in `SETOPTS_BITS` catalog order (byte ascending, then bit descending), joined the same way `describeVector` joins its parts.
    - `describeMaskVector(v, 'clear')` reports, per byte PRESENT in the vector, the catalog bits absent from that byte's mask, and reports nothing for bytes beyond the vector's length (an AND mask shorter than the full catalog says nothing about bytes it does not cover).
    - Both functions treat only bytes listed in `BYTE_GROUPS` as option bytes; bytes 5-6 (mask replacement data) and 10-16 (reserved) are never described as options.
  </behavior>
  <action>
Add two exported functions to `bbj-vscode/src/setopts-catalog.ts`, placed immediately after the existing `describeVector` so the whole describe family reads together.

`describeIorAndMask(byteNo: number, mask: number, kind: 'set' | 'clear'): string[]` filters `SETOPTS_BITS` to that byte and selects, for `'set'`, bits where `(mask & bit.mask) !== 0`, and for `'clear'`, bits where `(mask & bit.mask) === 0`. Return the labels. Add no new bit data.

`describeMaskVector(v: SetOptsVector, kind: 'set' | 'clear'): string` loops the byte numbers of `BYTE_GROUPS` in ascending order, skips any byte beyond `v.bytes.length`, calls `describeIorAndMask` per byte, and joins non-empty byte results as `Byte N: label · label` segments joined by `; ` — the exact segment shape `describeVector` already produces. When every byte yields nothing, return `'(no modelled options)'` for `'set'` and `'(clears no modelled options)'` for `'clear'`, so the caller never has to special-case an empty string.

Document on both functions, in the doc comment, that a `0` bit in an `AND` mask means the option is cleared, citing DISC-05's wording, so the inversion is not "simplified" away by a later refactor.

Extend `bbj-vscode/test/setopts-catalog.test.ts` with a `describe` block covering every bullet in the behavior block, using catalog labels read from `SETOPTS_BITS` rather than hard-coded strings where the assertion is about which bits are selected, and one hard-coded label assertion (byte 1, `0x08`) that pins the actual wording reaching the user.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/setopts-catalog.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function describeIorAndMask" /home/coder/repos/bbj-language-server/bbj-vscode/src/setopts-catalog.ts` is exactly 1.
    - `grep -c "export function describeMaskVector" /home/coder/repos/bbj-language-server/bbj-vscode/src/setopts-catalog.ts` is exactly 1.
    - A test named for the `'clear'` inversion asserts `describeIorAndMask(1, 0xF7, 'clear')` equals `['Console mode in public programs']` and would fail if the two branches were swapped.
    - `npx vitest run test/setopts-catalog.test.ts` exits 0 with zero failed tests.
  </acceptance_criteria>
  <done>The catalog can describe an `IOR` mask's set options and an `AND` mask's cleared options, with the inversion pinned by a test that fails if the branches are swapped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backward `OPTS`→`IOR`/`AND` chain walk with its conservative safe/unsafe boundary</name>
  <files>bbj-vscode/src/language/setopts-code-scanner.ts, bbj-vscode/test/setopts-code-scanner.test.ts</files>
  <read_first>
    - `bbj-vscode/src/language/setopts-code-scanner.ts` (as left by plan 88-01)
    - `bbj-vscode/src/language/validations/check-variable-scoping.ts` lines 44-66 (`walkStatements`, `getSymbolRefName`), 108-125 (the `for (const assignment of stmt.assignments)` comma-chain iteration) and 340-357 (`getStatements`)
    - `bbj-vscode/src/language/validations/check-function-calls.ts` lines 110-125 (`resolveLibFunction`, exported by plan 88-01)
    - `bbj-vscode/src/language/generated/ast.ts` — `Assignment`, `LetStatement`, `SetOptsStatement`, `MethodCall`, `ParameterCall`, `SymbolRef`, `LibFunction`, `LibVariable`, `CompoundStatement`, `DefFunction`, `MethodDecl`, `Program`
    - `bbj-vscode/src/language/bbj-scope-local.ts` lines 340-360 — the existing "a `CompoundStatement` is transparent to its parent scope" rule this walk mirrors
    - `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-RESEARCH.md` §"Traceability Algorithm" (steps 1-4) and §"Flat statement containers"
    - `examples/issue208-and-is-also-a-function.bbj` — the existing fixture proving `AND(a$,b$)` parses as a call in the same file that uses `AND` as a logical operator
  </read_first>
  <behavior>
    - `opts$=OPTS` / `opts$=IOR(opts$,"$08$")` / `SETOPTS opts$` → `safe: true`, two links folded to `effect.set` containing byte 1 mask `0x08`, `effect.clear` empty.
    - `opts$=OPTS` / `SETOPTS opts$` (zero reassignments) → `safe: true`, `links: []`, both effect lists empty.
    - `opts$=OPTS,other$="x"` / `opts$=IOR(opts$,"$08$")` / `SETOPTS opts$` → `safe: true`; the unrelated comma-chained assignment on the origin line does not break detection.
    - `opts$=IOR(opts$,"$08$")` then `opts$=AND(opts$,"$F7$")` then `SETOPTS opts$` with an `OPTS` origin above → byte 1 `0x08` appears in `effect.clear` and NOT in `effect.set` (last write wins per bit).
    - An `IfStatement`, `ElseStatement`, `IfEndStatement`, `WhileStatement`, `WhileEndStatement`, `ForStatement` or `GotoStatement` between the origin and the `SETOPTS` → `safe: false`, `unsafeReason: 'control-flow'`.
    - `opts$="hello"` (or any non-`IOR`/`AND` reassignment of the tracked variable) between origin and target → `safe: false`, `unsafeReason: 'reassigned'`.
    - `opts$=IOR(other$,"$08$")` (first argument is a different variable) → `safe: false`, `unsafeReason: 'alias'`.
    - `opts$=IOR(opts$,someVar$)` or a mask literal `parseVector` declines → `safe: false`, `unsafeReason: 'unparseable-mask'`.
    - `SETOPTS opts$` with no `OPTS` assignment anywhere in the enclosing statement array → `safe: false`, `unsafeReason: 'no-origin'`.
    - An `OPTS` origin that lives in an OUTER scope (e.g. assigned at program level, `SETOPTS` inside a `MethodDecl` body) → `safe: false`, `unsafeReason: 'no-origin'` — the walk does not cross scopes.
    - A `CompoundStatement` sibling (semicolon-joined line) is transparent: an `IOR` link inside one is found as though it were a direct element at that position.
    - Variable names and the `IOR`/`AND`/`OPTS` names compare case-insensitively (`OPTS$`, `Ior`, `and` all match).
    - The walk visits at most `siblings.length` elements and never recurses into a nested `MethodDecl`, `DefFunction` or `BbjClass`.
  </behavior>
  <action>
Implement `traceOptsChain(target: SetOptsStatement | MethodCall): SetOptsCodeShape | undefined` and the helper `foldChainEffect(links: SetOptsChainLink[]): SetOptsChainEffect` in `bbj-vscode/src/language/setopts-code-scanner.ts`, following 88-RESEARCH.md's Traceability Algorithm exactly. Import `resolveLibFunction` from `./validations/check-function-calls.js` — do not re-implement it.

Step 1 — anchor: walk `target.$container` upward until a `Program`, `MethodDecl`, `DefFunction` or `CompoundStatement` is reached, remembering the last node on the way up that is a direct element of that container's statement array. The container's array is `Program.statements`, `MethodDecl.body`, `DefFunction.body` or `CompoundStatement.statements`. Write a local `enclosingStatements(node)` helper covering all four (the existing `getStatements` in `check-variable-scoping.ts` covers only two and is private — mirror its shape, do not export it from there).

Step 2 — tracked name: for a `SetOptsStatement`, the lowercased `$refText` of `opts` when `opts` is a `SymbolRef`; for an `IOR`/`AND` `MethodCall`, the lowercased `$refText` of the `SymbolRef` in `args[0].expression`. When neither yields a name, return `undefined` (not an unsafe chain — there is no chain to report).

Step 3 — backward scan from the anchor index minus one down to zero. Flatten a `CompoundStatement` sibling's own `statements` into the scan at that position, innermost-last-first, so a link inside a semicolon-joined line is seen in the right order. For each element:
  - a control-flow marker (`isIfStatement`, `isElseStatement`, `isIfEndStatement`, `isWhileStatement`, `isWhileEndStatement`, `isForStatement`, `isGotoStatement`) stops the walk with `unsafeReason: 'control-flow'`;
  - a `LetStatement` is scanned across ALL entries of its `assignments` array (never just index 0) for one whose `variable` is a `SymbolRef` matching the tracked name case-insensitively;
  - a matching assignment whose `value` is a `SymbolRef` resolving to a `LibVariable` named `OPTS` (case-insensitive) is the origin: stop, `safe: true`;
  - a matching assignment whose `value` is a `MethodCall` that `resolveLibFunction` resolves to a `LibFunction` named `IOR` or `AND` AND whose `args[0].expression` is a `SymbolRef` with the same tracked name is a link: parse its `args[1].expression` mask via the same literal-to-`parseVector` path plan 88-01 built for the absolute shape, push a `SetOptsChainLink`, and continue backward. A mask that will not parse stops the walk with `unsafeReason: 'unparseable-mask'`; a first argument naming a different variable stops it with `unsafeReason: 'alias'`;
  - any other matching assignment stops the walk with `unsafeReason: 'reassigned'`;
  - anything that does not touch the tracked name is skipped;
  - reaching index 0 with no origin stops with `unsafeReason: 'no-origin'`.
Collect links newest-first while scanning and reverse them before returning so `links` is in source order.

`foldChainEffect` walks `links` in source order maintaining one map keyed `byte:mask` over every catalog bit in `SETOPTS_BITS`. An `IOR` link whose mask has that bit set marks it `set`; an `AND` link whose mask has that bit CLEAR marks it `clear`; every other combination leaves the bit's previous state untouched. Emit `set` and `clear` arrays in `SETOPTS_BITS` iteration order so output ordering is catalog order, not statement order.

Extend `detectSetOptsShape` so a `SetOptsStatement` whose `opts` is a `SymbolRef` routes to `traceOptsChain` and returns the `chain` variant (with `safe` and, when false, `unsafeReason`), and a `MethodCall` resolving to `IOR`/`AND` with a parseable mask returns the `mask-call` variant.

Add a bounded-walk regression to `bbj-vscode/test/setopts-code-scanner.test.ts`: build a synthetic program with a safe three-statement chain preceded by several hundred unrelated `PRINT` statements, and assert the returned `links` length and `safe` flag are identical to the same chain with no preamble, and that the walk terminates. Then build the same chain with the `OPTS` origin placed BEFORE the unrelated preamble and assert `safe: false` with `unsafeReason: 'reassigned'` or `'no-origin'` as the algorithm dictates — record which in the SUMMARY, since it documents where the boundary actually lands.

Cover every remaining bullet of the behavior block with a named test case. Include a case adapted from `examples/issue208-and-is-also-a-function.bbj` in which the same document uses `AND` as a logical operator on one line and `AND(...)` as a chain link on another, asserting the operator line is not mistaken for a link.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/setopts-code-scanner.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test, or the run does not terminate within the default vitest timeout (the bounded-walk case would hang on an unbounded walk)</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the five `SetOptsUnsafeReason` members is asserted by at least one named test case.
    - A test asserts the chain is `safe: true` for the zero-reassignment case and for the comma-chained-origin case.
    - A test asserts an `OPTS` origin in an outer scope yields `safe: false`.
    - `grep -c "resolveLibFunction" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/setopts-code-scanner.ts` is at least 1, and the single-source-of-truth guard from plan 88-01 still reports exactly one definition.
    - `npx vitest run test/setopts-code-scanner.test.ts` exits 0 with zero failed tests.
  </acceptance_criteria>
  <done>The backward walk answers safe/unsafe for every named boundary case with an executing test, and folds a safe chain into catalog-ordered set/clear lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hover markdown for the chain and single-call shapes</name>
  <files>bbj-vscode/src/language/setopts-code-scanner.ts, bbj-vscode/src/language/bbj-hover.ts, bbj-vscode/test/hover.test.ts, bbj-vscode/test/setopts-code-scanner.test.ts</files>
  <read_first>
    - `bbj-vscode/src/language/setopts-code-scanner.ts` (as left by Task 2)
    - `bbj-vscode/src/language/bbj-hover.ts` (the branch plan 88-01 added inside `getHoverContent`)
    - `bbj-vscode/src/setopts-catalog.ts` — `describeIorAndMask` and `describeMaskVector` from Task 1
    - `bbj-vscode/test/hover.test.ts` (the block plan 88-01 added)
  </read_first>
  <behavior>
    - Hovering `SETOPTS opts$` on a safe chain returns markdown that states the decode is relative to the runtime options vector, then lists the set options, then the cleared options; an empty side is stated explicitly rather than omitted.
    - Hovering `SETOPTS opts$` on an unsafe chain returns markdown that says the effective value cannot be determined statically and names the reason in user-facing wording (control flow / reassignment / alias / unreadable mask / no `OPTS` origin) — it never falls back to a fabricated absolute vector, and it never claims the block is editable.
    - Hovering the `IOR` token of `opts$=IOR(opts$,"$08$")` returns markdown naming the option that call sets.
    - Hovering the `AND` token of `opts$=AND(opts$,"$F7$")` returns markdown naming the option that call CLEARS, with wording that says the option is cleared.
    - Hovering the first argument `opts$` inside `IOR(opts$,"$08$")` returns no SETOPTS markdown — only the method-name token triggers shape (c).
    - Hovering the `AND` of a logical `IF x=1 AND y=2` line returns no SETOPTS markdown.
  </behavior>
  <action>
Extend `setoptsHoverTarget` so it also returns the `MethodCall` when the hovered leaf's `astNode` is a `SymbolRef` that is that call's own `method` expression. Do not return the call when the leaf sits inside a `ParameterCall` — that is how the first-argument case stays out of shape (c).

Extend `setoptsHoverMarkdown` with the two new variants.
For `chain` + `safe: true`: header `__SETOPTS <variable>__`, then a line stating the block is evaluated against the current runtime options vector returned by `OPTS`, then `Sets: <describeMaskVector-style catalog-ordered labels>` and `Clears: <...>`, each rendering an explicit `(none)` when its list is empty. Build the label lists from `effect.set` / `effect.clear` through `describeIorAndMask` so the wording and ordering come from one place.
For `chain` + `safe: false`: header `__SETOPTS <variable>__`, then one sentence saying the effective vector cannot be determined statically, then a reason sentence selected from a `Record<SetOptsUnsafeReason, string>` constant declared next to the type — one map, no inline string switch, so plan 88-03 can reuse the same wording in its `reason` field.
For `mask-call`: header `__IOR(...)__` or `__AND(...)__` with the parsed mask's uppercase hex, then for `IOR` the set-options list via `describeMaskVector(vector, 'set')` and for `AND` the cleared-options list via `describeMaskVector(vector, 'clear')` introduced by wording that says these options are cleared.

`bbj-hover.ts` needs no new branch — the existing call chain plan 88-01 added already routes any shape `detectSetOptsShape` returns. Confirm this by reading the file; if a variant escapes the existing branch, widen the existing call rather than adding a second branch.

Add the behavior bullets as named cases in `bbj-vscode/test/hover.test.ts` (real `getHoverContent` calls) and the pure-markdown assertions in `bbj-vscode/test/setopts-code-scanner.test.ts`.

Add one assertion that the unsafe-chain hover markdown does NOT contain the phrase used by the safe-chain hover to introduce its set list, so a future refactor cannot make the two render identically.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/setopts-code-scanner.test.ts test/hover.test.ts test/setopts-catalog.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - A named test asserts the `AND` single-call hover text names the option as cleared and does not name it as set.
    - A named test asserts hovering the first argument of an `IOR` call returns no SETOPTS markdown.
    - A named test asserts the logical-`AND` operator line produces no SETOPTS markdown.
    - `grep -c "getAstNodeHoverContent" /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-hover.ts` is exactly 1 — its single declaration, unchanged; the new shapes reuse the `getHoverContent` branch. <!-- planner-discipline-allow: getAstNodeHoverContent -->
    - `npx vitest run test/setopts-code-scanner.test.ts test/hover.test.ts test/setopts-catalog.test.ts` exits 0 with zero failed tests.
  </acceptance_criteria>
  <done>All three DISC-05 hover shapes render from the language server, with AND masks framed as cleared options and unsafe chains stated as undecidable rather than guessed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → language server (`textDocument/hover`) | Arbitrary `.bbj` source, including deliberately ambiguous option chains, reaches the walk |
| scanner chain walk → downstream edit gating (plan 88-03) | The `safe` flag this plan computes is the only thing standing between an undecidable shape and an offered edit action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering | `traceOptsChain` safe/unsafe classification | high | mitigate | The walk stops at the first element it cannot fully account for and assigns a named `unsafeReason`; Task 2 pins all five reasons plus the outer-scope case with executing tests, so a widening of the boundary fails the suite |
| T-88-03 | Tampering | `IOR`/`AND` mask literal parsing | medium | mitigate | ASVS V5: a mask reaches `foldChainEffect` only after `parseVector` round-trips it; anything else stops the walk with `unsafeReason: 'unparseable-mask'` — no partial parse |
| T-88-04 | Denial of Service | backward sibling walk | medium | mitigate | The walk is bounded by the directly enclosing statement array and never recurses into a nested declaration body; Task 2's synthetic large-preamble test asserts the result is identical with and without the preamble and that the run terminates |
| T-88-07 | Tampering | `AND`-mask rendering | high | mitigate | `describeIorAndMask`'s `'clear'` branch inverts the bit test; Task 1 pins the inversion with a test that fails if the two branches are swapped, and Task 3 pins the user-facing "cleared" wording |
| T-88-SC | Tampering | npm/gradle installs | high | accept | This plan installs no package; 88-RESEARCH.md §"Package Legitimacy Audit" records the phase as not applicable |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/setopts-code-scanner.test.ts test/hover.test.ts test/setopts-catalog.test.ts` exits 0.
- `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-intellij` is empty (D-01).
- Each `SetOptsUnsafeReason` member appears in at least one test assertion.
</verification>

<success_criteria>
- All three DISC-05 hover shapes work through the real `getHoverContent` path.
- `AND` masks are described as the options they clear, pinned by a swap-detecting test.
- Every named unsafe chain case reports `safe: false` with a specific reason; no case reports a false `safe: true`.
- The walk is bounded by the enclosing statement array, proven by a synthetic large-file test.
</success_criteria>

<output>
Create `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-02-SUMMARY.md` when done.
</output>
