---
phase: 88-setopts-in-code-hovers-tri-state-composer
plan: 13
type: execute
wave: 3
depends_on: ["88-11", "88-12"]
files_modified:
  - bbj-vscode/test/functional/installed-extension-e2e.test.ts
  - QA/FULL-TEST-CHECKLIST.md
  - .planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md
  - .planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md
autonomous: true
gap_closure: true
# `gap_refs` names the gaps this plan works on, for readers. `gap_ids` is DELIBERATELY EMPTY, and
# the two keys are NOT interchangeable:
#   - `gap_refs` is documentation. Nothing consumes it.
#   - `gap_ids` has exactly one consumer — /gsd-verify-work's reconcile_gaps step, which flips a gap
#     to `status: resolved` the moment ANY plan naming its gap_id has a SUMMARY.md on disk.
# `.planning/config.json` leaves `workflow.human_verify_mode` unset, so it defaults to
# `end-of-phase`: planner-emitted mid-flight human-verify checkpoints are suppressed and the
# verification detail rides in Task 3's `<verify><human-check>`, which the verifier harvests into
# the `human_needed` → `88-UAT.md` flow at end of phase. This plan's SUMMARY.md is therefore written
# by an autonomous run, BEFORE any human touches an IDE or a live BBjServices. Populating `gap_ids`
# would auto-resolve both gaps off automated evidence alone — the exact failure this whole
# gap-closure round exists to correct, and the trap 88-07 and 88-09 both guarded against.
# G-88-2 and G-88-3 stay `status: failed` until the harvested human-check is answered live.
gap_refs: [G-88-2, G-88-3]
gap_ids: []
requirements: [DISC-05, DISC-06]

# assumption-delta: this plan STAGES the falsification of 88-RESEARCH.md Assumption A2 (the
# 16-byte/32-hex-digit mask width) as a live human check. It does not decide it — no headless BBj
# run is possible here (see the objective's probed evidence).
# api-coverage: no external service is integrated by this plan — no COVERAGE.md is produced. If the
# pre-verify detector trips on prose, answer it with a <200-char "No external API integration:"
# COVERAGE.md rather than reshaping the plan.
assumption_delta: no-change

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both distributables are rebuilt and proven from the inside to carry this round's fixes: the installed VS Code bundle composes blocks whose mask arguments are bare delimited hex literals over a real connection, and the IntelliJ zip ships the new context-menu action, the Java hex-literal formatter and the composer intention."
    - "The QA checklist no longer instructs testers to hover syntax the decoder now correctly refuses — rows 15 and 19's sample `SETOPTS`/`IOR`/`AND` lines use the same bare form real BBj programs use, so a tester following them verbatim cannot file a false gap."
    - "The QA checklist carries a row for the IntelliJ composer's new context-menu entry point, so a future round exercises the path that does not depend on intention search."
    - "`88-LIVE-RETEST.md` scripts round two: the exact build identity to install, what each fix changed, the exact lines to read in the generated block, and both IntelliJ entry points — so the human spends attention only on what a human can see."
    - "`88-UAT.md`'s G-88-2 and G-88-3 records state precisely what is still owed, separate shipped-artifact evidence from live-editor evidence, and both still read `status: failed`."
    - "88-RESEARCH.md Assumption A2 is staged as a live check that a human actually runs — not inferred from unit tests, which is how it went unanswered the first time."
  artifacts:
    - ".planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md — round two of the scripted retest, replacing round one's content"
    - "QA/FULL-TEST-CHECKLIST.md — corrected sample syntax in the SETOPTS-in-code hover rows, plus a row for the IntelliJ context-menu entry"
    - ".planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md — both gap records narrowed to the residue, with automated evidence separated from live evidence"
    - "bbj-vscode/test/functional/installed-extension-e2e.test.ts — a shipped-bundle assertion that the composed block's literals are valid BBj"
  key_links:
    - "`bbj-vscode/out/language/main.cjs` → `copyLanguageServer` → the plugin zip's own `lib/language-server/main.cjs`: the IntelliJ plugin ships its OWN copy of the server, so the VS Code build must precede `buildPlugin` or the zip carries a stale server even when the VS Code side is fresh"
    - "the built zip's sha256 + the git HEAD sha → `88-LIVE-RETEST.md` → the human's installed plugin: without a recorded build identity a passing retest cannot be told apart from a retest against yet another stale install"
    - "the human's verdict → `88-UAT.md` gap records → `reconcile_gaps`: the only path by which G-88-2 or G-88-3 may be marked resolved"
---

<objective>
Ship the round, hand the last mile to a human as a scripted check, and record the verdict where the
next verification round reads it.

Plans 88-10 and 88-11 fixed G-88-3's generated and accepted text; plan 88-12 fixed G-88-2's server
half and added an IntelliJ entry point that does not pass through intention search. Everything those
three plans prove is a property of SOURCE and of ARTIFACTS. Two things remain that no automated gate
in this environment can reach:

  - A frozen modal dialog in a running IntelliJ. There is no IntelliJ sandbox, installed-plugin
    directory or IDE configuration directory anywhere under `/home/coder` — probed directly during
    plan 88-09; the tester's IntelliJ runs on a separate host.
  - 88-RESEARCH.md Assumption A2. This is the question UAT test 8 was designed to answer and it is
    STILL UNANSWERED: the quoting defect aborted the run before `AND()` ever saw two decoded
    operands, so the live behaviour of a 16-byte/32-hex-digit mask base against a real `OPTS` value
    has never actually been observed. Headless BBj cannot decide it here either — `/opt/bbx/bin/bbj`
    terminates with "Must have a display for GUI mode (SysWindow/ThinClient)" in GUI mode and
    "Could not find a termcap file: /etc/termcap" on the non-GUI terminal aliases, with no X server
    and no `xvfb-run` installed (both probed during plan 88-09). Fixing the quoting may still expose
    an independent width defect; that is exactly why this must be run, not assumed.

There is also a trap this plan has to defuse before the retest, not after. `QA/FULL-TEST-CHECKLIST.md`
rows 15 and 19 tell the tester to hover `SETOPTS "$04$"`, `IOR(var$,"$01$")` and `AND(var$,"$FE$")` —
the invalid quoted form. Plan 88-11 makes the decoder refuse exactly that, correctly, because BBj
never hex-decodes it. A tester following those rows verbatim after this round would see no hover and
file a false regression against a fix that is working. The sample syntax must be corrected in the
same round that tightens the decoder.

Purpose: turn three plans' worth of artifact evidence into either a recorded closure with a named
build identity, or a sharper failure the next round can act on.
Output: verified distributables, a corrected QA checklist, a round-two retest script, and narrowed
gap records.

Explicitly out of scope: any change to `bbj-vscode/src/` or `bbj-intellij/src/`. This plan also
never marks a gap resolved — a failing retest is answered by a fresh debug session with its own
diagnosis, not by a fix improvised here.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-10-SUMMARY.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-11-SUMMARY.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-12-SUMMARY.md
@.planning/debug/g-88-2-docker-pull-hang.md
@.planning/debug/g-88-3-composer-mask-literal-quoting.md
@QA/FULL-TEST-CHECKLIST.md
</context>

<interface_context>
Verified against the current tree and this devcontainer during planning — do not re-derive these.

**Building the two distributables**
- VS Code: `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then
  `bbj-ext-install` (the devcontainer helper that packages and installs into `~/.ext-test`). The
  e2e suite resolves the install from `~/.ext-test/extensions/extensions.json` by
  `identifier.id === 'basis-intl.bbj-lang'` and SKIPS every test when it is absent.
- IntelliJ: `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin` →
  `bbj-intellij/build/distributions/bbj-intellij-<version>.zip`. The version on disk at planning
  time is `0.1.0`; confirm with `ls` rather than assuming. The build fails fast if
  `bbj-vscode/out/language/main.cjs` is missing, so the VS Code build must run FIRST — the plugin
  copies that file into its own `lib/language-server/`.
- Verified zip layout: `bbj-intellij/lib/language-server/main.cjs`,
  `bbj-intellij/lib/bbj-intellij-0.1.0.jar` (the plugin jar, holding `META-INF/plugin.xml` and the
  class entries), `bbj-intellij/lib/bbj-intellij-0.1.0-searchableOptions.jar`. A
  `bbj-intellij-*.jar` glob matches BOTH jars — name the plugin jar exactly, or an assertion can
  pass against the wrong archive.
- Plan 88-09's Task 1 established this extraction idiom: `unzip -p <zip> '<jar path>' > /tmp/bbj-plugin.jar`
  once, then `unzip -p /tmp/bbj-plugin.jar META-INF/plugin.xml` and `unzip -l /tmp/bbj-plugin.jar`
  for the rest.

**What must now be inside the plugin jar** (in addition to what 88-09 already proved — the
`ConfigureSetoptsInCodeIntention` registration, its `intentionDescriptions/` resources, the
`SetoptsTriStateComposerDialog` class, and the five hover symbols in the bundled server):
- the new editor-popup action's id and class registration in the packaged `META-INF/plugin.xml`
  (its exact id and class name are recorded in `88-12-SUMMARY.md` — read them from there rather
  than guessing);
- `com/basis/bbj/intellij/actions/BbjComposeSetoptsInCodeAction.class`;
- `com/basis/bbj/intellij/composer/BbjHexLiteral.class` (plan 88-10's Java formatter).

**The e2e harness** (`bbj-vscode/test/functional/installed-extension-e2e.test.ts`): the
`tri-state composer client registration and server requests` describe already sends
`composeTriState` over a real connection to the INSTALLED bundle and asserts the block's shape
(around line 313). That is the natural home for a "the literals it composes are valid BBj"
assertion — same connection, same installed artifact, no new harness.

**`QA/FULL-TEST-CHECKLIST.md` structure**: `**Instructions:**` is a 6-entry numbered list whose
entry 1 (added by plan 88-09) already requires rebuilding and reinstalling both integrations before
any row runs. The VS Code table ends at row 16; the IntelliJ table runs 17-21. Multi-step cells use
`<br>` separators inside a single table cell. Rows 15 and 19 (hover decode, both IDEs) currently
show the quoted sample syntax; rows 16 and 20 (composers) refer to "the canonical chain above" and
inherit whatever 15/19 show. Row 21 is the live-BBjServices mask-width run. This file is USER-FACING
QA documentation: no plan number, decision id, gap id or review-finding id may appear in it. An
issue reference such as `#475` is fine and is already used.

**`88-UAT.md` gap-record shape**: keys in order `gap_id`, `truth`, `status`, `reason`, `severity`,
`test`, `root_cause`, `artifacts`, `automated_evidence` (present on G-88-1 and G-88-2 already, added
by plan 88-09), `missing`, `debug_session`. The `## Summary` block counts
`total`/`passed`/`issues`/`pending`/`skipped`/`blocked`. G-88-1 is already `status: resolved` — do
not touch it.

**The retest fixture**: `examples/issue475-setopts-in-code.bbj` — four groups, all in the bare hex
form, including the canonical safe `B$=OPTS` / `IOR` / `AND` / `SETOPTS B$` chain and the absolute
`SETOPTS $00C20240000000000000000000000000$` literal.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Rebuild both distributables and prove from inside them that this round's fixes ship</name>
  <precondition>`bbj-ext-install` is on PATH and `~/.ext-test/extensions/extensions.json` exists — the e2e suite silently skips every test without it, and a skipped suite proves nothing about the installed bundle.</precondition>
  <files>bbj-vscode/test/functional/installed-extension-e2e.test.ts</files>
  <read_first>
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-12-SUMMARY.md` — the new action's exact id and class name, and the reinstalled extension identity that plan recorded
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-10-SUMMARY.md` — the four literal block lines and the Java formatter's class name
    - `/home/coder/repos/bbj-language-server/bbj-vscode/test/functional/installed-extension-e2e.test.ts` lines 246-340 — the composer-request describe block that this task extends
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-09-SUMMARY.md` — the previous round's build identity and the in-distributable assertion idiom
  </read_first>
  <action>
    Build in dependency order and prove the artifacts, exactly as plan 88-09 did for the previous
    round — this is what makes the human's later "I installed it and it works" statement mean
    something.

    (1) `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build`, then
    `bbj-ext-install`. Record the installed version and `installedTimestamp` from
    `~/.ext-test/extensions/extensions.json`.

    (2) Extend the e2e's composer-request describe with one assertion it never had: over the real
    connection to the INSTALLED bundle, send `composeTriState` with one option Set and one Clear,
    and assert that every returned reassignment line's mask argument is a bare delimited hex
    literal — a dollar delimiter, hex digits, a dollar delimiter, directly between the comma and
    the closing parenthesis — and that no returned line contains a quote character. This is the
    shipped-artifact counterpart of plan 88-10's unit oracle: the same defect, asserted against the
    bundle a user actually loads, over the wire. Keep it inside the existing describe so it reuses
    that block's already-spawned server.

    (3) Re-run the whole e2e file and confirm it RAN — a reported pass count of zero means the
    install did not happen and every assertion was skipped.

    (4) `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin`. This is
    the one place `cd` is permitted, because Gradle needs its project directory; do not chain any
    read command onto it.

    (5) Establish the plugin artifact's identity and record it: the exact zip filename and mtime
    from `ls -la` (confirm the version rather than assuming), its `sha256sum`, and
    `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`. Carry all three into the SUMMARY
    and into Task 2's document.

    (6) Assert from INSIDE the distributable, extracting the plugin jar once by its exact path:
      a. the packaged `META-INF/plugin.xml` registers the new editor-popup action's id and class
         (read both from `88-12-SUMMARY.md`) — the entry point that does not depend on intention
         search;
      b. the packaged jar contains that action's class entry;
      c. the packaged jar contains the Java hex-literal formatter's class entry — plan 88-10's fix
         for the IntelliJ in-place writer;
      d. the packaged plugin.xml still registers `ConfigureSetoptsInCodeIntention` (unchanged by
         this round, and its absence would mean a descriptor edit went wrong).

    (7) If ANY of (6a)-(6d) fails, stop and record it as a new finding. A missing entry means the
    packaging is broken, which is materially different from anything the two gaps describe and must
    not be papered over by proceeding to a human retest.

    Do not modify any file under `bbj-vscode/src/` or `bbj-intellij/src/`. The zip is a gitignored
    build output and is not committed.
  </action>
  <verify>
    <automated>npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run test -- test/functional/installed-extension-e2e.test.ts</automated>
    <fails_when>vitest reports a failing test, OR reports zero passing tests in this file — an all-skipped run means the rebuilt extension was never installed, so nothing was proven about the shipped bundle.</fails_when>
    <automated>unzip -p /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip bbj-intellij/lib/bbj-intellij-0.1.0.jar > /tmp/bbj-plugin.jar</automated>
    <fails_when>a non-zero exit code, or `unzip` prints `caution: filename not matched` — the plugin jar is not at the expected path inside the zip (most likely the version is no longer `0.1.0`; confirm with `ls` and re-run with the real name), so every assertion below would be vacuous.</fails_when>
    <automated>unzip -l /tmp/bbj-plugin.jar | grep -c "com/basis/bbj/intellij/actions/BbjComposeSetoptsInCodeAction.class"</automated>
    <fails_when>the printed count is `0` — the new context-menu action is not in the distributable, so the entry point that bypasses intention search would not exist in the build handed to the tester.</fails_when>
    <automated>unzip -l /tmp/bbj-plugin.jar | grep -c "com/basis/bbj/intellij/composer/BbjHexLiteral.class"</automated>
    <fails_when>the printed count is `0` — the Java hex-literal formatter is not in the distributable, so IntelliJ's in-place writer would still emit the config-file form inside a BBj program.</fails_when>
    <automated>unzip -p /tmp/bbj-plugin.jar META-INF/plugin.xml | grep -c ConfigureSetoptsInCodeIntention</automated>
    <fails_when>the printed count is `0` — the lightbulb intention lost its registration, which this round must not have touched.</fails_when>
    <automated>sha256sum /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/bbj-intellij-0.1.0.zip</automated>
    <fails_when>a non-zero exit code or `No such file or directory` — `buildPlugin` produced no distributable, so there is no identified artifact to hand a human.</fails_when>
  </verify>
  <acceptance_criteria>
    - The VS Code extension was rebuilt and reinstalled; its version and `installedTimestamp` are recorded.
    - The e2e file ran with a non-zero pass count and includes the new composed-literal assertion against the installed bundle.
    - `./gradlew buildPlugin` produced a distributable whose filename, mtime, sha256 and source commit are recorded.
    - The packaged jar carries the new action class, the Java formatter class, and the unchanged intention registration; the packaged descriptor carries the new action's id and class.
    - `git -C /home/coder/repos/bbj-language-server status --short` shows no modification under `bbj-vscode/src/` or `bbj-intellij/src/`.
  </acceptance_criteria>
  <done>
    Two dated, identified artifacts exist, each proven from the inside to carry this round's fixes —
    so the human's job is "install exactly this and tell us what you see", not "find out whether the
    build has the fix".
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the checklist's sample syntax, add the new entry point's row, and script round two of the retest</name>
  <files>QA/FULL-TEST-CHECKLIST.md, .planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md</files>
  <read_first>
    - `/home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md` — the Instructions block, VS Code rows 14-16, and IntelliJ rows 17-21
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md` — round one, whose structure this replaces
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md` — tests 6, 7 and 8 and their `reported:` text, verbatim
    - `/home/coder/repos/bbj-language-server/examples/issue475-setopts-in-code.bbj` — the fixture the retest uses
    - the three SUMMARYs from `88-10`, `88-11` and `88-12` — what each fix changed, in the words the retest should use
  </read_first>
  <action>
    First, the correction that must not wait. `QA/FULL-TEST-CHECKLIST.md` rows 15 and 19 instruct the
    tester to hover sample lines written in the quoted form. The decoder now correctly refuses that
    form, because BBj never hex-decodes it, so a tester following those rows verbatim would observe
    no hover and file a false regression against a working fix. Rewrite the sample lines in both
    rows to the bare delimited form the fixture file uses throughout, keeping each row's steps,
    expectations and numbering otherwise identical. Check rows 16 and 20 too: they say "the
    canonical chain above" and inherit whatever 15 and 19 show, so they need no edit if the samples
    are corrected in place — confirm that rather than assuming it.

    Add one new IntelliJ row after row 21, for the composer's context-menu entry point: right-click
    on a SETOPTS-in-code line in a `.bbj` file, confirm the compose entry appears, invoke it, and
    confirm the same tri-state composer opens that the lightbulb opens; then right-click in the
    resolved config file and confirm this entry is ABSENT there (that file has its own SETOPTS
    composer entry, which row 18 already covers). Write the expectation so it captures what the row
    exists to prove: this path reaches the composer without going through the lightbulb, so it keeps
    working even when intention search is slow. Use the file's existing `<br>`-separated cell style
    and keep the table's column count exact. No plan number, decision id or gap id anywhere in this
    file — it is user-facing QA documentation; `#475` is an acceptable reference.

    Then rewrite `88-LIVE-RETEST.md` as round two. Round one's structure was right and should be
    kept; its content is now stale. Sections:

    - **Build identity to install.** VS Code: the version and `installedTimestamp` from Task 1, plus
      the commands to reproduce it. IntelliJ: the zip filename, its sha256 and the git HEAD sha from
      Task 1, plus the install path (Settings > Plugins > gear icon > Install Plugin from Disk,
      restart when prompted). State plainly that a retest against any other build proves nothing —
      that ambiguity is what made round one's IntelliJ evidence inconclusive.
    - **What changed since round one, in one table.** Three rows, each naming the round-one symptom
      and what was done: the generated mask arguments lost their surrounding quotes (the live
      `!ERROR=17`); the in-place `SETOPTS` write now puts back a complete literal (the reported
      `SETOPTS 20C2…` line missing its delimiters); and the server now answers code-action requests
      within a bounded time on the same document state hover uses, plus IntelliJ gained a
      context-menu entry that does not go through intention search (the "Searching for Context
      Actions..." freeze). This is what tells the tester what to look at.
    - **Already proven automatically — do not re-derive.** A short table of what this round proved
      and at which layer: the composed block's literals over a real connection to the installed
      bundle, the decoder's refusal of the invalid form, the cold-ordering code-action measurement
      with its number, and the in-distributable class/registration assertions. Keep the human's
      attention on the pixel layer and the live runtime, which is all that is left.
    - **Check 1 — the composer's output is valid BBj (either IDE).** Compose a new block with one
      option Set and one Clear and read the inserted lines: each `IOR`/`AND` argument must be a bare
      delimited hex literal with no quotes around it. Then edit the absolute literal
      (`SETOPTS $00C20240000000000000000000000000$`) in place and confirm the rewritten line still
      carries its delimiters. Give both round-one failure signatures verbatim so they are
      recognisable if they recur.
    - **Check 2 — IntelliJ reachability, both doors.** Alt+Enter on a SETOPTS-in-code line: the
      composer entry must appear and open, and the tester should note roughly how long the popup
      takes to appear. Then the right-click context-menu entry on the same line: it must open the
      same composer. Say explicitly that the second door is the one that does not depend on any
      other plugin's intention being fast, so a slow-but-working Alt+Enter and a working context
      menu is a PASS with a note, while a frozen Alt+Enter with a working context menu is a partial
      result worth its own report.
    - **Check 3 — live mask-width falsification (88-RESEARCH.md Assumption A2).** Using the composed
      block from Check 1, run the program as GUI/BUI/DWC against a live BBjServices. Expected: the
      generated `IOR`/`AND` calls, built on the 16-byte/32-hex-digit full-width mask base, run
      without raising a BBj `!ERROR`. State plainly that this question has never actually been
      answered — the quoting defect aborted the first attempt before `AND()` ever saw two decoded
      operands — and that a width defect, if one exists, would surface here as a length or range
      error rather than as a syntax complaint.
    - **Why these cannot be automated here.** Record both probed facts verbatim: no IntelliJ sandbox,
      installed-plugin directory or IDE configuration directory exists anywhere under `/home/coder`;
      and headless BBj terminates with "Must have a display for GUI mode (SysWindow/ThinClient)" in
      GUI mode and "Could not find a termcap file: /etc/termcap" on the non-GUI terminal aliases,
      with no X server and no `xvfb-run` installed.
    - **Verdict block.** An empty, pre-shaped block: one PASS/FAIL line per check per IDE, a field
      for the Alt+Enter timing, and free text for any new symptom. The answers land in `88-UAT.md`'s
      `result:`/`reported:` fields during the next UAT round — never earlier.
  </action>
  <verify>
    <automated>grep -c "issue475-setopts-in-code.bbj" /home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md</automated>
    <fails_when>the printed count is `0`, or grep exits 1 — the retest document does not name the fixture file, so the human would have to reconstruct the reproduction from the UAT record, which is the improvised retest this task exists to replace.</fails_when>
    <automated>grep -c "Assumption A2" /home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md</automated>
    <fails_when>the printed count is `0` — the one question this whole round most needs answered is not stated in the document the human will actually follow.</fails_when>
    <automated>grep -c "termcap" /home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md</automated>
    <fails_when>the printed count is `0` — the probed reason a live BBj run cannot happen here is missing, so a future round will re-litigate it instead of trusting it.</fails_when>
    <automated>grep -v '^#' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md | grep -c 'SETOPTS "\$' || true</automated>
    <fails_when>the printed count is anything other than `0` — a hover row still instructs the tester to use a quoted `SETOPTS` argument, which the decoder now correctly refuses, so the row would produce a false failure report.</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md</automated>
    <fails_when>the diff is empty (no correction was made), or any added line carries a plan number, decision id, gap id or review-finding id — this file is user-facing QA documentation and carries none of those.</fails_when>
  </verify>
  <acceptance_criteria>
    - Rows 15 and 19 show sample `SETOPTS`/`IOR`/`AND` lines in the bare delimited form; rows 16 and 20 were checked and left correct.
    - A new IntelliJ row covers the context-menu entry point, including its absence in the config file, in the file's existing cell style.
    - `88-LIVE-RETEST.md` has all eight named sections (build identity, what changed, already-proven, Checks 1-3, why-not-automated, verdict block).
    - The build identity section carries the IntelliJ zip filename, sha256 and git HEAD sha from Task 1, plus the VS Code version and `installedTimestamp`.
    - The why-not-automated section records both probed facts verbatim.
    - The QA diff carries no planning identifier.
  </acceptance_criteria>
  <done>
    The tester has one self-contained document naming the exact build, the exact lines to read, both
    IntelliJ doors and the live run that settles the open research assumption — and the standing QA
    checklist no longer contains syntax that would make a correct fix look broken.
  </done>
</task>

<task type="auto">
  <name>Task 3: Narrow both gap records to the residue and stage the live checks for the end-of-phase harvest</name>
  <files>.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md</files>
  <read_first>
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md` — in full; the `## Gaps` key order, G-88-1's already-resolved record (the shape to imitate, not to touch), and both open gaps' `missing:` lists
    - `/home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md` — the round-two procedure written in Task 2, which this task's human-check points at
    - the three SUMMARYs from `88-10`, `88-11` and `88-12` — the automated evidence to cite, and at which layer each piece sits
  </read_first>
  <action>
    Both open gaps' `missing:` lists were written before any of this round's work existed and now
    overstate what is left. Rewrite each so it names ONLY the residue, and so the next reader cannot
    mistake work already done for work still owed.

    Edit `88-UAT.md` in place, preserving its exact existing key order and YAML shape:

      - **G-88-2.** Replace `missing:` with: the live Alt+Enter invocation in IntelliJ against the
        Task 1 build (the popup appears and the composer opens, with the observed timing noted), and
        the live context-menu invocation of the new entry point on the same line. Both cite the zip
        filename and sha256 from Task 1. Drop the two items this round closed — the codeAction
        decoupling and the cold-ordering re-measurement are done — but say so in
        `automated_evidence:` rather than deleting them silently. Keep `truth`, `status`, `reason`,
        `severity`, `test`, `root_cause`, `artifacts` and `debug_session` untouched; `status` stays
        `failed`.
      - **G-88-3.** Replace `missing:` with: a live read of the composer's generated block in either
        IDE (each `IOR`/`AND` argument a bare delimited literal, the in-place `SETOPTS` rewrite
        keeping its delimiters), and the live BBjServices run that finally decides 88-RESEARCH.md
        Assumption A2. Say explicitly in that second item that this question is still UNANSWERED
        rather than "expected to pass" — the quoting defect aborted the original attempt before
        `AND()` ever saw two decoded operands, so a width defect would be a NEW finding, not a
        regression. Add an `automated_evidence:` key (placed after `artifacts:`, before `missing:`,
        matching the position G-88-1 and G-88-2 already use) summarising in two or three lines what
        plans 88-10, 88-11 and 88-13's Task 1 proved and at which layer: generated text, accepted
        text, and the shipped bundle's own output over a real connection. Keep every other key
        untouched; `status` stays `failed`.
      - Update G-88-2's existing `automated_evidence:` in the same spirit: name the bounded
        code-action answer, the document-state change, the cold-ordering measurement with its
        number, and the in-distributable assertions — and state plainly that all of it is
        artifact-and-socket evidence, never a rendered popup.
      - Leave `## Tests`, `## Summary`, every `result:` and every `reported:` field completely
        untouched. Those are the human's fields, filled by the UAT round, not by this task. Writing
        a verdict into them here would be inventing evidence.
      - Do not touch G-88-1's record at all. It is already `status: resolved`.

    Do not set either open gap to `resolved`. Nothing in this plan set observed a rendered popup, an
    unfrozen Alt+Enter modal, or a live BBj run, and a green gate is evidence about artifacts, not
    about what a user sees.
  </action>
  <verify>
    <human-check>
      Run the three checks in `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-LIVE-RETEST.md`, against the build identity that document names — nothing else proves anything. Install the VS Code extension and the IntelliJ plugin zip it names (Settings > Plugins > gear icon > Install Plugin from Disk, restart when prompted) before starting.

      Check 1 (the composer's output is valid BBj — either IDE, VS Code is enough if IntelliJ is blocked). Open `examples/issue475-setopts-in-code.bbj`. (a) Invoke the composer on a line with no SETOPTS shape nearby, set one option to Set and one to Clear, and insert. Read the inserted lines: each `IOR`/`AND` call's second argument must be a bare hex literal delimited by a dollar sign on each side, with NO double quotes around it. The round-one failure signature was `opts$=AND(opts$,"$DFFF…$")`, which raised `!ERROR=17 (Strings must be the same length.)` at run time. (b) Invoke the composer on the absolute line `SETOPTS $00C20240000000000000000000000000$`, change one option, apply, and confirm the rewritten line still has its `$` delimiters. The round-one failure signature was `SETOPTS 20C20240000000000000000000000000` — valid only in a config file, not in a program.

      Check 2 (IntelliJ reachability, both doors). Place the caret on a SETOPTS-in-code line and press Alt+Enter: "Configure SETOPTS options in code…" must appear and open the tri-state composer. Note roughly how long the popup took to appear — the round-one failure signature was a "Searching for Context Actions..." popup that never returned. Then right-click on the same line: the new compose entry must appear in the context menu and open the same composer. That second door deliberately does not go through IntelliJ's intention search, so report the two results separately — a working context menu with a still-frozen Alt+Enter is a partial result that needs its own report, not a pass.

      Check 3 (live mask-width falsification — 88-RESEARCH.md Assumption A2). Using the block composed in Check 1, run the program as GUI/BUI/DWC against a live BBjServices. Expected: the generated `IOR`/`AND` calls, built on the 16-byte/32-hex-digit full-width mask base, run without raising a BBj `!ERROR`. This question has never actually been answered — the quoting defect aborted the first attempt before `AND()` ever saw two decoded operands — so a length or range error here is a NEW finding about the mask width, not a return of the quoting defect. Report the exact `!ERROR` text and the offending line if one appears. This cannot be automated in this devcontainer: headless BBj terminates with "Must have a display for GUI mode (SysWindow/ThinClient)" in GUI mode and "Could not find a termcap file: /etc/termcap" on the non-GUI terminal aliases, with no X server and no `xvfb-run` present.

      Report a PASS or FAIL per check, per IDE where both apply. Only these answers may move G-88-2 or G-88-3 off `status: failed`.
    </human-check>
    <automated>grep -c "automated_evidence:" /home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md</automated>
    <fails_when>the printed count is less than `3` — one of the three gap records still fails to separate shipped-artifact evidence from live-editor evidence, which is the ambiguity that made the previous verification round inconclusive.</fails_when>
    <automated>grep -c "status: failed" /home/coder/repos/bbj-language-server/.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md</automated>
    <fails_when>the printed count is anything other than `2` — either an open gap was marked resolved by this autonomous plan before any human observed a live IDE or a live BBj run, or a gap record was lost.</fails_when>
    <automated>git -C /home/coder/repos/bbj-language-server diff --stat -- .planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-UAT.md</automated>
    <fails_when>the diffstat reports no change to `88-UAT.md`, or the accompanying `git diff` shows an edit to any `result:` line, any `reported:` line, the `## Summary` block, or G-88-1's record — those are the human's fields and the already-resolved gap, and this task must leave all of them untouched.</fails_when>
  </verify>
  <acceptance_criteria>
    - G-88-2's `missing:` names only the two live IntelliJ invocations, citing the Task 1 build identity.
    - G-88-3's `missing:` names only the live read of the generated block and the live Assumption A2 run, with the second stated as an open question rather than an expected pass.
    - G-88-3 gains an `automated_evidence:` key in the same position the other records use; G-88-2's is updated to cover this round.
    - Both open gaps still read `status: failed`; G-88-1's record is byte-identical to HEAD.
    - `## Tests`, `## Summary` and every `result:`/`reported:` field are byte-identical to HEAD.
    - The `<human-check>` block names both round-one failure signatures, both IntelliJ entry points, the live run, and the two probed reasons it cannot be automated here.
  </acceptance_criteria>
  <done>
    Both gap records state exactly what is still owed, against which build, and what has already been
    proven at which layer — and neither is resolved. The three live checks are staged in the form the
    end-of-phase verifier harvests, so the human is asked one sharp question instead of being handed
    an open-ended retest.
  </done>
</task>

</tasks>

## Artifacts this round produces

| Artifact | Kind | Purpose |
|---|---|---|
| `bbj-intellij/build/distributions/bbj-intellij-<version>.zip` | build output (gitignored) | The dated, hash-identified plugin proven to contain the new entry point, the Java formatter and the unchanged intention |
| `~/.ext-test` VS Code install | build output | The bundle the e2e suite and the human both exercise |
| `QA/FULL-TEST-CHECKLIST.md` | modified | Corrected sample syntax in the hover rows; a new row for the IntelliJ context-menu entry |
| `.planning/…/88-LIVE-RETEST.md` | rewritten | Round two of the scripted human retest |
| `.planning/…/88-UAT.md` | updated gap records | G-88-2 and G-88-3 narrowed to the residue, both still `failed` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built distributables → the human's IDEs on a separate host | The artifacts leave this environment and are installed by hand; nothing downstream re-verifies that what was installed is what was built |
| human verdict → `88-UAT.md` → `reconcile_gaps` | A recorded verdict is the sole authority for closing either gap; a verdict without a build identity cannot be audited |
| composed SETOPTS block → live BBjServices runtime | Check 3 runs generated code against a real BASIS runtime |
| QA checklist → future testers | A checklist instructing invalid syntax manufactures false failure reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-32 | Spoofing | the build the human actually installs | high | mitigate | Task 1 records the zip's sha256, mtime and source commit plus the installed extension's version and timestamp; Task 2 puts all of it in the retest document with an explicit statement that a retest against any other build proves nothing — the realised threat that left round one's IntelliJ evidence merely corroborated |
| T-88-33 | Repudiation | the recorded verdict and the evidence behind it | high | mitigate | Task 3 gives every gap record an `automated_evidence:` key so artifact evidence can never be read as live-editor evidence, leaves the human's own fields untouched, and its verify fails if an open gap turns `resolved` |
| T-88-34 | Tampering | a QA checklist that instructs syntax the decoder refuses | medium | mitigate | Task 2 corrects rows 15 and 19 to the bare form in the same round that tightens the decoder, with a verify gate that fails while a quoted `SETOPTS` sample remains |
| T-88-35 | Tampering | Check 3's generated block running against live BBjServices | low | accept | The block is generated by the phase's own composer from a tri-state selection over the known catalog, inserted into a file the human controls, and run in their own session. No new capability is granted to any untrusted input, and deciding whether the full-width mask base is safe at runtime is the entire point of the check |
| T-88-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan introduces no npm, pip or cargo install. `bbj-ext-install` installs this repo's own freshly built extension and `./gradlew buildPlugin` resolves only already-pinned Gradle dependencies, so no package-legitimacy checkpoint applies. |

No new source code, no new endpoint, no new listener, no new credential path. Both `high` threats
are evidence-integrity threats — that the artifact tested is the artifact built, and that the
recorded verdict names it — and both are mitigated in-plan. ASVS level 1, block-on `high`: no
unmitigated `high` remains.
</threat_model>

<verification>
Run from the repo root with absolute paths; `cd` is used only in front of Gradle.

1. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run build` then `bbj-ext-install`
2. `npm --prefix /home/coder/repos/bbj-language-server/bbj-vscode run test -- test/functional/installed-extension-e2e.test.ts`
   — confirm a non-zero pass count; an all-skipped run proves nothing.
3. `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin`
4. `ls -la /home/coder/repos/bbj-language-server/bbj-intellij/build/distributions/`,
   `sha256sum` of the zip, `git -C /home/coder/repos/bbj-language-server rev-parse HEAD`
5. Task 1's in-distributable gates, in order — the first writes `/tmp/bbj-plugin.jar` that the
   next three read.
6. Task 2's document gates, plus `git -C /home/coder/repos/bbj-language-server diff -- QA/FULL-TEST-CHECKLIST.md`
   for the register check (no plan number, decision id, gap id or review-finding id; `#475` is fine).
7. Task 3's gap-record gates.
8. Confirm nothing in the shipped code moved:
   `git -C /home/coder/repos/bbj-language-server status --short` — only the four files in
   `files_modified` may appear. No file under `bbj-vscode/src/` or `bbj-intellij/src/` may be listed.
9. The live retest. Nothing above substitutes for it. Every automated gate in this plan and in
   88-10, 88-11 and 88-12 proves properties of ARTIFACTS; only a human in a running editor, with a
   live BBjServices, observes the BEHAVIOUR the two gaps are actually about.
   `workflow.human_verify_mode` is unset and therefore defaults to `end-of-phase`, so Task 3's
   `<verify><human-check>` is harvested by the verifier into the `human_needed` → `88-UAT.md` flow
   rather than halting execution mid-plan. Both gaps stay `status: failed` until that round answers
   it.
</verification>

<success_criteria>
- Both distributables are rebuilt in dependency order and proven from the inside to carry this
  round's fixes, with a recorded build identity for each (D-03, D-06).
- The installed bundle's own composed output is asserted over a real connection to be valid BBj
  (D-05) — the shipped-artifact counterpart of the unit oracle.
- `QA/FULL-TEST-CHECKLIST.md` no longer instructs a tester to hover syntax the decoder correctly
  refuses (D-02), and covers the new IntelliJ entry point.
- `88-LIVE-RETEST.md` lets a human run all three checks without opening another file, and records
  the probed reasons the IntelliJ retest and the live run cannot be automated here.
- `88-UAT.md`'s two open gap records name only the residue, separate artifact evidence from live
  evidence, and both still read `status: failed`. No plan in this round populates `gap_ids`, so
  neither gap can be resolved by `reconcile_gaps` off an autonomous SUMMARY — only the UAT round's
  human answer can move them.
- 88-RESEARCH.md Assumption A2 is stated as an open question and staged for a live answer (D-04's
  safe-shape guarantee depends on the mask width being right, so a width defect here would be a new
  finding with its own diagnosis).
- Nothing under `bbj-vscode/src/` or `bbj-intellij/src/` changes.
</success_criteria>

<output>
Create `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-13-SUMMARY.md` when done.

It MUST state, prominently, that G-88-2 and G-88-3 are still `status: failed` and that this SUMMARY
is not evidence of their closure — the three live checks staged in Task 3's `<human-check>` are
harvested by the end-of-phase verifier and answered in the next UAT round.

Record at minimum:
- the IntelliJ zip filename, sha256, mtime and source commit, plus the four in-distributable
  assertion results;
- the VS Code extension's version and `installedTimestamp`, and the e2e run's pass count (to prove
  it was not skipped);
- the QA rows corrected and the row added, and why the correction was urgent — the tightened
  decoder makes the old sample syntax produce no hover, which would read as a regression;
- the rewritten `missing:` lists for both gaps and the `automated_evidence:` text added or updated,
  so the next reader sees exactly what moved and what did not;
- an explicit statement that Assumption A2 remains undecided, with the two probed error strings that
  block a headless BBj run in this devcontainer.
</output>
