---
phase: 88-setopts-in-code-hovers-tri-state-composer
plan: 05
type: execute
wave: 5
depends_on: ["88-04"]
files_modified:
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java
  - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureSetoptsInCodeIntention.java
  - bbj-intellij/src/main/resources/META-INF/plugin.xml
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/description.html
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/before.bbj.template
  - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/after.bbj.template
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLauncherChainSourceGuardTest.java
  - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/SetoptsInCodeSourceGuardTest.java
  - QA/FULL-TEST-CHECKLIST.md
autonomous: true
requirements: [DISC-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03: a lightbulb intention `ConfigureSetoptsInCodeIntention` is registered for the BBj language in `plugin.xml`, follows `ConfigureMsgboxIntention`'s exact shape (`isAvailable` / `invoke` / `startInWriteAction() == false` / `generatePreview` returning `IntentionPreviewInfo.Html`), and ships its own `intentionDescriptions/ConfigureSetoptsInCodeIntention/` resource directory."
    - "D-06: the tri-state dialog reuses Phase 87's byte-grouped catalog layout — the same `catalogs.byteGroups` / `catalogs.bits` iteration, the same BBj-annotation greying with tooltip, the same `PreviewDebouncer` at the same `PREVIEW_DEBOUNCE_MS` — with the per-row widget changed from a two-state checkbox to a three-state Set/Clear/Leave control."
    - "D-04: `ComposerLauncher` opens the existing two-state `SetoptsComposerDialog` for an absolute `SETOPTS <literal>` decode, the new tri-state dialog for a safe chain decode and for compose-new, and raises no edit affordance at all when the server reports `editable: false` — it renders the server's `reason` as a notice instead."
    - "D-05: the compose-new path inserts the server-composed canonical block at the caret's LINE START via the existing `insertAt(..., atLineStart = true)` helper, never mid-line."
    - "Both edit paths write through `StaleEditGuard.applyIfUnchanged` with `DecodeEquality::sameSetoptsInCode`, re-decoding through the same `setoptsDecodeInCode` request the launch used."
    - "EDGE/DISC-06/empty (explicit): with every option left Leave the dialog's OK/Insert still produces a well-formed block (origin line plus `SETOPTS` line) for compose-new, and an empty replacement region for an existing chain — never an empty insert and never a malformed line."
    - "EDGE/DISC-06/ordering (explicit): the dialog sends its selection as one entry per catalog bit in `SETOPTS_BITS` order, so the server's deterministic ordering is what reaches the document."
    - "`QA/FULL-TEST-CHECKLIST.md` gains one hand-check row per IDE for the hover decode and one per IDE for the in-code composer, plus the live-BBjServices check that falsifies or confirms the 16-byte mask-padding assumption (88-RESEARCH.md A2)."
  artifacts:
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java
    - bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureSetoptsInCodeIntention.java
    - bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/description.html
    - bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/SetoptsInCodeSourceGuardTest.java
  key_links:
    - "`ConfigureSetoptsInCodeIntention.invoke` → `ComposerLauncher.launch(project, editor, Kind.SETOPTS_IN_CODE)` → `server.setoptsDecodeInCode(new SetoptsInCodeDecodeParams(uri, line, character))`."
    - "`ComposerLauncher.openSetoptsInCode` → `SetoptsComposerDialog` (absolute) | `SetoptsTriStateComposerDialog` (chain, compose-new) → `StaleEditGuard.applyIfUnchanged` + `DecodeEquality::sameSetoptsInCode`."
    - "`SetoptsTriStateComposerDialog` → `server.setoptsComposeTriState` for its live debounced preview, through the existing `PreviewDebouncer` + `ComposerFlow.observe` sequence-check seam."
  prohibitions:
    - "The launcher must never construct an edit from a decode result the server marked not editable — the absent payload must not be worked around with a locally re-derived range."
    - "The tri-state dialog must never introduce a second `Alarm`/`Timer`; the debounce seam is `PreviewDebouncer`, already validated for this exact use case in Phase 87."
    - "Edit-in-place must never rewrite the user's `OPTS` origin line or `SETOPTS` line; only the reassignment region between them is replaced."
---

<objective>
Ship the IntelliJ user-facing half of DISC-06: a three-state Set/Clear/Leave composer dialog, a
launcher branch that routes each decoded shape to the right dialog and the right write, a
lightbulb intention that opens it, and the hand-check rows that cover what no automated test in
this repo can reach.

Purpose: every mechanism this needs already exists and was validated in Phase 87 — the dialog
layout, the debounce, the stale-edit guard, the notice vocabulary, the intention shape and its
description-resource requirement. The only genuinely new thing is the per-row three-state widget
and the branch that picks a dialog from the server's `mode`.

Output: `SetoptsTriStateComposerDialog`, `Kind.SETOPTS_IN_CODE` and its launch branch,
`ConfigureSetoptsInCodeIntention` plus resources and registration, updated source guards, and the
QA rows.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-CONTEXT.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-RESEARCH.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-PATTERNS.md
@.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-04-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section of `88-01-PLAN.md`. The symbols this plan lands
are: `SetoptsTriStateComposerDialog`, `ComposerLauncher.Kind.SETOPTS_IN_CODE`,
`ConfigureSetoptsInCodeIntention`, `SetoptsInCodeSourceGuardTest`.

## Flagged assumptions carried into this plan

- **88-RESEARCH.md A4** — this session found no built-in tri-state checkbox in IntelliJ's Swing
  toolkit, so the plan locks a `ButtonGroup`-backed three-radio row per option. If the executor
  finds a suitable `com.intellij.ui.components` component while reading the platform sources, it
  may substitute it, provided the row still exposes exactly three mutually exclusive states and
  the byte-group iteration is unchanged. Record the choice in the SUMMARY either way.
- **88-RESEARCH.md A2** — the 16-byte mask padding is unverified against BASIS runtime behavior.
  Task 3 files it as a live-BBjServices hand check rather than leaving it unstated.

<tasks>

<task type="auto">
  <name>Task 1: `SetoptsTriStateComposerDialog` — three-state rows over Phase 87's byte-grouped layout</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java</files>
  <read_first>
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsComposerDialog.java` (whole file — the constructor's OK-disabled-until-first-preview rule at lines 98-109, the byte-group loop at 129-152, `prefill` at 179-188, `scheduleRefresh` at 190-201 with its CR-01 rationale, `refresh` at 203-257 with the `ComposerFlow.observe` sequence check, `previewUnavailable` at 270-279, `apply` at 281-291, and the `CheckboxRow`/`SimpleDocumentListener` records at 334-343)
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/concurrency/PreviewDebouncer.java` (whole file)
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` — the SETOPTS-in-code DTOs added by plan 88-04
    - `bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java` (whole file — the `DIALOG_SOURCES` list at line 53 and every assertion applied to each entry)
  </read_first>
  <behavior>
    - The dialog renders one titled group per `catalogs.byteGroups` entry and, inside it, one row per `catalogs.bits` entry whose `byteNo` matches — the same iteration `SetoptsComposerDialog` uses.
    - Each row exposes exactly three mutually exclusive states labelled Set, Clear and Leave, defaulting to Leave when no prefill supplies a state.
    - A bit whose catalog entry carries a non-null `bbj` annotation renders de-emphasized with the same tooltip fallback chain `SetoptsComposerDialog` uses (`bbjDetail`, then `detail`, then a generated sentence).
    - Any row change disables OK synchronously and then triggers the debounced preview — the CR-01 rule, so OK can never be clicked while the shown block reflects a superseded selection.
    - A resolved preview enables OK, fills the read-only block preview and clears any unavailable message; a failed or superseded preview leaves OK disabled and shows an unavailable message naming the reason.
    - The preview request carries one entry per catalog bit in `catalogs.bits` order, with `state` set to `"set"`, `"clear"` or `"leave"`.
    - `getBlockText()` returns the server-composed text and `getSelection()` returns the entry list, both empty-safe before the first preview resolves.
  </behavior>
  <action>
Create `SetoptsTriStateComposerDialog` as a new `public final class ... extends DialogWrapper` in the composer package. Do NOT add a mode flag to `SetoptsComposerDialog` — that dialog's whole model is an absolute vector with mask-character and raw-tail fields, none of which apply to a read-modify-write block, and branching it would double every path in a 344-line file.

Copy `SetoptsComposerDialog`'s structural skeleton exactly: the same constructor dependencies (`Project`, `BbjComposerServer`, `SetoptsCatalogs`, plus a nullable `SetoptsTriStateSelection initial`, the `scope` string and the target variable name), the same `ComposerFlow` + `balloonOnce` + `PreviewDebouncer` construction with the same `PREVIEW_DEBOUNCE_MS` constant value, the same `setOKActionEnabled(false)` before the first refresh, the same `scheduleRefresh()` helper that disables OK before triggering the debouncer, the same `flow.observe(...)` call with the `mySeq == seq.get()` staleness check on BOTH the success and failure callbacks, the same `previewUnavailable(String)` shape, and the same `apply(...)` shape. Introduce no new `Alarm` or `Timer`.

Replace the `CheckboxRow` record with a `TriStateRow` record pairing a `SetoptsBit` with its three-state control. Implement the control as three `JBRadioButton`s in one `ButtonGroup` laid out horizontally next to the option label — Swing ships no tri-state checkbox. Keep the `catalogs.byteGroups` / `catalogs.bits` iteration byte-for-byte identical to the existing dialog's, including the `bit.byteNo != group.byteNo` continue and the bbj-annotation greying/tooltip block.

Replace the mask-character and raw-tail form region entirely — a read-modify-write block has no absolute data bytes to edit. In its place put a read-only multi-line preview of the composed block, and reuse the existing `unknownBitsText`-style preserved-bits line to state that options left Leave and every unmodelled bit are untouched by the generated lines (the "unknown/reserved bits" callout D-08 established in Phase 87, confirmed as wanted here).

`refresh()` builds a `SetoptsTriStateSelection` with one `SetoptsTriStateEntry` per `catalogs.bits` entry in catalog order, calls `server.setoptsComposeTriState(new SetoptsComposeTriStateParams(selection, variable, indent, scope))` through `flow.observe`, and on success stores `text` and the selection and enables OK.

Set the dialog title and OK button text from the mode: "Configure SETOPTS block" / "Apply" when editing an existing chain, "Compose SETOPTS block" / "Insert" for compose-new.

Add `SetoptsTriStateComposerDialog.java` to `ComposerDialogRefreshSourceGuardTest`'s `DIALOG_SOURCES` list as a fifth `Path` constant. Run that test and fix any assertion the new dialog fails by changing the DIALOG, not the assertion — every rule that guard enforces (observe both sides, sequence-check both, OK gating, one balloon per session) is a rule this dialog must also follow. If a guard assertion is genuinely inapplicable (for example one naming a field that only the absolute dialog has), scope that single assertion to the dialogs it applies to rather than deleting it, and record the scoping in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerDialogRefreshSourceGuardTest' --tests 'com.basis.bbj.intellij.concurrency.PreviewDebouncerTest'</automated>
    <fails_when>non-zero exit, or the Gradle output reports any failed test, or a named test class reports zero executed tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "SetoptsTriStateComposerDialog.java" /home/coder/repos/bbj-language-server/bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerDialogRefreshSourceGuardTest.java` is at least 1.
    - `grep -cE "new (Alarm|javax\.swing\.Timer)" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java` is 0. <!-- planner-discipline-allow: Alarm --> <!-- planner-discipline-allow: Timer -->
    - `grep -c "PreviewDebouncer" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java` is at least 2 (import and field).
    - `grep -c "ButtonGroup" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/SetoptsTriStateComposerDialog.java` is at least 1, or the SUMMARY records the platform component substituted for it.
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.ComposerDialogRefreshSourceGuardTest' --tests 'com.basis.bbj.intellij.concurrency.PreviewDebouncerTest'` exits 0 with zero failed tests.
  </acceptance_criteria>
  <done>A three-state composer dialog renders the shared catalog with Phase 87's layout, debounce and OK-gating rules, and passes the same dialog source guard the other four do.</done>
</task>

<task type="auto">
  <name>Task 2: `Kind.SETOPTS_IN_CODE` launch branch with mode-based dialog routing and guarded writes</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerLauncherChainSourceGuardTest.java, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/SetoptsInCodeSourceGuardTest.java</files>
  <read_first>
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` (whole file — the `Kind` enum at line 40, `isCaretOnCall` at 49-61, `launch`'s caret capture and switch at 63-92, `labelOf` at 95-102, `documentViewOf` at 142-160, `openSetopts` at 273-324 and `insertAt` at 333-344)
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/StaleEditGuard.java` (whole file — `applyIfUnchanged`'s signature and re-decode contract)
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerNotices.java` — the reason-keyed notice vocabulary to render an `editable: false` result with
    - `bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerApplyGuardSourceGuardTest.java` and `ComposerLauncherChainSourceGuardTest.java` (whole files — both count occurrences inside `ComposerLauncher.java`, so both need their expected counts updated for the new branch)
    - `bbj-vscode/src/language/setopts-in-code-request.ts` — the exact semantics of `mode`, `editable`, `absolute`, `chain` and `initial`
  </read_first>
  <behavior>
    - `Kind` gains a fifth constant `SETOPTS_IN_CODE`; `labelOf` returns a distinct label for it (the existing `SETOPTS` label continues to mean the config.bbx composer).
    - `launch` for the new kind captures the caret's document URI, zero-based line and zero-based column on the EDT and calls `server.setoptsDecodeInCode`.
    - `mode == "absolute"` and `editable` opens the existing `SetoptsComposerDialog` prefilled from the decoded hex, and on Apply replaces only that line's `hexRange` through `StaleEditGuard.applyIfUnchanged` with `DecodeEquality::sameSetoptsInCode`.
    - `mode == "chain"` and `editable` opens `SetoptsTriStateComposerDialog` prefilled from `initial`, and on Apply replaces the document text from the start of `chain.startLine` to the start of `chain.endLine` with the composed reassignment lines, through the same guard.
    - `found == false` opens `SetoptsTriStateComposerDialog` blank in compose-new mode and, on Insert, inserts the composed block at the caret's LINE START via the existing `insertAt(..., true)` helper.
    - `editable == false` with `found == true` opens no dialog at all and renders a notice carrying the server's `reason`.
    - A `chain` whose `startLine` equals its `endLine` results in an insertion at that line rather than a zero-width replacement that could swallow a character.
    - A null or empty composed text is refused before any write, mirroring the existing empty-value guards in `openSetopts` and `applyHexEdit`.
  </behavior>
  <action>
Add `SETOPTS_IN_CODE` to the `Kind` enum and a matching arm to `labelOf` returning `"SETOPTS in code"`. Add a `case SETOPTS_IN_CODE ->` arm to `launch`'s switch that calls `server.setoptsDecodeInCode(new SetoptsInCodeDecodeParams(uri, line, col))` and dispatches to a new private `openSetoptsInCode(...)`. Take the document URI on the EDT alongside the existing caret capture, using the editor's virtual file URL; when no virtual file is available, render the not-ready notice and return rather than sending a blank URI.

Do not modify the existing `SETOPTS` arm, `openSetopts`, or the `SetoptsDecodeCallParams` single-line flow in any way — the config.bbx composer's behavior is Phase 87's shipped contract.

Implement `openSetoptsInCode` following `openSetopts`'s structure: the `catalogs == null` not-ready guard first, then the three-way branch on the decode result described in the behavior block. Build each `StaleEditGuard` exactly as `openSetopts` does — the same `documentViewOf(editor)` live view, the same `WriteCommandAction.runWriteCommandAction` body wrapper with a command name naming the operation, the same `ComposerLauncher::onEdt`, the same notice renderer with a relaunch callback, and the same `StaleEditGuard.REDECODE_TIMEOUT_MILLIS` — passing `(currentLineText, currentCol) -> server.setoptsDecodeInCode(...)` as the re-decode and `DecodeEquality::sameSetoptsInCode` as the comparator.

For the chain replacement, compute the document offsets inside the write body from the live document (`getLineStartOffset(chain.startLine)` and, for the end, `getLineStartOffset(chain.endLine)`), never from offsets captured before the dialog opened. When the composed reassignment text is non-empty, ensure it ends with a line separator so the following `SETOPTS` line is not joined onto it.

When `found` is true and `editable` is false, render a notice built from `ComposerNotices` carrying the server's `reason` and return without constructing any edit. Do not fall back to a locally derived range.

Update the expected occurrence counts in `ComposerApplyGuardSourceGuardTest` and `ComposerLauncherChainSourceGuardTest` for the new branch — run each first, read the actual failure, and change only the numbers those failures name. Record the old and new value of every changed count in the SUMMARY so a reviewer can see exactly what widened.

Create `bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/SetoptsInCodeSourceGuardTest.java` as a plain-JUnit source guard over `ComposerLauncher.java` (read with `Files.readString`, comment lines stripped with the same `withoutCommentLines` helper style the existing guards use). Assert: the new kind's decode is sent through `setoptsDecodeInCode` and never through `setoptsDecodeCall`; both edit paths of the new branch route through `applyIfUnchanged` with `sameSetoptsInCode`; the compose-new path calls the line-start insert helper; and the not-editable path contains no `replaceString` or `insertString` call between its guard and its return.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*' --tests 'com.basis.bbj.intellij.concurrency.*'</automated>
    <fails_when>non-zero exit, or the Gradle output reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "SETOPTS_IN_CODE" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` is at least 3 (enum constant, `labelOf` arm, switch arm).
    - `grep -c "setoptsDecodeCall" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` is exactly 2, the same as its pre-plan value — the config.bbx flow was not touched. <!-- planner-discipline-allow: setoptsDecodeCall -->
    - `grep -c "sameSetoptsInCode" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerLauncher.java` is at least 2 (both guarded edit paths).
    - `SetoptsInCodeSourceGuardTest` exists and its four assertions pass.
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*' --tests 'com.basis.bbj.intellij.concurrency.*'` exits 0 with zero failed tests.
  </acceptance_criteria>
  <done>The launcher routes each decoded shape to the right dialog and the right guarded write, refuses to construct an edit the server did not authorize, and the source guards pin all four routes.</done>
</task>

<task type="auto">
  <name>Task 3: Lightbulb intention, its description resources, registration and the QA hand-check rows</name>
  <files>bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureSetoptsInCodeIntention.java, bbj-intellij/src/main/resources/META-INF/plugin.xml, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/description.html, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/before.bbj.template, bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/after.bbj.template, bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java, QA/FULL-TEST-CHECKLIST.md</files>
  <read_first>
    - `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ConfigureMsgboxIntention.java` (whole file — the exact template)
    - `bbj-intellij/src/main/resources/intentionDescriptions/ConfigureMsgboxIntention/description.html`, `before.bbj.template`, `after.bbj.template` — the three-file resource shape and the `<!-- tooltip end -->` marker `IntentionDescriptionResourcesTest` requires
    - `bbj-intellij/src/main/resources/META-INF/plugin.xml` lines 115-135 — the three existing `<intentionAction>` registrations
    - `bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/ComposerIntentionPreviewSourceGuardTest.java` lines 18-50 (`INTENTION_SOURCES`) and the per-class assertions
    - `bbj-intellij/src/test/java/com/basis/bbj/intellij/composer/IntentionDescriptionResourcesTest.java` lines 24-50 — the descriptor-driven enumeration that will pick the new registration up automatically
    - `QA/FULL-TEST-CHECKLIST.md` — the `## VS Code - LSP Features` table (rows 1-14) and the `## IntelliJ IDEA - LSP Features` table (rows 1-18), including row 18's SETOPTS wording as the style to match
  </read_first>
  <behavior>
    - `ConfigureSetoptsInCodeIntention.isAvailable` returns true only when the caret's line contains `SETOPTS`, `IOR(` or `AND(` case-insensitively at or before the caret — a cheap synchronous line-text gate with no LSP round trip, exactly like `ComposerLauncher.isCaretOnCall`.
    - `invoke` delegates to `ComposerLauncher.launch(project, editor, Kind.SETOPTS_IN_CODE)` and does nothing when the editor is null.
    - `startInWriteAction()` returns false.
    - `generatePreview` returns an `IntentionPreviewInfo.Html` describing the composer, so the popup never falls back to resolving the description resource.
    - `IntentionDescriptionResourcesTest` passes with the fourth registration present, which requires all three resource files to exist and the description to carry the tooltip-end marker.
  </behavior>
  <action>
Create `ConfigureSetoptsInCodeIntention` in the composer package as a `public final class ... implements IntentionAction`, copying `ConfigureMsgboxIntention`'s member order and modifiers exactly. `getText()` returns `"Configure SETOPTS options in code…"`; `getFamilyName()` returns the same `"BBj visual composer"` string the three existing intentions return, so all four group together in the lightbulb popup.

For `isAvailable`, add a small package-private static helper to `ComposerLauncher` — `isCaretOnSetoptsInCode(Editor)` — implementing the three-keyword gate. Reuse `isCaretOnCall`'s exact caret/line/lowercase mechanics rather than writing new document-reading code; the helper only widens the single keyword to a set of three. The authoritative safe/unsafe decision stays entirely server-side in `decodeInCode`, matching every other composer's split between a cheap client gate and an authoritative server decode.

Register the intention in `plugin.xml` as a fourth `<intentionAction>` block in the existing group, with `<language>BBj</language>`, the fully qualified class name, and `<category>BBj</category>` — matching the three above it exactly.

Create the three resource files under `intentionDescriptions/ConfigureSetoptsInCodeIntention/`. `description.html` follows the existing files' structure including the `<!-- tooltip end -->` marker; `before.bbj.template` shows a canonical `opts$=OPTS` / `opts$=IOR(opts$,"$...$")` / `SETOPTS opts$` block and `after.bbj.template` the same block after one option is added.

Add `ConfigureSetoptsInCodeIntention.java` as a fourth entry in `ComposerIntentionPreviewSourceGuardTest`'s `INTENTION_SOURCES` array and, where that test asserts a per-class occurrence in `plugin.xml`, add the matching assertion for the new class. Run the test and fix the INTENTION, not the assertion, for anything it flags.

Add four rows to `QA/FULL-TEST-CHECKLIST.md`, continuing each table's existing numbering and matching row 18's level of step detail:
  - VS Code row 15 — hover decode: hover an absolute `SETOPTS` literal, a `SETOPTS var$` on a canonical chain, a single `IOR(...)` call and a single `AND(...)` call; expected result names the options each sets, and states the AND case in terms of the options it CLEARS.
  - VS Code row 16 — in-code composer: invoke the code action on a canonical block, change one option to Clear and one to Set, apply, and confirm only the reassignment lines between the `OPTS` origin and the `SETOPTS` line changed; then invoke it on a line with no SETOPTS shape and confirm a whole new block is inserted at the line start; then invoke it on a block interrupted by an `IF`/`FI` and confirm no edit is offered and the reason is shown.
  - IntelliJ row 19 — the same hover checks through LSP4IJ's own hover popup.
  - IntelliJ row 20 — the same composer checks through the Alt+Enter lightbulb, plus: the live preview updates on a settle rather than per keystroke, BBj-ignored options are visibly de-emphasized with a tooltip, and the server never restarts or reconnects during the whole sequence.
Add one further row to the IntelliJ table (row 21) for the flagged mask-width assumption: generate a compose-new block against a live BBjServices, run the program, and confirm no `!ERROR` is raised by the generated `IOR`/`AND` calls — the check that falsifies or confirms 88-RESEARCH.md Assumption A2's 16-byte padding.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*'</automated>
    <fails_when>non-zero exit, or the Gradle output reports any failed test — in particular `IntentionDescriptionResourcesTest` failing names the missing resource file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "ConfigureSetoptsInCodeIntention" /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/META-INF/plugin.xml` is exactly 1.
    - `ls /home/coder/repos/bbj-language-server/bbj-intellij/src/main/resources/intentionDescriptions/ConfigureSetoptsInCodeIntention/` lists exactly `after.bbj.template`, `before.bbj.template` and `description.html`.
    - `test "$(grep -cE '^\| 16 \| ' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md)" = "1"` succeeds (the new VS Code row).
    - `test "$(grep -cE '^\| 21 \| ' /home/coder/repos/bbj-language-server/QA/FULL-TEST-CHECKLIST.md)" = "1"` succeeds (the new IntelliJ mask-width row).
    - `./gradlew test --offline --tests 'com.basis.bbj.intellij.composer.*'` exits 0 with zero failed tests, including `IntentionDescriptionResourcesTest` and `ComposerIntentionPreviewSourceGuardTest`.
  </acceptance_criteria>
  <done>The lightbulb intention is registered with its description resources, passes both intention guards, and the five hand-check rows covering both IDEs and the mask-width assumption are in the QA checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server → IntelliJ launcher | The decode result decides whether an edit is offered and where it is written |
| dialog → user's document (`WriteCommandAction`) | The only place in this phase where IntelliJ writes into a user's source file |
| document state at launch → document state at apply | The user may edit the file while the modal dialog is open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering | `openSetoptsInCode` edit routing | high | mitigate | The not-editable branch opens no dialog and constructs no range; `SetoptsInCodeSourceGuardTest` asserts no `replaceString`/`insertString` call appears on that path |
| T-88-05 | Tampering | write applied against a changed document | high | mitigate | Both edit paths go through `StaleEditGuard.applyIfUnchanged` with `DecodeEquality::sameSetoptsInCode` and a fresh `setoptsDecodeInCode` re-decode; offsets are read from the live document inside the write body, never captured pre-dialog |
| T-88-12 | Tampering | chain replacement range | high | mitigate | The replacement spans only `[startLine, endLine)` — the reassignment region — so the user's `OPTS` origin and `SETOPTS` lines are never rewritten; the empty-range case inserts rather than replacing |
| T-88-13 | Tampering | applying a superseded selection | medium | mitigate | The dialog disables OK synchronously the instant a preview is scheduled (Phase 87's CR-01 rule) and re-enables only on a resolved, non-superseded preview; `ComposerDialogRefreshSourceGuardTest` covers the new dialog |
| T-88-04 | Denial of Service | per-keystroke preview requests | medium | mitigate | The existing `PreviewDebouncer` at the existing delay is the only scheduling seam; an added `Alarm`/`Timer` fails an acceptance grep |
| T-88-SC | Tampering | npm/gradle installs | high | accept | This plan adds no Gradle dependency; 88-RESEARCH.md §"Standard Stack" confirms no new dependency for the phase |
</threat_model>

<verification>
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew test --offline` exits 0.
- `cd /home/coder/repos/bbj-language-server/bbj-intellij && ./gradlew buildPlugin --offline` exits 0 (the plugin archive still builds with the new registration and resources).
- No file under `bbj-vscode/` is modified by this plan.
</verification>

<success_criteria>
- A lightbulb intention opens a three-state SETOPTS composer on a `.bbj` line.
- Absolute literals, safe chains and compose-new each route to the correct dialog and the correct guarded write.
- An unsafe shape offers no edit and shows the server's reason.
- The QA checklist carries hand-check rows for both IDEs and for the mask-width assumption.
</success_criteria>

<output>
Create `.planning/phases/88-setopts-in-code-hovers-tri-state-composer/88-05-SUMMARY.md` when done.
</output>
