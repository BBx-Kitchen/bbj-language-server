---
phase: 99-parser-gaps-the-largest-groups
plan: 06
type: execute
wave: 6
depends_on: [99-05]
gap_closure: true
files_modified:
  - bbj-vscode/src/language/bbj.langium
  - bbj-vscode/src/language/bbj-validator.ts
  - bbj-vscode/test/test-data/conformance/field-verb.bbj
  - bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj
  - bbj-vscode/test/parser-keyword-statements.test.ts
  - bbj-vscode/test/line-break-validation.test.ts
  - .planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md
  - .planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `FIELD` verb line that carries the documented trailing error-branch option — own-words shape: the verb, the record part, a comma, the name part, the statement's own equals sign, the value part, then a comma, the word `ERR`, an equals sign and a line reference — parses with zero lexer and zero parser errors, at program level and inside a method body, in upper, lower and mixed case (D-08, D-09)."
    - "The same line parses when the value part itself carries an inner error-branch option inside its own function-call parentheses, so the inner option and the trailing one never compete for the same comma (D-08)."
    - "A `FIELD` verb without a value is still a parser error, and the class-member declaration together with its own no-type parser error stay byte-for-byte untouched (D-09, D-17)."
    - "Identifiers that are, start with or end with the words `field` and `err` stay ordinary names — a variable named `err`, a variable named `field`, and ordinary assignments to both, parse exactly as they did before this plan (D-09)."
    - "A comment sitting on a `:`-continuation line whose preceding statement line ends in a separator, and a comment sitting on a `:`-continuation line whose preceding line ends in the word `THEN`, both produce zero error-severity diagnostics — the compiler accepts both, so the check that flagged them was a false alarm (D-03)."
    - "A comment glued directly after an ordinary statement on the same line with no separator between them is still an error-severity diagnostic — the narrowed check keeps the one case it exists for (D-17)."
    - "Every `.bbj` file under `bbj-vscode/test/test-data/conformance/` still parses with zero lexer and parser errors and validates with zero error-severity diagnostics once linking diagnostics are excluded (D-16)."
    - "`bbj-vscode/src/language/generated/` is regenerated by `npm run langium:generate` under Node 22, never hand-edited and never staged (D-15)."
    - "The harness is run once more after this plan's last source change, with a snapshot of its details file taken first, and every movement claim is a file-set difference rather than a change in totals (D-03, D-11, D-12)."
    - "The closing gate table is restated with this plan's numbers, and the 99-05 attestation verdicts that the movement affects are corrected in place rather than left to contradict the newer table (D-06, D-13)."
    - "Only numbers, first-word groups, message groups with counts and own-words shape descriptions reach tracked files — no corpus file name, path or source line (D-14)."
    - statement: "The generator's ambiguity-warning output is unchanged by the grammar edit and the whole vitest suite reports zero failed tests, so neither gate was bought with a regression somewhere else."
      verification: backstop
  artifacts:
    - bbj-vscode/src/language/bbj.langium
    - bbj-vscode/src/language/bbj-validator.ts
    - bbj-vscode/test/test-data/conformance/field-verb.bbj
    - bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj
    - bbj-vscode/test/parser-keyword-statements.test.ts
    - bbj-vscode/test/line-break-validation.test.ts
    - .planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md
    - .planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
  key_links:
    - "The trailing option fragment reused by the verb rule <-> the same fragment already sitting inside the method-call rule's parentheses. The value part is a full expression that may end in a call carrying its own option, so the inner and the outer option must be resolved at two different decision points; if they ever compete, one of the two shapes stops parsing."
    - "The comment check's offsets <-> the string it indexes. Token offsets come from the lexer's rewritten text (the line splitter joins a `:`-continuation line into the head line, dropping the marker and padding the tail), while the CST root's full text is the original document the parser was handed. The two strings differ exactly where a continuation line was joined — which is exactly where the false alarms are."
    - "The narrowed comment check <-> the one case it must keep flagging. An exemption wide enough to clear all three false-alarm shapes must not clear a comment glued after an ordinary statement with no separator; that case is the only thing standing between the check and deletion."
    - "`bbj.langium` <-> the gitignored generated parser <-> the harness, which loads the language server from source. A grammar edit that is not regenerated leaves every test and every number describing the previous tree."
    - "The snapshot of the harness's own details file <-> the fresh one it overwrites. Without the snapshot only totals remain, and a pair of offsetting movements is invisible."
    - "This plan's gate table <-> the gate table and the closing attestation 99-05 already wrote. A newer table that leaves the older verdicts standing makes the record self-contradicting; the affected verdicts are corrected in place, in the same file."
  prohibitions:
    - statement: "MUST NOT widen a grammar rule or weaken a validation check in order to reach a gate number — a missed gate is recorded and escalated to a blocking human checkpoint, never engineered away."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT add the documented array-index part of the verb, a general key-equals-value option tail, or any option beyond the single documented error branch — no measured file needs them, and each is a widening the gate does not ask for."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT write a cause, a mechanism or an attribution for a file that moved the wrong way without the per-file look, and that look belongs to the orchestrator — this plan produces counts and hands them over."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT put proprietary corpus source text, corpus file names or corpus paths into any tracked file in this repository — fixtures are hand-written shapes with invented names, and the measurement record holds counts, word-group names and own-words shapes only."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT touch the class-member declaration rule, `bbj-token-builder.ts`, or any LSP provider — the phase's two deliberate non-goals (no blanket reserved-word rule, no new editor capability) are attested from the fact that those files carry no change."
      status: unresolved
      verification: judgment
---

<objective>
Close the two gates the closing measurement left open, then measure once more.

Gate row 2 is PARTIAL because one list-A file is a genuine residue of the verb form: the verb line
carries a trailing error-branch option after the value, and the rule added in plan 02 has no option
tail, so the parser stops at that second comma. Gate row 3 is a FAIL (A2 30 against a gate of 27)
because a pre-existing comment-separation check was unmasked when plan 02 removed the parser error
that had been suppressing it on 8 files — the check itself is wrong about a comment on a
`:`-continuation line, and the compiler accepts every shape it flags.

Purpose: the user's 2026-09-21 decision was to fix both rather than accept them as residue. The B
rise (665 to 666, plan 04's classified lost accidental catch) is accepted as recorded and nothing in
this plan addresses it.

Output: an option tail on the verb rule, a narrowed comment-separation check, two fixtures and two
test blocks pinning both the new positives and the cases that must stay flagged, and a closing
re-measurement that either seals the phase or stops for a human decision on a gate that is still
missed.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-CONTEXT.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-02-SUMMARY.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-05-SUMMARY.md
@CLAUDE.md
</context>

## What this plan changes, and who owns it

| Kind | Name / path | Owner |
|------|-------------|-------|
| Grammar rule edit | the verb rule gains the existing optional error-branch fragment as a trailing element | **99-06** |
| Validator change | `checkCommentNewLines` in `bbj-validator.ts` — narrowed so a comment at a legal statement-start position is not flagged | **99-06** |
| Regression fixture (extended) | `bbj-vscode/test/test-data/conformance/field-verb.bbj` | 99-02, extended here |
| Regression fixture (new) | `bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj` | **99-06** |
| Test file (extended) | `bbj-vscode/test/parser-keyword-statements.test.ts` — the existing verb describe block | 99-01, extended here |
| Test file (extended) | `bbj-vscode/test/line-break-validation.test.ts` — one new describe block | pre-existing, extended here |
| Measurement record | `99-CONFORMANCE.md` — the re-measure sections and the final gate table | **99-06** |
| Validation record | `99-VALIDATION.md` — three new Per-Task Verification Map rows | **99-06** |

Generated artifacts under the gitignored `bbj-vscode/src/language/generated/` are regenerated, never
hand-edited, never staged.

## Working rules for the executor (violating these stalls the run on a permission prompt)

- Absolute paths only. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`/`git`. Use
  `git -C /home/coder/repos/bbj-language-server ...`. The only allowed `cd` is in front of
  `npm`/`npx` in `/home/coder/repos/bbj-language-server/bbj-vscode`.
- Never use `git stash`. Stage with `git add <exact path>` only — never `git add -A` or `git add .`.
  Never stage `.planning/milestone.lock`.
- Never run `dispatch-isolation` or any other isolation verb; this plan runs in the working tree it
  was dispatched into.
- Commit with plain `git -C /home/coder/repos/bbj-language-server commit`, not the gsd commit
  helper: the helper writes a subject only, and every commit here must carry the trailer
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` (both trailer lines, if any, in one
  paragraph of a single `-m`).
- One atomic commit per task.
- vitest runs with cwd = `bbj-vscode` and `RUN_BBJ_TESTS=0`. Never write `--reporter=basic` — it does
  not exist in this project's vitest. For a probe that must print, use
  `--reporter=verbose --silent=false --disable-console-intercept`.
- `npm run build` does NOT regenerate the grammar. Only `npm run langium:generate` does, and it needs
  Node 22 (the repository default `node` is v24, on which `langium generate` throws from jsonschema).
  Resolve a Node 22 binary directory once with
  `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"` and prefix the generate call with
  `PATH="$NODE22_BIN:$PATH"`.
- Tests parse with `parseHelper` / `validationHelper`. Never `DocumentBuilder.build`.
- Register check: no plan id, decision id, requirement id or phase number in source comments, test
  names, test comments, fixture names or fixture content. GitHub issue numbers are fine. Planning
  identifiers inside `.planning/` are expected and fine.
- Do not read anything under `/home/coder/repos/bbj-corpus/corpus/`. The harness entry point, its
  `REPORT.md` and its `details.json` may be read; their contents leave this session as counts,
  word-group names and own-words shape descriptions only.
- Per-file inspection of a corpus file that moved the wrong way is the orchestrator's job. This plan
  produces counts and hands them over.
- Record deviations in the SUMMARY only. Do not open a `WINDOWS.md` ledger entry.
- Do not tick anything in `REQUIREMENTS.md` — the orchestrator closes the phase's requirement ids.
- Re-locate every grammar rule and every validator function by name; line numbers in this plan are
  orientation only and five plans have already edited both files.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The verb's trailing error-branch option — probe, one fragment, regenerate, probe again</name>
  <precondition>`npx -y node@22 -p process.execPath` resolves to a Node 22 binary; the repository's default `node` is v24, on which `npm run langium:generate` aborts before writing anything.</precondition>
  <reversibility rating="reversible">One optional fragment reference at the end of an existing rule; removing it restores the previous rule exactly, and the property it adds is optional in the generated type.</reversibility>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj.langium — the verb rule (search for the rule named `FieldStatement`, near the record-verb statements), the `Err` fragment and its siblings `Options`/`Option`/`Dom`/`Mode` (near the channel-option fragments), the `LabelRef` / `SymbolicLabelRef` rules, and the `MemberCall` rule whose method-call alternative already carries the same fragment inside its parentheses
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/parser-keyword-statements.test.ts — the header (one shared services instance, one top-level `beforeAll`) and the existing verb describe block, which this task extends rather than duplicates
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/field-verb.bbj — the fixture convention (leading `REM` lines stating what the file protects, invented names, one statement per line)
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/lib/labels.bbl — the symbolic labels the library declares, so the line reference used in the fixture is one that exists
  </read_first>
  <files>
    bbj-vscode/src/language/bbj.langium,
    bbj-vscode/test/test-data/conformance/field-verb.bbj,
    bbj-vscode/test/parser-keyword-statements.test.ts
  </files>
  <behavior>
    - The verb with a trailing error branch after the value parses with zero lexer and parser errors, in upper, lower and mixed case, at program level and inside a method body.
    - The verb whose value part carries an inner error branch inside its own call parentheses parses, both with and without the trailing one.
    - The verb without a trailing error branch keeps parsing exactly as before (every case already in the describe block stays green).
    - The verb without a value is still a parser error; the class-member declaration without a type is still a parser error.
    - A variable named `err`, a variable named `field`, and ordinary assignments and reads of both, parse with zero errors — before and after the edit.
  </behavior>
  <action>
    Probe first, add one optional fragment reference, regenerate, probe again.

    1. Write a throwaway vitest probe under `bbj-vscode/test/` with a clearly temporary name, in the
       shape plan 02 used (services over `EmptyFileSystem`, `initializeWorkspace`, `parseHelper` plus
       `validationHelper`, printing each top-level statement's `$type`, every parser error and every
       diagnostic; run it with `--reporter=verbose --silent=false --disable-console-intercept`).
       Record the BEFORE counts for:
       - the verb with a trailing error branch: the record part, a comma, the name part, the equals
         sign, a value, then a comma, the word `ERR`, an equals sign and a symbolic line reference;
       - the same line in upper, in lower and in mixed case, and inside a method body;
       - the verb whose value is a call carrying an inner error branch inside its parentheses, once
         without and once with the trailing option as well;
       - the verb with a trailing error branch whose line reference is an ordinary user label declared
         in the same program, not a symbolic one;
       - the two cases that must stay parser errors: the verb with no value, and the class-member
         declaration with no type;
       - the keyword-as-identifier cases that must stay clean: a variable named `err` assigned and
         read, a variable named `field` assigned and read, an identifier ending in each word, and a
         call-shaped and a member-shaped use of each word.
       The expected BEFORE result is a parser error on exactly the trailing-option shapes and clean
       parses everywhere else. If the trailing-option shape is already clean before the edit, stop and
       report: the gate row's premise would be wrong and the orchestrator has to look again.
    2. The trailing option is the existing `Err` fragment, used optionally, appended after the value
       part of the verb rule. Reuse that fragment; do not write a new one, do not use the general
       comma-separated option fragment, and do not introduce a keyword — the fragment's keyword
       already exists in the token vocabulary through the method-call, call, drop, run and execute
       rules, so this edit adds no lexer token at all. The BASIS documentation for this verb gives its
       syntax as the record, the name, an optional bracketed array index, the equals sign, the value
       and an optional error branch; the error branch is the only option it documents, which is why
       the narrow fragment is the right one and a general option tail is not.
    3. Do NOT implement the documented optional bracketed array index between the name part and the
       equals sign. No measured file needs it, the gate does not ask for it, and adding it puts a
       second decision point in front of the same comma. Record it in the SUMMARY as a documented
       form this repository still does not accept, for the next phase's long tail.
    4. Regenerate under Node 22 as described in the working rules, capturing the generator output
       before and after and comparing the ambiguity-warning lines. A NEW ambiguity warning is a
       stop-and-report signal; the pre-existing ones are benign.
    5. Re-run the probe. Gate on this step: every trailing-option shape now has zero parser errors;
       the inner-option shapes still have zero parser errors both with and without the trailing one;
       the two must-stay-errors are still errors; and the keyword-as-identifier error count is
       unchanged or lower than BEFORE. The inner option is expected to work already, because the
       method-call rule carries the same fragment inside its own parentheses — if the probe shows the
       inner form failing, stop and report rather than widening the call rule; that is a separate
       defect and not this plan's gate.
    6. If the trailing option and the value part turn out to compete (a value that ends in a call, a
       negative number or a bare name followed by the comma), work the documented options in order:
       confirm by probe which of the two paths the parser takes; if the lookahead is the problem,
       report the exact shape and the parser message rather than restructuring the value part. Do not
       change the level the name part is parsed at — plan 02 established it and every existing case
       depends on it.
    7. Extend `bbj-vscode/test/test-data/conformance/field-verb.bbj` — keep every line it already
       has, add one `REM` line to its header block saying the file now also protects the trailing
       error branch, then add: one verb line with a trailing error branch to a symbolic line
       reference, one to an ordinary user label declared in the same fixture, one whose value carries
       an inner error branch as well, and one lower-case and one mixed-case spelling of the trailing
       form. Invented record, field and variable names only.
    8. Extend the existing verb describe block in `parser-keyword-statements.test.ts` — do not add a
       second services instance and do not open a second describe block for the same construct. Add:
       the positive trailing-option cases from step 1 as table cases asserting zero lexer and zero
       parser errors; one AST-shape case asserting the verb line with a trailing option still produces
       exactly one statement of the verb's own type, reached through the generated type guard, with
       its record, name and value parts present and its error-branch property present; the
       keyword-as-identifier cases for the words `field` and `err`; and a validation case asserting
       the trailing-option line produces no error-severity diagnostic once linking diagnostics are
       excluded. Keep both still-flagged cases exactly as they are.
    9. Delete the probe file, run the register check over this task's source diff, and commit.
  </action>
  <acceptance_criteria>
    - The verb rule ends with an optional reference to the existing error-branch fragment, and `git -C /home/coder/repos/bbj-language-server diff -- bbj-vscode/src/language/bbj.langium` shows no other change: no new fragment, no new keyword, no change to the general option fragment, no change to the class-member declaration rule or the class-member alternation, and no change to the level the name part is parsed at.
    - `bbj-vscode/src/language/generated/ast.ts` is no older than `bbj-vscode/src/language/bbj.langium`, and the generated type for the verb now carries an optional error-branch property.
    - The generator's ambiguity-warning lines are identical before and after the edit.
    - The post-edit probe records zero parser errors for every trailing-option shape, for both inner-option shapes, for all three letter cases and for the method-body case, and an identifier-case error count no higher than the pre-edit run.
    - The two still-flagged cases (a verb with no value, a class-member declaration with no type) are parser errors in both the before and the after run, and their tests are unchanged.
    - `grep -c 'ERR' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/field-verb.bbj` reports 4 or more, and the fixture's first non-blank line is still a `REM` line.
    - `parser-keyword-statements.test.ts` still contains exactly one `createBBjServices(` call and exactly one top-level `beforeAll`.
    - `bbj-vscode/src/language/bbj-token-builder.ts` and every provider file under `bbj-vscode/src/language/` are untouched by this task.
    - No scratch probe file remains: `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/test` lists only the files named in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/parser-keyword-statements.test.ts test/conformance-regressions.test.ts test/parser.test.ts test/lexer.test.ts test/classes.test.ts test/declare-in-class.test.ts test/line-break-validation.test.ts test/example-files.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the run reports 0 tests collected</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 -- bbj-vscode | grep '^+' | grep -qE 'PARSE-0|CONF-0|D-[0-9][0-9]|99-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source</fails_when>
  </verify>
  <done>The verb accepts its documented trailing error branch, at every letter case and in a method body, with an inner error branch in the value as well; both still-flagged cases and every keyword-as-identifier case are unchanged; the generated parser is current and no probe file remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The comment-separation false alarm — reproduce each shape, establish the cause, narrow the check</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-validator.ts — `checkCommentNewLines` (its registration in the validation registry, its early return on parser errors and on a preceding label declaration, and its backwards walk over the CST root's full text), and the exported `findLeafNodeAtOffset` helper together with its `binarySearch`, which returns the nearest leaf at or before the requested offset
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/bbj-lexer.ts — `prepareLineSplitter` in full: it joins every `:`-continuation line into the head line, drops the leading marker, and appends one padding space per joined line
    - /home/coder/repos/bbj-language-server/bbj-vscode/src/language/validations/line-break-validation.ts — `getPreviousNode` (the same leaf lookup, one step away) and `isStandaloneStatement`'s existing exemptions, which are the precedent for "a statement in this position needs no separator"
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/line-break-validation.test.ts — the header (one shared services instance, one `beforeAll`, the diagnostics filter helper) and any describe block's still-flagged case, which this task copies in shape
    - /home/coder/repos/bbj-language-server/bbj-vscode/test/conformance-regressions.test.ts — what the fixture folder loop asserts: zero lexer errors, zero parser errors AND zero error-severity diagnostics once linking diagnostics are excluded, so a fixture may only carry shapes the fix actually clears
  </read_first>
  <files>
    bbj-vscode/src/language/bbj-validator.ts,
    bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj,
    bbj-vscode/test/line-break-validation.test.ts
  </files>
  <behavior>
    - A comment alone on a `:`-continuation line, where the preceding statement line ends in a separator, produces zero error-severity diagnostics.
    - A comment alone on a `:`-continuation line, where the preceding line's last word is `THEN`, produces zero error-severity diagnostics.
    - A comment directly after `THEN` on the same physical line produces zero error-severity diagnostics, or — if that cannot be had without losing the case below — stays flagged and is recorded as such.
    - A comment glued after an ordinary statement on the same line with no separator between them is still an error-severity diagnostic.
    - A comment on its own line, a comment after a separator on the same line, and a comment directly after a label declaration all stay clean, exactly as today.
  </behavior>
  <action>
    Reproduce, establish the cause per shape, then narrow — in that order (D-03).

    1. Write a throwaway vitest probe under `bbj-vscode/test/` with a clearly temporary name, using
       `validationHelper`, printing every diagnostic's severity, message and range, and run it with
       `--reporter=verbose --silent=false --disable-console-intercept`. Reproduce the three shapes as
       synthetic snippets with invented names — (a) a one-line nested if-then-else-with-closer
       statement whose line ends in a separator, with a comment alone on the following
       `:`-continuation line; (b) a line whose last word is `THEN`, with a comment alone on the
       following `:`-continuation line; (c) an if-then line with a comment directly after `THEN` on
       the same physical line. Vary the indentation after the continuation marker (none, one space,
       several spaces) and the number of consecutive continuation lines (one, two): the point is to
       find out which inputs the check flags and which it does not. Also record the BEFORE state of
       every shape that must keep working: a comment on its own line, a comment after a separator on
       the same line, a comment after a label declaration, and the glued-comment case that must stay
       an error.
    2. Establish the cause for each flagged shape from the probe plus the two source files, and write
       what you established into the SUMMARY. The prime suspect, to confirm or refute rather than to
       assume: the check walks backwards over `node.$cstNode.root.fullText`, which is the ORIGINAL
       document text the parser was handed, using an offset that came from the lexer's REWRITTEN
       text — and those two strings diverge exactly where a `:`-continuation line was joined, because
       joining drops the marker and the newline from in front of the continuation content and pays the
       length back as padding at the end of the joined line. A comment on a joined line therefore has
       its backwards walk start two characters early per joined line, landing on the marker or inside
       the previous line's code instead of on the character that really precedes it. Shape (c) has no
       join at all, so it cannot have that cause: its character before the comment is the last letter
       of the preceding keyword, and the check's alphabet of acceptable separators (a newline or a
       semicolon) has no room for a position where a statement legally begins.
    3. Fix narrowly, and only in `checkCommentNewLines` — plus, if the probe proves it strictly
       necessary, a helper that file already imports. Anything wider is a stop-and-report. The
       recommended shape, to confirm or refute by probe: decide from the CST instead of from raw text,
       so the decision is made entirely in the offset space the offsets belong to. Look up the leaf
       immediately preceding the comment with the file's own leaf-lookup helper (it already returns
       the nearest leaf at or before an offset, which is how the existing label-declaration exemption
       works), and treat the comment as correctly separated when there is no preceding leaf at all,
       when that leaf is the statement separator, when that leaf ends on an earlier line than the
       comment starts, or when that leaf is a keyword after which a statement legally begins — the
       then-branch and else-branch keywords. Everything else stays an error. Keep the existing early
       returns (parser errors present, preceding label declaration) exactly as they are.
    4. Check the exemption list against the case the check exists for before accepting it: a comment
       glued after an ordinary statement on the same line with no separator must still be flagged,
       because the leaf in front of it is part of an expression, not a separator and not a
       statement-start keyword. If the only way to clear shape (c) is an exemption that also clears
       that case, do NOT take it: leave shape (c) flagged, record it in the SUMMARY and in the
       measurement record as a remaining false alarm with its own-words shape, and move on — shapes
       (a) and (b) alone are 8 of the 9 files and bring the gate measure to 22.
    5. Do not change the message text and do not change which node the diagnostic is attached to; the
       measurement record's message-group tables are keyed on that text.
    6. Create `bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj`: a leading `REM`
       block stating in behaviour terms what the file protects, then one line group per shape the fix
       actually clears, with invented names. A shape left flagged in step 4 must NOT go into this
       fixture — the folder's loop asserts zero error-severity diagnostics, so putting it there would
       turn a recorded residue into a red suite. Confirm with the probe that the fixture parses with
       zero lexer and parser errors and validates with zero error-severity diagnostics before
       committing it; if a shape cannot be made parser-clean at all, report it rather than quietly
       dropping it.
    7. Add one new describe block to `bbj-vscode/test/line-break-validation.test.ts`, reusing the
       file's single services instance and `beforeAll`: the positive cases for every shape the fix
       clears, each asserting zero error-severity diagnostics; the glued-comment case asserting the
       diagnostic list is NOT empty (assert non-emptiness and, at most, that the message mentions
       comments — never an exact full message string); the three shapes that were already clean
       (comment on its own line, comment after a separator, comment after a label declaration); and,
       if step 4 left one flagged, one case pinning that shape as not yet accepted, with a comment
       saying the compiler accepts it and this repository does not.
    8. Delete the probe file, run the register check over this task's source diff, and commit.
  </action>
  <acceptance_criteria>
    - `git -C /home/coder/repos/bbj-language-server diff --stat -- bbj-vscode/src` lists only `bbj-vscode/src/language/bbj-validator.ts` for this task, and the diff inside it is confined to `checkCommentNewLines` (plus, if the probe proved it necessary, one helper it already imports — named and justified in the SUMMARY).
    - The diagnostic's message text and the node it is attached to are unchanged.
    - The probe's recorded BEFORE state shows each of the three shapes as flagged (or names precisely which inputs are flagged and which are not) and the AFTER state shows every shape the fix targets as clean.
    - The glued-comment case is an error-severity diagnostic in both the before and the after run, and a test asserts it is not empty.
    - `bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj` exists, its first non-blank line is a `REM` line, and `grep -v '^ *[Rr][Ee][Mm]' /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/comment-after-continuation.bbj | grep -c '[^[:space:]]'` reports 4 or more.
    - `line-break-validation.test.ts` still contains exactly one `createBBjServices(` call and exactly one top-level `beforeAll`, and the new describe block contains at least one assertion that a diagnostic list is NOT empty.
    - Any shape left flagged is named with its own-words shape in the SUMMARY and pinned by a test, not silently dropped.
    - The register check over this task's source diff produces no match.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/line-break-validation.test.ts test/conformance-regressions.test.ts test/validation.test.ts test/parser.test.ts test/comment-provider.test.ts test/example-files.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the run reports 0 tests collected</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 -- bbj-vscode | grep '^+' | grep -qE 'PARSE-0|CONF-0|D-[0-9][0-9]|99-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source</fails_when>
  </verify>
  <done>Each false-alarm shape was reproduced before it was fixed, its cause is written down from the code rather than assumed, the check no longer flags a comment at a position where a statement legally begins, the glued-comment case is still flagged, and anything left flagged is recorded and pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Closing re-measure, the final gate table, and the conditional stop</name>
  <precondition>`/home/coder/repos/bbj-corpus/conformance/run.mjs` and `/home/coder/repos/bbj-corpus/conformance/work/` both exist and are readable, and `/home/coder/repos/bbj-corpus/conformance/snapshots/` still holds the per-plan before-snapshots from the earlier plans of this phase.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md from the "Closing run" heading to the end of the file (the closing numbers, the four-row gate table, the orchestrator's per-file look, the closing decision, the movement sections and the closing attestation this task must reconcile with)
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md, the "Phase 99" block — the five success criteria in the wording the context commit amended
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md — the Per-Task Verification Map and the whole-suite judging rule
    - /home/coder/repos/bbj-corpus/conformance/REPORT.md — the A-by-first-word and A2-by-message group tables; read for counts and group names only
  </read_first>
  <files>
    .planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md,
    .planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
  </files>
  <action>
    Measure the tree this plan leaves behind, once, and write the gate (D-03, D-11, D-12, D-13, D-14).

    1. Prove the tree first, exactly as the closing plan did: the generated parser no older than the
       grammar (regenerate under Node 22 if it is not, and do not run `npm run build`); a clean
       `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src`; the whole
       vitest suite judged on the reported failed-TEST count rather than the failed-suite count, since
       a suite whose `beforeAll` timed out under contention is environment noise and one end-to-end
       suite fails for a documented environment reason; and the register check over the phase's whole
       source diff, resolving the base as the merge base of HEAD with `origin/main`. If a test
       genuinely fails, stop and report rather than measuring.
    2. Copy `/home/coder/repos/bbj-corpus/conformance/details.json` to
       `/home/coder/repos/bbj-corpus/conformance/snapshots/details-99-06-before.json` BEFORE the run —
       the harness overwrites that file and wipes its own working directory at the start of every run.
       Then run
       `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`.
       State in the record which commit was measured and that the source was unmodified at the time.
    3. Append a "## Run: plan 06 (gap closure re-measure)" section: the command, the date, the
       commit, and one table row per measure carrying the Phase 98 close baseline (A 167, A2 27,
       B 665 of 1,210), the closing-run value (A 53, A2 30, B 666), this run's value, both deltas,
       the gate and the verdict. Restate the direction of worse for each measure so a later reader can
       check the arithmetic instead of trusting it. Compute the file-SET differences against
       `details-99-06-before.json` with a small inline `node -e` over the snapshot and the fresh
       details file, for A, A2 and B alike, and print set SIZES only — how many files left each list
       and how many appeared. No id, no path, no source line.
    4. Append a "## Final gate table" with the same four rows the closing table used: list A at or
       below 80; no remaining list-A first-word group of `FIELD`, `READ`, `IOLIST` or the word
       `label`, each of the four named with its remaining count; A2 at or below 27; and B recorded
       with its evidence status against the accepted baseline of 666 — B is not a gate and its rise to
       666 is already accepted, so only a rise ABOVE 666 needs the file-set count and the hand-over.
       For the A2 row, name the message groups that left and any that appeared, established by set
       difference and not by the totals.
    5. Reconcile the older record with this one: the closing run's gate table, its note on the
       remaining first-word group, and the closing attestation's verdicts for the criteria this plan
       moved are corrected in place, each with a one-line pointer to this run's section, so the file
       does not carry two contradicting verdicts. Do not rewrite the closing run's own numbers — they
       were true of the tree they measured; only the verdicts and the notes that this plan supersedes.
    6. Add three rows to `99-VALIDATION.md`'s Per-Task Verification Map, one per task of this plan,
       each with its requirement ids, its threat reference, the secure behaviour it upholds, its test
       type, its automated command and its status; and confirm every pre-existing row still carries a
       status and a real task id.
    7. **The conditional stop.** Finish autonomously only if ALL of these hold: every gate row reads
       PASS; A2 is at or below 27; B is at 666 or lower; and the file-set difference shows no file
       newly entering A and none newly entering A2, even if the totals improved. Otherwise do not
       seal: stop and return a blocking human checkpoint using the executor's checkpoint return
       format, reporting `**Gate:** blocking-human`, which gate missed and by how much, the file-set
       counts for every list that moved, and the two options the decision is between — accept the
       shortfall as recorded residue, or hand a named group to the next phase. Hand the per-file look
       to the orchestrator and write no cause, no mechanism and no attribution for any moved file. Do
       not adjust a rule, a check or a fixture to convert a miss into a pass.
    8. Every number in these sections must be reproducible from the command, the snapshot and the
       harness report alone. Commit the record.
  </action>
  <acceptance_criteria>
    - `/home/coder/repos/bbj-corpus/conformance/snapshots/details-99-06-before.json` exists and was written before the run.
    - `99-CONFORMANCE.md` carries a "Run: plan 06 (gap closure re-measure)" section and a "Final gate table" section, each of the four named first-word groups appears with its remaining count, and the A2 claim cites a file-set difference rather than a change in totals.
    - The closing run's gate table, its remaining-first-word-group note and the closing attestation's affected verdicts are corrected in place with a pointer to this run, so no two sections of the file disagree.
    - `99-VALIDATION.md` carries one Per-Task Verification Map row per task of this plan, and every row in the table has a status and a real task id.
    - The whole vitest suite reports a failed-test count of 0, and the register check over the phase's whole source diff produces no match.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain` shows nothing under `bbj-vscode/src/language/generated/`, no scratch probe file, and `.planning/milestone.lock` unstaged.
    - If any gate row is not a PASS, or B rose above 666, or any file newly entered A or A2, the plan returned a blocking human checkpoint instead of sealing, and no source file was touched to move a number.
    - No corpus file name, corpus path or corpus source line appears anywhere in the record.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary (a non-zero exit caused only by hook-timeout failed suites and the one documented environment failure, with a failed-test count of 0, is not a failure)</fails_when>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server</automated>
    <fails_when>non-zero exit, or the run's output carries no falseRejects/falseAlarms/missed counts, or a falseRejects count higher than the 53 the closing run recorded</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked measurement record</fails_when>
  </verify>
  <done>The tree this plan leaves behind has been measured once, the final gate table states each gate's verdict with reproducible arithmetic and file-set evidence, the older record no longer contradicts it, and the phase either closes cleanly or stops for a human decision on a gate that is still missed.</done>
</task>

</tasks>

<!-- planner-discipline-allow: PARSE-0|CONF-0|D-[0-9][0-9]|99-[0-9][0-9] -->
<!-- planner-discipline-allow: ^+ -->
<!-- planner-discipline-allow: bbj-corpus/corpus|/corpus/ -->
<!-- planner-discipline-allow: ERR -->
<!-- planner-discipline-allow: [Rr][Ee][Mm] -->
<!-- planner-discipline-allow: createBBjServices( -->

<threat_model>
ASVS level 1; block on high. This plan changes how already-locally-readable `.bbj` source text is
parsed and validated. It adds no network surface, no authentication, no session or access-control
path and no new external input source.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workspace -> language-server parser and validator | a `.bbj` file of arbitrary content is parsed and validated in the editor's language-server process |
| private corpus -> this public repository | the measurement reads a private corpus's harness output; every tracked file here is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-99-06-01 | Information Disclosure | `field-verb.bbj`, `comment-after-continuation.bbj`, the two test files, `99-CONFORMANCE.md` | high | mitigate | Fixtures and tests are hand-written minimal shapes with invented names; the measurement record holds counts, first-word groups, message groups and own-words shapes only, and the inline set-difference script prints sizes rather than ids. Enforced by the register check in Tasks 1 and 2 and the corpus-path grep in Task 3. |
| T-99-06-02 | Repudiation | the final gate table and the movement sections | high | mitigate | Every movement claim is a file-set difference against a snapshot taken immediately before the run; no cause is written for a moved file without the per-file look, which belongs to the orchestrator; a missed gate returns a blocking checkpoint rather than an adjusted rule, so the record cannot claim a pass the tree does not support. The older gate table and attestation are corrected in place so the file carries one verdict per criterion. |
| T-99-06-03 | Tampering | grammar-derived parser artifacts | medium | mitigate | The generated directory is regenerated under Node 22, never hand-edited and never staged; Tasks 1 and 3 assert it is no older than the grammar, so no test and no measurement can silently run against a stale parser. |
| T-99-06-04 | Elevation of Privilege | the narrowed comment-separation check | high | mitigate | Narrowing a validation check is a deliberate reduction in what the tool rejects. The one case the check exists for — a comment glued after an ordinary statement with no separator — is pinned by a test asserting the diagnostic is still raised, in both the before and the after probe; the exemption list is confined to positions where a statement legally begins, and a shape that cannot be cleared without losing that case is left flagged and recorded instead. |
| T-99-06-05 | Denial of Service | lexer token patterns | low | accept | This plan adds no lexer token and no regex: the option tail reuses a fragment whose keyword is already in the token vocabulary, and the validator change removes a text scan rather than adding one. Catastrophic-backtracking risk is unchanged. |
| T-99-06-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no project package. 99-RESEARCH.md records the Package Legitimacy Audit as not applicable. The only network-touching command resolves a Node 22 runtime through the same invocation earlier plans of this phase already used; no dependency is added or upgraded. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/parser-keyword-statements.test.ts test/conformance-regressions.test.ts test/line-break-validation.test.ts test/validation.test.ts test/parser.test.ts test/lexer.test.ts test/classes.test.ts test/declare-in-class.test.ts test/comment-provider.test.ts test/example-files.test.ts`
   — the blast-radius set for one grammar-rule tail plus one validator narrowing.
2. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`
   — judged on `numFailedTests: 0`. Hook-timeout "failed suites" under contention and the one
   documented environment failure are not failed tests.
3. `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`
   — run once on the tree this plan leaves behind, with a snapshot of the harness's details file
   taken first.
4. The register check over the phase's whole source diff produces no match.
5. `git -C /home/coder/repos/bbj-language-server status --porcelain` shows nothing under
   `bbj-vscode/src/language/generated/`, no scratch probe file and no staged lock file.
</verification>

<success_criteria>
- The verb accepts its documented trailing error branch, in any letter case, at program level and in
  a method body, with an inner error branch inside the value's own call as well; the class-member
  declaration, both still-flagged cases and every keyword-as-identifier case are unchanged.
- A comment at a position where a statement legally begins — after a separator, on a continuation
  line, after the then-branch keyword — no longer produces an error-severity diagnostic, while a
  comment glued after an ordinary statement with no separator still does.
- Both fixtures are collected by the conformance folder loop and stay clean; anything left flagged is
  pinned by a test and recorded with its own-words shape.
- The final gate table states, with reproducible arithmetic and file-set evidence: A at or below 80;
  the remaining count for each of the four named first-word groups; A2 at or below 27; B at or below
  the accepted 666.
- The older gate table and attestation are corrected in place, so the record carries one verdict per
  criterion.
- Either every gate passes and the phase closes autonomously, or a blocking checkpoint reports the
  miss with its counts — never a number engineered into a pass.
</success_criteria>

<output>
Create `.planning/phases/99-parser-gaps-the-largest-groups/99-06-SUMMARY.md` when done. Record in it:
the final shape of the verb rule's tail and whether the inner option needed anything; the documented
array-index form left unimplemented, for the next phase's long tail; for each of the three
comment-check shapes, the cause you established and how the fix addresses it, plus any shape left
flagged and why; the before/after counts from both probes; the re-measured numbers with both deltas
and the file-set differences; which gates passed; and anything handed to the orchestrator or to the
next phase, by message group. Record deviations in the SUMMARY only — do not open a `WINDOWS.md`
ledger entry, and do not tick anything in `REQUIREMENTS.md`.
</output>
