---
phase: 99-parser-gaps-the-largest-groups
plan: 05
type: execute
wave: 5
depends_on: [99-04]
files_modified:
  - .planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md
  - .planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The conformance harness has been run once more after the last source change of the phase, including any code-review fix, and before any acceptance is asked for (D-12)."
    - "The closing gate table records: list A at or below 80; no remaining list-A first-word group of `FIELD`, `READ`, `IOLIST` or the word `label`; A2 at or below 27; and B with its own evidence status, recorded and not gated (D-11)."
    - "The A2 message groups that disappeared since the Phase 98 close are named, and the claim is backed by a file-set difference rather than by the totals alone (D-03)."
    - "Every newly-uncaught B file is counted by file-set difference against the snapshot taken before the run, and the count is handed to the orchestrator for the per-file look — no cause is written down without it (D-11)."
    - "If list A is above 80 after the four groups, what the still-rejected files of these groups now stop at is listed by message group, anything trivially related to a change already made is fixed and re-measured, and the rest is handed to the next phase's long tail — no group from that phase is pulled forward to reach the number (D-13)."
    - "Only numbers, first-word groups, message groups with counts and own-words shape descriptions reach tracked files — no corpus file name, path or source line (D-14)."
    - "Each of the phase's five success criteria, in the wording amended by the context commit, has a recorded verdict and named evidence (D-06)."
    - "The whole vitest suite on the final tree reports zero failed tests, and the phase's whole source diff carries no planning identifier."
    - statement: "The closing run and the last per-group run report the same numbers when no source changed between them, so any recorded movement is attributable to a specific commit rather than to run-to-run variation (D-12)."
      verification: backstop
  artifacts:
    - .planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md
    - .planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
  key_links:
    - "`bbj.langium` <-> the gitignored generated parser <-> the harness, which loads the language server from source. If the generated parser is older than the grammar at the moment of the run, every number in the closing table describes a tree that was never committed."
    - "The snapshot of the harness's own details file <-> the fresh one it overwrites. The snapshot is the only way a file-set difference can be computed at all; without it only totals remain, and a pair of offsetting movements is invisible."
    - "The closing table <-> the roadmap's five success criteria. A gate row with no criterion behind it, or a criterion with no row, is how a phase seals green while a promise is unmet."
  prohibitions:
    - statement: "MUST NOT widen a grammar rule, weaken a check or adjust a fixture in order to reach a gate number — a missed gate is recorded and escalated, never engineered away."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT write a cause, an attribution or an explanation for a moved file that was not established by looking at that file — a plausible mechanism recorded as fact is what the per-file evidence rule exists to prevent."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT put proprietary corpus source text, corpus file names or corpus paths into any tracked file in this repository — the measurement record holds counts, word-group names and own-words shapes only."
      status: unresolved
      verification: judgment
---

<objective>
Close the phase's measurement: re-run the private conformance harness once more on the final tree,
write the gate table, disposition what the four groups left behind, and record a verdict with named
evidence for each of the phase's five success criteria.

Purpose: the per-group runs in plans 01 to 04 each measured one construct against the tree as it
stood then. The gate is about the phase as a whole on the tree that will actually ship, after the last
source change including any code-review fix (D-12). A file can hide a second cause behind the first,
so the expected drop (roughly 167 to 56) is a projection, not a promise: if list A is still above 80,
this plan applies the recorded fallback rather than inventing one (D-13).

This plan changes no source. It runs the harness, computes file-set differences against the
snapshots, and writes the record.

Output: the closing sections of `99-CONFORMANCE.md` — the gate table, the disposition of what
remains, the A2 movement, the B evidence status — and a reconciled `99-VALIDATION.md`.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-CONTEXT.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-01-SUMMARY.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-02-SUMMARY.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-03-SUMMARY.md
@.planning/phases/99-parser-gaps-the-largest-groups/99-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase-wide list. This plan creates no source symbol; it closes the measurement record.

| Kind | Name / path | Owner |
|------|-------------|-------|
| Grammar rule edit | `LastVerifyOption` — fused `LEN` literal split into the ordinary keyword plus the plain equals token | 99-01 |
| Grammar rule + AST type | `FieldStatement` (properties `record`, `name`, `value`) | 99-02 |
| Grammar rule edit | `FeatureName` gains a `'label'` alternative | 99-03 |
| Grammar datatype rule | `LabelName` (`ID \| 'label'`), consumed by `LabelDecl.name` and the `UserLabelRef` cross-reference | 99-03 |
| Grammar rule + AST type | `IolistStatement` (property `items`) | 99-04 |
| Regression fixtures | `record-verbs-len-option.bbj`, `field-verb.bbj`, `label-word-as-name.bbj`, `iolist-statement.bbj` under `bbj-vscode/test/test-data/conformance/` | 99-01 … 99-04 |
| Test file | `bbj-vscode/test/parser-keyword-statements.test.ts` | 99-01 |
| Measurement record | `.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md` — closing sections | **99-05** |
| Validation record | `.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md` — reconciled Per-Task Verification Map | **99-05** |

Generated parser artifacts under the gitignored `bbj-vscode/src/language/generated/` are refreshed
only if the grammar turns out to be newer; never hand-edited, never staged.

## Working rules for the executor (violating these stalls the run on a permission prompt)

- Absolute paths only. Never chain `cd` with `grep`/`find`/`cat`/`sed`/`head`/`tail`/`git`. Use
  `git -C /home/coder/repos/bbj-language-server ...`. The only allowed `cd` is in front of
  `npm`/`npx` in `/home/coder/repos/bbj-language-server/bbj-vscode`.
- Never use `git stash`. Stage with `git add <exact path>` only — never `git add -A` or `git add .`.
- Commit with plain `git -C /home/coder/repos/bbj-language-server commit`, not the gsd commit
  helper: the helper writes a subject only, and every commit here must carry the trailer
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- vitest runs with cwd = `bbj-vscode` and `RUN_BBJ_TESTS=0`. Never write `--reporter=basic`.
- `npm run build` is NOT needed for the harness run: it loads the language server from source, so
  only the generated directory has to be current. Refresh it with `npm run langium:generate` under
  Node 22 — `NODE22_BIN="$(dirname "$(npx -y node@22 -p process.execPath)")"`, then prefix the
  generate call with `PATH="$NODE22_BIN:$PATH"` — and only if the grammar is newer than the generated
  files.
- Do not read anything under `/home/coder/repos/bbj-corpus/corpus/`. The harness entry point, its
  `REPORT.md` and its `details.json` may be read; their contents leave this session as counts,
  word-group names and own-words shape descriptions only.
- Per-file inspection of a corpus file that moved the wrong way is the orchestrator's job, not this
  plan's. This plan produces counts and hands them over.
- Planning identifiers are expected and fine inside `.planning/`. The register check applies to the
  source diff only.

<tasks>

<task type="auto">
  <name>Task 1: Prove the final tree is the tree being measured</name>
  <read_first>
    - the four plan SUMMARY files in /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/ (what each plan changed, which still-flagged case each pinned, what each left open — and the phase base commit recorded in the first one)
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md (the sampling contract and the whole-suite judging rule)
  </read_first>
  <files>.planning/phases/99-parser-gaps-the-largest-groups/99-VALIDATION.md</files>
  <action>
    Confirm coherence before any number is written down.

    1. Compare the modification time of `bbj-vscode/src/language/bbj.langium` with that of
       `bbj-vscode/src/language/generated/ast.ts`. If the grammar is newer, regenerate under Node 22
       as described in the working rules. Do not run `npm run build`.
    2. Confirm the working tree is clean under `bbj-vscode/src` and that nothing under the generated
       directory is staged or modified in git's view.
    3. Run the whole vitest suite with two workers and judge it on the reported failed-test count, not
       the failed-suite count: a suite that fails because a `beforeAll` hook timed out under
       contention is environment noise. If any test genuinely fails, stop and report rather than
       measuring.
    4. Confirm all four fixtures this phase promised are present in the conformance folder and that
       the conformance test collects them, and that the phase's parser test file holds one describe
       block per construct group with its still-flagged case.
    5. Run the register check over the phase's whole source diff. Resolve the base as the merge base
       of HEAD with `origin/main`; that is a superset of this phase's own commits, which is safe
       because every earlier phase enforced the same check over its own diff.
    6. Reconcile `99-VALIDATION.md`'s Per-Task Verification Map: for each row, confirm the named
       command exists and was run, set the status column, and note any row whose file column still
       reads as missing.
  </action>
  <acceptance_criteria>
    - The vitest run reports a failed-test count of 0.
    - `bbj-vscode/src/language/generated/ast.ts` is no older than `bbj-vscode/src/language/bbj.langium`.
    - `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src` prints nothing.
    - `ls -1 /home/coder/repos/bbj-language-server/bbj-vscode/test/test-data/conformance/` lists all four fixtures this phase promised alongside the nine inherited ones.
    - The register check over the phase's whole source diff produces no match.
    - Every row of the Per-Task Verification Map in `99-VALIDATION.md` carries a status and a real task id.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2</automated>
    <fails_when>a failed-test count above 0 in the vitest summary (a non-zero exit caused only by hook-timeout failed suites with a failed-test count of 0 is not a failure)</fails_when>
    <automated>! git -C /home/coder/repos/bbj-language-server diff -U0 "$(git -C /home/coder/repos/bbj-language-server merge-base HEAD origin/main)"..HEAD -- bbj-vscode | grep '^+' | grep -qE 'PARSE-0|CONF-0|D-[0-9][0-9]|99-[0-9][0-9]'</automated>
    <fails_when>non-zero exit, meaning the grep matched at least one added line carrying a planning identifier that must not reach source</fails_when>
  </verify>
  <done>The final tree has a current generated parser, a clean source status, a suite green on failed tests, all four fixtures live, a clean register check, and a reconciled verification map.</done>
</task>

<task type="auto">
  <name>Task 2: Closing harness run, the gate table, and the disposition of what remains</name>
  <precondition>`/home/coder/repos/bbj-corpus/conformance/run.mjs` and `/home/coder/repos/bbj-corpus/conformance/work/` both exist and are readable, and the four per-plan before-snapshots from plans 01 to 04 are still present in that work directory.</precondition>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md (the four per-plan run sections and the baselines)
    - /home/coder/repos/bbj-language-server/.planning/phases/98-line-break-validation-false-alarms-a2/98-CONFORMANCE.md sections 9 and 10 (the per-file evidence format and the closing re-run that caught a late movement)
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md, the "Phase 99" block (the five success criteria in their amended wording) and the "Phase 100" block (where a handed-over group lands)
    - /home/coder/repos/bbj-corpus/conformance/REPORT.md — the A-by-first-word and A2-by-message group tables; read for counts and group names only
  </read_first>
  <files>.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md</files>
  <action>
    Measure the final tree once and write the gate (D-11, D-12, D-13, D-14).

    1. Copy `/home/coder/repos/bbj-corpus/conformance/details.json` to
       `/home/coder/repos/bbj-corpus/conformance/work/details-99-05-before.json`, then run
       `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`.
       State in the record which commit was measured and that the source was unmodified at the time.
    2. Append a **Closing run** section with the command, the date, the commit, and one table row per
       measure carrying: the Phase 98 close baseline (A 167, A2 27, B 665 of 1,210), the value at the
       last per-group run, this run's value, both deltas, the gate, and the verdict. Restate the
       direction of worse for each measure so a later reader can check the arithmetic instead of
       trusting it.
    3. Append a **Gate table** with exactly four rows: list A at or below 80; no remaining list-A
       first-word group of `FIELD`, `READ`, `IOLIST` or the word `label` (each of the four named with
       its remaining count, zero or otherwise); A2 at or below 27; and B recorded with its evidence
       status — B is not a gate, but a rise needs the file-set count and the hand-over.
    4. Append an **A2 movement** section naming which message groups present at the Phase 98 close are
       now absent and which are new, established by a file-set difference between this run and the
       recorded Phase 98 closing set — not by the totals (D-03). The five residue files with
       messages related to the word this phase unfused were expected to clear; say whether they did.
    5. Append a **B movement** section with the count of newly-uncaught files from the file-set
       difference against the snapshot, and the line "N files newly uncaught — handed to the
       orchestrator for the per-file look". Write no cause, no mechanism and no attribution for any of
       them: the per-file look is what licenses a cause, and it is not this task's to do (D-11).
    6. If list A is above 80, apply the recorded fallback in order (D-13): list what the still-rejected
       files of these four groups now stop at, by message group with counts; fix only what is trivially
       related to a change this phase already made, and if anything is fixed, re-run the whole suite and
       the harness and record both again; hand the rest to the next phase's long tail by message group.
       Pull no group from that phase forward to reach the number. If A is still above 80 after that,
       stop and return a blocking checkpoint as described in Task 3.
    7. Every number in this section must be reproducible from the command, the snapshots and the
       harness report alone.
  </action>
  <acceptance_criteria>
    - `/home/coder/repos/bbj-corpus/conformance/work/details-99-05-before.json` exists and was written before the run.
    - `99-CONFORMANCE.md` carries a Closing run section, a four-row Gate table, an A2 movement section and a B movement section.
    - Each of the four named first-word groups appears in the gate table with its remaining count.
    - The A2 movement claim cites a file-set difference, not a change in totals.
    - The B movement section carries a count and the hand-over line, and no cause, mechanism or attribution for any moved file.
    - If A is above 80, the still-rejected shapes are listed by message group with counts and each is dispositioned as fixed here, accepted residue or handed to the next phase.
    - No corpus file name, corpus path or corpus source line appears anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server</automated>
    <fails_when>non-zero exit, or the run's output carries no falseRejects/falseAlarms/missed counts</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked measurement record</fails_when>
  </verify>
  <done>The final tree has been measured once, the gate table states each gate's verdict with reproducible arithmetic, the A2 and B movements are established by file-set difference, and every unexplained movement is counted and handed over rather than explained away.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion-by-criterion closing attestation, and the conditional stop</name>
  <read_first>
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md (the closing sections written by Task 2)
    - /home/coder/repos/bbj-language-server/.planning/ROADMAP.md, the "Phase 99" block — the five success criteria in the wording the context commit amended
    - /home/coder/repos/bbj-language-server/.planning/REQUIREMENTS.md — PARSE-01, PARSE-02, PARSE-03, PARSE-07 and the out-of-scope table
    - /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/COVERAGE.md (already present — keep it as it is, do not expand it)
  </read_first>
  <files>.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md</files>
  <action>
    Write the attestation and decide whether the phase may seal autonomously.

    1. Append a **Closing attestation** section with one entry per success criterion, in the amended
       wording (D-06). Each entry carries a verdict — holds, partially holds, or does not hold — and
       the named evidence: for criteria 1 to 4 the fixture file name plus the test command that
       asserts it and the still-flagged case that keeps the rule honest; for criterion 5 the closing
       numbers, the four group counts, the fixture list and the B evidence status. Also state, per
       requirement id, which fixture and which test cover it, so the requirement-to-evidence chain is
       readable without re-deriving it.
    2. State explicitly, in one line each, the two things this phase deliberately did not do: no
       blanket reserved-word rule anywhere, and no new editor capability — no hover text, no outline
       entry, no new validation — for either new statement. Name the evidence that each holds (the
       grammar diff being confined to the rules listed in the artifacts table, and the provider files
       being untouched).
    3. Leave `COVERAGE.md` exactly as it is. It already carries the one-line no-external-API
       declaration the seal-time detector needs; expanding it would only give the prose detector more
       to trip on.
    4. **The conditional stop.** If every gate row in Task 2's table reads PASS and B did not rise
       above the Phase 98 recorded 665, finish autonomously — no checkpoint. If ANY gate row is a
       FAIL, or B rose, do not seal: stop and return a blocking human checkpoint using the executor's
       checkpoint return format, reporting `**Gate:** blocking-human`, which gate missed, by how much,
       what the fallback in Task 2 step 6 already tried, and the two options the decision is between
       (accept the shortfall as recorded residue, or hand a named group to the next phase). Do not
       adjust a rule, a check or a fixture to convert a FAIL into a PASS.
  </action>
  <acceptance_criteria>
    - `99-CONFORMANCE.md` carries a Closing attestation section with one entry per success criterion, each with a verdict and named evidence.
    - Each of PARSE-01, PARSE-02, PARSE-03 and PARSE-07 is mapped to its fixture and its test.
    - The two deliberate non-goals are stated with their evidence.
    - `COVERAGE.md` is unchanged: `git -C /home/coder/repos/bbj-language-server status --porcelain -- .planning/phases/99-parser-gaps-the-largest-groups/COVERAGE.md` prints nothing.
    - If any gate row is a FAIL or B rose, the plan returned a blocking human checkpoint instead of sealing, and no source file was touched to move a number.
    - No corpus file name, corpus path or corpus source line appears anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run test/parser-keyword-statements.test.ts test/conformance-regressions.test.ts test/example-files.test.ts</automated>
    <fails_when>non-zero exit, or a "failed" count above 0 in the vitest summary, or the run reports 0 tests collected</fails_when>
    <automated>! grep -qniE 'bbj-corpus/corpus|/corpus/' /home/coder/repos/bbj-language-server/.planning/phases/99-parser-gaps-the-largest-groups/99-CONFORMANCE.md</automated>
    <fails_when>non-zero exit, meaning a corpus path string reached the tracked measurement record</fails_when>
  </verify>
  <done>Every success criterion has a recorded verdict with named evidence, the requirement-to-evidence chain is readable, the deliberate non-goals are stated, and the phase either closes cleanly or stops for a human decision on a missed gate.</done>
</task>

</tasks>

<!-- planner-discipline-allow: PARSE-0|CONF-0|D-[0-9][0-9]|99-[0-9][0-9] -->
<!-- planner-discipline-allow: ^+ -->
<!-- planner-discipline-allow: bbj-corpus/corpus|/corpus/ -->

<threat_model>
ASVS level 1; block on high. This plan changes no source. It reads a private corpus's harness output
and writes counts into tracked planning files.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private corpus -> this public repository | the harness report and its details file describe proprietary customer source; every tracked file in this repository is world-readable |
| this repository -> the harness | the harness loads the language server from source and executes it over the corpus in a local process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-99-05-01 | Information Disclosure | `99-CONFORMANCE.md` closing sections | high | mitigate | Only counts, first-word groups, message groups and own-words shape descriptions are written. The inline set-difference scripts print sizes, never ids or source lines. The corpus directory is never read. Enforced by the corpus-path grep in Tasks 2 and 3 and by the acceptance criteria forbidding a file name anywhere in the record. |
| T-99-05-02 | Repudiation | the gate table and the B movement section | high | mitigate | A cause may not be written for a moved file without a per-file look, and that look is the orchestrator's; this plan records counts and hands them over. A missed gate returns a blocking checkpoint rather than an adjusted rule, so the record cannot claim a pass the tree does not support. |
| T-99-05-03 | Tampering | the measured tree | medium | mitigate | Task 1 asserts the generated parser is no older than the grammar and the source status is clean before the run, so the closing numbers describe the committed tree and not a local edit. |
| T-99-05-04 | Denial of Service | the harness run | low | accept | One local run of about a minute over a fixed corpus, never in CI, with no java-interop contact. |
| T-99-05-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no project package. 99-RESEARCH.md records the Package Legitimacy Audit as not applicable. The only network-touching command is the Node 22 runtime resolution, and only if the generated parser turns out to be stale. |
</threat_model>

<verification>
1. `cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2`
   — judged on `numFailedTests: 0`. Hook-timeout "failed suites" under contention are not failures.
2. `node /home/coder/repos/bbj-corpus/conformance/run.mjs --ls /home/coder/repos/bbj-language-server`
   — run once on the final tree, with a snapshot of the harness's details file taken first.
3. The register check over the phase's whole source diff produces no match.
4. `git -C /home/coder/repos/bbj-language-server status --porcelain` shows nothing under
   `bbj-vscode/src/language/generated/` and no scratch file.
</verification>

<success_criteria>
- The harness has been run once more after the last source change and before any acceptance.
- The gate table states, with reproducible arithmetic: A at or below 80; the remaining count for each
  of the four named first-word groups; A2 at or below 27; B with its evidence status.
- The A2 groups that cleared are named from a file-set difference, not from the totals.
- Every newly-uncaught B file is counted and handed over, with no cause invented.
- Each of the five success criteria has a verdict and named evidence, and each requirement id is
  mapped to its fixture and test.
- Either every gate passes and the phase closes autonomously, or a blocking checkpoint reports the
  missed gate — never a number engineered into a pass.
</success_criteria>

<output>
Create `.planning/phases/99-parser-gaps-the-largest-groups/99-05-SUMMARY.md` when done. Record in it:
the closing numbers and the commit measured; which gates passed and which did not; the A2 groups that
cleared and any that appeared; the newly-uncaught B count handed to the orchestrator; whether the
list-A fallback was applied and what it moved; and anything handed to the next phase, by message
group. Record deviations in the SUMMARY only — do not open a WINDOWS.md ledger entry.
</output>
