---
phase: 109-completion-java-class-resolution
plan: 06
type: execute
wave: 3
depends_on: ["109-01", "109-02", "109-03", "109-04", "109-05"]
files_modified:
  - bbj-vscode/test/functional/java-class-lookups-real-interop.test.ts
  - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md
autonomous: true
requirements: [COMP-03, JINT-01, JINT-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Against the real java-interop backend on :5008, a cold start with debug logging (workspace initialization and implicit imports) sends no getClassInfo request and writes no `Resolving class` debug line for a primitive, `void`, array or blank name (ROADMAP criterion 4; D-07)"
    - "In the same cold start no class is requested under two spellings, and `java.util.AbstractMap.SimpleEntry` requested dotted and with `$` is fetched at most once and resolves to one object with its members (ROADMAP criterion 5; D-09, D-10)"
    - "The live test is skipped where :5008 is closed (as in CI); when it cannot run here, the SUMMARY says the live evidence was not captured and the counting-double tests from 109-04 and 109-05 stand as the automated proof"
    - "The whole vitest suite reports numFailedTests=0 with RUN_BBJ_TESTS=0 --maxWorkers=2, and with RUN_BBJ_TESTS=1 the live-gated files fail on no test name that passes on the phase base (a scratch worktree at the parent of the first commit tagged 109-0N); a new name stops the plan as a regression, never as environment noise"
    - "The phase's source and test diff carries no planning identifier (register check)"
    - "109-COMP03-MEASUREMENT.md gets a Final state table from the same recorder, compared row by row with the before-fix table, so every position's before and after verdict is on record (D-03)"
    - "Issue #561 is handled exactly as the maintainer chose at 109-01's checkpoint, with the recorded comment text; any position still out of reach is listed in the comment and #561 stays open (D-02)"
  artifacts:
    - path: "bbj-vscode/test/functional/java-class-lookups-real-interop.test.ts"
      provides: "cold-start lookup evidence against the real backend, gated by shouldRunBBjTests"
      contains: "shouldRunBBjTests"
    - path: ".planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md"
      provides: "Final state table and before/after comparison"
      contains: "## Final state"
  key_links:
    - from: "java-class-lookups-real-interop.test.ts"
      to: "JavaInteropService.getRawClass on :5008"
      via: "vi.spyOn call-through recording every requested spelling during initializeWorkspace/loadImplicitImports"
      pattern: "getRawClass"
  prohibitions:
    - requirement_id: JINT-01
      category: confidentiality
      statement: "MUST NOT copy local classpath contents, jar paths or customer class names from the live run into the repository, a SUMMARY or a commit message; report counts and JDK class names only"
      flagged: true
    - requirement_id: COMP-03
      category: trust
      statement: "MUST NOT post to or change issue #561 beyond the maintainer's recorded choice and text (D-02)"
      flagged: true
    - requirement_id: JINT-02
      category: trust
      statement: "MUST NOT relabel a failing test as environment noise without comparing failing names against the phase base"
      flagged: true
---

<objective>
Close the phase: prove criteria 4 and 5 against the real backend where it is reachable, run the phase
regression gate against the base commit, record the COMP-03 final state next to the before-fix
measurement, and carry out the maintainer's #561 choice.

Purpose: the unit tests use a counting double; a real cold start is the evidence the roadmap names, and
the regression gate must compare failing test names with the base, not counts.
Output: `test/functional/java-class-lookups-real-interop.test.ts`, the Final state section of
`109-COMP03-MEASUREMENT.md`, the #561 action, and gate results in the SUMMARY.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/109-completion-java-class-resolution/109-CONTEXT.md
@.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md
@.planning/phases/109-completion-java-class-resolution/109-01-SUMMARY.md
@.planning/phases/109-completion-java-class-resolution/109-04-SUMMARY.md
@.planning/phases/109-completion-java-class-resolution/109-05-SUMMARY.md
@bbj-vscode/test/functional/issue440-real-interop.test.ts

<interfaces>
- test/test-helper.ts: `shouldRunBBjTests()` (RUN_BBJ_TESTS=1 forces on, 0 off, unset → open :5008), `initializeWorkspace(shared)`.
- functional/issue440-real-interop.test.ts: the live pattern (`createBBjServices(NodeFileSystem)`, JavadocProvider init, `interop.setConnectionConfig('127.0.0.1', 5008)`, `initializeWorkspace`, warm-up polling, `test.runIf(run)`, long timeouts).
- java-interop.ts after 109-04/05: exported `isLocalJavaTypeName(name)`, `canonicalJavaClassName(name)`, `JAVA_PRIMITIVE_TYPE_NAMES`; `loadImplicitImports(token?)`; `getResolvedClass(name)` answers either spelling; the protected `getRawClass(className, token?)` is the only request site; `resolveClass` writes `[debug] Resolving class NAME: …` through `console.log` when the logger level is DEBUG (`logger.setLevel(LogLevel.DEBUG)` from `src/language/logger.js`; default WARN).
- Local baseline with RUN_BBJ_TESTS=1: linking.test.ts fails 11 interop tests by name (test-double drift, see STATE.md Blockers); issue447 is backend-agnostic since 97-03. Any other failing name is a regression until the base comparison says otherwise.
- `completion-method-body.test.ts` (109-01) appends one JSON line per row to the file named by `MEASURE_COMPLETION_OUT`.
- The #561 decision and the final comment text are in `109-COMP03-MEASUREMENT.md` under `## #561 decision` and in 109-01-SUMMARY.md: one of `comment-and-close`, `comment-close-at-release`, `keep-open-with-list`, `grammar-followup`.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`. Allowed
  `cd` forms: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …` and
  `cd /tmp/phase-109-base-wt/bbj-vscode && npx …`. Use `git -C` for git. Stage with `git add <exact path>`
  only. Never use `git stash`.
- vitest never uses the `basic` reporter. Whole-suite runs use `RUN_BBJ_TESTS=0 --maxWorkers=2`, judged on
  `numFailedTests`; "failed suites" with numFailedTests 0 are hook timeouts under contention.
- The scratch worktree for the base comparison is `/tmp/phase-109-base-wt`: `git -C … worktree add --detach`,
  symlink `bbj-vscode/node_modules` from the main checkout, copy `bbj-vscode/src/language/generated` from the
  main checkout when `bbj.langium` is unchanged between base and HEAD (otherwise run
  `npm run langium:generate` there under Node 22), remove it with `git -C … worktree remove --force` at the
  end.
- Source and test text must not contain planning identifiers (D-NN, COMP-/JINT- ids, 109-0N, CR-/WR-/IN-NN,
  T-109-NN). Issue numbers are fine. Commit messages carry no GitHub closing keyword followed by an issue
  number.
- Do not edit source files in this plan. A regression found here stops the plan and is reported; the
  orchestrator routes it to gap closure.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md.

## Artifacts this phase produces

- (109-01) `test/completion-method-body.test.ts`; `109-COMP03-MEASUREMENT.md`; the un-skipped DEF FN test.
- (109-02) the `MemberCall` class-reference branch in `bbj-scope.ts`; `test/completion-class-reference.test.ts`.
- (109-03) `argumentTypeOf`, `OverloadCandidate`, `overloadCandidates`, `bestOverloadCandidates`;
  overload re-selection in `bbj-type-inferer.ts`; `test/overload-return-type.test.ts`.
- (109-04) `JAVA_PRIMITIVE_TYPE_NAMES`, `isLocalJavaTypeName`, `localJavaTypeDto`, the local branch;
  `test/counting-java-interop.ts`; `test/java-interop-local-types.test.ts`.
- (109-05) `canonicalJavaClassName` and canonical keys; `test/java-interop-nested-class-names.test.ts`.
- (this plan) `test/functional/java-class-lookups-real-interop.test.ts`; the `## Final state` section of
  `109-COMP03-MEASUREMENT.md`; the #561 comment/state per the recorded choice; gate results in the SUMMARY.

<tasks>

<task type="tracer">
  <name>Task 1: A real cold start against :5008 sends no primitive, array or duplicate-spelling lookups</name>
  <files>bbj-vscode/test/functional/java-class-lookups-real-interop.test.ts</files>
  <read_first>
    - bbj-vscode/test/functional/issue440-real-interop.test.ts (whole file)
    - bbj-vscode/test/functional/unknown-java-member-real-interop.test.ts (lines 1-80, the implicit-import warm-up)
    - bbj-vscode/src/language/java-interop.ts (lines 438-476, 655-700, and the resolveClassByName/resolveClass entry after 109-05)
    - bbj-vscode/src/language/logger.ts
  </read_first>
  <action>
Capture criteria 4 and 5 on a real cold start, with the same assertions a human would make on the log.

1. Check the backend: `timeout 3 bash -c '</dev/tcp/127.0.0.1/5008'`. When it fails, still write and commit the test (it skips), and state in the SUMMARY that the live evidence was not captured because :5008 was unreachable; skip step 4's live run.
2. Create `bbj-vscode/test/functional/java-class-lookups-real-interop.test.ts` following issue440-real-interop.test.ts: `const run = await shouldRunBBjTests()`, `createBBjServices(NodeFileSystem)`, a `beforeAll` (returning early when not run, timeout 300000) that initializes JavadocProvider, calls `setConnectionConfig('127.0.0.1', 5008)`, sets `logger.setLevel(LogLevel.DEBUG)`, spies `console.log` to collect lines (suppressing output), spies the protected `getRawClass` with `vi.spyOn` through a structural cast so calls still go through while every requested spelling is recorded, then runs `initializeWorkspace(services.shared)`, `loadImplicitImports()` when `getResolvedClass('BBjAPI')` is still undefined, and finally `resolveClassByName('java.util.AbstractMap.SimpleEntry')` and `resolveClassByName('java.util.AbstractMap$SimpleEntry')`. `afterAll` restores the spies and sets the level back to WARN.
3. Tests (`test.runIf(run)`, 60 s timeout each):
   - 'sends no request and logs no lookup for primitive, void, array or blank names': no recorded spelling satisfies `isLocalJavaTypeName`, and no collected line starts with `[debug] Resolving class ` followed by such a name; also at least one request and one `Resolving class` line exist (the spies worked).
   - 'never requests one class under two spellings': grouping recorded spellings by `canonicalJavaClassName` gives exactly one distinct spelling per group (the same spelling twice, from an LRU eviction or a retried failure, is allowed).
   - 'resolves a nested class once for both spellings': the canonical key `java.util.AbstractMap.SimpleEntry` was requested at most once, `getResolvedClass` returns the same object for both spellings, and it has a method named `getKey`.
4. Run it with `RUN_BBJ_TESTS=1`. Record in the SUMMARY the number of requests, the number of `Resolving class` lines and the result of each test; name only JDK classes, never local classpath contents.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=1 npx vitest run test/functional/java-class-lookups-real-interop.test.ts</automated>
    <fails_when>non-zero exit or a "failed" count above 0; when the :5008 probe in step 1 succeeded, also a summary that shows every test skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "shouldRunBBjTests" /home/coder/repos/bbj-language-server/bbj-vscode/test/functional/java-class-lookups-real-interop.test.ts` prints 1 or more, and the same for `isLocalJavaTypeName`, `canonicalJavaClassName` and `LogLevel.DEBUG`
    - With :5008 reachable, all three tests report passed (not skipped); otherwise the SUMMARY records why the live evidence is missing
    - `RUN_BBJ_TESTS=0 npx vitest run test/functional/java-class-lookups-real-interop.test.ts` exits 0 with the tests skipped (CI behaviour)
    - After `git add` of this task's file and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>A live cold start shows no primitive/void/array lookups and no two-spelling pair, or the SUMMARY says why it could not run here.</done>
</task>

<task type="auto">
  <name>Task 2: Phase regression gate against the base commit, register check, and the COMP-03 final state</name>
  <files>.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md (whole file)
    - .planning/STATE.md (Blockers/Concerns: the test-harness false positive and the local baseline)
    - bbj-vscode/test/test-helper.ts
  </read_first>
  <action>
Judge the phase on names, not counts, and close the measurement record.

1. Whole suite on HEAD: `RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-109-06-vitest.json`; it must report numFailedTests=0.
2. Resolve the base: `B` = parent of the first commit whose message contains `(109-0` followed by a plan number (the register check's own resolution). Create the scratch worktree per the executor rules.
3. Live-gated files on both trees with `RUN_BBJ_TESTS=1 --maxWorkers=1 --reporter=json`: `test/linking.test.ts`, `test/functional/issue440-real-interop.test.ts`, `test/functional/issue447-real-interop.test.ts`, `test/functional/unknown-java-member-real-interop.test.ts` into `/tmp/phase-109-head-live.json` (main checkout) and `/tmp/phase-109-base-live.json` (worktree). The failing full names on HEAD must be a subset of those on the base. A HEAD-only failing name is a regression: bisect it across the phase's source commits in the worktree, report it, and stop.
4. Run the register check over the whole phase diff (second verify command).
5. COMP-03 final state: `rm -f /tmp/phase-109-method-body-final.jsonl`, run `completion-method-body.test.ts` with `MEASURE_COMPLETION_OUT=/tmp/phase-109-method-body-final.jsonl`, and fill `## Final state` in the record: the same table for HEAD, then a comparison line per row (before verdict → final verdict) and the list of positions still out of reach with their reasons (or none).
6. Remove the worktree, commit the record, and put the gate results (numFailedTests, the two failing-name sets, register result) in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && RUN_BBJ_TESTS=0 npx vitest run --maxWorkers=2 --reporter=json --outputFile=/tmp/phase-109-06-vitest.json; node -e "const r=require('/tmp/phase-109-06-vitest.json'); console.log('numFailedTests=' + r.numFailedTests + ' numTotalTests=' + r.numTotalTests); process.exit(r.numFailedTests === 0 && r.numTotalTests > 0 ? 0 : 1)"</automated>
    <fails_when>the node step exits non-zero, prints numFailedTests above 0 or numTotalTests=0, or cannot read /tmp/phase-109-06-vitest.json</fails_when>
    <automated>B=$(git -C /home/coder/repos/bbj-language-server rev-parse "$(git -C /home/coder/repos/bbj-language-server log --reverse --format=%H --grep='(109-0[1-9])' | head -1)^") && ! git -C /home/coder/repos/bbj-language-server diff "$B" HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b' && echo register-clean</automated>
    <fails_when>"register-clean" is not printed: the base could not be resolved, or an added source or test line carries a planning identifier</fails_when>
    <automated>node -e "const f=p=>new Set(require(p).testResults.flatMap(r=>r.assertionResults).filter(a=>a.status==='failed').map(a=>a.fullName)); const b=f('/tmp/phase-109-base-live.json'); const h=f('/tmp/phase-109-head-live.json'); const extra=[...h].filter(n=>!b.has(n)); console.log('base-failing=' + b.size + ' head-failing=' + h.size + ' head-only=' + extra.length); extra.forEach(n=>console.log('HEAD-ONLY: ' + n)); process.exit(extra.length===0?0:1)"</automated>
    <fails_when>non-zero exit: a JSON file is missing, or a "HEAD-ONLY:" line names a live-gated test that fails on HEAD but passes on the base</fails_when>
  </verify>
  <acceptance_criteria>
    - Whole suite prints `numFailedTests=0`; register check prints `register-clean`; the live comparison prints `head-only=0`
    - `grep -c "## Final state" /home/coder/repos/bbj-language-server/.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md` prints 1 and the section holds one table row per matrix row plus the before → final comparison
    - `git -C /home/coder/repos/bbj-language-server worktree list` no longer lists `/tmp/phase-109-base-wt`
    - The SUMMARY lists both failing-name sets (or their counts when identical) and the phase base commit
  </acceptance_criteria>
  <done>The suite is green, no live-gated test newly fails against the base, the diff is register-clean, and the COMP-03 record shows before and final side by side.</done>
</task>

<task type="auto">
  <name>Task 3: Carry out the maintainer's #561 choice</name>
  <files>.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md (## #561 decision and ## Final state)
    - .planning/phases/109-completion-java-class-resolution/109-01-SUMMARY.md
  </read_first>
  <action>
Do exactly what the recorded option says, with the recorded text, per D-02.

1. Read the option id and comment text under `## #561 decision`. If the Final state from Task 2 differs from what the comment says (a position changed verdict), stop and report instead of posting.
2. `comment-and-close`: write the text to `/tmp/phase-109-561-comment.md`, run `gh issue comment 561 -R BBx-Kitchen/bbj-language-server --body-file /tmp/phase-109-561-comment.md`, then `gh issue close 561 -R BBx-Kitchen/bbj-language-server --reason completed`.
   `comment-close-at-release`: post the comment the same way and do not close; add one line to the record that the release pull request closes #561.
   `keep-open-with-list`: post the comment (it lists every out-of-reach position and its reason) and leave the issue open.
   `grammar-followup`: post nothing; report that the grammar follow-up must run (and pass the D-04 harness gate) before #561 is handled.
3. Append the result under `## #561 decision` (posted comment URL from `gh`, final issue state, date) and commit the record.
  </action>
  <verify>
    <automated>gh issue view 561 -R BBx-Kitchen/bbj-language-server --json state,comments --jq '.state + " comments=" + (.comments | length | tostring)'</automated>
    <fails_when>non-zero exit (gh not authenticated or issue unreachable), or the printed state does not match the recorded option (CLOSED only for comment-and-close)</fails_when>
  </verify>
  <acceptance_criteria>
    - The record's `## #561 decision` section names the option, the posted comment URL (or "nothing posted" for grammar-followup) and the resulting issue state
    - The issue state shown by `gh issue view` matches the option: CLOSED for comment-and-close, OPEN otherwise
    - No commit message of this phase contains a GitHub closing keyword followed by `#561`: `! git -C /home/coder/repos/bbj-language-server log --format=%B "$(git -C /home/coder/repos/bbj-language-server log --reverse --format=%H --grep='(109-0[1-9])' | head -1)^..HEAD" | grep -iE '\b(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9]+'` exits 0
  </acceptance_criteria>
  <done>#561 carries the maintainer's chosen comment and state, and the record says what was done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live backend (:5008) → test output | class names and counts from the real classpath reach the executor |
| language server project → public issue tracker | #561 comment and state are public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-13 | Information disclosure | live-run SUMMARY | low | mitigate | Only counts and JDK class names are recorded; no local classpath contents, jar paths or customer class names. |
| T-109-14 | Repudiation | #561 action | low | mitigate | Only the option and text the maintainer chose at 109-01's blocking-human checkpoint are posted; the record keeps the URL and state; a changed final state stops the post. |
| T-109-15 | Tampering (release hygiene) | commit messages | low | mitigate | A log scan rejects closing keywords before the squash merge can close issues early. |
| T-109-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- Live cold-start test passes where :5008 is reachable (or its absence is recorded); skipped with RUN_BBJ_TESTS=0
- Whole suite numFailedTests=0; live-gated failing names ⊆ base; register-clean
- COMP-03 record has before and final states; #561 matches the recorded choice
</verification>

<success_criteria>
- ROADMAP criteria 4 and 5 hold on a real cold start (or the unit proof stands with the reason recorded)
- ROADMAP criterion 3's record is complete, and the phase shows no regression by test name against its base
</success_criteria>

<output>
Create `.planning/phases/109-completion-java-class-resolution/109-06-SUMMARY.md` when done
</output>
