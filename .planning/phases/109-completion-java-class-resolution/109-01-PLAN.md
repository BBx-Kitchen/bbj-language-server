---
phase: 109-completion-java-class-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bbj-vscode/test/completion-method-body.test.ts
  - bbj-vscode/test/completion-test.test.ts
  - bbj-vscode/src/language/bbj-completion-provider.ts
  - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md
autonomous: false
requirements: [COMP-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Before any completion-provider or grammar change of this phase, 109-COMP03-MEASUREMENT.md in the phase directory records the D-03 matrix: statement start, after `=`, a PRINT argument, a function-call argument, member access after `.`, inside an IF and inside a FOR nested in a method, and a DEF FN inside a method (the skipped test's scenario), each measured in a class method body and at program scope as the control, with label counts, the control labels missing in the method, and a verdict per row, plus the commit it measured (D-03)"
    - "The verbatim skipped test 'DEF FN parameters with $ suffix inside class method' was run un-skipped against the unmodified tree and its result is in the record (D-03)"
    - "Completion in an empty method body (only METHOD and METHODEND) offers candidates"
    - "Measurement and tests compare candidate label sets, never positions in the list"
    - statement: "Candidates are offered at the first statement line directly after METHOD and the last line directly before METHODEND, not only mid-body"
      verification: backstop
    - "Every position the measurement found working is pinned by a committed label-set test in test/completion-method-body.test.ts, and the DEF FN test in completion-test.test.ts runs un-skipped with its original assertions when the measurement found it passing (ROADMAP criterion 3)"
    - "Every position found broken is either fixed inside bbj-completion-provider.ts and pinned by a test, or kept as a test.skip whose label states why it stays out of reach and listed in the record and the SUMMARY; no Langium patch, fork or upstream PR (D-01, D-02)"
    - "A position that only a bbj.langium change could fix is not changed in this plan: it is recorded with the proposed contained grammar change and routed through the checkpoint, because a grammar change must first pass the private corpus harness gate (D-04)"
    - "The maintainer chooses how issue #561 is handled at a blocking checkpoint; nothing is posted to GitHub from this plan, and plan 109-06 carries out the recorded choice (D-02)"
    - "Every fixture is synthetic with invented names, and every control fixture has a statement after the cursor line (a marker as the last content before end of file reads as falsely broken)"
  artifacts:
    - path: ".planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md"
      provides: "before-fix measurement record, verdicts, #561 decision; a Final state section filled by 109-06"
      contains: "## Before-fix measurement"
    - path: "bbj-vscode/test/completion-method-body.test.ts"
      provides: "the D-03 position matrix, an env-gated measurement recorder and one label-set pin per working row"
      contains: "MEASURE_COMPLETION_OUT"
    - path: "bbj-vscode/test/completion-test.test.ts"
      provides: "the DEF FN `_f$`/`_t$` test running un-skipped (when measured passing)"
  key_links:
    - from: "bbj-vscode/test/completion-method-body.test.ts"
      to: "BBjCompletionProvider.getCompletion"
      via: "createBBjTestServices(EmptyFileSystem) + parseHelper with a unique documentUri per request, Invoked trigger"
      pattern: "CompletionProvider!?\\.getCompletion"
  prohibitions:
    - requirement_id: COMP-03
      category: scope
      statement: "MUST NOT patch, fork or open an upstream PR against Langium; a position that needs one is recorded as out of reach (D-01)"
      flagged: true
    - requirement_id: COMP-03
      category: trust
      statement: "MUST NOT comment on, close or reopen GitHub issue #561 from this plan; the maintainer decides at the checkpoint (D-02)"
      flagged: true
    - requirement_id: COMP-03
      category: trust
      statement: "MUST NOT weaken the skipped test's original assertions (both `_f$` and `_t$` offered, no truncated `_f`/`_t`) to make it pass"
      flagged: true
    - requirement_id: COMP-03
      category: confidentiality
      statement: "MUST NOT use corpus or customer BBj source text in fixtures, tests or the measurement record; fixtures stay synthetic (D-04)"
      flagged: true
  assumptions:
    - statement: "Research's informal probe found every D-03 position already working on the current tree, so the expected outcome is measure, pin and un-skip; if the formal measurement disagrees, Task 2's fix-or-record branch applies"
      source: "109-RESEARCH.md Pattern 3, Assumption A3"
      flagged: true
    - statement: "When no position stays out of reach, D-02's keep-open-with-a-list clause does not apply; whether #561 is closed now, at release, or kept open is the maintainer's choice at the checkpoint"
      source: "109-RESEARCH.md Open Question 1; orchestrator instruction"
      flagged: true
---

<objective>
Measure completion inside class method bodies before touching anything (D-03), record the result in the
phase directory, then pin every working position with a label-set test, un-skip the DEF FN `_f$`/`_t$`
test when it passes, fix or record any broken position within D-01's budget, and put the handling of
issue #561 to the maintainer (D-02).

Purpose: issue #561's recorded root cause is seven months old and research could not reproduce it. A
formal before/after record is the only way to say which positions were broken, and the pins keep them
working.
Output: `test/completion-method-body.test.ts`, `109-COMP03-MEASUREMENT.md`, the un-skipped test, and a
recorded #561 decision.
</objective>

<execution_context>
@~/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@~/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-completion-java-class-resolution/109-CONTEXT.md
@.planning/phases/109-completion-java-class-resolution/109-RESEARCH.md
@bbj-vscode/test/completion-test.test.ts
@bbj-vscode/test/bbj-test-module.ts

<interfaces>
- `createBBjTestServices(EmptyFileSystem).BBj` (test/bbj-test-module.ts) gives services with the fake Java classes `java.lang.String` (static field `CASE_INSENSITIVE_ORDER`, instance field `someInstanceField`, instance method `charAt`), `java.util.HashMap` (`put`, `getClass`), `java.lang.Class` (`getName`), `BBjAPI`, `com.test.SysGui`.
- `BBj.lsp.CompletionProvider!.getCompletion(document, params: CompletionParams, cancelToken?)` returns `CompletionList | undefined`; labels are `list.items.map(i => i.label)`. completion-test.test.ts lines 21-70 show the drivers (`fieldCompletion`, `useDotCompletion`, `useInvokedCompletion`): `parseHelper<Model>(bbjServices)(clean, { documentUri: 'file:///<unique>.bbj' })`, `doc.textDocument.positionAt(offset)`, `context: { triggerKind: CompletionTriggerKind.Invoked }` (or TriggerCharacter with `'.'`).
- The skipped test is at completion-test.test.ts:186-215 (`test.skip('DEF FN parameters with $ suffix inside class method', …)`), preceded by a stale comment block claiming 0 items inside method bodies; its assertion: exactly one `_f$` and one `_t$` item, no `_f`/`_t` item.
- BBj block syntax for fixtures: `class public Name` … `classend`; `method public void run()` … `methodend`; `IF cond THEN` newline … `FI`; `FOR i = 1 TO 3` newline … `NEXT i`; `DEF fnName(_f$,_t$)` newline … `FNEND`; `declare java.lang.String s!`.
- `BBjCompletionProvider.computeCompletion(document, params, cancelToken)` (bbj-completion-provider.ts ~270-318) already contains recovery logic for positions inside a class/method rule; any provider fallback belongs there or in `getCompletion`.
</interfaces>
</context>

## Executor rules (apply to every task)

- Shell: absolute paths only. Never chain `cd` with `grep`, `cat`, `find`, `sed`, `head` or `tail`. Allowed
  `cd` form: `cd /home/coder/repos/bbj-language-server/bbj-vscode && npx …`. Use `git -C` for git. Stage
  with `git add <exact path>` only. Never use `git stash`, and never run `git restore`/`git checkout` on a
  directory pathspec.
- vitest runs with cwd `bbj-vscode`; never the `basic` reporter. Tests parse with `parseHelper`, never
  `DocumentBuilder.build`.
- Source and test text must not contain planning identifiers (D-NN, COMP-/JINT- ids, 109-0N, CR-/WR-/IN-NN,
  T-109-NN). Issue numbers such as #561 are fine.
- Commit messages must not contain a GitHub closing keyword followed by an issue number (close, closes,
  closed, fix, fixes, fixed, resolve, resolves, resolved); write "issue #561" instead. A squash merge
  would otherwise close the issue early.
- Do not post anything to GitHub in this plan. Do not change `bbj.langium`; a grammar-only fix is routed
  through Task 3.
- Record deviations in this plan's SUMMARY.md only, never in WINDOWS.md. Do not mark COMP-03 complete in
  REQUIREMENTS.md; plan 109-06 closes it.
- A failing test elsewhere is compared with the phase base (the parent of the first commit tagged
  `(109-0N)`) before it is called pre-existing; never label it environment noise without that comparison.

## Artifacts this phase produces

- (this plan) `bbj-vscode/test/completion-method-body.test.ts` (position matrix, recorder gated by
  `MEASURE_COMPLETION_OUT`, label-set pins); `109-COMP03-MEASUREMENT.md`; the un-skipped DEF FN test in
  `completion-test.test.ts`; a provider fallback in `bbj-completion-provider.ts` only if a position is
  broken and fixable there.
- (109-02) class-reference detection for a `MemberCall` receiver in `bbj-scope.ts`;
  `test/completion-class-reference.test.ts`.
- (109-03) `argumentTypeOf`, `OverloadCandidate`, `overloadCandidates`, `bestOverloadCandidates` in
  `bbj-overload-selector.ts`; overload re-selection in `bbj-type-inferer.ts`; the inlay provider delegating
  to `argumentTypeOf`; `test/overload-return-type.test.ts`.
- (109-04) `JAVA_PRIMITIVE_TYPE_NAMES`, `isLocalJavaTypeName`, `localJavaTypeDto` and the local branch in
  `resolveClassByName` (`java-interop.ts`); `test/counting-java-interop.ts`;
  `test/java-interop-local-types.test.ts`.
- (109-05) `canonicalJavaClassName` and canonical keys in `java-interop.ts`;
  `test/java-interop-nested-class-names.test.ts`.
- (109-06) `test/functional/java-class-lookups-real-interop.test.ts`; the Final state section of
  `109-COMP03-MEASUREMENT.md`; the #561 action; the phase regression gate.

<tasks>

<task type="tracer">
  <name>Task 1: Measure every D-03 position against its program-scope control and record it before any fix</name>
  <files>bbj-vscode/test/completion-method-body.test.ts, .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</files>
  <read_first>
    - bbj-vscode/test/completion-test.test.ts (lines 1-70 drivers, 186-215 the skipped test, 217-240 the single-line DEF FN test, 533-552 the issue #440 test)
    - bbj-vscode/src/language/bbj-completion-provider.ts (lines 253-320 getCompletion and computeCompletion, read only)
    - bbj-vscode/test/bbj-test-module.ts (lines 61-85 and 429-473, the fake String class)
    - .planning/phases/109-completion-java-class-resolution/109-RESEARCH.md (Pattern 3 and Pitfall 2)
    - .planning/phases/109-completion-java-class-resolution/109-CONTEXT.md (D-01 to D-04)
  </read_first>
  <action>
Run the D-03 measurement end to end: matrix, recorder, raw results, record file. No provider, grammar or
existing-test change in this task.

1. Confirm nothing has changed the completion engine yet: `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/src/language/bbj-completion-provider.ts bbj-vscode/src/language/bbj.langium` prints nothing and `git -C /home/coder/repos/bbj-language-server log --format=%H --grep='(109-01)' -- bbj-vscode/src` prints nothing. If either prints something, stop and report.
2. Create `bbj-vscode/test/completion-method-body.test.ts`. Services: `createBBjTestServices(EmptyFileSystem).BBj`. A helper `labelsAt(text: string, trigger?: '.')` that strips the `<|>` marker, parses with a unique `documentUri` per call, requests completion at the marker (Invoked, or TriggerCharacter `.` when asked) and returns the labels as a `Set<string>`. A helper `inMethod(lines: string[])` that wraps body lines in `class public MethodBodyProbe` / `method public void run()` … `methodend` / `classend`; the control is the same lines at program scope. Export nothing.
3. Define the matrix as a `const` array of rows `{ name, body: string[], control?: string[], expected?: string[], absent?: string[], trigger?: '.' }` with these rows, all names invented and identical in both variants, and a statement after the marker line in the control of every row:
   - 'statement start': `probeVar = 1`, the marker line, `probeTail = 2`; expected includes `probeVar`
   - 'after =': `probeVar = 1`, `probeOut = <|>`, `probeTail = 2`
   - 'PRINT argument': `probeVar = 1`, `PRINT <|>`, `probeTail = 2`
   - 'function argument': `probeVar = 1`, `probeOut$ = STR(<|>)`, `probeTail = 2`
   - 'member after .': `declare java.lang.String probeStr!`, `probeStr!.<|>`, `probeTail = 2`, trigger `.`; expected includes `charAt`
   - 'inside IF': `probeVar = 1`, `IF probeVar > 0 THEN`, the marker line, `FI`, `probeTail = 2`
   - 'inside FOR': `FOR probeI = 1 TO 3`, the marker line, `NEXT probeI`, `probeTail = 2`
   - 'DEF FN in a method': `DEF fnIsText(_f$,_t$)`, `PRINT <|>`, `FNEND` (the skipped test's own body); expected `_f$` and `_t$`; absent `_f` and `_t`
   - 'first line after METHOD': the marker line first, then `probeTail = 2`
   - 'last line before METHODEND': `probeVar = 1`, the marker line last in the method; the control adds `probeTail = 2` after the marker
   - 'empty method body': only the marker line in the method; the control is the marker line then `probeTail = 2`
4. A pure function `verdict(inMethod: Set<string>, control: Set<string>, row)` returning `{ missingInMethod: string[] (sorted control labels absent in the method), verdict: 'works' | 'broken' | 'control-empty' }`: `works` when the method set is non-empty, contains every control label and every `expected` label, and no `absent` label; `control-empty` when the control set is empty (a fixture error: fix the control fixture and re-run, never record it as a measurement); `broken` otherwise. It compares sets only, never item order. If a control label is legitimately scope-specific (for example a keyword only valid at program scope), add it to a per-row `allow` list and justify it in the record; the default list is empty.
5. Recorder: `describe.runIf(!!process.env.MEASURE_COMPLETION_OUT)('method body completion measurement', …)` with one test per row that computes both label sets and appends one JSON line `{ row, inMethodCount, controlCount, missingInMethod, expectedInMethod, expectedInControl, verdict }` to the file named by `MEASURE_COMPLETION_OUT` (`appendFileSync` from `node:fs`); no assertions there.
6. Pins: `describe('completion inside class method bodies (issue #561)', …)` with one test per row that asserts `verdict(...).verdict === 'works'` and the row's expected/absent labels. Run the recorder first (step 7); any row whose verdict is not `works` is written as `test.skip('<row name>: broken before any fix, see the phase measurement', …)` for now, so the suite stays green until Task 2 resolves it.
7. Measure: `rm -f /tmp/phase-109-method-body-before.jsonl`, then run the file with `MEASURE_COMPLETION_OUT=/tmp/phase-109-method-body-before.jsonl`.
8. Measure the verbatim skipped test: check `git -C /home/coder/repos/bbj-language-server status --porcelain -- bbj-vscode/test/completion-test.test.ts` is empty, change only the `test.skip(` of the DEF FN test at line 186 to `test(`, run `npx vitest run test/completion-test.test.ts` and note the DEF FN test's result and the file's passed/failed counts, then put the file back with `git -C /home/coder/repos/bbj-language-server checkout -- bbj-vscode/test/completion-test.test.ts`.
9. Write `.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md` with: a `## Before-fix measurement` heading; the date, `git rev-parse HEAD`, and the statement from step 1; the re-run command; a table `| Row | In method | Control | Control labels missing in method | Verdict |` (separator row written `| --- | --- | --- | --- | --- |`) with one line per matrix row from the JSON file; any `allow` entries with their justification; the verbatim skipped-test result; a `## Broken positions` list (or the word none); an empty `## #561 decision` heading and an empty `## Final state` heading (filled by Task 3 and plan 109-06).
10. Run the new file without the env var (pins only), then commit the test file and the record together.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/completion-method-body.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>test "$(grep -c . /tmp/phase-109-method-body-before.jsonl)" -ge 11 && grep -c "## Before-fix measurement" /home/coder/repos/bbj-language-server/.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</automated>
    <fails_when>non-zero exit (fewer than 11 recorded rows, missing JSON file, or no Before-fix heading in the record)</fails_when>
  </verify>
  <acceptance_criteria>
    - `/tmp/phase-109-method-body-before.jsonl` holds one JSON line per matrix row (11 or more), each with a `verdict` field; no row has verdict `control-empty`
    - `grep -c "MEASURE_COMPLETION_OUT" /home/coder/repos/bbj-language-server/bbj-vscode/test/completion-method-body.test.ts` prints 1 or more
    - The record names the measured commit, states that `bbj-completion-provider.ts` and `bbj.langium` had no change of this phase at measurement time, lists every row with counts and verdict, and states the verbatim DEF FN test result
    - `git -C /home/coder/repos/bbj-language-server diff --stat HEAD~1 HEAD -- bbj-vscode/src` prints nothing (the measurement commit touches no source)
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>The D-03 matrix exists as a committed test file with an env-gated recorder, the before-fix results and the verbatim skipped-test result are committed in the phase directory, and no source file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin every working position, un-skip the DEF FN test, and fix or record any broken one</name>
  <files>bbj-vscode/test/completion-method-body.test.ts, bbj-vscode/test/completion-test.test.ts, bbj-vscode/src/language/bbj-completion-provider.ts, .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md (after Task 1)
    - bbj-vscode/test/completion-method-body.test.ts (after Task 1)
    - bbj-vscode/test/completion-test.test.ts (lines 180-216)
    - bbj-vscode/src/language/bbj-completion-provider.ts (lines 217-320; only when a row is broken)
  </read_first>
  <behavior>
    - Every row with verdict `works` in the record is a passing, non-skipped pin in completion-method-body.test.ts
    - 'DEF FN parameters with $ suffix inside class method' runs as a normal test with its original assertions (exactly one `_f$` and one `_t$`, no `_f`/`_t`) when the record shows it passed
    - A row fixed in the provider flips from skip to a passing pin, and the same row's control still passes
    - A row that stays out of reach remains `test.skip` with a label stating the reason in plain words
  </behavior>
  <action>
Close each measured position, following exactly one branch per row.

1. Branch A (expected; every row `works` and the verbatim test passed): in `completion-test.test.ts` change the DEF FN test from `test.skip(` to `test(`, keep its name, text and assertions exactly, and delete the stale comment block inside it that describes the old 0-items limitation and the future-fix note. Nothing else in the file changes.
2. Branch B (a row is broken and the program-scope control works): add the smallest fallback in `BBjCompletionProvider` (in `computeCompletion` or `getCompletion`) that produces candidates when the default grammar follower yields none for a position inside a `MethodDecl` body, per D-01 (for example re-running the default completion from the enclosing statement's start the way the existing class/method recovery does). Only `bbj-completion-provider.ts` changes. Turn the row's skip into a pin, re-run the whole completion-test.test.ts and completion-method-body.test.ts, and un-skip the DEF FN test once it passes.
3. Branch C (a row can only be fixed by changing `bbj.langium`, or by changing Langium itself): do not change either. Keep the row as `test.skip('<row name>: out of reach, <reason in plain words>', …)`; for a grammar-only fix, write the proposed contained grammar change in the record under `## Broken positions` and flag it for Task 3's `grammar-followup` option (the D-04 corpus harness gate needs its own plan). A position needing a Langium change is out of reach per D-01 and D-02.
4. Re-run the recorder into `/tmp/phase-109-method-body-after-01.jsonl`. When any row changed verdict, add a `## After 109-01` subsection to the record with the same table; when none changed, add one line saying the re-run matched the before-fix table.
5. Draft the #561 comment for the maintainer in the record under `## #561 decision` as a quoted proposal: which positions were measured, which work (with the pinning test file), which stay out of reach and why, in plain words without planning identifiers. Do not post it.
6. Run both test files, then commit.
  </action>
  <verify>
    <automated>cd /home/coder/repos/bbj-language-server/bbj-vscode && npx vitest run test/completion-method-body.test.ts test/completion-test.test.ts</automated>
    <fails_when>non-zero exit, a "failed" count above 0 in the summary, or "No test files found"</fails_when>
    <automated>grep -c "test('DEF FN parameters with \$ suffix inside class method'" /home/coder/repos/bbj-language-server/bbj-vscode/test/completion-test.test.ts; grep -c "## #561 decision" /home/coder/repos/bbj-language-server/.planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md</automated>
    <fails_when>the second grep exits non-zero; the first prints 0 while the record shows the verbatim test passed (branch A or B)</fails_when>
  </verify>
  <acceptance_criteria>
    - Every row recorded `works` is a non-skipped test in completion-method-body.test.ts; every skipped row's label contains "out of reach" and the same reason appears in the record
    - Branch A: `git -C /home/coder/repos/bbj-language-server diff --stat HEAD -- bbj-vscode/src` prints nothing and `grep -c "Tracked for future fix" /home/coder/repos/bbj-language-server/bbj-vscode/test/completion-test.test.ts` prints 0
    - `git -C /home/coder/repos/bbj-language-server diff --stat HEAD -- bbj-vscode/src/language/bbj.langium bbj-vscode/src/language/generated` prints nothing
    - The record's `## #561 decision` section holds the drafted comment
    - After `git add` of this task's files and before `git commit`: `! git -C /home/coder/repos/bbj-language-server diff HEAD -- bbj-vscode/src bbj-vscode/test | grep -E '^\+' | grep -E '\bD-[0-9]{2}\b|\b(COMP|JINT)-0[1-3]\b|\b109-0[1-9]\b|\b(CR|WR|IN)-[0-9]{2}\b|\bT-109-[0-9]+\b'` exits 0
  </acceptance_criteria>
  <done>Every measured position is pinned, fixed, or recorded out of reach with its reason; the DEF FN test runs un-skipped when it passes; the #561 comment is drafted, not posted.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: The maintainer decides how issue #561 is handled</name>
  <read_first>
    - .planning/phases/109-completion-java-class-resolution/109-COMP03-MEASUREMENT.md (whole file, including the drafted comment)
  </read_first>
  <decision>How issue #561 (completion inside class method bodies) is handled, given the measured result</decision>
  <context>
Per D-02, #561 stays open with a comment listing the remaining positions only when some position stays out
of reach. Research expected none. Posting to a public issue is visible to others, so the maintainer
chooses; plan 109-06 carries out the choice after the phase's regression gate. Present the record's
table, the Broken positions list and the drafted comment.
  </context>
  <options>
    <option id="comment-and-close">
      <name>Post the comment and close #561 as completed (only when no position stays out of reach)</name>
      <pros>The issue reflects reality now; the pinning tests guard it</pros>
      <cons>Closes before the release that carries the un-skipped test</cons>
    </option>
    <option id="comment-close-at-release">
      <name>Post the comment now; close #561 from the release pull request</name>
      <pros>The issue closes together with the shipped tests</pros>
      <cons>The issue stays open a little longer</cons>
    </option>
    <option id="keep-open-with-list">
      <name>Post a comment listing the positions that stay out of reach and keep #561 open (D-02)</name>
      <pros>Required when any position is out of reach</pros>
      <cons>Only valid when the record lists out-of-reach positions</cons>
    </option>
    <option id="grammar-followup">
      <name>A position needs a contained bbj.langium change: plan a follow-up with the D-04 corpus harness gate before deciding #561</name>
      <pros>Keeps the grammar change behind its required harness gate</pros>
      <cons>Adds a gap-closure plan to this phase</cons>
    </option>
  </options>
  <resume-signal>Reply with one option id (optionally with edits to the drafted comment). The continuation records the id and any edited comment text under "## #561 decision" in the record and in the SUMMARY; nothing is posted from this plan.</resume-signal>
  <acceptance_criteria>
    - The record's `## #561 decision` section names the chosen option id and the final comment text
    - This plan's SUMMARY names the chosen option and every out-of-reach position (or none)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language server → public issue tracker | a comment or state change on #561 is visible to every user |
| test fixtures → public repository | fixture text is published with the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-01 | Repudiation | #561 comment/close | low | mitigate | Nothing is posted from this plan; a `blocking-human` checkpoint records the maintainer's choice and the exact text, and 109-06 posts only that text. |
| T-109-02 | Information disclosure | fixtures, measurement record | low | mitigate | Invented names only (`MethodBodyProbe`, `probeVar`); no corpus or customer text; the register check keeps planning ids out of source and tests. |
| T-109-03 | Tampering (developer trust) | a provider fallback | low | mitigate | Only on a measured broken row; its control and the whole completion-test.test.ts must stay green; a grammar change is routed to a harness-gated plan (D-04). |
| T-109-SC | Tampering | package installs | low | accept | No package is installed. |
</threat_model>

<verification>
- `completion-method-body.test.ts` and `completion-test.test.ts` pass
- The record holds the before-fix table, the verbatim test result, the broken-positions list and the #561 decision
- No `bbj.langium` or generated change; register check clean
</verification>

<success_criteria>
- ROADMAP criterion 3: every measured position is fixed and pinned or recorded with its reason, including the DEF FN `_f$`/`_t$` position
- The #561 handling is the maintainer's recorded choice
</success_criteria>

<output>
Create `.planning/phases/109-completion-java-class-resolution/109-01-SUMMARY.md` when done
</output>
