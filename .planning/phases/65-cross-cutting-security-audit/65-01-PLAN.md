---
phase: 65-cross-cutting-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/65-COVERAGE.md
autonomous: true
requirements: [SEC-01, SEC-02]

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`.planning/reviews/65-COVERAGE.md` exists and carries the complete Phase 65 skeleton: header with the swept-tree SHA recorded once, the plain statement that this phase's completeness construct differs from its four predecessors' (no `RU-65-*` units, no grid, no cell gate, no file gate — with `grep -c 'RU-65' .planning/reviews/INVENTORY.md` run live and its literal `0` recorded as the evidence), the `## Surface Enumeration Register` holding **all four** denominators derived live by recorded commands with their literal outputs, the `## Inherited Findings Ledger` with one row per inherited D1 finding derived live from the four closed coverage files, the `## Stopping Rule & Write Contract`, four stubbed `## SEC-0N` surface sections and a stubbed `## Phase 65 Close-Out` — so plans `65-02` and `65-03` append into a frozen shape rather than re-deriving one (D-01, D-02, D-03, D-08)"
    - "**Every enumerated item of all four surfaces is stubbed as a placeholder line in this plan's Task 1**, before any of them is swept, so the denominators are closed at the moment they are derived and cannot be quietly widened or narrowed by a later plan to fit what that plan found. The closure gates are **arithmetic identities against the live enumeration commands**, never literals typed into a plan: the count of `[SEC-01][generator]` lines equals what the generator command prints, `[SEC-01][candidate]` equals what the candidate command prints, `[SEC-02][handler]` equals what the handler command prints, `[SEC-02][case]` equals the live case-arm sum, `[SEC-04][…]` equals four times the live site count, and `[SEC-05][candidate]` equals the live raw-candidate sum (D-01, D-02)"
    - "The **4/4 symmetry between SEC-01 and SEC-02 is asserted mechanically, not narrated**: the sorted output of the SEC-01 generator command and the sorted output of the SEC-02 handler command are compared for string equality and the comparison is recorded, because D-02 states that the same four composer webviews generate the HTML *and* receive the messages and that this is not a coincidence to gloss over — it is the reason D-06 pairs the two surfaces into this plan"
    - "`## SEC-01 — Webview HTML generation & CSP posture` is closed: all four generators and every candidate interpolation-or-DOM-sink site within them carries a verdict (`pass` | `fail` | `undetermined` | `n/a`), each `pass` carrying a written line naming the concrete checks applied, each `fail` naming the finding ID that discharges it — a **new `P65-D1-nnn` or an inherited `P6{1,2,3,4}-D1-nnn` owner**, which is D-04 rendered as a grep — and each `n/a` carrying a written exclusion reason (ROADMAP criterion 1, D-01, D-04)"
    - "`### CSP Posture` records the D-12 positive result **explicitly and per generator, with the check that established it** — the directive set, the per-render `nonce`, the `script-src 'nonce-…'` binding, the panel options, and whether `localResourceRoots` is set — so a checked-and-clean generator is distinguishable from an unchecked one and ROADMAP criterion 1's 'CSP posture is documented' clause is discharged by evidence rather than by silence (D-12)"
    - "`## SEC-02 — Webview → extension message trust` is closed: all four `onDidReceiveMessage` handlers and every `case` arm inside them carries a verdict, and `### Runtime Validation Posture` answers, **per handler and per arm**, the only question ROADMAP criterion 2 asks — *what runtime check sits between message receipt and the first side effect* — with the TypeScript annotation named as compile-time-only and explicitly refused as an answer (D-13)"
    - "The `default:`-arm distribution across the four handlers is **derived live and recorded per handler with the command's literal output**, because D-13 requires the case where the switch's own `default` branch is the de-facto guard to be stated precisely, and a distribution that is not uniform across four near-duplicate handlers is exactly the asymmetry D-04 names as a legitimate basis for a new `P65-*` ID"
    - "`## Inherited Findings Ledger` holds one row per inherited D1 finding across `61`/`62`/`63`/`64-COVERAGE.md`, each row carrying the ID, owning phase, `location:`, `severity:` and the surface(s) it bears on — with the row count asserted equal to the live sum of the four per-file `grep -c` outputs rather than to a number typed into this plan — plus the four inheritance facts re-confirmed rather than restated: `62-COVERAGE.md`'s and `63-COVERAGE.md`'s and `64-COVERAGE.md`'s close-out tables each carry a Phase 65 row, `61-COVERAGE.md` has no downstream-inheritance table at all, and INVENTORY's routing table (D-06) has no Phase 65 row"
    - "The header states the two recording-shape resolutions this phase's structural break forces, so no reader mistakes either for an omission: the finding record's required `unit:` field carries the **surface ID** (`SEC-01`/`SEC-02`/`SEC-04`/`SEC-05`) because Phase 65 has no `RU-*` unit to name, and Phase 64's `triage:` field is **absent by decision, not by oversight** (D-08, D-10)"
    - "Every recorded finding carries `path:line`, primary dimension `D1`, evidence clearing INVENTORY's `repro` tier, `effort` on the `{2,4,8}` scale, `classification: major` (forced for every record in this phase by INVENTORY 3c test (6), since every `P65-*` finding is D1-primary), and a non-blank `dedup:` checked against the frozen 15-issue snapshot (RVW-06, RVW-07, D-08, D-09)"
    - "A claim that does not clear its evidence tier appears under that surface's `### Not-reproducible dispositions` with what *is* established and why the rest is not, rather than being asserted or silently dropped — D-11's governing rule, written from `P63-D1-002`'s withdrawn `Files.copy` symlink claim"

    # ~~ edge probes (no SPEC.md; surfaced during planning, all authored)
    - "Boundary case: a candidate line that the sweep decides is not a real interpolation or sink is resolved to `n/a` **with a written reason on the same line**, never deleted — so the enumeration's arithmetic closure holds and a reader can audit every exclusion the refinement made rather than only the survivors"
    - "Boundary case: a spawn-, sink- or handler-shaped site the enumeration command does not reach is recorded as an `[extra]` line carrying a verdict, so the denominator can drift **upward** and be seen to have drifted; an enumeration that can only shrink is not a denominator"
    - "Boundary case: `65-01`, `65-02` and `65-03` all append to one file; concurrency is excluded structurally by the dependency chain, not by an assumption about the executor. If a plan is interrupted mid-append every unswept enumerated line still carries the literal placeholder token, which is mechanically countable at every wave and must reach zero only at the close-out"
    - "Boundary case: `P62-D1-002` establishes that the CSP nonce comes from `Math.random()`. `### CSP Posture` must not re-record that as a `P65-*` finding — it cross-references the owning ID (D-04) — while still stating the nonce mechanism as part of the documented posture, because criterion 1 asks for the posture and D-12 asks for the positive result, and a cross-reference is not a gap in either"

  artifacts:
    - ".planning/reviews/65-COVERAGE.md — created by this plan: header, `## Surface Enumeration Register` (four sub-blocks, one per surface, each with its derivation command and literal output), `## Inherited Findings Ledger`, `## Stopping Rule & Write Contract`, four stubbed `## SEC-0N` sections and a stubbed `## Phase 65 Close-Out`"
    - ".planning/reviews/65-COVERAGE.md §`## SEC-01 — Webview HTML generation & CSP posture` — `### Enumeration`, `### CSP Posture`, `### Verdicts`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-references`, `### Surface closure`"
    - ".planning/reviews/65-COVERAGE.md §`## SEC-02 — Webview → extension message trust` — `### Enumeration`, `### Runtime Validation Posture`, `### Verdicts`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-references`, `### Surface closure`"

  key_links:
    - "The skeleton and the four closed enumerations this plan writes -> plans `65-02` (SEC-04) and `65-03` (SEC-05 + close-out), which append into it and whose D-16 gates re-derive the same four denominators"
    - "`### CSP Posture` and `### Runtime Validation Posture` -> `65-03`'s close-out criterion gate, which cites them by name as the sections discharging ROADMAP criteria 1 and 2"
    - "`## Inherited Findings Ledger` -> every `fail` verdict in all three plans, whose discharging ID must resolve either to a new `P65-*` record or to a ledger row (D-04)"
    - "`62-COVERAGE.md`'s `### SEC-01/SEC-02 Surface Handoff` -> this plan's SEC-01 and SEC-02 verdicts, which test that conclusion against the whole enumerated surface rather than rediscovering it (D-05)"
    - "This whole file -> Phase 68's DOC-03 concatenation, which walks `6N-COVERAGE.md` and needs the header's statement that Phase 65 adds *surfaces*, not grid cells, so INVENTORY's 147-of-148 denominator stays clean (D-03, D-15)"

  prohibitions:
    - "MUST NOT assert a security mechanism without showing it. A finding needs a reproduction, or a line-by-line trace naming concrete inputs/state and the exact `file:line` where behaviour diverges. If confirming a claim would require constructing an exploit, it is recorded under `### Not-reproducible dispositions` with what *is* established and what is not — never promoted to a finding on the strength of plausibility. This is D-11, written from `P63-D1-002`, a false `Files.copy` symlink claim that survived a whole sweep and was caught only at verification, one phase before public filing"
    - "MUST NOT re-record an inherited finding under a new `P65-*` ID. A new ID is justified only by a gap *between* two modules, an asymmetry between the two IDEs on one concern, or a chain in which two individually-acceptable behaviours compose into an unacceptable one. Anything whose evidence sits entirely inside one already-reviewed file, saying what a `P6{1,2,3,4}-D1-*` record already says, is recorded with `disposition: duplicate` naming the owning ID (D-04, INVENTORY's disposition vocabulary at line 154)"
    - "MUST NOT publish a trigger sequence, a payload, a working procedure or a step-by-step exploitation recipe for any `critical` or `high` finding — this repository is public and forkable, git history retains an over-disclosure permanently, the surfaces in play carry Enterprise Manager credentials, and Phase 69 drafts these records into public issues. Record surface, problem class and impact, open the `evidence:` field with the exact phrase `Disclosure-limited per D-14`, and stop there (D-14)"
    - "MUST NOT fix anything, file or draft any GitHub issue, re-triage any `DEBT-*` item, or edit `.planning/reviews/INVENTORY.md`, `61-COVERAGE.md`, `62-COVERAGE.md`, `63-COVERAGE.md` or `64-COVERAGE.md`. INVENTORY is immutable for the v4.0 milestone (Phase 60 D-09) and all four sweeps are closed and verified; drift found in any of them is recorded as a finding here. Phase 66 re-triages debt, Phase 67 applies fixes, Phase 69 files issues under the ISSUE-01 gate (D-15)"
    - "MUST NOT run any command that mutates the working tree — no install, no formatter run, no build, no lockfile regeneration, no `git checkout`, no file write outside `.planning/reviews/65-COVERAGE.md`. Enforced by an acceptance criterion asserting `git status --porcelain` is empty across every reviewed tree and every immutable record"
    - "MUST NOT allocate a finding ID outside `P65-D1-nnn`. A `P65-D2-*` or any other dimension must not appear: if a claim is really about correctness rather than security it belongs to the module-owning phase, not here. `P00-*` is INVENTORY's reserved template namespace and is never allocated (D-08, D-04)"
    - "MUST NOT write a `triage:` field on any record — that vocabulary is Phase 64's D6 dependency triage and is inapplicable here; adding it would make Phase 68's assembly ambiguous about which findings the triage buckets cover (D-10)"
    - "MUST NOT write an `effort:` value off INVENTORY §3d's `{2,4,8}` scale. Phase 63 shipped three off-scale values that were unlabellable for ISSUE-03 and needed a post-hoc correction at verification; do not repeat it (D-09)"
    - "MUST NOT record `classification: easy` on any record — INVENTORY 3c test (6) makes every D1-primary finding `major` regardless of how small its edit is, and every `P65-*` finding is D1-primary by D-08. An `easy` classification here would route a security finding past `MAJOR-REFACTORS.md` into Phase 67's apply path"
    - "MUST NOT accept a TypeScript type annotation as evidence of runtime validation for ROADMAP criterion 2. The annotation is erased at compile time; the value arriving from the webview is whatever `postMessage` sent. The per-handler question is what runtime check exists between receipt and the first side effect, and 'it's typed' is not an answer (D-13)"
    - "MUST NOT record a verdict for a candidate site the sweep did not open, and MUST NOT record `pass` on a line whose named checks were not actually run — a written line describing checks that were not performed is a false-assurance record that Phase 67's apply path and Phase 68's DOC-03 both consume as if it were evidence"
    - "MUST NOT delete, merge or silently narrow an enumerated line. A candidate the refinement rejects is resolved to `n/a` with a written reason on the same line, so the enumeration's arithmetic closure holds and the exclusion is auditable"
    - "MUST NOT locate a finding in `java-interop/` (FUT-01), `bbj-vscode/src/language/generated/` (machine-generated), `bbj-vscode-deprecated/`, `CLAUDE.md`, `bbj-vscode/VERBs.md` or anything under `documentation/` — the first three are out of scope for the milestone and the last three are `RU-D8-01`'s surface, still the milestone's one unrecorded row and not Phase 65's to close"
    - "MUST NOT write a credential, an EM token, a password, a session value or an absolute developer-machine path into any field — every citation is a repository-relative `path:line` anchor"
    - "MUST NOT write into `## SEC-04`, `## SEC-05` or `## Phase 65 Close-Out` beyond the stubs Task 1 creates — those belong to `65-02` and `65-03`"
---

<objective>
Create `.planning/reviews/65-COVERAGE.md` — Phase 65's sole deliverable — with its complete
skeleton and **all four surface enumerations closed at the moment they are derived**, then sweep the
two surfaces that share the same four composer webviews: **SEC-01** (webview HTML generation and CSP
posture, HTML out) and **SEC-02** (webview → extension message trust, messages in).

Purpose: this plan carries the structural load of the phase. INVENTORY defines no `RU-65-*` units,
so every completeness mechanism Phases 61-64 relied on — the grid, the applicability cells, the
`n/a` carry-forwards, the cell gate, the file gate — is **absent here**. D-01 replaces all of it with
four closed surface enumerations, each with a denominator derived live by a recorded command and a
verdict on every enumerated item. This plan derives all four denominators, stubs every one of their
items, and then proves the recording shape on the first two surfaces. D-06 pairs SEC-01 and SEC-02
because splitting them would mean two plans reading the same four files — and because the 4/4
symmetry between generators and handlers is a fact about those files, not a coincidence.

Output: a created coverage file with a frozen skeleton, four closed enumerations, a live-derived
inherited-findings ledger, the SEC-01 and SEC-02 surfaces swept and closed, and any finding records,
not-reproducible dispositions and cross-references. **No source file is modified.**
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/65-cross-cutting-security-audit/65-CONTEXT.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 65 creates **no code symbol and modifies no source file**. Everything below is a planning
artifact. `bbj-vscode/`, `bbj-intellij/`, `.github/` and `java-interop/` are read-only review
subject matter for all three plans (D-15).

**Created by this plan**

- `.planning/reviews/65-COVERAGE.md` — the phase's sole deliverable, named for Phase 68's DOC-03
  concatenation, which walks `6N-COVERAGE.md` (D-03). This plan writes the whole skeleton plus
  `## SEC-01` and `## SEC-02`; plan `65-02` fills `## SEC-04`; plan `65-03` fills `## SEC-05` and the
  close-out.

**Named section anchors** (downstream phases consume these by name, so the literal headings matter):

- `## Surface Enumeration Register` — the four denominators, this plan.
- `## Inherited Findings Ledger` — the D-04 cross-reference source, this plan.
- `## Stopping Rule & Write Contract` — this plan.
- `## SEC-01 — Webview HTML generation & CSP posture` and
  `## SEC-02 — Webview → extension message trust` — this plan.
- `## SEC-04 — EM token lifecycle end to end` — stubbed here, filled by `65-02`.
- `## SEC-05 — Process-spawn argument & command injection` — stubbed here, filled by `65-03`.
- `## Phase 65 Close-Out` — stubbed here, filled by `65-03`.
- Per-surface, the sub-blocks every surface section carries: `### Enumeration`, `### Verdicts`,
  `### Findings`, `### Not-reproducible dispositions`, `### Cross-references`, `### Surface closure`.
  SEC-01 adds `### CSP Posture`; SEC-02 adds `### Runtime Validation Posture`; SEC-04 adds
  `### Lifecycle Matrix`.

**The structural break, stated once.** `grep -c 'RU-65' .planning/reviews/INVENTORY.md` returns `0`.
There are no review units, no grid rows, no applicability cells, no cell gate and no file gate for
this phase. Phases 61-64 could prove completeness because INVENTORY handed them a closed denominator;
Phase 65 constructs its own. The header says this plainly so no reader expects a cell gate and
concludes one is missing (D-01, D-03).

**Finding-ID namespace.** `P65-D1-nnn`, zero-padded to three digits, allocated monotonically in
discovery order across the whole phase. **D1 throughout** — all four requirements are security
concerns and D1 is the security dimension; a claim that is really about correctness belongs to the
module-owning phase (D-08, D-04). `P00-*` is INVENTORY's reserved template namespace and is never
allocated.

**Two recording-shape resolutions the structural break forces**, both stated in the header so
neither reads as an omission:

- INVENTORY's finding-record template requires a `unit:` field naming an `RU-{phase}-{seq}`. Phase 65
  has no such unit, so `unit:` carries the **surface ID** — `SEC-01`, `SEC-02`, `SEC-04` or `SEC-05`
  — which is the denominator the record belongs to under D-01. No record carries an `RU-` token.
- Phase 64's `triage:` field is **absent by decision** (D-10): that vocabulary was introduced for
  SEC-08's dependency triage and is required only on D6 dependency findings. Phase 65 records carry
  `classification:` and `disposition:` only.

**Plan → surface map (D-06, D-07).** One wave per plan, serial, because all three append to one file:

| Plan | Wave | `depends_on` | Surfaces | Also |
|---|---|---|---|---|
| `65-01` (this plan) | 1 | — | **SEC-01 + SEC-02** — the same four composer webviews, HTML out and messages in | Creates the skeleton and closes **all four** enumerations |
| `65-02` | 2 | `65-01` | **SEC-04** — EM token lifecycle, both IDEs plus the two `.bbj` scripts | **Owns** the token-as-process-argument question (D-07) |
| `65-03` | 3 | `65-02` | **SEC-05** — every process-spawn site across both IDEs and the tool scripts | **Plus the phase close-out** and its three D-16 gates |

**The enumerated-line grammar**, frozen by this plan and used by all three:

```
- [SURFACE][kind] <anchor> — <verdict> — <written line>
- [SURFACE][kind] <anchor> — pending
```

`<verdict>` is exactly one of `pass`, `fail`, `undetermined`, `n/a`. The second form is the
placeholder every enumerated item is stubbed as in Task 1 and which must reach zero at the close-out.
A `fail` line names the finding ID that discharges it — a new `P65-D1-nnn` **or** an inherited
`P6{1,2,3,4}-D1-nnn` owner, which is D-04 rendered as a grep. An `undetermined` line names its
`### Not-reproducible dispositions` entry. An `n/a` line carries a written exclusion reason.

<!-- The literals below are section headings and marker phrases the executor must create verbatim in
     the coverage file. Every one of them is asserted POSITIVELY by an acceptance criterion; no
     acceptance criterion in this plan negative-greps any of them, and every negative grep in this
     plan targets `.planning/reviews/65-COVERAGE.md` rather than this PLAN.md, so no criterion can be
     invalidated by this plan's own prose. -->
<!-- `RU-65` is negative-grepped ONLY against `.planning/reviews/INVENTORY.md` (asserting that no
     review unit is defined for this phase, which is the evidence for the whole D-01 construct). It is
     asserted POSITIVELY against the artifact this plan writes, so there is no self-invalidation risk. -->
<!-- The two `^id:` patterns are negative-grepped ONLY against `.planning/reviews/65-COVERAGE.md`
     (asserting Task 1 records no finding, and deriving the inherited ledger's row count from the four
     closed coverage files). Neither targets this PLAN.md. -->
<!-- planner-discipline-allow: RU-65 -->
<!-- planner-discipline-allow: ^id:[[:space:]]+P$c-D1- -->
<!-- planner-discipline-allow: ^id: -->
<!-- planner-discipline-allow: Disclosure-limited per D-14 -->
<!-- planner-discipline-allow: ## Surface Enumeration Register -->
<!-- planner-discipline-allow: ## Inherited Findings Ledger -->
<!-- planner-discipline-allow: ## Stopping Rule & Write Contract -->
<!-- planner-discipline-allow: ### Enumeration -->
<!-- planner-discipline-allow: ### CSP Posture -->
<!-- planner-discipline-allow: ### Runtime Validation Posture -->
<!-- planner-discipline-allow: ### Verdicts -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Not-reproducible dispositions -->
<!-- planner-discipline-allow: ### Cross-references -->
<!-- planner-discipline-allow: ### Surface closure -->

<tasks>

<task type="auto">
  <name>Task 1: Create 65-COVERAGE.md — the skeleton, all four surface enumerations derived live and closed, and the inherited-findings ledger</name>
  <files>.planning/reviews/65-COVERAGE.md</files>
  <precondition>The working tree is on branch `v4.0-stability-and-quality` — `git rev-parse --abbrev-ref HEAD`
  prints exactly that — `.planning/reviews/INVENTORY.md` and all four of `61`/`62`/`63`/`64-COVERAGE.md`
  exist, and `.planning/reviews/65-COVERAGE.md` does not yet exist. Halt and report if any fails.</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the immutable contract. Read §"Frozen Open-Issue Snapshot"
      (the 15-issue dedup list every finding is checked against), §"Finding Standard" 3a-3d — note
      that D1 is tier `repro`, that 3c test (6) makes **every** D1-primary finding `major` regardless
      of edit size, and that §3d fixes `effort` to `2`, `4` or `8` — §"Finding Record Template" (the
      exact 13 fields, in order, including the required `unit:` this phase resolves to a surface ID),
      §"Review Units" → confirm for yourself that **no `RU-65-*` unit is defined anywhere in the
      file**, §"Applicability Grid" and §"Grid totals" → the milestone-wide 148 / 84 / 232 figures
      Phase 68 measures against and which this phase must not pollute, and
      §"Routing table (D-06)" → confirm there is **no Phase 65 row**
    - `.planning/REQUIREMENTS.md` — **SEC-01** (line 44), **SEC-02** (45), **SEC-04** (47), **SEC-05**
      (48) verbatim, since all four enumerations are keyed to their wording; plus **RVW-06** (verified
      failure scenario) and **RVW-07** (dedup) as the standard every finding meets, and the D1-D8
      dimension table
    - `.planning/ROADMAP.md` §"Phase 65: Cross-Cutting Security Audit" — the goal and **all five**
      success criteria, since the skeleton's stubbed close-out names each one
    - `.planning/phases/65-cross-cutting-security-audit/65-CONTEXT.md` — **D-01..D-16 are binding,
      read in full**
    - `.planning/reviews/64-COVERAGE.md` **lines 1-60 and 3434-3470 only** — the frozen recording
      shape to inherit (header layout, placeholder convention, stopping-rule and write-contract
      wording, the finding-record rendering) and the opening of its close-out. Do **not** read the
      rest of that 480 KB file except the two ranges named below
    - `.planning/reviews/64-COVERAGE.md` **lines 3858-3872 only** — its downstream-inheritance table,
      whose **Phase 65** row states that nothing flows here as open work and names `P64-D1-001`,
      `P64-D1-002`, `P64-D1-003` and `62-COVERAGE.md:1489` as *context*
    - `.planning/reviews/62-COVERAGE.md` **lines 2066-2078 only** — its close-out inheritance table's
      **Phase 65** row
    - `.planning/reviews/63-COVERAGE.md` **lines 3220-3232 only** — its close-out inheritance table's
      **Phase 65** row, which also states that SEC-03 is closed and nothing on it flows here
    - `.planning/reviews/61-COVERAGE.md` — do **not** read it in full. Confirm mechanically that it
      carries **no downstream-inheritance table at all**, by grepping it for a Phase 65 mention and
      recording the literal output
    - **Do NOT read** any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and
      INVENTORY supersedes them
  </read_first>
  <action>
Create `.planning/reviews/65-COVERAGE.md` and write it in the order below. Inherit the recording
shape unchanged from `64-COVERAGE.md` (D-08) — Phase 61's D-05 checkpoint froze it and three phases
have since confirmed it transfers, so **no new format checkpoint is spent**. Phase 65 changes exactly
one thing about it, and that change is the whole point of this task: **the applicability grid and its
two gates are replaced by four closed surface enumerations** (D-01).

**A. Header.** Title it as Phase 65's coverage record for the four cross-cutting security concerns
(SEC-01, SEC-02, SEC-04, SEC-05). Record, as labelled lines:

- **The swept tree** — branch `v4.0-stability-and-quality` at the full 40-character SHA obtained by
  running `git rev-parse HEAD` **now, at execution time**, stated once for the whole phase and never
  re-anchored per plan.
- **The governing standard** — `.planning/reviews/INVENTORY.md`, named as immutable and not edited by
  this phase (Phase 60 D-09, D-15).
- **The structural break, stated plainly and evidenced rather than asserted.** This file's
  completeness construct **differs from its four predecessors'**: INVENTORY defines no `RU-65-*`
  review units, so there is no applicability grid, no cell, no `n/a` carry-forward, no cell gate and
  no file gate here. Run `grep -c 'RU-65' .planning/reviews/INVENTORY.md` and record the command with
  its literal output as the evidence. State the replacement in one sentence: four closed surface
  enumerations, one per requirement, each with a denominator derived live by a recorded command and
  **a verdict on every enumerated item** — the same auditable shape as the cell grid, keyed on
  security surfaces instead of INVENTORY rows, such that a reader can re-run each enumeration
  command, get the same denominator, and check every item against a verdict (D-01, D-03).
- **The two recording-shape resolutions.** That the required `unit:` field carries the **surface ID**
  (`SEC-01`/`SEC-02`/`SEC-04`/`SEC-05`) rather than an `RU-*` token, because this phase has no unit
  to name. And that Phase 64's `triage:` field is **absent by decision, not oversight** — that
  vocabulary was introduced for SEC-08's dependency triage and applies only to D6 dependency
  findings; adding an inapplicable field would make Phase 68's assembly ambiguous about which
  findings the triage buckets cover (D-08, D-10).
- **The finding-ID namespace** — `P65-D1-nnn`, D1 throughout, monotonic in discovery order across the
  whole phase, with the reason stated: all four requirements are security concerns, D1 is the
  security dimension, and a claim that is really about correctness belongs to the module-owning
  phase, not here. State that no other dimension digit is allocated in this phase and that
  INVENTORY's reserved template namespace is never allocated.
- **The classification consequence, stated as arithmetic rather than as a preference.** INVENTORY 3c
  test (6) makes a finding `major` whenever its severity is `critical` or `high` **or** its primary
  dimension is D1. Every `P65-*` finding is D1-primary, so **every record in this file is
  `classification: major`**, and Phase 67 inherits none of them as an apply candidate. Say so here
  rather than leaving Phase 67 to discover it by counting.
- **The effort scale** — INVENTORY §3d's `2` | `4` | `8`, with the reason stated: Phase 63 shipped
  three off-scale values that were unlabellable for ISSUE-03 and needed a post-hoc correction at
  verification time (D-09).
- **The dedup source** — INVENTORY's frozen 15-issue snapshot, queried 2026-08-17, with the note that
  Phase 69 re-queries the tracker live before filing. State — **after reading the snapshot's `Area`
  column yourself rather than assuming** — how many of the 15 touch webview markup, message handling,
  credential storage or process spawning, so a reader knows why most `dedup:` verdicts in this file
  resolve the way they do.
- **The synthesis rule (D-04), stated as the organising rule of the whole file.** The inherited D1
  findings are **inputs**; this phase cross-references them by ID and never re-records them. A new
  `P65-*` ID is justified **only** when the cross-cutting view shows something no single-module
  review could have seen: a gap that exists *between* two modules, an asymmetry between the two IDEs
  on the same concern, or a chain in which two individually-acceptable behaviours compose into an
  unacceptable one. Anything else is recorded with `disposition: duplicate` naming the owning ID, per
  INVENTORY's disposition vocabulary. State why: it is what keeps Phase 68's `MAJOR-REFACTORS.md`
  from listing one defect twice under two IDs.
- **The evidence rule (D-11), stated as the governing rule of this phase and with its provenance.**
  Do not assert a mechanism — show it, or dispose of it as not-reproducible. A finding needs a
  reproduction or a line-by-line trace naming concrete inputs/state and the exact `file:line` where
  behaviour diverges. If confirming a claim would require constructing an exploit, record a
  not-reproducible disposition with what *is* established. Name the precedent explicitly:
  `P63-D1-002` claimed `Files.copy` follows a symlink and overwrites the link's referent; it is
  false, `LinkOption.NOFOLLOW_LINKS` governs the source of a copy rather than the target, and the
  claim survived an entire sweep, its own unit closure and the close-out before verification caught
  it — one phase before Phase 69 would have filed it as a public vulnerability report.
- **The disclosure rule (D-14), inherited unchanged.** For any finding rated `critical` or `high`:
  surface, problem class and impact only; no trigger sequence, no payload, no working procedure. Open
  every such redacted `evidence:` field with the literal marker `Disclosure-limited per D-14`. Lower
  severities record normally with the `file:line` anchors Phase 67 needs. State why this phase is the
  most exposed in the milestone: it is the security synthesis itself, in a public forkable
  repository, and its records are the ones Phase 69 drafts into public issues.
- **The scope fence (D-15)** — this phase fixes nothing, files nothing and re-triages nothing; no
  source file is modified; `INVENTORY.md` and the four closed coverage files are not edited, and
  drift found in any of them is recorded as a finding here. And the Phase 68 note: Phase 65 adds
  **surfaces**, not grid cells, so INVENTORY's 148-`applies` denominator and Phase 64's 147-of-148
  position are unchanged by this file and must not be blended into.

**B. `## Surface Enumeration Register`.** Four sub-blocks, one per requirement, in the order
`SEC-01`, `SEC-02`, `SEC-04`, `SEC-05`. Each records: the requirement's own wording from
REQUIREMENTS.md; **the derivation command, run now, with its literal output**; the resulting
denominator; and **the D-02 baseline with an explicit drift statement** — the baseline is recorded so
a drift is visible rather than silently absorbed, and where the live derivation disagrees with it the
disagreement is written up **with its cause** rather than quietly adopted. Use exactly these
commands, and record each one's output verbatim:

```bash
# SEC-01 leg 1 — the webview HTML generators (D-02 baseline: 4)
grep -rln 'getHtml\|webview.html' bbj-vscode/src --include=*.ts | sort

# SEC-01 leg 2 — every interpolation-or-DOM-sink candidate within those generators
G4=$(grep -rln 'getHtml\|webview.html' bbj-vscode/src --include=*.ts | sort)
grep -nE '\$\{|innerHTML|outerHTML|insertAdjacentHTML|document\.write' $G4

# SEC-02 leg 1 — the onDidReceiveMessage handlers (D-02 baseline: 4)
grep -rn 'onDidReceiveMessage' bbj-vscode/src --include=*.ts

# SEC-02 leg 2 — the case arms inside those handlers
H4=$(grep -rln 'onDidReceiveMessage' bbj-vscode/src --include=*.ts | sort)
for f in $H4; do sed -n '/onDidReceiveMessage/,/^    });/p' "$f" | grep -cE "case '"; done

# SEC-02 leg 3 — the default-arm distribution across those handlers (D-13)
for f in $H4; do printf '%s %s\n' "$f" "$(sed -n '/onDidReceiveMessage/,/^    });/p' "$f" | grep -c 'default:')"; done

# SEC-04 — the token-lifecycle sites (D-02 baseline: 7)
{ grep -rln 'EMToken\|emToken\|EM_TOKEN\|em\.token' bbj-vscode/src bbj-intellij/src; \
  ls bbj-vscode/tools/em-login.bbj bbj-vscode/tools/em-validate-token.bbj; } | sort -u

# SEC-05 — the raw spawn-site candidates (D-02 baseline: 27)
grep -rnE 'child_process|spawn\(|spawnSync\(|execSync\(|execFile\(|exec\(' bbj-vscode/src --include=*.ts --include=*.cjs | grep -v '/generated/'
grep -rnE 'new GeneralCommandLine|new ProcessBuilder|Runtime\.getRuntime' bbj-intellij/src
ls bbj-vscode/tools/*.bbj
```

Then, in the SEC-01 and SEC-02 blocks together, record **the 4/4 symmetry as a mechanical fact
rather than a narrated one**: compare the sorted generator list `$G4` against the sorted handler list
`$H4` for string equality, record the comparison and its result, and state what it means — the same
four composer webviews generate the HTML *and* receive the messages, which is why D-06 pairs these
two surfaces into one plan instead of having two plans read the same four files.

In the SEC-04 block, state that the denominator is **four lifecycle stages × the enumerated sites**:
`acquisition`, `at-rest`, `exposure`, `expiry` — the four stages ROADMAP criterion 3 names — so its
enumerated-item count is four times the site count.

In the SEC-05 block, state the two-stage shape the sweep will use, because the raw greps deliberately
over-capture: **stage 1** is the raw candidate set enumerated above; **stage 2** resolves each
candidate either to a real process-spawn site carrying a verdict or to `n/a` with a written exclusion
reason (an import or `require` declaration, a comment, a `RegExp.prototype.exec` call, and so on).
State plainly that **no candidate is ever deleted** — the arithmetic must close, so every raw
candidate line becomes exactly one enumerated line, and the refinement is auditable rather than only
its survivors being visible. State that any real spawn site the raw greps did **not** reach is added
as an `[extra]` line carrying a verdict, so the denominator can drift upward and be seen to have
drifted: an enumeration that can only shrink is not a denominator.

**C. `## Inherited Findings Ledger`.** The D-04 cross-reference source. Derive it live rather than
transcribing it — run

```bash
for c in 61 62 63 64; do grep -A5 -E "^id:[[:space:]]+P$c-D1-" .planning/reviews/$c-COVERAGE.md \
  | grep -E '^id:|^location:|^severity:'; done
for c in 61 62 63 64; do printf '%s ' "$(grep -cE "^id:[[:space:]]+P$c-D1-" .planning/reviews/$c-COVERAGE.md)"; done; echo
```

and record both commands with their literal outputs. Write one table row per inherited D1 finding, in
this exact shape so the rows are countable:

```
| P61-D1-001 | 61 | <location:> | <severity:> | <surfaces it bears on> | <one clause: what it establishes for this phase> |
```

The surfaces column names zero or more of `SEC-01`, `SEC-02`, `SEC-04`, `SEC-05`; a row bearing on
none of the four says so, because a ledger that only lists convenient rows is not a ledger. State the
row count as the live sum of the four per-file counts rather than as a number carried from anywhere
else.

Then the **four inheritance facts, each re-confirmed now rather than restated**: `62-COVERAGE.md`'s
close-out inheritance table has a Phase 65 row (its `### SEC-01/SEC-02 Surface Handoff` plus every
`P62-D1-*` record); `63-COVERAGE.md`'s has a Phase 65 row (`RU-63-01`'s D1 records, plus the fact
that SEC-03 is closed and nothing on it flows here); `64-COVERAGE.md`'s has a Phase 65 row stating
**nothing flows as open work by design**, with `P64-D1-001`/`002`/`003` and `62-COVERAGE.md:1489`
named as *context*; and `61-COVERAGE.md` has **no downstream-inheritance table at all**, evidenced by
a grep with its literal output rather than by assertion. Add the fifth: INVENTORY's routing table
(D-06) has **no Phase 65 row**, likewise evidenced.

**D. `## Stopping Rule & Write Contract`.** A surface's sweep is complete when **all four** hold:
(i) every enumerated item of that surface carries a verdict — `pass`, `fail`, `undetermined` or
`n/a` — and no placeholder remains in its section; (ii) every `pass` carries a written line naming
the concrete checks applied, phrased against that requirement's own REQUIREMENTS.md wording, never a
bare verdict; every `fail` names the finding ID that discharges it, which may be a new `P65-D1-nnn`
**or** an inherited owner ID from the ledger; every `undetermined` names its
`### Not-reproducible dispositions` entry; every `n/a` carries a written exclusion reason;
(iii) every candidate claim raised during the sweep is either promoted to a finding record clearing
INVENTORY's `repro` tier for D1, or written under `### Not-reproducible dispositions` with its reason
— visible rather than silently dropped (RVW-06); and (iv) every ledger row whose surfaces column
names that surface carries a written cross-reference in `### Cross-references`, or an explicit
statement that it establishes nothing further for this surface. **Zero inherited items may be
silently dropped.**

Then the write contract: the enumerated-line grammar above, the placeholder convention, the fact that
three plans append to this one file with concurrency excluded structurally by the dependency chain
rather than by an assumption about the executor, and the rule that a plan writes only into its own
surface sections. State one further fact here, because it governs how every gate in this phase is
written: **the write contract necessarily names the placeholder token when it describes the
convention, so every gate is anchored on a line _shape_ rather than on a bare substring count.** A
gate counting occurrences of the token anywhere in the file would be invalidated by this very
paragraph — which is the defect Phase 64's close-out ran into and had to explain away in prose.

**E. The four stubbed surface sections.** Create `## SEC-01 — Webview HTML generation & CSP posture`,
`## SEC-02 — Webview → extension message trust`, `## SEC-04 — EM token lifecycle end to end` and
`## SEC-05 — Process-spawn argument & command injection`, each opening with the requirement's
verbatim wording and the ROADMAP criterion it discharges, and each carrying its sub-blocks:
`### Enumeration`, `### Verdicts`, `### Findings`, `### Not-reproducible dispositions`,
`### Cross-references`, `### Surface closure` — plus `### CSP Posture` under SEC-01,
`### Runtime Validation Posture` under SEC-02 and `### Lifecycle Matrix` under SEC-04.

**Under each `### Verdicts`, write one placeholder line for every enumerated item of that surface**,
in the frozen grammar's placeholder form, in the order the enumeration command printed them:

- SEC-01 — one `[SEC-01][generator]` line per generator path, and one `[SEC-01][candidate]` line per
  candidate `path:line` from leg 2.
- SEC-02 — one `[SEC-02][handler]` line per handler `path:line`, and one `[SEC-02][case]` line per
  case arm, anchored as `path:line` at the arm.
- SEC-04 — one `[SEC-04][acquisition]`, `[SEC-04][at-rest]`, `[SEC-04][exposure]` and
  `[SEC-04][expiry]` line **per site**, so the section holds four lines for each enumerated site.
- SEC-05 — one `[SEC-05][candidate]` line per raw candidate from all three legs, anchored at
  `path:line` for the two grep legs and at `path` for each tool script.

This is what closes the denominators: they are fixed at the moment they are derived, before any of
them is swept, so no later plan can quietly widen or narrow one to fit what it found.

**F. The stubbed `## Phase 65 Close-Out`.** Create the heading and stub the seven lettered sections
`65-03` fills — **A. Surface gate**, **B. Criterion gate**, **C. Requirement gate**, **D. Finding
accounting**, **E. Inherited-item accounting**, **F. Scope-fidelity note**, **G. Closing
confirmations** — each with a one-line statement of what it will carry, and name in §B all five
ROADMAP criteria verbatim and in §C all four requirement IDs, so `65-03` fills a shape rather than
inventing one.
  </action>
  <acceptance_criteria>
    - The coverage file exists and carries the swept-tree SHA recorded at execution time: `test -f .planning/reviews/65-COVERAGE.md` succeeds and `grep -c "$(git rev-parse HEAD)" .planning/reviews/65-COVERAGE.md` prints at least `1`
    - The structural break is stated and evidenced: the file contains `RU-65`, and independently `grep -c 'RU-65' .planning/reviews/INVENTORY.md` still prints `0`
    - All seven top-level sections exist: `grep -c '^## Surface Enumeration Register'`, `^## Inherited Findings Ledger`, `^## Stopping Rule & Write Contract`, `^## SEC-01 `, `^## SEC-02 `, `^## SEC-04 `, `^## SEC-05 ` and `^## Phase 65 Close-Out` each print `1`
    - Every surface section carries its sub-blocks: `grep -c '^### Enumeration$'`, `^### Verdicts$`, `^### Findings$`, `^### Not-reproducible dispositions$`, `^### Cross-references$` and `^### Surface closure$` each print `4`; and `^### CSP Posture$`, `^### Runtime Validation Posture$` and `^### Lifecycle Matrix$` each print `1`
    - **SEC-01 enumeration is closed against its live derivation**: the count of `^- \[SEC-01\]\[generator\] ` equals the line count of `grep -rln 'getHtml\|webview.html' bbj-vscode/src --include=*.ts`, and the count of `^- \[SEC-01\]\[candidate\] ` equals the line count of the leg-2 candidate command run over that same file list
    - **SEC-02 enumeration is closed against its live derivation**: the count of `^- \[SEC-02\]\[handler\] ` equals the line count of `grep -rn 'onDidReceiveMessage' bbj-vscode/src --include=*.ts`, and the count of `^- \[SEC-02\]\[case\] ` equals the live sum of per-handler `case '` counts
    - **The 4/4 symmetry is asserted mechanically**: the sorted generator file list and the sorted handler file list are byte-identical, and the file records the comparison
    - **SEC-04 enumeration is closed against its live derivation**: the count of `^- \[SEC-04\]\[` equals four times the live site count, and each of `^- \[SEC-04\]\[acquisition\] `, `^- \[SEC-04\]\[at-rest\] `, `^- \[SEC-04\]\[exposure\] ` and `^- \[SEC-04\]\[expiry\] ` equals that site count exactly
    - **SEC-05 enumeration is closed against its live derivation**: the count of `^- \[SEC-05\]\[candidate\] ` equals the sum of the three live legs — the VS Code grep, the IntelliJ grep and the `tools/*.bbj` listing
    - Every enumerated line is either resolved or an explicit placeholder, with no third state: the count of `^- \[SEC-0[1245]\]\[[a-z-]+\] ` equals the count of `^- \[SEC-0[1245]\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — ` plus the count of `^- \[SEC-0[1245]\]\[[a-z-]+\] .* — pending$`
    - Nothing is swept yet: the count of `^- \[SEC-0[1245]\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — ` prints `0`
    - The inherited ledger is complete and live-derived: the count of `^\| P6[1-4]-D1-[0-9]{3} \|` equals the live sum of `grep -cE '^id:[[:space:]]+P{c}-D1-'` over the four closed coverage files
    - All five inheritance facts are recorded: the file contains `61-COVERAGE.md`, `62-COVERAGE.md`, `63-COVERAGE.md`, `64-COVERAGE.md`, `routing table` and `P64-D1-001`
    - The recording-shape resolutions and rules are stated: the file contains `Disclosure-limited per D-14`, `disposition: duplicate`, `P63-D1-002`, `classification: major`, and each of the strings `SEC-01`, `SEC-02`, `SEC-04`, `SEC-05`
    - The close-out is stubbed with all seven sections: with the close-out extracted by `sed -n '/^## Phase 65 Close-Out/,$p'`, the strings `A.`, `B.`, `C.`, `D.`, `E.`, `F.` and `G.` each appear
    - No finding was recorded in this task: `grep -cE '^id:'` prints `0`
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/65-COVERAGE.md; test -f "$f"; grep -q "$(git rev-parse HEAD)" "$f"; test "$(grep -c "RU-65" .planning/reviews/INVENTORY.md)" = 0; grep -qF "RU-65" "$f"; for h in "^## Surface Enumeration Register" "^## Inherited Findings Ledger" "^## Stopping Rule & Write Contract" "^## SEC-01 " "^## SEC-02 " "^## SEC-04 " "^## SEC-05 " "^## Phase 65 Close-Out"; do test "$(grep -cE "$h" "$f")" = 1; done; for h in "^### Enumeration$" "^### Verdicts$" "^### Findings$" "^### Not-reproducible dispositions$" "^### Cross-references$" "^### Surface closure$"; do test "$(grep -cE "$h" "$f")" = 4; done; for h in "^### CSP Posture$" "^### Runtime Validation Posture$" "^### Lifecycle Matrix$"; do test "$(grep -cE "$h" "$f")" = 1; done; G4=$(grep -rln "getHtml\|webview.html" bbj-vscode/src --include=*.ts | sort); H4=$(grep -rln "onDidReceiveMessage" bbj-vscode/src --include=*.ts | sort); test "$G4" = "$H4"; ngen=$(printf "%s\n" "$G4" | wc -l); test "$(grep -cE "^- \[SEC-01\]\[generator\] " "$f")" = "$ngen"; ncand=$(grep -nE "\\\$\{|innerHTML|outerHTML|insertAdjacentHTML|document\.write" $G4 | wc -l); test "$(grep -cE "^- \[SEC-01\]\[candidate\] " "$f")" = "$ncand"; nh=$(grep -rn "onDidReceiveMessage" bbj-vscode/src --include=*.ts | wc -l); test "$(grep -cE "^- \[SEC-02\]\[handler\] " "$f")" = "$nh"; ncase=$(for x in $H4; do sed -n "/onDidReceiveMessage/,/^    });/p" "$x" | grep -cE "case .[a-z]"; done | awk "{s+=\$1} END{print s+0}"); test "$(grep -cE "^- \[SEC-02\]\[case\] " "$f")" = "$ncase"; nsite=$({ grep -rln "EMToken\|emToken\|EM_TOKEN\|em\.token" bbj-vscode/src bbj-intellij/src; ls bbj-vscode/tools/em-login.bbj bbj-vscode/tools/em-validate-token.bbj; } | sort -u | wc -l); test "$(grep -cE "^- \[SEC-04\]\[" "$f")" = "$((nsite * 4))"; for st in acquisition at-rest exposure expiry; do test "$(grep -cE "^- \[SEC-04\]\[$st\] " "$f")" = "$nsite"; done; nvs=$(grep -rnE "child_process|spawn\(|spawnSync\(|execSync\(|execFile\(|exec\(" bbj-vscode/src --include=*.ts --include=*.cjs | grep -vc "/generated/"); nij=$(grep -rnE "new GeneralCommandLine|new ProcessBuilder|Runtime\.getRuntime" bbj-intellij/src | wc -l); nsc=$(ls bbj-vscode/tools/*.bbj | wc -l); test "$(grep -cE "^- \[SEC-05\]\[candidate\] " "$f")" = "$((nvs + nij + nsc))"; tot=$(grep -cE "^- \[SEC-0[1245]\]\[[a-z-]+\] " "$f"); res=$(grep -cE "^- \[SEC-0[1245]\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — " "$f" || true); pend=$(grep -cE "^- \[SEC-0[1245]\]\[[a-z-]+\] .* — pending$" "$f"); test "$tot" = "$((res + pend))"; test "$res" = 0; led=0; for c in 61 62 63 64; do led=$((led + $(grep -cE "^id:[[:space:]]+P$c-D1-" .planning/reviews/$c-COVERAGE.md))); done; test "$(grep -cE "^\| P6[1-4]-D1-[0-9]{3} \|" "$f")" = "$led"; for s in "61-COVERAGE.md" "62-COVERAGE.md" "63-COVERAGE.md" "64-COVERAGE.md" "routing table" "P64-D1-001" "Disclosure-limited per D-14" "disposition: duplicate" "P63-D1-002" "classification: major" "SEC-01" "SEC-02" "SEC-04" "SEC-05"; do grep -qF "$s" "$f"; done; sed -n "/^## Phase 65 Close-Out/,\$p" "$f" > /tmp/p65co.txt; for s in "A." "B." "C." "D." "E." "F." "G."; do grep -qF "$s" /tmp/p65co.txt; done; test "$(grep -cE "^id:" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md)"; echo P65_01_SKELETON_OK'</automated>
  </verify>
  <done>`65-COVERAGE.md` exists with the full frozen skeleton: the swept-tree SHA, the structural break
  stated and evidenced by a live `RU-65` grep returning zero, the two recording-shape resolutions
  (`unit:` carries a surface ID; the Phase 64 triage vocabulary is absent by decision), the D1-only
  ID namespace with its `major`-for-every-record consequence spelled out, the `{2,4,8}` effort scale,
  the dedup source, and the synthesis, evidence, disclosure and scope rules — followed by
  `## Surface Enumeration Register` with all four denominators derived live from recorded commands
  and their literal outputs plus a drift statement against each D-02 baseline, the 4/4 generator/
  handler symmetry proven by string equality rather than narrated, `## Inherited Findings Ledger`
  with one live-derived row per inherited D1 finding and all five inheritance facts re-confirmed,
  `## Stopping Rule & Write Contract` with its four-part rule, four stubbed surface sections whose
  `### Verdicts` blocks already hold one placeholder line for every enumerated item of all four
  surfaces, and a stubbed seven-section close-out. Nothing is swept and no finding is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep SEC-01 — the four HTML generators, every interpolation and DOM-sink candidate within them, and the CSP posture</name>
  <files>.planning/reviews/65-COVERAGE.md</files>
  <precondition>`.planning/reviews/65-COVERAGE.md` exists with `## Surface Enumeration Register`,
  `## Inherited Findings Ledger`, `## Stopping Rule & Write Contract` and all four stubbed surface
  sections, Task 1 has committed, and `## SEC-01` still carries only placeholder verdict lines — the
  section contains no line matching the resolved-verdict shape. Halt and report otherwise; this task
  appends into a frozen skeleton and does not create one.</precondition>
  <reversibility rating="one-way">This is the first task in the phase that can record a `critical` or
  `high` security finding, and its output is committed to a public forkable repository where git
  history preserves an over-disclosed finding even after a later edit. **The `checkpoint:decision` a
  `one-way` rating would normally earn is deliberately waived by D-14 itself**, which states that
  Phase 62 D-09's checkpoint approved the rendered disclosure shape and that Phases 63 and 64 carried
  it unchanged, so a fourth re-approval of an unchanged rule is not worth a user interruption. The
  gate is enforced structurally instead: a `must_haves` prohibition, the required marker phrase
  `Disclosure-limited per D-14`, and an acceptance criterion asserting the file's `critical`/`high`
  severity count equals its marker count.</reversibility>
  <read_first>
    - `.planning/reviews/65-COVERAGE.md` — §header (the synthesis, evidence, disclosure and
      classification rules), §`## Surface Enumeration Register` → the SEC-01 block,
      §`## Inherited Findings Ledger` → every row whose surfaces column names `SEC-01`,
      §`## Stopping Rule & Write Contract`, and the stubbed `## SEC-01` section this task fills
    - `.planning/reviews/INVENTORY.md` §"Finding Standard" 3a-3d, §"Finding Record Template" (the
      exact 13 fields, in order), §"Frozen Open-Issue Snapshot" (the 15-issue dedup list),
      and the **disposition vocabulary at line 154**, including `duplicate`, which D-04 relies on
    - `.planning/REQUIREMENTS.md` — **SEC-01** verbatim, plus RVW-06 and RVW-07
    - `.planning/ROADMAP.md` §"Phase 65" — **criterion 1** in particular
    - `.planning/phases/65-cross-cutting-security-audit/65-CONTEXT.md` — D-04, D-05, D-08, D-09,
      D-11, D-12, D-14, D-15
    - `.planning/reviews/62-COVERAGE.md` **lines 87-96 only** — `### SEC-01/SEC-02 Surface Handoff`,
      whose fact (1) concludes that **no editor-selection, document-text, `config.bbx`,
      workspace-path or catalog value reaches any of the four `getHtml()` strings**, whose fact (2)
      documents the CSP directive set and the `Math.random()` nonce, and whose fact (4) bounds the
      blast radius. This is the baseline D-05 requires this task to **test against the whole
      enumerated surface**, not to rediscover
    - `.planning/reviews/62-COVERAGE.md` **lines 100-200 only** — the `P62-D1-001` and `P62-D1-002`
      records in full, since both are SEC-01/SEC-02 owners this task cross-references rather than
      re-records. Do not read the rest of that file
    - **Review targets, read in full — the four generators from Task 1's enumeration:**
      `bbj-vscode/src/msgbox-composer-webview.ts` (373),
      `bbj-vscode/src/addwindow-composer-webview.ts` (408),
      `bbj-vscode/src/addchildwindow-composer-webview.ts` (431),
      `bbj-vscode/src/setopts-composer-webview.ts` (321)
    - **Context only, no finding may be located in them:** `bbj-vscode/src/msgbox-composer.ts`,
      `addwindow-composer.ts`, `addchildwindow-composer.ts`, `setopts-composer-ui.ts` and
      `setopts-catalog.ts` — the UI/logic siblings that supply the values reaching the webview DOM.
      Read them only far enough to trace where a value in a candidate line comes from
    - **Do NOT read** any file under `.planning/codebase/`, and do not open `## SEC-02`, `## SEC-04`
      or `## SEC-05`
  </read_first>
  <action>
Sweep `## SEC-01 — Webview HTML generation & CSP posture` and close it against the four-part stopping
rule. Resolve **every** placeholder line in its `### Verdicts` block — no line is deleted, merged or
left pending — and write `### Enumeration`, `### CSP Posture`, `### Findings`,
`### Not-reproducible dispositions`, `### Cross-references` and `### Surface closure`.

**A. `### Enumeration`.** Restate the SEC-01 denominator from the register, re-run its two derivation
commands and record their literal outputs again here, so this section is self-contained for a reader
who starts at the surface rather than at the header. Where the re-derivation differs from Task 1's,
that is a drift *within the phase* and is written up as such.

**B. Generator verdicts.** For each `[SEC-01][generator]` line, resolve the placeholder to a verdict
plus a written line. The written line names the concrete checks applied, phrased against SEC-01's own
REQUIREMENTS.md wording: which values the generator interpolates into the returned HTML string, where
each of those values originates, whether any of them is escaped or provably safe, whether any
editor-selection, document-text, `config.bbx`, workspace-path or catalog value reaches the string,
and what the generator's panel options are. Cite `file:line` anchors inside the written line — a
verdict without anchors is a bare verdict wearing a sentence.

**C. Candidate verdicts.** For each `[SEC-01][candidate]` line, resolve the placeholder. Three
outcomes are legitimate and each has a required shape:

- **A real interpolation or sink that is safe** → `pass` with a written line naming what flows in,
  from where, and what makes it safe — an escaping step, a provably-constant source, or a DOM API
  that does not parse HTML. Naming the API is not enough on its own: name the *value* and its origin.
- **A real interpolation or sink that is not safe** → `fail` naming the discharging finding ID. Under
  D-04 that is an **inherited owner ID** wherever the defect sits entirely inside one already-reviewed
  file and says what a `P62-D1-*` record already says; it is a **new `P65-D1-nnn`** only where the
  cross-cutting view shows a gap between two modules, an asymmetry between generators, or a chain in
  which two acceptable behaviours compose into an unacceptable one.
- **Not an interpolation or sink at all** → `n/a` with a written exclusion reason on the same line
  (a document-edit interpolation rather than an HTML one, a CSP-string assembly step whose product is
  audited at its own line, a sink assigned a constant empty string, and so on). The line is resolved,
  never removed.

Where a candidate's safety turns on a value's provenance that you cannot settle without constructing
an input you cannot construct here, resolve it to `undetermined` and write the corresponding entry
under `### Not-reproducible dispositions` stating what *is* established and precisely what is not —
D-11's rule, and the reason `P63-D1-002` is named in this file's header.

**D. `### CSP Posture`.** ROADMAP criterion 1 requires the CSP posture to be **documented**, which
means a clean result must be stated as a checked-and-clean result, distinguishable from an unchecked
one (D-12). Record, **per generator and with the check that established each**: the full directive
set as emitted; whether a `nonce` is generated per render and how; whether `script-src` is bound to
that nonce; whether any script source outside the nonce is permitted; the panel options passed at
creation; and whether `localResourceRoots` is set or left to its default. Then state the
**cross-generator comparison explicitly** — where all four agree, say so as a positive result with
the check named; where any one differs from the other three, that difference is the finding, because
four near-duplicate generators diverging on a security control is precisely the asymmetry D-04 names.
Cross-reference `P62-D1-002` for the nonce-source question rather than re-recording it, while still
stating the nonce mechanism as part of the documented posture — a cross-reference discharges D-04 and
leaves criterion 1 fully answered.

**E. `### Findings`.** For every claim that clears INVENTORY's `repro` tier for D1, write one record
under `### Findings` using the 13-field template verbatim, one field per line, inside a fenced block,
in the template's exact field order: `id`, `unit`, `location`, `dimension`, `secondary`, `severity`,
`evidence_tier`, `evidence`, `failure_scenario`, `classification`, `effort`, `dedup`, `disposition`.
`unit:` is `SEC-01`. `dimension:` is `D1`. `classification:` is `major` with all six INVENTORY 3c
tests recorded pass/fail inline and test (6) noted as independently forcing it. `effort:` is `2`, `4`
or `8`. `dedup:` is never blank and is checked against the frozen 15. `evidence:` opens with the
basis and states in one clause why it carries no runnable reproduction where none is possible. No
`triage:` field is written. Any record rated `critical` or `high` follows D-14 strictly — surface,
problem class and impact; no trigger sequence, no payload, no working procedure — and opens its
`evidence:` field with the exact phrase `Disclosure-limited per D-14`, so the redaction is auditable
rather than indistinguishable from thin evidence. Any finding that duplicates an inherited record
carries `disposition: duplicate` naming the owning ID and a one-clause reason.

**F. `### Cross-references`.** One entry per ledger row whose surfaces column names SEC-01: the
inherited ID, what it establishes for this surface, and whether this sweep confirms, extends or
disagrees with it. **If this sweep disagrees with `62-COVERAGE.md`'s Surface Handoff conclusion, the
disagreement is recorded here as a finding with the reproduction that settles it** — not as a silent
correction, and not as an edit to that closed file (D-05, D-15).

**G. `### Surface closure`.** State the four-part stopping rule discharged part by part, each with
the count or command that evidences it, and state explicitly whether the live-derived denominator
agreed with D-02's baseline of four generators.
  </action>
  <acceptance_criteria>
    - Every SEC-01 enumerated line is resolved and none is pending: within the section extracted by `sed -n '/^## SEC-01 /,/^## SEC-02 /p' .planning/reviews/65-COVERAGE.md`, the count of `^- \[SEC-01\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — ` equals the count of `^- \[SEC-01\]\[[a-z-]+\] `, and the count of `^- \[SEC-01\]\[[a-z-]+\] .* — pending$` prints `0`
    - The enumeration is still closed against its live derivation — nothing was added or lost while sweeping: within the section, the count of `^- \[SEC-01\]\[generator\] ` equals the live generator-command line count, and the count of `^- \[SEC-01\]\[candidate\] ` equals the live candidate-command line count
    - Every `pass` line is a written line, not a bare verdict: within the section, the count of `^- \[SEC-01\]\[[a-z-]+\] .* — pass — ` equals the count of the same pattern with `.{120,}` appended
    - Every `fail` line names a discharging finding ID, new or inherited — D-04 as a grep: within the section, the count of `^- \[SEC-01\]\[[a-z-]+\] .* — fail — ` equals the count of `^- \[SEC-01\]\[[a-z-]+\] .* — fail — .*P6[1-5]-D[1-8]-[0-9]{3}`
    - Every `n/a` line carries a written exclusion reason: within the section, the count of `^- \[SEC-01\]\[[a-z-]+\] .* — n/a — ` equals the count of the same pattern with `.{40,}` appended
    - Every `undetermined` line routes to a disposition: within the section, the count of `^- \[SEC-01\]\[[a-z-]+\] .* — undetermined — ` equals the count of `^- \[SEC-01\]\[[a-z-]+\] .* — undetermined — .*not-reproducible`
    - Coverage is file-granular across all four generators: the section contains each of `msgbox-composer-webview.ts`, `addwindow-composer-webview.ts`, `addchildwindow-composer-webview.ts` and `setopts-composer-webview.ts`
    - `### CSP Posture` records the D-12 positive result with the checks that established it: the section contains `### CSP Posture`, and within it the strings `Content-Security-Policy`, `nonce`, `script-src`, `default-src`, `enableScripts`, `retainContextWhenHidden` and `localResourceRoots` all appear
    - The Phase 62 baseline is tested rather than rediscovered: the section contains `P62-D1-001`, `P62-D1-002` and `Surface Handoff`
    - Finding IDs are well-formed, unique, D1-only and never from the reserved namespace: `grep -oE '^id:[[:space:]]+P65-D1-[0-9]{3}' | sort | uniq -d` prints nothing, `grep -cE '^id:[[:space:]]+P65-D[2-9]-'` prints `0`, and `grep -cE '^id:[[:space:]]+P00-'` prints `0`
    - Every finding carries every required field and the `unit:` field carries a surface ID: the counts of `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:` and `^disposition:` each equal the count of `^id:`, and the count of `^unit:[[:space:]]+SEC-0[1245]$` also equals it while `^unit:[[:space:]]+RU-` prints `0`
    - The phase-wide record constraints hold: `grep -cE '^dedup:[[:space:]]*$'` prints `0`, `grep -c '^triage:'` prints `0`, `grep -cE '^effort:[[:space:]]+(2|4|8)$'` equals the `^id:` count, `grep -cE '^classification:[[:space:]]+major'` equals the `^id:` count, and `grep -cE '^classification:[[:space:]]+easy'` prints `0`
    - The disclosure rule is auditable phase-wide: the count of `^severity:[[:space:]]+(critical|high)` equals the count of `Disclosure-limited per D-14`
    - No finding is anchored outside this milestone's reviewable surface: the counts of `^location:[[:space:]]+java-interop/`, `^location:[[:space:]]+bbj-vscode/src/language/generated/`, `^location:[[:space:]]+bbj-vscode-deprecated/`, `^location:[[:space:]]+CLAUDE\.md`, `^location:[[:space:]]+bbj-vscode/VERBs\.md` and `^location:[[:space:]]+documentation/` each print `0`
    - The other three surfaces are untouched: the counts of `^- \[SEC-02\]\[[a-z-]+\] .* — pending$`, `^- \[SEC-04\]\[[a-z-]+\] .* — pending$` and `^- \[SEC-05\]\[[a-z-]+\] .* — pending$` each equal that surface's full enumerated-line count
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/65-COVERAGE.md; sed -n "/^## SEC-01 /,/^## SEC-02 /p" "$f" > /tmp/p65s1.txt; s=/tmp/p65s1.txt; tot=$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] " "$s"); res=$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — " "$s"); test "$tot" = "$res"; test "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — pending$" "$s" || true)" = 0; G4=$(grep -rln "getHtml\|webview.html" bbj-vscode/src --include=*.ts | sort); ngen=$(printf "%s\n" "$G4" | wc -l); test "$(grep -cE "^- \[SEC-01\]\[generator\] " "$s")" = "$ngen"; ncand=$(grep -nE "\\\$\{|innerHTML|outerHTML|insertAdjacentHTML|document\.write" $G4 | wc -l); test "$(grep -cE "^- \[SEC-01\]\[candidate\] " "$s")" = "$ncand"; test "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — pass — " "$s" || true)" = "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — pass — .{120,}" "$s" || true)"; test "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — fail — " "$s" || true)" = "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — fail — .*P6[1-5]-D[1-8]-[0-9]{3}" "$s" || true)"; test "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — n/a — " "$s" || true)" = "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — n/a — .{40,}" "$s" || true)"; test "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — undetermined — " "$s" || true)" = "$(grep -cE "^- \[SEC-01\]\[[a-z-]+\] .* — undetermined — .*not-reproducible" "$s" || true)"; for x in "msgbox-composer-webview.ts" "addwindow-composer-webview.ts" "addchildwindow-composer-webview.ts" "setopts-composer-webview.ts" "### CSP Posture" "Content-Security-Policy" "nonce" "script-src" "default-src" "enableScripts" "retainContextWhenHidden" "localResourceRoots" "P62-D1-001" "P62-D1-002" "Surface Handoff"; do grep -qF "$x" "$s"; done; test -z "$(grep -oE "^id:[[:space:]]+P65-D1-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P65-D[2-9]-" "$f" || true)" = 0; test "$(grep -cE "^id:[[:space:]]+P00-" "$f" || true)" = 0; n=$(grep -cE "^id:" "$f" || true); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f" || true)" = "$n"; done; test "$(grep -cE "^unit:[[:space:]]+SEC-0[1245]$" "$f" || true)" = "$n"; test "$(grep -cE "^unit:[[:space:]]+RU-" "$f" || true)" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f" || true)" = 0; test "$(grep -c "^triage:" "$f" || true)" = 0; test "$(grep -cE "^effort:[[:space:]]+(2|4|8)$" "$f" || true)" = "$n"; test "$(grep -cE "^classification:[[:space:]]+major" "$f" || true)" = "$n"; test "$(grep -cE "^classification:[[:space:]]+easy" "$f" || true)" = 0; test "$(grep -cE "^severity:[[:space:]]+(critical|high)" "$f" || true)" = "$(grep -cF "Disclosure-limited per D-14" "$f" || true)"; for p in "java-interop/" "bbj-vscode/src/language/generated/" "bbj-vscode-deprecated/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f" || true)" = 0; done; for su in 02 04 05; do test "$(grep -cE "^- \[SEC-$su\]\[[a-z-]+\] .* — pending$" "$f")" = "$(grep -cE "^- \[SEC-$su\]\[[a-z-]+\] " "$f")"; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md)"; echo P65_01_SEC01_OK'</automated>
  </verify>
  <done>`## SEC-01` is closed under the four-part stopping rule: every generator and every candidate
  interpolation-or-DOM-sink line carries a verdict with no placeholder left in the section, each
  `pass` carrying a written line of concrete checks with `file:line` anchors, each `fail` naming
  either a new `P65-D1-nnn` or the inherited owner that already covers it, each `n/a` carrying a
  written exclusion reason and each `undetermined` routed to a not-reproducible disposition;
  `### CSP Posture` documents the directive set, the per-render nonce, the `script-src` binding, the
  panel options and the `localResourceRoots` position for every generator, states the cross-generator
  comparison explicitly so a checked-and-clean result is distinguishable from an unchecked one, and
  cross-references the nonce-source owner rather than re-recording it; and `62-COVERAGE.md`'s Surface
  Handoff conclusion has been tested against the whole enumerated surface with any disagreement
  recorded here as a finding rather than as an edit to that closed file.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep SEC-02 — the four onDidReceiveMessage handlers, every case arm, and the runtime-validation posture between receipt and first side effect</name>
  <files>.planning/reviews/65-COVERAGE.md</files>
  <precondition>`## SEC-01` is closed — its section contains no line matching the pending shape — Task 2
  has committed, and `## SEC-02` still carries only placeholder verdict lines. Halt and report
  otherwise.</precondition>
  <read_first>
    - `.planning/reviews/65-COVERAGE.md` — §header, §`## Surface Enumeration Register` → the SEC-02
      block including the case-arm and `default:`-arm derivations, §`## Inherited Findings Ledger` →
      every row whose surfaces column names `SEC-02`, §`## Stopping Rule & Write Contract`, the
      stubbed `## SEC-02` section this task fills, and `## SEC-01`'s `### Verdicts` and
      `### CSP Posture` as the rendering to mirror. Do not re-read the rest of `## SEC-01`
    - `.planning/reviews/INVENTORY.md` §"Finding Standard" 3a-3d, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", and the disposition vocabulary at **line 154**
    - `.planning/REQUIREMENTS.md` — **SEC-02** verbatim, plus RVW-06 and RVW-07
    - `.planning/ROADMAP.md` §"Phase 65" — **criterion 2** in particular: *every webview→extension
      message handler validates message shape and value range before acting, with any gaps flagged*
    - `.planning/phases/65-cross-cutting-security-audit/65-CONTEXT.md` — **D-13 above all**, plus
      D-04, D-08, D-09, D-11, D-14, D-15
    - `.planning/reviews/62-COVERAGE.md` **lines 87-96 only** — Surface Handoff fact (3), which states
      that no handler performs a runtime shape, type or value-range check before acting, and fact (4),
      which bounds the blast radius to `vscode.workspace.applyEdit` on the user's open document. This
      is the baseline to test against the whole enumerated surface, and `P62-D1-001` is its owner
    - **Review targets, read in full — the four handlers and their surrounding files, already read in
      Task 2 and re-read here only at the handler bodies and the sinks they reach:**
      `bbj-vscode/src/msgbox-composer-webview.ts`, `addwindow-composer-webview.ts`,
      `addchildwindow-composer-webview.ts`, `setopts-composer-webview.ts`
    - **Context only, no finding may be located in them:** `bbj-vscode/src/msgbox-composer.ts`,
      `addwindow-composer.ts`, `addchildwindow-composer.ts`, `setopts-composer-ui.ts`,
      `setopts-catalog.ts` — the `build()` implementations each handler's payload flows into, read
      far enough to establish what the first side effect of each arm actually is
    - **Do NOT read** any file under `.planning/codebase/`, and do not open `## SEC-04` or `## SEC-05`
  </read_first>
  <action>
Sweep `## SEC-02 — Webview → extension message trust` and close it against the four-part stopping
rule. Resolve **every** placeholder line in its `### Verdicts` block and write `### Enumeration`,
`### Runtime Validation Posture`, `### Findings`, `### Not-reproducible dispositions`,
`### Cross-references` and `### Surface closure`.

**A. `### Enumeration`.** Restate the SEC-02 denominator, re-run its three derivation commands — the
handler enumeration, the case-arm count and the `default:`-arm distribution — and record their
literal outputs here. Note the 4/4 symmetry with SEC-01 established in the register: the same four
files. Any drift from Task 1's derivation is written up as such.

**B. Handler verdicts.** For each `[SEC-02][handler]` line, resolve the placeholder to a verdict plus
a written line. The written line answers, for that handler as a whole: what the message value
actually is at the moment of receipt; what runtime check — if any — sits between receipt and the
first side effect; whether an unrecognised `type` is rejected, ignored or falls through; and what the
handler's blast radius is, named as concrete sinks with `file:line` anchors.

**C. Case-arm verdicts.** For each `[SEC-02][case]` line, resolve the placeholder. The written line
names, for that specific arm: the **first side effect** the arm performs (a document edit, a
`postMessage` back, a state mutation, a no-op); every field of the payload the arm reads before that
side effect; and **the runtime check, if any, applied to each of those fields first** — a type check,
a range check, an allow-list membership test, a length bound, a numeric coercion with validation. An
arm that acts on a payload field with no runtime check gets `fail` naming its discharging ID; an arm
whose only action is a `postMessage` of constant data, or a no-op, gets `pass` or `n/a` with the
reason stated.

**D. `### Runtime Validation Posture`.** This subsection is where criterion 2 is discharged, and
D-13 governs it absolutely. State plainly and up front: **the handlers are typed with a TypeScript
annotation on the message parameter, that annotation is erased at compile time, and the value
arriving from the webview is whatever `postMessage` sent — so the annotation is not evidence of
validation and is not accepted as an answer here.** Then answer, per handler:

1. What runtime check sits between message receipt and the first side effect. If none, say none.
2. Whether the `switch` on the message type has a `default` branch, and if so what it does — because
   where the `default` branch is the de-facto guard, D-13 requires that to be said precisely rather
   than glossed. **Record the per-handler `default:` derivation command and its literal output**, and
   state whether the distribution is uniform across the four handlers.
3. Whether the payload's fields are checked for type, for shape or for value range before being read.
4. What the first side effect actually is, and therefore what an unchecked field can reach.

Then the **cross-handler comparison**, stated explicitly: where all four agree, that is a positive
result and is recorded as one with the check named (D-12); **where any one of the four differs from
the others on a security control, that difference is the finding**, because four near-duplicate
handlers diverging on a guard is exactly the asymmetry D-04 names as a legitimate basis for a new
`P65-*` ID. Do not assume the distribution — derive it, record the output, and let the record follow
the output.

Close the subsection by stating what a runtime shape validator would have to cover, expressed as the
concrete field set the four arms read — **as an observation about the gap, not as a proposal or a
design**, since adding such a validator is a new capability deferred out of this phase.

**E. `### Findings`.** Same 13-field template, same rules as Task 2: `unit:` is `SEC-02`,
`dimension:` is `D1`, `classification:` is `major` with all six 3c tests inline, `effort:` on
`{2,4,8}`, `dedup:` non-blank against the frozen 15, no `triage:` field, D-14's marker on any
`critical`/`high` record, `disposition: duplicate` naming the owner for anything an inherited record
already covers. `P62-D1-001` owns the no-runtime-validation claim at the four handlers; a new
`P65-*` ID here must earn itself on the cross-cutting view — an asymmetry between handlers, a gap
between the handler and the sink it reaches, or a chain composing two acceptable behaviours into an
unacceptable one — and its record must say which of the three it is.

**F. `### Cross-references`.** One entry per ledger row whose surfaces column names SEC-02, stating
what it establishes here and whether this sweep confirms, extends or disagrees with it.

**G. `### Surface closure`.** The four-part stopping rule discharged part by part with the count or
command evidencing each, and an explicit statement of whether the live denominator agreed with
D-02's baseline of four handlers.
  </action>
  <acceptance_criteria>
    - Every SEC-02 enumerated line is resolved and none is pending: within the section extracted by `sed -n '/^## SEC-02 /,/^## SEC-04 /p' .planning/reviews/65-COVERAGE.md`, the count of `^- \[SEC-02\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — ` equals the count of `^- \[SEC-02\]\[[a-z-]+\] `, and the count of `^- \[SEC-02\]\[[a-z-]+\] .* — pending$` prints `0`
    - The enumeration is still closed against its live derivation: within the section, the count of `^- \[SEC-02\]\[handler\] ` equals the live handler-command line count and the count of `^- \[SEC-02\]\[case\] ` equals the live case-arm sum
    - Every `pass` line is a written line, every `fail` names a discharging ID, every `n/a` carries a reason and every `undetermined` routes to a disposition — the same four identities as SEC-01, applied within this section
    - Coverage is file-granular across all four handlers: the section contains each of `msgbox-composer-webview.ts`, `addwindow-composer-webview.ts`, `addchildwindow-composer-webview.ts` and `setopts-composer-webview.ts`
    - `### Runtime Validation Posture` refuses the type annotation as evidence and answers the real question: the section contains `### Runtime Validation Posture`, and within it the strings `compile`, `postMessage`, `default`, `switch` and `applyEdit` all appear
    - The `default:`-arm distribution was derived rather than assumed: the section contains `default:` and contains at least one of `uniform` or `asymmetr`
    - The Phase 62 baseline is tested rather than rediscovered: the section contains `P62-D1-001`
    - Every finding record constraint from Task 2 still holds phase-wide: IDs unique and well-formed, no `P65-D[2-9]-` ID, no `P00-` ID, every required field count equal to the `^id:` count, `^unit:` always a surface ID and never an `RU-` token, no blank `dedup:`, zero `^triage:` lines, every `^effort:` on `{2,4,8}`, every `^classification:` `major` with zero `easy`, and the `critical`/`high` severity count equal to the `Disclosure-limited per D-14` count
    - No finding is anchored outside this milestone's reviewable surface: the six out-of-scope `^location:` prefixes each print `0`
    - SEC-04 and SEC-05 are untouched: the counts of `^- \[SEC-04\]\[[a-z-]+\] .* — pending$` and `^- \[SEC-05\]\[[a-z-]+\] .* — pending$` each still equal that surface's full enumerated-line count
    - No source file was modified anywhere and no immutable record was touched: `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/65-COVERAGE.md; sed -n "/^## SEC-02 /,/^## SEC-04 /p" "$f" > /tmp/p65s2.txt; s=/tmp/p65s2.txt; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] " "$s")" = "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — (pass|fail|undetermined|n/a) — " "$s")"; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — pending$" "$s" || true)" = 0; H4=$(grep -rln "onDidReceiveMessage" bbj-vscode/src --include=*.ts | sort); nh=$(grep -rn "onDidReceiveMessage" bbj-vscode/src --include=*.ts | wc -l); test "$(grep -cE "^- \[SEC-02\]\[handler\] " "$s")" = "$nh"; ncase=$(for x in $H4; do sed -n "/onDidReceiveMessage/,/^    });/p" "$x" | grep -cE "case .[a-z]"; done | awk "{s+=\$1} END{print s+0}"); test "$(grep -cE "^- \[SEC-02\]\[case\] " "$s")" = "$ncase"; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — pass — " "$s" || true)" = "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — pass — .{120,}" "$s" || true)"; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — fail — " "$s" || true)" = "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — fail — .*P6[1-5]-D[1-8]-[0-9]{3}" "$s" || true)"; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — n/a — " "$s" || true)" = "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — n/a — .{40,}" "$s" || true)"; test "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — undetermined — " "$s" || true)" = "$(grep -cE "^- \[SEC-02\]\[[a-z-]+\] .* — undetermined — .*not-reproducible" "$s" || true)"; for x in "msgbox-composer-webview.ts" "addwindow-composer-webview.ts" "addchildwindow-composer-webview.ts" "setopts-composer-webview.ts" "### Runtime Validation Posture" "compile" "postMessage" "default" "switch" "applyEdit" "default:" "P62-D1-001"; do grep -qF "$x" "$s"; done; grep -qE "uniform|asymmetr" "$s"; test -z "$(grep -oE "^id:[[:space:]]+P65-D1-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P65-D[2-9]-" "$f" || true)" = 0; test "$(grep -cE "^id:[[:space:]]+P00-" "$f" || true)" = 0; n=$(grep -cE "^id:" "$f" || true); for k in unit location dimension severity evidence_tier evidence failure_scenario classification effort dedup disposition; do test "$(grep -cE "^$k:" "$f" || true)" = "$n"; done; test "$(grep -cE "^unit:[[:space:]]+SEC-0[1245]$" "$f" || true)" = "$n"; test "$(grep -cE "^unit:[[:space:]]+RU-" "$f" || true)" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f" || true)" = 0; test "$(grep -c "^triage:" "$f" || true)" = 0; test "$(grep -cE "^effort:[[:space:]]+(2|4|8)$" "$f" || true)" = "$n"; test "$(grep -cE "^classification:[[:space:]]+major" "$f" || true)" = "$n"; test "$(grep -cE "^classification:[[:space:]]+easy" "$f" || true)" = 0; test "$(grep -cE "^severity:[[:space:]]+(critical|high)" "$f" || true)" = "$(grep -cF "Disclosure-limited per D-14" "$f" || true)"; for p in "java-interop/" "bbj-vscode/src/language/generated/" "bbj-vscode-deprecated/" "CLAUDE\.md" "bbj-vscode/VERBs\.md" "documentation/"; do test "$(grep -cE "^location:[[:space:]]+$p" "$f" || true)" = 0; done; for su in 04 05; do test "$(grep -cE "^- \[SEC-$su\]\[[a-z-]+\] .* — pending$" "$f")" = "$(grep -cE "^- \[SEC-$su\]\[[a-z-]+\] " "$f")"; done; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md)"; echo P65_01_SEC02_OK'</automated>
  </verify>
  <done>`## SEC-02` is closed under the four-part stopping rule: every handler and every case arm carries
  a verdict with no placeholder left in the section, each `pass` a written line with `file:line`
  anchors, each `fail` naming a new or inherited discharging ID, each `n/a` carrying a reason and
  each `undetermined` routed to a disposition. `### Runtime Validation Posture` states up front that
  the TypeScript annotation is erased at compile time and refuses it as evidence, then answers per
  handler what runtime check sits between receipt and the first side effect, what the `switch`'s
  `default` branch does — with the per-handler derivation command and its literal output recorded, so
  the distribution across the four handlers is derived rather than assumed — what the payload fields
  are checked for, and what an unchecked field can reach; the cross-handler comparison is stated
  explicitly, with agreement recorded as a positive result and any divergence recorded as the finding
  it is. `P62-D1-001` is cross-referenced as the owner of the no-runtime-validation claim rather than
  re-recorded, and every phase-wide record constraint still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure, false-assurance and record-integrity threats. Rows marked *subject-matter*
are boundaries this plan **documents**; remediation is Phase 67's.

| Boundary | Description |
|----------|-------------|
| webview content → the four generated HTML strings (*subject-matter*) | Everything interpolated into the markup crosses into a rendering context where a string becomes markup. |
| webview DOM → the client-side script's HTML sinks (*subject-matter*) | Values arriving after load via `postMessage` are written into the DOM; a sink that parses HTML is a different boundary from one that does not. |
| webview → extension host, via `onDidReceiveMessage` (*subject-matter*) | The message value is whatever `postMessage` sent; the TypeScript annotation on the parameter does not exist at runtime. |
| an unchecked payload field → `vscode.workspace.applyEdit` on the user's open document (*subject-matter*) | The first side effect of the `insert`/`apply` arms writes into a real file the user has open. |
| four near-duplicate generators/handlers → each other (*subject-matter*) | Where one guards something the other three do not, the divergence is invisible to any single-file review. |
| `65-COVERAGE.md` → public repository readers | `.planning/` is committed, this repository is public and forkable, and git history preserves an over-disclosure permanently. |
| code read during the sweep → the committed record | Credentials, tokens or absolute developer-machine paths encountered while reading can be copied verbatim into a record. |
| a written verdict line → a reader who cannot re-run the check | Phase 67's apply path and Phase 68's DOC-03 consume `pass` verdicts as evidence. |
| an inherited finding → this phase's ID namespace | Re-recording a `P6{1,2,3,4}-D1-*` defect under a `P65-*` ID makes Phase 68 list one defect twice. |
| a live-derived denominator → the completeness claim | With no INVENTORY grid, the enumeration *is* the completeness proof; a denominator that can be edited after the fact proves nothing. |
| shared `65-COVERAGE.md` → the other two plans | One file, three writers. |
| the reviewed working tree → any command this plan runs | A command with side effects would change the artifact under review mid-sweep. |
| `java-interop/`, `generated/`, `CLAUDE.md`, `documentation/` → this phase's finding namespace | Out of scope, or `RU-D8-01`'s unowned surface; a `location:` in any of them claims a row this phase does not own. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-P01-S1 | Tampering | values reaching the four generated HTML strings and the client-side DOM sinks within them (*subject-matter*) | high | transfer | Task 2 resolves every enumerated interpolation and sink candidate to a verdict with the value's origin named; remediation transfers to Phase 67, public disclosure to Phase 69 under D-14. |
| T-65-P01-S2 | Tampering | webview→extension messages reaching `vscode.workspace.applyEdit` with no runtime shape or range check (*subject-matter*) | high | transfer | Task 3's `### Runtime Validation Posture` answers per handler and per arm what check sits between receipt and the first side effect, with the type annotation explicitly refused as evidence (D-13); remediation transfers to Phase 67. |
| T-65-P01-01 | Information Disclosure | a `critical`/`high` finding written as a trigger sequence, payload or working procedure and committed to a public forkable repository | high | mitigate | D-14's two-tier rule applied strictly — surface, problem class, impact, and stop — with the exact phrase `Disclosure-limited per D-14` opening every such `evidence:` field and an acceptance criterion asserting the file's `critical`/`high` severity count equals its marker count. Rated `one-way` on Task 2; the `checkpoint:decision` is waived by D-14 itself and replaced by this structural enforcement. |
| T-65-P01-02 | Repudiation | a security mechanism asserted rather than shown — the `P63-D1-002` failure mode, a false claim surviving to public filing | high | mitigate | D-11 prohibition plus the stopping rule's part (iii): a claim that does not clear INVENTORY's `repro` tier for D1 goes under `### Not-reproducible dispositions` with what is established and what is not. Acceptance criteria require every `undetermined` verdict to name its disposition entry and every `pass` line to carry at least 120 bytes of written checks. |
| T-65-P01-03 | Repudiation | a denominator quietly widened or narrowed after the sweep, so "audited end to end" rests on an enumeration nobody can re-derive | high | mitigate | Task 1 closes all four enumerations before any is swept, and every acceptance criterion in all three plans compares the file's line counts against the **live enumeration commands** rather than against a number typed into a plan — so a post-hoc edit to either side fails the gate. |
| T-65-P01-04 | Repudiation | an inherited finding re-recorded under a new `P65-*` ID, double-listing one defect in `MAJOR-REFACTORS.md` | high | mitigate | D-04 prohibition, the `## Inherited Findings Ledger` as the cross-reference source, and the `fail`-line gate accepting an inherited `P6[1-4]-D1-nnn` owner as a discharging ID so cross-referencing is the path of least resistance rather than an extra step. |
| T-65-P01-05 | Elevation of Privilege | a security finding classified `easy` and routed into Phase 67's apply path without passing through `MAJOR-REFACTORS.md` | high | mitigate | INVENTORY 3c test (6) forces `major` for any D1-primary finding; the header states the consequence as arithmetic and an acceptance criterion asserts every record is `major` and none is `easy`. |
| T-65-P01-06 | Tampering | a command with working-tree side effects run during the sweep | high | mitigate | Prohibition plus an acceptance criterion on every task asserting `git status --porcelain` is empty across `bbj-vscode`, `bbj-intellij`, `java-interop`, `.github` and all five immutable planning records. |
| T-65-P01-07 | Information Disclosure | a credential, EM token, password or absolute developer-machine path copied into a record while reading | high | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value and no absolute path is written into any field. |
| T-65-P01-08 | Tampering | editing `INVENTORY.md` or one of the four closed coverage files so a claim agrees | medium | mitigate | D-15 prohibition plus an acceptance criterion asserting `git status --porcelain` is empty for INVENTORY and all four prior coverage files; drift found in any of them is recorded as a finding here instead. |
| T-65-P01-09 | Repudiation | a `P65-D2-*` or other non-D1 ID issued, blurring which phase owns a correctness defect | medium | mitigate | D-08 prohibition plus an acceptance criterion asserting `grep -cE '^id:[[:space:]]+P65-D[2-9]-'` prints `0`. |
| T-65-P01-10 | Repudiation | an `effort:` value off the `{2,4,8}` scale, unlabellable for ISSUE-03 — Phase 63's exact defect | medium | mitigate | D-09 prohibition plus an acceptance criterion asserting the count of `^effort:` values on the scale equals the `^id:` count. |
| T-65-P01-11 | Tampering | three plans writing one `65-COVERAGE.md` | medium | mitigate | Serial waves with a `depends_on` chain (D-06); every task additionally asserts that the surfaces it does not own still carry their full placeholder count, so a stray write into another surface fails the gate. |
| T-65-P01-12 | Repudiation | a candidate line deleted rather than resolved to `n/a`, hiding an exclusion the refinement made | medium | mitigate | Prohibition plus the arithmetic-closure gates: the file's enumerated-line count must equal what the live enumeration command prints, so a deletion fails immediately. |
| T-65-P01-13 | Denial of Service | the SEC-01 sweep expanding into the composer logic/UI siblings that Phase 62's `RU-62-03` owns | low | accept | Those files are read as context only and no finding may be located in them; the boundary is stated in `<read_first>`. Accepted because the boundary is explicit and the out-of-scope `location:` gate does not need to cover an already-swept in-milestone tree. |
| T-65-P01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `ls`, `grep`, `sed`, `awk` and `test` only, so INVENTORY's Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `.planning/reviews/65-COVERAGE.md` exists, records the swept-tree SHA obtained at execution time,
  and states the structural break with `grep -c 'RU-65' INVENTORY.md` re-run live and its literal
  output recorded.
- All four surface enumerations are closed against their live derivation commands: the file's
  `[SEC-01][generator]`, `[SEC-01][candidate]`, `[SEC-02][handler]`, `[SEC-02][case]`, `[SEC-04][…]`
  and `[SEC-05][candidate]` line counts each equal what the corresponding command prints **at
  verification time**, so neither the file nor the tree can have drifted without the gate failing.
  No count in this plan's prose substitutes for that comparison.
- The 4/4 generator/handler symmetry is proven by byte-equality of the two sorted file lists.
- `## SEC-01` and `## SEC-02` carry no placeholder line; every enumerated line in both carries one of
  the four verdict tokens; every `pass` carries at least 120 bytes of written checks; every `fail`
  names a `P6[1-5]-D[1-8]-nnn` discharging ID; every `n/a` carries at least 40 bytes of reason; every
  `undetermined` names its not-reproducible disposition.
- `### CSP Posture` names the directive set, the nonce, `script-src`, `default-src`, the panel
  options and `localResourceRoots`; `### Runtime Validation Posture` names the compile-time erasure,
  `postMessage`, the `switch`, the `default` branch and `applyEdit`, and states whether the
  `default:`-arm distribution is uniform.
- The inherited ledger's row count equals the live sum of the four per-file `P6N-D1-` counts, and all
  five inheritance facts are recorded.
- Every finding carries all 13 template fields with a unique well-formed `P65-D1-nnn` ID, `unit:` a
  surface ID and never an `RU-` token, no `triage:` line anywhere, `effort:` on `{2,4,8}`,
  `classification: major` with zero `easy`, and a non-blank `dedup:`.
- The `critical`/`high` severity count equals the `Disclosure-limited per D-14` marker count.
- Zero `^location:` lines resolve into `java-interop/`, `bbj-vscode/src/language/generated/`,
  `bbj-vscode-deprecated/`, `CLAUDE.md`, `VERBs.md` or `documentation/`.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .github .planning/reviews/INVENTORY.md .planning/reviews/61-COVERAGE.md .planning/reviews/62-COVERAGE.md .planning/reviews/63-COVERAGE.md .planning/reviews/64-COVERAGE.md`
  is empty.
</verification>

<success_criteria>
- The phase has a completeness construct a reader can check, in place of the grid INVENTORY does not
  supply: four enumerations whose denominators are derived by recorded commands and whose every item
  carries a verdict, with the gates written as identities against those commands rather than as
  numbers a plan asserts (D-01, D-02).
- The recording shape is proven on real content and frozen: plans `65-02` and `65-03` append into a
  skeleton that already carries all four closed enumerations, the inherited ledger, the stopping rule
  and a stubbed close-out — and no format checkpoint is spent (D-08).
- ROADMAP criterion 1 is discharged: every interpolated value and DOM sink in composer markup and the
  `setopts-composer-webview.ts` markup is confirmed safe or flagged, and the CSP posture is documented
  per generator with the checks that established it (D-12).
- ROADMAP criterion 2 is discharged: every webview→extension handler and every case arm is answered
  on what runtime check sits between receipt and the first side effect, with the TypeScript
  annotation refused as evidence and any gap flagged (D-13).
- SEC-01 and SEC-02 advance to closable, with the D-04 discipline visible in the record: inherited
  findings cross-referenced by ID, new `P65-*` IDs justified by a between-modules gap, an asymmetry
  or a composition chain, and nothing recorded twice.
</success_criteria>

<output>
Create `.planning/phases/65-cross-cutting-security-audit/65-01-SUMMARY.md` when done.
</output>
