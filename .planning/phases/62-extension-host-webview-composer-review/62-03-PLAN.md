---
phase: 62-extension-host-webview-composer-review
plan: 03
type: execute
wave: 3
depends_on: [62-02]
files_modified:
  - .planning/reviews/62-COVERAGE.md
autonomous: true
requirements: [RVW-03]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-62-03` (composer logic & UI layer, 8 files / 2,027 LOC — the largest unit by LOC in Phase 62) has all 7 live dimensions recorded in `62-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-10, D-11)"
    - "All 8 files — `msgbox-composer.ts`, `addwindow-composer.ts`, `addchildwindow-composer.ts`, `msgbox-composer-ui.ts`, `addwindow-composer-ui.ts`, `addchildwindow-composer-ui.ts`, `setopts-composer-ui.ts`, `setopts-catalog.ts` — are named at least once inside the `## RU-62-03` section, so coverage is file-granular (ROADMAP Phase 62 criterion 2)"
    - "`setopts-catalog.ts`'s D2 value-correctness check is performed under a **stated sampling protocol whose sample size, selection rule and source consulted are written into the cell**, not asserted — 335 lines of SETOPTS bitmask data is the one genuinely catalog-shaped file in this phase"
    - "The logic/UI-layer D4 duplication is recorded here by a **programmatic structural diff whose method and output are written into the cell**, and it **cross-references `RU-62-04`'s generator-layer finding rather than restating it**, so the same duplication is never double-counted (D-12)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #475 (SETOPTS decode hovers plus tri-state composer with IOR/AND-aware codegen) and #385 (launch the external Graffiti Composer) checked explicitly (RVW-07)"
    - "The D7 parity cell compares the BBj code this layer generates against IntelliJ's `ComposerModels.java` as reference material, records VS Code-side divergences as `P62-D7-*` findings located inside `bbj-vscode/`, and routes IntelliJ-side observations to `### Cross-unit referrals` addressed to `RU-63-04` (D-05, D-06)"
    - "The unit's 1 `n/a` cell still carries INVENTORY's R-D6-CENTRAL text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-14)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "`62-COVERAGE.md` contains exactly one `## RU-62-0N` section per unit — five in total — and no plan's section overwrites or merges into another's"
    - "No `applies` cell line in `62-COVERAGE.md` records a bare verdict: every one of the 35 carries a written check line naming the concrete checks applied, and zero cell lines remain `pending` at phase close"
    - "Finding IDs are monotonic within each `(62, dimension)` pair with no gaps and no reuse, so two findings discovered in the same unit have a deterministic order"
    - "No two Phase 62 plans share a wave; each of `62-02`..`62-05` declares its predecessor in `depends_on`, so the shared `62-COVERAGE.md` is never written concurrently"
    - "Both D4 duplication assessments compare against a 3-file `-composer.ts` baseline (msgbox/addwindow/addchildwindow), never 4 — the SETOPTS asymmetry is stated in the cell, not silently normalized"
    - "The composer duplication is allocated exactly once per layer — generator-layer to `RU-62-04`, logic/UI-layer to `RU-62-03` — with the second cross-referencing the first rather than restating it, so the same duplication is never double-counted"

  artifacts:
    - ".planning/reviews/62-COVERAGE.md §`## RU-62-03 — Composer logic & UI layer` — 7 live cells recorded, 1 `n/a` cell untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-62-03` D1 findings on the values this layer feeds into `RU-62-04`'s generators -> Phase 65's SEC-01 synthesis, alongside `RU-62-04`'s `### SEC-01/SEC-02 Surface Handoff` (D-07)"
    - "`RU-62-03`'s logic/UI-layer D4 finding -> cross-references `RU-62-04`'s generator-layer D4 finding, which is where ROADMAP Phase 62 criterion 2 is satisfied (D-12)"
    - "`RU-62-03` `### Cross-unit referrals` addressed to `RU-63-04` -> Phase 63's composer dialogs & bridge sweep, as durable records because Phase 63 may already be running (D-06)"
    - "`setopts-catalog.ts` findings -> #475's dedup neighbourhood and Phase 69's issue drafting"
    - "`RU-62-03` cells -> the phase-wide D-14 cell and file gates re-run by plan 62-05"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, XSS,
    # path traversal, injection taxonomies, GDPR) deliberately DROPPED — in this phase they are the
    # review *subject matter*, not constraints on the plan.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 62 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `bbj-intellij/` — that plugin is read as D7 comparison material only, no Phase 62 coverage cell covers it, and an IntelliJ-side defect belongs in `### Cross-unit referrals` addressed to `RU-63-04` (D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and the composer surface is unfixed (D-09, as approved at the 62-01 checkpoint)"
    - "MUST NOT write a credential, an API token, a session value or an absolute developer-machine path into any record — anchor every citation as a repository-relative `path:line`"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01, predate the webview composers entirely, and are superseded by INVENTORY.md (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53 fix(#494)` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-62-0N` section other than `RU-62-03`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03, D-04)"
    - "MUST NOT add a grid row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 62 slice defines (D-14)"
    - "MUST NOT normalize the composer baseline to four `-composer.ts` files — SETOPTS has none, the baseline is three, and the asymmetry is stated rather than smoothed (D-12, D-15)"
    - "MUST NOT restate `RU-62-04`'s generator-layer duplication finding — this unit's D4 finding is the logic/UI layer and cross-references the generator-layer finding by ID (D-12)"
    - "MUST NOT assert `setopts-catalog.ts`'s bitmask values are correct without recording the sampling protocol, the sample size and the source consulted in the cell"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-10)"
    - "MUST NOT record a cell verdict for a file the sweep did not open — a unit-level generalization over an unread file is a bare verdict wearing a written line, and this unit's four small `-ui.ts` files are swept at the same obligation as its 550-line `msgbox-composer.ts`"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-62-03` — Composer logic & UI layer** (`msgbox-composer.ts` 550,
`addwindow-composer.ts` 405, `addchildwindow-composer.ts` 308, `msgbox-composer-ui.ts` 193,
`addwindow-composer-ui.ts` 68, `addchildwindow-composer-ui.ts` 72, `setopts-composer-ui.ts` 96,
`setopts-catalog.ts` 335 — 8 files, 2,027 LOC, INVENTORY risk rank 3 of 5 and the largest unit by LOC
in Phase 62) across its 7 live dimensions and record the result in the `## RU-62-03` section of
`.planning/reviews/62-COVERAGE.md`.

Purpose: this is the logic that assembles the values `RU-62-04`'s generators interpolate into webview
HTML, and `setopts-catalog.ts` is where SETOPTS codegen actually lives — SETOPTS has no
`-composer.ts` file, so the D4 baseline here is three files, not four (D-12, D-15). It is also the
second of the two layers across which the composer duplication is allocated: the generator layer is
`RU-62-04`'s and is already recorded; this unit records the logic/UI layer and cross-references it.

Output: 7 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md
@.planning/phases/62-extension-host-webview-composer-review/62-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 62 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/62-COVERAGE.md` — section `## RU-62-03 — Composer logic & UI layer` only. The
  file's header, grid, D-14 gate, stopping rule, exclusion-reason block, the other four unit sections
  and `## Phase 62 Close-Out` are inherited from plan `62-01` and are not touched.

**Section anchors this plan fills.** `## RU-62-03 — Composer logic & UI layer` → its `### Cells`,
`### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`. Downstream phases
consume these anchors by name: Phase 65 (D1 records feeding the SEC-01 synthesis), Phase 63
(referrals), Phase 67 (`classification:`), Phase 68 (DOC-03), Phase 69 (issue drafting).

**Finding-ID namespace.** `P62-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(62, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `62-01` and
`62-02`. `P00-*` is INVENTORY's reserved template namespace and is never allocated.

**Plan → review-unit map.** This plan owns `RU-62-03`. The full 5-plan map, the wave chain and the
section shape live in `62-01-PLAN.md` §"Artifacts this phase produces".

**Inherited routing.** INVENTORY's routing table (D-06) has **no Phase 62 rows**; the absence is
deliberate.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-02 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-02 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-02 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-02 | concurrency | all 5 plans, incl. this one | explicit |
| 5 | RVW-03 | adjacency | 62-01 and this plan — the 3-file `-composer.ts` baseline | explicit |
| 6 | RVW-03 | empty | 62-01 (`RU-62-04`'s D4 asymmetric-baseline qualifier) | explicit |
| 7 | RVW-03 | ordering | 62-01 and this plan — duplication allocated once per layer | explicit |
| 8 | RVW-03 | concurrency | 62-01, as a flat-scalar `verification: backstop` marker | **backstop** |

Accounting: 8 surfaced = 8 authored = 0 dropped.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-62-03 at evidence tier `repro` — D1, D2, D3, D7</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`.planning/reviews/62-COVERAGE.md` exists, contains a `## RU-62-03 — ` heading whose 7
  live cell lines are still unrecorded, and plan `62-02` has committed — the phase-wide verdict count is
  exactly 14. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      completed `## RU-62-04` section (its `### Cells` line format, its 13-field finding records, its
      D4 generator-layer duplication finding **by ID** — this unit cross-references it — and its
      `### SEC-01/SEC-02 Surface Handoff`, which states which values reach the generated HTML and from
      where), and every `### Cross-unit referrals` entry addressed to `RU-62-03`
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent), §"Finding Record Template", §"Frozen Open-Issue Snapshot" (**#475** and
      **#385** are this surface's neighbours), §"Review Units" → `### RU-62-04`'s file-exception row
      (the D4 asymmetric baseline), §"Phase 62 review units … — remainder" → `### RU-62-03`
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-03, RVW-06, RVW-07; and
      SEC-01, whose cross-cutting synthesis is Phase 65's and whose concrete per-unit findings are
      this plan's (D-07)
    - **Review targets, read in full:** `bbj-vscode/src/msgbox-composer.ts` (550),
      `bbj-vscode/src/addwindow-composer.ts` (405),
      `bbj-vscode/src/addchildwindow-composer.ts` (308),
      `bbj-vscode/src/msgbox-composer-ui.ts` (193), `bbj-vscode/src/addwindow-composer-ui.ts` (68),
      `bbj-vscode/src/addchildwindow-composer-ui.ts` (72),
      `bbj-vscode/src/setopts-composer-ui.ts` (96), `bbj-vscode/src/setopts-catalog.ts` (335)
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` (245) read in
      full as the BBj-codegen comparison side; for `AddWindowComposerDialog.java`,
      `AddChildWindowComposerDialog.java` and `MsgboxComposerDialog.java` grep for the emitted-code
      construction only; confirm by listing
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/` whether any SETOPTS counterpart
      exists at all
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-62-03 — Composer logic & UI layer`, replacing the
placeholder on each with `pass` or `fail` plus a written line naming the concrete checks applied.
Append any resulting finding records under that section's `### Findings`. Touch no other section.
Enumerate the dimensions explicitly so none is absorbed by whichever is loudest (D-11): this task
covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource use, and
D7 Cross-IDE parity** — and no others.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
input, secret exposure, integrity gaps, privilege/trust-boundary errors. This layer is the **source**
of the values `RU-62-04`'s generators interpolate, so the check runs in the opposite direction from
`RU-62-04`'s: for every value this layer hands to a generator — read the handoff enumeration in
`RU-62-04`'s `### SEC-01/SEC-02 Surface Handoff` rather than re-deriving it — trace where the value
came from (editor selection, document text, `config.bbx` content, a workspace path, catalog data, a
webview message) and whether this layer escapes, validates or constrains it before handing it on, or
assumes the generator will. The concrete checks: what each `*-composer.ts` and `*-composer-ui.ts`
does with text the user typed into the composer before it becomes BBj source inserted into the user's
document — whether quotes, newlines or BBj statement separators in that text can break out of the
generated statement; what `setopts-composer-ui.ts` accepts from a `config.bbx` document before
handing it to `setopts-catalog.ts`'s parser; whether any of the eight files reads or writes a file
path, spawns a process, or executes a VS Code command with a value that originated in composer input;
and whether any message-handling code in this layer trusts a field the webview supplied. **Record
concrete findings here — do not defer this cell to Phase 65 (D-07).**

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource
leaks. The concrete checks: what each composer does with an empty, absent or malformed initial state
and with an empty selection; whether the generated BBj statement is correct at the boundaries — no
option selected, every option selected, an option whose value is zero, an option string at maximum
length; whether the document edit each composer applies is computed against the range it was opened
with even if the user edited the document meanwhile; whether any `await` can reject unhandled; and
whether `setopts-catalog.ts`'s hex/vector conversions round-trip losslessly for a short, over-long,
odd-length, non-hex or all-zero input, including the reserved byte ranges its own header comment
describes as pass-through.

**`setopts-catalog.ts` value-correctness — the stated sampling protocol.** This is the one genuinely
catalog-shaped file in the phase (335 lines of SETOPTS bitmask data, ~51 declared bit entries across
7 byte groups), so its D2 check is a sampling exercise and the protocol is recorded in the cell, not
assumed. Run it in two passes and write **both** into the cell:
  - **Structural pass, exhaustive.** Over every declared bit entry: every `byte` value falls in the
    documented set the file's own header names; every `mask` is a single power of two; no
    `(byte, mask)` pair occurs twice; every entry carries a non-empty `label`; and every entry marked
    `'ignored'` carries the `bbjDetail` its own type comment says explains why. Record the entry count
    observed and each check's result.
  - **Value pass, stratified sample.** Sample **two entries per byte group** — the first and last
    declared in each group, so the selection rule is deterministic and re-runnable — and check each
    entry's `byte`, `mask`, `label` and `since` against the BASIS SETOPTS documentation the file's own
    header docstring cites by URL. Record the sample size, the selection rule, and the source actually
    consulted. If that documentation is not reachable from this environment, say so explicitly in the
    cell and record what was checkable instead — an unreachable source is a stated limit on the
    evidence, not a silent pass.
A value mismatch found by either pass is a `P62-D2-{seq}` finding naming the entry by `file:line`.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks, missing
caches or debounces, redundant work and unbounded memory growth. The concrete checks: whether the
catalog is rebuilt or re-scanned per interaction rather than being a module-level constant; whether
any composer recomputes its full model on every field change; the cost of the vector/hex conversions
relative to how often they run; whether any composer holds a reference to a document, an editor or a
panel beyond its own lifetime; and whether `msgbox-composer.ts` at 550 lines does work on open that
could be deferred.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). The parity question is **equivalence of the generated BBj
code**, not of the UI toolkit. The concrete checks: for msgbox, addwindow and addchildwindow, compare
the statement this layer assembles against what `ComposerModels.java` assembles — argument order,
optional-argument omission, quoting and escaping of user-entered text, numeric formatting, defaults,
and any option that exists on one side only; and establish by listing
`bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/` whether a SETOPTS counterpart exists at
all on the IntelliJ side — if none does, that absence is itself the concrete parity divergence for the
SETOPTS surface and is recorded as such rather than skipped. Record a divergence whose defect is on
the **VS Code** side as a `P62-D7-{seq}` finding located inside `bbj-vscode/`, citing the IntelliJ
`file:line` in `evidence:` as the comparison point only. Where the defect is plainly on the
**IntelliJ** side, record nothing in `### Findings` and write a `### Cross-unit referrals` entry
addressed to **`RU-63-04`**, naming the surface, the check Phase 63 should run, and the VS Code-side
evidence that motivated it — a durable record, because Phase 63 may already be running (D-06).

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's 13-field
template verbatim, one field per line, inside a fenced block, in the template's exact field order.
`unit:` is `RU-62-03`. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline —
test (6) forces `major` for any `critical`/`high` severity **and** for any D1 primary dimension.
`dedup:` is never blank and is checked against the frozen 15, with **#475** and **#385** checked
explicitly by number. A `critical` or `high` D1 finding follows the disclosure rendering approved at
the `62-01` checkpoint (D-09). Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: `awk '/^## RU-62-03 /,/^## RU-62-05 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru03.txt; grep -cE '^- D[1237] .* — (pass|fail) — ' /tmp/p62ru03.txt` prints `4`
    - Each of the four dimensions this task owns is individually recorded: `grep -cE '^- D1 .* — (pass|fail) — ' /tmp/p62ru03.txt`, `grep -cE '^- D2 .* — (pass|fail) — ' /tmp/p62ru03.txt`, `grep -cE '^- D3 .* — (pass|fail) — ' /tmp/p62ru03.txt` and `grep -cE '^- D7 .* — (pass|fail) — ' /tmp/p62ru03.txt` each print `1`
    - The `setopts-catalog.ts` sampling protocol is recorded with its size, rule and source: the D2 cell text or an adjacent block names `setopts-catalog.ts`, states a sample size, and names the source consulted — `/tmp/p62ru03.txt` contains `setopts-catalog.ts` and contains the string `sample`
    - The D7 cell was filled from the IntelliJ comparison side and routes correctly: `/tmp/p62ru03.txt` contains `ComposerModels.java` and contains `RU-63-04`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `18` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `17`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Every `fail` line names the findings it recorded: `awk '/^- D[1-8] .* — fail — /{if ($0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print "UNNAMED: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed, none from the reserved namespace: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-03 /,/^## RU-62-05 /" "$f" > /tmp/p62ru03.txt; test "$(grep -cE "^- D[1237] .* — (pass|fail) — " /tmp/p62ru03.txt)" = 4; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru03.txt)" = 1; done; for x in setopts-catalog.ts sample ComposerModels.java RU-63-04; do grep -q "$x" /tmp/p62ru03.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 18; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 17; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(awk "/^- D[1-8] .* — fail — /{if (\$0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print \"UNNAMED\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_03_REPRO_OK'</automated>
  </verify>
  <done>`RU-62-03`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — the value-origin trace running opposite to `RU-62-04`'s for D1; boundary
  behavior of the generated BBj statement plus the two-pass `setopts-catalog.ts` sampling protocol with
  its size, selection rule and source recorded for D2; per-interaction and retention cost for D3; and the
  generated-BBj-code comparison against `ComposerModels.java` — including the absence of any IntelliJ
  SETOPTS counterpart — for D7, with IntelliJ-side observations routed to `RU-63-04`.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-62-03 at evidence tier `trace` — D4, D5, D8 — including the logic/UI-layer duplication callout</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`## RU-62-03`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed, and the
  phase-wide verdict count is exactly 18. Halt otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` §`## RU-62-03` — the section this task completes — and
      §`## RU-62-04`'s D4 cell plus its generator-layer duplication finding **by ID**, which this
      task's D4 cell cross-references rather than restates
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot", and `### RU-62-04`'s file-exception
      row stating the 3-file `-composer.ts` baseline
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-03
    - **Review targets:** the same 8 files as Task 1
    - **Test surface:** whatever `ls bbj-vscode/test/` returns, plus
      `grep -rl 'composer\|setopts' bbj-vscode/test/ 2>/dev/null` — establish by enumeration which of
      the eight files has any direct test coverage at all; `setopts-catalog.ts` is explicitly written
      to be `vscode`-free and unit-testable, so its coverage state is a direct D5 question
    - `CLAUDE.md` — a D8 target; note that it currently says almost nothing about the four composer
      subsystems, which is a candidate D8 check and not an asserted finding
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-62-03` and append any resulting records. Touch no other
section. Enumerate the dimensions explicitly so none is absorbed by another (D-11): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others.

*D4 Maintainability & code smells* (tier `trace`). This cell records the **logic/UI-layer**
duplication. It is mechanical, not an eyeball comparison (D-12): run a programmatic structural diff
across the three `-composer.ts` files (msgbox, addwindow, addchildwindow) and separately across the
four `-ui.ts` files (msgbox, addwindow, addchildwindow, setopts) — for example `diff` or
`git diff --no-index` between each ordered pair, plus a count of identical or near-identical line
runs — and write **both the method and its literal output** into the cell so a reader can re-run it.
Apply the D-15-confirmed asymmetry: the `-composer.ts` baseline is **three** files, not four, because
SETOPTS has no `-composer.ts` — its codegen lives in `setopts-catalog.ts`, which is in this same unit
and is therefore compared as the SETOPTS logic-layer counterpart rather than treated as a missing
file. State that asymmetry in the cell text; do not add a row, a cell line or a coverage-gap note for
it. **Cross-reference `RU-62-04`'s generator-layer duplication finding by its ID and do not restate
it** — this unit's D4 finding is about the logic/UI layer only, so the same duplication is counted
once per layer and never twice (D-12). Beyond duplication: god functions in `msgbox-composer.ts` at
550 lines; whether the four `-ui.ts` files agree on one contract for opening a composer or use
several; whether `setopts-catalog.ts` mixes catalog data with conversion logic in a way that blocks
reuse it explicitly claims (its header states it is `vscode`-free so the IntelliJ client can reuse
it — whether that reuse actually happens is a D7 and D8 question, whether the module is *shaped* for
it is this one); and dead or unreferenced exports across the eight files, confirmed by grep rather
than asserted.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). The concrete checks:
which of the eight files has a dedicated test, established by enumerating `bbj-vscode/test/` rather
than assumed; whether `setopts-catalog.ts` — the one file in the phase explicitly written to be
unit-testable, with no `vscode` dependency — has any test at all, and if so whether it covers the
hex/vector round-trip at the boundaries Task 1 checked; whether the BBj statement each composer
generates is asserted anywhere against an expected string; and whether each D1 or D2 finding recorded
in Task 1 has a test that would have caught it. Record gaps as `P62-D5-{seq}` findings naming the
untested branch or module by `file:line`; `dedup:` is never blank and names a `DEBT-*` requirement
where the gap restates one.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
or docs-site claims contradicted by code. The concrete checks: `setopts-catalog.ts`'s header docstring
— which makes several checkable claims at once (that the config.bbx SETOPTS string is absolute and
stateless with no IOR/AND, that bytes 5-6 are data and bytes 10-16 pass through as raw hex, that the
module has no `vscode` dependency, and that it is reusable by the IntelliJ client and by a future
BBj-code SETOPTS composer) — each verified against the code and against Task 1's D2 round-trip
results; every JSDoc block across the other seven files, in particular any comment claiming a value is
escaped, validated or safe, checked against Task 1's D1 trace; and whether `CLAUDE.md` describes the
composer subsystems accurately, promoting a finding only where a positive claim it makes is
contradicted, never merely because a section is absent. Record a divergence as a finding with the
exact `file:line` in `location:`.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason. Any defect whose `file:line` belongs to another
unit — anything in a `*-composer-webview.ts` file belongs to `RU-62-04` — goes under
`### Cross-unit referrals`, not into this unit's `### Findings`.
  </action>
  <acceptance_criteria>
    - All 7 live cells now carry a verdict: `awk '/^## RU-62-03 /,/^## RU-62-05 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru03.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/p62ru03.txt` prints `7`
    - Each of the three dimensions this task owns is individually recorded: `grep -cE '^- D4 .* — (pass|fail) — ' /tmp/p62ru03.txt`, `grep -cE '^- D5 .* — (pass|fail) — ' /tmp/p62ru03.txt` and `grep -cE '^- D8 .* — (pass|fail) — ' /tmp/p62ru03.txt` each print `1`
    - The `n/a` cell is untouched: `grep -cE '^- D6 .* — n/a — ' /tmp/p62ru03.txt` prints `1`
    - Every file in the unit is named inside its own section: `for f in msgbox-composer.ts addwindow-composer.ts addchildwindow-composer.ts msgbox-composer-ui.ts addwindow-composer-ui.ts addchildwindow-composer-ui.ts setopts-composer-ui.ts setopts-catalog.ts; do grep -q "$f" /tmp/p62ru03.txt || echo "MISSING $f"; done` prints nothing
    - The D4 callout is mechanical, with its method and output in the cell: `/tmp/p62ru03.txt` contains the string `diff`
    - The D4 cell cross-references the generator-layer finding by ID rather than restating it: `/tmp/p62ru03.txt` contains `RU-62-04` and contains a token matching `P62-D4-[0-9][0-9][0-9]`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `21` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `14`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - All three sub-blocks are present for this unit: `/tmp/p62ru03.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Every `pass` line remains a written line: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT"}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-03 /,/^## RU-62-05 /" "$f" > /tmp/p62ru03.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p62ru03.txt)" = 7; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru03.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p62ru03.txt)" = 1; for x in msgbox-composer.ts addwindow-composer.ts addchildwindow-composer.ts msgbox-composer-ui.ts addwindow-composer-ui.ts addchildwindow-composer-ui.ts setopts-composer-ui.ts setopts-catalog.ts diff RU-62-04 "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/p62ru03.txt; done; grep -qE "P62-D4-[0-9]{3}" /tmp/p62ru03.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 21; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 14; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_03_COMPLETE'</automated>
  </verify>
  <done>`RU-62-03` is complete under the stopping rule: all 7 live cells carry a verdict plus a written
  check line, all 8 files including the 68-line `addwindow-composer-ui.ts` are named inside the section,
  the logic/UI-layer duplication is recorded by a programmatic diff whose method and output are in the
  cell and cross-references `RU-62-04`'s generator-layer finding by ID rather than restating it, the
  3-file `-composer.ts` baseline asymmetry is stated rather than normalized, `setopts-catalog.ts`'s four
  header claims are each checked against the code, and every unpromoted claim is visible under
  `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure and record-integrity threats. Rows marked *subject-matter* are boundaries this
plan **documents**; remediation is Phase 65/67's.

| Boundary | Description |
|----------|-------------|
| composer input, document text and `config.bbx` content → the values handed to `RU-62-04`'s generators (*subject-matter*, SEC-01) | This layer assembles everything the generators interpolate; whether it escapes or defers escaping is the SEC-01 question viewed from the source side. |
| composer input → BBj source inserted into the user's document (*subject-matter*) | Quotes, newlines or statement separators in user-entered text can break out of the generated BBj statement and change what is written into the user's file. |
| `config.bbx` content → `setopts-catalog.ts` parse/emit (*subject-matter*) | A `config.bbx` SETOPTS string is parsed, mutated and re-emitted; a lossy or unchecked round-trip silently rewrites a user's configuration. |
| `62-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| code read during the sweep → the committed record | Secrets or absolute developer-machine paths encountered while reading can be copied into a record verbatim. |
| `bbj-intellij/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 63's rows. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `62-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-P03-S1 | Tampering | user-entered composer text → generated BBj statement inserted into the user's document | high | transfer | Task 1's D1 check asks specifically whether quotes, newlines or BBj statement separators in composer input can break out of the generated statement, traced to the exact `file:line`; recorded as `P62-D1-*` where evidence clears tier `repro`. |
| T-62-P03-S2 | Tampering | values this layer hands to `RU-62-04`'s HTML generators without escaping | high | transfer | Task 1's D1 check runs the value-origin trace opposite to `RU-62-04`'s, using that unit's `### SEC-01/SEC-02 Surface Handoff` enumeration as its starting point rather than re-deriving it. Cross-cutting SEC-01 synthesis transfers to Phase 65. |
| T-62-P03-S3 | Tampering | `config.bbx` SETOPTS hex round-trip in `setopts-catalog.ts` | medium | transfer | Task 1's D2 check tests the round-trip at the short, over-long, odd-length, non-hex and all-zero boundaries, including the reserved pass-through byte ranges the file's own header describes. |
| T-62-P03-S4 | Repudiation | unverified bitmask values in a 335-line catalog | medium | mitigate | Task 1's two-pass protocol — an exhaustive structural pass plus a deterministic stratified value sample — with the sample size, the selection rule and the source consulted written into the cell; an unreachable documentation source is recorded as a stated limit on the evidence, never as a silent pass. |
| T-62-P03-01 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-09's two-tier rule as rendered and approved at the `62-01` checkpoint: surface, problem class and impact; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-62-P03-02 | Information Disclosure | a secret or absolute developer path copied into a record while reading code | medium | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor. |
| T-62-P03-03 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or any D1 primary dimension; every record writes all six tests pass/FAIL inline. |
| T-62-P03-04 | Repudiation | double-counting the composer duplication across two units | medium | mitigate | D-12 prohibition plus an acceptance criterion requiring this unit's D4 cell to name `RU-62-04` and cite a `P62-D4-NNN` ID, so the generator layer is cross-referenced rather than restated. |
| T-62-P03-05 | Repudiation | silently normalizing the `-composer.ts` baseline to four files | medium | mitigate | D-12/D-15 prohibition plus an acceptance criterion requiring `setopts-catalog.ts` to be named in the section, so the SETOPTS asymmetry is stated rather than smoothed. |
| T-62-P03-06 | Tampering | five plans writing one `62-COVERAGE.md` | medium | mitigate | Wave 3 behind `depends_on: [62-02]` makes ordering structural (D-04); both tasks assert exact global counts (18/17, then 21/14) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-62-P03-07 | Tampering | reading `bbj-intellij/composer/` as D7 comparison material | medium | mitigate | D-05 prohibition plus an anchored acceptance grep: no `location:` may resolve inside `bbj-intellij/`. IntelliJ-side defects route to `### Cross-unit referrals` addressed to `RU-63-04` as durable records (D-06). |
| T-62-P03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and `diff` only. |
</threat_model>

<verification>
- `## RU-62-03`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's R-D6-CENTRAL text verbatim.
- All 8 unit files, including the 68-line `addwindow-composer-ui.ts`, are named inside the section.
- The D2 cell records `setopts-catalog.ts`'s two-pass sampling protocol with its sample size, its
  deterministic selection rule and the source actually consulted.
- The D4 cell records a programmatic diff's method and output, states the 3-file `-composer.ts`
  baseline, and cross-references `RU-62-04`'s generator-layer finding by ID rather than restating it.
- The D7 cell cites `ComposerModels.java` as the comparison side, records the absence of any IntelliJ
  SETOPTS counterpart as a concrete divergence, and routes IntelliJ-side observations to `RU-63-04`;
  no `location:` resolves inside `bbj-intellij/`.
- Phase-wide verdict count is 21 and placeholder count is 14; 21 + 14 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed ID and a non-blank `dedup:` that explicitly
  covered #475 and #385.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-03 advances: the 8 logic/UI-layer composer files — the second half of ROADMAP Phase 62
  criterion 2's 11 composer files plus `setopts-catalog.ts` — have a recorded pass/fail against every
  dimension that applies to them.
- The composer duplication is now recorded at both layers exactly once each, satisfying ROADMAP
  criterion 2's "explicitly called out under D4" without double-counting.
- Phase 65 inherits concrete VS Code-side D1 records on the value-assembly side of the SEC-01 surface.
- Phase 63 inherits durable `RU-63-04` referrals, including the absence of an IntelliJ SETOPTS
  counterpart.
- ROADMAP Phase 62 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/62-extension-host-webview-composer-review/62-03-SUMMARY.md` when done.
</output>
