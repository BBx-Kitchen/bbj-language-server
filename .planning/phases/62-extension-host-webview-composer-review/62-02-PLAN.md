---
phase: 62-extension-host-webview-composer-review
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - .planning/reviews/62-COVERAGE.md
autonomous: true
requirements: [RVW-02]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-62-01` (extension host & commands, 3 files / 1,805 LOC) has all 7 live dimensions recorded in `62-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-10, D-11)"
    - "All 3 files — `extension.ts`, `Commands/CompilerOptions.ts`, `Commands/Commands.cjs` — are named at least once inside the `## RU-62-01` section, so coverage is file-granular (ROADMAP Phase 62 criterion 1)"
    - "`Commands/Commands.cjs` is swept in full even though ROADMAP's Phase 62 success criteria do not name it — the grid is the contract and the criteria are a subset of it — and the cell text says so, so the extra coverage reads as deliberate rather than accidental (D-13)"
    - "The VS Code-side process-spawn paths are assessed and recorded as concrete D1 findings where evidence clears tier `repro`, not deferred with a pointer to Phase 65 — Phase 65 adds only the both-IDEs cross-cutting synthesis a per-unit sweep structurally cannot produce (D-07)"
    - "The D7 parity cell compares the VS Code command surface against IntelliJ's `actions/` package as reference material, records VS Code-side divergences as `P62-D7-*` findings located inside `bbj-vscode/`, and routes IntelliJ-side observations to `### Cross-unit referrals` addressed to `RU-63-01` (D-05, D-06)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #231 (custom classpath and command-line settings for starting BBj programs), #485 (custom-named config files) and #486 (watch config.bbx and re-apply changes) checked explicitly as this unit's plausible neighbours (RVW-07)"
    - "The unit's 1 `n/a` cell still carries INVENTORY's R-D6-CENTRAL text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-14)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "`62-COVERAGE.md` contains exactly one `## RU-62-0N` section per unit — five in total — and no plan's section overwrites or merges into another's"
    - "No `applies` cell line in `62-COVERAGE.md` records a bare verdict: every one of the 35 carries a written check line naming the concrete checks applied, and zero cell lines remain `pending` at phase close"
    - "Finding IDs are monotonic within each `(62, dimension)` pair with no gaps and no reuse, so two findings discovered in the same unit have a deterministic order"
    - "No two Phase 62 plans share a wave; each of `62-02`..`62-05` declares its predecessor in `depends_on`, so the shared `62-COVERAGE.md` is never written concurrently"

  artifacts:
    - ".planning/reviews/62-COVERAGE.md §`## RU-62-01 — Extension host & commands` — 7 live cells recorded, 1 `n/a` cell untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-62-01` D1 findings on the VS Code-side process-spawn and EM-command paths -> Phase 65's SEC-05 cross-cutting synthesis across both IDEs (D-07)"
    - "`RU-62-01` D1 findings on any extension-host webview wiring -> Phase 65's SEC-01/SEC-02 synthesis, alongside `RU-62-04`'s `### SEC-01/SEC-02 Surface Handoff`"
    - "`RU-62-01` `### Cross-unit referrals` addressed to `RU-63-01` -> Phase 63's run/compile/EM action sweep, as durable records because Phase 63 may already be running (D-06)"
    - "`Commands.cjs` coverage despite its absence from ROADMAP's criteria -> the D-13 scope-fidelity note plan 62-05 writes into `## Phase 62 Close-Out`"
    - "`RU-62-01` cells -> the phase-wide D-14 cell and file gates re-run by plan 62-05"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, XSS,
    # path traversal, injection taxonomies, GDPR) deliberately DROPPED — in this phase they are the
    # review *subject matter*, not constraints on the plan.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 62 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `bbj-intellij/` — that plugin is read as D7 comparison material only, no Phase 62 coverage cell covers it, and an IntelliJ-side defect belongs in `### Cross-unit referrals` addressed to `RU-63-01` (D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and these spawn and HTML-generation surfaces are unfixed (D-09, as approved at the 62-01 checkpoint)"
    - "MUST NOT write a credential, an EM token, a session value or an absolute developer-machine path into any record — anchor every citation as a repository-relative `path:line`"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and are superseded by INVENTORY.md (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53 fix(#494)` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-62-0N` section other than `RU-62-01`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03, D-04)"
    - "MUST NOT add a grid row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 62 slice defines (D-14)"
    - "MUST NOT defer this unit's D1 verdict to Phase 65 — a cell filled with a pointer instead of a verdict is exactly the hole D-07 exists to prevent"
    - "MUST NOT sweep `Commands/Commands.cjs` at reduced depth because ROADMAP's success criteria omit it — the grid is the contract and the criteria are a subset of it (D-13)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-10)"
    - "MUST NOT record a cell verdict for a file the sweep did not open — a unit-level generalization over an unread file is a bare verdict wearing a written line"
    - "MUST NOT re-record the generator-layer composer duplication `RU-62-04` already owns — note the ownership under `### Cross-unit referrals` instead (D-12)"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-62-01` — Extension host & commands** (`bbj-vscode/src/extension.ts` 894,
`bbj-vscode/src/Commands/CompilerOptions.ts` 506, `bbj-vscode/src/Commands/Commands.cjs` 405 —
3 files, 1,805 LOC, INVENTORY risk rank 2 of 5) across its 7 live dimensions and record the result in
the `## RU-62-01` section of `.planning/reviews/62-COVERAGE.md`.

Purpose: this is the activation entry point — every command, composer launch and editor feature in
Phase 62 is wired through `extension.ts` — and `CompilerOptions.ts` plus `Commands.cjs` are where the
extension builds and hands off command strings to `child_process`. It is also the unit where D-13
bites: `Commands.cjs` is in INVENTORY's grid but absent from ROADMAP's Phase 62 success criteria, and
it is swept in full anyway.

Output: 7 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md
@.planning/phases/62-extension-host-webview-composer-review/62-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 62 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/62-COVERAGE.md` — section `## RU-62-01 — Extension host & commands` only. The
  file's header, grid, D-14 gate, stopping rule, exclusion-reason block, the other four unit sections
  and `## Phase 62 Close-Out` are inherited from plan `62-01` and are not touched.

**Section anchors this plan fills.** `## RU-62-01 — Extension host & commands` → its `### Cells`,
`### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`. Downstream phases
consume these anchors by name: Phase 65 (D1 records), Phase 63 (referrals), Phase 67
(`classification:`), Phase 68 (DOC-03 concatenation), Phase 69 (issue drafting).

**Finding-ID namespace.** `P62-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(62, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plan `62-01`.
`P00-*` is INVENTORY's reserved template namespace and is never allocated.

**Plan → review-unit map.** This plan owns `RU-62-01`. The full 5-plan map, the wave chain and the
section shape live in `62-01-PLAN.md` §"Artifacts this phase produces".

**Inherited routing.** INVENTORY's routing table (D-06) has **no Phase 62 rows**; the absence is
deliberate. Do not go looking for pre-identified findings.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-02 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-02 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-02 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-02 | concurrency | all 5 plans, incl. this one | explicit |
| 5 | RVW-03 | adjacency | 62-01 and 62-03 (composer-layer D4 baseline) | explicit |
| 6 | RVW-03 | empty | 62-01 (`RU-62-04`'s D4 asymmetric-baseline qualifier) | explicit |
| 7 | RVW-03 | ordering | 62-01 and 62-03 (duplication allocated once per layer) | explicit |
| 8 | RVW-03 | concurrency | 62-01, as a flat-scalar `verification: backstop` marker | **backstop** |

Accounting: 8 surfaced = 8 authored = 0 dropped.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-62-01 at evidence tier `repro` — D1, D2, D3, D7</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`.planning/reviews/62-COVERAGE.md` exists, contains a `## RU-62-01 — ` heading whose 7
  live cell lines are still unrecorded, and plan `62-01` has committed — the phase-wide verdict count is
  exactly 7. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, and the
      completed `## RU-62-04` section as the shape to copy, including its `### Cells` line format, its
      13-field finding records and the disclosure rendering approved at the `62-01` checkpoint. Also
      read every `### Cross-unit referrals` entry addressed to `RU-62-01`
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent — the concrete divergent behaviour in both IDEs), §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Phase 62 review units … — remainder" → `### RU-62-01`,
      §"Routing table (D-06)" (confirm: no Phase 62 rows)
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-02, RVW-06, RVW-07; and
      SEC-01/SEC-02/SEC-05, whose cross-cutting synthesis is Phase 65's and whose concrete per-unit
      findings are this plan's (D-07)
    - **Review targets, read in full:** `bbj-vscode/src/extension.ts` (894),
      `bbj-vscode/src/Commands/CompilerOptions.ts` (506),
      `bbj-vscode/src/Commands/Commands.cjs` (405)
    - `bbj-vscode/package.json` — the `contributes.commands` list and the activation events, which
      define the command surface D7 compares against
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjRunActionBase.java` (423) and
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjEMLoginAction.java` (169) read in
      full as the comparison side; for `BbjCompileAction.java`, `BbjRunGuiAction.java`,
      `BbjRunBuiAction.java`, `BbjRunDwcAction.java` and `BbjRefreshJavaClassesAction.java` grep for
      the command/argument construction and the settings they read, rather than reading in full
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-62-01 — Extension host & commands`, replacing the
placeholder on each with `pass` or `fail` plus a written line naming the concrete checks applied.
Append any resulting finding records under that section's `### Findings`. Touch no other section.
Enumerate the dimensions explicitly so none is absorbed by whichever is loudest (D-11): this task
covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource use, and
D7 Cross-IDE parity** — and no others.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
input, secret exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks:
every `child_process` call in the unit — the two `exec` call sites inside `extension.ts` around the
EM validate and EM login flows (approximately `extension.ts:425` and `extension.ts:644`) and every
`exec` call in `Commands.cjs` — traced from the command string's construction back to the origin of
each interpolated segment, asking whether a workspace path, a `config.bbx` value, a
`project.properties` value, a user-entered value or a setting reaches a shell command string without
quoting or validation; whether `CompilerOptions.ts`'s `buildCompileOptions` / `validateOptions` pair
actually constrains the option values it emits into a command line, or only shapes them for the UI;
what `Commands.cjs` does with `PropertiesReader` output and with the `--` EM sentinel value; whether
any EM token, password or credential is passed as a process argument, written to the output channel,
or logged; whether any file path used for read, write or open is constrained to the workspace; and
what `extension.ts`'s registered commands expose to a caller that supplies its own `params` argument
(the `bbj.runBUI` and `bbj.runDWC` handlers both take one). **Record concrete findings here — do not
defer this cell to Phase 65 (D-07).** Phase 65 adds only the both-IDEs cross-cutting synthesis of
SEC-05 that a per-unit sweep structurally cannot produce.

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource
leaks. The concrete checks: whether `activate()` can partially fail and leave commands registered
against a client that never started; whether every `registerCommand` handler that is `async` handles
its own rejections, and what a rejected handler surfaces to the user; whether disposables — the
output channel, the language client, status-bar items, file watchers — are pushed onto
`context.subscriptions` or leaked; what each command does when no workspace folder is open, when the
active editor is not a BBj document, or when the configured BBj home is missing; whether the
`exec` callbacks distinguish a non-zero exit code from a spawn failure and from empty output;
whether `Commands.cjs`'s `stripSentinel` and its properties parsing handle an absent, empty or
malformed `project.properties`; and whether `CompilerOptions.ts`'s validation rejects an
out-of-range or wrong-typed option value or silently coerces it. Each claim needs a runnable
reproduction or a line-by-line trace naming the concrete input and the exact `file:line` where
behavior diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks,
missing caches or debounces, redundant work and unbounded memory growth. The concrete checks: what
`activate()` does synchronously before the extension becomes usable, and whether any filesystem or
process work happens on the activation path that could be deferred; whether any command re-reads
`config.bbx` or `project.properties` on every invocation rather than caching with invalidation;
whether repeated command invocations accumulate output channels, watchers, listeners or child
processes; and whether `CompilerOptions.ts`'s option model is rebuilt per UI interaction.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). The comparison side is IntelliJ's `actions/` package.
The concrete checks: enumerate the VS Code command surface from `package.json`'s
`contributes.commands` and from the `registerCommand` calls in `extension.ts`, then set it against
IntelliJ's action set — run GUI, run BUI, run DWC, compile, EM login, refresh Java classes — and name
every capability present on one side and absent or behaviourally different on the other; compare what
each side does with the same inputs for at least the run and compile paths — which settings it reads,
how it assembles arguments, and what it does when BBj home is unset; and compare the EM login and
token handling flows on both sides at the level of what is passed where, without reproducing any
token value. Record a divergence whose defect is on the **VS Code** side as a `P62-D7-{seq}` finding
whose `location:` is inside `bbj-vscode/`, citing the IntelliJ `file:line` in `evidence:` as the
comparison point only. Where the defect is plainly on the **IntelliJ** side, record nothing in
`### Findings` and instead write a `### Cross-unit referrals` entry addressed to **`RU-63-01`**,
naming the surface, the check Phase 63 should run, and the VS Code-side evidence that motivated it —
a durable record, because Phase 63 may already be running (D-06).

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's 13-field
template verbatim, one field per line, inside a fenced block, in the template's exact field order.
`unit:` is `RU-62-01`. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline —
test (6) forces `major` for any `critical`/`high` severity **and** for any D1 primary dimension
regardless of edit size. `dedup:` is never blank and is checked against the frozen 15, with **#231**
(custom classpath and command-line settings for starting BBj programs), **#485** (custom-named or
custom-located config files) and **#486** (watch `config.bbx` and re-apply PREFIX/USE changes without
a restart) checked explicitly by number, since all three sit on this unit's surface. A `critical` or
`high` D1 finding follows the disclosure rendering approved at the `62-01` checkpoint: name the
surface, the problem class and the impact, state that the evidence exists and what it establishes,
and publish no trigger sequence, no payload and no proof-of-concept (D-09). Any claim that does not
clear its tier goes under `### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: `awk '/^## RU-62-01 /,/^## RU-62-03 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru01.txt; grep -cE '^- D[1237] .* — (pass|fail) — ' /tmp/p62ru01.txt` prints `4`
    - Each of the four dimensions this task owns is individually recorded: `grep -cE '^- D1 .* — (pass|fail) — ' /tmp/p62ru01.txt`, `grep -cE '^- D2 .* — (pass|fail) — ' /tmp/p62ru01.txt`, `grep -cE '^- D3 .* — (pass|fail) — ' /tmp/p62ru01.txt` and `grep -cE '^- D7 .* — (pass|fail) — ' /tmp/p62ru01.txt` each print `1`
    - The D1 cell is a verdict, not a deferral: `/tmp/p62ru01.txt` contains `child_process` and the D1 cell line does not contain the string `see Phase 65`
    - The D7 cell was filled from the IntelliJ comparison side and routes correctly: `/tmp/p62ru01.txt` contains `BbjRunActionBase.java` and contains `RU-63-01`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `11` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `24`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Every `fail` line names the findings it recorded: `awk '/^- D[1-8] .* — fail — /{if ($0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print "UNNAMED: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed, none from the reserved namespace: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-01 /,/^## RU-62-03 /" "$f" > /tmp/p62ru01.txt; test "$(grep -cE "^- D[1237] .* — (pass|fail) — " /tmp/p62ru01.txt)" = 4; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru01.txt)" = 1; done; grep -q "child_process" /tmp/p62ru01.txt; grep -q "BbjRunActionBase.java" /tmp/p62ru01.txt; grep -q "RU-63-01" /tmp/p62ru01.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 11; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 24; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(awk "/^- D[1-8] .* — fail — /{if (\$0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print \"UNNAMED\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_01_REPRO_OK'</automated>
  </verify>
  <done>`RU-62-01`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — every `child_process` command-string construction traced to its inputs for
  D1; activation-failure, disposal and rejection paths for D2; activation-path and per-invocation cost
  for D3; and the VS Code command surface set against IntelliJ's `actions/` package for D7, with
  IntelliJ-side observations routed to `RU-63-01` rather than recorded here.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-62-01 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`## RU-62-01`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed, and the
  phase-wide verdict count is exactly 11. Halt otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` §`## RU-62-01` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", §"Test & Build Baseline"
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-02
    - `.planning/ROADMAP.md` §"Phase 62" success criterion 1 — note that it names `extension.ts` and
      `Commands/CompilerOptions.ts` but **not** `Commands/Commands.cjs`, the D-13 discrepancy this
      cell records
    - **Review targets:** the same 3 files as Task 1
    - **Test surface:** whatever `ls bbj-vscode/test/` returns, plus
      `grep -rl 'extension\|Commands\|CompilerOptions' bbj-vscode/test/ 2>/dev/null` — establish by
      enumeration which of the three files has any direct test coverage at all
    - `CLAUDE.md` §Repository Structure, §Build & Test Commands and §Architecture — D8 targets
      checkable against the code just read, in particular any claim about how commands, the run tools
      or the compiler integration are wired
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-62-01` and append any resulting records. Touch no other
section. Enumerate the dimensions explicitly so none is absorbed by another (D-11): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks: the size
and responsibility count of `extension.ts` at 894 lines — how many distinct concerns `activate()` and
its helpers carry, named individually; whether `Commands.cjs` being CommonJS while the rest of the
extension is TypeScript ESM is a deliberate boundary or an unmigrated remnant, established from how it
is imported and what it depends on rather than asserted; whether the `exec`-plus-callback shape is
repeated across `extension.ts` and `Commands.cjs` with no shared helper; whether option definitions
are duplicated between `CompilerOptions.ts` and `package.json`'s `configuration` contributions;
whether any registered command handler is dead — registered but unreachable from any contributed
menu, keybinding or palette entry; and whether the three files agree on one convention for surfacing
errors to the user or use several. The code shape is the evidence — name it and the `file:line`.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). The concrete checks:
which of the three files has a dedicated test, established by enumerating `bbj-vscode/test/` rather
than assumed; whether `CompilerOptions.ts`'s `validateOptions` — the only pure, directly testable
logic in the unit — has any test; whether any command handler, activation path or `exec` command-string
construction is exercised by a currently-passing test; and whether each D1 or D2 finding recorded in
Task 1 has a test that would have caught it. Record gaps as `P62-D5-{seq}` findings naming the untested
branch by `file:line`. Where a gap restates a `DEBT-*` requirement, say so in `dedup:` so Phase 66
re-triages rather than re-derives; the field is never blank.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
or docs-site claims contradicted by code. The concrete checks: every JSDoc block in `Commands.cjs` and
`CompilerOptions.ts` against what the code does — including `stripSentinel`'s documented `--`
semantics and any comment claiming a value is validated or escaped, checked directly against Task 1's
D1 trace; `extension.ts`'s comments about activation ordering and client lifecycle; and `CLAUDE.md`'s
§Repository Structure claim about where development happens, its §Build & Test Commands list, and its
§IDE Integration claims about the shared language-server binary and the shared run tools `web.bbj` and
`em-login.bbj`, each verified against this unit's code. Record a divergence as a finding with the exact
`CLAUDE.md` line in `location:`.

**D-13 scope-fidelity note.** In the D4 or D5 cell text — whichever names it first — state plainly that
`Commands/Commands.cjs` is swept in full despite not appearing in ROADMAP's Phase 62 success criteria,
because the grid is the contract and the criteria are a subset of it. Plan `62-05` repeats this in the
close-out; recording it here keeps the reason adjacent to the coverage claim.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason. Do **not** re-record the generator-layer composer
duplication — `RU-62-04` owns it; note that ownership under `### Cross-unit referrals` if the subject
comes up.
  </action>
  <acceptance_criteria>
    - All 7 live cells now carry a verdict: `awk '/^## RU-62-01 /,/^## RU-62-03 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru01.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/p62ru01.txt` prints `7`
    - Each of the three dimensions this task owns is individually recorded: `grep -cE '^- D4 .* — (pass|fail) — ' /tmp/p62ru01.txt`, `grep -cE '^- D5 .* — (pass|fail) — ' /tmp/p62ru01.txt` and `grep -cE '^- D8 .* — (pass|fail) — ' /tmp/p62ru01.txt` each print `1`
    - The `n/a` cell is untouched: `grep -cE '^- D6 .* — n/a — ' /tmp/p62ru01.txt` prints `1`
    - Every file in the unit is named inside its own section: `for f in extension.ts CompilerOptions.ts Commands.cjs; do grep -q "$f" /tmp/p62ru01.txt || echo "MISSING $f"; done` prints nothing
    - The D-13 scope-fidelity note is recorded adjacent to the coverage claim: `/tmp/p62ru01.txt` contains the string `success criteria`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `14` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `21`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - All three sub-blocks are present for this unit: `/tmp/p62ru01.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Every `pass` line remains a written line: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT"}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-01 /,/^## RU-62-03 /" "$f" > /tmp/p62ru01.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p62ru01.txt)" = 7; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru01.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p62ru01.txt)" = 1; for x in extension.ts CompilerOptions.ts Commands.cjs "success criteria" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/p62ru01.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 14; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 21; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_01_COMPLETE'</automated>
  </verify>
  <done>`RU-62-01` is complete under the stopping rule: all 7 live cells carry a verdict plus a written
  check line, all 3 files including `Commands.cjs` are named inside the section, the D-13 discrepancy
  (`Commands.cjs` swept despite ROADMAP's criteria omitting it) is stated adjacent to the coverage claim,
  CLAUDE.md's Repository Structure / Build & Test / IDE Integration claims are checked against the code,
  and every unpromoted claim is visible under `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure and record-integrity threats. Rows marked *subject-matter* are boundaries this
plan **documents**; remediation is Phase 65/67's.

| Boundary | Description |
|----------|-------------|
| workspace paths, `config.bbx` values, `project.properties` values, settings → `child_process` command strings (*subject-matter*) | `extension.ts`'s EM validate/login `exec` sites and every `exec` in `Commands.cjs` build shell command strings from values the extension does not fully control. |
| EM credentials and tokens → process arguments, output channel, logs (*subject-matter*) | The EM login flow handles secret material that must not reach a process argument list or a log sink. |
| command `params` arguments → command handlers (*subject-matter*) | `bbj.runBUI` and `bbj.runDWC` accept a caller-supplied `params` object; any VS Code extension or task can invoke a registered command. |
| `62-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record is world-readable on landing, and git history preserves it. |
| code read during the sweep → the committed record | EM tokens, passwords or absolute developer-machine paths encountered while reading can be copied into a record verbatim. |
| `bbj-intellij/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 63's rows. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `62-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-P02-S1 | Elevation of Privilege | `child_process.exec` command-string construction in `extension.ts` and `Commands.cjs` | high | transfer | Task 1's D1 check traces every interpolated segment of every command string back to its origin and asks whether quoting or validation stands between them; recorded as `P62-D1-*` where evidence clears tier `repro`. Cross-cutting SEC-05 synthesis across both IDEs transfers to Phase 65; remediation to Phase 67. |
| T-62-P02-S2 | Information Disclosure | EM credential/token handling across the login and validate flows | high | transfer | Task 1's D1 check asks whether any credential is passed as a process argument, written to the output channel, or logged — stated by `file:line`, never by quoting the value. |
| T-62-P02-S3 | Elevation of Privilege | caller-supplied `params` reaching `bbj.runBUI` / `bbj.runDWC` handlers | medium | transfer | Task 1's D1 check asks what a caller that supplies its own `params` can influence, since any extension or task can invoke a registered command. |
| T-62-P02-S4 | Denial of Service | activation-path work and per-invocation resource accumulation | medium | transfer | Task 1's D2 and D3 checks cover partial activation failure, leaked disposables and child processes, and repeated re-reads of `config.bbx` / `project.properties`. |
| T-62-P02-01 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-09's two-tier rule, as rendered and approved at the `62-01` checkpoint: surface, problem class and impact; state that the evidence exists and what it establishes; no trigger sequence, no payload, no proof-of-concept. Enforced as a `must_haves` prohibition. |
| T-62-P02-02 | Information Disclosure | an EM token, password or absolute developer path copied into a record while reading code | high | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value and no absolute path is written into any field. |
| T-62-P02-03 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or any D1 primary dimension; every record writes all six tests pass/FAIL inline. |
| T-62-P02-04 | Repudiation | a D1 cell filled with a Phase 65 pointer instead of a verdict | high | mitigate | D-07 prohibition plus an acceptance criterion asserting the D1 cell carries a verdict and names `child_process`; a pointer would leave the phase's own criterion 1 unmet. |
| T-62-P02-05 | Tampering | five plans writing one `62-COVERAGE.md` | medium | mitigate | Wave 2 behind `depends_on: [62-01]` makes ordering structural (D-04); both tasks assert exact global counts (11/24, then 14/21) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-62-P02-06 | Repudiation | a coverage claim with no evidence behind it | medium | mitigate | RVW-06's drop-vs-disposition rule plus D-10: every `pass` cell carries a written line of at least 120 bytes naming the concrete checks applied; every `fail` cell names its finding IDs. |
| T-62-P02-07 | Tampering | reading `bbj-intellij/actions/` as D7 comparison material | medium | mitigate | D-05 prohibition plus an anchored acceptance grep: no `location:` may resolve inside `bbj-intellij/`. IntelliJ-side defects route to `### Cross-unit referrals` addressed to `RU-63-01` as durable records (D-06). |
| T-62-P02-08 | Repudiation | silently thinning `Commands.cjs` because ROADMAP's criteria omit it | medium | mitigate | D-13 prohibition plus an acceptance criterion requiring `Commands.cjs` to be named in the section and the omission to be stated in the cell text. |
| T-62-P02-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep` and `awk` only. |
</threat_model>

<verification>
- `## RU-62-01`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's R-D6-CENTRAL text verbatim.
- All 3 unit files — `extension.ts`, `CompilerOptions.ts`, `Commands.cjs` — are named inside the section.
- The D1 cell names `child_process` and carries a verdict rather than a Phase 65 deferral.
- The D7 cell cites `BbjRunActionBase.java` as the comparison side and routes IntelliJ-side
  observations to `RU-63-01`; no `location:` resolves inside `bbj-intellij/`.
- The D-13 discrepancy on `Commands.cjs` is stated in the section text.
- Phase-wide verdict count is 14 and placeholder count is 21; 14 + 21 + 5 = 40.
- Every finding has all 13 fields, a unique well-formed ID and a non-blank `dedup:` that explicitly
  covered #231, #485 and #486.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-02 advances: `extension.ts` and `Commands/CompilerOptions.ts` — ROADMAP Phase 62 criterion 1's
  first two named surfaces — plus `Commands.cjs` have a recorded pass/fail against every dimension
  that applies to them.
- Phase 65 inherits concrete, evidenced VS Code-side D1 records on the process-spawn and EM-credential
  paths rather than a deferral pointer.
- Phase 63 inherits durable `RU-63-01` referrals for every IntelliJ-side parity observation.
- ROADMAP Phase 62 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/62-extension-host-webview-composer-review/62-02-SUMMARY.md` when done.
</output>
