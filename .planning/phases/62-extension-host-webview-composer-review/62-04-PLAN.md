---
phase: 62-extension-host-webview-composer-review
plan: 04
type: execute
wave: 4
depends_on: [62-03]
files_modified:
  - .planning/reviews/62-COVERAGE.md
autonomous: true
requirements: [RVW-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-62-05` (TextMate grammar & language configuration, 3 files / 256 LOC) has all 7 live dimensions recorded in `62-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-10, D-11)"
    - "All 3 files — `syntaxes/bbj.tmLanguage.json`, `bbj-language-configuration.json`, `bbx-language-configuration.json` — are named at least once inside the `## RU-62-05` section, so coverage is file-granular"
    - "`RU-62-05` is swept in full even though ROADMAP's Phase 62 success criteria do not name it — the grid is the contract and the criteria are a subset of it — and the cell text says so, so the extra coverage reads as deliberate rather than accidental (D-13)"
    - "Every `RU-62-05` finding is dedup-checked against **#381** (`config.bbx` is no longer highlighted and now shows up as plain text) by number, because #381 is exactly this surface's failure mode and is in the frozen snapshot — a `#381 duplicate` or `#381 partial-overlap` verdict is the expected outcome here, not a defect (D-13)"
    - "The D7 parity cell is filled by a stated method, recorded in the cell: a programmatic diff establishing whether the IntelliJ side is a build-time copy of these files, plus a full read of both contribution manifests — `bbj-vscode/package.json` and `bbj-intellij/src/main/resources/textmate/bbj-bundle/package.json` — which is where a divergence in how the same bytes are consumed would live"
    - "The discrepancy between INVENTORY's `RU-62-05` per-file table (which names one file under `syntaxes/`) and INVENTORY's Surface Accounting (which assigns the whole `syntaxes/` directory to `RU-62-05`) is **re-derived from the tree and surfaced as an observation**, not silently adopted and not resolved by editing INVENTORY, and it adds no grid row (D-14)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "The unit's 1 `n/a` cell still carries INVENTORY's R-D6-CENTRAL text verbatim, the phase-wide carried-forward `n/a` count is still 5, and the phase-wide cell-line count is still 40 (D-03, D-14)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "`62-COVERAGE.md` contains exactly one `## RU-62-0N` section per unit — five in total — and no plan's section overwrites or merges into another's"
    - "No `applies` cell line in `62-COVERAGE.md` records a bare verdict: every one of the 35 carries a written check line naming the concrete checks applied, and zero cell lines remain `pending` at phase close"
    - "Finding IDs are monotonic within each `(62, dimension)` pair with no gaps and no reuse, so two findings discovered in the same unit have a deterministic order"
    - "No two Phase 62 plans share a wave; each of `62-02`..`62-05` declares its predecessor in `depends_on`, so the shared `62-COVERAGE.md` is never written concurrently"

  artifacts:
    - ".planning/reviews/62-COVERAGE.md §`## RU-62-05 — TextMate grammar & language configuration` — 7 live cells recorded, 1 `n/a` cell untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"

  key_links:
    - "`RU-62-05` `### Cross-unit referrals` addressed to `RU-63-02` -> Phase 63's language registration, editor support & notifications sweep, whose `BbjTextMateBundleProvider.java` is the IntelliJ-side consumer of these files, as durable records because Phase 63 may already be running (D-05, D-06)"
    - "`RU-62-05` findings dedup-checked against #381 -> Phase 69's issue drafting, which must not re-file what #381 already covers (RVW-07, ISSUE-04)"
    - "The `syntaxes/` enumeration discrepancy -> the scope-fidelity note plan 62-05 writes into `## Phase 62 Close-Out` (D-13, D-14)"
    - "`RU-62-05` cells -> the phase-wide D-14 cell and file gates re-run by plan 62-05"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, XSS,
    # path traversal, injection taxonomies, GDPR) deliberately DROPPED — in this phase they are the
    # review *subject matter*, not constraints on the plan.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 62 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `bbj-intellij/` — that plugin is read as D7 comparison material only, no Phase 62 coverage cell covers it, and an IntelliJ-side defect belongs in `### Cross-unit referrals` addressed to `RU-63-02` (D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding (D-09, as approved at the 62-01 checkpoint)"
    - "MUST NOT write a credential, an API token or an absolute developer-machine path into any record — anchor every citation as a repository-relative `path:line`"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and are superseded by INVENTORY.md (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53 fix(#494)` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone, so the `syntaxes/` enumeration discrepancy is surfaced as an observation and never corrected in place (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-62-0N` section other than `RU-62-05`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03, D-04)"
    - "MUST NOT add a grid row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 62 slice defines — the `syntaxes/` enumeration discrepancy is an observation recorded in prose, not a new row (D-14)"
    - "MUST NOT sweep this unit at reduced depth because its three files are small and declarative — 256 LOC of JSON carries the same 7-dimension obligation as the 1,533-LOC generator unit, and open issue #381 sits on exactly this surface (D-13)"
    - "MUST NOT record a finding on this surface without an explicit `#381` verdict in `dedup:` — `none` is a legitimate verdict, an unexamined one is not (RVW-07, D-13)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-10)"
    - "MUST NOT record a cell verdict for a file the sweep did not open"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-62-05` — TextMate grammar & language configuration**
(`bbj-vscode/syntaxes/bbj.tmLanguage.json` 74, `bbj-vscode/bbj-language-configuration.json` 100,
`bbj-vscode/bbx-language-configuration.json` 82 — 3 files, 256 LOC, INVENTORY risk rank 4 of 5)
across its 7 live dimensions and record the result in the `## RU-62-05` section of
`.planning/reviews/62-COVERAGE.md`.

Purpose: these three files are the highlighting and editing-behavior source **shared by both IDEs** —
`bbj-intellij/build.gradle.kts`'s `copyTextMateBundle` task copies them out of `bbj-vscode/` into the
plugin's TextMate bundle — which is what makes this the phase's most genuine D7 parity surface, and
open issue **#381** (`config.bbx` lost syntax highlighting and now shows as plain text) is exactly this
surface's failure mode. It is also the second D-13 unit: INVENTORY's grid contracts it, ROADMAP's
Phase 62 success criteria do not name it, and it is swept in full anyway.

Output: 7 recorded cells with written check lines, plus any finding records, not-reproducible
dispositions and cross-unit referrals. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md
@.planning/phases/62-extension-host-webview-composer-review/62-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 62 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/62-COVERAGE.md` — section
  `## RU-62-05 — TextMate grammar & language configuration` only. The file's header, grid, D-14 gate,
  stopping rule, exclusion-reason block, the other four unit sections and `## Phase 62 Close-Out` are
  inherited from plan `62-01` and are not touched.

**Section anchors this plan fills.** `## RU-62-05 — TextMate grammar & language configuration` → its
`### Cells`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`.
Downstream phases consume these anchors by name: Phase 63 (referrals to `RU-63-02`), Phase 67
(`classification:`), Phase 68 (DOC-03), Phase 69 (issue drafting, gated on the #381 dedup verdicts
recorded here).

**Finding-ID namespace.** `P62-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(62, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `62-01`,
`62-02` and `62-03`. `P00-*` is INVENTORY's reserved template namespace and is never allocated.

**Plan → review-unit map.** This plan owns `RU-62-05`. The full 5-plan map, the wave chain and the
section shape live in `62-01-PLAN.md` §"Artifacts this phase produces".

**Inherited routing.** INVENTORY's routing table (D-06) has **no Phase 62 rows**; the absence is
deliberate.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-02 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-02 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-02 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-02 | concurrency | all 5 plans, incl. this one | explicit |
| 5 | RVW-03 | adjacency | 62-01 and 62-03 (composer-layer D4 baseline) | explicit |
| 6 | RVW-03 | empty | 62-01 (`RU-62-04`'s D4 asymmetric-baseline qualifier) | explicit |
| 7 | RVW-03 | ordering | 62-01 and 62-03 (duplication allocated once per layer) | explicit |
| 8 | RVW-03 | concurrency | 62-01, as a flat-scalar `verification: backstop` marker | **backstop** |

Accounting: 8 surfaced = 8 authored = 0 dropped.

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-62-05 at evidence tier `repro` — D1, D2, D3, D7</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`.planning/reviews/62-COVERAGE.md` exists, contains a `## RU-62-05 — ` heading whose 7
  live cell lines are still unrecorded, and plan `62-03` has committed — the phase-wide verdict count is
  exactly 21. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      completed `## RU-62-04` section as the shape to copy, and every `### Cross-unit referrals` entry
      addressed to `RU-62-05`
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent), §"Finding Record Template", §"Frozen Open-Issue Snapshot" — read the **#381**
      row in full, since it is this surface's exact failure mode; §"Phase 62 review units … —
      remainder" → `### RU-62-05` with its per-file table and its risk-rank rationale;
      §"Surface Accounting & Named Exclusions" → the `bbj-vscode/` breakdown row that dispositions
      `syntaxes/`, `bbj-language-configuration.json` and `bbx-language-configuration.json`
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-02, RVW-06, RVW-07
    - `.planning/STATE.md` §"Tech Debt" — the entry "IntelliJ TextMate bundle cannot exclude
      config.bbx at filename level", which is this unit's D7 subject matter and a known constraint
      rather than a novel finding
    - **Review targets, read in full:** `bbj-vscode/syntaxes/bbj.tmLanguage.json` (74),
      `bbj-vscode/bbj-language-configuration.json` (100),
      `bbj-vscode/bbx-language-configuration.json` (82)
    - `bbj-vscode/package.json` — the `contributes.languages`, `contributes.grammars` and
      `configurationDefaults` blocks, plus the `bbx-config` language ID registration and its
      `onLanguage:` activation event: this is the VS Code-side consumption manifest
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-intellij/src/main/resources/textmate/bbj-bundle/package.json` (the IntelliJ-side
      consumption manifest, read in full), `bbj-intellij/build.gradle.kts` — the
      `copyTextMateBundle` task around lines 83-91 that copies these files out of `bbj-vscode/` — and
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjTextMateBundleProvider.java` (49), the
      bundle's registration point
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of
`## RU-62-05 — TextMate grammar & language configuration`, replacing the placeholder on each with
`pass` or `fail` plus a written line naming the concrete checks applied. Append any resulting finding
records under that section's `### Findings`. Touch no other section. Enumerate the dimensions
explicitly so none is absorbed by whichever is loudest (D-11): this task covers **D1 Security,
D2 Correctness & error handling, D3 Performance & resource use, and D7 Cross-IDE parity** — and no
others.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording. These are declarative JSON
files with no executable logic, so the honest D1 question is what a *pattern* can do rather than what
code can do: whether any regular expression in `bbj.tmLanguage.json` is susceptible to catastrophic
backtracking on adversarial input (nested quantifiers, overlapping alternations) such that opening a
crafted `.bbj` file stalls the editor's tokenizer — this is a real availability-class D1/D3 boundary
and the finding belongs to whichever dimension the evidence supports, with the other listed in
`secondary:`; whether any `include` or external reference in the grammar points outside these files;
and whether the two language-configuration files enable any behavior beyond comment markers, bracket
pairs, auto-closing pairs, surrounding pairs, folding and word patterns. If the honest verdict is that
no D1 finding is obtainable from declarative highlighting data, record `pass` with a written line
saying exactly which of those questions were asked and what each returned — a `pass` here must be
stronger evidence than an `n/a` exclusion would have been, never weaker (D-10).

*D2 Correctness & error handling* (tier `repro`). Assess against wrong edge-case behavior and
off-by-one — for a grammar, that means mis-tokenization. The concrete checks: every pattern in
`bbj.tmLanguage.json` against BBj's actual lexical rules, in particular the case-insensitivity the
language requires and `CLAUDE.md` states explicitly; string literals including BBj's doubled-quote
escape; line continuations; comments (`REM` and any alternative form); and labels. For the two
language-configuration files: whether the comment markers, bracket pairs, auto-closing pairs and word
pattern match what the grammar tokenizes, and whether `bbx-language-configuration.json`'s settings are
correct for a `config.bbx`-style file rather than copied from the BBj one. Validate all three files
parse as JSON (`node -e` or `python3 -m json.tool`) and record the command used. Each claim needs a
concrete input — an exact BBj or config.bbx snippet — and the exact `file:line` of the pattern that
mis-handles it.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost. The concrete checks:
pattern complexity in `bbj.tmLanguage.json` — unbounded quantifiers, nested groups, alternations that
can retry — measured against how often a TextMate tokenizer re-runs them (per line, per edit); and
whether any pattern's worst case is superlinear in line length. Where a pathological pattern is found,
record the concrete input shape and the observed cost rather than asserting the risk.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). **This is the phase's most genuine parity surface and it
gets a stated two-part method, both parts recorded in the cell:**
  - **Part 1, mechanical.** Establish from `bbj-intellij/build.gradle.kts`'s `copyTextMateBundle` task
    exactly which files are copied from `bbj-vscode/` into the plugin bundle, then run a programmatic
    diff between each source file and its copied counterpart under
    `bbj-intellij/build/resources/main/textmate/bbj-bundle/` if a build output exists. Record the
    command and its literal output. If the copy is byte-identical, say so — that is a real finding
    about where divergence *cannot* live, and it redirects the rest of the cell.
  - **Part 2, full read of both consumption manifests.** The divergence, if any, lives in how the same
    bytes are declared to each host. Compare `bbj-vscode/package.json`'s `contributes.languages`,
    `contributes.grammars` and `configurationDefaults` against
    `bbj-intellij/src/main/resources/textmate/bbj-bundle/package.json`'s own `contributes` block,
    naming every difference: language IDs, file extensions, filename patterns, scope names, which
    configuration file each language points at, and — specifically — whether the same set of files
    resolves to the same language on each side for `config.bbx`, `Config.bbx`, `config.min` and a
    plain `.bbx` file. `.planning/STATE.md` records as known tech debt that the IntelliJ TextMate
    bundle cannot exclude `config.bbx` at filename level; confirm that against the manifest actually
    read rather than restating it, and note which side's declaration produces which result.
  Record a divergence whose defect is on the **VS Code** side as a `P62-D7-{seq}` finding located
  inside `bbj-vscode/`, citing the IntelliJ `file:line` in `evidence:` as the comparison point only.
  Where the defect is plainly on the **IntelliJ** side, record nothing in `### Findings` and write a
  `### Cross-unit referrals` entry addressed to **`RU-63-02`** — whose `BbjTextMateBundleProvider.java`
  is the IntelliJ-side consumer of these exact files — naming the surface, the check Phase 63 should
  run, and the VS Code-side evidence that motivated it, as a durable record (D-06).

**Enumeration re-derivation (D-14).** Run `ls bbj-vscode/syntaxes/` and record its literal output in
the cell text. INVENTORY's `RU-62-05` per-file table names one file under `syntaxes/`, while
INVENTORY's Surface Accounting dispositions the whole `syntaxes/` directory to `RU-62-05`. If the tree
contains more files than the per-file table names, **surface that disagreement as an observation** —
state which files exist, which are hand-written versus generated (check `bbj-vscode/langium-config.json`
for any generated output target before calling a file generated), and which are contributed in
`package.json` — and note that `bbx.tmLanguage.json` in particular is the grammar the `bbx-config`
language ID resolves to, making it directly relevant to #381. **Do not edit INVENTORY, do not add a
grid row or cell line, and do not adopt the smaller number as if the disagreement did not exist**
(D-14). If a defect in a file outside the per-file table is found and recorded, its record must say
in `evidence:` that the file is inside INVENTORY's Surface-Accounting scope for this unit but outside
its per-file table, so the extra coverage is visible rather than accidental.

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's 13-field
template verbatim, one field per line, inside a fenced block, in the template's exact field order.
`unit:` is `RU-62-05`. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline.
`dedup:` is never blank and **every record in this unit states an explicit verdict on #381** — `none`,
`#381 duplicate`, or `#381 partial-overlap — <what this adds>` — because #381 is exactly this
surface's failure mode. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: `awk '/^## RU-62-05 /,/^## RU-62-02 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru05.txt; grep -cE '^- D[1237] .* — (pass|fail) — ' /tmp/p62ru05.txt` prints `4`
    - Each of the four dimensions this task owns is individually recorded: `grep -cE '^- D1 .* — (pass|fail) — ' /tmp/p62ru05.txt`, `grep -cE '^- D2 .* — (pass|fail) — ' /tmp/p62ru05.txt`, `grep -cE '^- D3 .* — (pass|fail) — ' /tmp/p62ru05.txt` and `grep -cE '^- D7 .* — (pass|fail) — ' /tmp/p62ru05.txt` each print `1`
    - The D7 method is stated, not assumed: `/tmp/p62ru05.txt` contains `copyTextMateBundle`, contains `package.json`, and contains `RU-63-02`
    - The #381 surface is addressed explicitly: `/tmp/p62ru05.txt` contains `#381` and contains `bbx-config`
    - The `syntaxes/` enumeration was re-derived from the tree: `/tmp/p62ru05.txt` contains `bbx.tmLanguage.json`
    - All three JSON files parse: `for j in bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json; do node -e "JSON.parse(require('fs').readFileSync('$j','utf8'))" || echo "BAD $j"; done` prints nothing
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `25` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `10`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Every `fail` line names the findings it recorded: `awk '/^- D[1-8] .* — fail — /{if ($0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print "UNNAMED: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed, none from the reserved namespace: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-05 /,/^## RU-62-02 /" "$f" > /tmp/p62ru05.txt; test "$(grep -cE "^- D[1237] .* — (pass|fail) — " /tmp/p62ru05.txt)" = 4; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru05.txt)" = 1; done; for x in copyTextMateBundle package.json RU-63-02 "#381" bbx-config bbx.tmLanguage.json; do grep -q "$x" /tmp/p62ru05.txt; done; for j in bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json; do node -e "JSON.parse(require(\"fs\").readFileSync(\"$j\",\"utf8\"))"; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 25; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 10; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(awk "/^- D[1-8] .* — fail — /{if (\$0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print \"UNNAMED\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_05_REPRO_OK'</automated>
  </verify>
  <done>`RU-62-05`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — pattern-level availability and reference-scope questions for D1;
  mis-tokenization against BBj's actual lexical rules including case-insensitivity and the doubled-quote
  escape for D2; pattern worst-case cost for D3; and the two-part parity method — the `copyTextMateBundle`
  diff plus a full read of both consumption manifests — for D7, with `bbx-config` file resolution and
  #381 addressed explicitly and IntelliJ-side observations routed to `RU-63-02`. The `syntaxes/`
  enumeration is re-derived from the tree and any disagreement with INVENTORY's per-file table is
  surfaced as an observation without editing INVENTORY or adding a grid row.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-62-05 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`## RU-62-05`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed, and the
  phase-wide verdict count is exactly 25. Halt otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` §`## RU-62-05` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot" — the **#381** row
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-02
    - `.planning/ROADMAP.md` §"Phase 62" success criteria — note that none of them names `RU-62-05`'s
      three files, the D-13 discrepancy this cell records
    - **Review targets:** the same 3 files as Task 1, plus `bbj-vscode/syntaxes/bbx.tmLanguage.json`
      read as the D4 duplication counterpart to `bbj.tmLanguage.json` (it is inside INVENTORY's
      Surface-Accounting scope for this unit but outside its per-file table — say so where it is used)
    - **Test surface:** `bbj-vscode/test/` — in particular whether any test asserts tokenization or any
      language-configuration behavior; `bbj-vscode/test/test-data/` and
      `bbj-vscode/test/example-files.test.ts`, which parse `.bbj` files through the *language server*
      rather than through the TextMate grammar — establish and state that distinction rather than
      counting parser coverage as grammar coverage
    - `CLAUDE.md` §IDE Integration — its claim that both IDEs share the TextMate grammar
      `syntaxes/bbj.tmLanguage.json`, checkable against the `copyTextMateBundle` evidence gathered in
      Task 1
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-62-05` and append any resulting records. Touch no other
section. Enumerate the dimensions explicitly so none is absorbed by another (D-11): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, dead code, inconsistent
patterns and missing abstractions. The concrete checks: duplication between
`bbj-language-configuration.json` and `bbx-language-configuration.json` — run a programmatic diff
between the two and record the command and its output, then say which differences are deliberate
(different language, different comment syntax) and which look like copy-paste drift; duplication
between `bbj.tmLanguage.json` and `bbx.tmLanguage.json` on the same basis; unreachable or shadowed
patterns inside `bbj.tmLanguage.json` — a pattern that can never match because an earlier one always
wins; scope-name consistency against TextMate conventions across all patterns; and whether
`gen-bbj.tmLanguage.json` is a live artifact or a generated leftover, established from
`bbj-vscode/langium-config.json` and from whether anything contributes or references it, and recorded
as a fact either way rather than asserted as dead code.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). The concrete checks:
whether any test in `bbj-vscode/test/` asserts TextMate tokenization at all, or asserts any
language-configuration behavior (comment toggling, bracket matching, auto-closing); and — stated
explicitly, because it is the easy mistake here — whether `example-files.test.ts`'s parse-every-file
guarantee covers the *language server's* lexer/parser rather than the *TextMate grammar*, so
grammar-side regressions like #381 have no automated guard. Record gaps as `P62-D5-{seq}` findings
naming the untested surface; every `dedup:` states an explicit #381 verdict.

*D8 Comment & doc accuracy* (tier `trace`). JSON carries no comments, so the D8 target is the prose
that describes these files. The concrete checks: `CLAUDE.md`'s §IDE Integration claim that both IDEs
share the TextMate grammar and the run tools, checked against Task 1's `copyTextMateBundle` evidence —
including whether the claim names the full set of shared files or only a subset; any `package.json`
description or documentation-site page describing which files get highlighted; and whether the
`bbx-config` language's registration in `bbj-vscode/package.json` matches what any prose says about
`config.bbx` support. Record a divergence as a finding with the exact `file:line` in `location:`.

**D-13 scope-fidelity note.** In the D4 or D5 cell text — whichever names it first — state plainly
that `RU-62-05` is swept in full despite not appearing in ROADMAP's Phase 62 success criteria, because
the grid is the contract and the criteria are a subset of it. Plan `62-05` repeats this in the
close-out; recording it here keeps the reason adjacent to the coverage claim.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 7 live cells now carry a verdict: `awk '/^## RU-62-05 /,/^## RU-62-02 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru05.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/p62ru05.txt` prints `7`
    - Each of the three dimensions this task owns is individually recorded: `grep -cE '^- D4 .* — (pass|fail) — ' /tmp/p62ru05.txt`, `grep -cE '^- D5 .* — (pass|fail) — ' /tmp/p62ru05.txt` and `grep -cE '^- D8 .* — (pass|fail) — ' /tmp/p62ru05.txt` each print `1`
    - The `n/a` cell is untouched: `grep -cE '^- D6 .* — n/a — ' /tmp/p62ru05.txt` prints `1`
    - Every file in the unit is named inside its own section: `for f in bbj.tmLanguage.json bbj-language-configuration.json bbx-language-configuration.json; do grep -q "$f" /tmp/p62ru05.txt || echo "MISSING $f"; done` prints nothing
    - The D4 duplication assessment is mechanical: `/tmp/p62ru05.txt` contains the string `diff`
    - The grammar-versus-parser coverage distinction is stated: `/tmp/p62ru05.txt` contains `example-files.test.ts`
    - The D-13 scope-fidelity note is recorded adjacent to the coverage claim: `/tmp/p62ru05.txt` contains the string `success criteria`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `28` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `7`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - All three sub-blocks are present for this unit: `/tmp/p62ru05.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Every `pass` line remains a written line: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT"}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-05 /,/^## RU-62-02 /" "$f" > /tmp/p62ru05.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p62ru05.txt)" = 7; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru05.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p62ru05.txt)" = 1; for x in bbj.tmLanguage.json bbj-language-configuration.json bbx-language-configuration.json diff example-files.test.ts "success criteria" "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/p62ru05.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 28; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 7; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_05_COMPLETE'</automated>
  </verify>
  <done>`RU-62-05` is complete under the stopping rule: all 7 live cells carry a verdict plus a written
  check line, all 3 files are named inside the section, the two language-configuration files and the two
  grammars are diffed programmatically with method and output recorded, the grammar-versus-language-server
  coverage distinction is stated explicitly rather than conflated, the D-13 discrepancy (`RU-62-05` swept
  despite ROADMAP's criteria omitting it) is stated adjacent to the coverage claim, every record carries
  an explicit #381 verdict, and every unpromoted claim is visible under
  `### Not-reproducible dispositions`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure and record-integrity threats. Rows marked *subject-matter* are boundaries this
plan **documents**.

| Boundary | Description |
|----------|-------------|
| an opened `.bbj` / `config.bbx` file → the TextMate tokenizer (*subject-matter*) | Untrusted document content drives every regular expression in `bbj.tmLanguage.json` on every edit; a catastrophically-backtracking pattern turns opening a file into an editor stall. |
| `bbj-vscode/` grammar and configuration files → the IntelliJ plugin bundle (*subject-matter*) | `copyTextMateBundle` copies these files across a build boundary into a second host that declares them under different language IDs and filename patterns. |
| `62-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| INVENTORY's `RU-62-05` per-file table vs its Surface Accounting | Two statements of the same scope that may disagree; adopting the smaller silently would hide coverage, editing INVENTORY would break its immutability. |
| `bbj-intellij/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 63's rows. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `62-COVERAGE.md` → the other four plans | One file, five writers. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-P04-S1 | Denial of Service | regular expressions in `bbj.tmLanguage.json` re-run per line per edit | medium | transfer | Task 1's D1 and D3 checks look for nested quantifiers and overlapping alternations with a superlinear worst case, and require the concrete input shape and observed cost rather than an asserted risk; the finding is filed against whichever dimension the evidence supports with the other in `secondary:`. |
| T-62-P04-S2 | Tampering | divergent language/filename declarations across the two consumption manifests | medium | transfer | Task 1's D7 Part 2 compares both `contributes` blocks file-resolution by file-resolution for `config.bbx`, `Config.bbx`, `config.min` and plain `.bbx`, confirming the known `config.bbx` filename-exclusion constraint against the manifest actually read rather than restating it. |
| T-62-P04-01 | Repudiation | adopting INVENTORY's smaller per-file enumeration and hiding the rest of `syntaxes/` | medium | mitigate | D-14 requires re-derivation from the tree, not restatement; the acceptance criteria require `bbx.tmLanguage.json` to be named in the section, and the prohibitions forbid both editing INVENTORY and adding a grid row to "fix" it. |
| T-62-P04-02 | Repudiation | counting language-server parser coverage as TextMate grammar coverage | medium | mitigate | Task 2's D5 check requires the distinction to be stated explicitly and `example-files.test.ts` to be named, so a grammar-side coverage gap like #381's cannot be masked by parser-side green tests. |
| T-62-P04-03 | Repudiation | re-filing what #381 already covers | medium | mitigate | Every record in this unit must state an explicit `#381` verdict in `dedup:`; `none` is legitimate, unexamined is not. Enforced as a prohibition and inherited by Phase 69's ISSUE-04 gate. |
| T-62-P04-04 | Repudiation | sweeping a 256-LOC declarative unit thinly because it is small | medium | mitigate | Prohibition against reduced depth, plus per-dimension acceptance criteria requiring each of the 7 live cells individually and a minimum-length written line on every `pass`. |
| T-62-P04-05 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-09's two-tier rule as approved at the `62-01` checkpoint; enforced as a `must_haves` prohibition. |
| T-62-P04-06 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or any D1 primary dimension; every record writes all six tests pass/FAIL inline. |
| T-62-P04-07 | Tampering | five plans writing one `62-COVERAGE.md` | medium | mitigate | Wave 4 behind `depends_on: [62-03]` makes ordering structural (D-04); both tasks assert exact global counts (25/10, then 28/7) plus the invariant 5 `n/a` lines and 40 total cell lines. |
| T-62-P04-08 | Tampering | reading `bbj-intellij/` as D7 comparison material | medium | mitigate | D-05 prohibition plus an anchored acceptance grep: no `location:` may resolve inside `bbj-intellij/`. IntelliJ-side defects route to `### Cross-unit referrals` addressed to `RU-63-02` as durable records (D-06). |
| T-62-P04-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk`, `diff`, `ls` and an already-installed `node -e` JSON parse check. |
</threat_model>

<verification>
- `## RU-62-05`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's R-D6-CENTRAL text verbatim.
- All 3 unit files are named inside the section, and all 3 parse as valid JSON.
- The D7 cell records both parts of its stated method — the `copyTextMateBundle` diff and the
  manifest-versus-manifest comparison — and routes IntelliJ-side observations to `RU-63-02`.
- `#381` and `bbx-config` are addressed explicitly; every finding record states a `#381` dedup verdict.
- The `syntaxes/` enumeration is re-derived from the tree and any disagreement with INVENTORY's
  per-file table is surfaced in prose; INVENTORY is unedited and the grid is still 40 cells.
- The D-13 discrepancy on `RU-62-05` is stated in the section text.
- Phase-wide verdict count is 28 and placeholder count is 7; 28 + 7 + 5 = 40.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-02 advances: the TextMate grammar and both language-configuration files have a recorded
  pass/fail against every dimension that applies to them, despite ROADMAP's criteria not naming them.
- The phase's most genuine D7 parity surface is assessed by a stated method whose output is recorded,
  not by assertion.
- Phase 69 inherits an explicit #381 dedup verdict on every record from this surface, so ISSUE-04 has
  a decided answer rather than a re-triage.
- Phase 63 inherits durable `RU-63-02` referrals for every IntelliJ-side parity observation.
- ROADMAP Phase 62 criteria 3 and 4 hold for every record written here.
</success_criteria>

<output>
Create `.planning/phases/62-extension-host-webview-composer-review/62-04-SUMMARY.md` when done.
</output>
