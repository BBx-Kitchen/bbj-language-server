---
phase: 62-extension-host-webview-composer-review
plan: 05
type: execute
wave: 5
depends_on: [62-04]
files_modified:
  - .planning/reviews/62-COVERAGE.md
autonomous: true
requirements: [RVW-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths
    - "`RU-62-02` (editor feature modules, 4 files / 268 LOC — the smallest unit by LOC in Phase 62) has all 7 live dimensions recorded in `62-COVERAGE.md` — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-10, D-11)"
    - "All 4 files — `document-formatter.ts`, `line-numbering.ts`, `tokenized-bbj.ts`, `decompile-io.ts` — are named at least once inside the `## RU-62-02` section, so coverage is file-granular (ROADMAP Phase 62 criterion 1)"
    - "**Gate 1 (D-14.1), re-derived not restated:** the awk pass over INVENTORY's five `RU-62-0[1-5]` grid rows is re-run at phase close, its output recorded, and set against both the stated totals and a third independent count of what `62-COVERAGE.md` itself now contains — three sources that must agree, with any disagreement surfaced as a defect rather than adopted as a number"
    - "**Gate 2 (D-14.2), re-derived not restated:** all 22 in-scope files are enumerated **from the tree** by the `ls … | wc -l` pass, not from any list typed into a plan, and each of the 22 basenames is confirmed present somewhere in `62-COVERAGE.md` by a loop whose output is recorded"
    - "Zero cell lines remain `pending` at phase close: all 35 `applies` cells carry a verdict plus a written check line and all 5 `n/a` cells carry INVENTORY's R-D6-CENTRAL text verbatim, totalling 40"
    - "`## Phase 62 Close-Out` records the D-13 scope-fidelity discrepancy explicitly — that `RU-62-05` and `Commands/Commands.cjs` were swept in full despite not appearing in ROADMAP's Phase 62 success criteria, and why — so the extra coverage reads as deliberate"
    - "Every one of ROADMAP's four Phase 62 success criteria is answered in the close-out with the evidence that establishes it, including criterion 2's D4 duplication requirement, which `RU-62-04` satisfies at the generator layer and `RU-62-03` cross-references at the logic/UI layer"
    - "Every cross-unit referral written anywhere in this phase is accounted for in the close-out: those addressed to another Phase 62 unit are confirmed resolved by their target unit's own text, and those addressed to `RU-63-01`/`RU-63-02`/`RU-63-04` are listed as outstanding **by design** — durable records Phase 63 inherits, not a handoff assuming Phase 63 has not started (D-06)"
    - "Every recorded finding carries `path:line`, a primary dimension, evidence clearing its tier, and a non-blank `dedup:` checked against the 15 frozen open issues, with #65 (support tokenized BBj files) checked explicitly as this unit's neighbour (RVW-06, RVW-07)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced across the phase, all 8 authored, 0 dropped)
    - "`62-COVERAGE.md` contains exactly one `## RU-62-0N` section per unit — five in total — and no plan's section overwrites or merges into another's"
    - "No `applies` cell line in `62-COVERAGE.md` records a bare verdict: every one of the 35 carries a written check line naming the concrete checks applied, and zero cell lines remain `pending` at phase close"
    - "Finding IDs are monotonic within each `(62, dimension)` pair with no gaps and no reuse, so two findings discovered in the same unit have a deterministic order"
    - "No two Phase 62 plans share a wave; each of `62-02`..`62-05` declares its predecessor in `depends_on`, so the shared `62-COVERAGE.md` is never written concurrently"

  artifacts:
    - ".planning/reviews/62-COVERAGE.md §`## RU-62-02 — Editor feature modules` — 7 live cells recorded, 1 `n/a` cell untouched, plus `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals` for this unit"
    - ".planning/reviews/62-COVERAGE.md §`## Phase 62 Close-Out` — the 22-file tree enumeration and its per-basename presence check, the closing three-source cell-gate re-derivation, finding counts by dimension and by disposition, the not-reproducible and cross-unit-referral accounting, the D-13 scope-fidelity note, the four ROADMAP success criteria answered with evidence, and the ISSUE-01 and INVENTORY-immutability confirmations"

  key_links:
    - "`## Phase 62 Close-Out` -> Phase 68's DOC-03 coverage statement, which concatenates the five `{NN}-COVERAGE.md` files against INVENTORY's grid without re-deriving scope"
    - "the close-out's outstanding `RU-63-*` referral list -> Phase 63, which re-triages each one whether or not it has already swept the named unit (D-06)"
    - "the close-out's finding-count-by-disposition table -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split (INVENTORY 3c)"
    - "`RU-62-02` D1 findings on the formatter's `java -jar` spawn -> Phase 65's SEC-05 synthesis; the vendored JAR's own provenance belongs to `RU-64-03` in Phase 64, not here"
    - "`tokenized-bbj.ts` findings -> #65's dedup neighbourhood and Phase 69's issue drafting"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, XSS,
    # path traversal, injection taxonomies, GDPR) deliberately DROPPED — in this phase they are the
    # review *subject matter*, not constraints on the plan.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 62 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `bbj-intellij/` — that plugin is read as D7 comparison material only, and an IntelliJ-side defect belongs in `### Cross-unit referrals` addressed to `RU-63-01` or `RU-63-02` (D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding (D-09, as approved at the 62-01 checkpoint)"
    - "MUST NOT write a credential, an API token or an absolute developer-machine path into any record or into the close-out — anchor every citation as a repository-relative `path:line`, and record command *outputs* rather than pasting a machine-specific working directory"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01 and are superseded by INVENTORY.md (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53 fix(#494)` as a live finding (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone, so a gate disagreement is surfaced in the close-out and never corrected in place (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-62-0N` section other than `RU-62-02` — the close-out is the only other region this plan writes — and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03, D-04)"
    - "MUST NOT add a grid row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 62 slice defines, and MUST NOT introduce any close-out line beginning with a `- D` bullet, which would corrupt the cell-line count the gate depends on (D-14)"
    - "MUST NOT restate either D-14 gate's numbers from a plan or from the file header — both are re-run against the tree and against INVENTORY at execution time, and a disagreement is itself a defect to surface, not a number to adopt (D-14)"
    - "MUST NOT record a `RU-63-*` referral as unresolved-and-therefore-a-problem — those are durable records Phase 63 inherits by design, and closing the phase does not require them to be answered (D-06)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-10)"
    - "MUST NOT sweep this unit at reduced depth because its four modules are the smallest in the phase — 268 LOC carries the same 7-dimension obligation as the 1,533-LOC generator unit"
    - "MUST NOT record a finding about the vendored `tools/formatter/*.jar` binaries' provenance or pinning — `RU-64-03` owns those in Phase 64; note the boundary under `### Cross-unit referrals` instead"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Sweep review unit **`RU-62-02` — Editor feature modules** (`bbj-vscode/src/document-formatter.ts` 96,
`bbj-vscode/src/line-numbering.ts` 49, `bbj-vscode/src/tokenized-bbj.ts` 39,
`bbj-vscode/src/decompile-io.ts` 84 — 4 files, 268 LOC, INVENTORY risk rank 5 of 5) across its 7 live
dimensions, then **close the phase**: re-derive both D-14 gates, account for every finding, referral
and disposition, record the D-13 scope-fidelity discrepancy, and answer ROADMAP's four Phase 62
success criteria with the evidence that establishes each.

Purpose: this is the last unit and the last wave. After it, `62-COVERAGE.md` must contain a verdict
for all 35 `applies` cells, INVENTORY's R-D6-CENTRAL text verbatim in all 5 `n/a` cells, and zero
placeholders — and the close-out must prove that from the tree and from INVENTORY rather than
asserting it.

Output: 7 recorded cells with written check lines, any finding records, dispositions and referrals,
and a complete `## Phase 62 Close-Out`. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md
@.planning/phases/62-extension-host-webview-composer-review/62-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 62 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact.

**Modified**

- `.planning/reviews/62-COVERAGE.md` — two regions only: the section
  `## RU-62-02 — Editor feature modules`, and `## Phase 62 Close-Out`. The file's header, grid, D-14
  gate block, stopping rule, exclusion-reason block and the other four unit sections are inherited
  from plans `62-01`..`62-04` and are not touched.

**Section anchors this plan fills.** `## RU-62-02 — Editor feature modules` → its `### Cells`,
`### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`; and
`## Phase 62 Close-Out`. Downstream phases consume these anchors by name: Phase 68 reads the close-out
for DOC-03, Phase 67 reads the disposition table, Phase 69 reads the dedup verdicts, Phase 63 reads
the outstanding-referral list.

**Finding-ID namespace.** `P62-D{dimension}-{seq}`, zero-padded to three digits, allocated
monotonically in discovery order within each `(62, dimension)` pair **across the whole phase**. This
plan continues from the highest sequence already present for each dimension after plans `62-01`
through `62-04`, and closes the namespace. `P00-*` is INVENTORY's reserved template namespace and is
never allocated.

**Plan → review-unit map.** This plan owns `RU-62-02` and the phase close-out. The full 5-plan map,
the wave chain and the section shape live in `62-01-PLAN.md` §"Artifacts this phase produces".

**Inherited routing.** INVENTORY's routing table (D-06) has **no Phase 62 rows**; the close-out states
that as a fact, so a reader can see the absence was deliberate rather than an omission.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-02 | adjacency | all 5 plans, incl. this one | explicit |
| 2 | RVW-02 | empty | all 5 plans, incl. this one | explicit |
| 3 | RVW-02 | ordering | all 5 plans, incl. this one | explicit |
| 4 | RVW-02 | concurrency | all 5 plans, incl. this one | explicit |
| 5 | RVW-03 | adjacency | 62-01 and 62-03 (composer-layer D4 baseline) | explicit |
| 6 | RVW-03 | empty | 62-01 (`RU-62-04`'s D4 asymmetric-baseline qualifier) | explicit |
| 7 | RVW-03 | ordering | 62-01 and 62-03 (duplication allocated once per layer) | explicit |
| 8 | RVW-03 | concurrency | 62-01, as a flat-scalar `verification: backstop` marker | **backstop** |

Accounting: 8 surfaced = 8 authored = 0 dropped. The close-out states this accounting once for the
phase.

<!-- The literal below is part of a counting command the close-out must run and record verbatim, and
     it also appears inside this plan's acceptance greps. It is exempted from the
     comment-text-discipline gate because it names a real field prefix to be counted, not a banned
     construct. -->
<!-- planner-discipline-allow: ^id: -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep RU-62-02 at evidence tier `repro` — D1, D2, D3, D7</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`.planning/reviews/62-COVERAGE.md` exists, contains a `## RU-62-02 — ` heading whose 7
  live cell lines are still unrecorded, and plan `62-04` has committed — the phase-wide verdict count is
  exactly 28. Halt and report otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` — the header, `## Stopping Rule & Write Contract`, the
      completed `## RU-62-04` section as the shape to copy, and every `### Cross-unit referrals` entry
      addressed to `RU-62-02`
    - `.planning/reviews/INVENTORY.md` §3a-3d (note D7 is tier `inherited`, resolving to
      repro-equivalent), §"Finding Record Template", §"Frozen Open-Issue Snapshot" — read the **#65**
      row (support tokenized BBj files), which is `tokenized-bbj.ts`'s exact subject;
      §"Phase 62 review units … — remainder" → `### RU-62-02`; §"What counts as an in-scope file" →
      the binary vendored-artifact row, which assigns the `tools/formatter/*.jar` files to `RU-64-03`
      for D1 provenance and D6 pinning only
    - `.planning/REQUIREMENTS.md` lines 14-23 — D1, D2, D3, D7 wording; RVW-02, RVW-06, RVW-07; and
      SEC-05, whose cross-cutting synthesis is Phase 65's and whose concrete per-unit findings are
      this plan's (D-07)
    - **Review targets, read in full:** `bbj-vscode/src/document-formatter.ts` (96),
      `bbj-vscode/src/line-numbering.ts` (49), `bbj-vscode/src/tokenized-bbj.ts` (39),
      `bbj-vscode/src/decompile-io.ts` (84)
    - `bbj-vscode/package.json` — the formatter, denumber, decompile and decompile-readonly command
      contributions and any `languages` entry that gates them
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjLanguageCodeStyleSettingsProvider.java`,
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/BbjCommenter.java` and
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/actions/BbjCompileAction.java` — enough to
      establish, for each of the four VS Code editor features, whether an IntelliJ counterpart exists
      at all; grep `bbj-intellij/src/main/java/` for a formatter, denumber, decompile or
      tokenized-file counterpart rather than reading the whole plugin
  </read_first>
  <action>
Record the D1, D2, D3 and D7 cell lines of `## RU-62-02 — Editor feature modules`, replacing the
placeholder on each with `pass` or `fail` plus a written line naming the concrete checks applied.
Append any resulting finding records under that section's `### Findings`. Touch no other section.
Enumerate the dimensions explicitly so none is absorbed by whichever is loudest (D-11): this task
covers **D1 Security, D2 Correctness & error handling, D3 Performance & resource use, and
D7 Cross-IDE parity** — and no others.

*D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
input, secret exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks:
`document-formatter.ts`'s `child_process.spawn('java', …)` path around `document-formatter.ts:58-59`
— how `jarPath` is constructed from `__dirname` at `document-formatter.ts:10`, whether `java` is
resolved from `PATH` or from a configured absolute location and what a hostile `PATH` entry or a
workspace-relative resolution would mean, whether any document path, workspace path or user setting
reaches the argument array unquoted or unvalidated, and whether `spawn` is used with an argument array
throughout rather than a shell string (state which, with the `file:line`); whether the formatter reads
or writes any temporary file and where; `decompile-io.ts`'s `fs.promises.open`/`stat` calls around
`decompile-io.ts:16-31` — whether the path they receive is constrained to the workspace and what
happens for a symlink, a directory, a device node or a file the process cannot read;
`line-numbering.ts`'s document edits — whether any edit can be applied to a document the user did not
select; and `tokenized-bbj.ts`'s magic-byte detection — whether a truncated or empty read can be
mis-classified in a way that causes a binary file to be treated as text or the reverse. **Record
concrete findings here — do not defer this cell to Phase 65 (D-07).** The vendored JAR's own
provenance and pinning are `RU-64-03`'s in Phase 64: note that boundary under
`### Cross-unit referrals` rather than recording a finding about the binary itself.

*D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource
leaks. The concrete checks: whether `document-formatter.ts` distinguishes a non-zero `java` exit, a
missing `java` binary, a missing JAR and empty stdout, and what each produces for the user; whether
the `spawn` streams are fully consumed and the child reaped, and what happens if the child never
exits; whether `decompile-io.ts`'s `FileHandle` is closed on every path including the throwing ones —
the `let handle: fs.promises.FileHandle | undefined` shape at `decompile-io.ts:16` makes this a direct
`finally`-block question; `line-numbering.ts` at the boundaries — an empty document, a single line, a
document already fully numbered, a document with a line number at the maximum width, and the
off-by-one at the last line; and `tokenized-bbj.ts` for a file shorter than the magic length, a
zero-byte file, and a file whose first bytes match the magic by coincidence. Each claim needs a
runnable reproduction or a line-by-line trace naming the concrete input and the exact `file:line`
where behavior diverges.

*D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, missing debounces,
redundant work and unbounded memory growth. The concrete checks: whether the formatter spawns a JVM
per format request and whether repeated requests can overlap or queue; whether `line-numbering.ts`
rewrites the whole document rather than the changed range; whether `decompile-io.ts` reads a whole
file into memory when only a prefix or a size is needed — it has both a magic-length read and a `stat`
path, so state which callers use which; and whether `tokenized-bbj.ts`'s detection runs per keystroke,
per open, or once.

*D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
concrete divergent behaviour in both IDEs). The concrete checks: for each of the four editor features
— format document, line numbering / denumber, tokenized-BBj detection, and decompile — establish by
grepping `bbj-intellij/src/main/java/` whether an IntelliJ counterpart exists at all, and where one
does, compare what each side actually does with the same input; where none exists, that absence is
itself the concrete parity divergence and is recorded as such rather than skipped. Note that
`tokenized-bbj.ts` sits on open issue **#65**'s subject, so a parity observation there is very likely
`#65 partial-overlap` rather than novel — check it by number. Record a divergence whose defect is on
the **VS Code** side as a `P62-D7-{seq}` finding located inside `bbj-vscode/`, citing the IntelliJ
`file:line` in `evidence:` as the comparison point only. Where the defect is plainly on the
**IntelliJ** side, record nothing in `### Findings` and write a `### Cross-unit referrals` entry
addressed to **`RU-63-01`** (run/compile/EM actions) or **`RU-63-02`** (language registration and
editor support), whichever owns the surface, naming the surface, the check Phase 63 should run, and
the VS Code-side evidence that motivated it — a durable record (D-06).

For every claim that clears its tier, write one record under `### Findings` using INVENTORY's 13-field
template verbatim, one field per line, inside a fenced block, in the template's exact field order.
`unit:` is `RU-62-02`. `classification:` records pass/FAIL for all six INVENTORY 3c tests inline.
`dedup:` is never blank and is checked against the frozen 15, with **#65** checked explicitly by
number. A `critical` or `high` D1 finding follows the disclosure rendering approved at the `62-01`
checkpoint (D-09). Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with the tier it failed and why.
  </action>
  <acceptance_criteria>
    - The four `repro`-tier cells are recorded: `awk '/^## RU-62-02 /,/^## Phase 62 Close-Out/' .planning/reviews/62-COVERAGE.md > /tmp/p62ru02.txt; grep -cE '^- D[1237] .* — (pass|fail) — ' /tmp/p62ru02.txt` prints `4`
    - Each of the four dimensions this task owns is individually recorded: `grep -cE '^- D1 .* — (pass|fail) — ' /tmp/p62ru02.txt`, `grep -cE '^- D2 .* — (pass|fail) — ' /tmp/p62ru02.txt`, `grep -cE '^- D3 .* — (pass|fail) — ' /tmp/p62ru02.txt` and `grep -cE '^- D7 .* — (pass|fail) — ' /tmp/p62ru02.txt` each print `1`
    - The D1 cell addresses the spawn path concretely: `/tmp/p62ru02.txt` contains `spawn` and contains `RU-64-03`
    - The D7 cell routes to a Phase 63 unit and addresses #65: `/tmp/p62ru02.txt` contains `RU-63-0` and contains `#65`
    - Phase-wide counts prove no other section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `32` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `3`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - Every `pass` line is a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Every `fail` line names the findings it recorded: `awk '/^- D[1-8] .* — fail — /{if ($0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print "UNNAMED: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed, none from the reserved namespace: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No blank dedup: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in a vendored binary owned by Phase 64: `grep -cE '^location:[[:space:]]+.*tools/formatter/.*\.jar' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-02 /,/^## Phase 62 Close-Out/" "$f" > /tmp/p62ru02.txt; test "$(grep -cE "^- D[1237] .* — (pass|fail) — " /tmp/p62ru02.txt)" = 4; for d in 1 2 3 7; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru02.txt)" = 1; done; for x in spawn RU-64-03 RU-63-0 "#65"; do grep -q "$x" /tmp/p62ru02.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 32; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 3; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(awk "/^- D[1-8] .* — fail — /{if (\$0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print \"UNNAMED\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+.*tools/formatter/.*\.jar" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_02_REPRO_OK'</automated>
  </verify>
  <done>`RU-62-02`'s D1, D2, D3 and D7 cells each carry `pass` or `fail` plus a written line naming the
  concrete checks applied — the `java -jar` spawn path, `jarPath` construction and `fs.promises` file
  handling for D1; exit-code, stream, handle-closure and boundary behavior for D2; JVM-per-request and
  whole-document-rewrite cost for D3; and a counterpart-exists-or-not comparison across all four editor
  features for D7, with #65 checked by number and IntelliJ-side observations routed to `RU-63-01` or
  `RU-63-02`. The vendored JAR's provenance is referred to `RU-64-03` rather than recorded here.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-62-02 at evidence tier `trace` — D4, D5, D8</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`## RU-62-02`'s D1, D2, D3 and D7 cells already carry verdicts — Task 1 committed, and the
  phase-wide verdict count is exactly 32. Halt otherwise.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` §`## RU-62-02` — the section this task completes
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited`), §3c,
      §"Finding Record Template", §"Frozen Open-Issue Snapshot" — the **#65** row
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-02
    - **Review targets:** the same 4 files as Task 1
    - **Test surface:** whatever `ls bbj-vscode/test/` returns, plus
      `grep -rl 'formatter\|line-numbering\|tokenized\|decompile' bbj-vscode/test/ 2>/dev/null` —
      establish by enumeration which of the four modules has any direct test coverage;
      `tokenized-bbj.ts` is pure, dependency-free byte logic, so its coverage state is a direct D5
      question
    - `CLAUDE.md` §Build & Test Commands and §Architecture — D8 targets checkable against the code
      just read; `tokenized-bbj.ts`'s own header docstring, which documents the `<<bbj>>` magic bytes
      with a worked `hexdump` example, is a directly checkable D8 claim
  </read_first>
  <action>
Record the D4, D5 and D8 cell lines of `## RU-62-02` and append any resulting records. Touch no other
section. Enumerate the dimensions explicitly so none is absorbed by another (D-11): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others.

*D4 Maintainability & code smells* (tier `trace`). Assess against duplication, god functions, dead
code, tangled coupling, inconsistent patterns and missing abstractions. The concrete checks: whether
the four modules agree on one convention for surfacing an error to the user and for logging, or use
several — `document-formatter.ts` imports the shared `logger`, so check whether the other three do
anything comparable; whether any of the four exports something no caller uses, confirmed by grep
rather than asserted; whether `decompile-io.ts`'s two responsibilities (magic-prefix read and size
`stat`) belong in one module and whether both have callers; whether the `spawn`-plus-stream-handling
shape in `document-formatter.ts` duplicates the `exec` shape `RU-62-01` already recorded, and if so
note the cross-unit relationship rather than restating that unit's finding; and whether
`line-numbering.ts` at 49 lines and `tokenized-bbj.ts` at 39 lines each earn their own module or are
fragments of a larger concern.

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). The concrete checks:
which of the four modules has a dedicated test, established by enumerating `bbj-vscode/test/` rather
than assumed; whether `tokenized-bbj.ts` — pure, `vscode`-free byte logic with an exported magic
constant and a documented length, the single most trivially testable module in the phase — has any
test at all, and whether the short-file and zero-byte boundaries Task 1 checked are asserted anywhere;
whether `line-numbering.ts`'s boundary behavior is asserted; whether the formatter has any test that
does not require a JVM; and whether each D1 or D2 finding recorded in Task 1 has a test that would
have caught it. Record gaps as `P62-D5-{seq}` findings naming the untested branch or module by
`file:line`; every `dedup:` is explicit and names a `DEBT-*` requirement where the gap restates one.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
or docs-site claims contradicted by code. The concrete checks: `tokenized-bbj.ts`'s header docstring
— which asserts the exact magic bytes, a worked `hexdump` output and a little-endian word-swap
explanation — verified byte for byte against the exported constant and against the detection logic;
every JSDoc block in the other three modules against what the code does, in particular any comment
claiming a path is validated or a handle is released, checked directly against Task 1's D1 and D2
traces; and `CLAUDE.md`'s §Build & Test Commands and §Architecture claims insofar as they touch these
four features. Record a divergence as a finding with the exact `file:line` in `location:`.

For each cell write `pass` plus a written line naming the concrete checks applied, or `fail` plus the
IDs of the findings recorded. Continue finding-ID allocation monotonically from the highest sequence
already present for each dimension — this task closes the phase's ID namespace. Any claim that does
not clear its tier goes under `### Not-reproducible dispositions` with its reason.
  </action>
  <acceptance_criteria>
    - All 7 live cells now carry a verdict: `awk '/^## RU-62-02 /,/^## Phase 62 Close-Out/' .planning/reviews/62-COVERAGE.md > /tmp/p62ru02.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/p62ru02.txt` prints `7`
    - Each of the three dimensions this task owns is individually recorded: `grep -cE '^- D4 .* — (pass|fail) — ' /tmp/p62ru02.txt`, `grep -cE '^- D5 .* — (pass|fail) — ' /tmp/p62ru02.txt` and `grep -cE '^- D8 .* — (pass|fail) — ' /tmp/p62ru02.txt` each print `1`
    - The `n/a` cell is untouched: `grep -cE '^- D6 .* — n/a — ' /tmp/p62ru02.txt` prints `1`
    - Every file in the unit is named inside its own section: `for f in document-formatter.ts line-numbering.ts tokenized-bbj.ts decompile-io.ts; do grep -q "$f" /tmp/p62ru02.txt || echo "MISSING $f"; done` prints nothing
    - **The phase's cell coverage is now complete:** `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `35` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `0`
    - The grid is unchanged in size and the `n/a` carry-forwards are intact: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40` and `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`; 35 + 0 + 5 = 40
    - All three sub-blocks are present for this unit: `/tmp/p62ru02.txt` contains `### Findings`, `### Not-reproducible dispositions` and `### Cross-unit referrals`
    - Every `pass` line remains a written line: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT"}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-02 /,/^## Phase 62 Close-Out/" "$f" > /tmp/p62ru02.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p62ru02.txt)" = 7; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru02.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p62ru02.txt)" = 1; for x in document-formatter.ts line-numbering.ts tokenized-bbj.ts decompile-io.ts "### Findings" "### Not-reproducible dispositions" "### Cross-unit referrals"; do grep -q "$x" /tmp/p62ru02.txt; done; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 35; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 0; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_02_COMPLETE'</automated>
  </verify>
  <done>`RU-62-02` is complete under the stopping rule and the phase's 35 `applies` cells all carry a
  verdict with zero placeholders remaining: all 7 live cells carry a written check line, all 4 files
  including the 39-line `tokenized-bbj.ts` are named inside the section, that module's `hexdump`-anchored
  header docstring is verified byte for byte against its exported constant, and every unpromoted claim is
  visible under `### Not-reproducible dispositions`.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase — re-derive both D-14 gates, account for every record and referral, and answer ROADMAP's four criteria</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>All 35 `applies` cells carry a verdict and zero cell lines end with the placeholder —
  `grep -cE '^- D[1-8] .* — (pass|fail) — '` prints 35 and `grep -cE '^- D[1-8] .* — pending$'` prints 0.
  Halt and report otherwise; the close-out cannot certify coverage that is not yet recorded.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` — the whole file, read once end to end: the header's recorded
      sweep SHA, the grid, the `## D-14 Cell-Total Gate` block written by plan `62-01`, the
      exclusion-reason block, and all five unit sections including every `### Findings` record, every
      `### Not-reproducible dispositions` entry and every `### Cross-unit referrals` entry. This is the
      only task in the phase licensed to read the file in full
    - `.planning/reviews/61-COVERAGE.md` lines 3261-3329 — `## Phase 61 Close-Out`, the pattern this
      close-out reproduces: the tree-derived file enumeration plus its per-basename presence loop, the
      finding-count tables, the referral accounting, the success-criteria answers, and the ISSUE-01 and
      INVENTORY-immutability confirmations. Do **not** read the rest of that file
    - `.planning/ROADMAP.md` §"Phase 62: Extension Host & Webview Composer Review" — the four success
      criteria, verbatim, each of which the close-out answers with evidence
    - `.planning/reviews/INVENTORY.md` §"Applicability Grid" (the five `RU-62-0[1-5]` rows),
      §"Recording protocol (D-09)", §"Grid totals", §"Routing table (D-06)"
    - `.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md` D-13 and D-14
  </read_first>
  <action>
Write `## Phase 62 Close-Out`, replacing plan `62-01`'s stub. Touch no other region of the file. Both
gates are **re-run at execution time**; neither number is copied from a plan or from the file header,
and a disagreement between a re-derivation and a stated total is surfaced as a defect rather than
adopted (D-14).

**Formatting constraint.** No line in this section may begin with `- D` followed by a digit — that
prefix is the cell-line shape the coverage gate counts, and a close-out bullet in that shape would
corrupt the count. Use markdown tables for the per-dimension breakdowns.

**A. Gate 2 — the 22-file tree enumeration (D-14.2).** Enumerate the in-scope files **from the tree,
not from a list typed into any plan**, and record the command and its literal output:

    ls bbj-vscode/src/*.ts bbj-vscode/src/Commands/* \
       bbj-vscode/syntaxes/bbj.tmLanguage.json \
       bbj-vscode/bbj-language-configuration.json \
       bbj-vscode/bbx-language-configuration.json | wc -l

Then confirm every basename appears somewhere in this coverage file, with a loop over the same `ls`
piped through `xargs -n1 basename`, printing the name of anything missing. Record the command and its
literal output. List the 22 basenames inline so a reader can audit the claim without re-running it.
If the count disagrees with 22, or if any basename is missing, **say so plainly and state what the
disagreement means** — do not adjust the number to match.

**B. Gate 1 — the closing cell-total re-derivation (D-14.1), three sources.** Re-run the identical awk
pass over INVENTORY (not restated from plan `62-01`'s block) and record command and output:

    awk '/^\| `RU-62-0[1-5]` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md

Then count independently what this coverage file itself now contains — recorded verdicts, verbatim
`n/a` carry-forwards, total cell lines, and remaining placeholders — recording each command and its
output. State the verdict explicitly: the stated totals (35/5/40), the closing INVENTORY
re-derivation, and the coverage file's own content must all three agree, with the placeholder count at
zero. If they do not, that disagreement is itself a defect to surface, named as such.

**C. Finding accounting.** A count by primary dimension and a count by disposition, each as a markdown
table, each produced by a recorded command over this file (for example
`grep -oE '^dimension:[[:space:]]+D[1-8]' | sort | uniq -c` and the equivalent over
`^disposition:`). Both must sum to `grep -c '^id:'`; state that identity. Also record: the count of
`### Not-reproducible dispositions` entries across the phase and which unit each belongs to, so RVW-06's
drop-vs-disposition rule is visibly honoured; and the count of findings whose `dedup:` resolved to a
frozen issue rather than `none`, naming the issue numbers that came up — #381, #475, #385, #231, #65,
#485 and #486 are the ones the plans checked by name.

**D. Cross-unit referral accounting.** List every referral written anywhere in this phase, in two
groups. Group 1: referrals addressed to another **Phase 62** unit — for each, confirm from the target
unit's own `### Cells` or `### Findings` text that it was promoted, dismissed with evidence, or
dispositioned in place, and say which. Group 2: referrals addressed to **`RU-63-01`**, **`RU-63-02`**
or **`RU-63-04`** — list each with its target unit and its subject, and state that they are
**outstanding by design**: they are durable records Phase 63 inherits and re-triages whether or not it
has already swept the named unit, so an unanswered Phase 63 referral is not a Phase 62 defect (D-06).
State the total for each group.

**E. D-13 scope-fidelity note.** State plainly, in prose: that `RU-62-05` (the TextMate grammar and the
two language-configuration JSONs) and `bbj-vscode/src/Commands/Commands.cjs` are in INVENTORY's grid
but are **not** named in ROADMAP's Phase 62 success criteria, that both were swept in full anyway
because the grid is the contract and the criteria are a subset of it, and that this is recorded here so
a reader can see the phase covered more than its criteria asked and why. Carry forward, in the same
place, any enumeration discrepancy plan `62-04` surfaced between INVENTORY's `RU-62-05` per-file table
and INVENTORY's Surface Accounting for `syntaxes/`, restating its evidence in one sentence and noting
that INVENTORY was not edited to resolve it. Also state as a fact that INVENTORY's routing table has
no Phase 62 rows, so no pre-identified finding was inherited and none was missed.

**F. ROADMAP success criteria.** Answer each of the four Phase 62 criteria in turn, quoting it and
marking it Met or not, with the evidence that establishes it:
  1. The `extension.ts` / `CompilerOptions.ts` / formatter / line-numbering / tokenized-BBj /
     decompile-io surfaces each have a recorded pass/fail against D1-D8 — evidenced by section A's
     file enumeration and section B's gate, noting that D6 is `n/a` by INVENTORY's own grid rather
     than unrecorded.
  2. All 11 webview composer files plus `setopts-catalog.ts` have a recorded pass/fail against D1-D8,
     with the duplication explicitly called out under D4 — evidenced by `RU-62-04`'s generator-layer
     D4 finding (cited by ID) and `RU-62-03`'s logic/UI-layer D4 finding (cited by ID), each produced
     by a programmatic diff whose method and output are in its cell, against the 3-file `-composer.ts`
     baseline.
  3. Every recorded finding carries `file:line`, dimension and a verified failure scenario — evidenced
     by the equal field counts across all records and by the evidence tier each clears.
  4. Every recorded finding has been checked against the 15 open GitHub issues — evidenced by a zero
     count of blank `dedup:` fields and by the issue numbers section C names.

**G. Closing confirmations.** State that Phase 62 filed no GitHub issue, since ISSUE-01 is a hard gate
owned by Phase 69 — no issue was opened, commented on or filed by any Phase 62 plan. State that
`.planning/reviews/INVENTORY.md` was not edited by this phase, confirmed by
`git status --porcelain .planning/reviews/INVENTORY.md` returning nothing at every commit point across
all five plans, and that no file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` was modified.
State the spec-less edge-probe accounting once for the phase: 8 edges surfaced, 8 authored into
`must_haves`, 0 dropped, 1 of them a backstop. Finally, name what each downstream phase inherits from
this file: Phase 65 (the `### SEC-01/SEC-02 Surface Handoff` and every `P62-D1-*` record), Phase 63
(the outstanding referrals), Phase 66 (any finding whose `dedup:` names a `DEBT-*` requirement),
Phase 67 (`classification: easy|major`), Phase 68 (DOC-03 concatenation), Phase 69 (issue drafting).
  </action>
  <acceptance_criteria>
    - The close-out exists and is filled, not a stub: `awk '/^## Phase 62 Close-Out/,0' .planning/reviews/62-COVERAGE.md > /tmp/p62close.txt; wc -l < /tmp/p62close.txt` is at least 40
    - Gate 2 is re-derived from the tree and agrees: `ls bbj-vscode/src/*.ts bbj-vscode/src/Commands/* bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json | wc -l` prints `22`, and the close-out contains both that command's shape and the number `22`
    - Every one of the 22 basenames appears somewhere in the coverage file: `for b in $(ls bbj-vscode/src/*.ts bbj-vscode/src/Commands/* bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json | xargs -n1 basename); do grep -q "$b" .planning/reviews/62-COVERAGE.md || echo "MISSING $b"; done` prints nothing
    - Gate 1 is re-derived and agrees: `awk '/^\| \`RU-62-0[1-5]\` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md` prints `35 5 40`, and the close-out records that output
    - The coverage file's own content agrees with both gates: `grep -cE '^- D[1-8] .* — (pass|fail) — '` prints `35`, `grep -cE '^- D[1-8] .* — n/a — '` prints `5`, `grep -cE '^- D[1-8] '` prints `40`, and `grep -cE '^- D[1-8] .* — pending$'` prints `0`
    - No close-out line corrupts the cell-line count: `grep -cE '^- D[1-8] ' /tmp/p62close.txt` prints `0`
    - The finding tables sum correctly: the close-out contains the totals from `grep -oE '^dimension:[[:space:]]+D[1-8]' .planning/reviews/62-COVERAGE.md | wc -l` and `grep -c '^id:' .planning/reviews/62-COVERAGE.md`, and those two numbers are equal
    - The D-13 scope-fidelity note is present and names both surfaces: `/tmp/p62close.txt` contains `RU-62-05` and contains `Commands.cjs` and contains the string `success criteria`
    - The referral accounting names the Phase 63 targets: `/tmp/p62close.txt` contains at least one of `RU-63-01`, `RU-63-02`, `RU-63-04`, and contains the string `by design`
    - All four ROADMAP criteria are answered: `/tmp/p62close.txt` contains four occurrences of the string `Met` or an explicit not-met statement for each, and cites a `P62-D4-` finding ID for criterion 2
    - The closing confirmations are present: `/tmp/p62close.txt` contains `ISSUE-01` and contains `INVENTORY.md`
    - Finding IDs remain unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing, `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`, and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in an excluded surface: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## Phase 62 Close-Out/,0" "$f" > /tmp/p62close.txt; test "$(wc -l < /tmp/p62close.txt)" -ge 40; test "$(ls bbj-vscode/src/*.ts bbj-vscode/src/Commands/* bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json | wc -l)" = 22; for b in $(ls bbj-vscode/src/*.ts bbj-vscode/src/Commands/* bbj-vscode/syntaxes/bbj.tmLanguage.json bbj-vscode/bbj-language-configuration.json bbj-vscode/bbx-language-configuration.json | xargs -n1 basename); do grep -q "$b" "$f" || { echo "MISSING $b"; exit 1; }; done; test "$(awk "/^\\| .RU-62-0[1-5]. \\|/ {a+=gsub(/applies/,\"applies\"); n+=gsub(/n\\/a/,\"n\\/a\")} END{print a, n, a+n}" .planning/reviews/INVENTORY.md)" = "35 5 40"; grep -q "35 5 40" /tmp/p62close.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 35; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 0; test "$(grep -cE "^- D[1-8] " /tmp/p62close.txt)" = 0; test "$(grep -oE "^dimension:[[:space:]]+D[1-8]" "$f" | wc -l)" = "$(grep -c "^id:" "$f")"; for x in RU-62-05 Commands.cjs "success criteria" "by design" ISSUE-01 INVENTORY.md; do grep -q "$x" /tmp/p62close.txt; done; grep -qE "RU-63-0[124]" /tmp/p62close.txt; grep -qE "P62-D4-[0-9]{3}" /tmp/p62close.txt; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo PHASE_62_CLOSED'</automated>
  </verify>
  <done>`## Phase 62 Close-Out` records both D-14 gates as live re-derivations with their commands and
  literal outputs — the 22-file tree enumeration with its per-basename presence check, and the
  three-source cell-total agreement (stated totals, INVENTORY re-derivation, this file's own content)
  with zero placeholders remaining. Findings are counted by dimension and by disposition and both sum to
  the `id:` count; every not-reproducible disposition and every cross-unit referral is accounted for,
  with Phase 63-addressed referrals listed as outstanding by design. The D-13 discrepancy — `RU-62-05`
  and `Commands.cjs` swept despite ROADMAP's criteria omitting them, plus `62-04`'s `syntaxes/`
  enumeration observation — is stated explicitly. All four ROADMAP criteria are answered with evidence.
  No GitHub issue was filed and INVENTORY.md is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes **one markdown file in a public repository and modifies no code**. The realistic
threats are disclosure and record-integrity threats. Rows marked *subject-matter* are boundaries this
plan **documents**.

| Boundary | Description |
|----------|-------------|
| document/workspace paths and `PATH` resolution → `spawn('java', …)` in `document-formatter.ts` (*subject-matter*) | The formatter builds a `-jar` argument from `__dirname` and invokes `java`; how the binary and the JAR path resolve, and what reaches the argument array, is the SEC-05-adjacent question for this unit. |
| arbitrary file paths → `fs.promises.open`/`stat` in `decompile-io.ts` (*subject-matter*) | Raw filesystem handles are opened on a supplied path; symlinks, directories and unreadable files are the edge set, and an unclosed handle is a leak. |
| `62-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public. |
| the close-out's coverage certification → Phase 68's DOC-03 | A close-out that restates numbers instead of re-deriving them would let a real coverage gap ship as a certified claim. |
| the vendored `tools/formatter/*.jar` binaries → this phase's finding namespace | Their provenance and pinning belong to `RU-64-03` in Phase 64; a finding located there would double-count. |
| `bbj-intellij/` → this phase's finding namespace | Read as D7 comparison material only. |
| shared `62-COVERAGE.md` → the other four plans | One file, five writers; this plan is the last and also writes the close-out. |

## STRIDE Threat Register

ASVS level 1; `high` blocks.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-P05-S1 | Elevation of Privilege | `cp.spawn('java', formatFlags)` with a `__dirname`-derived JAR path in `document-formatter.ts` | high | transfer | Task 1's D1 check traces how `java` and `jarPath` resolve, whether an argument array or a shell string is used, and what document/workspace/setting values reach the arguments; recorded as `P62-D1-*` where evidence clears tier `repro`. Cross-cutting SEC-05 synthesis transfers to Phase 65; the JAR's own provenance to `RU-64-03`. |
| T-62-P05-S2 | Information Disclosure | `fs.promises.open`/`stat` on a supplied path in `decompile-io.ts` | medium | transfer | Task 1's D1 check asks whether the path is workspace-constrained and what happens for a symlink, a directory, a device node or an unreadable file; Task 1's D2 check asks whether the handle is closed on every path including throwing ones. |
| T-62-P05-S3 | Tampering | magic-byte classification in `tokenized-bbj.ts` | medium | transfer | Task 1's D1 and D2 checks cover a truncated read, a zero-byte file and a coincidental prefix match, so a binary-treated-as-text or text-treated-as-binary misclassification is evidenced rather than assumed. |
| T-62-P05-01 | Repudiation | a close-out that restates the gates instead of re-deriving them | high | mitigate | D-14 requires live re-derivation of both gates at execution time; the verify gate independently re-runs the `ls … \| wc -l` file gate, the per-basename presence loop and the INVENTORY awk pass, and asserts the coverage file's own content agrees with all three. A disagreement is surfaced as a defect, never adopted. |
| T-62-P05-02 | Tampering | close-out prose corrupting the cell-line count the gate depends on | medium | mitigate | Explicit formatting prohibition against any close-out line beginning `- D<digit>`, plus an acceptance criterion asserting the close-out region contains zero such lines while the file total stays at 40. |
| T-62-P05-03 | Repudiation | recording Phase 63-addressed referrals as unresolved defects, or dropping them | medium | mitigate | D-06 prohibition plus an acceptance criterion requiring the close-out to list them by target unit and mark them outstanding by design, so they are neither silently dropped nor miscounted as Phase 62 failures. |
| T-62-P05-04 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-09's two-tier rule as approved at the `62-01` checkpoint; enforced as a `must_haves` prohibition. The close-out summarizes severity distribution without restating any redacted detail. |
| T-62-P05-05 | Information Disclosure | an absolute developer-machine path pasted into the close-out with a command output | medium | mitigate | Prohibition: record command outputs, not machine-specific working directories; every citation is a repository-relative `path:line` anchor. |
| T-62-P05-06 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity or any D1 primary dimension; the close-out's disposition table makes the split auditable in one place. |
| T-62-P05-07 | Tampering | five plans writing one `62-COVERAGE.md` | medium | mitigate | Wave 5 behind `depends_on: [62-04]` makes ordering structural (D-04); Tasks 1 and 2 assert exact global counts (32/3, then 35/0) and Task 3 asserts the terminal invariant 35 + 0 + 5 = 40. |
| T-62-P05-08 | Tampering | a finding located in a Phase 64-owned vendored binary | low | mitigate | Prohibition plus an anchored acceptance grep: no `location:` may resolve to a `tools/formatter/*.jar` path; the boundary is noted under `### Cross-unit referrals` addressed to `RU-64-03` instead. |
| T-62-P05-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk`, `ls`, `xargs` and `wc` only. |
</threat_model>

<verification>
- `## RU-62-02`'s 7 live cells each carry `pass` or `fail` plus a written check line of at least
  120 bytes; the D6 cell still carries INVENTORY's R-D6-CENTRAL text verbatim; all 4 files are named
  inside the section.
- The phase's coverage is terminal: 35 recorded verdicts, 0 placeholders, 5 verbatim `n/a`
  carry-forwards, 40 cell lines — and the close-out proves each of those numbers from an independent
  count rather than restating it.
- Gate 2 re-derives `22` from the tree and every one of the 22 basenames is confirmed present in the
  coverage file.
- Gate 1 re-derives `35 5 40` from INVENTORY and agrees with both the stated totals and the coverage
  file's own content.
- The close-out contains no line beginning `- D<digit>`, so the cell-line gate is uncorrupted.
- Finding counts by dimension and by disposition both sum to the `id:` count.
- Every referral is accounted for; Phase 63-addressed ones are marked outstanding by design.
- The D-13 discrepancy is stated for both `RU-62-05` and `Commands.cjs`, together with `62-04`'s
  `syntaxes/` enumeration observation.
- All four ROADMAP Phase 62 criteria are answered with evidence, criterion 2 citing a `P62-D4-NNN` ID.
- No GitHub issue was filed; `git status --porcelain bbj-vscode bbj-intellij java-interop
  .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-02 and RVW-03 are complete: all 5 units, 22 files and 5,889 LOC have a recorded pass/fail
  against every dimension INVENTORY's grid marks `applies` for them, with the 5 `n/a` cells carried
  forward verbatim.
- Both D-14 gates close as live re-derivations that agree with the coverage file's own content, and
  any disagreement that did arise is surfaced in the close-out rather than adopted.
- Phase 68 can assemble DOC-03 by concatenating this file against INVENTORY's grid with no
  re-derivation of scope.
- Phase 63, Phase 65, Phase 66, Phase 67 and Phase 69 each have a named, addressed inheritance from
  this file.
- ROADMAP Phase 62's four success criteria are each answered with the evidence that establishes them.
</success_criteria>

<output>
Create `.planning/phases/62-extension-host-webview-composer-review/62-05-SUMMARY.md` when done.
</output>
