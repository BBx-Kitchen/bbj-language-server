---
phase: 62-extension-host-webview-composer-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/62-COVERAGE.md
autonomous: false
requirements: [RVW-02, RVW-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ~~ goal-backward truths (phase goal: 5 units / 22 files / 5,889 LOC swept across the 7 live dimensions)
    - "`.planning/reviews/62-COVERAGE.md` exists and carries the complete Phase 62 slice of INVENTORY.md's applicability grid as a skeleton: 5 unit rows x 8 dimensions = 40 cell lines, of which all 5 `n/a` cells already carry INVENTORY's R-D6-CENTRAL text verbatim and the remaining 35 carry the placeholder (D-03, D-04)"
    - "The tree the sweep reads is stated once, unambiguously: branch `v4.0-stability-and-quality` at a 40-character commit SHA captured by this plan at execution time, so every plan in the file describes the same tree and no SHA was pinned at planning time (D-15)"
    - "The D-14 cell gate is **re-derived from INVENTORY, not restated**: the awk pass over the five `RU-62-0[1-5]` grid rows is run, its command and its literal output are recorded in the file, and a disagreement with the stated totals is surfaced as a defect rather than adopted as a number (D-14.1)"
    - "`RU-62-04` (composer webview HTML generators, 4 files / 1,533 LOC) has all 7 live dimensions recorded — D1, D2, D3, D7 at evidence tier `repro`/repro-equivalent and D4, D5, D8 at tier `trace` — each as `pass` or `fail` plus a written line naming the concrete checks applied against that dimension's own REQUIREMENTS.md wording (D-10, D-11)"
    - "Every one of the 4 `RU-62-04` files — `msgbox-composer-webview.ts`, `addwindow-composer-webview.ts`, `addchildwindow-composer-webview.ts`, `setopts-composer-webview.ts` — is named at least once inside the `## RU-62-04` section, so coverage is file-granular and not merely unit-granular"
    - "`### SEC-01/SEC-02 Surface Handoff` exists under `RU-62-04` and states as facts, each anchored to a `path:line`: which values reach generated HTML and from where; the CSP posture of each of the four webviews as it actually is; which `onDidReceiveMessage` handlers exist and what shape/range validation each performs before acting; and what a webview can reach through the extension host (D-08)"
    - "The generator-layer D4 duplication callout ROADMAP Phase 62 criterion 2 requires is recorded here, produced by a **programmatic structural diff whose method and output are written into the cell**, not by eyeball comparison (D-12)"
    - "Every recorded finding carries `path:line`, a primary dimension, and evidence clearing its tier; a claim that does not clear its tier appears under `### Not-reproducible dispositions` with its reason rather than vanishing (RVW-06, INVENTORY 3b)"
    - "Every recorded finding carries a non-blank `dedup:` checked against the 15 frozen open issues, with #475 and #385 — INVENTORY's two named `RU-62-04` dedup neighbours — checked explicitly (RVW-07, ROADMAP Phase 62 criterion 4)"
    - "A `critical` or `high` D1 finding names the surface, the problem class and the impact, and states that its evidence exists and what it establishes — without a trigger sequence, a payload or a proof-of-concept, because this repository is public and this surface is unfixed (D-09)"

    # ~~ spec-less edge probe (no SPEC.md; 8 applicable edges surfaced, all 8 authored, 0 dropped)
    - "`62-COVERAGE.md` contains exactly one `## RU-62-0N` section per unit — five in total — and no plan's section overwrites or merges into another's"
    - "No `applies` cell line in `62-COVERAGE.md` records a bare verdict: every one of the 35 carries a written check line naming the concrete checks applied, and zero cell lines remain `pending` at phase close"
    - "Finding IDs are monotonic within each `(62, dimension)` pair with no gaps and no reuse, so two findings discovered in the same unit have a deterministic order"
    - "No two Phase 62 plans share a wave; each of `62-02`..`62-05` declares its predecessor in `depends_on`, so the shared `62-COVERAGE.md` is never written concurrently"
    - "Both D4 duplication assessments compare against a 3-file `-composer.ts` baseline (msgbox/addwindow/addchildwindow), never 4 — the SETOPTS asymmetry is stated in the cell, not silently normalized"
    - "`setopts-composer-webview.ts`'s missing `-composer.ts` sibling is recorded as an asymmetric baseline qualifier on `RU-62-04`'s existing D4 `applies` cell — not as a coverage gap and not as a 41st grid row"
    - "The composer duplication is allocated exactly once per layer — generator-layer to `RU-62-04`, logic/UI-layer to `RU-62-03` — with the second cross-referencing the first rather than restating it, so the same duplication is never double-counted"
    - statement: "Each composer webview's behaviour when a message arrives after disposal, or while a second composer instance is open, is assessed under D2 and recorded — as a finding if it clears tier `repro`, otherwise as a `not-reproducible` disposition with its reason"
      verification: backstop

  artifacts:
    - ".planning/reviews/62-COVERAGE.md — created by this plan: header with the execution-time sweep SHA, the Phase 62 applicability grid, the D-14 cell-total gate re-derived from INVENTORY, the single verbatim R-D6-CENTRAL exclusion block, the stopping rule and write contract, 5 stubbed per-unit sections, and a stubbed `## Phase 62 Close-Out`"
    - ".planning/reviews/62-COVERAGE.md §`## RU-62-04 — Composer webview HTML generators` — filled end to end: 8 cell lines, `### SEC-01/SEC-02 Surface Handoff`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals`"

  key_links:
    - "`62-COVERAGE.md` per-unit section shape -> plans 62-02..62-05, which each fill exactly one inherited section (D-03, D-04)"
    - "`### SEC-01/SEC-02 Surface Handoff` + every `P62-D1-*` record -> Phase 65's SEC-01/SEC-02 cross-cutting synthesis (D-07, D-08)"
    - "`### Cross-unit referrals` addressed to `RU-63-04`/`RU-63-02`/`RU-63-01` -> Phase 63, which may already be running, so each referral is a durable record and not a handoff (D-05, D-06)"
    - "`RU-62-04` D4 generator-layer duplication finding -> `RU-62-03`'s logic/UI-layer D4 cell in plan 62-03, which cross-references rather than restates it (D-12)"
    - "`62-COVERAGE.md` -> Phase 68 DOC-03, which concatenates the five `{NN}-COVERAGE.md` files against INVENTORY's grid without re-deriving scope"
    - "recorded `classification: easy|major` -> Phase 67's apply path and Phase 68's EASY-FIXES / MAJOR-REFACTORS split (INVENTORY 3c)"

  prohibitions:
    # descriptor-less: each disposes flagged-unverified. Canon security/compliance items (OWASP, XSS,
    # path traversal, injection taxonomies, GDPR) deliberately DROPPED — in this phase they are the
    # review *subject matter*, not constraints on the plan.
    - "MUST NOT modify any file under `bbj-vscode/`, `bbj-intellij/` or `java-interop/` — Phase 62 records findings; Phase 67 is the only phase that applies fixes and Phase 66 the only one that resolves DEBT items"
    - "MUST NOT record a finding whose `location:` is inside `bbj-intellij/` — that plugin is read as D7 comparison material only, no Phase 62 coverage cell covers it, and an IntelliJ-side defect belongs in `### Cross-unit referrals` addressed to `RU-63-04`, `RU-63-02` or `RU-63-01` (D-05)"
    - "MUST NOT publish a trigger sequence, a payload or a proof-of-concept for a `critical` or `high` D1 finding — this repository is public and the webview HTML-generation surface is unfixed (D-09)"
    - "MUST NOT write a credential, an API token, a session value or an absolute developer-machine path into any record — anchor every citation as a repository-relative `path:line`"
    - "MUST NOT read any file under `.planning/codebase/` — all seven maps are dated 2026-02-01, predate the webview composers entirely, and `CONCERNS.md` lists FIXMEs already resolved in v3.8 (D-16)"
    - "MUST NOT record the cyclic-inheritance hang fixed by `a7e1b53 fix(#494)` as a live finding — it is fixed on this branch and merely unreleased (D-16)"
    - "MUST NOT edit `.planning/reviews/INVENTORY.md` — it is immutable for the v4.0 milestone (Phase 60 D-09)"
    - "MUST NOT write into any `## RU-62-0N` section other than `RU-62-04`, and MUST NOT reword, reorder or re-derive any carried-forward `n/a` text (D-03, D-04)"
    - "MUST NOT add a grid row, a cell line, or a unit section beyond the 40 cells INVENTORY's Phase 62 slice defines — `setopts-composer-webview.ts`'s D4 asymmetry is a qualifier on an existing cell, not a 41st row (D-14)"
    - "MUST NOT normalize the composer baseline to four `-composer.ts` files — SETOPTS has none, the baseline is three, and the asymmetry is stated rather than smoothed (D-12, D-15)"
    - "MUST NOT record a claim as a finding when it does not clear its evidence tier — it belongs under `### Not-reproducible dispositions` with its reason (RVW-06)"
    - "MUST NOT leave a `dedup:` field blank, and MUST NOT record a `pass` cell without a written line naming the concrete checks applied (RVW-07, D-10)"
    - "MUST NOT record a cell verdict for a file the sweep did not open — a unit-level generalization over an unread file is a bare verdict wearing a written line"
    - "MUST NOT build a webview injection harness or any other test fixture — that is deferred infrastructure, and any harness a specific finding demands is a Phase 67 deliverable"
    - "MUST NOT open, comment on or file any GitHub issue — ISSUE-01 is a hard gate owned by Phase 69"
---

<objective>
Create `.planning/reviews/62-COVERAGE.md` — Phase 62's sole deliverable — and prove the whole recording
pipeline end to end on **one** review unit before the other four are swept. This plan is the tracer of
the decomposition D-01 fixes — tracer plus one plan per review unit, five plans total — sequenced in
the INVENTORY risk-rank order D-02 fixes (`RU-62-04` → `RU-62-01` → `RU-62-03` → `RU-62-05` →
`RU-62-02`), and it creates the skeleton the other four fill.

The tracer unit is **`RU-62-04`, the composer webview HTML generators**
(`msgbox-composer-webview.ts` 373, `addwindow-composer-webview.ts` 408,
`addchildwindow-composer-webview.ts` 431, `setopts-composer-webview.ts` 321 — 4 files, 1,533 LOC).
It is INVENTORY risk rank 1 of 5 and it front-loads every hard part of this phase at once: the entire
SEC-01/SEC-02 handoff surface (D-07, D-08), the public-repo redaction rule on an unfixed surface
(D-09), the generator-layer D4 duplication callout ROADMAP criterion 2 requires (D-12), the
`setopts-composer-webview.ts` asymmetric-baseline qualifier (D-15), and the phase's first live D7
cell — a dimension with no worked precedent anywhere in `61-COVERAGE.md`. If the recording shape is
wrong, it is wrong here first, at the only point where correcting it is cheap.

Purpose: four downstream plans and Phase 68's DOC-03 concatenation inherit this file's section shape,
and Phase 65 inherits its `### SEC-01/SEC-02 Surface Handoff`.

Output: `.planning/reviews/62-COVERAGE.md` with the grid, the re-derived D-14 cell gate, the single
verbatim `n/a` carry-forward, 5 stubbed unit sections, a stubbed close-out, and `RU-62-04` filled in
full. No source file changes.
</objective>

<execution_context>
@$HOME/.claude-shared/dot-claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude-shared/dot-claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/reviews/INVENTORY.md
@.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md
@.planning/phases/62-extension-host-webview-composer-review/62-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 62 creates **no source symbol and modifies no source file**. Everything below is a planning
artifact; drift verification must treat it as new.

**New file**

- `.planning/reviews/62-COVERAGE.md` — created by this plan (skeleton + `RU-62-04`), appended by plans
  `62-02`..`62-05`, one unit section each. This is the phase's only deliverable.

**Section anchors created inside `62-COVERAGE.md`** (created by this plan, filled by the owning plan)

`## Applicability Grid — Phase 62 slice`, `## D-14 Cell-Total Gate`,
`## Stopping Rule & Write Contract`, `## Exclusion reasons carried forward`, then
`## RU-62-04 …`, `## RU-62-01 …`, `## RU-62-03 …`, `## RU-62-05 …`, `## RU-62-02 …` — each with
`### Cells`, `### Findings`, `### Not-reproducible dispositions`, `### Cross-unit referrals` — and
finally `## Phase 62 Close-Out`. `RU-62-04` additionally carries
`### SEC-01/SEC-02 Surface Handoff`. Downstream phases consume these anchors by name.

**Finding-ID namespace minted by this phase.** `P62-D{dimension}-{seq}`, dimension 1-8, `seq`
zero-padded to three digits, allocated monotonically in discovery order within each `(62, dimension)`
pair **across the whole phase, not per plan** — plan `62-0N` continues from the highest sequence
already present for that dimension. This plan opens the phase and therefore allocates from `001` in
each dimension it uses; INVENTORY says explicitly that Phase 62's real first D1 finding is allocated
against `RU-62-04` as `P62-D1-001`. The reserved `P00-*` namespace is INVENTORY's template
illustration and is never allocated.

**Plan → review-unit map** (plan number and unit number deliberately do not match; D-02 orders by
INVENTORY risk rank)

| Plan | Wave | Unit | Subject | Files / LOC |
|---|---|---|---|---|
| 62-01 | 1 | `RU-62-04` | Composer webview HTML generators | 4 / 1,533 |
| 62-02 | 2 | `RU-62-01` | Extension host & commands | 3 / 1,805 |
| 62-03 | 3 | `RU-62-03` | Composer logic & UI layer | 8 / 2,027 |
| 62-04 | 4 | `RU-62-05` | TextMate grammar & language configuration | 3 / 256 |
| 62-05 | 5 | `RU-62-02` | Editor feature modules | 4 / 268 |
| | | | **Total** | **22 / 5,889** |

**Inherited routing.** INVENTORY's routing table (D-06) has **no Phase 62 rows** — all six routed
items belong to Phase 61 (five) and Phase 63 (one). The absence is deliberate; do not go looking for
pre-identified Phase 62 findings.

## Spec-less Edge-Probe Ledger (phase-level, 8 rows, 0 dropped)

No SPEC.md exists for this phase, so neither `## Edge Coverage` nor `## Prohibitions` was supplied.
The deterministic probe over RVW-02 and RVW-03 returned 8 applicable edges, all `unresolved`; all 8
were resolved and authored, none auto-dismissed.

| # | Req | Category | Authored into `must_haves.truths` of | Verification |
|---|---|---|---|---|
| 1 | RVW-02 | adjacency | all 5 plans | explicit |
| 2 | RVW-02 | empty | all 5 plans | explicit |
| 3 | RVW-02 | ordering | all 5 plans | explicit |
| 4 | RVW-02 | concurrency | all 5 plans | explicit |
| 5 | RVW-03 | adjacency | 62-01 (this plan) and 62-03 | explicit |
| 6 | RVW-03 | empty | 62-01 (this plan) | explicit |
| 7 | RVW-03 | ordering | 62-01 (this plan) and 62-03 | explicit |
| 8 | RVW-03 | concurrency | 62-01 (this plan), as a flat-scalar `verification: backstop` marker | **backstop** |

Accounting: 8 surfaced = 8 authored = 0 dropped. Row 8 is a backstop: a verifier that cannot confirm
it with explicit evidence abstains to `human_needed` rather than passing it silently.

<!-- The two literals below are section headings the executor must create verbatim in the coverage
     file, and they are also counted by this plan's acceptance greps. They are exempted from the
     comment-text-discipline gate because they name real output structure, not a banned construct. -->
<!-- planner-discipline-allow: ### Findings -->
<!-- planner-discipline-allow: ### Cross-unit referrals -->

<tasks>

<task type="tracer">
  <name>Task 1: Create 62-COVERAGE.md and sweep RU-62-04 at evidence tier `repro` — D1, D2, D3, D7 — including the SEC-01/SEC-02 Surface Handoff</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>The working tree is on branch `v4.0-stability-and-quality` — `git rev-parse --abbrev-ref HEAD`
  prints exactly that — and `.planning/reviews/INVENTORY.md` exists. Halt and report if either fails; the
  sweep target and the finding standard both depend on it (D-15, Phase 60 D-09).</precondition>
  <read_first>
    - `.planning/reviews/INVENTORY.md` — the immutable contract. Read §"Frozen Open-Issue Snapshot"
      (the 15-issue dedup list; #475 and #385 are this unit's named neighbours), §"Finding Standard"
      3a-3d (note D7 is tier `inherited`, resolving to repro-equivalent — "the concrete divergent
      behaviour in both IDEs"), §"Finding Record Template" (the exact 13 fields, in order),
      §"Review Units" → `### RU-62-04` in full including its file-exception row and its
      `P00-D1-001` worked record, §"Phase 62 review units … — remainder" (the other four units'
      file lists and LOC, needed for the stubs), §"Applicability Grid" rows `RU-62-01`..`RU-62-05`,
      §"Exclusion reasons" → **R-D6-CENTRAL** only, §"Recording protocol (D-09)",
      §"Routing table (D-06)" (confirm: no Phase 62 rows)
    - `.planning/REQUIREMENTS.md` lines 14-23 — the D1-D8 table. Every `pass` line is phrased against
      that dimension's own "what counts as a finding" wording. Also RVW-02, RVW-03, RVW-06, RVW-07,
      and SEC-01/SEC-02 (Phase 65's, bounded here by D-07)
    - `.planning/ROADMAP.md` §"Phase 62: Extension Host & Webview Composer Review" — the four success
      criteria, and criterion 2's explicit D4 duplication requirement
    - `.planning/phases/62-extension-host-webview-composer-review/62-CONTEXT.md` — D-01..D-16 are binding
    - `.planning/phases/62-extension-host-webview-composer-review/62-PATTERNS.md` — the literal shape
      to copy, with line-anchored excerpts
    - `.planning/reviews/61-COVERAGE.md` — the frozen precedent (D-03). Read lines 1-19 (header,
      stopping rule, write contract, placeholder), lines 44-56 (the D-17 gate re-derivation pattern),
      lines 80-100 (verbatim exclusion-reason presentation plus identity check), and lines 102-200
      (`## RU-61-06` — the approved per-unit rendering: `### Cells` line format,
      `### SEC-06 Trust Boundary` as the structural mirror for this unit's handoff, and the first two
      13-field finding records). Do **not** read the remaining ~3,100 lines
    - **Review targets, read in full:** `bbj-vscode/src/msgbox-composer-webview.ts` (373),
      `bbj-vscode/src/addwindow-composer-webview.ts` (408),
      `bbj-vscode/src/addchildwindow-composer-webview.ts` (431),
      `bbj-vscode/src/setopts-composer-webview.ts` (321)
    - `bbj-vscode/package.json` — the `contributes` surface: `commands`, `languages` (including the
      `bbx-config` language ID), `grammars`, and the confirmed absence of any `customEditors`
      contribution, which is what makes these four files the *complete* HTML-generation surface
    - **Reference only for D7, no finding may be located here (D-05):**
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/ComposerModels.java` (245) and
      `bbj-intellij/src/main/java/com/basis/bbj/intellij/composer/MsgboxComposerDialog.java` (273) —
      read these two in full as the BBj-codegen comparison side; for
      `AddWindowComposerDialog.java`, `AddChildWindowComposerDialog.java` and `ComposerLauncher.java`
      grep for the emitted-code construction only rather than reading them in full
  </read_first>
  <action>
Create `.planning/reviews/62-COVERAGE.md`. Write it in this order. Copy `61-COVERAGE.md`'s frozen
shape unchanged (D-03) — Phase 62 adds exactly two shape elements, both defined below, and spends no
new format checkpoint on a question Phase 61 already answered.

**A. Header.** Title it as Phase 62's coverage record for the VS Code extension host, the webview
composer subsystems, the TextMate grammar and the two language-configuration JSONs (RVW-02, RVW-03).
Record, as four labelled lines: the swept tree — branch `v4.0-stability-and-quality` at the full
40-character SHA obtained by running `git rev-parse HEAD` **now, at execution time**, stated once for
the whole phase and never re-anchored per plan (D-15 — no SHA is pinned in any plan text because HEAD
advances with every planning commit); the governing standard, `.planning/reviews/INVENTORY.md`, named
as immutable and not edited by this phase; the dedup source, INVENTORY's frozen 15-issue snapshot,
with the note that Phase 69 re-queries live; and the slice size — 5 unit rows x 8 dimensions = 40
cells, 35 `applies` and 5 `n/a`. State in one sentence that the recording shape is inherited unchanged
from `61-COVERAGE.md` (D-03) rather than re-derived, and name the two Phase 62 additions:
`### SEC-01/SEC-02 Surface Handoff` under `RU-62-04`, and live D7 cells whose IntelliJ-side
observations route through `### Cross-unit referrals`.

**B. `## Applicability Grid — Phase 62 slice`.** The file's layout is grid table on top, per-unit
detail below, so Phase 68 can concatenate mechanically and each unit's evidence stays adjacent to its
own coverage claim. Use INVENTORY's exact column shape
`Unit | D1 Security | D2 Correctness | D3 Performance | D4 Maintainability | D5 Test coverage | D6 Dependency health | D7 Cross-IDE parity | D8 Doc accuracy`, one row per unit in D-02's risk-rank order:
`RU-62-04`, `RU-62-01`, `RU-62-03`, `RU-62-05`, `RU-62-02`. Each row is `applies` in all columns
except D6, which is `n/a — R-D6-CENTRAL`. Note beneath the table that Phase 62 has **no
file-exception rows**: `setopts-composer-webview.ts`'s D4 asymmetric baseline is recorded inside
INVENTORY's `RU-62-04` block as a qualifier on an existing `applies` cell, not as a separate row, so
adding one would break the 40-cell gate. Note also that D7 is live for all five units here, unlike
Phase 61 where it was `n/a — R-D7-SHARED-LS` throughout.

**C. `## D-14 Cell-Total Gate`.** State the expected totals — 35 `applies`, 5 `n/a`, 40 total (5 unit
rows x 8 dimensions) — and immediately **re-derive them from INVENTORY rather than restating them**,
by running this exact command and recording both the command and its literal output in the file:

    awk '/^\| `RU-62-0[1-5]` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md

State explicitly that if this re-derivation ever disagrees with the stated totals, that disagreement
is itself a defect to surface, not a number to quietly adopt (D-14). Note that plan `62-05` re-runs
this gate, together with the 22-file tree enumeration, as the phase's closing check.

**D. `## Stopping Rule & Write Contract`.** Carry forward Phase 61's rendered three-part stopping rule
(the planner confirms it, adjusted only for this phase's 7 live dimensions instead of 6): a unit's
sweep is complete when (i) each of its 7 live `applies` cells carries a verdict plus a written line
naming the concrete checks applied; (ii) every file in the unit's file list is named at least once
inside that unit's own section — in a check line or in a finding's `location:` — so coverage is
file-granular, not merely unit-granular; and (iii) every candidate claim raised during the sweep is
either promoted to a finding record clearing its evidence tier, or written under that unit's
`### Not-reproducible dispositions` with its reason. Once (i)-(iii) hold, the unit is done and no
further reading is licensed. Write contract: plans `62-02`..`62-05` each fill exactly one unit
section and touch nothing else — no fragment files, no assembly plan, no whole-file rewrite, no
rewording of a carried-forward `n/a` reason (D-03) — and ordering across this shared file is enforced
structurally by the wave dependency chain (D-04), one plan per wave, waves 1-5, each plan's
`depends_on` naming its predecessor in D-02's risk-rank order. Placeholder: every not-yet-recorded
live-dimension cell line ends with the single lowercase word `pending`, so the remaining work is
mechanically countable at every wave.

**E. `## Exclusion reasons carried forward`.** One block only. Phase 62 carries forward exactly one
marker, **R-D6-CENTRAL** (5 cells — one D6 cell per unit row), whose sentence is copied **verbatim
from INVENTORY §"Exclusion reasons", not reworded and not re-derived**. State the identity check:
5 = 5, matching the 5 `n/a` cells in this slice. State in one line that no `R-D7-SHARED-LS` block
applies here because D7 is live for all five units, and that no `.bbl`-style exclusion markers exist
in this phase.

**F. Five stubbed unit sections**, in D-02's order. One `## RU-62-0N — <name>` heading per unit, each
carrying: a `**Files (<count> / <LOC> LOC):**` line listing every file path with its individual LOC
from INVENTORY's per-unit table; then `**Risk rank:**`, `**Sweep method (D-08):**` and
`**Owning plan:**` lines. Sweep method is `full read` for `RU-62-04`, `RU-62-01`, `RU-62-03` and
`RU-62-02`, and `mechanical diff + full read of both contribution manifests` for `RU-62-05`. Then a
`### Cells` block with exactly 8 lines, one per dimension, each beginning `- D{n} <dimension name> — `
using REQUIREMENTS.md's dimension names verbatim (`D1 Security`, `D2 Correctness & error handling`,
`D3 Performance & resource use`, `D4 Maintainability & code smells`, `D5 Test coverage gaps`,
`D6 Dependency health`, `D7 Cross-IDE parity`, `D8 Comment & doc accuracy`). The D6 line is completed
now as `n/a — ` followed by the verbatim R-D6-CENTRAL text in double quotes. The seven live lines end
with the placeholder word from section D and nothing after it. Then three blocks — `### Findings`,
`### Not-reproducible dispositions`, `### Cross-unit referrals` — each holding the literal text
`_(none recorded)_`. Give `RU-62-04` an additional `### SEC-01/SEC-02 Surface Handoff` block. Finally
add a `## Phase 62 Close-Out` heading holding `_(recorded by plan 62-05)_`, so every unit section has
a stable terminator for range extraction from wave 1 onward.

**G. Sweep `RU-62-04` at tier `repro` / repro-equivalent — D1, D2, D3, D7.** Read all four target
files in full, then record those four cell lines, the handoff subsection, and any resulting records.
Enumerate each dimension explicitly so none is absorbed by whichever is loudest (D-11).

  *D1 Security* (tier `repro`). Assess against REQUIREMENTS.md's D1 wording — injection, untrusted
  input, secret exposure, integrity gaps, privilege/trust-boundary errors. The concrete checks, per
  generator: every value interpolated into the generated HTML string, traced back to its origin, and
  whether an escaping or sanitization step stands between the origin and the interpolation point;
  whether the Content-Security-Policy meta element each of the four files emits actually constrains
  script/style/img/connect sources or is permissive enough to be decorative; whether the nonce each
  file computes is freshly generated per panel and actually applied to every inline script and style
  it is supposed to cover; whether `webview.options` sets `enableScripts` and what `localResourceRoots`
  is set to; what each `onDidReceiveMessage` handler accepts and whether it validates message shape,
  type and value range before acting; whether any handler reaches the filesystem, spawns a process,
  writes into the user's document, or executes a command with a message-supplied value; and whether
  `setopts-composer-webview.ts`'s SETOPTS hex string round-trip can be driven to emit a value the
  parser did not produce. Where a concrete evidence-clearing defect exists, promote it to a
  `P62-D1-{seq}` record starting at `P62-D1-001`; where the answer is a posture rather than a defect,
  state it as a fact in section H's handoff and do not invent a finding for it.

  *D2 Correctness & error handling* (tier `repro`). Assess against null/undefined safety, unhandled
  rejections, swallowed exceptions, async races, off-by-one, wrong edge-case behavior and resource
  leaks. Three checks are required by name because they are this phase's translated concurrency edge
  probe: what each webview does when a message arrives **after** the panel has been disposed; what
  happens when a **second** composer instance is opened while the first is still live — whether a
  singleton panel is revealed, a second panel is created, or two panels race on the same disposable;
  and whether every `onDidDispose` actually releases its subscriptions, listeners and timers. Beyond
  those three: what each generator does with an absent, empty or malformed initial state; whether any
  `await` inside a message handler can reject unhandled; and whether the SETOPTS hex parse/emit path
  in `setopts-composer-webview.ts` handles a short, over-long, odd-length or non-hex input. A claim
  that cannot be reproduced or traced to the exact `file:line` where behavior diverges goes under
  `### Not-reproducible dispositions`, not into `### Findings`.

  *D3 Performance & resource use* (tier `repro`). Assess against hot-path cost, quadratic walks,
  missing caches or debounces, redundant work and unbounded memory growth. The concrete checks:
  whether the full HTML string is rebuilt on every state change or only on panel creation; whether
  any message handler recomputes the entire document or catalog per keystroke; whether panels,
  listeners or disposables accumulate across repeated open/close cycles; and the cost of the largest
  generated markup body (`addchildwindow-composer-webview.ts` at 431 lines) relative to how often it
  is regenerated.

  *D7 Cross-IDE parity* (tier `inherited`, resolving to repro-equivalent — a parity gap needs the
  concrete divergent behaviour in both IDEs). The parity question for this unit is **not** UI
  equivalence — VS Code renders webviews while IntelliJ presents native Swing dialogs — it is
  **equivalence of the generated BBj code**, as INVENTORY's `RU-62-04` D7 reason states. The concrete
  checks: for msgbox, addwindow and addchildwindow, compare the BBj statement each VS Code generator
  ultimately emits against what `ComposerModels.java` and `MsgboxComposerDialog.java` emit — argument
  order, optional-argument omission, quoting/escaping of user-entered text, numeric formatting, and
  which options exist on one side only; and record that SETOPTS has **no IntelliJ counterpart at all**
  (no `SetoptsComposerDialog.java` exists under `bbj-intellij/.../composer/`), which is itself a
  concrete parity divergence to assess rather than a gap to skip. Record a divergence whose defect is
  on the VS Code side as a normal `P62-D7-{seq}` finding whose `location:` is inside `bbj-vscode/`,
  citing the IntelliJ file:line in `evidence:` as the comparison point only. Where the defect is
  plainly on the IntelliJ side, record **nothing** in `### Findings` and instead write a
  `### Cross-unit referrals` entry addressed to `RU-63-04`, naming the surface, the check Phase 63
  should run, and the VS Code-side evidence that motivated it — written as a durable record, because
  Phase 63 may already be running (D-06).

**H. `### SEC-01/SEC-02 Surface Handoff`.** A narrative subsection under `RU-62-04`, structurally
mirroring `61-COVERAGE.md`'s `### SEC-06 Trust Boundary`. It states **facts, not findings** (D-08),
each anchored to a `path:line`, under four numbered headings:
  (1) **What reaches the generated HTML.** Enumerate every interpolated value across all four
  generators and name its origin — editor selection, document text, `config.bbx` content, workspace
  path, catalog data, or a prior message — and say for each whether an escaping step stands between
  origin and interpolation.
  (2) **CSP posture, per webview.** State the actual `Content-Security-Policy` directive set each of
  the four files emits, whether a nonce is generated and where it is applied, and what
  `webview.options` grants. State the posture as it is, even where it is not a defect; it becomes a
  finding only via what it enables.
  (3) **Message handlers and their validation.** List every `onDidReceiveMessage` handler that exists,
  the message shapes it accepts, and exactly what shape/type/range validation each performs before
  acting — the malicious-webview-message counterpart to `SEC-06`'s malicious-peer enumeration.
  (4) **Blast radius through the extension host.** State what a webview can reach through the
  handlers above — the user's open document, the filesystem, a spawned process, a VS Code command, a
  stored setting — or confirm that it reaches none of them.
Close each numbered fact with an inline `recorded as P62-D1-NNN` cross-reference where a defect was
promoted, mirroring the SEC-06 rendering. End the subsection with one sentence stating that
`bbj-intellij/` was read only as far as the D7 comparison required and that no finding is located
there (D-05).

**I. Finding records.** For every claim that clears its tier, write one record under `### Findings`
using INVENTORY's 13-field template verbatim, one field per line, inside a fenced block, in the
template's exact field order. `unit:` is `RU-62-04`. `classification:` records pass/FAIL for all six
INVENTORY 3c tests inline, exactly as `61-COVERAGE.md` renders them — note that test (6) forces
`major` for any `critical`/`high` severity **and** for any D1 primary dimension regardless of edit
size. `dedup:` is never blank and is checked against the frozen 15, with **#475** (SETOPTS
assistance / tri-state composer) and **#385** (launch the external Graffiti Composer) checked
explicitly by number for every record in this unit, since INVENTORY names them as this unit's dedup
neighbours. A `critical` or `high` D1 finding names the surface, the problem class and the impact,
and states that its evidence exists and what it establishes — **without** the trigger sequence, the
payload or a proof-of-concept, because this repository is public and this surface is unfixed (D-09).
All D2/D3/D7 findings, and D1 findings rated `medium` or `low`, carry full concrete detail.
  </action>
  <reversibility rating="costly">Committing a `critical` or `high` D1 record for the unfixed webview
  HTML-generation surface to this **public** repository has a permanent component — git history
  preserves an over-disclosed record even if the file is later edited. It is rated `costly` rather than
  `one-way` deliberately: the door itself was already walked through by 62-CONTEXT D-09, which locks
  the two-tier disclosure rule, so no decision remains here to gate with a *preceding*
  `checkpoint:decision` without re-litigating a locked decision. What remains at execution time is a
  judgement *within* that settled policy — which specific findings rate `critical`/`high` — and
  correcting an over- or under-redacted rendering costs one unit's re-render, the `costly` definition.
  Task 3 is nonetheless placed as a blocking disclosure checkpoint, stronger than a `costly` rating
  requires, because it gates the point where the rendering is inherited by four more plans and by
  Phase 65 — which is where a misjudgement would scale. It deliberately is **not** the format
  checkpoint D-03 declines to spend.</reversibility>
  <acceptance_criteria>
    - The file exists: `test -f .planning/reviews/62-COVERAGE.md` exits 0
    - The sweep tree is recorded as a full SHA on the right branch: `git rev-parse --abbrev-ref HEAD` prints `v4.0-stability-and-quality`, and `grep -cE '[0-9a-f]{40}' .planning/reviews/62-COVERAGE.md` is at least 1
    - All 5 unit sections plus the close-out are stubbed: `grep -cE '^## RU-62-0[1-5] — ' .planning/reviews/62-COVERAGE.md` prints `5` and `grep -c '^## Phase 62 Close-Out' .planning/reviews/62-COVERAGE.md` prints `1`
    - The cell-line inventory is exactly the INVENTORY slice, with no invented 41st row: `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40`
    - All 5 `n/a` cells are carried forward and all are D6: `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5` and `grep -cE '^- D6 .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5`
    - Exactly this task's 4 `repro`-tier cells are recorded and 31 remain: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `4` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `31`
    - The invariant holds: 4 + 31 + 5 = 40
    - The R-D6-CENTRAL text is verbatim: `grep -qF 'No distinct third-party dependency of its own; dependency-tree health (npm and Gradle) is assessed once, exhaustively, at' .planning/reviews/62-COVERAGE.md` exits 0
    - The D-14 gate re-derives rather than restates: `awk '/^\| \`RU-62-0[1-5]\` \|/ {a+=gsub(/applies/,"applies"); n+=gsub(/n\/a/,"n\/a")} END{print a, n, a+n}' .planning/reviews/INVENTORY.md` prints `35 5 40`, and both that command and that output appear inside the `## D-14 Cell-Total Gate` section
    - Every `RU-62-04` file is named inside its own section (file-granular coverage): `awk '/^## RU-62-04 /,/^## RU-62-01 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru04.txt; for f in msgbox-composer-webview.ts addwindow-composer-webview.ts addchildwindow-composer-webview.ts setopts-composer-webview.ts; do grep -q "$f" /tmp/p62ru04.txt || echo "MISSING $f"; done` prints nothing
    - The `RU-62-04` section carries all 8 cell lines, of which the 4 named `repro`-tier dimensions D1, D2, D3 and D7 carry a verdict: `grep -cE '^- D[1-8] ' /tmp/p62ru04.txt` prints `8` and `grep -cE '^- D[1237] .* — (pass|fail) — ' /tmp/p62ru04.txt` prints `4`
    - Every recorded `pass` cell carries a written line, not a bare verdict: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Every recorded `fail` cell names the findings it recorded: `awk '/^- D[1-8] .* — fail — /{if ($0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print "UNNAMED: " $0}' .planning/reviews/62-COVERAGE.md` prints nothing
    - The SEC-01/SEC-02 handoff exists and answers all four named facts: `grep -c '### SEC-01/SEC-02 Surface Handoff' .planning/reviews/62-COVERAGE.md` prints `1`, and the block names each of `Content-Security-Policy`, `onDidReceiveMessage` and `nonce`, and contains at least 6 anchors matching `[a-z-]+\.ts:[0-9]+`
    - The D7 cell is filled from the IntelliJ comparison side rather than asserted: `/tmp/p62ru04.txt` contains `ComposerModels.java` and the string `RU-63-04`
    - Every finding record is complete: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` in the file are all equal
    - Finding IDs are well-formed, unique, and from this phase only: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing, and `grep -cE '^id:[[:space:]]+P00-' .planning/reviews/62-COVERAGE.md` prints `0`
    - The first D1 finding of the phase is allocated as INVENTORY specifies: if any `^id:[[:space:]]+P62-D1-` line exists, `P62-D1-001` is among them
    - No `dedup:` is blank: `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified: `git status --porcelain bbj-vscode bbj-intellij java-interop` prints nothing
    - INVENTORY was not edited: `git status --porcelain .planning/reviews/INVENTORY.md` prints nothing
    - The three sub-blocks exist in every stubbed section: `grep -c '### Findings' .planning/reviews/62-COVERAGE.md` prints `5`, `grep -c '### Not-reproducible dispositions' .planning/reviews/62-COVERAGE.md` prints `5`, `grep -c '### Cross-unit referrals' .planning/reviews/62-COVERAGE.md` prints `5`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; test -f "$f"; test "$(git rev-parse --abbrev-ref HEAD)" = v4.0-stability-and-quality; test "$(grep -cE "[0-9a-f]{40}" "$f")" -ge 1; test "$(grep -cE "^## RU-62-0[1-5] — " "$f")" = 5; test "$(grep -c "^## Phase 62 Close-Out" "$f")" = 1; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test "$(grep -cE "^- D6 .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 4; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 31; grep -qF "No distinct third-party dependency of its own; dependency-tree health (npm and Gradle) is assessed once, exhaustively, at" "$f"; test "$(awk "/^\\| .RU-62-0[1-5]. \\|/ {a+=gsub(/applies/,\"applies\"); n+=gsub(/n\\/a/,\"n\\/a\")} END{print a, n, a+n}" .planning/reviews/INVENTORY.md)" = "35 5 40"; awk "/^## RU-62-04 /,/^## RU-62-01 /" "$f" > /tmp/p62ru04.txt; for x in msgbox-composer-webview.ts addwindow-composer-webview.ts addchildwindow-composer-webview.ts setopts-composer-webview.ts ComposerModels.java RU-63-04 "### SEC-01/SEC-02 Surface Handoff" Content-Security-Policy onDidReceiveMessage nonce; do grep -q "$x" /tmp/p62ru04.txt; done; test "$(grep -cE "^- D[1-8] " /tmp/p62ru04.txt)" = 8; test "$(grep -cE "^- D[1237] .* — (pass|fail) — " /tmp/p62ru04.txt)" = 4; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(awk "/^- D[1-8] .* — fail — /{if (\$0 !~ /P62-D[1-8]-[0-9][0-9][0-9]/) print \"UNNAMED\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^id:[[:space:]]+P00-" "$f")" = 0; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test "$(grep -c "### Findings" "$f")" = 5; test "$(grep -c "### Cross-unit referrals" "$f")" = 5; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo TRACER_OK'</automated>
  </verify>
  <done>`.planning/reviews/62-COVERAGE.md` exists with the full 40-cell skeleton, the single R-D6-CENTRAL
  `n/a` reason carried forward verbatim across 5 cells, the D-14 gate re-derived from INVENTORY with its
  command and output recorded, and `RU-62-04`'s four `repro`-tier cells (D1, D2, D3, D7) recorded with
  written check lines. `### SEC-01/SEC-02 Surface Handoff` states the four named facts with `path:line`
  anchors. Every finding that cleared its tier is a complete 13-field record with a non-blank dedup check
  naming #475 and #385; every claim that did not is visible under `### Not-reproducible dispositions`; no
  finding is located inside `bbj-intellij/`. No source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RU-62-04 at evidence tier `trace` — D4, D5, D8 — including the mechanical generator-layer duplication callout</name>
  <files>.planning/reviews/62-COVERAGE.md</files>
  <precondition>`.planning/reviews/62-COVERAGE.md` exists and its `## RU-62-04` section already carries
  verdicts for D1, D2, D3 and D7 — Task 1 committed. Halt if the section is still fully unrecorded.</precondition>
  <read_first>
    - `.planning/reviews/62-COVERAGE.md` §`## RU-62-04` — the section this task completes; match the
      cell-line and finding-record shape Task 1 established
    - `.planning/reviews/INVENTORY.md` §3b (D4 and D8 are tier `trace`; D5 is `inherited` and follows
      whatever it asserts, so a missing test is `trace`-evidenced), §3c, §"Finding Record Template",
      §"Frozen Open-Issue Snapshot", and `### RU-62-04`'s file-exception row — the D4 asymmetric
      baseline stating that `setopts-composer-webview.ts` is compared against **three** `-composer.ts`
      siblings, not four
    - `.planning/REQUIREMENTS.md` D4, D5, D8 rows; RVW-03
    - **Review targets:** the same 4 `*-composer-webview.ts` files as Task 1
    - **Test surface:** whatever `ls bbj-vscode/test/` and
      `grep -rl 'composer' bbj-vscode/test/ 2>/dev/null` return — establish by enumeration, not by
      assumption, which of the four generators has any direct test coverage at all
    - `CLAUDE.md` — its §Repository Structure and §Architecture sections are D8 targets checkable
      against the code just read; note that it currently says almost nothing about the four composer
      subsystems, which is a candidate D8 check and not an asserted finding
  </read_first>
  <action>
Fill the remaining three `RU-62-04` cell lines and append any resulting records. Touch no other
section. Enumerate the dimensions explicitly so none is absorbed by another (D-11): this task covers
**D4 Maintainability & code smells, D5 Test coverage gaps, and D8 Comment & doc accuracy** — and no
others.

*D4 Maintainability & code smells* (tier `trace`). This cell carries the duplication callout **ROADMAP
Phase 62 criterion 2 requires**, and INVENTORY's own `RU-62-04` D4 row names it here. It is
**mechanical, not an eyeball comparison** (D-12): run a programmatic structural diff across the four
generators — for example `diff` or `git diff --no-index` between each ordered pair, plus a count of
identical or near-identical line runs — and write **both the method and its literal output** into the
cell so a reader can re-run it. Apply the D-15-confirmed asymmetry: the `-composer.ts` baseline is
**three** files (msgbox, addwindow, addchildwindow), because SETOPTS has no `-composer.ts` sibling —
its codegen lives in `setopts-catalog.ts`, which belongs to `RU-62-03`. State that asymmetry in the
cell text as an asymmetric-baseline qualifier on this existing `applies` cell; do **not** add a row,
a cell line or a coverage-gap note for it. Beyond duplication: whether each generator's HTML-emitting
function has crossed into god-function territory, whether the nonce/CSP boilerplate is copied four
times rather than shared, whether dead branches or unreferenced markup exist, and whether the four
files agree on a single convention for panel creation and disposal or use three. Record the
duplication as a `P62-D4-{seq}` finding with an effort estimate from INVENTORY's `2|4|8` scale, and
state in the cell that `RU-62-03` records the logic/UI-layer duplication separately and
cross-references this finding rather than restating it (D-12).

*D5 Test coverage gaps* (tier `inherited`; a missing test is `trace`-evidenced). Concrete checks:
which of the four generators has any dedicated test file at all, established by enumerating
`bbj-vscode/test/` rather than assumed; whether the HTML-generation path, the CSP/nonce construction,
or any `onDidReceiveMessage` handler is exercised by any currently-passing test; whether the SETOPTS
hex round-trip has a regression test; and whether any D1/D2 finding recorded in Task 1 has a test that
would have caught it. Record gaps as `P62-D5-{seq}` findings naming the untested branch by
`file:line`. Every `dedup:` names `none` or a frozen issue explicitly.

*D8 Comment & doc accuracy* (tier `trace`). Assess against stale comments, wrong JSDoc, and CLAUDE.md
or docs-site claims contradicted by code. Concrete checks: every file-level and function-level comment
block in the four generators against what the code does — including any comment claiming a value is
escaped, sanitized, validated or nonce-protected, which is checked directly against Task 1's D1
trace; whether `setopts-composer-webview.ts`'s header comments describing the config.bbx SETOPTS
string format match the code's actual parse/emit behavior; and whether `CLAUDE.md`'s §Repository
Structure and §Architecture sections describe the composer subsystems accurately — its silence on
them is a candidate D8 observation, promoted to a finding only if a positive claim it does make is
contradicted, never asserted merely because a section is absent.

For each of the three cells write `pass` plus a written line naming the concrete checks applied, or
`fail` plus the IDs of the findings recorded. Continue finding-ID allocation monotonically from the
highest sequence already present for each dimension. Any claim that does not clear its tier goes under
`### Not-reproducible dispositions` with the tier it failed and why. Any defect whose `file:line`
belongs to another unit — in particular anything in `setopts-catalog.ts` or a `-composer.ts` /
`-ui.ts` file, all of which belong to `RU-62-03` — goes under `### Cross-unit referrals` naming the
target unit, not into this unit's `### Findings`.
  </action>
  <acceptance_criteria>
    - All 7 live `RU-62-04` cells now carry a verdict: `awk '/^## RU-62-04 /,/^## RU-62-01 /' .planning/reviews/62-COVERAGE.md > /tmp/p62ru04.txt; grep -cE '^- D[1-8] .* — (pass|fail) — ' /tmp/p62ru04.txt` prints `7`
    - The three dimensions this task owns are each individually recorded: `grep -cE '^- D4 .* — (pass|fail) — ' /tmp/p62ru04.txt`, `grep -cE '^- D5 .* — (pass|fail) — ' /tmp/p62ru04.txt` and `grep -cE '^- D8 .* — (pass|fail) — ' /tmp/p62ru04.txt` each print `1`
    - The `n/a` cell is untouched and still verbatim: `grep -cE '^- D6 .* — n/a — ' /tmp/p62ru04.txt` prints `1`
    - The D4 callout is mechanical, with its method and output in the cell: `/tmp/p62ru04.txt` contains the string `diff`
    - The 3-file baseline asymmetry is stated, not normalized: `/tmp/p62ru04.txt` contains `setopts-catalog.ts` and contains `RU-62-03`
    - A D4 finding was allocated for the generator-layer duplication: `grep -cE '^id:[[:space:]]+P62-D4-[0-9]{3}' /tmp/p62ru04.txt` is at least 1
    - No other unit's section was written: `grep -cE '^- D[1-8] .* — (pass|fail) — ' .planning/reviews/62-COVERAGE.md` prints `7` and `grep -cE '^- D[1-8] .* — pending$' .planning/reviews/62-COVERAGE.md` prints `28`
    - The 5 carried-forward `n/a` lines are intact and the grid is still 40 cells: `grep -cE '^- D[1-8] .* — n/a — ' .planning/reviews/62-COVERAGE.md` prints `5` and `grep -cE '^- D[1-8] ' .planning/reviews/62-COVERAGE.md` prints `40`
    - Every `pass` line remains a written line: `awk '/^- D[1-8] .* — pass — /{if (length($0) < 120) print "SHORT"}' .planning/reviews/62-COVERAGE.md` prints nothing
    - Finding IDs remain unique and well-formed; no blank dedup: `grep -oE '^id:[[:space:]]+P62-D[1-8]-[0-9]{3}' .planning/reviews/62-COVERAGE.md | sort | uniq -d` prints nothing and `grep -cE '^dedup:[[:space:]]*$' .planning/reviews/62-COVERAGE.md` prints `0`
    - Every finding still carries every required field: the counts of `^id:`, `^unit:`, `^location:`, `^dimension:`, `^severity:`, `^evidence_tier:`, `^evidence:`, `^failure_scenario:`, `^classification:`, `^effort:`, `^dedup:`, `^disposition:` are all equal
    - No finding is anchored in the read-only IntelliJ plugin: `grep -cE '^location:[[:space:]]+bbj-intellij/' .planning/reviews/62-COVERAGE.md` prints `0`
    - No source file was modified and INVENTORY is unchanged: `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/reviews/62-COVERAGE.md; awk "/^## RU-62-04 /,/^## RU-62-01 /" "$f" > /tmp/p62ru04.txt; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " /tmp/p62ru04.txt)" = 7; for d in 4 5 8; do test "$(grep -cE "^- D$d .* — (pass|fail) — " /tmp/p62ru04.txt)" = 1; done; test "$(grep -cE "^- D6 .* — n/a — " /tmp/p62ru04.txt)" = 1; grep -q "diff" /tmp/p62ru04.txt; grep -q "setopts-catalog.ts" /tmp/p62ru04.txt; grep -q "RU-62-03" /tmp/p62ru04.txt; test "$(grep -cE "^id:[[:space:]]+P62-D4-[0-9]{3}" /tmp/p62ru04.txt)" -ge 1; test "$(grep -cE "^- D[1-8] .* — (pass|fail) — " "$f")" = 7; test "$(grep -cE "^- D[1-8] .* — pending$" "$f")" = 28; test "$(grep -cE "^- D[1-8] .* — n/a — " "$f")" = 5; test "$(grep -cE "^- D[1-8] " "$f")" = 40; test -z "$(awk "/^- D[1-8] .* — pass — /{if (length(\$0) < 120) print \"SHORT\"}" "$f")"; test -z "$(grep -oE "^id:[[:space:]]+P62-D[1-8]-[0-9]{3}" "$f" | sort | uniq -d)"; test "$(grep -cE "^dedup:[[:space:]]*$" "$f")" = 0; test "$(grep -cE "^location:[[:space:]]+bbj-intellij/" "$f")" = 0; test -z "$(git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md)"; echo RU62_04_COMPLETE'</automated>
  </verify>
  <done>`RU-62-04` is complete under the stopping rule: all 7 live cells carry a verdict plus a written
  check line, all 4 files are named inside the section, the generator-layer duplication is recorded as a
  D4 finding produced by a programmatic diff whose method and output are in the cell, the 3-file
  `-composer.ts` baseline asymmetry is stated rather than normalized, and every unpromoted claim is
  visible under `### Not-reproducible dispositions`.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the public-repo disclosure rendering before four more plans inherit it (D-09)</name>
  <decision>
    How much detail a `critical` or `high` D1 record carries in this **public** repository, as rendered
    in `## RU-62-04` — specifically, whether the `evidence:` and `failure_scenario:` fields of the
    highest-severity records name the surface, the problem class and the impact without disclosing a
    trigger sequence, a payload or a proof-of-concept, and whether the
    `### SEC-01/SEC-02 Surface Handoff` states the CSP and message-validation posture at a level a
    reader can act on defensively without it doubling as an exploitation guide. This is a review of
    **disclosure level**, not of the findings themselves and not of the recording format — D-03
    deliberately spends no format checkpoint and this checkpoint does not reintroduce one.
  </decision>
  <context>
    The webview HTML-generation surface is unfixed and this repository is public, so a committed record
    is world-readable the moment it lands and git history preserves it even if the file is later
    edited — the one genuinely irreversible act in a phase that otherwise writes only markdown. Phase
    61's D-12 rationale (a malicious java-interop peer already implies local access) does not transfer:
    a webview injection reachable from values a developer's own project supplies needs no attacker
    foothold at all. Four more plans and Phase 65's synthesis inherit whatever rendering is approved
    here, so this is the point at which over-disclosure would scale. Read `## RU-62-04`'s
    `### Findings` and `### SEC-01/SEC-02 Surface Handoff` in `.planning/reviews/62-COVERAGE.md`,
    together with the severity distribution the sweep actually produced.
  </context>
  <options>
    <option id="approve">
      <name>Approve the rendering as written — plans 62-02..62-05 apply the same tier to their own D1 records</name>
      <pros>No re-rendering cost; one consistent disclosure posture across all five units and into Phase 65.</pros>
      <cons>Any over-disclosure already committed stays in git history; any under-disclosure leaves Phase 67 with a thinner record to fix from.</cons>
    </option>
    <option id="redact-further">
      <name>Redact further, then re-render RU-62-04 before plan 62-02 runs</name>
      <pros>Corrects an over-disclosure while only one unit is affected, and before four more inherit it.</pros>
      <cons>Costs one extra pass; the original text remains recoverable from git history, so redaction limits future exposure rather than undoing past exposure.</cons>
    </option>
    <option id="disclose-more">
      <name>Disclose more concrete detail — the surface is judged low-risk enough that full detail aids the fix</name>
      <pros>Phase 67 and Phase 69 get a maximally actionable record with no reconstruction cost.</pros>
      <cons>Overrides D-09's locked two-tier rule for this phase; must be recorded in the coverage file as an explicit deviation with the developer's reason.</cons>
    </option>
  </options>
  <resume-signal>Select: approve, redact-further, or disclose-more — and for the latter two, describe the change</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase writes **one markdown file in a public repository and modifies no code**. The realistic
threats are therefore disclosure and record-integrity threats, not application-security threats. Rows
marked *subject-matter* are the boundaries this plan exists to **document**; their remediation is
Phase 65/67's, not this plan's.

| Boundary | Description |
|----------|-------------|
| composer webview content → generated HTML (*subject-matter*, SEC-01) | Values from the editor selection, document text, `config.bbx` content, workspace paths and catalog data are interpolated into HTML strings by four generators; this is the complete SEC-01 surface, since `package.json` declares no `customEditors`. |
| webview → extension host via `onDidReceiveMessage` (*subject-matter*, SEC-02) | Messages originating in webview content cross into extension-host code that may touch the user's document, the filesystem or a spawned process. |
| `62-COVERAGE.md` → public repository readers | `.planning/` is committed and this repository is public; an unfixed `critical`/`high` D1 record becomes world-readable the moment it lands, and git history preserves it. |
| code read during the sweep → the committed record | Credentials, tokens or absolute developer-machine paths encountered while reading source can be copied into a record verbatim. |
| `bbj-intellij/` → this phase's finding namespace | Read as D7 comparison material only; a `location:` inside it would double-count against Phase 63's own rows. |
| recorded `classification:` → Phase 67 apply path | An `easy` classification routes a fix past MAJOR-REFACTORS.md review. |
| shared `62-COVERAGE.md` → five writers | One file, five plans; a write outside a plan's own section silently destroys another unit's recorded coverage. |

## STRIDE Threat Register

ASVS level 1; `high` blocks. Rows `T-62-P01-S*` are subject-matter threats disposed `transfer`
(Phase 62 records and evidences; Phase 65 synthesizes, Phase 67 fixes, Phase 69 files). Rows
`T-62-P01-0*` are threats to this plan's own execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-P01-S1 | Tampering | interpolated values → generated webview HTML across the four `*-composer-webview.ts` generators | high | transfer | Task 1's D1 check traces every interpolated value to its origin and records whether an escaping step stands between them; Task 1 §H fact (1) enumerates the full set. Recorded as `P62-D1-*` where evidence clears tier `repro`. Remediation transfers to Phase 65/67. |
| T-62-P01-S2 | Elevation of Privilege | `onDidReceiveMessage` handlers reaching the user's document, the filesystem, a spawned process or a VS Code command | high | transfer | Task 1's D1 check enumerates every handler and the shape/type/range validation each performs before acting; §H facts (3) and (4) state the handler inventory and the blast radius through the extension host. |
| T-62-P01-S3 | Information Disclosure | CSP directive set and nonce application per webview | medium | transfer | §H fact (2) states each webview's actual CSP posture and where its nonce is applied, as a fact rather than a finding — it becomes a finding only via what it enables. |
| T-62-P01-S4 | Denial of Service | message-after-disposal and second-instance races in the composer panels | medium | transfer | Task 1's D2 check names all three cases explicitly (post-disposal message, concurrent instance, `onDidDispose` release completeness) as the phase's translated concurrency edge probe; a case that cannot be reproduced is dispositioned, never silently dropped. |
| T-62-P01-01 | Information Disclosure | an unfixed `critical`/`high` D1 record committed to a public repository | high | mitigate | D-09's two-tier rule is stated in the action and enforced as a `must_haves` prohibition: name the surface, the problem class and the impact; state that the evidence exists and what it establishes; publish no trigger sequence, no payload, no proof-of-concept. Task 3 is a blocking human checkpoint on the rendering before four more plans inherit it. |
| T-62-P01-02 | Information Disclosure | a credential, token or absolute developer path picked up while reading code and copied into a record | medium | mitigate | Explicit prohibition: every citation is a repository-relative `path:line` anchor; no literal secret value, no absolute path. |
| T-62-P01-03 | Elevation of Privilege | `classification:` on a D1 finding → Phase 67's apply path | high | mitigate | INVENTORY 3c test (6) forces `major` for any `critical`/`high` severity **or** any D1 primary dimension regardless of edit size; every record writes all six tests pass/FAIL inline, and the acceptance criteria require equal field counts across all records. |
| T-62-P01-04 | Tampering | five plans writing one `62-COVERAGE.md` | medium | mitigate | Ordering is structural, not conventional: one plan per wave, waves 1-5, `depends_on` chained (D-04). Each task asserts the exact global counts of recorded verdicts, remaining placeholders, carried-forward `n/a` lines and total cell lines, so any write outside its own section fails that task's own verify gate. |
| T-62-P01-05 | Repudiation | a coverage claim with no evidence behind it | medium | mitigate | RVW-06's drop-vs-disposition rule plus D-10: every `pass` cell carries a written line naming the concrete checks applied, enforced by a minimum-length gate; every `fail` cell must name the finding IDs it recorded. |
| T-62-P01-06 | Tampering | reading `bbj-intellij/` as D7 comparison material | medium | mitigate | D-05 prohibition plus an anchored acceptance grep: no `location:` field may resolve inside `bbj-intellij/`. IntelliJ-side defects route to `### Cross-unit referrals` addressed to `RU-63-04`/`RU-63-02`/`RU-63-01` as durable records (D-06). |
| T-62-P01-07 | Tampering | inventing a 41st cell for the `setopts-composer-webview.ts` D4 asymmetry | low | mitigate | Prohibition plus an exact `grep -cE '^- D[1-8] '` = 40 assertion in both tasks; the asymmetry is written as a qualifier on the existing D4 cell. |
| T-62-P01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install task — its commands are `git`, `grep`, `awk` and `diff` only. The package legitimacy gate is not applicable and no RESEARCH.md audit table is required. |
</threat_model>

<verification>
- `.planning/reviews/62-COVERAGE.md` exists, records branch `v4.0-stability-and-quality` and a full
  40-character SHA captured at execution time, and carries exactly 40 cell lines.
- All 5 `n/a` cells are D6 and carry INVENTORY's R-D6-CENTRAL text verbatim; the anchor sentence greps
  clean.
- The D-14 gate command re-derives `35 5 40` from INVENTORY, and both the command and its output are
  recorded in the file.
- `RU-62-04`'s 7 live cells all carry a verdict plus a written check line of at least 120 bytes; its D6
  cell is untouched; all 4 of its files are named inside its section.
- `### SEC-01/SEC-02 Surface Handoff` states all four named facts with `path:line` anchors and names
  `Content-Security-Policy`, `onDidReceiveMessage` and `nonce`.
- The D7 cell cites `ComposerModels.java` as the comparison side and routes IntelliJ-side observations
  to `RU-63-04`; no `location:` resolves inside `bbj-intellij/`.
- The D4 cell records a programmatic diff's method and output, states the 3-file `-composer.ts`
  baseline, and names `RU-62-03` as owner of the logic/UI-layer duplication.
- Every finding record has all 13 fields, a unique well-formed `P62-D{n}-{seq}` ID, and a non-blank
  `dedup:` that checked #475 and #385 explicitly.
- Exactly 7 verdicts are recorded phase-wide and 28 placeholders remain, proving no other section was
  written; 7 + 28 + 5 = 40.
- `git status --porcelain bbj-vscode bbj-intellij java-interop .planning/reviews/INVENTORY.md` is empty.
</verification>

<success_criteria>
- RVW-03 is opened: 1 of 5 units is swept across all 7 live dimensions with file-granular coverage.
- ROADMAP Phase 62 criterion 2's duplication requirement is satisfied at the generator layer, by a
  mechanical diff whose method and output are recorded in the cell.
- ROADMAP Phase 62 criteria 3 and 4 hold for every record written here: `file:line`, dimension, a
  verified failure scenario, and a dedup check against the frozen 15.
- Phase 65 inherits a `### SEC-01/SEC-02 Surface Handoff` stating the injection surface, the CSP
  posture, the message-handler validation inventory and the blast radius as facts.
- A plan `62-02`..`62-05` executor can fill its unit section by copying this one, with no clarifying
  question.
</success_criteria>

<output>
Create `.planning/phases/62-extension-host-webview-composer-review/62-01-SUMMARY.md` when done.
</output>
